# 0298 — An allowlist entry naming an unscanned file is validated by nothing

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Web |
| **Module** | web / accessibility / tooling |
| **Phase** | 8 — Polish and follow-ups |
| **First seen** | 2026-08-27 |
| **Commit** | `3900e5b` |
| **Closed** | 2026-08-27 |

## Description

`#0280` gave the live-region guard's allowlists a required-justification rule
and a staleness check. Both are real and both fire — `#0286`'s review proved the
staleness check by re-adding a removed entry and watching the suite fail.

**But both checks are per-file.** `unjustifiedEntryViolations` and
`staleEntryViolations` each filter on `e.file === fileName`, so they only ever
run for entries whose file the scan actually visits. An entry naming a file that
does not exist is never examined by either.

Measured by `#0286`'s reviewer: adding

```js
{ file: 'web/src/views/DoesNotExistAnywhere.svelte', match: 'nothing', reason: '' }
```

to `KNOWN_STABLE_BRANCH_SITES` leaves the whole suite **green** — despite an
empty `reason`, which the justification rule exists to reject.

**It is inert rather than a bypass**, because `findAllowlistEntry` also matches
on file, so a bogus entry cannot excuse a real site. And the hole predates
`#0280`. But it is now the **likeliest way an entry goes stale in practice** — a
component rename, a deletion, or a typo in the path — and criterion 3's whole
point was that a stale entry must fail loudly.

## Acceptance criteria

1. Every allowlist entry is validated against the **union of scanned files**,
   once per run, rather than per file. An entry naming a file the scan never
   visits fails loudly, naming the entry and the path.
2. Both rules apply to that pass: an entry with an empty or missing `reason`
   fails even when its file does not exist, since a bogus path must not become a
   way to smuggle in an unjustified entry.
3. This covers **every** allowlist the guard carries — `KNOWN_LOADING_PLACEHOLDERS`,
   `KNOWN_STABLE_BRANCH_SITES`, and any successor. Do not fix one and leave the
   others.
4. Prove it with a fixture: an entry naming a non-existent file must fail, and
   the current real entries must all still pass. Per CLAUDE.md §8, the oracle
   must not be the same bytes as the subject.
5. The check must fail closed if the scanned-file set is empty — `#0275`'s
   lesson on the Go side, which applies identically here.
6. `ISSUE=NNNN scripts/check.sh web` passes.

## Relation

- Found by: [#0286](0286.md)'s review, measured rather than argued
- Relates to: [#0280](0280.md) — which added the per-file rules this completes
- Relates to: [#0275](0275.md) — the same fail-open class on the Go guards
- Relates to: [#0242](0242.md) — the guard all of these harden

## Work log

Added `unscannedFileViolations(list, allowlistName, scannedFiles)` in
`web/src/lib/liveRegionGuard.structuralGuard.test.ts`, called once per
allowlist (not per file) after the real-tree scan loop finishes, against
`scannedFiles` — the union of every `.svelte` file the scan actually visited
that run. For each entry in a list, if `!scannedFiles.has(e.file)` it fails
loudly (criterion 1), naming the entry's `match` and `file`; if the entry's
`reason` is also empty, a *second*, separate violation fires for that
(criterion 2) — so a bogus path cannot double as a way to dodge the
justification rule. Fails closed (criterion 5, `#0275`'s lesson) when
`scannedFiles` is empty, rather than treating an empty set as "nothing to
validate against, so nothing fails."

Wired into the real-tree test for all three allowlists
(`KNOWN_DYNAMIC_ROLE_SITES`, `KNOWN_LOADING_PLACEHOLDERS`,
`KNOWN_STABLE_BRANCH_SITES`) — criterion 3. Added a `scannedFileSet()` helper
so a fixture can validate the real entries against the real scan result
rather than a hand-typed stand-in.

Added a doc-comment block in the file header, next to the other allowlist
history notes, explaining why the per-file checks miss this case and what
the new once-per-run pass adds.

Four new fixtures (criterion 4): an entry naming a nonexistent file with an
empty reason fails on *both* rules; an entry naming a scanned file with a
real reason passes; an empty scanned-file set fails closed; and every entry
in all three real allowlists passes against the real `scannedFileSet()`.
Assertions read for independently written substrings, not a copy of the
message the function emits (CLAUDE.md §8).

Proved the fix against the issue's own repro: temporarily added
`{ file: 'web/src/views/DoesNotExistAnywhere.svelte', match: 'nothing',
reason: '' }` to the real `KNOWN_STABLE_BRANCH_SITES`, ran the real-tree
test, confirmed it now fails with both the unscanned-file violation and the
missing-justification violation (previously green, per the issue's own
Description), then restored the file and verified it is byte-identical to
before (`diff -q`, and a full suite re-run afterward).

## Verification

`ISSUE=0298 scripts/check.sh web` — see report for exact output; all
`liveRegionGuard.structuralGuard.test.ts` tests pass (22, up from 18), and
the repro above confirms the new checks actually fire rather than merely
existing.

## Review notes

**Verdict: pass.** Re-verified independently on 2026-08-27 against commit
`3900e5b`. `uptime` 1.90/2.26/2.23 and 2.02/1.96/2.08 across the run — well
under the ~8 threshold, so no timing caveat applies (and this guard is AST-only
anyway). `ISSUE=0298r scripts/check.sh web` → `svelte-check` 287 files, 0
errors / 0 warnings; `vitest run` 40 files, **3975 passed**;
`VERIFICATION PASSED`.

**The issue's own repro, re-run by the reviewer rather than taken on trust.**
In a throwaway detached worktree (`git worktree add … HEAD --detach`, its
`web/node_modules` symlinked to the main checkout's, removed afterwards — the
main tree was never mutated), I injected the Description's exact entry

```js
{ file: 'web/src/views/DoesNotExistAnywhere.svelte', match: 'nothing', reason: '' }
```

into the real `KNOWN_STABLE_BRANCH_SITES`:

| Tree | Result |
|---|---|
| `3900e5b^` (pre-fix), bogus entry present | **18 passed, 0 failed** — green, exactly as the Description claims |
| `3900e5b` (post-fix), bogus entry present | **2 failed / 24** — the real-tree test *and* the criterion-4 fixture |

Both violations fire, in the same pass, with the entry named:

```
KNOWN_STABLE_BRANCH_SITES entry match="nothing" names a file the scan never visited …
KNOWN_STABLE_BRANCH_SITES entry match="nothing" ALSO carries no justification text …
```

Criterion 1 and criterion 2 confirmed by measurement, not by reading.

**Criterion 3 confirmed on a second allowlist, not just the one in the repro.**
Injected `{ file: 'web/src/views/GhostPlaceholder.svelte', match: 'nothing',
reason: 'has a reason' }` into `KNOWN_LOADING_PLACEHOLDERS`: the unscanned-file
violation fired naming `KNOWN_LOADING_PLACEHOLDERS`, and — correctly — the
empty-reason violation did **not**, proving the two rules are independent
rather than a single conjoined check. `KNOWN_DYNAMIC_ROLE_SITES` is currently
empty, so it contributes no entries, but its call site is present and takes the
same code path. All three calls are covered.

**Once-per-run placement genuinely sees the union.** `scannedFiles` is filled
inside the scan loop (`scannedFiles.add(file)` beside `filesScanned++`), and the
three `unscannedFileViolations` calls sit *after* the loop closes, before the
`allViolations.length > 0` throw. So each call sees every file the scan visited
that run — not a partial set. Verified in the diff and by the fixture
`#0298 criterion 4`, which independently recomputes the same set via
`scannedFileSet()` and asserts `>= 35` before using it (so an empty or collapsed
glob cannot make that fixture vacuously pass either).

**Criterion 5 (fail closed) is real.** `unscannedFileViolations` returns a
violation for an empty `scannedFiles` *before* iterating the list, so an empty
set fails rather than passing every entry. The fixture asserts
`violations.length > 0`, and my `GhostPlaceholder` run exercised the non-empty
path, so both branches are covered.

**Criterion 4's oracle is not the subject (CLAUDE.md §8).** The fixtures assert
on independently-written substrings (`'never visited'`, `'justification'`,
`'empty'`) rather than a stored copy of the emitted message, and the real-entry
fixture validates against `scannedFileSet()` — the actual glob result — rather
than a hand-typed stand-in that could drift. Both are the right shape.

**On "any non-empty `reason`" — judged, as asked.** It is a box to tick:
`reason: ' x '` passes, and nothing checks that the text is *about* the entry it
justifies. But it is a box worth ticking. Its value is not semantic validation
(which no static check can do here); it is that an entry cannot be added
silently — someone has to write a sentence, and that sentence is what the next
reviewer reads and can falsify. Tightening it further (a minimum length, a
required keyword) would be measuring a proxy, and CLAUDE.md §5's warning about
sizing constants against nothing applies. **Not a bounce, and not worth filing.**

**Standing properties re-derived, not accepted.** The `'../**/*.svelte'` glob is
unchanged (the `#0242` re-narrowing to `web/src/views` has not recurred);
`expect(filesScanned).toBeGreaterThanOrEqual(35)` is intact and my own
instrumented pass measured `filesScanned=38`, matching the comment; the
`alertSites >= 61`, `statusSites >= 32`, `loadingPlaceholders >= 20` floors are
unchanged and my independent count reproduced 61 / 52 status sites (32 of which
reach `statusSites`) / 20 exactly.

**Test-count accounting, measured at four commits.** The `3973 → 3975` framing
in the dispatch was right for `#0299` alone but hid `#0298`'s own contribution;
the full ledger is: `3900e5b^` **3964** → `3900e5b` (#0298) **3968** (+4) →
`e1d47ff` (another agent's `#0291`/`#0292`) **3973** (+5) → `ea08188` (#0299)
**3975** (+2). Within this file: 18 → 22 → 24. Both issues' `## Verification`
counts are accurate.

**Housekeeping note, not a defect.** The `## Work log` prose was committed
inside the code commit `3900e5b` rather than left for the resolution commit
(`issues/Issues.md` §Git tracking splits these). Harmless here — the hash the
`**Commit**` row needed was the same commit's — but worth not repeating.
