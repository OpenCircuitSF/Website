# 0222 — Four gaps in the modal focus-wiring guard, and an undercount in its prose

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Web |
| **Module** | web |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-23 |
| **Commit** | `52366eb` |
| **Closed** | 2026-08-24 |

> 🟢 Resolved 2026-08-24 — the `{:else}` inversion, the whitespace false
> failure and the reactivity gap are all closed and independently
> mutation-proved; the remaining two gaps are named in the guard's header, and
> the ten-of-eleven mount count checks out against the tree.

## Description

`#0216`'s second review passed the AST-based focus-wiring guard after re-running
all four probes at its own chosen sites and surveying all 11 panels
mechanically. It then mapped where the new mechanism is still weak. **None of
these occurs in the tree today.**

**The false passes — guard green, wiring broken.** These are the ones that
matter:

1. **`{:else}` branches take the `{#if}`'s condition** from `enclosingGuard`. A
   panel in `{:else}` of `{#if a}` whose focus call sits under `if (a)` is
   **inverted** — dead wiring — and passes. The converse, correct wiring, fails.
   The violation message is misleading in both directions. This one is not named
   in the guard's header.
2. **Two panels in one file under textually identical conditions, with swapped
   focus targets.** This *is* named in the header as the residual gap, and the
   review confirmed it is real.
3. **Nothing requires the focus call to be reactive.** A `tick().then(() =>
   V?.focus())` inside a plain function that is never called satisfies the check;
   no `$effect` enclosure is required.
4. **`{#each}`-rendered panels** — theoretical today, since none exists.

**The false failures — guard red, wiring fine.** Lower stakes, but they are what
gets a guard silenced: `guardKey` is whitespace-sensitive, so `{#if a&&b}`
against `if (a && b)` fails. Newlines, indentation, and extra parentheses are
handled correctly; only whitespace *runs inside* the condition differ.

**The prose undercount.** `CampaignSendDialog.behavior.test.ts` says "the six
sites nothing mounts today". Only that one file imports
`@testing-library/svelte`, so **ten** of eleven are unmounted — and the sentence
contradicts the list of seven immediately preceding it.

The review deliberately did **not** bounce on this, and the reasoning is worth
keeping: it predates the fix commit, the first review examined those exact lines
and ruled them honest, and it understates *breadth* rather than overstating what
the guard *proves*. That distinction is the one `#0216` was filed about.

## Acceptance criteria

- [x] A panel in `{:else}` is matched against the negation of the `{#if}` condition, not the condition itself — inverted wiring fails, correct wiring passes
- [x] Each of the four false passes is either closed or named in the guard's header; the `{:else}` case must be **closed**, since it silently blesses dead wiring
- [x] `guardKey` normalises whitespace inside a condition, so `{#if a&&b}` matches `if (a && b)`
- [x] The focus call must be inside a `$effect`, or the header says why not
- [x] Each closure is proved by mutation: construct the shape, confirm the guard fails, restore, verify byte-identity
- [x] The mount undercount reads correctly — ten of eleven, or whatever is true when the work lands
- [x] All four of `#0216`'s probes still fail as recorded, and the 11-site survey still finds 11
- [x] `scripts/check.sh web` passes

## Notes

Reported by `#0216`'s second phase-3 review under `CLAUDE.md` §9.

Criterion 1 is the one with teeth. The other three false passes need a
contrived or absent shape; the `{:else}` inversion needs only an ordinary
two-branch modal, and it fails **silently in the direction that matters** —
blessing dead wiring while flagging the correct version.

Note the asymmetry that should drive the design: a false failure gets the guard
edited or silenced, a false pass gets it trusted. `#0199`'s review made the same
argument about a discount rule that was too broad.

`#0216`'s two `## Review notes` sections carry the probe shapes and the 11-site
enumeration; they are the regression suite for criterion 7.

## Relation

- Found by: [#0216](0216.md) — the focus-wiring guard
- Relates to: [#0220](0220.md) — the TypeScript citation guard and `tabindex="-1"`, both still open
- Relates to: [#0094](0094.md) — the harness that would let the unmounted ten be tested behaviourally

## Implementation notes

Redesigned `web/src/lib/modalFocusWiring.structuralGuard.test.ts`'s guard
matching (`collectPanels` on the template side, `collectFocusCallSites` on
the script side) around a `GuardConstraint { text, negated }` — each side's
nearest enclosing `{#if}` / `if (...)` REPLACES (not accumulates) the guard
handed to its consequent with the affirmative condition, and to its
alternate with the negated one. "Replaces, not accumulates" matters: the
real tree nests each modal's own condition (e.g. `deactivatingUser`) inside
outer admin-section routing conditions (`{#if section === 'users'}`,
`{#if !$currentUser?.is_admin}`) that the corresponding `$effect`'s own
`if` never repeats. An early version of this fix accumulated the full
ancestor chain and broke all 11 real sites at once — caught immediately by
the repo-wide scan test, which is exactly what it's for — before being
corrected to narrowest-only, matching the pre-#0222 guard's own established
behavior (which found the narrowest SPAN containing an offset) with only
the negation added.

**{:else} inversion (criterion 1, closed).** The old code recorded one
span (and one guard) for an entire `IfBlock`, start to end — consequent
and alternate together — so a panel in `{:else}` of `{#if a}` matched
against the SAME key as the `{#if a}` branch. Fixed as described above.
Two new synthetic fixtures prove both directions: an inverted panel
(focus call under `if (a)`, panel in `{:else}`) fails, and the correctly
negated version (focus call under `else { ... }` of the same `if`)
passes.

**guardKey whitespace (part of criterion 1's "false failures" list,
closed).** `guardKey` was collapsing whitespace *runs* to a single space
but not removing already-present single spaces, so `{#if a&&b}` (key
`"a&&b"`) and `if (a && b)` (key `"a && b"`) never matched — a false
failure. Now strips all whitespace from the condition's span
(`.replace(/\s+/g, '')`), so both normalize to `"a&&b"`. A genuinely
different expression (`(a&&b)` vs `a&&b`) is still, correctly, a
different key — only internal whitespace runs were the problem.

**Reactivity (criterion 3, closed).** Nothing previously required the
matched `tick().then(() => V?.focus())` call to be reactive — a call
inside a plain, never-invoked function satisfied the check. Every one of
the 11 real sites already sits inside `$effect(() => { ... })` (confirmed:
`grep -n 'effect('` over all six host files finds one wrapping each site,
no exception), so requiring it costs nothing on the real tree.
`collectFocusCallSites` now threads an `inEffect` flag, set `true` only
while walking inside a `$effect(...)` (or `$effect.pre(...)`) call's own
arguments, and a match is only accepted when `inEffect` is true.

**Named, not closed (criterion 2 and the `{#each}` gap, criterion "closed
or named").** Two panels in the same file under textually identical
conditions remain indistinguishable to this guard — pre-existing gap from
`#0216`'s own header, kept and reworded slightly for the new mechanism but
not newly closed here, per this issue's acceptance criteria treating it as
optional. `{#each}`-rendered panels get no special handling — purely
theoretical, since no `{#each}`-rendered modal panel exists in the tree
today; the header now says so explicitly (it did not before).

**Prose undercount (fixed).** `modalFocusWiring.structuralGuard.test.ts`'s
header said "the six sites nothing mounts today" while its own preceding
sentence enumerated seven (Admin.svelte ×5, CampaignEditor.svelte ×1,
Campaigns.svelte ×1) — internally contradictory, and both undercounted the
true figure. `CampaignSendDialog.behavior.test.ts`'s header had the same
"six sites" error, naming only Admin.svelte's five and CampaignEditor's
one. Confirmed the actual figure directly:
`grep -rl '@testing-library/svelte' src` finds only
`CampaignSendDialog.behavior.test.ts` and `Dashboard.behavior.test.ts`
(Dashboard isn't one of the 11 focus-wiring-site files), so exactly 1 of
the 11 sites (CampaignSendDialog's own) is covered by mounting — the
other **ten** (Admin.svelte ×5, CampaignEditor.svelte ×1, Campaigns.svelte
×1, WorkshopEditor.svelte ×2, Workshops.svelte ×1) are not. Both headers
now say "ten," with the previously-missing WorkshopEditor/Workshops sites
named explicitly.

## Verification

`uptime` before starting: load average ~2.9–3.3 (idle, well under
`CLAUDE.md` §5's ~8 threshold).

- `npx vitest run src/lib/modalFocusWiring.structuralGuard.test.ts` —
  15/15 passed: the repo-wide scan (all 11 real sites found, zero
  violations) plus 14 synthetic fixtures (10 carried over from `#0216`
  unchanged in intent, 4 new: whitespace-mismatch pass, `{:else}`-inverted
  fail, `{:else}`-correct pass, out-of-`$effect` fail).
- Criterion 7 (the four `#0216` probes plus the 11-site survey): all
  covered by the same run above — the repo-wide `it` IS the 11-site
  survey (`expect(sites.length).toBeGreaterThanOrEqual(11)`, satisfied),
  and the four original probes are the "flags a panel with bind:this but
  no onkeydown," "flags the vacuousness case," "flags the symmetric-swap
  shape," and "does not accept a comment-only / HTML-comment-only match"
  fixtures, all still present and passing.
- Guards this issue must not weaken: `npx vitest run
  src/views/admin/CampaignSendDialog.structuralGuard.test.ts
  src/views/admin/CampaignSendDialog.guard.test.ts src/lib/modalKeydown.test.ts
  src/lib/modalEscapeGuard.test.ts src/views/admin/CampaignSendDialog.behavior.test.ts
  src/lib/modalFocusWiring.structuralGuard.test.ts` — 6 files, 68 tests,
  all passed.
- `npm run check` — 270 files, 0 errors, 0 warnings (no new file from this
  issue; the +1 over the 269 baseline is `#0219`'s
  `Admin.behavior.test.ts`, worked in the same session).
- `npm test` (full suite) — 30 test files, 3794 tests passed (baseline 29
  files / 3779 tests; this issue alone contributes +4 tests, all in
  `modalFocusWiring.structuralGuard.test.ts`).
- `ISSUE=0219and0222 scripts/check.sh web` — own database, both issues'
  final state together: `npm run check` 270/0/0, `npm test` 30 files /
  3794 tests, no leftover processes, **VERIFICATION PASSED**.

**jsdom vs. real browser:** not applicable to this file — every assertion
here is against a parsed AST (`svelte/compiler`'s `parse`), not a mounted
component, so nothing rests on jsdom's fidelity either way.

**Mutation proofs, all against the real tree (not just fixtures), each
reverted and confirmed byte-identical with `shasum -a 256` and an empty
`git diff --stat -- web/src/views/Admin.svelte`
(`65dd0e13d1c44b2f074f66d0ce85445ad4194734945e8fc032981984529d0242`
throughout — these two mutations were done before `#0219`'s own edit
landed the export-link anchor in the same file, which is why the hash
here differs from the one `#0219`'s `## Verification` records):**

1. **Reactivity.** Rewrote `Admin.svelte`'s real deactivate-modal
   `$effect(() => { ... })` into a same-named plain function
   (`mutationProof0222NeverCalled`) nothing calls. Guard failed at exactly
   `Admin.svelte:1187` ("... no ... call inside a `$effect(...)`, guarded
   the same way, was found ..."). Reverted; hash matched.
2. **Whitespace.** Removed the internal spaces from `Admin.svelte`'s real
   compound condition, `{#if viewingSubscriber || viewingLoading ||
   viewingError}` → `{#if viewingSubscriber||viewingLoading||viewingError}`,
   leaving the script side's spaced original untouched. Guard still
   passed (0 violations) — proving the normalization works against real,
   not just synthetic, code. Reverted; hash matched.
3. **{:else} inversion.** Proved with the two new synthetic fixtures
   rather than a live-tree edit (no real panel sits in an `{:else}`
   branch today, so constructing this shape live would mean adding new
   markup rather than mutating existing markup — a materially different,
   riskier operation on a 2200+ line shared file than flipping an
   existing condition or removing an existing wrapper). Both fixtures are
   permanent regression tests, not reverted mutations: "flags an inverted
   `{:else}` panel" and "passes a correctly wired `{:else}` panel."

Each of the three real-tree edits was made with `Edit` and reverted with
`Edit` (never `git checkout --`, `git stash`, or `git restore`, per
`CLAUDE.md` §8a), and `git diff --stat -- web/src/views/Admin.svelte` was
confirmed empty immediately after each revert, before the next mutation
was applied.

## Files changed

- `web/src/lib/modalFocusWiring.structuralGuard.test.ts` — redesigned
  guard matching around `GuardConstraint`/negated branches, whitespace
  normalization in `guardKey`, an `inEffect` requirement in
  `collectFocusCallSites`, corrected header prose (site count, mechanism
  description), and 4 new synthetic fixtures.
- `web/src/views/admin/CampaignSendDialog.behavior.test.ts` — corrected
  header prose ("six sites" → "ten sites," with the previously-uncounted
  files named).

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Token counts not available to this subagent — see `CLAUDE.md` "Token usage
and cost tracking": the orchestrator records usage from the dispatching
session's transcript, which this implementation pass does not have access
to.

## Review notes — 2026-08-24 (phase 3, approved)

Re-verified independently at load average 1.63 (idle). All three mutations
below ran in a throwaway `git worktree` at `HEAD` — no `git stash`, no
`git checkout --` on a shared-tree path (`CLAUDE.md` §8a).

**I re-ran all three closures as mutations myself rather than reading the
implementer's account of them.** Baseline in the worktree first:
`npx vitest run src/lib/modalFocusWiring.structuralGuard.test.ts` → 15/15.

1. **Whitespace (criterion 3).** Reverted `guardKey`'s
   `.replace(/\s+/g, '')` to the old `.replace(/\s+/g, ' ')` → **1 failed /
   14 passed**, the failure being exactly *"passes a panel guarded by
   whitespace inside the condition differently on each side"*. Nothing else
   moved, so the change is scoped to the false failure it claims.
2. **Reactivity (criterion 4).** Dropped `&& s.inEffect` from the
   `focusSites.find(...)` at line 457 → **1 failed / 14 passed**, the failure
   being *"flags a focus call that matches everything EXCEPT that it sits
   outside any `$effect`"*.
3. **`{:else}` inversion (criterion 1, the one with teeth).** Flipped
   `collectPanels`' alternate branch back to `negated: false` → **2 failed /
   13 passed**: the inverted fixture stopped being flagged *and* the
   correctly-negated fixture started being flagged — the exact bidirectional
   symptom the issue describes. That is the strongest of the three, because
   a one-sided pass would have left the real hole open.

**Criterion 2 — "closed or named".** Both remaining false passes are named in
the header at lines 120–172: two panels under textually identical conditions
(with the reason closing it is out of proportion, and what closing it would
require), and `{#each}`-rendered panels (with why `bind:this` could not
meaningfully target one iteration anyway). The `{:else}` case, which the
criterion says **must** be closed, is closed. Header prose at lines 1–172
matches what `checkFile` now does, including the "REPLACES, does not
accumulate" design and the negated key — I read the header against
`collectPanels`/`collectFocusCallSites` line by line and found no
overclaim.

**Criterion 6 — the counts, recomputed rather than trusted.**
`grep -rn 'role="dialog"' web/src --include='*.svelte'` gives Admin ×5,
CampaignEditor ×1, Campaigns ×1, CampaignSendDialog ×2, WorkshopEditor ×2,
Workshops ×1 = 12 elements, of which the guard's own scan reports 11 sites
(one of CampaignSendDialog's two carries no `bind:this`). 5+1+1 = 7 plus
`#0052`'s 3 = **10** unmounted, +1 mounted = 11. The arithmetic in both
corrected headers is right.

**Criterion 7.** The repo-wide `it` is the survey, and it passed with zero
violations and `sites.length >= 11` satisfied. All four `#0216` probes are
still present as fixtures and still pass.

**Criterion 8.** `ISSUE=0220rev scripts/check.sh`: Go all `ok`, **zero
skips**, `npm run check` 274/0/0, `npm test` 33 files / 3825 tests.

**One thing that went stale between the commit and this review, recorded so
the next editor is not misled.** `## Implementation notes` above justifies
"ten of eleven" with *"`grep -rl '@testing-library/svelte' src` finds only
`CampaignSendDialog.behavior.test.ts` and `Dashboard.behavior.test.ts`"*.
That grep now returns **three** files — `#0219` added
`web/src/views/Admin.behavior.test.ts` 25 seconds before this issue's own
commit landed. The **conclusion is still correct**: that test mounts
`Admin.svelte` but only clicks through to the Subscribers tab, so none of
Admin's five modal panels ever enters the DOM, and the guard's header claim
("the TEN sites nothing mounts today") is about the *sites*, not the files.
Left as-is rather than rewritten, since the notes are a record of what that
pass ran — but the supporting evidence no longer reproduces, and anyone
re-deriving the count should count opened panels, not files importing
`@testing-library/svelte`.

**What I looked for and did not find.** A `{:else if}` chain keyed as
`!(a) && b` rather than narrowest-wins `b` — it is keyed as `b`, which is
consistent with the deliberate narrowest-wins design the `GuardConstraint`
comment argues for (accumulating ancestors breaks all 11 real sites), so it
is a documented property, not a silent one. Also checked that
`collectFocusCallSites` does not descend into an `IfStatement`'s `test`
(it does not, and no real focus call lives in a condition) and that the
shared `seen` set cannot cause a node to be scored under the wrong guard
(each AST node is reached once, through its owning branch).
