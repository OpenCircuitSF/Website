# 0462 — Two stale snapshots in the migration-citation guard's own comments

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | handlers / guards |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |
| **Closed** | 2026-09-05 |
| **Commit** | 55ac51a |

## Description

**Low priority, and deliberately so — fold this into the next touch of the file
rather than spinning up a pass for it.** `#0459`'s second reviewer said as much
when it reported these, and it was right.

Two comments in `internal/handlers/migration_citation_content_guard_test.go`
record measurements that have since moved:

1. The **floor constants' doc comment** parenthesises the corpus as **74**
   citations. The tree reports **73** — falsified by `#0459`'s own second
   commit, which deleted a dead branch whose doc comment self-cited a migration
   twice. The floors themselves (40 and 15) are unaffected.
2. The **forward-window bullet's file list** is a pre-deletion snapshot. Its
   direction and magnitude still hold — `#0459`'s reviewer independently
   reproduced 21 extra checks and 5 false positives — but one of the named
   locations is now a different file, since two of the original five were that
   guard's own deleted comments.

## Why it is worth recording at all

This is the same class `#0456` and `#0459` are about: a comment asserting a
measured fact that has stopped being true. Both are explicitly labelled as
"while writing this guard" snapshots, which is honest and is why neither was a
bounce — but a snapshot that is silently wrong still costs the next reader a
re-derivation to discover it was only nearly right.

## Acceptance criteria

1. The floor comment's parenthetical matches what the tree reports. **Re-derive
   the count** rather than writing 73 from this issue — `#0459`'s review
   recorded that the harness's own file is inside the scan roots, so a
   measurement taken with a temporary same-package file reads one higher than
   the tree's own figure. That subtlety is exactly how a corrected number goes
   wrong again.
2. The forward-window bullet's file list reflects the current tree.
3. **Do not change the floors** (40 and 15) or any of the three load-bearing
   extraction parts — clause bounding, negation markers, and the field-name
   conversion path. `#0459`'s two reviews verified all of it, and its second
   bounce was for adding machinery that could not execute.
4. **No new mechanism** (§8, `#0258`). Comment text only.
5. If you are here for another reason and these are merely nearby, fix them and
   say so; if this is the whole task, keep it to one commit.

## Fix

Comment-text only, in `internal/handlers/migration_citation_content_guard_test.go`:

1. The floor constants' doc comment now parenthesises the corpus as **73**
   citations (was 74), 34 identifier checks unchanged. Re-derived with a
   throwaway harness kept entirely **outside** `internal/handlers` (a
   standalone `go run` script in the scratchpad, never a same-package test
   file), so the figure it reports is the tree's own count, not one inflated
   by a measuring file sitting inside the guard's own scan roots — the exact
   trap `#0459`'s second review named. It reports 285 files visited, 73
   citations, 34 identifier checks, 0 hits, matching the 285-file "tree's own
   figure" that review's harness-inclusive 286 was checked against.
2. The forward-window bullet's file list now reads
   `internal/db/prd_index_parity_test.go`, `internal/mailing/worker_store.go`,
   `internal/subscribers/erase.go`, `internal/sesnotify/store.go`, and this
   guard's own doc comments — four distinct files plus one self-reference,
   replacing the old list's four-plus-two shape (four files with two of the
   five hits landing back on the guard's own now-deleted branches). Re-derived
   by reproducing the additive forward-window measurement the second review
   used (backward candidates kept, forward-window candidates added only where
   not already found backward): 21 extra identifier checks, 5 false positives,
   at exactly the five locations now named. Direction and magnitude both still
   hold, matching both prior reviews' figures.

Floors (40, 15) and the three load-bearing extraction parts (clause bounding,
negation markers, `goFieldNameToSnakeCase`/field-doc path) are untouched — the
diff is two comment hunks only. No mechanism added.

## Verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0462 scripts/check.sh go ./internal/handlers/... ./internal/db/... ./internal/subscribers/...
```

`VERIFICATION PASSED`. `internal/handlers` `ok 7.237s`, `internal/db`
`ok 7.475s`, `internal/subscribers` `ok 0.661s` — zero skips, no `0.00s`
package. `go build`, `go vet`, `gofmt -l` all clean; `gofmt -w` left the
file's SHA-256
(`f97e6c448af8f1a02365000f922d30af1cad993e8db5064d00a2aaaa3478eb88`) unchanged.
Real test declarations covered by this package run (all passing, none
individually re-run after `check.sh` dropped the scratch database on exit,
per its own cleanup):
`TestMigrationCitationIdentifierMatchesContent`,
`TestMigrationCitationExtractionGuardFiresOnEmptyOrLowCount`,
`TestMigrationCitationNegationMarkerExcludesRealAuditTestShape`,
`TestMigrationCitationSnakeTokenExtractionCatchesSyntheticExample`,
`TestMigrationCitationFieldNameConversionCatchesSyntheticExample`,
`TestGoFieldNameToSnakeCaseMatchesRealSubscriberFields`,
`TestMigrationContentHasIdentifierRejectsSubstringMatch`,
`TestMigrationCitationGuardCatchesBothRealHistoricalCases`. Machine load:
`load averages: 2.53 2.76 2.50` (§5 — recorded, not reasoned from).
`git status --porcelain` before the run showed nothing dirty beyond this
issue's own markdown edit; no other agent's file was touched.

## Files changed

- `internal/handlers/migration_citation_content_guard_test.go` — two
  comment-only edits: the floor doc comment's citation count (74 → 73) and
  the forward-window bullet's file list (refreshed to the current tree).

## Gotchas

- The re-derivation harness lived entirely outside `internal/handlers` (a
  standalone Go program under the scratchpad, `go run`, no `go.mod`, stdlib
  only) specifically so it could not itself become a same-package file the
  guard's own `walkGoFiles` would count — the inflation `#0459`'s second
  review documented and warned the next reader about. Deleted after use;
  confirmed absent with `git status --porcelain` (nothing outside this one
  tracked file was ever touched) and a directory listing of the scratchpad.

## Relation

- `#0459` built the guard; its two reviews found both snapshots.
- `#0456` is the defect that motivated the guard, and the same class as this.
- `CLAUDE.md` §8's citation-guard-prose entry explains why a guard's own
  comments are unusually prone to this.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/handlers/... ./internal/db/... ./internal/subscribers/...`. Scan bytes with `python3`, never BSD `grep -P`.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-09-05 | implement | claude-sonnet-5 | — | — | — | — | — |
| 2026-09-05 | review | claude-opus-5 | — | — | — | — | — |

Token usage not available from this session's environment; recorded as `—`
per the "never fabricate counts" rule.

## Review notes

> 🟢 Passed 2026-09-05 — a two-comment fix, reviewed proportionately. Both
> refreshed numbers were re-derived independently and both match; every
> executable byte in the file is unchanged.

**Verdict: pass.** All five acceptance criteria hold.

### The count, re-derived by a different method than the implementer used

The implementer measured with a standalone stdlib harness kept outside
`internal/handlers`, to dodge the inflation the second `#0459` review
documented (a measuring file placed inside the scan roots is itself walked,
reading one higher). I took a different route to the same guarantee: a
throwaway `git worktree add --detach` at the commit under review, in which I
raised **only floor constants** — `citedTestScanRootsMinPlausibleFileCount` in
one run, `migrationCitationMinPlausibleIdentifierCount` in another — so the
guard's own failure messages report the live figures. Those constants are
code, not comment text, so nothing the comment scan can see moved, and no
file was added to the scan roots. This measures the tree with the guard's own
machinery rather than a reimplementation of it, which is the stronger oracle.

| Figure | Reported by the tree | Comment now says |
|---|---|---|
| `.go` files visited | 285 | (not stated) |
| `migrations/NNNNNN` citations | **73** | **73** |
| identifier-against-migration checks | **34** | 34 |
| hits | 0 | — |

Criterion 1 holds: 73 is the tree's own figure, not an inflated one, and 34
was correctly left alone.

### The forward-window bullet, reproduced

In the same worktree I re-applied the naive symmetric forward window the
bullet describes — keeping the backward clause-bounded candidates and adding
snake tokens between the citation and the next `.`/`:`/`;` after it, deduped
through the existing candidate map, so only genuinely new ones count. The
result matches both prior reviews and the refreshed list exactly:

- **21 extra identifier checks** — 55 under the forward window against 34
  without it.
- **5 false positives**, at five distinct locations:
  `internal/db/prd_index_parity_test.go`, `internal/mailing/worker_store.go`,
  `internal/subscribers/erase.go`, `internal/sesnotify/store.go`, and one in
  the guard's own doc comments.

So both of the things worth confirming about the rewritten bullet check out.
`internal/sesnotify/store.go` genuinely is one of the current five — it is not
a substitution made to keep the count at five. And **"this guard's own doc
comments" is now correctly singular**: the pre-deletion snapshot said "twice"
because two of the five landed on the branches `#0459`'s second commit
removed; exactly one such location survives, and the bullet's four-files-plus-
one-self-reference shape is what the tree reports.

### Nothing else moved

Stronger than hashing the eight declarations: I stripped every comment line
from the file at the commit and at its parent and hashed the remainder. Both
sides give `2dc4219f…4d6bd3` over 583 code lines — so the floors (40 and 15)
and all three load-bearing extraction parts (clause bounding, negation
markers, and the `goFieldNameToSnakeCase`/field-doc conversion path) are
byte-identical, and criteria 3 and 4 are satisfied by construction rather
than by inspection. Independently, every added and removed line in the diff
matches a comment-line pattern — zero non-comment changed lines — and the
diff is the two comment hunks the `## Fix` section claims, 4 insertions and
3 deletions in one file.

### No scratch file survived

`git status --porcelain --ignored -- internal/handlers/` is empty, the
directory contains no non-`.go` entry, and the only dirty path in the repo
attributable to this issue is this markdown file. A leftover measuring file
in that directory would have been walked by the guards it shares a package
with — which is precisely the inflation trap this issue's criterion 1 warns
about — so this was checked rather than assumed. My own worktree was removed
with `git worktree remove --force`; the remaining entry in `git worktree
list` belongs to another agent and was not touched.

### Nothing further worth tracking

Asked explicitly, and the answer is no. `#0459`'s two reviews audited the
guard itself thoroughly, including proving each load-bearing part by mutation
and verifying the fail-closed path; this issue closed the last two stale
snapshots those reviews reported. The one residual thought is not worth an
issue: the floor comment is a snapshot by nature and will drift again the
next time the tree's citation count moves. But it is honestly labelled as a
"while writing this guard" measurement, the floors sit well below it on
purpose so drift is harmless, and pinning it to a live count would be exactly
the new mechanism criterion 4 forbids and `#0258` warns against. Leave it.

### Verification

Machine load recorded, not reasoned from (§5): `load averages: 1.74 2.08
2.26` before the run, well inside the quiet band. `git status --porcelain`
before the run showed `issues/0461.md` dirty from another agent — left
untouched — plus this file's own edit.

```
ISSUE=0462r scripts/check.sh go ./internal/handlers/... ./internal/db/... ./internal/subscribers/...
```

`VERIFICATION PASSED`. `internal/handlers` `ok 7.697s`, `internal/db`
`ok 7.876s`, `internal/subscribers` `ok 0.579s` — zero skips, no package
reporting `0.00s`. `go build`, `go vet` and `gofmt -l` all clean, and
`gofmt -w` left the guard file's SHA-256
(`f97e6c448af8f1a02365000f922d30af1cad993e8db5064d00a2aaaa3478eb88`)
unchanged, independently confirming the implementer's recorded hash. Real
test declarations exercised in this package run, all passing:
`TestMigrationCitationIdentifierMatchesContent`,
`TestMigrationCitationExtractionGuardFiresOnEmptyOrLowCount`,
`TestMigrationCitationNegationMarkerExcludesRealAuditTestShape`,
`TestMigrationCitationSnakeTokenExtractionCatchesSyntheticExample`,
`TestMigrationCitationFieldNameConversionCatchesSyntheticExample`,
`TestGoFieldNameToSnakeCaseMatchesRealSubscriberFields`,
`TestMigrationContentHasIdentifierRejectsSubstringMatch`, and
`TestMigrationCitationGuardCatchesBothRealHistoricalCases`.
