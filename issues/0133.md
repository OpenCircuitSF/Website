# 0133 — `progress_test.go` still describes seven `email_sends.status` values after `#0131` cut them to five

| | |
|---|---|
| **Status** | in-progress |
| **Module** | mailing / docs |
| **Platform** | All |
| **Phase** | 5 — Campaigns |
| **First seen** | 2026-08-22 |

## Description

`#0131` removed `'bounced'` and `'complained'` from the `email_sends.status` CHECK, leaving five reachable values. `internal/mailing/progress_test.go` was not part of that change and still describes seven: comments at lines 20 and 44 say "the seven `email_sends.status` values", and the test itself is named `TestWorker_PublishProgress_SevenStatusArithmetic` (lines 59 and 61).

The test seeds only the five live statuses, so it passes and its arithmetic is right. Only the naming and the comments are stale — but a test named for a count the schema no longer permits is exactly the kind of thing a future reader trusts and a future change breaks.

## Acceptance criteria

- [x] The comments at `internal/mailing/progress_test.go:20` and `:44` describe the five statuses the CHECK actually admits
- [x] `TestWorker_PublishProgress_SevenStatusArithmetic` is renamed to match what it now covers, and the rename is reflected everywhere the name appears
- [x] `ISSUE=0133 scripts/check.sh go ./internal/mailing/...` passes and the renamed test is observed running by name
- [x] While in the file: `issues/0049.md`'s ride-along correction says `CampaignStats.svelte`'s `.length >` is at line 44; it is at line 55. Correct the line number

## Notes

Found by `#0131`'s phase-3 review, which swept the tree for surviving references to the two dropped values. It classified this one as belonging to `#0048`'s territory (the progress publisher) rather than to `#0131`, and recorded the `0049.md` line-number nit separately without touching that file.

Cosmetic and non-urgent — nothing is wrong at runtime. Filed so it does not get rediscovered by the next person who greps for `bounced`.

## Relation

- Found by: [#0131](0131.md) — `email_sends.status` permits two values nothing can ever write
- Relates to: [#0048](0048.md) — Stream send progress over SSE
- Relates to: [#0049](0049.md) — Build the campaign stats view

## Root cause

`internal/mailing/progress_test.go` was written for `#0048` when `email_sends.status`
admitted seven values under `migrations/000018` (`queued`, `sending`, `sent`,
`failed`, `bounced`, `complained`, `skipped`). `#0131` dropped `'bounced'`/
`'complained'`, narrowing the CHECK to five, but that change touched only
`internal/handlers/admin_campaign_stats_test.go` (renamed its own
`SevenStatuses` test) and left this file's package doc comment, a helper
comment, and the test's own name still narrating the old count of seven
(and, in the package doc's history note, "the original six" predating
`'sending'` — actually four, per `000017`).

## Fix

- Package doc comment (`progress_test.go:4-7`): "gained a SEVENTH status" →
  "gained a FIFTH status"; "the original six statuses" → "the original four
  statuses" — matching `000017`'s four (`queued`, `sent`, `failed`, `skipped`)
  widened to five by `000018`'s `'sending'`.
- `seedSendWithStatus`'s comment (`:44`): "the seven `email_sends.status`
  values" → "the five `email_sends.status` values".
- Renamed `TestWorker_PublishProgress_SevenStatusArithmetic` to
  `TestWorker_PublishProgress_StatusArithmetic` everywhere the name appeared
  in this file (doc comment at old `:20`, doc comment and `func` at old
  `:59`/`:61`) — dropping the count from the name entirely, following
  `#0131`'s own precedent of renaming `BucketsAllSevenStatuses` to
  `BucketsAllValidStatuses` so a future CHECK change can't make the name
  stale again. No assertions changed; the test already seeded only the five
  live statuses and its arithmetic was already correct.
- Ride-along (criterion 4): verified `CampaignStats.svelte`'s
  `let hasFailedSends = $derived(failedSends.length > 0)` is at **line 55**
  directly (`grep -n "length >" web/src/views/admin/CampaignStats.svelte`),
  confirming `#0131`'s reviewer's correction of the earlier wrong `44`.
  Corrected both occurrences of "line 44" in `issues/0049.md` — one added by
  `#0049`'s own resolution review, one added by `#0131`'s ride-along — to
  `line 55`. Touched only those two lines.
- Left historical mentions of `TestWorker_PublishProgress_SevenStatusArithmetic`
  in `issues/0048.md`, `issues/0118.md`, and `issues/0131.md` untouched —
  those are work-log narration of what was true at the time each ran, not
  live references to the code, and rewriting them would misrepresent
  history rather than correct stale documentation.

## Verification

- `uptime` before running: load average ~2.1–2.2 (idle).
- `go build ./internal/mailing/...` and `go vet ./internal/mailing/...`:
  clean, no output.
- `ISSUE=0133 scripts/check.sh go ./internal/mailing/...`: the `go test
  ./internal/mailing/... -p 2` step reported `ok
  github.com/brennanMKE/OpenCircuitSF/internal/mailing 0.885s`, zero skips.
  The script's overall verdict was `VERIFICATION FAILED` only because its
  whole-tree `go build ./...`/`go vet ./...` steps failed on
  `internal/handlers/admin_workshops.go: undefined: isSafeCoverImage` /
  `coverImageErrorMessage` — confirmed via `git status` to be `#0138`'s
  concurrent, uncommitted in-flight work (also dirty: several `web/src/lib/`
  and `web/src/views/admin/WorkshopEditor.svelte` files), not anything this
  issue touched. `internal/mailing` builds, vets, and tests clean in
  isolation.
- Ran the renamed test by name in a private database
  (`scripts/testdb.sh create 0133`):
  `go test ./internal/mailing/... -run '^TestWorker_PublishProgress_StatusArithmetic$' -v -p 2`
  → `=== RUN   TestWorker_PublishProgress_StatusArithmetic` /
  `--- PASS: TestWorker_PublishProgress_StatusArithmetic (0.02s)` / `ok`.
  Dropped the scratch database afterward (`scripts/testdb.sh drop 0133`).
- `grep -rn "SevenStatusArithmetic" --include="*.go" .` — no matches; the old
  name no longer exists anywhere in Go source.
- `pgrep -fl 'go test|\.test '` — none left running.

## Files changed

- `internal/mailing/progress_test.go` — comment wording and test rename (see `## Fix`)
- `issues/0049.md` — corrected two "line 44" → "line 55" ride-along citations

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 84 | 6,952 | 2,841,507 | 65,437 | $1.20 |

**Total: $1.20**
