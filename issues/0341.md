# 0341 — `AdminResendConfirmation`'s claim guards `complained` differently from its twin

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | subscribers |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-28 |
| **Closed** | 2026-09-01 |
| **Commit** | `c01605d` |

## Description

`#0314`'s plan adds a `status <> 'complained'` guard to
`ClaimAndEnqueueConfirmation`'s claim, inside the SQL.

`AdminResendConfirmation`, its twin, guards `status = 'pending'` in Go under
`FOR UPDATE` instead. That is **sound** — the row lock makes the check-then-act
atomic, and `pending` excludes `complained` today.

~~The defect is the asymmetry, not a live hole.~~ **Correction (#0341,
2026-09-01).** This was written on the assumption that `#0314`'s plan — "adds a
`status <> 'complained'` guard to `ClaimAndEnqueueConfirmation`'s claim, inside
the SQL" — had already landed by the time this issue would be worked. It had
not: as of this implementation pass, `#0312`/`#0313`/`#0314` are still `open`,
carrying only the shared `## Plan` section from their 2026-08-28 planning pass
(commit `b8af685`), with no implementing commit. So the code this issue was
actually implemented against had **no status guard on `ClaimAndEnqueueConfirmation`'s
claim at all** — not even the `<> 'complained'` form this description compares
against — which is a live hole, not merely an asymmetry of expression: a
`complained` row that lost its `confirm_token` race (an SES complaint landing
between a caller's status read and this claim's commit) would be claimed and
mailed a fresh confirmation. See `## Verification` below for the failing-test
proof against the pre-fix code. Two functions doing the same job
now express the same safety property two different ways, in two different
layers, and only one of them names `complained` explicitly. `complained`
subscribers never auto-resubscribe — only an admin clears that state — and §9
lists that among the restricted rules. A rule that load-bearing should be
expressed the same way everywhere it is enforced, or the divergence should be
deliberate and documented.

It is worth an issue rather than a note because the next person to add a third
claim path has two incompatible precedents to copy, and the Go-side one depends
on an implicit `pending`-excludes-`complained` invariant that nothing asserts.

## Acceptance criteria

1. Both claim paths express the `complained` exclusion the same way, or the
   divergence is documented at both sites with the reason.
2. The implicit invariant — that `status = 'pending'` excludes `complained` —
   is either asserted by a test or made explicit in the predicate.
3. No path can auto-resubscribe a `complained` address (§9, restricted). Proved
   by mutation against both claim paths: the mutation must fail on the un-fixed
   code.
4. `FOR UPDATE`'s atomicity is preserved wherever it currently provides it —
   this must not become a check-then-act race.
5. Enumerate every claim path that transitions a subscriber toward `confirmed`
   and confirm each is covered; there may be more than these two.

## Verification

**Premise check (before implementing, per `CLAUDE.md`'s "check an issue's own
… language before following it").** The dispatch for this issue asserted
`#0312`/`#0314` had "landed." Checked against `git log --oneline -- issues/0312.md
issues/0313.md issues/0314.md` and `scripts/list-issues-by-phase --open
--no-color`: all three are still `status: open`, carrying only their shared
`## Plan` section (commit `b8af685`, 2026-08-28) and no implementing commit.
The premise was stale; see the struck-and-corrected paragraph in `##
Description` above. Implemented against the code as it actually stands today,
per `CLAUDE.md`'s instruction for exactly this situation.

**What changed** (`internal/subscribers/store.go`,
`internal/subscribers/pending.go`):

- `ClaimAndEnqueueConfirmation`'s claim UPDATE gained `AND status = $4` (bound
  to `StatusPending`) in its `WHERE` clause, alongside the existing
  `confirm_sent_at`-cooldown condition. This is the same predicate
  `AdminResendConfirmation` already checks in Go (`sub.Status != StatusPending`
  -> `ErrNotPending`) under its `SELECT … FOR UPDATE` — criterion 1 ("the same
  way") and criterion 2 (no implicit invariant: the predicate now says
  `pending`, full stop, rather than relying on `pending` happening to exclude
  `complained`) are both closed by using the identical predicate value, in the
  layer each method's own transaction shape calls for.
- No change to `AdminResendConfirmation`'s mechanism or to any `FOR UPDATE`
  site — criterion 4 holds by construction (nothing that had a row lock lost
  it or gained a check-then-act gap; `ClaimAndEnqueueConfirmation` never had a
  prior `SELECT`, so its claim staying a single atomic conditional `UPDATE`
  with one more `AND` term preserves the exact same atomicity it had before).
- Both methods' doc comments now cross-reference each other and record the
  reasoning for the two-different-layers choice, closing criterion 1's
  "or documented" branch as belt-and-suspenders alongside the "same predicate"
  fix itself.
- `ClaimAndEnqueueConfirmation`'s doc comment enumerates all three
  outbox.KindConfirmation-producing paths (`Create`, `ClaimAndEnqueueConfirmation`,
  `AdminResendConfirmation`) and states why each is covered — criterion 5.
  Verified nothing else produces this event kind: `grep -rn KindConfirmation
  internal/` (not pasted here, just the file list) turns up only
  `internal/outbox/store.go` (the constant), `internal/subscribers/pending.go`
  and `store.go` (the three claim methods, `pending.go`'s doc comment), the two
  admin handler files that only *display* pending state, and
  `internal/mailing/outbox_worker.go`, which sends whatever a claim already
  enqueued and never re-derives or re-checks subscriber status.
- Criterion 3 (no path can auto-resubscribe a `complained` address):
  independent of this issue, `Confirm` (`store.go`) already refuses outright on
  `sub.Status == StatusComplained` (`ErrComplainedLocked`) before any status
  write — that is the actual resubscribe gate, and it was never in question
  here. What this issue closes is the narrower, still real gap the mutation
  test below proves: `ClaimAndEnqueueConfirmation` claiming and mailing a
  confirmation to an already-`complained` row, which it could do today even
  though the mail would lead nowhere (any click would hit `Confirm`'s guard).

**New test, and the required failing-code proof.** Added
`TestClaimAndEnqueueConfirmation_RefusesComplainedRow` and a parameterized
seed helper `coldConfirmationSubscriberWithStatus` in
`internal/subscribers/store_test.go`. Per `CLAUDE.md` §5/§8b, proved the test
fails on the pre-fix code before trusting it: saved a byte-identical copy of
the fixed `store.go` to the scratchpad, edited the tracked file in place to
restore the pre-fix `WHERE` clause (only the cooldown condition, no status
term), ran the new test alone against an isolated per-issue database, confirmed
the failure, then restored the fixed file from the saved copy and verified
`shasum -a 256` matched before re-running the full suite. This is the same
copy-aside-and-hash-verify technique `CLAUDE.md` §8a prescribes for a
before/after measurement on a file no other agent shares.

```
$ ISSUE=0341 scripts/check.sh go ./internal/subscribers/... -run TestClaimAndEnqueueConfirmation_RefusesComplainedRow -v
    store_test.go:1408: claimed = true for a status=complained row, want false (the status guard must refuse this claim)
--- FAIL: TestClaimAndEnqueueConfirmation_RefusesComplainedRow (0.00s)
FAIL
```

After restoring the fix (hash-verified identical to the saved copy):

```
$ ISSUE=0341 scripts/check.sh go ./internal/db/... ./internal/handlers/... ./internal/mailing/... ./internal/outbox/... ./internal/subscribers/...
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.288s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	6.279s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	9.288s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/outbox	3.635s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/subscribers	0.557s
(skip audit: no skips)
VERIFICATION PASSED
```

```
$ scripts/check.sh guards
go_file_visit_floor_guard_test.sh: all guards hold (0 failures)
VERIFICATION PASSED
```

`gofmt -l` clean on all three touched files
(`internal/subscribers/store.go`, `internal/subscribers/pending.go`,
`internal/subscribers/store_test.go`); `go vet ./...` and `go build ./...`
both clean as part of the `check.sh` run above. Scratch database
`opencircuit_test_0341` was torn down by `check.sh` on exit.

**Second implementation pass (`c01605d`), applying the review's R1–R5.**
Applied all five as pure transcription — each anchor was confirmed unique
in the tree with `grep -n` before editing, and no line was changed that
the review did not name. Independently re-checked R3 against the actual
`statusLockedFromNonAdmin` package doc comment (`store.go` lines 29–41)
before applying it: it says the check "lives in one place ... so a future
mutator can't add a new unguarded status write", i.e. deliberate
*convergence* on one mechanism — confirming the review's finding that the
removed sentence had inverted this, and that acceptance criterion 1's
"or documented" branch had been resting on a mis-citation.

**Structural proof the pass is comment-only.** Built a small Python
checker (kept in the scratchpad, not the tree) that walks `git diff --
internal/subscribers/store.go internal/subscribers/pending.go` and
requires every `+`/`-` line to be blank or start with `//` once the
leading marker is stripped, printing one `VERDICT: PASS`/`VERDICT: FAIL`
line. Proved falsifiable and fail-closed *before* trusting it: a
built-in self-test asserts the checker (a) rejects a synthetic diff
containing one genuine code-line mutation, (b) fails on an empty/absent
diff rather than passing vacuously, and (c) still passes a diff that is
genuinely comment/blank-only. All four self-checks passed, then the real
run reported `changed_lines=22`, all blank or `//` comments, `VERDICT:
PASS`. This matches the code commit's `git show --stat` (16
insertions/6 deletions across the two files) and the two files' `gofmt
-l` staying clean (no output) after the edits.

```
$ ISSUE=0341b scripts/check.sh go ./internal/db/... ./internal/handlers/... ./internal/mailing/... ./internal/outbox/... ./internal/subscribers/...
--- FAIL: TestNoOpenIssueVerificationCitesUndefinedTestFunction (0.06s)
    issue_citation_guard_test.go:671: issue file cites a test function that no
    function in the tree defines (see this section's own "On #0314"
    paragraph below for the offending identifier)
FAIL	github.com/brennanMKE/OpenCircuitSF/internal/db	0.268s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	6.373s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	9.316s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/outbox	3.639s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/subscribers	0.728s
```

(That last code line is paraphrased rather than pasted verbatim from the
real transcript, deliberately — pasting the literal offending identifier
a second time would trip this same guard again, on this new paragraph.
The real line is below, at what is now line 415 after this insertion.)

That one failure is **pre-existing and out of this pass's scope**: it is
`internal/db`'s citation guard (`#0265`/`#0268`) rejecting the bare
identifier inside this issue's own `## Review notes` ("On `#0314`"
paragraph below, currently at line 415) — the *previous* review commit
(`ed96e20`), describing a test `#0314`'s plan will add, which does not
exist yet. This pass did not touch `## Review notes` and did not
introduce this citation; confirmed by
re-running the isolated test directly against the unmodified working
copy of `issues/0341.md`. Reported to the orchestrator rather than
fixed here, since editing the reviewer's own `## Review notes` record is
outside this pass's authorized R1–R5 scope. `go build`, `go vet`, and
`gofmt -l` were all clean in the same run (visible above the pasted
tail). Re-running the scoped suite a second time reproduced the identical
single failure, confirming it is deterministic, not load-related
(`uptime` before this pass: load averages 1.52 1.70 1.80).

```
$ scripts/check.sh guards
go_file_visit_floor_guard_test.sh: all guards hold (0 failures)
VERIFICATION PASSED
```

Scratch database `opencircuit_test_0341b` was torn down by `check.sh` on
exit.

## Files changed

- `internal/subscribers/store.go` — `ClaimAndEnqueueConfirmation`'s `WHERE`
  clause gains `AND status = 'pending'`; doc comment rewritten.
- `internal/subscribers/pending.go` — `AdminResendConfirmation`'s doc comment
  cross-references the new guard.
- `internal/subscribers/store_test.go` — new
  `coldConfirmationSubscriberWithStatus` helper and
  `TestClaimAndEnqueueConfirmation_RefusesComplainedRow`.

## Review notes

> 🔴 Bounced 2026-09-01 — the code change is correct, minimal, and
> independently proved; five factual claims in the doc comments it added
> are not. Remedy below is transcription-level.

**Independently verified, and all of it holds.**

- **The gap was real, and the premise correction is right.** Read
  `9cf6898^:internal/subscribers/store.go` directly: the claim was
  `WHERE id = $1 AND (confirm_sent_at IS NULL OR confirm_sent_at < $3)` —
  no status term of any kind, not the `<> 'complained'` form `##
  Description` originally compared against. `#0312`/`#0313`/`#0314` are all
  still `status: open` (`git log -- issues/0312.md issues/0313.md
  issues/0314.md` shows only `91800b6` filing, `b8af685` planning, and
  `bcf3239`'s PRD-deviation note — no implementing commit). The
  implementation pass was right to check the premise and right about what
  it found.
- **`KindConfirmation` producing paths — re-derived, not taken on trust.**
  `grep -rn 'KindConfirmation' --include='*.go' .` yields exactly three
  non-test producers: `Create` (`store.go:476`),
  `ClaimAndEnqueueConfirmation` (`store.go:614`), `AdminResendConfirmation`
  (`pending.go:214`). Cross-checked from the other direction by
  enumerating every non-test `EnqueueTx`/`Enqueue` call site — the
  remaining ones in this package are `ClaimAndEnqueueAlreadySubscribed`
  (`KindAlreadySubscribed`), `Confirm` (`KindWelcome`) and
  `ImportStore.Commit` (`KindImportInvite`), none of them confirmations.
  `RestartSignup` writes no outbox row at all. `internal/mailing`'s
  `OutboxWorker.render`'s `KindConfirmation` arm reads only `row.Payload`
  and `row.Recipient` and consults neither `subscribers.status` nor
  `suppressions`, so the claim really is the last gate. **No fourth path.**
- **Concurrency: the two mechanisms are equivalent, and the SQL one is if
  anything the stronger.** Under pgx's default READ COMMITTED, an `UPDATE`
  that meets a concurrently-locked row blocks and then re-evaluates its
  `WHERE` against the newly committed version (EvalPlanQual), so all four
  interleavings are safe: complaint commits before the claim → predicate
  fails, 0 rows; complaint's transaction open when the claim arrives →
  claim blocks, re-qualifies against `complained`, 0 rows; claim commits
  first → both methods legitimately mail a row that was pending at commit
  time; two concurrent claims → the loser re-qualifies against the
  now-stamped `confirm_sent_at`. `AdminResendConfirmation` gets the same
  four outcomes from holding the row lock across check and write. The
  difference is where the correctness rests: there it depends on the `FOR
  UPDATE` being retained (which is why that site's comment is so emphatic,
  `#0263`), here there is no read-then-act window to lose in the first
  place. **Not weaker.**
- **The new test genuinely fails without the fix, re-proved from scratch.**
  `git archive HEAD` into a scratchpad export (§8b — never the shared
  tree), own database `ISSUE=0341m`, mutation M2 (drop `AND status = $4`
  and its bind, exactly `#0314`'s plan's named mutation). Result:
  `TestClaimAndEnqueueConfirmation_RefusesComplainedRow` FAILs at
  `store_test.go:1408` and **every other test in the package passes** — so
  the new test is the sole detector and nothing pre-existing covered this.
  Restoring `store.go` from `HEAD` (hash-verified
  `64dd15e4…b980875`, identical to the tracked file) makes it PASS. The
  test seeds its own row through `uniqueEmail`, never a literal or seeded
  id (§8b).
- **Atomicity unchanged.** One more `AND` term on the same single
  conditional `UPDATE`; no new statement, no new lock, no change to the
  `Begin`/`EnqueueTx`/`postEnqueueCommitHook`/`Commit` sequence or to the
  `RowsAffected() != 1 → return false, nil` degrade. A lost claim still
  returns `claimed=false` with nothing written, which every caller already
  treats as "decline to send".
- **The strike-and-date correction is exact.** Checked programmatically
  against `95ec252:issues/0341.md`: the struck span with `~~` removed is
  byte-identical to the original sentence, the note is the dated block form
  `**Correction (#0341, 2026-09-01).**`, and the remainder of `##
  Description` is byte-identical after whitespace normalisation. Nothing
  paraphrased away.
- **Verification quality.** `## Verification` names the specific test, and
  the suite really is green. Re-run here: `ISSUE=0341r scripts/check.sh go
  ./internal/db/... ./internal/handlers/... ./internal/mailing/...
  ./internal/outbox/... ./internal/subscribers/...` → all five packages
  `ok`, `(no skips)`, `gofmt -l` clean, `VERIFICATION PASSED`;
  `scripts/check.sh guards` → all five guard scripts pass. `gofmt -w` on
  all three touched files is a byte-level no-op (proved on the export, not
  the shared tree). No secrets in the pasted transcripts. The
  template-digest rebuild notice is `#0360`/`#0320`, expected.
  - One nit for next time, not a defect: the pasted `scripts/check.sh
    guards` transcript shows only `go_file_visit_floor_guard_test.sh`'s
    summary line. That mode runs **five** scripts. Eliding four of them
    reads as if only one ran.

**What must change before this can be resolved.** All four are in prose,
and each is a single substitution.

**R1 + R2 — `internal/subscribers/store.go`, the `# #0341` block.** The
sentence currently reads `— a race #0341's review found this method had no
defense against at all, unlike its twin below.` Two problems: no review of
`#0341` had happened when that was written (the *implementation* pass found
it, and `#0314`'s plan had already recorded it independently — `#0341`'s own
filing asserted the opposite), and `AdminResendConfirmation` is not "below" —
it is in `internal/subscribers/pending.go`, a different file, so the
positional referent points at nothing. Replace these three lines:

```go
// that read and this claim's commit — a race #0341's review found this
// method had no defense against at all, unlike its twin below.
// AdminResendConfirmation guards the identical predicate
```

with:

```go
// that read and this claim's commit — a race this method had no defense
// against at all, unlike its twin AdminResendConfirmation in this
// package's pending.go. #0314's plan recorded the same gap independently
// ("if an SES complaint lands between dispatchMutation's FindByEmail and
// this claim, the claim still fires on a complained row and mails it");
// #0341 was filed believing it was a stylistic asymmetry only, and its
// implementation pass found otherwise — see that issue's struck-and-
// corrected Description.
// AdminResendConfirmation guards the identical predicate
```

**R3 — same file, the "Both methods now express the same predicate"
paragraph.** It ends by citing the package doc comment as precedent: `— the
same deliberate-divergence shape the package doc comment above already
documents for statusLockedFromNonAdmin.` That inverts what the package doc
says. Its words are *"The check lives in one place (statusLockedFromNonAdmin
…) so a future mutator can't add a new unguarded status write the way an
earlier version of this file did"* — it documents deliberate **convergence**
on one mechanism, with exactly one named exception (`AdminClearComplaint`)
and one plumbing variant (querier vs pool, same `UPDATE`). This matters
beyond tidiness: acceptance criterion 1's "or the divergence is documented
at both sites with the reason" branch is currently leaning on a mis-citation.
Replace:

```go
// property differs — the same deliberate-divergence shape the package doc
// comment above already documents for statusLockedFromNonAdmin.
```

with:

```go
// property differs. This is deliberately NOT the statusLockedFromNonAdmin
// shape the package doc comment above describes: that constant keeps the
// complained guard in ONE place across every status MUTATOR, and neither
// method here writes status at all — each only refuses to act on a row
// that is not pending. The divergence recorded here is of mechanism inside
// one predicate, and it is confined to these two methods.
```

**R4 — `internal/subscribers/store.go`, same block, one phrase.** `gets the
identical atomicity property without a row lock or a second query` is
literally false: the `UPDATE` does take a row-level lock on the tuple it
updates. What it avoids is a *prior, separate* lock. Change `without a row
lock or a second query` to `without a prior SELECT ... FOR UPDATE or a
second query`.

**R5 — `internal/subscribers/pending.go`, guard 2's added note.** `this one
already needs the FOR UPDATE read for guards 4 and 5` is under-inclusive:
guard 3 (the unaccepted-invitation test, `sub.ImportID != nil &&
sub.ConsentBasis == nil`) is read off the same locked row. Change `guards 4
and 5` to `guards 3, 4 and 5`.

**R6 — this file.** The `**Commit**` row is still `—`. Issues.md phase 2
step 7 requires the code-fix hash; set it to whatever the final code commit
is (`9cf6898` if the remedy lands as an amend-style follow-up, else the new
hash).

**Not required, and deliberately out of scope.** Two things this review
found that `#0341` should *not* grow to cover — reported separately to the
orchestrator rather than added here:

1. There is no send-time backstop. A complaint that lands *after* a claim
   commits but before `OutboxWorker` drains the row still gets a
   confirmation delivered, because the worker consults neither
   `subscribers.status` nor `suppressions` for `KindConfirmation`. Both
   claim paths share this, it predates `#0341`, and closing it is a
   different change in a different package.
2. The payload token is still the *caller's* possibly-stale
   `sub.ConfirmToken`. `#0314` step 1 fixes exactly that with its
   `SELECT ... FOR UPDATE` re-read; nothing here makes it worse.

**On `#0314`, since this fix pre-empts part of it.** `#0314`'s plan already
specifies this exact predicate (`AND status = $6 -- 'pending'`, and in so
many words *"It is strictly stronger than `<> 'complained'`"*), plus a test
`…_RefusesComplained` (planned, not yet defined) and mutation M2. So the
predicate as landed will survive that rewrite — it is written into it — but
`#0314`'s implementer following the plan literally will re-add a
near-identically named test alongside this issue's
`…_RefusesComplainedRow`. The doc-comment note ("Whoever implements #0314
should fold this WHERE-clause predicate into that rewrite rather than drop
it") is the right mechanism and is correctly placed, but it is only visible
to someone already reading this method; `#0314`'s own plan is what its
implementer will read first. Recommended to the orchestrator, not done here:
amend `#0314`'s plan to record that `#0341` landed the predicate and the
test.

## Review notes — second pass, 2026-09-01 (`claude-opus-5`)

> 🟢 **Resolved** at `9cf6898` (the behaviour change) and `c01605d`
> (the doc-comment corrections). Every one of R1-R6 landed as pure
> transcription, every corrected sentence now reads true against the code,
> and all five acceptance criteria were re-verified independently.

**Each corrected sentence read against the code, not against the issue.**

- **R1 + R2 — provenance and the dangling referent.** The false
  "`#0341`'s review found" attribution is gone; the replacement credits the
  implementation pass and `#0314`'s plan, and the referent now names
  `AdminResendConfirmation` and the file it lives in. Checked the quotation
  attributed to `#0314`'s plan character by character against `issues/0314.md`
  — the plan reads *"Today, if an SES complaint lands between
  `dispatchMutation`'s `FindByEmail` and this claim, the claim still fires on
  a `complained` row and mails it"*, so the doc comment's version is the same
  sentence with the backticks and the leading "Today," dropped. Nothing
  paraphrased into it. `dispatchMutation` and `FindByEmail` are both real
  identifiers in `internal/handlers/subscribe.go`, and `AdminResendConfirmation`
  really is in this package's `pending.go`.
- **R3 — the `statusLockedFromNonAdmin` citation. Read from the source, not
  from the bounce.** The package doc comment says the complained check *"lives
  in one place (statusLockedFromNonAdmin, used by setStatusTx and by
  Unsubscribe's and RestartSignup's own UPDATEs) so a future mutator can't add
  a new unguarded status write the way an earlier version of this file did"*,
  with `AdminClearComplaint` named as the sole exception. That is deliberate
  **convergence** on one mechanism, so the bounced sentence — which cited it as
  a "deliberate-divergence shape" — did invert it, and the first review's
  finding holds. The replacement does not overstate in the other direction: it
  says only that this is *not* that shape, gives the reason (`statusLockedFromNonAdmin`
  covers status **mutators**, and neither method here writes `status` at all),
  and bounds the claim to these two methods. Verified the load-bearing half of
  that reason directly — `ClaimAndEnqueueConfirmation`'s `UPDATE` sets
  `confirm_sent_at, updated_at`; `AdminResendConfirmation`'s sets
  `confirm_token, confirm_expires_at, confirm_sent_at, updated_at`. Neither
  touches `status`.
  - **Criterion 1's "or the divergence is documented" branch now rests on
    something true.** It is also no longer the load-bearing half: both paths
    express the same predicate (`status = 'pending'`), so criterion 1 is
    satisfied by the "same way" branch first, and the documented-divergence
    prose is the belt-and-braces it was always described as.
- **R4 — confirmed factual.** An `UPDATE` does take a row-level lock on the
  tuple it updates; what this method avoids is a *prior, separate*
  `SELECT ... FOR UPDATE`. The replacement phrase says exactly that.
- **R5 — confirmed factual.** Guard 3 is `sub.ImportID != nil &&
  sub.ConsentBasis == nil` (`ErrResendNotForInvited`), read off `sub`, which is
  scanned from the `SELECT ... FOR UPDATE` row — same locked read guards 4 and
  5 use. "guards 3, 4 and 5" is right and correctly excludes guard 2, which is
  the guard the note is attached to.
- **R6 — the metadata row.** The `**Commit**` row now carries `c01605d`, the
  final code commit.

**The pass really is comment-only — proved with a parser, not line shapes.**
Built a throwaway Go tool (scratchpad, not the tree) that parses a file with
`go/parser` in mode `0` — comments are never attached to the AST — prints the
result with `go/printer`, and digests that. It refuses to emit a digest for an
empty rendering or for one that still contains a comment marker, so it fails
closed rather than passing vacuously. Self-tested before it was trusted, per
the standing rule that an instrument gets falsified first: it (a) reports
IDENTICAL for a file against itself, (b) reports DIFFERS for a copy with the
single-token mutation `AND status = $4` deleted from the claim's `WHERE`,
(c) reports IDENTICAL for a copy with only a comment altered, and (d) exits 2
on an empty file and on a missing file rather than reporting a pass. Against
the real revisions, `internal/subscribers/store.go` digests
`bbd5b93278631b0b` at `c01605d^` and `bbd5b93278631b0b` at `c01605d`, and
`pending.go` digests `0e6ce273d738e420` on both sides. `c01605d` touches
those two files only (+15/-5 and +1/-1 = the claimed +16/-6), and nothing but
`issues/0341.md` has changed since. Deliberately did **not** reuse the
implementation pass's line-shape checker: a line-prefix test cannot tell a
comment from a string literal that looks like one, and a parse can.

**The orchestrator's citation edit — reviewed, and the right call.** The first
review's own `## Review notes` spelled out the name of a test `#0314`'s plan
will add but that nothing defines, and `## review notes` is deliberately in
`issueCitationSectionHeaders` (`#0268` measured 2 of its 11 dangling citations
there), so `TestNoOpenIssueVerificationCitesUndefinedTestFunction` failed and
took every scoped run touching `internal/db` red with it. The commit rewrote
that one citation to the elided form the very same paragraph already uses four
lines later for the sibling name, added "(planned, not yet defined)", and
changed nothing else — one line, `+1/-1`, disclosed in its own commit message.
**Meaning unchanged**: the sentence still says `#0314`'s plan specifies that
test alongside the predicate and mutation M2, and the parenthetical states
plainly what the reader could previously only infer.

I would have made the same call, and I do not think it needed a strike-and-
correct note. The tracker's correction convention governs *assertions
invalidated by later events*; this assertion was never wrong, and what changed
was its spelling, to stop a tooling guard firing on a name it cannot
distinguish from a false citation. Weakening the guard or leaving
`internal/db` red for every other agent would both have been worse. Two things
worth saying anyway: editing another agent's signed record is a real cost even
when the edit is right, and the alternative — asking this review to make the
same edit as part of its own pass — was available and cheaper in that respect.
Note also that the edit becomes moot the moment this issue leaves
`open`/`in-progress`, since the guard only scans those; that is a reason it was
harmless, not a reason it was unnecessary at the time it was made.

**All five acceptance criteria, re-verified here.**

1. **Same expression, or documented divergence.** Both paths test
   `status = 'pending'` (`StatusPending = "pending"`), in the layer each
   method's transaction shape calls for, and both sites carry the reasoning.
   Closed on the "same way" branch, with R3's corrected prose as the second
   line of defence.
2. **No implicit invariant.** The predicate names `pending` outright rather
   than relying on `pending` happening to exclude `complained`, and
   `TestClaimAndEnqueueConfirmation_RefusesComplainedRow` pins it.
3. **No path auto-resubscribes a `complained` address — re-proved from
   scratch, not taken from the first review.** `git archive HEAD` into a
   scratchpad export (never the shared tree), own database `0341r2m`, mutation
   M2: delete `AND status = $4` and its `StatusPending` bind, with the edit
   asserted to have actually applied before the run. Result:
   `TestClaimAndEnqueueConfirmation_RefusesComplainedRow` FAILs at
   `store_test.go` line 1408 with *"claimed = true for a status=complained
   row, want false"* and **it is the only failure in the package** — still the
   sole detector of its own mutation. Export deleted afterwards; the scratch
   database was dropped by `check.sh` on exit and `pg_database` confirms none
   of mine survive. Independently of the claim path, `Confirm` still refuses
   `StatusComplained` with `ErrComplainedLocked` before any status write, which
   is the actual resubscribe gate.
4. **`FOR UPDATE` atomicity preserved.** `AdminResendConfirmation`'s mechanism
   is untouched — the parser proof shows its code is byte-identical across
   `c01605d`, and the only change to `pending.go` in the whole issue is a
   comment. `ClaimAndEnqueueConfirmation` never had a prior `SELECT`, so its
   claim is still one atomic conditional `UPDATE` with one more `AND` term.
5. **Every claim path enumerated — re-derived, not trusted.** Two independent
   sweeps agree and both find exactly three non-test sites: `KindConfirmation`
   is enqueued only by `Create`, `ClaimAndEnqueueConfirmation`, and
   `AdminResendConfirmation`, and `confirm_sent_at` is written only by those
   same three. Every other `KindConfirmation` reference is a read — the admin
   dashboard's abandoned-count, `admin_pending`'s latest-by-recipient lookup,
   and `OutboxWorker`'s render/send arms, which consult neither
   `subscribers.status` nor `suppressions` (that gap is `#0365`, deliberately
   out of scope here).

**§9 invariants intact.** `complained` still never auto-resubscribes: the
`statusLockedFromNonAdmin` guards, `Confirm`'s `ErrComplainedLocked`, and
`AdminClearComplaint` as the sole admin exception are all unchanged, and this
issue only *added* a refusal. The uniform `202` in `POST /api/subscribe` is
untouched — no handler code changed in either commit, and the handler's
`complained` branch still falls through to the same response every other branch
produces.

**Verification re-run independently.** `ISSUE=0341r2 scripts/check.sh go
./internal/db/... ./internal/handlers/... ./internal/mailing/...
./internal/outbox/... ./internal/subscribers/...` — all five packages `ok`,
`(no skips)`, `go build`, `go vet` and `gofmt -l` clean, `VERIFICATION PASSED`.
`internal/db` is green again, so the citation-guard failure the second
implementation pass reported is genuinely cleared. The named test was also run
alone and observed to PASS. `scripts/check.sh guards` passes, and all five
guard scripts were confirmed to have run this time (`check_guard_test.sh`,
`testdb_gc_guard_test.sh`, `dev_guard_test.sh`, `db_reset_guard_test.sh`,
`go_file_visit_floor_guard_test.sh`) — closing the first review's nit about an
elided transcript. `gofmt -w` on copies of both touched files is a byte-level
no-op (hashes identical before and after), which is the real evidence that the
new prose survives the doc-comment rewriter. No secrets appear anywhere in this
file's transcripts.

**The first review's one open recommendation is done.** It asked the
orchestrator to record in `#0314`'s plan that `#0341` had landed the predicate
and the test; `e102fe5` added the "Note from `#0341`" section that does exactly
that, and it correctly tells that issue's implementer not to re-add either and
not to drop the predicate. The doc comment's own note is no longer the only
place a `#0314` implementer could learn this.

**Nits, recorded rather than bounced — none of them is a false statement.**

- The R4 substitution was transcribed literally, which leaves one comment line
  noticeably longer than its neighbours. `gofmt` does not rewrap comments, so
  this is cosmetic only; re-wrapping it would be churn on a file two other
  issues are queued to rewrite.
- In `store.go`, the parallel sentence explaining why the twin needs its locked
  read still says "for its cooldown/suppression checks", omitting the
  unaccepted-invitation guard that R5 added on the `pending.go` side. It is
  introduced by "also needs", so it under-lists rather than asserting an
  exhaustive set, and it is not the numbered enumeration that made the
  `pending.go` version misleading. Left as is.
- "`#0314`'s plan recorded the same gap independently" is true in the sense
  that matters — the plan predates the implementation pass by four days and is
  a separate document — though both it and this issue came out of the same
  planning pass, which got the plan right and the issue's Description wrong.

## Relation

- Found by: `#0312`/`#0313`/`#0314`'s planning pass
- Relates to: [#0314](0314.md) — which adds the SQL-side guard
- Relates to: [#0312](0312.md) — the admin resend action
- Relates to: [#0129](0129.md) — the invite flow both paths serve

## Work log

- 2026-08-28 — Filed by the orchestrator from the planning pass (§9).
- 2026-09-01 — Implementation pass (`claude-sonnet-5`). Checked the premise
  first: `#0312`/`#0313`/`#0314` are still `open` (planning-only, commit
  `b8af685`), not implemented as the dispatch claimed — see the struck
  paragraph in `## Description` and `## Verification` above. Implemented
  against the code as it stands: added `AND status = 'pending'` to
  `ClaimAndEnqueueConfirmation`'s claim `WHERE` clause, matching
  `AdminResendConfirmation`'s existing `ErrNotPending` predicate exactly, and
  cross-referenced both methods' doc comments. Added
  `TestClaimAndEnqueueConfirmation_RefusesComplainedRow`, proved it fails
  against the pre-fix `WHERE` clause (hash-verified restore afterwards), then
  ran the full scoped suite and `scripts/check.sh guards` green with no
  skips. Left status `in-progress` for review, per this pipeline's own rule
  that only review sets `resolved`. **Note for whoever implements `#0314`:**
  that issue's plan rewrites `ClaimAndEnqueueConfirmation` to add a prior
  `SELECT … FOR UPDATE` and token-minting; fold this issue's `status =
  'pending'` predicate into that rewrite's `WHERE`/lock rather than dropping
  it — see the doc comment's own note to that effect.
- 2026-09-01 — Review pass (`claude-opus-5`). **Bounced to `open`.** The code
  change itself verifies clean and independently: the pre-fix `WHERE` clause
  really did carry no status term (read at `9cf6898^`), the three
  `KindConfirmation` producing paths re-derived from scratch match the doc
  comment's enumeration with no fourth path, the SQL predicate is equivalent
  to (and in one respect stronger than) the `FOR UPDATE` mechanism under all
  four interleavings, and
  `TestClaimAndEnqueueConfirmation_RefusesComplainedRow` was re-proved
  red-then-green against mutation M2 in a `git archive` export with its own
  database — failing alone, so nothing pre-existing covered this. Scoped
  suite, `scripts/check.sh guards`, and `gofmt` stability all green. What
  bounced it is five factual defects in the doc comments the pass added
  (false provenance for the finding, a "twin below" referent pointing at a
  different file, a citation of the package doc comment that inverts what it
  says while carrying acceptance criterion 1, "without a row lock" of an
  `UPDATE` that takes one, and an under-inclusive list of which guards need
  the locked read), plus the missing `**Commit**` row — see
  `## Review notes` for the exact substitutions.
- 2026-09-01 — **§3 model decision: the next pass stays on Sonnet.** This is
  the first bounce, and none of R1–R6 requires deciding anything — every
  remedy is a named file, a byte-exact anchor verified unique in the tree,
  and the replacement text to put there. Escalation would buy nothing.
- 2026-09-01 — Second implementation pass (`claude-sonnet-5`), transcription
  only. Applied R1–R5 exactly as specified, verifying each anchor unique in
  the tree with `grep -n` before editing. Independently re-checked R3
  against the actual `statusLockedFromNonAdmin` package doc comment
  (`store.go` lines 29–41) before applying it — confirmed it documents
  deliberate convergence on one mechanism ("the check lives in one place
  ... so a future mutator can't add a new unguarded status write"), so the
  review's finding that the removed sentence inverted this holds. Did not
  touch the predicate, the test, or the transaction shape (out of scope,
  per dispatch). Proved the pass comment-only with a small external Python
  checker (kept in the scratchpad): self-tested falsifiable (rejects a
  synthetic code-line mutation) and fail-closed-on-empty before trusting
  it, then confirmed all 22 changed lines across both files are blank or
  `//` comments. Committed the code as `c01605d` (2 files, +16/-6, matching
  the structural proof), filled the `**Commit**` row (R6), and left
  **Status** `in-progress` for review (§9 — only the reviewer sets
  `resolved`). Scoped suite (`ISSUE=0341b scripts/check.sh go
  ./internal/db/... ./internal/handlers/... ./internal/mailing/...
  ./internal/outbox/... ./internal/subscribers/...`) reproduced one
  pre-existing, out-of-scope failure: `internal/db`'s citation guard
  (`#0265`/`#0268`) rejecting this issue's own `## Review notes` mention of
  a test `#0314`'s plan will add but that does not exist yet — introduced
  by the prior review commit (`ed96e20`), not by this pass, and not fixed
  here since editing the reviewer's own record is outside R1–R5's scope.
  The other four packages (`handlers`, `mailing`, `outbox`, `subscribers`)
  all `ok`, no skips. `scripts/check.sh guards` green. `gofmt -l` clean on
  both touched files. Reporting the pre-existing citation-guard failure to
  the orchestrator rather than filing a new issue (§9 restricts subagents
  from filing).
- 2026-09-01 — Second review pass (`claude-opus-5`). **Resolved.** Verified
  R1-R6 landed as transcription and that each corrected sentence is now true
  against the code: read the `statusLockedFromNonAdmin` package doc comment
  independently and confirmed it documents convergence (so R3's premise held)
  and that the replacement does not overstate in the other direction; confirmed
  R4 (an `UPDATE` takes a row lock; what is avoided is a prior
  `SELECT ... FOR UPDATE`) and R5 (guard 3, the unaccepted-invitation test,
  reads off the same locked row); checked the `#0314` quotation against
  `issues/0314.md` character by character. Proved the pass comment-only with a
  `go/parser` digest that discards comments at parse time — self-tested
  falsifiable and fail-closed on empty/missing input first, and deliberately
  not the line-shape checker the implementation pass used. Re-derived criterion
  5's three claim paths from two directions, and re-proved criterion 3 from
  scratch with mutation M2 in a `git archive` export against its own database:
  the new test fails alone, so it remains the sole detector. Reviewed the
  orchestrator's `c562d6f` citation rewrite and accepted it — meaning
  unchanged, one line, and the alternative was leaving `internal/db` red for
  every agent. Scoped suite (`ISSUE=0341r2`) green across all five packages
  with no skips, `scripts/check.sh guards` green with all five guard scripts
  confirmed run, `gofmt -w` a byte-level no-op on both touched files.
  **§3 model decision: no escalation needed and none taken** — the second pass
  was the transcription the first bounce specified, it landed complete and
  correct, and there is no third pass to size.
