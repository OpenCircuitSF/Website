# 0436 — The documented certbot command would replace a working wildcard cert with a narrower one

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | docs / deploy |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |
| **Closed** | 2026-09-05 |
| **Commit** | `270fed9`, `d2c445f` |

> 🟢 **Resolved 2026-09-05.** All three bounce remedies were transcribed
> faithfully and, re-derived independently for this pass, all three are true
> rather than merely different. `docs/deployment.md` now carries **one** account
> of TLS: an independent whole-file byte sweep finds no surviving statement that
> describes the HTTP-01/`--apache` world as live. `#0447` is pointed at, not
> absorbed — no deploy hook was added and nothing on the box was changed.

## Description

`docs/deployment.md` contradicts itself about TLS, and the wrong half is the
runnable one.

**Line 106 is right.** It records the cert as a single ECDSA **wildcard**
covering `opencircuitsf.com` and `*.opencircuitsf.com`, issued through the
**`dns-route53`** authenticator, renewed by `certbot-renew.timer` — and notes
that renewal therefore never reads the vhosts or `/.well-known/acme-challenge`,
so no Apache change in this project can break it.

**Line 889 is a command that contradicts it:**

	sudo certbot --apache -d opencircuitsf.com -d www.opencircuitsf.com

`--apache` is HTTP-01. **HTTP-01 cannot issue a wildcard** — Let's Encrypt
requires DNS-01 for `*` names. And the `-d` list names only two hosts.

Verified against the live endpoint: the served certificate's SAN is
`DNS:*.opencircuitsf.com, DNS:opencircuitsf.com`.

### What running it would do

Replace a wildcard covering every subdomain with a certificate for two names,
and switch the renewal path from DNS-01 to HTTP-01.

**`go.opencircuitsf.com` would lose coverage** — that is the ShortLinks deploy
(`CLAUDE.md` §1), a *different* project on the same box. So the blast radius
crosses a project boundary.

It would also make renewal depend on Apache and `/.well-known/acme-challenge`
again — and `CLAUDE.md` §7 records that `/.well-known/` is a deliberate
`ProxyPass … !` carve-out serving `atproto-did` from disk. Coupling renewal back
to that path is exactly the fragility line 106 says the current setup avoids.

**Found by `#0431`'s sweep**, which noticed the documented command could not
have produced the live certificate.

## A second discrepancy in the same DNS material

`docs/deployment.md`'s DNS table names the DKIM CNAME under the **apex**
(`<sel1..3>._domainkey.opencircuitsf.com`), while `docs/email-setup.md` — and
`docs/deployment.md`'s own prose 21 lines later — put it under
**`mailing.opencircuitsf.com`**.

The `mailing.` form is right: `CLAUDE.md` §10 records the verified SES identity
as `mailing.opencircuitsf.com`, and §9 forbids touching the apex, which carries
real Google Workspace mail. A DKIM record published at the apex would be both
wrong and adjacent to a restricted area.

Confirmed: both bare `_domainkey.*` names resolve only through the zone's
wildcard, so neither is a real record — the live ones carry selector prefixes.

## The text moved under this issue, 2026-09-05

`#0431`'s sweep of the same file rewrote the surrounding TLS paragraph while
deliberately leaving the `certbot --apache` command itself untouched (correct —
it is this issue's). But the new paragraph now describes that command as

> a syntactically valid fallback … if that were ever wanted instead

**Read as it now stands, that presents the wrong command as an endorsed
option** — and it omits the blast radius entirely: dropping
`*.opencircuitsf.com` takes `go.opencircuitsf.com` with it, a **different
project** on the same box, and re-couples renewal to
`/.well-known/acme-challenge`, which `CLAUDE.md` §7 records as a deliberate
carve-out serving `atproto-did`.

So this issue's job grew slightly: not only replacing the command, but
correcting the framing that now surrounds it. **Re-read that paragraph as it
currently stands before writing anything** — `#0431` has had two passes over
this file and a third is running.

Reported by `#0431`'s review, which flagged it precisely because the text had
moved under an issue that had not started.

## Acceptance criteria

1. The certbot command matches how the cert is actually issued — DNS-01 via
   `dns-route53`, covering the wildcard. **Re-derive the live cert's SAN,
   issuer and authenticator yourself** rather than copying this issue.
2. The contradiction between line 106 and the command is gone: **one account of
   TLS in that file, not two.**
3. The DKIM CNAME's parent is `mailing.opencircuitsf.com` everywhere in the
   file, matching `docs/email-setup.md` and its own later prose.
4. **State why DNS-01 is required**, not just what to run — the reason
   (wildcard) is what stops someone "simplifying" it back to `--apache`.
5. Follow `#0072`'s dated-note convention; leave genuine decision-log history
   intact per `#0271`'s `## Scope`. Say which occurrences you judged to be
   which. Note `#0431` has just swept this file for a different shape — read the
   current state.
6. **Change nothing on the box.** Do not run certbot, do not touch DNS. This is
   documentation. Read-only verification only; say what you ran.
7. **No mechanism** (`#0330`/`#0347`/`#0356`/`#0381`).
8. Run `./internal/db/...` and `./internal/handlers/...`. Per §5 name the
   specific tests that ran — real function names, not a `-run` prefix. No
   Go-file-plus-line-number citation (`#0352`), scanned with `python3` bytes.

## Review notes

> 🔴 **Bounced 2026-09-05.** The command itself is right and independently
> re-derived; the DKIM row is right and now measured rather than inferred. Two
> things block: the file still carries a *second* account of TLS 600 lines
> above §9 (criterion 2), and the rewritten reload paragraph asserts something
> the implementer's own box reading contradicts.

Code commit `270fed9` is left in place — every remedy below is an edit on top
of it, not a revert.

### What passes, verified independently

**The corrected command is exactly right.** Re-derived read-only via
`ssh ec2`, not copied from the issue or the work log:

- `sudo certbot certificates` → one cert for this domain,
  `Certificate Name: opencircuitsf.com`, `Key Type: ECDSA`,
  `Domains: opencircuitsf.com *.opencircuitsf.com`,
  `Expiry Date: 2026-11-16 20:12:34+00:00`. The `-d` list in the new command
  matches that SAN set exactly, in the same order, so the run resolves to the
  existing lineage rather than creating a second one.
- `/etc/letsencrypt/renewal/opencircuitsf.com.conf` → `authenticator =
  dns-route53`, `key_type = ecdsa`, and **no `installer =` line at all**. So
  `certonly` is the correct subcommand, not a stylistic choice — `certbot
  plugins` confirms `dns-route53` advertises `Interfaces: Authenticator,
  Plugin` with no Installer, so there is nothing for a non-`certonly`
  invocation to install with.
- Shell quoting survives as written: `'*.opencircuitsf.com'` in single quotes
  is protected from glob expansion under both the bash the runbook targets and
  the zsh an agent would paste it into. (Unquoted, zsh would abort the whole
  command with `no matches found`, which at least fails loudly — but quoted is
  correct and it is quoted.)

**The DKIM row is correct, and stronger than its citation.** The pass justified
it from `docs/email-setup.md` plus `CLAUDE.md` §9. That is on the right side of
`#0431`'s distinction — it attributes rather than asserting a measurement it
did not take — but this review took the measurement. Read-only against the
hosted zone: all three DKIM CNAMEs exist under
`<selector>._domainkey.mailing.opencircuitsf.com`, and the only `_domainkey`
name at the apex is `google._domainkey.opencircuitsf.com`, a **TXT** record
belonging to Google Workspace, exactly as `CLAUDE.md` §10 item 5 records.
There is no DKIM CNAME at the apex and never was. Criterion 3 is met, and the
change was squarely in scope — criterion 3 names it explicitly.

**Both "do not fix this back" claims are true as written.** A wildcard
identifier can only be validated by DNS-01, which is an ACME/Let's Encrypt
policy constraint rather than a certbot limitation; and the `--apache` plugin
is an HTTP-01 authenticator, so no flag combination makes `--apache` produce
this SAN set. `certbot plugins` on the box shows `apache` as
`Authenticator, Installer, Plugin` — HTTP-01 — confirming it.

**Verification is genuine.** Re-run independently; see the bottom of this
section.

### Blocking 1 — the file still contains two accounts of TLS (criterion 2)

Criterion 2 asks for **one account of TLS in that file, not two**. §9 is now
correct, but the prerequisites section roughly 600 lines earlier still
describes the HTTP-01 world, and an operator working the runbook top to bottom
hits it *before* reaching §9. As it stands the runbook installs the wrong
plugin and then runs a command that cannot work:

	sudo dnf install -y openssl certbot python3-certbot-apache

`--dns-route53` is provided by `python3-certbot-dns-route53`, which this line
does not install. Measured on the box, both are in fact present
(`python3-certbot-2.6.0`, `python3-certbot-apache-2.6.0`,
`python3-certbot-dns-route53-2.6.0`, `certbot-2.6.0`), so the box is fine —
the **document** is what is wrong. A fresh box built from this runbook would
fail §9's command with an unable-to-find-authenticator error.

**Remedy, verbatim.** In the `**`openssl` and `certbot`**` prerequisite,
replace the fenced command with:

	sudo dnf install -y openssl certbot python3-certbot-dns-route53

and add, immediately after that fence, a dated note in the same style the DNS
table rows in this file already use:

	**Corrected 2026-09-05, `#0436`:** this step previously installed
	`python3-certbot-apache`. That is the plugin §9 now says must never be
	used here — it is HTTP-01 and cannot issue this domain's wildcard — and
	it does not provide `--dns-route53`, so the runbook as it stood
	installed one plugin and then invoked another. Measured on the box,
	both packages are present (`python3-certbot-apache-2.6.0` and
	`python3-certbot-dns-route53-2.6.0`); only the latter is required by
	anything this document tells you to run.

Do **not** strike the old bytes inside the fence — strikethrough does not
render in a code block. The prose note above is the correct form of `#0072`'s
convention for a fenced command, and it is what `#0421` did for the DNS table.

### Blocking 2 — the same section still explains the DNS check as HTTP-01

Two bullets below that, still untouched by this pass:

	- **DNS already resolving** — confirm before requesting a TLS certificate
	  (Certbot's HTTP-01 challenge needs the hostname to already resolve to this
	  box):

The parenthetical is the old HTTP-01 account restated as the *reason* for the
check. It is false for the path this document now documents: DNS-01 never
resolves the certificate's hostnames or connects to the box at all. What it
actually needs is Route 53 write access to publish a TXT record under
`_acme-challenge.opencircuitsf.com` — which §9 already says.

**Remedy, verbatim.** Keep both `dig` commands (they remain a worthwhile check
for the proxy and vhost work that follows) and replace the bullet's lead-in
with:

	- **DNS already resolving** — not a certificate prerequisite, but confirm
	  it before the Apache and proxy steps below. **Corrected 2026-09-05,
	  `#0436`:** this bullet previously justified itself as "Certbot's
	  HTTP-01 challenge needs the hostname to already resolve to this box",
	  which describes a path this box does not use. DNS-01 via `dns-route53`
	  (§9) never connects to the box or resolves these names; what it needs
	  is permission to write a TXT record under
	  `_acme-challenge.opencircuitsf.com` in the hosted zone.

### Blocking 3 — the rewritten reload paragraph asserts what the box contradicts

This pass rewrote the reload lead-in from "Reload once more if certbot didn't
already" to:

	Reload Apache only if a manual `certonly` run (not the timer) obtained a new
	certificate and Apache hasn't picked it up:

and the paragraph above it to open "Certbot's own renewal timer needs no manual
step here."

Read together, those tell an operator that the timer path requires no reload.
**Nothing on this box reloads Apache after a renewal — the timer's included.**
Measured read-only, four ways:

- `/etc/letsencrypt/renewal-hooks/deploy/` — empty.
- `/etc/letsencrypt/renewal-hooks/post/` — empty.
- `/etc/sysconfig/certbot` — `PRE_HOOK=""`, `POST_HOOK=""`, `DEPLOY_HOOK=""`;
  every reload example in it is commented out.
- `certbot-renew.service` — `ExecStart=/usr/bin/certbot renew --quiet`, and
  nothing else. No `ExecStartPost`, no reload.

The renewal config has no installer either, so certbot has no path to touch
Apache on its own. `mod_ssl` reads the certificate at configuration time, so
after an unattended renewal Apache keeps serving the **previous** certificate
until something reloads it.

This is the `#0431` distinction firing on this pass's own text: the work log
records exactly this finding — "no `/etc/letsencrypt/renewal-hooks/deploy/`
script exists on the box" — and then the document was written to say the
opposite for the automatic path. A statement that goes past its evidence to
assert a fact is the defect; here the evidence was in hand and pointed the
other way.

**Remedy, verbatim.** Replace the two spans above. For the timer paragraph's
opening sentence, use:

	The renewal timer itself needs no manual step — it is already installed
	and running.

For the reload lead-in, use:

	**Reload Apache after any renewal, including the timer's.** Re-verified
	read-only for this pass: `/etc/letsencrypt/renewal-hooks/deploy/` and
	`/etc/letsencrypt/renewal-hooks/post/` are both empty,
	`/etc/sysconfig/certbot` sets `PRE_HOOK`, `POST_HOOK` and `DEPLOY_HOOK`
	to the empty string, and `certbot-renew.service` is a bare
	`certbot renew --quiet`. The renewal config configures no installer. So
	nothing reloads Apache when the certificate is replaced, and `mod_ssl`
	goes on serving the previous one until it is. After a manual `certonly`,
	run the reload yourself. For the unattended path there is currently no
	mechanism at all — a gap in the box's configuration, not something this
	document can fix:

Leave the existing fenced `systemctl reload httpd` / `curl … /health` block
where it is; it is the right pair.

**Do not change anything on the box** — criterion 6 still holds, and this
remedy is documentation only. **Do not file an issue for the missing deploy
hook** (`CLAUDE.md` §9); report it and let the orchestrator allocate the
number. It is genuinely separate from `#0430` and `#0435`, neither of which
this pass absorbed — correctly.

### Scope audit of the 119 lines

81 insertions, 38 deletions. Hunk by hunk:

| Hunk | What | Asked for? |
|---|---|---|
| §9 opening paragraph | `#0431`'s note restruck per `#0072`; its factual half kept | Yes — criterion 5 |
| §9 new `#0436` correction paragraph | the blast radius `#0431` omitted: `go.opencircuitsf.com`, the `/.well-known/` carve-out | Yes — the issue's own "text moved under this issue" note |
| the command fence | `--apache` → `certonly --dns-route53` | Yes — criterion 1 |
| "Why DNS-01, not `--apache`" | the protocol reason | Yes — criterion 4 |
| "Do not 'simplify' this back" | reversal guard | Yes — criterion 4's stated purpose |
| timer paragraph | rewritten | In scope, but see Blocking 3 |
| reload paragraph | rewritten | In scope, but see Blocking 3 |
| closing TLS paragraph | stale-marker dropped once the staleness was fixed | Yes — criterion 2 |
| DNS table DKIM row | apex → `mailing.` parent | Yes — criterion 3 |
| DKIM prose below the table | unresolved-discrepancy note struck and resolved | Yes — criteria 3 and 5 |

No hunk is unasked-for. The bulk of the line count is reflow, not new claims —
this is not scope creep, it is an incomplete sweep.

### Not blocking, reported rather than absorbed

The DNS table's `www.opencircuitsf.com` row says **A → `44.222.209.183`**. The
hosted zone holds a **CNAME** to `ec2.smallsharptools.com` (which then resolves
to that address) — the same shape the `go.` row already flags as "a CNAME in
practice, not the A record PRD §10.2 planned". Pre-existing, untouched by
`270fed9`, and out of this issue's scope. Worth a separate issue; not filed
here (`CLAUDE.md` §9).

### Verification

Machine load recorded, not reasoned from: `uptime` reported load averages
6.37 / 9.07 / 8.53 — the five- and fifteen-minute figures sit above `CLAUDE.md`
§5's ~8 threshold because other agents are testing concurrently. Nothing was
widened and no timing result was treated as a defect.

	ISSUE=0436r scripts/check.sh go ./internal/db/... ./internal/handlers/...

`VERIFICATION PASSED`; `gofmt -l` clean; skip audit reported `(no skips)`. Both
DB-backed packages reported real elapsed time — `internal/db` `ok 0.690s`,
`internal/handlers` `ok 13.283s` — not `0.00s`. All 27 migrations applied to a
private database, dropped on exit.

Guards that actually exercise this change, by name. In `internal/db`:
`TestPRDSectionIndexParity`, `TestPRDSectionIndexTotalMatchesFile`,
`TestDocTableRowLines`, `TestDatabaseDocMigrationParity`,
`TestResolvedOrClosedIssueCarriesClosedRow`,
`TestNoOpenIssueEvidenceCitesGoFileByLineNumber`,
`TestNoOpenIssueVerificationCitesUndefinedTestFunction`,
`TestIssueClosedRowGuardExemptsWontfixOpenAndInProgress`,
`TestClaudeMDRepoWideGuardPackageSetParity`. In `internal/handlers`:
`TestNoCommentCitesUnresolvedPathOrSection`,
`TestNoCommentCitesGoFileByLineNumber`,
`TestNoCommentCitesUndefinedTestFunction`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`TestNoAdminFacingStringCitesInternalDocs`. The run was repeated after this
section was written, since the open-issue prose guards only scan `open` and
`in-progress` files.

Box reads performed for this review, all read-only, nothing changed:
`sudo certbot certificates`, `sudo cat
/etc/letsencrypt/renewal/opencircuitsf.com.conf`, `sudo ls -la
/etc/letsencrypt/renewal-hooks/{deploy,post}/`, `sudo cat
/etc/sysconfig/certbot`, `systemctl cat certbot-renew.service
certbot-renew.timer`, `systemctl list-timers certbot-renew.timer`,
`sudo certbot plugins`, `rpm -qa | grep certbot`,
`aws route53 list-resource-record-sets` (names and types only), and `dig`.
One read was **refused**, recorded rather than worked around: `aws sesv2
get-email-identity` returns `AccessDeniedException` — the box's default CLI
identity is `certbot-dns-updater`, which has no `ses:GetEmailIdentity`
permission, as `docs/deployment.md` already notes. The Route 53 listing
supplied the same fact directly, so nothing rests on the refused call. No
selector token values or other secrets are reproduced here (`CLAUDE.md` §8b).

### Second review, 2026-09-05 — pass

`d2c445f` transcribes all three remedies. Checked each for *truth*, not merely
for difference from what it replaced.

**Remedy 1 — the prerequisite package. True.** Re-derived read-only on the box
rather than taken from the predecessor's notes: `rpm -qa | grep -i certbot`
returns `certbot-2.6.0`, `python3-certbot-2.6.0`,
`python3-certbot-apache-2.6.0` and `python3-certbot-dns-route53-2.6.0`, so the
dated note's parenthetical ("both packages are present") is accurate to the
version string. `python3-certbot-dns-route53` is the package that supplies the
`--dns-route53` authenticator §9's command invokes, and it is now the one the
runbook installs. The old bytes were left inside the fence and corrected by a
dated prose note above it, which is the right form of `#0072`'s convention for
a fenced command.

**Remedy 2 — the `_acme-challenge` claim. True, and it does not overclaim.**
This is the one genuinely *new* factual assertion in the commit — the remedy
handed it over verbatim, but a verbatim remedy is not evidence, and `#0431`'s
standing distinction (a statement that goes past its citation to assert a
production fact is a defect) applies to it just as it would to any other. Three
checks, all clear:

- **The name is right.** ACME's DNS-01 challenge validates a TXT record at
  `_acme-challenge.<identifier>`, and for a wildcard identifier the `*` label is
  stripped first — so both `opencircuitsf.com` and `*.opencircuitsf.com`
  validate at the single name `_acme-challenge.opencircuitsf.com`. The document
  names exactly that, and §9's own "Why DNS-01" paragraph (from the first pass,
  already accepted) names the same record, so the two halves of the file now
  agree on the mechanism as well as on the command.
- **The box corroborates it.** The retained `/var/log/letsencrypt/` logs do not
  record a challenge record name for this domain, but they do for two other
  domains renewed by the same `dns-route53` plugin on the same box, and both
  appear in exactly the `_acme-challenge.<domain>` form. That is the plugin's
  observed behaviour here, not an inference from documentation.
- **It stops short of credentials and IAM.** The sentence says only "permission
  to write a TXT record … in the hosted zone" — it names no role, no policy, no
  action, and asserts nothing about whether the permission is currently held.
  The one place the file does claim the box holds that permission is §9's
  pre-existing "this box already has IAM permissions for" line, which is
  `270fed9`'s and was accepted last round on the strength of the cert actually
  renewing through `dns-route53`. Nothing new was claimed here.

The bullet's new lead-in ("not a certificate prerequisite, but confirm it
before the Apache and proxy steps below") is also correct: DNS-01 never
resolves the certificate's hostnames and never connects to the box, so
resolution genuinely is not a prerequisite for issuance — while the two `dig`
commands remain worth running before the proxy and vhost work, and both were
kept unchanged.

**Remedy 3 — the reload paragraph. True.** Re-derived read-only for this pass,
all four measurements, independently of both prior passes:
`/etc/letsencrypt/renewal-hooks/deploy/` and
`/etc/letsencrypt/renewal-hooks/post/` each contain zero entries;
`/etc/sysconfig/certbot` has `PRE_HOOK`, `POST_HOOK` and `DEPLOY_HOOK` all set
to the empty string; `certbot-renew.service` is a bare
`ExecStart=/usr/bin/certbot renew --quiet` with no `ExecStartPost`; and
`/etc/letsencrypt/renewal/opencircuitsf.com.conf` has `authenticator =
dns-route53` and no `installer` line at all. The document's four-way statement
matches all four. The timer paragraph's replacement opening sentence — the
timer needs no manual step because it is installed and running — is now
scoped to the timer's own operation and no longer implies the reload question
is settled by it, which was the contradiction that bounced this issue.

**Independent whole-file sweep — criterion 2.** The bounce was for an
incomplete sweep, so the predecessor pass's report of its own sweep is not
evidence. Re-derived with `python3` over all 2,346 lines (never BSD `grep -P`,
`CLAUDE.md` §8), using a deliberately wider pattern set than the pass's:
`HTTP-01`, `HTTP01`, `--apache`, `certbot-apache`, `webroot`,
`acme-challenge`, `.well-known`, `DNS-01`, `dns-route53`, `challenge`,
`certonly`, `certbot`, `reload`, `TLS`, `SSL`, `cert`, `wildcard`,
`*.opencircuitsf`, `renew`, `letsencrypt`, `mod_ssl`, `SAN`, `authenticator`,
`plugin`, `443`, `https` — case-insensitive, 159 matching lines read in full.

Every surviving occurrence of the HTTP-01 world is now one of three things: a
struck-through historical claim, a dated correction explaining why the old text
was wrong, or a statement of why `--apache` must not be used. There is no
remaining line that instructs or justifies the HTTP-01 path as live. The two
places the predecessor pass named (the production-facts `certbot renewal
schedule` row and the `#0426` cutover bullet "certbot needs no change …
renewal runs off `dns-route53`") do agree with §9, and the sweep found no third
place it missed. The brief top-of-file summary row records only issuer and
expiry — incomplete, but not a competing account, and not this issue's.

**`#0447` is pointed at, not absorbed.** The sentence attributes the gap to
the box — "a gap in the box's configuration tracked separately as `#0447`, not
something this document can fix" — which names the tracker item without
claiming the document closed it. `d2c445f` touches two files and neither is a
hook script; no renewal hook, `ExecStartPost`, or deploy-hook instruction was
added anywhere, and nothing on the box was changed, so criterion 6 holds. The
existing `systemctl reload httpd` / `curl … /health` fence is left where it
was.

**`#0448` deliberately untouched.** The DNS table's
`www.opencircuitsf.com | A | 44.222.209.183` row and the production-facts line
that says the same are byte-identical to their pre-`d2c445f` state; `d2c445f`'s
diff does not reach either. Correctly left for its own issue and not re-raised
here.

**No mechanism added** (`#0330`/`#0347`/`#0356`/`#0381`) — the commit is
documentation plus this tracker file.

### Verification, second review

Machine load recorded, not reasoned from: `uptime` reported load averages
6.38 / 13.23 / 12.99 at the start of the pass and 3.25 / 10.42 / 11.93 at the
verification run. The five- and fifteen-minute figures sit above `CLAUDE.md`
§5's ~8 threshold because other agents are testing concurrently. Nothing was
widened, and no timing result was treated as a defect.

    ISSUE=0436d scripts/check.sh go ./internal/db/... ./internal/handlers/...

`gofmt -l` clean; skip audit `(no skips)`; no leftover processes. Both packages
reported real elapsed time, not `ok … 0.00s`. All 27 migrations applied to a
private database, dropped on exit.

Guards exercised by this change, by name. In `internal/db`:
`TestNoOpenIssueEvidenceCitesGoFileByLineNumber`,
`TestNoOpenIssueVerificationCitesUndefinedTestFunction`,
`TestResolvedOrClosedIssueCarriesClosedRow`,
`TestIssueClosedRowGuardExemptsWontfixOpenAndInProgress`,
`TestPRDSectionIndexParity`, `TestPRDSectionIndexTotalMatchesFile`,
`TestDocTableRowLines`, `TestDatabaseDocMigrationParity`,
`TestClaudeMDRepoWideGuardPackageSetParity`. In `internal/handlers`:
`TestNoCommentCitesUnresolvedPathOrSection`,
`TestNoCommentCitesGoFileByLineNumber`,
`TestNoCommentCitesUndefinedTestFunction`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`TestNoAdminFacingStringCitesInternalDocs`. The run was repeated after this
section was written, since the open-issue prose guards scan only `open` and
`in-progress` files.

The repeat run reported `VERIFICATION PASSED` outright — `internal/db`
`ok 0.583s`, `internal/handlers` `ok 6.496s`, zero skips, `gofmt -l` clean, no
leftover processes — the other agent having cleaned up its own file in the
interval.

**Every failure seen names a file outside this issue's scope.** On the
pre-notes run `internal/handlers` passed clean (`ok 6.183s`) and `internal/db`
failed on `TestNoOpenIssueEvidenceCitesGoFileByLineNumber` alone, naming two
lines in `issues/0442.md` — a file another agent has dirty in the working tree
(`git status --porcelain` showed `M issues/0442.md` on every run). Neither
`docs/deployment.md` nor `issues/0436.md` is named by any failure, and the
guard reports all violations it finds rather than the first, so its silence on
those two files is a positive result. `issues/0411.md`, which failed for the
previous pass, is now committed and clean and no longer appears. Both files
were left completely alone.

Box reads performed for this review, all read-only, nothing changed:
`rpm -qa | grep certbot`, `sudo ls -A /etc/letsencrypt/renewal-hooks/deploy/`
and `.../post/` (counted, not listed), `sudo grep -E
'^(PRE|POST|DEPLOY)_HOOK' /etc/sysconfig/certbot`, `systemctl cat
certbot-renew.service`, `sudo grep -E '^(authenticator|installer)'
/etc/letsencrypt/renewal/opencircuitsf.com.conf`, and a grep of
`/var/log/letsencrypt/*.log` for challenge record names. No secrets, tokens or
selector values are reproduced here (`CLAUDE.md` §8b).


## Relation

- Found by: [#0431](0431.md)'s sweep of the same file
- Relates to: [#0427](0427.md) — the DMARC record name, the same DNS tables
- Relates to: [#0426](0426.md), [#0414](0414.md), [#0418](0418.md),
  [#0421](0421.md) — the same document, corrected for other facts
- Relates to: `CLAUDE.md` §7 (the `/.well-known/` carve-out and the wildcard
  cert) and §9 (the apex is restricted)

## Work log

- 2026-09-05 — Filed by the orchestrator from `#0431`'s implementation pass (§9).
  The live SAN, both documented certbot forms, and both DKIM parent names were
  confirmed directly before filing.

- 2026-09-05 — **Orchestrator: recorded that the surrounding text changed.**
  `#0431` rewrote the TLS paragraph around the command without touching the
  command, which was the right scope call, but the new framing presents
  `--apache` as an available fallback and omits the cross-project blast radius.
  Correcting that framing is now part of this issue.

- 2026-09-05 — **Implementation pass (Sonnet).** Re-derived every fact
  read-only via `ssh ec2` rather than trusting the filing or prior passes:
  `sudo certbot certificates` lists exactly one cert for this domain
  (`Certificate Name: opencircuitsf.com`, `Domains: opencircuitsf.com
  *.opencircuitsf.com`, ECDSA, expiry 2026-11-16); its renewal config
  (`/etc/letsencrypt/renewal/opencircuitsf.com.conf`) reads `authenticator =
  dns-route53`, `key_type = ecdsa`, no installer configured; and
  `systemctl list-timers certbot-renew.timer` shows it firing on schedule
  (last run 2026-09-05 00:00 UTC, next 12:00 UTC). Changed nothing on the box.

  In `docs/deployment.md` §9 (TLS): replaced the `sudo certbot --apache -d
  opencircuitsf.com -d www.opencircuitsf.com` command with
  `sudo certbot certonly --dns-route53 -d opencircuitsf.com -d
  '*.opencircuitsf.com'`, matching the live cert's domain list and
  authenticator exactly. Corrected the framing `#0431` left around it — the
  old text presented `--apache` as "a syntactically valid fallback … if that
  were ever wanted instead"; the new text explains why running it would
  actively destroy the wildcard (dropping coverage for `go.opencircuitsf.com`,
  a different project — ShortLinks — on the same box) and re-couple renewal to
  `/.well-known/acme-challenge` and Apache vhost state, colliding with the
  `/.well-known/` `ProxyPass … !` carve-out (`CLAUDE.md` §7). Added a "Why
  DNS-01, not `--apache`" paragraph (wildcard SAN requires DNS-01; HTTP-01
  cannot issue one under any flag set) and a "Do not 'simplify' this back to
  `--apache`" paragraph, mirroring §7's apex/`www` "settled — do not fix this
  back" note so the correction doesn't invite reversal. Old wrong prose is
  struck through and dated per `#0072`'s convention rather than deleted, per
  `#0271`'s scope.

  Also fixed the DKIM CNAME parent in the DNS table
  (`<sel1..3>._domainkey.opencircuitsf.com` → `<sel1..3>._domainkey.
  mailing.opencircuitsf.com`), matching `docs/email-setup.md`'s own row and
  `CLAUDE.md` §9's apex restriction, and updated the paragraph below the table
  that had previously flagged this as unresolved ("worth a closer look, not
  fixed by this pass") to record the resolution, strikethrough-and-correction
  style, rather than deleting the discrepancy note.

  No mechanism added — this is a documentation-only fix (`#0330`/`#0347`/
  `#0356`/`#0381`).

  Verification: `uptime` showed load average ~13-15 (another agent active;
  not a defect signal per `CLAUDE.md` §5). `ISSUE=0436 scripts/check.sh go
  ./internal/db/... ./internal/handlers/...` — zero skips, both packages
  reported real elapsed time: `internal/db` `ok 0.564s` (includes
  `TestPRDSectionIndexParity`, `TestPRDSectionIndexTotalMatchesFile`),
  `internal/handlers` `ok 6.101s` (includes the repo-wide citation-guard
  family — `TestNoCommentCitesUnresolvedPathOrSection`,
  `TestNoCommentCitesGoFileByLineNumber`,
  `TestNoCommentCitesUndefinedTestFunction`,
  `TestNoDocCommentNamesADifferentDeclarationInSameFile` — plus every other
  test in the package). `git status --porcelain` before staging showed five
  other files dirty from other agents (`cmd/opencircuit/main.go`,
  `internal/audit/actions.go`, `internal/handlers/admin_campaigns.go`,
  `internal/mailing/campaigns.go`, `internal/mailing/campaigns_test.go`) —
  none touched; staged only `docs/deployment.md`.

  Not absorbed: `#0430` (deferred-S3 backup rationale) and `#0435` (the
  renewal timer was never installed) are separate open issues in this same
  file's neighbourhood and were left alone. Worth flagging for a follow-up
  issue, not fixed here (out of this issue's scope): no
  `/etc/letsencrypt/renewal-hooks/deploy/` script exists on the box, so a
  manual `certonly` renewal outside the timer would need the documented
  `systemctl reload httpd` step by hand — confirmed read-only, not changed.

  Status set to `in-progress`; leaving `resolved` to the reviewer per
  `issues/Issues.md`.

- 2026-09-05 — **Review pass (Opus): bounced.** Independently re-derived the
  live cert, its renewal config, the certbot plugin set and the DKIM records
  read-only via `ssh ec2`; the corrected command and the DKIM row both hold up,
  and the DKIM parent is now measured against the hosted zone rather than
  inferred from `docs/email-setup.md`. Three blocking items in `## Review
  notes`, each with the replacement text written out: the prerequisites section
  still installs `python3-certbot-apache` instead of the plugin §9's command
  needs; the DNS-resolution bullet still explains itself as an HTTP-01
  requirement; and the rewritten reload paragraph tells the operator the timer
  path needs no reload, which the box contradicts (both renewal-hook
  directories empty, all three hooks in `/etc/sysconfig/certbot` empty,
  `certbot-renew.service` a bare `certbot renew --quiet`, no installer in the
  renewal config). **First bounce, and the remedies are transcription rather
  than design — staying on Sonnet, no escalation** (`CLAUDE.md` §3). Two things
  reported for the orchestrator to file rather than absorbed (`CLAUDE.md` §9):
  the missing certbot deploy hook, and the DNS table's `www` row calling a
  CNAME an A record. Changed nothing on the box.

- 2026-09-05 — **Second implementation pass (Sonnet).** Transcribed all three
  remedies verbatim from `## Review notes`, no redesign:

  1. Prerequisites fence (`docs/deployment.md`, "`openssl` and `certbot`"
     bullet): `sudo dnf install -y openssl certbot python3-certbot-apache` →
     `sudo dnf install -y openssl certbot python3-certbot-dns-route53`, plus
     the dated `#0436` note explaining why (§9 forbids `--apache`, it doesn't
     provide `--dns-route53`, both packages are in fact present on the box).
  2. The "DNS already resolving" bullet's lead-in: replaced the HTTP-01
     justification with "not a certificate prerequisite, but confirm it
     before the Apache and proxy steps below," plus a dated `#0436` note
     naming the real DNS-01 requirement (Route 53 write access for a TXT
     record under `_acme-challenge.opencircuitsf.com`). Both `dig` commands
     kept, unchanged.
  3. §9's timer paragraph opening sentence: "Certbot's own renewal timer
     needs no manual step here" → "The renewal timer itself needs no manual
     step — it is already installed and running." §9's reload lead-in:
     "Reload Apache only if a manual `certonly` run (not the timer) obtained
     a new certificate and Apache hasn't picked it up" → "**Reload Apache
     after any renewal, including the timer's**," with the reviewer's
     four-way box measurement (both renewal-hook directories empty, all
     three `/etc/sysconfig/certbot` hooks blank, `certbot-renew.service` a
     bare `certbot renew --quiet`, no installer configured) restated as the
     reason, and a pointer to `#0447` (now filed) for the missing deploy
     hook itself — not absorbed, not added.

  Swept the whole file for other TLS/certbot/reload statements
  (`grep -n "HTTP-01|--apache|reload|TLS|wildcard|certbot|acme"`): the
  production-facts table row and the `#0426`/`#0431` cutover-verification
  bullet ("certbot needs no change... renewal runs off `dns-route53`") both
  already agree with the corrected §9 account; nothing else needed a change.

  No box access needed for this pass — every fact used was already measured
  and cited in the reviewer's notes; did not re-run `ssh ec2`.

  No mechanism added — documentation only (`#0330`/`#0347`/`#0356`/`#0381`).

  Verification: `uptime` reported load averages 6.74/15.96/13.03, then
  9.01/14.32/12.76 on a later run — above `CLAUDE.md` §5's ~8 five/fifteen-
  minute threshold from concurrent agents; not treated as a defect signal.
  `ISSUE=0436b scripts/check.sh go ./internal/db/... ./internal/handlers/...`
  run three times. `internal/handlers` failed once
  (`TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch`, a timing
  failure under load) then passed clean on the next two runs (`ok 25.570s`,
  `ok 5.978s` — real elapsed time, zero skips). `internal/db` failed on all
  three runs, but only on two citation-guard hits
  (`TestNoOpenIssueVerificationCitesUndefinedTestFunction`,
  `TestNoOpenIssueEvidenceCitesGoFileByLineNumber`) both naming lines in
  `issues/0411.md` and `issues/0442.md` — files this pass was explicitly
  scoped away from (other agents mid-edit; `git status --porcelain` showed
  both dirty on every run). Neither failure names `docs/deployment.md` or
  `issues/0436.md`. Not fixed here; out of this pass's scope per the
  dispatch instructions.

  Status left `in-progress`; leaving `resolved` to the reviewer.

- 2026-09-05 — **Second review pass (Opus): approved, status `resolved`.** All
  three remedies were transcribed faithfully and all three re-derived as true
  rather than merely different, independently of both prior passes. The
  `_acme-challenge.opencircuitsf.com` claim — the commit's one new factual
  assertion — is correct (DNS-01 validates that exact name for both the apex and
  the wildcard identifier, and the box's own letsencrypt logs show the same
  plugin writing that form for other domains) and stops short of asserting
  anything about credentials or IAM. The four box facts behind the reload
  paragraph were re-measured read-only and all four match. Criterion 2 was
  re-checked with an independently derived whole-file `python3` byte sweep over
  all 2,346 lines using a wider pattern set than the implementation pass used —
  no surviving statement describes the HTTP-01/`--apache` path as live. `#0447`
  is pointed at rather than absorbed (no hook added, nothing changed on the
  box), and `#0448`'s `www` A-record row is byte-identical to its prior state.
  Verification failures seen were confined to `issues/0442.md`, another agent's
  dirty file, which was left alone. Changed nothing on the box; no new issues
  filed (`CLAUDE.md` §9).
