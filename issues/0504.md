# 0504 — `CLAUDE.md` §7's production IP is stale, and `www` no longer resolves to the host the wildcard points at

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | docs / deploy |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-09-11 |

## Superseded by `#0508`

**This issue's central conclusion — "It is the same service" — is wrong.**
`#0508` (filed 2026-09-12) found that `www.opencircuitsf.com` and
`i-0e3bd89e87d1c2364` (`bluesky.sstools.co`, the box this issue and
`CLAUDE.md` §7 describe) are **not** the same host: `systemctl is-active
opencircuit.service` on that instance reports `inactive` while the public
site returns 200. Production is a genuinely different instance,
`i-01c45429c78f3adf7` (`photon.sstools.co`), not a re-addressed one. The
`/.well-known/atproto-did` hash match and matching Apache/OpenSSL versions
that led to "same service" here are explained instead by the new box being a
faithful rebuild of the old one — those checks cannot distinguish *the same
host* from *a replacement host serving the same content*.

`#0508` answers this issue's own acceptance criteria directly: the new
instance ID, type, public IP, and IAM role are all recorded in `CLAUDE.md`
§7 and corrected throughout the in-scope docs; the `44.222.209.183` /
`ec2.smallsharptools.com` split is explained (that name was never
repointed — it still answers for the old, now-decommissioned box, which is
still up and still answers this vhost with a 503); and the CNAME-vs-A-record
correction for the wildcard is carried through wherever `#0508` touched.
**Recommendation: close this issue as superseded by `#0508`** — that is the
user's call, not this pass's; left `open` here.

One item is explicitly **not** resolved by `#0508` and remains open work: a
full row-by-row reconciliation of every DNS record in `docs/deployment.md`'s
DNS table against the live zone (this issue's fourth criterion, "every other
occurrence... found and corrected") is `#0427`'s scope, not `#0508`'s —
`#0508` fixed the specific values it found while correcting the host facts,
but did not re-derive every CNAME/A distinction row by row.

## Description (original filing — see the correction above)

`CLAUDE.md` §7 records the production public IP as **`44.222.209.183`**.
Measured today, that is no longer where the site answers:

| Name | Resolves to |
|---|---|
| `www.opencircuitsf.com` | **`98.84.75.184`** |
| `ec2.smallsharptools.com` (the wildcard's CNAME target) | `44.222.209.183` |
| `*.opencircuitsf.com` | an **A** record at `98.84.75.184` |

**It is the same service**, so this is a stale fact rather than an outage:
`#0057`'s review confirmed `/.well-known/atproto-did` still hashes to §7's
expected `4198e742…5948d`, and Apache 2.4.68 / OpenSSL 3.5.7 still match.

The part worth a second look is not the number but the **split**. `www` and the
wildcard now answer at `98.84.75.184` while `ec2.smallsharptools.com` — which
§7 and `docs/` treat as the box — still answers at `44.222.209.183`. Those were
the same host when §7 was written. Either the instance was re-addressed and one
name was left behind, or there are now two hosts. Nobody has recorded which,
and the answer changes what the SSH alias for that host actually reaches.

### A second, related error the same investigation found

`docs/email-setup.md` described the zone's wildcard as a **CNAME** to
`ec2.smallsharptools.com`. Measured against the authoritative nameserver
`ns-923.awsdns-51.net`, it is an **A record** at `98.84.75.184` — a CNAME query
returns empty. `#0057` corrected the three occurrences in that file
(`cc91629`); this issue covers `CLAUDE.md` §7 and any other file carrying the
old IP or the CNAME description.

## Acceptance criteria

- [ ] `CLAUDE.md` §7's public IP matches what `www.opencircuitsf.com` actually resolves to, measured rather than assumed
- [ ] The `ec2.smallsharptools.com` / `www` split is **explained** in §7, not merely papered over with a new number — say whether the instance moved, whether the SSH alias for that host still reaches the box serving the site, and whether `44.222.209.183` is still in use by anything
- [ ] `i-0e3bd89e87d1c2364` is confirmed (or corrected) as the instance actually serving `www` — read from the box, not inferred from DNS
- [ ] Every other occurrence of `44.222.209.183` in the repo is found and corrected or explained — `grep -rn` the tree, do not fix only the ones this issue names
- [ ] Any other description of the zone wildcard as a CNAME is corrected to an A record
- [ ] `/.well-known/atproto-did` still hashes to `4198e742…5948d` after any change, per `CLAUDE.md` §7's own rule — verify by hash, never by status code

## Notes

Found 2026-09-11 while preparing `#0057`'s DNS artifacts, and confirmed
independently by the orchestrator with `dig`.

**Do not "fix" this by editing the IP alone.** §7 is the file agents trust for
production facts, and a number updated without the explanation behind it
invites the same confusion next time. The reason `#0057` hit this at all is
that a rollback check told the reader to expect one IP at a name that serves
another — a stale fact in a rollback procedure is worse than no fact.

**Nothing here should change DNS.** This is a documentation-correctness issue.
If the investigation concludes a DNS record *is* wrong, file that separately
rather than changing a zone from inside a doc fix — and remember the apex MX
must never be touched (`CLAUDE.md` §9).

## Relation

- Relates to: [#0057](0057.md) — whose DNS work surfaced this
- Relates to: [#0427](0427.md) — the deployment DNS-table reconciliation against the live zone
