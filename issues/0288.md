# 0288 — MarkDone does not clear the error from a failed attempt

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | internal/outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |
| **Commit** | 1cf190d |
| **Closed** | 2026-08-27 |

## Resolution notes

> 🟢 Resolved 2026-08-27 — `MarkDone` now clears `error` in the same single
> `UPDATE` that stamps `sent`; both admitted source states are covered by new
> DB-backed tests, and both were mutation-proved by an independent reviewer to
> fail against the pre-fix `SET` clause.

## Description

`#0272` made `MarkSent` clear `error = NULL` in the same `UPDATE` that sets
`status='sent'`, so a row is never observable as sent-with-error.

**`outbox.Store.MarkDone` has the same shape and was not changed.** It also
transitions a row to `status='sent'` — from `queued` *or* `sending` — and blanks
the payload, but it leaves `error` populated. It is called from
`internal/handlers/subscribe.go:571`.

`#0272`'s reviewer confirmed this is reachable, not theoretical: the recovery
poller claims an intake row, fails it via `MarkRetryOrAbandon` (which writes the
error and puts the row back to `queued`), and then the request's fast path
`MarkDone`s that same row — the convergence `subscribe.go`'s own comment
anticipates. The row ends up `sent` carrying the error from an attempt that was
subsequently superseded.

This was **not** a `#0272` bounce: that issue's criterion 1 names `MarkSent`
specifically, and its fix is correct and complete for what it specifies.

## Acceptance criteria

1. `MarkDone` clears `error` in the same `UPDATE` that sets `status='sent'` and
   blanks the payload. One statement, not a follow-up write.
2. The `queued` → `sent` and `sending` → `sent` transitions both clear it —
   `MarkDone` admits either source state and both are reachable.
3. `MarkRetryOrAbandon` stays untouched. A `queued` row awaiting retry and a
   terminal `abandoned` row must both still keep their error, exactly as
   `#0272` established.
4. A DB-backed test drives the real convergence: fail an intake row through
   `MarkRetryOrAbandon`, then `MarkDone` it, then assert `error IS NULL` while
   the sent-state columns are set.
5. Note that [#0283](0283.md) is open on `MarkSent`'s WHERE predicate. This
   issue is the SET clause only — do not pre-empt that decision.
6. `ISSUE=NNNN scripts/check.sh go ./internal/... ./cmd/opencircuit/...` passes
   with no skips.

## Relation

- Found by: [#0272](0272.md)'s review
- Relates to: [#0272](0272.md) — the identical fix, applied to `MarkSent`
- Relates to: [#0254](0254.md) — which added `MarkDone` and the recovery poller
  whose convergence makes this reachable
- Relates to: [#0283](0283.md) — the WHERE-clause question on the same family of
  statements

## Files changed

- `internal/outbox/store.go` — `MarkDone`'s `SET` clause now clears `error`;
  doc comment updated.
- `internal/outbox/store_test.go` — added
  `TestOutbox_MarkDone_ClearsErrorFromSupersededAttempt` and
  `TestOutbox_MarkDone_ClearsErrorFromSendingState`.

## Verification

1. `internal/outbox/store.go`'s `MarkDone` now sets `error = NULL` in the
   same `UPDATE` that sets `status = $2` (`'sent'`), `sent_at`, blanks
   `payload`, and clears `claimed_at`. One statement, no follow-up write.
2. New test `TestOutbox_MarkDone_ClearsErrorFromSupersededAttempt` drives
   the exact convergence this issue describes: claims a row, fails it via
   `MarkRetryOrAbandon` (asserts `error` populated on the resulting
   `queued` row), then calls `MarkDone` on it and asserts `status='sent'`,
   `error IS NULL`, `sent_at` set, `payload='{}'`. New test
   `TestOutbox_MarkDone_ClearsErrorFromSendingState` covers `MarkDone`'s
   other admitted source state (`'sending'`) directly. Both source states
   named in criterion 2 are exercised.
3. `MarkRetryOrAbandon` was not touched — no hunk inside it. The pre-existing
   `TestOutbox_AbandonsAtMaxRetries_RetainsLastError` (terminal `abandoned`)
   and `TestOutbox_MarkRetryOrAbandon_RetriesBeforeMax` (`queued` awaiting
   retry) both still pass unmodified, so both keep their error exactly as
   `#0272` established.
4. See item 2 — `TestOutbox_MarkDone_ClearsErrorFromSupersededAttempt` is
   the DB-backed test criterion 4 asks for.
5. `MarkSent`'s `WHERE` clause was not touched by this issue — only its
   `SET` clause carries the `#0283`-adjacent doc comments now on the file,
   and this issue's own diff to `MarkSent` is nil (only `MarkDone` and its
   doc comment changed for #0288). See `#0283` (implemented in the same
   batch) for the `WHERE`-predicate decision.
6. `ISSUE=0288 scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
   ./internal/handlers/... ./cmd/opencircuit/...` — all four packages `ok`,
   zero skips (`internal/outbox` 0.353s, DB-backed, not a 0.00s no-op).
   `gofmt -l` clean, `go vet` clean, `go build` clean. Scratch database
   `opencircuit_test_0288` dropped on exit; no leftover `go test`/`.test`
   processes of this run's own.

This issue was implemented together with `#0283` and `#0285` in one batch
(all three touch `internal/outbox/store.go`/`store_test.go`); the single
`check.sh` run above covers all three.

## Review notes

Independent phase-3 review of `1cf190d`, 2026-08-27. **Approved.**

**Re-verification.** `ISSUE=0283r scripts/check.sh go ./internal/outbox/...
./internal/mailing/... ./internal/handlers/... ./cmd/opencircuit/...` —
`VERIFICATION PASSED`, all four packages `ok`, skip audit reported `(no
skips)`, `gofmt -l` clean, `go vet` clean. `uptime` before the run showed a
load average of 2.87, well under the ~4 idle baseline. `internal/outbox` ran
0.360s (DB-backed, not a 0.00s no-op). Both new tests were then run
explicitly by name and observed to pass:

```
--- PASS: TestOutbox_MarkDone_ClearsErrorFromSupersededAttempt (0.00s)
--- PASS: TestOutbox_MarkDone_ClearsErrorFromSendingState (0.00s)
```

**Mutation proof (criterion 4).** In a throwaway worktree detached at
`1cf190d`, `MarkDone`'s `SET` clause was reverted to drop `error = NULL` and
both tests were re-run. Both failed, naming the exact defect:

```
store_test.go:524: error = recovery poller: transient failure, want NULL — a done row must not carry a stale error from a superseded attempt
store_test.go:576: error = stray error, want NULL after MarkDone from 'sending'
```

So these are genuine guards, not tests that would pass either way. The
worktree was removed and the scratch database dropped.

**Criteria.**

1. Met — one `UPDATE`, `error = NULL` alongside `status`, `sent_at`,
   `payload = '{}'::jsonb`, `claimed_at = NULL`. No follow-up write.
2. Met — `queued → sent` via
   `TestOutbox_MarkDone_ClearsErrorFromSupersededAttempt` (which drives the
   real `MarkRetryOrAbandon`-then-`MarkDone` convergence), `sending → sent`
   via `TestOutbox_MarkDone_ClearsErrorFromSendingState`.
3. Met, re-checked at the source rather than taken on report:
   `MarkRetryOrAbandon`'s statement is byte-identical to its pre-commit form
   (`git show 1cf190d` contains no hunk inside its body — only its doc
   comment) and still writes `error = $2` unconditionally on **both**
   branches, so a `queued` row awaiting retry and a terminal `abandoned` row
   both keep their error.
   `TestOutbox_AbandonsAtMaxRetries_RetainsLastError` and
   `TestOutbox_MarkRetryOrAbandon_RetriesBeforeMax` both still assert that
   and both pass.
4. Met — see the mutation proof above.
5. Met — `MarkSent`'s `WHERE` predicate is `#0283`'s change, not this one's;
   this issue's diff touches only `MarkDone`'s `SET` clause and doc comment.
6. Met — see re-verification above.

**One cosmetic nit, not blocking.** The paragraph added to `MarkDone`'s doc
comment opens `(#0288 — the identical fix #0272 applied to MarkSent …` and
never closes the parenthesis. Worth fixing in passing on any later edit to
this file; it does not affect behaviour, `gofmt`, or any guard.

**Note for the reader.** `#0283` and `#0285` share this commit and were
**bounced** in the same review pass — both for inaccurate load-bearing
reasoning recorded in code comments, not for behaviour. The `MarkDone` change
this issue specifies is independent of both and stands on its own; the code
commit stays in place.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-27 | implement | claude-sonnet-5 | — | — | — | — | — |

Token/cost columns left for the orchestrator to fill from this subagent's
transcript (this subagent has no transcript access to its own usage).
