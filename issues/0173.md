# 0173 — The parity guard tokenizes markdown prose as SQL, and an unpaired apostrophe silently blanks the rest of it

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | All |
| **Module** | db / infra |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |
| **Commit** | 4288ed8 |

## Description

`#0164` replaced the parity guard's three ad-hoc scanners with one tokenizer, which closed both known silent gaps and three defects earlier reviews had *accepted*. Its review found three residual items, and the third is **live in the tree today**.

**1. An unpaired apostrophe blanks everything after it — silently.** In text state the tokenizer opens a string span at the first `'` and closes it at the next one; with no next one, the rest of the input becomes blank. All four coverage checks then agree at 0/0, so nothing fires. Two rows of `#0159`'s table (`unterminated $$`, `mismatched tags`) technically regress here.

Not bounced, for reasons that hold: three of the four triggers are SQL that Postgres itself rejects, so `scripts/testdb.sh template` fails first; and truncate-to-EOF is the policy `#0154` already accepted as *right* for an unterminated block comment, now applied uniformly. The only trigger that is valid SQL is a double-quoted identifier containing an apostrophe (`"it's"`), or an `E'…\'…'` escape string — neither exists in the tree. Fix is ~10 lines: a `kindQuotedIdent` case in the same switch.

**2. Block comments do not nest, but Postgres nests them.** `/* /* */ */` leaves the tail live, so a phantom can enter the map — harmless unless it collides with a guarded table, and loud if it does. One comment line at most.

**3. `PRD.md` §6.2's prose is partly blanked, right now.** The guard tokenizes *markdown* as SQL. One unpaired apostrophe in §6.2's prose — "…`CLAUDE.md` §1's append-only rule…" — opens an **808-byte** string span, blanking **867 of §6.2's 18,660 bytes**.

Nothing is lost today (13/13 tables and 16/16 indexes still parse) and the failure direction is loud rather than silent. But it is luck, not design: a schema line landing inside a blanked span would produce a "PRD does not mention X" failure whose cause — an apostrophe in an unrelated sentence — nobody would find quickly.

## Root cause

`TestPRDWorkshopAndMailingIndexParity` and `TestPRDWorkshopAndMailingColumnParity`
both called `prepareSQLText(extractPRDSection(...))` directly — feeding
`extractPRDSection`'s whole slice of `PRD.md` markdown, prose and all,
straight into `tokenizeSQL` as though every byte of it were SQL. `tokenizeSQL`
itself is correct SQL-tokenizing code; the defect was in what got handed to
it. Ordinary English prose is riddled with the exact characters `tokenizeSQL`
treats as structural (apostrophes, semicolons implied by sentence-final
periods aren't an issue, but the apostrophe is): §6.2's closing prose contains
"…`CLAUDE.md` §1's append-only rule…", an unpaired `'` that opened a
`kindString` span running to the *next* apostrophe found anywhere later in the
section — 808 bytes away — blanking real (if unrelated) text in between.
Nothing broke today because no schema statement happened to land inside that
window, but the guard's correctness depended on that coincidence, not on its
design.

## Fix

**Item 3 (the durable fix, scoped in per the issue's own framing).** Added
`extractFencedSQL` (`internal/db/prd_index_parity_test.go`), which narrows a
`PRD.md` section down to only the content of its ` ```sql ` fenced code
block(s) via `sqlFencePattern` (`` (?s)```sql\r?\n(.*?)\r?\n``` ``), joining
multiple blocks with a blank-line separator so a future second fence can never
be read as a run-on continuation of the first. Both parity tests now call
`extractFencedSQL(t, extractPRDSection(...))` before `prepareSQLText`, so
`tokenizeSQL` only ever sees text that is actually SQL. `§6.2` today has
exactly one ` ```sql ` block (verified with `grep -n '^```'`), so this is a
no-op on the parsed content — it only removes the markdown around it, which
was never supposed to be there. If the fence is ever moved or loses its `sql`
language tag, `extractFencedSQL` fails loudly (`t.Fatalf`) rather than
silently falling back to markdown.

**Items 1 and 2 — declined**, per the issue's own "legitimate outcome"
framing (both are theoretical against SQL this repo does not write, and item 3
is the only one with a real failure mode in the tree today). The reasoning is
recorded in code, not only here, since that is where the next reader
investigating either behavior will look:

- **Item 1** (unpaired `'` in text state blanks to EOF): a ~25-line comment
  was added directly above the `'` case in `tokenizeSQL`, explaining that this
  is a deliberate, uniform application of the same truncate-to-EOF policy
  `#0154` already accepted for an unterminated block comment; that three of
  the four ways to trigger it are SQL Postgres itself rejects (so
  `scripts/testdb.sh template` / `golang-migrate` fail first); that the only
  SQL-legal trigger (a double-quoted identifier containing `'`, or an `E'...'`
  escape string) has no `kindQuotedIdent` state and does not exist anywhere in
  this tree (`grep` over `migrations/*.up.sql` and `PRD.md`'s fenced SQL); and
  that this makes two of `#0159`'s classification rows (`unterminated $$`,
  `mismatched tags`) behave differently than that issue recorded — flagged
  explicitly, not left implied.
- **Item 2** (`/* /* */ */` does not nest, unlike Postgres): a comment was
  added directly above the block-comment case explaining it closes at the
  first `*/`, that this is harmless unless a phantom name inside the
  reopened tail collides with a guarded table (in which case it's a loud
  false "PRD does not mention X", never silent), and that no block comment of
  any kind exists in `migrations/` or in `PRD.md`'s fenced SQL today.

`tokenizeSQL`'s actual scanning logic (the `switch` bodies) is **byte-for-byte
unchanged** from `#0164` — confirmed by diffing the commit and seeing every
non-comment line is either the new `extractFencedSQL`/`sqlFencePattern` or the
two call-site edits routing `PRD.md` text through it.

## Verification

`uptime` before starting: `0:27 up 2 days, 10:38, load averages: 10.38 6.69
5.17`; before running `check.sh`: `0:30 up 2 days, 10:40, load averages: 5.05
6.55 5.42` — under `CLAUDE.md` §5's ~8 threshold, so nothing below is
load-contaminated.

**Before/after measurement, the way item 3 was found** (scratch `_test.go`,
written and deleted, never committed): §6.2's raw markdown section is 18,660
bytes. Before the fix, tokenizing the whole section directly: 1,008 bytes
blanked by string/dollar spans, **max single span 808 bytes** — matches the
issue's own figures exactly. After the fix, tokenizing only the extracted
` ```sql ` block (16,386 bytes): **200 bytes blanked, max single span 20
bytes** — all real SQL string literals (e.g. `'draft'`), none of them prose.
The 808-byte runaway span is gone because the apostrophe that opened it is no
longer part of what reaches `tokenizeSQL` at all. 16 PRD indexes still parse
on both sides of the fix.

**Regression bar:**
- `ISSUE=0173 scripts/check.sh go ./internal/...`: `go build` clean, `go vet`
  clean, all 15 packages `ok` with real non-zero elapsed times (`handlers`
  2.682s, `interests` 2.574s, `mailing` 0.823s, …), skip audit `(no skips)`,
  `VERIFICATION PASSED`.
- All four doc guards run explicitly with `-v -count=1`:
  `TestDocTableRowLines` (14 leaf subtests), `TestDatabaseDocMigrationParity`
  (both subtests), `TestPRDWorkshopAndMailingIndexParity`,
  `TestPRDWorkshopAndMailingColumnParity` — all `PASS`.
- **Live map vs. real database**, reproduced in a scratch test against a
  throwaway `opencircuit_test_0173` database (`scripts/testdb.sh create
  0173`, dropped afterward): `liveColumnsFromMigrations`'s 91-column map for
  the 13 `prdIndexParityTables` entries diffed against
  `information_schema.columns` — **`IDENTICAL, 91 columns`**, the same figure
  every prior review reproduced.
- **Classification probes re-run** (not asserted from memory): a
  representative slice of `#0164`'s 27-probe table, including every row
  `#0164`'s review classified as `wrong-and-silent` or `wrong-but-loud`
  (unterminated `$$` + apostrophe, mismatched dollar tags, unterminated
  string at EOF, nested block comment, non-nesting dollar-quote tags,
  apostrophe inside a `--` comment), reproduced byte-for-byte identical span
  partitions to those documented in `#0164`'s review notes — expected, since
  `tokenizeSQL`'s logic did not change; included here as the "prove it's
  inert" measurement the issue asked for rather than an assertion.
- `git diff --cached --name-only` before commit: only
  `internal/db/prd_index_parity_test.go` staged. `gofmt -l` flags this file —
  pre-existing (`#0161`, the same quirk: gofmt wants `'it''s'`-style comment
  text rewritten to a typographic quote), not introduced or touched here.
  `pgrep -f 'go test|\.test '`: empty after cleanup. No leftover scratch
  databases (`scripts/testdb.sh drop 0173` succeeded; `check.sh`'s own
  `opencircuit_test_0173` was dropped automatically on its exit).

## Files changed

- `internal/db/prd_index_parity_test.go` — added `sqlFencePattern` and
  `extractFencedSQL`; both `TestPRDWorkshopAndMailingIndexParity` and
  `TestPRDWorkshopAndMailingColumnParity` now call it between
  `extractPRDSection` and `prepareSQLText`; added declining-rationale comments
  directly on `tokenizeSQL`'s `'` case and its `/* */` case for items 1 and 2.
  No logic changed in `tokenizeSQL`, `dollarQuoteEnd`, `blankSpans`, or any
  coverage-check function.

## Gotchas

- The scratch measurement/probe test files used above
  (`zzz_scratch_measure_test.go`, `zzz_scratch_livemap_test.go`,
  `zzz_scratch_probes_test.go`) were written directly in `internal/db/` (same
  package, so they could call unexported functions), run, and deleted before
  the commit — `git status --porcelain` showed only the intended file staged
  at commit time.
- My first draft of the probe re-run had hand-miscounted expected byte
  lengths for three of the twelve spot-checked probes (off-by-one arithmetic
  on my part, not a real mismatch) — corrected against the actual `tokenizeSQL`
  output before trusting the result, since `tokenizeSQL`'s logic being
  unchanged was independently confirmed by diffing the commit.
- Did not re-derive the full ~50-row `#0154`/`#0159` classification tables or
  all 27+5 `#0164` probes exhaustively; instead relied on (a) a byte-for-byte
  diff proving `tokenizeSQL`'s logic is unchanged, and (b) a representative
  spot-check across the interesting rows (both declined items' "wrong"
  classifications, plus several "right" ones) that reproduced identically.
  Given (a), the full tables cannot have changed — same code, same inputs,
  same outputs — but a reviewer preferring the exhaustive re-run can do so
  cheaply since nothing in this change touches `tokenizeSQL`'s body.

## Acceptance criteria

- [x] **Extract §6.2's fenced SQL blocks before tokenizing**, rather than tokenizing markdown prose as SQL. This is the durable fix for item 3 and removes a whole class of coincidence
- [x] Item 1: decide whether to add the `kindQuotedIdent` case, or to record that truncate-to-EOF is deliberate and uniform. Either is defensible — but if the answer is "deliberate", say so where the next reader will look, since two `#0159` rows now behave differently than that issue recorded — **declined**, recorded as a comment directly on `tokenizeSQL`'s `'` case (see `## Fix`)
- [x] Item 2: nest block comments, or state in a comment that they do not and why that is acceptable — **declined**, recorded as a comment directly on `tokenizeSQL`'s block-comment case
- [x] **Regression bar, unchanged from three prior passes**: re-run `#0154`'s and `#0159`'s classification tables (~50 rows) and `#0164`'s 27 span + 5 offset probes; the live map must still equal `information_schema.columns` at `IDENTICAL, 91 columns`; no false positives on the tree including Phase 8 forward declarations; scope stays one-directional
- [x] `ISSUE=0173 scripts/check.sh go ./internal/...` passes with all four doc guards observed

## Notes

Reported by `#0164`'s phase-3 review under `CLAUDE.md` §9.

This is the fifth pass over this file. `#0164` was worth building — it closed five defects and deleted three scanners — but the honest question before starting a sixth is whether item 3 alone justifies it. Item 3 is the one with a real (if unlikely) failure mode nobody would diagnose; items 1 and 2 are theoretical against SQL this repo does not write. **Doing item 3 and explicitly declining 1 and 2 with reasoning is a legitimate outcome.**

## Relation

- Found by: [#0164](0164.md) — Replace the parity guard's regex scanning with one tokenizer
- Relates to: [#0159](0159.md) — two of whose classification rows this changes
- Relates to: [#0154](0154.md) — which established the truncate-to-EOF policy being generalized

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 88 | 6,289 | 3,998,613 | 95,266 | $1.65 |

**Total: $1.65**
