# 0065 — The config test helper omits BASE_URL, so TestLoad_DefaultsApplied fails

| | |
|---|---|
| **Status** | resolved |
| **Module** | config |
| **Platform** | All |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-08-18 |
| **Commit** | 030dfca |
| **Closed** | 2026-08-18 |

## Description

`go test ./...` exits non-zero on a clean checkout: `internal/config` `TestLoad_DefaultsApplied` fails with `config: missing required variable BASE_URL`. The `setRequired` test helper sets five of the loader's six required variables and silently omits `BASE_URL`, so any test relying on the helper alone cannot construct a valid config.

Carried over from ShortLinks — it fails identically there at `bbdd0f7`, so the port did not introduce it. `#0001` recorded it as pre-existing rather than fixing it, which was correct for that issue's scope. This issue closes it.

## Steps to reproduce

1. `cd /Users/brennan/Developer/OpenCircuitSF/Website`
2. `go test ./internal/config/ -run TestLoad_DefaultsApplied -v`

## Expected behavior

The test constructs a config from the five required variables plus defaults and asserts that `PORT`, `SES_SMTP_PORT`, `CACHE_MAX_COST`, and `CACHE_TTL_SECONDS` fall back to their default values. It should pass.

## Actual behavior

`loadFromFile` returns `config: missing required variable BASE_URL` and the test fails at `config_test.go:74` before reaching a single default assertion.

> 🟢 Resolved 2026-08-18 — `setRequired` now sets `BASE_URL` alongside the other five required variables, making `TestLoad_DefaultsApplied` pass, and a `missing BASE_URL` case in `TestLoad_MissingRequired` closes the coverage gap that let the omission hide; `internal/config/config.go` is untouched and `go test ./...` now exits zero.

## Root cause

`setRequired` (`internal/config/config_test.go:14`) sets `DATABASE_URL`, `WEBAUTHN_RP_ID`, `WEBAUTHN_RP_ORIGIN`, `SESSION_SECRET`, and `ADMIN_EMAIL`. The loader's required list (`internal/config/config.go:112`) has six entries — the same five plus `BASE_URL`. The helper's name promises every required variable; it delivers five of six.

The other tests hide the gap:

- `TestLoad_AllRequiredPresent` sets `BASE_URL` explicitly on its own, so it never depends on the helper.
- `TestLoad_MissingRequired` and `TestLoad_MissingRequiredReportsAll` expect an error and assert only that the message *contains* the variable under test. A spurious `BASE_URL` entry in that message changes nothing, so they pass regardless.

`TestLoad_DefaultsApplied` is the only test that depends on `setRequired` producing a *valid* config, so it is the only one that fails.

Confirmed by re-running `go test ./internal/config/ -run TestLoad_DefaultsApplied -v` before making any change: it failed with `config_test.go:74: loadFromFile returned error: config: missing required variable BASE_URL`, exactly as described. No changes were needed to this diagnosis.

## Fix

Added one line to `setRequired` in `internal/config/config_test.go` setting `BASE_URL` to `https://go.sstools.co`, matching the style and domain of the other still-ShortLinks-flavoured values already in the helper (`WEBAUTHN_RP_ID`, `WEBAUTHN_RP_ORIGIN`) so the diff stays a single added line. Added a `{"missing BASE_URL", "BASE_URL", "BASE_URL"}` case at the top of `TestLoad_MissingRequired`'s table (ahead of `DATABASE_URL`, matching the field order in `config.go`'s `required` slice) so `BASE_URL`'s required-ness is now actually asserted rather than relying on the other tests' incidental coverage. `internal/config/config.go` was not touched. No rebranding was performed — the helper's `go.sstools.co` values and `.env.example` remain as they were, per `#0007`'s ownership.

## Verification

- `go test ./internal/config/ -run TestLoad_DefaultsApplied -v 2>&1 | tail -20` — **before** the fix: `--- FAIL: TestLoad_DefaultsApplied`, `config: missing required variable BASE_URL`. Confirms the reported failure.
- `go test ./internal/config/ -count=1 -v 2>&1 | tail -60` — **after** the fix: all tests pass, 0 failures, 0 skips. `TestLoad_DefaultsApplied` passes. The new subtest `TestLoad_MissingRequired/missing_BASE_URL` appears in the verbose output and passes, alongside the five pre-existing `TestLoad_MissingRequired` subtests, `TestLoad_AllRequiredPresent`, `TestLoad_MissingRequiredReportsAll`, and all four `TestLoad_InvalidInteger` subtests.
- `go build ./... 2>&1 | tail -20` — no output, clean.
- `go vet ./... 2>&1 | tail -20` — no output, clean.
- `TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable' go test ./... -count=1 2>&1 | tail -60` — every package reports `ok`: `cmd/opencircuit`, `internal/audit`, `internal/auth`, `internal/config`, `internal/db`, `internal/devstore`, `internal/events`, `internal/handlers`, `internal/middleware`; `internal/testdb` and `web` report `[no test files]` (expected — no Go tests in those packages). Exit code 0. With `TEST_DATABASE_URL` set, the DB-backed tests in `internal/db`, `internal/handlers`, etc. actually executed rather than skipping.

## Files changed

- `internal/config/config_test.go` — added `t.Setenv("BASE_URL", "https://go.sstools.co")` to `setRequired`; added a `{"missing BASE_URL", "BASE_URL", "BASE_URL"}` row to `TestLoad_MissingRequired`'s table.

## Acceptance criteria

- [ ] `setRequired` sets `BASE_URL` alongside the other five required variables
- [ ] `TestLoad_MissingRequired`'s table gains a `BASE_URL` case, closing the coverage gap that let this hide — `BASE_URL`'s required-ness is currently never asserted
- [ ] `go test ./internal/config/ -count=1 -v` passes with zero failures and zero skips
- [ ] `go test ./... -count=1` exits zero with `TEST_DATABASE_URL` set
- [ ] `go build ./...` and `go vet ./...` stay clean
- [ ] No change to `internal/config/config.go` — the loader is correct; the test helper is wrong

## Notes

Fix the helper, not the loader. `BASE_URL` is genuinely required: it is the canonical origin used to build confirmation and unsubscribe links, and a config without it would produce broken links in email rather than failing loudly at startup. Making it optional would trade a startup error for a silent production defect.

**Out of scope, deliberately.** The helper's values are still ShortLinks-flavoured (`go.sstools.co`), and `.env.example` still points at `go.sstools.co` and the `shortlinks` database. Those belong to [#0007](0007.md), which rewrites the config loader and its environment for this project. Do not rebrand them here — this issue makes the suite green and nothing else.

Keep the value used for `BASE_URL` consistent with the existing helper entries so the diff stays a single added line plus the new table case.

## Relation

- Depends on: [#0001](0001.md) — Scaffold the repository from the ShortLinks skeleton
- Related: [#0007](0007.md) — Extend the config loader for the new environment variables
