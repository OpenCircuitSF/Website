# 0347 — No guard stops a Go comment naming a claim-machinery call its file's code does not contain

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | outbox / mailing |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | 2dad4da |

## Description

`#0342` has now bounced **twice** on the same criterion — its sweep for stale
comments — and both bounces found the same *class* of defect: a comment
asserting, in the present tense, that some code calls `ClaimDue` when the file
containing it has zero `ClaimDue` call sites. `#0297` removed those calls; the
comments describing them did not go with them.

The tally so far is nine stale sites across two implementation passes and three
reviews, each pass finding what the last missed. The second review diagnosed
why: **every pass used the same instrument** — the criterion's keyword grep,
then a human read of each paragraph. The failure is in triage, not in the grep,
so repeating the grep repeats the miss.

That review's replacement instrument is the thing worth keeping. It parses each
Go file **twice** — once with `parser.ParseComments` for the comments, once
with mode `0` so comments never enter the AST — and then asks whether each
identifier a comment names actually appears in that file's *code*. Over 269
files, 4963 comment groups and 456 identifier hits, it found the three
remaining sites by construction rather than by judgement, and it reproduced the
site the first review had found by hand when pointed at the older snapshot.

`#0267`'s misattached-doc-comment guard family does not reach this class: these
are inline and doc comments that are attached to the right declaration and are
simply false about what that declaration does.

## Acceptance criteria

1. A Go guard test fails when a comment names `ClaimDue`, `SelectDue`,
   `OrphanSweep`, `ClaimRow`, or `ClaimBatch` in the present tense while the
   file's own code contains zero calls to it.
2. The census of what the code contains is taken from a mode-`0` parse, so
   comment text can never contribute to the population a comment is checked
   against. A comment must not be able to satisfy the check that measures it
   (§8 — a copy of the answer stored next to the question is not a check).
3. History paragraphs are allowed, via an explicit allowlist or an explicit
   past-tense/marker convention. `#0342`'s sweep confirmed that correctly
   framed historical references are common and legitimate — the guard must not
   force their removal. State the convention it recognises.
4. The guard is proved falsifiable: pointed at the `ffef8cf` snapshot (or an
   equivalent reconstruction) it flags `outbox_worker_test.go:746`'s
   `OutboxWorker.ClaimDue`, and against the fixed tree it does not.
5. It fails closed on empty input and on a zero-hit census — §8 records a
   fail-open that arose in exactly that way, where an extraction contributing
   nothing was read as agreement.
6. **The guard belongs in Go, not in an external shell harness.** Per §8's
   rule, the deciding question is whether the mutation leaves the assertion
   falsifiable by the thing it measures: adding a false comment changes what
   the scan finds, so the in-package test is a legitimate, non-circular oracle.
   Record that reasoning in the guard's doc comment.
7. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal is
   introduced into any Go comment — `scripts/go_file_visit_floor_guard_test.sh`
   greps for that syntax and comment text containing it inflates the population
   it measures. Re-run that harness and record the population (currently 42).

## Added by `#0342`'s third review, 2026-08-31

That review resolved `#0342` and, in discharging its sweep criterion three
different ways, measured two corrections this issue needs **before** anyone
implements it.

**1. Criterion 1 as written would fire on nine correct comments in one file.**
`internal/handlers/subscribe_intake_test.go` contains **nine** comment mentions
of `ClaimDue` and **zero** code calls to it. The reviewer read all nine: every
one is correct. They name `outbox.Store.ClaimDue` as an existing API — it is,
at `internal/outbox/store.go:351`, still filtering by kind — or they narrate a
deliberate worktree mutation (`:303`, `:320`, `:331`), or they state what the
test deliberately does **not** use (`:223`).

So the distinction the guard must encode is **"names a call this file's code
makes"** versus **"names a method that exists"** — *not* present tense versus
past tense. Criteria 1 and 3 above are written in terms of tense and must be
rewritten in those terms instead. Add this file as a **named negative fixture**:
a correct implementation produces zero findings in it.

**2. Add a directional axis.** Two of the three sites `#0342`'s third pass
fixed were *direction* errors — a comment saying "the same set X below is
scoped to" where nothing named X is below — and a pure existence census cannot
see one, because the identifier does exist. The axis checks a comment's
"above"/"below" claims against the named identifier's own positions in the same
file.

It is cheap and it closes a real subclass, but it needs the **high-precision
window variant**: the naive form produced 480 wide flags where the precise one
produced 16. Ungated, it is far too noisy to fail a build.

### What no instrument in this family can catch

Worth recording so a later pass does not believe the guard closes the whole
class. Three sweeps now, each with a different blind spot, and the reviewer's
own falsifiability work shows the limit is structural: these instruments find
**structural** errors — a direction the code does not satisfy, a package member
that does not exist, a wrong arity or variadic form, a missing file.

**A comment that is false about *behaviour*, while every identifier it names
exists in the right file, direction and arity, is invisible to all of them.**
That is exactly `#0342`'s original site 1 — *"a `Stop` landing anywhere in
`sendOne` still sees it as outstanding"* — which named only real things and was
simply wrong about what happens. Only a human read found it, and only a human
read will find the next one. Do not let this guard's greenness be read as
"there are no stale comments."

### Tools left for the implementer

`#0342`'s third review left its instruments in the session scratchpad:
`r3-0342/audit0342r3.go` (the four-axis sweep — directional, cross-package,
arity/variadicity, file referents), `r3-0342/astfp0342r3.go` (a position-free
AST fingerprint for comment-only proofs), and `r3-0342/commentscan0342r3.go`
(a `go/scanner`-based banned-literal scan).

**§8b warns the scratchpad is not session-private in practice** — two agents
have had a script pick up a previous session's leftover file. Verify these are
what they claim before relying on them, and re-prove falsifiability rather than
inheriting the claim. Both of the reviewer's own tools had bugs it found and
fixed mid-review: a left-to-right scan that let an earlier word consume the
`below` and hide a real site, and a snippet printer that sliced by byte index,
split a multi-byte rune, and produced invalid UTF-8 that BSD `grep` then
silently refused to match — §8's trap, hit twice in one session.

## Root cause

No mechanism existed to check a Go comment's claim about the claim-machinery
identifiers against the file's own code, or about a comment's "above"/"below"
position claim against the file's own layout — every prior sweep (three of
them, across `#0342`'s two implementation passes and three reviews) was a
keyword grep plus a human read, so each pass found only what the last
missed.

## Fix

Added `internal/outbox/claim_comment_referent_guard_test.go` with two guard
tests sharing one mode-0/`ParseComments` walk over `claimCommentGuardScanRoots`
(`internal/` + `cmd/`, matching this package's sibling
`claim_kinds_call_site_guard_test.go`):

- **`TestNoCommentClaimsClaimMachineryCallFileCodeLacks`** — the existence
  axis (criterion 1, corrected) plus the directional axis (the amendment's
  addition). A comment is flagged for the existence axis only when it
  writes `Type.Ident` where `Type` is declared in the FILE's OWN PACKAGE
  (not an external package's type) and the word "call"/"calls" appears
  immediately after `Ident`, and the file's own mode-0 code census for
  `Ident` is zero. This is deliberately narrower than "any bare mention
  followed by 'call'" — an early draft used that broader rule and produced
  double-digit false positives in this package's own sibling guard file and
  in `store.go`, both of which correctly and routinely write bare "X call"
  prose describing calls that happen in OTHER files they scan or declare.
  That imprecision, once fixed, is why criterion 1's own bullet list in this
  guard's doc comment now says bare mentions are never flagged regardless of
  nearby wording — see that doc comment's "What 'names a call this file's
  code makes' means here" section for the full reasoning and the concrete
  false positives that drove it.

  The directional axis anchors on each "above"/"below" occurrence and takes
  only the NEAREST target identifier before it (not every identifier in a
  fixed-width window) — an earlier draft took every identifier in a 90-char
  window and flagged a real, correct doc comment in `store.go` whose
  sentence names one method as its subject and, in a trailing parenthetical,
  points a directional word at a second, different, nearer method.

  Both axes exempt a sentence/clause carrying an explicit historical,
  hypothetical, or negating marker (criterion 3's convention — see the
  guard's own doc comment for the exact word list), scoped to the CLAUSE
  containing the identifier, not the whole comment group/paragraph. An
  early draft scoped it to the whole group and, proved by this guard's own
  falsifiability fixture, exempted the real `ffef8cf` defect because an
  unrelated "rather than" later in the same paragraph matched — the same
  class of over-broad exemption criterion 1's amendment warns about, one
  level down. Clause boundaries include the em dash ("—"), which this
  codebase writes far more than periods; an earlier draft used only
  `.;:!?` and the same falsifiability fixture caught it.

- **`TestNoClaimMachineryCallSyntaxLiteralInAnyComment`** — criterion 7,
  enforced in Go via `go/scanner` (lexer-level, so a string or raw literal
  containing the banned substrings can never be mistaken for a comment).
  Complements, not duplicates, `scripts/go_file_visit_floor_guard_test.sh`'s
  existing external, text-based oracle (CLAUDE.md §8: the two homes are not
  redundant — that harness is what a mutation to THIS file's own detection
  logic can't disarm, and this test is what runs on every ordinary
  `go test` without needing the shell harness invoked separately).

Two standing regression tests pin the required proofs permanently rather
than leaving them as one-off manual runs:

- `TestClaimCommentGuardFlagsKnownStaleSiteFromFfef8cf` — criterion 4's
  falsifiability proof, against an in-memory reconstruction of `ffef8cf`'s
  and `HEAD`'s real `internal/mailing/outbox_worker_test.go` comment text,
  fetched byte-for-byte via `git show` (including the real U+2014 em dash,
  not a retyped ASCII substitute — CLAUDE.md §8's backslash-escape gotcha
  generalizes to "don't retype punctuation a human wrote, either").
- `TestClaimCommentGuardIsCleanOnKnownNegativeFixture` — parses the REAL,
  on-disk `internal/handlers/subscribe_intake_test.go` and asserts zero
  existence-axis findings, per the amendment's named negative fixture.

Fail-closed floors (criterion 5) cover an empty scan-roots list, a low
file-visit count, a zero call census, and a zero comment-group count, all
enforced live inside `TestNoCommentClaimsClaimMachineryCallFileCodeLacks`
and separately proved in isolation
(`TestGoFileVisitFloorImplausibleGuardFiresOnEmptyOrLowCount`, the pure
function; `TestClaimCommentGuardCensusFloorCatchesRootsWithNoRealPopulation`,
the real pipeline end-to-end against `internal/audit`, a real package
confirmed by direct grep to hold zero claim-machinery mentions).

The guard's doc comment records where the oracle lives and why (criterion
6, matching §8's rule) and states explicitly what this guard family cannot
reach — a comment false about *behaviour* while every identifier it names
is structurally correct, per `#0342`'s third review's own finding — so a
green run is not misread as "the comments are accurate".

Not implemented: no `## Obstacles`/`## Plan` section exists on this issue
(phase 1 was skipped, consistent with `CLAUDE.md` §3's "mechanical, ports of
already-validated work" criterion not applying here — this one did need real
design decisions, discovered live rather than planned; recorded honestly in
`## Gotchas` below rather than glossed over).

## Verification

```
uptime                                                             # 19:07, load averages 1.35 1.78 2.09 — not saturated
go build ./...                                                     # clean
go vet ./...                                                       # clean
gofmt -l internal/outbox/claim_comment_referent_guard_test.go      # clean
sha256(file) == sha256(gofmt stdout)                                # equal, confirmed after every edit pass
ISSUE=0347c scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
scripts/check.sh guards
```

`go build`/`go vet`/`gofmt -l` clean. `internal/outbox` ok (0.698s),
`internal/mailing` ok (3.714s), `internal/handlers` ok (7.295s) — zero
skips, no package reported `0.00s`. `VERIFICATION PASSED`.
`scripts/check.sh guards` — port `:5173` confirmed free via
`lsof -nP -iTCP:5173 -sTCP:LISTEN` before running, never `pkill -f` — ran
all four scripts (`check_guard_test.sh`, `testdb_gc_guard_test.sh`,
`dev_guard_test.sh`, `db_reset_guard_test.sh`,
`go_file_visit_floor_guard_test.sh`), all reporting "all guards hold (0
failures)"; `VERIFICATION PASSED`.

New tests, named and confirmed running (via `go test ./internal/outbox/...
-run 'ClaimComment|GoFileVisitFloorImplausible|NoCommentClaims|NoClaimMachinery'
-v`, all `PASS`):
`TestNoCommentClaimsClaimMachineryCallFileCodeLacks`,
`TestNoClaimMachineryCallSyntaxLiteralInAnyComment`,
`TestGoFileVisitFloorImplausibleGuardFiresOnEmptyOrLowCount`,
`TestClaimCommentGuardCensusFloorCatchesRootsWithNoRealPopulation`,
`TestClaimCommentGuardFlagsKnownStaleSiteFromFfef8cf`,
`TestClaimCommentGuardIsCleanOnKnownNegativeFixture`.

**Population, measured directly** (temporary probe, not committed): 271
files visited under `claimCommentGuardScanRoots`, 18 packages indexed, 59
real claim-machinery calls (mode-0 census sum), 5018 comment groups
scanned, **0 findings on `HEAD`**.

**Criterion 7's harness, re-run and re-measured**:
`scripts/go_file_visit_floor_guard_test.sh`'s
`claimKindsGuardMinPlausibleCallSiteCount` external, grep-based population
moved **42 → 43**; `claimKindsGuardMinPlausibleNonExemptCallSiteCount`
stayed at **12**. The +1 is not comment inflation —
`TestNoClaimMachineryCallSyntaxLiteralInAnyComment` independently proves,
via `go/scanner`, zero banned call-syntax literals in any comment across
the whole scanned tree — it is this new file's own
`claimCommentGuardCallSyntaxLiterals` ban-list constant
(`[]string{".ClaimDue(", ".OrphanSweep(", ".SelectDue("}`), a CODE-level
string literal on one source line, textually indistinguishable from a real
call site to that harness's grep but not to `go/ast`. This is the identical,
already-accepted inflation shape `claim_kinds_call_site_guard_test.go`'s own
raw-string test fixtures produce, documented in that file's own
`claimKindsGuardMinPlausibleCallSiteCount` doc comment ("the error direction
is inflation, which only loosens the floor's upper bound and never tightens
it"). Both floors (5 and 8) remain far below the real population; nothing
is under-protected.

### Second implementation pass (bounce 1 fix), 2026-08-31

Transcribed the first review's four remedies (B1–B4), verifying each before
and after applying it, in a scratch scan against a real historical checkout
rather than trusting the review's numbers unread.

```
uptime                                                              # load averages 1.65/1.33/1.45 across the pass — not saturated
```

**B1 (directional axis reproduction).** Added a temporary in-package test
reading `/tmp/prefix_outbox_worker.go` (a `git show db9bff7:...` checkout)
and the real `internal/mailing/outbox_worker.go`, calling
`findClaimCommentGuardFindings` directly — deleted before this commit, not
part of the tracked tree. Before the fix: 0 findings on the pre-fix file
(reproducing the review's claim exactly). After applying all three of B1's
changes: `total findings on pre-fix db9bff7 file: 2` — `[directional]
outbox_worker.go:165 ident=ClaimDue` and `[directional]
outbox_worker.go:471 ident=ClaimDue`, matching the review's remedy claim
byte-for-byte down to the line numbers; `total findings on HEAD
outbox_worker.go: 0`.

**B1 part 4 (falsifiability test).** Building its own fixtures with real
call syntax for the two OTHER identifiers each site names (needed so the
directional axis's nearest-vs-every-identifier fix is actually exercised)
inflated `scripts/go_file_visit_floor_guard_test.sh`'s external population
by 3 (45 vs. the target 42) — a receiver-qualified call
(`w.store.OrphanSweep(...)`) contains the same `.OrphanSweep(` shape the
harness bans, textually indistinguishable from a real site to that
harness even though `go/ast` never treats it as one. Rewrote the fixtures'
calls as bare package-level functions instead
(`OrphanSweep(0, mailKinds)`) — the AST-based guard treats a bare call
identically for census purposes, but it contains no leading `.` for the
harness's grep to match, avoiding the inflation entirely rather than
accepting it as a second "unavoidable" site.

**Self-inflicted regression, found and fixed mid-pass.** The new doc
comments' own prose — quoting the real defect sentences ("below is scoped
to", "OrphanSweep... below") as illustrative examples — tripped this
guard's own two tests against itself: `TestNoCommentClaimsClaimMachineryCallFileCodeLacks`
flagged 5 directional findings inside this very file, and separately one
draft of the explanation for why bare calls were chosen literally spelled
out `.OrphanSweep(` / `.SelectDue(` as example substrings, which
`TestNoClaimMachineryCallSyntaxLiteralInAnyComment` correctly flagged.
Both were rephrased (synonyms for "above"/"below", describing the banned
shape instead of spelling it) without changing what either explains;
re-ran `go test ./internal/outbox/...` after each rephrasing until clean.

**B2 (population).** `scripts/go_file_visit_floor_guard_test.sh` re-run
after assembling `claimCommentGuardCallSyntaxLiterals`: population back to
exactly **42 total / 12 non-exempt**, both `PASS`.

**B3 (floors).** Added `claimCommentGuardMinPlausibleFileCount` and
`claimCommentGuardMinPlausibleCommentGroupCount` to
`scripts/go_file_visit_floor_guard_test.sh`'s new "outbox
claim-comment-referent guard floors (#0347 B3)" section, mirroring the
existing claim-kinds floor section exactly (`extract_const`, `sha_of`,
`outbox_floor_plausible`, mutate-a-copy-to-`0`). Left
`claimCommentGuardMinPlausibleCallCensusCount` where it was — it already
has a genuine in-package oracle
(`TestClaimCommentGuardCensusFloorCatchesRootsWithNoRealPopulation`, which
narrows scan roots rather than the floor itself, so `go test` can actually
falsify it) — and amended the guard file's own "Where the oracle lives"
doc comment to say so explicitly, so a reader does not conclude the whole
file is self-sufficient.

**B4 (byte-for-byte).** Restored the missing leading clause ("The whole
point of this test is that id2 is deliberately left") to
`claimCommentGuardFixedFixtureSrc`, extracted from the real
`internal/mailing/outbox_worker_test.go`, not retyped. Verified by a
Python byte comparison (not by reading): the fixture's embedded comment
now matches the real file's comment exactly, including the leading tab
and every em dash. Re-verified the stale (`ffef8cf`) fixture the same way
— unaffected by this pass, still byte-for-byte.

```
go build ./...                                                      # clean
go vet ./...                                                        # clean
gofmt -l internal/outbox/claim_comment_referent_guard_test.go        # clean
sha256(file) == sha256(gofmt stdout)                                 # equal
go test ./internal/outbox/... -run 'ClaimComment|GoFileVisitFloorImplausible|NoCommentClaims|NoClaimMachinery' -v
  # all PASS, including the new TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7
ISSUE=0347b scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
  # internal/outbox ok 0.657s, internal/mailing ok 3.593s, internal/handlers ok 7.229s
  # zero skips, VERIFICATION PASSED
scripts/check.sh guards
  # all five harnesses PASS, including the two new #0347 B3 floor entries
  # (claimCommentGuardMinPlausibleFileCount=150 vs. population 270;
  # claimCommentGuardMinPlausibleCommentGroupCount=500 vs. population 30478;
  # both correctly judged IMPLAUSIBLE when mutated to 0)
  # VERIFICATION PASSED
```

Port `:5173` confirmed free via `lsof -nP -iTCP:5173 -sTCP:LISTEN` before
`scripts/check.sh guards`; no `pkill -f` used. `scripts/testdb.sh drop
0347b` reported the database already gone (cleaned up by `check.sh` on
exit). No leftover `go test`/`.test` processes at the end of the pass.

Disagreement with the review, recorded rather than silently followed:
none found. All four remedies (B1–B4) verified correct as specified; the
only addition beyond transcription was rewriting the new falsifiability
fixtures' call syntax (bare functions instead of receiver-qualified calls)
once it became clear the qualified form the review's own snippets implied
would reintroduce a scaled-down version of the exact B2 defect — a
consequence of adding the required B1-part-4 test that the review's B2
section (written before B1 part 4 was implemented) could not have
anticipated.

## Files changed

- `internal/outbox/claim_comment_referent_guard_test.go` — new file, 929
  lines. Two guard tests plus their shared walk/census infrastructure, two
  falsifiability-fixture tests, one negative-fixture regression test, and
  three fail-closed floor proofs.

### Second implementation pass (bounce 1 fix)

- `internal/outbox/claim_comment_referent_guard_test.go` — B1: added
  `claimCommentGuardDirHistoricalRe`, `claimCommentGuardOpenParen`, and
  rewrote the directional axis's window/matching loop (every distinct
  identifier, not only the nearest); added
  `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`
  plus its two fixtures. B2: `claimCommentGuardCallSyntaxLiterals` now
  assembles its three literals instead of writing them whole. B4: restored
  `claimCommentGuardFixedFixtureSrc`'s missing leading clause. Also
  amended the top-of-file "historical-marker convention" and "Where the
  oracle lives" doc comments to describe the two-marker-set split (B1) and
  the findings-vs-floors oracle distinction (B3).
- `scripts/go_file_visit_floor_guard_test.sh` — B3: added
  `claimCommentGuardMinPlausibleFileCount` and
  `claimCommentGuardMinPlausibleCommentGroupCount` to a new "outbox
  claim-comment-referent guard floors (#0347 B3)" section, mirroring the
  existing claim-kinds floor section.

## Gotchas

- **Getting to zero false positives on the real tree took four iterations
  measured against real code, not reasoned in the abstract.** A first
  design (bare identifier + "call"/"calls" adjacency, whole-comment-group
  historical exemption, every identifier in a fixed window for the
  directional axis) produced 17 findings on `HEAD` and failed its own
  falsifiability fixture in the opposite direction (it silently exempted
  the required positive). Each fix was driven by running the guard against
  the real tree and reading what it actually flagged, not by re-deriving
  the rule on paper — consistent with `CLAUDE.md` §8's repeated lesson this
  session (`#0342`'s own tools) that an instrument's first draft reliably
  has a bug its author cannot see by inspection alone.
- **This file is itself scanned by the citation-guard family in
  `internal/handlers`.** Two of my own draft doc comments tripped
  `TestNoCommentCitesUndefinedTestFunction` (a doc comment named a test
  function I had planned to write but never did) and
  `TestNoCommentCitesUnresolvedPathOrSection` (two doc comments cited
  `r3-0342/audit0342r3.go` and `r3-0342/commentscan0342r3.go` by path —
  real scratchpad files, but not part of this repo tree). Both were caught
  by `ISSUE=0347 scripts/check.sh go ...`, not by this file's own tests,
  which is exactly the "two homes, not exclusive" point CLAUDE.md §8 makes
  about guard families protecting each other.
- **Package #0356's plan asked whether this guard's walker is reusable.**
  Partially. `walkClaimKindsGuardFiles` (this package's existing walk
  helper, from `claim_kinds_call_site_guard_test.go`) is reused as-is, not
  duplicated. The census-building helpers
  (`claimCommentGuardBuildFileFacts`, `claimCommentGuardBuildPackageTypeIndex`)
  are new and specific to this guard's need for a per-file code census and a
  cross-file package-type index — #0356's own guard needs neither (its plan
  says so explicitly: "no code census at all"), so there is nothing further
  for it to share beyond the walk, which was already shared. If #0356 lands
  in this same package it can call `walkClaimKindsGuardFiles` directly; if
  it lands in `internal/handlers` (where the other citation guards already
  live), it will use that package's own `walkGoFiles`/`goFileVisitCountImplausible`
  pair instead, and the two guard families stay independent by design (per
  #0356's own plan §8, since their exemption models are incompatible).

## Review notes

> 🔴 **Bounced 2026-08-31.** The existence axis is sound and well calibrated, but
> the **directional axis catches neither of the two real defects it was added
> for**, and the guard avoidably inflated the population `#0323`/criterion 7
> exist to protect. Both remedies are specified below precisely enough to be
> transcription; both were proved working by this review before being written.

### What passed independent re-verification

- `gofmt -l internal/outbox/` clean; file hash equals `gofmt` stdout hash
  (`788d78c6…5b770`) — formatting stable.
- `ISSUE=0347r scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` → `VERIFICATION PASSED`, zero skips, `internal/outbox`
  ok 0.865s, `internal/mailing` ok 3.791s, `internal/handlers` ok 7.277s.
- `scripts/check.sh guards` → all five harnesses, `0 failures`,
  `VERIFICATION PASSED`. Port `:5173` confirmed free by `lsof -nP -iTCP:5173
  -sTCP:LISTEN` first; no `pkill -f` used.
- **Existence axis, attacked directly and correctly calibrated.** Against
  constructed fixtures run through `findClaimCommentGuardFindings` in a
  throwaway `git archive` export (§8b — the tracked tree was never mutated): a
  same-package `Type.Ident` + "call" with a zero code census **is** flagged, and
  so is a reconstruction of the real `ffef8cf` shape. Five correct-but-similar
  shapes are all correctly silent — an external package's qualifier, a
  same-package `Type.Ident` with no "call" word, a bare mention followed by
  "call", a clause carrying a historical marker, and a file that really does
  make the call.
- **Negative fixture re-run and clean.** `internal/handlers/subscribe_intake_test.go`
  produces zero existence-axis findings, with its nine `ClaimDue` mentions
  confirmed present. Criterion 1's corrected framing is correctly encoded.
- **Criterion 4's stale fixture is byte-exact.** Verified by extraction, not by
  reading: `git show ffef8cf:internal/mailing/outbox_worker_test.go` piped
  through `python3` and compared as bytes against
  `claimCommentGuardStaleFixtureSrc` — identical, U+2014 (`e2 80 94`) included.
  The implementer's warning about retyping punctuation was heeded correctly.
- Criterion 6's in-Go placement reasoning **is** recorded in the doc comment,
  and it is right for the findings assertion: adding a false comment moves what
  the scan finds, so the in-package test is a non-circular oracle.
- Criterion 2 holds: `claimCommentGuardBuildFileFacts` parses with mode `0` and
  additionally *asserts* `len(f.Comments) == 0`, so comment text cannot reach
  the census. That belt-and-braces check is a genuinely good touch.

### B1 — BLOCKING: the directional axis misses both sites it was added for

The amendment added this axis for exactly two measured defects — `#0342` sites
**A** (`outbox_worker.go:169`) and **C** (`:471`). Pointed at the real pre-fix
file, the axis reports **zero findings**:

```
git show db9bff7:internal/mailing/outbox_worker.go > /tmp/prefix.go
# findClaimCommentGuardFindings("outbox_worker.go", <that source>, realPkgTypes)
findings on PRE-FIX outbox_worker.go: 0
```

Instrumenting the axis shows two independent causes, either sufficient alone:

1. **`claimCommentGuardHistoricalRe` contains `never` and `cannot`**, which are
   ordinary present-tense behavioural modals, not historical markers. Both
   sites' clauses contain one:
   - site A clause: `"ClaimDue's and OrphanSweep's kinds filters in pass, below,
     so this worker **never** claims OR sweeps a row it cannot render"`
   - site C clause: `"scoped to mailKinds, the same set ClaimDue below is scoped
     to, so this sweep can **never** release a live claim…"`

   This is the same over-broad-exemption defect the `## Fix` section says an
   earlier draft was caught on with `"rather than"` — reappearing at clause
   scope under a different word. Criterion 3's convention is about *history*;
   importing negation and modality into it disarms the axis on ordinary prose.

2. **Nearest-identifier-only masks site A.** The nearest target to `below` is
   `OrphanSweep`, which *does* occur below (`:477`), so the axis resolves and
   goes silent — while the false claim is about `ClaimDue`, which has **zero**
   occurrences anywhere in that file (`identOffsets["ClaimDue"] = []`). This is
   the mirror image of the bug this issue explicitly warned about: `#0342`'s own
   tool "let an earlier word consume the `below` and hide a real site". Here a
   *nearer* identifier consumes it and hides a *farther* real error.

**And the axis has no falsifiability proof at all.** `TestClaimCommentGuardFlagsKnownStaleSiteFromFfef8cf`
proves only the *existence* axis. Nothing in the file demonstrates the
directional axis ever firing on a real historical defect — which is precisely
how it reached zero-on-`HEAD` without anyone noticing it detects almost nothing.
Constructed probes confirm the breadth of the gap: an em dash, an ASCII `--`, a
colon, or >90 characters between the identifier and the direction word each hide
a real error, and the em dash is this codebase's most common clause separator.

**Remedy (all three proved by this review: clean on `HEAD`, both sites caught).**

1. Add a directional-only marker set, dropping the negating/modal words. Keep
   `claimCommentGuardHistoricalRe` exactly as it is for the existence axis (its
   negation words are load-bearing there — several of the nine negative-fixture
   mentions rely on them):

   ```go
   claimCommentGuardDirHistoricalRe = regexp.MustCompile(`(?i)\b(before #|since #|no longer|used to|formerly|the old |at the time|was removed|removed by|replaced|predates|pre-#|historical|reverting|revert(ed)?)\b`)
   ```

   and use it — only in the directional axis — in place of
   `claimCommentGuardHistoricalRe`.

2. In the directional axis, check **every distinct target identifier in the
   window**, not only `matches[len(matches)-1]`. Loop over `matches` with a
   `seen map[string]bool`, keeping the existing resolve-and-report body per
   identifier.

3. Change (2) alone reintroduces one real false positive —
   `internal/outbox/store.go:463`, `"ClaimRow is the second half of #0297's
   per-row claim path (see SelectDue above)"`, where the direction word sits in
   a parenthetical about `SelectDue` but the sentence subject `ClaimRow` gets
   pulled in. Fix it by scoping the window to the innermost still-open
   parenthesis when the direction word is inside one:

   ```go
   // claimCommentGuardOpenParen returns the byte offset just after the
   // innermost still-open "(" in s, or -1 if none is open.
   func claimCommentGuardOpenParen(s string) int {
   	depth := 0
   	for i := len(s) - 1; i >= 0; i-- {
   		switch s[i] {
   		case ')':
   			depth++
   		case '(':
   			if depth == 0 {
   				return i + 1
   			}
   			depth--
   		}
   	}
   	return -1
   }
   ```

   and in the window computation, after the existing `claimCommentGuardLastBoundary`
   clamp:

   ```go
   if pOpen := claimCommentGuardOpenParen(flat[:dm[0]]); pOpen > lo {
   	lo = pOpen
   }
   ```

4. Add a standing directional falsifiability regression test mirroring
   `TestClaimCommentGuardFlagsKnownStaleSiteFromFfef8cf`, built from
   `git show db9bff7:internal/mailing/outbox_worker.go` (sites A and C) — the
   pre-fix text extracted, not retyped — asserting both are flagged and that
   `HEAD`'s wording is not. Without this the axis can silently regress again.

With 1–3 applied: `go test ./internal/outbox/` green on `HEAD` (zero findings),
and the pre-fix file yields exactly the two expected findings —
`line 165 ident=ClaimDue` and `line 471 ident=ClaimDue`.

### B2 — BLOCKING: criterion 7 / `#0323` — the guard inflated the population it measures

Independently measured, not taken from the draft: the harness's grep population
moved **42 → 43** (`3e43c30` → `2dad4da`), and `scripts/check.sh guards` now
prints "externally-measured (grep-based) population of 43". The sole cause is
**line 151**, `claimCommentGuardCallSyntaxLiterals` — the three banned literals
written contiguously on one source line. The implementer's diagnosis is exactly
right (it is code, not comment; `grep -c` counts lines, hence +1 not +3), and
`TestNoClaimMachineryCallSyntaxLiteralInAnyComment` genuinely does prove zero
banned literals in any comment.

**But the "already-accepted inflation" precedent does not extend to this case,
and the codebase says so in as many words.** The sibling's six inflating sites
are *unavoidable*: they are Go-source fixtures that must contain real call
syntax for `go/ast` to parse them as calls. This one is *trivially avoidable* —
nothing requires those bytes to be contiguous. Decisively,
`claim_kinds_call_site_guard_test.go`'s own
`claimKindsGuardMinPlausibleCallSiteCount` doc comment records a deliberate
decision not to do this:

> "Deliberately not written as literal guarded-call-syntax prose in THIS
> paragraph … — doing so would **make this paragraph a seventh divergent site of
> the exact class it describes**."

`#0347` has now created that seventh divergent site. Two doc comments are false
as a result: that paragraph's "The six extras all sit inside THIS file", and
`scripts/go_file_visit_floor_guard_test.sh`'s "this function measures 42". An
issue whose entire subject is comments that are false about the code should not
land by making two more of them.

Criterion 7's literal prohibition ("into any Go **comment**") is satisfied; its
instruction ("re-run … and record the population") was followed honestly. The
violation is of `#0323`'s principle and criterion 2's self-referential-population
hazard, which is what both criteria exist to serve.

**Remedy — make the bytes unmatchable by the harness's grep, not raise any
floor.** Assemble the literals instead of writing them whole, so the runtime
values are unchanged:

```go
// Assembled rather than written as contiguous literals on purpose: the
// external harness (scripts/go_file_visit_floor_guard_test.sh) counts this
// syntax textually and cannot tell code from a real call site, so writing
// them whole would add a seventh divergent site to the population that
// bounds claimKindsGuardMinPlausibleCallSiteCount -- exactly what
// claim_kinds_call_site_guard_test.go's own doc comment deliberately
// declined to do (#0323). Do not "simplify" this back.
var claimCommentGuardCallSyntaxLiterals = []string{
	"." + "ClaimDue" + "(",
	"." + "OrphanSweep" + "(",
	"." + "SelectDue" + "(",
}
```

Then re-run `scripts/check.sh guards` and confirm the printed population is back
to **42** and the non-exempt population is **12**. With that, both existing doc
comments become true again and no doc updates are needed anywhere else.

### B3 — Two of the three fail-closed floors are themselves fail-open

Criterion 5's letter is met (empty roots and a zero-hit census both fail), but
"fails closed by construction" does not hold. Mutating each constant to `0` on a
private copy (§8b — the tracked tree was never touched):

| Constant mutated to `0` | `go test ./internal/outbox/` |
|---|---|
| `claimCommentGuardMinPlausibleFileCount` | **still passes** |
| `claimCommentGuardMinPlausibleCallCensusCount` | fails (caught by `TestClaimCommentGuardCensusFloorCatchesRootsWithNoRealPopulation`) |
| `claimCommentGuardMinPlausibleCommentGroupCount` | **still passes** |

None of the three is pinned by any external harness. This is precisely §8's
direction-of-comparison rule: a *floor* mutated *downward* is not falsifiable by
the thing it measures (`got < 0` can never fire), so its proof belongs in
`scripts/go_file_visit_floor_guard_test.sh` — which already does exactly this
for `claimKindsGuardMinPlausibleCallSiteCount`,
`claimKindsGuardMinPlausibleNonExemptCallSiteCount`, and
`citedTestScanRootsMinPlausibleFileCount`.

`TestGoFileVisitFloorImplausibleGuardFiresOnEmptyOrLowCount` proves
`claimCommentGuardFloorImplausible` *works*; it cannot prove the constants are
non-zero, and its doc comment's claim that its numbers are chosen so a change to
the floor constant "cannot make this test agree with itself" is true but
addresses a different risk than the one that matters here.

**Remedy.** Add `claimCommentGuardMinPlausibleFileCount` and
`claimCommentGuardMinPlausibleCommentGroupCount` to
`scripts/go_file_visit_floor_guard_test.sh` alongside the existing outbox
floors, with an externally measured population for each (a `find`-based `.go`
file count under `internal/` + `cmd/` for the first; a `grep -c` count of
comment-opening lines, or simply a fixed plausibility bound, for the second) and
the same mutated-copy `=0` check the other entries already use. Also amend this
file's doc comment to distinguish the two comparison directions: the *findings*
assertion is correctly in Go (criterion 6), while the *floors* are not, and
saying so is what stops the next reader concluding the whole file is
self-sufficient.

### B4 — The "byte-for-byte" claim on the fixed fixture is false

`claimCommentGuardFixedFixtureSrc`'s doc comment says it "reproduces the exact
post-fix comment from HEAD's outbox_worker_test.go, **byte-for-byte**". Verified
by extraction: it is a strict *suffix* of the real comment, missing the leading
`"The whole point of this test is that id2 is deliberately left "`. Materially
harmless — the dropped clause contains no target identifier and no direction
word, and both axes reach the same verdict either way — but it is the `#0330`
class exactly, and it sits in a guard whose subject is comments that claim more
than the code delivers. Either restore the missing leading clause (preferred —
it costs one line and makes the claim true) or soften the wording to name what
was dropped and why. The stale fixture's identical claim **is** true and needs
no change. The same overclaim appears in `## Fix` above and should be corrected
there too.

### Not blocking — for the orchestrator, not this issue

- **`#0356` can proceed, but its plan's §8 sequencing rationale is now stale.**
  The plan expected `#0347` to leave a shared census helper in the
  `internal/handlers` citation-guard family. It landed in `internal/outbox`
  instead and reuses `walkClaimKindsGuardFiles`, which **predates** it (it comes
  from `claim_kinds_call_site_guard_test.go`), so `#0347` contributed no new
  shared infrastructure for `#0356` to build on. The plan's "land it first,
  its census helper is the shared piece" is moot — `#0356` needs no census by
  its own account — and its concurrency warning that "both edit
  `internal/handlers`" no longer holds, since `#0347` does not touch that
  package at all. `#0356` should take the walk + floor discipline from whichever
  family it lands in and treat `#0347` as neither a blocker nor a dependency.
  The implementer's `## Gotchas` entry describes this accurately.
- The directional axis has a live false-positive surface worth knowing about:
  ordinary prose such as "ClaimDue semantics matter, and the table below
  explains why" flags, because the direction word need not refer to the named
  identifier at all. It does not fire on `HEAD` today and the B1 remedy does not
  worsen it, but it is the likeliest source of a future spurious build break.

## Relation

- Found by: [#0342](0342.md)'s second review, which built the instrument and
  recommended keeping it
- Depends on: [#0342](0342.md) — whose third pass fixes the three sites this
  would have caught; land that first
- Relates to: [#0297](0297.md) — whose change made the comments false
- Relates to: [#0267](0267.md) — the guard family that does not reach this class
- Relates to: [#0303](0303.md) — which removed the variadic `ClaimDue` form one
  of the stale comments still shows
- Relates to: [#0323](0323.md), [#0345](0345.md) — the self-referential
  population hazard criterion 2 guards against

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0342`'s second review (§9).
- 2026-08-31 — Amended by the orchestrator from `#0342`'s third review, which
  resolved that issue: criterion 1's present-tense framing would fire on nine
  correct comments in one file and must be rewritten as "names a call this
  file's code makes"; a directional axis is worth adding, with its
  high-precision window; and the class this guard cannot reach is recorded so
  its greenness is not over-read.
- 2026-08-31 — Implementation (Sonnet, fresh subagent): added
  `internal/outbox/claim_comment_referent_guard_test.go` (commit `2dad4da`).
  See `## Root cause`, `## Fix`, `## Verification`, `## Gotchas` above for
  the full account, including four iterations of false-positive/
  false-negative fixes driven by running the guard against the real tree.
  Token usage/cost — orchestrator to record (a subagent cannot measure its
  own totals per this file's own convention).
- 2026-08-31 — Review (Opus, fresh subagent): **bounced**, first bounce. Verified
  independently: `gofmt` stable, `ISSUE=0347r scripts/check.sh go
  ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...` and
  `scripts/check.sh guards` both `VERIFICATION PASSED`; existence axis attacked
  with constructed positive and correct-but-similar fixtures and correctly
  calibrated; negative fixture clean; `ffef8cf` stale fixture confirmed
  byte-identical by extraction. Four defects found, two blocking — the
  directional axis catches neither of the two real sites it was added for, and
  the guard added a seventh divergent site to the population `#0323` protects.
  **§3 decision: no escalation** — this is bounce one, and the remedies above
  are transcription: every one of the three directional changes was implemented
  and proved by this review (clean on `HEAD`, both pre-fix sites caught), and
  the population fix is a mechanical rewrite of one declaration. Next pass stays
  on Sonnet. Token usage/cost — orchestrator to record.
- 2026-08-31 — Implementation, second pass (Sonnet, fresh subagent): applied
  the review's four remedies (B1–B4, commit `3918191`). Independently
  reproduced B1's core claim before applying the fix (0 findings on a real
  `db9bff7:internal/mailing/outbox_worker.go` checkout) and after (exactly
  `line 165 ident=ClaimDue` and `line 471 ident=ClaimDue`, matching the
  review's proof); added the required directional falsifiability test
  (B1 part 4); re-verified population at 42/12 after the B2 assembly fix;
  added the two missing floor entries to
  `scripts/go_file_visit_floor_guard_test.sh` (B3); restored the fixed
  fixture's missing leading clause and confirmed genuine byte-for-byte
  equality by Python comparison (B4). One deviation from the review's
  exact snippets, not a disagreement: the new directional falsifiability
  fixtures needed real call syntax the review had not itself written, and
  a receiver-qualified form of that call syntax would have re-inflated the
  same population B2 fixes — rewritten as bare package-level calls
  instead, which the AST-based guard treats identically. Also found and
  fixed a self-inflicted defect the review could not have anticipated: the
  new doc comments' own explanatory prose tripped this guard's two tests
  against itself, caught by running `go test` after each edit rather than
  assumed clean. See `## Verification`'s "Second implementation pass" and
  `## Files changed`'s matching subsection for the full account.
  `ISSUE=0347b scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` and `scripts/check.sh guards` both `VERIFICATION
  PASSED`, zero skips. Token usage/cost — orchestrator to record.
