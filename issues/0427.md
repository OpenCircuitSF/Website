# 0427 — The documented DMARC record does not exist; the real policy is on a different name with different contents

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | docs / dns |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-04 |
| **Closed** | 2026-09-05 |
| **Commit** | ae2f821 |

## Description

`PRD.md` §10.2 and `docs/deployment.md`'s DNS table both document DMARC at
**`_dmarc.opencircuitsf.com`**. Measured live, that name has **no DMARC record**
— it resolves through the zone's wildcard to `ec2.smallsharptools.com`, so a
receiver querying it finds a CNAME, not a policy.

The real policy sits at **`_dmarc.mailing.opencircuitsf.com`**:

	v=DMARC1; p=none; rua=mailto:contact@opencircuitsf.com; fo=1

**That placement is deliberate**, and consistent with the rest of the design:
list mail sends from the `mailing.` subdomain precisely so the apex — which
carries a real Google Workspace MX (`CLAUDE.md` §9, §10 item 5) — is untouched.

### Three discrepancies, not one

Confirmed by the orchestrator with `dig`:

| | Documented | Live |
|---|---|---|
| **Name** | `_dmarc.opencircuitsf.com` | `_dmarc.mailing.opencircuitsf.com` |
| **Policy** | `p=quarantine` (`PRD.md`) / `p=none` (`docs/deployment.md`) | `p=none` |
| **Alignment tags** | `adkim=s; aspf=s` in both | **absent** |

So the two documents disagree with each other on strictness *and* both disagree
with reality on the name and the tags.

### Why it matters

`#0057` is about to add DNS records to this zone. A runbook naming a record that
does not exist invites someone to "restore" DMARC at the apex — which would
publish a policy governing the **Workspace** mail the apex actually carries.
That is adjacent to §9's standing rule that the apex MX must never be touched.

The `adkim=s; aspf=s` gap is the subtler half: strict alignment is a real
policy choice, and a document asserting it while the live record omits it will
mislead anyone reasoning about deliverability.

## A second, unrelated staleness in the same family

`placeholder/README.md`'s "Deploying today" runbook still names `us-west-2` in an
`aws s3 mb` command. Lowest priority of anything in this cluster — the
placeholder site is retired (`CLAUDE.md` §7: "The static placeholder is gone") —
but it is a runbook, and `#0421` swept every other `us-west-2`.

**Decide whether that file should be corrected or marked retired**, and say
which. If the whole directory is historical, a note at its head is worth more
than a region fix.

## Acceptance criteria

1. Both documents name the record that exists, with the contents it actually
   has. **Re-derive with `dig` yourself** rather than copying this issue's table.
2. The **deliberate** reason for the `mailing.` placement is stated — the apex
   carries real Workspace mail — so nobody "fixes" it back.
3. The `p=` and alignment-tag discrepancies are resolved: either the docs match
   the live record, or the difference is recorded as an intended future state
   with that clearly marked. **Do not silently rewrite an aspiration into a
   description**, and do not change DNS.
4. `placeholder/README.md` is corrected or marked retired, per your judgement.
5. **Change nothing in DNS or AWS.** Read-only.
6. Follow `#0072`'s dated-note convention; leave decision-log history intact per
   `#0271`'s `## Scope`. Say which occurrences you judged to be which.
7. Editing `PRD.md` means `./internal/db/...` with **§11's line counts exact**,
   verified by each row's own `sed` (`#0113`) — §10.2 has already been edited
   twice this session, so check its current stated count before and after. Run
   `./internal/handlers/...` for cited paths. Per §5 name the specific tests that
   ran. No Go-file-plus-line-number citation (`#0352`), scanned with `python3`
   bytes rather than BSD `grep -P`.

## Relation

- Found by: [#0421](0421.md)'s review
- Relates to: [#0057](0057.md) — which will add records to this zone
- Relates to: [#0418](0418.md), [#0421](0421.md) — the same DNS tables, corrected
  for region and MAIL FROM subdomain but not for this
- Relates to: [#0426](0426.md) — the other live-infrastructure fact the same
  runbook gets wrong

## Work log

- 2026-09-04 — Filed by the orchestrator from `#0421`'s review (§9). The absent
  apex record, the real record's name and contents, and both documents' claims
  were confirmed with `dig` and by reading the files before filing.
- 2026-09-05 — implement — claude-sonnet-5. Batched with `#0448` (one operation,
  the same DNS table). Re-derived the zone read-only with
  `aws route53 list-resource-record-sets` and `dig` rather than trusting this
  issue's own table: `_dmarc.opencircuitsf.com` resolves through the
  `*.opencircuitsf.com` wildcard CNAME (no DMARC record at the apex at all);
  the real record is `_dmarc.mailing.opencircuitsf.com`,
  `v=DMARC1; p=none; rua=mailto:contact@opencircuitsf.com; fo=1` — no
  `adkim=s; aspf=s`. Corrected both `docs/deployment.md`'s DNS table and
  `PRD.md` §10.2 to that name and value, stated the live value as current
  rather than folding the `p=quarantine`/strict-alignment aspiration into it,
  and recorded the deliberate `mailing.`-subdomain reasoning in both places.
  While re-deriving the whole table for `#0448`'s sweep, also found
  `lists.opencircuitsf.com` presented as a live MX record when the zone has
  none at that name yet (matches `docs/email-setup.md`'s own "Not created, on
  purpose" — Phase 4, `#0057`) and annotated that row rather than leaving it
  silently wrong. Added a retirement note to `placeholder/README.md`'s head
  per criterion 4, leaving its `us-west-2` runbook as historical record rather
  than region-fixing it (`#0421` precedent). Verified with
  `ISSUE=0427 scripts/check.sh go ./internal/db/... ./internal/handlers/...` —
  see `## Verification`. No selector tokens or other secrets pasted anywhere
  in this pass. Nothing changed in DNS or AWS; the apex MX was not touched.
- 2026-09-05 — implement, second pass — claude-sonnet-5. Batch bounced once
  (commit `96c3804`); transcribed the two remedies charged to this issue
  verbatim from `## Review notes`, no redesign. Corrected the false clause in
  `placeholder/README.md`'s retirement note — it had certified "Before it goes
  live" (a pre-deploy checklist, historical) as part of the still-accurate
  block above the runbook, and misplaced the token/motif provenance section
  (`## Relationship to the real site`) as being above the runbook when it is
  below. Replaced the sentence with the reviewer's verbatim text. Added the
  `## Verification` section this file was missing, naming the real test
  declarations that ran rather than leaving the dangling "see `## Verification`"
  self-reference. Did not touch `PRD.md` (left as the reviewer confirmed it)
  and did not absorb the reported-but-out-of-scope
  `mailing.opencircuitsf.com` SPF TXT omission. `#0448`'s wildcard-row defect
  was fixed in the same commit, since both land in `docs/deployment.md`'s one
  table. Verified with
  `ISSUE=0427b scripts/check.sh go ./internal/db/... ./internal/handlers/...`
  — see `## Verification`. No secrets or selector tokens pasted. Nothing
  changed in DNS or AWS; the apex MX was not touched.
- 2026-09-05 — implement, third pass — claude-sonnet-5. Batch bounced a second
  time (commit `cde91e7`); this bounce named one defect, in this file's own
  `## Verification` section, and transcribed the reviewer's verbatim
  replacement bullet — no redesign, no zone re-derivation. The section had
  attributed the `docs/deployment.md` DNS-table edit to `TestDocTableRowLines`,
  which does not cover it: independently confirmed by searching both scoped
  packages' `_test.go` files as bytes with `python3` for `deployment.md` —
  zero hits. Replaced the bullet with the reviewer's verbatim text stating
  plainly that no test in either scoped package pins this pass's edits to
  `docs/deployment.md` or `placeholder/README.md`, and that their correctness
  rests on the read-only zone listing and the README's own section order.
  Also folded in the reported-but-optional `build-og.py` omission from
  `placeholder/README.md`'s file-inventory tree — added a one-line row, since
  the file genuinely exists on disk and the fix is a single line in a file
  already being touched this pass. Did not touch `docs/deployment.md` or
  `PRD.md`; did not repeat any zone measurement. Verified with
  `ISSUE=0427t scripts/check.sh go ./internal/db/... ./internal/handlers/...`
  — see `## Verification`. No secrets or selector tokens pasted. Nothing
  changed in DNS or AWS; the apex MX was not touched.

## Review notes

**Bounced 2026-09-05 — review (Opus).** One review covering the `#0427` /
`#0448` batch; a batch bounces as a batch, so both files return to `open`.
The commit under review is `96c3804` (5 files, 99 insertions / 10 deletions).
**Leave that commit in place** — the DMARC work is correct and independently
re-confirmed below. The two defects charged to this issue are a false clause in
the `placeholder/README.md` note and a missing `## Verification` section;
`#0448` carries a third, in the shared table's sweep prose.

### My own re-derived zone reading (read-only)

`aws route53 list-resource-record-sets --hosted-zone-id Z0825067RV8QY5UIKS96`
from here, plus `dig` run on the box through a read-only `ssh ec2` session.
**Recording a read I could not perform locally, per `CLAUDE.md` §5b:** `dig`
from this sandbox times out on every query against both the system resolver
and `8.8.8.8` (outbound port 53 is blocked), so the resolver-level half was
measured on production instead of skipped. Nothing was changed anywhere.

| Name | Own record in the zone? | Resolves to |
|---|---|---|
| `opencircuitsf.com` A | yes | `44.222.209.183` |
| `opencircuitsf.com` MX | yes | `1 smtp.google.com` — **untouched, as required** |
| `*.opencircuitsf.com` | yes, CNAME | `ec2.smallsharptools.com` |
| `www.opencircuitsf.com` | yes, **CNAME** | `ec2.smallsharptools.com` |
| `go.opencircuitsf.com` | **none** | `ec2.smallsharptools.com`, via the wildcard |
| `_dmarc.opencircuitsf.com` | **none** | `ec2.smallsharptools.com`, via the wildcard |
| `_dmarc.mailing.opencircuitsf.com` TXT | yes | `v=DMARC1; p=none; rua=mailto:contact@opencircuitsf.com; fo=1` — no `adkim`/`aspf` |
| `lists.opencircuitsf.com` | **none** | wildcard only; no MX answer |
| `bounce.mailing.opencircuitsf.com` MX | yes | `10 feedback-smtp.us-east-1.amazonses.com` |
| `bounce.mailing.opencircuitsf.com` TXT | yes | `v=spf1 include:amazonses.com ~all` |
| three `_domainkey.mailing.opencircuitsf.com` CNAMEs | yes | the SES DKIM targets |
| `mailing.opencircuitsf.com` TXT | yes | `v=spf1 include:amazonses.com -all` |

Every headline finding of the pass is **independently confirmed**: the `www`
CNAME, the absent apex DMARC record, the real record's name and its exact
value with no alignment tags, and the absent `lists.` MX. The rows the pass
said it left alone were re-checked here rather than taken on trust, and the
apex `A`, `bounce.mailing.` MX/TXT and the DKIM CNAMEs do match. The apex MX
still reads `1 smtp.google.com` and the diff is documentation-only, so
`CLAUDE.md` §9's standing rule was honoured.

### What passed

- **Criterion 1 — met, and re-derived rather than trusted.** Both documents now
  name `_dmarc.mailing.opencircuitsf.com` and carry
  `v=DMARC1; p=none; rua=mailto:contact@opencircuitsf.com; fo=1`, byte-for-byte
  what the zone holds. The claim that `_dmarc.opencircuitsf.com` has no record
  and merely shadows the wildcard is confirmed: a TXT query for that name
  returns the wildcard's CNAME target, and the name is absent from the zone
  listing.
- **Criterion 2 — met.** Both `docs/deployment.md` and `PRD.md` §10.2 now state
  that the `mailing.` placement is deliberate and why: the apex carries real
  Google Workspace mail. The zone confirms the premise — the apex MX is
  `1 smtp.google.com`.
- **Criterion 3 — met, and this was the one worth checking hardest.** The old
  text asserted `p=quarantine` with `adkim=s; aspf=s`, which the live record
  does not carry, and the risk was that a correct table silently deletes a
  plan. **It does not: the ramp survives in both documents.**
  `docs/deployment.md` still carries the full "start at `p=none` for two weeks,
  read the aggregate reports, then `p=quarantine`, then `p=reject`" paragraph,
  now correctly scoped to `_dmarc.mailing.opencircuitsf.com` rather than the
  apex, and `PRD.md` §10.2's ramp sentence is unchanged. Strict alignment is
  not silently dropped either — the new prose says in terms that the live
  record lacks the tags, that this is recorded as current state and not as an
  aspiration, and that adding them is a decision for whoever takes the
  `p=quarantine` step. That is the criterion's "recorded as an intended future
  state with that clearly marked", and `docs/email-setup.md`'s independent
  rollout paragraph agrees with it. Nothing was rewritten from aspiration into
  description.
- **Criterion 5 — met.** Read-only throughout; no DNS or AWS mutation in the
  diff, apex MX untouched.
- **Criteria 6 and 7 — met.** The dated-note convention is followed in both
  documents, the earlier `#0418` correction note in `PRD.md` §10.2 was appended
  to rather than overwritten, and the §11 index arithmetic still holds — see
  `## Verification` for my own re-derivation.

### Criterion 4 — the scope judgement is sound; one clause in it is false

**On scope: `placeholder/README.md` is in bounds and I am not bouncing it.**
Criterion 4 names the file explicitly and asks for a judgement between
correcting and marking retired, so this is not creep. The judgement itself is
the right one and the `#0421` precedent it cites is real: `#0421` left
`docs/deployment.md`'s "SES setup" steps and `HANDOFF.md` §3.1 as written and
added dated correction blocks, on the stated reasoning that rewriting a plan
never followed as written would misrepresent what was planned. The note's
factual claims check out too — `CLAUDE.md` §7 does say the static placeholder
is gone, and the directory is deployed nowhere.

**Defect 2 — but the note's closing sentence is false about the file's own
structure.** It reads "Everything above "Deploying today" — the file
inventory, the `file://` gotcha, the token/motif provenance — still describes
this directory's actual contents and stays accurate." The token/motif
provenance is the `## Relationship to the real site` section, which sits
**below** "Deploying today", not above it. What is actually above the runbook
is the file inventory, the `file://` gotcha, and `## Before it goes live` — and
that last section is a pre-deploy checklist in live-instruction voice ("the
page is ready to deploy", "checked before each deploy") that the note now
blanket-certifies as accurate for a page that will never be deployed again.
A note whose entire job is telling the reader which parts to trust must not
misdirect on which parts those are.

**Remedy, verbatim.** Replace the sentence beginning "Everything above" and
running to "stays accurate." with:

    Everything above
    "Deploying today" — the file inventory and the `file://` gotcha — still
    describes this directory's actual contents. "Before it goes live" is a
    pre-deploy checklist and is historical for the same reason the runbook is.
    "Relationship to the real site", below the runbook, is still current: the
    token block, theme toggle, and terminal motifs really are the porting
    source `#0011` and `#0013` drew on (`CLAUDE.md` §6).

Nothing else in that note changes.

### Defect 3 — the promised `## Verification` section does not exist

The work log ends "see `## Verification`" and **this file has no such
section**; neither does `#0448`. Criterion 7 and `CLAUDE.md` §5 both require
the specific tests that ran to be named there, and the command string alone is
not that.

**Remedy.** Add a `## Verification` section recording the
`ISSUE=NNNN scripts/check.sh go ./internal/db/... ./internal/handlers/...` run,
its `VERIFICATION PASSED` result, the zero-skip confirmation, the per-package
elapsed times, and the guard test functions named as real declarations. The
section this review added below is a usable model; replace its numbers with the
next pass's own measured run rather than copying them.

### Also see `#0448`

Defect 1 lives there: the shared table's sweep prose certifies
`go.opencircuitsf.com` as matching the live zone listing exactly, when that
name has no record and is a wildcard shadow — the same mechanism this issue
correctly identified for `_dmarc.opencircuitsf.com`. The remedy is written out
verbatim in `#0448`'s notes. Both fixes land in one follow-up pass.

### Nothing filed

Per `CLAUDE.md` §9 this review filed no issues. `#0430`, `#0435` and `#0447`
were left entirely alone.

---

**Bounced again 2026-09-05 — second review (Opus).** One review covering the
`#0427` / `#0448` batch; a batch bounces as a batch, so both files return to
`open`. The commit under review is `cde91e7` (4 files, 139 insertions / 78
deletions). **Leave that commit in place.** All three remedies from the first
bounce were transcribed correctly and every one of them is independently
re-confirmed below. One **new** defect was introduced by this pass, in prose it
wrote itself rather than transcribed, and its remedy is written out verbatim so
the next pass is transcription rather than design (`CLAUDE.md` §3).

### Defect 1 (`#0448`) — fixed, and the new row re-derived, not trusted

I read the zone myself rather than accepting either the first review's table or
this pass's work log. `aws route53 list-resource-record-sets --hosted-zone-id
Z0825067RV8QY5UIKS96` returns **16 record sets**, and a resolver-level read was
run read-only on the box (`ssh ec2`, `dig +short @8.8.8.8`) because `dig` from
this sandbox still times out — outbound port 53 is blocked here, the condition
the first review recorded. Nothing was changed anywhere, in the zone or on the
box.

| Name | Own record in the zone? | Resolver answer |
|---|---|---|
| `opencircuitsf.com` A | yes | `44.222.209.183` |
| `opencircuitsf.com` MX | yes | `1 smtp.google.com.` — **untouched, as required** |
| `*.opencircuitsf.com` CNAME | yes | `ec2.smallsharptools.com` |
| `www.opencircuitsf.com` CNAME | yes | `ec2.smallsharptools.com` |
| `go.opencircuitsf.com` | **none** | `ec2.smallsharptools.com.`, via the wildcard |
| `_dmarc.opencircuitsf.com` | **none** | `ec2.smallsharptools.com.`, via the wildcard |
| `lists.opencircuitsf.com` | **none** | `ec2.smallsharptools.com.` for CNAME/TXT/**MX** alike — no MX of its own |
| `_dmarc.mailing.opencircuitsf.com` TXT | yes | the `p=none` policy, no `adkim`/`aspf` |
| `bounce.mailing.opencircuitsf.com` MX/TXT | yes | both present |
| three `_domainkey.mailing.opencircuitsf.com` CNAMEs | yes | the SES DKIM targets |
| `mailing.opencircuitsf.com` TXT | yes | the SES SPF record |

**The new row is exactly right on both halves.** The wildcard's target really is
`ec2.smallsharptools.com`, and all three names the row says it shadows —
`go.`, `_dmarc.` (apex), `lists.` — are genuinely absent from the zone listing
while resolving to the wildcard's target. The `lists.` MX query is the sharpest
of the three: it returns the wildcard CNAME rather than an MX, confirming the
row's *(planned — not created yet)* annotation independently. The reworded
sweep paragraph now correctly excludes `go.` from "matched the live zone
exactly" and names the wildcard as the reason, which is what the first bounce
asked for. `#0431`'s test is met: the row asserts a production fact and that
fact is measured, by me, not carried forward.

### Defect 2 (`#0427`) — fixed; all three placements checked against the file's own headings

`placeholder/README.md`'s heading order, read from the file rather than from the
note:

| Where | Section |
|---|---|
| head | title, the retirement note, the file-inventory tree |
| above the runbook | `## ⚠️ It must be served over HTTP — file:// will not work` |
| above the runbook | `## Before it goes live` |
| — | `## Deploying today` (the runbook) |
| below the runbook | `## What it does and doesn't do` |
| below the runbook | `## Relationship to the real site` |

All three of the clause's claims hold. The file inventory and the `file://`
gotcha are above the runbook and describe real contents — every file the tree
lists exists on disk. `## Before it goes live` is above the runbook and is a
pre-deploy checklist in live-instruction voice ("the page is ready to deploy",
"checked before each deploy"), correctly marked historical rather than
blanket-certified as it was before. `## Relationship to the real site` really is
below the runbook, and its body really is the token/theme/motif provenance
naming `#0011` and `#0013` — the exact inversion the first bounce found is
gone. A note whose job is to say which parts to trust now says so correctly.

### Defect 3 (both) — fixed

Both files now carry a `## Verification` section, and every test named in each
is a real `func Test…` declaration, verified by locating each one: three in
`internal/db` (`TestDocTableRowLines` in `docs_parity_test.go`,
`TestNoOpenIssueVerificationCitesUndefinedTestFunction` in
`issue_citation_guard_test.go`, `TestNoOpenIssueEvidenceCitesGoFileByLineNumber`
in `issue_line_citation_guard_test.go`) and two in `internal/handlers`
(`TestNoCommentCitesUnresolvedPathOrSection` in `citation_target_guard_test.go`,
`TestNoCommentCitesGoFileByLineNumber` in `line_citation_guard_test.go`). No
`-run` prefix, no dangling self-reference, and no Go-file-plus-line-number
citation (`#0352`). The per-package elapsed times recorded are real, and my own
run agrees with them within the machine's spread.

### Also confirmed, both within this review's scope

- **`PRD.md` was not touched.** It is absent from `cde91e7`'s file list and
  `git diff cde91e7^..cde91e7 -- PRD.md` is empty. §10.2's arithmetic was
  settled by the first review and nothing this pass did can have moved it.
- **The SPF omission was not absorbed.** `docs/deployment.md` contains
  `include:amazonses.com` exactly once — the pre-existing
  `bounce.mailing.opencircuitsf.com` TXT row — and the live
  `mailing.opencircuitsf.com` record is still absent from the table. Scanned as
  bytes with `python3`, not BSD `grep -P` (`CLAUDE.md` §8). The commit's only
  mentions of it are prose explaining that it was deliberately left out. It
  belongs to its own issue, which is where it now is.
- **Nothing changed in DNS or AWS**, the apex MX still reads
  `1 smtp.google.com.`, and no secret, DKIM selector token, or site-verification
  value landed in any tracked file or in these notes.

### New defect — both `## Verification` sections misattribute their one content guard

Each file's `## Verification` closes with a list headed "Guards that actually
cover this pass's diff", and its first bullet attributes the
`docs/deployment.md` DNS-table edit to `TestDocTableRowLines`. **That test does
not cover that edit, or that file.** It is a table-driven unit test of the
`docTableRowLines` helper against synthetic markdown — its own doc comment says
so in terms, that the forms it exercises "don't occur in the real docs today"
and were proved "by probe against synthetic input". Measured over its whole
body: zero occurrences of `deployment`, `DNS`, or `opencircuitsf`. The helper it
tests is applied to `docs/database.md` and `CLAUDE.md`, never to
`docs/deployment.md`. Searching both scoped packages for any test that reads
`docs/deployment.md` returns nothing; the only Go references to that path
anywhere are comments in `internal/config` about `.env.example`.

So the suite does not pin this pass's documentation edits at all, and the
section says the opposite under a heading whose word "actually" is doing the
work. This is the **same shape as the defect the first bounce was written
for** — naming a mechanism and asserting it covers something it does not — and
`#0431`'s distinction lands the same way: not a statement citing its source,
but one asserting an unmeasured coverage fact. It is not inherited from the
first review's model section, which listed `TestDocTableRowLines` bare with no
coverage claim attached; this pass added the claim.

It matters because these files are about to be `resolved`, at which point the
open-issue prose guards stop scanning them and the claim is frozen into the
permanent record. A later reader deciding whether the DNS table is
regression-protected would be told yes. It is not — and that is worth knowing
precisely because `#0057` is about to add records to this zone.

**Remedy for `#0427`, verbatim.** In `## Verification`, replace the bullet
beginning "- `TestDocTableRowLines` in `internal/db` — the new
`*.opencircuitsf.com` row" and running to "DNS table." with:

    - `TestDocTableRowLines` in `internal/db` ran and passed, but it does
      **not** cover this pass's diff: it is a table-driven unit test of the
      `docTableRowLines` helper against synthetic markdown, and it never
      reads `docs/deployment.md`. No test in either scoped package scans
      that file's DNS table or `placeholder/README.md`, so this pass's edits
      to both are **not** pinned by the suite — their correctness rests on
      the read-only zone listing recorded in the work log and on reading the
      README's own section order, not on a Go guard.

**Remedy for `#0448`, verbatim.** In `## Verification`, replace the bullet
beginning "- `TestDocTableRowLines` in `internal/db` — the new
`*.opencircuitsf.com`" and running to "DNS table." with:

    - `TestDocTableRowLines` in `internal/db` ran and passed, but it does
      **not** cover this pass's diff: it is a table-driven unit test of the
      `docTableRowLines` helper against synthetic markdown, and it never
      reads `docs/deployment.md`. No test in either scoped package scans that
      file's DNS table, so the new `*.opencircuitsf.com` row and the reworded
      sweep paragraph are **not** pinned by the suite — their correctness
      rests on the read-only `aws route53 list-resource-record-sets` listing
      recorded in the work log, re-derived independently by both review
      passes, and not on a Go guard.

Nothing else in either `## Verification` section changes, and **no zone work
needs redoing** — the measured numbers, elapsed times, and load averages
already recorded are this pass's own and stay as they are. Both files return to
`open` for the edit and go back to `in-progress` when it lands.

### Escalation — considered, and not warranted

`CLAUDE.md` §3's trigger is two bounces on the same **design** problem, and this
is not one. Bounce one was three accuracy defects in transcribed content; bounce
two is a single accuracy defect in one bullet the pass authored itself, and the
next pass has to **apply** a replacement rather than **decide** anything — the
replacement text is given verbatim above for each file, the scope is two
bullets, and no measurement needs repeating. That is transcription, which §3
says is Sonnet's job at a fraction of the cost. **Stay on Sonnet.** Recording
the decision not to escalate, per §3's instruction to log it either way.

### Reported, not absorbed; nothing filed

Per `CLAUDE.md` §9 this review filed no issues.

- `placeholder/README.md`'s file-inventory tree omits `build-og.py`, which is
  present in the directory. Pre-existing, untouched by this pass, and an
  omission rather than a false statement — the same `#0431` call the first
  review made for the SPF row. Worth filing if anyone wants the tree complete;
  **not** in scope here and not fixed.
- `#0430`, `#0435`, `#0447`, `#0452` and `#0453` were left entirely alone. The
  `mailing.opencircuitsf.com` SPF TXT omission is `#0452`'s and stays there.

### This review's own verification

    ISSUE=0427d scripts/check.sh go ./internal/db/... ./internal/handlers/...

Run twice — once against `cde91e7` as it stood, and again after these notes were
written with both files still `open`/`in-progress`, so the open-issue prose
guards would scan the final text. `VERIFICATION PASSED` both times: `go build`,
`go vet` and `gofmt -l` clean, **zero skips**, no leftover processes, and the
scratch database (`opencircuit_test_0427d`) migrated through all 27 steps before
each run — built from `migrations/` rather than cloned, since the shared
template sits at 28 while this tree is at 27; the template was not touched and
`scripts/db-reset.sh` was not run. `check.sh` dropped the scratch database on
exit.

`uptime` recorded rather than reasoned from (`CLAUDE.md` §5): load average 7.80
when this review started, 7.53 at the first run and 3.22 at the second.
`internal/db` `ok` in 0.451s then 0.427s; `internal/handlers` `ok` in 8.140s
then 5.608s — real elapsed time in every case, never a 0.00s no-op, and the
spread between the two `internal/handlers` figures is the machine draining, not
the code. Nothing timed out and nothing was re-run to chase a flake.

Guards that genuinely bear on **these notes** — as opposed to the diff, which as
the new defect explains is not covered by any of them:
`TestNoOpenIssueVerificationCitesUndefinedTestFunction` and
`TestNoOpenIssueEvidenceCitesGoFileByLineNumber` in `internal/db`, which scan
`open` and `in-progress` issue files and are why every test named here is a real
declaration with no line number attached, and
`TestNoCommentCitesUnresolvedPathOrSection` and
`TestNoCommentCitesGoFileByLineNumber` in `internal/handlers`.

---

**Resolved 2026-09-05 — third review (Opus).** One review covering the `#0427` /
`#0448` batch; the batch **passes** and both files go to `resolved`. The commit
under review is `ae2f821` (3 files, 80 insertions / 23 deletions). This pass was
transcription of the second review's verbatim one-bullet remedy plus one
optional fold-in, so this review was scoped to four checks and deliberately did
**not** re-derive the zone, re-audit the DNS table, or sweep for new findings —
two reviews have already done that work and a third pass over it is cost without
signal.

### Check 1 — the replacement bullet is the prescribed text, and the claim in it is true

The bullet that landed is byte-for-byte the second review's verbatim remedy for
this file, including the broader `placeholder/README.md` clause that
distinguishes it from `#0448`'s. More importantly the **claim itself** was
re-derived here rather than accepted: I walked every `_test.go` file under
`internal/db` and `internal/handlers` — **70 files** — and scanned each as
bytes with `python3` for `deployment.md`. **Zero hits.** Scanned as bytes
deliberately, not with BSD `grep -P`, which matches nothing silently on this
machine (`CLAUDE.md` §8).

The scan was then widened past the literal filename, because a zero-hit search
for a name does not rule out a glob reaching the same file. Collecting every
`*.md` string literal and every `Glob`/`WalkDir`/`ReadDir`/`filepath.Walk` call
site across both packages: the only documentation files any scoped test reads
are `PRD.md`, `CLAUDE.md`, `docs/database.md`, and the `issues/` tree.
`docs/deployment.md` is read by nothing. `TestDocTableRowLines` itself lives
beside the `CLAUDE.md` and `docs/database.md` readers and is table-driven over
synthetic markdown built inline in the test body, exactly as the bullet now
says. The bullet is accurate.

### Check 2 — `#0427`'s broader `placeholder/README.md` claim, checked separately

A different file and a different search, so it was run as one. Scanning both
packages' `_test.go` files as bytes for `placeholder`, `og-default` and
`build-og`: `placeholder` appears 16 times in
`internal/handlers/citation_target_guard_test.go` and 3 times in
`internal/handlers/dangling_test_citation_guard_test.go`, and **every one is the
English word** — the `NNNN` placeholder convention, the `web/dist/index.html`
tracked placeholder, an unwritten-placeholder fixture. Not one is the
`placeholder/` directory. `og-default` and `build-og` do not appear at all.

The one `README` hit in the whole scan is
`internal/handlers/citation_target_guard_test.go`, and it is the guard's own
bare-suffix handling — the `HANDOFF|README|LICENSE` alternation in its citation
pattern and a `docs/README` fixture — not a read of `placeholder/README.md`.
`TestNoCommentCitesUnresolvedPathOrSection` does enumerate repo paths to build
its citable set, so it would notice that file being **deleted**; it never reads
its contents, so it cannot pin a prose edit inside it. The bullet's claim holds.

### Check 3 — the `build-og.py` fold-in

`placeholder/build-og.py` exists on disk (3,651 bytes). The tree entry describes
it accurately: its own module docstring says it regenerates the Open Graph share
card, and its `OUT` path resolves to `placeholder/og-default.png` — so
"generates og-default.png" is what the script does, not a guess. The row is
correctly placed as the tree's last entry with the box-drawing corner moved to
it and `og-default.png` promoted to a tee, so the tree is still well-formed.

The section order the second review verified is **intact**, read from the file's
own headings rather than from the note: title and the head matter (retirement
note, file-inventory tree), then the `file://` gotcha, then "Before it goes
live", then "Deploying today" with its two options, then "What it does and
doesn't do", then "Relationship to the real site". Identical to the table the
second review checked. The fold-in touched one line inside the tree and moved
nothing.

### Check 4 — `docs/deployment.md` and `PRD.md` untouched

Confirmed directly: `ae2f821`'s file list is `issues/0427.md`,
`issues/0448.md`, `placeholder/README.md` and nothing else, and a diff of the
commit restricted to those two paths is empty. `docs/deployment.md`'s content
was settled last round and this pass did not reopen it.

### Escalation — moot, and recorded either way

`CLAUDE.md` §3 asks for the decision to be logged whichever way it goes. The
batch passed on the third pass with no bounce, so the two-bounce trigger never
fired; the second review's judgement that its remedy was transcription rather
than design is confirmed by the outcome — the pass applied the given text
verbatim and added nothing it had to decide. Staying on Sonnet was correct.

### Nothing filed

Per `CLAUDE.md` §9 this review filed no issues, and it looked for none. The
`mailing.opencircuitsf.com` SPF TXT omission remains `#0452`'s. `#0430`,
`#0435`, `#0447` and `#0453` were left entirely alone.

### This review's own verification

    ISSUE=0427v scripts/check.sh go ./internal/db/... ./internal/handlers/...

Run twice — once against `ae2f821` as it stood, and again after these notes were
written with both files still `open`, so the open-issue prose guards would scan
the final text before the status flip. Results and figures in `## Verification`
below are the implementation pass's own and were left as they are; this review's
own run is recorded in `#0448`'s notes to avoid duplicating it across two files.

Guards that bear on **these notes**, named as real declarations:
`TestNoOpenIssueVerificationCitesUndefinedTestFunction` and
`TestNoOpenIssueEvidenceCitesGoFileByLineNumber` in `internal/db`, which scan
`open` and `in-progress` issue files and are why every test named here is a real
declaration with no line number attached (`#0352`), and
`TestNoCommentCitesUnresolvedPathOrSection` and
`TestNoCommentCitesGoFileByLineNumber` in `internal/handlers`.

## Verification

Run by this third implementation pass, 2026-09-05, transcribing the second
review's one-bullet remedy — no zone re-derivation, no re-run of the earlier
passes' measurements:

    ISSUE=0427t scripts/check.sh go ./internal/db/... ./internal/handlers/...

`VERIFICATION PASSED`, run twice — once before adding this section and the
work-log entry, and again afterward so the issue-citation guards would scan
the final file content: `go build`, `go vet` and `gofmt -l` clean, **zero
skips**, no leftover processes both times. The scratch database
(`opencircuit_test_0427t`) was provisioned directly from `migrations/` through
all 27 steps rather than cloned from the shared template, since the template
sits at 28 while this working tree's `migrations/` is at 27 (`CLAUDE.md`
§5a, `#0315`) — expected, not a broken environment; the shared template was
not touched. `internal/db` `ok` in 0.417s then 0.447s; `internal/handlers`
`ok` in 5.875s then 5.957s — real elapsed time in every case, never a 0.00s
no-op.

`uptime` was recorded rather than reasoned from, per `CLAUDE.md` §5: load
average 11.04 (1/5/15-minute averages 11.04, 9.47, 8.88) before the first run,
8.20 (8.20, 8.95, 8.72) before the second. The machine swung between roughly 2
and 38 today; these figures are recorded, not reasoned from. Nothing timed out
and nothing was re-run to chase a flake.

Guards that actually cover this pass's diff, named as real declarations:

- `TestDocTableRowLines` in `internal/db` ran and passed, but it does
  **not** cover this pass's diff: it is a table-driven unit test of the
  `docTableRowLines` helper against synthetic markdown, and it never
  reads `docs/deployment.md`. No test in either scoped package scans
  that file's DNS table or `placeholder/README.md`, so this pass's edits
  to both are **not** pinned by the suite — their correctness rests on
  the read-only zone listing recorded in the work log and on reading the
  README's own section order, not on a Go guard.
- `TestNoOpenIssueVerificationCitesUndefinedTestFunction` and
  `TestNoOpenIssueEvidenceCitesGoFileByLineNumber` in `internal/db` — these
  scan `open` and `in-progress` issue files, which is exactly what this
  section and `#0448`'s are, and is why every test named here is a real
  declaration rather than a `-run` prefix or a Go-file-plus-line citation
  (`#0352`).
- `TestNoCommentCitesUnresolvedPathOrSection` and
  `TestNoCommentCitesGoFileByLineNumber` in `internal/handlers`.

This pass did not edit `PRD.md` — its §11 arithmetic and the DMARC/`www`
correctness were already independently re-derived and confirmed by the review
pass (`PRD.md` is 1871 lines, §10.2 row 17 raw / 16 stated, unchanged by this
pass) — left alone per scope.

**No secrets or selector tokens landed.** Scanned the whole tracked tree as
bytes with `python3` (`CLAUDE.md` §8 — BSD `grep -P` matches nothing on this
machine, silently): the strings `dkim.amazonses.com` and
`google-site-verification` appear only inside `docs/email-setup.md`'s
`<token>.dkim.amazonses.com` placeholder, `issues/0057.md`'s placeholder row,
and this section's own prose describing that fact — never as a live selector
token, public key, or verification value. The live
`mailing.opencircuitsf.com` SPF TXT row was left out of
`docs/deployment.md`'s table, per the reviewer's judgement that it is an
omission outside this pass's scope, to be filed separately by the
orchestrator — not absorbed here.
