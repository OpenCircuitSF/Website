# 0475 — There is no supported way to change an interest's slug

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | interests |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-08 |
| **Closed** | 2026-09-08 |
| **Commit** | `edfaab1` |

> 🟢 Resolved 2026-09-08 — the supported answer is recorded on all three
> surfaces criterion 2 asked for, and `#0023`'s design is intact.
> **What an operator can and cannot do to an interest's slug:** it cannot be
> changed through the admin console or the admin API at all —
> `PATCH /admin/interests/{id}` with a body carrying `slug` is refused with
> 400 and a message naming the reason, before any other field is merged, so a
> rejected request never partially applies. The one supported route is a new,
> additively-numbered migration running
> `UPDATE interests SET slug = 'new-slug' WHERE slug = 'old-slug';`, under the
> five conditions the "Changing an existing slug" bullet of
> `docs/mailing-list.md` now states. That rename is safe because every table
> that references an interest references it by `id` —
> `subscriber_interests`, `campaign_interests`, and `workshop_interests` — and
> no issued preference-center link or other URL carries a slug at all, so the
> row keeps its `id` and every association survives. **What an operator must
> not do instead:** create a replacement interest and deactivate the old one.
> That mints a new `id` and strands every subscriber selection, campaign
> segment, and workshop tag on a row no longer offered on the signup form,
> which is strictly worse than the rename it was previously documented as
> replacing.

## Description

An interest's **display name** can be changed at runtime; its **slug** cannot.
`Store.Update` takes no slug parameter, and `patchInterestRequest` has no slug
field under `DisallowUnknownFields`, so a `PATCH` carrying one returns **400**.
That immutability is deliberate — `#0023` designed it that way.

A migration could change a slug, but **nothing blesses or warns against that**,
and it is not obviously safe: preference-center links already issued to
subscribers encode interests, and `migrations/000009` is frozen so the seed
cannot be corrected at source (`#0471`).

Found by `#0471`'s review, which noted the gap matters most in exactly the case
that motivated the parent work.

## Why it is worth settling rather than leaving implicit

**`#0232`'s spinoff will hit this directly.** The user's scope for it includes
configurable domain nouns — "upcoming events which we are calling workshops for
this website but could be called something else". A second tenant with a
different taxonomy needs different slugs, not different display names, because
slugs appear in URLs and in the preference center's own semantics.

So the question "can a slug change, and at what cost" stops being hypothetical
the moment that work starts. Answering it now is cheap; answering it inside a
larger migration is not.

## Acceptance criteria

1. **Establish what actually breaks** when a slug changes. Trace every place a
   slug is persisted or transmitted — preference-center links, any stored URL,
   `subscriber_interests` if it references by slug rather than id, import
   mappings, campaign audience definitions. **Measure rather than reason**; this
   lineage has repeatedly found the code doing something other than what the
   prose said.
2. Decide the supported answer and record it: slugs are permanently immutable; or
   they may change by migration under stated conditions; or a rename path exists
   that also rewrites what depends on them. **Any of the three is acceptable — an
   unstated answer is not.**
3. If the answer is "immutable", say so where an operator will meet it — the
   admin API's own error, and `docs/mailing-list.md`'s taxonomy section, which
   `#0471` added.
4. **Do not weaken `#0023`'s design** to make a rename possible without deciding
   criterion 2 first. Slug immutability is a deliberate constraint, not an
   oversight.
5. Do not touch `000009` — it is frozen below production's applied version
   (`CLAUDE.md` §1, re-derived as `22|f` on 2026-09-08).

## Relation

- `#0023` made slugs immutable at runtime, deliberately.
- `#0471` documented the taxonomy's two change channels; its review found this.
- `#0474` is the sibling gap in the delete guard.
- `#0232` is where this becomes urgent, and is scheduled last by the user.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/interests/... ./internal/handlers/... ./internal/subscribers/... ./internal/db/...`.

## Plan

**Planning pass, 2026-09-08 (phase 1, Opus).** Every claim in `## Findings`
below was measured against the working tree and against a scratch database at
migration 27; nothing here is inferred from the issue's own prose. `uptime` at
the start of the pass: load averages 2.30 / 3.03 / 3.13.

### The decision (criterion 2)

**Interest slugs may change, by migration, under the five conditions stated in
step 1 below. They stay immutable through the admin API, exactly as `#0023`
designed** — so criterion 4 is satisfied by construction: nothing in this plan
adds a slug parameter to `interests.Store.Update`, and nothing lets the runtime
channel write the column.

The measurement decides it. Every table that references an interest references
it **by `id`**, never by slug: `subscriber_interests`
(`migrations/000010_create_subscribers.up.sql`), `campaign_interests`
(`migrations/000017_create_campaigns.up.sql`), and `workshop_interests`
(`migrations/000020_create_workshops.up.sql`). No URL anywhere in the product
contains an interest slug — a preference-center link is
`{BASE_URL}/preferences?token={manage_token}` and carries nothing else. So a
one-statement `UPDATE interests SET slug = …` preserves the row's `id` and every
association that hangs off it, and breaks no issued link at all.

That makes the currently-documented replacement pattern — create a new interest
and deactivate the old one — **strictly worse than the thing it was protecting
against**: it changes the `id`, so it strands every subscriber, every campaign's
recorded segment, and every workshop tag on a row that no longer appears in the
signup form. The premise that justified it is false (see Finding 1).

**What the rejected options would have cost.** *Permanently immutable* costs a
downstream twelve permanently-dead rows and twelve orphaned subscriber sets for
a constraint with no measured technical basis — and it is the one answer
`#0232` would have to undo, since a second tenant's whole taxonomy is different
slugs. *A runtime rename path* (`PATCH` accepting `slug`) costs a store method,
a duplicate-slug 409, a new audit action, an admin form field, and its tests —
built against a case that has never arisen here and that `#0232`'s second tenant
will not use, because a second tenant **seeds** its own taxonomy rather than
renaming this one. That is `#0232`'s piece C, and the migration channel is
precisely the surface piece C already builds on.

**This is therefore a documentation-and-error-message change, not a feature.**
Four files, one new test, one extended test. No migration is written by this
issue, and `migrations/000009_create_interests.up.sql` is not touched
(criterion 5).

### Steps

1. **`docs/mailing-list.md` — replace the slug sub-bullet and add the third
   channel.** In the "Changing the taxonomy" section, the first bullet's
   `slug` sub-bullet currently gives a false reason ("a slug change breaks
   every already-issued preference-center link that references it") and
   prescribes create-new-and-deactivate as the replacement route. Rewrite that
   sub-bullet to say: the slug is immutable **through the admin channel**, that
   `interests.Store.Update` takes no slug parameter, that a `PATCH` carrying
   `slug` is refused with an explicit 400 rather than silently ignored, and
   that the reason is deliberate scope — a slug is the taxonomy's stable
   external name and changing it is a reviewed, recorded operation rather than
   a form field. Delete the create-new-and-deactivate advice; replace it with a
   pointer to the new third bullet. State plainly that create-new-and-deactivate
   is **not** the way to change a slug, because it mints a new `id` and strands
   every association.

   Then add a third top-level bullet, **"Changing an existing slug"**, stating
   the blessed shape and its five conditions:

   - The statement is `UPDATE interests SET slug = 'new-slug' WHERE slug =
     'old-slug';` in a new, additively-numbered migration. Never a delete plus
     an insert: that mints a new `id`, and `subscriber_interests`,
     `campaign_interests`, and `workshop_interests` all reference the row by
     `id`, so the associations would be cascaded away rather than carried
     across.
   - The new value must satisfy the lowercase-hyphenated `interests_slug_format`
     CHECK added in `migrations/000009_create_interests.up.sql`.
   - A collision with an existing slug raises a unique violation and aborts the
     migration. That is the correct fail-closed outcome and must not be softened
     with `ON CONFLICT`.
   - Re-running the migration is a no-op, because `WHERE slug = 'old-slug'`
     matches nothing the second time. Unlike the seed channel, the matching
     `.down.sql` **may** reverse it — `UPDATE interests SET slug = 'old-slug'
     WHERE slug = 'new-slug';` removes no row and fires no cascade, so the
     no-op-down rule that governs a seeded row does not apply here.
   - The migration must **not** rewrite `audit_log.metadata`. Audit rows record
     what an operator actually did at the time and are the consent-evidence
     record; a historical row naming a since-renamed slug is correct, not stale.

   Close the bullet with the two visible, fail-closed consequences an operator
   should expect, both measured in Finding 3: a stale browser tab holding the
   old slug gets a 400 "unknown interest" on its next preference save until it
   is reloaded, and a CSV import file still carrying the old slug is reported in
   the preview's `unknown_interest_slugs` rather than silently importing without
   the link.

2. **`internal/interests/store.go` — correct `Update`'s doc comment.** It
   currently states that renaming a slug "would break any already-issued
   preference-center link that references it by slug". That is false, and
   `#0023`'s own Gotchas section already said so at the time. Replace the
   sentence with the measured reason: nothing references an interest by slug —
   the three join tables all use `id` and no URL carries a slug — so the
   immutability here is a scope decision about the admin channel, and the
   supported way to change a slug is a migration, per the "Changing the
   taxonomy" section of `docs/mailing-list.md`. Do not change the method
   signature.

3. **`internal/handlers/interests.go` — make the refusal say why (criterion 3).**
   Today a `PATCH` carrying `slug` is rejected by `decodeJSON`'s
   `DisallowUnknownFields` and the operator sees `{"error":"invalid request
   body"}`, which is byte-identical to what a typo or malformed JSON produces.
   Make the refusal explicit:

   - Add `"encoding/json"` to the file's imports.
   - Add one field to `patchInterestRequest`, a `Slug` of type
     `json.RawMessage` tagged with the JSON name `slug` plus `omitempty`:

     ```go
     Slug json.RawMessage `json:"slug,omitempty"`
     ```

     `json.RawMessage` rather than `*string` deliberately, so that the mere
     **presence** of the key is refused whatever its value, including an
     explicit `null`, which a `*string` would decode to nil and let through as
     an empty patch.
   - Immediately after the `decodeJSON` call in `Patch`, and before any field is
     merged onto the current row, add:

     ```go
     if req.Slug != nil {
         writeError(w, http.StatusBadRequest, "an interest's slug cannot be changed through this endpoint — see \"Changing the taxonomy\" in docs/mailing-list.md for the migration that can")
         return
     }
     ```

   - Rewrite `patchInterestRequest`'s doc comment: the field exists **only** to
     produce this refusal, `interests.Store.Update` still takes no slug
     parameter, and the request is rejected outright rather than partially
     applied. Drop the false preference-center-link rationale here too, the same
     way step 2 drops it from the store.

   This does not weaken `#0023` (criterion 4): the store cannot write the column,
   the whole request is refused, and the enforcement moves from an incidental
   decoder rejection to an explicit, tested one.

4. **`internal/handlers/interests_test.go` — extend the existing assertion.**
   `TestAdminInterests_PatchUpdatesFieldsAndRejectsSlug` already issues exactly
   the request in question and asserts a 400 plus an unchanged slug. Add to it:
   read the response body and assert it contains the literal substring
   `slug cannot be changed`, so the message is pinned rather than decorative.
   Add a second request in the same test sending `{"slug":null}` and assert it
   is refused with the same 400, which is the case a `*string` field would have
   let through. Do not rename the test and do not add a new test function for
   this; the request under test is the one it already makes.

5. **`internal/db/interest_taxonomy_seed_freeze_guard_test.go` — pin that the
   blessed rename migration goes quiet.** The guard bans DELETE/TRUNCATE/DROP
   against `interests` and non-idempotent INSERTs; an `UPDATE interests SET
   slug = …` trips neither today. Add one test beside the existing six,
   following their naming pattern — the same interest-taxonomy migration-guard
   prefix, ending in a phrase for allowing a slug-rename update. (The identifier
   is described rather than written because the repo-wide citation guard
   requires an open issue's test-function citations to resolve against the tree,
   and this one does not exist yet; `CLAUDE.md` §8's `#0392` note is the
   precedent.) Feed it a `000031`-numbered up/down pair containing the exact
   statements step 1 blesses — the forward `UPDATE` in the `.up.sql` and its
   reversal in the `.down.sql` — and assert `interestTaxonomyGuardViolations`
   returns zero violations for both files. Model it on
   `TestInterestTaxonomyMigrationGuardAllowsIdempotentInsert`, which is the
   existing goes-quiet test.

   This step is not optional bookkeeping. `#0471` bounced on precisely this
   shape: the document prescribed a migration pattern that the guard in this
   same file rejected, and nothing caught the disagreement until a reviewer
   probed it by hand. Pinning the newly-blessed pattern is what stops step 1's
   prose and this guard drifting apart.

6. **Verify.** `ISSUE=0475 scripts/check.sh go ./internal/interests/...
   ./internal/handlers/... ./internal/subscribers/... ./internal/db/...` — the
   command the issue itself names. `internal/handlers` is required anyway
   because step 2 and step 3 edit Go doc comments, which the citation-guard
   family scans. Confirm zero skips, real elapsed time on each package, and a
   clean `gofmt -l`. Run the extended handler test and the new guard test by
   their own full names as well as inside the package `ok`, and record both.

### What this plan deliberately does not do

- It writes **no migration**. There is no slug this project wants to change
  today; the deliverable is the supported answer plus the pattern, not an
  exercise of it.
- It adds **no runtime rename path**, per the reasoning above and criterion 4.
- It does not touch `migrations/000009_create_interests.up.sql`, any other
  migration file, `internal/interests/store.go`'s method signatures, or
  `web/src/`. The SPA's `updateInterest` doc comment in `web/src/lib/api.ts`
  already says "the server rejects a body that carries one", which stays true.

## Findings

Criterion 1's trace, as measured. Commands are given so they can be re-run.

### 1. What an already-issued preference-center link encodes — the sharp question

**A `manage_token` and nothing else.** Every construction site builds the same
string:

```
grep -rn 'preferences?\|/manage' internal/mailing/*.go | grep -v '_test.go'
```

Four builders in `internal/mailing/transactional_templates.go` and one in
`internal/mailing/campaign_render.go`, all of the form
`baseURL + "/preferences?token=" + manageToken`. No interest slug is present,
and there is no second parameter.

Those links are therefore **fully durable across a slug change.** The premise
recorded in `internal/interests/store.go`'s `Update` doc comment, repeated in
`internal/handlers/interests.go`'s `patchInterestRequest` doc comment, and
repeated again in `docs/mailing-list.md` — that a rename breaks issued links —
is false, and was false when written. `#0023`'s own Gotchas section says so
explicitly in the same commit that introduced the rationale: "A
signup-confirmation or preference-center link that ever embedded a slug (none
currently do — everything keys off numeric interest ids)". The conditional was
correct; the doc comment dropped it and the two later copies inherited the
absolute form.

### 2. Where an interest slug is durably persisted — two places, one of them
correct to leave alone

Schema, measured against a scratch database at migration 27
(`scripts/testdb.sh create 0475p`, dropped afterwards):

```
psql "$PGURL" -tAc "select table_name||'.'||column_name from information_schema.columns
                    where table_schema='public' and column_name like '%slug%'"
```

returns exactly three columns — `interests.slug`, `workshops.slug`,
`email_campaigns.slug`. The latter two are unrelated namespaces.

```
psql "$PGURL" -tAc "select con.conrelid::regclass, con.confrelid::regclass
                    from pg_constraint con where con.contype='f'
                    and con.confrelid='interests'::regclass"
```

returns exactly three foreign keys — `subscriber_interests`,
`campaign_interests`, `workshop_interests` — **all on `interest_id`, all
targeting `interests(id)`**. Not one references the slug. This matches the
count `#0474` established and was re-derived here rather than carried over.

The only other durable home is `audit_log.metadata`, in two shapes, found by
grepping the audit writes:

- The interests CRUD handler records `"slug"` in the metadata of the
  `interest.created`, `interest.updated` (and its deactivated/reactivated
  variants), and `interest.deleted` entries.
- The manual-add path in `internal/handlers/admin_subscribers.go` records
  `"interests": req.Interests` — the operator-supplied **slugs** — in a
  `subscriber.manual_add` entry.

Both are historical records of what happened at the time, so a rename must not
rewrite them. That is a finding in favour of the migration channel rather than
against it, and step 1 states it as a condition.

### 3. Where a slug is transmitted but never stored

- `POST /api/subscribe` takes `interests: [slug, …]` and resolves them to ids
  before writing anything (`internal/handlers/subscribe.go`).
- `GET`/`PATCH /api/preferences` take and return slugs, resolved fresh on every
  request via `GetBySlug` / `GetByIDs`. The preference center's checkbox grid is
  built from whatever the server returns **on that request**, so after a rename
  a reloaded page is immediately correct.
- The confirm response, `GET /api/interests`, the public workshop payloads, the
  admin list, the admin dashboard, and the admin subscriber detail all carry
  slugs in JSON only.
- CSV import and CSV export both use a slug column, but those are operator-held
  files outside the database. `internal/subscribers/imports.go` reports every
  unrecognised slug through `UnknownInterestSlugs`, surfaced to the admin screen
  as `unknown_interest_slugs`, so a stale import file fails visibly rather than
  importing rows with no interest linked.

The one transient breakage a rename causes: a browser tab left open across the
change still holds the old slug, and its next preference save is refused with a
400 "unknown interest". The refusal covers the whole request, so no selection is
silently dropped — it fails closed and a reload fixes it.

### 4. Candidates named in the issue that turned out **not** to reference slugs

Stating these plainly, as asked:

- **Campaign audience definitions do not.** `campaign_interests` stores
  `interest_id`, `internal/mailing/audience.go` selects `interest_id`, and the
  admin campaign handler's own error text is "one or more interest ids do not
  exist". No slug is involved in targeting.
- **Import mappings do not persist one.** `subscriber_imports` has no interest
  column at all; `ImportStore.Commit` takes an in-memory
  `InterestSlugToID` map and writes `subscriber_interests` rows by id.
- **The outbound queue does not.** Every payload struct in
  `internal/mailing/outbox_worker.go` and its producer twin in
  `internal/subscribers/store.go` was read; the only interest-bearing one is the
  welcome payload, and it carries interest **names**, deliberately captured at
  confirm time so a later preference change cannot rewrite an already-queued
  email.
- **No stored or issued URL does.** `internal/handlers/routes.go` has no route
  containing "interest"; `web/src/lib/router.ts` has no interest route; the
  sitemap emits none. The two dynamic public paths are `/workshops/{slug}` and
  `/archive/{slug}`, which are workshop and campaign slugs.
- **No sent email does.** The footer's "Manage your interests" link is the
  `manage_token` URL from Finding 1; the campaign renderer never emits a slug.
- **No production code depends on a specific seeded slug value.** Each of the
  twelve seeded slugs was grepped across `internal/`, `cmd/`, and `web/src/`
  excluding tests: the only hits are decorative marketing copy in
  `web/src/lib/crtScreen.ts` and `home-automation` used as a form-placeholder
  example in `web/src/lib/admin.ts` and `web/src/views/Admin.svelte`. None is a
  lookup.
- **`internal/devstore` has no interests at all**, consistent with `CLAUDE.md`
  §5's note that `STORAGE=json` has no mailing list.

### 5. The current admin refusal is uninformative

`decodeJSON` sets `DisallowUnknownFields` and its caller maps any decode error
to `{"error":"invalid request body"}`. So a `PATCH` attempting a rename is
indistinguishable, to the operator, from a typo'd field name or a malformed
body. `TestAdminInterests_PatchUpdatesFieldsAndRejectsSlug` asserts the 400 and
the unchanged slug but not the message, so step 3's improvement is compatible
with the existing assertion and step 4 is what makes the message load-bearing.

### 6. Worth reporting to the orchestrator, not filed (§9)

Nothing here rises to a new issue. Two observations for the record: the false
preference-center-link rationale had propagated to three separate places before
anyone measured it, which is the same prose-outruns-code pattern `#0471`,
`#0474`, and `#0343` each hit in this lineage; and `docs/mailing-list.md`'s
second channel already blesses `UPDATE interests SET … WHERE slug = '…'` for
renaming a seeded default, which — read literally — already permitted a slug
change that the first bullet forbids. Step 1 resolves that ambiguity rather than
leaving two bullets disagreeing.

## Obstacles

Per `CLAUDE.md` §5b. **Nothing blocks dispatch.** The scan, run during this
pass:

| Class | Checked | Result |
|---|---|---|
| Tool availability | `command -v psql migrate npm go gofmt` | all five present |
| Database privileges | `select rolcreatedb from pg_roles where rolname=current_user` | `t` — the grant `CLAUDE.md` §5b records is in place |
| Scratch database | `scripts/testdb.sh create 0475p` | created at migration 27 in ~1s and dropped again; `check.sh` will do the same for `ISSUE=0475` |
| Credentials | none needed | no SES, no outward-facing step, no approval gate |
| Shared resources | no port is bound, no build is run, `web/dist/` is not touched | the work is Go, Markdown, and tests only |

Two things for the implementer to know rather than clear:

- **The shared test template is at 28 while this tree is at 27.** `check.sh`
  detects the mismatch and provisions the scratch database straight from
  `migrations/` instead of cloning the stale template. Do **not** run
  `scripts/testdb.sh template` or `scripts/db-reset.sh` to "fix" it — other
  agents depend on the current state.
- **Editing a Go doc comment means running `internal/handlers`** (`CLAUDE.md`
  §5's repo-wide guard table), which steps 2 and 3 both do. It is already in the
  verification command the issue names, so no widening is needed — just do not
  narrow the run to `./internal/interests/...` on the grounds that the store is
  where the comment lives.

## Work log

- 2026-09-08 — **Planning pass (phase 1, Opus).** Wrote `## Plan`,
  `## Findings`, and `## Obstacles`; no code, no migration, status left `open`.
  Criterion 1 was measured, not reasoned: three slug columns and three
  `interests(id)` foreign keys read off a scratch database at migration 27, the
  preference-center URL builders read in `internal/mailing`, every outbound-queue
  payload struct read in both producer and consumer, and all twelve seeded slugs
  grepped across production Go and TypeScript. The decisive finding is that an
  issued preference-center link encodes only a `manage_token`, so the rationale
  recorded in three places for slug immutability is false and has been false
  since `#0023` — whose own Gotchas section stated the correct, conditional
  version. Recommended answer for criterion 2: **changeable by migration under
  five stated conditions, immutable through the admin API**, which leaves
  `#0023`'s design intact and is the surface `#0232`'s piece C already needs.
  Rejected permanent immutability (it strands a downstream's twelve taxonomies
  on dead rows for no measured technical reason) and a runtime rename path (new
  store method, 409 handling, audit action, admin form, tests — built against a
  case a second tenant does not have, since it seeds rather than renames). The
  product is a documentation change plus one honest error message: four files,
  one new guard test, one extended handler test, no migration.

- 2026-09-08 — **Implementation pass (phase 2, Sonnet).** Followed the plan's
  five steps exactly, with one deviation forced by a guard the plan's own
  suggested error string would have tripped. `internal/handlers/citation_guard_test.go`'s
  `TestNoAdminFacingStringCitesInternalDocs` bans `docs/\S*\.md` inside any
  admin-facing string *literal* in `internal/handlers` — the plan's example
  refusal text cited `docs/mailing-list.md` directly in the `writeError` call,
  which is exactly that shape. Caught by running the full scoped suite before
  committing, not by inspection. Rephrased per `CLAUDE.md` §8's prescription
  (never delete the message, never move it out of the response) to: "an
  interest's slug cannot be changed through this endpoint — it is a
  deliberately reviewed, recorded operation performed by a database
  migration, not an admin-console field" — same substance, no path citation.
  The doc-comment citation of `docs/mailing-list.md` in `patchInterestRequest`'s
  own comment is unaffected (comments are structurally excluded from that
  guard by design).

  Changes: `docs/mailing-list.md`'s "Changing the taxonomy" intro now says
  three channels; its live-catalog slug sub-bullet drops the false
  preference-center-link rationale and points at a new third bullet, "Changing
  an existing slug", stating the five conditions from the plan; the second
  channel's own `UPDATE ... WHERE slug = '...'` example was clarified to say
  the slug is the match key, never the `SET` target, resolving the
  contradiction the plan's Finding 6 flagged. `internal/interests/store.go`'s
  `Update` doc comment now gives the measured reason (nothing references an
  interest by slug) instead of the false one, with no signature change.
  `internal/handlers/interests.go` adds a `json.RawMessage` `Slug` field to
  `patchInterestRequest` and an explicit check in `Patch` that refuses the
  whole request before any field is merged, whether `slug` is a value or
  literal `null` — `req.Slug != nil` catches both, since `json.RawMessage`'s
  `UnmarshalJSON` copies the raw bytes `null` rather than leaving the field
  nil the way a `*string` would. `internal/handlers/interests_test.go` extends
  `TestAdminInterests_PatchUpdatesFieldsAndRejectsSlug` (not a new test
  function) to assert the response body contains "slug cannot be changed" and
  to add the `{"slug":null}` case, both against the same request the test
  already issues. `internal/db/interest_taxonomy_seed_freeze_guard_test.go`
  gains `TestInterestTaxonomyMigrationGuardAllowsSlugRenameUpdate`, pinning
  that a plain `UPDATE interests SET slug = ... WHERE slug = ...` pair (and
  its reversal) trips neither of the guard's two existing rules, modeled on
  `TestInterestTaxonomyMigrationGuardAllowsIdempotentInsert`. No migration
  file was added or touched; `migrations/000009` untouched (criterion 5).

  Confirmed after the change: the only SQL statement in
  `internal/interests/store.go` that writes to the `slug` column is `Create`'s
  `INSERT` (a new row, at admin-initiated creation time, not a rename of an
  existing one); `Update`'s `SET` clause lists only `name, description,
  sort_order, active`. No other file under `internal/` writes `interests.slug`
  from an HTTP-reachable path — `grep -rn "SET slug" internal/` (excluding
  tests) returns nothing at all, since `Update` never mentions the column.
  `#0023`'s design is therefore unweakened: the store still cannot write the
  column from the admin channel, and the `Slug` field exists solely to
  produce a better-worded refusal.

  Verification: `uptime` before the run showed load averages 2.60 / 2.82 /
  2.94. `ISSUE=0475 scripts/check.sh go ./internal/interests/...
  ./internal/handlers/... ./internal/subscribers/... ./internal/db/...`
  provisioned `opencircuit_test_0475` directly from `migrations/` (template
  at 28, tree at 27 — expected per the plan's Obstacles) and reported `ok`
  with real elapsed time on all four packages (`interests` 0.236s, `handlers`
  7.729s, `subscribers` 7.852s, `db` 0.418s), zero skips, `gofmt -l` clean,
  `go build`/`go vet` clean. Additionally ran
  `TestAdminInterests_PatchUpdatesFieldsAndRejectsSlug` and
  `TestInterestTaxonomyMigrationGuardAllowsSlugRenameUpdate` individually by
  full name against a freshly created scratch database, both `PASS`. Dropped
  the scratch database and confirmed no leftover `go test` processes
  afterward.

  Left status at `open` and this issue file uncommitted, per instruction —
  the orchestrator/reviewer sets `resolved`. Token/cost accounting left for
  the orchestrator to fill in, consistent with `#0023`'s batched-cost
  precedent.

- 2026-09-08 — **Review pass (phase 3, Opus). Pass; status set to `resolved`.**
  Verified `edfaab1` independently rather than from the drafted account. Ran
  the issue's own scoped command twice, before and after writing `## Review
  notes`: `uptime` showed load averages 2.13 / 2.50 / 2.77 on the first run and
  1.68 / 2.25 / 2.62 on the second, both far below the ~4 idle ceiling, and
  both runs were clean — `go build`, `go vet`, and `gofmt -l` green, all four
  packages `ok` with real elapsed time, zero skips. Criterion 4 was re-derived
  across the whole package rather than accepted at the call site: only
  `Create`'s `INSERT` names the slug column, `Update`'s `SET` never mentions
  it, no SQL anywhere sets it, and the refusal returns before the merge block
  begins. The `null` semantics were confirmed empirically against a standalone
  decoder, so the tests pin real behaviour. The new guard test was proved
  falsifiable by mutation in a throwaway worktree pinned to the commit under
  review, leaving the shared tree untouched. No new issues to file; two
  observations recorded in `## Review notes` — the error message could name the
  documented section title without tripping the guard that forced the
  rephrasing, and the implementation pass used its work-log entry in place of
  the separate resolution sections. Neither is worth a bounce.

## Review notes

**Review pass (phase 3, Opus), 2026-09-08 — pass.** `uptime` before the first
verification run: load averages 2.13 / 2.50 / 2.77; before the confirming
re-run after these notes were written: recorded in the work-log row below.
Both are well under the ~4 idle ceiling `CLAUDE.md` §5 sets, so no timing
result here is load-contaminated.

### Criterion 4 — `#0023` is genuinely unweakened, verified independently

The implementer added a field to a request struct in order to *reject* it,
which is subtle enough to re-derive rather than accept. Measured across the
whole package rather than at the one call site: exactly four SQL statements in
`internal/interests/store.go` touch the `interests` table, and only `Create`'s
`INSERT` names the `slug` column — a brand-new row at admin-initiated creation
time, which is not a rename. `Update`'s `SET` clause lists `name`,
`description`, `sort_order`, and `active` and never mentions the column, so
the store still has no way to write it. A search for any SQL `SET` fragment
naming `slug` across non-test Go under `internal/` and `cmd/` returns nothing.
The new `Slug` field is read in exactly one place, a presence check that
returns before the merge block begins.

The ordering claim holds: the refusal sits immediately after the `decodeJSON`
call and before `name := current.Name`, so no field is merged and
`interests.Store.Update` is never reached on a rejected request. There is no
partial-application path.

### The `null` refusal is a decision, and the tests pin what the code does

Confirmed empirically rather than reasoned, against a standalone decoder using
the same struct shape and `DisallowUnknownFields`: `{"slug":null}` leaves
`Slug` non-nil holding the four raw bytes `null`, `{"slug":"y"}` and
`{"slug":""}` likewise, an absent key leaves it nil, and an unrecognised key
still fails the decode. So presence rather than value triggers the refusal,
exactly as the doc comment claims, and
`TestAdminInterests_PatchUpdatesFieldsAndRejectsSlug` pins the real behaviour
rather than an assumed one.

Refusing `{"slug":null}` is the right call, and it is a decision rather than
an artifact — the plan chose `json.RawMessage` over `*string` for this
specific reason, the field's doc comment states it, and the test carries a
comment explaining it. It is worth recording that this deliberately departs
from the endpoint's own convention for its sibling fields, where an explicit
`null` decodes to a nil pointer and means "leave unchanged". The departure is
correct here because `Slug` is not an updatable field at all but a tripwire,
and a tripwire that fires on some values and not others is a worse tripwire:
a client sending the key at any value believes the field is patchable, and
that belief is what the refusal exists to correct.

Nothing else shifted with the field. An unknown key is still rejected by the
decoder and still yields the generic `invalid request body`; only `slug`
moved from unknown to known-and-explicitly-refused.

### The error message — good, with one observation that is not a defect

It clears the bar criterion 3 sets easily: the old response was byte-identical
to what a typo produces, and the new one names both the reason and the route.
At 188 characters it is the longest error string in `internal/handlers`, but
not an outlier in kind — the package already carries several explanatory
operator-facing refusals in the same register, including ones that name the
alternative route to take instead. The register fits.

The observation, recorded because the guard trip makes it worth stating rather
than because it blocks: the rephrasing forced by
`TestNoAdminFacingStringCitesInternalDocs` removed the pointer to where the
five conditions are written, and what replaced it names the route in kind
("performed by a database migration") without a handle for finding them. The
substance survives, and the path is not lost — the guard's own prescribed
remedy is a citation in a comment beside the string, and that citation exists
on the `patchInterestRequest` doc comment naming the "Changing an existing
slug" section, with the store's `Update` doc comment repeating it. The only
audience that can reach this error is someone hand-crafting an admin API
request, since the admin SPA offers no slug field; that reader has both
comments. Naming the section title alone would satisfy the guard and close the
last hop, and is worth doing opportunistically the next time this line is
touched — it is not worth a bounce on its own.

### The new guard test can actually fail, proved by mutation

Applied `CLAUDE.md` §8's test — can an edit to the subject also satisfy the
oracle — in a throwaway worktree pinned to the commit under review, so the
shared tree was never modified. Widening the guard's destructive-statement
regexp to also treat `UPDATE ... interests` as a removal made
`TestInterestTaxonomyMigrationGuardAllowsSlugRenameUpdate` fail, naming both
the up and the down file. So the blessed pair is genuinely scanned and the
assertion is falsifiable by the thing it measures, not passing on an empty
result.

The residual hazard that a negative assertion invites — the scan silently
finding nothing and the test passing vacuously — is closed by siblings rather
than by this test itself. Forcing the version gate to skip every file left
this test green but failed
`TestInterestTaxonomyMigrationGuardCatchesDestructiveDelete` and two other
positive assertions in the same file. That is the acceptable structure: the
negative test proves the pattern is allowed, and the positive tests prove the
scanner is looking.

### `docs/mailing-list.md` is internally consistent end to end

Read the whole "Changing the taxonomy" section rather than the diff. The intro
now says three channels and the three bullets are present. The contradiction
the plan flagged is resolved from both sides: channel 1 points forward to the
new bullet and states plainly that create-new-and-deactivate is not a
substitute for a rename, and channel 2 now says its `WHERE slug` clause
selects the row and never appears on the `SET` side, with its `SET` example
narrowed to `name` and `description`. The apparent conflict between channel
2's no-op-down rule and channel 3's reversible down is addressed explicitly
rather than left for a reader to notice, and the reason given is correct — an
`UPDATE` removes no row and fires no cascade.

The five conditions are stated, not gestured at: five sub-bullets covering the
statement shape, the format constraint, collision behaviour, re-run and
rollback, and the prohibition on rewriting `audit_log.metadata`. Four of the
document's load-bearing factual claims were checked against the tree rather
than taken on trust: the `interests_slug_format` CHECK constraint exists in
`migrations/000009_create_interests.up.sql`; the slug column is declared
`UNIQUE`, so a collision does raise the violation the bullet promises; a
preference save naming an unrecognised slug does return 400 `unknown
interest`; and the import preview does surface `unknown_interest_slugs`.

### Criterion 5

Confirmed from the diff: the commit touches five files and not one of them is
under `migrations/`. `migrations/000009_create_interests.up.sql` is still at
the commit that created it, and no new migration was added.

### Verification

`ISSUE=0475r scripts/check.sh go ./internal/interests/... ./internal/handlers/...
./internal/subscribers/... ./internal/db/...`, run independently rather than
trusting the drafted account. The scratch database was provisioned straight
from `migrations/` rather than cloned, as expected with the shared template at
28 and the tree at 27; neither `scripts/testdb.sh template` nor
`scripts/db-reset.sh` was run. `go build`, `go vet`, and `gofmt -l` were clean,
all four packages reported `ok` with real elapsed time — `interests` 0.306s,
`handlers` 7.516s, `subscribers` 7.573s, `db` 0.482s — and the skip audit
reported no skips.

Per `CLAUDE.md` §8's gofmt warning, since doc comments were edited: `gofmt` on
each of the four touched Go files produces bytes identical to what is on disk,
so the formatting is already at its fixed point and no doubled quote will be
rewritten into a curly one on the next sweep. No smart quotes are present in
the two edited doc comments; the em-dashes are intentional.

### Process note, not a defect

The implementation pass recorded root cause, approach, verification, and files
changed as narrative inside its `## Work log` entry rather than as the separate
`## Root cause`, `## Fix`, `## Verification`, and `## Files changed` sections
`issues/Issues.md`'s phase-2 step asks for, and left the status at `open` with
no `**Commit**` row for the reviewer to confirm. Every required fact was
present and accurate, so this is a formatting deviation rather than a gap in
the record; the `**Commit**` row was added as part of this resolution. Worth
mentioning to the next implementer on this lineage, not worth a bounce.
