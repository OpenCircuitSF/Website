# 0181 — Add one guard that fails if any admin-facing message cites an internal document

| | |
|---|---|
| **Status** | resolved |
| **Platform** | All |
| **Module** | handlers / infra |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |
| **Commit** | `920a959`, `5a116f5` |
| **Closed** | 2026-08-23 |

## Description

Citations of our own tracker and guidance files leaking into copy an admin reads has now been found **three times, on three different axes**:

- `#0172` swept `web/` and fixed two.
- `#0175`'s review swept **Go strings that render verbatim** and found three more, which `web/`-only sweeps structurally could not see.
- `#0178`'s review swept **backwards from the SPA** — tracing which response fields `api.ts` renders — and confirmed those were the last, while finding one more surface nobody had covered (below).

Each pass was thorough within its method and blind outside it. `#0178`'s reviewer proposed the durable fix: **one assertion that no admin-facing message literal contains `CLAUDE.md`, `PRD §`, or `#0NNN`.** It would have failed at `#0172` rather than requiring a third sweep on a fourth axis.

This is **not** the phrase-pinning `#0174` warns against. It does not assert what any message *says*; it asserts that none of them cites a document only we can read. The rule is stable even as the copy changes.

## Acceptance criteria

- [x] A test fails when an admin-facing message literal contains `CLAUDE.md`, `PRD §`, or an issue reference (`#0` followed by digits)
- [x] It is scoped to strings that actually reach a user. `#0178`'s review established the boundary precisely: only `internal/handlers`, `internal/middleware` (machine codes) and `internal/seo` (plain text, non-SPA) write HTTP bodies, and `api.ts`'s `request()` collapses non-2xx bodies to `err?.error ?? err?.message`. **Code comments must not trip it** — the same citations are correct and wanted there
- [x] **CAN-SPAM §7704 and env var names must still pass** — `#0175`'s review drew that line deliberately: a statute is something an admin can look up, and `EMAIL_REPLY_TO` is sometimes the entire actionable fact
- [x] Prove it fails against the pre-`#0178` strings — restore one temporarily and watch it fire
- [x] Decide whether the guard belongs on the Go side, the `web/` side, or both, and record why. The three leaks were all Go-authored, but `#0172`'s two were in Svelte
- [x] `ISSUE=0181 scripts/check.sh go ./internal/...` and `scripts/check.sh web` both pass

## Root cause

Three sweeps (`#0172` in `web/`, `#0175`'s review over Go strings that
render verbatim, `#0178`'s review tracing backwards from the SPA) each
found the same defect on a different axis and closed only the instances
they could see. Nothing asserted the *class*: that no string reaching an
admin cites a document only the team can read. `#0178`'s reviewer named
the fix — one assertion, not a fourth sweep — and this issue builds it.

## Fix

Two guards, one per language, both AST-based rather than a text grep so
comments (where these citations correctly live, per `#0172`/`#0178`) are
excluded structurally instead of by allowlist.

**Go — `internal/handlers/citation_guard_test.go`.** Walks every non-test
`.go` file's `*ast.BasicLit` string literals (via `go/parser`/`go/ast`,
never visiting comments) in four directories:

- `internal/handlers`, `internal/middleware`, `internal/seo` — the three
  packages `#0178`'s review established write an HTTP body directly.
- `internal/mailing` — **added after verifying rather than assuming the
  three-package boundary.** `PreflightFailure.Message` (the exact field
  `#0178` fixed a citation in) is authored entirely in
  `internal/mailing/preflight.go` and serialized untouched by a handler;
  a guard scoped to only the three body-writing packages would not have
  caught the original `#0178` leak at all, since the literal never lived
  in `internal/handlers`. Grepped every other `internal/` package for
  `Message string` fields and `.Error()` calls and found nothing else
  that threads a literal into a response body (the two near-misses —
  `internal/sesnotify`'s `rawMessage` parameter name and one `.Error()`
  hit in an `internal/auth/login.go` comment — are not admin-facing
  strings).

Pattern: `` CLAUDE\.md|PRD §|#0[0-9]{3}\b `` — the trailing `\b` matters:
without it, `internal/mailing/templates.go`'s `colorButtonText =
"#04140a"` false-positives, since `#0414` is a substring of `#04140a`.
CAN-SPAM §7704 and `EMAIL_REPLY_TO`/`EMAIL_LIST_DOMAIN` need no
allowlisting — neither matches the pattern, so they pass by construction.

**web — `web/src/lib/citationGuard.test.ts`.** Built even though the
three most recent leaks were Go-authored, because `#0172`'s original two
were in Svelte — a Go-only guard would not have caught the case that
started this. Uses the TypeScript compiler's AST for `.ts` files
(`StringLiteral`/`NoSubstitutionTemplateLiteral`/`TemplateHead`/`Middle`/
`Tail` nodes) and `svelte/compiler`'s `parse({ modern: true })` AST for
`.svelte` files (`Text` nodes in the template fragment; `Literal`/
`TemplateElement` nodes in the `<script>` instance/module content).
Comments are separate node types (`Comment` in the fragment tree) or a
separate array (`ast.comments`, for the script) in both ASTs, so neither
is ever visited by the literal/text collector. Required adding
`@types/node` (`web/package.json`) and `"node"` to `tsconfig.json`'s
`types` array so `svelte-check` can resolve the `node:fs`/`node:path`/
`node:url` imports the file-walk needs — without it `npm run check`
failed with "Cannot find module 'node:fs'" (TS7016-class error, the
`types` array restricts ambient global typings to an explicit allowlist
and Node wasn't in it).

Both guard files also carry synthetic-fixture unit tests (not dependent
on the current state of the real tree) proving each of the three
exclusions individually and proving the guard fires on a citation — see
Verification.

### Fix pass 2

Addresses the bounce recorded in `## Review notes` below. All four items the
reviewer named:

1. **Required — the Svelte fragment blind spot.** `scanSvelteSource` in
   `web/src/lib/citationGuard.test.ts` collected only `Text` from
   `ast.fragment`; a `Literal`/`TemplateElement` node inside an
   `ExpressionTag`'s own ESTree subtree — which lives in the fragment tree,
   not the instance/module script — was never visited. Fixed by widening the
   `collectByType` call over `ast.fragment` to `Set(['Text', 'Literal',
   'TemplateElement'])`, and factoring a shared `extractLiteralValue()` so
   the fragment walk and the script walk extract identically. This is the
   only change that touches production-scan behavior; every prior passing
   proof (comment exclusion, CAN-SPAM, env vars, hex colors) still holds
   because none of those shapes are `Literal`/`TemplateElement` nodes with a
   citation-matching string value.
2. **`PRD §` → `PRD\s*§`** in both `citationPattern`s (Go and web) — closes
   `PRD  §6.6` (two spaces) and `PRD§6.6` (no space), which both missed
   before.
3. **Go guard scope widened** from the four hand-reasoned directories
   (`internal/handlers`, `../middleware`, `../seo`, `../mailing`) to a
   recursive `filepath.WalkDir` over `internal/` and `cmd/` — matching the
   reviewer's own whole-repo scan, which found zero hits outside the
   original four. `scanDirForCitations` now walks instead of `os.ReadDir`
   one level; `scanGoRoots` replaces `citationGuardDirs`.
4. **`Issues.md`, `HANDOFF.md`, `docs/*.md`** added to both patterns
   (`Issues\.md|HANDOFF\.md|docs/\S*\.md`), per the orchestrator's explicit
   ruling in the dispatch for this pass — these are exactly as unreadable to
   an admin as `CLAUDE.md`/`PRD §`, and `#0178`'s own whole-tree grep already
   included `Issues\.md|HANDOFF`.

Also did the recommended, not-required item: replaced the `node:fs`/
`node:path`/`node:url` file walk (`walk()`, `readdirSync`, `statSync`,
`readFileSync`, `fileURLToPath`) with
`import.meta.glob('../**/*.{ts,svelte}', { query: '?raw', import: 'default',
eager: true })`, verified in this repo by the reviewer. It type-checks under
the original narrow `types` array, so `web/package.json`'s `@types/node`
devDependency, `web/package-lock.json`'s corresponding entries, and
`web/tsconfig.json`'s `"node"` entry in `types` are all reverted.

Code commit: `5a116f5` — "#0181 Fix: scan Svelte template expressions, widen
Go scope, and reduce guard footprint".

## Verification

- `uptime` before every run: 1.2–4.3 across the session, well under the
  ~8 threshold — no timing readings to discount.
- **Real-tree run, clean:** `ISSUE=0181 scripts/check.sh go
  ./internal/...` — build and vet clean, all 15 packages `ok` with real
  elapsed times (`handlers` 2.445s, `interests` 2.139s, `mailing` 0.709s
  — none of the DB-backed packages reported 0.00s), zero skips,
  `VERIFICATION PASSED`. Scratch database `opencircuit_test_0181` dropped
  on exit. `scripts/check.sh web` — `npm run check`: 358 files / 0
  errors / 0 warnings (up from 232 before the `@types/node`/tsconfig
  change, expected — that package's own `.d.ts` files now enter the
  program). `npm test`: 21 files, **3,634 passed** (baseline 3,624 + 10
  new guard tests), `VERIFICATION PASSED`.
- **Synthetic proof the guard fires and the three exclusions hold**
  (criteria 1–3): both files include table/describe-driven unit tests
  against in-memory fixture source — not the real tree — so the proof
  doesn't depend on what the tree currently contains.
  - Go: `TestCitationScanExcludesCommentsAndAllowedCitations` (a comment
    citing all three documents, plus CAN-SPAM §7704 and both env var
    names, as string literals — zero violations),
    `TestCitationScanCatchesDocCitation` (a `PRD §6.6` string literal —
    exactly one violation), `TestCitationPatternIgnoresHexColors` (the
    `\b`-boundary proof). All three pass (`go test ./internal/handlers/...
    -run 'TestCitationScan|TestCitationPattern' -v`).
  - web: `citation guard exclusions (synthetic fixtures)` (5 cases: a
    code comment, CAN-SPAM §7704, both env var names, a hex color, and a
    Svelte HTML comment) and `citation guard catches (synthetic
    fixtures)` (4 cases: a `.ts` string literal, a `.ts` template
    literal, rendered Svelte template text, and a Svelte `<script>`
    string literal — each citing one of the three forbidden patterns).
    All 9 pass alongside the real-tree scan (10 tests total in
    `citationGuard.test.ts`).
- **Real-regression proof the guard fires** (criterion 4, "restore one of
  `#0178`'s pre-fix strings temporarily and watch it fire"): copied
  `internal/mailing/preflight.go` and
  `web/src/views/admin/WorkshopEditor.svelte` aside first (§8a), then in
  turn:
  - Restored `preflight.go:171`'s pre-`#0178` wording ("Reply-To address
    is not configured (EMAIL_REPLY_TO). PRD §6.6 requires a monitored
    reply address, not noreply@.") — `go test
    ./internal/handlers/... -run TestNoAdminFacingStringCitesInternalDocs
    -v` failed, reporting exactly that literal at `preflight.go:171:4`.
    Restored the file from the pre-edit copy; `git diff --stat` showed no
    change to it afterward (byte-identical), and the test passed again.
  - Restored `WorkshopEditor.svelte`'s pre-`#0172` cover-image hint
    (`"...there is no upload control here (#0138: no upload endpoint
    exists, by design -- see PRD §5.2) and an external URL is not
    accepted (CLAUDE.md §9: no external CDNs)."`) — `npx vitest run
    src/lib/citationGuard.test.ts` failed, reporting that literal at
    `WorkshopEditor.svelte:547`. Restored the file from the pre-edit
    copy; `git diff --stat` showed no change afterward, and the test
    passed again (10/10).
- `git status --porcelain` before commit showed only my five files plus
  another in-progress agent's `web/src/lib/sendConfirm.test.ts` (owned by
  a concurrent `#0179`/`#0180` review, per dispatch instructions) — left
  untouched, not staged, not committed.
- `pgrep -f 'go test|\.test |vitest'` empty after every run.
  `scripts/testdb.sh gc` reported no scratch databases (already dropped
  by `scripts/check.sh`'s own exit trap).

### Verification pass 2

- `uptime`: 1.6–2.1 across this pass, well under the ~8 threshold.
- **Go real-tree run:** `ISSUE=0181b scripts/check.sh go ./internal/...` —
  build and vet clean, all 15 packages `ok` with real elapsed times
  (`handlers` 2.828s, `interests` 2.658s, `mailing` 0.850s — no DB-backed
  package reported 0.00s), zero skips, `VERIFICATION PASSED`. Confirms the
  widened `internal/`+`cmd/` walk and the `Issues.md`/`HANDOFF.md`/`docs/*.md`
  pattern additions produce zero real-tree hits.
- **Web check:** `scripts/check.sh web` — `npm run check`: **232 FILES 0
  ERRORS 0 WARNINGS** (back down from 358; confirms the `import.meta.glob`
  replacement type-checks clean under the original narrow `types: ["svelte",
  "vite/client"]`, with no `@types/node`). `npm test`: 21 files, **3,651
  passed** — `citationGuard.test.ts` itself grew from 10 to 16 tests (the 6
  new fixtures below); the remainder of the delta versus the dispatch's
  quoted 3,640 baseline is other agents' work landed on `main` since pass 1
  (`#0182`/`#0183`/`#0184`), not anything in this pass.
- **New Svelte coverage, all four bounced expression forms, proved firing**
  (`npx vitest run src/lib/citationGuard.test.ts`, synthetic fixtures, all
  pass):
  - `<p>{'see PRD §6.6'}</p>` — string literal expression — **catches**.
  - `` <p>{cond ? 'see CLAUDE.md §9' : ''}</p> `` — ternary — **catches**.
  - `` <p>{`see #0138`}</p> `` — template literal expression — **catches**.
  - `` {@html 'see PRD §6.6'} `` — **catches**.
  - Plus one fixture for `Issues.md`/`HANDOFF.md`/`docs/*.md` catching, and
    one for a comment citing all three new terms *not* tripping the guard.
- **Real-string regression re-run, Go** (proves the bounce fix didn't break
  what pass 1 already verified): copied `internal/mailing/preflight.go`
  aside (§8a), restored the pre-`#0178` wording ("… PRD §6.6 requires a
  monitored reply address, not noreply@."). `go test
  ./internal/handlers/... -run TestNoAdminFacingStringCitesInternalDocs -v`
  failed, reporting exactly that literal at `preflight.go:172:4`. Restored
  from the copy; `git status --porcelain` on the file was empty afterward
  (byte-identical), and the test passed again.
- **Real-string regression re-run, Svelte:** copied
  `web/src/views/admin/WorkshopEditor.svelte` aside, restored `#0172`'s
  pre-fix cover-image hint ("… #0138: no upload endpoint exists, by design
  -- see PRD §5.2 … CLAUDE.md §9: no external CDNs."). `npx vitest run
  src/lib/citationGuard.test.ts` failed, reporting it at
  `WorkshopEditor.svelte:552`. Restored from the copy; `git status
  --porcelain` on the file was empty afterward, guard green again (16/16).
- **Exclusions re-confirmed against the real tree**: the Go real-tree run
  above passing *is* the exclusion proof — every `CAN-SPAM §7704` literal,
  every `EMAIL_REPLY_TO`/`EMAIL_LIST_DOMAIN` literal, and every comment
  citing `CLAUDE.md`/`PRD §`/an issue number/`Issues.md`/`HANDOFF.md`/
  `docs/*.md` across all of `internal/` and `cmd/` produced zero violations.
  Synthetic fixtures additionally cover the new terms specifically (see
  above).
- `git diff --cached --name-only` before commit showed exactly the five
  intended files (`internal/handlers/citation_guard_test.go`,
  `web/package-lock.json`, `web/package.json`,
  `web/src/lib/citationGuard.test.ts`, `web/tsconfig.json`); the pathspec was
  passed explicitly to both `git add` and `git commit`.
- `pgrep -f 'go test|\.test |vitest'` empty after every run. No
  `opencircuit_test_0181*` scratch database left behind (dropped by
  `scripts/check.sh`'s own exit trap).

## Files changed

- `internal/handlers/citation_guard_test.go` (new) — the Go guard:
  `TestNoAdminFacingStringCitesInternalDocs` plus three synthetic-fixture
  proof tests.
- `web/src/lib/citationGuard.test.ts` (new) — the web guard: the
  real-tree scan plus nine synthetic-fixture proof tests.
- `web/package.json`, `web/package-lock.json` — added `@types/node`
  devDependency.
- `web/tsconfig.json` — added `"node"` to the `types` array so
  `svelte-check` resolves the guard's `node:fs`/`node:path`/`node:url`
  imports.

Code commit: `920a959` — "#0181 Add a guard that fails if any
admin-facing message cites an internal document".

## Gotchas

- **The three-package boundary from `#0178`'s review is about who writes
  the HTTP body, not who authors the string.** `internal/mailing` writes
  no response body itself but is where `#0178`'s actual leak lived
  (`PreflightFailure.Message`, serialized untouched by a handler). A
  guard that trusted the "only handlers/middleware/seo write bodies"
  framing literally, without tracing where the *literals* originate,
  would have shipped with a hole exactly the shape of the bug it exists
  to prevent. Worth remembering for any future package that gains a
  similar "authored elsewhere, serialized here" field.
- **The `\b` word-boundary is load-bearing, not decorative.** Without it,
  `#0[0-9]{3}` alone matches inside `internal/mailing/templates.go`'s
  `colorButtonText = "#04140a"` (a real string literal, not a comment) —
  a false positive that would have broken the guard on the very first
  run had the pattern been written the more obvious way.
- **`svelte-check`'s `types` array is a strict allowlist, not a
  starting point.** Adding `@types/node` to `node_modules` alone did
  nothing; `tsconfig.json`'s `"types": [...]` array had to name `"node"`
  explicitly or `svelte-check` kept reporting `Cannot find module
  'node:fs'` even though the package was installed and `vitest` ran the
  file fine (Vite/esbuild don't type-check imports the way `tsc` does).
- **AST-based exclusion of comments has no failure mode a regex-based
  one would.** An earlier design considered stripping comments with
  regex before pattern-matching raw text; discarded because `//` inside
  a legitimate string (e.g. a URL) would have been misread as a comment
  start, silently under-scanning. Parsing properly (`go/ast`,
  TypeScript's compiler, `svelte/compiler`) sidesteps the whole class of
  edge case.

## Notes

Reported by `#0178`'s phase-3 review under `CLAUDE.md` §9.

Worth noting what makes this worth building where `#0176` was not: that issue proposed hardening a guard against inputs the repo does not produce, five passes in. This one closes a class that has actually recurred three times, and the assertion is a few lines rather than a parser.

## Relation

- Found by: [#0178](0178.md) — Go-authored strings citing internal documents
- Relates to: [#0172](0172.md) — the first sweep, in `web/`
- Relates to: [#0175](0175.md) — the second, which drew the CAN-SPAM line

## Review notes — 2026-08-23 (phase 3, **bounced**)

Re-verified independently at `57d6d92` in a throwaway worktree (`node_modules`
copied in, not symlinked). `uptime` 1.70–2.72 across every run, far under the
~8 threshold. Two other agents were live in the main tree
(`internal/mailing/worker.go` dirty, `issues/0183.md` staged) — reading the
working copy would have measured a different tree, which is why nothing here
was run in the main checkout.

**The verification reproduces exactly, and the guard is real.** The bounce is
for one demonstrated blind spot in the `web/` half, described in full below.

### Verification reproduced

- `ISSUE=0181rev scripts/check.sh go ./internal/...` — build and vet clean,
  **all 15 packages `ok`** with real elapsed times (`handlers` 2.602s,
  `interests` 2.472s, `mailing` 0.821s, `middleware` 0.793s, `auth` 0.704s —
  no DB-backed package reported 0.00s), skip audit `(no skips)`,
  `VERIFICATION PASSED`.
- `scripts/check.sh web` — `358 FILES 0 ERRORS 0 WARNINGS`, `21 passed (21)` /
  **`3634 passed (3634)`**, the reported figure exactly.
- **Real-string proof, Go, re-done by hand.** Copied `preflight.go` aside
  first (§8a), restored the pre-`#0178` wording ("… PRD §6.6 requires a
  monitored reply address, not noreply@."), and
  `TestNoAdminFacingStringCitesInternalDocs` failed reporting exactly that
  literal at `preflight.go:172:4`. Restored from the copy; `git status
  --porcelain` empty afterwards (byte-identical), guard green again.
- **Real-string proof, Svelte, re-done by hand.** Same procedure on
  `WorkshopEditor.svelte`, restoring `#0172`'s pre-fix cover-image hint. The
  web guard failed reporting it at `WorkshopEditor.svelte:552` — and the
  HTML comment three lines above it, which cites `CLAUDE.md §9` and `#0172`,
  did **not** trip. That is the comment exclusion proved against the real
  tree rather than a fixture. Restored byte-identically; 10/10 green again.
- **Stray-`.go` sweep:** 207 `.go` files on disk, 207 tracked, zero
  untracked anywhere in the repo. Nothing left behind.
- Worktree removed, `opencircuit_test_0181rev` dropped, `pgrep -f 'go
  test|\.test |vitest'` empty. (`opencircuit_test_0182brev` remains — the
  concurrent `#0182` reviewer's, left alone.)

### What must be fixed before this resolves

**The Svelte scanner reads only `Text` nodes in the fragment, so a string
literal inside a template expression is never scanned.** `scanSvelteSource`
walks `ast.fragment` for `Text` and `ast.instance` / `ast.module` for
`Literal` / `TemplateElement` — but an `ExpressionTag` in the markup carries
its own ESTree subtree, which belongs to the fragment and contains neither.
Measured, not reasoned (probe against the shipped scan functions):

| Shape | Result |
|---|---|
| `<p>see PRD §6.6</p>` | CATCH |
| `<p title="see PRD §6.6">` | CATCH |
| `<Foo msg="see PRD §6.6" />` | CATCH |
| `<script>` / `<script module>` literal | CATCH |
| `<p>{'see PRD §6.6'}</p>` | **MISS** |
| `<p>{cond ? 'see CLAUDE.md §9' : ''}</p>` | **MISS** |
| ``<p>{`see #0138`}</p>`` | **MISS** |
| `{@html 'see PRD §6.6'}` | **MISS** |

This is not a hypothetical shape. `web/src` renders admin-facing copy from a
template-expression literal **38 times** across 11 components — 17 in
`Admin.svelte`, 6 in `WorkshopEditor.svelte`, which is one of the two files
`#0172` fixed. Every one of them today is a short button label
(`{saving ? 'Saving…' : 'Save'}`), so nothing is leaking right now; the
defect is that the guard would sleep through it if one ever did.

That matters more here than it would for an ordinary test, because of what
this issue claims. `#0181` exists to end a pattern where each sweep was
"thorough within its method and blind outside it," and to let the next
reviewer stop sweeping. A guard with a blind spot in `.svelte` markup — the
file type that produced `#0172`'s two original findings — will be trusted as
complete when it is not, which is a worse position than no guard. The Go
half has no analogous gap (it walks every `*ast.BasicLit` in the file), and
neither does the `.ts` scanner.

**The fix is small.** Collect `Literal` and `TemplateElement` from
`ast.fragment` as well as `Text`, reusing the value extraction the
instance/module loop already has. No false-positive risk: non-string
`Literal` values are already filtered by the `typeof n.value === 'string'`
check, and a class-name or numeric literal cannot match the pattern. Add a
fixture case for the ternary shape alongside the existing nine.

### Recommended in the same pass, not required

1. **Replace `@types/node` with `import.meta.glob`.** Ruling on the
   dependency change: acceptable as shipped, but a strictly better option
   exists and I proved it works in this repo. See the section below.
2. **Widen the Go guard from four directories to all non-test `.go` under
   `internal/` and `cmd/`.** See the boundary section below.
3. **`PRD\s*§` instead of `PRD §`** — closes the `PRD  §6.6` (two spaces)
   and `PRD§6.6` (no space) variants at zero cost. Both currently miss.

### Evasion attempts — should-catch vs. acceptable-miss

Run against the shipped `citationPattern` and both shipped scanners.

**Should catch, and does:** every single-literal form of all three patterns;
`#0138's` (the `\b` succeeds against the apostrophe — the most common real
form); a Go raw backtick literal; `` `see PRD §6.6 for ${x}` `` via
`TemplateHead`; `` `for ${x} see PRD §6.6` `` via `TemplateTail`.

**Acceptable misses — the guard is not an adversary detector, and none of
these is a shape anyone writes by accident:**

- `"see CLAUDE" + ".md §9"` and `"see #0" + "138"` — a citation split across
  a concatenation. Nobody splits a sentence mid-token when writing copy;
  catching it would require constant folding for no realistic gain.
- `` `see PRD ${s}§6.6` `` — an interpolation inside the token, same reasoning.
- `fmt.Sprintf("see PRD %s6.6", "§")` — same.
- `"see issue 0138"` and `"see PRD section 6.6"` in words — these do not cite
  a file path or a section anchor; an admin reading "issue 0138" learns
  nothing they could look up, but it is also not the defect class, which is
  about *pointing at a document*. Out of scope by the acceptance criteria.
- `"see #138"` unpadded — the repo's convention is four-digit zero-padded
  everywhere (`#0172`, `#0175`, `#0178`), and all five historical leaks used
  the padded form. Widening to `#[0-9]{1,4}` would false-positive on ordinary
  prose ("#1 priority").
- `claude.md` / `CLAUDE.MD` case variants — the correct case is what people
  type, and the file is literally named `CLAUDE.md`.

**Borderline misses, worth a cheap fix (recommendation 3):** `PRD  §6.6`
with two spaces and `PRD§6.6` with none. Both are plausible typography a
human could produce in copy, and `PRD\s*§` costs nothing.

**Out-of-pattern but arguably in-class (report only, not in the criteria):**
`Issues.md`, `HANDOFF.md`, and `docs/*.md` are internal documents an admin
cannot read either, and `#0178`'s own whole-tree grep included
`Issues\.md|HANDOFF` in its pattern. The acceptance criteria name only three
patterns, so this is in-spec as shipped — flagging it as an orchestrator
call, not a defect.

### False-positive sweep

The `\b` is load-bearing exactly as the Gotchas say, and the pattern's only
false-positive shape is **`#0` followed by exactly three digits and then a
non-word character**. Enumerated:

| Shape | Result |
|---|---|
| `#04140a` (6-digit hex, the real `colorButtonText`) | safe |
| `#0142ab`, any 6-digit hex starting `#0` | safe |
| `#012` (3-digit hex) | safe |
| `#01234567` (8-digit hex) | safe |
| `#0123` (**4-digit RGBA shorthand, valid CSS Color 4**) | **would fire** |
| `"element #0123"` (an id in prose) | would fire |

Swept for that shape and found nothing. Every `#0NNN` occurrence in the four
scanned packages is a `//` comment citing an issue — the guard passing proves
no string literal among them. `.svelte` `<style>` blocks live on `ast.css`,
a sibling of `ast.fragment`, so they are never walked; `walk()` skips `.css`
entirely, and `web/src` contains only `.css`, `.svelte`, `.ts` (one `.d.ts`,
correctly skipped). No 4-digit numeric hex anywhere in the tree. The
exclusions were also confirmed against the **real tree**, not just fixtures:
`preflight.go:171` carries CAN-SPAM §7704 in a live literal in a scanned
package, `preflight.go:176` carries `EMAIL_REPLY_TO`, `preflight.go:175` is a
`// PRD §6.6` comment three lines from a scanned literal, and
`WorkshopEditor.svelte:550` is an HTML comment citing both `CLAUDE.md §9` and
`#0172`. All four pass.

### The four-package boundary — right, and worth widening anyway

**The implementer's reasoning for adding `internal/mailing` is correct**, and
I re-derived it the way `#0178`'s review did, backwards from the render path.
`PreflightFailure.Message` is authored entirely in `preflight.go` and
serialized untouched, so a guard scoped to the three body-writing packages
would not have caught the `#0178` leak it exists to prevent. Extending was
right.

Tracing for a fifth package: only five `.Error()` interpolations reach a
written body (`admin_workshop_preview.go:87`,
`admin_campaign_preview.go:314,383,406`, `health.go:56`), and their sources
are goldmark, the SES mailer (`internal/mailing`, in scope) and pgx — no
in-repo package outside the four. Only one Sprintf-into-`writeError`
(`admin_campaigns.go:671`, in `internal/handlers`). The only
`Message string \`json` field outside the four is
`internal/sesnotify/message.go:31`, which is the **inbound** SNS envelope,
not outbound copy. `internal/audit` authors machine action codes, not prose.
So **four is the complete set today.**

But I also ran the pattern — widened to include `Issues.md`, `HANDOFF.md`
and `docs/*.md` — over **every** non-test `.go` string literal in the whole
repo, `cmd/` included: **zero hits.** There is no legitimate reason for any
non-test Go string literal in this project to cite an internal document, so
the four-directory scope buys nothing and costs a maintenance hazard: a
future package that gains an authored-here-serialized-there field falls
silently outside. That hazard already fired once, on this guard's first
outing — `internal/mailing` had to be reasoned in by hand. Walking
`internal/` and `cmd/` recursively is the same cost and needs no boundary
argument to stay correct. Recommended, not required.

### Ruling on the `@types/node` / `tsconfig.json` change

**`package-lock.json`'s diff is only the new dev dependency** — `@types/node`
26.2.0 and its single transitive `undici-types` 8.3.0, both `"dev": true`.
Nothing else moved.

**The 358-file count is fully explained and hides nothing.** Measured
directly rather than inferred: reverting `types` to `["svelte",
"vite/client"]` with the guard file removed gives `230 FILES 0 ERRORS`; with
the guard file present but no `"node"`, `232 FILES` and exactly the three
`Cannot find module 'node:fs'/'node:path'/'node:url'` errors (that 232 is
where the drafted Verification's figure came from — the true pre-change
baseline is 230, matching `#0178`'s review). Adding `"node"` takes it to
`358 FILES 0 ERRORS 0 WARNINGS`. The +126 is `@types/node`'s and
`undici-types`' reachable `.d.ts` set. Zero errors before and after.

**The cost is real and I measured it, twice.** With `"node"` in `types`:

- `export const p = process.env.NODE_ENV; export const b = Buffer.from('x');`
  in an ordinary `web/src/lib` module type-checks **clean**. That is a
  runtime crash in a browser bundle that `npm run check` now waves through.
- `export const id: number = setTimeout(() => {}, 10);` now **fails** with
  `Type 'Timeout' is not assignable to type 'number'` — correct browser code,
  rejected, because the Node overload wins. Nothing in the tree does this
  today, which is why the run is green, but the next debounce someone writes
  hits it.

**A `/// <reference types="node" />` is not a narrower alternative** — I
tested it: identical `358 FILES`, and `process`/`Buffer` still type-check
clean in app code, because `@types/node`'s global declarations are
program-wide however they enter. A per-test `tsconfig` would narrow it, but
`svelte-check` takes one `--tsconfig`, so the guard would then be excluded
from `npm run check` entirely.

**The genuinely narrower option is to not read the filesystem at all**, and I
verified it works in this repo:

```ts
const files = import.meta.glob('../**/*.{ts,svelte}',
  { query: '?raw', import: 'default', eager: true }) as Record<string, string>;
```

82 entries (35 `.svelte`, 47 `.ts`), full raw source text, keys already the
relative paths the failure message prints. It type-checks under the original
narrow `types` array, needs no `@types/node`, no `tsconfig.json` change, and
deletes `walk()`, `readdirSync`, `statSync`, `readFileSync` and
`fileURLToPath` outright. `vite/client` already declares `import.meta.glob`.

**Ruling: acceptable as shipped — this alone would not have bounced the
issue.** But since the file is being reopened for the Svelte gap anyway, do
it in the same pass and revert `web/package.json`, `web/package-lock.json`
and `web/tsconfig.json`. If a later pass decides the `?raw` glob is worse for
some reason I have not seen, keeping `@types/node` is defensible — record
why, and note the `setTimeout` sharp edge in the Gotchas so the next agent
does not lose an hour to it.

### What the next pass must do

1. Scan ESTree string/template literals inside `.svelte` template
   expressions, plus a fixture for the ternary shape. **Required.**
2. Re-run `ISSUE=NNNN scripts/check.sh go ./internal/...` and
   `scripts/check.sh web`, and re-do the `WorkshopEditor.svelte` real-string
   proof (copy aside first, restore byte-identically).
3. Optionally 1–3 from "Recommended in the same pass".

Nothing in the Go half needs to change. Do not re-derive the boundary or
re-do the Go real-string proof — both are confirmed above.

## Review notes (pass 2) — 2026-08-23 (phase 3, **passed**)

Re-verified independently at `5a116f5` in a throwaway worktree, with `web/node_modules`
built by `npm ci` **from the reverted lockfile** rather than symlinked — deliberately,
because the main checkout's `node_modules` still physically contains `@types/node` and
`undici-types` left over from pass 1, and symlinking would have made the revert
unfalsifiable. The worktree's `node_modules/@types` is `chai`, `deep-eql`, `estree`,
`trusted-types` — no `node`. Everything below therefore proves the guard type-checks and
runs with the dependency genuinely gone. `uptime` 1.46–2.72 across every run, far under
the ~8 threshold. The main tree was dirty with `#0182`'s in-flight
`internal/mailing/{worker,ses_mailer}.go`, which is the other reason nothing was run in it.

**Result: pass.** The bounce is fixed, the fix is broader than the bounce asked for, and
the three recommended items were all taken correctly. Six small notes for the
orchestrator at the end — none of them a defect in this issue.

### Verification reproduced

- `ISSUE=0181rev2 scripts/check.sh go ./internal/...` — build and vet clean, **all 15
  packages `ok`** with real elapsed times (`handlers` 2.579s, `interests` 2.442s,
  `mailing` 0.842s, `middleware` 0.817s, `auth` 0.726s — no DB-backed package reported
  0.00s), skip audit `(no skips)`, `VERIFICATION PASSED`.
- `scripts/check.sh web` — **`232 FILES 0 ERRORS 0 WARNINGS`**, `21 passed (21)` /
  **`3651 passed (3651)`**. Both figures exactly as reported, and the 232 is the true
  pre-issue baseline plus the two new source files (pass 1's reviewer measured 230
  without the guard file, 232 with it).
- `web/package.json`, `web/package-lock.json` and `web/tsconfig.json` at `5a116f5` are
  **SHA-256-identical to their `920a959^` versions**. Not "reverted in spirit" —
  byte-identical. Confirmed for all three.

### Test arithmetic, reconciled by measurement

The implementer's 3,651 is right, and its attribution is right. Rather than accept the
sum, I ran the web suite at each intervening revision in the worktree:

| Revision | Tests | Delta |
|---|---|---|
| `920a959` — guard, pass 1 | 3,634 | (pass-1 reviewer's independent figure) |
| `e5192de` — `#0183` | 3,640 | +6 |
| `372aae4` — `#0184` | 3,645 | +5 |
| `a4893ef` — `#0184` work log | 3,645 | +0 |
| `5a116f5` — this fix | **3,651** | **+6** |

The +6 at `5a116f5` is exactly `citationGuard.test.ts` growing 10 → 16, measured
directly (`npx vitest run src/lib/citationGuard.test.ts` → 16 passed). Nothing else in
the suite moved in this commit. `#0183` filed "four one-line test cases" that expand to
six test instances, which is where the arithmetic looks off by two if you count commit
messages instead of running them; the implementer's own note ("baseline 3,640") was
comparing against `e5192de` rather than the immediate parent `a4893ef` (3,645), so its
prose is imprecise while its conclusion is correct.

### The bounce fix — all four forms re-proved, then pushed much further

Probed against the **shipped** `scanSvelteSource`, extracted verbatim from
`citationGuard.test.ts` by `sed` (no retyping of the scanner) into a scratch probe file,
with the shipped `describe` blocks stripped so only the scanner ran. 24 shapes:

| Shape | Result | Classification |
|---|---|---|
| `<p>{'see PRD §6.6'}</p>` | **CATCH** | should-catch — the bounce |
| `<p>{cond ? 'see CLAUDE.md §9' : ''}</p>` | **CATCH** | should-catch — the bounce |
| ``<p>{`see #0138`}</p>`` | **CATCH** | should-catch — the bounce |
| `{@html 'see PRD §6.6'}` | **CATCH** | should-catch — the bounce |
| ``{`see #0138 for ${x}`}`` — interpolation elsewhere in the same string | **CATCH** | should-catch |
| ``{`x ${c ? "see PRD §6.6" : ""} y`}`` — literal *inside* the interpolation | **CATCH** | should-catch |
| `<Foo opts={{ hint: 'see PRD §6.6' }} />` — object literal in a prop | **CATCH** | should-catch |
| `<Foo items={['see PRD §6.6']} />` — array in a prop | **CATCH** | should-catch |
| `<p title={'see PRD §6.6'}>` — attribute expression | **CATCH** | should-catch |
| `{#if s === 'see PRD §6.6'}` — block expression | **CATCH** | should-catch |
| `{#if s}…{:else}<p>{'see PRD §6.6'}</p>{/if}` | **CATCH** | should-catch |
| `{#each ['see PRD §6.6'] as i}` | **CATCH** | should-catch |
| `{@const m = 'see PRD §6.6'}` inside an each | **CATCH** | should-catch |
| `{#snippet hint()}<p>see PRD §6.6</p>{/snippet}` — snippet body text | **CATCH** | should-catch |
| `{#snippet hint()}<p>{'see PRD §6.6'}</p>{/snippet}` — snippet body expression | **CATCH** | should-catch |
| `{@render hint('see PRD §6.6')}` — render argument | **CATCH** | should-catch |
| `{#await p then v}<p>{'see PRD §6.6'}</p>{/await}` | **CATCH** | should-catch |
| `<p>{f('see PRD §6.6')}</p>` — call argument | **CATCH** | should-catch |
| `<p>{'see docs/architecture.md'}</p>` — new pattern term via an expression | **CATCH** | should-catch |
| `<p>{/* see PRD §6.6 and #0138 */ 'ok'}</p>` — JS comment **inside** an expression tag | MISS | **correct** — comment |
| `<script>` `// see PRD §6.6 …` | MISS | correct — comment |
| `<!-- see PRD §6.6 / CLAUDE.md §9 / #0138 / Issues.md / docs/x.md -->` | MISS | correct — comment |
| `<style>.x { color: #04140a; }</style>` | MISS | correct — `ast.css` is never walked |
| `{'CAN-SPAM §7704 … EMAIL_REPLY_TO … EMAIL_LIST_DOMAIN'}` in an expression | MISS | correct — by construction |

The four bounced forms all catch. The interesting result is the twelve *further* shapes:
because `collectByType` walks the whole fragment tree by node `type` rather than by
structural position, every nested ESTree form — object, array, call argument, snippet
body, `{@const}`, block-tag test expressions, `{#await}` branches — is covered for free.
There is no shape I could construct where a string literal renders in Svelte markup and
the scanner does not see it. Equally important, **C1 is the regression the widening could
have caused and did not**: a JS comment inside an expression tag is still not a
`Literal`/`TemplateElement`, so it stays excluded structurally.

`{@html}` deserves a note: it catches, but a citation reaching `{@html}` is almost always
via a variable rather than a literal, so this is coverage of the shape, not of the risk.

### The three original exclusions still hold — against the real tree, not fixtures

The passing `scripts/check.sh go ./internal/...` over all 94 non-test `.go` files *is*
the exclusion proof, and I confirmed the specific live instances still exist to be
excluded: `preflight.go:167` carries `CAN-SPAM §7704` and `preflight.go:172`
`EMAIL_REPLY_TO` (and `:185` `EMAIL_LIST_DOMAIN`) in live literals in a scanned package;
`preflight.go:170` is a live `// PRD §6.6` comment two lines above one of those literals,
with three more `PRD §6.6` comments at `:1`, `:92` and `:111` in the same file; and `WorkshopEditor.svelte:547–551` is an HTML
comment citing `#0138`, `#0052`, `#0153`, `PRD §5.2` and `CLAUDE.md §9` — five citations,
directly above admin-facing copy, all silent.

### False-positive sweep, including the new `docs/\S*\.md` term

Zero real-tree hits, and I checked *why* rather than only *that*. Every occurrence of the
three new terms anywhere in scanned scope is a comment:

- Scanned Go (`internal/`, `cmd/`, non-test): exactly one `docs/` occurrence,
  `cmd/opencircuit/seed.go:20`, a `//` comment. Zero `Issues.md`/`HANDOFF.md`.
- `web/src`: `docs/frontend.md` appears twice — `app.css:77` (a `.css` file, never
  scanned) and `Logo.svelte:11` (a `//` comment in the instance script). The only other
  `.md`-shaped hits are `assets/logo/README.md` in comments and `issues/0180.md` /
  `issues/0171.md` in `workshopAdmin.test.ts` comments (a `.test.ts`, skipped twice
  over).

Pattern-level enumeration of what the new term *would* fire on: any string containing
`docs/` + non-whitespace + `.md`. That includes `https://example.com/docs/guide.md` — an
external URL — which is the one realistic false positive the widening introduces. It is
also nil in this project today and `CLAUDE.md` §9 forbids external CDNs, so the shape has
nowhere to come from. Confirmed safe: `docs.google.com/...`, `"docs/ guide.md"` (space
breaks `\S*`), and prose like "read the docs or any .md file" all pass.

The `PRD\s*§` widening does what it claims: `PRD §6.6`, `PRD  §6.6`, `PRD§6.6` and even
`PRD\n§6.6` all fire; the pass-1 acceptable-misses are unchanged (`"see CLAUDE" + ".md"`
concatenation, `` `see PRD ${s}§6.6` ``, `#138` unpadded, `claude.md` case variants — all
still pass, all still correctly out of scope). The `\b` boundary still holds: `#04140a`
and `#012` pass, `#0123` (4-digit RGBA shorthand) still fires, and there is still no
4-digit hex in the tree.

### Go scope: the widened walk covers exactly what it should

Measured, not reasoned. A scratch test reusing the shipped `scanGoRoots` and the shipped
walk predicate collected the file list: **94 files walked, 94 non-test `.go` files on
disk under `internal/` + `cmd/`, set-identical, zero difference in either direction.**

- `_test.go` excluded — load-bearing, because `citation_guard_test.go` itself contains
  `"… (see #0172, #0175, #0178, #0181)"` as a live string literal and would self-trip.
- No vendored code (`vendor/` does not exist) and no generated code (zero
  `Code generated … DO NOT EDIT` files in the repo).
- `testdata/` recursion is harmless: `internal/mailing/testdata` and the repo-root
  `testdata` contain zero `.go` files (html/txt golden files and one JSON fixture).
- All 94 parsed cleanly under `parser.ParseFile`, so the walk's `t.Fatalf` on parse error
  never triggers.

### `import.meta.glob` versus the old walk — coverage is equal, measured both ways

This was the item most worth checking, since a glob that silently misses a directory
would reproduce this issue's own failure mode. A scratch probe dumped the glob's keys:

| | Count |
|---|---|
| `web/src` `.ts` + `.svelte` on disk | **82** (47 `.ts`, 35 `.svelte`) |
| Keys yielded by `import.meta.glob('../**/*.{ts,svelte}')` | **82** (47 `.ts`, 35 `.svelte`) |
| Set difference, either direction | **0** |
| Actually scanned after the `.test.ts` / `.d.ts` skips | **60** |
| Files the old `walk()` scanned (same root, same skips, same self-exclusion) | **60** |

Directories represented in the keys: `lib/`, `views/`, `views/admin/`, plus the two
top-level files `App.svelte` and `main.ts` — the `**` does match zero directories, which
was the specific way a glob could have quietly dropped `main.ts`. It does not.

One implicit property, measured rather than assumed: **Vite's `import.meta.glob` excludes
the importing module itself.** With the probe file on disk there were 83 files and the
glob returned 82, the missing one being the probe. Harmless here — the importer is
`citationGuard.test.ts`, already skipped by the `.test.ts` filter, exactly as the old
walk's explicit `if (path === THIS_FILE) continue` did — but it is undocumented in the
file and worth knowing if the guard is ever moved into a non-test module.

### Both regression proofs re-run by hand

Copied each file aside first (§8a), in the worktree, so nothing in the main checkout was
touched.

- **Go.** Restored `preflight.go`'s pre-`#0178` wording ("… `PRD §6.6` requires a
  monitored reply address, not noreply@."). `TestNoAdminFacingStringCitesInternalDocs`
  failed reporting exactly that literal at `preflight.go:172:4`. Restored from the copy;
  `git status --porcelain` on the file empty afterward, guard green again.
- **Svelte.** Restored `WorkshopEditor.svelte`'s pre-`#0172` cover-image hint. The web
  guard failed reporting it at `views/admin/WorkshopEditor.svelte:552`, and the
  five-citation HTML comment immediately above it stayed silent. Restored from the copy;
  status empty, 16/16 green again.
- **Failure-message path, checked deliberately** by planting a citation in
  `web/src/lib/branding.ts` at the same time. Both paths are usable:
  `views/admin/WorkshopEditor.svelte:552` and `./branding.ts:6`, with correct line
  numbers. The Go side prints an absolute path — verbose but unambiguous.

### Notes for the orchestrator — report only, none of them a defect here

1. **`web/embed.go` is the one non-test `.go` file outside the walk's roots.** The walk
   covers `internal/` and `cmd/`; `web/embed.go` is neither. It authors a single panic
   string about a malformed embedded FS, which is a compile-time guarantee rather than
   admin-facing copy, so nothing is exposed. But the guard's comment says "every non-test
   `.go` file", and that is 94 of 95. Adding `"../../web"` to `scanGoRoots` would close
   it for one line.
2. **`issues/NNNN.md` paths are not in the widened pattern, though `docs/*.md` now is.**
   `"see issues/0181.md"` passes the guard while `"see docs/architecture.md"` fails it —
   an asymmetry, since a tracker file is the *least* readable document of the set. Low
   value in practice (the repo's convention is the `#0181` form, which does fire), but it
   is a gap in the "guard one internal document, guard them all" argument the pass-2
   comment makes.
3. **`docs/\S*\.md` fires on an external URL containing `/docs/<name>.md`.** Nil risk
   today; recorded so a future reviewer who hits it knows it is the pattern, not a leak.
4. **The failure message's path form is inconsistent for `web/src/lib/*`.** The
   normalization `path.replace(/^\.\.\//, '')` only handles the `../` keys; same-directory
   keys stay as `./branding.ts`, which is relative to `web/src/lib/` rather than to
   `web/src` like every other row. Cosmetic.
5. **`## Files changed` above is now stale.** It still says `web/package.json`,
   `web/package-lock.json` and `web/tsconfig.json` were changed to add `@types/node`;
   pass 2 reverted all three to byte-identical, so the issue's net diff is two new test
   files and nothing else. Left as written rather than edited, since the `### Fix pass 2`
   section records the revert accurately — but a reader of the summary section alone
   would be misled.
6. **The main checkout's `web/node_modules` is out of sync with the lockfile**, carrying
   `@types/node` and `undici-types` that `npm ci` would not install. Harmless (nothing
   references them, and `tsconfig`'s `types` allowlist keeps them out of the program —
   which the worktree's clean `npm ci` proves independently), but the next agent who
   wonders why `@types/node` is on disk after the revert has the answer here. A
   `npm ci` in `web/` clears it.

### Acceptance criteria

All six met. Criterion 5 ("decide Go, web, or both, and record why") is answered in the
`## Fix` section and now demonstrated on both halves. Criterion 4's real-string proof is
independently re-done above for both languages.

Worktree removed, `opencircuit_test_0181rev2` dropped by `scripts/check.sh`'s exit trap
(only `opencircuit_test_template` remains), `pgrep -f 'go test|\.test |vitest'` empty,
main checkout untouched — the two dirty files in it (`internal/mailing/worker.go`,
`internal/mailing/ses_mailer.go`) are `#0182`'s and were never read from, written to, or
staged.


## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 122 | 5,122 | 6,382,419 | 120,097 | $2.42 |
| 2026-08-23 | review (bounced) | claude-opus-5 | 94 | 1,491 | 4,151,198 | 113,975 | $2.83 |
| 2026-08-23 | implement (pass 2) | claude-sonnet-5 | 106 | 6,324 | 4,716,831 | 93,619 | $1.85 |
| 2026-08-23 | review (pass 2) | claude-opus-5 | 76 | 2,399 | 3,314,847 | 108,160 | $1.94 |

**Total: $9.04**

---

*Correction (orchestrator, 2026-08-23): `## Files changed` above still lists `web/package.json`, `web/package-lock.json` and `web/tsconfig.json` as changed. Pass 2 reverted all three to byte-identical with their pre-`920a959` state (SHA-256 verified by the pass-2 review), so the shipped change touches only the two guard files. Noted rather than rewritten, so the record shows what each pass did.*
