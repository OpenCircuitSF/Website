# 0343 — `admin_attested` is a latent net `-1` waiting for its first writer

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | subscribers / admin |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | — |

## Description

`ConsentBasisAdminAttested` is declared in `internal/subscribers/store.go` and
permitted by `migrations/000023`'s CHECK constraint. **Nothing writes it today.**

`#0336` narrowed `imported_30d` to `consent_basis = ConsentBasisImportedPriorConsent`
while `unsubscribed_30d` continues to exclude only `consent_basis IS NULL`. That
asymmetry is correct for the two values that exist (`#0336`'s review measured
it). It is **wrong for the third**.

The first change that writes `admin_attested` onto an import-sourced row gets:

- **no arrival** — `imported_30d` demands exactly `imported_prior_consent`, and
  `confirmed_30d` does not claim it either; and
- **a full departure** — `unsubscribed_30d`'s exclusion only fires on `IS NULL`.

Net `-1` per person. That is `#0311`'s bug reopened, silently, by a change that
was correct when made. Neither the predicate nor `#0336`'s new doc comments
mention the value.

This is filed now, while nothing writes it, precisely because the failure
appears in a *later* issue that looks unrelated — whoever adds the first
`admin_attested` writer will see a growth figure drift and have no reason to
suspect a predicate written months earlier.

## Acceptance criteria

1. `admin_attested` is handled deliberately: either `imported_30d` uses an
   `IN`-list covering every basis that represents a real arrival, or the value
   is explicitly documented as excluded with the reason.
2. Whichever is chosen, the arrival and departure predicates agree about it —
   no basis may be excluded from arrivals while counted as a departure.
3. The doc comments on `Growth30Days`, `RestartSignup`, and
   `web/src/lib/dashboard.ts`'s `formatGrowthDetail` name every `consent_basis`
   value and say which bucket each belongs to, so the next value added cannot
   be silently omitted.
4. A test asserts the three components (`confirmed`, `imported`, `unsubscribed`)
   **separately**, not the net — `#0336`'s review showed a net-only table hides
   compensating errors (its cells O3/P3 netted zero from two opposite errors).
5. Covers a row that reaches `admin_attested` and then unsubscribes, proving it
   nets 0 rather than −1. Prove by mutation against the un-fixed predicate.
6. A guard or test fails if a new `consent_basis` value is added to the CHECK
   constraint without being classified by the predicates. The value set and the
   predicate must not be able to drift apart again.
7. No migration needed; `000001`–`000022` are frozen (§1) and the tree is at
   `000025`.

## Relation

- Found by: [#0336](0336.md)'s review
- Depends on: [#0336](0336.md) — whose narrowing created the asymmetry
- Relates to: [#0311](0311.md) — the bug this silently reopens
- Relates to: [#0324](0324.md), [#0305](0305.md) — the growth-counting family
- Relates to: [#0344](0344.md) — the other unmeasured cell from the same review

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0336`'s review (§9).
- 2026-09-05 — Implemented (Sonnet, phase 2), alongside `#0344` (one commit,
  one review pass — the two findings of `#0336`'s own review, batched per
  CLAUDE.md §4). Chose criterion 1's first option: `imported_30d`'s FILTER
  now reads `consent_basis = ANY($3)` against a package-level slice,
  `growthArrivalConsentBases`, holding both `ConsentBasisImportedPriorConsent`
  and `ConsentBasisAdminAttested`, rather than a single equality. Both values
  represent a standing consent attested by someone OTHER than the subscriber
  (an import batch, or an admin), as opposed to `ConsentBasisDoubleOptIn`
  (the subscriber's own later confirmation, which stays confirmed_30d's
  alone and is never in this slice). `unsubscribed_30d` reads the SAME slice
  in its own departure predicate (this is `#0344`'s generalization, done as
  one shared change — see that issue's own entry for why the two could not
  be fixed separately without reopening the asymmetry this issue is about).
  Criterion 2 (arrival/departure agreement) falls out of sharing one slice
  between both predicates rather than being asserted separately: a value
  cannot be an arrival for one and not the other, structurally.
  `RestartSignup`'s own doc comment was extended to name all three
  CHECK-permitted values explicitly (criterion 3) — its code needed no
  change, since its ELSE branch already clears `consent_basis`
  unconditionally, by value rather than by name.
  Criterion 4 (component test covering admin_attested then unsubscribe,
  netting 0): `TestGrowth30Days_AdminAttestedArrivalAndDeparture`
  (`internal/subscribers/imports_test.go`) seeds a row directly via SQL
  (nothing in this tree writes `admin_attested` yet — this issue's own
  premise), asserting confirmed/imported/unsubscribed SEPARATELY at each
  step rather than a net, per `#0336`'s review: `imported delta = %d, want 1
  — admin_attested must classify as an imported_30d arrival` and, after
  Unsubscribe, `unsubscribed delta = %d, want 1 — the matching departure`,
  with a final explicit net-delta assertion noting "this is #0343's own bug:
  before the fix this read -1". Criterion 5 (mutation proof): temporarily
  reverted the query text and its argument to the pre-fix, single-value
  equality form (`consent_basis = $3` / `ConsentBasisImportedPriorConsent`
  alone) plus the pre-`#0344` departure guard
  (`NOT (source = $2 AND consent_basis IS NULL)`) — the exact `#0336`-era
  predicate this issue describes as "the un-fixed predicate" — confirmed the
  new test fails exactly as predicted (`imported delta = 0, want 1`, `net
  delta = -1`), then restored the file from a pre-mutation copy and verified
  the restored file's SHA-256 matches the pre-mutation hash byte-for-byte
  before re-running the full suite green.
  Criterion 6 (a guard against future drift): `TestConsentBasisValuesAreClassified`
  (`internal/subscribers/imports_test.go`) reads
  `migrations/000023_create_subscriber_imports.up.sql`'s
  `subscribers_consent_basis_check` CHECK constraint directly via regexp — an
  oracle independent of the Go source, not a hand-copied restatement of it —
  and fails, naming the value, if a consent_basis value the CHECK permits
  isn't classified by the test's own `classified` map, or if that map's
  classification disagrees with `growthArrivalConsentBases` itself. Confirmed
  this guard is a real oracle, not a decorative one: reverting the query (the
  same mutation used for criterion 5) does not trip it, because that mutation
  never touches `growthArrivalConsentBases` or the migration file — it is a
  check on the value-set-vs-classification pairing, not on the query text,
  and does not claim to be both.
  No migration needed — `consent_basis`'s CHECK constraint already permitted
  `admin_attested` since `migrations/000023`; the tree is still at `000027`.
  No test targets a literal or seeded id — every row is created fresh via
  `uniqueImportEmail`/`uniqueEmail` or a direct INSERT of a brand-new row
  within the test itself.
  **Verification.** `uptime` before starting: load averages 3.34–6.07 across
  several runs — quiet machine throughout, nothing below is
  load-attributable. `ISSUE=0343 scripts/check.sh go
  ./internal/subscribers/... ./internal/handlers/... ./internal/db/...
  ./internal/outbox/...` — VERIFICATION PASSED, `gofmt -l` clean (confirmed
  additionally via `shasum -a 256` before/after a second `gofmt -w` pass —
  byte-identical), zero skips, all four packages `ok` with real elapsed time
  (`internal/subscribers` 0.537s–0.611s, `internal/handlers` ~7.4–7.7s,
  `internal/db` ~7.1–7.5s, `internal/outbox` ~0.8–1.1s — none at `0.00s`).
  `scripts/check.sh web` — VERIFICATION PASSED (`web/src/lib/dashboard.ts`'s
  doc comment also names all three consent_basis values, for `#0313`'s
  benefit and consistency with the Go side): `npm run check` 291 files / 0
  errors / 0 warnings; `npm test` 41 files / 4016 tests passed — a
  doc-comment-only TS change, matching `#0324`'s and `#0336`'s own precedent
  of not adding a TS test for a comment edit.
  Named test declarations run directly with `-v`, confirmed passing:
  `TestGrowth30Days_AdminAttestedArrivalAndDeparture`,
  `TestConsentBasisValuesAreClassified`,
  `TestGrowth30Days_RestartedImportRowSecondUnsubscribeStaysExcluded`
  (`#0344`'s own regression coverage for `#0336`'s rows O3/P3, proving this
  issue's change does not disturb them), and the pre-existing
  `TestGrowth30Days_TruthTable_0336` (all 18 rows, unchanged, still net-only
  by that test's own original design). Order-independence: `go test
  ./internal/subscribers/... -shuffle=on` and `-shuffle=20260905` (a fixed
  non-default seed, not `-count=N`) both `ok`.
  Left for review: `resolved` is the reviewer's call, not the
  implementer's.
