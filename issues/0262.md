# 0262 — `runpipe()`'s definition-count anchor has the same gap, masked by luck

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | infra / tooling |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-08-24 |
| **Commit** | `be81732` — [#0258](0258.md)'s fourth-pass commit; the same `scripts/check.sh` change closes both |

## Description

`#0258`'s third review found that `RUN_DEF_COUNT` is `grep -cE '^run\(\)' "$0"`,
which sees only a **column-0, contiguously-spelled** definition — so a one-line
decoy in any other shape shadows `run()` at runtime while the count stays at 1.

**`GUARD-0208`'s `RUNPIPE_DEF_TOTAL` uses the same `^runpipe\(\)` anchor and
inherits the identical gap.**

The difference is that it is **not exploitable today, by accident.** An indented
`runpipe` decoy must still spell `bash -c` to do anything useful, and the
shell-`-c` scan catches that. So the anchor's weakness is masked by a *different*
check happening to cover it.

**That masking is incidental, not designed.** It holds only while every useful
`runpipe` body contains `bash -c`. Nothing states that dependency, nothing tests
it, and a future refactor that changes how `runpipe` invokes its subshell would
silently remove the cover. The same widened anchor `#0258` needs would close
this one deliberately rather than by luck.

**A second, smaller issue in both guards.** They scan `"$0"`, while `REPO` is
derived from `${BASH_SOURCE[0]}`. Invoked by a relative path from outside the
repository root, `$0` no longer resolves after `cd "$REPO"`, and the guard fires
`found 0`. It **fails safe** — a false alarm rather than a false pass — but it is
a spurious failure in the script everything else is verified with, which is the
expensive kind. `${BASH_SOURCE[0]}` is strictly more robust for both.

## Acceptance criteria

- [ ] `RUNPIPE_DEF_TOTAL`'s anchor tolerates leading whitespace and the `function` keyword form, matching whatever `#0258` lands for `run()`
- [ ] Proved by the same decoy set that defeats the narrow anchor: leading space, tab indent, `function runpipe {`, `runpipe () {` — each must be counted
- [ ] The count is **1** in the tracked file and **2** in each mutant, verified by running rather than by reading the regex
- [ ] Both guards scan `${BASH_SOURCE[0]}` rather than `$0`, and are proved to work when `scripts/check.sh` is invoked by a relative path from outside the repository root
- [ ] The incidental-masking dependency is either removed or written down — if `runpipe`'s cover still relies on the shell-`-c` scan for any case, say so in the comment rather than leaving it implicit
- [ ] `#0208`'s nine mutants and `#0251`'s three fakes all still behave as documented

## Notes

Reported by `#0258`'s third phase-3 review under `CLAUDE.md` §9. Filed separately
because `#0258`'s own criterion covers `run()`; this is the sibling guard, and it
is not currently exploitable, so it should not block that issue closing.

**Worth doing in the same pass as `#0258`'s fix**, since the anchor is the same
regex and the reviewer has already measured a working replacement:

```
^[[:space:]]*(function[[:space:]]+)?run[[:space:]]*\(\)|^[[:space:]]*function[[:space:]]+run[[:space:]]*\{
```

which matched exactly 1 in the tracked file and 2 in each of the four mutants.

**The general lesson**, three bounces into this guard: each fix has closed the
shape it was shown, and the next review has found the adjacent one. The pattern
is the same as the audit-metadata guard's nine rounds — enumerating recognised
forms, with everything else silent. Whatever lands should be judged on whether
it fails *closed* on an unrecognised shape, not on whether it catches the four
shapes named here.

## Relation

- Found by: [#0258](0258.md) — the sibling guard whose anchor this shares
- Relates to: [#0208](0208.md) — the funnel and its shell-`-c` scan, which is what currently masks this
- Relates to: [#0251](0251.md) — the `runpipe()` assertion this sits beside
- Relates to: [#0261](0261.md) — the same enumerate-recognised-forms pattern in the audit guard

## Fix pass

Implemented in the same pass as `#0258`'s fourth fix pass, in the same
`scripts/check.sh` commit — see `issues/0258.md`'s `## Fix pass (fourth
pass)` for the full mechanism, judgement call, and verification detail;
this section covers what's specific to `runpipe()` and to this issue's own
acceptance criteria.

**Anchor widening — not the handed-over regex, the shared `resolve_func_hash`
mechanism `#0258` landed instead.** `#0258`'s third review handed over a
measured, working regex
(`^[[:space:]]*(function[[:space:]]+)?run[[:space:]]*\(\)|...`) and this
issue's own notes suggested applying the same pattern (substituting
`runpipe`) to `RUNPIPE_DEF_TOTAL`. Judged that widening the anchor a second
time, for a second name, was the same enumerate-recognized-forms trade the
notes above call out by name as the thing to stop doing — so instead,
`GUARD-0208`'s `RUNPIPE_DEF_TOTAL`/`RUNPIPE_LINE` (the narrow
`^runpipe\(\)` anchor and its extraction) were replaced by
`resolve_func_hash runpipe`, compared against a pinned SHA-256 digest,
using the exact same shared mechanism `run()`'s check now uses. Proved
against the acceptance criteria's own named decoy set — leading space, tab
indent, `function runpipe {`, `runpipe () {` — each one added as a single
line after `# END GUARD-0208`, real line 77 untouched, run end to end
against the fixed script: all four CAUGHT, `PIPEFAIL REGRESSION
(#0208/#0251/#0262): runpipe()'s resolved definition ... no longer matches
the pinned SHA-256 digest`, exit 2, before `=== machine` ever prints. The
count in the tracked (unmutated) file resolves to exactly the pinned digest
(1, in the "how many candidates does `runpipe` shadow-resolve to" sense
this design actually uses instead of a literal count) and each of the four
mutants resolves to a DIFFERENT hash (the decoy, which appears later in
file order and so wins bash's own shadowing) — verified by running, not by
reading the regex, per the acceptance criteria's explicit instruction.

**Masking dependency — removed, not merely documented.** This issue's
acceptance criteria allowed either outcome ("either removed or written
down"). It is removed: `resolve_func_hash runpipe` catches all four decoy
shapes on its own, using bash's own parser to resolve what `runpipe` is
actually bound to, independent of `SHELLC_HITS` or any other check in the
file. The code comment at the point `BR_COUNT`/`ER_COUNT` sits (where the
old masking argument used to live, since that's the check that was
previously doing double duty) now states this explicitly: an indented
`runpipe()` decoy no longer needs to spell `bash -c` to be caught, so
`SHELLC_HITS` catching it is now redundant coverage rather than the sole
mechanism.

**`$0` → `${BASH_SOURCE[0]}` → `$SELF`.** Reproduced the relative-path
failure independently first (`cd
/Users/brennan/Developer/OpenCircuitSF && bash ./Website/scripts/check.sh`
→ `sed: ./Website/scripts/check.sh: No such file or directory`, then
`PIPEFAIL REGRESSION (#0208): ... found 0`, exit 2 — fails safe, as this
issue predicted, but a spurious failure in the canonical verification
script). Found during implementation that `${BASH_SOURCE[0]}` alone —
this issue's literally-named fix — is NOT sufficient: it holds the same
relative invocation string as `$0` for a top-level script, so swapping the
variable name alone reproduces the identical failure (confirmed; see
`issues/0258.md`'s fourth pass for the byte-for-byte repro of this exact
mistake on a first draft). The working fix resolves an ABSOLUTE path
(`$SELF`) once, before `cd "$REPO"`, using the same subshell-cd-then-pwd
trick `REPO` already uses one line above it — every guard-code file
argument now uses `$SELF`. Re-verified after the real fix: the same
relative invocation runs cleanly through both self-checks and a full
`go ./internal/config/...` run, `VERIFICATION PASSED`, exit 0.

**`#0208`'s nine mutants and `#0251`'s three fakes for `runpipe()`,
re-run against the fixed script** — all documented in `issues/0258.md`'s
fourth pass; summary here since this issue names them in its own
acceptance criteria: 8/9 of `#0208`'s mutants caught (mutant 7, `run
"$SHELL" -c`, is the same pre-existing accepted gap, unrelated to this
issue); all 3 of `#0251`'s fakes (A trailing comment, B decoy marker pair,
C non-definition line) caught.

### Files changed

- `scripts/check.sh` — see `issues/0258.md`'s fourth pass for the full
  list; the parts specific to this issue are `GUARD-0208`'s
  `RUNPIPE_DEF_TOTAL`/`RUNPIPE_LINE` replaced by `resolve_func_hash
  runpipe` against a pinned digest, the masking-dependency comment
  rewritten at the `BR_COUNT`/`ER_COUNT` check, and `$SELF` (not a bare
  `$0` or `${BASH_SOURCE[0]}`) used everywhere a guard reads the file.
- `issues/0262.md` — this `## Fix pass` section, `Status` left
  `in-progress`.

### Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Exact token counts pending the orchestrator's transcript accounting — this
subagent has no access to its own usage totals to report here without
fabricating them.

## Review notes (phase-3 review of the fix pass, 2026-08-24)

**BOUNCED.** Three of the five criteria are met and independently re-verified;
two are not. Status stays `in-progress`. The full measurement record, the
harness, and the five new bypasses live in
[`issues/0258.md`](0258.md)'s `## Review notes (re-review of the fourth pass,
2026-08-24)` — this file records only what is specific to `#0262`. All probing
ran in a throwaway `git worktree` pinned at `8b2192a` and in an untracked
sandbox copy, every command under an explicit `/bin/bash` 3.2.57 invocation.
`git cat-file -t 8b2192a` → `commit` (docs-only; the code landed in `be81732`,
also `commit`, which this file's metadata row correctly cites).

### Criterion by criterion

- **Anchor tolerates the other spellings** — met, and better than asked:
  `RUNPIPE_DEF_TOTAL` no longer exists at all (`grep` confirms neither it nor
  `RUN_DEF_COUNT` remains), replaced by `resolve_func_hash`.
- **Proved by the same decoy set** — met. Leading space, tab indent,
  `function runpipe {`, `runpipe () {`, each inserted after `runpipe()`'s real
  definition: **all four CAUGHT**, `PIPEFAIL REGRESSION (#0208/#0251/#0262)`,
  exit 2, before any test runs.
- **"The count is 1 in the tracked file and 2 in each mutant"** — superseded by
  design rather than met. There is no count any more. The equivalent property
  (each mutant caught, the tracked file clean) is verified above, and the
  substitution is an improvement, not a shortfall.
- **`$0` → `${BASH_SOURCE[0]}`, proved by relative invocation from outside the
  repo root** — met, and the implementer's disclosure that its *first* fix was
  insufficient is confirmed. Three variants of the same file, invoked as
  `bash ./oc-0258rev3-rel/Website/scripts/check.sh guards` from `/private/tmp`:

  | variant | result |
  |---|---|
  | `"$0"` | `sed: ./…/check.sh: No such file or directory`, then `expected at least 9 runpipe() call sites … found 0`, exit 2 |
  | bare `"${BASH_SOURCE[0]}"` | **byte-identical failure** — confirms it is not a fix |
  | `"$SELF"` (shipped) | guards pass, reaches `=== machine`, real work runs |

  Note the criterion as written asks for `${BASH_SOURCE[0]}`, which is
  *wrong* — measured above, and now recorded in `CLAUDE.md` §8. `$SELF`, an
  absolute path resolved before `cd "$REPO"`, is the correct fix and is what
  shipped.
- **The incidental-masking dependency removed** — **partly met, and the code
  comment overstates it.** For the four enumerated shapes the removal is real:
  re-run with bodies spelling `run "$SHELL" -c` instead of `bash -c`, so
  `SHELLC_HITS` cannot possibly supply the catch, all four are still CAUGHT by
  the digest alone. But the comment says the mechanism *"closes this directly,
  on its own, independent of SHELLC_HITS or any other check in this file — the
  masking dependency is removed, not just documented."* That is false for four
  other one-line shapes bash accepts and the candidate regex does not see. Each,
  spelled for `runpipe` with `-o pipefail` dropped and the shell reached through
  a variable, produced a real **`VERIFICATION PASSED`** with the flag genuinely
  gone, guard silent:

  | shape | result |
  |---|---|
  | `runpipe() { … };` (trailing `;`) | *** BYPASS *** |
  | `runpipe() ( … )` (subshell body) | *** BYPASS *** |
  | `{ runpipe() { … }; }` (group command) | *** BYPASS *** |
  | `if true; then runpipe() { … }; fi` | *** BYPASS *** |

  Measured with a pipefail-sensitive oracle (`web` mode in a sandbox with an
  empty `web/`, so the failing command is *inside* the pipe), against a control
  where the named leading-space shape with the identical body is CAUGHT.
- **`#0208`'s nine mutants and `#0251`'s three fakes behave as documented** —
  met. 1, 2, 3, 4, 5, 6, 8, 9 CAUGHT; 7 not caught (the accepted gap); A, B, C
  all CAUGHT. `-cu`/`-ce`/`-cx`/`-uc` all CAUGHT.

### Why this bounces rather than passing on four of six

This file's own Notes set the judging criterion: *"Whatever lands should be
judged on whether it fails closed on an unrecognised shape, not on whether it
catches the four shapes named here."* It does not. It fails **open** on an
unrecognised shape exactly as the narrow anchor did — the recogniser is simply
wider. The masking that `#0262` was filed to remove is removed for the four
shapes it enumerated and still present, unstated, for four others.

Two further structural defects found in the shared mechanism apply to this guard
identically and are detailed in `#0258`'s notes: a single ordinary helper line
containing a `#` inside quotes blinds the whole extraction (both digests then
pass silently), and the "private subshell" inherits the running script's own
function table, so a failed extraction degrades to measuring the live process
instead of the file. The second is why the first stays silent instead of failing
closed.

No new issues filed — all of this is inside `#0258`/`#0262`'s scope
(`CLAUDE.md` §9, reported not filed).
