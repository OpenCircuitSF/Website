# 0444 — Dropping `slug` from `CampaignEditor`'s PATCH payload would fail silently, and the new preview hides it

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Web |
| **Module** | web / admin |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |

## Description

`#0410` wired the archive slug into `CampaignEditor.svelte`. Its review traced
all three legs of that wiring and found them present and correct — but recorded
that the change made one regression shape **less** visible than it was before.

Remove `slug: slugToSend` from `saveDraft`'s `updateCampaign(...)` payload and:

- the field still accepts typing,
- autosave still reports "Saved",
- and the read-only Archive URL preview **still updates** to the new value —
  because `#0410` deliberately made the preview read the live buffer rather than
  the persisted `campaign.slug`.

So every visible signal says the slug was saved, and none of it was. The
preview improvement is correct and worth keeping; it just removed the last cue
that would have exposed a broken write.

Nothing in the tree would catch it. Nothing mounts `CampaignEditor.svelte` —
`web/src/lib/modalFocusWiring.structuralGuard.test.ts`'s own comment names it as
too network-dependent to mount cheaply — and `STORAGE=json` has no mailing
subsystem at all, so no end-to-end path exercises it either.

## Why a guard here, when this repo adds them reactively

`CLAUDE.md` records that `.svelte` structural guards are added **after** a review
finds a silent failure, not per field. `#0410`'s review followed that rule and
did **not** bounce for a guard, since there is no present defect. This issue is
the follow-up it recommended instead.

The distinguishing fact is the one above: the failure is silent in every channel
a human would check, including the one the same change introduced.

## Acceptance criteria

1. A structural guard modelled on
   `web/src/lib/modalFocusWiring.structuralGuard.test.ts` parses
   `web/src/views/admin/CampaignEditor.svelte` with `svelte/compiler` and
   asserts **over the AST, not with a regex over raw text**:
   1. the `updateCampaign(...)` call's object argument has a `slug` property —
      **this is the load-bearing one**, the leg whose loss is silent;
   2. `archiveURLValue`'s initializer passes the `slug` identifier, not a
      `campaign` member expression, to `archiveURL`;
   3. the input with `id="campaign-slug"` carries a `disabled` attribute whose
      expression reads `slugEditable`.
2. Prove it by mutation: delete each of the three legs in turn from a **copy**
   of the component, confirm the guard fails naming that leg, and restore.
   Never `git stash` or `git checkout --` (`CLAUDE.md` §8a).
3. Assertions 2 and 3 are cheap once the file is parsed — include them, but do
   not let them grow into a general component-shape checker. One guard file,
   three assertions.
4. **The oracle must not be the same bytes as its subject** (§8). A quoted copy
   of the expected source next to the component is satisfied by one edit that
   rewrites both; the AST is a different representation and is the right shape
   here. Do not add a text fixture alongside it.

## Added 2026-09-05 — `#0411` widened the unmounted surface

`#0411` added a **Delete campaign** control to the same component, and `#0411`'s
reviewer asked that this issue absorb it. Four more legs now exist that only
`CampaignEditor.svelte` reaches:

- the delete **offer gate** (draft-only),
- the **confirm-message binding**,
- the **confirm handler**, and
- its **`onBack()`** call.

`#0411` added five web tests, and all five cover the two **pure helpers** —
nothing exercises the wiring. So the same silent-write shape this issue was
filed for now applies to a **destructive** action: the button can stop being
wired to the handler, or the handler stop calling the API, with no test
noticing.

That raises the stakes but not the remedy. Add these to criterion 1's AST
assertions rather than building a second mechanism:

5. the delete confirm handler's `deleteCampaign(...)` call is present;
6. the delete control's `disabled`/offer expression reads the draft-only gate.

Keep it one guard file. `#0411`'s review separately confirmed the **server**
refuses a non-draft delete independently, so the offer gate is cosmetic and a
missing gate is a UI defect rather than a data-loss one — assertion 5 is the
load-bearing one here, as assertion 1 is above.

## Also fold in — three small findings from the same review

None is a defect; all three are one-line tidies in the files this issue already
opens:

1. `resyncCampaignStatus`'s doc comment enumerates **six** protected buffers.
   `slug` is now a seventh and belongs in the list. Behaviour is already
   correct; only the comment is short.
2. `saveDraft` trims the slug unconditionally, even when the field is disabled.
   A padded stored slug would then 409 on every autosave of a scheduled
   campaign. Unreachable today, since the server coerces an empty `in.Slug` to
   the current value — but falling back to `campaign.slug` when `!slugEditable`
   is tidier and removes the reasoning step.
3. The Copy button copies the **unsaved buffer's** URL. That is a direct
   consequence of `#0410`'s criterion 1 and may be exactly right; decide and
   record which, rather than leaving it unexamined.

Deliberately **not** in scope: clearing the field shows the "cannot be blank"
message alongside a successful "Saved" and a bare `/archive/` preview.
`#0410`'s review judged that a deliberate trade — blocking the whole autosave
would strand the name, subject, and body — and criterion 4 there only asked that
blank be rejected client-side, which it is, before the request.

## Relation

- `#0410` wired the slug into the console and its review recommended this.
- `#0405` chose the opt-in `MM-YYYY` template whose repair path `#0410` built.
- `#0411` is the other half of "no console-reachable repair" and is still open.

Verify with `scripts/check.sh web`. Editing an issue file also requires
`ISSUE=NNNN scripts/check.sh go ./internal/db/...` per `CLAUDE.md` §5's table.

## Work log

- 2026-09-05 — implement (claude-sonnet-5), fresh subagent, no phase-1 plan
  (mechanical: one AST guard file modelled on an existing one, plus three
  one-line tidies already spelled out in acceptance criteria).

  **Guard file**: `web/src/lib/campaignEditorSlugAndDeleteWiring.structuralGuard.test.ts`
  (one new file, no text fixture alongside it). Parses
  `web/src/views/admin/CampaignEditor.svelte` via a single `import.meta.glob(...,
  { query: '?raw', eager: true })` (the CampaignSendDialog.structuralGuard.test.ts
  pattern, not modalFocusWiring's repo-wide glob — this guard is deliberately
  scoped to one file) and checks five things over the real `svelte/compiler`
  AST, each throwing a specific, file-and-expression-quoting error the moment
  its own extraction step finds nothing (never a silent zero-violations pass):
  1. `updateCampaign(...)`'s object argument has a `slug` property
     (load-bearing — criterion 1);
  2. `archiveURLValue`'s `archiveURL(...)` call passes the live `slug`
     identifier, not a `campaign.slug` member expression (criterion 2);
  3. `<input id="campaign-slug">`'s `disabled` expression's AST contains an
     `Identifier` named `slugEditable` (criterion 3);
  4. the delete-confirm modal's confirm `<Button variant="danger">`'s
     `onclick` handler is resolved by name to its function/arrow declaration
     in `<script>`, and that declaration's body contains a `deleteCampaign(...)`
     call (load-bearing — criterion 5 in the "Added 2026-09-05" section);
  5. the `{#if ...}` guarding the "Delete campaign" trigger
     (`<Button onclick={openDelete}>`) is resolved to its `$derived` gate
     variable by name, and that variable's initializer contains a
     `canDeleteCampaign(...)` call (criterion 6 in the "Added 2026-09-05"
     section).

  Check 5's offer-gate lookup needed one iteration: the first version picked
  the OUTERMOST `{#if}` whose subtree transitively contained the trigger
  button (the unrelated `{:else if campaign}` branch several levels up), not
  the nearest one, and failed the guard's own dry run against the real,
  unmutated file — `deleteOffered` resolved to `campaign`'s own `$state`
  declaration instead. Fixed by collecting every `{#if}` whose consequent
  contains the button and taking the one with the smallest source span
  (the innermost), mirroring modalFocusWiring's "nearest guard, not
  accumulated" rule. Re-ran clean afterward (5/5 passing) — recorded here per
  CLAUDE.md's fail-open warning, since this was itself a case of an
  extraction silently finding *something*, just the wrong something, which is
  exactly the failure class `#0428` and `#0258` are cited against.

  **Oracle independence**: no assertion compares against a quoted copy of
  CampaignEditor.svelte's source. Every expected name (`slug`, `slugEditable`,
  `deleteCampaign`, `canDeleteCampaign`, `openDelete`) is a short identifier
  string compared against the AST node it names — a different representation
  from the subject, not a restatement of it (CLAUDE.md §8's oracle rule).

  **Mutation proof** (five legs, one at a time, against the real tracked
  file — backed up first to a scratchpad copy namespaced `0444`, restored
  after each leg and hash-verified before the next): starting hash
  `5c27c9dac911fc4516169a4a60b6d9e4a6ce56c9db5611b2219e1c4d6b2286f3` for
  `web/src/views/admin/CampaignEditor.svelte`, restored to that exact hash
  after every leg (never used `git stash`/`checkout`/`restore`/`reset`
  — plain `cp` to and from a scratchpad file, per CLAUDE.md §8a).
  1. Removed `slug: slugToSend,` from the `updateCampaign(...)` payload —
     failed naming "updateCampaign(...)'s payload ... has no `slug`
     property".
  2. Changed `archiveURL(window.location.origin, { slug })` to
     `{ slug: campaign.slug }` — failed naming "archiveURL(...)'s `slug` is
     `campaign.slug`, not the live `slug` edit buffer".
  3. Changed `<input id="campaign-slug">`'s `disabled={!slugEditable}` to
     `disabled={false}` — failed naming "disabled expression is `false`,
     which does not reference `slugEditable`".
  4. Removed `await deleteCampaign(campaignId);` from `onConfirmDelete` —
     failed naming "`onConfirmDelete` (wired to the delete-confirm button)
     does not call deleteCampaign(...)".
  5. Changed `deleteOffered`'s initializer from
     `canDeleteCampaign(campaign.status)` to `canEditCampaign(campaign.status)`
     — failed naming "`deleteOffered` is `$derived(campaign ?
     canEditCampaign(campaign.status) : false)`, which does not call
     canDeleteCampaign(...)".

  All five restores confirmed byte-identical by `shasum -a 256` before
  proceeding to the next leg; `git status --porcelain -- web/src/views/admin/CampaignEditor.svelte`
  was empty immediately before the (separate, deliberate) tidy edits below
  began. The guard needs no jsdom — it parses with `svelte/compiler` and
  never mounts anything, consistent with #0410's own "too network-dependent
  to mount cheaply" note that named this component.

  **Tidies** (all three in `web/src/views/admin/CampaignEditor.svelte`,
  the file this issue already opens):
  1. `resyncCampaignStatus`'s doc comment's buffer enumeration widened from
     `name/subject/preheader/bodyMd/mode/interestIds` to
     `name/subject/preheader/bodyMd/slug/mode/interestIds` (position matches
     `load()`'s own assignment order).
  2. `saveDraft` no longer trims the slug buffer unconditionally. `slugToSend`
     now falls back to `campaign.slug` outright when `!slugEditable`, only
     running `validateSlugInput`/`.trim()` when the field is actually
     editable — same result today (the buffer already equals `campaign.slug`
     whenever `slugEditable` is false), but it drops the reasoning step for
     the next reader and stays correct even if a stored slug ever carried
     incidental whitespace. Judged: a genuine (if currently unreachable)
     tidiness improvement, not a defect fix — recorded per the issue's own
     framing ("Unreachable today ... but ... tidier").
  3. `copyArchiveURL` decided and recorded (not left unexamined): copying
     `archiveURLValue` — the same live-buffer-derived string the read-only
     field displays — is the right behavior. Added a doc comment explaining
     why: the input, the preview, and the Copy button all read one source,
     so the operator's clipboard can never disagree with what they see, which
     is the same anti-drift reasoning `#0410`'s criterion 1 used for the
     preview itself. No code change — decision recorded, not "fixed".

  **Verification**: `uptime` before the runs read `up 15 days, 12:50 ...
  load averages: 21.29 19.01 13.33` (above CLAUDE.md's ~8 baseline — another
  agent's work, not this pass's). `scripts/check.sh web` at that load: 41
  files, 4016 tests, all passed (`npm run check`: 291 files, 0
  errors/warnings; `npm test`: 4016 passed), including the 5 new tests in
  `campaignEditorSlugAndDeleteWiring.structuralGuard.test.ts`. Named test
  functions verified individually as real declarations (matching
  CLAUDE.md's citation-guard requirement): every `it(...)` in the new guard
  file, run again standalone via
  `npx vitest run src/lib/campaignEditorSlugAndDeleteWiring.structuralGuard.test.ts`
  — 5 passed. `ISSUE=0444 scripts/check.sh go ./internal/handlers/...
  ./internal/db/...` failed once at load average 21.29 with
  `TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch` timing out
  (`blockingSuppressionChecker.IsSuppressed was never called`) — an
  unrelated, load-induced timing failure per CLAUDE.md §5 (this pass never
  touched `internal/handlers` or `internal/subscribers`), not a defect.
  Re-ran at load average 11.02 (dropping): both packages reported real
  elapsed time and zero skips —
  `ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	16.450s` and
  `ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	16.718s`. Scanned
  this file's own prose with `python3`, never BSD `grep -P`, per CLAUDE.md's
  byte-scanning note. Status left `in-progress` for review — not mine to
  resolve (CLAUDE.md §9).

  **Nothing to file.** Scope respected: touched only
  `web/src/lib/campaignEditorSlugAndDeleteWiring.structuralGuard.test.ts`
  (new), `web/src/views/admin/CampaignEditor.svelte` (tidies only), and this
  file. Did not touch `internal/`, `cmd/`, `.env.example`, `docs/`, or
  `issues/0441.md`, which another agent had dirty for the whole of this pass.
