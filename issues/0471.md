# 0471 — The interest taxonomy is DML inside a frozen migration, so nobody can change it

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | subscribers / db |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-08 |
| **Closed** | 2026-09-08 |
| **Commit** | 8c9f8df |

> 🟢 Resolved 2026-09-08 — second review. The taxonomy is changeable today and
> always was: `POST`/`PATCH`/`DELETE /admin/interests` (`#0024`) take effect
> with no migration and no deploy, and `docs/mailing-list.md`'s "Changing the
> taxonomy" section now says so. **Two real limits on that channel were
> documented here and NOT fixed by this issue; one has since been fixed and
> one is still open — read both before pruning anything.** `DELETE`'s 409
> guard consulted `subscriber_interests` only when this issue was resolved;
> `#0474` has since widened it to `campaign_interests` and
> `workshop_interests` too, so a delete that would drop a workshop tag or a
> campaign's recorded segment is now refused with a 409 naming the blocker.
> And `slug` is immutable through the admin channel, so replacing one means
> create-new-and-deactivate, with subscribers left behind on the old row — that is
> **`#0475`, open**. Resolving this issue documented those gaps; it did not
> close them. The taxonomy is **not** safe to prune.

## Description

The twelve interest rows are seeded as **DML inside `migrations/000009`** — which
is **inside the frozen range**. `CLAUDE.md` §1's rule is that a migration applied
to production is never edited, and production is at version 22.

So **the file that owns the taxonomy cannot be changed by anyone**: not this
project, not a downstream. ~~Adding, renaming or removing an interest requires a
new migration that mutates rows the frozen one inserted, and nothing in the
repository says so.~~

> **Corrected 2026-09-08 (`#0471` review).** The struck sentence is false, and
> a later reader meets it first, so it is struck rather than left standing.
> Adding, redescribing, reordering, deactivating or deleting an interest
> requires **no migration at all**: `internal/interests` +
> `AdminInterestsHandler` (`#0024`) expose `POST /admin/interests`,
> `PATCH /admin/interests/{id}` and `DELETE /admin/interests/{id}`, all wired
> in `cmd/opencircuit/main.go`'s admin route table, and they take effect with
> no deploy. The implementation pass found this; the review pass confirmed it
> independently by reading the store and the handler.
>
> What is genuinely true, and is the defect worth fixing, is narrower: nothing
> documented that channel, and there was no stated safe pattern for changing
> what a **fresh install seeds by default**, since `000009` can never move.
> Two real limits on the runtime channel survive the correction and are what
> the review bounces on — the **slug is immutable** through it, and `DELETE`
> is gated on `subscriber_interests` alone.

Found by `#0232`'s planning pass while measuring what a second tenant would have
to change. It is filed separately because **it is a real defect on its own
terms** — it constrains this project today, independently of whether the
reusability work ever happens.

## Why it is worth fixing rather than documenting

`PRD.md` §6.1 treats the taxonomy as product content — the thing a marketing site
would plausibly revise. Content that lives in an immutable file is content nobody
revises, and the failure is silent: someone edits `000009`, the local database
rebuilds correctly from `migrations/`, and production diverges the moment it is
next migrated from a checkout that never re-runs an applied version.

## Acceptance criteria

1. **Confirm the freeze applies.** Re-derive production's applied version with
   the read-only query in `CLAUDE.md` §1 — do not trust the number recorded
   there, which §1 itself warns goes stale. If `000009` is genuinely below the
   bound, the file is frozen and the fix is a new migration.
2. Decide **where the taxonomy should live** so it can change: a new migration
   that establishes it as ordinary data, a seed path outside `migrations/`, or
   the `settings` table. Record the reasoning; `#0232`'s plan notes `settings`
   already holds `default_from_name` and `physical_address`, which is the
   nearest precedent.
3. **Do not edit `000009`.** §1 records that `migrations/000007` exists because
   ShortLinks broke this rule once, and `#0125` was bounced for following a
   stale criterion that instructed exactly this edit.
4. Whatever lands must be **idempotent against a production database that
   already holds the twelve rows**, and must not renumber or orphan an existing
   interest — subscribers reference them.
5. Say plainly in `docs/` how someone changes the taxonomy afterwards. The
   current absence of that sentence is half the defect.

## Relation

- `#0232`'s planning pass found it while measuring hard-coded content.
- `CLAUDE.md` §1 carries the append-only rule and the `#0125` precedent.
- `PRD.md` §6.1 defines the taxonomy.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/subscribers/... ./internal/handlers/... ./internal/db/...` — per §5's table `internal/subscribers` reaches into all of `internal/`, and `internal/db` scans `migrations/`.

## Work log

- 2026-09-08 — Implementation pass (phase 2, Sonnet).
  **Criterion 1 — re-derived, not trusted.** Ran the read-only query over
  `ssh ec2` myself: `select version, dirty from schema_migrations` returned
  `22|f`. `000009` is 13 versions below the applied bound, so it is
  genuinely frozen under CLAUDE.md §1's applied-to-production principle,
  independent of the recorded `000001`-`000022` range going stale. Changed
  nothing on the box.
  **Criterion 3 — did not touch `000009`.** Confirmed via `git status
  --porcelain -- migrations/` before finishing: no migration file is
  modified, and no new migration file was added — inventing a data change
  nobody asked for (e.g. re-running 000009's own INSERT in a new file) would
  have been scope creep with no PRD-driven content change behind it.
  **Criterion 2 — decision.** The taxonomy stays ordinary rows in
  `interests` (not `settings` — `settings` is scalar key/value, unsuited to
  an ordered list of rows with independent lifecycle/soft-delete semantics,
  and `internal/interests.Store` already models exactly that well). What
  actually needed fixing was not *where* the rows live but *how they are
  understood to change after `000009` runs* — and on inspection the runtime
  channel already exists and already works: `internal/interests` +
  `AdminInterestsHandler` (`#0024`) expose
  `POST`/`PATCH`/`DELETE /admin/interests` today, so an admin can add,
  rename, redescribe, reorder, deactivate, or (if unused) delete an interest
  with no migration and no deploy. The defect this issue names is real but
  narrower than "nobody can change it": it is that (a) nothing documents
  that channel as the answer, and (b) the *other* channel — changing what a
  **fresh install** seeds by default, i.e. `PRD.md` §6.1's canonical list
  itself changing — has no stated, safe pattern, since the file that used to
  hold that content is now permanently frozen. Documented both channels in
  `docs/mailing-list.md`'s new "Changing the taxonomy" section (linked from
  `docs/database.md`'s `000009` row) — this is criterion 5. The second
  channel's pattern is: a new, additively-numbered migration using the
  exact idempotent shape `000009` already uses (`INSERT ... ON CONFLICT
  (slug) DO NOTHING` to add, `UPDATE ... WHERE slug = '...'` to rename/
  redescribe in place), never a `DELETE`.
  **Criterion 4 — idempotency and no-renumber/no-orphan, enforced rather
  than only asserted.** Added
  `internal/db/interest_taxonomy_seed_freeze_guard_test.go`:
  `interestTaxonomyGuardViolations` scans every migration file numbered
  after `000009` and fails if one removes an `interests` row
  (DELETE/TRUNCATE/DROP — the orphan/renumber risk, since
  `subscriber_interests`/`workshop_interests`/`campaign_interests` all hold
  FKs to it) or `INSERT`s into `interests` without `ON CONFLICT (slug) DO
  NOTHING` (the idempotency requirement). Proved the guard actually
  discriminates rather than asserting it does: one test plants a destructive
  `DELETE` in a fake later migration and confirms the guard fires and names
  the offending file; one plants a non-idempotent `INSERT` and confirms the
  guard fires; one plants the *same* `INSERT` with `ON CONFLICT` added and
  confirms the guard goes quiet (without this, a guard that always fails
  would look identical to one that discriminates correctly); one plants a
  bare `DROP TABLE interests` under the literal `000009` filename and
  confirms it is correctly out of scope, since the guard must never
  constrain `000009` itself. A fifth test runs the guard against every real
  file in `migrations/` and passes vacuously today — no migration after
  `000009` touches `interests` yet — and becomes the tripwire the moment one
  does.
  **Verification.** `uptime` before starting: load averages 8.82 / 4.78 /
  3.35 (briefly over the ~8 idle mark, consistent with the two other
  concurrent agents named in this dispatch — not treated as a defect signal
  per CLAUDE.md §5). `ISSUE=0471 scripts/check.sh go
  ./internal/subscribers/... ./internal/handlers/... ./internal/db/...` —
  VERIFICATION PASSED. `internal/subscribers` ok 0.663s, `internal/handlers`
  ok 8.147s, `internal/db` ok 7.713s — all real elapsed time, zero skips,
  `gofmt -l` silent. `check.sh` detected the shared template is at 28
  against this tree's 27 and correctly provisioned `opencircuit_test_0471`
  directly from `migrations/` instead of cloning the stale template; did
  **not** run `scripts/testdb.sh template`. Confirmed the five new tests ran
  by name (`go test ./internal/db/... -run
  TestInterestTaxonomyMigrationGuardPassesOnRealMigrations -v` and the same
  for each of `TestInterestTaxonomyMigrationGuardCatchesDestructiveDelete`,
  `TestInterestTaxonomyMigrationGuardCatchesNonIdempotentInsert`,
  `TestInterestTaxonomyMigrationGuardAllowsIdempotentInsert`, and
  `TestInterestTaxonomyMigrationGuardIgnores000009Itself`), each `PASS`. Confirmed no scratch database or process was left behind
  afterward (`opencircuit_test_0471` absent from `pg_database`; `pgrep -f
  'go test|\.test '` empty). No test targets a literal or seeded id — the
  new tests operate entirely on an in-memory `map[string]string` of
  fabricated filenames, never on a live database or a real seeded row.
  **Files changed.** `internal/db/interest_taxonomy_seed_freeze_guard_test.go`
  (new) — the guard and its five tests. `docs/mailing-list.md` — new
  "Changing the taxonomy" subsection. `docs/database.md` — one sentence
  added to the `000009` row pointing at it. `issues/0471.md` — this entry;
  status set to `in-progress`.
  **Scope discipline.** Did not touch `internal/mailing/`,
  `internal/config/`, `cmd/opencircuit/`, `internal/outbox/`,
  `issues/0470.md`, or `issues/0472.md`; confirmed via `git status
  --porcelain` before staging that only the four files named above are
  dirty. Filed no new issues (§9) — nothing turned up beyond what is
  already recorded here. Status left at `in-progress`; `resolved` is the
  reviewer's call.

- 2026-09-08 — Implementation pass, second attempt (phase 2, Sonnet).
  Transcribed the reviewer's three verbatim fixes rather than redesigning
  anything; nothing it upheld (the corrected diagnosis, the guard's
  homing, the freeze re-derivation) was touched. `uptime` before starting:
  load averages 5.94 / 4.93 / 3.95, then 4.03 / 4.57 / 3.93 at
  verification — both under §5's ~8 idle mark.
  **(1)** Replaced `docs/mailing-list.md`'s "Changing the taxonomy" first
  bullet with the reviewer's verbatim text: rename is now scoped to "the
  display `name` only", and the bullet states both load-bearing limits —
  slug immutability through the admin channel (`interests.Store.Update`
  takes no slug parameter; `#0023`'s rationale; the create-new-and-
  deactivate replacement pattern) and that `DELETE`'s 409 guard covers
  `subscriber_interests` only, leaving `workshop_interests`
  (`migrations/000020_create_workshops.up.sql`) and `campaign_interests`
  (`migrations/000017_create_campaigns.up.sql`) — both `ON DELETE
  CASCADE` — silently exposed, with `PATCH {"active": false}` named as the
  safe alternative.
  **(2)** Added the reviewer's verbatim paragraph to the second bullet,
  after the sentence ending "for every migration numbered after
  `000009`": the `.down.sql` half of a future additive migration must be a
  documented no-op, never a `DELETE`, because the seeded row may have
  acquired join-table associations while it existed and `ON DELETE
  CASCADE` would take them with it.
  **(3)** In
  `internal/db/interest_taxonomy_seed_freeze_guard_test.go`, appended the
  reviewer's exact suffix string to the delete-violation message so a
  `.down.sql` violation now names the remedy. Added one new test beside
  the existing five, following their naming pattern, feeding the same
  `000030`-numbered up/down pair the reviewer's probe used (an idempotent
  `ON CONFLICT` insert paired with a `DELETE FROM interests WHERE slug =
  …` in the `.down.sql`) and asserting exactly one violation naming the
  `.down.sql` file — pinning item 2's contract rather than leaving it only
  described in prose. Did not name the test in this entry before writing
  it, to stay clear of the undefined-test-name trap the review pass hit
  (`#0392`'s shape); it exists in the diff this entry accompanies.
  **Verification.** `gofmt -w` on the changed test file made no rewrite —
  hash `3884f0c4b06e232902b1444ea0257bc93f9c0d2ef59747dab7af39d07b943faa`
  before and after. Re-ran all three of the reviewer's mutation proofs
  against a scratch copy, restoring hash-identical after each: neutering
  the delete regex still fails `CatchesDestructiveDelete` ("expected a
  violation … got none"); short-circuiting the `ON CONFLICT` match to
  always-true still fails `CatchesNonIdempotentInsert`; relaxing the
  version gate from `version <= 9` to `version < 0` still fails
  `Ignores000009Itself`, naming `000009_create_interests.down.sql`. All
  six tests (the original five plus the new one) pass on the real tree,
  each run by its own full name.
  `ISSUE=0471b scripts/check.sh go ./internal/subscribers/...
  ./internal/handlers/... ./internal/db/...` → `VERIFICATION PASSED`:
  `internal/subscribers` ok 0.610s, `internal/handlers` ok 8.021s,
  `internal/db` ok 7.695s — real elapsed time, zero skips, `gofmt -l`
  clean. `opencircuit_test_0471b` was provisioned directly from
  `migrations/` (shared template at 28 against this tree's 27, per the
  environment note) and dropped by `check.sh` on exit; confirmed absent
  from `pg_database` afterward and no leftover `go test`/`.test`
  processes. Did not run `scripts/db-reset.sh` or `scripts/testdb.sh
  template`. Confirmed via `git status --porcelain -- migrations/`
  (empty) and a diff review that `000009` and every other migration file
  are untouched, and did not modify `internal/interests/`,
  `internal/handlers/`, `internal/mailing/`, `internal/config/`,
  `cmd/opencircuit/`, `issues/0470.md`, or `issues/0472.md` — those three
  markdown files were already dirty from other concurrent agents before
  this pass started and were left alone. Filed no new issues (§9); the
  two gaps the review pass flagged for the tracker are the orchestrator's
  to file. Status left at `in-progress`; `resolved` is the reviewer's
  call.

## Review notes

- 2026-09-08 — Review pass (phase 3, Opus). **Bounced.** `uptime` at start:
  load averages 2.37 / 3.72 / 3.27; at verification 1.58 / 2.91 / 3.03 — well
  under §5's ~8 idle mark, so no timing result here is attributable to load.

  **The corrected diagnosis is upheld, and the Description is struck above.**
  Verified independently rather than accepted: `internal/interests/store.go`
  exposes `Create`, `Update`, `Deactivate` and `Delete`;
  `internal/handlers/interests.go` exposes `List`/`Create`/`Patch`/`Delete`;
  and `cmd/opencircuit/main.go`'s admin route table wires all four verbs of
  `/admin/interests`. An interest genuinely can be added, redescribed,
  reordered and deactivated at runtime with no migration and no deploy, so the
  Description's "requires a new migration" was false and the implementation
  pass was right to say so. **Criterion 1 re-derived, not trusted**: the
  read-only `ssh ec2` query returned `22|f`, putting `000009` thirteen
  versions below the applied bound — frozen. **Criterion 3 confirmed**:
  `git status --porcelain -- migrations/` is empty and `5d5c980` touches no
  file under `migrations/`. **The guard is warranted and correctly homed**;
  three independent mutation proofs re-run in a throwaway worktree are
  recorded below. The bounce is not about any of that.

  **What the next pass must fix — three items, all small and fully specified.**

  **(1) `docs/mailing-list.md` overstates the runtime channel on two points,
  and criterion 5 makes that doc the deliverable.** Replace the whole first
  bullet of "Changing the taxonomy" with, verbatim:

  ```
  - **A live catalog change** — add, rename (the display `name` only),
    redescribe, reorder, deactivate, or hard-delete an interest no subscriber
    has ever selected — is an **admin-console action**, not a migration:
    `POST`/`PATCH`/`DELETE /admin/interests` (`internal/interests`, `#0024`).
    It takes effect immediately, with no deploy and no restart. Two limits are
    load-bearing and are not obvious from the verb list:
    - **`slug` is immutable through this channel.** `interests.Store.Update`
      takes no slug parameter and the PATCH body type has no `slug` field, so
      a PATCH carrying one is rejected with 400 rather than silently ignored
      (`#0023`'s decision: a slug change breaks every already-issued
      preference-center link that references it). To replace a slug, `POST`
      the new interest and deactivate the old one — subscribers do not move
      across, and a downstream re-theming the taxonomy wholesale is doing
      creates-and-deactivates, not renames.
    - **`DELETE` is refused (409) only when a `subscriber_interests` row
      references the interest.** `workshop_interests` and `campaign_interests`
      are `ON DELETE CASCADE` (`migrations/000020_create_workshops.up.sql`,
      `migrations/000017_create_campaigns.up.sql`) and are **not** consulted,
      so deleting an interest that no subscriber has selected but a workshop
      is tagged with, or a campaign targeted, silently drops those rows.
      Prefer `PATCH {"active": false}`: deactivation preserves the row and
      every `subscriber_interests`/`workshop_interests`/`campaign_interests`
      row that references it.
  ```

  The current text says a bare "rename", and pairs "(if unused) hard-delete"
  with a following sentence naming all three referencing tables — which invites
  exactly the wrong inference about what "unused" is measured against. Read
  the store and handler: neither package mentions `workshop_interests` or
  `campaign_interests` anywhere, and `Delete`'s `NOT EXISTS` subquery covers
  `subscriber_interests` only.

  **(2) The guard rejects the up/down pair the same document prescribes.**
  `interestTaxonomyGuardViolations` scans `.down.sql` files too, so the natural
  reversal of the documented additive seed trips it. Measured, not inferred —
  a reviewer probe over the exact prescribed pattern
  (`000030_add_ai_ml_interest.up.sql` with `ON CONFLICT (slug) DO NOTHING`,
  plus `000030_add_ai_ml_interest.down.sql` containing
  `DELETE FROM interests WHERE slug = 'ai-ml';`) returns **one violation**,
  naming the `.down.sql`. Every migration in this repo ships an up/down pair,
  so an operator following the doc must write that file and will hit a red
  test whose message tells them to use runtime deactivation — which a
  `.down.sql` cannot do. Keep the ban (rolling back a seeded row would cascade
  away associations made while it existed) and state the alternative. Add to
  the second bullet, after the sentence ending "for every migration numbered
  after `000009`":

  ```
    The matching `.down.sql` must be a documented no-op — a comment saying the
    seeded row is deliberately not removed on rollback — never a `DELETE`: the
    row may have acquired `subscriber_interests`/`workshop_interests`/
    `campaign_interests` associations while it existed, and `ON DELETE CASCADE`
    would take them with it. The guard enforces this in both directions.
  ```

  And extend the delete violation message in
  `internal/db/interest_taxonomy_seed_freeze_guard_test.go` so the failure
  names the remedy for the down case, appending to the existing string:
  `" (in a .down.sql, write a documented no-op instead — see docs/mailing-list.md)"`.

  **(3) Add one test pinning item 2's contract**, beside the existing five, so
  the no-op-down rule is enforced rather than only described. Name it after
  its siblings' pattern — the same interest-taxonomy migration-guard prefix,
  ending in a phrase for rejecting a delete inside a down migration. (The
  identifier is described rather than written here because the repo-wide
  citation guard requires an open issue's test-function citations to resolve
  against the tree, and this one does not exist yet; §8's `#0392` note is the
  precedent for rephrasing instead of deleting the guidance.) Feed it the same
  `000030` pair as above, and assert exactly one violation naming the
  `.down.sql` file.

  **Guard verdict — warranted, and sensitive.** It is not `#0258`'s spiral: it
  does not live inside the file it guards and it guards no guard. Its subject
  is data outside the test file (the real `migrations/` tree), so mutating the
  scan input changes `got` and the in-package Go home is the legitimate one
  under §8's floor/ceiling rule rather than an external harness. Its five tests
  pass on the real tree. Sensitivity re-proved by this review in a throwaway
  worktree at `5d5c980` (never `git stash` — another agent held uncommitted
  work throughout), mutating a copy and restoring it hash-identical
  (`cb84ce2f…36cc81` before and after):
  - neutering the delete regex → `CatchesDestructiveDelete` FAILS
    ("expected a violation … got none");
  - short-circuiting the `ON CONFLICT` requirement to `false` →
    `CatchesNonIdempotentInsert` FAILS;
  - relaxing the version gate from `version <= 9` to `version < 0` →
    `Ignores000009Itself` FAILS, naming `000009_create_interests.down.sql`.
  All five pass again on restore. So the tests are load-bearing, not
  decorative, and the vacuous pass on today's tree is the intended tripwire
  state rather than an untested one.

  **Verification (review pass, re-run after writing these notes).**
  `ISSUE=0471r scripts/check.sh go ./internal/subscribers/... ./internal/handlers/... ./internal/db/...`
  → `VERIFICATION PASSED`. `internal/subscribers` ok 0.599s,
  `internal/handlers` ok 9.458s, `internal/db` ok 9.164s — real elapsed time,
  the skip audit reported `(no skips)`, and `gofmt -l` was clean. The scratch
  database `opencircuit_test_0471r` was provisioned straight from
  `migrations/` (the shared template is at 28 against this tree's 27) and
  dropped by `check.sh` on exit; `scripts/db-reset.sh` and
  `scripts/testdb.sh template` were not run. The leftover processes
  `check.sh` listed belong to another agent's concurrent
  `./internal/handlers/... ./internal/outbox/...` run and were left alone
  (§5a — kill only what you started). No sign of `#0470`'s flake in this run.
  Nothing was filed (§9); the two gaps worth a tracker entry are reported to
  the orchestrator instead.

- 2026-09-08 — Review pass, second attempt (phase 3, Opus). **Passed.**
  `uptime` at start: load averages 7.12 / 5.87 / 4.84; 4.19 / 5.23 / 4.69 at
  the mutation work; recorded per §5. Scope of this pass was the four checks
  the bounce named — the corrected diagnosis, the freeze re-derivation
  (`22|f`), `000009` being untouched, and the guard's homing were all settled
  by the first review and were not re-litigated.

  **(1) Both doc bullets describe today's behaviour, verified against the
  code rather than against the prose.** The slug bullet: `interests.Store.Update`
  is `Update(ctx, id, name, description, sortOrder, active)` — no slug
  parameter, and its `UPDATE interests SET name = $2, description = $3,
  sort_order = $4, active = $5` never writes the column;
  `internal/handlers/interests.go`'s PATCH body type carries no `slug` field
  and `decodeJSON` sets `DisallowUnknownFields`, so a PATCH sending one is a
  400 rather than a silent drop, exactly as the bullet says. The delete
  bullet: `interests.Store.Delete` is a single `DELETE ... WHERE id = $1 AND
  NOT EXISTS (SELECT 1 FROM subscriber_interests WHERE interest_id = $1)`,
  returning `ErrHasSubscribers` — `subscriber_interests` and nothing else —
  and both cited cascades check out
  (`migrations/000020_create_workshops.up.sql` and
  `migrations/000017_create_campaigns.up.sql` each declare
  `interest_id ... REFERENCES interests(id) ON DELETE CASCADE`). Neither
  bullet implies a fix is coming: the delete bullet states the gap in the
  present tense and names `PATCH {"active": false}` as the alternative, with
  no forward-looking language. That matters because `#0474` owns the fix and
  is open — the doc has to stay true until it lands, and it does.

  **(2) The new test earns its place, and it is the only thing that catches
  its regression.** `TestInterestTaxonomyMigrationGuardRejectsDeleteInDownMigration`
  feeds the prescribed pair — an `ON CONFLICT (slug) DO NOTHING` up file and
  the natural `DELETE FROM interests WHERE slug = ...` reversal in the down
  file — and asserts `len(violations) != 1` is fatal plus that the single
  violation names the `.down.sql`. Both halves matter: the count assertion is
  what makes the up file's silence load-bearing rather than incidental.
  Proved it would fail if down migrations stopped being scanned, rather than
  reasoning about it: in a throwaway worktree at the commit under review, a
  `strings.HasSuffix(name, ".down.sql") { continue }` skip inserted at the
  top of the scan loop left **all five original tests passing** and failed
  this one alone, "expected exactly one violation for the down migration's
  DELETE, got: []". A guard regression that the previous five tests are
  blind to is precisely what a sixth test is for.

  **(3) Re-ran one of the three mutation proofs myself**, since the pass
  edited the delete-violation message inside the same function the delete
  proof exercises, and that is where sensitivity degrades quietly. Same
  throwaway worktree: prefixing `deleteRe`'s pattern with a literal that
  cannot occur left `TestInterestTaxonomyMigrationGuardCatchesDestructiveDelete` failing with "expected a
  violation for a post-000009 migration deleting an interests row, got
  none". The message extension did not blunt the match. Restored after each
  mutation and confirmed byte-identical
  (`3884f0c4b06e232902b1444ea0257bc93f9c0d2ef59747dab7af39d07b943faa`, the
  same digest the implementation pass recorded); worktree removed, `git
  worktree list` shows only the main checkout and one unrelated agent's. Used
  a worktree specifically to avoid `git stash` (§8a) — two other agents had
  uncommitted work throughout.

  **(4) Nothing out of scope moved.** `8c9f8df` touches exactly three files
  (`docs/mailing-list.md`, the guard test, `issues/0471.md`; 123 insertions /
  10 deletions). `interests.Store.Delete` is untouched, `migrations/000009`
  is untouched, and nothing under `internal/interests/` or
  `internal/handlers/` changed — `git status --porcelain` over
  `migrations/`, `internal/interests/`, `internal/handlers/interests.go` and
  `docs/` is empty. `gofmt -l internal/db/` silent and the file hash stable
  across the mutation cycle.

  **The two gaps are documented, not fixed.** Recorded in the banner above
  as well as here, because a reader landing on a `resolved` issue should not
  conclude the taxonomy is now safe to prune: the cascade-blind `DELETE` is
  **`#0474`** and the immutable slug is **`#0475`**, both open. This issue
  resolved the documentation defect it was filed for — nothing about the
  runtime behaviour changed in it.

  **Verification (run after these notes were written).** `uptime`
  immediately before: load averages 3.24 / 4.82 / 4.57 — under §5's ~8 mark,
  so nothing here is load-attributable. `ISSUE=0471d scripts/check.sh go
  ./internal/subscribers/... ./internal/handlers/... ./internal/db/...` →
  `VERIFICATION PASSED`. `internal/subscribers` ok 0.665s,
  `internal/handlers` ok 9.949s, `internal/db` ok 9.626s — real elapsed time
  on all three, `(no skips)` from the skip audit, `gofmt -l` `(clean)`.
  **No test failed**: the `issues/0472.md` breakage this dispatch warned to
  expect in `internal/db` did not occur, so there was nothing to attribute to
  a file I did not touch — the concurrent agent evidently corrected it before
  this run. `opencircuit_test_0471d` was provisioned directly from
  `migrations/` (this tree at 27 against the shared template's 28) and
  dropped by `check.sh` on exit; confirmed absent from `pg_database`
  afterward. Did not run `scripts/db-reset.sh` or `scripts/testdb.sh
  template`. `check.sh`'s leftover-process listing named a
  `go test ./internal/handlers/... -count=1 -v` pair belonging to another
  agent; resolved both pids with `ps` rather than pattern-killing, found them
  already exited, and signalled nothing (§5a). The six guard tests were each
  observed running by their own full declaration name during the mutation
  work above, not merely inside a package-level `ok`.
