# 0025 — Create the subscribers migration

| | |
|---|---|
| **Status** | open |
| **Module** | db / subscribers |
| **Platform** | All |
| **Phase** | 3 — Mailing list capture |
| **First seen** | 2026-08-18 |

## Description

The `subscribers` and `subscriber_interests` tables, holding list membership independently of any user account, plus the consent evidence that makes the list defensible.

## Acceptance criteria

- [ ] `subscribers` table exactly per PRD §6.2, including `signup_ip`, `signup_user_agent`, `utm_*`, both token columns, and the unsubscribe columns
- [ ] `subscriber_interests` join table with a composite primary key and cascade deletes
- [ ] Indexes on status, `created_at DESC`, the interest side of the join, and a partial index on `confirm_expires_at`
- [ ] Email stored `lower(trim(...))` — enforced in the store layer and covered by a test
- [ ] Both token columns are `UNIQUE`
- [ ] `internal/subscribers` store with create, find-by-email, find-by-confirm-token, find-by-manage-token, set-interests, and status transitions
- [ ] Down migration drops cleanly
- [ ] `go test ./internal/subscribers/...` passes against a real database

## Notes

PRD §6.2. **Do not strip Gmail dots or `+tag` suffixes.** They are distinct addresses per RFC and people use them deliberately to segment their own mail; normalizing them merges accounts that should stay separate.

The consent columns are not bookkeeping — signup IP, signup user agent, signup timestamp, and confirmation timestamp are the record produced if a deliverability complaint ever needs answering. Populate them from the start.

Tokens are 32 random bytes from `crypto/rand`, base64url-encoded — not an HMAC of the email. Random tokens are revocable by rotating the column and leak nothing if a URL ends up in a referrer header or a screenshot.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §6.2 — Database schema · 158 lines
  `sed -n '/^### 6\.2 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0004](0004.md) — Renumber and apply the auth migrations
- Depends on: [#0007](0007.md) — Extend the config loader for the new environment variables
- Depends on: [#0023](0023.md) — Create the interests migration and seed the taxonomy
- Blocks: [#0026](0026.md) — Build the subscribe endpoint with anti-abuse controls
- Blocks: [#0030](0030.md) — Build the confirmation flow
- Blocks: [#0031](0031.md) — Build the preference center
- Blocks: [#0032](0032.md) — Build the subscribers admin screen
- Blocks: [#0033](0033.md) — Create the suppressions migration and store
- Blocks: [#0034](0034.md) — Build the one-click unsubscribe endpoint
- Blocks: [#0040](0040.md) — Create the campaigns migration
