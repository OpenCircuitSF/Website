# 0276 — `newTestWorker`'s doc sits on a const block, hidden by #0267's group exclusion

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | mailing / tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |
| **Closed** | 2026-08-27 |
| **Commit** | `86da218` |

## Resolution notes

> 🟢 Resolved 2026-08-27 — The `newTestWorker` sentence now sits on `func
> newTestWorker` and the constants keep their own caption; `gofmt` was proven
> stable across the split by hashing copies before and after two `gofmt -w`
> passes (byte-identical, per CLAUDE.md §8), `#0267`'s false claim is
> corrected in place rather than edited away, and the cost note landed in the
> header block that `collectDeclDocs`'s own doc points a guard-widening agent
> at.

## Description

`internal/mailing/worker_test_helpers_test.go:86` has

```go
// newTestWorker constructs a Worker …
const (
	testWorkerBaseURL = …
	…
)
```

so `func newTestWorker` is undocumented and the sentence describing it is
attributed to a block of constants. This is structurally the same defect
`#0267` fixed at `internal/handlers/subscribe_test.go:1605` — a doc comment
naming one declaration attached to its neighbour.

**`#0267`'s guard does not catch it, by design.** The guard excludes the
group-level doc of a `GenDecl` with two or more specs, because six such
instances exist on this tree and five are the benign, pervasive idiom of
captioning a `const`/`var` group (`CampaignStatus*`, `PreflightCode*`,
`SuppressionReason*`, `Source*`, `Status*`). This one is the sixth, and it is
not benign.

`#0267`'s `## Implementation notes` states of those six that **"None is
`#0267`'s defect shape"**. That is wrong for this instance, and `#0267`'s review
corrected the record rather than bouncing the issue, since the stakes are a
test-only helper and the comment's second sentence genuinely does describe the
constants.

## Why no structural rule fixes this

The review checked the obvious narrowing before endorsing the exclusion:
*"check a group doc when its first word names a function in the same file"*
would still flag `Preflight` (`internal/mailing/preflight.go:150`) and
`Unsubscribe` (`internal/db/store.go:824`), both of which are legitimate
captions that happen to share a name with a function. No cheap structural rule
separates the good from the bad here, which is why the exclusion stands and
this instance needs fixing by hand.

Also recorded, for whoever touches the guard next: a **single-member**
parenthesised `const (…)` / `var (…)` block carrying a category-noun doc *is*
checked and would false-positive. None exists on the tree today; the
`nonTestIdentifierAllowlist`-style allowlist is the escape hatch if one appears.

## Acceptance criteria

1. The `newTestWorker` sentence moves onto `func newTestWorker`. The remaining
   sentence, which describes the constants, stays on the `const` block — this
   is a split, not a move of the whole comment.
2. `go doc` on the helper is not the check (it is an unexported test symbol);
   instead assert by reading that the comment above `func newTestWorker` opens
   with `newTestWorker`.
3. `#0267`'s `## Implementation notes` claim that none of the six group-level
   hits is its defect shape is corrected in `issues/0267.md`, with a pointer to
   this issue. Do not silently edit the sentence away — record that the
   exclusion is knowingly hiding one real instance.
4. A comment at `#0267`'s group-size exclusion records the same, so the next
   agent to widen the guard knows what the exclusion currently costs.
5. `ISSUE=NNNN scripts/check.sh go ./internal/...` passes with no skips, and
   `gofmt -l` is clean — note CLAUDE.md §8 on gofmt rewriting doc comments.

## Relation

- Found by: [#0267](0267.md) — its review, which endorsed the exclusion and
  found this instance sitting behind it
- Relates to: [#0127](0127.md) — the original misattachment that motivated the
  guard
- Relates to: [#0263](0263.md) — comments that are wrong rather than
  misattached, the class no guard catches

## Work log

- 2026-08-27 — Implemented (phase 2, Sonnet).
  `internal/mailing/worker_test_helpers_test.go`: split the comment above the
  `const` block — "newTestWorker constructs a Worker wired to
  pool/mailer/progress with fast defaults (no real waiting), and its own
  SendStore." now sits directly above `func newTestWorker`; "baseURL/listDomain
  are fixed test constants shared by every worker test." stays on the `const`
  block. `gofmt -w` run twice, hash stable both times, `gofmt -l` clean.
  `internal/handlers/misattached_doc_comment_guard_test.go` (confirmed clean
  in `git status` before editing): added a paragraph at the end of the
  group-size exclusion's own comment (criterion 4) recording that the
  `worker_test_helpers_test.go` entry it lists was not actually benign, that
  `#0276` fixed the instance, and that the exclusion itself is unchanged and
  still cannot structurally tell a real corruption of this shape from the
  five genuinely benign category-noun captions beside it.
  `issues/0267.md`: added a `> **Correction (#0276, 2026-08-27).**` blockquote
  immediately after the `## Implementation notes` paragraph that wrongly
  claims "None is `#0267`'s defect shape" (criterion 3) — the original
  sentence is left in place, per the issue's own instruction not to silently
  edit it away, and the correction explains which of the six instances was
  wrong and points to this issue and to the matching note in the guard file.
  `#0267` stays `resolved`; only its record was corrected.

## Verification

`uptime` before the scoped run: load averages 1.35/1.94/2.00, well under the
~8 threshold (§5).

`gofmt -l` on every file touched: clean. `go build ./...` and
`go vet ./internal/mailing/... ./internal/handlers/...`: clean.

`ISSUE=0276 scripts/check.sh go ./internal/mailing/... ./internal/handlers/...`
reported `internal/handlers` FAILED, but the single failure
(`TestNoCommentCitesUndefinedTestFunction`, citing
`internal/subscribers/imports_test.go:106`) is unrelated to this issue: that
file is untracked, in-flight work from a different concurrently running agent
(`git status --porcelain` shows `internal/subscribers/imports.go` and
`internal/subscribers/imports_test.go` both `??`, matching the orchestrator's
brief for this session). Re-verified in an isolated database
(`opencircuit_test_0276`, created via `scripts/testdb.sh create 0276`,
dropped after): `go test ./internal/mailing/... -p 2` → `ok` (2.932s, no
skips); `go test ./internal/handlers/... -run
'TestNoDocCommentNamesADifferentDeclarationInSameFile|TestMisattachedDocCommentGuard'
-v` → all six PASS, including the tree-wide guard reporting zero hits with
this issue's fix in place; the same failing
`TestNoCommentCitesUndefinedTestFunction` reproduced in isolation, confirming
it is pre-existing and unrelated. `pgrep -f 'go test|\.test '`: none left
running.

## Review notes

> 🟢 Approved 2026-08-27 (phase 3, Opus). Re-verified independently; nothing
> was taken from the implementer's `## Verification`.

**Criterion 1 — the split.** Confirmed by reading
`internal/mailing/worker_test_helpers_test.go:86-95`: the `const` block now
carries only "baseURL/listDomain are fixed test constants shared by every
worker test.", and "newTestWorker constructs a Worker wired to
pool/mailer/progress with fast defaults (no real waiting), and its own
SendStore." sits directly above `func newTestWorker` with no blank line
between. That is a split, not a move — both sentences survive, each on the
declaration it describes.

**Criterion 2 — the comment opens with `newTestWorker`.** Asserted by reading,
as the issue directs (`go doc` cannot see an unexported test symbol).

**Criterion 5 / gofmt stability — redone here, not trusted.** CLAUDE.md §8 is
explicit that gofmt rewrites doc comments and that the check is a
before/after hash, so this review copied all six touched files into a
scratch directory (never `gofmt -w` on the shared tree — two other agents
had uncommitted work) and hashed them before, after one `gofmt -w`, and
after a second. All six SHA-256s byte-identical across all three
measurements; `gofmt -l` clean on the copies. `scripts/check.sh`'s own
tree-wide `gofmt -l` stage also reported `(clean)`.

`ISSUE=0276r scripts/check.sh go ./internal/mailing/... ./internal/handlers/...`
→ `VERIFICATION PASSED`. `internal/mailing` `ok` 3.153s, `internal/handlers`
`ok` 7.140s, skip audit `(no skips)`, `go build`/`go vet`/`gofmt -l` all
clean. Note this run was **fully green including `internal/handlers`** — the
`TestNoCommentCitesUndefinedTestFunction` failure the implementer recorded
was indeed the other agent's in-flight `internal/subscribers/imports_test.go`
and has since cleared on its own, which confirms the attribution.

The six misattached-doc guard tests re-run individually:
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`...CatchesThe0127Shape`, `...CleanOnceReattached`,
`...SkipsMultiMemberGroupLevelDoc`,
`...StillChecksPerMemberDocInsideAGroup`,
`...IgnoresOrdinaryProseOpeners` — all PASS. The first is the tree-wide scan,
so its pass **is** the zero-hits result.

**Criterion 3 — corrected, not edited away.** Verified in `issues/0267.md`:
the original sentence "None is `#0267`'s defect shape" is still present at
its original position, with a `> **Correction (#0276, 2026-08-27).**`
blockquote immediately after it naming which of the six was wrong and
pointing here and at the guard file. `git show 86da218 -- issues/0267.md`
changed no metadata-table row: `#0267` is still `resolved`, `**Closed**
2026-08-27`, `**Commit** 64480e4`.

**Criterion 4 — does the note actually stop the next agent?** Judged yes, on
the strength of the pointer chain rather than the prose alone. The note
landed at the end of the header block's "Two exclusions" list (immediately
above `misattachedDocFirstWordPattern`), which is *documentation* of the
exclusion, not its implementation — the implementation is
`collectDeclDocs`'s `if d.Doc != nil && len(d.Specs) == 1`. That would be a
weakness if the code site were silent, but it is not:
`collectDeclDocs`'s own doc says "a GenDecl with two or more Specs is a
genuinely grouped block, and **this file's header explains why** its
group-level Doc is not checked at all". An agent editing the group-size
condition is sent to the header, and the header's last paragraph now states
what the exclusion costs and that a future misattributing group doc "will
again pass silently until a human reads it, exactly as this one did". The
bullet above still lists `worker_test_helpers_test.go` among the benign
examples — deliberately, and the new paragraph opens by naming that entry as
the one that was not benign, which is the same correct-don't-delete
treatment criterion 3 asked for in `issues/0267.md`.

**Scope and process.** `git show --stat 86da218` lists exactly three files —
`internal/handlers/misattached_doc_comment_guard_test.go` (+14),
`internal/mailing/worker_test_helpers_test.go` (+3/-3), `issues/0267.md`
(+16). No migration touched, no other agent's file present, so the
`git restore --staged` recovery the implementer disclosed (CLAUDE.md §8a's
own documented remedy) worked: nothing of theirs was swept in or damaged.
