# 0059 — Export subscribers as CSV

| | |
|---|---|
| **Status** | in-progress |
| **Module** | subscribers / admin |
| **Platform** | All |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-18 |
| **Commit** | b434fab |

## Description

A CSV export of the list for backup, analysis, and portability.

## Acceptance criteria

- [x] `GET /admin/subscribers/export` streams CSV
- [x] Columns: email, status, interests (semicolon-joined), confirmed_at, created_at, utm_source, utm_medium, utm_campaign
- [x] Respects the current status and interest filters
- [x] Streamed rather than buffered so a large list does not exhaust memory
- [x] Export is audited — who exported, when, how many rows
- [x] Fields containing commas, quotes, or newlines are correctly quoted

## Notes

PRD §8. Auditing the export matters: a full list export is the highest-value action an admin account can take, and it is exactly what an attacker with a stolen session would do.

Guard against CSV injection — a leading `=`, `+`, `-`, or `@` in a field is executed as a formula when the file opens in Excel. Prefix such fields with a single quote.

**Amended 2026-08-21 by `#0046`'s second phase-3 review.** `subscribers` gained
a `synthetic boolean NOT NULL DEFAULT false` column in migration `000019`
(`#0046`): `true` only for the per-admin campaign test-send recipient rows
`ensureTestRecipient` creates. **The export must exclude `synthetic = true`.**
If this issue reuses `subscribers.Store.List` it inherits the exclusion for
free — `List` applies it unconditionally and it is not a `ListFilter` option.
If it writes its own streaming query instead (likely, given the "streamed
rather than buffered" criterion), that query must carry the predicate itself,
or every admin who has ever run a campaign test send puts a
`campaign-test+admin-<id>@internal.opencircuitsf.test` fixture row into an
operator-facing CSV.

## Fix

Added `GET /admin/subscribers/export`, gated by the same
`RequireSession`→`RequireAdmin` middleware chain as every other admin route
and registered through `cmd/opencircuit/main.go`'s `adminRoutes()` — so
`TestMountAndServe_AdminRoutesRequireSessionAndAdmin` (the single
authoritative wiring proof) covers it automatically, with no test edit
required.

- **Store layer**: `internal/subscribers/export.go` adds
  `Store.StreamExport(ctx, filter, fn)`, a new query rather than a reuse of
  `List` — it invokes `fn` once per `pgx` row as rows arrive (`rows.Next()`),
  never collecting a `[]Subscriber`, and computes each subscriber's
  interests with `string_agg(i.slug, ';' ORDER BY i.slug)` over a `LEFT
  JOIN`, so no per-row interest query is needed. `filter.InterestID` is
  applied as an `EXISTS` subquery, independent of the aggregation join, so
  filtering to "has interest X" never truncates a matching subscriber's
  OTHER interests out of the semicolon-joined column. `synthetic = false` is
  unconditional, per this issue's amended Notes.
- **Handler**: `internal/handlers/admin_subscribers_export.go` validates
  `status`/`interest_id` exactly like `List` (400 before any header is
  written), sets headers, then writes an `encoding/csv.Writer` directly over
  the `http.ResponseWriter` inside the `StreamExport` callback — so neither
  layer buffers the full result set. Ends with one `subscriber.exported`
  audit row (new `internal/audit/actions.go` constant), success or
  mid-stream failure, recording `row_count`, `filter_status`,
  `filter_interest_id`, `filter_query`.
- **Column set**: email, status, interests (semicolon-joined **slugs**, not
  display names — stable, and structurally CSV-safe since slugs are
  `interests_slug_format`-constrained lowercase-hyphenated), confirmed_at,
  created_at, utm_source, utm_medium, utm_campaign — exactly the eight
  columns above, nothing more. Deliberately excludes `confirm_token` /
  `manage_token` (live bearer credentials — no column even exists for them
  in `subscribers.ExportRow`), `id`, and
  `signup_ip`/`signup_user_agent`/`unsubscribed_at`/`unsubscribe_source`
  (not asked for, and not shown on the admin **list** screen either — only
  the one-row detail view has them).
- **CSV injection**: `csvInjectionGuard` prefixes any field whose first byte
  is `=`, `+`, `-`, or `@` with a single quote, per this issue's Notes.
  Applied to every externally-influenced text column: email, interests,
  utm_source, utm_medium, utm_campaign. `utm_*` is the realistic vector — it
  comes straight from the public, unauthenticated `/api/subscribe` query
  string.
- **NULL handling**: `derefOrEmpty`/`formatExportTime` map a nil pointer to
  `""`, never the literal text `<nil>`/`NULL`.
- **Headers**: `Content-Type: text/csv; charset=utf-8`,
  `Content-Disposition: attachment; filename="subscribers-export.csv"` (a
  fixed literal — no user input is ever interpolated into it), and
  `X-Content-Type-Options: nosniff`.

## Verification

`uptime` before starting: load average ~2.7–3.6 (idle; well under the ~8
threshold CLAUDE.md §5 flags).

Own database throughout (`ISSUE=0059 scripts/check.sh`, which creates and
drops `opencircuit_test_0059`; a second scratch database
`opencircuit_test_0059x` was created via `scripts/testdb.sh create 0059x`
for an ad hoc `-run` pass and dropped afterward with `scripts/testdb.sh drop
0059x`). Every seeded row is scoped with a `zz-subtest-` email prefix (or,
for the two CSV-injection-in-the-email-column rows, a per-id
`t.Cleanup(... DELETE FROM subscribers WHERE id = $1 ...)` — never a
hardcoded or seeded id, per CLAUDE.md §8b).

- `ISSUE=0059 scripts/check.sh go ./internal/... ./cmd/...` — zero skips,
  every DB-backed package (`internal/audit`, `internal/auth`, `internal/db`,
  `internal/handlers`, `internal/middleware`, `internal/subscribers`,
  `cmd/opencircuit`, …) reported `ok` with real non-zero elapsed times
  (`internal/handlers 2.968s`, `cmd/opencircuit 1.574s`, etc.), not `[no test
  files]`.
- `go test ./internal/handlers/... -run 'TestAdminSubscribers_Export|TestAdminSubscribers_NonAdminForbidden' -v`
  — all 7 pass: `TestAdminSubscribers_Export_Headers`,
  `TestAdminSubscribers_Export_CSVEdgeCases` (comma, double quote, embedded
  newline, UTF-8, a 6000-byte field, NULL UTM columns, and CSV/formula
  injection in `utm_source`/`utm_medium`/`utm_campaign` **and** in the email
  column itself, all round-tripped through a real `encoding/csv.Reader`),
  `TestAdminSubscribers_Export_InterestsSemicolonJoined`,
  `TestAdminSubscribers_Export_ExcludesSynthetic`,
  `TestAdminSubscribers_Export_RespectsStatusAndInterestFilters`,
  `TestAdminSubscribers_Export_Audited`, and
  `TestAdminSubscribers_NonAdminForbidden` (extended with the new export
  route — a non-admin session gets 403, proven by an actual request against
  a running `httptest.Server`, not by inspection).
- `go test ./cmd/opencircuit/... -run TestMountAndServe_AdminRoutesRequireSessionAndAdmin -v`
  — PASS, against the real `mountAndServe` wiring (not the handler-local
  mux), confirming the export route inherits the session+admin guard exactly
  like every other admin route with no test edit required (the test
  enumerates `adminRoutes()` automatically).

## Files changed

- `internal/subscribers/export.go` — new: `ExportRow`, `Store.StreamExport`.
- `internal/handlers/admin_subscribers_export.go` — new: the `Export`
  handler, CSV injection guard, NULL-safe formatters.
- `internal/handlers/admin_subscribers.go` — extended `adminSubscriberStore`
  with `StreamExport`; documented the new route in the handler's doc
  comment.
- `internal/audit/actions.go` — added `ActionSubscriberExported`.
- `cmd/opencircuit/main.go` — registered
  `GET /admin/subscribers/export` in `adminRoutes()`.
- `internal/handlers/admin_subscribers_test.go` — wired the export route
  into `adminSubscribersMux` and `TestAdminSubscribers_NonAdminForbidden`'s
  case table.
- `internal/handlers/admin_subscribers_export_test.go` — new test file (7
  tests) covering headers, CSV edge cases/injection, interests joining,
  synthetic exclusion, filters, and the audit row.

## Gotchas

- Go 1.22+'s `net/http.ServeMux` prefers a literal path segment over a
  wildcard regardless of registration order, so `GET
  /admin/subscribers/export` and `GET /admin/subscribers/{id}` coexist on
  the same mux with no collision risk — `export` is never parsed as an
  `{id}`. Confirmed by the passing wiring test, not just by reading the
  stdlib docs.
- Did not touch `web/` (owned by `#0094` in this dispatch) or
  `internal/db/` (under `#0113` review) — the export has no admin-UI piece
  in this pass; a "download" button/link on the subscribers screen would be
  a natural follow-up for whichever issue/agent owns that screen next.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §8 — HTTP API · 47 lines
  `sed -n '/^## 8\. /,/^## [0-9]/p' PRD.md`
- Depends on: [#0032](0032.md) — Build the subscribers admin screen
- Blocks: [#0060](0060.md) — Handle GDPR erasure requests

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 134 | 7,251 | 8,934,912 | 157,633 | $3.38 |

**Total: $3.38**
