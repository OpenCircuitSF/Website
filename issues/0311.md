# 0311 — `net_30d` subtracts an imported subscriber twice when they leave in-window

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | subscribers / admin |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |

## Description

`#0305` added `imported_30d` so import-driven growth stops being invisible, and
`net_30d` became `confirmed_30d + imported_30d - unsubscribed_30d`. That is
correct as far as it goes, and `#0305`'s review verified the predicate is exact
against every subscriber status and that the two positive buckets are mutually
exclusive, so nothing double-counts on the way *in*.

**But `imported_30d` is a current-state count while its two siblings are event
counts.** Its predicate is `status='active' AND confirmed_at IS NULL AND
created_at >= since` — the `status='active'` clause means a subscriber who
leaves stops being counted, retracting their own `+1`, *and* is counted again by
`unsubscribed_30d`.

Measured by `#0305`'s reviewer in a rolled-back transaction on a scratch
database:

| Step | `net_30d` |
|---|---|
| baseline | 1 |
| after import | 2 |
| after that subscriber unsubscribes | **0** |

So someone who joined and left inside the window contributes **−1**. A locally
confirmed subscriber correctly nets 0, because `confirmed_at` is never cleared —
the asymmetry is entirely in the stock-versus-event mismatch.

**`ImportStore.Revoke` (`#0125`) makes this reachable in bulk.** Revoking a
500-address import renders `net_30d = −500` on a list that is merely flat.

**This is not a regression.** The old code produced the identical end state and
never showed the `+1` at all, so `#0305`'s formula is no worse in any case and
better in most — which is why its review resolved rather than bounced.

## The doc comment is also describing a flow while measuring a stock

`Growth30Days`'s comment says imported subscribers "landed active via an import
… since that time". Today it describes an event and measures a current state.
The fix below makes the sentence literally true.

## Acceptance criteria

1. `imported_30d` becomes an **event** count like its siblings — the natural
   form is `created_at >= since AND source = 'import'`, independent of current
   status. Confirm `source` is set on every import path before relying on it.
2. `net_30d` then behaves symmetrically: a subscriber who joins by import and
   leaves inside the window nets 0, exactly as a locally confirmed one does.
   **Prove that with the reviewer's three-step measurement**, not by inspection.
3. A bulk `Revoke` of an import does not drive `net_30d` negative on a flat
   list. Test it with a seeded import and revoke.
4. `Growth30Days`'s doc comment and `formatGrowthDetail`'s must still state the
   same formula and now be literally accurate — `#0305`'s criterion 3 invariant,
   which this must not break.
5. `#0292`'s recorded decision in that doc comment survives intact.
6. Check whether `confirmed_30d` and `unsubscribed_30d` have any equivalent
   stock-versus-event mismatch, and report the answer even if it is no.
7. `ISSUE=NNNN scripts/check.sh go ./internal/subscribers/... ./internal/handlers/...`
   and `scripts/check.sh web` pass with no skips.

## Relation

- Found by: [#0305](0305.md)'s review, measured rather than argued
- Relates to: [#0292](0292.md) — which stopped stamping `confirmed_at` on imports
- Relates to: [#0125](0125.md) — whose `Revoke` makes the bulk case reachable
- Relates to: [#0061](0061.md) — the growth dashboard

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-08-27 | implement (batch with #0317, #0316) | claude-sonnet-5 | — | — |

**Predicate.** `internal/subscribers.Store.Growth30Days`'s `imported` count is
now `count(*) FILTER (WHERE source = $2 AND confirmed_at IS NULL AND
created_at >= $1)`, parameterized on `SubscriberSourceImport`, replacing the
old `status = 'active' AND confirmed_at IS NULL AND created_at >= $1`.

**Not the issue's literal suggested form.** The issue's "natural form" was
`created_at >= since AND source = 'import'` alone. Implemented with an added
`confirmed_at IS NULL` guard instead, because `source` is never rewritten
after INSERT (`#0317`), so a committed IMPORT INVITE that is later accepted
within the same window keeps `source='import'` even after `Confirm` sets its
`confirmed_at` — without the guard it would count in both `imported_30d` AND
`confirmed_30d` for the same join, double-counting one new subscriber as two.
`confirmed_at IS NULL` is exactly the test that already defines "confirmed"
for the first return value, so the two buckets stay mutually exclusive on the
way in — `#0305`'s reviewer-verified invariant (criterion 4/5) — while still
being a pure event count, independent of `status`, on the way out: nothing a
row does later (unsubscribe, get bulk-revoked, bounce) changes whether it was
counted here, which is what actually fixes the double-subtraction. Read
`#0317` first, per the batch's instruction — its "Source is historical
provenance, never rewritten" finding is what makes this guard correct rather
than an unrelated afterthought.

**Criterion 1 (source set on every import path).** Verified: both `Commit`
branches in `internal/subscribers/imports.go` — `ConsentModeInvite` (line
~582) and `ConsentModePriorConsent` (line ~620) — pass
`SubscriberSourceImport` into the INSERT explicitly. The only other outcome
(`existing[key] || suppressed[key]`) skips the row entirely rather than
touching it, so no import path can produce a row whose `source` is unset or
wrong.

**Criterion 2 (three-step proof).** Added
`TestGrowth30Days_ImportedSubscriberLeavingInWindowNetsZero`
(`internal/subscribers/imports_test.go`): baseline net (confirmed + imported
- unsubscribed) → after a `prior_consent` `Commit` (net = baseline+1) → after
that same subscriber's `Unsubscribe` (net = baseline, not baseline-1). Passes.

**Criterion 3 (bulk revoke).** Added
`TestGrowth30Days_BulkRevokeDoesNotDriveNetNegative` (same file): seeds a
3-address import, confirms net = baseline+3 after `Commit`, then
`ImportStore.Revoke`s the whole batch and confirms net returns to baseline
(not baseline-3). Passes.

**Criteria 4 and 5 (doc comments, `#0292`'s decision).** Rewrote
`Growth30Days`'s doc comment (`internal/subscribers/store.go`) and
`formatGrowthDetail`'s (`web/src/lib/dashboard.ts`) to describe the new
event-based predicate and why the `confirmed_at IS NULL` guard is there,
while keeping the formula itself (`net_30d = confirmed + imported -
unsubscribed`) and `#0292`'s "confirmed_at left NULL on a prior_consent
import" decision unchanged and explicitly reaffirmed in both comments.
`TestAdminDashboardOverview_GrowthCountsImportSeparateFromConfirmed`
(`internal/handlers/admin_dashboard_test.go`, `#0305`'s handler-level proof
of the same invariant) still passes unmodified.

**Criterion 6 (confirmed_30d / unsubscribed_30d — any equivalent mismatch?
No, for the bug pattern this issue is about.** Neither predicate filters on
current `status` at all (`confirmed_at >= $1` / `unsubscribed_at >= $1`), so
there is no status-gated retraction like the one `imported_30d` had. Noted
one adjacent, PRE-EXISTING, out-of-scope observation for the record:
`RestartSignup` clears `confirmed_at` back to `NULL` (it always has — not
introduced by `#0129` or this issue) when a row that previously unsubscribed
restarts as a fresh self-initiated signup, so a confirm → unsubscribe →
resignup sequence, all inside one 30-day window, would retroactively drop
that row out of `confirmed_30d` while `unsubscribed_30d` keeps counting the
earlier departure. This differs from `imported_30d`'s bug in mechanism (a
column reset on a THIRD event, not a current-status filter on the existing
two) and needs three events in sequence rather than the two-event
join-then-leave `#0311` is about. Not fixed here — no acceptance criterion
asked for it, and it predates this batch. Flagging for the orchestrator to
judge whether it's worth its own issue.

## Verification

Shared database for this three-issue batch (`ISSUE=0311
scripts/testdb.sh create 0311`, dropped at the end via `scripts/check.sh`'s
own cleanup). No migration — no schema change.

```
ISSUE=0311 scripts/check.sh go ./internal/subscribers/... ./internal/handlers/...
ISSUE=0311 scripts/check.sh web
```

Go: `internal/subscribers` ok (0.542s), `internal/handlers` ok (4.733s), zero
skips — includes both new tests above plus every pre-existing
`Growth30Days`/dashboard test (`TestGrowth30Days_BoundaryAndSyntheticExclusion`,
`TestImportStore_Commit_DoesNotInflateGrowth30Days`,
`TestAdminDashboardOverview_SubscriberCountsAndGrowthExcludeSynthetic`,
`TestAdminDashboardOverview_GrowthCountsImportSeparateFromConfirmed`), all
still green against the revised predicate. Web: `svelte-check` 290 files / 0
errors / 0 warnings; `vitest` 40 files / 3986 tests passed (includes
`dashboard.test.ts`'s `formatGrowthDetail` suite, unaffected by the doc-only
comment change). `gofmt -l` clean.
