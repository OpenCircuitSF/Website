# 0466 — Three prepared instructions reference repo files the box does not have

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | deploy |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-08 |
| **Closed** | 2026-09-09 |
| **Commit** | `92e56b3`, `a86ddd0` |

> 🟢 Resolved 2026-09-09 — all three prepared box instructions now state the
> missing `scp` precondition ahead of the step that consumes it, with the reason
> `scp` was chosen over `git pull` recorded per instruction; box state re-derived
> read-only and independently in review.

## Description

`#0447`, `#0435` and `#0465` each prepared an approval-ready instruction for the
user to run on the box. **All three reference files from the repository, and the
box's checkout does not contain them.**

Measured read-only on 2026-09-08:

| Checkout | HEAD | certbot hook | `backup-media.sh` |
|---|---|---|---|
| `/opt/opencircuit` | `ef0a58f` — `#0274` | absent | absent |
| `/home/ec2-user/OpenCircuitSF` | `6e7af65` "adds placeholder website" | absent | absent |

`/opt/opencircuit` is **711 commits behind** `main`. That is not itself a defect
— nothing has been deployed since `#0274`, which matches the state of the work —
but it means every instruction of the form *"install this file from the repo"*
fails at its first step, in a way the user discovers rather than the author.

`#0435`'s pass found this independently for its own scripts and documented a
`scp` pre-step. `#0447`'s and `#0465`'s instructions were written without it, and
`#0447` is already `resolved` — so the correction has to reach a closed issue's
recorded instruction, not just an open one.

## Why this is one issue and not three

The three instructions share a single missing precondition. Fixing them
separately invites three different spellings of it, and `#0431`'s lineage is a
standing demonstration of what happens when the same fact is maintained in
several places by hand.

There is also a second-order point worth settling once: **`#0435` chose `scp` and
a SHA-256 comparison; a `git pull` would bring everything at once.** Those are
different trades — `scp` touches exactly the files named and needs no network
access from the box to GitHub; `git pull` is one command but moves 711 commits,
including a binary rebuild's worth of source. Pick one per instruction with a
stated reason rather than by habit.

## Acceptance criteria

1. Each of `#0447`, `#0435` and `#0465`'s instructions states the precondition
   explicitly, with the command that satisfies it, before the step that consumes
   it.
2. **Re-derive the checkout state yourself** rather than copying the table above
   — `CLAUDE.md` §7's standing instruction, and every inherited number in this
   repo's recent history has been wrong at least once.
3. Decide `scp`-versus-`git pull` **per instruction**, with the reason recorded.
   `#0435` already chose `scp` for three files and verified by hash; do not
   silently change it, but say whether it is still right alongside the others.
4. `#0447` is `resolved`. Amend its recorded instruction **additively** —
   `#0421`'s precedent for touching settled content — and do not reopen it
   unless the amendment changes what was decided rather than how it is run.
5. **Nothing runs on the box.** Read-only confirmation only; these are
   instructions for the user (§5b, §9).
6. Check whether any *other* prepared instruction in the tracker has the same
   shape. One missed precondition is weak evidence the rest are complete.

## Relation

- `#0447` (resolved, prepared-not-applied), `#0435` and `#0465` are the three
  instructions.
- `#0434` added the media scripts the box's checkout predates.
- `CLAUDE.md` §7 records the box; §5b requires clearing approval-gated steps in
  advance rather than stalling mid-change.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/db/... ./internal/handlers/...`. No test reads `docs/deployment.md` — say so plainly rather than implying coverage.

## Work log

- 2026-09-08 — **Implementation pass (Sonnet).**

  **Criterion 2 — re-derived, not copied.** `ssh -o BatchMode=yes ec2` (a
  live read-only session, not the config file's claim):

  - `/opt/opencircuit`: `git rev-parse HEAD` → `ef0a58f6426f9d305cebbc4081df9c5634c3f572`
    (`#0274`), matching the filed table exactly.
  - `/home/ec2-user/OpenCircuitSF`: `git rev-parse HEAD` →
    `6e7af65e43d75c24f61a6173c5b49241319529aa` ("adds placeholder website"),
    also matching.
  - `deploy/certbot/`, the certbot hook, and `scripts/db/backup-media.sh` /
    `restore-media.sh` — all confirmed **absent** on `/opt/opencircuit` (`ls`:
    "No such file or directory" for each).
  - `systemctl show opencircuit.service -p ReadWritePaths` on the box →
    empty, and `grep ReadWritePaths` on `/opt/opencircuit`'s own copy of
    `deploy/systemd/opencircuit.service` → no match — the file exists there
    but is the pre-`#0465` version, not absent, which is the more precise
    defect for that one instruction (see criterion 1 below).
  - **The commit count has moved**: `git rev-list --count
    ef0a58f..HEAD` against this working tree's `main` is now **716**, not
    711 — five commits landed since the issue was filed a few hours ago.
    Both numbers describe the same underlying fact (the box predates `#0274`
    by hundreds of commits); this pass's own doc edits state it as "several
    hundred commits" rather than pin a count that will drift again by the
    time anyone reads this.
  - **One additional read, disclosed:** `git remote -v` on the box shows
    `origin` is `https://github.com/OpenCircuitSF/Website.git`, and
    `git ls-remote --exit-code origin HEAD` succeeded, returning a ref — the
    box can reach GitHub over the network. This is a real-but-read-only
    network probe (no objects fetched, no working-tree or ref change on the
    box); it does not contradict the issue's `scp`-needs-no-network-*from-
    the-box* framing (that's about the direction the connection runs), it
    just confirms `git pull` is technically reachable, not merely a
    theoretical alternative, which matters for criterion 3 below.

  **Criterion 1 — the precondition as it now appears in each instruction,
  quoted:**

  - `#0447` (`docs/deployment.md` §9, before the "In order" install
    sequence): *"Precondition — the box's checkout does not have this file
    yet (`#0466`)... `deploy/certbot/reload-apache-deploy-hook.sh`... Step 1
    below fails at its first command... with 'No such file or directory'
    until this is done."*
  - `#0465` (`docs/deployment.md` §7, before the `chgrp`/`chmod`/`cp`
    sequence): *"Precondition — the box's checkout of this file is stale
    (`#0466`)... its copy of `deploy/systemd/opencircuit.service` predates
    `#0465`'s `ReadWritePaths=/var/www/media` line entirely... Step 2 below
    ... would therefore install the *old* unit unchanged — no error, no
    `ReadWritePaths=`."*
  - `#0435` (`deploy/systemd/README.md`, "Bring the on-box checkout current
    for the media leg" — pre-existing, unchanged by this pass): *"A second,
    independent blocker, found by re-deriving rather than trusting the last
    recorded state: `/opt/opencircuit`'s git checkout is at `ef0a58f`... 711
    commits behind... `scripts/db/backup-media.sh` and
    `scripts/db/restore-media.sh` do not exist there at all."*

  **Criterion 3 — `scp` vs `git pull`, decided per instruction:**

  | Instruction | Decision | Reason |
  |---|---|---|
  | `#0447` | `scp` (new decision) | One file, no Go/SPA rebuild needed; `git pull` on this 418 MB box (`CLAUDE.md` §7) would force `go build`/`npm run build` for a change that touches neither the binary nor the SPA. |
  | `#0465` | `scp` (new decision) | Same reasoning — one unit file, no rebuild needed. |
  | `#0435` | `scp` (reviewed, unchanged) | Already chosen and hash-verified for its three files; re-examined rather than assumed still right, and the reasoning holds for the same rebuild-cost reason — `deploy/systemd/README.md` already states this trade-off in its own text ("that rebuilds the whole Go binary and SPA on a 418 MB box... not a prerequisite for turning backups on today"). |

  All three land on `scp` for the same underlying reason: none of the three
  changes needs a rebuilt binary or SPA, and `git pull` would force one
  anyway (`golang-migrate`/`go build`/`npm run build`) while also landing
  ~716 commits of otherwise-unreleased work onto the production checkout in
  one step — a materially bigger and riskier change than any of the three
  instructions is asking for. A `git pull` remains the right tool for the
  next full redeploy (`#0404`'s job, per `docs/deployment.md`'s own Redeploy
  procedure), not for these narrow fixes.

  **Criterion 4 — `#0447` amended additively, not reopened.** Added a
  `## Correction (`#0466`, 2026-09-08)` section at the end of `issues/0447.md`
  stating plainly that the hook script, the deploy-hooks-directory choice,
  the two flag claims, and the scoped `--cert-name` command are all
  unchanged; only the file-reaches-the-box precondition was missing. This
  changes *how the sequence is run*, not *what was decided* — no reopening.
  `issues/0447.md`'s `Status` row is unchanged (`resolved`).

  **Criterion 5 — nothing ran on the box.** Every command used above was one
  of: `ssh ... 'git rev-parse HEAD'`, `git log -1`, `ls -la`, `sha256sum`,
  `systemctl show ... -p ReadWritePaths` (a read-only property query, not a
  restart), `grep`, `git remote -v`, and `git ls-remote` (read-only, see
  above). No file was written, copied, or modified on the box; no service
  was started, stopped, or reloaded; no package was installed.

  **Criterion 6 — sweep of the rest of the tracker.** Grepped every
  `issues/*.md` and the `deploy/`/`docs/deployment.md` prose for
  approval-ready/prepared-but-not-applied language and for the
  `sudo cp deploy/…`/`sudo install -m`/`scp …ec2:` shape that this defect
  actually takes. Found `#0417` (resolved — files already `scp`'d and
  hash-confirmed present on the box, nothing pending), `#0464` (resolved —
  its prepared instruction is a *transient* systemd unit with synthetic
  values, not a file copied from the repo, so the defect shape does not
  apply), and `#0429` (resolved — a `.env.example` inline-comment defect,
  unrelated). **Nothing else found.** No new issues filed (`CLAUDE.md` §9).

  **Verification.**

      uptime
      15:34  up 19 days,  1:44, 29 users, load averages: 10.31 5.89 5.20

  Elevated (another agent's concurrent work, per `git status --porcelain`
  showing `#0433`'s in-flight files) but not treated as signal for anything
  timing-related, per `CLAUDE.md` §5 — no timing-sensitive assertion is made
  anywhere in this pass.

      ISSUE=0466 scripts/check.sh go ./internal/db/... ./internal/handlers/...

      ok  	github.com/brennanMKE/OpenCircuitSF/internal/db        0.549s
      ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers  6.602s

  Zero skips (skip audit: "(no skips)"); both packages show real elapsed
  time, not `0.00s`. `git status --porcelain` was checked before this run
  and showed `#0433`'s in-flight files (`internal/media/`,
  `internal/handlers/admin_media_upload.go` and its test, plus several
  modified `cmd/opencircuit/*_test.go` and `internal/config/*` files) — none
  of them touched by this pass. Unlike the sibling passes on `#0447`/`#0465`,
  **this run reported no failures at all**, including in `internal/handlers`
  — `#0433`'s files had evidently reached a compiling, passing state by the
  time this scoped run executed. `scripts/check.sh` dropped
  `opencircuit_test_0466` on exit, per its own cleanup.

  **No test in either package reads `docs/deployment.md`** — independently
  byte-scanned both packages' `_test.go` files with a `python3` script (not
  BSD `grep -P`, per `CLAUDE.md` §8) searching for the literal string; zero
  hits in either package. So this Go run confirms only that this issue file
  and the edited docs/READMEs carry no dangling Go-file-plus-line-number
  citation or unresolved path/section reference, and that nothing else in
  the tree regressed — it says nothing about the substance of the
  precondition fix itself, which rests entirely on the read-only `ssh`/`scp`
  reasoning recorded above.

  **Files changed, commit `92e56b3`:** `deploy/certbot/README.md` (+7),
  `docs/deployment.md` (+68/-1), `issues/0435.md` (+18), `issues/0447.md`
  (+33), `issues/0465.md` (+19) — 144 insertions, 1 deletion, confirmed with
  `git show --stat HEAD` against the intended file list before writing this
  entry. Status set `in-progress` here in `issues/0466.md`; only the
  reviewer sets `resolved` (`CLAUDE.md` §9).

- 2026-09-08 — **Review pass (Opus). PASS.** `uptime`: `15:37 up 19 days,
  1:48, load averages: 5.46 6.08 5.43` — within the normal band, so no
  finding here rests on machine load.

  **Criterion 2 — re-derived independently, not read from the Work log.** A
  fresh read-only `ssh -o BatchMode=yes ec2` session:

  - `/opt/opencircuit` → `ef0a58f6426f9d305cebbc4081df9c5634c3f572`,
    `#0274 Update the wiring tests for mountAndServe's new parameter`.
  - `/home/ec2-user/OpenCircuitSF` → `6e7af65e43d75c24f61a6173c5b49241319529aa`,
    "adds placeholder website".
  - `deploy/certbot/`, `deploy/certbot/reload-apache-deploy-hook.sh`,
    `scripts/db/backup-media.sh`, `scripts/db/restore-media.sh` — all four
    **ABSENT** on `/opt/opencircuit`.
  - `deploy/systemd/opencircuit.service` — **PRESENT**, `grep -c
    ReadWritePaths` → `0`, `sha256sum` → `e7f33862…d3a8` against this repo's
    `5df0b896…62c8`. **Stale, not missing**, exactly as the implementer
    corrected it. That distinction is load-bearing and the pass handled it
    correctly: for `#0447` the consuming step dies loudly with "No such file
    or directory", whereas for `#0465` `sudo cp` of an existing stale file
    **succeeds silently** and installs a unit with no `ReadWritePaths=`. The
    §7 precondition says precisely that ("no error, no `ReadWritePaths=`"),
    which is the harder of the two failure modes to describe and the one
    worth getting right.

  **A destination check the criteria did not ask for.** Both `scp` targets
  and the `mkdir -p` must be writable by the SSH user or the instruction
  fails at the copy rather than at the install. Confirmed read-only:
  `id` → `uid=1000(ec2-user)`, and `/opt/opencircuit/deploy`,
  `/opt/opencircuit/deploy/systemd`, and the unit file itself are all
  `ec2-user`-owned and report `-w`. So no `sudo` is needed on the `scp` leg
  and none is written — correct as it stands. Destination paths were checked
  against the source paths the consuming steps then use: `#0447` copies to
  `/opt/opencircuit/deploy/certbot/reload-apache-deploy-hook.sh` (with the
  `mkdir -p` that the absent directory genuinely requires) and step 1 then
  resolves `deploy/certbot/reload-apache-deploy-hook.sh` relative to
  `/opt/opencircuit`, which the amended lead-in now states explicitly ("In
  order (from `/opt/opencircuit`, per the precondition above)"). `#0465`
  copies to `/opt/opencircuit/deploy/systemd/opencircuit.service` and step 2
  reads the same path. Both match.

  **Every documented hash re-derived locally, and all five match.** These are
  the genuinely load-bearing values — a stale hash sends the user chasing a
  copy that actually worked: `5df0b896…62c8` (unit), `6fa010d0…f97ee`
  (certbot hook), and `#0435`'s three: `98b7d45f…724d`, `542aeffd…a5f9`,
  `edf5aa0b…034e8`. All agree with `main` as committed.

  **Criterion 4 — proved mechanically, not by reading.** `git show --numstat
  92e56b3 -- issues/0447.md` → `33	0	issues/0447.md`. **Thirty-three
  insertions, zero deletions** — the amendment is additive by construction,
  so no prior sentence of a `resolved` issue was altered or removed. `Status`
  reads `resolved` and `Closed`/`Commit` are untouched. The added
  `## Correction` section names what changed (a missing precondition on how
  the sequence is run) and what did not (hook type and its reasoning, the two
  flag claims, the scoped `--cert-name` command, the eight-lineage safety
  analysis). Its factual claim was spot-checked: `8fc216d` is indeed the
  commit that added `deploy/certbot/reload-apache-deploy-hook.sh`, so
  `ef0a58f` predating it is correct.

  **Command blocks extracted programmatically and checked, not retyped.** A
  `python3` pass pulled every fenced block out of the committed
  `docs/deployment.md` and `deploy/certbot/README.md` regions and tested each
  for the `#0464` failure mode and its neighbours. Result across all eight
  blocks: **zero tab-indented lines**, **zero heredocs of any kind** (so the
  unterminated-heredoc half-run cannot arise here at all), zero dangling
  line-continuations, and `bash -n` clean on every block carrying a `bash`
  fence. The only indented lines are the second halves of `\` continuations,
  which paste correctly. The two expected-output blocks are unfenced-language
  and contain a hash line each, as intended.

  **Criterion 1 — ordering confirmed by line number, not by impression.** In
  `docs/deployment.md`: §7's precondition opens at line 711 and the
  `sudo cp … /etc/systemd/system/` it protects is at line 759; §9's
  precondition opens at line 1214 and the `sudo install -m 0755 …` it
  protects is at line 1253. In `deploy/certbot/README.md` the precondition is
  at line 55 ahead of the install block at 62. `#0435`'s, pre-existing and
  unchanged, sits in "Bring the on-box checkout current for the media leg"
  ahead of "Install and enable the timer". All four precede their step.

  **Criterion 6 — swept independently rather than taking the report's word.**
  Grepped `docs/*.md` and `deploy/*/README.md` for every command form that
  consumes a repo-relative path on the box, then hashed the survivors against
  the box directly. Of the seven other files any prepared instruction
  references, **six are byte-identical on the box** — `backup.sh`,
  `restore.sh`, `pull-backups.sh`, `backup-alert.sh`,
  `opencircuit-backup.timer`, `opencircuit-backup-alert.service` — and the
  seventh, `opencircuit-backup.service` (`f57bcf50…` on the box vs
  `edf5aa0b…` here), is exactly the stale file `#0435` already identifies and
  already covers with its own `scp` line. So the sweep reproduces `#0435`'s
  claim by measurement and finds **no further instance** of the defect. The
  implementer's `#0417`/`#0464`/`#0429` triage agrees with this from the
  other direction. Nothing to file.

  **On `scp` for all three — endorsed.** The reasoning is sound and, unusually
  for this kind of trade, it was tested rather than assumed: the pass ran
  `git ls-remote` and established the box *can* reach GitHub, which is what
  makes `git pull` a real alternative to reject rather than a strawman. The
  rejection stands on its own terms — none of the three changes needs a
  rebuilt binary or SPA, and a pull would land several hundred unreleased
  commits plus a `go build`/`npm run build` on a 418 MB host to move one or
  three small files. Deferring the pull to the next real redeploy (`#0404`)
  keeps the blast radius of each instruction proportional to what it is
  actually asking for. Consistency across all three is a bonus, not the
  argument.

  **On "several hundred commits" — the right call, and now demonstrated.**
  The issue was filed at 711, the implementer re-derived 716 hours later, and
  this review measures `git rev-list --count ef0a58f..HEAD` → **719**. Three
  values in under a day for one unchanged underlying fact. A precise count
  buys nothing here — no step branches on it — while a stale one invites the
  next reader to "correct" it, which is `#0431`'s lineage all over again.
  Prose that cannot go stale is the better artifact. Noted without action:
  three pre-existing `711`s survive in `deploy/systemd/README.md:159`,
  `issues/0435.md:110` and `docs/deployment.md:2428`. Criterion 3 explicitly
  forbade silently rewriting `#0435`'s recorded text, they are dated
  measurements in historical context, and no command depends on them — so
  leaving them is correct, not an oversight.

  **Criterion 5 — nothing was run on the box by this review either.** Every
  command was one of `git rev-parse`, `git log -1`, `ls`/`[ -e ]`/`[ -w ]`,
  `grep -c`, `id`, and `sha256sum`. No write, no copy, no service action, no
  package operation. No read was refused; the only stderr was the local SSH
  config's `bind [127.0.0.1]:5432: Address already in use` port-forward
  warning, which is another process holding the local end of a tunnel and
  affects nothing measured here.

  **Verification.**

      ISSUE=0466r scripts/check.sh go ./internal/db/... ./internal/handlers/...

      ok  	github.com/brennanMKE/OpenCircuitSF/internal/db        0.597s
      ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers  6.172s

  `VERIFICATION PASSED`, skip audit `(no skips)`, `gofmt -l` clean, and both
  DB-backed packages report real elapsed time rather than `0.00s`. **No test
  failed**, in either package, so no failure named a file this pass did or
  did not touch; `git status --porcelain` was empty before the run (`#0433`
  had landed at `a41402e`). `scripts/check.sh` dropped
  `opencircuit_test_0466r` on exit.

  **No test in either package reads `docs/deployment.md`.** Stated plainly:
  this run proves the citation and doc-comment guards are satisfied by the
  edited issue files, docs, and READMEs — including these notes, since
  `#0446` brought `## Work log` into scope — and that nothing else in the
  tree regressed. It proves nothing about the substance of the precondition
  fix, which rests entirely on the read-only box measurements recorded above.

  Re-ran after writing these notes, since the prose guards only scan
  `open`/`in-progress` issues.

  No new issues filed (`CLAUDE.md` §9); nothing found worth filing. `#0447`
  left `resolved` and otherwise untouched; `#0435` and `#0465` left
  `in-progress` as their own pipelines require.

- 2026-09-09 — **Review pass (Opus). PASS — status set `resolved`.**

  **Criterion 2 — re-derived independently, not read from the entry above.**
  One read-only `ssh -o BatchMode=yes ec2` session; every command in it is
  disclosed below, and all of them are reads:

      whoami; id
      git -C /opt/opencircuit rev-parse HEAD
      git -C /opt/opencircuit log -1 --format='%h %s'
      ls -ld /opt /opt/opencircuit /opt/opencircuit/deploy \
             /opt/opencircuit/deploy/systemd /opt/opencircuit/scripts \
             /opt/opencircuit/scripts/db /opt/opencircuit/deploy/certbot
      ls -l  /opt/opencircuit/scripts/db/backup-media.sh \
             /opt/opencircuit/scripts/db/restore-media.sh \
             /opt/opencircuit/deploy/systemd/opencircuit.service
      grep -n ReadWritePaths /opt/opencircuit/deploy/systemd/opencircuit.service
      sha256sum /opt/opencircuit/deploy/systemd/opencircuit.service

  Nothing was written, copied, created, installed, started, stopped or
  reloaded; no `mkdir`, no `scp`, no `systemctl` verb of any kind (not even
  the implementer's `systemctl show`, which was unnecessary once the unit
  file itself could be grepped). Measured values:

  | Fact | Measured this pass |
  |---|---|
  | `/opt/opencircuit` HEAD | `ef0a58f6426f9d305cebbc4081df9c5634c3f572` — `#0274 Update the wiring tests for mountAndServe's new parameter` |
  | `/opt/opencircuit/deploy/certbot/` | **absent** — `No such file or directory` |
  | `scripts/db/backup-media.sh`, `restore-media.sh` | **absent**, both |
  | `deploy/systemd/opencircuit.service` | **present** (2899 bytes), `grep ReadWritePaths` exits 1 — stale, not absent, exactly as the implementation pass characterised it |
  | its SHA-256 on the box | `e7f33862572c39880e83a70cd553fa8c8e25359ba8f72e579ef9430c18e2d3a8` — differs from the repo's `5df0b896…`, confirming the two are genuinely different files |

  **Ownership — the one thing neither the filing nor the implementation pass
  checked, and the thing most likely to break these `scp` commands.**
  `/opt/opencircuit` is `ec2-user:ec2-user 755`, and `deploy/`,
  `deploy/systemd/`, `scripts/`, `scripts/db/` are all `ec2-user:ec2-user
  775`. The ssh login is `ec2-user` (uid 1000). So a bare
  `scp … ec2:/opt/opencircuit/…` **does** have write permission, and
  `ssh ec2 mkdir -p /opt/opencircuit/deploy/certbot` succeeds without
  `sudo` — no `sudo`-over-`scp` dance is needed. Recorded here because the
  root-owned-`/opt` assumption is the obvious objection to raise against
  these instructions, and it is wrong for this box.

  **The commit distance has drifted again, as the filing predicted.** Filed
  as 711, implementation pass measured 716, this pass measures
  `git rev-list --count ef0a58f..HEAD` → **719**. This is exactly why the
  implementation pass's own new prose says "several hundred commits" instead
  of a number, and that judgement is endorsed.

  **Criterion 1 — read the edited text in place, not the quotations of it.**
  All three preconditions appear before the step that consumes them:

  - `#0447`, `docs/deployment.md` §9 (lines ~1216–1246): precondition sits
    between the hook rationale and the "**In order**" block, names
    `ssh ec2 mkdir -p …` + `scp …` explicitly, and the "In order" line now
    reads "(from `/opt/opencircuit`, per the precondition above)" so step 1's
    relative path resolves. `deploy/certbot/README.md` carries a matching
    pointer ahead of its own `sudo install` block.
  - `#0465`, `docs/deployment.md` §7 (lines ~711–746): precondition sits
    between "What may be approved" and the numbered `chgrp`/`chmod`/`cp`
    block, and correctly identifies the failure as a *silent* one — the old
    unit installs with no error.
  - `#0435`, `deploy/systemd/README.md` "Bring the on-box checkout current
    for the media leg" (line 155), ahead of "Install and enable the timer"
    (line 212). Pre-existing and unchanged, verified still correctly ordered.

  **The `scp` commands were checked as commands, not as prose.** Source
  paths exist in this repo; destination directories exist on the box (except
  `deploy/certbot/`, which the instruction creates first); destinations are
  writable by `ec2-user` per the ownership table above. Both expected
  SHA-256 values were recomputed here and match byte-for-byte, from the
  working tree **and** from `git show HEAD:` — so they are the committed
  bytes, not a dirty local file:

      5df0b896983a68cd5b7c99f958afe78ee53806b06fee24068e647a3c20f862c8  deploy/systemd/opencircuit.service
      6fa010d0f74e3d87acc67bbca65fe08295d12270cca6f5961346f685f67f97ee  deploy/certbot/reload-apache-deploy-hook.sh

  `8fc216d` was checked and does resolve — it is `#0447 Prepare a certbot
  deploy hook to reload Apache after renewal`, the commit that adds the hook
  script, so §9's "predates … by every commit since `8fc216d`" is accurate.

  **Criterion 3 — accepted.** The `scp`-per-instruction reasoning is sound
  and is stated per instruction rather than once generically. Endorsed
  independently: none of the three changes needs a rebuilt binary or SPA, so
  `git pull` would buy nothing and cost a `go build`/`npm run build` on a
  418 MB host plus 719 commits of unreleased work landing on production in
  one step. `#0435`'s prior choice was re-examined rather than assumed.

  **Criterion 4 — verified.** `issues/0447.md` gains only a trailing
  `## Correction (#0466, 2026-09-08)` section (+33 lines, no deletions —
  confirmed with `git diff 92e56b3^ 92e56b3 -- issues/0447.md`); its
  `Status` row still reads `resolved`. The section changes only how the
  sequence is run and says so explicitly, listing the decisions it does not
  disturb (hook type and its reason, the two flag claims, the scoped
  `--cert-name` command, the eight-lineage safety analysis).

  **Criterion 5 — upheld by this pass too.** See the disclosed command list
  above; all reads.

  **Criterion 6 — swept independently, and one place the implementation
  pass did not name was checked.** Grepped every `*.md` for the
  `scp …ec2:` / `sudo install -m` / `sudo cp deploy/…` shape, and separately
  for `prepared, not yet applied` / `pending approval` / `approval-ready` /
  `not yet applied`. Results:

  - `docs/deployment.md` lines 499, 601, 879, 2508 — the original
    first-install walkthrough (§4, §7, §8) and the Redeploy procedure. Not
    the defect shape: the walkthrough was executed on 2026-08-25 to create
    this checkout, and the Redeploy procedure runs `git pull` as its own
    first step.
  - `deploy/systemd/README.md` line 30 (`## Install`) — same, the original
    install procedure.
  - `deploy/systemd/README.md` line 59, `## Workshop media write access
    (#0465) — prepared, not yet applied`. **The implementation pass's sweep
    did not name this one**, and it is the most plausible place for a fourth
    instance. Checked: it deliberately carries no command sequence of its
    own and defers to `docs/deployment.md` §7 for the approval-ready
    sequence, which is the section that now has the precondition. Correct as
    it stands; no fix needed.
  - `deploy/systemd/README.md` line 212 (`### Install and enable the timer`)
    copies three unit files; `opencircuit-backup.service` is covered by the
    line-155 precondition, and the other two were hash-confirmed
    byte-identical on the box by `#0435`. Covered.
  - `issues/0320.md` and `issues/0429.md` — false positives. `#0320`'s match
    is about migrations "not yet applied"; `#0429`'s two matches are its own
    sweep quoting these same `sudo cp` lines, not a pending instruction.

  Agrees with the implementation pass's finding: **nothing else in the
  tracker has this shape.** No new issues recommended for filing.

  **Two observations, neither bounce-worthy, recorded so they are not
  rediscovered.** (1) The stale figure "711 commits behind" survives at
  `deploy/systemd/README.md:159` and `docs/deployment.md:2428` — both
  `#0435`'s pre-existing text, not re-introduced by this pass, and the true
  figure today is 719. No command depends on the number and its operational
  meaning ("hundreds of commits stale") is unchanged, so this is decorative
  precision rather than a defect; fold it into the next pass that edits
  those subsections, replacing it with the drift-proof phrasing this pass
  already established. (2) `docs/deployment.md` §9 twice wraps
  `deploy/certbot/reload-apache-deploy-hook.sh` across a line break *inside*
  a code span, which renders as a path containing a space. Cosmetic only —
  the copy-pasteable commands are in fenced blocks and are correct.

  **Verification, re-run by this pass rather than trusted:**

      uptime
      13:49  up 98 days, 19:39, 37 users, load averages: 5.49 5.49 4.07

      ISSUE=0466 scripts/check.sh go ./internal/db/... ./internal/handlers/...

      === gofmt -l (#0161 — formatting drift)
      (clean)
      ok  	github.com/brennanMKE/OpenCircuitSF/internal/db        0.655s
      ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers  6.334s
      === skip audit
      (no skips)
      VERIFICATION PASSED   [exit 0]

  Load 5.49 — above this laptop's idle ~4 because of the concurrent agent
  whose `prototypes/` and `FEDERATED-ACCOUNTS.md` files are dirty, but below
  §5's ~8 threshold, and nothing in this pass rests on a timing assertion.
  Both packages report real elapsed time, not `0.00s`. **No test in either
  package reads `docs/deployment.md`, `deploy/certbot/README.md`, or
  `deploy/systemd/README.md`** — this run proves only that the tree still
  builds, vets, formats clean, and carries no unresolved path or section
  citation; it says nothing about the substance of the precondition fix,
  which rests entirely on the read-only box measurements recorded above.
  The implementation pass stated this plainly too, as the issue asked.

  **Resolved.** All six acceptance criteria met.
