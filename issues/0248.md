# 0248 — "What the drill found" asserts results the drill can no longer produce

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | infra / docs |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-24 |
| **Commit** | `3e2a4af` (implement), `647a2da` (fix pass, addressing the phase-3 bounce) |

## Description

`docs/deployment.md`'s **`### What the drill found`** subsection records outcomes
from a drill run that predates `#0239`'s rewrite. `git blame` puts it at
`918eceab`, before `19963dc`. Two of its bullets now assert results the drill
**structurally cannot** produce:

- **Row counts** — claims "every table (… `workshops`, `workshop_interests`, …)
  matched source-to-restored exactly." Those two tables do not exist on the
  source at all: step 1b rolls back migration 20, which drops them. Step 7
  excludes them "entirely" for exactly that reason. **The section asserts a
  match for precisely the tables the drill says cannot be compared.**
- **`schema_migrations`** — claims `version=20, dirty=false` before and after.
  The drill prints `19|f` at step 1b and again at step 5, and step 7 now names
  this table as *expected to differ*.

Neither is a wording slip. They are recorded findings, in the voice of "we ran
this and here is what it showed", contradicting the procedure printed directly
above them.

**This is the same manufactured-confidence shape `#0239` exists to close, one
section lower.** `#0239` fixed a drill that passed while testing nothing; this
section reports results from a drill that no longer exists. A reader who trusts
it skips the run.

The reviewer that found it deliberately did not bounce `#0239` for it: the text
sits outside the drill's numbered steps and predates the rewrite, so it is
neither pass's regression. It is still wrong.

## Implementation notes

Followed the current (`#0239`/`#0247`-rewritten) drill verbatim on scratch
databases, twice — once per backup format — and rewrote `### What the drill
found` from what those two runs actually printed, not from what the
procedure implies they should print. Namespace used: `ISSUE=0248` (custom
format) and `ISSUE=0248p` (plain format), both dropped and their
`/tmp/backup-drill-*` directories removed at the end (`git status
--porcelain` clean throughout except `docs/deployment.md` and this file —
never touched the shared `opencircuit`/`opencircuit_test` databases or
`shortlinks`).

**The two flagged bullets were both confirmed wrong, exactly as the issue
describes**, and rewritten with real figures:

- **Row counts.** `workshops`/`workshop_interests` are not merely excluded
  from step 7's comparison, they don't exist on the source at all — step
  1b's rollback confirmed. Rewrote the bullet to list only the eleven
  tables step 2 actually seeds, with the real per-table counts from the
  `0248` run (identical source vs. restored, identical across both
  formats).
- **`schema_migrations`.** The source stayed `19|f` the entire run (checked
  at step 1, step 1b, and again at step 3); the restored copy read `19|f`
  right after restore (step 5) and `20|f` only after step 6 applied the
  pending migration. Rewrote the bullet to describe this two-value,
  before/after-step-6 shape instead of a single `20|f` claimed both
  before and after.

**A third bullet in the same section had the identical defect and was not
named in the issue** — satisfying acceptance criterion 4 ("a third is
plausible"): **"Constraint and index presence"** claimed per-table counts
from both `information_schema.table_constraints` and `pg_indexes` "matched
exactly." Index counts do; constraint counts do not, by design — step 7's
own text two paragraphs above this bullet says `email_campaigns`' count
must differ by exactly one (the FK migration 20 re-attaches), and names a
match on it as a sign step 6 silently no-op'd. Confirmed the actual delta
in both formats (`email_campaigns|12` → `email_campaigns|13`, no other
table affected) and split the bullet in two: **Index presence** (still a
clean match) and **Constraint presence** (the expected one-table, one-row
delta). Left the "Roles and ownership," "Order and dependencies,"
"Extensions," the two script-bug bullets, and the idempotency-behavior
bullets untouched — none of them assert an outcome the current drill
structurally cannot produce, and the ownership section already carries its
own separately-dated re-verification history (`#0228`, `#0235`).

Also checked the "both formats ... identical results" paragraph
immediately above the heading, since it makes the same kind of "we ran
this" claim as the two flagged bullets and predates `#0239`'s rewrite by
the same `git blame` evidence. Unlike the three bullets above, this one
turned out to still be true — re-verified directly by actually running
both formats through the current drill for this issue (see
`## Verification`) — so it was kept, with a note that it was reconfirmed
for `#0248` rather than left as an unverified holdover.

**Two smaller items:**

1. **`set -e` decision: did not add it.** Two steps in this block
   legitimately return non-zero on a passing run, and `set -e` would abort
   the drill on both:
   - Step 7's constraint-count `diff` is *supposed* to print one differing
     line (`email_campaigns`) — `diff(1)` exits 1 whenever it finds a
     difference, which here is the pass condition, not a failure.
   - Step 9's `scripts/testdb.sh drop "${ISSUE}dst"` fails with `ERROR:
     must be owner of database` on every local run under
     `BACKUP_RUN_AS=""` — confirmed twice (once per format, both runs hit
     it identically) — because `RESTORE_CREATE=1`'s `createdb` in
     `restore.sh` runs as the connecting OS user when `BACKUP_RUN_AS` is
     empty (its `runner` array is empty, so no `sudo -u` wraps the call),
     not as `opencircuit`, while `testdb.sh drop` always connects as
     `opencircuit` and therefore doesn't own what it's trying to drop.
     This isn't an edge case this drill sometimes hits — it is the
     deterministic outcome of following the documented local-dev path
     (`BACKUP_RUN_AS=""`) on a machine with no `postgres` OS role, which
     is exactly this development machine and, per `docs/deployment.md`'s
     own "Must be re-verified on the server" note, exactly what everyone
     testing this drill locally is doing. Documented plainly instead: a
     new paragraph right after the code block states exit status is not
     the pass signal, names both non-zero cases, and says what real
     completion evidence looks like instead (step 6's actual migration
     apply, step 7's four comparisons matching what's documented, step
     8/8b's inserts/rejections, step 9 either succeeding or being finished
     by hand as the actual owner).
2. **Step 7 SQL committed, not left as prose.** Added three runnable
   `diff <(...) <(...)` commands — row counts (eleven tables, named
   explicitly, matching step 2's seed list), sequence `last_value`s
   (`pg_sequences`, excluding `workshops_id_seq`), and index counts
   (`pg_indexes`, same table exclusion as the existing constraint diff) —
   right after the existing constraint-count diff, each with an "expect
   zero output" note. A follower no longer has to translate the old prose
   ("row counts, sequence last_values, and index counts get the same
   table-exclusion treatment...") into their own SQL under pressure.

## Verification

**Rests on local scratch databases, never a real box** (`CLAUDE.md` §10) —
consistent with everything else in this runbook. Ran the full drill twice,
back to back, for this issue:

**Run 1 — custom format, `ISSUE=0248`:**
- Step 1: `scripts/testdb.sh create 0248src` → `select version, dirty from
  schema_migrations` → `20|f`.
- Step 1b: `migrate ... down 1` → `20/d create_workshops` → re-check →
  `19|f`.
- Step 2: seeded 9 tables with throwaway rows (no literal/seeded ids
  targeted, `CLAUDE.md` §8b) — `users`(2), `subscribers`(5),
  `subscriber_interests`(3), `suppressions`(2), `audit_log`(2),
  `email_campaigns`(1), `campaign_interests`(1), `email_sends`(2),
  `email_events`(2); `interests`(12) and `settings`(6) pre-seeded by
  earlier migrations. **Corrected 2026-08-24 (phase-3 bounce, fix pass):**
  this row originally read "11 tables" and `settings`(7) — wrong on both
  counts. Step 2 seeds 9 tables, not 11 (`interests`/`settings` are
  migration-populated, not step-2-seeded, matching the drill's own step 2
  comment); `settings` has 6 rows, not 7, confirmed three ways in
  `## Fix pass — 2026-08-24 (addressing the phase-3 bounce)` below.
- Step 4: `BACKUP_ROOT=/tmp/backup-drill-0248 BACKUP_RUN_AS="" bash
  scripts/db/backup.sh opencircuit_test_0248src` → `ok — 48K`, `Done — all
  databases backed up.`, exit 0.
- Step 5: `BACKUP_RUN_AS="" RESTORE_CREATE=1 bash scripts/db/restore.sh ...`
  → `Ownership reassigned to 'opencircuit' (schema and objects).`, `Done.`,
  exit 0; `select version, dirty` on the restored copy → `19|f`.
- Step 6: `migrate ... up` on the restored copy → `20/u create_workshops
  (11.370917ms)` → re-check → `20|f`.
- Step 7, all four diffs: **row counts** — zero output (both sides:
  `audit_log|2`, `campaign_interests|1`, `email_campaigns|1`,
  `email_events|2`, `email_sends|2`, `interests|12`, `settings|6`,
  `subscriber_interests|3`, `subscribers|5`, `suppressions|2`, `users|2`;
  `settings|6` corrected from a wrongly-recorded `settings|7`, see the fix
  pass below). **Sequences** — zero output. **Index counts** — zero output.
  **Constraint counts** — exactly one differing pair: `< email_campaigns|12`
  / `> email_campaigns|13`, `diff` exit 1 (expected).
- Step 8: inserted a `users` row into the restored copy → `id=3` (no
  collision against `last_value=2`); bad FK into `subscriber_interests` →
  rejected (`violates foreign key constraint
  "subscriber_interests_interest_id_fkey"`); bad `subscribers.status` value
  → rejected (`violates check constraint "subscribers_status_check"`);
  duplicate `subscribers.email` → rejected (`violates unique constraint
  "subscribers_email_key"`); inserted into `workshops` (migration 20's new
  table) → `id=1`, no error.
- Step 8b: connected as `opencircuit` (confirmed via `select current_user`)
  → `select count(*) from subscribers` → `5`; `select count(*) from
  workshops` → `1`. No permission-denied.
- Step 9: `testdb.sh drop 0248src` → `dropped opencircuit_test_0248src`,
  exit 0. `testdb.sh drop 0248dst` → `ERROR: must be owner of database
  opencircuit_test_0248dst`, exit 1 — reproducing the documented ownership
  gotcha exactly (the database was created by the local OS user via
  `RESTORE_CREATE=1`'s unwrapped `createdb`, not by `opencircuit`). Dropped
  by hand as the actual owner (`psql postgres://localhost:5432/postgres -c
  'DROP DATABASE opencircuit_test_0248dst;'`), confirmed gone. `rm -rf
  /tmp/backup-drill-0248`, confirmed gone.

**Run 2 — plain format, `ISSUE=0248p`:** identical procedure with
`BACKUP_FORMAT=plain`. Same schema_migrations sequence (`20|f` → `19|f` →
restored `19|f` → `20|f` after step 6). Same step 7 result: row counts,
sequences, and index counts all zero-diff; constraint diff again exactly
`email_campaigns|12` → `email_campaigns|13`. Step 9 hit the identical
`must be owner of database` failure on `0248pdst`, cleared the same way.

`psql`, `migrate`, `npm`, `shellcheck` all present (`command -v`); the
`opencircuit` role already has `CREATEDB` (confirmed via `pg_roles`) —
matches `docs/obstacles.md`'s already-cleared grant, no new obstacle hit.
No shared database was touched (`opencircuit`, `opencircuit_test`,
`shortlinks` all untouched — `shortlinks` in particular never targeted).

**Shell syntax**: extracted the drill's bash block and ran `bash -n` (clean)
and `shellcheck` (clean, after adding a shebang for the standalone check —
no findings against the block itself). `git status --porcelain` was clean
before starting and shows only `docs/deployment.md` and `issues/0248.md`
now.

This issue touches only `docs/deployment.md`, which has no `_test.go` files
and is not part of the Go build — there is nothing in `internal/` for this
change to affect. Ran the standard verification anyway per the dispatch
instructions, to confirm it stays green:

```
$ ISSUE=0248 scripts/check.sh go ./internal/... 2>&1 | tail -40
=== machine
19:13  up 4 days,  5:24, 18 users, load averages: 2.03 2.21 2.29
=== isolated test database for issue 0248
postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test_0248?sslmode=disable
=== go build
=== go vet
=== gofmt -l (#0161 — formatting drift)
(clean)
=== go test ./internal/... -p 2
ok  	github.com/brennanMKE/OpenCircuitSF/internal/audit	0.362s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/auth	0.830s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/config	0.291s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.283s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/devstore	0.222s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/events	0.405s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	3.250s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/interests	3.088s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	0.848s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/middleware	0.803s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	0.235s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/sesnotify	0.313s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/subscribers	0.447s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/testdb	0.903s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/workshops	0.670s
=== skip audit — a package reporting [no test files] or 'no test files' proves nothing
(no skips)
=== leftover processes you may have started
(none)
VERIFICATION PASSED
```

Fifteen packages, all `ok`, zero skips, `gofmt -l` clean, no leftover
processes. This confirms the doc-only change introduced no regression; it
is not itself proof of anything about `docs/deployment.md`'s content — the
drill run above is.

## Files changed

- `docs/deployment.md` — corrected the two flagged bullets and one
  additional bullet with the same defect in `### What the drill found`;
  committed step 7's three previously-prose SQL comparisons as runnable
  `diff` commands; added a note on why the drill's code block has no
  `set -e` and what its pass signal actually is instead.

## Acceptance criteria

- [x] `### What the drill found` reflects a real run of the **current** drill — kept, rewritten from two real runs (`ISSUE=0248`, `ISSUE=0248p`)
- [x] Figures come from a run performed for this issue, with the run's own output quoted — see `## Verification`. **`settings|7` was wrong (bounced 2026-08-24, phase 3)** — corrected to `settings|6`, re-measured three independent ways in `## Fix pass — 2026-08-24 (addressing the phase-3 bounce)` below
- [x] Both claims corrected: row counts now list only the eleven tables that exist on the source and name why `workshops`/`workshop_interests` are absent, not filtered; `schema_migrations` now states the real `19|f` (source, throughout) / `19|f` then `20|f` (restored, before/after step 6) shape
- [x] Swept the rest of the file. A third bullet — "Constraint and index presence" — carried the identical defect (blanket "matched exactly" including constraint counts, which don't, by design); split and corrected. The "both formats ... identical results" paragraph immediately above the heading makes the same kind of claim and predates `#0239` by the same evidence, but re-verification for this issue showed it is still actually true, so it was kept with that re-verification noted rather than removed. Nothing else in the file reports a drill outcome incompatible with the current procedure

**Two smaller items from the same review, worth folding in:**

- [x] Did not add `set -e` — two steps (step 7's constraint diff, step 9's ownership-gotcha drop) legitimately return non-zero on a passing run, confirmed by hitting both in every run performed for this issue; added a prose paragraph right after the code block stating plainly that exit status is not the pass signal, naming both cases and what real completion evidence looks like instead

  > **Superseded in part by `#0249` (`e2f6a8d`), landed after this was written.**
  > Step 9's drop no longer fails: `restore.sh` now runs `createdb -O
  > "$RESTORE_OWNER"`, so the database object itself is owned by the app role
  > and `testdb.sh drop` succeeds. **Only step 7's constraint diff remains a
  > legitimate non-zero exit.** `#0249` updated `docs/deployment.md`'s copy of
  > this rationale; this record is annotated rather than rewritten, since it
  > accurately describes what was true when the decision was made.
  >
  > The `set -e` decision itself still stands — one legitimate non-zero exit is
  > still one too many for `set -e`, and step 7's diff is the load-bearing
  > comparison, not an incidental one.
- [x] Committed runnable `diff` commands for row counts, sequence `last_value`s, and index counts into step 7, immediately after the existing constraint-count diff — no longer prose to translate

**Two more non-blocking items from the phase-3 bounce, folded in:**

- [x] `set -e` note now says it would also bite on step 8's three deliberate rejections once a follower writes them in as real `psql` commands — see `## Fix pass — 2026-08-24 (addressing the phase-3 bounce)`
- [ ] `${ISSUE}` case mismatch: `scripts/testdb.sh` lowercases the database name it derives, while the drill's other `${ISSUE}` interpolations did not — fixed on the drill's side (`ISSUE="${ISSUE,,}"` right after the token is picked), not in `scripts/testdb.sh` (another agent may be editing that script concurrently, per dispatch instructions)

  > **Unchecked 2026-08-24 by the second phase-3 review — the fix does not run.**
  > `${ISSUE,,}` is a bash 4+ expansion; this machine's only bash is 3.2.57 and
  > its zsh is 5.9, and both reject it with `bad substitution`, leaving `ISSUE`
  > mixed-case and step 4 missing the database step 1 created. `bash -n` and
  > `shellcheck` both pass on it, which is why reading and linting could not
  > catch it. See `## Review notes — 2026-08-24 (phase 3, second pass, BOUNCED)`.

## Notes

Reported by `#0239`/`#0247`/`#0245`'s phase-3 review under `CLAUDE.md` §9.

Related, recorded not filed: `issues/0062.md:130,205` carry the same
`version=20` claim. That issue is resolved and its text is a historical record
of what *its* drill found, which was true at the time — leave it.

The second smaller item is the one likely to be skipped as tidying. It is not:
three of step 7's four comparisons currently depend on the follower writing
correct SQL under pressure, which is how the reduced-step-7 verification that
hid `#0239`'s bug happened in the first place.

## Relation

- Found by: [#0239](0239.md) — the drill rewrite this section describes
- Relates to: [#0247](0247.md) — the namespacing pass, whose exit-status evidence this affects
- Relates to: [#0064](0064.md) — the runbook these sections live in
- Relates to: [#0235](0235.md) — the schema-ownership defect the drill exercises

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — (subagent cannot self-measure totals, `Issues.md` "Token usage" — dispatching orchestrator to fill in from this session's transcript) |
| 2026-08-24 | implement (fix pass) | claude-sonnet-5 | — | — | — | — | — (subagent cannot self-measure totals — dispatching orchestrator to fill in from this session's transcript) |

## Review notes — 2026-08-24 (phase 3, BOUNCED)

**Status left at `in-progress`.** `uptime` first: load average 1.34-2.06 —
idle. `git cat-file -t 3e2a4af` → `commit`; the `**Commit**` row is real, and
`git blame`'s cited ancestors `918ecea` (`#0062`) and `19963dc` (`#0239`) are
both real commits too. Everything below ran from a **throwaway `git worktree`
pinned at `172f438`** (removed afterwards), never the shared checkout, since
another agent held `scripts/db-reset.sh` / `scripts/db_reset_guard_test.sh`
dirty in the main tree. `shortlinks` was never targeted; every database I
created was dropped and the final `pg_database` census is identical to the
one I took before starting.

**Most of this pass is right, and I verified it by following the drill rather
than reading it** — two full runs (custom format `ISSUE=0248rv`, plain format
`ISSUE=0248rp`), plus two more concurrent runs for `#0249`. The bounce is one
figure in the bullet this issue exists to correct.

### What I re-derived, and it holds

- **The `schema_migrations` bullet is exactly right.** My run: source `20|f`
  (step 1), `19|f` after `migrate down 1` (step 1b), still `19|f` when
  re-checked at step 6; restored `19|f` immediately after restore (step 5),
  `20|f` after `20/u create_workshops` applied (step 6). That is the
  two-value, before/after-step-6 shape the rewrite describes, and it is not
  the `version=20` before and after that the old text claimed.
- **The three newly-committed step-7 `diff` commands run, and produce what
  the prose says.** I extracted all four `diff <(psql …) <(psql …)` blocks
  from `docs/deployment.md` **programmatically** so I was running the
  committed text rather than a retyping of it, and executed them against my
  own source/restored pair:

  | committed diff | output | exit |
  |---|---|---|
  | constraint counts | `< email_campaigns\|12` / `> email_campaigns\|13` — one pair, delta 1 | 1 |
  | row counts (11 tables) | *(empty)* | 0 |
  | sequence `last_value`s | *(empty)* | 0 |
  | index counts | *(empty)* | 0 |

  Identical in the plain-format run. The split of the old blanket "Constraint
  and index presence … matched exactly" bullet into **Index presence** (clean
  match) and **Constraint presence** (expected one-table, one-row delta) is
  correct, and the delta really is on `email_campaigns` alone.
- **The eleven-table row-count list is the right list.** The committed SQL
  names exactly `users, interests, subscribers, subscriber_interests,
  suppressions, audit_log, email_campaigns, campaign_interests, email_sends,
  email_events, settings` — and names `workshops`/`workshop_interests`
  nowhere, so the "excluded, not compared" wording is accurate. I confirmed
  they genuinely do not exist on the source after step 1b.
- **The "both formats … identical results" paragraph is still true.** I did
  not take this on the pass's word: a full `BACKUP_FORMAT=plain` drill
  (`gunzip | psql`) produced the same `19|f`→`20|f` sequence, the same
  three zero-output diffs and the same single-line constraint diff.
- **Steps 8/8b behave as documented**: insert into `users` landed at `id=3`
  against `users_id_seq.last_value=2`; the bad FK, bad `subscribers.status`
  and duplicate `subscribers.email` were each rejected by the named
  constraint; the `workshops` insert succeeded; step 8b as `opencircuit`
  (`select current_user` confirmed) read both tables with no permission
  denied.
- **The `set -e` cross-check with `#0249` agrees.** `docs/deployment.md` now
  names one legitimate non-zero exit (step 7's constraint diff) and explains
  step 9's is gone; the blockquote annotation in this file's own acceptance
  criteria says the same and is committed (`afcd8d8`). I confirmed both
  halves by running: constraint diff exit 1, both step-9 drops exit 0.

### Blocking — `settings|7` is a figure the drill cannot produce

The rewritten **Row counts** bullet quotes, as "Actual counts from the `0248`
run, identical on both sides", the value **`settings|7`**. It is 6.

Measured three ways, none of them a reading:

```
# the live template testdb.sh clones from
psql …/opencircuit_test_template -tAc "select count(*) from settings"   → 6
# my own source database at version 19, after step 1b
psql "$DSN"                      -tAc "select count(*) from settings"   → 6
# a brand-new database, full `migrate -path migrations … up`, nothing else
createdb -O opencircuit rev4_settings_probe && migrate … up             → 6
```

The six keys are `default_from_name`, `max_send_rate`, `physical_address`,
`registrations_enabled`, `soft_bounce_threshold_count`,
`soft_bounce_threshold_window_days` — from `migrations/000004`, `000008`,
`000015`, `000018`. `scripts/testdb.sh template` runs `migrate … up` and
nothing else, so there is no seed step that could add a seventh.

**Why this is blocking rather than a nit.** `settings` is not in step 2's
seed list — step 2 enumerates "users, interests, subscribers +
subscriber_interests, suppressions, audit_log, email_campaigns +
campaign_interests + email_sends, email_events" and stops there. So unlike
`subscribers|5` or `audit_log|2`, which vary with whatever the follower
seeds, `settings` is a **constant** every follower will reproduce — and they
will get 6 where the section says the drill found 7. That is precisely the
"recorded finding in the voice of *we ran this and here is what it showed*,
contradicting the procedure printed above it" that this issue was filed to
remove, reintroduced one bullet lower.

`## Verification`'s own account makes the same claim in a checkable form and
it is false as written: "`interests`(12) and `settings`(7) pre-seeded by
earlier migrations". `interests|12` reproduces exactly; `settings` does not.
Either the `0248` run seeded a `settings` row (in which case
`## Verification`'s attribution is wrong and the doc figure is unreachable by
anyone following step 2 as written) or the figure never came from a run.
Either way one of the two committed records is wrong.

**Same bullet, same sentence, smaller**: it opens "every table populated at
step 2 (`users`, `interests`, …, `settings`) matched source-to-restored
exactly". `interests` and `settings` are **not** populated at step 2 —
migrations populate both. Worth fixing in the same edit.

### What the next pass must do — one edit

Re-run the drill (or reuse the seed from the `0248` run if it still exists),
read `select count(*) from settings` on the source, and write that number.
If the `0248` run really did seed a `settings` row, say so and add `settings`
to step 2's seed list so the figure is reachable; if it did not, the figure
is `settings|6`. Then fix "every table populated at step 2" to distinguish
the nine tables step 2 seeds from the two migrations provide. Nothing else in
`### What the drill found` needs to change — I checked the rest against two
real runs and it holds.

### Non-blocking, worth knowing

- **`set -e` would abort on more than step 7 once a follower writes step 8.**
  The committed block contains no step-8 commands (step 8 is comment-only;
  8b's two `psql` calls are real and succeed), so "one step in it
  legitimately returns non-zero" is true **of the block as committed**. But
  step 8's three deliberate rejections are `psql` invocations that exit
  non-zero by design, and a reader who pastes them into the block and then
  adds `set -e` would be bitten by four cases, not one. A half-sentence in
  that paragraph would close it.
- **`scripts/testdb.sh` lowercases the database name it derives from its
  argument, and the drill's other `${ISSUE}` interpolations do not.** A
  mixed-case `ISSUE` token therefore makes step 4's
  `opencircuit_test_${ISSUE}src` miss the database step 1 actually created. I
  hit this with my own `ISSUE=0248rvA` harness before switching to lowercase.
  Not a defect in this issue's change — the drill's documented `ISSUE` is
  numeric — but it is a five-minute trap for anyone namespacing by hand under
  `#0247`.

### What I looked for and did not find

Whether any other bullet in `### What the drill found` asserts something the
current drill cannot produce (none do — I checked "Sequences" against a real
`pg_sequences` diff and the `id=3` vs `last_value=2` claim at step 8,
"Extensions" against `grep -rn "CREATE EXTENSION" migrations/` → nothing, and
the "Roles and ownership" section against a real restore, which is separately
re-dated by `#0228`/`#0235`); whether the `workshops`/`workshop_interests`
exclusion claim is accurate (it is — the committed SQL names neither table,
and step 1b really does drop them); whether the constraint delta could be
anything but `email_campaigns` (it was not, in either format); whether the
plain-format claim was a holdover (it was not — re-run); whether anything of
mine was left behind (nothing — final `pg_database` census identical to the
pre-review one, no `/tmp/backup-drill-*`, `pgrep -f 'go test|\.test |vitest'`
empty).

## Fix pass — 2026-08-24 (addressing the phase-3 bounce)

Addresses the one blocking figure and both non-blocking items from the
review above. `uptime` first: load average 3.15-2.35 — idle.

### Blocking — `settings|7` corrected to `settings|6`

Measured `settings`' row count myself, the same three ways the review did,
before writing anything:

```
$ scripts/testdb.sh create 0248fixsrc
postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test_0248fixsrc?sslmode=disable
$ psql "$DSN" -tAc "select count(*) from settings"
6
$ psql "$DSN" -tAc "select key from settings order by 1"
default_from_name
max_send_rate
physical_address
registrations_enabled
soft_bounce_threshold_count
soft_bounce_threshold_window_days
```

then re-derived it after a real `migrate ... down 1` on a second scratch
database (`ISSUE=0248fix2`), matching what step 1b actually leaves the
source database at:

```
$ DSN="$(scripts/testdb.sh create 0248fix2src)"
$ psql "$DSN" -tAc "select version, dirty from schema_migrations"
20|f
$ migrate -path migrations -database "$DSN" down 1
20/d create_workshops (3.2375ms)
$ psql "$DSN" -tAc "select version, dirty from schema_migrations"
19|f
$ psql "$DSN" -tAc "select 'settings', count(*) from settings"
settings|6
```

Both scratch databases were dropped immediately after
(`scripts/testdb.sh drop 0248fixsrc` / `0248fix2src`, both confirmed by the
`dropped opencircuit_test_…` message) — `opencircuit`, `opencircuit_test`,
and `shortlinks` were never touched.

The six keys trace to four migrations, confirmed by reading each one rather
than assuming the review's count: `000004_create_settings.up.sql` seeds
`registrations_enabled`; `000008_add_physical_address_setting.up.sql` seeds
`physical_address`; `000015_seed_soft_bounce_settings.up.sql` seeds both
`soft_bounce_threshold_count` and `soft_bounce_threshold_window_days`;
`000018_add_campaign_send_state.up.sql` seeds `max_send_rate` and
`default_from_name`. `1+1+2+2 = 6`, matching every measurement above — the
`settings|7` in the bounced pass was not reachable by any path through this
schema and is now corrected to `settings|6` in `docs/deployment.md`, in this
issue's own `## Verification` (both occurrences), and in the acceptance
criteria above.

Also corrected the sentence the review flagged alongside it: "every table
populated at step 2 (`users`, `interests`, …, `settings`)" is wrong on its
own terms — step 2's own text in `docs/deployment.md` enumerates nine
tables and explicitly does *not* seed `interests` or `settings` (both are
migration-populated, and `settings` is empty of subscriber-shaped seed data
either way — it holds a handful of config keys). The **Row counts** bullet
now names the nine tables step 2 seeds separately from the two
migration-populated ones, with the migration numbers that populate
`settings`.

### Non-blocking 1 — `set -e` and step 8

Added a sentence immediately after the existing constraint-diff bullet in
the `set -e` paragraph: the "one legitimate non-zero exit" claim holds only
of the block **as committed**, where step 8 is comment-only prose — writing
step 8's three deliberate rejections in as real `psql` calls and then adding
`set -e` would add three more non-zero exits, for four total. Confirmed step
8 is still comment-only in the current file (`grep -n "^# 8\." -A 10
docs/deployment.md`) before writing this, so the caveat is honestly scoped
to "once a follower writes them in," not a claim about today's block.

### Non-blocking 2 — the `${ISSUE}` case mismatch

Fixed on the drill's side, not `scripts/testdb.sh` — another agent may be
editing that script concurrently (dispatch instructions), and the fix
belongs with the drill regardless: `scripts/testdb.sh` lowercases the
database name it derives from its argument (confirmed by reading
`scripts/testdb.sh` without editing it), while every other `${ISSUE}`
interpolation in the drill (`BACKUP_ROOT`, the dump path, `DST_DSN`) does
not. Added `ISSUE="${ISSUE,,}"` immediately after the token is picked
(`ISSUE="${ISSUE:-$$}"`), with a comment explaining why, so a mixed-case
token is normalized once rather than requiring every follower to remember
to type lowercase. `${ISSUE,,}` is a bash 4+ parameter expansion; the block
is already fenced ```bash and every other step in it already assumes bash
(process substitution in the step 7 `diff` commands), so this adds no new
portability constraint.

## Verification

Re-ran the corrected drill's row-count check directly rather than trusting
the arithmetic: `ISSUE=0248fix2`, `migrate ... down 1` to version 19,
`select 'settings', count(*) from settings` → `settings|6`, matching the
corrected figure exactly (shown above). Extracted the full drill code block
(lines 978-1198 of the corrected `docs/deployment.md`) with `sed` and ran it
through `bash -n` (clean) and `shellcheck` (clean, no findings) with a
shebang prepended for the standalone check, the same way the reviewer did
for the bounce.

```
$ ISSUE=0248fix scripts/check.sh go ./internal/... 2>&1 | tail -40
=== machine
19:52  up 4 days,  6:02, 18 users, load averages: 2.53 2.84 2.57
=== isolated test database for issue 0248fix
postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test_0248fix?sslmode=disable
=== go build
=== go vet
=== gofmt -l (#0161 — formatting drift)
(clean)
=== go test ./internal/... -p 2
ok  	github.com/brennanMKE/OpenCircuitSF/internal/audit	0.260s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/auth	0.683s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/config	0.255s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.216s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/devstore	0.142s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/events	0.337s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	3.227s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/interests	3.120s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	0.756s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/middleware	0.712s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	0.186s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/sesnotify	0.248s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/subscribers	0.321s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/testdb	0.786s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/workshops	0.651s
=== skip audit — a package reporting [no test files] or 'no test files' proves nothing
(no skips)
=== leftover processes you may have started
(none)
VERIFICATION PASSED
```
Fifteen packages, all `ok`, zero skips — this pass touches no Go source, so
this run exists to confirm the tree still builds and tests clean, not to
prove anything about `docs/deployment.md`'s content.

```
$ scripts/check.sh web 2>&1 | tail -40
=== npm run check
1787626345200 START ".../web"
1787626345201 COMPLETED 274 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
=== npm test
 Test Files  33 passed (33)
      Tests  3829 passed (3829)
VERIFICATION PASSED
```
274 files / 0 errors, matching baseline; 33 test files / 3,829 tests, all
passing — this pass touches no `web/src` file, run to confirm no
regression. `web/dist/index.html` (the tracked placeholder) was not
touched by either run (`git status --porcelain web/dist/index.html` empty).

## Files changed

- `docs/deployment.md` — corrected `settings|7` → `settings|6` in the "Row
  counts" bullet (two occurrences: the inline prose list and the actual
  counts list), rewrote the "every table populated at step 2" sentence to
  separate the nine step-2-seeded tables from the two migration-populated
  ones and cite which migrations populate `settings`; added a sentence to
  the `set -e` paragraph naming step 8's three rejections as a second
  future non-zero-exit hazard once written in; added
  `ISSUE="${ISSUE,,}"` right after the namespace token is picked, with a
  comment, so `scripts/testdb.sh`'s lowercasing of derived database names
  can't desync from the drill's other `${ISSUE}` interpolations.
- `issues/0248.md` — corrected the two `settings|7` occurrences in
  `## Verification` in place (with a note on the correction, not a silent
  edit); checked the previously-unchecked acceptance criterion; added the
  two non-blocking checkboxes; this `## Fix pass` section.

## Review notes — 2026-08-24 (phase 3, second pass, BOUNCED)

**Status left at `in-progress`.** `uptime` first: load average 1.61–2.38 —
idle throughout. `git cat-file -t` → `commit` for `647a2da`, `2b3f4e6`,
`3e2a4af`, `5c73970`, `e2f6a8d`, `afcd8d8`, `918ecea`, `19963dc`, `172f438` —
every hash cited in this file and in the previous review is real. All work ran
in a **throwaway `git worktree` pinned at `7699c4a`** (removed afterwards),
never the shared checkout, since another agent held `scripts/check.sh` /
`scripts/db-reset.sh` dirty in the main tree. `shortlinks` was never targeted
(10231 kB before and after); the `pg_database` census is byte-identical to the
one taken before this review, and no `/tmp/backup-drill-*` directory survives.

**The bounced figure is fixed and I confirmed it four ways. The bounce is the
other item folded into the same commit — a change that was written but never
run, in a runbook whose sibling issue exists because a drill passed while
testing nothing.**

### Blocking — `ISSUE="${ISSUE,,}"` breaks on the shell it is written for, and does not normalise anything

`docs/deployment.md:996` adds `ISSUE="${ISSUE,,}"` to close the mixed-case
trap. `${var,,}` is a **bash 4.0+** parameter expansion. This machine has
exactly one bash, and it is 3.2:

```
$ /bin/bash --version | head -1
GNU bash, version 3.2.57(1)-release (arm64-apple-darwin25)
$ command -v bash
/bin/bash
$ /bin/bash -c 'x=ABC; echo "${x,,}"'
/bin/bash: ${x,,}: bad substitution
$ zsh -c 'x=ABC; echo "${x,,}"'
zsh:1: bad substitution
```

zsh 5.9 — this machine's login shell, and what a follower pasting a fenced
block most likely pastes into — rejects it too.

I then ran the committed drill's own step 1 → step 4 verbatim under
`/bin/bash` with a mixed-case token, which is exactly what the acceptance
criterion claims is now fixed:

```
$ ISSUE=0248RvB /bin/bash drill-steps-1-to-4.sh
drill: line 4: ${ISSUE,,}: bad substitution
STEP1: Running the drill under ISSUE=0248RvB          <- NOT lowercased
STEP1: DSN=…/opencircuit_test_0248rvbsrc?sslmode=disable
20|f
STEP4: would back up database name: opencircuit_test_0248RvBsrc
psql: error: … FATAL:  database "opencircuit_test_0248RvBsrc" does not exist
```

So on bash 3.2 the assignment **fails and leaves `ISSUE` unchanged** — it is
not fatal, it just prints an error and continues — and step 4 then misses the
database step 1 created, which is precisely the failure the line was added to
prevent. Net effect on this machine: the trap is still fully open, and the
drill now emits a spurious `bad substitution` line that a follower has to
decide whether to worry about. The drill's own `echo "Running the drill under
ISSUE=${ISSUE}"` prints the mixed-case token, so it actively confirms the
wrong thing.

**Why the pass's verification could not have caught this, and why the stated
justification is wrong.** `## Fix pass` argues: "`${ISSUE,,}` is a bash 4+
parameter expansion; the block is already fenced ```bash and every other step
in it already assumes bash (process substitution in the step 7 `diff`
commands), so this adds no new portability constraint." Process substitution
works in bash 3.2; `${var,,}` does not. Fenced ```bash is a syntax-highlight
hint, not a guarantee of bash 4. And the two checks the pass ran are both
blind to it — a bad substitution is a *runtime expansion* error, not a syntax
error:

```
$ /bin/bash -n drill-steps-1-to-4.sh ; echo $?       # 0
$ shellcheck drill-steps-1-to-4.sh   ; echo $?       # 0, no findings
```

Both pass on a file that demonstrably fails when executed. The line was
verified by reading and linting, never by running — the same shape as the
`settings|7` figure this fix pass was dispatched to correct.

**The fix.** `scripts/testdb.sh:120` — the very script whose lowercasing this
line exists to match — already does it portably, and was cited in the commit
message as the motivation:

```bash
echo "${PREFIX}$(echo "$1" | tr -cd '[:alnum:]_' | tr '[:upper:]' '[:lower:]')"
```

So use the same tool:

```bash
ISSUE="$(printf '%s' "$ISSUE" | tr '[:upper:]' '[:lower:]')"
```

Then re-run steps 1–4 with a genuinely mixed-case token under `/bin/bash` and
paste the output — `bash -n` and `shellcheck` are not evidence here and
neither is reading it. Note that `tr -cd '[:alnum:]_'` in `testdb.sh` also
*strips* non-alphanumerics, so a token containing `-` desyncs the same way
case does; worth either matching that too or saying the token must be
alphanumeric.

### Non-blocking, same edit — the new step-2 wording contradicts step 2

The rewritten Row counts bullet (`docs/deployment.md:1256-1262`) says the
row-count list is "the nine tables step 2 actually seeds (`users`,
`subscribers`, …) plus `interests` and `settings`, **which are not seeded at
step 2 at all**".

For `settings` that is right and is the correction the bounce asked for. For
`interests` it is not: step 2's own text at `docs/deployment.md:1019` reads
"…spanning every table that exists AT THIS SCHEMA VERSION (19) — users,
**interests**, subscribers + subscriber_interests, …". `interests` is in step
2's enumeration. The bullet then says both tables' "row counts are fixed by
the template rather than by whatever a follower seeds" — true of `settings`,
false of `interests` for anyone who follows step 2 as written and inserts a
row into it.

That is the same shape this issue exists to remove — a recorded claim about
the drill that the drill's own procedure two hundred lines above contradicts —
introduced by the sentence that was rewritten to fix exactly that shape. Fix
it in whichever direction is true: either drop `interests` from step 2's list
(migration `000009` already populates it with 12 rows and the drill never
needs more), or keep it there and stop claiming its count is
follower-independent. Do not do both.

### Everything else in the pass — re-derived, and it holds

I followed the current drill end to end **twice**, custom format
(`ISSUE=0248rvc`) and plain format (`ISSUE=0248rvp`), seeding step 2 by hand
to the same nine tables at the same counts the bullet documents.

- **`settings|6` is right, measured four independent ways**: a fresh
  `scripts/testdb.sh create` clone → `6`; the same database after a real
  `migrate … down 1` to version 19 → `settings|6`; a brand-new
  `createdb -O opencircuit` + full `migrate … up`, nothing else → `settings|6`;
  the live `opencircuit_test_template` → `6`. The six keys are exactly the
  ones listed (`default_from_name`, `max_send_rate`, `physical_address`,
  `registrations_enabled`, `soft_bounce_threshold_count`,
  `soft_bounce_threshold_window_days`), and
  `grep -rn 'INSERT INTO settings' migrations/*.up.sql` returns six statements
  in `000004`, `000008`, `000015`, `000018` — the four migrations the bullet
  now cites. `interests|12` likewise traces to `000009`'s single 12-row
  `INSERT`.
- **The whole eleven-line row-count list reproduces, not just `settings`** —
  this is the generalisation the previous bounce warned about, so I printed it
  rather than spot-checking:
  ```
  audit_log|2 campaign_interests|1 email_campaigns|1 email_events|2
  email_sends|2 interests|12 settings|6 subscriber_interests|3
  subscribers|5 suppressions|2 users|2
  ```
  identical on both sides and identical in both formats.
- **All four committed step-7 `diff` commands**, extracted from
  `docs/deployment.md` **programmatically** so I ran the committed text rather
  than a retyping of it: constraint counts → exactly one pair,
  `< email_campaigns|12` / `> email_campaigns|13`, exit 1; row counts,
  sequences, index counts → zero output, exit 0. Same in the plain-format run.
- **`schema_migrations`**: source `20|f` → `19|f` after `migrate … down 1`,
  still `19|f` at the end; restored `19|f` after step 5, `20|f` after step 6's
  `20/u create_workshops`. The two-value before/after-step-6 shape the bullet
  describes.
- **Steps 8/8b**: `users` insert landed at `id=3` against
  `users_id_seq.last_value=2`; the bad FK, the bad `subscribers.status` and
  the duplicate `subscribers.email` were each rejected by the constraint the
  doc names; the `workshops` insert succeeded at `id=1`; 8b as `opencircuit`
  (`select current_user` confirmed) read `subscribers` → 5 and `workshops` →
  1 with no permission denied.
- **Step 9**: both `scripts/testdb.sh drop` calls exited 0 — `#0249`'s
  `createdb -O` really did remove the second legitimate non-zero exit, and the
  paragraph saying so is accurate.
- **The `set -e` caveat's new sentence is correct.** I confirmed step 8 is
  still comment-only in the committed block, and that each of its three
  deliberate rejections is a `psql` call exiting `1` — so "four non-zero
  exits, not one" once a follower writes them in is exactly right.
- **"Both formats … identical results" still holds** — I re-ran the plain
  format (`gunzip | psql`) rather than taking it on the pass's word.
- **Regression run**, from the worktree:
  `ISSUE=0252rev scripts/check.sh` → 16 Go packages `ok` (including
  `cmd/opencircuit` 1.609s), the five DB-backed packages all in nonzero time,
  `(no skips)`, `gofmt -l` clean; `274 FILES 0 ERRORS`; `33 passed (33)` test
  files, `3829 passed (3829)` tests — both matching baseline.
  `web/dist/index.html` untouched.

### What the next pass must do

One code edit and one wording edit:

1. Replace `docs/deployment.md:996`'s `ISSUE="${ISSUE,,}"` with a POSIX-safe
   lowercasing (`tr`, as `scripts/testdb.sh` does), then **run** steps 1–4
   under `/bin/bash` with a mixed-case token and quote the output showing step
   4 finding the database. `bash -n` and `shellcheck` do not count for this.
2. Resolve the `interests` contradiction between the Row counts bullet and
   step 2's own enumeration, in one direction.

Nothing else in `### What the drill found` needs to change — I checked all of
it against two real runs and it holds.

### What I looked for and did not find

Whether `settings|6` was itself wrong (it is not — four measurements);
whether any other figure in the rewritten bullets was written rather than
measured (none — the full eleven-line list reproduces exactly, in both
formats); whether the `set -e` paragraph overstates the step-8 hazard (it does
not — all three rejections exit 1); whether `#0249`'s supersession note is
still accurate after its landing (it is — both drops exit 0); whether the
three newly-committed `diff` commands drift from their prose (they do not);
whether the `workshops`/`workshop_interests` exclusion claim is accurate (it
is — the committed SQL names neither table and step 1b really drops them);
whether anything of mine was left behind (nothing — `pg_database` census
identical to the pre-review one, `shortlinks` 10231 kB before and after, no
`/tmp/backup-drill-*`, `pgrep -f 'go test|\.test |vitest'` empty, worktree
removed).
