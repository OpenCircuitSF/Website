# 0431 — `docs/deployment.md`'s pre-deploy verification notes were falsified wholesale by the deploy

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | docs / deploy |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-04 |
| **Closed** | 2026-09-05 |
| **Commit** | `4dfb98c`, `1e52ae0`, `5d2c323` |

> 🟢 Resolved 2026-09-05 — the ~15-occurrence sweep of the
> falsified-by-deploy shape stands, and the last defect is closed: the two
> sentences that asserted `SEND_WORKER_ENABLED=false` as production's current
> setting now cite the 2026-09-03 read-only `/etc/opencircuit/config.env`
> reading (`SES_SANDBOX=false`, `SEND_WORKER_ENABLED=true`) recorded in
> `#0415`, `#0057` and `#0423`, and hand the reconciliation to `#0415` rather
> than settling it here. Every `SEND_WORKER_ENABLED` statement left in
> `docs/deployment.md` now agrees, and the statements that merely cite
> `CLAUDE.md` §10 item 2 for the sandbox state were correctly left unhedged.

## Description

`docs/deployment.md` carries a **class** of notes written before the 2026-08-25
deploy, each explaining that something could not be verified because the
infrastructure did not exist. The deploy falsified all of them at once, and they
were never swept.

Confirmed by the orchestrator:

| Where | Says | Reality (`CLAUDE.md` §7) |
|---|---|---|
| Production-facts table | `Currently served \| the static placeholder (`placeholder/`)` | **This project, since 2026-08-25. The static placeholder is gone.** |
| First-admin-login section | "no SES account exists to send the recovery email through yet… and **no live instance**" | SES is live; the instance serves the site |
| TLS section | "**Not run anywhere** — this needs a real, publicly resolvable hostname" | `www.opencircuitsf.com` resolves and serves |
| Backups limitations | "none of those AWS resources exist yet" | the account and instance role exist |

**Found by `#0426`'s review**, which called it *"a class, not an instance"* —
and noted the implementer neither fixed nor mentioned the first-admin-login one,
the only disclosure gap in an otherwise careful pass.

## Root cause

`docs/deployment.md` accreted its "not run / does not exist / cannot be
checked" language across many passes written before 2026-08-25, and each
subsequent correction pass (`#0414`, `#0418`, `#0421`, `#0423`, `#0426`)
fixed only the specific claim it was dispatched to fix, leaving sibling
occurrences of the same shape elsewhere in the ~2,100-line file untouched —
several of them directly contradicted by a correction already sitting a few
lines above or below them in the same section, which nobody had reconciled.

## Fix

Swept the whole file (not a string search) for the shape described in the
issue: any claim that something cannot be checked, does not exist, or has
not been run, premised on the pre-2026-08-25 state. For each genuine hit
outside the Backups section, added a dated `**Corrected `#0431`, ...**`
paragraph ahead of the original text (kept as an "Original note, left for
context" block) that re-derives the current state and explains the
per-occurrence judgement, per the issue's `#0271`/`#0072` framework:

- **Production-facts table** (`Currently served`, `Already on the box`): this
  table is explicitly a current-state record (the issue's own least-ambiguous
  call), so both rows were rewritten outright rather than left with an
  "original note" — updated to say the site is served by `opencircuit.service`
  since 2026-08-25, re-verified against a live request.
- **"SES is not configured yet" section**: genuinely ambiguous — the heading
  and "deliberate, at the user's direction" framing is historical, but "What
  this costs until SES is live" reads as current instruction and is now
  false (SES is live, just sandboxed). Retitled the heading as historical,
  added a correction explaining the current state, and reframed the bullets
  as a historical record of the pre-SES gap. **Corrected by the third review,
  2026-09-05:** this bullet originally recorded that
  `SEND_WORKER_ENABLED=false` is "still today's real setting", which is the
  claim the second review bounced as contradicted by every recorded reading of
  production's `/etc/opencircuit/config.env`, and which should not stand here
  as a recorded finding either. As shipped, the correction paragraph cites the
  2026-09-03 read (`SES_SANDBOX=false`, `SEND_WORKER_ENABLED=true`) instead of
  asserting a value, and claims only that the variable gates the campaign
  worker and not the `outbound_queue` transactional path.
- **Database setup, Configuration, Build, Migrations, systemd, Apache
  "not run on a real box" notes**: each of these is a maintained-style
  runbook statement (`#0072`) that a reader today would take as the current
  verification status of that step. All were false — the 2026-08-25 deploy
  ran every one of them for real (the database/role, the config file behind
  a real `EnvironmentFile=`, the SPA and Go build, `migrate ... up`, the
  systemd unit, and the Apache vhost). Several were self-contradicting:
  the Migrations and systemd notes each sat within a few paragraphs of a
  prior correction that already said the opposite.
- **TLS section**: fixed the "Not run anywhere" claim (confirmed sample), and
  additionally found that the documented `certbot --apache` command (HTTP-01,
  two named hosts) could not have produced what is actually live — a
  **wildcard** certificate, which requires DNS-01. Re-verified with
  `openssl s_client` against the real host; the SAN is
  `DNS:*.opencircuitsf.com, DNS:opencircuitsf.com`, confirming the
  production-facts table's own claim that the real authenticator is
  `dns-route53`, not `--apache`.
- **First admin login section**: fixed (confirmed sample). Both premises
  ("no SES account", "no live instance") are false; left honestly
  unresolved whether the recovery ceremony itself has been run against the
  live site, since nothing in this document's record establishes that
  either way.
- **DNS table's "cannot be known before an instance exists" paragraph**: the
  public IP is already used two rows above it in the same table, and SES's
  DKIM is reported `SUCCESS`, so the selector tokens exist in Route 53 today.
  Corrected to point at `docs/email-setup.md` rather than inventing the
  token values here (avoids a second copy that can drift, the same reasoning
  `#0301`/`#0421` already applied to this table). Also flagged, but did not
  fix, an apparent hostname mismatch: this table's DKIM row names the apex
  (`opencircuitsf.com`) while `docs/email-setup.md`'s own DKIM row names the
  `mailing.` subdomain — worth a follow-up, out of this issue's scope.
- **"None of the above was executed" line closing the SES setup plan**:
  contradicted the correction blockquote immediately above it in the same
  section; corrected to name what genuinely was not executed (production
  access, at the time; the IAM role, since done).
- Also corrected two smaller "still stands" claims in "What this document is,
  and is not, verified against" that were superseded by corrections made
  earlier in this same pass (the Apache-vhost and systemd-hardening bullets
  in the closing summary).

**Left alone, per the issue's `## Relation` to `#0430`**: every Backups-section
occurrence of this same premise — the "AWS resources... do not exist yet" /
S3-deferred language, the `pull-backups.sh` step's "this project's own EC2
host is not provisioned" reference, and the backup-timer's "not yet exercised
on a real box" status. All are inside `## Backups` and belong to `#0430`.

**Not touched:** `PRD.md` — none of the fixes above required it, so §11's line
counts are unaffected.

## Verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0431 scripts/check.sh go ./internal/db/... ./internal/handlers/...
```

Ran twice — once before drafting these issue-file sections, once after (per
§5, since the citation guards below only scan `open`/`in-progress` issues and
this one is `in-progress` throughout). Both runs: `go build`, `go vet`, and
`gofmt -l` clean; `go test ./internal/db/... ./internal/handlers/... -p 2`
reported `ok` for both packages with real elapsed time (`internal/db` ~0.6s,
`internal/handlers` ~5.6s, not `0.00s`); zero skips reported by the audit
step; `VERIFICATION PASSED`. Test binaries exercised the repo-wide citation
guards this edit is subject to, by name, including
`TestNoCommentCitesUnresolvedPathOrSection`,
`TestNoCommentCitesUndefinedTestFunction`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`TestNoCommentCitesGoFileByLineNumber`, `TestPRDSectionIndexParity`, and
`TestDatabaseDocMigrationParity`, alongside the rest of each package's suite.

Also re-verified several of the corrected claims read-only against the live
host, not just against `CLAUDE.md` §7:

```
curl -sI https://www.opencircuitsf.com/            # 200, Apache 2.4.68 (Amazon Linux), the documented CSP header
curl -s https://www.opencircuitsf.com/.well-known/atproto-did | shasum -a 256   # matches the documented hash
echo | openssl s_client -connect www.opencircuitsf.com:443 -servername www.opencircuitsf.com \
  | openssl x509 -noout -issuer -dates                 # Let's Encrypt, valid to 2026-11-16
echo | openssl s_client -connect www.opencircuitsf.com:443 -servername www.opencircuitsf.com \
  | openssl x509 -noout -text | grep -A2 'Subject Alternative Name'  # wildcard SAN, confirming dns-route53/DNS-01
```

No production state was changed by any of the above — all read-only GETs and
TLS introspection.

### Second-pass verification (phase 2, bounce fix)

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0431b scripts/check.sh go ./internal/db/... ./internal/handlers/...
```

`uptime` first: load averages `3.75 3.44 3.10` — under §5's ~4 idle
threshold. `ISSUE=0431b` deliberately used (not `0431`/`0431r`, already used
by the prior two passes) so this run gets its own database — dropped on exit
per §5a/§8b, and it is the suffix this pass's dispatch specified. The shared
template is at migration 28 while `migrations/` in this working tree is at
27 (another session's uncommitted `crt_commands` work, per this pass's
dispatch note), so `scripts/check.sh` provisioned `opencircuit_test_0431b`
directly from `migrations/` (27 files, ending
`add_outbound_queue_skipped_status`) instead of cloning the ahead-of-tree
template — slower, but correct against this tree regardless of which side
moved. Did not touch `scripts/testdb.sh template` or `scripts/db-reset.sh`.

`go build`, `go vet`, and `gofmt -l` clean. `go test
./internal/db/... ./internal/handlers/... -p 2` reported `ok` for both
packages with real elapsed time, not `0.00s`. Ran twice with the same
`ISSUE=0431b` suffix: once after the `docs/deployment.md` edits but before
drafting this issue-file section (`internal/db` 0.431s, `internal/handlers`
5.415s), once again after writing this section, since both citation guards
scan `open`/`in-progress` issue files and this one is `in-progress`
throughout (`internal/db` 0.509s, `internal/handlers` 5.429s; load averages
`5.61 3.83 3.25` for this second run). Skip audit both times: `(no skips)`.
`VERIFICATION PASSED` both times. Repo-wide guards this edit is subject to
ran by name inside those packages:
`TestNoCommentCitesUnresolvedPathOrSection`,
`TestNoCommentCitesUndefinedTestFunction`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`TestNoCommentCitesGoFileByLineNumber`, `TestPRDSectionIndexParity`,
`TestPRDSectionIndexTotalMatchesFile`, `TestDatabaseDocMigrationParity`.

`PRD.md` was not touched by this pass (only `docs/deployment.md` was
edited), so §11's line counts are unaffected.

Also confirmed the `## Backups` section is still byte-identical
(`sha256=9465de9c…cfa52`, 48176 bytes for the `## Backups` heading through
the line before `## Redeploy procedure`, identical before and after this
pass's edits) and that `#0436`'s two items — the `certbot --apache -d
opencircuitsf.com -d www.opencircuitsf.com` command and the apex
`<sel1..3>._domainkey.opencircuitsf.com` DKIM row — are byte-for-byte
unchanged, confirmed with `diff` against the pre-edit file rather than by
re-stating the prior pass's claim.

### Third-pass verification (phase 2, second bounce fix)

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0431c scripts/check.sh go ./internal/db/... ./internal/handlers/...
```

`uptime` first: load averages `1.72 2.67 2.93` — under §5's ~4 idle
threshold, so nothing here is load-induced. `ISSUE=0431c` is the suffix this
pass's dispatch specified, giving the run its own database, dropped on exit.
`migrations/` in this working tree is still at 27 while the shared template
(`opencircuit_test_template`) is at 28 (another session's uncommitted
`crt_commands` work), so `scripts/check.sh` provisioned
`opencircuit_test_0431c` directly from `migrations/` (27 files, ending
`add_outbound_queue_skipped_status`) rather than cloning the ahead-of-tree
template — slower, but correct against this tree regardless of which side
moved. Did not run `scripts/db-reset.sh` or `scripts/testdb.sh template`.

`go build`, `go vet`, and `gofmt -l` clean. `go test ./internal/db/...
./internal/handlers/... -p 2` reported `ok` for both packages with real
elapsed time, not `0.00s` (`internal/db` 0.575s, `internal/handlers`
5.668s). Ran once after the two `docs/deployment.md` sentence replacements
and after drafting this issue-file section — both citation guards below scan
`open`/`in-progress` issues and this one is `in-progress` throughout this
pass. Skip audit: `(no skips)`. `VERIFICATION PASSED`. Confirmed by name
(via `grep -rn 'func Test…'` against the two packages' test files) that the
repo-wide guards this edit is subject to are the ones that ran inside that
`go test` invocation: `TestNoCommentCitesUnresolvedPathOrSection`
(`internal/handlers/citation_target_guard_test.go`),
`TestNoCommentCitesUndefinedTestFunction`
(`internal/handlers/dangling_test_citation_guard_test.go`),
`TestNoDocCommentNamesADifferentDeclarationInSameFile`
(`internal/handlers/misattached_doc_comment_guard_test.go`),
`TestNoCommentCitesGoFileByLineNumber`
(`internal/handlers/line_citation_guard_test.go`), `TestPRDSectionIndexParity`
and `TestPRDSectionIndexTotalMatchesFile`
(`internal/db/prd_section_index_test.go`), `TestDatabaseDocMigrationParity`
(`internal/db/docs_parity_test.go`),
`TestIssueLineCitationGuardCatchesPlantedStaleCitationAndSparesCorrectingQuotation`
(`internal/db/issue_line_citation_guard_test.go`), and
`TestIssueVerificationCitationGuardCatchesPlantedFalseCitation`
(`internal/db/issue_citation_guard_test.go`).

`PRD.md` was not touched by this pass (only `docs/deployment.md` was
edited), so §11's line counts are unaffected.

Confirmed with `diff` against the pre-edit file that only the two targeted
sentences changed (the `SEND_WORKER_ENABLED` clause in the intro correction
paragraph, and its sibling sentence under the `MAILER_NOOP=true` heading) —
nothing else in the file moved. Re-confirmed the `## Backups` section is
still byte-identical to the pre-edit file
(`sha256=ba755b58…f45d1`, 48177 bytes for the `## Backups` heading through
the line before `## Redeploy procedure`) and that `#0436`'s certbot command
and apex DKIM row each still occur exactly once, unchanged, via `python3`
byte counts (not BSD `grep -P`, per `CLAUDE.md` §8). Did not read
`/etc/opencircuit/config.env` or otherwise touch production to settle the
`SEND_WORKER_ENABLED` question — `#0415` owns that, per this pass's
dispatch instructions and the second review's own decision not to.

## Files changed

- `docs/deployment.md` — corrected every non-Backups occurrence of the
  falsified-by-deploy shape, as itemized in `## Fix` above.

## Gotchas

- Several of the falsified notes were self-contradicting within the file
  itself (a correction elsewhere already said the opposite), which was
  usable as strong evidence independent of any new box read — e.g. the
  Migrations section's "not been run against a real box" sat a few lines
  below a `#0293` correction already stating that exact command ran against
  production on 2026-08-25.
- Confirming the TLS certificate is a **wildcard** (not the two-name cert the
  documented `certbot --apache` command would produce) came from actually
  reading the SAN with `openssl x509 -text`, not from re-stating the
  production-facts table — the table already implied it (`dns-route53`
  authenticator) but did not say "wildcard" in so many words.

## Why one pass rather than piecemeal

Five separate issues this session have each corrected one or two of these
(`#0414`, `#0418`, `#0421`, `#0423`, `#0426`), and each found more on arrival
than its filing named. The remainder share a single cause — **notes written
against a pre-deploy world** — so a sweep for the *shape* will finish the job
where another string-hunt will not.

The shape to search for: any claim that something cannot be checked, does not
exist, or has not been run, **premised on the pre-deploy state**. Not just
`us-west-2` or `hello@`.

## The judgement this needs

A pre-deploy verification note is genuinely ambiguous under this project's own
conventions:

- Under `#0271`'s `## Scope` it can be **historical** — an honest record of what
  was and was not verified at the time, which is exactly what a verification
  section is for.
- Under `#0072` it can be **maintained** — a runbook statement a reader will act
  on today.

**Decide per occurrence and say why.** The distinguishing question is whether a
reader today would take it as instruction or as history. A dated "verified as of
2026-08-24" reads as history; a bare "Not run anywhere" reads as current state.

The Production-facts `Currently served` row is the least ambiguous: that table is
explicitly a current-state record, and `CLAUDE.md` §7 already contradicts it.

## Acceptance criteria

1. Every occurrence of the shape is found — **sweep for the pattern, not for the
   four rows above**, which are a sample the orchestrator confirmed, not an
   inventory.
2. Each is corrected with a dated note (`#0072`) or explicitly left as history
   (`#0271`), **with the reason recorded**.
3. **Re-derive what is actually true** from `CLAUDE.md` §7 and, where cheap, from
   the box read-only. Do not copy this issue's right-hand column.
4. The `Currently served` row matches reality.
5. **No mechanism** (`#0330`/`#0347`/`#0356`/`#0381`).
6. If `PRD.md` is touched, **§11's line counts must stay exact**, verified by the
   row's own `sed` (`#0113`) — §11 and the total changed twice today, so read
   the current state.
7. Run `./internal/db/...` and `./internal/handlers/...` (§5's repo-wide-guard
   table). Per §5 name the specific tests that ran — **real function names, not
   a `-run` prefix**, which has bounced four issues today. Re-run after writing
   your issue-file edits, and note both issue-prose guards scan only
   `open`/`in-progress`, so a run after resolution proves nothing about your own
   file. No Go-file-plus-line-number citation (`#0352`), scanned with `python3`
   bytes rather than BSD `grep -P`.

## Relation

- Found by: [#0426](0426.md)'s review
- Relates to: [#0430](0430.md) — the backups instance of the same premise; that
  issue's implementer should fix **both** its `#0229` quote and the limitations
  list, per the same review
- Relates to: [#0414](0414.md), [#0418](0418.md), [#0421](0421.md),
  [#0423](0423.md), [#0426](0426.md) — five passes that each corrected part of
  this file and each found more than their filing named
- Relates to: [#0271](0271.md), [#0072](0072.md) — the two conventions the
  per-occurrence judgement chooses between

## Review notes

**Bounced 2026-09-05.** The judgement work is good and the live claims hold —
but the sweep is not complete, and it introduced two dangling cross-references
of its own. Three concrete defects, all in text this pass wrote or edited.

### What passed, so the next pass does not redo it

- **The live claims re-derived independently, read-only, by this review.**
  `curl -sI https://www.opencircuitsf.com/` → `HTTP/1.1 200 OK`,
  `Server: Apache/2.4.68 (Amazon Linux) OpenSSL/3.5.7`.
  `/.well-known/atproto-did` hashes to
  `4198e742e721856d6832f926be55b916b14ad70ddd47a750adb566774cb5948d`, matching
  `CLAUDE.md` §7 exactly — verified by hash, not status code, per §7.
  `openssl s_client` + `openssl x509 -noout -issuer -dates -ext subjectAltName`
  → Let's Encrypt, `notAfter=Nov 16 20:12:34 2026 GMT`,
  `DNS:*.opencircuitsf.com, DNS:opencircuitsf.com`. The wildcard SAN is real,
  so the TLS finding is correct and `dns-route53` is the only authenticator
  that could have issued it. The `Content-Security-Policy` header served live
  was compared byte-for-byte in Python against the line in the Apache section
  and is identical, so that claim is not overstated either. No production
  state was changed.
- **Backups is untouched.** The `## Backups` section is **byte-identical**
  before and after the commit (SHA-256 `ba755b58…f45d1`, 47858 bytes, both
  sides), so all three `#0430` occurrences survive intact.
- **`#0436` was noted, not fixed.** `sudo certbot --apache -d
  opencircuitsf.com -d www.opencircuitsf.com` is present verbatim and
  unchanged, and the apex DKIM row
  `| `<sel1..3>._domainkey.opencircuitsf.com` | CNAME |` is unchanged. Neither
  was half-corrected.
- **The historical-versus-maintained calls are right, including the outright
  ones.** Rewriting the two Production-facts rows with no "original note" kept
  is correct: that table is labelled a current-state record and carries no
  date, so a reader takes every cell as instruction. Nothing dated was erased
  anywhere — every superseded verification note elsewhere is kept under an
  "Original note, left for context" heading or a strikethrough, which is the
  `#0271` treatment.
- The scoped suite is green (see **Re-verification** below).

### 1. A falsified premise the sweep missed, inside a section it edited

`## 10. First admin login`, numbered step 3:

> `setup** below; until SES exists, `MAILER_NOOP=true` logs the recovery`

SES exists. That is the same premise the pass declared false twenty lines
lower in the same section ("SES is live … DKIM and MAIL FROM `SUCCESS`"), so
§10 now contradicts itself — which is this issue's own `## Root cause`
verbatim ("directly contradicted by a correction already sitting a few lines
above or below them in the same section"). It is also a numbered runbook step,
the most maintained-style text in the file, so `#0072` applies and there is no
historical reading available.

**Remedy, exactly:** in that sentence replace `until SES exists` with the
current constraint. SES exists but the account is **sandboxed**
(`CLAUDE.md` §10 item 2), so a recovery mail only reaches an address that is
itself a verified SES identity until production access lands — that, not
SES's non-existence, is what a reader running this step today needs. Keep the
existing `never a production setting` clause; `MAILER_NOOP=true` is refused
outright on a production `BASE_URL` by `cmd/opencircuit/main.go`'s
`checkMailerNoOp` (`CLAUDE.md` §10), and the sentence should not read as
though it were an option on the box.

### 2. "both premises below are now false" points at nothing

Same section, the correction paragraph opens:

> `**Corrected `#0431`, 2026-09-04 — both premises below are now false.** SES is`

The commit **deleted** the original note that stated those two premises rather
than keeping it under an "Original note, left for context" heading, so there
is no "below" for the reader to check the correction against. Every other
correction in this pass keeps its original; this one does not, and still cites
it.

**Remedy:** either restore the original note verbatim below the correction
under the same "Original note, left for context —" heading the rest of the
pass uses (preferred — it is the `#0271` treatment and `issues/Issues.md`
records that paraphrasing a superseded note instead of preserving it is what
`#0293` had to undo), or rewrite the opening to name the two premises inline
and drop the word "below".

### 3. A second dangling reference, in `## 5. Migrations`

> `AL2023 PostgreSQL 16 the paragraph below assumed — consistent with the`

The paragraph below no longer assumes anything about AL2023 or PostgreSQL 16 —
the sentence that did ("It has not been run against a freshly `initdb`'d
AL2023 PostgreSQL 16 cluster") was removed by this same commit. The same
paragraph also says the `#0293` correction is "two paragraphs above it" in its
bolded lead and "just above" one sentence later; the second is right.

**Remedy:** drop `the paragraph below assumed` in favour of naming what
assumed it (the pre-deploy note this correction replaces, or the Prerequisites
section's original PostgreSQL 16 assumption), and make the lead say "just
above" so it agrees with its own next sentence.

### Sweep evidence — what this review checked and did not find

Scanned `docs/deployment.md` with `python3` reading the file as bytes (never
BSD `grep -P`, `CLAUDE.md` §8) for two families: absence claims (`does not
exist`, `not run`, `never run`, `nothing to`, `no live`, `not been run`, `not
yet`, `cannot be`, `was not`, `not exercised`, `unverified`, `not confirmed`,
`not provisioned`) and deferral claims (`PLACEHOLDER`, `once the … exists`,
`until … exists`, `unknown`, `has never`, `not validated`, `aspirational`,
`assumed`, `pending`). Every non-Backups hit was read in context. Findings 1–3
above are the only genuine misses. The remaining hits are each correct as they
stand and must **not** be "fixed":

- The intro table's `Backups … **Not installed yet.**` row — true, and
  backups material either way.
- `SES_INBOUND_BUCKET` "does not exist yet" (§3) and the `InboundBucketScoped`
  "not built yet" note (`## IAM`) — both gated on `#0057`, genuinely unbuilt.
- The GitHub repo rename "not done as of this writing" (§1) — `CLAUDE.md` §10
  item 1 still says not done.
- The `## SES setup` opening paragraph — already framed as a record of what
  the writing pass knew, and already answered by `#0421`'s blockquote.
- The apex-DKIM `[PLACEHOLDER: …]` row — `#0436`.
- `## Open items blocking a real deploy` — the heading is loosely worded now
  that a deploy has happened, but its body was corrected by `#0414` and its
  items are the email-path ones still genuinely open. Not worth churning.

### Re-verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0431r scripts/check.sh go ./internal/db/... ./internal/handlers/...
```

`uptime` first: load averages `2.59 3.02 2.86` — under §5's ~4 idle
threshold, so no timing result here is load-induced. Re-run after every edit to this file,
because both issue-prose guards scan only `open`/`in-progress` files and this
file is `open` from the first of those edits onward. Every run: `go build`,
`go vet`, and `gofmt -l` clean; `go test ./internal/db/...
./internal/handlers/... -p 2` reported `ok` for both packages with real
elapsed time, never `0.00s` (`internal/db` 0.632s, 0.491s, 0.404s;
`internal/handlers` 6.970s, 5.387s, 5.295s over the first three); skip audit
`(no skips)`; `VERIFICATION PASSED`. The
repo-wide guards this edit is subject to ran by name inside those packages:
`TestNoCommentCitesUnresolvedPathOrSection`,
`TestNoCommentCitesUndefinedTestFunction`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`TestNoCommentCitesGoFileByLineNumber`, `TestPRDSectionIndexParity`,
`TestPRDSectionIndexTotalMatchesFile`, `TestDatabaseDocMigrationParity`,
`TestIssueLineCitationGuardCatchesPlantedStaleCitationAndSparesCorrectingQuotation`,
and `TestIssueVerificationCitationGuardCatchesPlantedFalseCitation`.

`PRD.md` was not touched by the commit or by this review, so §11's line counts
are unaffected — `TestPRDSectionIndexParity` passing confirms it.

### Worth filing (not filed here, §9)

- The `## Backups` section is byte-identical, so nothing there moved — but
  `## 6. Seed` says "The interest taxonomy is seeded by migration `#0023`",
  writing a migration number in issue-reference form. Pre-existing, unrelated
  to this issue, and outside its scope; a one-line correction to `000023` if
  anyone wants it filed.
- `#0436`'s implementer should reword this pass's new TLS paragraph, which
  now describes the `certbot --apache` command as "left only as a
  syntactically valid fallback … if that were ever wanted instead". That reads
  as an endorsed option and does not state the blast radius `#0436` documents
  (running it drops `*.opencircuitsf.com`, taking `go.opencircuitsf.com` — a
  different project — with it, and re-couples renewal to `/.well-known/`).
  Correct as far as it goes, but `#0436` owns that fix.

### Second review, 2026-09-05 — bounced again

**The three prescribed remedies were applied correctly.** All three are
confirmed below, independently. What blocks resolution is a *fourth* defect,
in text `4dfb98c` wrote and this review found on arrival: a current-state
claim about production's `/etc/opencircuit/config.env` that four recorded
read-only measurements contradict. It is the same class the issue exists to
eliminate — an unmeasured assertion about live state — written into the
section the pass rewrote to remove exactly that.

#### The three remedies: all applied, verified independently

1. **`## 10. First admin login` step 3 no longer says `until SES exists`.**
   The replacement matches the prescribed wording, keeps the `never a
   production setting` clause, and adds the `checkMailerNoOp` refusal. That
   citation resolves: `checkMailerNoOp` is a real declaration in
   `cmd/opencircuit/main.go`, and the doc's paraphrase ("refuses to start it
   outright on a production `BASE_URL`") matches `CLAUDE.md` §10, which
   states the guard refuses to start unless `BASE_URL`'s host is
   `localhost`/`127.0.0.1`. `See **SES setup** below` also resolves —
   `## SES setup` is a real heading, and it is below this section.
2. **The restored note is genuinely verbatim.** Extracted the pre-`4dfb98c`
   paragraph from git and compared it in `python3` against the restored block
   with whitespace and `**` markers normalised: **identical**. The only
   changes are the original's own bolded lead being absorbed into the
   `**Original note, left for context — accurate only for the pre-deploy
   state:**` heading and a re-wrap, which is the convention every other
   "Original note" block in this pass follows. Nothing was dropped — unlike
   the Migrations block, which was truncated (see the residual below).
   `both premises below are now false` now points at both premises.
3. **`## 5. Migrations` reads consistently.** The lead now says `just above`
   and agrees with its own next sentence, and the `#0293` correction really
   is the immediately preceding paragraph — one, not two. `the paragraph
   below assumed` is gone.

**Live claims re-derived a third time, read-only:** `curl -sI
https://www.opencircuitsf.com/` → `HTTP/1.1 200 OK`, `Server: Apache/2.4.68
(Amazon Linux) OpenSSL/3.5.7`; `/.well-known/atproto-did` hashes to
`4198e742e721856d6832f926be55b916b14ad70ddd47a750adb566774cb5948d`, matching
`CLAUDE.md` §7 by hash. No production state was read or changed beyond those
two GETs.

**Scope holds, measured across all three revisions** (pre-`#0431`,
`4dfb98c`, current), in `python3` over bytes: the `## Backups` section is
byte-identical at every step (`sha256=f45fb9cdf5c0…`, 48177 bytes for the
heading through the line before `## Redeploy procedure`), and `#0436`'s
`sudo certbot --apache -d opencircuitsf.com -d www.opencircuitsf.com` and
apex ``| `<sel1..3>._domainkey.opencircuitsf.com` | CNAME |`` row each occur
exactly once and are unchanged. Nothing of `#0430`'s or `#0436`'s moved.

#### 4. A production config value asserted against the only reading anyone has taken

`### SES is not configured yet (historical …)`, in the `**Correction
(`#0431`, 2026-09-04)**` paragraph `4dfb98c` added:

> `still pending), which is why `SEND_WORKER_ENABLED=false` below is still the`
> `real production setting today, not a leftover from before SES existed.`

Production's `/etc/opencircuit/config.env` was read read-only on 2026-09-03
by `#0271`'s review and carries **`SEND_WORKER_ENABLED=true`** (with
`SES_SANDBOX=false`). That reading is recorded in three places in this
tracker — `#0415`'s `## Description`, `#0057`'s "Production flags" facts row,
and `#0423`'s work log, which flags the contradiction with `CLAUDE.md` §10
item 2 explicitly. There is no measurement anywhere behind the doc's `false`.

This fails acceptance criterion 3 ("**Re-derive what is actually true** …
where cheap, from the box read-only") in the sharpest available way: the
sentence does not merely restate a stale value, it adds an invented causal
account for it ("which is why … not a leftover from before SES existed"),
resolving an *open* question — `#0415` exists because nobody knows whether
production access was granted — on the side the evidence contradicts. A
reader deciding whether the campaign send worker is running in production
would be told the opposite of what the box was last observed to say, and
`#0415` records that the user is about to send a first real newsletter.

**Remedy, exactly.** Replace the clause after `still pending),` — from
`which is why` through `before SES existed.` — with:

> — but production's own `/etc/opencircuit/config.env`, read read-only on
> 2026-09-03, carries `SES_SANDBOX=false` **and `SEND_WORKER_ENABLED=true`**
> (`#0415`, `#0057`'s facts table, `#0423`), which is the flip §10 item 2
> says to make only *after* access is granted. `#0415` is open to establish
> `ProductionAccessEnabled` from AWS and owns reconciling the two — do not
> take either side as settled from this document.

Keep the sentence that follows unchanged; "That variable gates only the
**campaign** send worker … not the `outbound_queue`" is correct and is the
useful half of the paragraph.

**And fix its sibling in the same section, or the fix recreates the
self-contradiction this issue is about.** Thirteen lines below, under
`**`MAILER_NOOP=true` is not the way to express this in production.**`:

> `/etc/opencircuit/config.env` therefore leaves it unset and constructs the
> real SES v2 mailer, with `SEND_WORKER_ENABLED=false` so no campaign send
> worker starts.

That sentence predates `#0431` and asserts the same unmeasured value.
Replace from `, with `SEND_WORKER_ENABLED=false`` to the end of the sentence
with a full stop, then add: `(It does **not** additionally disable the
campaign worker: the same 2026-09-03 read shows `SEND_WORKER_ENABLED=true` —
see the correction above and `#0415`.)` Correcting only the first of the two
leaves the section contradicting itself, which is this issue's own
`## Root cause`.

**Also correct the issue file's own record.** `## Fix`'s "SES is not
configured yet" bullet repeats the claim — "`SEND_WORKER_ENABLED=false` is
still today's real setting, for a different reason than the section implied"
— and should not stand as a recorded finding.

#### On the new SES sandbox sentence in step 3: not an overclaim

Called out for judgement because `#0415` is open over exactly this. The
verdict is that it stands as written. It attributes the sandbox state to
`CLAUDE.md` §10 item 2 rather than asserting an independent measurement, and
§10 item 2 does say precisely that; it is also the fifth statement of the
same fact in this file (the intro correction, the `MAX_SEND_RATE` bullet,
`## Open items` item 2, and the closing verified-against section all say it
already), none of which hedge. The uncertainty lives in the *source*, and
`#0415` owns the source. Requiring every downstream citation to hedge would
duplicate `#0415`'s fix in five places and drift the moment it lands. What
is **not** acceptable is what defect 4 does — going past the citation to
assert an unmeasured production config value as measured fact.

Worth knowing for whoever takes `#0415`: resolving it means updating five
places in `docs/deployment.md`, not one.

#### Residual, not blocking — the Migrations reference names a text that no longer says what it is cited for

`the AL2023 PostgreSQL 16 assumed by the pre-deploy note this correction
replaces` is the first of the two options the prior review offered, and it is
honest — but the note it names is preserved directly below under `**Original
note, left for context — accurate only for the drills it actually
describes:**` with the assuming sentence ("It has not been run against a
freshly `initdb`'d AL2023 PostgreSQL 16 cluster") removed by `4dfb98c`. So a
reader who maps the phrase onto the block below finds no PostgreSQL 16
assumption there. It reads clearly enough not to bounce on — the sentence is
self-contained (production is 15.18) and its second half points at the
Prerequisites section's correction, which does resolve: §1 states 15.18, and
the closing verified-against section records that Prerequisites "originally
assumed 16 … they now state 15.18". Left as-is deliberately; noted so a later
pass does not rediscover it as a defect.

#### Every relative reference in the touched text, checked

`See **SES setup** below` ✔ (heading exists, is below). `CLAUDE.md` §10 item
2 ✔ (twice). `CLAUDE.md` §10 for `checkMailerNoOp` ✔. `docs/configuration.md`
✔. `both premises below` ✔ (now restored). `just above` in Migrations ✔
(immediately preceding paragraph). `the Prerequisites section's own
correction` ✔. `the pre-deploy note this correction replaces` — resolves to a
historical text, see the residual above. `see the Production-facts correction
above` ✔ (pre-existing, unchanged by this pass).

#### Re-verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0431r2 scripts/check.sh go ./internal/db/... ./internal/handlers/...
```

`uptime` first: load averages `2.24 3.08 3.10` — under §5's ~4 idle
threshold, so nothing here is load-induced. `ISSUE=0431r2` gives this review
its own database, dropped on exit. Did not run `scripts/db-reset.sh` or
`scripts/testdb.sh template`; the tree is at migration 27 while the shared
template is at 28, so `check.sh` provisioned `opencircuit_test_0431r2` from
`migrations/` directly. Run before and after writing these notes, since both
issue-prose guards scan only `open`/`in-progress` files and this one is
`in-progress` at the first run and `open` at the second. Both runs: `go
build`, `go vet`, `gofmt -l` clean; `go test ./internal/db/...
./internal/handlers/... -p 2` reported `ok` for both packages with real
elapsed time, never `0.00s`; skip audit `(no skips)`; `VERIFICATION PASSED`.
The repo-wide guards this edit is subject to ran by name inside those
packages: `TestNoCommentCitesUnresolvedPathOrSection`,
`TestNoCommentCitesUndefinedTestFunction`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`TestNoCommentCitesGoFileByLineNumber`, `TestPRDSectionIndexParity`,
`TestPRDSectionIndexTotalMatchesFile`, `TestDatabaseDocMigrationParity`, and
`TestIssueVerificationCitationGuardCatchesPlantedFalseCitation`.

`PRD.md` was not touched by either commit or by this review, so §11's line
counts are unaffected — `TestPRDSectionIndexParity` passing confirms it.

All byte-level scanning in this review used `python3` reading the files as
bytes, never BSD `grep -P` (`CLAUDE.md` §8).

#### Worth filing (not filed here, §9)

- Both items the prior review listed still stand: `## 6. Seed`'s "migration
  `#0023`" wrong-form citation, and `#0436` owning the reword of this pass's
  new TLS paragraph.
- New: `#0415` should be re-read before it is worked. It is scoped as a
  `CLAUDE.md` §10 item 2 correction, but the same unconfirmed sandbox state
  is now asserted in five places in `docs/deployment.md` and in
  `#0057`'s facts table. Whoever resolves it needs that inventory, or the
  next sweep of this file finds the drift again.

### Third review, 2026-09-05 — resolved

**Both replacements are the prescribed text, and both are true.** Compared in
`python3` over bytes (never BSD `grep -P`, `CLAUDE.md` §8) against the two
blockquotes the second review wrote out, whitespace-normalised for the
re-wrap: **identical**, both of them. The sentence the remedy said to keep —
"That variable gates only the **campaign** send worker … not the
`outbound_queue`" — is unchanged, and `5d2c323` touches nothing else in the
file (two hunks, +9/-4, both inside `### SES is not configured yet`).

The three citations record what the new text says they record:

- `#0415` `## Description` reproduces the read of production's
  `/etc/opencircuit/config.env` — `SES_SANDBOX=false`, `SEND_WORKER_ENABLED=true`
  — first seen 2026-09-03, and it is `open`, so "`#0415` is open to establish
  `ProductionAccessEnabled` from AWS and owns reconciling the two" is accurate.
- `#0057`'s facts table carries `| Production flags | `SES_SANDBOX=false`,
  `SEND_WORKER_ENABLED=true` | `config.env` |`, from a planning pass that
  states nothing in AWS was created or modified.
- `#0423` records the same values read read-only over `ssh ec2`, and flags the
  contradiction with `CLAUDE.md` §10 item 2 explicitly.

"the flip §10 item 2 says to make only *after* access is granted" is also
true: `CLAUDE.md` §10 item 2 says "When granted, flip `SES_SANDBOX=false` and
`SEND_WORKER_ENABLED=true`." The bare `§10 item 2` resolves — `CLAUDE.md` §10
item 2 is named in full two sentences earlier in the same paragraph.

**The section no longer contradicts itself, and neither does the file.** Every
`SEND_WORKER_ENABLED` occurrence in `docs/deployment.md` was enumerated by
byte offset. There are three: the intro correction and its sibling under
`**`MAILER_NOOP=true` is not the way to express this in production.**`, both
now saying `true` from the same 2026-09-03 read; and the pre-existing
ShortLinks-split note, which says the variable "should stay `true` on this
single-instance topology" and corroborates rather than conflicts. No `false`
survives anywhere in the file.

**Nothing correctly-cited was hedged.** The diff is the whole answer here: the
two sentences the remedy named are the only text that moved. The five
statements attributing the sandbox state to `CLAUDE.md` §10 item 2 — the ones
the second review deliberately cleared, because the uncertainty lives in the
source and `#0415` owns the source — are byte-unchanged. No over-correction.

**Production was not read.** Neither replacement introduces a fact that is not
already in the prescribed wording or in the three cited issues; nothing in
`5d2c323` could only have come from `/etc/opencircuit/config.env` itself. The
pass records taking the wording as written and declining the read, which is
what both prior reviews decided. This review did not read it either.

**Scope unmoved, measured again across all four revisions** (pre-`#0431`,
`4dfb98c`, `1e52ae0`, `5d2c323`, worktree), in `python3` over bytes: the
`## Backups` section is byte-identical at every step
(`sha256=f45fb9cdf5c0…`, 48177 bytes from the `## Backups` heading through the
line before `## Redeploy procedure`), and `#0436`'s `sudo certbot --apache -d
opencircuitsf.com -d www.opencircuitsf.com` occurs exactly once, unchanged.
The apex `<sel1..3>._domainkey.opencircuitsf.com` DKIM row is unchanged; the
string occurs twice rather than once because `4dfb98c` added the prose note
flagging the hostname mismatch, and that count has been stable since.

**Live claims re-checked read-only, a fourth time:** `curl` to
`https://www.opencircuitsf.com/` → `200`, `Server: Apache/2.4.68 (Amazon
Linux) OpenSSL/3.5.7`; `/.well-known/atproto-did` hashes to
`4198e742e721856d6832f926be55b916b14ad70ddd47a750adb566774cb5948d`, matching
`CLAUDE.md` §7 by hash, not by status code. Two GETs, nothing changed.

#### One element of remedy 4 was missed, and this review applied it rather than bouncing

The second review's defect 4 had a third part — "**Also correct the issue
file's own record.** `## Fix`'s "SES is not configured yet" bullet repeats the
claim … and should not stand as a recorded finding." The third implementation
pass applied the two `docs/deployment.md` sentences and did not apply this
one; its work log says "Applied defect 4's two prescribed remedies", so the
third part was neither applied nor disclosed. `## Fix` therefore still carried
`SEND_WORKER_ENABLED=false` as "still today's real setting" — the exact shape
this issue exists to eliminate, in this issue's own record.

Corrected here rather than bounced. The deliverable — `docs/deployment.md` —
is complete and correct; what remained was one false parenthetical in a prose
narrative, in the file this review must stage for the resolution commit
anyway. A fourth bounce would have cost an implement-and-review cycle to
change nothing in the shipped document. The correction names itself as the
reviewer's, keeps what the pass actually did, and drops only the standing
claim. Flagged to the orchestrator as a disclosure gap.

#### On escalation

Not warranted. `CLAUDE.md` §3's trigger is two bounces on the same *design*
problem; the three bounces here were three different defects (a missed
falsified premise plus two dangling references; then a newly-found production
overclaim; then nothing), each remedied by transcribing text a reviewer wrote
out, and each pass applied what it was handed. The one miss above is an
omission, not a decision the pass got wrong — Sonnet remained the right tool
throughout, and there is no fourth pass to escalate.

#### Re-verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0431r3 scripts/check.sh go ./internal/db/... ./internal/handlers/...
```

Run with every prose edit above already on disk and **Status** still
`in-progress`, so both issue-prose guards actually scanned this file — they
skip anything not `open`/`in-progress`, and the only edit made after the run
was the `resolved` flip itself, which cannot introduce a citation. `uptime`
first. `ISSUE=0431r3` is the suffix this review's dispatch specified, giving
the run its own database, dropped on exit; `scripts/db-reset.sh` and
`scripts/testdb.sh template` were not run, the tree being at migration 27
while the shared template is at 28 from another session. `go build`, `go vet`
and `gofmt -l` clean; `go test ./internal/db/... ./internal/handlers/... -p 2`
reported `ok` for both packages with real elapsed time, not `0.00s`; skip
audit `(no skips)`; `VERIFICATION PASSED`. The repo-wide guards this edit is
subject to ran by name inside those packages:
`TestNoCommentCitesUnresolvedPathOrSection`,
`TestNoCommentCitesUndefinedTestFunction`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`TestNoCommentCitesGoFileByLineNumber`, `TestPRDSectionIndexParity`,
`TestPRDSectionIndexTotalMatchesFile`, `TestDatabaseDocMigrationParity`,
`TestIssueLineCitationGuardCatchesPlantedStaleCitationAndSparesCorrectingQuotation`,
and `TestIssueVerificationCitationGuardCatchesPlantedFalseCitation`.

`PRD.md` was untouched by every commit on this issue and by this review, so
§11's line counts are unaffected — `TestPRDSectionIndexParity` passing
confirms it.

#### Worth filing (not filed here, §9)

- Both standing items carry over: `## 6. Seed`'s "migration `#0023`"
  wrong-form citation, and `#0436` owning the reword of this pass's TLS
  paragraph.
- `#0415` has since been re-scoped (`cf8f9e8`) with the five-place inventory
  the second review asked for, so that one is discharged.
- `docs/deployment.md` is clean of **this issue's** shape outside `## Backups`.
  What remains is not this issue's: the sandbox state itself is asserted in
  five places and is unconfirmed until `#0415` establishes
  `ProductionAccessEnabled`, and `#0430` still owns the three Backups
  occurrences.

## Work log

- 2026-09-04 — Filed by the orchestrator from `#0426`'s review (§9). The four
  sample occurrences were confirmed in the file before filing; the inventory is
  deliberately left to criterion 1.
- 2026-09-05 — Implemented (Sonnet, phase 2). Swept the full file rather than
  the four sample strings; found roughly a dozen further occurrences beyond
  the samples (database setup, configuration, build, migrations, systemd, and
  Apache "not run on a real box" notes; the "SES is not configured yet"
  section; the SES-setup plan's closing "none of the above was executed"
  line; the DNS table's "cannot be known before an instance exists"
  paragraph; two stale "still stands" bullets in the closing summary).
  Corrected each in place with a dated note, keeping the superseded text as
  historical context per `#0271`. Left every Backups-section occurrence for
  `#0430`, per the issue's own `## Relation`. Did not touch `PRD.md`. Commit
  `4dfb98c`. Re-ran `ISSUE=0431 scripts/check.sh go ./internal/db/...
  ./internal/handlers/...` after writing this section — `ok` for both
  packages, zero skips, `VERIFICATION PASSED`. Not marking `resolved`;
  leaving that to the reviewer per §9.
- 2026-09-05 — Reviewed (Opus, phase 3). **Bounced to `open`.** The
  historical-versus-maintained calls are sound and the live claims were
  independently re-derived read-only (200 + Apache 2.4.68 banner, the
  `atproto-did` SHA-256 matching `CLAUDE.md` §7, the wildcard SAN and
  2026-11-16 expiry, and a byte-for-byte CSP comparison); the `## Backups`
  section is byte-identical to its pre-commit state and `#0436`'s certbot
  command and apex DKIM row are unchanged. Three defects block resolution:
  a missed falsified premise in `## 10. First admin login` step 3 that now
  contradicts the pass's own correction twenty lines below it, and two
  dangling "the paragraph below" references the pass created by deleting or
  rewriting the text it cites (`## 10` and `## 5. Migrations`). Each remedy is
  written out in `## Review notes`, so the next pass applies rather than
  decides — staying on Sonnet per `CLAUDE.md` §3. Code commit `4dfb98c` left
  in place. Verified with `ISSUE=0431r scripts/check.sh go ./internal/db/...
  ./internal/handlers/...`, run before and after these notes.
- 2026-09-05 — Implemented, second pass (Sonnet, phase 2). Applied the three
  remedies the review wrote out exactly: replaced `## 10. First admin
  login` step 3's `until SES exists` with the real current constraint (SES
  live, account sandboxed, recovery mail only reaches an already-verified
  SES identity until production access lands) while keeping the `never a
  production setting` clause and adding the `checkMailerNoOp` production-
  refusal citation; restored the original pre-deploy note verbatim under an
  "Original note, left for context" heading below the section's `#0431`
  correction paragraph, so its own `both premises below are now false`
  citation now points at something; and in `## 5. Migrations`, changed the
  correction's dangling `the paragraph below assumed` to name what actually
  assumed AL2023 PostgreSQL 16 (`the pre-deploy note this correction
  replaces`, since that assuming sentence was deleted, not moved), and
  changed its lead from `two paragraphs above it` to `just above` to agree
  with its own next sentence, matching the review's finding that the
  `#0293` correction is one paragraph above, not two. **§3 escalation was
  considered and declined**: this was a design-problem bounce, not a typo
  bounce by the letter of the trigger, but every remedy was prescribed
  precisely enough (exact replacement text or exact naming choice, per
  `CLAUDE.md` §3's "handed the replacement expression" test) that the pass
  was transcription, not decision — so it stayed on Sonnet. Confirmed by
  `diff` against the pre-edit file that only these three spots changed, that
  `## Backups` is still byte-identical (sha256 `9465de9c…cfa52`, 48176
  bytes, matching before and after), and that `#0436`'s certbot command and
  apex DKIM row are untouched. Did not touch `PRD.md`. Verified with
  `ISSUE=0431b scripts/check.sh go ./internal/db/... ./internal/handlers/...`
  — `ok` for both packages with real elapsed time, zero skips,
  `VERIFICATION PASSED`; database dropped on exit per `ISSUE=0431b`'s own
  isolated database. Not marking `resolved`; leaving that to the reviewer
  per §9.
- 2026-09-05 — Reviewed, second pass (Opus, phase 3). **Bounced to `open`.**
  The three prescribed remedies were all applied correctly and verified
  independently: step 3's `until SES exists` is gone and its
  `checkMailerNoOp` and `SES setup` citations both resolve; the restored
  pre-deploy note is byte-verbatim against the pre-`4dfb98c` text once the
  bold lead absorbed into the heading and the re-wrap are normalised, so
  `both premises below` resolves; and `## 5. Migrations` now says `just
  above` in agreement with its own next sentence. Scope confirmed across
  all three revisions in `python3` over bytes — `## Backups` is
  byte-identical (`sha256=f45fb9cdf5c0…`, 48177 bytes) and `#0436`'s
  certbot command and apex DKIM row each occur once, unchanged. Live
  claims re-derived read-only a third time (200, Apache 2.4.68, the
  `atproto-did` SHA-256 matching `CLAUDE.md` §7). **One new defect blocks
  resolution**, found on arrival in text `4dfb98c` wrote: the intro
  correction asserts `SEND_WORKER_ENABLED=false` is "still the real
  production setting today", where the only reading anyone has taken of
  production's `/etc/opencircuit/config.env` (2026-09-03, recorded in
  `#0415`, `#0057` and `#0423`) says `true` — an unmeasured current-state
  claim, with an invented causal account attached, resolving `#0415`'s open
  question on the side the evidence contradicts. That is acceptance
  criterion 3. The new SES-sandbox sentence in step 3 was examined for the
  same fault and **cleared**: it cites `CLAUDE.md` §10 item 2 rather than
  asserting independently, and matches four other statements in the file.
  **§3 escalation considered and declined**: this is bounce two but on a
  different defect from bounce one, and both replacement sentences are
  written out verbatim in `## Review notes`, so the next pass is
  transcription — it stays on Sonnet. Code commits `4dfb98c` and `1e52ae0`
  left in place. Verified with `ISSUE=0431r2 scripts/check.sh go
  ./internal/db/... ./internal/handlers/...`, run before and after these
  notes.
- 2026-09-05 — Implemented, third pass (Sonnet, phase 2). Applied defect 4's
  two prescribed remedies exactly, both in `### SES is not configured yet`:
  replaced the intro correction's `which is why SEND_WORKER_ENABLED=false
  below is still the real production setting today, not a leftover from
  before SES existed` — an unmeasured assertion with an invented causal
  account, resolving `#0415`'s open question on the side the evidence
  contradicts — with the exact sentence the review wrote out, citing the
  2026-09-03 read-only `/etc/opencircuit/config.env` read
  (`SES_SANDBOX=false`, `SEND_WORKER_ENABLED=true`) recorded in `#0415`,
  `#0057`'s facts table, and `#0423`, and naming `#0415` as owning the
  reconciliation; and fixed its unmeasured sibling under `MAILER_NOOP=true`
  is not the way to express this in production, thirteen lines below, the
  same way, so the section no longer contradicts itself. Left every other
  SES statement in the file unhedged, per the review's own finding that they
  are defensible (they cite `CLAUDE.md` §10 item 2 rather than asserting a
  measurement, and §10 item 2 does say that) — hedging them would duplicate
  `#0415`'s fix in five places. Did not read production to settle the
  question; took the replacement sentences as written, per this pass's
  dispatch instructions and the prior review's own declination to do so.
  **§3 escalation was considered and declined**: this is the third
  implementation pass, but the reviewer judged both remedies transcription —
  the replacement text was written out verbatim — and this bounce was a
  *different* defect from the second pass's three (a production-config
  overclaim newly introduced by `4dfb98c`, not a repeat of the earlier
  dangling-reference or falsified-premise defects), so it stayed on Sonnet.
  Confirmed by `diff` against the pre-edit file that only the two targeted
  sentences changed, that `## Backups` is still byte-identical
  (`sha256=ba755b58…f45d1`, 48177 bytes, matching before and after), and
  that `#0436`'s certbot command and apex DKIM row each still occur exactly
  once, unchanged. Did not touch `PRD.md`. Verified with `ISSUE=0431c
  scripts/check.sh go ./internal/db/... ./internal/handlers/...` — `ok` for
  both packages with real elapsed time, zero skips, `VERIFICATION PASSED`;
  database dropped on exit per `ISSUE=0431c`'s own isolated database. Not
  marking `resolved`; leaving that to the reviewer per §9.
- 2026-09-05 — Reviewed, third pass (Opus, phase 3). **Resolved.** Both
  `docs/deployment.md` replacements are byte-exact to the second review's
  prescribed blockquotes once re-wrapping is normalised, and both are true:
  `#0415` (`open`), `#0057`'s Production-flags row and `#0423` each record the
  2026-09-03 read-only `/etc/opencircuit/config.env` reading
  (`SES_SANDBOX=false`, `SEND_WORKER_ENABLED=true`), and `CLAUDE.md` §10 item
  2 does say to flip both only once production access is granted. All three
  `SEND_WORKER_ENABLED` occurrences left in the file agree, so neither the
  section nor the file contradicts itself, and the five statements that merely
  cite §10 item 2 for the sandbox state are byte-unchanged — nothing was
  over-hedged. Production was not read by the pass or by this review. Scope
  re-measured over bytes across all four revisions: `## Backups` byte-identical
  (`sha256=f45fb9cdf5c0…`, 48177 bytes) and `#0436`'s certbot command
  unchanged. Live claims re-checked read-only (200, Apache 2.4.68, the
  `atproto-did` SHA-256 matching `CLAUDE.md` §7). **One gap, corrected here
  rather than bounced:** defect 4's third element — correcting `## Fix`'s own
  repetition of the `SEND_WORKER_ENABLED=false` claim — was neither applied
  nor disclosed by the third implementation pass, so this review applied it in
  the resolution commit and marked it as the reviewer's. **§3 escalation
  considered and declined**: the three bounces were three distinct defects,
  each remedied by transcription, and the remaining gap was an omission rather
  than a wrong decision. Code commits `4dfb98c`, `1e52ae0` and `5d2c323` left
  in place. Verified with `ISSUE=0431r3 scripts/check.sh go ./internal/db/...
  ./internal/handlers/...`, run with **Status** still `in-progress` so both
  issue-prose guards scanned this file's final text.
