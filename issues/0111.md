# 0111 — `admin_subscribers.go` still documents the soft-bounce fields as a count of Transient-bounce rows

| | |
|---|---|
| **Status** | open |
| **Module** | handlers |
| **Platform** | All |
| **Phase** | 4 — Unsubscribe and list hygiene |
| **First seen** | 2026-08-20 |

## Description

`internal/handlers/admin_subscribers.go`'s doc comment on `subscriberView`
(lines 190–197) describes `SoftBounceCount`/`SoftBounceThreshold`/
`SoftBounceWindowDays` as "the count of **Transient-bounce** `email_events`
rows for this address within the currently configured window". After `#0109`
(`8f900fe`) that is wrong in both directions, exactly as `#0110`'s text was:
the count also includes `Undetermined` bounces, and it excludes the
sender-fault `Transient` subtypes (`MessageTooLarge`, `ContentRejected`,
`AttachmentRejected`).

`internal/handlers/soft_bounce.go`'s package-level file comment (line 3) has
the same narrower wording — it says `ses_notifications.go` uses the threshold
"to decide whether a **Transient** bounce crosses the threshold", but that
arm is now `case sesnotify.BounceTypeTransient, sesnotify.BounceTypeUndetermined`
(`ses_notifications.go:456`).

Documentation only — no behaviour is affected, and **this does not block Phase
4's deployable gate.** Same severity and same class as `#0110`.

## Root cause

`#0110` corrected the SPA-side mirror of this comment
(`web/src/lib/types.ts`) and its Notes recorded, on the strength of `#0109`'s
review, that types.ts was "the *only* remaining stale description of the
widened count". That sweep covered `web/` and `docs/` but not
`internal/handlers/`. `admin_subscribers.go` is the *authoritative* one of the
pair — `types.ts` mirrors the Go struct, not the other way round — so the two
are now corrected in the wrong order, with the source-of-truth still stale.

Current text, `internal/handlers/admin_subscribers.go` lines 190–197:

```
// SoftBounceCount/SoftBounceThreshold/SoftBounceWindowDays are #0039's
// "admin can see the current soft-bounce count" criterion: the count of
// Transient-bounce email_events rows for this address within the currently
// configured window, plus the threshold/window themselves so the admin
// screen can show "N of THRESHOLD in the last WINDOW days" rather than a
// bare number. Like Interests, populated by Get only (List omits them to
// avoid a query per row); all three are nil together when the handler was
// constructed with a nil bounces/settings dependency (STORAGE=json dev mode).
```

The rest of that sentence is accurate and was re-verified during `#0110`'s
review: the three fields really are set together in one
`if h.bounces != nil && h.settings != nil` block whose only other exit is an
error return, so "all three are nil together" holds. Only the
"Transient-bounce" clause is wrong.

## Acceptance criteria

- [ ] `subscriberView`'s doc comment describes what
      `Store.CountRecentSoftBounces` actually counts: `Transient` **and**
      `Undetermined` bounces, excluding the sender-fault `Transient` subtypes
      (`MessageTooLarge`, `ContentRejected`, `AttachmentRejected`)
- [ ] `soft_bounce.go`'s file comment no longer says the threshold decides
      "whether a `Transient` bounce crosses" it — the arm covers `Transient`
      and `Undetermined`
- [ ] Both pick up `#0109` as the issue that widened the count, matching the
      convention `store.go`, `admin.ts`, and now `types.ts` follow
- [ ] Wording matches `web/src/lib/types.ts`'s corrected comment (`#0110`,
      `896e61c`) so the Go struct and its SPA mirror cannot drift again
- [ ] `go build ./...` and `go vet ./...` clean (comment-only change)

## Notes

Comment-only, in Go this time, so the frontend checks do not apply and no
database is needed — `go build` and `go vet` are the whole verification
surface. Like `#0110` this does not warrant its own plan → implement → review
dispatch; batch it into the next issue that touches
`internal/handlers/admin_subscribers.go` or the soft-bounce subsystem.

Surveyed during `#0110`'s review, and accurate as they stand — do not sweep
these:

- `web/src/lib/types.ts` (`#0110`, `896e61c`) and `web/src/lib/admin.ts`
  (`#0109`)
- `internal/sesnotify/store.go`'s `CountRecentSoftBounces` comment and
  `internal/sesnotify/event.go`'s sender-fault subtype block (`#0109`)
- `ses_notifications.go` lines 358–359, 411–413, 462–466, 522–527 (`#0109`)
- `docs/unsubscribe.md` and `docs/database.md`'s `000014`/`000016` rows
- test-file comments naming "Transient bounce" — those describe the specific
  fixtures they seed and are correct as written

`migrations/000014`'s `-- #0039 counts transient bounces` column comment is
deliberately left alone: migrations are append-only (`CLAUDE.md` §1), and
`000016` already records the widened predicate.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §6.5 — Unsubscribe — three paths, all required · 93 lines
  `sed -n '/^### 6\.5 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Follows: [#0110](0110.md) — `types.ts` still documents `soft_bounce_count` as a count of Transient-bounce rows
- Follows: [#0109](0109.md) — An address that only ever produces `Undetermined` bounces is never suppressed
- Follows: [#0039](0039.md) — Suppress addresses after repeated soft bounces
