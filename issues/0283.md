# 0283 — A zero-row MarkSent leaves a row that will be sent again

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | internal/mailing / internal/outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |
| **Commit** | 1cf190d, 62c475f |
| **Closed** | 2026-08-27 |

## Resolution notes

> 🟢 Resolved 2026-08-27 — `MarkSent`'s predicate admits `'sending'` and
> `'queued'`, excludes `'sent'`, `'abandoned'` and `'failed'`; the widened
> guard was mutation-proved by an independent reviewer, and the second pass
> corrected the doc comment's false single-call-site premise and completed the
> `KindSubscribeIntake` safety argument, which the reviewer verified at the
> source and found non-circular.

## Description

`MarkSent` updates `WHERE id = $1 AND status = 'sending'`. If something released
the claim while the send was in flight, it affects zero rows: the message has
**already gone to SES**, but the row stays `'queued'` and a later pass claims and
sends it again.

`#0254` added a `Warn` when that happens, which is right and is what would have
exposed its own bug rather than letting a healthy-looking pass hide it. But the
reviewer was explicit that the warning **narrates the duplicate rather than
preventing it**.

The proven cause of a stolen claim is now structurally impossible — `#0254`
scoped both sweeps by kind, and the reviewer verified neither consumer can touch
the other's rows in either direction. So this is no longer reachable by the route
that motivated it. It stays open because the state machine still has the shape
where a lost claim means a duplicate send, and duplicate confirmation links and
recovery magic links are the thing this subsystem exists to prevent.

## The stronger fix, and why it is its own issue

Record the send unconditionally:

```sql
UPDATE outbound_queue SET status='sent', … WHERE id = $1 AND status <> 'sent'
```

so that whoever holds the claim, the fact that SES accepted the message is
written down. That changes the queue's state machine — a row can then move to
`'sent'` from a state the current model says it cannot — and it interacts with
the retry and abandonment paths. It was deliberately kept out of `#0254`'s scope
rather than folded into a bounce fix.

## Acceptance criteria

1. Decide between recording the send unconditionally and keeping the strict
   `status='sending'` guard, and record the decision with its reasoning in the
   code, not only in this issue.
2. If unconditional: `MarkSent` must still be safe against a row that legitimately
   moved on — a row already `abandoned` must not silently become `sent` without
   that being a deliberate, tested choice. Enumerate every source state the new
   predicate admits and say what each means.
3. The retry and abandonment paths (`MarkRetryOrAbandon`) are re-checked against
   the new predicate. `#0272` is open on adjacent `MarkSent` behaviour — read it
   and sequence the two rather than having both edit the same statement.
4. A DB-backed test drives the actual scenario: claim a row, release the claim
   underneath the sender, complete the send, and assert the row is not sent
   twice.
5. The `Warn` `#0254` added stays, whatever is decided. It is the signal that
   something took a claim, and that is worth knowing even when the outcome is
   handled.
6. `ISSUE=NNNN scripts/check.sh go ./internal/... ./cmd/opencircuit/...` passes
   with no skips.

## Relation

- Found by: [#0254](0254.md)'s review, which judged the `Warn` sufficient for
  that issue and this insufficient to leave unrecorded
- Relates to: [#0272](0272.md) — the other open issue on `MarkSent`'s statement
- Relates to: [#0126](0126.md) — which built the queue and its state machine

## Decision

**Widen the guard, but not all the way to fully unconditional.**
`MarkSent`'s `WHERE` clause now reads `status IN ('sending', 'queued')`
instead of `status = 'sending'` — recording the send whenever the row is
still in one of the two states a legitimate in-flight claim can leave it in,
and stopping short of the issue's example (`status <> 'sent'`), which would
also silently resurrect a row already `'abandoned'`.

Reasoning, in full, is written into `MarkSent`'s doc comment in
`internal/outbox/store.go` (not only here, per criterion 1) — summarized:

**Correction (post-review):** the paragraph originally here said "the call
site" (singular) and described only `OutboxWorker.sendOne`'s SES call. That
was false, and stated as verified fact — there are **two** call sites of
`Store.MarkSent`, and the second, `SubscribeHandler.processIntakeRow`
(`internal/handlers/subscribe_intake.go:250`), marks `KindSubscribeIntake`
rows, which are **not email and involve no SES call at all** (see that
Kind's own doc comment in `internal/outbox/store.go`). This decision widens
the predicate for both callers, since `MarkSent` cannot distinguish which
caller invoked it — so both are in scope below, not just the mail path.

- Both call sites always call `MarkSent` synchronously, immediately after an
  operation that genuinely completed for that row — a successful SES send
  for the mail kinds, or a finished `dispatchMutation` call for
  `KindSubscribeIntake` — so every invocation follows a real, completed
  operation, never a stale or speculative one. The only open question is
  what status the row is allowed to be in when it runs.
- `'sending'` — the expected case, unchanged.
- `'queued'` — newly admitted. This is the exact race #0283 describes: the
  claim was released or stolen back to `'queued'` while the operation that
  just completed was still in flight (`Release`, or a same-kind
  `OrphanSweep` racing a still-live claim — the residual, intra-kind version
  of `#0254`'s now-fixed cross-kind race). For the mail kinds, SES already
  has the message; recording `sent` here is what stops a second pass from
  claiming and sending it again. For `KindSubscribeIntake`, `dispatchMutation`
  already ran to completion; recording `sent` here stops a second pass from
  reprocessing the same row. That reprocessing would in fact be **safe** to
  repeat, not merely wasteful — verified directly against the code, not
  assumed: `processIntakeRow` re-reads `IsSuppressed`/`FindByEmail` fresh
  rather than trusting anything captured earlier, and `dispatchMutation` is
  idempotent by construction (`newSignup`'s `ErrEmailExists` handling makes
  a raced `Create` a safe no-op; `ClaimAndEnqueueConfirmation`/
  `ClaimAndEnqueueAlreadySubscribed` are atomic, cooldown-gated claims where
  a second send attempt is already a required no-op for ordinary concurrent
  requests). Admitting `'queued'` still avoids the redundant work of
  re-running a dispatch whose outcome is already durable.
- `'sent'` — excluded. A second `MarkSent` call for the same id must stay a
  no-op (the existing `TestOutbox_MarkSent_ScrubsPayload` idempotency
  assertion), not re-stamp `sent_at`/`ses_message_id`. For
  `KindSubscribeIntake`, `'sent'` here means "the signup attempt was durably
  processed", not "delivered by SES" — but the exclusion is identical
  either way: a second call for an already-processed row must stay a no-op
  regardless of what `'sent'` means for that kind.
- `'abandoned'` — excluded, deliberately. A row here already reached
  `MarkRetryOrAbandon`'s terminal state, keeping its error/payload as the
  diagnostic. Silently flipping that to `'sent'` would erase a decision an
  operator can currently see. Criterion 2 is explicit that resurrecting an
  abandoned row must be a deliberate, tested choice, not a side effect of a
  broader predicate — and `#0254`'s reviewer already established that the
  proven cause of the stolen-claim race (cross-kind sweep) is now
  structurally impossible, so admitting `'abandoned'` would be buying
  defence against a scenario nobody can currently produce, at the cost of a
  real, visible information loss. Left excluded; revisit only alongside a
  concrete scenario that reaches it.
- `'failed'` — reserved, unused by any writer in this codebase; excluded for
  the same reason as `'abandoned'`.
- The `Warn` `#0254` added when `MarkSent` affects zero rows is unchanged
  (criterion 5) — it still fires for the cases the new predicate excludes
  (`'sent'`, `'abandoned'`), and remains the signal that something took a
  claim even though the widened predicate now handles the one case
  (`'queued'`) that used to produce a silent duplicate send.

`MarkRetryOrAbandon`'s own `WHERE status = 'sending'` clause is unchanged
(criterion 3) — its doc comment now records why the two methods cannot
productively race each other: a given claim's lifecycle calls at most one of
`MarkSent` or `MarkRetryOrAbandon`, and in the only cross-claim interaction
(a stolen-claim race), whichever of the two commits first correctly
determines what the other can still do to the row.

## Files changed

- `internal/outbox/store.go` — `MarkSent`'s `WHERE` clause widened to
  `status IN ('sending', 'queued')`; extensive doc comment recording the
  decision and source-state enumeration; `MarkRetryOrAbandon`'s doc comment
  extended with the "re-checked against the new predicate" note.
- `internal/outbox/store_test.go` — added
  `TestOutbox_MarkSent_RecordsSendAfterClaimReleasedMidSend` and
  `TestOutbox_MarkSent_DoesNotResurrectAbandonedRow`.

**Post-review pass:** corrected `MarkSent`'s doc comment (`store.go`) to name
both call sites — `OutboxWorker.sendOne` (SES) and
`SubscribeHandler.processIntakeRow` (`internal/handlers/subscribe_intake.go`,
no SES call) — restated the shared premise accurately ("an operation that
genuinely completed", not "a real SES call"), and extended the `'queued'`
and `'sent'` bullets to say what each means for `KindSubscribeIntake` and why
admitting `'queued'` is safe there (fresh re-reads + idempotent
`dispatchMutation`, verified against `subscribe.go`/`subscribe_intake.go`
directly). Updated this `## Decision` section to match. No behavior change
— comments only.

## Verification

1. Decision recorded above and, per criterion 1, in `MarkSent`'s doc comment
   in `internal/outbox/store.go` — not only in this issue file.
2. Every source state the new predicate admits or excludes is enumerated in
   that same doc comment (`sending`, `queued`, `sent`, `abandoned`,
   `failed`), with what each means. New test
   `TestOutbox_MarkSent_DoesNotResurrectAbandonedRow` proves an abandoned
   row is not silently resurrected: `MarkSent` on it returns `done=false`
   and the row's `status`, `error`, and `ses_message_id` are all unchanged.
3. `MarkRetryOrAbandon` re-checked against the new predicate — its own
   `WHERE` clause is untouched (no hunk inside it), and its doc comment now
   states the reasoning for why the two methods' predicates do not conflict.
   `#0272` (adjacent `MarkSent` behaviour) is already `resolved`, so there
   was nothing to sequence against — its `error = NULL` clause is preserved
   unchanged in the new `SET` list.
4. New test `TestOutbox_MarkSent_RecordsSendAfterClaimReleasedMidSend`
   drives the actual scenario: claims a row, releases the claim underneath
   the sender (`store.Release`, simulating the mid-send race), completes the
   send (`MarkSent`), and asserts the row is recorded `'sent'` — then forces
   it due again and asserts a subsequent `ClaimDue` does NOT reclaim it
   (not sent twice).
5. The `#0254` `Warn` in `OutboxWorker.sendOne` (`internal/mailing/outbox_worker.go`)
   is untouched — no hunk inside `internal/mailing` at all for this issue.
6. `ISSUE=0288 scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
   ./internal/handlers/... ./cmd/opencircuit/...` — all four packages `ok`,
   zero skips (`internal/outbox` 0.353s, DB-backed). `gofmt -l` clean,
   `go vet` clean, `go build` clean. Scratch database dropped on exit; no
   leftover `go test`/`.test` processes of this run's own.

Implemented together with `#0288` and `#0285` in one batch (all three touch
`internal/outbox/store.go`/`store_test.go`); the single `check.sh` run above
(issued under `#0288`'s scratch database) covers all three.

## Review notes (first pass — bounced)

Independent phase-3 review of `1cf190d`, 2026-08-27. **Bounced — not for
behaviour, which I could not break, but for the reasoning the issue's own
criterion 1 makes the deliverable.** The code commit stays in place; the next
pass is a comment correction, not a code change.

Status left at `in-progress` (rather than `Issues.md`'s `open`) on the
orchestrator's explicit instruction for this dispatch.

### What I verified, and what held

`ISSUE=0283r scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
./internal/handlers/... ./cmd/opencircuit/...` — `VERIFICATION PASSED`, all
four packages `ok`, skip audit `(no skips)`, `gofmt`/`vet`/`build` clean,
`uptime` load average 2.87. Both new tests run by name and observed passing.

**Mutation proof.** In a throwaway worktree detached at `1cf190d`, `MarkSent`'s
predicate was reverted to `status = $4` (`'sending'` only).
`TestOutbox_MarkSent_RecordsSendAfterClaimReleasedMidSend` then failed with
`MarkSent reported done=false — the send would be silently lost and the row
re-sent later`, and passes with the fix. It is a genuine guard.
`TestOutbox_MarkSent_DoesNotResurrectAbandonedRow` correctly passes both ways —
it guards the exclusion, which is what criterion 2 asks for.

**I attacked the newly-admitted `queued` state and found no defect.** Taking
the scenario directly: a send fails, `MarkRetryOrAbandon` re-queues the row for
a legitimate retry, and a stale in-flight sender from an earlier claim then
calls `MarkSent` on it. That sender's own SES call **succeeded** — the message
reached the recipient — so recording `'sent'` is correct and the retry it
suppresses would have been the duplicate this subsystem exists to prevent. The
same holds in reverse: if the stale sender had failed, it would call
`MarkRetryOrAbandon`, not `MarkSent`. A given claim's lifecycle calls at most
one of the two, and `MarkRetryOrAbandon`'s doc-comment argument to that effect
is sound — I checked it against the code rather than accepting it. Its `'queued'`
branch and its `'abandoned'` branch are both covered by that comment, and both
statements are true.

**The `'abandoned'` exclusion is the right call and I endorse it.** Its one cost
is real and worth stating: a row that was in fact delivered can end up reading
`'abandoned'` (slow successful send + a second actor exhausting retries at the
same instant). But that requires `attempts >= maxRetries` to land in exactly
that window, it preserves an operator-visible terminal decision rather than
silently erasing it, and the `#0254` `Warn` still fires. Resurrecting it would
buy defence against a scenario nobody can currently produce at the cost of real
information loss. Correct as decided.

**`MarkDone` and `MarkSent` both moving `queued → sent` is fine.** They have
disjoint callers (the fast path in `subscribe.go` vs. the recovery poller), each
records an operation that genuinely completed, and whichever lands first
excludes the other through the `'sent'` exclusion. No stall, no double-stamp, no
interaction with `#0254`'s recovery poller that leaves a row unreachable.

`claimed_at` is not cleared by `MarkSent`, but a `'queued'` row always has it
`NULL` already (every writer that produces `'queued'` — `Release`,
`MarkRetryOrAbandon`, `OrphanSweep` — nulls it), so the widening introduces no
new stale-`claimed_at` case. `next_attempt_at` is likewise left in the future on
a row that goes `queued → sent`; harmless, since `ClaimDue` filters on
`status = 'queued'`.

### What must change

**`MarkSent`'s doc comment states a false premise, and the whole enumeration
rests on it.** It opens:

> The call site (OutboxWorker.sendOne) always calls MarkSent synchronously,
> immediately after a real SES call that just succeeded for THIS row — so every
> invocation follows a genuine send, never a stale or speculative one.

There are **two** call sites of `outbox.Store.MarkSent`, not one:

- `internal/mailing/outbox_worker.go:486` — `OutboxWorker.sendOne`, the one the
  comment describes.
- `internal/handlers/subscribe_intake.go:250` —
  `SubscribeHandler.processIntakeRow`, the `#0254` recovery poller. It calls
  `MarkSent` for `outbox.KindSubscribeIntake`, **which is not email and involves
  no SES call at all**. `KindSubscribeIntake`'s own doc comment, twelve lines up
  the same file being edited, says so explicitly: "exactly reusing
  ClaimDue/OrphanSweep/MarkSent and nothing else", and "'sent' for a row of this
  kind means 'the signup attempt was durably processed', not that any mail went
  out."

This matters for three reasons, in ascending order:

1. It is simply wrong on a fact discoverable in the file under edit, and it is
   asserted as verified ("always", "every invocation"). `CLAUDE.md` is explicit
   that a comment asserting an unverified invariant is `#0263`'s and `#0095`'s
   defect class.
2. Criterion 2 asks what admitting each source state **means**. For the intake
   caller `'sent'` means something different — "durably processed" — so
   admitting `'queued'` there has a second, unstated meaning the enumeration
   never gives. (I checked it: it is also safe, because `processIntakeRow`
   re-reads everything fresh and `dispatchMutation` is idempotent by
   construction — but that is the argument the comment needed to make and did
   not.)
3. The premise is the *safety* argument for admitting `'queued'`. A future
   author who adds a third `MarkSent` caller that does **not** follow a
   completed operation would read this comment as licence.

Nothing outside `internal/outbox` mentions that this change alters the intake
path's semantics too — not the comment, not the commit message, not this issue's
`## Decision`.

**Required for the next pass** (comment-and-prose only; no code change is
needed, and none should be made):

- Correct `MarkSent`'s doc comment to name **both** call sites, and restate the
  premise accurately: every invocation follows an operation that genuinely
  completed for this row — a successful `mailer.Send` for the mail kinds, or a
  completed `dispatchMutation` for `KindSubscribeIntake` — rather than "a real
  SES call".
- In the `'queued'` bullet, say what admitting it means for **both** kinds, and
  record why it is safe for the intake poller (fresh re-reads + idempotent
  dispatch, per `subscribe_intake.go`'s own package doc comment).
- Update this issue's `## Decision` to match, so the issue file and the code do
  not disagree.

Re-run `ISSUE=NNNN scripts/check.sh go ./internal/outbox/...
./internal/mailing/... ./internal/handlers/... ./cmd/opencircuit/...` after the
edit — a doc-comment change to an exported declaration is exactly the shape
`gofmt` rewrites (`CLAUDE.md` §8), so it must be re-checked, not assumed clean.

## Review notes (second pass — approved)

Independent phase-3 re-review of `62c475f` (on top of `1cf190d`, which stays),
2026-08-27. **Approved.** Scoped to the corrected reasoning, per the
orchestrator's instruction — the predicate, the `'abandoned'` exclusion and the
stale-sender race were settled in the first pass and are not re-litigated here.

### The change is comment-only, which I verified rather than assumed

```
git diff 1cf190d 62c475f -- internal/outbox/store.go | grep '^[+-]' | grep -v '^[+-][+-]' | grep -v '^[+-]//'
→ (empty)
```

Every changed line in `store.go` is a comment line. The SQL, the parameter
list and the `RowsAffected` handling are byte-identical to what the first pass
mutation-proved, so that verification still stands unmodified.

`#0288`'s parenthesis fix is the only `#0288` change — the single hunk at
`MarkDone`'s doc comment moving `)` from after "convergence" to after
"MarkSent" — and `issues/0288.md` is not among the four files in `62c475f`.
Confirmed at `git show --stat`.

### The load-bearing check: is the intake idempotency argument circular?

It is not. I read the three cited mechanisms at the source rather than taking
the summary, and each one exists for reasons that have nothing to do with this
predicate:

- **`newSignup`'s `ErrEmailExists` no-op** (`internal/handlers/subscribe.go`)
  — its own comment says it covers "a double-submit or two tabs" losing a race
  between `FindByEmail` and `Create`. Introduced by `#0026` in `c0cff5d`.
- **`ClaimAndEnqueueConfirmation`** — `sendConfirmation`'s doc comment states
  the claim "is what enforces PRD §6.3's once-per-hour resend limit AND what
  makes concurrent double-submits of the same address send at most once", and
  cites `#0026`'s review finding 3, which traced a real duplicate-send bug to
  the resend branch having its own separately-timed cooldown instead of this
  one atomic claim. Introduced by `#0126` in `bace190`.
- **`ClaimAndEnqueueAlreadySubscribed`** — `sendAlreadySubscribed`'s comment
  records that `#0026`'s review measured 20 sequential submits of one active
  address producing 20 emails before this claim existed: "an unauthenticated
  mail-amplification vector, and half of a two-probe enumeration oracle."
  Also `bace190`.

All three predate `1cf190d`, are required by PRD §6.3 and by anti-abuse
constraints that stand entirely independently of `outbox.Store`, and none of
them reads or depends on `MarkSent`'s status predicate. So the argument runs
one way only: an independently-required idempotency property makes admitting
`'queued'` safe for the intake kind. Not circular.

I also checked the weaker half of the claim — that `processIntakeRow` re-reads
`IsSuppressed`/`FindByEmail` fresh rather than trusting the payload. It does,
and `subscribe_intake.go`'s package doc comment ("Why reprocessing is safe even
if it races the fast path") already said so before this change.

One precision point worth recording, since the comment gets it right and could
easily have got it wrong: `dispatchMutation` has several deliberate no-op
branches (`isBot`, reserved test email, suppressed, `complained`). The comment
claims only that it **ran to completion**, not that it succeeded — which
matches `processIntakeRow`'s and `markIntakeDone`'s existing "processed, not
necessarily succeeded" convention. Accurate as written.

### The corrected comment

Both call sites are now named with the right locations, and the shared premise
is restated as "an operation that genuinely completed for this row — a
successful mailer.Send for the email kinds, or a finished dispatchMutation call
for KindSubscribeIntake", which is true of both. The `'queued'` and `'sent'`
bullets each say what they mean for the intake kind. `## Decision` in this file
was updated to match, and carries an explicit "Correction (post-review)" note
rather than quietly rewriting history. No new claim in either the comment or
the issue prose required disproving.

### Re-verification

`ISSUE=0283rr scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
./internal/handlers/... ./cmd/opencircuit/...` — `VERIFICATION PASSED`, four
packages `ok`, skip audit `(no skips)`, `gofmt -l` clean, `go vet` clean.
`uptime` load average 1.40, comfortably idle.

**`gofmt` stability checked myself, on copies, not the shared tree** — this is
a doc-comment change and `CLAUDE.md` §8 names that as exactly what `gofmt`
rewrites. Both files were copied to a scratch directory, `gofmt -w` run twice,
and the results compared byte-for-byte against the originals:

```
93ae5383…  store.go.orig      93ae5383…  store.go       (identical)
74167f28…  store_test.go.orig 74167f28…  store_test.go  (identical)
```

`gofmt` is a fixed point on both. No smart-quote rewrite is latent.

### Note

`#0285` shares this commit and was **bounced a second time** — the same
"concurrently-running" claim it was bounced for survives verbatim in nine
per-test comments in `internal/outbox/store_test.go`, including two on this
issue's own tests (`TestOutbox_MarkSent_RecordsSendAfterClaimReleasedMidSend`,
`TestOutbox_MarkSent_DoesNotResurrectAbandonedRow`). Those comments are about
kind-scoping, which is `#0285`'s subject and is tracked there; nothing in them
concerns this issue's predicate, and `#0283`'s own artifacts in `store.go` are
correct and complete.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-27 | implement | claude-sonnet-5 | — | — | — | — | — |
| 2026-08-27 | implement (re-pass, review bounce) | claude-sonnet-5 | — | — | — | — | — |

Re-pass fixed the false "one call site" premise in `MarkSent`'s doc comment
and this issue's `## Decision` — named both `OutboxWorker.sendOne` and
`SubscribeHandler.processIntakeRow`, and completed the argument for why
`KindSubscribeIntake` is safe under the widened `'queued'` admission
(fresh re-reads + idempotent `dispatchMutation`, verified directly against
`subscribe.go`/`subscribe_intake.go` rather than asserted). No behavior
change. Also closed an unclosed parenthesis in `MarkDone`'s doc comment
(`#0288`, cosmetic-only, per that pass's instruction). Verified with
`ISSUE=0283b scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
./internal/handlers/... ./cmd/opencircuit/...`.

Token/cost columns left for the orchestrator to fill from this subagent's
transcript (this subagent has no transcript access to its own usage).
