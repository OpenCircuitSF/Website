# 0330 — The claim-kinds floor counts call sites instead of asserting the one that matters

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-28 |
| **Closed** | 2026-08-31 |
| **Commit** | `98d9ee0` |

> 🟢 Resolved 2026-08-31 — the presence check is live and load-bearing (proved
> by negative control, not inherited), and the comments that shipped alongside
> it now claim exactly what the mechanism delivers: I reproduced all three
> defeating mutations myself and the corrected text is honest about each,
> without understating what items 2 and 4 genuinely catch.

## Description

`#0322` raised `claimKindsGuardMinPlausibleNonExemptCallSiteCount` from 6 to 8,
closing the specific gap it was filed for: a `mailing`-only scan-roots
narrowing yields 7 non-exempt call sites, which cleared a floor of 6 while
silently dropping `internal/handlers/subscribe_intake.go` — the caller whose
loss produced `#0254`'s duplicate-send defect.

That fix is correct for the narrowing it names. It does not close the class.

A **count** is a proxy for the property actually wanted, which is
**presence**: that `subscribe_intake.go`'s `ClaimDue`/`OrphanSweep` call sites
are in the scanned population. Any narrowing that happens to leave 8 or more
non-exempt sites standing still passes the floor while dropping the one site
the guard exists to protect. The floor gets stronger only by being raised, and
each raise costs headroom against ordinary growth.

`#0322`'s review measured that headroom and found there is **none**. Four of
the 12 non-exempt sites — not three, as `#0304` stated and `#0322` copied — are
the unrelated same-named `mailing.SendStore.OrphanSweep`, the fourth being
`internal/mailing/worker.go:575`. Renaming it leaves **8**: the floor sitting
exactly at its own population. Put the other way, 8 is precisely the count of
genuine non-collision outbox call sites outside the package, so **the floor's
entire margin is currently supplied by a name collision.**

The growth direction is worse than the shrink direction, and the review
demonstrated it: adding one ordinary, correctly-scoped
`ClaimDue` call to `internal/mailing` takes that package to 8, and the
mailing-only narrowing passes again — `go test` green, harness green,
population merely grown to 13. The shrink direction at least fails loudly. The
growth direction expires **silently**, one caller wide.

A guard that trips on an unrelated rename is a maintenance trap; a guard that
passes while dropping its subject is worse; this one now does both.

`#0275` is the opposite failure mode already on record — a floor above its
population — and the two together are the argument that counting is the wrong
instrument here.

## Root cause

`claimKindsGuardMinPlausibleNonExemptCallSiteCount` (`#0322`, currently 8) is a
count over the non-exempt population with no memory of *which* sites it
counted. `#0322`'s own doc comment already demonstrates the exploit: narrowing
`claimKindsGuardScanRoots` to `internal/mailing` alone plus adding one ordinary,
correctly-scoped call anywhere in `internal/mailing` reaches 8 non-exempt sites
again, silently, with `internal/handlers/subscribe_intake.go` — the caller
`#0254`'s duplicate-send defect came from — dropped from the walk entirely. A
count can never distinguish that state from a healthy one.

## Fix

Added a presence check, `claimKindsGuardRequiredNonExemptCallSites` (a named
`[]namedCallSite{ {file, method}, ... }` list) plus a loop in
`TestNoUnscopedOutboxClaimCallOutsidePackage` that asserts, by name, that each
required `(file, method)` pair is present among the real, non-exempt walk
results — not merely that the walk found "enough" sites. Retained the existing
count floor (`claimKindsGuardMinPlausibleNonExemptCallSiteCount`) alongside it,
per acceptance criterion 5: it is the coarse "did the walk even run" backstop
(an empty `allSites` slice makes every presence lookup report "not found" for
the same reason a broken walk would, so the presence check alone cannot
distinguish "walk broken" from "walk fine, but this one file dropped" without
the floor's help), while the presence check is the precise "did it find the
ONE caller that matters" check a count structurally cannot express.

**Oracle placement (criterion 3) — deliberate, not by convention.** Per
`CLAUDE.md` §8's own distinguishing question — does the mutation change what
the walk *finds*, or only the threshold the result is compared against —
this split into two different homes:

- **In Go, in-package** (`TestPresenceCheckCatchesMailingOnlyNarrowing`, a
  permanent standing test alongside `TestNonExemptFloorCatchesScanRootsNarrowedToSelf`):
  narrowing `claimKindsGuardScanRoots` changes `got` itself — the walk
  genuinely finds different files — so an in-package `go test` is a
  legitimate, non-circular oracle for that mutation class, exactly as the
  existing `{"."}`-narrowing test already is for the count floor. Proved
  directly (not merely asserted) in a throwaway `git worktree`: narrowing to
  `{"../mailing"}` plus one added, correctly-scoped call reaches 8 non-exempt
  sites (satisfying the OLD count floor) while `subscribe_intake.go` is
  dropped — and the new presence check, not the floor, is what fails,
  confirming it closes a gap the floor alone does not. See `## Verification`.
- **External harness, `scripts/go_file_visit_floor_guard_test.sh`** (new
  `#0330` section, textual/grep-based, never parses Go): the mutation class an
  in-package test *cannot* prove anything about is an edit to
  `claim_kinds_call_site_guard_test.go` itself — weakening or emptying
  `claimKindsGuardRequiredNonExemptCallSites`, or the loop that checks it —
  since that is an edit to the guard's own subject, and an assertion living
  entirely inside that file can be satisfied by editing the same file. This
  section (a) independently confirms via `grep` on the real
  `internal/handlers/subscribe_intake.go` production source (never the guard,
  never go/ast) that the `OrphanSweep`/`SelectDue` calls it claims are
  present really exist; (b) confirms the tracked guard file's required-list
  literally names both, via an anchored line-shape grep that cannot also
  match prose describing the same file; (c) mutates a private copy by
  rewriting every occurrence of `subscribe_intake.go` to a name the same
  anchored grep cannot match, and confirms the extraction drops to zero for
  both methods, proving the check is falsifiable; (d) ~~confirms the tracked
  file's real assertion — the loop inside
  `TestNoUnscopedOutboxClaimCallOutsidePackage` — still calls
  `requiredNonExemptSitePresent(allSites, ...)` **exactly once**~~
  **corrected 2026-08-31 (#0330, third implementation pass):** confirms the
  tracked *file* contains `requiredNonExemptSitePresent(allSites, ...)` on
  exactly one line — this catches outright deletion of the loop that reads
  the list, but it does **not** establish that the call is live: a grep
  over the whole file cannot tell whether it sits inside
  `TestNoUnscopedOutboxClaimCallOutsidePackage` or is reachable. The second
  review defeated this exact claim twice while holding the count at
  exactly 1 (M-N1: extracting the block into an uncalled helper function;
  M-N2: appending `[:0]` to the loop's range expression) — see
  `## Review notes — second pass`, "D2"
  (`CLAUDE.md` §8, `GUARD-0208`'s marker-block rule), so deleting that loop
  while leaving the list itself untouched — a class (b) alone cannot see,
  since it never reads anything but the list's own bytes — is
  independently caught too. This is the same shape
  `scripts/go_file_visit_floor_guard_test.sh` already uses
  for the three `#0300` floors and the two `#0304`/`#0322` outbox floors —
  reused rather than reinvented, since `extract_const`'s helpers (`sha_of`,
  `fatal`/`fail`/`pass`) already exist in that file.

**Second pass, addressing the review bounce (`a764508`) — transcription of
the three remedies it supplied verbatim, per `CLAUDE.md` §3's "the next pass
is transcription, not design" call.** Each remedy was independently
reproduced before and after in a throwaway `git worktree`
(`/tmp/oc-0330b-mut`, never the shared tree), including negative controls,
before transcribing it into the shared tree:

- **R1 — the loop-deletion class.** Reproduced exactly: deleting the
  presence-check loop from `TestNoUnscopedOutboxClaimCallOutsidePackage`
  while leaving `claimKindsGuardRequiredNonExemptCallSites` itself intact
  left both `go test` (`PASS`, the var stays referenced by
  `TestPresenceCheckCatchesMailingOnlyNarrowing`) and the harness (`all
  guards hold (0 failures)`) green. Applied the reviewer's exact remedy — a
  new `#0330` harness subsection asserting
  `requiredNonExemptSitePresent(allSites,` appears in the tracked file
  exactly once, mutated by renaming the callee via `sed` (a pattern
  different from the detector's own `grep`) — and re-ran the same deletion:
  `go test` stays green (unchanged, as expected) but the harness now
  reports `FAIL: REGRESSION #0330: … calls
  requiredNonExemptSitePresent(allSites, ...) 0 time(s), want exactly 1`.
  The negative control (a deliberately drifted `sed` callee name that
  doesn't match) leaves the mutant's count at 1, which is itself a `FAIL`
  ("the presence-loop mutation did not take"), proving the mutation is
  genuinely falsifiable rather than vacuous.
- **R2 — the tautological required-list mutation.** Reproduced exactly via
  the reviewer's own M2 (replacing the required list with a present,
  irrelevant decoy `{"internal/handlers/admin_pending_test.go",
  "ClaimDue"}`): the tracked-list check (b) correctly `FAIL`ed twice, while
  the old `grep -v` mutation-sensitivity check (c) still printed `PASS`
  twice — vacuously, since the `subscribe_intake.go` lines the mutation
  claims to remove were already absent before it ran. Applied the
  reviewer's exact remedy (`sed
  's|subscribe_intake\.go|zzz_required_site_removed_by_mutation.go|g'` in
  place of the `grep -vE` alternation-based deletion) and re-ran against the
  **real** (non-decoy) list: the fixed mutation still correctly drops both
  methods' counts to 0 (`PASS`), and the negative control (a drifted
  `sed 's|subscribe_intake_XX\.go|zzz.go|g'` anchor that cannot match) now
  reports `FAIL: … did not take (still found 1 … line(s))` for both
  methods, rather than a vacuous `PASS` — ~~proving this mutation is
  genuinely sensitive to the list's content, unlike its predecessor.~~
  **corrected 2026-08-31 (#0330, third implementation pass):** the `sed`
  form is sensitive to a *drifted anchor*, not to the list's content, and
  is equivalent to its predecessor on the M2 case the first review used to
  condemn it — the second review measured this directly: replaying M2 (a
  present-but-irrelevant decoy required list) against the `sed` form
  reproduces the exact output the first review condemned (item 2 `FAIL`s
  twice, item 3 `PASS`es twice), because the detector's pattern embeds
  `REQUIRED_SITE_FILE`, so `MUT_N` is 0 by construction regardless of the
  list's content (see `## Review notes — second pass`, "D1").
- **R3 — criterion 5's headroom paragraph.** Cross-checked the replacement
  paragraph's arithmetic against this pass's own re-measurement (harness:
  42 total / 12 non-exempt, unchanged) and against `go test`'s live output:
  floor 8 against population 12 trips on the loss of any **five**; mailing
  at 0 alone already trips it (5 < 8) with handlers untouched, so the old
  paragraph's `combined with internal/handlers dropping below 3` conjunct
  was never load-bearing; the old paragraph's own parenthetical (deleting
  `outbox_worker.go`'s two calls and `worker_store_test.go`'s three) leaves
  mailing at exactly 2 (not "below 1") and the total at 7 — already under
  the floor. All four objections the review raised check out against this
  pass's own numbers; the replacement paragraph transcribed verbatim.

**Criterion 4 (fail closed).** The presence loop's default is "not found";
an empty `claimKindsGuardRequiredNonExemptCallSites` fails closed explicitly
(`t.Fatal`, mirroring the existing `claimKindsGuardScanRoots`-empty check).
The harness's ground-truth grep and required-list extraction both call
`fatal` on a non-numeric result, matching every other extraction in that
script.

**Criterion 6 (no population inflation).** The new Go identifiers use bare
string literals (`"OrphanSweep"`, `"SelectDue"`) with no leading `.` or
trailing `(`, so `scripts/go_file_visit_floor_guard_test.sh`'s
`count_outbox_call_sites` grep pattern (`\.(ClaimDue|OrphanSweep|SelectDue)\(`)
does not match them. Confirmed by re-running the harness after the change:
total population still 42, non-exempt still 12, both unchanged. Also
confirmed by grepping the diff's added (`+`) lines directly for the call
syntax — zero hits.

**Third pass, addressing the second review bounce (`c86f69e`) — comment-only,
transcription of the four remedies it supplied verbatim (F1–F4), adding no
mechanism, per the reviewer's own explicit judgement in "The recursion
question" that the terminating move here is deleting an overclaim, not
adding a fifth check.** No Go file changed at all; `internal/outbox/claim_kinds_call_site_guard_test.go`
stays byte-identical to `22f021f`
(sha `95193675a31156614410e3a9a4a05a7c3b4ea422091c295d5f39340e675f36d6`),
confirmed both before and after this pass.

- **F1 — replaced header bullet `3.`** in the `#0330` section of
  `scripts/go_file_visit_floor_guard_test.sh` with the reviewer's exact
  text: states plainly that the item 3 mutation proves the detector *can*
  report absent, not that it is sensitive to the required list's content,
  and explains why (the detector's pattern embeds `REQUIRED_SITE_FILE`, so
  `MUT_N` is 0 by construction for any file content).
- **F2 — replaced header bullet `4.`** with the reviewer's exact text:
  states the exactly-once check proves the assertion has not been deleted
  outright and nothing more, names both defeating mutations (M-N1, M-N2)
  verbatim, and explicitly instructs against adding a fifth textual check.
- **F3 — three runtime message corrections**, applied verbatim: the
  `count_presence_loop_call` comment's final sentence, item 4's `pass`
  line, and item 3's `pass` line (inside the `for METHOD in
  $REQUIRED_METHODS` mutant loop). Item 4's `LOOP_MUTANT` `pass` line was
  trimmed of the "DIFFERENT pattern from the detector" clause per the
  reviewer's instruction — the same false distinction D1 identifies for
  R2's item-3 mutation — while keeping the rest of the sentence intact.
- **F4 — this issue's own `## Fix`**, corrected using the same
  strike-through-plus-dated-note convention already established above for
  criterion 1 (`issues/Issues.md`'s "Correcting text that later events made
  wrong"): bullet `(d)`'s overclaim and the second-pass R2 bullet's
  conclusion are each struck verbatim and followed by a
  `**corrected 2026-08-31 (#0330, third implementation pass).**` note
  restating what D1/D2 actually established. Both struck spans verified
  programmatically (`~~` stripped) to be byte-identical to the
  pre-correction text, not paraphrases.

Every replacement byte matches the reviewer's supplied text exactly; no
independent wording was substituted for F1–F3, and F4's corrections restate
what D1/D2 measured, not a new claim.

## Verification

```
uptime                                                              # 1.80–2.72, idle
go build ./internal/outbox/...  &&  go vet ./internal/outbox/...    # clean
gofmt -l internal/outbox/claim_kinds_call_site_guard_test.go        # clean; sha256(file) == sha256(gofmt stdout)
go test ./internal/outbox/... -run 'TestNoUnscopedOutboxClaimCallOutsidePackage|TestPresenceCheckCatchesMailingOnlyNarrowing|TestNonExemptFloorCatchesScanRootsNarrowedToSelf|TestClaimKindsGuardFiresOnFixtureWithNoKinds' -v
ISSUE=0330 scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
scripts/check.sh guards                                             # run twice
```

- `go build`/`go vet`/`gofmt -l` clean on the touched file.
- Named tests all `PASS`; `ISSUE=0330 scripts/check.sh go ...` — `internal/outbox`,
  `internal/mailing`, `internal/handlers` all `ok`, zero skips.
- `scripts/check.sh guards` — `VERIFICATION PASSED` on **two consecutive
  runs**, including the new `#0330` section of
  `go_file_visit_floor_guard_test.sh`, and both floor sections unchanged
  (population 42 total / 12 non-exempt, matching `#0345`'s baseline exactly).
- **Criterion 2, mutation-tested in a throwaway `git worktree`** (`CLAUDE.md`
  §8a — never the shared tree), own copy of the modified guard file:
  narrowing `claimKindsGuardScanRoots` to `{"../mailing"}` alone (`#0322`'s
  own evading narrowing) drops `TestNoUnscopedOutboxClaimCallOutsidePackage`
  at the **count floor** (7 < 8) — expected, the pre-`#0330` behavior. Adding
  one further syntactically-valid, correctly-scoped guarded call inside a
  scratch `internal/mailing/zzz_scratch_extra_call_0330.go` (deleted before
  the worktree was removed) brings the non-exempt count to exactly 8,
  satisfying the count floor — and at that point the failure shifts to the
  **new presence check** (`required non-exempt call site missing:
  internal/handlers/subscribe_intake.go's OrphanSweep call was not found`),
  proving the presence check catches a gap the count floor alone passes.
  Worktree removed afterward (`git worktree remove --force`); the `.bak`
  produced by the in-worktree `sed` mutation was restored before removal;
  nothing under version control in the main tree was touched — confirmed
  with `git status --porcelain` before and after (only the two files this
  issue edits were ever dirty in the main tree).
- Own database `opencircuit_test_0330`, dropped by `scripts/check.sh`'s exit
  cleanup (confirmed absent via `scripts/testdb.sh drop 0330` → "does not
  exist"). No leftover `go test`/`.test` processes (`pgrep -f 'go
  test|\.test '` — none).

### Second-pass verification (remedies R1–R3)

```
uptime                                                              # 3.43–1.54, idle
go build ./internal/outbox/... && go vet ./internal/outbox/...      # clean
gofmt -l internal/outbox/claim_kinds_call_site_guard_test.go        # clean; sha256(file) == sha256(gofmt stdout) == 95193675a31156614410e3a9a4a05a7c3b4ea422091c295d5f39340e675f36d6
go test ./internal/outbox/... -run 'TestNoUnscopedOutboxClaimCallOutsidePackage|TestPresenceCheckCatchesMailingOnlyNarrowing|TestNonExemptFloorCatchesScanRootsNarrowedToSelf|TestClaimKindsGuardFiresOnFixtureWithNoKinds' -v
ISSUE=0330b scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
scripts/check.sh guards                                             # run twice
bash -n scripts/go_file_visit_floor_guard_test.sh
shellcheck -S warning scripts/go_file_visit_floor_guard_test.sh
```

- `go build`/`go vet`/`gofmt -l` clean; the doc-comment rewrite (R3) contains
  no doubled `'` or doubled backtick, and its sha is stable through `gofmt`.
- All 4 named tests `PASS`. `ISSUE=0330b scripts/check.sh go …` —
  `internal/outbox` `ok` 0.295s, `internal/mailing` `ok` 3.687s,
  `internal/handlers` `ok` 7.799s, zero skips.
- `scripts/check.sh guards` — `VERIFICATION PASSED` on **two consecutive
  runs**; population unchanged (42 total / 12 non-exempt), including the
  new item 4 (`requiredNonExemptSitePresent(allSites,` count exactly 1) and
  the corrected item 3 (`sed`-based required-list mutation) in the `#0330`
  section.
- `bash -n` and `shellcheck -S warning` both clean on the harness; no
  bash-4 syntax present (`${var,,}`, `declare -A`, `mapfile`, `&>>` — none)
  and no heredocs, so the unquoted-heredoc backtick hazard doesn't apply.
- **R1 and R2 independently reproduced end to end**, before and after, in a
  throwaway `git worktree` at `/tmp/oc-0330b-mut` (`CLAUDE.md` §8a — never
  the shared tree; removed with `git worktree remove --force`), each
  restore hash-verified back to `ef740190af0f66f946ebe4bf8b63518eabcaf17e7cfea39267f10878dacf3f1e`
  (matching the reviewer's own baseline sha) before the next mutation.
  Details and exact FAIL/PASS text in `## Fix`'s "Second pass" note above.
  Both remedies' own negative controls were also reproduced and behaved as
  the reviewer specified (a drifted mutation anchor produces a genuine
  `FAIL`, not a vacuous `PASS`).
- **R3's arithmetic independently re-derived**, not merely trusted: floor 8,
  population 12 (this pass's own harness measurement, matching the
  reviewer's and the first pass's), trips on the loss of any five; the
  parenthetical example in the *old* paragraph was re-computed by hand
  (mailing 7 − 5 = 2, total 12 − 5 = 7) and matches the review's objection
  exactly. No disagreement with any of the three remedies as supplied —
  transcribed verbatim.
- Own database `opencircuit_test_0330b`, dropped by `scripts/check.sh`'s
  exit cleanup (confirmed absent via `scripts/testdb.sh drop 0330b` →
  "does not exist"). No leftover `go test`/`.test` processes. Worktree
  `/tmp/oc-0330b-mut` removed; `git status --porcelain` in the main tree
  showed only this pass's own two files dirty throughout.

### Third-pass verification (F1–F4, comment-only)

```
uptime                                                              # 6.53/9.25/6.28 at start, drained to ~5.9 before the scoped Go run
shasum -a 256 internal/outbox/claim_kinds_call_site_guard_test.go   # 95193675…f36d6, matches 22f021f before AND after this pass
bash -n scripts/go_file_visit_floor_guard_test.sh                   # clean
shellcheck -S warning scripts/go_file_visit_floor_guard_test.sh     # clean, exit 0
scripts/check.sh guards                                             # run twice
ISSUE=0330c scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
go build ./internal/outbox/... && go vet ./internal/outbox/...      # clean
gofmt -l internal/outbox/claim_kinds_call_site_guard_test.go        # clean (no output)
go test ./internal/outbox/... -run 'TestNoUnscopedOutboxClaimCallOutsidePackage|TestPresenceCheckCatchesMailingOnlyNarrowing|TestNonExemptFloorCatchesScanRootsNarrowedToSelf|TestClaimKindsGuardFiresOnFixtureWithNoKinds' -v
```

- `internal/outbox/claim_kinds_call_site_guard_test.go` is byte-identical to
  `22f021f` — `git status --porcelain` never showed it dirty this pass, and
  the sha matches before and after every other edit.
- `bash -n` and `shellcheck -S warning` both clean on the harness.
- `scripts/check.sh guards` — `VERIFICATION PASSED` on **two consecutive
  runs**; population unchanged (42 total / 12 non-exempt), the `#0330`
  section's four items still hold, all with the corrected `pass` message
  text visible in the output.
- `ISSUE=0330c scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` — all three packages `ok` (outbox 0.489s, mailing
  5.554s, handlers 10.470s), zero skips, `VERIFICATION PASSED`.
- All 4 named `go test` tests `PASS`.
- Own database `opencircuit_test_0330c`, dropped by `scripts/check.sh`'s
  exit cleanup (confirmed absent via `scripts/testdb.sh drop 0330c` →
  "does not exist"). No leftover `go test`/`.test` processes belonging to
  this pass (one `go test ./internal/seo/...` process was observed in the
  first `guards` run's "leftover processes" listing — not started by this
  pass; belongs to a concurrent session working `#0353` per the
  orchestrator's brief, and was gone by the second run without this pass
  touching it).

## Files changed

- `internal/outbox/claim_kinds_call_site_guard_test.go` — added
  `namedCallSite`, `claimKindsGuardRequiredNonExemptCallSites`,
  `requiredNonExemptSitePresent`; added the presence-check loop to
  `TestNoUnscopedOutboxClaimCallOutsidePackage`; added the permanent
  regression test `TestPresenceCheckCatchesMailingOnlyNarrowing`; added a
  short connecting paragraph to `claimKindsGuardMinPlausibleNonExemptCallSiteCount`'s
  doc comment addressing criterion 5 (why the count floor is retained
  alongside the new presence check).
- `scripts/go_file_visit_floor_guard_test.sh` — added the `#0330` section:
  ground-truth grep against the real `subscribe_intake.go` source, an
  anchored-line extraction of the guard's required-site list, and a
  removal mutation proving that extraction is falsifiable — all folded into
  the existing before/after `sha_of` byte-identity window for the outbox
  guard file, reusing `fatal`/`fail`/`pass`/`sha_of` rather than duplicating
  them.

**Second pass (review remedies R1–R3):**

- `internal/outbox/claim_kinds_call_site_guard_test.go` — replaced
  `claimKindsGuardMinPlausibleNonExemptCallSiteCount`'s criterion-5
  paragraph with the reviewer's exact replacement text (R3): corrects the
  headroom arithmetic (floor 8 vs. population 12 trips on the loss of any
  five, not the ~10-site loss the old paragraph implied) and removes the
  self-contradiction against the paragraph above it.
- `scripts/go_file_visit_floor_guard_test.sh` — added item 4 to the `#0330`
  section (R1): an exactly-once check that
  `TestNoUnscopedOutboxClaimCallOutsidePackage` still calls
  `requiredNonExemptSitePresent(allSites, ...)`, with a `sed`-based mutation
  and its own negative control, closing the loop-deletion gap the header
  already claimed to cover but didn't. Replaced item 3's `grep -vE`
  required-list mutation with a `sed` filename-rewrite (R2), since the old
  mutation's pattern was a strict superset of what the per-method detector
  could ever match and so could never fail. Updated the section's numbered
  header comment and the "or deleting the loop that checks it" sentence to
  match what the script now actually proves.

**Third pass (review remedies F1–F4, comment-only, no mechanism added):**

- `scripts/go_file_visit_floor_guard_test.sh` — replaced header bullets `3.`
  and `4.` in the `#0330` section (F1, F2) with the reviewer's exact text,
  which states plainly what each item does and does not prove and instructs
  against adding a fifth textual check; corrected three runtime `pass`
  messages (F3) — the `count_presence_loop_call` comment, item 4's `pass`
  line, and item 3's `pass` line — to stop claiming the exactly-once check
  establishes liveness or that the required-list mutation is sensitive to
  content, and trimmed item 4's `LOOP_MUTANT` `pass` line of the same false
  "different pattern from the detector" distinction. No executable logic
  changed — every `if`/`fail`/`pass` branch condition is byte-identical to
  before this pass; only string literals inside comments and `pass` calls
  changed.
- `internal/outbox/claim_kinds_call_site_guard_test.go` — **unchanged**;
  confirmed byte-identical to `22f021f` before and after this pass
  (sha `95193675a31156614410e3a9a4a05a7c3b4ea422091c295d5f39340e675f36d6`).
- `issues/0330.md` — corrected two overclaiming sentences in `## Fix` (F4)
  using the strike-through-plus-dated-note convention already established
  in this file for criterion 1: bullet `(d)`'s claim that the exactly-once
  check confirms the assertion is "the real assertion" and "still calls"
  the presence check, and the second-pass R2 bullet's conclusion that the
  `sed` mutation is "genuinely sensitive to the list's content". Both
  struck spans verified programmatically to be byte-identical to the
  pre-correction text.

## Gotchas

- `#0322`'s own `nameMatchesGuardedMethod` doc comment (untouched by this
  pass, out of scope) still names `worker.go:543`/`worker.go:575` for
  `mailing.SendStore`'s unrelated `OrphanSweep` collision; re-grepping the
  live tree during this pass's independent census found it at `worker.go:625`
  today — a further drift, not introduced here, not part of this issue's
  acceptance criteria. Reporting it rather than fixing it (§9 — subagents
  report, the orchestrator allocates issue numbers).
- Building the mutation proof's extra call site required a syntactically
  valid but non-compiling-in-context Go file, since `findOutboxCallSitesInFile`
  only ever calls `go/parser` on each file in isolation — it never needs
  `internal/mailing` to actually build. That is a genuine property of the
  guard's own design (parsing, not compiling), not a shortcut taken for the
  proof.

## Acceptance criteria

1. The guard asserts **by name** that `internal/handlers/subscribe_intake.go`'s
   ~~`ClaimDue` and `OrphanSweep`~~ call sites appear in the non-exempt population,
   not merely that the population is large enough.

   **Correction (implementation pass, 2026-08-31).** This criterion's own
   method names were stale by the time this issue was implemented.
   `#0297`/`#0303` moved `subscribe_intake.go` off `ClaimDue` onto the
   per-row claim path, so its two guarded calls today are `OrphanSweep`
   (`:246`) and `SelectDue` (`:260`) — **zero** calls to `ClaimDue`. Measured
   independently for this pass with a standalone `go/ast` census (not
   copied from this issue, `#0345`, or any other prose): 269 files visited,
   36 total call sites, 12 non-exempt — reproducing `#0345`'s review census
   exactly. The criterion's *intent* (assert by name that this file's real
   guarded calls survive a narrowing) is what was implemented, against the
   real call sites (`OrphanSweep`, `SelectDue`), not the stale pair named
   above.
2. The assertion fails when scan roots are narrowed to any set excluding that
   file — proven by mutation, with the mutation reverted afterward.
3. The oracle for the expected call sites lives **outside** the file it
   protects, per `#0300` and `CLAUDE.md` §8: a copy of the answer stored next
   to the question is not a check. An edit to the guard's subject must not be
   able to satisfy the oracle.
4. An empty or zero-match extraction fails closed and is never treated as a
   passing input (§8, `#0275`).
5. If the count-based floor is retained alongside the presence check, its value
   is justified against measured headroom and the justification names what
   ordinary growth would trip it.
6. The presence check does not itself reproduce the `.ClaimDue(`/`.OrphanSweep(`
   substrings in a way that changes the population it measures — `#0323` hit
   exactly this, where documenting the pattern inflated the grep count 35 to 36.

## Relation

- Found by: [#0322](0322.md)'s implementation pass, which recommended it in its
  own doc comment and declined to implement it as out of scope
- Depends on: [#0322](0322.md) — the raised floor is the current mitigation
- Relates to: [#0275](0275.md) — the opposite failure mode, a floor above its
  population
- Relates to: [#0300](0300.md) — the external-oracle requirement
- Relates to: [#0254](0254.md) — the duplicate-send defect the dropped call
  site produced
- Relates to: [#0323](0323.md) — the self-referential-population hazard
  criterion 6 guards against

## Review notes

> 🔴 Bounced 2026-08-31 (review, `claude-opus-5`) at `52fdec3`/`e901b68`. The
> presence check itself is the right instrument, correctly scoped, and the
> oracle split is sound — I proved by mutation that it is *necessary*, not
> just defensible. It is bounced on three claims that ship alongside it and
> are not true of the tree: the external harness does not catch the mutation
> class its own header says it exists for, its list mutation can never fail,
> and criterion 5's new justification is arithmetically wrong and contradicts
> the paragraph forty lines above it in the same doc comment. Remedies below
> are transcription — exact replacement text and shell, each verified,
> including negative controls.

### Independent census — criterion 1 PASSES

Re-measured with a standalone `go/ast` walk written for this review from the
guard's *spec* (roots `internal/` + `cmd/`, prune `node_modules`/`dist`,
`*.go` only, selector-call name in {ClaimDue, OrphanSweep, SelectDue}, exempt
= file's directory is `internal/outbox`), reusing none of the guard's
helpers and no number from any issue, comment, or report:

| | |
|---|---|
| files visited | **269** |
| total call sites | **36** |
| non-exempt | **12** |
| exempt | **24** |

| File | total | non-exempt |
|---|---|---|
| `internal/handlers/admin_dashboard_test.go` | 2 | 2 |
| `internal/handlers/admin_pending_test.go` | 1 | 1 |
| `internal/handlers/subscribe_intake.go` | 2 | 2 |
| `internal/mailing/outbox_worker.go` | 2 | 2 |
| `internal/mailing/outbox_worker_test.go` | 1 | 1 |
| `internal/mailing/worker.go` | 1 | 1 |
| `internal/mailing/worker_store_test.go` | 3 | 3 |
| `internal/outbox/select_due_claim_row_test.go` | 4 | 0 |
| `internal/outbox/store_test.go` | 20 | 0 |

`internal/handlers/subscribe_intake.go` holds exactly `OrphanSweep` at
`:246` and `SelectDue` at `:260` — **zero** `ClaimDue`. Every figure
reproduces `#0345`'s review census and the implementer's independently.

**The stale-criterion correction is handled correctly.** Removing the two
`~~` from the struck span reproduces the pre-`e901b68` bytes exactly
(checked programmatically against `git show e901b68^:issues/0330.md`, not by
eye); the note is dated and attributed. Naming the *pass* rather than an
issue number is a defensible deviation from `Issues.md`'s
`**Correction (#NNNN, date).**` form, since the correcting issue is `#0330`
itself and citing it would be circular.

**And it serves the criterion's intent, not an easier set.** The required
list names exactly the two calls that file really makes — nothing omitted,
nothing substituted for a more convenient site — and mutation M4 below shows
the assertion built on it genuinely catches the narrowing `#0322`'s count
floor cannot. This is the stronger reading of the criterion, not a
weakening of it.

### Criterion 3 — the split is sound, and I proved it necessary

The orchestrator's question was whether an edit to
`claim_kinds_call_site_guard_test.go` **alone** can satisfy the in-Go oracle.
It can. **M2**: replace the two required entries with a single decoy that is
genuinely present and genuinely outside `internal/mailing`
(`{"internal/handlers/admin_pending_test.go", "ClaimDue"}`, so the sibling
narrowing test still passes) →

```
go test ./internal/outbox  ->  ok            (subscribe_intake.go now unprotected)
scripts/go_file_visit_floor_guard_test.sh -> 2 FAILs
```

So the in-Go home is satisfiable by editing its own subject, and the external
harness is exactly what closes that. The implementer's reasoning for the
split is correct and the split earns its cost. Criterion 3 is met **for the
list's content**; R1 below is where it is not met for the loop that reads it.

### Criteria 2, 4, 6 — re-proved independently

All mutations run in **my own throwaway worktree** (`git worktree add
--detach`, removed with `--force`; the pre-existing `/private/tmp/oc-live`
worktree was not touched), every restore hash-verified back to
`ef740190af0f66f946ebe4bf8b63518eabcaf17e7cfea39267f10878dacf3f1e`, main
tree `git status --porcelain` empty before and after.

- **M4 (criterion 2), the interesting step, reproduced exactly as reported.**
  Narrowing `claimKindsGuardScanRoots` to `{"../mailing"}` alone fails at the
  **count floor** (`:674`, "found only 7 … want at least 8"). Adding one
  further ordinary, correctly-scoped guarded call in a scratch
  `internal/mailing` file brings non-exempt to exactly 8, the count floor
  passes, and the failure moves to the **new presence check** (`:696`,
  "required non-exempt call site missing: …subscribe_intake.go's OrphanSweep
  call was not found"). The presence check is demonstrably catching a gap the
  floor alone passes.
- **M1 (criterion 4) fails closed in both homes.** Emptying the list →
  `t.Fatal` in both `TestNoUnscopedOutboxClaimCallOutsidePackage` (`:685`)
  and `TestPresenceCheckCatchesMailingOnlyNarrowing` (`:989`), *and* two
  harness `FAIL`s.
- **M5 (criterion 4, external home).** Truncating the guard file to empty →
  `FATAL` (in `extract_const`, before the `#0330` section is reached).
  Moving `subscribe_intake.go` aside → the section's own
  `FATAL: … not found -- has it moved?`. Renaming the production
  `OrphanSweep` call → the ground-truth `FAIL` plus its `NOTE:` banner. No
  path treats a zero or empty result as a pass. The script is
  `set -uo pipefail` and deliberately **not** `-e`, so a `grep -c` returning
  `0`/exit 1 lands in the numeric guard rather than aborting.
- **Criterion 6 — no population inflation, structurally and by measurement.**
  `count_outbox_call_sites` reads only `find … -name '*.go'`, so nothing
  added to `scripts/go_file_visit_floor_guard_test.sh` can reach its
  population at all; and the new Go identifiers are bare string literals with
  no leading `.` or trailing `(`. Measured: **42 total / 12 non-exempt**,
  identical to `#0345`'s baseline, on both a direct run and via
  `scripts/check.sh guards`.

### Verification I ran

```
uptime                                                          # 1.97 — idle
gofmt -l internal/outbox/claim_kinds_call_site_guard_test.go    # clean
#   sha256(file) == sha256(gofmt stdout) — ef740190af0f…dacf3f1e, stable
go vet ./internal/outbox/...                                    # clean
go test ./internal/outbox/... -run '…4 named tests…' -v         # 4/4 PASS
ISSUE=0330r scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
#   outbox ok 0.365s · mailing ok 3.552s · handlers ok 7.653s · (no skips) · VERIFICATION PASSED
scripts/check.sh guards                                         # VERIFICATION PASSED
bash -n scripts/go_file_visit_floor_guard_test.sh               # clean
shellcheck -S warning scripts/go_file_visit_floor_guard_test.sh # clean, exit 0
```

No bash-4 syntax in the added shell (`${var,,}`, `declare -A`, `mapfile`,
`&>>` — none present; `local a="$1" b="$2"` and `grep -cE` are 3.2-safe), and
the script contains no heredocs, so §8's unquoted-heredoc backtick hazard
does not arise. `opencircuit_test_0330r` dropped (`scripts/testdb.sh drop
0330r` → "does not exist"); no leftover `go test`/`.test` processes.

---

## What the next pass must fix

Three items. Each is a text or shell replacement with the exact content
below; none requires a design decision.

### R1 — the harness does not catch the mutation class its own header claims

**Measured, not inferred.** Delete the presence-check block from
`TestNoUnscopedOutboxClaimCallOutsidePackage` (the `if len(...) == 0 {
t.Fatal(...) }` through the `for _, req := range ...` loop) and leave
`claimKindsGuardRequiredNonExemptCallSites` itself intact — it stays
referenced by `TestPresenceCheckCatchesMailingOnlyNarrowing`, so it is not
even an unused-variable error. Result:

```
go test ./internal/outbox                          ->  ok
scripts/go_file_visit_floor_guard_test.sh          ->  all guards hold (0 failures)
```

Nothing anywhere asserts presence in the real walk any more, and both homes
are green. That is precisely the fail-open shape `CLAUDE.md` §8 names
("assert the marker block appears **exactly once**"; `GUARD-0208`).

Two places currently claim this *is* covered and must not be left saying so:

- `scripts/go_file_visit_floor_guard_test.sh`'s `#0330` header — "someone
  weakening or emptying `claimKindsGuardRequiredNonExemptCallSites` itself,
  **or deleting the loop that checks it** … This section is the oracle for
  exactly that class".
- this issue's `## Fix` — "weakening or emptying
  `claimKindsGuardRequiredNonExemptCallSites`, **or the loop that checks it**".

**Remedy — add this block to the `#0330` section**, immediately after the
required-list `for METHOD in $REQUIRED_METHODS` loop and before the
`REQUIRED_MUTANT` block. Verified working, with a negative control (a
deliberately drifted `sed` anchor leaves the detector at 1, i.e. the
mutation check would `fail` rather than pass vacuously):

```sh
# The two checks above prove the LIST still names both sites. They do not
# prove anything still READS it in the guard's real assertion: deleting the
# presence loop from TestNoUnscopedOutboxClaimCallOutsidePackage while
# leaving the var intact leaves `go test` green (the var stays referenced by
# TestPresenceCheckCatchesMailingOnlyNarrowing, so it is not even an
# unused-variable error) AND leaves this script green -- measured in #0330's
# review, not assumed. So the call site that reads the list gets CLAUDE.md
# §8's marker-block treatment: assert it appears EXACTLY ONCE, with a
# mutation whose pattern is not this detector's pattern.
count_presence_loop_call() {
  grep -cE 'requiredNonExemptSitePresent\(allSites,' "$1"
}

LOOP_N="$(count_presence_loop_call "$OUTBOX_SRC")"
case "$LOOP_N" in '' | *[!0-9]*) fatal "count_presence_loop_call returned a non-numeric count ('$LOOP_N') for $OUTBOX_GUARD_FILE" ;; esac
if [ "$LOOP_N" -eq 1 ]; then
  pass "tracked ${OUTBOX_GUARD_FILE} still calls requiredNonExemptSitePresent on the REAL walk results (allSites) exactly once -- the presence assertion inside TestNoUnscopedOutboxClaimCallOutsidePackage is still wired up"
else
  fail "REGRESSION #0330: tracked ${OUTBOX_GUARD_FILE} calls requiredNonExemptSitePresent(allSites, ...) ${LOOP_N} time(s), want exactly 1 -- 0 means the presence assertion was deleted from TestNoUnscopedOutboxClaimCallOutsidePackage, which leaves both go test AND the required-list checks above green; >1 means a second, possibly shadowing copy was added (CLAUDE.md §8, GUARD-0208's exactly-once rule)"
fi

LOOP_MUTANT="$WORKDIR/$(basename "$OUTBOX_GUARD_FILE").presence_loop_disabled"
sed 's/requiredNonExemptSitePresent(allSites,/requiredNonExemptSitePresentDISABLED(allSites,/' "$OUTBOX_SRC" > "$LOOP_MUTANT"
LOOP_MUT_N="$(count_presence_loop_call "$LOOP_MUTANT")"
case "$LOOP_MUT_N" in '' | *[!0-9]*) fatal "count_presence_loop_call returned a non-numeric count ('$LOOP_MUT_N') on the presence-loop mutant" ;; esac
if [ "$LOOP_MUT_N" -eq 0 ]; then
  pass "mutated copy with the presence-loop call renamed is correctly detected as missing by this external oracle -- and the mutation (sed, renaming the callee) is a DIFFERENT pattern from the detector (grep for the call), so a mutation that failed to take shows up as a FAIL instead of passing vacuously"
else
  fail "REGRESSION #0330: the presence-loop mutation did not take (still ${LOOP_MUT_N} match(es) in the mutant) -- the sed anchor has drifted from the detector's pattern; fix the mutation before trusting the exactly-once check above"
fi
```

Then extend the section header comment's numbered list with a `4.` describing
this check, and correct `## Fix` and the header's "or deleting the loop that
checks it" so the claim matches what the script now actually does.

### R2 — the required-list mutation can never fail

The detector regex is a strict **subset** of the mutation regex:

```
detector : ^[[:space:]]*\{"…subscribe_intake.go",[[:space:]]*"OrphanSweep"\},?[[:space:]]*$
mutation : grep -vE ^[[:space:]]*\{"…subscribe_intake.go",[[:space:]]*"(OrphanSweep|SelectDue)"\},?[[:space:]]*$
```

Every line the detector can match is a line the mutation deletes, so
`MUT_N` is `0` by construction and the check can never fire. Demonstrated:
under **M2** (list already replaced by a decoy, so the thing step (c) claims
to be sensitive to was already gone) step (b) `FAIL`ed twice while step (c)
still printed `PASS` twice. Its own comment calls it "proving the extraction
is genuinely sensitive to the list's content (falsifiable)"; it is not.

**Remedy — replace the `grep -vE` mutation line**

```sh
grep -vE "^[[:space:]]*\{\"${REQUIRED_SITE_FILE}\",[[:space:]]*\"(OrphanSweep|SelectDue)\"\},?[[:space:]]*\$" "$OUTBOX_SRC" > "$REQUIRED_MUTANT"
```

**with**

```sh
sed 's|subscribe_intake\.go|zzz_required_site_removed_by_mutation.go|g' "$OUTBOX_SRC" > "$REQUIRED_MUTANT"
```

Verified: both methods drop to `0` on the mutant, and the negative control
(`sed 's|subscribe_intake_XX\.go|zzz.go|g'`, a deliberately drifted anchor)
leaves both at `1` — so the mutation failing to take now produces a `FAIL`.
Update bullet `3.` of the section header comment to describe rewriting the
named file rather than "removing those two struct-literal lines", and say
why the mutation's pattern must differ from the detector's.

### R3 — criterion 5's justification is wrong, and contradicts the same comment

The paragraph added to
`claimKindsGuardMinPlausibleNonExemptCallSiteCount`'s doc comment ends:

> Ordinary growth that would trip this floor: any change that drops
> internal/mailing's non-exempt count from 7 to below 1 (e.g. deleting
> outbox_worker.go's two calls and worker_store_test.go's three without
> replacement) combined with internal/handlers dropping below 3 — unlikely
> as an accidental single commit, which is exactly why 8 was judged an
> acceptable value to leave in place rather than lower.

Four things wrong with it, against my census (handlers 5, mailing 7, total
12, floor 8 → trips on the loss of any **five**):

1. The `combined with` conjunct is **not necessary**. `internal/mailing` at
   0 leaves 5 < 8; the floor has already tripped with `internal/handlers`
   untouched.
2. Its own parenthetical example refutes it: deleting `outbox_worker.go`'s
   two and `worker_store_test.go`'s three leaves `internal/mailing` at
   **2**, not "below 1" — and the total at **7**, which trips the floor
   with neither stated condition met.
3. It presents a ~10-site loss as the trigger where **5** suffices, and so
   contradicts the paragraph forty lines above it, which correctly says a
   rename of `mailing.SendStore.OrphanSweep` drops four at once, "leaving
   ZERO sites of headroom, not one", and that "this floor's entire margin
   is currently supplied by that name collision". "Unlikely as an accidental
   single commit" is the opposite conclusion from the same measurement.
4. It labels deletions "ordinary **growth**". Growth cannot trip a floor.

**Remedy — replace the paragraph** (from `// #0330 built that presence check`
through `// judged an acceptable value to leave in place rather than lower.`)
with:

```go
// #0330 built that presence check
// (claimKindsGuardRequiredNonExemptCallSites, below) rather than retiring
// THIS floor. Retained deliberately, per #0330 criterion 5: this floor is
// what fails when the walk finds no non-exempt sites AT ALL (a broken
// method-name check, an emptied scan-roots list, or every caller genuinely
// removed) — a state the presence check alone does not distinguish from
// "the walk never ran," since an empty allSites slice makes every presence
// lookup above report "not found" for the same reason a broken walk would.
// The two are complementary, not redundant: this floor is the coarse "did
// the walk work at all" backstop the #0275/#0304 family already
// established; the presence check is the precise "did it find the ONE
// caller that matters" check a count can never express.
//
// Its VALUE (8) is left where #0322 put it, and the headroom is the one
// measured in the paragraphs above, not a larger one: 12 non-exempt sites
// today, so this floor trips on the loss of any FIVE of them. The smallest
// realistic change that does it is the one already named above — renaming
// or removing mailing.SendStore.OrphanSweep drops four at once
// (worker.go's call plus worker_store_test.go's three), leaving exactly 8
// and therefore zero headroom — plus any ONE further ordinary removal,
// e.g. deleting a single ClaimDue call from admin_dashboard_test.go or
// admin_pending_test.go. That is two commits' distance from a false
// failure, not the large deletion it might look like. It is accepted
// rather than lowered for the reason #0322 gave: 8 is what closes the
// mailing-only narrowing (7 < 8), and a shrink below the floor fails
// LOUDLY on the next `go test`, where the growth direction described above
// expires silently. The presence check below is what makes that trade
// affordable — it, not this number, is now the thing protecting
// subscribe_intake.go.
```

Then re-run `gofmt -w` on the file and confirm the sha is stable (§8: doc
comments get rewritten. The block contains no doubled `'` and no doubled
backtick, which are the two sequences gofmt smart-quotes; the single
backtick pair around `go test` is the same shape already present and stable
in this comment today, so the file should hash identically), plus
`ISSUE=NNNN scripts/check.sh go ./internal/outbox/...` and
`scripts/check.sh guards`.

### Not in scope for the next pass — reported to the orchestrator

The line-number drift reported in `## Gotchas` is **real but misreported**,
and the report should not be transcribed as-is. `nameMatchesGuardedMethod`'s
doc comment is *correct* — it says `worker.go:625`, which my census
confirms. The single stale citation is at line **204**, inside
`claimKindsGuardMinPlausibleNonExemptCallSiteCount`'s `#0322` paragraph,
and reads `worker.go:575`. The string `:543` appears nowhere in the file.
Do not fix it here; the orchestrator has the recommendation.

---

## Review notes — second pass

> 🔴 Bounced 2026-08-31 (review, `claude-opus-5`) at `22f021f`/`e9d82e7`.
> **All six acceptance criteria are met**, and I re-proved the load-bearing one
> (criterion 2) from scratch: the presence check genuinely catches a narrowing
> the count floor passes. R3's arithmetic is correct — I re-derived it from my
> own per-file census, not from the report. It is bounced on the **same class
> the first review bounced it for, one level up**: two of the three remedies
> ship comments claiming more than the mechanism delivers, and I measured both
> claims false. **R2's remedy does not fix the vacuity it documents at length**,
> and **R1's exactly-once check does not establish that the assertion is live**.
> The remedy below is **comment-only and deliberately adds no mechanism** — see
> "The recursion question" for why that is the terminating move rather than the
> fifth round of it.

### What I re-verified and accept

- **Criterion 2, reproduced independently.** Narrowing `claimKindsGuardScanRoots`
  to `{"../mailing"}` fails at the count floor (`:685`, "found only 7 … want at
  least 8"). Adding one ordinary, correctly-scoped `ClaimDue` call in a scratch
  `internal/mailing` file takes non-exempt to exactly 8, the floor passes, and
  the failure moves to the presence check (`:707`, "required non-exempt call
  site missing: …subscribe_intake.go's OrphanSweep call was not found"). The
  presence check is doing real work the floor cannot.
- **Criterion 4, fail-closed in all three homes.** Empty required list →
  `t.Fatal` at `:696` and `:1000` **and** two harness `FAIL`s. Truncated guard
  file → `FATAL` in `extract_const`. `subscribe_intake.go` moved aside → the
  section's own `FATAL: … not found -- has it moved?`. No path treats zero as a
  pass.
- **Criterion 6.** Zero `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call syntax
  in any added line of `22f021f` (grepped the `+` lines directly). Population
  re-measured **42 total / 12 non-exempt**, unchanged.
- **Criterion 5 / R3 — arithmetic correct, re-derived from my own census.**
  `admin_dashboard_test.go` 2 `ClaimDue`, `admin_pending_test.go` 1 `ClaimDue`,
  `subscribe_intake.go` `OrphanSweep`:246 + `SelectDue`:260, `outbox_worker.go`
  2, `outbox_worker_test.go` 1 `ClaimDue`, `worker.go` 1 `OrphanSweep` (at
  **:625**), `worker_store_test.go` 3 `OrphanSweep`. Handlers 5 + mailing 7 =
  12; collision = worker.go 1 + worker_store_test.go 3 = 4; non-collision = 8.
  So: floor 8 trips on the loss of any five (12−5=7 < 8) ✔; the named
  4-site collision loss leaves exactly 8 with zero headroom ✔; "plus any ONE
  further ordinary removal, e.g. a single `ClaimDue` from
  `admin_dashboard_test.go` or `admin_pending_test.go`" names calls that really
  are `ClaimDue` in those files ✔. No contradiction with the paragraph forty
  lines above (4 headroom ≡ trips on loss of 5), and no "growth" language
  applied to deletions. **R3 is accepted in full.**
- `gofmt -l` clean and byte-stable (`sha256(file) == sha256(gofmt stdout) ==
  95193675a31156614410e3a9a4a05a7c3b4ea422091c295d5f39340e675f36d6`); no doubled
  `'` or doubled backtick in the added doc comment. `bash -n` and
  `shellcheck -S warning` clean; no bash-4 syntax in the added shell
  (`${var,,}`, `${var^^}`, `declare -A`, `mapfile`, `&>>` — none); the script
  contains **zero** heredocs, so §8's unquoted-heredoc backtick hazard cannot
  arise; no backtick outside comments in the added lines.

### D1 — R2's remedy does not fix the vacuity it documents

**Measured, by replaying the first review's own M2 against the new form.**
Replacing the required list with the decoy
`{"internal/handlers/admin_pending_test.go", "ClaimDue"}` and running the
harness:

```
FAIL  item 2: … no longer names …/subscribe_intake.go / OrphanSweep
FAIL  item 2: … no longer names …/subscribe_intake.go / SelectDue
PASS  item 3: … correctly detected as missing OrphanSweep … falsifiable
PASS  item 3: … correctly detected as missing SelectDue … falsifiable
```

That is **exactly the output the first review condemned**, unchanged. The
reason is the same relation, not a different one: `count_required_site_line`'s
regex embeds `${REQUIRED_SITE_FILE}`, so *every* line the detector can match
contains `subscribe_intake.go`, and the new `sed` rewrites every occurrence of
that substring. `MUT_N` is `0` **by construction, for any content of the file**
— which is R2's own diagnosis of the `grep -v` form, applied verbatim to its
replacement. Swapping a line-deletion for a substring rewrite changed the
mechanism and not the relation.

The half of R2's claim that *is* true — "a mutation that fails to take (a
drifted anchor) … reports FAIL instead of a vacuous PASS" — was equally true of
the `grep -v` form: a drifted `grep -v` anchor also deletes nothing and leaves
`MUT_N` at 1. So it does not distinguish the two forms either.

**This is not a fail-open.** Under M2 the section as a whole correctly fails,
because item 2 does its job. Item 3 is a falsifiability self-check on the
*detector*; jointly with item 2 (tracked ≥ 1 **and** mutant == 0) the pair is
informative. **The defect is the prose, which is now emphatic, 18 lines long,
and wrong** — and a future agent will read it as settled.

One secondary observation, offered so the correction is honest rather than to
demand a revert: the new form is marginally **weaker** in one dimension. It
scrubs `subscribe_intake.go` from the whole file including comments, so a
detector accidentally loosened to match prose would report 0 on the mutant and
still `PASS`, where the old line-anchored `grep -v` would have left those prose
lines standing and produced a `FAIL`. Not worth reverting for; worth not
claiming the opposite.

### D2 — R1's exactly-once check does not establish what it says it does

`count_presence_loop_call` greps the **whole file** for one line containing
`requiredNonExemptSitePresent(allSites,`. It cannot tell whether that one call
sits inside `TestNoUnscopedOutboxClaimCallOutsidePackage`, nor whether it is
reachable. Its header bullet and its `PASS` line both say it does
("the presence assertion inside TestNoUnscopedOutboxClaimCallOutsidePackage is
still wired up").

I defeated it twice. **Both keep the count at exactly 1**, and both leave
`gofmt -l`, `go vet`, `go test ./internal/outbox` and the entire harness green:

- **M-N1 — an ordinary refactor, not an adversarial decoy.** Extract the
  presence block verbatim into
  `func assertRequiredSitesPresent(t *testing.T, allSites []outboxCallSite)` and
  forget to call it. Go does not flag an unused function, so nothing anywhere
  notices. Measured: `go test ./internal/outbox … ok`, harness
  `all guards hold (0 failures)`.
- **M-N2 — four characters.** Append `[:0]` to the loop's range expression
  (`range claimKindsGuardRequiredNonExemptCallSites[:0]`). The call site is
  byte-identical. Measured: same, both green.

Under either, `internal/handlers/subscribe_intake.go` is unprotected: a real
narrowing of `claimKindsGuardScanRoots` would then fail nothing.
`TestPresenceCheckCatchesMailingOnlyNarrowing` does **not** cover this — it
walks a hard-coded `{"../mailing"}` and never reads
`claimKindsGuardScanRoots`, so it tests the mechanism, not the live roots.

### The recursion question — my explicit judgement

**The structure is finished. The recursion terminates here, and the terminating
move is deleting a claim, not adding a check.**

M-N2 is why. The obvious next patch — scope the exactly-once grep to
`TestNoUnscopedOutboxClaimCallOutsidePackage`'s body with `awk` — closes M-N1
and does **nothing** for M-N2, which is a four-character edit. Round 4 would buy
one mutation's worth of coverage and round 5 would find the next shape. That is
precisely `CLAUDE.md` §8's `#0258` case: "every mechanism added to defend a file
… is one more thing an edit to that file can neutralise. Adding a seventh
in-file check buys a seventh bounce."

The general fact underneath: **a grep cannot establish that a Go assertion is
live.** Only compiling and running it can. So there are exactly two honest
positions, and the cheap one is right here:

1. **Accept the limit and document it precisely** (what I am asking for). The
   checks that matter still hold: item 2 catches the list being weakened —
   measured, M2 → two `FAIL`s, an edit to the subject that the in-Go oracle
   accepts and this one rejects, which is criterion 3 discharged. Item 4
   catches outright deletion. What neither catches is a *live-ness* mutation,
   and saying so in the file is worth more than a fifth pattern.
2. If the gap is ever judged worth closing, the instrument is a **different
   kind of oracle**, not another grep: mutate `claimKindsGuardScanRoots` in a
   copy of the package and assert `go test` **fails naming the required site**.
   Unlike the floor constants — where `got < 0` can never fire, which is why
   this script rightly refuses `go test` as an oracle — a narrowing genuinely
   changes what the walk finds, so a `go test` oracle is mathematically
   available *for this check specifically*. It costs a compile per run in
   `scripts/check.sh guards`. **I am not asking for it**, and I would argue
   against it: this is a guard on a guard on a guard protecting one unscoped
   sweep call, and the marginal risk it removes is smaller than the maintenance
   it adds.

So: no fifth mechanism, no sixth. Correct the two claims, and the file stops
promising something no textual instrument can deliver.

---

## What the next pass must fix — comment-only, no new mechanism

**Add nothing executable.** Three text replacements in
`scripts/go_file_visit_floor_guard_test.sh`, plus two small corrections in this
issue's `## Fix`. Every byte is supplied.

### F1 — replace header bullet `3.` in the `#0330` section

Replace the block beginning `#   3. Mutates a COPY by textually rewriting` and
ending `#      their constants.` with:

```sh
#   3. Mutates a COPY by textually rewriting every occurrence of
#      "subscribe_intake.go" to a name the required-list's anchored grep
#      cannot match, and confirms the SAME anchored grep, re-run against
#      the mutated copy, finds zero for both methods.
#
#      READ WHAT THIS DOES AND DOES NOT PROVE. It proves the detector is
#      not hard-wired to report "present": it can return 0, through its
#      own unmodified code path, on bytes that lack the entries. It does
#      NOT on its own prove the detector is sensitive to the LIST's
#      content, and no mutation of this shape can. The detector's pattern
#      embeds REQUIRED_SITE_FILE, so every line it can match contains
#      "subscribe_intake.go" and is therefore rewritten by this sed --
#      MUT_N is 0 by construction, for any content of the file. #0330's
#      SECOND review measured that directly: replacing the list with a
#      present-but-irrelevant decoy makes item 2 correctly FAIL twice
#      while this item still prints PASS twice. That was equally true of
#      the `grep -v` form this replaced (found by #0330's FIRST review),
#      and swapping a line-deletion for a substring rewrite did not change
#      it -- the claim once written here that it did was wrong and has
#      been removed. What this item does still catch is a mutation that
#      fails to TAKE (a drifted sed anchor): MUT_N stays at 1 and the
#      else-branch FAILs. Sensitivity to the list's CONTENT comes from
#      item 2; the two are informative jointly (tracked >= 1 AND mutant
#      == 0) and neither carries that claim alone.
```

### F2 — replace header bullet `4.` in the `#0330` section

Replace the block beginning `#   4. Confirms the tracked guard file's real
assertion --` and ending `#      mutation that fails to take is a FAIL here
too, not a vacuous PASS.` with:

```sh
#   4. Confirms the tracked guard file contains the text
#      "requiredNonExemptSitePresent(allSites," on EXACTLY ONE line
#      (CLAUDE.md §8, GUARD-0208's marker-block rule). Items 1-3 above
#      prove the LIST still names both sites; they say nothing about
#      whether anything still READS it. #0330's first review measured
#      that deleting the presence-check loop from
#      TestNoUnscopedOutboxClaimCallOutsidePackage, while leaving
#      claimKindsGuardRequiredNonExemptCallSites itself untouched, leaves
#      BOTH `go test` green (the var stays referenced by
#      TestPresenceCheckCatchesMailingOnlyNarrowing, so it is not even an
#      unused-variable error) AND items 1-3 green. This item closes that
#      one mutation: the deletion takes the count to 0 and FAILs.
#
#      ITS LIMIT, MEASURED, SO NOBODY RELIES ON MORE THAN IT GIVES. This
#      is a grep over the whole file. It cannot tell whether the one call
#      it counted sits inside TestNoUnscopedOutboxClaimCallOutsidePackage,
#      nor whether it is reachable. #0330's SECOND review defeated it
#      twice, both times leaving the count at exactly 1 and leaving
#      gofmt, go vet, `go test ./internal/outbox` and this whole script
#      green:
#
#        - extract the presence block verbatim into a helper --
#          func assertRequiredSitesPresent(t *testing.T, allSites []outboxCallSite)
#          -- and never call it; Go does not flag an unused function, so
#          nothing anywhere notices;
#        - append [:0] to the loop's range expression, leaving the call
#          site byte-identical.
#
#      DO NOT ADD A FIFTH TEXTUAL CHECK HERE. Scoping this grep to the
#      function body closes the first of those and does nothing for the
#      second, and the shape after that is the one CLAUDE.md §8 records as
#      costing #0258 six passes. A grep cannot establish that a Go
#      assertion is live; only compiling and running it can. If this gap
#      is ever judged worth closing, the instrument is a DIFFERENT KIND of
#      oracle -- mutate claimKindsGuardScanRoots in a copy of the package
#      and assert `go test` fails naming the required site, which unlike
#      the floor constants above is mathematically available here (a
#      narrowing genuinely changes what the walk finds; see this section's
#      "WHY THIS SECTION EXISTS" note above) -- not one more grep. #0330's
#      second review judged that not worth its cost and did not ask for it.
```

### F3 — three runtime message corrections

1. The in-script comment above `count_presence_loop_call`: replace its final
   sentence

   ```sh
   # §8's marker-block treatment: assert it appears EXACTLY ONCE, with a
   # mutation whose pattern is not this detector's pattern.
   ```

   with

   ```sh
   # §8's marker-block treatment: assert it appears EXACTLY ONCE. This
   # catches outright deletion and nothing more -- see item 4's limit note
   # in the header comment above for the two measured mutations that keep
   # this count at 1 while disarming the assertion.
   ```

2. Item 4's `pass` line — replace with:

   ```sh
     pass "tracked ${OUTBOX_GUARD_FILE} names requiredNonExemptSitePresent(allSites, ...) on exactly one line -- the presence assertion has not been deleted outright. NOT proof it is live: see item 4's limit note in this section's header comment"
   ```

3. Item 3's `pass` line inside the `for METHOD in $REQUIRED_METHODS` mutant
   loop — replace with:

   ```sh
     pass "mutated copy with ${REQUIRED_SITE_FILE} rewritten out is correctly detected as missing ${METHOD} -- the detector can return 0 through its own code path, and a mutation that failed to take would FAIL here. Sensitivity to the list's CONTENT comes from the tracked-file check above, not from this one (see item 3 in this section's header comment)"
   ```

   Item 4's mutant `pass` line may keep its wording except for the clause
   "the mutation (sed, renaming the callee) is a DIFFERENT pattern from the
   detector (grep for the call)", which is the same false distinction as D1 —
   cut that clause, keep the rest.

### F4 — this issue's own `## Fix`

Two sentences overclaim in the same way and must be corrected, not deleted:

- bullet `(d)`: "confirms the tracked file's **real assertion** — the loop
  inside `TestNoUnscopedOutboxClaimCallOutsidePackage` — still calls
  `requiredNonExemptSitePresent(allSites, ...)` **exactly once**" → say it
  confirms the *file* contains that call on exactly one line, that this catches
  outright deletion, and that it does not establish liveness (cite M-N1/M-N2).
- the second-pass **R2** bullet's conclusion "proving this mutation is
  genuinely sensitive to the list's content, unlike its predecessor" → replace
  with what D1 above establishes: the `sed` form is sensitive to a *drifted
  anchor*, not to the list's content, and is equivalent to its predecessor on
  the M2 case the first review used to condemn it.

### Verification the next pass must run

Comment-only, so the bar is small but exact:

```
bash -n scripts/go_file_visit_floor_guard_test.sh
shellcheck -S warning scripts/go_file_visit_floor_guard_test.sh
scripts/check.sh guards                       # expect VERIFICATION PASSED, 42 total / 12 non-exempt
ISSUE=NNNN scripts/check.sh go ./internal/outbox/...
```

`internal/outbox/claim_kinds_call_site_guard_test.go` must be **byte-identical**
to `22f021f` — confirm `sha256 ==
95193675a31156614410e3a9a4a05a7c3b4ea422091c295d5f39340e675f36d6`. No Go change
is wanted; R3 is accepted as it stands.

### Reported to the orchestrator, not filed (§9)

Nothing new beyond `#0352`, which stays correctly out of scope. The
`worker.go:625` location is re-confirmed by this review's census, and the
stale `:575` citation at line 204 is still there and still `#0352`'s.

---

## Review notes — third pass

> 🟢 **Resolved 2026-08-31** (review, `claude-opus-5`) at `98d9ee0`/`d0a5989`.
> The comment-only pass landed correctly. F1–F4 are byte-verbatim
> transcriptions, no executable logic moved, and — the thing this pass existed
> to decide — **the corrected text now claims exactly what the mechanism
> delivers, in both directions.** I reproduced all three defeating mutations
> from scratch rather than inheriting them, and added the negative control no
> prior pass ran: with the loop intact, an unsatisfiable required list makes
> `go test` **FAIL**, which is what makes the M-N1/M-N2 "green" results
> evidence of disarming rather than of an assertion that never enforced.

### Scope check — the hard one, run first

`internal/outbox/claim_kinds_call_site_guard_test.go` is byte-identical to
`22f021f`: sha256 `95193675a31156614410e3a9a4a05a7c3b4ea422091c295d5f39340e675f36d6`
in the worktree, at `HEAD`, and at `22f021f`; `git diff 22f021f HEAD --` on that
path is empty. `98d9ee0` touches one file (+62/−34), `d0a5989` touches one file
(+157/−6). No Go change, as specified.

**The shell edit is genuinely comment-only, proved rather than asserted.**
Stripping every full-line comment from both revisions leaves 306 lines each,
differing on exactly **three** lines — all `pass` message strings. Every
`if`/`elif`/`else`/`fi`/`case`/`esac`/`for`/`while`/`do`/`done`/`[`/`fail`/`fatal`
line — **117 of them** — is byte-identical between `22f021f` and `98d9ee0`. The
`pass`/`fail` line counts are unchanged (11 `pass`), so no branch was added,
removed, or re-selected.

### The honesty question — measured in both directions

I re-ran every mutation the corrected text describes, in my own throwaway
worktree (`git worktree add --detach`, removed `--force`; `/private/tmp/oc-live`
untouched), each restore hash-verified back to `95193675…675f36d6`.

| Mutation | Result | Does the shipped text claim it correctly? |
|---|---|---|
| **M-N1** — presence block extracted verbatim into an uncalled `assertRequiredSitesPresent` helper | `gofmt` clean, `go vet` clean, `go test ./internal/outbox` **ok**, harness **0 failures**, count held at **1** | ✅ named verbatim in item 4's limit note |
| **M-N2** — `[:0]` appended to the range expression (4 chars) | same: all green, count **1** | ✅ named verbatim in item 4's limit note |
| **M2** — required list replaced by a present-but-irrelevant decoy | `go test` **ok**; harness **item 2 FAILs twice, item 3 PASSes twice** | ✅ exactly the output item 3's corrected text now predicts and disclaims |
| **Outright deletion** of the presence block | `go test` **ok**; harness **FAILs** (`0 time(s), want exactly 1`) | ✅ item 4's *positive* claim holds — it is not decoration |

**Did the correction overshoot into under-claiming?** This was the failure mode
nobody had checked, and the answer is no. The two things the section genuinely
does are both still stated plainly and attributed to the right item: item 2 is
named as where sensitivity to the list's *content* lives (and M2 proves it —
`go test` green, harness red, which is criterion 3 discharged), and item 4 is
credited with catching outright deletion (verified above). Item 3's text keeps
its real claim — the detector can return 0 through its own unmodified code path,
i.e. it is not hard-wired to report "present" — while dropping only the
falsified content-sensitivity claim. Nothing here would lead a next agent to
rebuild a check that already exists.

One imprecision, recorded and deliberately **not** bounced: the
`count_presence_loop_call` comment says the exactly-once assertion "catches
outright deletion and nothing more", which omits the `>1` shadowing-duplicate
case that the same check's own `fail` branch does catch and that `GUARD-0208`
exists for. The phrase "assert it appears EXACTLY ONCE" in the same sentence and
the `fail` message four lines below both carry that case, so no reader is
misled. Not worth a fourth pass, and not worth an issue.

### Independent census — criteria 1 and 5

Own `go/ast` walk, written for this review from the guard's spec, reusing no
number from any issue, comment, or prior report:

| | |
|---|---|
| files visited | **269** |
| total call sites | **36** |
| non-exempt | **12** |

`admin_dashboard_test.go` 2 `ClaimDue` · `admin_pending_test.go` 1 `ClaimDue` ·
`subscribe_intake.go` `OrphanSweep` + `SelectDue` · `outbox_worker.go`
`OrphanSweep` + `SelectDue` · `outbox_worker_test.go` 1 `ClaimDue` · `worker.go`
1 `OrphanSweep` · `worker_store_test.go` 3 `OrphanSweep`. Handlers 5 + mailing 7
= 12.

- **Criterion 1** — `subscribe_intake.go` holds exactly `OrphanSweep` and
  `SelectDue`, **zero** `ClaimDue`. The required list names precisely that pair.
- **Criterion 5 / R3** — collision = `worker.go` 1 + `worker_store_test.go` 3 =
  **4**, so non-collision = **8**; floor 8 against population 12 trips on the
  loss of any **five**; the named 4-site collision loss leaves exactly 8 with
  zero headroom, and the "one further ordinary removal" the comment cites really
  is a `ClaimDue` in both files named. R3's arithmetic reproduces exactly.

The grep-based harness population (42) vs. this AST census (36) is `#0323`'s
documented divergence, not drift.

### Criteria 2, 3, 4, 6 — re-verified

- **2 (and the control no prior pass ran).** With the loop intact, pointing the
  required list at a file absent from the walk fails at `:707` —
  `required non-exempt call site missing: …'s OrphanSweep call was not found`.
  This is the negative control that makes M-N1/M-N2 meaningful.
  `TestPresenceCheckCatchesMailingOnlyNarrowing` passes as the permanent
  in-package proof for the narrowing class.
- **3.** M2 leaves `go test` green and the harness red — an edit to the guard's
  own subject that the in-Go oracle accepts and the external one rejects.
- **4.** Empty required list → `t.Fatal` at `:696` **and** `:1000`; the harness
  `fatal`s on a non-numeric extraction and on a moved `subscribe_intake.go`.
  Zero is never an input.
- **6.** Population **42 total / 12 non-exempt**, unchanged. Zero
  `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` syntax in `98d9ee0`'s added lines;
  the three pre-existing occurrences in the harness are `#0303`'s (`80925b3`),
  and `count_outbox_call_sites` scans `find … -name '*.go'` only, so shell text
  cannot reach the population structurally.

### Corrections and transcription fidelity

- F1 (1,609 bytes) and F2 (2,484 bytes) are present in the script **byte-verbatim**;
  all three F3 replacements are present dedented and their superseded text is
  gone; the false "DIFFERENT pattern from the detector" clause is absent.
- **F4's strikes are byte-faithful.** Pairing `~~` markers sequentially (the
  file also contains two *literal* backticked `` `~~` `` mentions in prose, which
  naive matching miscounts) gives three real strike spans. Both new ones — 189
  and 93 bytes — appear **literally**, including line wrapping and indentation,
  in `98d9ee0:issues/0330.md`. Not paraphrases. The pre-existing criterion-1
  strike still matches too. The inline `**corrected <date> (#NNNN…)**` form is
  `Issues.md`'s sanctioned variant for mid-sentence continuations, and naming
  the pass alongside `#0330` is strictly more informative than either form alone.

### The recursion question — I agree, and why the guard is worth having

**Agreed: the structure is finished, and deleting a claim was the right
terminating move.** M-N2 settles it — four characters, call site byte-identical.
Scoping item 4's grep to the function body closes M-N1 and does *nothing* for
M-N2, so round 4 buys one mutation and round 5 finds the next shape. That is
`CLAUDE.md` §8's `#0258` case verbatim, and §8's rule about where a proof belongs
says the same thing from the other side: a grep cannot establish that a Go
assertion is live — only compiling and running it can, which is why that proof
lives in Go (`TestPresenceCheckCatchesMailingOnlyNarrowing`, plus the control
above) and not in the harness.

**Why it is worth having anyway** — the part that belongs in the record:

1. **It fully closes the class it was built for.** `#0330` exists because a
   *scan-roots narrowing* silently drops `subscribe_intake.go` while the count
   floor still passes. That is the shape ordinary maintenance produces by
   accident, and against it the presence check is complete and proved.
2. **Two of the three deliberate subject-edit shapes are caught.** Weakening the
   list → item 2 (M2, measured). Deleting the assertion → item 4 (measured).
   Only *disarming* it escapes — and that now sits under a capitalised, measured
   limit note naming both mutations, rather than under a false claim of coverage.
3. **The escaping mutations are not accidents.** M-N1 and M-N2 require editing
   the assertion inside the file whose sole purpose is that assertion, in a diff
   a reviewer reads. The accidental class is the narrowing, and it is closed.

The available fourth instrument — compiling a mutated copy inside
`scripts/check.sh guards` — is mathematically sound here (a narrowing genuinely
changes `got`, unlike the `got < 0` floors), but it costs a compile per run to
defend a guard on a guard on a guard protecting one unscoped sweep call. I agree
with the second review that it is not worth it, and I would argue against adding
it.

### Verification I ran

```
uptime                                                          # 1.95 / 2.54 — idle
shasum -a 256 internal/outbox/claim_kinds_call_site_guard_test.go
#   95193675…675f36d6 — matches 22f021f and HEAD; git diff 22f021f HEAD -- <path> empty
bash -n scripts/go_file_visit_floor_guard_test.sh               # clean (bash 3.2.57)
shellcheck -S warning scripts/go_file_visit_floor_guard_test.sh # clean, exit 0
bash scripts/go_file_visit_floor_guard_test.sh                  # all guards hold (0 failures), 42 / 12
ISSUE=0330r3 scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
#   outbox ok 0.516s · mailing ok 3.895s · handlers ok 7.816s · (no skips) · gofmt clean · VERIFICATION PASSED
scripts/check.sh guards                                         # VERIFICATION PASSED, 42 / 12
go test ./internal/outbox/... -run '…4 named tests…' -v         # 4/4 PASS
```

No bash-4 syntax in the file (`${var,,}`, `${var^^}`, `declare -A`, `mapfile`,
`readarray`, `&>>` — all scanned, none present), and **zero heredocs**, so §8's
unquoted-heredoc backtick hazard cannot arise; script behaviour was checked by
invoking it under explicit `bash`, never by pasting its innards into the zsh tool
shell. Both mutation worktrees removed with `--force`; the pre-existing
`/private/tmp/oc-live` worktree was not touched, and `git worktree prune` was
deliberately not run. `opencircuit_test_0330r3` dropped (`testdb.sh drop 0330r3`
→ "does not exist"); no leftover `go test`/`.test` processes; main tree
`git status --porcelain` empty before and after.

### Reported to the orchestrator, not filed (§9)

**Nothing to file.** `#0352` still correctly owns the stale `worker.go:575`
citation at line 204 (re-confirmed present; my census re-confirms the real
location is `worker.go`, one `OrphanSweep`). The one imprecision I found this
pass — "catches outright deletion and nothing more" omitting the `>1` case — is
noted above and is not worth an issue.

## Work log

- 2026-08-28 — Filed by the orchestrator from `#0322`'s implementation report.
  Subagents do not file issues (§9); the implementer reported it and the
  orchestrator allocated the number.
- 2026-08-31 — implement (`claude-sonnet-5`): found criterion 1's named method
  pair (`ClaimDue`/`OrphanSweep`) stale — re-measured, corrected inline with a
  dated strike (see criterion 1's correction note) — and implemented its
  intent against the real call sites (`OrphanSweep`, `SelectDue`). Added a
  presence check (`claimKindsGuardRequiredNonExemptCallSites`) asserting by
  name that `subscribe_intake.go`'s guarded calls survive a scan-roots
  narrowing, backed by a permanent in-package regression test
  (`TestPresenceCheckCatchesMailingOnlyNarrowing`) and a new external-harness
  section in `scripts/go_file_visit_floor_guard_test.sh`. Retained the
  existing count floor per criterion 5. Mutation-proved criterion 2 in a
  throwaway `git worktree` (never the shared tree). Confirmed no population
  inflation (criterion 6) and fail-closed behavior (criterion 4). Full
  details in `## Root cause`, `## Fix`, `## Verification`, `## Files changed`
  above. Token usage not captured by this subagent; left for the
  orchestrator's usual per-transcript accounting. Left `in-progress` for
  review (§9 — the implementer never sets `resolved`).
- 2026-08-31 — review (`claude-opus-5`): **bounced.** Census re-measured
  from scratch with a standalone `go/ast` walk (269 files, 36 total, 12
  non-exempt) — reproduces `#0345`'s and the implementer's exactly, and
  confirms criterion 1's original method pair was genuinely stale, that the
  strike is byte-faithful, and that the implemented list is the criterion's
  intent rather than an easier set. Criteria 1, 2, 4 and 6 independently
  re-proved by mutation in my own throwaway worktree, including the
  "add one call to reach exactly 8" step, which does shift the failure from
  the count floor to the new presence check. The oracle split was proved
  *necessary*: swapping the required list for a present decoy leaves
  `go test` green and is caught only by the harness. Bounced on three items
  (R1–R3 in `## Review notes`): the harness does not catch the loop-deletion
  class its own header claims, its required-list mutation is tautological
  and can never fail, and criterion 5's new justification is arithmetically
  wrong and contradicts the paragraph above it. **§3 decision: no
  escalation.** This is the first bounce, and all three remedies are supplied
  as exact replacement text and verified shell (with negative controls), so
  the next pass is transcription, not design — it stays on Sonnet.
- 2026-08-31 — implement, second pass (`claude-sonnet-5`): transcribed the
  three remedies from `## Review notes` (`a764508`) verbatim. Before
  transcribing, independently reproduced R1's and R2's demonstrations —
  including their negative controls — in a throwaway `git worktree`
  (`/tmp/oc-0330b-mut`, never the shared tree, removed afterward), and
  cross-checked R3's arithmetic against this pass's own re-measurement
  (harness: 42 total / 12 non-exempt, unchanged). All three checked out
  exactly as the review described; no disagreement to report. Applied: R1
  (a new exactly-once `#0330` harness check that
  `requiredNonExemptSitePresent(allSites,` is still called from the real
  assertion, closing the loop-deletion gap the header wrongly claimed was
  already covered), R2 (replaced the tautological `grep -vE` required-list
  mutation with a `sed` filename rewrite that can genuinely fail to take),
  and R3 (replaced criterion 5's headroom paragraph with the corrected
  arithmetic). `go build`/`go vet`/`gofmt -l` clean, gofmt-stable
  (sha `95193675…f36d6`); named `go test` 4/4 `PASS`; `ISSUE=0330b
  scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` — all three packages `ok`, zero skips;
  `scripts/check.sh guards` `VERIFICATION PASSED` on two consecutive runs;
  `bash -n` and `shellcheck -S warning` both clean on the harness. Own
  database `opencircuit_test_0330b` dropped on exit; no leftover
  `go test`/`.test` processes. Full detail in `## Fix`'s "Second pass" note
  and `## Verification`'s "Second-pass verification" block above. Token
  usage not captured by this subagent; left for the orchestrator's usual
  per-transcript accounting. Left `in-progress` for review (§9 — the
  implementer never sets `resolved`).
- 2026-08-31 — review, second pass (`claude-opus-5`): **bounced.** Re-verified
  every acceptance criterion independently: criterion 2 reproduced from scratch
  (floor fires at 7 < 8 under the `{"../mailing"}` narrowing; adding one
  ordinary scoped call moves the failure to the presence check at `:707`),
  criterion 4 fails closed in all three homes, criterion 6 clean with the
  population unchanged at 42/12, and **R3's arithmetic re-derived from my own
  per-file census and accepted in full** (12 non-exempt, floor 8, trips on the
  loss of any five; the named 4-site collision loss leaves exactly 8; the
  `ClaimDue` calls it names really are `ClaimDue`). `gofmt` byte-stable,
  `bash -n`/`shellcheck` clean, no bash-4 syntax, no heredocs. Bounced on two
  items, both the first review's own remedies and both measured false rather
  than argued: **R2's replacement mutation does not fix the vacuity it
  documents** — replaying the first review's M2 decoy against the new `sed`
  form reproduces the condemned output exactly (item 2 `FAIL`s twice, item 3
  `PASS`es twice), because the detector's pattern embeds `REQUIRED_SITE_FILE`
  so `MUT_N` is 0 by construction either way; and **R1's exactly-once check
  does not establish that the assertion is live** — defeated twice with the
  count held at exactly 1 and every home green, once by an ordinary
  extract-to-helper-and-forget-to-call refactor (Go does not flag unused
  functions) and once by appending `[:0]` to the loop's range expression.
  Neither is a fail-open in the protection itself: the code is right and the
  criteria are met. The defect is that the file now states two things about its
  own strength that are not true, which is the class the first review bounced
  for. **The remedy is comment-only and adds no mechanism, deliberately** — see
  "The recursion question": M-N2 shows no textual oracle can establish liveness,
  so the `awk`-scoping patch that would close M-N1 buys exactly one round, and
  the terminating move is to document the limit rather than chase it.
  **§3 decision: no escalation — stays on Sonnet.** This is the second bounce,
  but not two bounces on the same *design* problem in §3's sense: the next pass
  decides nothing. Every replacement is supplied as exact bytes, the one design
  question (add a fifth check, or state the limit?) is answered in the remedy,
  and no Go file changes at all. Mutations run in my own throwaway worktree
  (`/private/tmp/oc-0330r2-mut`, `git worktree add --detach`, removed with
  `--force`; the pre-existing `/private/tmp/oc-live` worktree was not touched),
  each restore hash-verified back to
  `95193675a31156614410e3a9a4a05a7c3b4ea422091c295d5f39340e675f36d6`. Own
  database `opencircuit_test_0330r2`, dropped on exit; no leftover
  `go test`/`.test` processes.
- 2026-08-31 — implement, third pass (`claude-sonnet-5`): applied the second
  review's four comment-only remedies (F1–F4) verbatim — no design decision,
  per §3's "the next pass is transcription" call, which the reviewer made
  explicit. F1/F2 replaced header bullets `3.` and `4.` in
  `scripts/go_file_visit_floor_guard_test.sh`'s `#0330` section with the
  reviewer's exact text; F3 corrected three runtime `pass` messages and
  trimmed the false "different pattern from the detector" clause from item
  4's mutant `pass` line; F4 corrected two overclaiming sentences in this
  issue's own `## Fix`, using the strike-through-plus-dated-note convention
  already established here for criterion 1 — both struck spans verified
  programmatically to be byte-identical to the pre-correction text. No
  executable logic changed in the shell (every `if`/`fail`/`pass` branch
  condition byte-identical to before). **No Go file touched**:
  `internal/outbox/claim_kinds_call_site_guard_test.go` confirmed
  byte-identical to `22f021f`
  (sha `95193675a31156614410e3a9a4a05a7c3b4ea422091c295d5f39340e675f36d6`)
  both before and after this pass. `bash -n` and `shellcheck -S warning`
  clean; `scripts/check.sh guards` `VERIFICATION PASSED` on two consecutive
  runs, population unchanged (42 total / 12 non-exempt); `ISSUE=0330c
  scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` — all three `ok`, zero skips; the 4 named `go
  test` tests `PASS`. Full detail in `## Fix`'s "Third pass" note and
  `## Verification`'s "Third-pass verification" block above. Code commit
  `98d9ee0` (`scripts/go_file_visit_floor_guard_test.sh` only, confirmed via
  `git show --stat HEAD`). Own database `opencircuit_test_0330c`, dropped on
  exit; no leftover `go test`/`.test` processes belonging to this pass (one
  `go test ./internal/seo/...` process observed transiently in the first
  `guards` run's leftover-process listing, started by a concurrent session
  working `#0353`, not touched). Token usage not captured by this subagent;
  left for the orchestrator's usual per-transcript accounting. Left
  `in-progress` for review (§9 — the implementer never sets `resolved`).
- 2026-08-31 — review, third pass (`claude-opus-5`): **resolved.** Hard scope
  check first: `internal/outbox/claim_kinds_call_site_guard_test.go` is
  byte-identical to `22f021f` (sha `95193675…675f36d6`) in the worktree, at
  `HEAD`, and by an empty `git diff`. Proved the shell edit comment-only rather
  than trusting it: stripping full-line comments leaves 306 lines on both
  revisions differing on exactly three `pass` strings, and all **117**
  `if`/`else`/`case`/`for`/`fail`/`fatal` lines are byte-identical. Reproduced
  all three defeating mutations from scratch in my own throwaway worktree —
  M-N1 (uncalled-helper extraction) and M-N2 (`[:0]`) both hold the count at 1
  with `go test` and the whole harness green, and the M2 decoy replay reproduces
  item 2 `FAIL`ing twice while item 3 `PASS`es twice — and added the **negative
  control no prior pass ran**: with the loop intact, an unsatisfiable required
  list fails at `:707`, which is what makes the M-N1/M-N2 green results evidence
  of disarming rather than of an assertion that never enforced. Also verified
  item 4's *positive* claim (outright deletion → harness `FAIL`), so it is not
  decoration. **The corrected text claims exactly what the mechanism delivers in
  both directions** — the over-claim is gone and nothing is understated: item 2
  is still credited with content sensitivity and item 4 with deletion-catching.
  F1/F2 byte-verbatim (1,609 and 2,484 bytes), all three F3 replacements
  present, false "DIFFERENT pattern" clause absent; F4's two new strike spans
  (189 and 93 bytes) literally byte-identical to the pre-correction text,
  wrapping and indentation included. Own `go/ast` census (269 files, 36 total,
  12 non-exempt; handlers 5 + mailing 7) re-derives criterion 1 (zero `ClaimDue`
  in `subscribe_intake.go`) and criterion 5 (collision 4, floor 8, trips on the
  loss of five). Criteria 3, 4, 6 re-proved; population unchanged at 42/12.
  `bash -n`, `shellcheck -S warning`, `scripts/check.sh guards`, and
  `ISSUE=0330r3 scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` all clean, zero skips; 4/4 named tests `PASS`.
  **Agreed with the second review's recursion judgement**: M-N2 is four
  characters and no textual oracle can reach it, so deleting a claim rather than
  adding a fifth check was the terminating move — reasoning recorded in
  `## Review notes — third pass`. **§3 decision: no escalation, and none was
  needed** — the third pass was pure transcription of supplied bytes and landed
  complete and correct on Sonnet, exactly as §3 predicts for a bounce that
  specifies its remedy precisely. Both mutation worktrees removed;
  `/private/tmp/oc-live` untouched; `opencircuit_test_0330r3` dropped; no
  leftover processes. Nothing new to file — `#0352` still owns the stale `:575`
  citation.
