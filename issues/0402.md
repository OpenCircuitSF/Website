# 0402 — No way into the admin console under `STORAGE=postgres` locally without registering a passkey

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | tooling / auth |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-09-03 |
| **Commit** | e4a29b6 |
| **Closed** | 2026-09-09 |

> 🟢 Resolved 2026-09-09 — the loopback gate held against every hostile
> `BASE_URL` shape attacked at it, `#0409`'s three lockout states heal to 200
> through both the real route table and a real running binary, and the
> loader-variable floor's correction to 22 was re-derived independently.

## Description

`middleware.DevAutoLogin` bypasses WebAuthn for local development, and
`scripts/dev.sh` advertises it — "logs in automatically as admin@localhost".
But it is wired **only** in `serveDevMode`, the `STORAGE=json` path, and it
panics at construction time outside dev mode by design.

**`STORAGE=json` has no mailing list** (`CLAUDE.md` §5): no interests, no
subscribers, no campaigns, no suppressions. So every admin screen that matters
for the mailing subsystem — subscribers, imports, pending, suppressions,
deliverability, campaigns, and now `#0393`'s CRT commands — can only be
exercised against Postgres, and the Postgres path has **no way in at all**
without registering a real passkey.

Hit while testing `#0393` locally. The observed sequence, all of it a dead end:

- `dev.sh` prints "logs in automatically as admin@localhost" regardless of
  `STORAGE`, which is **wrong under Postgres** and sends you looking for a
  session that was never created.
- `GET /admin/*` answers `401`.
- `POST /auth/register/start` for the seeded `admin@localhost` returns a
  uniform `200 {"message":"Check your email"}` and creates **no**
  `pending_registrations` row, because the user already exists. The uniform
  response is correct and must not change (it is the same anti-enumeration
  reasoning as `#0026`'s 202) — but it means the flow gives no signal that it
  is the wrong one.
- It is also gated by `registrations_enabled`, which seeds `false`, and the
  only way to flip that is `PATCH /admin/settings` — which needs the admin
  session you are trying to obtain.
- `POST /auth/recover` does work and logs a magic link under `MAILER_NOOP=true`,
  but it lands on a passkey registration ceremony, which is exactly what a
  local dev loop should not require.

The workaround actually used was inserting a `sessions` row by hand and pasting
the cookie into the browser console. That works because session tokens are
stored raw, but it is not something a developer should have to derive.

## Root cause

`middleware.DevAutoLogin` is hard-gated to `devMode == true` (`STORAGE=json`),
and `STORAGE=json`'s dev store has no mailing-list tables at all (`CLAUDE.md`
§5) — no interests, subscribers, campaigns, or suppressions. So every admin
screen that exercises that subsystem could only be reached on the Postgres
path, and the Postgres path had no bypass of the real WebAuthn ceremony:
`registrations_enabled` seeds `false`, `POST /auth/register/start` answers a
uniform 202 for an existing account by design (`#0026`'s anti-enumeration
reasoning), and flipping either requires the very admin session being sought.

## Fix

Followed the plan as written, with no deviation. Two pieces:

1. **The gate.** Extracted `baseURLIsLoopback` out of `mailerNoOpAllowed`
   (`cmd/opencircuit/main.go`) — `mailerNoOpAllowed` is now a one-line
   wrapper, its name/doc-framing/behaviour untouched, and
   `TestMailerNoOpAllowed` passes unmodified against it.
   `newDevAdminAutoLogin` reuses that predicate: off ⇒ `(nil, nil)`, on ⇒
   refuse to start unless `BASE_URL`'s host is `localhost`/`127.0.0.1`
   — checked **before** any `store`/`pool` use — then resolve `ADMIN_EMAIL`
   via `store.LookupUserByEmail` (never creating the row; `ErrUserNotFound`
   names `opencircuit seed`), warn once, and build a session-minting closure
   over `auth.NewSessionToken` + `store.CreateSession` (no new exported
   `auth` API).
2. **The middleware.** `middleware.DevAdminAutoLogin`, a sibling of
   `DevAutoLogin` in the same file with its own `sync.Once`
   (`devAdminAuthOnce`), built around one invariant that makes `#0409`'s
   defect structurally unreachable rather than merely patched: it never
   branches on a credential being *present*, only on `ResolveSession`
   judging it *valid* — the same check `RequireSession` makes moments later.
   On mint it rebuilds the request's `Cookie` header via `Header.Set` (never
   `r.AddCookie`, which appends and leaves a stale value winning at
   `r.Cookie`'s first-match extraction) and calls `Header.Del("Authorization")`
   (since `sessionToken` prefers a bearer header over the cookie). A
   mutex-guarded cache-or-mint helper validates the cached token before
   reuse, so parallel credential-less requests mint exactly once and a
   cached token whose row was deleted out of band heals on the next request.

   `cmd/opencircuit`'s `servePostgres` now passes `newDevAdminAutoLogin`'s
   result as `mountAndServe`'s `outerMiddleware` argument (previously a
   literal `nil`) — nil unless the flag is genuinely set, so the production
   handler chain is byte-for-byte unchanged by default.

3. **The trap flagged in planning.** `internal/config`'s
   `minLoaderVariables`/`TestEnvExampleCoversLoaderVariables` floor was
   documented as `20` but, measured directly via the same AST walk the test
   uses, config.go already read **21** distinct variables before this issue
   (`#0433`'s `MEDIA_DIR` had moved it from 20 to 21 without the floor or its
   prose being updated). Adding `DEV_ADMIN_LOGIN` makes it **22**. Raised the
   constant to `22` and corrected the prose to state the true current count
   and explain both contributors, rather than bumping 20→21 as the plan's
   estimate assumed — the floor is a `<` check either way, so this is a
   more-accurate correction, not a behavior change.

4. `scripts/dev.sh` gained `--postgres`/`-p`, combinable with `--built`/`-b`
   via a `for arg in "$@"` loop (bash 3.2.57-safe — no `${var,,}`; lowercasing
   for the truthy check uses `tr`), setting `DATABASE_URL`/`DEV_ADMIN_LOGIN`/
   `MAILER_NOOP` (each honouring a caller override) alongside the existing
   `STORAGE` override. The "logs in automatically" banner and the preflight
   `storage:`/`database:`/`dev admin login:` lines now report the truth under
   both backends instead of claiming auto-login unconditionally.

5. `docs/dev.md` gained a "Running against Postgres" section plus
   `DATABASE_URL`/`DEV_ADMIN_LOGIN` table rows; `docs/configuration.md`
   gained a `DEV_ADMIN_LOGIN` row matching `MAILER_NOOP`'s level of detail.

## Verification

Machine load throughout: `uptime` reported load averages around 1.7–2.3
(well under the ~8 threshold `CLAUDE.md` §5 names as busy).

```
ISSUE=0402 scripts/check.sh go ./internal/config/... ./internal/middleware/... ./internal/auth/... ./cmd/opencircuit/... ./internal/handlers/... ./internal/db/...
```

Result: `go build`/`go vet` clean, `gofmt -l` clean (no output), **zero
skips** (the skip-audit step reported "(no skips)"), and every package
reported `ok` with real elapsed time against an isolated per-issue database
(`opencircuit_test_0402`, provisioned from `migrations/` since the shared
template was ahead at 28 against this tree's 27 — expected, per the
script's own note, and not something this issue's migrations touch):
`internal/config` 0.310s, `internal/middleware` 0.491s, `internal/auth`
0.939s, `cmd/opencircuit` 1.976s, `internal/handlers` 8.211s, `internal/db`
7.900s.

Specific new tests confirmed to run and pass (`-v`, re-run against a
separate scratch database `opencircuit_test_0402b`, dropped afterward):

- `TestMailerNoOpAllowed` — passes **unmodified**, proving the
  `baseURLIsLoopback` extraction changed nothing about `mailerNoOpAllowed`'s
  behaviour, including the sharp cases `sub.localhost` and
  `127.0.0.1.evil.com` (both still `false`).
- `TestBaseURLIsLoopback_MatchesMailerNoOpCases` — the same case table
  driven through the new predicate directly.
- `TestDevAdminAutoLogin_PanicsWhenNotAllowed`,
  `TestDevAdminAutoLogin_PanicsOnNilDependencies` — the structural backstop.
- `TestDevAdminAutoLogin_NoCredentialMintsAndComposes` — full
  `DevAdminAutoLogin → RequireSession → handler` chain, no credential, one
  mint, response cookie present.
- `TestDevAdminAutoLogin_ValidCookiePassesThroughUnchanged` — the only
  pass-through branch: zero mint calls, zero `Set-Cookie` on the response.
- `TestDevAdminAutoLogin_StaleCookieHeals`,
  `TestDevAdminAutoLogin_GarbageCookieHeals` — `#0409`'s two acceptance
  criteria, proved here: 200 (not 401), and exactly one
  `opencircuit_session` cookie survives in the rebuilt request header.
- `TestDevAdminAutoLogin_UnusableBearerTokenDropped` — `#0402`'s second
  mechanical finding: an unusable bearer token with no cookie present heals
  to 200, and `Authorization` is empty by the time `RequireSession` reads
  the request.
- `TestDevAdminAutoLogin_CachesAcrossCredentialLessRequests` — ten
  successive credential-less requests, exactly one mint call.
- `TestDevAdminAutoLogin_CacheHealsAfterInvalidation` — cache hit on request
  2, then a fresh mint (call count 1→2) after the cached token is
  invalidated out of band.
- `TestNewDevAdminAutoLogin_OffIsNilNil` — `(nil, nil)`, no log output.
- `TestNewDevAdminAutoLogin_RefusedOutsideLocalhost_BeforeAnyDBAccess` —
  passes **nil `store` and nil `pool`**; a refusal (not a panic) proves the
  loopback check runs before any database access.
- `TestNewDevAdminAutoLogin_RealRouteTable` (DB-backed, real
  `mountAndServe`) — server A (wired) answers `GET /api/me` with no cookie
  200, reporting the seeded admin; server B (identical wiring,
  `outerMiddleware` nil) answers 401 to the same request; five repeated
  credential-less requests against server A leave exactly one `sessions`
  row for the uniquely-seeded admin (`devadmin-<unixnano>@localhost`,
  cleaned up in `t.Cleanup` by its own id — never a literal/seeded id, per
  `CLAUDE.md` §8b). No `t.Parallel()` used anywhere in the package; no
  `truncateAdminWiringTables`/`registerWiringTest` call, since this test
  never touches the shared wiring tables.

`gofmt -w` run against all six touched/created Go files
(`internal/config/config.go`, `internal/config/env_example_guard_test.go`,
`internal/middleware/devauth.go`, `internal/middleware/devadminauth_test.go`,
`cmd/opencircuit/main.go`, `cmd/opencircuit/devadmin_test.go`) with a
before/after SHA-256 comparison on each — all six reported **stable** (no
rewrite), and `gofmt -l .` (excluding `web/`) reported nothing, so the new
doc comments (several containing double-quote and backtick-adjacent prose,
per `CLAUDE.md` §8's gofmt-rewrite gotcha) are confirmed stable rather than
merely assumed so.

`scripts/dev.sh -h` was run directly (not read from source) to confirm the
extended header's line range still matches the `-h` branch's `sed -n
'2,56p'` after the header grew — full output compared against the file by
hand, nothing truncated. `shellcheck -x scripts/dev.sh` and `bash -n
scripts/dev.sh` both reported clean. The flag-loop and banner/preflight
logic (`--postgres`, `-b -p`, an explicit `DEV_ADMIN_LOGIN=false` override,
and an unknown-flag rejection) were exercised directly by extracting the
relevant script fragments into throwaway harnesses — `scripts/dev.sh`
itself was **not started**, since `:5173`/`:8080` were held by another
agent's processes (pids 47875/47827, confirmed not mine) for the duration
of this work; `scripts/check.sh guards` (which binds `:5173` for ~48s via
`scripts/dev_guard_test.sh`) was likewise not run for the same reason —
this is a mock-level verification of the new flag/banner logic, not an
end-to-end run of the real script.

## Files changed

- `internal/config/config.go` — added `DevAdminLogin bool` to `Config`,
  read via `getBool("DEV_ADMIN_LOGIN", false, &errs)`, not added to the
  `required` slice.
- `.env.example` — added `DEV_ADMIN_LOGIN=false` with a two-line comment.
- `internal/config/env_example_guard_test.go` — raised `minLoaderVariables`
  from `20` to `22` and corrected its doc comment to the true current count
  (see `## Fix` above for why 22, not the plan's estimated 21).
- `cmd/opencircuit/main.go` — extracted `baseURLIsLoopback` out of
  `mailerNoOpAllowed` (now a one-line wrapper); added
  `newDevAdminAutoLogin`; wired its result into `servePostgres`'s
  `mountAndServe` call (replacing the literal `nil` outer-middleware
  argument); updated `mountAndServe`'s doc comment to describe both dev
  auto-login call sites; added the `strings` import.
- `cmd/opencircuit/devadmin_test.go` (new) — `TestNewDevAdminAutoLogin_OffIsNilNil`,
  `TestNewDevAdminAutoLogin_RefusedOutsideLocalhost_BeforeAnyDBAccess`,
  `TestBaseURLIsLoopback_MatchesMailerNoOpCases`, and the DB-backed
  `TestNewDevAdminAutoLogin_RealRouteTable` plus its `startDevAdminWiringServer`/
  `devWiringPort` helpers.
- `internal/middleware/devauth.go` — added `devAdminAuthOnce`,
  `DevAdminSessionMinter`, and `DevAdminAutoLogin`, alongside the untouched
  `DevAutoLogin`; added `context`/`strings` imports.
- `internal/middleware/devadminauth_test.go` (new) — `fakeDevAdminResolver`,
  `newCountingMinter`, and the ten tests listed under `## Verification`
  above.
- `scripts/dev.sh` — `--postgres`/`-p` flag via a loop over `"$@"`; the four
  postgres-mode env vars; truthful preflight/banner reporting via a new
  `is_true` helper; header comment rewritten to document both backends; `-h`
  branch's `sed` range extended from `2,40p` to `2,56p`.
- `docs/dev.md` — new "Running against Postgres" section; `DATABASE_URL`
  and `DEV_ADMIN_LOGIN` rows added to the environment-variable table.
- `docs/configuration.md` — new `DEV_ADMIN_LOGIN` table row.

## Gotchas

- **The plan's estimate of the `minLoaderVariables` floor (20→21) was
  already stale at planning time.** `#0433`'s `MEDIA_DIR` had silently moved
  the real count from 20 to 21 without anyone bumping the floor or its
  prose (the floor is a `<` check, so it kept passing without complaint).
  Re-derived the true count with the test's own AST walk before touching
  the constant, rather than trusting the plan's arithmetic — see `## Fix`
  item 3.
- **`scripts/dev.sh` and `scripts/check.sh guards` were not exercised
  end-to-end** because `:5173`/`:8080` were held by another agent's
  processes for this issue's entire working window. The flag-parsing,
  preflight-reporting, and banner logic were proved correct by extracting
  those exact script fragments into throwaway harnesses (see
  `## Verification`), and `shellcheck`/`bash -n` both passed, but a reviewer
  with a free `:5173`/`:8080` should run `scripts/dev.sh --postgres` (and
  `scripts/check.sh guards`) for a genuine end-to-end pass before treating
  this as fully proven in the real script.

## Review notes — 2026-09-09 (phase 3, approved)

Approved. Everything below was re-run rather than read off the drafted
`## Verification`.

### What enables the bypass, stated plainly

Two conditions together, both evaluated once at startup: `DEV_ADMIN_LOGIN`
parses as true, **and** `BASE_URL`'s host is exactly `localhost` or
`127.0.0.1`. Either alone does nothing — the flag off returns a nil
middleware and leaves the handler chain identical to the one production runs
today, and the flag on with any other host refuses to start the process at
all. To enable this on the live box an operator would have to set the flag in
production's config **and** rewrite `BASE_URL` from the canonical host to a
loopback URL, which is the address every magic link, confirmation link,
unsubscribe URL and canonical tag is built from. That second half is the
residual, it is the same one already accepted for `MAILER_NOOP`, and it is
self-announcing: the first email anyone receives carries a `localhost` link.

The residual is documented where an operator meets it. `.env.example` is what
`docs/deployment.md`'s install step copies onto production verbatim, and it
now ships `DEV_ADMIN_LOGIN=false` with the refusal rule spelled out beside it
rather than leaving the variable absent and unexplained.

One correction to the planning argument, which does not change the verdict:
the plan leans partly on `WEBAUTHN_RP_ORIGIN` having to match the browser's
real origin, but that is a separate variable, so rewriting `BASE_URL` alone
would not break a passkey ceremony. The link-breakage half of the argument
carries the residual on its own.

### Verification runs

`uptime` reported load average 1.49 before the scoped run and 2.33 before a
middleware-only re-run, both well under the threshold `CLAUDE.md` §5 names as
a busy machine.

```
ISSUE=0402r scripts/check.sh go ./internal/config/... ./internal/middleware/... ./internal/auth/... ./cmd/opencircuit/... ./internal/handlers/... ./internal/db/...
```

`go build`, `go vet` and `gofmt -l` all clean, the skip audit reported no
skips, and every package reported `ok` with real elapsed time against an
isolated per-issue database: config 0.234s, middleware 0.353s, auth 0.959s,
cmd 1.978s, handlers 8.544s, db 7.539s. `gofmt -l` over the six touched Go
files listed nothing, so the new doc comments are hash-stable rather than
merely assumed so.

### The gate, attacked rather than read

Thirty-eight `BASE_URL` shapes were driven through both `baseURLIsLoopback`
and `newDevAdminAutoLogin`, the latter with a nil store and a nil pool so
that an accepted host shows up as a dereference panic rather than as a quiet
pass. The harness was injected with the Go toolchain's overlay mechanism, so
the working tree was never modified.

Exactly six shapes were accepted, and every one of them has a genuinely
loopback host: `http://localhost`, `http://localhost:8080`,
`https://localhost`, `http://127.0.0.1`, `http://127.0.0.1:8080`, and
`http://user:pass@localhost/` — plus the degenerate scheme-relative
`//localhost`, whose host is still `localhost`. Nothing else got through.

The shapes that a naive host check accepts were all refused, and the userinfo
family is the important one: `http://localhost@evil.com/`,
`http://127.0.0.1@evil.com/` and `http://localhost:8080@evil.com/` each parse
to a hostname of `evil.com` and are rejected. So are suffix and prefix
tricks (`localhost.evil.com`, `sub.localhost`, `127.0.0.1.evil.com`), the
token appearing in a path, query or fragment, alternative IPv4 spellings
(`127.1`, `127.0.0.001`, `2130706433`, `0x7f000001`), percent-encoded hosts,
a backslash in the authority, leading and trailing whitespace, a bare
`localhost:8080` with no scheme, and an empty string.

Three shapes are refused that a browser would resolve to loopback: uppercase
`LOCALHOST`, a trailing-dot `localhost.`, and the IPv6 literal `[::1]`. All
three fail closed rather than open, all three behave identically under
`mailerNoOpAllowed`, and none is a bypass — they are worth knowing only so
that a refusal is not mistaken for a defect.

### The mailer guard was extracted, not weakened

The commit touches neither the file holding `TestMailerNoOpAllowed` nor
`checkMailerNoOp`, and that test passes unmodified in the run above. Beyond
that, `mailerNoOpAllowed` and `baseURLIsLoopback` were driven over the same
case table plus the new hostile cases with no divergence on any input, and
the two sharp cases the original table exists for — `sub.localhost` and
`127.0.0.1.evil.com` — are still false.

### `#0409` is unreachable, not merely fixed

`TestDevAdminAutoLogin_StaleCookieHeals`,
`TestDevAdminAutoLogin_GarbageCookieHeals` and
`TestDevAdminAutoLogin_UnusableBearerTokenDropped` were run verbosely and
pass, and each drives the composed chain through the real `RequireSession`
rather than a stand-in.

There is genuinely no branch keyed on a credential being present. The single
pass-through requires `ResolveSession` to return no error; the mint branch
rebuilds the `Cookie` header with `Header.Set` so the new token is the only
session entry, and deletes `Authorization` outright so an unusable bearer
token cannot win the extractor's preference. `DevAdminAutoLogin` has its own
`devAdminAuthOnce`, and `DevAutoLogin` is byte-for-byte unchanged — the
commit's diff of that file contains no deleted lines at all.

The whole thing was then proved against a real running binary, booted on a
private port against the local development database, with the listening pid
compared against the pid this review forked before a single request was sent
(`CLAUDE.md` §8b). No credential, a stale cookie, a garbage cookie, an
unusable bearer token, and an unusable bearer token together with a stale
cookie each answered 200 reporting the seeded admin; `/admin/settings`,
`/admin/interests` and `/admin/subscribers` each answered 200 with no
credential at all. Eight credential-less requests left exactly one new
`sessions` row, which this review then deleted by its own id.

### Fail-closed, by execution

With the flag true and `BASE_URL` set to the production host, the binary
logged the refusal naming both the flag and the offending value, exited
non-zero, and bound no port. A value for the flag that does not parse as a
boolean is a `config.Load` error rather than a silent false, so there is no
configuration that starts quietly in the wrong state.

### The loader-variable count, re-derived

An independent AST walk over the parent commit's and this commit's
`internal/config/config.go`, using the same predicate the guard uses, counted
21 and 22 respectively. The implementer's correction is right and the plan's
20 → 21 was wrong: `MEDIA_DIR` had already moved the real count to 21 while
the floor stayed at 20. All 22 names appear in `.env.example`, so
`TestEnvExampleCoversLoaderVariables` passes for the right reason and not
because the floor is slack.

Worth knowing about that guard: a floor only ever catches a shrink, so a
silent *growth* leaves its doc comment's stated count false — which is what
happened here for two issues running, with nothing complaining. The floor
itself behaved as designed; it is the prose beside it that drifted.

### Two accepted deviations, neither worth a bounce

Startup emits **two** WARN lines rather than the one the plan asked for — one
from `newDevAdminAutoLogin` naming the admin address, one from
`DevAdminAutoLogin` through its own `sync.Once`. Both were observed in the
live boot. The deviation is in the safe direction, the two lines carry
different information, and correcting it would cost a full implementation
pass to delete a warning; recorded here instead.

`scripts/dev.sh` still could not be run to completion. `:5173` and `:8080`
have been held since 2026-09-03 by a process tree this review did not start,
and signalling it is not this review's call. Everything short of binding was
exercised live rather than in a harness: flag parsing for `--postgres`, `-b
-p`, an unknown flag (exit 1) and `-h` (55 lines, the whole header), the
postgres environment block, `is_true` against an explicit override, and the
preflight and banner lines under both backends. The script correctly refused
to reclaim a port it did not start, and named the holder. `shellcheck -x` and
`bash -n` are clean, and a scan for bash-4 constructs found only a mention
inside a comment, not a use.


## Proposed fix

A `DEV_ADMIN_LOGIN=true` env var, honoured on the Postgres path, that
establishes a session for the configured admin exactly as `DevAutoLogin` does
for the dev store.

**Guard it with the pattern this repo already has**, not a new one:
`checkMailerNoOp`/`mailerNoOpAllowed` in `cmd/opencircuit/main.go` refuse to
start unless `BASE_URL`'s host is `localhost` or `127.0.0.1`. `CLAUDE.md` §10
records that guard as correct and not to be weakened. `DEV_ADMIN_LOGIN` must
refuse identically — **fail to boot**, not silently ignore the flag, so a
production host carrying it dies loudly rather than serving an open admin
console.

Note this is a strictly stronger requirement than `DevAutoLogin`'s current
`devMode` panic, which keys off `STORAGE=json` and therefore cannot be reused
as-is.

## Plan

**The gate, decided first, because every step below follows from it.**
`DEV_ADMIN_LOGIN=true` **plus** a `BASE_URL` whose host is exactly
`localhost` or `127.0.0.1`. Set the flag with any other `BASE_URL` and
`servePostgres` returns an error, `main`'s `log.Fatalf` prints it and the
process exits non-zero — the service does not start at all. This is
`checkMailerNoOp`'s shape, deliberately, and it reuses that guard's own host
predicate rather than inventing a second one (`CLAUDE.md` §10 records the
mailer guard as correct and not to be weakened; step 2 preserves it byte-for-byte
in behaviour, name, and tests).

**The construction invariant that makes `#0409` unreachable.** The new
middleware never branches on a credential being *present*. It branches only on
a credential being *usable*, where usable means `ResolveSession` returned a
live session — the same call `RequireSession` will make a moment later. And
when it mints, it **replaces** the request's credential rather than appending
one, so the post-condition holds unconditionally:

> after the middleware runs, the token `RequireSession` will extract from the
> request is exactly the token whose validity this middleware just established.

A stale, revoked, garbage, or expired cookie therefore cannot survive into
`RequireSession`, because minting overwrites it. That is a property of the
control flow, not a case that was remembered.

### 1. `internal/config/config.go` — the flag

1. Add `DevAdminLogin bool` to `Config`, next to `MailerNoOp`, with a doc
   comment naming `DEV_ADMIN_LOGIN` and stating it is honoured only on the
   Postgres path and only for a loopback `BASE_URL`.
2. In `loadFromFile`'s struct literal add
   `DevAdminLogin: getBool("DEV_ADMIN_LOGIN", false, &errs),`. Do **not** add
   it to the `required` slice — absent means off.
3. Add `DEV_ADMIN_LOGIN=false` to `.env.example` (it is copied verbatim onto
   production by `docs/deployment.md`'s install step, so shipping an explicit
   `false` is the point), with a two-line comment saying local-Postgres
   development only and that the service refuses to boot with it set outside
   `localhost`/`127.0.0.1`.
4. `internal/config/env_example_guard_test.go` carries a floor,
   `minLoaderVariables`, currently `20`, with a doc comment stating config.go
   "reads exactly 20 variables today". Raise the constant to `21` and update
   that sentence. This is a floor mutated *upward*, which `CLAUDE.md` §8 says
   is observable in Go — no external harness needed.

### 2. `cmd/opencircuit/main.go` — one host predicate, two callers

5. Extract the body of `mailerNoOpAllowed` into a new
   `baseURLIsLoopback(baseURL string) bool` with the same semantics (parse
   failure ⇒ false; `Hostname()` must equal `localhost` or `127.0.0.1`
   exactly). Leave `mailerNoOpAllowed` in place as a one-line call through to
   it, keeping its name, its doc comment's `MAILER_NOOP` framing, and
   `cmd/opencircuit/mailer_noop_test.go` untouched and still passing. Do not
   duplicate the five lines, and do not rename or delete `mailerNoOpAllowed` —
   §10 forbids weakening that guard, and preserving the identifier is the
   cheapest proof that nothing about it changed.

### 3. `internal/middleware/devauth.go` — a sibling, not an extension

6. Add `DevAdminAutoLogin` **alongside** `DevAutoLogin`, in the same file, and
   change nothing about `DevAutoLogin` (`#0409` asks explicitly that the
   `STORAGE=json` path not move as a side effect; the presence-vs-validity
   question on that path is `#0409`'s to answer). Three reasons it is a
   sibling rather than a parameter on the existing function: its guard keys on
   `BASE_URL`, not on `STORAGE=json`; its data seam is Postgres session
   minting plus resolution, not `devstore.CreateDevSession`; and one function
   carrying two mutually exclusive guards is weaker than two functions each
   carrying one. The name matches what `#0409` already calls it.
7. Declare the seam next to it:

   - `type DevAdminSessionMinter func(ctx context.Context) (token string, expiresAt time.Time, err error)`
   - the resolver is the existing `SessionResolver` in
     `internal/middleware/auth.go`, which `*auth.Store` already satisfies via
     `ResolveSession`.

   Do **not** add an exported session-minting method to `auth.Store`. The
   closure is built in `servePostgres` from pieces that are already exported
   (`auth.NewSessionToken` and `Store.CreateSession`, whose `q` parameter the
   live `*pgxpool.Pool` satisfies structurally), so this issue leaves the
   `internal/auth` API surface unchanged.
8. Signature: `func DevAdminAutoLogin(resolver SessionResolver, mint DevAdminSessionMinter, allowed bool) func(http.Handler) http.Handler`.
   It panics at construction when `allowed` is false, or when `resolver` or
   `mint` is nil — the same structural backstop `DevAutoLogin` has. The
   load-bearing refusal is step 10's startup error; this panic exists so a
   future miswiring cannot construct it silently.
9. Use a **new** `sync.Once` (e.g. `devAdminAuthOnce`) for the startup warning.
   Sharing `devAuthOnce` with `DevAutoLogin` would swallow one of the two
   warnings. Request behaviour, in exactly this order:

   a. `tok := sessionToken(r)` — the *same* unexported extractor
      `RequireSession` uses, so header-vs-cookie precedence can never diverge
      between the two.
   b. If `tok != ""` **and** `resolver.ResolveSession(r.Context(), tok, time.Now())`
      returns no error: call `next` with the request untouched. No mint, no
      `Set-Cookie`, no new row. This is the only pass-through branch, and it is
      keyed on validity.
   c. Otherwise obtain a token from the cache-or-mint helper (step 9d), then:
      `auth.SetSessionCookie(w, token, expiresAt)`; rebuild the request's
      `Cookie` header from `r.Cookies()` with any existing
      `auth.SessionCookieName` entry **dropped** and the new one appended, via
      `r.Header.Set("Cookie", …)` — never `r.AddCookie`, which appends and
      leaves a stale value first in the header where `r.Cookie` will find it;
      and `r.Header.Del("Authorization")` when the incoming header carried a
      bearer scheme, since `sessionToken` prefers it and it has just been
      judged unusable. Then call `next`.
   d. Cache-or-mint, guarded by a `sync.Mutex` held across the whole
      operation: if a cached token exists **and** `ResolveSession` says it is
      still live, reuse it; otherwise call `mint`, store the result, and use
      it. Validating before reuse is what stops a cached token whose row was
      deleted (a `db-reset.sh`, a `TRUNCATE`, a logout) from being handed out
      dead. Holding the mutex across the whole operation makes "a first page
      load's twenty parallel asset requests create exactly one `sessions` row"
      true rather than probable; this is a dev-only path, so the
      serialisation costs nothing that matters.
   e. A `mint` error is a 500 with a named message and an `slog.Error`,
      mirroring `DevAutoLogin`'s existing failure branch.
10. The doc comment must state the step-8 invariant in full, must say that it
    is wired only from `servePostgres` behind the loopback check, and must say
    that sign-out is a no-op while the mode is active — `POST /auth/logout`
    deletes the row and clears the cookie, and the next request mints a fresh
    session, which is the mode's contract ("you are the admin") and is the
    opposite failure from `#0409`'s stuck 401. Per `CLAUDE.md` §8's rename
    hazard note, every new doc comment must name its own declaration.

### 4. `cmd/opencircuit/main.go` — the startup gate

11. Add `newDevAdminAutoLogin(ctx context.Context, cfg *config.Config, store *auth.Store, pool *pgxpool.Pool, logger *slog.Logger) (func(http.Handler) http.Handler, error)`,
    placed next to `checkMailerNoOp`, doing exactly this in this order:

    a. `if !cfg.DevAdminLogin { return nil, nil }` — off changes nothing.
    b. `if !baseURLIsLoopback(cfg.BaseURL) { return nil, fmt.Errorf(…) }` —
       the error text must name `DEV_ADMIN_LOGIN`, the offending `BASE_URL`,
       and the rule, mirroring `checkMailerNoOp`'s wording. **This check
       precedes any use of `store` or `pool`**, which is a testable property:
       step 15 passes nil for both.
    c. Resolve the identity: `store.LookupUserByEmail(ctx, strings.ToLower(strings.TrimSpace(cfg.AdminEmail)))`.
       On `auth.ErrUserNotFound`, return an error naming the address **and the
       command that fixes it** — `opencircuit seed` (or `scripts/db-reset.sh`,
       which runs it). On `!acct.Active`, return an error saying the account is
       deactivated. Never create the row: `ensureAdminUser` in
       `cmd/opencircuit/seed.go` is the one place a user is created, and this
       mode must not become a second one.
    d. One `logger.Warn` through `devAdminAuthOnce`'s counterpart at the
       middleware layer — pass the logger down or warn here, either is fine,
       but exactly one line, naming the admin address and carrying
       `DevAutoLogin`'s "NEVER run this in production" wording plus "refused
       outside localhost/127.0.0.1".
    e. Build the minter closure over `store`, `pool`, and `acct.ID`:
       `auth.NewSessionToken()`, then `store.CreateSession(ctx, pool, acct.ID, token, time.Now())`,
       returning `(token, expiresAt, err)`. Note this closure must use the
       **request's** context, not the startup one — take `ctx` from the
       `DevAdminSessionMinter` parameter and ignore the captured startup ctx.
    f. Return `middleware.DevAdminAutoLogin(store, minter, true)`.

### 5. Wiring

12. In `servePostgres`, immediately after `requireAdmin` is built and before
    the `return mountAndServe(...)`, call `newDevAdminAutoLogin` and return
    its error unchanged. Replace the `nil, /* no outer middleware in
    production */` argument with the returned value — a nil result when the
    flag is off, so the production path is byte-for-byte the same handler
    chain it is today.
13. Update `mountAndServe`'s doc comment, which currently says
    `outerMiddleware` is "used in dev mode only" and that "the production path
    always passes nil". It now also carries the Postgres dev-admin middleware,
    and is nil on the Postgres path only when `DEV_ADMIN_LOGIN` is off.
    Editing a Go comment means `internal/handlers` is in the verification
    scope (`CLAUDE.md` §5).

### 6. Tests

14. New `internal/middleware/devadminauth_test.go` (no database; extend the
    existing `fakeDevStore` pattern with a fake that counts mint calls and can
    have a token invalidated out of band). Leave
    `internal/middleware/devauth_test.go` untouched. Cover: construction
    panics when `allowed` is false; no credential ⇒ mints, sets the response
    cookie, and the composed `RequireSession` chain answers 200 with the admin
    on the context; a **valid** cookie ⇒ zero mint calls and no `Set-Cookie`;
    a **stale** cookie (token whose row is gone) ⇒ 200, and the request's
    `Cookie` header carries exactly one session cookie whose value is the new
    token; a **garbage** cookie ⇒ same; an unusable `Authorization: Bearer`
    header with no cookie ⇒ 200 (proving the header was dropped); ten
    successive credential-less requests ⇒ exactly one mint call; and, after
    invalidating the cached token in the fake, the next request mints again.
    The stale/garbage/valid trio is `#0409`'s acceptance criteria proved here
    at construction time rather than as a later fix.
15. New `cmd/opencircuit/devadmin_test.go`, no database, for
    `newDevAdminAutoLogin`: off ⇒ `(nil, nil)` and no log output; on with
    `BASE_URL: "https://www.opencircuitsf.com"` and **nil `store` and `pool`**
    ⇒ error (which simultaneously proves the refusal precedes every database
    access); a table over the same host cases `TestMailerNoOpAllowed` uses,
    driven through `baseURLIsLoopback`.
16. A DB-backed test in the same file for the real route table, following
    `cmd/opencircuit/admin_wiring_test.go`'s harness shape: skip when
    `TEST_DATABASE_URL` is unset; ephemeral `127.0.0.1:0` port (`CLAUDE.md`
    §8b); **no `t.Parallel()`** anywhere in this package
    (`TestNoWiringTestUsesParallel`' subject, enforced by the AST guard in
    `cmd/opencircuit/wiring_parallel_guard_test.go`). Do **not** call
    `truncateAdminWiringTables` and therefore do not need `registerWiringTest`:
    seed a uniquely-named admin (`fmt.Sprintf("devadmin-%d@localhost", time.Now().UnixNano())`)
    and clean up only that user's own rows, never a literal or seeded id
    (`CLAUDE.md` §8b). Assert: with the middleware wired, `GET /api/me` with no
    cookie answers 200 and reports that admin; with `nil` passed for
    `outerMiddleware`, the identical request answers 401; and repeated
    credential-less requests leave exactly one row in `sessions` for that user.
17. A startup-refusal proof for acceptance criterion 9: the mutation is
    flipping `BASE_URL`'s host, and step 15's nil-store case *is* that
    assertion. It belongs in Go, not in an external harness, because mutating
    the host changes the value under test rather than disabling the comparison
    (`CLAUDE.md` §8's floor-vs-ceiling rule).

### 7. `scripts/dev.sh`

18. Replace the single-flag `case` with a loop over `"$@"` accepting
    `--built`/`-b` and a new `--postgres`/`-p` (combinable, order-independent —
    `scripts/db-reset.sh` already had to learn this lesson). `--postgres` sets:
    `STORAGE=""`, `DATABASE_URL="${DATABASE_URL:-postgres://opencircuit:opencircuit@localhost:5432/opencircuit?sslmode=disable}"`
    (the DSN `scripts/db-reset.sh` builds by default), `DEV_ADMIN_LOGIN=true`,
    and `MAILER_NOOP=true` — the last because `newSESSender` otherwise
    constructs the real SES mailer and `mailing.NewSESMailer` refuses without
    `SES_CONFIGURATION_SET`, so the service would not boot. All four honour a
    caller-supplied value.
19. Make the banner honest (acceptance criterion 7): print the "logs in
    automatically as …" line only when auto-login is genuinely active — always
    under `STORAGE=json`, and under Postgres only when `DEV_ADMIN_LOGIN=true`.
    Otherwise print the truth: no session is created, and `/admin` will answer
    401. Also extend the preflight `ok` lines to report the storage mode and,
    under Postgres, the database.
20. The header comment must document `--postgres`. Note the `-h` branch prints
    a **fixed line range** of this file, so extending the header means
    re-checking that range still covers it — verify by running the help flag,
    not by reading the code.
21. `bash` here is 3.2.57 (`CLAUDE.md` §8): no `${var,,}`, no `declare -A`, no
    `mapfile`. Keep the flag loop to POSIX-ish `case`.

### 8. Docs

22. `docs/dev.md`: a new "Running against Postgres" section — why you would
    (`STORAGE=json` has no mailing list, `CLAUDE.md` §5), the one command
    (`./scripts/dev.sh --postgres`), the prerequisite that the admin row must
    already exist and how to create it, `DEV_ADMIN_LOGIN`'s localhost-only
    refusal, and the sign-out caveat from step 10. Add `DEV_ADMIN_LOGIN` and
    `DATABASE_URL` rows to that file's environment-variable table.
23. `docs/configuration.md`: one table row for `DEV_ADMIN_LOGIN` — not
    required, default `false`, what it does, and that the service refuses to
    start with it set against a non-loopback `BASE_URL`. Match the
    `MAILER_NOOP` row's level of detail.

### 9. Verification

24. Scoped run, own database:
    `ISSUE=0402 scripts/check.sh go ./internal/middleware/... ./internal/config/... ./cmd/opencircuit/... ./internal/handlers/... ./internal/db/...`
    — `internal/config` for the `.env.example` floor, `internal/handlers`
    because Go comments changed, `internal/db` for the docs and issue-file
    guards.
25. Confirm the DB-backed packages reported `ok` and that nothing skipped
    (`CLAUDE.md` §5) — a `TEST_DATABASE_URL`-less run proves nothing here.
26. `scripts/check.sh guards` covers `scripts/dev_guard_test.sh`, which costs
    ~48s and **binds `:5173`**. Run it only after confirming that port is free;
    it is the one step in this issue that can collide with another agent.
27. Do not start `scripts/dev.sh` to "see it work" while another agent may hold
    `:5173` or `:8080`. If a manual end-to-end check is wanted, use
    `--postgres` with an explicit free `PORT` and say so in `## Verification`.
    Redact any token from a pasted transcript (`CLAUDE.md` §8b).

## Findings

Measured against the tree at planning time, load average 2.84.

**The Postgres path really has no seam missing except the one.**
`mountAndServe` already takes an `outerMiddleware func(http.Handler) http.Handler`
parameter and applies it as the outermost wrapper around the whole mux;
`serveDevMode` passes `DevAutoLogin`'s result and `servePostgres` passes a
literal `nil` with the comment "no outer middleware in production". Nothing
else in the repo references `outerMiddleware`, and no AST guard asserts the
Postgres call site passes nil. So this issue adds no plumbing — it fills an
argument that already exists.

**`DevAutoLogin` cannot be reused, for two independent reasons.** Its guard
takes `devMode bool` and panics when false, and `servePostgres` is by
definition `DevMode() == false`. Its data seam is
`devSessionCreator.CreateDevSession(userID, ttl)`, which `*auth.Store` does not
have and which carries no `context.Context`. It also holds `devAuthAdminID = 1`
as a package constant mirroring the dev store's seeded id — a literal id, which
`CLAUDE.md` §8b forbids aiming at a real database.

**Minting a Postgres session needs no new exported API.**
`auth.NewSessionToken` and `Store.CreateSession(ctx, q, userID, token, now)` are
both already exported. `CreateSession`'s `q` parameter has an unexported
interface type, so no other package can *name* it — but a `*pgxpool.Pool`
satisfies it structurally and can be passed as a value, and `servePostgres`
already holds the live pool. That is why the plan builds a closure at the wiring
site instead of adding a `MintSession` method: the "create a session for an
arbitrary user id with no authentication" capability stays confined to one
function in `main.go` rather than becoming permanent surface on `auth.Store`.

**Where `#0409`'s defect comes from, mechanically.** `DevAutoLogin` short-circuits
on `r.Cookie(auth.SessionCookieName)` returning any non-empty value, then, in
the minting branch, calls `r.AddCookie`. `AddCookie` *appends* to the existing
`Cookie` header, and `(*http.Request).Cookie` returns the **first** match — so
even if the presence branch were removed and the cookie were always overwritten
by `AddCookie`, a stale value would still win at extraction time. Both halves
have to change together: branch on validity, and replace the header rather than
append to it. The plan's step-8 invariant is what ties them.

**`RequireSession` reads through `sessionToken`, which prefers
`Authorization: Bearer` over the cookie.** Any middleware that wants to
guarantee what `RequireSession` will see must use that same extractor and must
neutralise the header when it mints — otherwise an unusable bearer token
produces exactly `#0409`'s 401, by a different route. Nothing in `#0409`
mentions this; it is the same defect one layer over.

**`ResolveSession` is a safe validity oracle and is idempotent enough to call
twice.** It bumps `last_seen_at` and extends `expires_at` on success, so the
middleware's check followed by `RequireSession`'s check just sets the same
sliding window twice in the same request. It returns `ErrSessionInvalid` for an
unknown or expired token and `ErrAccountInactive` for a deactivated owner, and
best-effort deletes an expired row. Treating any error as "not usable" is
correct for a dev bypass.

**The admin row must already exist, and two commands create it.**
`cmd/opencircuit/seed.go`'s `ensureAdminUser` inserts `ADMIN_EMAIL` with
`is_admin = TRUE, active = TRUE`, `ON CONFLICT (email) DO NOTHING`, then
re-promotes an existing row — idempotent. `scripts/db-reset.sh` runs
`go run ./cmd/opencircuit seed` as its last step unless `--no-seed` is passed,
defaulting `ADMIN_EMAIL` to `admin@localhost`. So on a database built by
`db-reset.sh`, the identity this mode assumes is already there; on any other
database it is one command away. `LookupUserByEmail` selects only `id` and
`active`, not `is_admin`, so the startup check proves the account exists and is
active, and `RequireAdmin` remains the thing that decides admin — a non-admin
`ADMIN_EMAIL` would get a 403, which is a correct and legible outcome.

**Where `checkMailerNoOp`'s pattern actually sits.** `mailerNoOpAllowed` parses
`BASE_URL` and compares `u.Hostname()` against `localhost` and `127.0.0.1`
exactly, treating an unparseable URL as disallowed. `checkMailerNoOp` returns
early when the flag is off, returns an error when it is on and disallowed, and
otherwise emits a single `slog.Warn`. `servePostgres` returns that error, and
`main` turns it into `log.Fatalf`. `TestMailerNoOpAllowed` already covers the
sharp cases a hand-rolled check gets wrong — `sub.localhost` and
`127.0.0.1.evil.com` both false — which is the strongest argument for reusing
the predicate rather than writing a second one.

**The residual, stated plainly.** An operator who sets `DEV_ADMIN_LOGIN=true`
*and* rewrites production's `BASE_URL` to a loopback URL would get an open admin
console behind Apache's proxy. This is the same residual `CLAUDE.md` §10 already
accepts for `MAILER_NOOP`, and the second half is self-defeating in a way that
does not stay hidden: `BASE_URL` is what every magic link, confirmation link and
unsubscribe URL is built from, and `WEBAUTHN_RP_ORIGIN` must match the browser's
real origin (§7), so a production instance pointed at `localhost` breaks visibly
within one email. Shipping `DEV_ADMIN_LOGIN=false` in `.env.example`, which is
what production's `config.env` is copied from, means the variable is present and
explicitly off rather than absent and unexplained. A per-request loopback check
was considered and rejected: production traffic reaches this service *through*
Apache on `127.0.0.1`, so a remote-address check would not distinguish the case,
and layering an `X-Forwarded-For` heuristic on top is exactly the second
mechanism `CLAUDE.md` §8 warns produces a sixth pass rather than a fix.

**`.env.example` is guarded, and the guard has a floor.**
`TestEnvExampleCoversLoaderVariables` re-derives the loader's variable set from
`config.go`'s AST and asserts each name appears as a `NAME=` line, with
`minLoaderVariables` currently `20` as a floor against the walk silently
shrinking. Adding a variable without adding the `.env.example` line fails the
guard; adding both leaves the floor stale at 20 while the real count is 21,
which is why the plan raises it.

**Guard-scope routing for this change.** `internal/config` reaches the repo root
for `.env.example`; `internal/handlers` reaches the whole tree for the
comment-citation family; `internal/db` reads `issues/`, `docs/`, `PRD.md` and
`CLAUDE.md`. All three are in scope here, plus `cmd/opencircuit` and
`internal/middleware` for the code itself. `internal/seo` is not — nothing here
touches `web/dist/index.html`.

**Cookie mechanics worth knowing before debugging a browser.**
`auth.SetSessionCookie` sets `Secure` and `SameSite=Strict`. Chrome and Firefox
treat `http://localhost` as a secure context and store such cookies; this is
already what makes `DevAutoLogin` work under `dev.sh` today, so the Postgres
mode inherits both the behaviour and the caveat rather than introducing one.

## Obstacles

1. **A local Postgres database with an admin row must exist.** Without it the
   new startup check refuses to boot — by design, but it will stop an
   implementer mid-verification. Clear it before starting:

   ```bash
   scripts/db-reset.sh            # rebuilds opencircuit from migrations and seeds admin@localhost
   ```

   It refuses without `--force` if anything else holds the database, and it is
   also the user's dev database (`CLAUDE.md` §5a) — so if it reports a live
   connection, do not force it; seed instead against the existing database:

   ```bash
   DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit?sslmode=disable' \
   BASE_URL=http://localhost:8080 WEBAUTHN_RP_ID=localhost \
   WEBAUTHN_RP_ORIGIN=http://localhost:8080 SESSION_SECRET=dev \
   ADMIN_EMAIL=admin@localhost AWS_REGION=us-east-1 \
   EMAIL_FROM='Open Circuit SF <dev@localhost>' EMAIL_LIST_DOMAIN=lists.localhost \
   go run ./cmd/opencircuit seed
   ```

2. **A per-agent test database, for the DB-backed test in step 16.** Use
   `ISSUE=0402 scripts/check.sh …`, which creates and drops one for you. Do
   **not** run `scripts/testdb.sh template` — this issue adds no migration, so
   the template needs no rebuild, and rebuilding it disturbs other agents.

   ```bash
   psql -tAc "select rolcreatedb from pg_roles where rolname=current_user"   # must be t
   ```

3. **`:5173` and `:8080` are shared.** `scripts/check.sh guards` binds `:5173`
   for ~48s via `scripts/dev_guard_test.sh`, and so does any `dev.sh` another
   agent is running. Check before running either, and never `pkill` by pattern
   to clear it (`CLAUDE.md` §5a, §8b):

   ```bash
   lsof -ti tcp:5173 -sTCP:LISTEN; lsof -ti tcp:8080 -sTCP:LISTEN
   ```

4. **Booting the Postgres path locally needs more than `DATABASE_URL`.**
   `config.Load` requires `BASE_URL`, `WEBAUTHN_RP_ID`, `WEBAUTHN_RP_ORIGIN`,
   `SESSION_SECRET`, `ADMIN_EMAIL`, `AWS_REGION`, `EMAIL_FROM` and
   `EMAIL_LIST_DOMAIN` regardless of storage, and without `MAILER_NOOP=true`
   the SES mailer refuses to construct without `SES_CONFIGURATION_SET`
   (`CLAUDE.md` §10). Step 18's `--postgres` flag exists precisely to stop this
   from being rediscovered by hand each time; until it lands, set them
   explicitly.

5. **No SES, no AWS, no production access needed.** Nothing in this issue sends
   mail or touches the live box, so none of `CLAUDE.md` §10's open items block
   it.

6. **No approval-gated step.** No migration, no destructive operation, no
   outward-facing change. The only guard-removal-style mutation the plan calls
   for (step 15's non-loopback host) is a pure-function argument, not a
   disabled authorization check, so `CLAUDE.md` §8b's private-database rule
   does not apply.

## Acceptance criteria

- [ ] `DEV_ADMIN_LOGIN=true` establishes an admin session on the `STORAGE=postgres` path without any passkey ceremony
- [ ] It is refused **at startup**, with a clear error and a non-zero exit, whenever `BASE_URL`'s host is anything but `localhost`/`127.0.0.1` — the same rule as `mailerNoOpAllowed`, tested the same way
- [ ] Unset or `false` changes nothing: the Postgres path behaves exactly as it does today, proved by a test that the admin routes still answer 401
- [ ] A single startup `WARN` is logged when it is active, matching `DevAutoLogin`'s wording about never running it in production
- [ ] The session it mints is a real `sessions` row with a normal TTL — not a bypass of `RequireSession`/`RequireAdmin`, which must keep running unchanged
- [ ] The admin user it targets is the configured `ADMIN_EMAIL`, and it fails clearly if no such user exists rather than creating one silently
- [ ] `scripts/dev.sh`'s "logs in automatically" banner tells the truth under both storage modes — it currently claims auto-login unconditionally
- [ ] `scripts/dev.sh` gains a documented way to run against Postgres; today `STORAGE`/`DATABASE_URL` must be discovered by reading the script
- [ ] `docs/dev.md` (and `docs/configuration.md`) document the variable and its localhost-only guard
- [ ] A guard test proves the flag cannot take effect on a non-localhost `BASE_URL` — the mutation to try is flipping the host, and the assertion is that startup fails

## Notes

Filed 2026-09-03 after `#0393` could not be exercised locally without hand-minting
a session row.

**Do not weaken `registrations_enabled`'s default or the uniform
`register/start` response to solve this.** Both are correct. The gap is the
absence of a dev affordance on the Postgres path, not a defect in the auth
flows — and a fix that loosens either one would trade a local inconvenience for
a production weakness.

Worth considering alongside: a `scripts/dev.sh --postgres` flag, so the whole
local mailing-subsystem loop is one documented command rather than three env
vars.

## Relation

- Relates to: [#0393](0393.md) — the CRT commands admin section that surfaced this
- Relates to: [#0117](0117.md) — `dev.sh`'s guard tests, where the banner-accuracy check belongs
