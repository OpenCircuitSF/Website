# 0241 — The SSE key-set parity check is one-directional

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | All |
| **Module** | handlers / web |
| **Phase** | 8 — Polish and follow-ups |
| **First seen** | 2026-08-24 |

## Description

`#0095` built a parity guard tying the client's complained-subscriber copy to
the server's, and deliberately did **not** extend it to the CampaignProgress SSE
payload — using that issue's own escape hatch, and recording the narrowing
rather than taking it silently. That was the right call: the SSE case is a
structurally different problem (a Go struct versus a TypeScript interface,
rather than two string constants).

But the concern `#0095`'s 2026-08-22 amendment raised is still live and should
not die with the issue that declined it:

**A field added on the TypeScript side alone is caught by nothing.** The
existing assertion checks that the keys the server sends are handled by the
client. It does not check the converse. So a client that reads
`progress.remaining` when the server never sends it compiles, type-checks,
passes every test, and silently renders `undefined` — during a campaign send,
which is the one operation in this system with no undo.

## Acceptance criteria

- [x] The SSE payload's key set is pinned in both directions: a field added on either side alone fails
- [x] Proved by mutation in both directions — add a field to the Go struct only, then to the TypeScript interface only, and confirm each fails naming the side that diverged
- [x] The guard reads both from source rather than restating either by hand; a hand-maintained third list is a third thing to drift
- [x] It follows the established parity-guard shape (`routes_parity_test.go`, `docs_parity_test.go`, and `#0095`'s own `complained_copy_parity_test.go`) rather than inventing a fourth pattern
- [x] `ISSUE=0241 scripts/check.sh go ./internal/handlers/...` and `scripts/check.sh web` both pass

## Fix

Added `internal/handlers/campaign_progress_parity_test.go`
(`TestCampaignProgressParity_KeySet`), in the same filesystem-only, no-database
shape as `#0071`'s `routes_parity_test.go` and `#0095`'s
`complained_copy_parity_test.go`.

- **Go side is read via `reflect`, not text parsing.** Unlike `router.ts`'s or
  `preferences.go`'s Go halves — plain string literals the existing guards have
  no choice but to regex out of source text — `mailing.CampaignProgress` is an
  importable Go type. `reflect.TypeOf(mailing.CampaignProgress{})`'s `json`
  struct tags are the actual field names `encoding/json` emits, always in sync
  with the compiled program by construction and immune to the
  gofmt-reformat-breaks-the-regex failure mode `routes_parity_test.go`'s own
  doc comment describes. This is the honest tool here, and it sidesteps
  needing a second Go-source parser: there is nothing more authoritative to
  read than the live type itself.
- **TypeScript side has no such shortcut** — an `interface` is erased at build
  time — so `parseCampaignProgressTS` reads `web/src/lib/campaignProgress.ts`'s
  `CampaignProgress` interface block directly: an anchored block regex
  (`campaignProgressInterfaceBlockRe`) captures the declaration body, then a
  per-line walk requires every non-blank, non-comment line to match
  `campaignProgressFieldLineRe` (`name: type;` / `name?: type;`), tracking
  `/** ... */` JSDoc state (the `status` field carries a multi-line one) so
  those lines are skipped without needing to match the field regex. Any
  unrecognized line is a `campaignProgressTSParseError`, not a silent skip —
  the same structural fix `#0071`'s review required of
  `parseStaticRoutesFromRouterTS`, so a field can't go missing by "not
  matching" a scan-and-skip parser.
- **Fail-closed floor (#0275).** Both sides assert they found a plausible
  number of keys (`len(...) == 0` is fatal on the Go side via
  `campaignProgressGoJSONKeys`; a `minPlausibleKeys = 5` floor on both sides in
  the test itself) before comparing — an empty or near-empty extraction is
  treated as "the parser broke," not as "there's nothing to compare."
- **Only the key set is compared**, not field types — matching the acceptance
  criteria's own framing ("a field added on either side alone fails"). This
  guard doesn't reach for `go/types` because it never claims type equivalence
  in the first place, so there's no name-match/type-match gap to fall into
  (`CLAUDE.md`'s `#0282` note) — it's an honest string-set comparison, not a
  disguised type check.
- Corrected three doc comments that described this agreement as hand-kept —
  `internal/mailing/worker.go`'s `CampaignProgress` doc comment,
  `web/src/lib/campaignProgress.ts`'s `CampaignProgress` interface doc
  comment, and two spots in `internal/handlers/campaign_progress_test.go`'s
  `TestCampaignProgressPublisher_PublishesJSONFrameToBroker` — to name
  `TestCampaignProgressParity_KeySet` and be explicit that the existing
  hand-typed `want` map in that test only ever catches a Go-side addition.
  That test is kept, not replaced: it proves the real marshal output for
  concrete field values, which the new guard (key names only) doesn't.

### Mutation testing

Both directions proved to fail, then reverted and hash-verified restored (per
`CLAUDE.md` §8a — copied both files aside first rather than `git stash`, since
another agent had ~21 dirty files in the tree throughout, including files in
`internal/subscribers` and `internal/handlers`):

- **Go-side mutation** — added `StartedAt int64 \`json:"started_at"\`` to
  `mailing.CampaignProgress` in `worker.go`. Result:
  ```
  campaign_progress_parity_test.go:247: campaign progress parity: 1 key(s)
  present in mailing.CampaignProgress's json tags but absent from
  ../../web/src/lib/campaignProgress.ts's CampaignProgress interface: started_at
  add the missing field(s) to web/src/lib/campaignProgress.ts's CampaignProgress interface
  --- FAIL: TestCampaignProgressParity_KeySet
  ```
  Reverted; `shasum -a 256` matched the pre-mutation hash
  (`fbeef1c551160a8fe49b1da70be04b51faae4d46558c66ae00feb73b0619fba7`), and
  `git diff -- internal/mailing/worker.go` showed nothing after restoring.
- **TypeScript-side mutation** — added `eta_seconds: number;` to
  `campaignProgress.ts`'s `CampaignProgress` interface. Result:
  ```
  campaign_progress_parity_test.go:240: campaign progress parity: 1 key(s)
  present in ../../web/src/lib/campaignProgress.ts's CampaignProgress interface
  but absent from mailing.CampaignProgress's json tags: eta_seconds
  either add the missing field(s) to internal/mailing.CampaignProgress (worker.go),
  or the client is reading a key the server never sends
  --- FAIL: TestCampaignProgressParity_KeySet
  ```
  Reverted; `shasum -a 256` matched the pre-mutation hash
  (`821edbb345756fa6ba9354420fdb5b5603a891452f531bdc6139ae9b205e6d5f`), and
  `git diff -- web/src/lib/campaignProgress.ts` showed nothing after restoring.

Each mutation was caught by the guard in the specific direction it introduced,
naming the side that diverged, as the acceptance criteria require.

## Verification

- `ISSUE=0241 scripts/check.sh go ./internal/mailing/...` — build, vet,
  `gofmt -l` clean, `go test ./internal/mailing/... -p 2` → `ok`, own database
  (`opencircuit_test_0241`), dropped on exit.
- `go test ./internal/handlers/... -run TestCampaignProgressParity -v` — passes
  against the real tree (`--- PASS: TestCampaignProgressParity_KeySet`), plus
  the two mutation runs quoted above.
- `go test ./internal/handlers/... -p 2 -count=1` (own scratch database
  `opencircuit_test_0241zz`) — one pre-existing failure,
  `TestNoCommentCitesUndefinedTestFunction`, confirmed via
  `git status --porcelain` to be caused entirely by another agent's in-flight,
  uncommitted `internal/subscribers/imports_test.go` (untracked, not authored
  by this issue) citing a test function that file doesn't yet define. Nothing
  in this issue's files is implicated; every other test in the package,
  including the new guard, passed.
- `scripts/check.sh web` — `npm run check`: 287 files / 0 errors / 0 warnings.
  `npm test`: 40 test files / 3,932 tests passed.
- `gofmt -l` on all four changed files — clean.
- `pgrep -f 'go test|\.test '` — none left running; both scratch databases
  (`opencircuit_test_0241`, `opencircuit_test_0241zz`) dropped.

## Files changed

- `internal/handlers/campaign_progress_parity_test.go` — new. The bidirectional
  guard: `TestCampaignProgressParity_KeySet`, the reflection-based Go-side
  reader, the TS-interface parser, and `campaignProgressTSParseError`.
- `internal/handlers/campaign_progress_test.go` — two doc comments corrected to
  name the new guard and be explicit about what the existing hand-typed
  key-count check does and doesn't catch.
- `internal/mailing/worker.go` — `CampaignProgress`'s doc comment corrected to
  name the new guard instead of asserting an unenforced "keep in sync by hand"
  promise.
- `web/src/lib/campaignProgress.ts` — `CampaignProgress` interface's doc
  comment corrected the same way.

## Dirty files skipped

Not touched, per this task's instruction to skip anything dirty from the other
agent's in-flight feature: `cmd/opencircuit/main.go`,
`internal/handlers/admin_subscribers.go`, `internal/subscribers/*`,
`internal/audit/actions.go`, `migrations/*`, `docs/database.md`, `CLAUDE.md`,
and (newly dirty mid-session) `internal/handlers/misattached_doc_comment_guard_test.go`,
`internal/mailing/recording_mailer.go`, `internal/mailing/recording_mailer_test.go`,
`internal/mailing/worker_store_test.go`, `internal/mailing/worker_test.go`,
`internal/mailing/worker_test_helpers_test.go`. None overlapped the files this
issue needed to change.

## Notes

Reported by `#0063`/`#0095`'s phase-3 review under `CLAUDE.md` §9.

Parsing a Go struct and a TypeScript interface and comparing field sets is more
work than comparing two string constants, which is exactly why `#0095` declined
it and why it deserves its own issue rather than being bolted on. `#0095`'s test
already parses TypeScript with the technique this needs.

Phase 8 rather than 7: nothing blocks on it, and the send path it protects is
already guarded at the points that matter (`#0045`'s physical-address refusal,
`#0124`'s circuit breaker). This closes a reporting gap, not a sending one.

## Relation

- Found by: [#0095](0095.md) — the copy parity guard, whose narrowing this completes
- Relates to: [#0071](0071.md), [#0082](0082.md), [#0083](0083.md) — the established parity-guard pattern
- Relates to: [#0081](0081.md) — the async send queue whose progress this reports

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-27 | implement | claude-sonnet-5 | — | — | — | — | — |

Token counts intentionally left as `—`: per `issues/Issues.md`'s "Token usage and
cost tracking," the implementation subagent cannot measure its own totals — the
orchestrator sums them from this session's transcript after the subagent returns.
