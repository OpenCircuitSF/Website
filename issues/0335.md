# 0335 — `workshopCacheInvalidator` is misnamed; it also serves the campaign archive toggle

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | handlers / seo |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-28 |
| **Commit** | `83036c3` |

## Description

`#0325` renamed `InvalidateWorkshops` to `Invalidate` because the method had
outgrown its name: it invalidates the SEO caches for workshop mutations, the
campaign archive toggle (`#0319`), and the send worker. That rename is done.

The **interface holding it** was not renamed. `workshopCacheInvalidator` in
`internal/handlers` still carries the narrow name, and since `#0319` it is
satisfied by, and passed to, the campaign archive handler as well as the
workshop handlers. So the type name now makes the same false promise the method
name used to.

`#0325`'s implementer found this while doing the rename and correctly declined
to widen its own scope — the issue's criteria named the method, not the type.

Low priority: this is a naming defect with no behavioural consequence. It earns
an issue rather than a note because the next reader of
`internal/handlers` will reasonably conclude the archive toggle does not
invalidate SEO caches, which is exactly the wrong belief `#0319` was filed to
correct.

## Acceptance criteria

1. `workshopCacheInvalidator` is renamed to something naming what it does
   rather than one of its callers — it invalidates SEO caches, whatever mutated.
2. Every implementer, test double, and call site is updated; the change is
   rename-and-comments only, with no behavioural difference.
3. Doc comments name the current callers (workshop mutation, campaign archive
   toggle, send worker), consistent with what `#0325` wrote for the method.
4. Proved mechanical **structurally**, not with `grep -v '^[+-]#'`: parse both
   revisions without comments and compare the executable content.
5. Confirm the shortened or changed name does not accidentally satisfy or
   shadow another interface — a Go rename that shortens an identifier is how
   accidental interface satisfaction happens.
6. `gofmt` stability proved: file sha256 equals sha256 of `gofmt` stdout (§8).
7. `#0326`'s `servepostgres_seo_instance_guard_test.go` recognises site-shaped
   parameters by resolving a locally-declared interface whose method set is
   exactly `Invalidate()` — confirm the rename does not break that discovery,
   and re-run its mutation proofs.

## Relation

- Found by: [#0325](0325.md)'s implementation pass, which declined it as out of
  its mandated scope
- Depends on: [#0325](0325.md) — the method rename this completes
- Relates to: [#0319](0319.md) — which made the archive toggle a second caller
- Relates to: [#0326](0326.md) — whose guard resolves the `Invalidate()` method
  set and must keep working across this rename
- Relates to: [#0051](0051.md) — cited historically by the doc comments

## Work log

- 2026-08-28 — Filed by the orchestrator from `#0325`'s implementation report.
  Subagents do not file issues (§9).
- 2026-08-31 — Implemented (Sonnet, phase 2), as one commit (`83036c3`) with
  `#0337` and `#0338` — the orchestrator dispatched all three as one
  coherent SEO/cache-invalidator family, ordered `#0335` first per its own
  suggestion (the rename lands before `#0338`'s floor is re-measured against
  the post-rename tree).
  `workshopCacheInvalidator` renamed to `seoCacheInvalidator` in
  `internal/handlers/admin_workshops.go`, with every call site
  (`admin_campaign_archive.go`), test double
  (`countingWorkshopInvalidator` → `countingCacheInvalidator`, reused by both
  `admin_workshops_test.go` and `admin_campaign_archive_test.go`), and doc
  comment updated to name the current three callers (workshop mutation,
  campaign archive toggle, send worker) — the same set `#0325` wrote for the
  method. `cmd/opencircuit/servepostgres_seo_instance_guard_test.go`'s own
  doc-comment mention of the old name updated too (criterion 2's "every ...
  call site" reading extended to the one doc-comment citation in a file this
  issue's own file set names).
  **Criterion 4 (mechanical, structurally).** Parsed both revisions
  (`HEAD` and the working tree) of all four touched handler files with
  `go/parser`, stripped `Comments`/`Doc`, applied the rename map
  (`workshopCacheInvalidator`→`seoCacheInvalidator`,
  `countingWorkshopInvalidator`→`countingCacheInvalidator`) to every
  `*ast.Ident`, re-printed with `go/format`, and hashed: all four files
  hash byte-identical before/after — the change is rename-and-comments only,
  confirmed structurally rather than by `grep -v '^[+-]#'`.
  **Criterion 5.** `seoCacheInvalidator`'s method set (`Invalidate()`) is
  unchanged by this rename — only the interface's own identifier moved, not
  its method — so this rename does not itself alter which types satisfy it;
  the pre-existing/growing satisfier-set risk from `#0325`'s *method* rename
  is `#0337`'s own scope, fixed in the same commit. Grepped for any other
  declaration of `seoCacheInvalidator` (none) to confirm no collision.
  **Criterion 7.** Re-ran `#0326`'s guard and its full mutation suite
  (seven of `#0326`'s own plus one new to `#0338`) after the rename, in a
  throwaway `git worktree` — see `#0338`'s own Work log entry for the full
  mutation table; all behave identically with the renamed interface.
  **Verification.** `uptime` before starting: load average 2.64–2.98.
  `ISSUE=0335 scripts/check.sh go ./internal/handlers/... ./internal/seo/...
  ./cmd/...` — VERIFICATION PASSED, zero skips,
  `internal/handlers`/`internal/seo`/`cmd/opencircuit` all `ok`. `gofmt`
  stability (criterion 6, and general for every touched file): `sha256(file)
  == sha256(gofmt stdout)` for all ten touched/added files, confirmed
  individually, not just via `check.sh`'s `gofmt -l` (clean). No SPA files
  touched, so `scripts/check.sh web` not run.
  Left for review: `resolved` is the reviewer's call, not the implementer's.
