# 0137 — The workshops API never accepts a slug, so `#0052`'s "editable before first publish" criterion cannot be built

| | |
|---|---|
| **Status** | in-progress |
| **Module** | handlers / workshops / web |
| **Platform** | All |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |
| **Commit** | `b619eba` |

## Description

`#0052`'s acceptance criteria require the admin UI to let a slug be edited before a workshop is first published. `#0051`'s API makes that impossible: neither the create nor the patch request struct carries a `slug` field, and the store generates it server-side from the title with a collision suffix. `#0052`'s reviewer verified both — no `slug` on either struct, no route that accepts one — and ruled the criterion unbuildable in the UI, leaving it unticked.

So one of the two is wrong, and this issue decides which.

## Root cause — a decision, not a bug

Not a bug to diagnose: `#0052` and `#0051` genuinely disagreed about whether a
slug should be editable, and `#0051` shipped first, so the API's contract
(immutable, server-generated) is what actually exists. This issue settles the
disagreement in the API's favor.

**Decision: option (a).** Slugs stay server-generated and immutable through
both `POST /admin/workshops` and `PATCH /admin/workshops/{id}`. Reasoning,
carried forward from the orchestrator's brief and confirmed against the code
rather than re-litigated:

- `#0052`'s own `## Notes` already argue for this: a stable, server-generated
  slug is one less thing an admin can get wrong, and the collision-suffix
  retry in `internal/workshops/store.go`'s `Create` already guarantees
  uniqueness without any client input.
- `#0144`/`#0145`'s review established that `internal/workshops` `slugify`
  is an ASCII allowlist with exactly **one** write path — `Create`'s
  `INSERT`; `Update` deliberately sets eleven columns and never `slug`. That
  single-write-path guarantee is *why* `#0145` could safely interpolate a
  slug into a URL without re-validating it at read time. Making slugs
  client-supplied would reopen a second write path and pull `#0138`'s
  "a value reaches a URL unvalidated" class of defect back into scope.
- **Accepted cost, not a surprise:** a title typo caught after publish leaves
  a permanently wrong slug, since editing the title does not regenerate it.
  Proved by execution below — PATCHing only `title` leaves `stored.slug`
  byte-identical to what `Create` generated.

## Fix

This is bookkeeping, not a code change to behavior — the API already refused
a client-supplied slug and the UI already showed it read-only. What changed:

- **`#0052`'s slug criterion struck** with a note pointing here (commit
  `b619eba`, in `issues/0052.md`).
- **Confirmed the UI already matches the decision.** `WorkshopEditor.svelte`
  never renders the slug as an `<input>` — it's a plain `<p class="slug-value
  mono">{workshop.slug}</p>`, so there's no "disabled" state to mislabel. The
  hint text underneath it already reads "Auto-generated from the title and
  set once at creation -- the server does not accept edits to it through this
  screen," which correctly frames it as *generated*, not merely *disabled*.
  No UI code change was needed.
- **Found and fixed a real documentation bug while confirming the API's
  refusal.** `admin_workshops.go`'s `Create` doc comment claimed an unknown
  `"slug"` key in the request body is "simply ignored ... recognized-but-
  unsettable, intentionally not rejected." That claim is **false**, proved by
  execution (see `## Verification`): `createWorkshopRequest` has no `Slug`
  field at all, so `decodeJSON`'s `DisallowUnknownFields` rejects any request
  carrying an unknown `"slug"` key with `400 {"error":"invalid request
  body"}` — the same treatment every other unrecognized field gets elsewhere
  in this package. The same is true of `patchWorkshopRequest`. This is
  actually the *stronger* of the two options the issue asked me to weigh
  (genuinely refuses vs. silently drops) — no code behavior needed to change,
  only the comment, which now describes the real, verified behavior and
  explains why it's the intended one. No production caller is affected: the
  admin UI never includes a `"slug"` key in a create or patch payload (`grep
  slug web/src/lib/api.ts web/src/lib/workshopAdmin.ts` — the only wire-side
  `slug` field is the read-only value in the *response* type, never in a
  request-shape type).
- **Updated the two "flagged for the reviewer" header comments** in
  `web/src/lib/workshopAdmin.ts` and `WorkshopEditor.svelte` that presented
  the slug question as still open, so a future reader doesn't reopen a
  decided question. `isSlugEditable()`'s behavior (`return false`) is
  unchanged — only its doc comment now says "by design (#0137)" instead of
  "today."

## Verification

```
uptime                                                    # 22:27, load average 1.83 1.72 1.85 — idle
export TEST_DATABASE_URL="$(scripts/testdb.sh create 0137)"
go build ./...                                            # clean
go vet ./...                                              # clean
go test ./internal/handlers/... -p 2 -count=1             # ok, 2.452s (real DB, non-zero)
ISSUE=0137 scripts/check.sh go ./internal/handlers/...    # VERIFICATION PASSED, zero skips
scripts/check.sh web                                      # svelte-check 230 FILES 0 ERRORS 0 WARNINGS;
                                                            # vitest 20 files / 3595 tests passed — VERIFICATION PASSED
scripts/testdb.sh drop 0137                                # cleaned up
```

**Immutability and unknown-field-rejection proved by execution**, with a
throwaway test file (`internal/handlers/zz_manual_0137_test.go`, written,
run, then deleted before the commit — never part of the shipped suite):

```
MANUAL0137 POST with unknown slug key: status=400 body={"error":"invalid request body"}

MANUAL0137 created: slug=zz-subtest-workshop-44684839747586 title=zz-subtest-workshop-44684839747586
MANUAL0137 PATCH with slug+summary key: status=400 body={"error":"invalid request body"}

MANUAL0137 after title-only PATCH: slug=zz-subtest-workshop-44684839747586 title=zz-subtest-workshop-44684839747586 EDITED TITLE
MANUAL0137 CONFIRMED: slug unchanged after title edit ("zz-subtest-workshop-44684839747586")
```

This proves, against a real database and the real HTTP handlers (not a
mock): (1) a `POST`/`PATCH` body carrying `"slug"` is rejected outright with
`400`, never silently accepted or dropped; (2) a `PATCH` containing only
`title` leaves `slug` byte-identical, confirming the accepted-cost claim in
`## Root cause`. `git status --porcelain` after cleanup shows no leftover
test file and no leftover database (`scripts/testdb.sh list` shows only
`opencircuit_test_template`); `pgrep -f 'go test|\.test '` empty.

No new permanent tests were added: no behavior changed (the rejection and
the immutability were already true and already exercised indirectly by
`#0051`'s existing suite — e.g. `TestCreate_SlugCollisionGetsDistinctSuffix`
proves slug generation, and no test anywhere ever sends a client-supplied
`slug`, which is itself consistent with the API never having accepted one).
Only two source comments were corrected to match already-shipped,
already-tested behavior.

## Files changed

- `internal/handlers/admin_workshops.go` — corrected `Create`'s doc comment:
  an unknown `"slug"` key is genuinely rejected (400), not silently
  ignored; records the `#0137` decision and why the current behavior is
  intended.
- `web/src/lib/workshopAdmin.ts` — updated the module header's slug section
  and `isSlugEditable`'s doc comment from "flagged for the reviewer" (open)
  to "decided by #0137" (resolved). No logic change.
- `web/src/views/admin/WorkshopEditor.svelte` — updated the header comment
  above the slug field to match; the rendered field and hint text were
  already correct and are unchanged.
- `issues/0052.md` — struck the slug acceptance criterion with a note
  pointing here (rode along in the same commit per this issue's dispatch
  instructions).

Code commit: `b619eba` — `#0137 Record: slugs stay server-generated and
immutable`.

## Acceptance criteria

- [x] Decide deliberately, and record the reasoning:
      - **(a) The API is right.** Slugs stay server-generated and immutable. `#0052`'s criterion is struck with a note, and the UI keeps showing the slug read-only
      - ~~(b) The criterion is right...~~
      **(a) chosen.** See `## Root cause` / `## Fix` below for the full reasoning; it restates and confirms the orchestrator's brief rather than re-litigating it.
- [x] ~~If (b): the slug remains UNIQUE-enforced...~~ **N/A — (b) not taken.** Struck: this criterion only applies under option (b), which was not chosen.
- [x] ~~If (b): changing a slug before publish leaves no dangling public URL...~~ **N/A — (b) not taken.** Struck: this criterion only applies under option (b), which was not chosen.
- [x] `#0052`'s criterion ends up either ticked or struck with a note — struck in `issues/0052.md` (commit `b619eba`), pointing here.
- [x] The relevant tests cover the decided behavior — see `## Verification`: existing `internal/handlers` suite already covers slug generation/immutability at the store and handler level (`#0051`'s `TestCreate_SlugCollisionGetsDistinctSuffix`, etc.); this issue additionally *proved* the immutability and unknown-field-rejection claims by execution with a throwaway test (deleted before commit, per `CLAUDE.md` §8a/§8b), rather than adding new permanent tests, since no behavior changed — only two code comments were corrected to match already-tested, already-shipped behavior.

## Notes

`#0052`'s own `## Notes` argue for the API's position — a stable, server-generated slug is one less thing an admin can get wrong, and the collision suffix already guarantees uniqueness. That makes **(a)** the likely answer, but it is a product decision, not a technical one, so it is recorded here rather than settled by whoever next opens the file.

One consideration for (a): a title typo caught after publish leaves a permanently wrong slug, since editing the title does not regenerate it. That is the cost of immutability and it is probably acceptable — but it should be an accepted cost, not a surprise.

## Relation

- Found by: [#0052](0052.md) — Build the workshops admin UI
- Relates to: [#0051](0051.md) — Build the workshops CRUD API

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 88 | 6,275 | 4,640,048 | 112,283 | $1.91 |

**Total: $1.91**
