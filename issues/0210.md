# 0210 — gofmt silently rewrites `''` in doc comments, and `check.sh` now enforces the result

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | db / infra |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-08-23 |
| **Commit** | `7483d0d` |
| **Closed** | 2026-08-23 |

## Description

`#0161` made the repo gofmt-clean and wired `gofmt -l` into `scripts/check.sh`.
Its phase-3 review confirmed the commit is exactly a gofmt run — for each of the
six files, `gofmt` applied to the parent revision is **byte-identical** to the
committed content — and found the one side effect worth following up.

Since Go 1.19, gofmt's doc-comment pass applies smart quotes: a doubled `''`
collapses into a single curly `”`. `internal/db/prd_index_parity_test.go`'s doc
comments illustrate Postgres's `''` string escape, so:

- `'it''s'` became `'it”s'` — a reader copying it writes something Postgres
  treats as ordinary text, not an escaped quote.
- ``  `""` doubles like a string's `”` does `` — the sentence is *about* the
  doubling parallel, and the rewrite destroys the very thing it illustrates.

Exactly two curly-quote introductions repo-wide, both in that file, both
comment-only. **Zero behavioural impact** — the tokenizer and tests are
untouched, and this is what the tree's own formatter produces, so applying it
as-is was correct under `#0161`'s scope.

**What makes it worth an issue rather than a shrug is the new enforcement.** Now
that `check.sh` fails on unformatted files, any *new* declaration doc comment
containing `''` will be silently rewritten the first time someone runs the
formatter. Existing ones are already normalized, so this is about what happens
next, not about repairing the past.

## Acceptance criteria

- [x] Both doc comments in `internal/db/prd_index_parity_test.go` convey the doubled-`'` escape without depending on a literal `''` surviving gofmt — spell it out ("two consecutive `'` characters"), or move the example into an indented code block, which the doc-comment pass leaves alone
- [x] The rewritten comments are **gofmt-stable**: running `gofmt -w` on the file changes nothing — demonstrated, not assumed
- [x] No curly quote remains in either comment
- [x] The comments still explain the escape accurately enough that a reader can act on them
- [x] `gofmt -l .` stays empty and `#0161`'s check still reports clean
- [x] `ISSUE=0210 scripts/check.sh go ./internal/...` passes with zero skips

## Implementation notes

**Established the current bytes before touching anything.** Byte-level scan
(`python3 -c "print(open(P,'rb').read())"` and a codepoint census) of
`internal/db/prd_index_parity_test.go` before any edit found exactly two
occurrences of `\xe2\x80\x9d` (U+201D RIGHT DOUBLE QUOTATION MARK), zero of
U+201C/U+2018/U+2019 — matching `#0161`'s review notes exactly:

- line 205: `` // literal (Postgres escapes an embedded quote by doubling it: 'it”s'), or ``
- line 275: `` // unescaped `"`, `""` doubles like a string's `”` does) would close this for ``

Both `”` sit inside plain comment prose — the second is even inside
backticks (`` `”` ``) — which rules out "wrap it in backticks" as a fix:
backtick code spans are *not* exempt from gofmt's doc-comment smart-quote
pass, only genuine indented preformatted blocks (`//\t...`) are. Confirmed
this by checking that the *first* mangled instance (`'it”s'`, not
backticked) and the *second* (`` `”` ``, backticked) were both mangled
identically — if backticks protected text, only one of the two would have
been hit.

**Route chosen: indented code block, for the primary example; prose for the
second, cross-referencing site.** For the `sqlSpanKind` doc comment, moved
the `'it''s'` example into an indented code block (`//\t'it''s'`), matching
the existing convention for examples elsewhere in this codebase (e.g.
`internal/middleware/ratelimit.go`'s `NewRateLimiter` doc comment) and per
the issue's own recommendation: it depends on a property of gofmt (it
leaves preformatted blocks alone) rather than on wording nobody later
"improves" back into a doubled-quote shape. For the `tokenizeSQL` doc
comment's `kindQuotedIdent` aside, the mangled text sits mid-sentence inside
a parenthetical remark, not as a standalone illustration — interrupting
that sentence with a code block reads worse than the original. Spelled it
out in prose instead ("doubled the same way a string literal's ' is
doubled") and pointed back at the `sqlSpanKind` comment's now-fixed example,
so the escape is demonstrated once, in one place, and referenced everywhere
else. Neither route requires two straight `'` characters to sit adjacent
anywhere in plain comment text (only inside the indented block, which gofmt
skips) — that adjacency is exactly what gofmt's smart-quote pass looks for.

**Scope respected.** Both comments touched are pure doc comments on
declarations (`type sqlSpanKind` and `func tokenizeSQL`); no code, test
logic, or the tokenizer itself was changed. `git diff --stat` confirms one
file, comment-only hunks.

**Concurrency.** `#0209` was running concurrently against
`cmd/opencircuit/`, entirely disjoint from this issue's
`internal/db/prd_index_parity_test.go`. It committed mid-session
(`1257be8`, `4480ee8`); `git status --porcelain` before my own commit showed
only my file dirty, so nothing of #0209's was swept in.

## Verification

Machine: `uptime` load average 2.0–2.75 throughout, well under the ~8
threshold (`CLAUDE.md` §5) — no result below is load-explained.

- **Byte census, before:** exactly 2 × U+201D, 0 × U+201C/U+2018/U+2019 in
  the file (Python `bytes.count`).
- **Byte census, after:** 0 × U+201C/U+201D/U+2018/U+2019 anywhere in the
  file — same script, re-run post-edit.
- **gofmt-stability proof (the criterion that actually proves the fix):**
  `shasum -a 256` of the file immediately after the manual edit —
  `c143a24...15c2cd7`. Ran `gofmt -l internal/db/prd_index_parity_test.go`
  — empty output (file already clean). Ran `gofmt -w` on it, then
  `shasum -a 256` again — **identical hash**, and `diff` against a
  pre-`gofmt -w` copy exited 0 (no output). `gofmt -l .` over the whole repo
  is also empty, matching `#0161`'s clean baseline.
- `go build ./internal/db/...` and `go vet ./internal/db/...` — both
  silent/clean.
- `ISSUE=0210 scripts/check.sh go ./internal/...` — own isolated database
  (`opencircuit_test_0210`), `go build` clean, `go vet` clean, `gofmt -l
  (#0161 — formatting drift)` reports `(clean)`, all 15 `internal/...`
  packages report `ok` with real elapsed times (0.131s–2.753s, none 0.00s),
  skip audit reports `(no skips)`, no leftover processes, `VERIFICATION
  PASSED`.
- Confirmed the scratch database `opencircuit_test_0210` was dropped after
  the run (`psql` query against `pg_database` returns nothing) and no
  `go test`/`.test` processes of mine remained (`pgrep -fl`).
- `git status --porcelain` showed only `internal/db/prd_index_parity_test.go`
  dirty before commit; committed with a narrow pathspec
  (`git commit -- internal/db/prd_index_parity_test.go`), confirmed via
  `git diff --stat` on the resulting commit that exactly one file changed.

## Notes

Reported by `#0161`'s phase-3 review under `CLAUDE.md` §9.

The indented-code-block route is the more durable of the two: it is a property
of gofmt's pass (it leaves code blocks alone) rather than a wording someone
might later "improve" back into the mangling shape. Whichever you choose,
criterion 2 is the one that proves it — a fix that gofmt immediately undoes is
not a fix.

`#0161`'s `## Review notes` records the two exact strings before and after, and
confirms the tokenizer is unaffected.

## Relation

- Found by: [#0161](0161.md) — the gofmt sweep and its new check
- Relates to: [#0113](0113.md) — the PRD index parity this file tests

## Review notes — 2026-08-23 (phase 3, **resolved**)

Reviewed `7483d0d` (`internal/db/prd_index_parity_test.go`, comments only). All
verification ran in a throwaway worktree pinned to `7483d0d`
(`git worktree add --detach ... 7483d0d`, removed afterwards) so nothing here
could touch `#0209`'s concurrent review in `cmd/opencircuit/`. Machine:
`uptime` load average 2.6-6.0 throughout, under the ~8 threshold (`CLAUDE.md`
§5), so nothing below is load-explained. Own database
(`opencircuit_test_0210rev`), confirmed dropped — `pg_database` afterwards
shows only `opencircuit_test` and `opencircuit_test_template`. No leftover
`go test`/`.test` processes.

Everything below was checked **by bytes** (Python `bytes.count` / `repr` of each
line), never by rendering. Per `CLAUDE.md` §8, `grep -P` is not usable on this
machine for that purpose.

### Criterion 2 — gofmt stability, reproduced independently

| Step | Result |
|---|---|
| `shasum -a 256` before | `c143a204173115a2c41697d9142b56b70252e11a6a8a82d1b8d87a9c419c5e64` |
| `gofmt -l <file>` | empty |
| `gofmt -w <file>`, then `shasum -a 256` | `c143a204173115a2c41697d9142b56b70252e11a6a8a82d1b8d87a9c419c5e64` — **identical** |
| `diff` against a pre-`gofmt -w` copy | exit 0, no output |
| Same hash in the main checkout at `HEAD` | matches (the `HEAD` commit `4321910` touches only `CLAUDE.md` and `issues/0210.md`) |

The fix is gofmt-stable. This is the criterion that matters, and it holds.

*One recording nit, not a defect:* `## Verification` writes the hash as
`c143a24...15c2cd7`. The prefix is right; the suffix is not — the digest ends
`...c419c5e64`, and `15c2cd7` does not occur anywhere in it. The claim it
supports is true (verified above); only the transcription is wrong.

### The durability claim, tested by experiment rather than cited

Wrote a scratch `.go` file with six shapes and ran `gofmt -w` on it. Byte-exact
results:

| Shape in a **declaration doc comment** | Survives `gofmt -w`? |
|---|---|
| A — doubled `'` in plain prose | **No** — collapses to one U+201D |
| B — doubled `'` inside a backtick code span | **No** — collapses to one U+201D, *inside* the backticks |
| C — doubled `'` in a **tab**-indented block (`//` + 0x09) | **Yes** — byte-identical |
| D — doubled `'` in a **four-space**-indented block | Text survives, but gofmt **rewrites the indent to a tab** — so the file is not byte-stable and `gofmt -l` flags it |
| E — doubled backtick in plain prose | **No** — collapses to U+201C |
| F/G — doubled `'` in a trailing or interior **non-doc** comment | **Yes** — the pass only touches doc comments on declarations |

A and B together confirm the implementer's conclusion exactly: a backtick code
span is **not** a protected region. C confirms the exemption it relied on.

D is the sharp edge worth naming: a space-indented block would still protect the
text, but would fail `#0161`'s `gofmt -l` check on the first format. `CLAUDE.md`
§8's wording ("`//` + tab") is therefore the correct guidance, not merely
approximate.

**The committed block uses a real tab.** Line 210 reads, byte-exactly,
`//` + 0x09 + `'it''s'` (Python `repr` renders it as `"//\t'it''s'"`). Not
spaces. It is preceded by a bare `//` line, which is what makes it a separate
preformatted block rather than a continuation of the paragraph above.

### Byte census — the whole file, not just the two comments

| Codepoint | Count |
|---|---|
| U+2018 LEFT SINGLE QUOTATION MARK | 0 |
| U+2019 RIGHT SINGLE QUOTATION MARK | 0 |
| U+201C LEFT DOUBLE QUOTATION MARK | 0 |
| U+201D RIGHT DOUBLE QUOTATION MARK | 0 |

Full non-ASCII census of the 57,456-byte file: only U+00A7 SECTION SIGN (33),
U+2014 EM DASH (124), U+2026 HORIZONTAL ELLIPSIS (10). No stray invisible
characters, no NBSP, no thin space. Criterion 3 holds, and holds file-wide
rather than only in the two comments.

Adjacency scan for the mangling shape: the **only** doubled `'` in any doc
comment is line 210, inside the tab-indented block. The other hits are Go rune
literals in the tokenizer body (lines 298, 302, 303) and triple-backtick fence
markers (lines 1077, 1084, 1106) — none of which the pass rewrote, as the
whole-file stability result above proves.

### Scope — comments only

`git show 7483d0d --stat`: one file, 11 insertions, 6 deletions. Every added and
removed line in both hunks begins with `//`. No code, no test logic, no
tokenizer line, no test data changed. The second site's prose contains only
non-adjacent single `'` characters (`literal's ' is doubled`), which the pass
leaves alone — so gofmt stability covers **both** sites, not just the indented
one.

### Criterion 4 — the comments judged as documentation, read cold

**`sqlSpanKind` (lines 200-211): good.** Pulling the parenthetical out of the
four-construct list improved the list, which now reads cleanly. The new
paragraph states the rule in prose ("escapes an embedded quote inside a string
literal by doubling it — two consecutive `'` characters, not one curly quote")
and then shows `'it''s'` in a block a reader can copy correctly. The "not one
curly quote" clause is doing real work here: it is the file's own history, and a
future reader who finds a curly quote in this comment now knows immediately that
it is damage rather than intent.

**`tokenizeSQL` (lines 278-283): acceptable, marginally less concrete.** The
sentence is still *about* the doubling parallel, which is what `#0161`'s review
said the original was for — "close on the next unescaped `"` — doubled the same
way a string literal's `'` is doubled" preserves it. What it loses is the literal
`""` token that the pre-gofmt original carried. Worth noting that `""` was never
at risk: the pass rewrites a doubled `'` and a doubled backtick, not a doubled
`"` (experiment E vs. the surviving `""` elsewhere in the file), so keeping the
concrete token would have been safe. Dropping it is a defensible readability
call, not an error, and the cross-reference recovers the concreteness.

**The cross-reference resolves.** "as shown on `sqlSpanKind` above" points at
`type sqlSpanKind` on line 211 of the same file; `tokenizeSQL` is on line 284.
Same file, genuinely above, and the example named is exactly the one at line 210.
Not dangling — unlike `#0203`, `#0190`, and `#0192`.

Judging the mixed approach overall: correct. The one place a reader goes to copy
the escape is protected by a property of gofmt; the mid-sentence aside, where a
code block would have broken the sentence, is protected by containing no doubled
quote at all. Both routes are verified stable by the same whole-file hash.

### Suite

`gofmt -l .` repo-wide: empty, in both the worktree and the main checkout.

`ISSUE=0210rev scripts/check.sh go ./internal/...` — `go build` silent, `go vet`
silent, `gofmt -l (#0161 — formatting drift)` reports `(clean)`, all **15**
`internal/...` packages report `ok` with real elapsed times (0.200s-2.870s, none
0.00s), skip audit `(no skips)`, leftover processes `(none)`,
`VERIFICATION PASSED`. `internal/db`'s parity tests were additionally run with
`-v` and observed to `PASS`, not skip.

### Verdict

Pass. Criterion 2 reproduced independently and holds; criteria 1, 3, 4, 5 and 6
verified by bytes and by execution. Status set to `resolved`.

**Reported for filing (`CLAUDE.md` §9 — reviewers report, they do not file):**
the new `CLAUDE.md` §8 entry names only `''` as the mangling shape, but
experiment E shows the same doc-comment pass also collapses a **doubled backtick**
into U+201C. A one-line addition to that entry would make the rule complete. Low
priority; no file in the tree currently trips it.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 74 | 3,485 | 2,598,071 | 67,070 | $1.08 |
| 2026-08-23 | review | claude-opus-5 | 48 | 2,757 | 1,443,171 | 61,480 | $1.18 |

**Total: $2.26**
