# 0159 — The column-parity guard's ADD-coverage ceiling misses multi-clause, `ONLY`, and quoted `ALTER TABLE` forms, and its lexer has no dollar quoting

| | |
|---|---|
| **Status** | open |
| **Platform** | All |
| **Module** | db / docs |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |

## Description

`#0154` built `TestPRDWorkshopAndMailingColumnParity` and the two coverage
checks that make an unparseable statement fail loudly instead of vanishing
(`checkTableStatementCoverage`, `checkColumnAddCoverage`, in
`internal/db/prd_index_parity_test.go`). Both work — `#0154`'s pass-2 review
proved they genuinely fire, and proved the guard's live column map is
identical, column for column, to the real migrated schema (91 columns across
the ten guarded tables).

Three forms still slip past **silently** — no failure, no coverage error, green
suite — and all three come from the same place: `looseColumnAddPattern`
(`ALTER\s+TABLE\s+\w+\s+ADD\b`) is supposed to be the maximally permissive
ceiling the strict pattern is measured against, but it is not permissive
enough to be one.

**1. Multi-clause `ADD` — the ordinary one.** Postgres allows

```sql
ALTER TABLE workshops ADD COLUMN mc_one TEXT, ADD COLUMN mc_two TEXT;
```

Both the loose and the strict pattern match once, at the `ALTER TABLE workshops
ADD` prefix. The second and later `ADD` clauses are invisible to *both* sides,
so the counts agree and the coverage check cannot fire. Proved fully silent: a
scratch migration adding `mc_one` and `mc_two`, with only `mc_one` documented
in a scratch `PRD.md`, ran green — `mc_two` was never guarded. This repo
already writes multi-clause `ALTER`s: `migrations/000007` ends with
`ALTER TABLE sessions ALTER COLUMN user_id SET NOT NULL, ALTER COLUMN
created_at SET NOT NULL, …`.

**2. `ALTER TABLE ONLY` and a quoted table name.**

```sql
ALTER TABLE ONLY workshops ADD COLUMN only_col TEXT;
ALTER TABLE "workshops"  ADD COLUMN q_col    TEXT;
```

Neither matches the loose pattern's `\w+` table-name slot, so loose and strict
are both `0` and the mismatch the coverage check looks for never exists. Both
proved silent end-to-end. Note the asymmetry with the table side, which is
already correct here: `looseTableStartPattern` is just `CREATE\s+TABLE\b`, so a
quoted `CREATE TABLE "t" (` *does* fire the coverage check. The ADD ceiling
should be equally dumb.

**3. No dollar quoting in the quote-aware scanner.** `stripSQLComments`,
`splitTopLevel`, and `columnsFromCreateTable`'s close-paren finder each track
`'…'` state (with Postgres's `''` escape) but know nothing about `$tag$…$tag$`.
A dollar-quoted string containing an *odd* number of apostrophes flips the
scanner into "inside a string" for the rest of the file. Proved:

```sql
DO $$ BEGIN RAISE NOTICE $x$it's fine$x$; END $$;

CREATE TABLE scratch_dq (
    a INT,                 -- the worker's column
    b TEXT,
    c INT
);
```

The `--` comment is no longer stripped, so the body scan meets its apostrophe
and the table parses as `[a, "--"]`: a bogus column named `--` (loud) with
**`b` and `c` silently dropped**. The current tree is clear —
`migrations/000007` is the only file containing `$$` at all, its `RAISE
EXCEPTION` literal has balanced apostrophes, and it touches only auth tables,
which are outside `prdIndexParityTables` — but the exposure is real for any
future migration that ships a trigger or function body.

None of the three exist in `migrations/` today, so nothing is currently
mis-guarded. This is the same standing `#0154` had when `#0148`'s review filed
it, and it is filed the same way: as hardening, deliberately not a bounce.

## Acceptance criteria

- [ ] A multi-clause `ALTER TABLE t ADD COLUMN a …, ADD COLUMN b …;` records
      **every** column it adds, or fails loudly. Prove it with the same
      control-then-trigger method `#0154`'s reviews used: document the first
      column in a scratch `PRD.md`, leave the second undocumented, and confirm
      the run is red
- [ ] `ALTER TABLE ONLY t ADD …` and `ALTER TABLE "t" ADD …` either parse or
      fire `checkColumnAddCoverage`. Widening the *loose* pattern toward
      `ALTER\s+TABLE\b` is the likely shape — it must stay a true ceiling, so
      re-check that a loose count can never come out *below* the strict count
      once it changes (the message assumes `found >= matched`)
- [ ] `ADD CONSTRAINT` / `ADD PRIMARY KEY` / `ADD UNIQUE` / `ADD FOREIGN KEY` /
      `ADD EXCLUDE USING` are still classified out identically on both sides —
      there are seven such `ALTER`s in the tree (`000009`, `000010`, `000012`,
      `000013`, `000014`, `000017`, `000020`) and a one-sided classification
      change turns them into a permanent false positive
- [ ] The scanner tracks `$tag$…$tag$` (including the untagged `$$`), so an
      odd apostrophe inside a dollar-quoted body cannot corrupt quote state for
      the rest of the file. Prove it with the `scratch_dq` case above
- [ ] **No false positives**: re-run the live-map-vs-real-database diff that
      `#0154`'s pass-2 review used — dump `information_schema.columns` for the
      ten guarded tables from `opencircuit_test_template` and diff it against
      the parser's live map. It must stay identical, all 91 columns
- [ ] Scope stays strictly one-directional (migrations → PRD §6.2). No
      bidirectional check, no allowlist — see `#0154`'s Notes
- [ ] `ISSUE=0158 scripts/check.sh go ./internal/db/...` passes with all four
      doc-parity guards observed running

## Notes

Filed by `#0154`'s phase-3 review (pass 2). `#0154` itself is sound and was
resolved: all seven forms its first review bounced on are fixed, the coverage
checks demonstrably bite (`ALTER TABLE t ADD;` → `found 1 … parsed 0`;
`CREATE TABLE "t" (` / `CREATE TABLE t AS …` / `PARTITION OF` /
`public.t` all fire on the table side), and the guard's answer on the real tree
matches the real database exactly.

Worth deciding while the file is open: at some point the accumulated
quote/comment/paren scanning is a small SQL lexer with a regex API bolted on
top, and one honest tokenizer would be shorter than the fourth widening of a
loose pattern. Not a requirement — but if this is the third round of "widen the
regex", say so in the resolution rather than doing it a fourth time.

## Relation

- Found by: [#0154](0154.md) — the PRD index-parity guard silently skips index
  forms it cannot parse, and does not cover columns
- Relates to: [#0148](0148.md) — PRD §6.2's workshops index is two revisions
  behind the migration

---

*Filed as `#0159`, not `#0158`. `#0154`'s reviewer wrote this file as `issues/0158.md` while the orchestrator was filing a different issue under that number; the orchestrator's commit overwrote it. Recovered verbatim from `3489754` and renumbered. `#0158` is the `testSlug` clock-collision issue. This is the second such collision in one session — see `CLAUDE.md` §9 for the rule that now prevents it.*
