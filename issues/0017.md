# 0017 — Build the site header, navigation, and footer

| | |
|---|---|
| **Status** | open |
| **Module** | web |
| **Platform** | All |
| **Phase** | 2 — Brand and marketing pages |
| **First seen** | 2026-08-18 |

## Description

The persistent shell around every public page: logo, primary navigation, and a footer carrying the legal and social links.

## Acceptance criteria

- [ ] Header with the monochrome logo mark, wordmark, and nav links to Workshops, About, Subscribe
- [ ] Active route is visually indicated and marked `aria-current="page"`
- [ ] Mobile layout: nav collapses or wraps cleanly at ≤ 480 px with ≥ 40 px tap targets
- [ ] Footer with Discord link, contact address, privacy policy link, and copyright
- [ ] Staff sign-in is reachable but unobtrusive — a footer link, not primary nav
- [ ] Signed-in staff see an Admin link in the header
- [ ] Keyboard navigable end to end with a visible focus ring on every interactive element
- [ ] A skip-to-content link is the first focusable element

## Carried in from `#0018`'s review (2026-08-18)

Three obligations transferred here rather than left half-met on `#0018`:

- [ ] **Mount `Logo.svelte` in the header.** `#0018` built the mask-technique
      component but could not mount it — no header existed. Use
      `variant="mark"`; `"full"` is illegible below ~96 px.
- [ ] **Fix the retina blur.** `Logo.svelte` hardcodes `/mark-mask-64.png` at
      every size, which renders visibly soft on a 2×/3× display — the reviewer
      rendered it at 64 CSS px against `mark-mask-256.png` and the shipped one is
      clearly blurred. `assets/logo/mark-mask-256.png` already exists; select the
      raster by requested size.
- [ ] **Wire the theme toggle.** `theme.ts` exports `Theme`, `currentTheme`,
      `nextTheme`, `applyTheme`, `readStoredTheme` and is DOM-injectable, but no
      button exists yet. The header owns it.

**Export surface from `#0018` — build against this, it is stable:**
`Logo.svelte` takes `variant?: 'mark'|'full'` (default `'mark'`) and
`size?: number` (default `34`); both masks are square so one `size` is correct.
Motifs are `Prompt {text}`, `StatusList {items: string[]}`, `CommandLine
{command}`, `TerminalPanel {title?, noPadding?, children}`, `TraceDivider` (no
props).

Two smaller carried findings, fix if cheap: `site.webmanifest` has no 192×192
icon (Chromium's install heuristics have historically wanted one), and
`:where(a, button):focus-visible` has zero specificity and misses `summary` and
`[tabindex]`.

## Notes

PRD §5.1, §7.5. The privacy policy link is required — the site collects email, interests, signup IP, and UTM source, and #0060's erasure flow needs a documented home. If the policy page does not exist yet, file a follow-on rather than linking to nothing.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §5.1 — Public routes · 16 lines
  `sed -n '/^### 5\.1 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0013](0013.md) — Build the terminal motif components
- Depends on: [#0014](0014.md) — Add a History API path router
- Depends on: [#0018](0018.md) — Produce the logo, favicon, and app icon assets
- Blocks: [#0063](0063.md) — Run the accessibility audit
