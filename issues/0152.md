# 0152 — `signup_url` is validated in the browser but never at the API boundary

| | |
|---|---|
| **Status** | resolved |
| **Module** | handlers |
| **Platform** | All |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |
| **Closed** | 2026-08-22 |
| **Commit** | `d9c410b` |

## Description

`#0138` closed the missing-server-side-validation gap for `cover_image`: a bad value now gets a 400 from `Create` and `Patch` instead of being stored and later concatenated into `og:image` and the JSON-LD.

`signup_url` has the same shape and was not covered. The admin UI checks it with `isSafeLinkHref`, but nothing on the server does, so a `javascript:` URL can be **stored** by any caller that does not go through the editor.

It is not a live XSS today. `#0054`'s detail view renders the link only through `hasExternalSignup`, which `#0054`'s own fix pass gated on `isSafeLinkHref` — so a stored `javascript:` value is refused at render time. The defect is that the database is allowed to hold a value the product has decided is unsafe, and the only thing standing between it and a reader is one client-side call site.

## Acceptance criteria

- [x] `signup_url` is validated at the API boundary in both `Create` and `Patch`, returning 400 rather than storing the value
- [x] The server-side rule matches the client's `isSafeLinkHref` semantics — including whatever `#0138`'s fix pass lands for interior control characters. Do not write a third, subtly different rule
- [x] A test proves a `javascript:` (and a control-character-bearing) `signup_url` is rejected by both verbs, and that an ordinary external ticketing URL still round-trips
- [x] Check whether any other column reaches a rendered attribute without a server-side check — `#0138` closed `cover_image`, this closes `signup_url`; say whether the set is now complete
- [x] `ISSUE=0152 scripts/check.sh go ./internal/handlers/...` passes

## Root cause

`signup_url` was round-tripped through `normalizeOptionalCampaignField` in
both `AdminWorkshopsHandler.Create` and `.Patch` with no safety check at all
— unlike `cover_image`, which `#0138` gated behind `isSafeCoverImage` at the
same boundary. The only thing standing between a stored `javascript:` value
and a reader was one client-side call: `#0054`'s `hasExternalSignup`
(`web/src/lib/workshopDetail.ts`) gates rendering behind `isSafeLinkHref`
(`web/src/lib/markdown.ts`) before it becomes a `href`. That made it not a
live XSS, but it meant the database could hold a value the product has
decided is unsafe, reachable by any caller that skips the SPA — the same
missing-boundary shape `#0138` closed for `cover_image`.

## Fix

Added `isSafeLinkHref` to `internal/handlers/admin_workshops.go` — the Go
twin of `web/src/lib/markdown.ts`'s `isSafeLinkHref`, **not** of
`isSafeCoverImage`: a link is a navigation the reader chooses to follow
(unlike a cover image, which the page loads unasked), so an absolute
`http(s)://` URL to any host stays allowed (a workshop's `signup_url` is
routinely an external ticketing page) along with `mailto:`; what's rejected
is any other scheme, a protocol-relative `//host` destination and its
backslash spellings, and — per `#0138`'s bounce — any C0 control character
(0x00–0x1f) or DEL (0x7f) anywhere in the string, since a browser deletes
ASCII tab/LF/CR before resolving a URL and an interior control character can
reassemble a dangerous scheme a naive check would miss.

Wired into both `Create` and `Patch`, mirroring `isSafeCoverImage`'s call
shape exactly: `normalizeOptionalCampaignField(req.SignupURL)` first, then
`isSafeLinkHref` on the (possibly nil) result, 400 with a new
`signupURLErrorMessage` constant on rejection, before the store call.

**Trim decision.** `isSafeLinkHref` trims internally (matching the
TypeScript function's own `href.trim()`) before testing for emptiness or a
scheme, so neither `Create` nor `Patch` pre-trims the value — same
convention `isSafeCoverImage` already uses. `normalizeOptionalCampaignField`
still tests `TrimSpace(*v) == ""` but returns the untrimmed pointer (the
divergence `#0138` pass 2 flagged as "worth knowing if `#0152` generalises"),
so a `signup_url` with stray leading/trailing whitespace can still be
*stored* once it passes the safety check — unchanged, pre-existing behavior
shared by every optional campaign-style field, not something this issue
introduces. Generalizing `normalizeOptionalCampaignField` to trim is left
out of scope; the render-time gate (`hasExternalSignup`) already trims
before calling `isSafeLinkHref`, so stray whitespace never affects what's
shown.

**A real Go/TS parity bug, found and fixed by the codepoint sweep.** The
first draft used `strings.TrimSpace` for the internal trim. Sweeping leading
codepoints through the real Go and TypeScript functions side by side found
two genuine disagreements: `unicode.IsSpace` (and therefore
`strings.TrimSpace`) treats U+0085 (NEL) as whitespace where JS's
`String.prototype.trim()` does not, and does *not* treat U+FEFF
(BOM/ZWNBSF) as whitespace where JS's `trim()` does. The BOM direction is
the dangerous one: a leading-BOM `javascript:` value could have been
**accepted** by the naive Go twin while the TypeScript original correctly
rejected it — exactly the "third, subtly different rule" criterion 2
forbids. Replaced the trim with a hand-written `isJSWhitespace` predicate
that matches ECMA-262's WhiteSpace + LineTerminator productions exactly
(tab/VT/FF/CR/LF/space/NBSP/BOM plus the Unicode `Zs` separators), and swept
again — 0 mismatches.

**Is the set now complete?** Yes, for discrete DB fields that become a
rendered `href`/`src` attribute directly. A repo-wide search
(`grep -rn 'src={\|href={' web/src`, plus the `workshops` migration's schema)
turns up exactly two: `cover_image` (`#0138`) and `signup_url` (`#0152`) —
both now validated server-side. Every other `<a href>`/`<img src>` bound to a
non-constant value in the SPA (`Header.svelte`, `Footer.svelte`, `About.svelte`)
uses a hardcoded constant (`DISCORD_URL`, `LUMA_URL`), not DB content. The
third instance the issue's own notes name — Markdown link destinations
inside `body_md` — is a different shape: free text can embed arbitrarily many
links, so there's no single discrete value to accept/reject at the boundary;
instead every extracted link is individually filtered by `isSafeLinkHref`
inside `renderMarkdownPreview` itself, on both the admin preview and the
public `WorkshopDetail` render path, already reviewed sound by `#0138` pass 1
("`renderMarkdownPreview` emits no `<a>` for any rejected form"). The
separate `email_campaigns.body_md` send pipeline
(`internal/mailing/campaign_markdown.go`) has its own independent
protection via goldmark's safe-mode `isDangerousLinkURL`, unrelated to this
issue's fields.

## Verification

- `ISSUE=0152 scripts/check.sh go ./internal/handlers/...` — `ok
  github.com/brennanMKE/OpenCircuitSF/internal/handlers 2.104s` (a real
  elapsed time, not 0.00s), skip audit `(no skips)`, `VERIFICATION PASSED`.
  Machine idle throughout (`uptime` load average 1.55–4.06, well under the
  ~8 threshold).
- New tests: `TestIsSafeLinkHref` (unit, 20 cases including every C0/DEL
  scheme-bypass shape `#0138` established), plus the HTTP-level
  `TestAdminWorkshops_CreateRejectsUnsafeSignupURL`,
  `TestAdminWorkshops_CreateAcceptsSafeSignupURLAndRoundTrips`,
  `TestAdminWorkshops_PatchRejectsUnsafeSignupURL`,
  `TestAdminWorkshops_PatchAcceptsSafeSignupURLAndRoundTrips` — real
  requests against a real Postgres, throwaway `zz-subtest-workshop-<nanos>`
  rows only (§8b), asserting a 400 for `javascript:`, `data:`,
  `//evil.host/x`, and `java<TAB>script:alert(1)` with the row left
  unchanged on PATCH, and a 201/200 round-trip for
  `https://eventbrite.com/e/soldering-101`.
- **Go/TS parity proved by execution, not by reading.** A throwaway Go test
  (`go test -run TestZZSweep0152Dump`) dumped `isSafeLinkHref` verdicts for
  581 payloads — every interior codepoint 0x00–0xFF inside a relative path
  and inside a scheme string, a curated leading-position sweep of 19
  whitespace/format codepoints (before a `javascript:` scheme and before a
  `//host` destination), and 31 scheme/shape payloads — to
  `scratchpad/go_sweep_0152.json`. A throwaway vitest test dumped the same
  581 payloads through the real, unmodified `web/src/lib/markdown.ts`
  `isSafeLinkHref` to `scratchpad/ts_sweep_0152.json`. Diffed with a small
  Python script: first pass found 4 mismatches (the NEL/BOM bug above);
  after the `isJSWhitespace` fix, **0 mismatches on all 581 payloads**. The
  interior sweep independently confirms `isSafeLinkHref` rejects exactly the
  33 codepoints `0x00`–`0x1f` ∪ `{0x7f}` — the same C0∪DEL set `#0138`
  proved for `isSafeCoverImage`/`isSafeLinkHref`. Both throwaway sweep files
  (`internal/handlers/zzsweep_0152_test.go`,
  `web/src/lib/zzsweep_0152.test.ts`) were deleted before committing; only
  the permanent tests above remain.
- **Mutation-proved.** Backed up `admin_workshops.go` (`shasum -a 256`
  recorded first), inserted `return true` as the first line of
  `isSafeLinkHref`, and reran the affected tests against a private scratch
  database (`scripts/testdb.sh create 0152mut`, dropped afterward — never
  the shared `opencircuit_test`, per §8b). `TestIsSafeLinkHref` failed 15 of
  20 subtests; `TestAdminWorkshops_CreateRejectsUnsafeSignupURL` failed all
  4 cases with observed `status = 201, want 400` and the malicious
  `signup_url` echoed back in the created row's JSON body;
  `TestAdminWorkshops_PatchRejectsUnsafeSignupURL` failed with `status =
  200, want 400` and the stored `SignupURL` non-nil after the "rejected"
  PATCH. Restored the file and confirmed byte-identical via
  `shasum -a 256 -c` against the pre-mutation checksum. `pgrep -f 'go
  test|\.test '` empty afterward; scratch database dropped.

## Files changed

- `internal/handlers/admin_workshops.go` — added `isSafeLinkHref`,
  `hasURLScheme`, `isASCIILetter`, `hasSafeLinkScheme`, `isJSWhitespace`, and
  `signupURLErrorMessage`; wired the check into `Create` and `Patch`.
- `internal/handlers/admin_workshops_test.go` — added `TestIsSafeLinkHref`
  and the four `Create`/`Patch` accept/reject HTTP-level tests for
  `signup_url`.

Commit: `d9c410b`.

## Gotchas

- `strings.TrimSpace`/`unicode.IsSpace` is **not** a safe stand-in for JS's
  `String.prototype.trim()` when writing a Go twin of a TypeScript security
  rule that trims internally. They disagree at U+0085 (NEL, Go trims it, JS
  doesn't) and U+FEFF (BOM, JS trims it, Go doesn't) — confirmed by
  execution, not by reading either stdlib's docs. The BOM direction is
  security-relevant (Go would have silently accepted a value TS rejects);
  the NEL direction merely makes Go stricter (fails safe, but still a
  parity break). Any future Go twin of a JS function that trims should use
  the same `isJSWhitespace`-style explicit predicate rather than
  `strings.TrimSpace`, or sweep leading-position codepoints before trusting
  the two agree.
- The admin editor's own `signup_url` field check
  (`web/src/lib/workshopAdmin.ts`'s `isHttpUrl`) is a *different*, narrower
  client-only UX convenience (http/https only, no `mailto:`/relative) — it
  is **not** the rule this issue closes. The actual safety gate this issue
  mirrors is `#0054`'s `hasExternalSignup` → `isSafeLinkHref`
  (`workshopDetail.ts`/`markdown.ts`), the one that decides whether a stored
  value is ever allowed to become a rendered `href`. Confirm which of two
  same-field client checks is the security rule before writing a server
  twin of "the" client check — this codebase has both kinds for
  `signup_url` and they are not interchangeable.

## Notes

Found by `#0138`'s phase-3 review while confirming the `cover_image` boundary fix. It was explicitly ruled out of scope for that issue rather than folded in.

Worth noting the pattern: this is the third instance of "the browser checks it, the server does not" in this subsystem (`cover_image`, `signup_url`, and the Markdown link destinations `#0052` hardened). A rule enforced only in the SPA is a rule enforced only for people using the SPA.

## Review notes

> 🟢 **Passed 2026-08-22.** The `strings.TrimSpace` / `String.prototype.trim()`
> mismatch is real and reproduces in **both** directions, including the
> dangerous one — the first-draft Go twin **accepts** `<BOM>javascript:alert(1)`
> while the TypeScript original rejects it. The `isJSWhitespace` replacement is
> not "95% right": swept against V8 over **all 1,114,112 codepoints** it is the
> *exact* set `String.prototype.trim()` strips — zero missing, zero extra. Go
> and TS `isSafeLinkHref` agree on **all 3,918** payloads I put through the real
> functions (the implementer swept 581), all four validators reject exactly
> C0∪{DEL}, and nothing the server stores and the page renders resolves to a
> dangerous scheme in the WHATWG parser. Resolved on `d9c410b`.

Machine idle throughout (`uptime` load average 1.88–2.70, far under the ~8
threshold, so nothing below is machine noise). Verified in a throwaway worktree
detached at `d9c410b` on its own `opencircuit_test_0152rev` database, because
`#0154`'s reviewer was working the same tree concurrently. Confirmed the verdict
survives the tree moving: `main` advanced to `e50b16b` mid-review, and
`git diff d9c410b..HEAD` over `admin_workshops.go`, `admin_workshops_test.go`,
`markdown.ts`, `workshopDetail.ts` and `workshopAdmin.ts` is **empty** — every
file this review reasons about is byte-identical at HEAD. Worktree removed, both
scratch databases dropped, `pgrep -f 'go test|\.test '` empty afterwards.

### The `TrimSpace` / `trim()` mismatch is real — reproduced, both directions

I rebuilt the first-draft `naiveIsSafeLinkHref` (identical to the committed
function except it uses `strings.TrimSpace`) and ran it beside the real one:

| Payload | `unicode.IsSpace` | JS `trim()` strips | naive Go verdict | TS verdict | committed Go |
|---|---|---|---|---|---|
| `<U+0085 NEL>javascript:alert(1)` | **true** | **no** | REJECT | ACCEPT | ACCEPT ✅ |
| `<U+FEFF BOM>javascript:alert(1)` | **false** | **yes** | **ACCEPT** | REJECT | REJECT ✅ |

The BOM row is the one that matters: `strings.TrimSpace` leaves the BOM in
place, so `hasURLScheme` sees a leading `\xEF` rather than a letter, falls
through to the "no scheme, therefore relative" branch, and **accepts a value the
TypeScript original rejects**. A naive twin would have been a bypass in exactly
the direction criterion 2 forbids. The NEL row is the reverse — Go stricter,
fails safe, but still a parity break. Both are closed by the committed code.

(Interesting corollary, and the reason the committed function is right to accept
the NEL row: `<NEL>javascript:…` is genuinely *safe*. U+0085 is a C1 control,
not a C0 one, so the WHATWG parser does not strip it, the string has no valid
scheme, and it resolves same-origin. That is the same ruling `#0138` pass 2
reached for `/<NEL>/evil.host/x.jpg`. The twins agree *and* they agree with the
URL parser.)

### `isJSWhitespace` audit — exact, not approximate

I did not compare the switch against a remembered ECMA-262 list. I computed the
ground truth by asking the engine: for every codepoint `0x0000`–`0x10FFFF`
(surrogates skipped), does `(c + "Z").trim() !== c + "Z"` in Node v26.7.0 —
then diffed that against the real `isJSWhitespace` dumped from the compiled Go.

```
JS trim strips  (25): 0009 000A 000B 000C 000D 0020 00A0 1680 2000..200A
                      2028 2029 202F 205F 3000 FEFF
isJSWhitespace  (25): 0009 000A 000B 000C 000D 0020 00A0 1680 2000..200A
                      2028 2029 202F 205F 3000 FEFF
MISSING from isJSWhitespace (JS trims, Go does not):  NONE
EXTRA   in  isJSWhitespace (Go trims, JS does not):   NONE
unicode.IsSpace vs JS trim:  IsSpace-only = U+0085   JStrim-only = U+FEFF
```

Line by line against ECMA-262's two productions, everything is present and
correctly categorised:

- **WhiteSpace**: TAB `0009` ✅, VT `000B` ✅, FF `000C` ✅, ZWNBSP `FEFF` ✅.
- **WhiteSpace → USP** (general category `Zs`): SP `0020` ✅, NBSP `00A0` ✅,
  OGHAM `1680` ✅, `2000`–`200A` all eleven ✅, NNBSP `202F` ✅, MMSP `205F` ✅,
  IDEOGRAPHIC SP `3000` ✅. That is the complete current `Zs` category — 17
  codepoints, none omitted.
- **LineTerminator**: LF `000A` ✅, CR `000D` ✅, LS `2028` ✅, PS `2029` ✅.

Two near-misses it gets *right* by omission, which is where a hand-written set
usually goes wrong:

- **U+180E** (MONGOLIAN VOWEL SEPARATOR) is correctly **absent**. It was `Zs`
  before Unicode 6.3 and is `Cf` now; V8 does not trim it, and neither does
  this. A list written from an older reference would have included it and
  diverged.
- **U+200B** (ZWSP) is correctly **absent** — it is `Cf`, not `Zs`, despite
  being "a space" colloquially. V8 does not trim it either.

The only forward-looking caveat, recorded rather than blocking: the set is a
hardcoded snapshot of `Zs`. If a future Unicode revision adds a `Zs` codepoint,
the Go table and the JS engine would both learn it and this literal would not.
That is a strictly better failure mode than `unicode.IsSpace`, which is wrong
about two codepoints *today*, and there is no `Zs` addition pending.

**Dead-code note, harmless:** the C0 members of the set (`0009`, `000A`, `000B`,
`000C`, `000D`) can never fire, because the C0∪DEL rejection runs *before* the
trim in both twins. They are correct to include — the set is documented as
"what JS trims" and removing them would make the predicate a lie — and the TS
side has the same ordering, so parity is unaffected either way.

### Sweep: three (four) validators, one rule — 0 mismatches over 3,918 payloads

Driven through the **real** functions, not reimplementations: the real
unexported `internal/handlers.isSafeLinkHref` and `isSafeCoverImage` under
`go test`, and the real `web/src/lib/markdown.ts` `isSafeLinkHref` plus
`workshopAdmin.ts`'s `isSafeCoverImage` (via an `export`-added copy, `diff`-proved
to differ from the committed file by the `export` keyword and nothing else)
under vitest, over one shared 3,918-payload list — every codepoint `0x00`–`0xFF`
plus ~50 notable non-ASCII format/separator codepoints, each in interior,
leading, doubled-leading and trailing position, before a `javascript:` scheme,
before a `//host` destination, before a legitimate `https://` URL and before a
relative path, plus ~80 curated scheme/shape payloads.

```
RULE  go isSafeLinkHref   vs ts isSafeLinkHref    →  0 mismatches / 3918
RULE  go isSafeCoverImage vs ts isSafeCoverImage  →  0 mismatches / 3918  (#0138 regression check)

interior codepoint sweep, /x<CP>y.jpg, CP in 0x00..0xFF:
  go isSafeLinkHref    rejects 33  == C0∪{DEL}: true
  go isSafeCoverImage  rejects 33  == C0∪{DEL}: true
  ts isSafeLinkHref    rejects 33  == C0∪{DEL}: true
  ts isSafeCoverImage  rejects 33  == C0∪{DEL}: true
```

All four agree, and on exactly the set `#0138`'s review established. There is no
third variant, and the new Go function did not become one.

### Ruling on item 1: it is the **link** rule, not the image rule — confirmed

The commonest way this change could have gone wrong is copying `#0138`'s
same-origin-only cover-image rule onto a field that legitimately points off-site.
It did not. Verified case-for-case against both Go validators:

| `signup_url` | link rule (`isSafeLinkHref`) | image rule (`isSafeCoverImage`) |
|---|---|---|
| `https://eventbrite.com/e/soldering-101` | **ACCEPT** ✅ | reject |
| `HtTp://example.com/rsvp` | **ACCEPT** ✅ | reject |
| `mailto:hello@opencircuitsf.com`, `MAILTO:a@b.c` | **ACCEPT** ✅ | reject |
| `/rsvp`, `rsvp`, `./rsvp`, `?a=b`, `#frag` | ACCEPT | (varies) |
| `//evil.host/x`, `\\evil.host/x`, `/\evil.host/x`, `\/evil.host/x`, `///evil.host/x` | REJECT | reject |
| `javascript:`, `JavaScript:`, `data:`, `vbscript:`, `ftp:`, `file:`, `tel:`, `about:`, `blob:` | REJECT | reject |
| `java<TAB>script:alert(1)`, `/<TAB>/evil.host/x`, `<BOM>javascript:…`, `<NBSP>javascript:…`, `<U+3000>javascript:…`, `<LS>javascript:…` | REJECT | reject |

An external ticketing page — the normal case — round-trips. The image rule would
have rejected every one of the first three rows. The right twin was ported.

One deliberately-checked non-issue: `<U+212A KELVIN SIGN>avascript:alert(1)` is
accepted by **both** twins. Correct — JS's `/i` flag in non-`u` mode never
case-folds a ≥U+0080 code unit onto an ASCII one, so `HAS_SCHEME` does not match
it either, and `isASCIILetter` agrees. It also does not spell a scheme.

### Real 400s at both verbs, row unchanged — re-proved, including a gap the tests leave

The committed `Create` reject test covers four payloads; the committed `Patch`
reject test covers only `javascript:` and `/<TAB>/evil.host/x`, so I proved the
protocol-relative family at `PATCH` myself, against a real Postgres, on a
throwaway row seeded with a *good* `signup_url` so a silent overwrite would show:

```
PATCH "//evil.host/x"        -> 400 {"error":"signup_url must be an http(s) URL, …"}
PATCH "\\evil.host/x"        -> 400   (row re-fetched, still https://eventbrite.com/e/soldering-101)
PATCH "/\evil.host/x"        -> 400   (row unchanged)
PATCH "data:text/html,evil"  -> 400   (row unchanged)
PATCH "\x00javascript:…"     -> 400   (rejected at JSON decode; never stored)
```

Plus the committed `…AcceptsSafeSignupURLAndRoundTrips` pair, which I watched
pass at both verbs: `https://eventbrite.com/e/soldering-101` is stored and read
back intact. Throwaway `zz-subtest-workshop-<nanos>` rows only; no literal or
seeded id anywhere (§8b). **Not a bounce** — the rule is shared and `Patch` calls
the identical function, so this is a coverage nit, not a hole; noted so a future
pass can widen `TestAdminWorkshops_PatchRejectsUnsafeSignupURL` to the same four
payloads `Create` uses.

### The decisive test: nothing the server stores can render as a dangerous scheme

Rule-level parity proves the twins agree; it does not prove they agree with a
browser. So I resolved every payload the **server would actually store** against
`https://www.opencircuitsf.com/workshops/soldering-101` using Node's WHATWG
parser, and asked what protocol comes out:

```
payloads the server would STORE (non-null):                        2858
of those, payloads the page WOULD RENDER as an href:               2858
   → same-origin or mailto:                                        2583
   → off-site http(s)  (allowed by design — it is the LINK rule)    273
DANGEROUS-SCHEME among rendered:                                      0
unparseable among rendered:  2  ("http:", "https://") — inert, both twins agree
```

Zero. The two unparseable degenerates are dead links, not vulnerabilities, and
both twins treat them identically.

### Ruling on item 4, the trim decision: **leaving it alone is right.**

`isSafeLinkHref` trims internally, so neither handler pre-trims, and
`normalizeOptionalCampaignField`'s "tests trimmed, returns untrimmed" quirk
stays. I measured the resulting divergence between the **server boundary** and
the **render gate** (`hasExternalSignup`, which trims *before* calling the rule)
across the same 3,918 payloads. 65 differ, in two clean families and no others:

| Family | Count | What it is |
|---|---|---|
| Server **400s**, render gate would have shown it | 15 | exactly `{TAB, LF, VT, FF, CR}` × `{leading before https://…, leading before /path, trailing after https://…}`. `isSafeLinkHref` rejects C0 anywhere; `hasExternalSignup` strips it first. **Go stricter — fails safe.** |
| Server stores, page renders nothing | 50 | whitespace-only values. `normalizeOptionalCampaignField` nils them, so the column is `NULL` and there is nothing to render. Same outcome by two routes, not a divergence. |

Neither family is reachable through the product: `validateWorkshopForm` already
sends `fields.signupUrl.trim()` and gates on `isHttpUrl`, so the editor cannot
produce either shape. And the security-relevant direction — server accepts
something the render gate would display as an unsafe `href` — is **empty by
construction**, because the rule is trim-invariant (it trims with the same
JS-whitespace set the render gate's `.trim()` uses) and the two agree on all
3,918 payloads. `#0138` pass 2's 12-of-367 finding reproduces here at 15-of-3918
in the same direction and with the same conclusion: not this issue's to fix.
Generalizing `normalizeOptionalCampaignField` would touch every optional
campaign field for no security gain, and the `## Fix` section says so plainly
rather than hiding it. Correct scope call.

### Item 3, set completeness: **confirmed — and one path the `## Fix` prose omits**

I swept it myself rather than taking the claim. Every DB-backed value that
reaches a rendered attribute in the SPA:

| Sink | Value | Server-side check |
|---|---|---|
| `WorkshopDetail.svelte:121` `src={workshop.cover_image}` | `workshops.cover_image` | `isSafeCoverImage` (`#0138`) ✅ |
| `WorkshopDetail.svelte:161` `href={workshop.signup_url}` | `workshops.signup_url` | `isSafeLinkHref` (`#0152`) ✅ |
| `WorkshopCard.svelte:31` `href="/workshops/{workshop.slug}"` | `workshops.slug` | server-**generated**, never client-supplied: `store.go:147`'s `slugify` reduces to `[a-z0-9-]+`, and the href is prefixed `/workshops/`, so no scheme and no `//` prefix is expressible |
| `Header.svelte`, `Footer.svelte`, `About.svelte`, `PrivacyPolicy.svelte`, `PreferenceCenter.svelte`, `Logo.svelte` | `DISCORD_URL`, `LUMA_URL`, `CONTACT_EMAIL`, nav constants, `FULL_RASTER` | hardcoded constants, no DB content |
| `{@html}` × 2 (`WorkshopDetail.svelte:173`, `WorkshopEditor.svelte:393`) | `body_md` | different shape — every extracted destination filtered per-link by `isSafeLinkHref` inside `renderMarkdownPreview`; input HTML-escaped first |

The schema agrees: `migrations/000020_create_workshops.up.sql` has exactly two
URL-shaped columns, `signup_url` and `cover_image`, and no other table in
`migrations/` has one. No `location.href` / `location.assign` / `window.open` /
`srcset` / `poster` / `formaction` / `xlink:href` sink exists anywhere in
`web/src`. **The set is complete. There is no fourth issue in this family.**

**One reach the `## Fix` section does not name, checked and sound.** `signup_url`
has a *third* consumer beyond the SPA render and the editor:
`internal/handlers/admin_workshop_announce.go:219` (`#0145`) interpolates it into
a campaign body as `[Sign up](signupURL)`, which goldmark renders to an `<a href>`
at send time. That path was already protected — goldmark runs in safe mode
(`campaign_markdown.go` never passes `html.WithUnsafe()`, so `isDangerousLinkURL`
drops `javascript:`/`vbscript:`/`data:`) — and is now protected a second time by
this boundary check, which is the better layer for it. Worth recording because
the `## Fix` prose describes `email_campaigns.body_md` as "unrelated to this
issue's fields" when in fact `signup_url` flows into it; the *conclusion* is
unaffected. On the Go side, `internal/mailing/templates.go`'s `ButtonURL` — the
only other server-emitted `href` — is built solely from `baseURL` plus a token
(`transactional_templates.go`), never from a DB free-text column.

### Mutation, re-done independently

Copied `admin_workshops.go` aside first (§8a), inserted `return true` as the
first statement of `isSafeLinkHref`, and ran the affected tests on my own
private database (`scripts/testdb.sh create 0152mut`, dropped after — never the
shared `opencircuit_test`, per §8b):

- `TestIsSafeLinkHref` — **16 of its 21 subtests fail**, with the concrete
  observed values printed (`isSafeLinkHref("javascript:alert(1)") = true, want
  false`, and so on through the whole C0/DEL and protocol-relative set). The 5
  that still pass are the legitimate-URL cases, which is exactly right for an
  accept-everything mutation. (The implementer reported 15 of 20; I count 21
  cases and 16 failures. Either way, teeth.)
- `TestAdminWorkshops_CreateRejectsUnsafeSignupURL` — all four payloads
  `status = 201, want 400`, with the malicious value echoed back in the created
  row's JSON body.
- `TestAdminWorkshops_PatchRejectsUnsafeSignupURL` — `status = 200, want 400`
  for both payloads, **and** the row-unchanged assertion fires
  (`after rejected PATCH, SignupURL = 0x…, want nil`). The test asserts stored
  state, not just a status code.
- Both accept/round-trip tests still pass under the mutation, confirming they
  are not accidentally testing the guard.

Restored from the copy and confirmed byte-identical with `shasum -a 256 -c`
against the pre-mutation checksum: `admin_workshops.go: OK`. Worth noting that
`go vet` flags the mutation as `unreachable code`, so it cannot be committed by
accident — the run needed `-vet=off`.

### `scripts/check.sh` verdicts, re-run by me

Truthful post-`20d540c` (`#0140`), from the worktree:

- `ISSUE=0152rev scripts/check.sh go ./internal/...` — every DB-backed package
  `ok` with a real elapsed time (`internal/handlers 2.335s`, `internal/auth
  0.668s`, `internal/middleware 0.790s`, `internal/audit 0.286s`, `internal/db
  0.255s` — none of them an 0.00s that never reached a database). Skip audit
  shows only `internal/testdb [no test files]`, a helper package with no tests,
  not a skipped suite. `VERIFICATION PASSED`.
- `ISSUE=0152rev scripts/check.sh go ./internal/handlers/` — `ok … 2.100s`, skip
  audit `(no skips)`, `VERIFICATION PASSED`.
- `scripts/check.sh web` — svelte-check `228 FILES 0 ERRORS 0 WARNINGS`; vitest
  `19 passed / 464 tests passed`. Unchanged from `#0138` pass 2's numbers, as
  expected — this issue touched no TypeScript.

### One thing worth filing, not blocking

**Nothing automatically guards the Go↔TypeScript parity of these twins.** There
are now three cross-language security-rule pairs in this codebase
(`isSafeLinkHref` ×2 languages, `isSafeCoverImage` ×2 languages, and the shared
C0∪DEL set), and their agreement has been established three times — by `#0138`
pass 1, `#0138` pass 2, and this review — each time with a **throwaway** sweep
that was deleted immediately after. A future edit to either side would not be
caught by any committed test. `#0152` itself is the proof that this matters:
the parity break it fixed was invisible to every existing test and was found
only because someone ran a sweep by hand. A small committed fixture — one
checked-in payload/verdict list that both `go test` and vitest assert against —
would make the next divergence a red build instead of a review finding.
Recommending it as a new issue rather than folding it in here; it is a testing
-infrastructure change spanning two languages and three functions, well outside
this issue's scope.

## Relation

- Found by: [#0138](0138.md) — Cover images and protocol-relative URLs
- Relates to: [#0054](0054.md) — renders `signup_url` behind `hasExternalSignup`
- Relates to: [#0052](0052.md) — where the link-scheme hardening lives

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 134 | 4,568 | 7,408,969 | 152,589 | $2.86 |

**Total: $2.86**
