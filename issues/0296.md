# 0296 — `collectKindConstants` misses an alias-annotated Kind

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | mailing / tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |
| **Commit** | 858bf09 |
| **Closed** | 2026-08-27 |

> 🟢 Resolved 2026-08-27 — a `collectKindTypeNames` pre-pass closes the
> alias-annotated `Kind` gap, `isBare` now propagates through the
> const-repetition rule, and the deliberate same-block false positive is pinned
> by a permanent test that the reviewer confirmed still fires.

## Description

`#0282` extended `collectKindConstants` to resolve four `Kind` declaration
styles. Its review confirmed the fix and the reproduction, and then found a
fifth style and one incompleteness in the third.

**1. An alias-annotated constant is invisible.**

```go
type KindAlias = Kind
const KindE KindAlias = "e"
```

`go/types` resolves `KindE` to `Kind`. The shipped detector returns
`detected=false`, because it matches the **spelling** `Kind` and a non-nil
`vs.Type` never reaches the bare-constant branch. Contrived, but it is a
fail-open in a guard whose whole purpose is to fail closed — a `Kind` declared
that way is claimed by no poller and stalls in `'queued'` forever.

**2. `isBare` does not propagate through the repetition rule.**

```go
const (
    KindA Kind = "a"
    KindB      = "b"
    KindC
)
```

`KindC` inherits `isKind` via Go's const-repetition rule but is missed, because
`isBare` is not carried forward. `go/types` also reports `untyped string` here,
so this is heuristic incompleteness rather than something type analysis would
fix.

**3. The "zero marginal precision" claim needs narrowing.** `#0282`'s work log
states `go/types` buys zero marginal precision over the AST walk. That is
strictly false — item 1 is a case where `go/types` gets the right answer and the
detector does not. The conclusion (that `go/types` is not worth its cost here)
may well stand; the *claim as written* should not, since a future reader will
rely on it. `#0282`'s reviewer flagged this as an overstatement rather than
bouncing for it.

**4. `intakeException` is a single `const`, not a set**, so adding a second
exception requires restructuring first. Minor friction, worth fixing while here.

Also measured and worth knowing: the same-block heuristic **does** false-positive
on an unrelated untyped constant sharing the `Kind` block (a
`defaultKindNote = "…"` fails the guard, naming it). It fails **loud** and in
the safe direction with an actionable message, and `internal/outbox` has no such
constant today — so this is acceptable behaviour, not a defect. Do not "fix" it
by weakening the detector.

## Acceptance criteria

1. A pre-pass collects `type X = Kind` and `type X Kind` names declared in the
   package, and `vs.Type` is matched against that set rather than the literal
   spelling `Kind`.
2. `isBare` propagates through the const-repetition rule, so `KindC` above is
   detected.
3. Each gap is **reproduced as a failing test before being closed** — per
   `#0282`'s criterion 2 discipline, which its review verified was followed.
4. `intakeException` becomes a set, with each entry carrying a written reason.
   An exception list that anyone can append to without argument is a guard with
   a hole — see [#0280](0280.md) for the same lesson on the web side.
5. The doc comment's "exact for styles 1-3 / zero marginal precision" wording is
   corrected to reflect item 1. State what `go/types` would and would not buy,
   accurately.
6. The false-positive behaviour on an unrelated block constant is **preserved
   and documented** as deliberate, not removed.
7. Fail-closed on a zero-constant parse and specific-kind naming both survive —
   `#0282`'s review verified both; do not regress them.
8. `ISSUE=NNNN scripts/check.sh go ./internal/mailing/... ./internal/outbox/...`
   passes with no skips.

## Relation

- Found by: [#0282](0282.md)'s review
- Relates to: [#0282](0282.md) — which built the detector
- Relates to: [#0254](0254.md) — which added the guard this extends
- Relates to: [#0290](0290.md) — the same `go/ast` vs `go/types` question on the
  audit-metadata guard
- Relates to: [#0280](0280.md) — the allowlist-hygiene lesson item 4 applies

## Work log

**2026-08-27, phase-2 implementation.**

**Criterion 3 discipline — each gap reproduced as a failing test BEFORE
closing it**, mirroring `#0282`'s own methodology exactly. Added two new
fixtures/tests to `internal/mailing/outbox_kind_detection_test.go`
(`TestCollectKindConstants_AliasAnnotatedKind` for item 1,
`TestCollectKindConstants_RepetitionAfterBareSibling` for item 2) BEFORE
touching `collectKindConstants`/`collectKindConstantsInBlock`, and ran them
against the unmodified detector:

```
=== RUN   TestCollectKindConstants_AliasAnnotatedKind
    outbox_kind_detection_test.go:192: collectKindConstants missed [KindE];
    found only [KindAnnotated] ...
--- FAIL: TestCollectKindConstants_AliasAnnotatedKind (0.00s)
=== RUN   TestCollectKindConstants_RepetitionAfterBareSibling
    outbox_kind_detection_test.go:251: collectKindConstants missed [KindC];
    found only [KindA KindB] ...
--- FAIL: TestCollectKindConstants_RepetitionAfterBareSibling (0.00s)
```

Both gaps reproduced with the exact symptom the issue describes. Also added a
third fixture/test at this same step, `sameBlockFalsePositiveFixture` /
`TestCollectKindConstants_SameBlockFalsePositiveIsDeliberate`, and ran it
against the SAME unmodified detector to confirm criterion 6's false positive
already exists pre-fix (so this fix must not accidentally remove it): it
PASSed immediately (`defaultKindNote` — untyped, sharing `KindConfirmation`'s
block — was already swept in), establishing the baseline this fix is
obligated to preserve, not create.

**Item 1 — pre-pass for alias/defined-type names (`collectKindTypeNames`,
`internal/mailing/outbox_worker_kinds_guard_test.go`).** Walks every `type`
`GenDecl` in the parsed files, recording `(name, base)` for every `type X
<ident>` spec (both `type X = Kind` aliases and `type X Kind` defined types —
the issue's acceptance criterion 1 names both forms explicitly), seeded with
`{"Kind": true}`, then runs a small fixed-point loop so a chain (an alias of
an alias) resolves regardless of declaration order — none exists in real
source today, but the loop costs nothing. `collectKindConstantsInBlock`'s
style-1 case (`isIdent(vs.Type, "Kind")`) became
`isKindTypeIdent(vs.Type, kindTypeNames)`, matching the SET rather than one
literal spelling; `isKindConversion` (style 2) takes the same set for the
same reason, so `KindX = KindAlias("x")` resolves too, not just `KindX =
Kind("x")` — a natural extension of the same pre-pass output the issue's
acceptance criteria did not explicitly ask for but cost nothing additional to
close. `collectKindConstants` now runs `collectKindTypeNames` once per call
and threads it through — the exported (package-level-visible) signature is
unchanged, so `TestMailKindsCoversEveryOutboxKind` needed no changes.

**Item 2 — `isBare` propagates through the repetition rule.** Style 3's case
(`vs.Type == nil && !nonEmpty`) previously copied only `info.isKind =
lastNonEmpty.isKind`; now copies `info.isBare = lastNonEmpty.isBare` too. A
repetition of a bare style-4 sibling (`KindA Kind = "a"; KindB = "b"; KindC`)
now inherits `isBare` from `KindB`, and the same-block backfill (which checks
`isKindBlock && info.isBare`) then sweeps `KindC` in, exactly as `KindB`
already was.

**Re-ran both new tests against the fixed detector — both now PASS**
(confirmed below, criterion 3's "then closed" half). The third
(false-positive) test also still PASSes — confirmed the fix does not
accidentally narrow style 4 (criterion 6).

**Item 4 — `intakeException` (single `const`, compared with `==`) became
`kindExceptions` (`map[string]string`, name → written reason).** One entry
today, `"KindSubscribeIntake": "not an email — claimed by internal/handlers.
SubscribeHandler's own recovery poller instead of this worker..."` — same
`#0280` allowlist-hygiene lesson this issue cites: a bare name with no reason
is no longer possible to add without visibly standing out at review.
`TestMailKindsCoversEveryOutboxKind`'s check became `if _, exempt :=
kindExceptions[kind]; exempt || claimedByMailKinds[kind]`; its failure
message updated from "the KindSubscribeIntake exception" to the generic
"neither mailKinds nor kindExceptions", confirmed via a real mutation (added
a temporary `KindWhatever Kind = "whatever"` to `internal/outbox/store.go` in
place, ran the test, confirmed it failed and named `KindWhatever` with the
new message text, then restored the file and verified the hash matched the
pre-mutation copy).

**Item 5 — corrected `#0282`'s "zero marginal precision" claim.**
`collectKindConstants`' "Why go/ast rather than go/types" doc comment now
states plainly that the original claim was strictly false: style 5 (an alias
annotation, item 1's own case) is one where `go/types` DOES resolve the
constant's real type correctly while a spelling-based AST match does not —
`#0282`'s own review had already measured this (`go/types` reporting
`fixture.Kind` for `KindE`, not `untyped string`). The corrected framing: with
`collectKindTypeNames` now closing that exact gap via a self-contained AST
fact rather than full type-checking, `go/types` buys NEGLIGIBLE marginal
precision over (AST type-name pre-pass + same-block heuristic) — not zero,
but not enough to justify its real cost (a working importer over
`internal/outbox`'s full import graph). Style 4 is unaffected by this
correction: `go/types` still reports `untyped string` there, so the same-block
heuristic is still the only signal, exactly as `#0282`'s review measured.

**Criterion 6 — the deliberate false positive.** Preserved, not weakened, and
now pinned by a permanent test
(`TestCollectKindConstants_SameBlockFalsePositiveIsDeliberate`) rather than
only documented in prose — see the criterion-3 reproduction above for proof
it existed before this fix and survives after. `collectKindConstantsInBlock`'s
same-block backfill logic (`info.isKind || (isKindBlock && info.isBare)`) was
not touched by items 1 or 2's fixes.

**Criterion 7 — fail-closed and specific-kind-naming survive.**
`TestCollectKindConstants_EmptySourceYieldsEmptySet` (unchanged, still
passes) covers the zero-constant fail-closed path. The `KindWhatever`
mutation proof above (item 4) re-confirms specific-kind naming end to end
through the real guard, matching `#0282`'s review methodology.

**Fixed a self-inflicted regression before it reached verification.** Placing
`kindExceptions`'s new doc comment between two paragraphs of
`TestMailKindsCoversEveryOutboxKind`'s existing doc comment (no blank line
separating them) merged the two into ONE comment block, which Go/the
misattached-doc-comment guard (`internal/handlers`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`, `#0127`/`#0267`)
correctly attributed to `kindExceptions` (the immediately following
declaration) rather than to the test function the prose was actually about.
Caught by a scoped `internal/handlers` test run, not by inspection. Fixed by
moving `kindExceptions` and its own, now clearly separated, doc comment
entirely before `TestMailKindsCoversEveryOutboxKind`'s block, with a real
blank line between the two declarations' comments.

Files changed: `internal/mailing/outbox_worker_kinds_guard_test.go`
(`collectKindTypeNames` new, `collectKindConstants`/
`collectKindConstantsInBlock`/`isKindConversion` signatures and logic
updated, `isIdent` removed and folded into `isKindTypeIdent`,
`intakeException` replaced by `kindExceptions`, doc comments corrected),
`internal/mailing/outbox_kind_detection_test.go` (three new fixtures/tests).
Did not touch `internal/outbox` or `#0294`/`#0295`/`#0297`.

## Verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0294r scripts/check.sh go ./internal/handlers/... ./internal/mailing/... ./internal/outbox/...
```

(Same run covered both `#0294` and `#0296` — one dev pass, one database, both
issues' scoped packages overlap.) `uptime` at start: load averages 2.15 1.90
2.01 (idle). Result: `internal/handlers` ok 5.675s, `internal/mailing` ok
8.336s, `internal/outbox` ok 2.876s — skip audit `(no skips)`, no package
reported `0.00s`, `gofmt -l` clean, `go vet` clean, `VERIFICATION PASSED`.
Individually re-ran this issue's own new/changed tests with `-v`:
`TestCollectKindConstants_AliasAnnotatedKind`,
`TestCollectKindConstants_RepetitionAfterBareSibling`,
`TestCollectKindConstants_SameBlockFalsePositiveIsDeliberate`,
`TestCollectKindConstants_AllDeclarationStyles`,
`TestCollectKindConstants_DoesNotLeakAcrossUnrelatedBlocks`,
`TestCollectKindConstants_EmptySourceYieldsEmptySet`,
`TestMailKindsCoversEveryOutboxKind` — all PASS. Confirmed `gofmt -w` is a
fixed point on both changed files (hashed copies before/after, identical
hashes, CLAUDE.md §8). Scratch database `opencircuit_test_0294r` already
absent when checked with `scripts/testdb.sh drop`. `pgrep -f 'go test|\.test
'` clean at the end of this session.

No load-sensitive behavior anywhere in this change — this is a pure `go/ast`
parse-and-compare guard with no timing dependency (criterion 8 has no
load-related clause, unlike `#0294`'s).

## Review notes

**Verdict: pass.** Re-verified independently on 2026-08-27 against commit
`858bf09`. `uptime` 3.07/2.34/2.16 and 1.90/2.16/2.11 — idle; nothing in this
change is timing-sensitive (a pure `go/ast` parse-and-compare guard).

`ISSUE=0294r scripts/check.sh go ./internal/handlers/... ./internal/mailing/...
./internal/outbox/... ./cmd/opencircuit/...` → `handlers` ok 5.604s, `mailing`
ok 8.911s, `outbox` ok 3.488s, `cmd/opencircuit` ok 1.432s; skip audit
`(no skips)`; `gofmt -l` clean; `go vet` clean; `VERIFICATION PASSED`.

All mutation work below was done in a throwaway detached worktree at `858bf09`,
restored after each mutation (`git status --porcelain` empty at every step) and
removed afterwards. The main tree was never mutated.

### Criterion 3 — both gaps re-reproduced against a pre-fix detector

Not taken from the work log. Each fix was reverted *separately*, so each test's
specificity is proved rather than assumed:

| Reverted | Alias test | Repetition test | False-positive test |
|---|---|---|---|
| `isKindTypeIdent` → literal `id.Name == "Kind"` | **FAIL** ("missed [KindE]; found only [KindAnnotated]") | PASS | PASS |
| style 3 propagates only `isKind` | PASS | **FAIL** ("missed [KindC]; found only [KindA KindB]") | PASS |

Each test fails for its own gap and only its own gap. Both messages match the
issue's Description exactly.

### Criterion 6 — the deliberate false positive still fires

This was the criterion most at risk of being "cleaned up", so it was mutated
rather than read. Narrowing style 4 (dropping the same-block backfill,
`if info.isKind || (isKindBlock && info.isBare)` → `if info.isKind`) makes
`TestCollectKindConstants_SameBlockFalsePositiveIsDeliberate` **FAIL** with its
intended message:

```
collectKindConstants no longer sweeps in an unrelated untyped constant sharing
the Kind block — this is a DELIBERATE false positive (#0296 criterion 6,
#0282's review), not a defect: do not narrow style 4 to fix this, doing so
risks a real Kind going undetected instead
```

`TestCollectKindConstants_AllDeclarationStyles` and the item-2 test fail
alongside it, so the behaviour is pinned from three directions. Preserved, not
removed, and now enforced rather than merely documented.

### Criterion 7 — fail-closed and specific-kind naming both survive

Mutation-proved, not inspected:

- **Fail closed on a zero-constant parse.** Forcing `collectKindConstants` to
  return an empty map makes `TestMailKindsCoversEveryOutboxKind` **FAIL** at
  `outbox_worker_kinds_guard_test.go:72` — "found zero Kind constants in
  internal/outbox — the parse likely broke, not a real empty set". It does not
  pass vacuously. `TestCollectKindConstants_EmptySourceYieldsEmptySet` also
  still passes.
- **Specific-kind naming, through the real guard, both directions.** Adding
  `KindWhatever Kind = "whatever"` to `internal/outbox/store.go` → **FAIL**
  naming `[KindWhatever]` with the new `kindExceptions` wording. Emptying
  `kindExceptions` → **FAIL** naming `[KindSubscribeIntake]`. So the map is
  load-bearing and the message names the offending constant, not just the fact
  of a mismatch.

### Transitive alias resolution — terminates, and resolves out of order

The pre-pass goes beyond criterion 1 (which asked only for a flat set), so its
fixed-point loop was probed directly with a scratch test in the worktree:

- **Cycle** (`type A = B; type B = A`, alongside a real `Kind`): terminates
  immediately and resolves `[KindOK]` only. Neither `A` nor `B` ever reaches
  `Kind`, so `changed` stays false after one sweep — no hang, no stack growth.
  The loop's bound is the candidate count regardless, since a name is only ever
  added once. A constant annotated `A` is correctly *not* detected.
- **Out-of-order chain** (`type K3 = K2` declared before `type K2 = Kind`,
  before `type Kind string`): `KindDeep K3 = "deep"` resolves. This is what the
  fixed point buys over a single pass, and it is why the loop is not gratuitous
  — Go permits any declaration order.

### Criterion 4 — the required reason, judged against `#0298`'s precedent

`intakeException` (a bare `const` compared with `==`) is now
`kindExceptions map[string]string`, one entry, reason a substantive paragraph.
The criterion's letter — "a set, with each entry carrying a written reason" —
is met, and the map type makes a reasonless append syntactically conspicuous.

Judged, not waved through: `#0298`'s web-side equivalent goes one step further
and **enforces** it — an entry with `reason: ''` fires its own violation,
independent of the unscanned-file rule, which that review verified by
injection. Here `"KindFoo": ""` would compile and exempt silently. That is a
real (small) asymmetry with the sibling, but it is beyond what criterion 4
asks for, the list has one entry, and the fix is a four-line assertion. Not a
bounce; **recommended as a follow-up** rather than left unsaid.

### The self-inflicted doc-comment regression

Confirmed clean. `kindExceptions` and its doc comment now sit entirely before
`TestMailKindsCoversEveryOutboxKind`'s block with a blank line between them, so
neither comment is attributed to the other declaration.
`TestNoDocCommentNamesADifferentDeclarationInSameFile` (`#0127`/`#0267`) passes
when run on its own and inside the full `internal/handlers` package run. Worth
recording that the guard caught this at all — it is the second time this
session that a comment placed between two paragraphs merged into one block.

### Criterion 5 — the corrected claim reads accurately

`#0282`'s "zero marginal precision" is now stated as strictly false, with the
narrower true claim in its place: `go/types` resolves style 5 correctly where a
spelling match does not, that precision is now obtained without `go/types` via
an AST type-name pre-pass, and style 4 remains `untyped string` under
`go/types` so the same-block heuristic is still required either way. The
"exact for 1-3" wording became "exact for 1, 2, 3, and 5, and heuristic only
for style 4". Both halves check out against `#0282`'s own measurements.
