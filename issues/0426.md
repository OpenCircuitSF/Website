# 0426 — `docs/deployment.md` says the IAM role was never attached; it is attached, and the doc calls that the reason SES cannot work

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | docs / infra |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-04 |
| **Closed** | 2026-09-04 |
| **Commit** | `47e844a` |

> 🟢 Resolved 2026-09-04 — the role, region, sending identity and account
> ID were all re-measured against the box by the reviewer, read-only and
> independently of the issue text: instance metadata returns
> `opencircuit-instance`, an instance-profile ARN under account `378152330719`,
> `us-east-1`, and `i-0e3bd89e87d1c2364`, and `EMAIL_FROM` confirms the
> `mailing.` subdomain identity. The live IAM policy is genuinely unreadable
> from the box — `AccessDenied` both as `certbot-dns-updater` and as the
> instance role itself — and both documents say so plainly, ahead of the JSON
> rather than in a footnote. The `identity/*` requirement and the
> simulator-address caveat are stated with their mechanism and the real
> `AccessDeniedException` incident behind them, and that incident record is
> byte-unchanged.

## Description

`docs/deployment.md`'s Production-facts table says the instance has **"None
attached"** for its IAM role, and calls that *"the reason SES cannot work yet."*
Its `## IAM` section says the role has **"never been created or attached to a
real role."**

**The role is attached.** Instance metadata returns `opencircuit-instance` —
confirmed independently by the orchestrator against the live box, read-only.
`CLAUDE.md` §10 item 2 already records it as attached "and **proven by a real
delivered send**."

`docs/aws-iam-setup.md`'s "Current instance role: none" is stale the same way.

### Why this one is worse than ordinary drift

The doc does not merely record a stale fact — it **draws a conclusion from it**:
that SES cannot work. A reader troubleshooting a send would chase a missing role
that is present, and might create or attach a second one.

**And `#0421` has just pointed readers at that table.** Its correction block
directs a reader to the Production-facts table as the current-state record, so
the stale row is now more prominent than it was.

### A second wrong fact in the same section

The IAM policy in `docs/deployment.md` names
`arn:aws:ses:us-west-2:…:identity/opencircuitsf.com`. Both halves are wrong: the
region is `us-east-1`, and the real identity is **`mailing.opencircuitsf.com`**.

`CLAUDE.md` §10 item 2 records a trap that makes this consequential: in the SES
sandbox, `SendEmail` is authorised against the **recipient's** identity ARN too,
so the policy needs `identity/*` rather than a single identity — and a test send
to a simulator address will **not** catch a policy that gets this wrong, because
simulator addresses are not identities.

## Acceptance criteria

1. The Production-facts row and the `## IAM` section state the role that is
   actually attached, and no longer assert that a missing role blocks SES.
   **Re-derive the attachment from the box yourself**, read-only — do not copy
   `opencircuit-instance` from this issue.
2. `docs/aws-iam-setup.md`'s "Current instance role" is corrected the same way.
3. The policy ARN's region and identity are corrected, and the `identity/*`
   requirement from `CLAUDE.md` §10 item 2 is stated with the reason — including
   that a simulator-address test does not exercise it.
4. **Read the actual attached policy if you can.** If the live policy differs
   from what the doc shows, the doc should describe reality, not the plan. Note
   the box's default identity is denied most IAM reads — say what you could and
   could not read, and **do not create or hunt credentials**.
5. Follow `#0072`'s dated-note convention for maintained references and leave
   genuine decision-log history intact per `#0271`'s `## Scope`. Say which
   occurrences you judged to be which.
6. **Change nothing in AWS or on the box.** This is documentation.
7. **No mechanism** (`#0330`/`#0347`/`#0356`/`#0381`).
8. Editing `PRD.md` means `./internal/db/...` with §11's line counts exact,
   verified by each row's own `sed` (`#0113`). Run `./internal/handlers/...` for
   cited paths. Per §5 name the specific tests that ran. No
   Go-file-plus-line-number citation (`#0352`), scanned with `python3` bytes
   rather than BSD `grep -P`.

## Verification

- `ISSUE=0426 scripts/check.sh go ./internal/db/... ./internal/handlers/...`
  — `go build`, `go vet`, `gofmt -l` (clean), then
  `go test ./internal/db/... ./internal/handlers/... -p 2`:
  `ok github.com/brennanMKE/OpenCircuitSF/internal/db 0.465s` and
  `ok github.com/brennanMKE/OpenCircuitSF/internal/handlers 8.988s` (final
  canonical run, after the issue-file edits below — re-run twice more in
  between with real elapsed times each time) — zero
  skips, both DB-backed packages reported real elapsed time. Ran against a
  per-issue database (`opencircuit_test_0426`) provisioned directly from
  `migrations/` rather than cloned from the shared template, because the
  template is at 28 and this tree's `migrations/` is at 27 — `check.sh`
  detected the mismatch itself and took the slower, correct path (per
  CLAUDE.md §5a / `#0315`). Database dropped on exit per `check.sh`'s normal
  cleanup.
- `python3 -m json.tool` against the corrected JSON block in
  `docs/deployment.md`'s `## IAM` section (extracted with `sed -n
  '979,998p'`) — parses clean, matching the region/account/identity now
  described in prose around it.
- Read `docs/deployment.md` and `docs/aws-iam-setup.md` in full (`sed -n`,
  `grep -n`) before and after editing to find every occurrence of the stale
  claim, not just the two the issue quoted.

## Facts re-derived against the box (read-only, `#0426`, 2026-09-04)

- **Role attachment.** `ssh ec2` (read-only), then against the instance
  metadata service with a fetched token:
  `curl -H "X-aws-ec2-metadata-token: $TOKEN"
  http://169.254.169.254/latest/meta-data/iam/security-credentials/` →
  `opencircuit-instance`. Not copied from this issue's filing text.
- **Region.** The same instance metadata service,
  `.../latest/meta-data/placement/region` → `us-east-1` — independent of
  `/etc/opencircuit/config.env`, which separately confirms
  `AWS_REGION=us-east-1`.
- **Identity.** `/etc/opencircuit/config.env`'s `EMAIL_FROM` is `"Open
  Circuit SF <contact@mailing.opencircuitsf.com>"`, confirming the sending
  identity is the `mailing.` subdomain, not the apex.
- **Live attached policy — could not read it.** The box's default AWS CLI
  identity is `certbot-dns-updater` (`aws sts get-caller-identity`).
  `aws iam get-role --role-name opencircuit-instance`,
  `aws iam list-role-policies --role-name opencircuit-instance`, and
  `aws iam list-attached-role-policies --role-name opencircuit-instance` each
  returned `AccessDenied` naming that user and the missing action. No
  credentials were created or hunted for; this is the box's existing default
  identity, used read-only. `docs/deployment.md`'s `## IAM` section now says
  this explicitly and cites `docs/aws-iam-setup.md`'s own account of the
  attached policy (including a real production `AccessDeniedException`
  incident and its fix) as the source for the corrected JSON, rather than a
  live read this pass could make itself.

## Relation

- Found by: [#0421](0421.md)'s review
- Relates to: [#0421](0421.md) — whose correction block now points readers at the
  stale table
- Relates to: [#0415](0415.md) — the other open question about SES's real state
- Relates to: [#0414](0414.md), [#0418](0418.md) — the same runbook, corrected
  for address and region but not for this
- Relates to: [#0057](0057.md) — which will need the real policy shape

## Work log

- 2026-09-04 — Filed by the orchestrator from `#0421`'s review (§9). The role's
  attachment was confirmed against instance metadata before filing; the policy
  ARN's contents were not independently read.
- 2026-09-04 — Implementation pass (phase 2, docs only). Re-derived the role
  attachment, region, and sending identity against the box myself, read-only
  (see `## Verification`'s "Facts re-derived" list), rather than trusting the
  issue text or `CLAUDE.md`.

  **Occurrences changed, and why each was judged maintained-reference rather
  than decision-log history (`#0072`'s dated-note convention, `#0271`'s
  `## Scope`):**
  1. `docs/deployment.md`'s Production-facts table, `IAM instance role` row —
     the row the issue quotes. Maintained: it is the "measured on the box"
     table `#0421`'s correction block now points readers at.
  2. `docs/deployment.md`'s `## IAM` section — the policy JSON and its prose.
     Maintained: it states what to do/expect today, not what was true at
     some past moment. Also fixed the region, account ID, and identity in
     the JSON itself, added the `identity/*` requirement with the sandbox
     reason and the simulator-address caveat (`CLAUDE.md` §10 item 2), and
     removed a dangling orphaned sentence fragment ("capable of that exist in
     this environment...") left over from an earlier edit that split one
     sentence in two.
  3. `docs/deployment.md`'s top-of-file orientation table (the "Section /
     Status" table right after the intro) — **not named in the issue**, but
     it asserts the identical false claim in bold at the very top of the
     document ("the instance has no IAM role attached at all"). Judged
     maintained (current SES/IAM status), fixed narrowly: struck only that
     clause, added a `#0426` correction, and reworded the surrounding
     sentence to scope "still not followed" / "no identity existed" to *that
     row's original 2026-08-25 site deploy* specifically, which is still
     historically true — SES was genuinely not configured as part of
     bringing the site up, only in the separate later pass
     `docs/aws-iam-setup.md` documents. Left "No AWS SES identity exists" as
     a claim about that original deploy, not corrected to present tense,
     since the identity's current existence is `#0415`/`#0421`'s territory,
     not named in this issue's acceptance criteria.
  4. `docs/deployment.md`'s "What this document is, and is not, verified
     against" section — two bullets asserting "No SES identity exists and
     the instance has no IAM role" and "not validated against a real AWS
     account, which does not exist." Judged maintained (verification-status
     claims about current reality, not a log of a past verification
     attempt), both corrected with the same `#0426` dated-note pattern. Also
     removed "and the account ID" from a sentence listing which
     `[PLACEHOLDER: ...]` markers remain unresolved, since the account ID
     (`378152330719`) is no longer one of them after fix #2 above.
  5. `docs/aws-iam-setup.md`'s "The short version" opening paragraph, the
     "Current instance role" row in its facts table, and step 5 of "After the
     role is attached". All maintained (current status of a still-relevant
     runbook), all corrected. Left Parts 1–3 (the console walkthrough) and
     "Why not static access keys" untouched — they are reference material for
     *how* the role was built, still accurate as instructions, not assertions
     that it isn't built yet.

  **Left as genuine decision-log history, not touched:** `docs/deployment.md`
  lines ~1093–1095, `#0229`'s `## Decision` on deferring S3-backed backups
  ("building it needs an AWS account, which does not exist yet"). This is an
  explicit quotation of what `#0229` decided at the time, about a different
  subsystem (backups, not SES/IAM) and not cited by this issue; rewriting it
  would falsify that record per `#0271`'s `## Scope`. It is now also stale
  (the account exists), but that is `#0229`'s or a new issue's concern, not
  this one's — flagged to the orchestrator rather than fixed here.

  **What could not be verified.** The box's default AWS CLI identity
  (`certbot-dns-updater`) is denied `iam:GetRole`,
  `iam:ListRolePolicies`, and `iam:ListAttachedRolePolicies` against
  `opencircuit-instance` — confirmed read-only, no credentials created or
  sought. The corrected policy JSON in `docs/deployment.md`'s `## IAM`
  section is therefore transcribed from `docs/aws-iam-setup.md`'s own account
  of what is attached (which itself documents a real production
  `AccessDeniedException` incident and its fix), not from a live
  `GetRolePolicy`/`ListRolePolicies` read this pass could make itself. Said
  so explicitly in both the doc and here rather than presenting the
  transcription as independently verified.

  **Not filed (subagents report, don't file, §9):** `docs/deployment.md`'s
  §10.6/backups bullet above (stale "AWS account does not exist" language,
  unrelated subsystem) is worth a follow-up issue if nobody already has one
  open for it. `#0428` was the highest allocated number at the start of this
  pass; did not check again since nothing here needed filing.

  Verification: `ISSUE=0426 scripts/check.sh go ./internal/db/...
  ./internal/handlers/...` — zero skips, both DB-backed packages reported
  real elapsed time (see `## Verification` above for the exact lines).
  Committed the two doc files with a pathspec after checking
  `git diff --cached --name-only` showed only them; this issue file is left
  uncommitted for the reviewer per the phase-2 convention.

## Review notes

Phase-3 review (Opus), 2026-09-04, against `47e844a`. Docs only — the commit
touches `docs/deployment.md` and `docs/aws-iam-setup.md` and nothing else.

### Facts re-measured independently, not taken from the issue or the implementer

All read-only against the live box via `ssh ec2`; nothing in AWS or on the box
was changed, and no credential was created or hunted. IMDSv2 token fetched,
then:

- `iam/security-credentials/` → `opencircuit-instance`. **Role attached,
  confirmed.**
- `iam/info` → `"InstanceProfileArn": "arn:aws:iam::378152330719:instance-profile/opencircuit-instance"`,
  `"Code": "Success"`. This is a **second, independent** confirmation and it
  also establishes the **account ID `378152330719`** from the box rather than
  from any document — the corrected policy JSON's account ID is therefore
  measured, not transcribed.
- `placement/region` → `us-east-1`; `instance-id` → `i-0e3bd89e87d1c2364`.
  Both match the corrected text and `CLAUDE.md` §7.
- `/etc/opencircuit/config.env`: `AWS_REGION=us-east-1`,
  `EMAIL_FROM="Open Circuit SF <contact@mailing.opencircuitsf.com>"`,
  `SES_CONFIGURATION_SET=opencircuit-transactional`. The sending identity is
  the `mailing.` subdomain, confirming the corrected ARN's identity half.

So all three facts the correction turns on — role, region, identity — plus the
account ID hold up under my own measurement.

### The unread policy: the honesty holds, and I confirmed the denial two ways

The implementer's claim that it could not read the attached policy is true, and
stronger than it reported:

- As the box's default CLI identity: `aws iam get-role`,
  `aws iam list-role-policies` and `aws iam list-attached-role-policies`
  against `opencircuit-instance` each returned `AccessDenied` naming
  `arn:aws:iam::378152330719:user/certbot-dns-updater` and the missing action.
- I also tried it **through the instance role itself**, using no new
  credential — `AWS_SHARED_CREDENTIALS_FILE=/dev/null AWS_CONFIG_FILE=/dev/null`
  so the default chain falls through to instance metadata.
  `aws sts get-caller-identity` then returned
  `arn:aws:sts::378152330719:assumed-role/opencircuit-instance/i-0e3bd89e87d1c2364`
  (a third confirmation the role is attached to this instance), and
  `aws iam list-role-policies` from that principal was **also** `AccessDenied`.

The policy is genuinely unreadable from the box by any principal available
without hunting a credential. An honestly unread policy is the right outcome
and it is what was recorded.

**Placement of the disclosure is correct.** The `## IAM` section states
"this block is still partly a plan, not a transcription of what is attached
today" in the paragraph **immediately preceding** the JSON — where a reader
meets it before the code block, not in a footnote — and repeats it after the
block under a bold "Could not independently re-read the live policy for this
pass." heading naming the denied calls. Nothing presents the transcription as
measured.

### The `identity/*` requirement and the simulator caveat are usable, not merely present

Both are stated **with the mechanism and the evidence**, which is the part that
would save a debugging session: that in the sandbox SES authorizes
`ses:SendEmail` against the *recipient's* identity ARN as well as the sender's,
so a policy naming only `identity/mailing.opencircuitsf.com` fails every send
to an address that is not itself a verified identity — which is every real
subscriber; and that a smoke test to `success@simulator.amazonses.com` cannot
catch it, because simulator addresses are not SES identities so no
recipient-resource check ever fires. `docs/deployment.md` states the reasoning
in its own words and points at `docs/aws-iam-setup.md`'s section for the
incident record rather than duplicating it.

**The `AccessDeniedException` history is intact and untouched.**
`docs/aws-iam-setup.md`'s "Why `identity/*` and not just the sending domain"
section — the verbatim `AccessDeniedException` on
`identity/brennan@opencircuitsf.com`, the "the way this was missed is worth
more than the fix" paragraph about the simulator smoke test, and the ~14
minutes of `#0126` queue retries that saved the mail — is byte-unchanged: the
commit's three hunks in that file land at "The short version", the facts
table, and step 5 of "After the role is attached", none of them near it. That
record is the evidence the transcribed policy rests on, and it survived.

### My own end-to-end sweep

I re-read both files myself rather than trusting the implementer's list, and
swept both with a `python3` byte scan for the whole false-premise family
(`no IAM role`, `no AWS`, `does not exist`, `never been created`, `us-west-2`,
`PLACEHOLDER`, `identity/opencircuitsf.com`, `None attached`, `Still blocked`).

The five occurrence-groups the work log names are all genuinely corrected, and
`docs/aws-iam-setup.md` is clean — nothing stale about the role remains in it.
`docs/deployment.md`'s residual hits are all either already corrected inline by
`#0418`/`#0421`/`#0426`, or properly framed as history (its `## SES setup`
preamble is explicitly past-tense — "recorded this as not started **when this
section was written**" — and already carries `#0421`'s "this plan was not what
actually happened" block).

**Three residuals I judge out of `#0426`'s scope but worth filing** (reported to
the orchestrator, not filed here, per `CLAUDE.md` §9 — see the report):

1. `docs/deployment.md` `## 10. First admin login`, closing note: "**Not run
   against a real deploy** — no SES account exists to send the recovery email
   through yet, and no live instance exists to receive the HTTPS request."
   Both halves are false and the second is flatly so. The implementer neither
   corrected nor mentioned this one. It is the same *shape* as the two
   "verified against" bullets it did correct, but its subject is the admin
   bootstrap step rather than the IAM role, and it is one of a **class**:
   `## 9. TLS` carries "**Not run anywhere** — this needs a real, publicly
   resolvable hostname pointed at a real box … there is nothing to run it
   against yet", and the orientation table near the top still reads
   "Currently served | the static placeholder". These are pre-deploy
   verification notes the 2026-08-25 deploy falsified wholesale, not
   IAM/SES-role drift, and correcting them one issue at a time is the wrong
   shape — they want one pass over the runbook.
2. `docs/deployment.md`'s backups limitations list carries a **second**
   instance of the premise `#0430` was filed for: "No S3. … none of those AWS
   resources exist yet … `PRD.md` §10.6 records it as an addable upload leg
   once the AWS account exists". `#0430` quotes only the adjacent `#0229`
   `## Decision` passage. Same subsystem, so it belongs to `#0430` — noting it
   so that pass corrects both, not one.
3. The new text asserts "the account remains sandboxed", sourced to
   `docs/aws-iam-setup.md` and `CLAUDE.md` §10 item 2. That is **not** measured
   — `aws sesv2 get-account` is denied to the box's identity (I confirmed:
   `AccessDeniedException` on `ses:GetAccount`), and production actually
   carries `SES_SANDBOX=false`. `#0415` already owns exactly this question and
   is cited in this issue's `## Relation`, so no new issue is needed; recording
   it here only so nobody later reads that clause as a measurement. It does not
   affect the policy: `identity/*` is correct either way.

### Scope calls I checked, and agree with

- Leaving `#0229`'s `## Decision` quote alone was **right** — it is an explicit
  quotation of a past decision about a different subsystem, and rewriting it
  would falsify the record `#0271`'s `## Scope` protects. Now tracked as
  `#0430`.
- Leaving `docs/aws-iam-setup.md`'s Parts 1–3 console walkthrough and "Why not
  static access keys" untouched was right — they are instructions for *how* the
  role was built, still accurate, not assertions that it is not built.
- The dated-note convention (`#0072`) is followed consistently: every
  correction strikes the old text rather than deleting it and names `#0426`
  and the date.

### Residual imprecision, not worth a bounce

`docs/deployment.md` now states that SES v2 `SendEmail` "authorizes the
configuration set as a separate resource, **not a condition key**". The first
half is sourced from `docs/aws-iam-setup.md`'s own account and is consistent
with the attached policy's two-ARN `Resource`; the "not a condition key" half
is an inference about AWS's IAM model that this pass did not verify against
AWS's service-authorization reference. It misleads nobody toward a wrong
action — the policy shown is the one documented as attached — but it is a
sentence stated more firmly than its evidence.

Also noted: the implementer's `## Verification` reports package-level `ok`
lines but names no individual test functions, which criterion 8 asks for. The
run itself was real and I reproduced it; the specific functions are named
below rather than bouncing a correct change over its own record.

### Verification I ran myself

`ISSUE=0426r scripts/check.sh go ./internal/db/... ./internal/handlers/...`
— `go build`, `go vet`, `gofmt -l` clean, then
`go test ./internal/db/... ./internal/handlers/... -p 2`. Zero skips; both
DB-backed packages reported real elapsed time, not `0.00s`. Run against its own
database (`opencircuit_test_0426r`), dropped by `check.sh` on exit. `uptime`
checked first and the machine was quiet (load ~3), so no timing result here is
load-confounded.

Ran once with this section present and the issue still `in-progress` — which is
the only state in which the two issue-prose guards scan this file, since both
admit `open`/`in-progress` only — and again after the `resolved` transition
below, which is the state the `**Closed**` row guard scans.

The specific guard functions that exercise the two edited documents and this
issue file, all of which passed:

- `internal/db`: `TestNoOpenIssueVerificationCitesUndefinedTestFunction`,
  `TestNoOpenIssueEvidenceCitesGoFileByLineNumber`,
  `TestResolvedOrClosedIssueCarriesClosedRow`,
  `TestPRDSectionIndexParity`, `TestPRDSectionIndexTotalMatchesFile`.
- `internal/handlers`: `TestNoCommentCitesUnresolvedPathOrSection` — the
  repo-wide guard that resolves every path cited from a Go comment, which is
  why both edited docs are in this run's scope at all — and
  `TestNoCommentCitesGoFileByLineNumber`.

Separately, outside `check.sh`: `python3 -m json.tool` over the `## IAM`
section's JSON block (extracted with a parser, not a line range) — parses
clean, and its region, account ID, identity wildcard and configuration-set ARN
match what I measured on the box. And a `python3` byte scan of both edited
files for the `<file>.go:<line>` shape `#0352` forbids — zero hits in either.
No BSD `grep -P` was used anywhere in this review.

### Verdict

**Approved.** Every acceptance criterion is met: the role is stated correctly
in all three places and no longer blamed for SES (1, 2), the policy's region,
account and identity are corrected with the `identity/*` requirement and the
simulator caveat both reasoned rather than asserted (3), the live policy's
unreadability is disclosed plainly and in the right place (4), the
maintained-vs-history split is stated and correct (5), nothing in AWS or on the
box was changed (6), no mechanism was added (7), and the scoped run is green
(8). The three residuals above are follow-up work, not defects in this change.
