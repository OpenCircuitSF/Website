# 0154 — The PRD index-parity guard silently skips index forms it cannot parse, and does not cover columns

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | All |
| **Module** | db / docs |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |
| **Commit** | `689a5de` |

## Description

`#0148` added `TestPRDWorkshopAndMailingIndexParity` (`internal/db/prd_index_parity_test.go`), which replays every `CREATE`/`DROP INDEX` across `migrations/*.up.sql` and asserts `PRD.md` §6.2 names each one correctly. It works, and `#0148`'s review re-proved it bites in both branches (missing index, diverged definition). Two gaps in it are worth closing on their own clock.

**1. Unmatched statements are skipped in silence.** The extraction regex is

```
(?i)CREATE\s+INDEX\s+(\w+)\s+ON\s+(\w+)\s*(\([^;]*?\))(?:\s+WHERE\s+([^;]*?))?;
```

Probed directly (`#0148` review), it handles more than its own comment claims — functional indexes (`lower(email)`), doubled parens, `INCLUDE`, and multi-line statements all match, because Go's leftmost-first submatch semantics expand the non-greedy column-list capture past a nested close-paren. But four real SQL forms produce **no match at all**, and a no-match on the migrations side means the index simply never enters the live map and is never checked:

| Form | Result |
|---|---|
| `CREATE UNIQUE INDEX …` | silently skipped |
| `CREATE INDEX IF NOT EXISTS …` | silently skipped |
| `CREATE INDEX CONCURRENTLY …` | silently skipped |
| statement with no terminating `;` | silently skipped |

None of these exist in `migrations/` today, so the guard is correct right now. The failure mode is the one that makes a guard worse than no guard: the first `CREATE UNIQUE INDEX` anyone adds is invisible to it, and the suite stays green while the PRD falls behind — exactly the drift `#0148` was filed about.

A related, lesser fragility: the patterns run over raw file text including SQL comments, so a commented-out `-- CREATE INDEX …` in a migration would register as a live index. That direction at least fails loudly (as a false "PRD does not mention"), so it is the smaller problem.

**2. Column parity was deferred, and the deferral's stated reason is weaker than it looks.** `#0148` scoped the guard to indexes, arguing a column would require "replaying a `CREATE TABLE` plus every later `ALTER TABLE ADD COLUMN` across up to 20 files with no single-statement anchor — materially more parsing surface". `#0148`'s review prototyped exactly that during the sweep: ~30 lines, comparing both column names and their type/default text, across all 20 migrations. It reproduced the one real column drift (`email_sends.claimed_at`, live since `000018`, absent from §6.2 until `#0148` fixed it by hand) and produced no false positives on the Phase 8 forward declarations. The extraction is not materially harder than the index case; the honest reason to defer was cost, not difficulty.

## Acceptance criteria

- [x] `TestPRDWorkshopAndMailingIndexParity` matches `CREATE UNIQUE INDEX`, `CREATE INDEX CONCURRENTLY`, and `CREATE INDEX IF NOT EXISTS`, in any combination
- [x] **The guard fails loudly on anything it cannot parse.** Count the `CREATE …INDEX` occurrences in each migration and in §6.2, compare against the number of statements the pattern actually matched, and `t.Errorf` on a mismatch naming the file. A silent skip must become a test failure, not a gap
- [x] SQL `--` comments are stripped before extraction on both sides, so a commented-out statement cannot register as live
- [x] A column-parity check in the same one direction (migrations → PRD §6.2): every column a migration creates on a `prdIndexParityTables` table appears in §6.2's `CREATE TABLE` for that table. Never the reverse — §6.2 legitimately leads with `subscribers.source`/`invited_at`/`consent_basis`/`import_id`/`soft_bounce_streak`/`last_bounce_at`/`last_delivery_at`, `email_campaigns.slug`/`archive_status`/`archived_at`, and the `subscriber_imports`/`subscriber_events`/`outbound_queue` tables, all owned by open Phase 8 issues `#0123`–`#0129`
- [x] Column *names* are the assertion; comparing type/default text is optional and should only ship if it is false-positive-free against the current tree
- [x] Deleting `email_sends.claimed_at` from `PRD.md` §6.2 in a scratch copy fails the new check, and restoring it passes — proved before the change is called done
- [x] `ISSUE=NNNN scripts/check.sh go ./internal/db/...` passes with all three doc-parity guards observed running

## Root cause

Two independent gaps in `internal/db/prd_index_parity_test.go`, both consequences of the same design choice: the guard is a hand-written regex, not a real SQL parser, and nothing checked that the regex's coverage matched what was actually in the file.

1. `createIndexPattern`'s prefix was `CREATE\s+INDEX\s+` only. `CREATE UNIQUE INDEX`, `CREATE INDEX CONCURRENTLY`, and `CREATE INDEX IF NOT EXISTS` all have extra tokens between `CREATE` and the index name that the pattern didn't account for, so none of them matched at all. A non-match on the migrations side of `liveIndexesFromMigrations` simply meant the statement contributed nothing to the `live` map — no error, no skip notice, just absence. Same failure shape for a statement missing its terminating `;`, since the pattern's own closing anchor is the semicolon.
2. Column parity was never built. `#0148` scoped the original guard to indexes only, reasoning (in the code comment) that column extraction would need "replaying a `CREATE TABLE` plus every later `ALTER TABLE ADD COLUMN` … with no single-statement anchor". `#0148`'s own review prototyped exactly that in ~30 lines and found it wasn't materially harder than the index case — the stated reason was cost, not difficulty — and it reproduced `email_sends.claimed_at`'s known drift mechanically.

## Fix

**Gap 1 (silent parse failures).**
- Widened `createIndexPattern`'s prefix to `CREATE\s+(?:UNIQUE\s+)?INDEX\s+(?:CONCURRENTLY\s+)?(?:IF\s+NOT\s+EXISTS\s+)?`, matching Postgres's required clause order. Capture group numbering is unchanged (the three new clauses are non-capturing), so no caller needed updating.
- Added `looseIndexStartPattern` (`CREATE\s+(?:UNIQUE\s+)?INDEX\b`) and `checkIndexStatementCoverage`, which counts loose occurrences against `createIndexPattern`'s actual match count and `t.Errorf`s on a mismatch, naming the file (or `PRD.md §6.2`). This is what makes a *remaining* unparseable form (a missing `;`, or any future form the prefix doesn't yet handle) fail loudly instead of vanishing. Wired into `liveIndexesFromMigrations` (once per migration file) and into `TestPRDWorkshopAndMailingIndexParity` (once for the PRD section).
- Added `stripSQLComments` (`--[^\n]*` stripped) and applied it to both the migration file text and the extracted PRD section before any pattern runs, so a commented-out `-- CREATE INDEX …`/`-- CREATE TABLE …` can never register as live on either side, and so the loose/strict counts in `checkIndexStatementCoverage` are counting the same text.

**Gap 2 (column parity).**
- Added `columnsFromCreateTable`, which locates a `CREATE TABLE name (` body by depth-tracking to the matching close paren (handles nested parens from `REFERENCES t(id)` etc. — something a `createIndexPattern`-style non-greedy regex can't safely do for a column list), splits the body on top-level commas with `splitTopLevel`, and takes each segment's first token as the column name unless it's a table-constraint keyword (`PRIMARY`, `UNIQUE`, `CHECK`, `FOREIGN`, `CONSTRAINT`, `EXCLUDE`).
- Added `liveColumnsFromMigrations`, which replays `CREATE TABLE` and `ALTER TABLE … ADD COLUMN` across `migrations/*.up.sql` in migration-version order and by byte position within each file — the same ordering discipline `liveIndexesFromMigrations` already uses for `idx_email_events_soft_bounce`'s `DROP`+`CREATE` pair, preserved here even though every column change in this tree today is a monotonic addition, so a future `DROP COLUMN` or table recreation doesn't silently get the wrong answer.
- Added `prdColumnsFromSection`, the PRD-side counterpart using the same `columnsFromCreateTable`.
- Added `TestPRDWorkshopAndMailingColumnParity`: one direction only (migrations → PRD §6.2), scoped to `prdIndexParityTables` (the same table set the index guard already uses), asserting column *names* only — not types/defaults, per the acceptance criteria's "optional, ship only if false-positive-free" and to keep the assertion to one axis.

Did not change the guard's one-directional scope, per the issue's explicit instruction — no bidirectional check, no allowlist.

## Verification

`uptime` before starting: `20:05  up 2 days, load averages: 1.90 1.76 1.75` — well under CLAUDE.md §5's ~8 threshold throughout.

`go build ./internal/db/...` and `go vet ./internal/db/...` — clean.

Baseline (no false positives), `go test ./internal/db/... -run 'TestPRDWorkshopAndMailingIndexParity|TestPRDWorkshopAndMailingColumnParity|TestDatabaseDocMigrationParity' -v`: all three doc-parity guards `PASS`, including the five real Phase 8 PRD-only indexes (`idx_email_events_recipient_time`, `idx_outbound_queue_due`, and `subscriber_events`'s three) and every Phase 8 forward-declared column correctly unflagged.

Three failure modes proved on scratch copies, one at a time, restored byte-identically after each:

1. **`CREATE UNIQUE INDEX` the PRD does not mention** — added `migrations/999999_scratch_unique_index.up.sql` with `CREATE UNIQUE INDEX idx_scratch_interests_slug_unique ON interests (slug);`. Result:
   ```
   PRD.md §6.2 does not mention index "idx_scratch_interests_slug_unique" (table interests), defined in
   migrations/999999_scratch_unique_index.up.sql — PRD.md's schema listing has fallen behind the migration.
   Add or correct the CREATE INDEX line in PRD.md §6.2 — see #0148.
   ```
   Proves the form is now parsed and enters the live map (previously silently skipped, zero failures). Also proved `CREATE INDEX IF NOT EXISTS` and `CREATE INDEX CONCURRENTLY` the same way (bonus, not a separate acceptance item) — both correctly flagged as "does not mention". File removed after each run; `git status --porcelain -- migrations/` empty.

2. **Unparseable statement (missing terminating `;`)** — added `migrations/999998_scratch_no_semicolon.up.sql` with `CREATE INDEX idx_scratch_no_semicolon ON interests (name)` (no `;`, end of file). Result:
   ```
   migrations/999998_scratch_no_semicolon.up.sql: found 1 "CREATE [UNIQUE] INDEX" occurrence(s) but only parsed 0 as a
   complete statement — one of them uses a SQL form createIndexPattern can't parse (check for a missing terminating
   ';', or a form not yet handled). A silent skip here means the index is invisible to this guard — fix the
   statement or widen createIndexPattern, don't leave the mismatch. See #0154.
   ```
   Proves `checkIndexStatementCoverage` catches what the widened prefix still can't parse, instead of the previous zero-failures silence. File removed after; tree clean.

3. **Deleted `email_sends.claimed_at`** — copied `PRD.md` aside, removed the `claimed_at` column line and its wrapped comment (lines 557–562) from the real `PRD.md` (the test hardcodes `prdPath = "../../PRD.md"`, so no way to point it at a copy). Result:
   ```
   PRD.md §6.2's CREATE TABLE email_sends does not mention column "claimed_at", added by
   migrations/000018_add_campaign_send_state.up.sql — PRD.md's schema listing has fallen behind the migration.
   Add the column to PRD.md §6.2's CREATE TABLE email_sends — see #0154.
   ```
   Restored from the copy; `diff` against the pre-edit backup and `git diff --stat -- PRD.md` both empty (byte-identical). Re-ran both guards afterward: `PASS`.

Final scoped check: `ISSUE=0154 scripts/check.sh go ./internal/db/...` — `go build`/`go vet` clean, `internal/db` `ok` in 0.224s, zero skips, `VERIFICATION PASSED`. Broader check: `ISSUE=0154 scripts/check.sh go ./internal/...` — all 15 packages `ok` with real non-zero elapsed times (`handlers` 2.352s, `interests` 1.970s, `mailing` 0.846s, …), only `internal/testdb` reporting `[no test files]` (genuinely has none), zero other skips, `VERIFICATION PASSED`. No leftover processes (`pgrep -f 'go test|\.test '` empty) and no leftover scratch database.

## Files changed

- `internal/db/prd_index_parity_test.go` — widened `createIndexPattern`'s prefix; added `looseIndexStartPattern`, `sqlCommentPattern`, `stripSQLComments`, `checkIndexStatementCoverage`; wired comment-stripping and coverage-checking into `liveIndexesFromMigrations` and `TestPRDWorkshopAndMailingIndexParity`; added `columnDef`, `tableConstraintKeywords`, `createTableStart`, `columnAddPattern`, `splitTopLevel`, `columnsFromCreateTable`, `liveColumnsFromMigrations`, `prdColumnsFromSection`, and `TestPRDWorkshopAndMailingColumnParity`; updated the package-level comment to describe both fixes. Commit `689a5de`.

## Gotchas

- `prdPath` is hardcoded to the real `../../PRD.md`, not injectable, so proving the column-deletion failure mode required editing the actual tracked file in place (copied aside first per CLAUDE.md §8a) rather than a throwaway scratch copy. Restored and diffed byte-identical before moving on.
- Scratch migration files (`999999_…`, `999998_…`, `999997_…`) break `TestDatabaseDocMigrationParity`'s contiguity/down-file checks if run through the whole `internal/db` package — each proof was run scoped to just the affected test (`-run 'TestPRDWorkshopAndMailing…'`) and the scratch file deleted before running anything broader, so the full-package and full-suite verification runs never saw them.
- `looseIndexStartPattern` intentionally does not try to parse `CONCURRENTLY`/`IF NOT EXISTS` — it only needs to count "a CREATE [UNIQUE] INDEX statement started here", and letting the strict pattern be the one that determines whether it actually completed is what makes the coverage check meaningful (a widened strict pattern narrows the gap; the loose count is the fixed ceiling).

## Notes

Filed by `#0148`'s phase-3 review. `#0148` itself is sound — the drift it fixed is fixed, its sweep was complete (independently re-run at column, type, and index level: zero remaining drift), and its guard is correctly one-directional. These are hardening items, deliberately not a bounce.

The one-directional scope is the part **not** to change. A bidirectional guard would fire today on five PRD-only indexes (`idx_email_events_recipient_time`, `idx_outbound_queue_due`, and `subscriber_events`' three), every one of them genuine Phase 8 design, and would need an allowlist that itself drifts.

## Relation

- Found by: [#0148](0148.md) — PRD §6.2's workshops index is two revisions behind the migration
- Relates to: [#0123](0123.md), [#0124](0124.md), [#0125](0125.md), [#0126](0126.md) — the Phase 8 issues that own §6.2's forward-declared schema

---

*Filed as `#0154`, not `#0152`. `#0148`'s reviewer wrote this file as `issues/0152.md` at the same moment the orchestrator filed a different issue under that number, and the orchestrator's commit overwrote it. Recovered verbatim from `db10ab7` and renumbered; everything above is the reviewer's text, unedited apart from the heading and a missing `Platform` row. `#0152` is the `signup_url` boundary-validation issue.*

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 90 | 5,456 | 4,556,594 | 114,242 | $1.87 |

**Total: $1.87**
