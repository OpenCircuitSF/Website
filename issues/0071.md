# 0071 — Enforce the Go/TypeScript route-table parity with a test

| | |
|---|---|
| **Status** | resolved |
| **Module** | web |
| **Platform** | All |
| **Phase** | 2 — Brand and marketing pages |
| **First seen** | 2026-08-18 |
| **Commit** | dfc26b3, bdfedc7 |
| **Closed** | 2026-08-18 |

## Description

`#0022` gave the Go SPA handler a route table so an unrecognized deep link
returns HTTP 404 instead of an indexable soft-404. That table,
`internal/handlers/routes.go`'s `knownStaticRoutes` plus
`workshopDetailPattern`, is a hand-typed mirror of `web/src/lib/router.ts`'s
`STATIC_ROUTES` and `WORKSHOP_DETAIL`. Nothing keeps the two honest.

`routes.go`'s own doc comment says the tables "stay in sync by construction
rather than by convention". That is true of `#0019` reusing the *Go* table from
Go, and false of the Go-to-TypeScript relationship, which is the one that
actually decides whether a page ships with the right status code. Two files in
two languages cannot be in sync by construction unless one is generated from
the other or a test fails when they diverge. Neither is the case.

**Proven at `#0022`'s review, 2026-08-18**, in a throwaway worktree, in both
directions:

- added `/privacy` to `STATIC_ROUTES` only → `npm run check` (187 files, 0
  errors), `npm test` (103 passed), `go build ./...` and `go test ./...` all
  green;
- added `/sponsors` to `knownStaticRoutes` only → same, all green.

The two tables happen to agree today. Nothing will tell anyone when they stop.

> 🟢 Resolved 2026-08-18 — `routes_parity_test.go` fails `go test ./...` in both
directions when `STATIC_ROUTES` and `knownStaticRoutes` diverge, and after the
bounce fix it `t.Fatal`s on any line inside the `STATIC_ROUTES` literal it cannot
parse, so a one-sided route can no longer hide behind a syntax the parser does not
recognize.

## Fix

Chose Approach 1 from the issue's own recommendation: a Go test
(`internal/handlers/routes_parity_test.go`) that parses
`web/src/lib/router.ts` directly and compares it against `routes.go`'s
tables. It rides `go test ./...`, which every backend verification pass
already runs, and needs no new build step, generator, or shared manifest —
the project has deliberately avoided adding a build step so far (per the
issue's own Approach 2 tradeoff), and a JSON manifest (Approach 3) would
touch the Vite build and the embed for a guarantee this test already
provides at far lower cost.

Two tests:

- `TestRouteTableParity_StaticRoutes` extracts the `STATIC_ROUTES` object
  literal's keys (regex over the block between `const STATIC_ROUTES: ... = {`
  and the closing `};`, then a per-line, anchored entry regex for each
  `'path': 'Name'`) and diffs the resulting set against `knownStaticRoutes` in
  both directions. **Revised at review (bounce), 2026-08-18**: the original
  version scanned the block for entry-shaped substrings and only errored when
  it found zero entries, so a single line the regex didn't recognize was
  silently skipped rather than failing — a missing route on the TS side then
  looked exactly like agreement. Reproduced clean-`PASS`-while-diverged twice:
  a `"/privacy": "privacy" as RouteName,` entry in double quotes among
  single-quoted neighbours, and a route composed in via `...EXTRA_ROUTES,`
  spread. The parser now walks the block line by line and `t.Fatal`s
  (`routerTSParseError`, "could not parse STATIC_ROUTES entry: `<line>`") on
  any line that is not blank, not a `//` comment, not a bare brace, and that
  the (now-anchored) entry regex doesn't match in full — closing both
  reproductions and any future syntax the parser doesn't understand, because
  unrecognized now always means "fix the parser," never "nothing here."
- `TestRouteTableParity_WorkshopDetailPattern` extracts the source between
  `WORKSHOP_DETAIL`'s `= /` and the line-final `/;`, compiles it as a Go
  `regexp` (JS and Go RE2 agree closely enough for this specific pattern —
  character class, one capture group, anchors, and a backslash-escaped `/`
  that Go accepts unescaped just as well — verified by hand), and runs both
  the TS-sourced pattern and `workshopDetailPattern` against a shared table
  of paths, failing on any path where the two disagree.

Parse failures (`routerTSParseError`) are raised as `t.Fatal`, distinct from
parity failures (`t.Errorf` naming the offending path and side), so a
reformat of `router.ts` that breaks the narrow regex reads as "fix the
parser" rather than "the tables disagree" — called out explicitly in the
issue's Approach section.

Also corrected `routes.go`'s doc comment: it previously claimed the
Go-to-TypeScript relationship was "in sync by construction", which was never
true — only the Go-to-Go reuse (`#0019` reusing this table from Go) earns
that phrase. The comment now says the TypeScript mirror is instead kept
honest by `routes_parity_test.go` (this issue).

### Dynamic pattern: in scope, but normalization is hand-mirrored, not parsed

The trailing-slash normalization (`isKnownRoute` in `routes.go`,
`parsePath` in `router.ts`: strip one trailing slash on a path longer than
`"/"`) is **not** re-derived from source. It is a two-line rule already
independently documented and independently tested on each side
(`TestIsKnownRoute_TrailingSlash` here; `router.test.ts`'s equivalent on the
TS side). Re-parsing it would add another format-sensitive regex for a rule
that already has its own coverage on both sides. Instead,
`normalizeTrailingSlashForParity` reimplements that exact two-line rule once
in the test, and the test applies it identically to both the TS-sourced and
Go patterns before matching — so what's actually verified is regex
equivalence (the part with no other cross-check), while the normalization
step rides the existing per-side tests. This is a deliberate, disclosed
scope decision, not an oversight. **Confirmed at review**: AC 2's
normalization clause is therefore met by delegation to those per-side tests,
not by this guard itself — applying one mirrored rule identically to both
inputs cannot make the regex comparison lie, but it also cannot catch a
coordinated change to the rule on both sides at once. See `## Gotchas`.

## Verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
go build ./... 2>&1 | tail -20      # clean
go vet ./...   2>&1 | tail -20      # clean
go test ./... -count=1 2>&1 | tail -40
```
All packages `ok`, including the DB-backed ones (`internal/audit`,
`internal/auth`, `internal/db`, `internal/handlers`, `internal/middleware`)
with real elapsed times, not `[no test files]`. New tests confirmed by name:
`go test ./internal/handlers/... -run TestRouteTableParity -v -count=1` →
`--- PASS: TestRouteTableParity_StaticRoutes`,
`--- PASS: TestRouteTableParity_WorkshopDetailPattern`.

```
cd web && npm run check && npm test
```
`npm run check` — 187 files, 0 errors, 0 warnings. `npm test` — 7 test files,
103 tests passed (router.ts untouched by this change, so the count matches
the pre-existing baseline).

**Re-verified at the parser-strictness fix (bounce re-implementation),
2026-08-18**, in a fresh throwaway `/tmp` worktree
(`/tmp/oc71-mutate2`, `git worktree add --detach HEAD`, removed afterward with
`git worktree remove --force`; the updated `routes_parity_test.go` was copied
in by hand since worktree creation doesn't carry uncommitted changes). All
four cases the bounce asked for, plus the benign edits and the five
parse-failure cases, re-run against the fixed parser:

1. **Route added to `STATIC_ROUTES` only, existing single-quoted style**
   (`'/privacy': 'privacy',`) — still FAILS, naming `/privacy` and the file to
   edit:

   ```
   === RUN   TestRouteTableParity_StaticRoutes
       routes_parity_test.go:178: route table parity: 1 path(s) present in router.ts's STATIC_ROUTES but missing from routes.go's knownStaticRoutes: /privacy
           add the missing entry/entries to internal/handlers/routes.go's knownStaticRoutes
   --- FAIL: TestRouteTableParity_StaticRoutes (0.00s)
   === RUN   TestRouteTableParity_WorkshopDetailPattern
   --- PASS: TestRouteTableParity_WorkshopDetailPattern (0.00s)
   FAIL
   ```

2. **Route added to `knownStaticRoutes` only** (`"/sponsors": true,`) — still
   FAILS, naming `/sponsors` and the other file:

   ```
   === RUN   TestRouteTableParity_StaticRoutes
       routes_parity_test.go:185: route table parity: 1 path(s) present in routes.go's knownStaticRoutes but missing from router.ts's STATIC_ROUTES: /sponsors
           add the missing entry/entries to web/src/lib/router.ts's STATIC_ROUTES
   --- FAIL: TestRouteTableParity_StaticRoutes (0.00s)
   === RUN   TestRouteTableParity_WorkshopDetailPattern
   --- PASS: TestRouteTableParity_WorkshopDetailPattern (0.00s)
   FAIL
   ```

3. **The bounce case — mixed quote style.** `"/privacy": "privacy" as
   RouteName,` added to `STATIC_ROUTES` only, every other entry left
   single-quoted. Previously a silent `PASS`; now correctly FAILS, as a parse
   error naming the exact offending line:

   ```
   === RUN   TestRouteTableParity_StaticRoutes
       routes_parity_test.go:160: could not parse STATIC_ROUTES entry: `"/privacy": "privacy" as RouteName,` from ../../web/src/lib/router.ts
   --- FAIL: TestRouteTableParity_StaticRoutes (0.00s)
   === RUN   TestRouteTableParity_WorkshopDetailPattern
   --- PASS: TestRouteTableParity_WorkshopDetailPattern (0.00s)
   FAIL
   ```

4. **The bounce case — spread composition.** A second `EXTRA_ROUTES` object
   holding `'/privacy': 'privacy',`, spread into `STATIC_ROUTES` via
   `...EXTRA_ROUTES,`. Previously a silent `PASS`; now correctly FAILS:

   ```
   === RUN   TestRouteTableParity_StaticRoutes
       routes_parity_test.go:160: could not parse STATIC_ROUTES entry: `...EXTRA_ROUTES,` from ../../web/src/lib/router.ts
   --- FAIL: TestRouteTableParity_StaticRoutes (0.00s)
   === RUN   TestRouteTableParity_WorkshopDetailPattern
   --- PASS: TestRouteTableParity_WorkshopDetailPattern (0.00s)
   FAIL
   ```

   All four reverted from a saved pre-edit copy of `router.ts`
   (`routes.go` similarly saved/restored for case 2) between mutations;
   re-ran → both tests pass again after every revert.

**Benign edits still pass, unregressed:**

- Dropping the trailing comma on `STATIC_ROUTES`'s last entry (`'/admin':
  'admin'` with no trailing `,`) — `--- PASS` both tests.
- Inserting a `//` comment line inside the literal (between `'/account':
  'account',` and `'/admin': 'admin',`) — `--- PASS` both tests.

**The five parse-failure cases stay `t.Fatal`, distinguishable from a parity
failure, unregressed:**

| Mutation | Result |
|---|---|
| `STATIC_ROUTES` literal collapsed onto one line | `could not parse the STATIC_ROUTES literal from ../../web/src/lib/router.ts` |
| Every entry switched to double quotes | could not parse STATIC_ROUTES entry: `"/": "home",` (first unrecognized line — previously "could not parse any STATIC_ROUTES entries"; still a could-not-parse message, still distinct from a parity message) |
| Constant renamed to `STATIC_ROUTE_TABLE` | `could not parse the STATIC_ROUTES literal from ../../web/src/lib/router.ts` |
| Declaration rewritten with a `satisfies Readonly<Record<string, RouteName>>` clause | `could not parse the STATIC_ROUTES literal from ../../web/src/lib/router.ts` |
| `WORKSHOP_DETAIL` rewritten as `new RegExp('^\\/workshops\\/([^/]+)$')` | `could not parse the WORKSHOP_DETAIL regex literal from ../../web/src/lib/router.ts` |

**Independently re-verified at phase 3 re-review, 2026-08-18** (throwaway
worktree `/tmp/oc71-rr2`, `git worktree add --detach HEAD`, removed afterward;
main tree never edited). Reproduced from scratch, not read off the claims
above: both bounce cases now `t.Fatal` with "could not parse STATIC_ROUTES
entry" naming the exact offending line — the double-quoted
`"/privacy": "privacy" as RouteName,` and the `...EXTRA_ROUTES,` spread
respectively; both parity directions still fail with the
path-and-side message and go green again on revert; the two benign edits
(dropped trailing comma, `//` comment line inside the literal) and a blank
line inside the literal all still `PASS`; and all five parse-failure cases
still read `could not parse ...`, never a parity message — including the
all-double-quotes case, whose message is now "could not parse STATIC_ROUTES
entry: `"/": "home",`" (the first unrecognized line) and is still
unmistakably a parse failure. A seventeen-construct slip battery was also run
against the fixed parser — see `## Gotchas`.

Worktree removed with `git worktree remove --force /tmp/oc71-mutate2`
afterward; main tree was never edited, and `git status --short` before and
after mutation testing shows only this issue's own two files
(`internal/handlers/routes_parity_test.go`, `issues/0071.md`) plus the
pre-existing, untouched `web/dist/index.html`.

## Files changed

- `internal/handlers/routes_parity_test.go` — from the original
  implementation (`dfc26b3`): new file, two tests
  (`TestRouteTableParity_StaticRoutes`,
  `TestRouteTableParity_WorkshopDetailPattern`) plus the narrow TS-source
  parsers they use (`parseStaticRoutesFromRouterTS`,
  `parseWorkshopDetailPatternFromRouterTS`) and the mirrored
  trailing-slash-normalization helper. **From this pass (bounce
  re-implementation)**: `parseStaticRoutesFromRouterTS` rewritten to walk the
  `STATIC_ROUTES` block line by line and `t.Fatal` (via `routerTSParseError`)
  on any non-blank, non-comment, non-brace line the entry regex doesn't match
  in full, instead of scanning the block for entry-shaped substrings and
  erroring only on zero matches; `staticRoutesEntryRe` anchored (`^...$`) to
  match a whole trimmed line rather than a substring; new `isBraceOnlyLine`
  helper.
- `internal/handlers/routes.go` — corrected the doc comment above
  `knownStaticRoutes` that falsely claimed the Go-to-TypeScript relationship
  was in sync "by construction"; it now names `routes_parity_test.go` as
  what actually enforces that. (From `dfc26b3`; unchanged this pass.)

## Gotchas

- **Why parse-from-Go over the other two shapes.** A generated file
  (Approach 2) would be the strongest guarantee and would actually earn the
  original doc comment's wording, but it adds a `go:generate` build step this
  project has so far avoided, for a codebase where the route tables change
  rarely. A shared JSON manifest (Approach 3) is cleanest in principle but
  has the most churn — it touches `router.ts`, `routes.go`, the Vite build,
  and the embed, none of which exist today for this purpose. Parsing
  `router.ts` from a Go test needs none of that and rides a verification
  command every backend change already runs.
- **The parser is deliberately narrow and format-sensitive**, per the issue's
  own warning. It anchors on `const STATIC_ROUTES: ... = {` (the type
  annotation, unlikely to be reformatted away) and on `const WORKSHOP_DETAIL
  = /.../ ;` as a single line with no regex flags. A reformat of either
  declaration would fail parsing with a distinct `t.Fatal` message
  ("could not parse ... from ...") rather than a silent false pass or a
  parity-style `t.Errorf`, so the two failure modes cannot be confused.
- **Fixed at review (bounce): a scan-and-skip parse can be silently green
  while the tables genuinely diverge.** The original `parseStaticRoutesFromRouterTS`
  scanned the whole `STATIC_ROUTES` block for entry-shaped substrings and only
  errored when it found *zero* — so a single line the entry regex didn't
  recognize was skipped rather than failing, and the resulting missing route
  looked exactly like agreement. The reviewer demonstrated two clean `PASS`
  runs with the tables actually diverged: a `"/privacy": "privacy" as
  RouteName,` entry added in double quotes among single-quoted neighbours,
  and a route composed in via `...EXTRA_ROUTES,` spread. The fix parses line
  by line and `t.Fatal`s on any line inside the block that is not blank, not
  a `//` comment, not a bare brace, and that `staticRoutesEntryRe` (now
  anchored, `^'...'...'...,?$`) doesn't match in full — so the failure mode is
  always "I don't understand this line" rather than "I found nothing here".
  Both reproductions above, plus the five pre-existing parse-failure cases
  (collapsed literal, all-double-quotes, renamed constant, `satisfies` clause,
  `new RegExp(...)`) and the two benign edits (dropped trailing comma, a `//`
  comment line inside the literal), were re-verified against this version —
  see `## Verification`.
- **No JS-to-Go regex translation needed for the current pattern.** Verified
  by hand (`regexp.Compile` in Go accepts `^\/workshops\/([^/]+)$` — the
  backslash-escaped slash — identically to the unescaped form) rather than
  writing a general JS-regex-to-RE2 translator, which would be its own
  source of bugs for a one-pattern use case.
- **The dynamic-pattern comparison is example-based, not proof of regex
  equivalence.** `TestRouteTableParity_WorkshopDetailPattern` only catches a
  divergence its eleven sample paths happen to expose. The reviewer showed
  this concretely: narrowing the TS pattern to `/^\/workshops\/([a-z0-9-]+)$/`
  — a real behavioural divergence for any slug with an uppercase letter,
  underscore, or dot — passes clean, while narrowing to `([^/]{3,})` is
  caught. This is an accepted, disclosed limitation of comparing regexes by
  sampling rather than translation, and it must not be described as full
  pattern equivalence.
- **Trailing-slash normalization is intentionally out of the parsed scope**
  (see `## Fix`) — it's asserted by mirroring, not by parsing, because it
  already has independent test coverage on both sides and re-parsing it would
  only add another format-sensitive regex without closing a real gap.
  Reworded per review: **AC 2's normalization clause is met by delegation,
  not by this test.** `normalizeTrailingSlashForParity` produces test
  *inputs*, not assertions, and is applied identically to both the TS-sourced
  and Go patterns before matching, so it cannot make the comparison lie — the
  regex comparison itself is still real. But nothing here cross-checks the
  normalization *rule* across languages: a coordinated change that updated
  each side's own independent test (`TestIsKnownRoute_TrailingSlash` here,
  `router.test.ts`'s equivalent) to a different rule in lockstep would pass
  everything this test asserts. That residual is accepted, but it is "by
  convention" — the very thing this issue exists to eliminate elsewhere — so
  it is recorded here rather than implied to be covered.
- **Re-review slip battery, 2026-08-18: the parser is structural *inside* the
  literal; the two remaining holes are outside it.** Seventeen constructs were
  tried against the fixed parser, each adding `/privacy` to `router.ts` alone
  and leaving `routes.go` untouched: the two original bounce cases; a computed
  key (`['/privacy']:`); a template-literal key; a backslash-escaped
  (`'\/privacy'`) and a `'\u002Fprivacy'` key; key and value split across two
  lines; a backslash line-continuation inside the key string; a U+2028 line
  separator; a lone CR separator; two entries on one line; a nested `...{ }`
  spread; a `/* */` block-comment line; an entry whose value is an expression
  (`PRIVACY_ROUTES['/privacy']`); and the whole declaration indented into a
  nested scope. **All fifteen that put the route inside the literal failed
  loudly** — thirteen as `could not parse STATIC_ROUTES entry: <line>` or
  `could not parse the STATIC_ROUTES literal`, and the two escaped-key forms as
  an ordinary parity failure naming the mis-read path (`\/privacy`,
  `\u002Fprivacy`). **Two slipped, and both define the route outside the
  `STATIC_ROUTES` literal**: `Object.assign(STATIC_ROUTES, { '/privacy':
  'privacy' });` after the closing `};`, and an `if (path === '/privacy')
  return {...}` special case in `parsePath` that never touches the table at
  all. Both pass `svelte-check` (187 files, 0 errors) *and* both parity tests.
  This is a **scope boundary, not an enumeration gap in the parser**: the guard
  compares two named tables, so a route defined outside the table is invisible
  to it — and would be equally invisible to Approach 2's generated file and
  Approach 3's shared manifest. Nothing short of executing `parsePath` itself
  closes it. Accepted and recorded; the convention it rests on is "routes live
  in `STATIC_ROUTES`", which is this file's entire design.
- **The one innocuous edit the guard rejects: a trailing same-line comment.**
  `'/admin': 'admin', // #0021` does not match the anchored entry regex, so it
  `t.Fatal`s with `could not parse STATIC_ROUTES entry:` naming that exact
  line. That is the deliberate price of "any unrecognized line is a parse
  failure" — it errs loud rather than silent, and the message points straight
  at the culprit — but it is worth knowing before someone assumes the guard is
  broken. If it ever annoys anyone, widen `staticRoutesEntryRe`'s tail to allow
  an optional `\s*//.*` suffix; do **not** reintroduce a skip. A `//` comment
  on its own line, a blank line, and a dropped trailing comma on the last entry
  all still pass.
- **`web/dist/index.html`** shows as modified in `git status` throughout this
  work; it was not touched by this issue (no `npm run build` was run) — it
  predates this session's work, belongs to other in-flight work, and was left
  alone.

## Why this is not theoretical

The very next queued issue reproduces the failure. `#0070` (privacy policy
page) adds `/privacy` to `router.ts`. If that pass does not also edit
`routes.go`, `/privacy` ships as a real, fully rendered page that the server
answers with **HTTP 404** — deindexed, and reported as broken by every link
checker, while looking perfectly fine to anyone who clicks it. The reverse
drift is just as bad: a path known only to Go returns 200 and renders the
client-side 404 view, which is precisely the soft-404 `#0022` exists to
prevent.

The blast radius grows from here. `#0019` (per-route meta tags) and `#0020`
(sitemap) are both specified to build on this table, so a single silent
divergence ships wrong meta tags and a wrong sitemap at the same time.
`#0053`/`#0054` add the workshop routes on top of it.

## Acceptance criteria

- [ ] A test fails when `web/src/lib/router.ts`'s `STATIC_ROUTES` and
      `internal/handlers/routes.go`'s `knownStaticRoutes` disagree in either
      direction — a path present in one and absent from the other
- [ ] The dynamic pattern is covered too: `WORKSHOP_DETAIL` and
      `workshopDetailPattern` must accept and reject the same paths, on inputs
      normalized for the trailing slash both sides implement independently.
      The normalization *rule itself* is not cross-checked by this test —
      that is met by delegation to each side's own independent test
      (`TestIsKnownRoute_TrailingSlash` here, `router.test.ts`'s equivalent),
      not by this guard, since applying an identical mirrored rule to both
      inputs here cannot detect a coordinated one-rule-in-two-places drift
- [ ] The failure message names the offending path and which side is missing
      it, so the fix is obvious without reading either file
- [ ] The check runs as part of an existing verification command
      (`go test ./...` or `npm test`) — not a separate script someone has to
      remember
- [ ] Correct the false claim in `routes.go`'s doc comment: reusing the Go
      table from `#0019` is Go-to-Go reuse and genuinely is by construction;
      the TypeScript mirror is not, and after this issue it is enforced by
      *this test*, which is what the comment should say
- [ ] Mutation-verify it: delete one entry from each table in turn and confirm
      the new test fails both times — a parity test that cannot fail is worse
      than none, because it advertises a guarantee it does not provide

## Approach — pick one, do not gold-plate

Either direction is acceptable; the point is that divergence becomes loud.

1. **Assert parity from Go.** A `routes_test.go` case that parses
   `../../web/src/lib/router.ts` for the `STATIC_ROUTES` literal and compares
   the key set against `knownStaticRoutes`. Cheapest to write and it rides
   `go test ./...`, which every backend verification already runs. The cost is
   a regex over a source file, which breaks if the literal is reformatted —
   keep the parse narrow and make the failure message say "could not parse
   STATIC_ROUTES" distinctly from "tables disagree" so a reformat is not
   mistaken for a drift.
2. **Generate one from the other.** Emit a small `routes_gen.go` from
   `router.ts` via `go:generate`, and have the test assert the checked-in file
   matches what the generator produces. Genuinely by construction, and it earns
   the doc comment's original wording — but it adds a build step, which this
   project has so far avoided.
3. **A shared JSON manifest** both sides import. Cleanest in principle, most
   churn: it touches `router.ts`, `routes.go`, the Vite build and the embed.

Option 1 is the recommended default. Whichever is chosen, the criterion that
matters is that a one-sided edit turns something red.

## Notes

Filed from `#0022`'s phase 3 review, 2026-08-18. `#0022` itself was approved —
its own acceptance criteria are met, the 404 behaviour is correct and
mutation-proven, and the two tables do agree as shipped. What is filed here is
the missing guard and the corrected claim, not a defect in the delivered
behaviour.

This landed before `#0070`, so it catches `/privacy` automatically. **Confirmed
at the phase 3 re-review**: `#0070` can rely on this guard *unconditionally* to
catch a missing `routes.go` entry, whatever quoting or formatting the new
`STATIC_ROUTES` entry uses — either the route is written as a recognizable entry
and the parity check names it, or the line is unrecognizable and the parse
`t.Fatal`s. The one caveat is not about formatting at all: the route has to go
*into* `STATIC_ROUTES`. A `/privacy` special-cased inside `parsePath`, or spliced
onto the table after the fact, is outside what any table-comparison guard can
see (see `## Gotchas`).

## Relation

- Specified by: [`PRD.md`](../PRD.md) §5.1 — Public routes · 16 lines
  `sed -n '/^### 5\.1 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0014](0014.md) — Add a History API path router
- Depends on: [#0022](0022.md) — Build the 404 Not Found view
- Blocks: [#0019](0019.md) — Inject per-route meta tags server-side
- Blocks: [#0020](0020.md) — Serve sitemap.xml and robots.txt
- Related: [#0070](0070.md) — Build the privacy policy page (adds `/privacy` to `router.ts`)
- Related: [#0053](0053.md) — Build the public workshops index
- Related: [#0054](0054.md) — Build the workshop detail view

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-08-18 | implement | claude-sonnet-5 | 80,086 | — |
| 2026-08-18 | review (bounced) | claude-opus-5 | 72,880 | — |
| 2026-08-18 | implement (2nd) | claude-sonnet-5 | 87,252 | — |
| 2026-08-18 | review | claude-opus-5 | 94,477 | — |

One bounce: the guard against silent divergence could itself be diverged past
silently, because its parser skipped lines it did not recognise and errored only
on zero matches. Harness reports aggregate subagent totals only, so cost is left
uncomputed rather than estimated.
