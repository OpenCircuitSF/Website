# 0396 — The per-row CRT width warning is not announced, and the reason it was omitted is guard avoidance

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Web |
| **Module** | web |
| **Phase** | 9 — Reusable as a dependency |
| **First seen** | 2026-09-03 |
| **Commit** | `e52c13a`, `8991a38` |
| **Closed** | 2026-09-03 |

## Description

`#0393` added a per-line over-budget warning to the CRT commands admin section
(`web/src/views/admin/CrtCommands.svelte`): type an output line longer than
`CRT_LINE_CHARS` and the editor says so.

It appears in two places. The **create form**'s warning carries `role="status"`
and is announced. The **per-row inline editor**'s warning is the same message,
the same wording and the same trigger — and is silent to a screen reader.

The omission was deliberate, and the recorded reason is the problem: the
implementer left `role="status"` off because the editor sits behind
`{#if editing[c.id]}` and adding it would have required a new named entry in
`web/src/lib/liveRegionGuard.structuralGuard.test.ts`'s
`KNOWN_STABLE_BRANCH_SITES`. **That is shaping markup to avoid tripping a
guard**, which is the wrong direction of reasoning even where the conclusion
would have been convenient.

Here the conclusion is not convenient either. The guard is not an obstacle in
this case, it is the mechanism:

- the `{#if editing[c.id]}` branch mounts when the operator clicks Edit and
  stays mounted for the whole editing session — it is not driven by the
  warning's own presence;
- it holds a full form, well past `#0306`'s 8-element threshold;
- that is exactly what `KNOWN_STABLE_BRANCH_SITES` exists to admit, by the same
  argument already recorded for `WorkshopEditor.svelte`'s
  `unsavedInterestsHint`.

The "it would announce on every keystroke" objection does not hold: the text
changes only when a line **crosses** the budget, not as each character is typed.

`#0393`'s reviewer found this and deliberately did not bounce on it — on scope,
not on merit. The line-width feature was designated a follow-up rather than a
`#0393` acceptance criterion, so it is filed here instead.

## Acceptance criteria

- [x] The per-row inline editor's over-budget warning carries `role="status"`, matching the create form's
- [x] `liveRegionGuard.structuralGuard.test.ts` gains one `KNOWN_STABLE_BRANCH_SITES` entry for the `{#if editing[c.id]}` site, with a justification comment naming why the branch is stable — modelled on the existing `WorkshopEditor.svelte` entry
- [x] The code comment recording why the role was omitted is **rewritten**, not deleted: it currently reasons from guard avoidance, and a future reader following that reasoning will reproduce the defect elsewhere
- [x] A mounted component test (`// @vitest-environment jsdom`) asserts the announcement is present when a row's output crosses the budget while editing
- [x] `scripts/check.sh web` passes, and the live-region guard passes **because** of the justified entry rather than because the role is absent

## Notes

Found by `#0393`'s second review pass, 2026-09-03. Filed rather than bounced
because the width warning was itself a follow-up folded into `#0393`, not one
of its criteria.

The general rule worth carrying: when a guard objects to correct markup, the
answer is a justified allowlist entry that records why, not markup bent to keep
the guard quiet. The first is a decision anyone can audit later; the second is
invisible.

## Merging this trips a new assertion (added 2026-09-08 by `#0406`)

`#0406` pinned `web/src/lib/liveRegionGuard.structuralGuard.test.ts`'s
`KNOWN_STABLE_BRANCH_SITES` to a hand-edited constant:

    expect(KNOWN_STABLE_BRANCH_SITES).toHaveLength(KNOWN_STABLE_BRANCH_SITES_RECORDED_COUNT)

On `main` the array holds **twelve** entries and the constant says twelve. This
issue's implementation — currently living on an unmerged worktree branch — adds
a **thirteenth**. **The moment it merges, that assertion goes red and names the
constant.**

That is deliberate. `#0406` was filed believing the array already held thirteen;
its implementer measured `main` and found the premise false, then built the
assertion so the drift cannot recur silently rather than editing a sentence.

**What the merge has to do**, per `#0406`'s review — three things, not one:

1. bump `KNOWN_STABLE_BRANCH_SITES_RECORDED_COUNT` to 13;
2. update the prose that reads "All twelve clear …";
3. add the row to the per-entry measurement table.

The assertion catches only the first. `#0406`'s reviewer recorded that residual
deliberately: tying the *prose* to the array as well would mean the test reading
its own source and parsing a numeral, which is the in-file-guard accretion
`CLAUDE.md` §8 warns about. The constant's own doc comment routes the editor to
the prose instead.

**A measurement trap for whoever does it:** a naive brace count over that range
returns **38**, not 13 — the entries' `reason` strings contain Svelte `{#if}`
text. Count array entries, not braces.

**Done, 2026-09-10, on the transplant onto `main`.** All three steps applied in
this transplant's final commit, and the trap held exactly as written — the file auto-merged with
zero conflict markers and the two count assertions went red only when the suite
ran. Line numbers re-derived rather than trusted (the file had moved): the
constant at **913**, the array literal spanning **915–989**, and the five
`"twelve"` prose sites about *this array* at **316**, **329**, **900**,
**1309**, and **1318**. Entries counted at the 2-space-indented `  {` that opens
each element — **13**, where a brace count over the same range returns 38, as
predicted.

Two `"twelve"` sites were deliberately **left alone**: line **424** ("one of the
twelve *then-current* entries did not clear the new bar") and line **434** ("the
other eleven all cleared it") are scoped by their own words to `#0306`'s
historical set, so bumping them would have made true sentences false. Two more —
lines **174–175** and **1258** — count `.svelte` *files* under `web/src/lib`, not
allowlist entries, and are unrelated.

The per-entry table row was **measured, not assumed**: `19` elements, from a
throwaway copy of the guard file under a unique name with a `console.log` on
`branchSize`, run and deleted (working tree verified clean before and after; the
real file never modified). That same measurement showed five of the twelve
existing figures had drifted upward since `#0306` recorded them, so the table was
refreshed to this pass's numbers rather than left half-stale — every one moved
further above the floor, never toward it.

## Fix

`web/src/views/admin/CrtCommands.svelte`: the per-row inline editor's
over-budget warning went from `<p class="text-warn">{crtOverBudgetWarning(
draft.output) ?? ''}</p>` to `<p class="text-warn"
role="status">{crtOverBudgetWarning(draft.output) ?? ''}</p>` — otherwise
byte-identical to the create form's already-announced warning. The old
comment above it ("No role="status": ... requires ... an allowlist entry ...
isn't the kind of state change that rule is protecting") reasoned from guard
avoidance and has been rewritten in place (not deleted) to state the actual
argument: the `{#if editing[c.id]}` branch is a genuine
`KNOWN_STABLE_BRANCH_SITES` case — it mounts once per Edit click and stays
mounted for the whole editing session (a full form: command text, output
textarea, source select, active checkbox), well past `#0306`'s 8-element
`KNOWN_STABLE_BRANCH_MIN_ELEMENT_COUNT` — and the earlier reasoning is
flagged as the mistake this issue exists to correct, not left silently
replaced.

`web/src/lib/liveRegionGuard.structuralGuard.test.ts`: added one
`KNOWN_STABLE_BRANCH_SITES` entry for
`web/src/views/admin/CrtCommands.svelte`, `match:
'<p class="text-warn" role="status">{crtOverBudgetWarning(draft.output) ??
\'\'}</p>'`, with a justification reason modelled on
`KNOWN_STABLE_BRANCH_REASON_MAIN_FORM` (the existing `unsavedInterestsHint`
entry this issue's Description points at) — same argument, applied to this
component's `{#if editing[c.id]}` branch instead of `WorkshopEditor.svelte`'s
main form, and it names the reversed-reasoning history explicitly so a
future reader of the old comment (still visible in `git log` and in this
entry's own text) doesn't repeat it.

## Verification

`web/src/views/admin/CrtCommands.behavior.test.ts` (`// @vitest-environment
jsdom`, `@testing-library/svelte`), new test: "shows a role="status" warning
in the row editor once a line crosses the CRT width budget" — opens a row's
inline editor, types a 37-character line (one over `CRT_LINE_CHARS` = 36),
and asserts both that the warning text appears and that the element carrying
it has `role="status"`. Passing.

Ran via `ISSUE=0403i scripts/check.sh web` (shared run with `#0403`, same
batch, same worktree): `npm run check` — 294 files, 0 errors, 0 warnings;
`npm test` — 42 files, 4057 tests, all passing, zero skips — including
`liveRegionGuard.structuralGuard.test.ts` itself, which is what proves the
guard passes **because of** the new justified entry (the entry's `match`
string is checked against the real AST of the real file by
`checkFile`/`unscannedFileViolations`/`unjustifiedEntryViolations` in that
same run, not asserted separately). Also ran directly: `npx vitest run
src/views/admin/CrtCommands.behavior.test.ts
src/lib/liveRegionGuard.structuralGuard.test.ts` — 2 files, 40 tests passed.
`uptime` before the run: load averages 2.33/2.79/3.38 — no contention.

jsdom claim: the mount, the textarea `input` event, and the resulting
`role="status"` attribute presence are all solid under jsdom. Whether an
actual screen reader announces the mutation is not observable under jsdom —
that rests on the same persistence argument (`#0063`) the create form's
already-shipped, presumably-working `role="status"` warning already rests
on, not on anything newly measured here.

## Files changed

- `web/src/views/admin/CrtCommands.svelte` — added `role="status"` to the
  per-row warning; rewrote the comment above it
- `web/src/lib/liveRegionGuard.structuralGuard.test.ts` — added one
  `KNOWN_STABLE_BRANCH_SITES` entry
- `web/src/views/admin/CrtCommands.behavior.test.ts` — added the test above
  (shared file with `#0403`'s 4 tests, batched per this pass's brief)

## Work log

Implementation (this pass) — Sonnet, phase 2, subagent batching `#0396` and
`#0403` together (same file, same live-region guard). Token/cost accounting
not available from within this subagent; the orchestrator records it.

## Review notes

Reviewed 2026-09-03 (phase 3, Opus, fresh subagent, batched with `#0403`).
**Resolved.** Every criterion independently re-verified; `#0403` is bounced
separately and does not hold this one up.

**Re-ran, not taken on trust.** `ISSUE=0403r scripts/check.sh web` in this
worktree: `npm run check` 294 files / 0 errors / 0 warnings; `npm test` 42
files, **4057 tests, 0 failures, 0 skips**. `uptime` before the run showed
load 8.69 — above the ~ 8 line in CLAUDE.md §5 — but nothing timing-shaped
failed, so no deadline was touched and no result is being excused by it.

**Criterion 1** — the per-row warning now reads `<p class="text-warn"
role="status">{crtOverBudgetWarning(draft.output) ?? ''}</p>`, byte-identical
to the create form's except for the `draft.` prefix. Confirmed in the file,
not just in the diff.

**Criterion 2 and the "did the allowlist entry weaken the guard?" question.**
The entry is keyed on `file` + the element's exact `rawSource`, and
`checkFile` additionally measures the governing branch against
`KNOWN_STABLE_BRANCH_MIN_ELEMENT_COUNT` — so the prose justification is
checked against the real AST rather than believed. Proved narrowness by
mutation, against an **in-memory copy** (a throwaway copy of the guard file
under a unique name, run with `npx vitest run`, deleted immediately; the
working tree was `git status --porcelain`-clean before and after, and the
real `CrtCommands.svelte` was never modified):

| Mutation | Result |
|---|---|
| baseline, real file | 0 violations |
| add a *second* in-branch `role="status"` (`draft.command` instead of `draft.output`) in the same file | flagged — `not named in KNOWN_STABLE_BRANCH_SITES` |
| same markup placed in a *different* file | flagged |
| strip `role="status"` back off the allowlisted element | fails (the entry goes stale) |

The last row is the direct proof of criterion 5's "passes **because** of the
justified entry rather than because the role is absent": remove the role and
the suite goes red, so the entry is load-bearing rather than dead weight.

**Criterion 3 — the comment was rewritten, not deleted.** This is the point
of the issue and it was honoured. The replacement states the affirmative
argument (the `{#if editing[c.id]}` branch mounts on Edit and stays mounted
for the whole editing session; a full form well past `#0306`'s 8-element
threshold) *and* explicitly flags the old guard-avoidance reasoning as the
defect `#0396` was filed to correct — so a future reader who finds the old
text in `git log` is told why not to follow it. The allowlist entry's own
`reason` carries the same history.

**Criterion 4 and what jsdom is evidence for.** The new mounted test opens a
row editor, types 37 characters (one over `CRT_LINE_CHARS` = 36), and asserts
both the text and `role="status"`. Under jsdom that proves the attribute is
present on the mutating node; it does **not** prove a screen reader speaks
it, and the `## Verification` above says exactly that rather than
overclaiming. Accepted on the same `#0063` persistence argument the create
form's already-shipped warning rests on.

**Housekeeping done in this pass:** a `## Relation` bullet for `#0397` had
been stranded below `## Work log`, and the blank line before `## Fix` was
missing. Both corrected; no content changed.

**Nothing to file from this issue.**

## Relation

- Relates to: [#0393](0393.md) — the CRT commands admin section this corrects
- Relates to: [#0306](0306.md) — the live-region guard and its stable-branch threshold
- Relates to: [#0397](0397.md) — the other half of the width feature, `cmd` rather than `out`
