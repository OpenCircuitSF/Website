# 0147 — A whitespace-only optional value is stored rather than cleared, and one comment says otherwise

| | |
|---|---|
| **Status** | in-progress |
| **Module** | handlers / web |
| **Platform** | All |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |
| **Commit** | `379c334` |

## Description

Two small, related defects `#0139`'s review found while confirming that issue's fix.

**1. Whitespace is not empty.** `normalizeOptionalCampaignField` compares `*v == ""` without trimming, so a value of `"   "` is stored rather than cleared. The workshop editor trims client-side and so never sends one, but `CampaignEditor.svelte` sends `preheader` straight from `bind:value`, untrimmed — so a preheader left as spaces is persisted and then reloads looking blank. The real blanking gesture yields exactly `""`, which is why this is narrow rather than serious.

Trimming inside `normalizeOptionalCampaignField` (`strings.TrimSpace(*v) == ""`) fixes both editors at once and matches what `parseOptionalTime` already does.

**2. A comment that is simply wrong.** The comment added at `CampaignEditor.svelte:384` says it sends "the trimmed value". It does not trim.

## Acceptance criteria

- [x] `normalizeOptionalCampaignField` treats a whitespace-only value as empty, matching `parseOptionalTime`
- [x] A test covers `"   "` → cleared, for both the workshop and campaign patch paths
- [x] The `CampaignEditor.svelte:384` comment describes what the code does
- [x] Check whether trimming should also happen client-side in `CampaignEditor.svelte` for consistency with `workshopAdmin.ts`, and record the decision either way
- [x] `ISSUE=0147 scripts/check.sh go ./internal/handlers/...` and `scripts/check.sh web` both pass

## Notes

Filed from `#0139`'s phase-3 review, which ruled it not bounce-worthy: the actual user gesture of clearing a field produces `""`, not spaces, so the defect needs someone to type whitespace deliberately.

The wrong comment is the part worth fixing promptly. `#0134` exists because of a cluster of doc claims that contradicted their code, and this is one more — cheap now, and misleading to whoever next reads that line.

## Relation

- Found by: [#0139](0139.md) — Blanking an optional field does not clear it
- Relates to: [#0134](0134.md) — the earlier cluster of doc claims that contradicted the code

## Root cause

`normalizeOptionalCampaignField` (`internal/handlers/admin_campaigns.go:430`)
is the single helper both `admin_campaigns.go` and `admin_workshops.go` route
every optional string field through, on both Create and Patch. It compared
`*v == ""` with no trimming, so a value consisting only of spaces (`"   "`)
failed that check and was returned unchanged — stored as literal spaces
rather than being treated as a clear (`nil`). `parseOptionalTime`, the
sibling helper for `starts_at`/`ends_at`, already trims before its blank
check (`strings.TrimSpace(*raw) == ""`), so this was an inconsistency between
two helpers that are supposed to implement the same "empty means unset"
convention, not two different behaviors by design.

Verified before changing it, not assumed: both `admin_campaigns.go`'s Create
(line 381) and Patch (line 508) call sites for `preheader`, and every
optional-string field in `admin_workshops.go`'s Create (lines 269–278) and
Patch (lines 385–429) call sites, all route through this one function. A
single change to it covers both editors' Create and Patch paths at once.

`CampaignEditor.svelte:384`'s comment ("send the trimmed value") was added by
`#0139` and was simply inaccurate — `saveDraft` sent `preheader` straight
from `bind:value` with no `.trim()` anywhere in the chain.

## Fix

- `internal/handlers/admin_campaigns.go` — `normalizeOptionalCampaignField`
  now checks `strings.TrimSpace(*v) == ""` instead of `*v == ""`. `strings`
  was already imported. This is the single change that fixes both the
  workshop and campaign patch (and create) paths, confirmed by reading both
  call sites rather than trusting the issue's claim.
- `web/src/views/admin/CampaignEditor.svelte` — `saveDraft` now sends
  `preheader: preheader.trim()` instead of the raw `$state` value, and the
  `#0139` comment was corrected: it no longer claims something the old code
  didn't do, and a new `#0147` comment explains the added trim and cites the
  whitespace-clearing helper change.

**Decision — client-side trim, criterion 4:** yes, add it.
`workshopAdmin.ts`'s `validateWorkshopForm` already unconditionally trims
every optional string field (`fields.summary.trim()`, etc.) before sending,
established by `#0139`. Trimming `preheader` in `CampaignEditor.svelte` before
the PATCH/PUT brings the two editors into the same convention, makes the
`#0139` comment (as corrected) actually true, and gives users the intuitive
behavior — a preheader with accidental trailing spaces around real text is
tidied on save, exactly as `workshopAdmin.ts` already does for its fields.
The server-side fix is not made redundant by this: it remains as
defense-in-depth for any other caller of the PATCH API (a future admin
client, a script, direct API use) that doesn't go through this Svelte
component.

**Decision — trimming real content at the Go layer: no, deliberately.**
`normalizeOptionalCampaignField` now only changes what counts as *blank*; a
non-blank value is still returned exactly as received, leading/trailing
whitespace and all. Reasons:
- It's the minimal, targeted fix the issue and `#0139`'s review both
  described (`strings.TrimSpace(*v) == ""`), not a broader "always trim
  optional strings" change to a helper twelve fields route through.
- The existing codebase is already inconsistent about server-side trimming
  of *non-blank* content — `admin_campaigns.go`'s Patch trims and stores
  `name`/`subject` (`trimmed := strings.TrimSpace(*req.Name); name =
  trimmed`) but checks-without-storing-trimmed for `body_md` (`if
  strings.TrimSpace(*req.BodyMD) == "" { ... }; bodyMD = *req.BodyMD`).
  Extending trimming to every `normalizeOptionalCampaignField` caller would
  paper over that inconsistency rather than fix this issue's actual defect.
- The two admin editors already trim their own optional string fields
  client-side before sending (`workshopAdmin.ts` unconditionally;
  `CampaignEditor.svelte`'s `preheader` now too, per the decision above), so
  real content reaching the server with meaningful leading/trailing
  whitespace is already the unusual case, not the common one — the server
  change here is about the blank/non-blank boundary, not about
  normalizing everyday content.

## Verification

Machine idle throughout (`uptime` load averages 1.14–2.30, well under the
~8 threshold).

```
go build ./...                                          # clean
ISSUE=0147 scripts/check.sh go ./internal/handlers/...   # VERIFICATION PASSED, zero skips, 1.858s–1.958s (real DB time)
scripts/check.sh web                                     # VERIFICATION PASSED
                                                           # svelte-check 228 FILES 0 ERRORS 0 WARNINGS
                                                           # vitest 19 files / 456 tests passed
```

New tests, named:

- `TestAdminWorkshops_PatchTreatsWhitespaceOnlyOptionalFieldAsCleared`
  (`internal/handlers/admin_workshops_test.go`) — seeds a workshop with
  `summary` and `signup_url` both set, PATCHes
  `{"summary":"   ","location_note":"  Padded real content  "}`, then reads
  the row back with `workshops.Store.GetByID` (the database, not the
  response body): `Summary` is `nil` (cleared), `SignupURL` (never mentioned
  in the PATCH) is unchanged, and `LocationNote` is stored **verbatim**
  including its padding — confirming the fix does not mangle real content.
- `TestAdminCampaigns_Patch_TreatsWhitespaceOnlyPreheaderAsCleared`
  (`internal/handlers/admin_campaigns_test.go`) — same shape for
  `preheader`/`subject` via `mailing.CampaignStore.GetByID`, plus a second
  PATCH in the same test proving a padded, non-blank `preheader` is stored
  exactly as sent.

**Both new Go tests were proved to have teeth by mutation, then restored
byte-for-byte** (sha256 of `admin_campaigns.go` matched before and after,
`git diff --stat` clean throughout): reverting
`normalizeOptionalCampaignField`'s check back to `*v == ""` made both new
tests fail with the exact expected diff (`Preheader = "   " … want nil
(cleared)`, `Summary = "   " … want nil (cleared)`), then the file was
restored from the pre-mutation copy and re-verified to pass again.

**Inverse confirmed in the same requests, not separate tests:**
- A field the PATCH body never mentions (`signup_url`, `subject`) is
  byte-identical to its pre-PATCH value afterward.
- A field with real, non-blank content padded with leading/trailing spaces
  (`location_note`, and `preheader` in a second PATCH within the campaign
  test) is stored **exactly as sent**, spaces and all — the trim fix only
  changes the blank/non-blank boundary, it does not normalize genuine
  content. This directly exercises the deliberate decision recorded above.

## Files changed

- `internal/handlers/admin_campaigns.go` — `normalizeOptionalCampaignField`
  trims before its blank check.
- `internal/handlers/admin_campaigns_test.go` — new
  `TestAdminCampaigns_Patch_TreatsWhitespaceOnlyPreheaderAsCleared`.
- `internal/handlers/admin_workshops_test.go` — new
  `TestAdminWorkshops_PatchTreatsWhitespaceOnlyOptionalFieldAsCleared`.
- `web/src/views/admin/CampaignEditor.svelte` — `saveDraft` trims
  `preheader` before sending; the stale `#0139` comment corrected, a new
  `#0147` comment added.

Commit: `379c334` — `#0147 Fix: Trim whitespace-only optional fields to a
real clear, and correct a stale comment`.

## Gotchas

- Two other subagents were concurrently active in the same checkout the
  whole session: `#0055`'s reviewer (`internal/seo/seo.go`,
  `internal/seo/seo_test.go`, `issues/0055.md`, `web/index.html`,
  `web/dist/index.html`, all left uncommitted throughout) and `#0143`, which
  landed its own commit (`66f59744…`, "`#0143 Work log: record the implement
  pass`") partway through this session. `git status`/`git diff --stat` was
  checked before editing, and `git diff --cached --name-only` was checked
  immediately before both `git add` and `git commit` — each time showing
  exactly this issue's four files. None of those other files were touched or
  staged by this commit.
- `scripts/check.sh web` runs only `npm run check` + `npm test`, no
  `npm run build` — confirmed by reading `scripts/check.sh` before running
  it, since `#0055`'s in-flight, uncommitted edits to `web/index.html` and
  `web/dist/index.html` would have been destroyed by a build-then-restore-
  from-HEAD sequence (CLAUDE.md's guidance for *this issue's own* build
  output does not apply to another agent's uncommitted work).

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 104 | 6,945 | 3,925,747 | 78,288 | $1.77 |

**Total: $1.77**
