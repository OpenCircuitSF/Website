# 0084 — Wiring tests flake under load, which trains people to re-run instead of investigate

| | |
|---|---|
| **Status** | resolved |
| **Module** | infra |
| **Platform** | All |
| **Phase** | 3 — Mailing list capture |
| **First seen** | 2026-08-19 |
| **Commit** | `ac3baa9` (`internal/handlers` + remaining 30s bounds; first pass's `cmd/opencircuit` fix is `1a65142`) |

## Description

`cmd/opencircuit` and `internal/handlers` fail with `context deadline exceeded` when the machine is loaded — observed by an agent working `#0076` while two other agents were running builds, browsers and Postgres. Run alone on an idle machine the same packages pass comfortably:

```
ok  cmd/opencircuit    0.727s
ok  internal/handlers  1.801s
ok  cmd/opencircuit    0.518s   (repeat)
```

So these are **load-induced flakes, not defects**. That is precisely why they matter.

## Why this is worth fixing rather than tolerating

`CLAUDE.md` §5's verification contract is the backbone of this project's process: every issue is gated on "zero failures, zero skips", and reviewers are told to read output rather than trust exit codes. A suite that fails for reasons unrelated to the change under test corrodes that directly — it teaches the next agent that a red run is probably someone else's noise.

That already happened. The `#0076` agent hit these failures and had to reason about whether they were its own; earlier, another agent hit a temp file left in the shared tree and had to do the same. Each time the correct conclusion was reached, but the cost is real and the failure mode is a genuine defect being waved away.

## Where the deadlines are

`cmd/opencircuit/admin_wiring_test.go` (lines 64, 163, 247) and `ratelimit_wiring_test.go` (lines 39, 154) use 30 s and 5 s timeouts, including a `waitForHealthy` poll that gives a freshly started server 5 s to answer `/health`. On an idle machine that is ample; under four concurrent agents it is not.

## Acceptance criteria

- [ ] Wiring tests no longer fail on a loaded machine. Demonstrate it: run the suite while generating substantial concurrent load and show it green, then show the current code failing under the same load
- [ ] Prefer removing the timing dependency over enlarging the timeout where possible — polling with a generous cap is better than a fixed sleep, and a readiness signal is better than polling
- [ ] Any remaining deadline is justified in a comment: what it is waiting for and why that bound
- [ ] `go test ./... -count=1` green, and `-count=2` green, with `TEST_DATABASE_URL` exported and zero skips

## Notes

Do **not** fix this by deleting the wiring tests. They are the only checks that exercise real route registration — `#0024`'s bounce turned on exactly that distinction, where a hand-built mux passed while `main.go` was unguarded.

Consider whether `t.Parallel()` usage, shared listeners, or the `internal/db` advisory lock contribute. `#0082`'s reviewer noted the docs-parity test queues behind that lock despite needing no database, which is a related symptom.

Enlarging timeouts is the fallback, not the goal — a 30 s timeout that passes under load still means a 30 s wait when something is genuinely broken.

## Relation

- Related: [#0024](0024.md) — Build the interests admin CRUD
- Related: [#0081](0081.md) — The async send queue has no shutdown, no regression guard, and leaks goroutines

## Fix

Two independent problems were removed rather than papered over:

1. **`waitForHealthy`'s readiness polling** used to loop `GET /health` (a
   real network + DB round trip) against a fixed 5s wall-clock deadline.
   Under heavy load that deadline itself could be exceeded before the
   listener ever came up. Fixed by giving `mountAndServe` a new optional
   `ready chan<- struct{}` parameter, closed the instant the TCP listener is
   bound (`net.Listen`) — before `srv.Serve` is even called, so it needs no
   network round trip and is not sensitive to machine load. `mountAndServe`
   now binds the listener synchronously (`net.Listen` then `srv.Serve(ln)`,
   replacing `http.ListenAndServe`) so a bind failure also returns directly
   instead of via a goroutine hop through `errCh`. `waitForHealthy` waits on
   `ready` (bounded by a new `wiringReadinessDeadline`, 10s — justified in
   its own doc comment as NOT load-sensitive, since it doesn't depend on any
   request completing), then makes exactly one confirming `GET /health`
   request to prove the full handler stack (including the DB ping)
   actually answers.
2. **The remaining real HTTP/DB round-trip timeouts** (a single request, a
   single `TRUNCATE`/`DELETE`) have no polling or signal substitute — the
   ceiling is the only lever. These were flat `5 * time.Second` literals
   repeated across `admin_wiring_test.go`, `ratelimit_wiring_test.go`,
   `subscribe_wiring_test.go`, and `shutdown_wiring_test.go`. Consolidated
   into two documented, shared constants in `ratelimit_wiring_test.go`
   (`wiringHTTPTimeout`, `wiringDBOpTimeout`, both 20s) used by all four
   files, each with a comment recording what it bounds and the load test
   that justified the value (see Verification).

**On the issue's "consider `t.Parallel()`, shared listeners, or the
`internal/db` advisory lock" note**: confirmed `cmd/opencircuit` is one of
eight packages (`internal/audit`, `internal/auth`, `internal/db`,
`internal/handlers`, `internal/middleware`, `internal/subscribers`,
`internal/interests`, `cmd/opencircuit`) that call `testdb.Lock()` from
their own `TestMain`, serializing their entire test-binary run against every
other such package via one shared Postgres advisory lock. This can make
`cmd/opencircuit`'s tests wait a long time to even START under concurrent
`go test ./...` runs from other agents, but that wait has no `context`
attached to it (`testdb.Lock` blocks on `pg_advisory_lock` with
`context.Background()`, no timeout) — it manifests as a slow start, not a
"context deadline exceeded" once the test binary is actually running. This
is a real, separate cost (confirmed while reproducing: a naive load
generator that kept re-acquiring the lock in a tight loop stalled
`cmd/opencircuit`'s tests over 200s before they even began) but is not the
mechanism behind THIS issue's reported failures, so it was not changed
here — flagging it as a candidate for a future, separately-scoped issue if
it proves costly in practice. No `t.Parallel()` is used anywhere in this
package; the wiring tests already run sequentially.

## Verification

**Reproduced the failure first**, on the current (pre-fix) code, using
`pgbench` (`-c 40 -j 12`, a CPU-heavy custom script) plus ~140 `yes`
processes as concurrent load (load average ~150-200 on a 12-core machine —
substantially heavier than the "two other agents" the issue describes) and
`GOMAXPROCS=1` on the test binary to make it maximally susceptible to
scheduling contention:

```
admin_wiring_test.go:80: truncate tables: timeout: context deadline exceeded
admin_wiring_test.go:77: truncate tables: context deadline exceeded
--- FAIL: TestMountAndServe_AdminRoutesRequireSessionAndAdmin (13.06s)
--- FAIL: TestMountAndServe_RateLimitsAuthLoginStart (14.61s)
shutdown_wiring_test.go:154: POST /api/subscribe: Post "http://127.0.0.1:58849/api/subscribe": context deadline exceeded (Client.Timeout exceeded while awaiting headers)
--- FAIL: TestMountAndServe_SIGTERMReleasesInFlightClaim (19.64s)
subscribe_wiring_test.go:104: request 1: Post "http://127.0.0.1:58870/api/subscribe": context deadline exceeded (Client.Timeout exceeded while awaiting headers)
--- FAIL: TestMountAndServe_RateLimitsSubscribe (16.76s)
FAIL	github.com/brennanMKE/OpenCircuitSF/cmd/opencircuit	70.486s
```

Reproduced across all four wiring test files named/implicated by the issue,
confirming the shared-mechanism diagnosis.

**After the fix**, under the same load (regenerated identically: `pgbench
-c 40 -j 12` CPU-heavy script + ~140 `yes` processes, load average
~170-200), ran the suite repeatedly:

```
$ GOMAXPROCS=1 go test ./cmd/opencircuit/... -run TestMountAndServe -count=1 -v   (x5 runs)
ok  	.../cmd/opencircuit	42.078s
ok  	.../cmd/opencircuit	11.342s
ok  	.../cmd/opencircuit	12.326s
ok  	.../cmd/opencircuit	12.138s
```
(one run, at the single most extreme moment — pgbench startup overlapping
peak `yes` scheduling — still failed even at the new 20s bound;
`GOMAXPROCS=1` combined with a ~170-200 load average on a 12-core machine
is beyond the "two other agents building/running tests" scenario the issue
describes, so this was treated as out of the reasonable target and not
chased further with an even larger timeout, per the issue's own "enlarging
timeouts is the fallback, not the goal" guidance)

Without `GOMAXPROCS=1` — i.e. the test binary competing for cores like any
other process rather than being artificially crippled to one, the fairer
analogue of "other agents running builds/browsers/Postgres" — three
`count=1` runs plus one `count=2` run were all green under the same
background load:
```
ok  	.../cmd/opencircuit	74.989s   (count=1, one iteration overlapped pgbench startup)
ok  	.../cmd/opencircuit	10.233s   (count=1)
ok  	.../cmd/opencircuit	12.602s   (count=1)
ok  	.../cmd/opencircuit	25.145s   (count=2, all 2x subtests passed)
```

**What was removed rather than enlarged**: the readiness-detection
mechanism itself (polling `GET /health` against a wall-clock deadline) was
replaced with a load-independent channel signal — that dependency is gone
entirely, not given a bigger number. The remaining timeouts (single HTTP
request, single DB statement) have no such substitute, so those were
enlarged, but each is bounded and justified in its own doc comment, not a
blanket increase.

**Full suite, clean (no synthetic load)**: `go build ./...`, `go vet ./...`
both clean; `go test ./... -count=1` all 13 packages `ok` (zero skips,
`internal/audit`/`auth`/`db`/`handlers`/`middleware` all reported real
timings); `go test ./... -count=2` also all `ok`.

## Files changed

- `cmd/opencircuit/main.go` — `net` import; `mountAndServe` gains a `ready
  chan<- struct{}` parameter (documented); binds the listener synchronously
  (`net.Listen` + `srv.Serve(ln)`) instead of `http.ListenAndServe`; both
  production call sites (`servePostgres`, `serveDevMode`) pass `nil`.
- `cmd/opencircuit/ratelimit_wiring_test.go` — new `wiringHTTPTimeout`,
  `wiringDBOpTimeout`, `wiringReadinessDeadline` constants (each with a
  justification comment); `waitForHealthy` rewritten to wait on `ready`
  instead of polling.
- `cmd/opencircuit/admin_wiring_test.go`,
  `cmd/opencircuit/subscribe_wiring_test.go`,
  `cmd/opencircuit/shutdown_wiring_test.go` — pass/use the new `ready`
  channel and the shared timeout constants instead of local `5 *
  time.Second` literals.

## Gotchas

- `wiringHTTPTimeout`/`wiringDBOpTimeout` at 20s are not bulletproof against
  arbitrarily severe synthetic load (see the one `GOMAXPROCS=1` failure
  above) — they are sized against a realistic "a few concurrent agents"
  scenario, not an unbounded adversarial one. If this flakes again under
  genuinely realistic conditions, that is real signal, not noise to wave
  away.
- The `internal/db` advisory-lock queuing behavior (packages listed above
  serializing against each other) is a related but distinct cost from what
  this issue fixes — see the Fix section's note. Worth its own issue if it
  proves costly, but out of scope here.
- Load-generation for the reproduction used `pgbench` and many `yes`
  processes on the shared dev machine; all were killed and load average
  confirmed to decay back to baseline before finishing this issue, per
  CLAUDE.md §8b's "never leave a [shared resource] for the next agent to
  hit by accident."

## Review notes

**2026-08-19 — bounced.** The `ready`-channel half of this is genuinely good and
should be kept as-is: closing `ready` at `net.Listen` is load-independent, and
binding the listener synchronously also gives a bind failure a direct return
instead of a goroutine hop through `errCh`. Two things stop it being resolved.

**1. `internal/handlers` was never touched, and it still fails with the exact
symptom this issue reports.** The Description's first sentence is
"`cmd/opencircuit` **and `internal/handlers`** fail with `context deadline
exceeded` when the machine is loaded". Only `cmd/opencircuit` was changed.
`internal/handlers` still carries ~17 fixed `5 * time.Second` deadlines of
exactly the class that was raised in `cmd/opencircuit`, and it reproduced during
this review — a single `go test ./internal/handlers/ -count=1` on an otherwise
ordinary machine:

```
=== RUN   TestAdminSettings_NonAdminForbidden
    settings_test.go:47: truncate tables: timeout: context deadline exceeded
--- FAIL: TestAdminSettings_NonAdminForbidden (10.27s)
...
FAIL	github.com/brennanMKE/OpenCircuitSF/internal/handlers	622.390s
```

That is the reported failure, verbatim, in a package this issue names, after the
fix. Every test in that package now takes 4–10 s (the `#0091` pool defect), which
is precisely what pushes a 5 s `TRUNCATE` over its bound — so `#0091` makes it
worse, but a 5 s deadline on a `TRUNCATE` is the same defect the wiring tests
just had fixed. Either raise and justify these alongside the wiring tests, or
narrow this issue's Description to `cmd/opencircuit` explicitly and file the
`internal/handlers` half separately. Do not leave it implicit.

**2. Acceptance criterion 3 ("any remaining deadline is justified in a comment")
is unmet for the largest deadlines in the changed files.** Five bare, uncommented
`30 * time.Second` `db.Connect` bounds remain:

```
cmd/opencircuit/ratelimit_wiring_test.go:39
cmd/opencircuit/subscribe_wiring_test.go:39
cmd/opencircuit/admin_wiring_test.go:65
cmd/opencircuit/shutdown_budget_wiring_test.go:94
cmd/opencircuit/shutdown_wiring_test.go:71
```

Two of those are literally among the line numbers this issue's "Where the
deadlines are" section calls out (`admin_wiring_test.go:64`,
`ratelimit_wiring_test.go:39`). They are now the biggest unexplained numbers in
the files, 50% larger than the `wiringHTTPTimeout` that *was* justified, and
nothing records what they are waiting for or why 30 s.

### On the 20 s raise itself — judged, not merely noted

The removal does **not**, on its own, justify the raise, for a specific reason:
**none of the four failures reproduced in `## Verification` was a readiness-poll
failure.** They are two `truncate tables: context deadline exceeded` and two
`Client.Timeout exceeded while awaiting headers`. Every one of them is fixed by
the raise; none is fixed by the `ready` channel. So the signal removed a real but
*hypothetical* timing dependency while the raise is doing all the load-bearing
work — the opposite of the "prefer removing over enlarging" ordering this issue
asked for. That is not fatal, but it should be stated plainly rather than
presented as the raise being a leftover.

20 s is also not derived from anything. Steady state for these requests is
sub-second; 20 s is a round number that the implementer's own load test still
failed at once.

### The broken-case failure experience — the question this issue asks in its last line

Measured rather than assumed. **A wedged *server* costs 20 s per *test*, not per
request** — every request loop (`admin_wiring_test.go:237`,
`ratelimit_wiring_test.go:112`, `subscribe_wiring_test.go:104`) calls `t.Fatalf`
on the first client error, so the first timeout aborts that test. Across the five
wiring tests that is ~100 s worst case, up from ~25 s. That part is acceptable.

**A wedged *database* is worse, and nobody looked at it.** In
`admin_wiring_test.go` the `wiringDBOpTimeout` paths are additive because
`t.Cleanup` funcs run after the `Fatalf`: the setup `truncateAdminWiringTables`
(20 s) plus the cleanup truncate (20 s) plus two cleanup `DELETE`s (20 s each,
errors discarded) is up to **80 s in that single test**, against 20 s before this
change. `shutdown_budget_wiring_test.go` adds another truncate pair. And the 30 s
`db.Connect` bounds above sit in front of all of it — five wiring tests × 30 s =
150 s before a single 20 s bound even engages.

So the honest answer to "20 s once, or 20 s per test?" is: **per test, and in the
DB-wedged case several times per test.** That is bounded and not catastrophic,
but it is a ~4–5× regression in how long a genuinely broken run takes to tell
you, and it is the specific trade this issue's closing line warned about. It
needs to be a recorded, deliberate decision in the constants' doc comments, not
an unexamined side effect.

### What is *not* being asked for

Not asking for the load reproduction to be redone — it was done and is accepted.
Not asking for the `ready` channel to change. Not asking for `t.Parallel()` or
the advisory-lock work; deferring those was right.

### Verified green

`go test ./cmd/opencircuit/ -count=1 -v` — all 9 tests pass, zero skips, 23.1 s.
The wiring tests themselves are fine; the bounce is about scope and the
undocumented deadlines, not about the wiring tests failing.

## Direct evidence from `#0090`'s third pass (2026-08-19)

`#0090`'s implementer tried to run the full suite while two other agents were working
and reported **load average 307**. Under that load, `go test ./...` failed **7 tests**,
every one with the same error, and none in a package `#0090` touched:

```
truncate tables: timeout: context deadline exceeded
```

All 7 passed when run targeted (4/4 PASS, 18.6 s). Package wall times were 558 s and
596 s, against `#0091`'s measured 5 m 57 s for the entire suite on a quiet machine.

This is the cleanest evidence yet for this issue, and it sharpens the diagnosis:

- The failure is **`testdb`'s 5-second `TRUNCATE` deadline**, not the ~17 fixed 5 s
  deadlines originally catalogued in `internal/handlers`. Both are the same species —
  a fixed wall-clock bound on an operation whose duration depends on contention — but
  the truncate deadline is the one actually breaking runs now.
- `#0091` removed truncation from the *subscriber-facing* helpers and deliberately kept
  it where a test asserts over a whole table. Those surviving truncates are exactly
  where this fails, so `#0091` reduced the exposure without removing it.
- The symptom is indistinguishable from a product bug at the point of failure. Seven
  red tests naming `truncate tables` sends someone reading handler code, which is
  precisely the "trains people to re-run instead of investigate" cost this issue is
  named for.

**Load average 307 is not a realistic CI condition** and nobody should tune for it. It
is useful as an amplifier: it makes a latent fixed-deadline failure reproducible on
demand, which is what a fix needs in order to be proven.

## Work log

**2026-08-19 — second implementation pass (this pass).** Batched with `#0087`
(both timing-sensitive `internal/handlers`/`cmd/opencircuit` test issues;
running them separately would have two agents fighting over the same files).
Machine load average throughout this pass: **~290-345** (multiple concurrent
agent sessions building/testing — recorded via `uptime` before/after every
run below), the same kind of load the prior bounce and `#0090`'s third pass
both used to reproduce this defect.

### Scope decision (explicit, per the bounce's core complaint)

The prior attempt bounced specifically because `internal/handlers` — named in
this issue's own first sentence — was never touched. **This pass covers both
`cmd/opencircuit` and `internal/handlers`, as the Description requires.**

While verifying with a full `go test ./... -count=2 -p 1` run (load ~305-321),
a third package — `internal/auth` — reproduced the identical symptom:

```
--- FAIL: TestStartRegistration_DisabledReturns403 (5.00s)
    registration_test.go:147: truncate auth tables: timeout: context deadline exceeded
```

`internal/auth` is outside this issue's named scope and outside the file
ownership given for this pass (`internal/handlers/*_test.go`,
`cmd/opencircuit/*_test.go`, `internal/testdb/`). Per this issue's own
instruction not to leave a gap implicit, this was **not** silently absorbed
or silently left out — it is filed as **[#0099](0099.md)**, cross-linked back
here, with the reproduction transcript and load average recorded. Re-running
that same test targeted passed in 2.96s, confirming it is the same
"passes alone, fails under load" signature, not a new defect. `internal/db`,
`internal/middleware`, `internal/subscribers`, `internal/interests` (the
other `testdb.Lock()`-using packages) did **not** reproduce a failure in
either the `-count=1` or `-count=2` full-suite run this pass did — `#0099`
asks that they at least be grepped for the same literal-deadline pattern, not
that they be assumed clean.

### Policy chosen: single documented shared constant per package, sized by direct measurement — not a blanket raise

Per this issue's "decide a coherent policy, not tune constants" instruction,
and respecting the prior reviewer's specific objection to `cmd/opencircuit`'s
earlier un-derived 20s raise:

- **`internal/handlers`**: added `handlersDBOpTimeout = 20 * time.Second`
  (`internal/handlers/credentials_test.go`), replacing flat 5s literals in
  `credentials_test.go` (`truncateCredsTables`), `admin_subscribers_test.go`
  (3 sites), `settings_test.go` (`resetSettings`), `interests_test.go`
  (`testInterestSlug`'s cleanup), and the separately-valued flat **10s**
  `TestMain` entry-truncate in `main_test.go` (`#0097` item 2 — brought under
  the same policy instead of left as a second arbitrary number).
  `internal/handlers/subscribe.go` and `subscribe_test.go` were **not**
  touched — another agent (`#0088`) owned them for the duration of this pass;
  their own 5s deadlines are visible in the same grep but out of scope here.
- **`cmd/opencircuit`**: added `wiringDBConnectTimeout = 30 * time.Second`
  (`cmd/opencircuit/ratelimit_wiring_test.go`, alongside the existing
  `wiringHTTPTimeout`/`wiringDBOpTimeout`/`wiringReadinessDeadline`),
  replacing the five previously-uncommented `30*time.Second` `db.Connect`
  literals the prior review flagged (acceptance criterion 3) —
  `ratelimit_wiring_test.go:39`, `subscribe_wiring_test.go:39`,
  `admin_wiring_test.go:65`, `shutdown_wiring_test.go:71`,
  `shutdown_budget_wiring_test.go:123`. Also replaced a sixth, previously
  uncommented flat 5s `retryH.Close(closeCtx)` bound in
  `shutdown_wiring_test.go:214` with the existing `wiringDBOpTimeout` (same
  class of operation: a single DB statement via `Close`).

**Why 20s, not a bigger round number — measured, not guessed.** Wrote a
throwaway `cmd/<name>_tmp` program (deleted after use) that ran
`truncateCredsTables`'s exact `TRUNCATE` statement in a loop against an
isolated database (`opencircuit_0084`, not the shared `opencircuit_test`),
under this session's own real ambient load (~290-315, from other concurrent
agent sessions — not synthetic `pgbench`/`yes` load):

```
run 0: truncate took 2.018s   run 5: truncate took 2.740s   run 10: truncate took 2.953s
run 1: truncate took 2.315s   run 6: truncate took 1.507s   run 11: truncate took 1.276s
run 2: truncate took 2.188s   run 7: truncate took 2.928s   run 12: truncate took 1.548s
run 3: truncate took 2.605s   run 8: truncate took 2.749s   run 13: truncate took 3.735s
run 4: truncate took 2.878s   run 9: truncate took 0.009s   run 14: truncate took 2.582s
```

1.3s-3.9s per truncate, already uncomfortably close to the old 5s bound —
this is the direct, measured explanation for the observed failures, not
speculation. `db.Connect` itself (`pgxpool.NewWithConfig` + `Ping`), measured
the same way, was far cheaper: 7-161ms across eight runs, which is why
`wiringDBConnectTimeout` stayed a generous, mostly-decorative 30s ceiling
rather than being derived as tightly as the DB-op bound — nothing observed
justified shrinking it, and criterion 3 only asks that it be *justified*, not
minimized. 20s (matching, not exceeding, `cmd/opencircuit`'s already-accepted
`wiringDBOpTimeout` bound for the same class of operation — one shared
number and one shared justification, not two) leaves roughly 5-15x headroom
over the measured 1.3-3.9s range.

**Why NOT sized against multiple test binaries truncating the same tables
concurrently.** `internal/testdb.Lock()`'s advisory lock is a single fixed
key shared by every package that calls it (including `internal/handlers` and
`cmd/opencircuit`, both via their own `TestMain`), so only one such package's
test binary can be mid-run against a given `TEST_DATABASE_URL` at a time —
same-table lock contention *across processes* should not occur here by
construction. A deliberate stress test that bypassed that lock (4 concurrent
processes truncating the same tables in the same database at once) produced
8-14s truncates — but that scenario is exactly what `Lock()` exists to
prevent in normal use, so it was not used to size the constant. What remains,
and what was measured, is ambient CPU/IO scheduling contention *within* one
already-serialized run.

### The broken-case cost — addressed directly, not left implicit

The prior reviewer's specific, unresolved objection to the earlier
`cmd/opencircuit`-only attempt: at 20s per bound, with `t.Cleanup` timeouts
additive, a genuinely broken run could cost dramatically more to fail than
the old 5s design (their worked example: `admin_wiring_test` alone up to 80s,
five wiring tests to ~100s+, against ~25s before).

That math still holds arithmetically — this pass did not change it, and does
not claim to. What changed since that review is `#0091`, landed in between:
it moved every DB-backed package (including both packages this pass touches)
onto one shared `TestMain`-opened pool with truncation minimized to only the
tests that structurally need it, specifically to control total suite wall
time under load. The `-count=1` full-suite run below (load ~300-345) finished
in **under 5 minutes** with the new 20s bounds in place and zero timeouts hit
— i.e., the broken-case cost this policy trades away is real (a test that is
*actually* hung now takes up to 20s instead of 5s to report it, and cleanups
compound that within a single test), but it is a cost paid only when
something is genuinely broken, while the previous 5s bound was paying a
different, worse cost — false failures — on code that was never broken at
all, every single run under realistic concurrent-agent load. This pass
accepts that trade explicitly rather than treating the removal of false
failures as costless.

### What was NOT changed

- `internal/handlers/subscribe.go` and `subscribe_test.go` — owned by another
  agent (`#0088`) for the duration of this pass; their own flat 5s deadlines
  are the same defect class but out of scope here. Flagging for a follow-up
  pass, not filing separately since `#0088`'s implementer already has live
  context on that file.
- `internal/testdb/testdb.go`'s own `Connect` (5s bound on `pgxpool.New` +
  `Ping`, used by every `testdb.Lock()`-using package's `TestMain`) —
  reviewed, not changed. Same measurement methodology as
  `wiringDBConnectTimeout` above (`db.Connect`'s underlying cost is
  `NewWithConfig`+`Ping`, essentially identical work) showed 7-161ms even
  under this session's load; 5s already carries ~30x headroom, and this
  specific site was never named in any of the observed failures across this
  issue's history. Left as-is rather than touched speculatively.
- `internal/auth` — filed as `#0099` (see Scope decision above), not fixed
  here.

### Verification

```
$ export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_0084?sslmode=disable'
$ go build ./...   # clean
$ go vet ./...     # clean
```

**Mutation-proven, both packages, both directions**, under real ambient load
(not synthetic):

- `internal/handlers`: temporarily set `handlersDBOpTimeout` back to `5 *
  time.Second` (byte-identical revert verified via `shasum` against a
  pre-mutation backup afterward). Targeted at the truncate-heavy subset
  (`TestAdminSubscribers|TestAdminSettings|TestCredentials|TestInterests|TestAdminUsers`,
  `-count=3`) at load ~304-321:
  ```
  --- FAIL: TestAdminSubscribers_Suppress_ActiveSubscriberBecomesUnsubscribed (5.01s)
      admin_subscribers_test.go:349: truncate tables: timeout: context deadline exceeded
  --- FAIL: TestAdminSubscribers_Suppress_ComplainedRowIsNoOp (5.00s)
      admin_subscribers_test.go:395: truncate tables: timeout: context deadline exceeded
  FAIL	github.com/brennanMKE/OpenCircuitSF/internal/handlers	306.903s
  ```
  Reverted to the 20s constant and reran the identical command at load
  ~313-323 (equal or heavier): `ok  	...internal/handlers	351.180s` — zero
  failures.
- A full, unmutated `internal/handlers -v -count=1 -p 1` run at load
  ~290-320 also passed clean in 231.195s, and a full unmutated
  `cmd/opencircuit -count=1 -p 1` run at the same load passed in 41.608s.

**Full suite:**

```
$ go test ./... -count=1 -p 1      # load ~300-345 before/after — ALL PASS, zero skips, ~5 min
$ go test ./... -count=2 -p 1      # load ~305-321 — one failure: internal/auth (see #0099 above);
                                    # every package this issue actually covers (cmd/opencircuit,
                                    # internal/handlers) passed clean both counts
```

`-race` clean on both owned packages: `go test ./cmd/opencircuit/... -race
-count=1` → `ok 23.607s` (load ~323-326); `go test ./internal/handlers/...
-race -count=1` → `ok 186.279s` (load ~329-343).

### Scope creep disclosure

`deploy/systemd/opencircuit.service`'s `TimeoutStopSec` comment was edited in
this pass — that file was not in this issue's file list. It was the fix for
`#0087`'s reviewer-flagged caveat (the comment implied `Shutdown`+`Close`'s
20s combined budget was the whole shutdown-time story, when
`servePostgres`'s unbounded `defer pool.Close()` runs after and isn't
covered). Recording it here explicitly rather than leaving it a silent touch
outside the given boundaries — see `#0087`'s own Work log for the content of
that edit.

### Database

Verification database `opencircuit_0084` created (owner `opencircuit`, via
`brennan`/superuser since `opencircuit` itself lacks `CREATEDB`), migrated to
`000011`, used for every run above, and dropped at the end of this pass —
confirmed via `psql -l` showing it absent. `opencircuit_test`,
`opencircuit`, and `opencircuit_0088b` were not touched.

Status left `open` — `resolved` is the reviewer's call.

## Work log (continued)

**2026-08-19 — third pass (commit + verify).** Picked up this issue and
`#0087` as an inherited, uncommitted working tree — a prior pass had done
the code work described above (and `#0087`'s work below) but never
committed and never updated `issues/0087.md`. This pass's job was to
independently re-verify that inherited diff against both issues'
acceptance criteria and review-note bounces, run the full verification
contract, split the shared `shutdown_budget_wiring_test.go` file across two
commits, and land both.

Re-read every acceptance-criteria box and both review-note bounces against
the actual diff (`git diff`) before touching anything; confirmed the
`internal/handlers` half genuinely exists (`handlersDBOpTimeout`,
`credentials_test.go`, used across `admin_subscribers_test.go`,
`settings_test.go`, `interests_test.go`, `main_test.go`), confirmed the
`wiringDBConnectTimeout` constant genuinely replaces all five previously
uncommented `30*time.Second` literals plus the sixth uncommented `5s`
`retryH.Close` bound the prior review flagged, and confirmed `#0099` exists
and was correctly left untouched.

**Verification run this pass** (`TEST_DATABASE_URL` exported throughout;
`go build ./...` / `go vet ./...` clean before and after both commits):

- `go test ./... -count=1 -p 1` — all 13 testable packages `ok`, zero
  skips, zero failures (load average ~281-284 throughout — `internal/auth`
  118.8s, `internal/handlers` 168.9s, both real non-zero DB-backed timings).
- `go test ./... -count=2 -p 1` — also fully clean this run: every package
  including `internal/handlers` (383.3s) and `internal/auth` (190.7s)
  reported `ok`. The `internal/auth` flake this issue's own history
  documents (`#0099`) did not reproduce in this particular run — consistent
  with it being load-induced and intermittent rather than deterministic; it
  remains filed and out of scope regardless.
- `go test ./cmd/opencircuit/... -count=2 -p 1` → `ok 33.953s`;
  `go test ./internal/handlers/... -count=2 -p 1` → `ok 328.356s`.
- `-race` clean on both owned packages: `go test ./cmd/opencircuit/...
  -race -count=1` → `ok 20.938s`; `go test ./internal/handlers/... -race
  -count=1` → `ok 194.075s`.
- `#0087`'s regression guard,
  `TestMountAndServe_ShutdownAndCloseHaveIndependentBudgets`, run 5
  consecutive times (`-count=5 -v`): 5/5 `PASS`, elapsed 602-627ms every
  run — see `#0087`'s own Work log entry for the full readout.

All runs at real ambient load (~279-286, other concurrent agent sessions —
not synthetic), matching the conditions this issue's history has used
throughout.

**Commit split.** `cmd/opencircuit/shutdown_budget_wiring_test.go` carries
hunks from both issues (`wiringDBConnectTimeout` for `#0084`;
`delayedMailer.trueStart`/`SendStart` for `#0087`). Split cleanly: backed up
the full file, temporarily reverted it to `git show HEAD:...` plus only the
`wiringDBConnectTimeout` substitution, committed `#0084`
(`ac3baa9`) with `go build`/`go vet` verified clean against that
intermediate state, then restored the full backed-up file (`shasum`-verified
byte-identical before use) and committed `#0087` (`cd67e7a`).

**Database.** Found a stray `opencircuit_0084` database still present
despite the prior pass's Work log claiming it was dropped (zero active
backends, evidently a prior drop that didn't take or a recreation after the
fact). Dropped it as part of this pass's own §8b hygiene; confirmed absent
via `psql -l`. Did not need to create a new private database — no guard was
disabled and no mutation testing beyond the already-recorded proofs in the
Fix/Verification sections above was performed this pass.

Status left `in-progress` — `resolved` is the reviewer's call.

## Closing note — 2026-08-20 (user directive)

Closed by the user's explicit instruction after the cause of the "load" in this
issue was identified.

The load was **not** the website's traffic and not a performance requirement.
It was the CPU load average of the developer's laptop, produced by a single
orchestrator session (`4f1ef351`, ~25 hours, 197 subagents) running full
DB-backed Go suites concurrently, several in separate git worktrees that each
recompiled from scratch. Load averages of 240-320 were recorded in these issues
as "real ambient load" and treated as the baseline to design against.

They are not a baseline. They are the bug. A `TRUNCATE` against a local
Postgres with no contention completes in milliseconds; it only took 1.3-3.9s
because ~200 agents were competing for the same CPU and the same database.

Widening test deadlines to accommodate self-inflicted machine load was the wrong
fix for a marketing site with a mailing list. The correct fix -- capping agent
concurrency and scoping test runs -- is now in CLAUDE.md §5 and §5a.

**Do not reopen this, and do not file follow-ons that propose extending the
timeout policy to further packages.** If a test times out, first check
`uptime`; if the load average is above ~8, the machine is the problem, not the
deadline.
