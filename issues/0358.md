# 0358 — Pin the directional comment axis's calibration in fixtures, and retire its prose limit list

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | `5ed14b6` (first pass), `906115a` (second pass — B1–B6 from the bounce) |

## Description

`#0347`'s directional axis in
`internal/outbox/claim_comment_referent_guard_test.go` is a **heuristic
window**, not a decision procedure. Its two reviews measured **six shapes**
where it is silent or spurious, none of which fires on `HEAD`:

**False negatives**
- the direction word sits inside a parenthetical the identifier is outside of —
  `// …ClaimDue's kinds filter (applied in pass, below) so…`
- the identifier is named **after** the direction word — `// Below, ClaimDue
  claims the row`
- a clause boundary separates them (em dash, ASCII `--`, or colon)
- more than 90 characters separate them

**False positives**
- non-positional "above"/"below" — `// the table below explains why`,
  `// ClaimDue's retry budget stays below eight attempts`

**Each is a deliberate calibration trade against a real opposing error**, so
none is a defect standing alone. The innermost-parenthesis scoping, for
instance, exists because without it `internal/outbox/store.go:463` regresses
into a false positive — the trade was made knowingly.

**The problem is that nothing in the tree records the trades or would notice if
a future edit widened them.** A change to the window logic currently discovers
its own consequences in a build break, or worse, does not.

This is the same lesson as `#0330` and `#0354`/`#0355` in their own families: a
check that is correct while nothing pins the part doing the work.

## Acceptance criteria

1. A small table of pinned fixtures asserts the **current** calibration in both
   directions: each measured false negative stays silent, each measured false
   positive stays flagged-or-silent as it is today, and the two real sites
   (`db9bff7`'s A and C) stay caught.
2. Each fixture names, in a comment, **which trade it pins and against what
   opposing error** — the point is that a future change must state which trade
   it is moving, not discover it. Name `store.go`'s false positive as the
   reason the parenthesis scoping exists.
3. Prove the table discriminates: mutate the window logic in each of the three
   directions (marker set, every-identifier, innermost-paren) and confirm the
   corresponding fixture fails. A fixture that cannot fail pins nothing — that
   is `#0354`'s finding, in this family.
4. Do **not** try to fix the six shapes. They are calibration, not defects, and
   widening the window re-opens the false positives it was narrowed to close.
   If a pass believes one is genuinely fixable without cost, it must say so and
   stop rather than change it.
5. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal enters
   any Go comment, and the harness population stays **42 total / 12
   non-exempt** — `#0347`'s first pass moved it to 43 via its own ban-list, and
   `#0323` moved it 35 → 36 the same way. Re-run
   `scripts/go_file_visit_floor_guard_test.sh` and record it.
6. Fixtures built by **extraction, not retyping** — `#0347`'s reviews confirmed
   the existing ones are byte-identical to source including a U+2014 em dash,
   and retyping one as ASCII was an early bug. `python3` or a hex dump to
   verify; BSD `grep -P` cannot prove a byte sequence present (§8).
7. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout (§8).

## Added by `#0347`'s third review, 2026-08-31

That review resolved `#0347` and measured **two more trade surfaces**, neither
recorded anywhere. It also gave this issue a second job.

**Fifth false-negative row — the marker-exemption surface.** Any of
`has not called`, `rather than`, `instead of`, or `would` appearing in the same
sentence as the direction word exempts a **genuinely stale** directional claim.
Measured against a no-marker control that correctly flags:

| Fixture (`ClaimDue` absent from the file's code, so every claim is false) | Findings |
|---|---|
| control — same false claim, **no marker** | **1 (flags)** |
| `…rather than a bare loop, so ClaimDue below claims the row.` | 0 |
| `…instead of a batch, and ClaimDue below marks it sending.` | 0 |
| `A retry would be wasteful here, so ClaimDue below claims the row…` | 0 |
| `The intake has not called the sweep yet, so ClaimDue below is what claims…` | 0 |

This is calibration, not a defect: criterion 3 of `#0347` **requires** exempting
those markers, and the alternative is concretely worse — it produces false
positives on prose `internal/mailing/outbox_worker.go` writes on `HEAD` today.

**Third false-positive row.** A genuinely historical comment whose only marker
is `never` or `cannot` is flagged, because those two are dropped from the
directional set by design. Three of four such constructed comments are false
positives today; none exists in the tree. The asymmetry is principled and
should be recorded rather than fixed: directional prose routinely asserts what
an identifier does *not* do in the same breath as where it sits — which is
exactly how both real sites survived under the broader marker set. Negation is
load-bearing for the existence axis and disarming for the directional one.

### This issue's second job: retire the prose list

`#0347`'s C2 doc addition **describes** four window limits in prose. The third
review found a fifth, and a future window change will make a sixth — at which
point the list goes stale. That is precisely the failure that produced all
three of `#0347`'s bounces: **a sentence claiming more than the code beside it
delivers.**

So this issue is now explicitly responsible for **replacing C2's prose list
with the fixture table** — making the limits executable rather than described,
and deleting the prose once the fixtures cover it. The `#0330`-style
terminating move here is not to narrow the guard but to stop describing
calibration in prose at all.

**Do not narrow or remove the directional axis.** The same review recorded that
it earns its keep: it is the only instrument that catches `#0342`'s sites A and
C — real, shipped defects invisible to the existence census, to `#0267`'s guard
family, and to three separate human sweeps. Recall 2/2 on the actual defect
population, zero false positives across 30,513 comment groups.


## Relation

- Found by: [#0347](0347.md)'s second review
- Depends on: [#0347](0347.md) — land it first; its directional axis is the
  subject, and its marker regex is still being corrected
- Relates to: [#0342](0342.md) — sites A and C, the real defects this axis
  exists to catch
- Relates to: [#0330](0330.md), [#0354](0354.md), [#0355](0355.md) — the same
  lesson in three other guard families
- Relates to: [#0323](0323.md) — the population-inflation trap criterion 5
  guards against

## Fix

`internal/outbox/claim_comment_referent_guard_test.go` (commit `5ed14b6`):

- **Job 1 — calibration table.** Added
  `claimCommentGuardDirectionalCalibrationCases`, 17 rows over 8 grouped
  shapes, each with its own comment naming which trade it pins and against
  what opposing error (criterion 2):
  - false negative: paren-scoping (1 row) — trades against `store.go:463`'s
    real false positive, named explicitly and pinned by the sibling test
    below.
  - false negative: identifier named after the direction word (1 row).
  - false negative: clause boundary (3 rows — em dash, colon, ASCII `--`).
  - false negative: >90 characters, no boundary (1 row).
  - false negative: marker-exemption surface (5 rows — a no-marker control
    that flags, plus `rather than`/`instead of`/`would`/`has not called`,
    each silently exempting).
  - false positive: non-positional "above"/"below" (2 rows — spatial,
    numeric).
  - false positive: historical comment whose only marker is `never`/`cannot`
    (3 rows flagged, 1 row rescued by `since #`).

  `TestClaimCommentGuardDirectionalAxisCalibrationTable` iterates the table
  via `t.Run`, calling `findClaimCommentGuardFindings` directly (the pure
  core, not the file-walking test) against each in-memory fixture and
  counting directional `ClaimDue` findings. All fixtures are synthetic
  (`ClaimDue` never appears in any fixture's CODE, only in the comment under
  test, so every claim about it is genuinely false) except the
  marker-exemption and false-positive row texts, ~~which are copied verbatim
  from `#0347`'s own review tables (extraction from the issue markdown, not
  retyped from memory).~~ **Correction (#0358, 2026-09-01).** Byte-compared
  against the sources by this issue's second review: the `never`/`cannot`
  false-positive rows (including the `since #` rescue row) are byte-exact
  extractions from `#0347`'s own review tables. The rest — the four
  marker-exemption rows and the two non-positional false-positive rows — are
  those same review-table texts, abbreviated for the fixture (an ASCII
  three-dot ellipsis in place of the source's U+2026, `#0347`'s wording
  truncated, and, for the two non-positional rows, a trailing period the
  source lacks). Functionally harmless — each row was instrumented and still
  exempts/flags for its named reason — but the original claim overstated
  what was done. (The em dash in the clause-boundary row **is** a real
  U+2014, verified as the bytes `e2 80 94`, and is load-bearing — the window
  cuts at it.)

  The two real sites (`#0342` A and C) are **not** duplicated here — they
  stay pinned by the pre-existing
  `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`.

- Added `TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping`,
  criterion 2's explicit ask: it reads the REAL
  `internal/outbox/store.go` from disk (extraction, not a retyped fixture —
  CLAUDE.md §8) and asserts the directional axis produces zero findings for
  `ClaimRow`/`SelectDue` against its real `:463` doc comment — the exact
  false positive `claimCommentGuardOpenParen`'s scoping exists to avoid
  (`#0347`, bounce 1, B1 part 3).

- **Job 2 — retired the prose list.** Replaced the "What this guard cannot
  do" section's four-limit prose paragraph (`#0347`'s C2 addition, already
  proven to understate by one in that issue's own third review) with a
  pointer at the fixture table above, explaining why: a sentence claiming
  more than the code beside it delivers is this guard's own subject class,
  and `#0347`'s three bounces were all instances of exactly that failure.

**Self-inflicted regressions, found and fixed mid-pass** (matching
`#0347`'s own precedent — this guard scans its own file):

1. `TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping`'s first
   draft quoted `store.go`'s real comment verbatim, including "above" next
   to `ClaimRow`/`SelectDue` — which the directional axis correctly flagged
   against itself. Rephrased (named the identifiers, described the
   parenthetical shape, dropped the literal "above") rather than deleted,
   per `#0347`'s own stated convention for this exact situation.
2. A `"store.go:463"` reference in two doc comments tripped
   `TestNoCommentCitesGoFileByLineNumber` (`#0356`/`#0352`'s settled rule
   against line-number citations, which drift silently). Replaced with a
   stable declaration-name reference (`store.go`'s `ClaimRow` doc comment).

Both caught by running the real test suites after each edit, not assumed
clean.

## Verification

Every mutation for criterion 3 ran against a `git archive HEAD` export under
a run-unique scratchpad subdirectory
(`/private/tmp/claude-501/.../scratchpad/0358-work/export-0358`, deleted
after use), never the tracked tree (CLAUDE.md §8b) — confirmed by sha256 of
the tracked file matching before and after every probe.

```
uptime                                    # 0:42-0:55, load averages 1.8-3.5 -- not saturated
git archive HEAD | tar -x -C <export>     # export guard-file sha256 == tracked (5b2abeeb...)
```

**Criterion 3's three mutations, each run against the FINAL committed
version, one at a time, restored from a pristine backup between runs:**

1. **Marker set → broad `claimCommentGuardHistoricalRe`.** Reverts
   `#0347`'s bounce-1 B1-part-1 fix.
   `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`
   FAILS (sites A/C go silent again); this table's `never`/`cannot` false
   positive rows (3 of them) also FAIL (go silent, since the broad set
   exempts them) — two independent instruments catch the same mutation.
2. **Every distinct identifier → nearest-only.** Reverts `#0347`'s
   bounce-1 B1-part-2 fix.
   `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`
   FAILS (site A drops from 2 findings to 1 — masked by the nearer,
   correctly-resolving `OrphanSweep`). This table's own rows do not exercise
   this shape (by design — none of them has two distinct identifiers in one
   window) and correctly stay green; the falsifiability test is the
   discriminator for this direction.
3. **Remove `claimCommentGuardOpenParen`'s clamp entirely.** Reverts
   `#0347`'s bounce-1 B1-part-3 fix.
   `TestClaimCommentGuardDirectionalAxisCalibrationTable`'s paren-scoping row
   FAILS (0 → 1 finding) AND
   `TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping` FAILS
   (0 → 1, reproducing the real pre-`#0347`-bounce-1 false positive on
   `store.go`) — both new tests catch this mutation; the pre-existing
   falsifiability test does not (by design, since sites A/C's clauses are
   not parenthetical). **Correction (#0358, 2026-09-01) — a third catcher,
   found by this issue's second review and re-confirmed independently
   against the real, unmodified `store.go` in a full-tree export**: the
   whole-tree `TestNoCommentClaimsClaimMachineryCallFileCodeLacks` ALSO
   fails under this mutation (it flags store.go:463's real `ClaimRow`
   comment directly, reproducing the same pre-`#0347`-bounce-1 false
   positive against the live tree, not a fixture). The original pass's own
   `## Fix`/`## Verification` recorded only the two dedicated tests; this is
   the same "a sentence claiming more than the code delivers" class this
   whole file exists to close, one level up.

All three restorations verified byte-identical to the pristine committed
file by sha256 before the next mutation.

```
go build ./...                                                # clean
go vet ./...                                                  # clean
gofmt -l internal/outbox/claim_comment_referent_guard_test.go # clean
sha256(file) == sha256(gofmt stdout)                          # equal (80bedcb1...)
ISSUE=0358 scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
  # internal/outbox ok 0.984s, internal/mailing ok 4.025s, internal/handlers ok 7.352s
  # zero skips, VERIFICATION PASSED
scripts/check.sh guards
  # all five harnesses PASS, 0 failures, VERIFICATION PASSED
  # claimKindsGuardMinPlausibleCallSiteCount population = 42 (unchanged)
  # claimKindsGuardMinPlausibleNonExemptCallSiteCount population = 12 (unchanged)
  # claimCommentGuardMinPlausibleFileCount population = 271
  # claimCommentGuardMinPlausibleCommentGroupCount population = 30843
scripts/testdb.sh drop 0358    # already gone, cleaned up by check.sh on exit
pgrep -f 'go test|\.test '     # empty
lsof -nP -iTCP:5173 -sTCP:LISTEN   # empty (checked before scripts/check.sh guards); no pkill -f used
```

Criterion 5's population (42 total / 12 non-exempt) is unchanged from
`#0347`'s third review — this issue's new fixtures use bare comment text with
no `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call syntax and no calls in
their synthetic source, so nothing was added to the grep-based population.

Criterion 6 (extraction, not retyping): the marker-exemption and
false-positive fixture texts were extracted verbatim from `issues/0358.md`
and the cached copy of `#0347`'s third review (`git show` of the issue
markdown), not retyped from memory; `TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping`
reads `store.go` from disk directly rather than embedding any copy of its
text at all.

**ENVIRONMENT HAZARD (#0360), encountered and handled correctly**:
`ISSUE=0358 scripts/check.sh` detected the shared `opencircuit_test` template
was stale relative to this working tree's migrations and fell back to
provisioning `opencircuit_test_0358` directly from `migrations/` (a `check.sh`
mechanism already built for exactly this, not a workaround improvised here) —
all 25 migrations applied cleanly, tests then ran against the correct schema.
No `column ... does not exist` errors.

### Second pass verification (2026-09-01, applying the bounce's B1–B6)

All six remedies applied as transcribed (`commit PENDING`). Each was verified
in a `git archive`/full-tree export under a run-unique scratchpad
subdirectory before being written to the tracked file, and re-verified
against the tracked file afterward; the tracked tree was untouched during
every export-based check (confirmed by diffing the export copies, never the
real `internal/outbox/store.go` or the guard file itself).

**B1 reproduced, then the remedy verified to fix it.** On the pristine
pre-bounce file, mutating the directional window to also look forward 90
characters past the direction word (the exact change the row's comment
claimed to guard against) left `TestClaimCommentGuardDirectionalAxisCalibrationTable`
entirely green, including the `identifier-after-direction-word` row —
confirming the row pinned nothing. After swapping that row's `src` to `"The
row is claimed below by ClaimDue and marked sending."` (name and comment
unchanged, per the remedy), the same forward-window mutation now correctly
fails that one sub-test (`want 0, got 1`) while every other row stays green;
on HEAD (unmutated) the row passes. The added capitalisation pair
(`case-sensitivity control` / `capitalised direction word is invisible`)
both pass on HEAD as specified.

**B5 reproduced, then the remedy verified to fix it.** Rewording
`store.go`'s parenthetical from `(see SelectDue above)` to `(see SelectDue,
declared earlier)` in a private copy left the PRE-bounce
`TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping` still
PASSING — the old `strings.Contains(src, "see SelectDue")` sentinel does not
require the direction word, so the test kept "passing" while pinning
nothing. The replacement `claimCommentGuardStoreGoParenSentinelRe` (`see
SelectDue[\s/]+above`) was verified directly (a standalone Go program) to
match the real, unmodified `store.go` (which wraps the parenthetical across
two comment lines: `"see SelectDue\n// above)"`, confirming the reviewer's
note that a plain `"see SelectDue above"` substring would NOT have matched)
and to stop matching once "above" is reworded away.

**B4's new row verified to discriminate correctly.** On a full-tree copy
with the directional axis's every-distinct-identifier check mutated to
nearest-only, the new `every-distinct-identifier` row fails (`want 1, got
0`) while all 16 other rows stay green — matching the review's measured
table exactly. Unmutated, it passes (1 finding).

**The third catcher (criterion-3 mutation 3) independently re-confirmed**
in a full-tree export: removing `claimCommentGuardOpenParen`'s clamp
entirely fails all three of `TestClaimCommentGuardDirectionalAxisCalibrationTable`'s
paren-scoping row, `TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping`,
AND the whole-tree `TestNoCommentClaimsClaimMachineryCallFileCodeLacks`
(which now reports store.go:463's real `ClaimRow` comment directly). `##
Verification` item 3, above, corrected to record this.

**Mutations 1 and 2 re-run against the fully-edited file (sanity check that
the new rows don't change the existing mutation behaviour B2 describes):**
mutation 1 (broad `claimCommentGuardHistoricalRe` in the directional axis)
fails exactly the three `never`/`cannot` false-positive rows plus the
falsifiability test, with all four marker-exemption rows and the two new
rows (case-sensitivity, every-distinct-identifier) staying green — matching
the corrected B2 doc comment. Mutation 2 (nearest-identifier-only) fails
the falsifiability test and the new every-distinct-identifier row only, with
all 16 other rows staying green.

```
uptime                                     # load 1.68-2.20, not saturated
go build ./...                             # clean
go vet ./...                               # clean
gofmt -l internal/outbox/claim_comment_referent_guard_test.go   # clean
sha256(file) == sha256(gofmt stdout)       # dee64308...e3d84a, equal -- criterion 7 stable
ISSUE=0358b scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
  # internal/outbox ok 0.756s, internal/mailing ok 3.823s, internal/handlers ok 8.001s
  # zero skips, VERIFICATION PASSED
scripts/check.sh guards
  # all guard harnesses PASS, 0 failures, VERIFICATION PASSED
  # claimKindsGuardMinPlausibleCallSiteCount population = 42 (unchanged, criterion 5)
  # claimKindsGuardMinPlausibleNonExemptCallSiteCount population = 12 (unchanged, criterion 5)
  # claimCommentGuardMinPlausibleFileCount population = 271 (unchanged)
  # claimCommentGuardMinPlausibleCommentGroupCount population = 30873 (was 30843 -- +30 from
  #   this pass's added fixture comments/doc text, expected; floor is 500, no concern)
scripts/testdb.sh drop 0358b     # already dropped by check.sh on exit
pgrep -f 'go test|\.test '       # empty
lsof -nP -iTCP:5173 -sTCP:LISTEN # empty, checked before scripts/check.sh guards; no pkill -f used
```

Criterion 5 unchanged: the new fixtures (`The row is claimed below by
ClaimDue...`, the two capitalisation-pair rows, and `var _ = OrphanSweep`)
use bare comment text and a non-`.`-qualified declaration — no
`.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call syntax entered any comment.

Criterion 6: the new fixture texts are plain ASCII, constructed to the exact
shapes B1/B4's remedies specify (verified against the review's own measured
table above), not extracted from another source — no em dash or other
non-ASCII byte is introduced by this pass. The existing em dash
(clause-boundary row) is untouched.

`#0342` sites A and C remain pinned:
`TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7` passes
on HEAD and was re-confirmed to fail under mutations 1 and 2 above. No
production-logic file (`internal/outbox/store.go` or any file outside this
guard test file) was touched by this pass — the diff is confined to
`internal/outbox/claim_comment_referent_guard_test.go`.

## Review notes

> 🔴 **Bounced 2026-09-01.** Job 2 is genuinely done — the four-limit prose list
> is gone and nothing replaces it with another enumeration — and 16 of the 17
> fixtures discriminate for exactly the reason their comment names. But **one
> row pins nothing**: `identifier-after-direction-word` is silent because
> `claimCommentGuardDirWordRe` is case-**sensitive** and its fixture capitalises
> `Below`, so the axis never reaches the window logic at all. Proved: adding a
> forward-looking window — the precise change that row's comment says it guards
> against — leaves the whole calibration table green. That is `#0354`'s finding
> inside the family this issue exists to close. Two further claims in the new
> code are measurably false, both of them this issue's own subject class. Every
> remedy below was proved working in an export before being written; each is
> transcription.

### Verification independently re-run (all of it, not read)

Every mutation ran in a `git archive HEAD` export under a review-unique
scratchpad subdirectory, restored from a pristine copy and sha256-verified
between runs; the tracked tree was never mutated
(`internal/outbox/claim_comment_referent_guard_test.go` sha256
`80bedcb1…bdc290` identical before and after, `git status --porcelain` empty).

```
uptime                                   # 0:57-1:03, load 2.00-2.36 -- not saturated
gofmt -l internal/outbox/                # clean
sha256(file) == sha256(gofmt stdout)     # 80bedcb1...bdc290 both -- criterion 7 STABLE
ISSUE=0358r scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
  # internal/outbox ok 0.759s, internal/mailing ok 3.699s, internal/handlers ok 7.547s
  # (no skips), VERIFICATION PASSED
scripts/check.sh guards                  # 0 failures, VERIFICATION PASSED
  # claimKindsGuardMinPlausibleCallSiteCount           population = 42   <- criterion 5, unchanged
  # claimKindsGuardMinPlausibleNonExemptCallSiteCount  population = 12   <- criterion 5, unchanged
  # claimCommentGuardMinPlausibleFileCount             population = 271
  # claimCommentGuardMinPlausibleCommentGroupCount     population = 30843
```

`:5173` confirmed free with `lsof -nP -iTCP:5173 -sTCP:LISTEN` before the guards
run; no `pkill -f` used. `#0360`'s stale-template fallback fired again and
`check.sh` provisioned `opencircuit_test_0358r` from `migrations/` directly —
all 25 applied, no `column … does not exist`. Export deleted; scratch database
already gone.

**Criterion 3's three mutations, each re-run by this review, one at a time:**

| Mutation | Result | Which assertion caught it |
|---|---|---|
| 1. directional axis uses the broad `claimCommentGuardHistoricalRe` | FAIL | `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7` **and** the table's three `never`/`cannot` **false-positive** rows |
| 2. every distinct identifier → nearest only | FAIL | `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7` **only** |
| 3. remove `claimCommentGuardOpenParen`'s clamp | FAIL | the table's paren-scoping row, `TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping`, **and** the whole-tree `TestNoCommentClaimsClaimMachineryCallFileCodeLacks` |

**Ruling on the every-identifier trade (mutation 2), which this review was asked
to scrutinise.** It is caught — loudly, by name, against the *real* pre-fix file
rather than a fixture — so criterion 3 is met in substance and this is **not** a
`#0355` "pinned by nothing". But the table's own doc comment now bills itself as
"the current, authoritative account of what this axis catches and what it
doesn't", and while this trade is absent from it that framing is an overclaim. A
discriminating row costs three lines and is specified in B4 below; add it.

**Row-by-row spot-check, instrumented rather than counted.** Every row was run
through `findClaimCommentGuardFindings` with the sentence, the directional
marker match, the computed `lo`, *which clamp produced it*, and the window
contents printed. 16 of 17 are silent-or-flagged for exactly the cause their
comment names: the three clause-boundary rows cut at the em dash / colon / `--`
respectively (`lo` = 37 / 34 / 36); the >90-char row cuts at the 90-char cap with
no boundary in between (`lo` = 31); the paren row cuts at the open paren
(`lo` = 25, window `"applied in pass, "`); the four marker rows are exempted by
`rather than` / `instead of` / `would` / `has not called` respectively; the
control and the five false-positive rows all flag with `ClaimDue` genuinely
inside the window and an empty directional-marker match. The seventeenth is B1.

### B1 — BLOCKING: the `identifier-after-direction-word` row pins nothing

`claimCommentGuardDirWordRe` is a word-boundary alternation of `below|above`
with **no** `(?i)` flag — case-sensitive. The row's fixture is `// Below,
ClaimDue claims the row and marks it sending.`, so
`FindAllStringSubmatchIndex` returns **no match at all** and the directional
axis is never entered. The row's comment attributes its silence to the window
looking only backwards (`flat[lo:dm[0]]`); that is not why it is silent.

Proved rather than argued. Mutating the window to also look forward 90
characters past the direction word — exactly the change the row says it guards
against — leaves `TestClaimCommentGuardDirectionalAxisCalibrationTable`
**green**:

```
window := flat[lo:dm[0]]
+ hi := dm[1] + 90; if hi > len(flat) { hi = len(flat) }
+ window += " " + flat[dm[1]:hi]
go test ./internal/outbox/ -run DirectionalAxisCalibrationTable   # ok
```

A lowercase, offset-0 variant is silent too (the backwards window is empty
there), so the fixture cannot separate the two causes in either direction.

This also surfaces a **trade surface nobody has recorded**: a capitalised,
sentence-initial `Below,`/`Above,` is invisible to the axis outright. `#0347`'s
third review probed this same shape and read the result as confirming the
backwards-window limit; it was measuring capitalisation.

**Remedy — replace one `src` and add one discriminating pair, both proved in an
export.** Keep the existing row's name and comment (which then become true) and
swap only its `src`:

```go
	{
		name:                 "identifier-after-direction-word false negative",
		src:                  "package outbox\n\n// The row is claimed below by ClaimDue and marked sending.\n",
		wantClaimDueFindings: 0,
	},
```

Then add, as its own group:

```go
	// FALSE NEGATIVE -- claimCommentGuardDirWordRe is case-SENSITIVE, so a
	// capitalised, sentence-initial direction word is invisible to the axis
	// entirely and its window logic is never reached. TRADE: not a
	// deliberate one -- measured by #0358's review, recorded here rather
	// than fixed, per this issue's criterion 4. The lowercase control
	// carries the identical sentence and flags, so this pair pins the
	// capitalisation surface specifically and nothing else.
	{
		name:                 "case-sensitivity control: lowercase direction word flags",
		src:                  "package outbox\n\n// ClaimDue claims the row below.\n",
		wantClaimDueFindings: 1,
	},
	{
		name:                 "capitalised direction word is invisible to the axis",
		src:                  "package outbox\n\n// ClaimDue claims the row Below.\n",
		wantClaimDueFindings: 0,
	},
```

Measured behaviour of all three, on `HEAD` and under each mutation — each row
moves under exactly one and nothing else:

| Fixture | HEAD | forward-looking window | case-insensitive direction word | nearest-only |
|---|---|---|---|---|
| `The row is claimed below by ClaimDue…` | 0 | **1** | 0 | 0 |
| `ClaimDue claims the row below.` | 1 | 1 | 1 | 1 |
| `ClaimDue claims the row Below.` | 0 | 0 | **1** | 0 |

**Do not make the regex case-insensitive in this pass** — criterion 4. Recorded
for the orchestrator instead: the flag is measurably **free** today
(whole-package run green, whole-tree scan of 271 files / 30,843 comment groups
still 0 findings, `#0342` sites A and C still caught), so it is a genuine
candidate follow-up, not a trade. Reported below.

### B2 — BLOCKING: the calibration table's doc comment misstates which rows catch mutation 1

`TestClaimCommentGuardDirectionalAxisCalibrationTable`'s doc comment says
reverting the directional marker set to the broad
`claimCommentGuardHistoricalRe` "fails both this table's four marker-exemption
rows AND `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`".

Measured: the four marker-exemption rows **stay green** (a broader marker set
keeps them exempt; nothing moves). What fails is the **three `never`/`cannot`
false-positive rows** — because the broad set exempts those, and they expect 1.
The issue's own `## Verification` states this correctly; the file states it
wrongly, and the file is what the next reader has.

A sentence claiming more than the code beside it delivers, in the file whose
subject is that — the same shape as `#0347`'s B4 and C1.

**Remedy (transcription):** replace "fails both this table's four
marker-exemption rows AND" with "fails this table's three `never`/`cannot`
false-positive rows (the four marker-exemption rows stay green — a broader
marker set only exempts more) AND". The commit message carries the same error;
nothing needs doing about that, but do not copy it forward.

### B3 — BLOCKING: the Job-2 pointer claims more than the table establishes

The replacement text in "What this guard cannot do" ends: *"Read the table for
the current, authoritative account of what this axis catches and what it doesn't
-- a green `go test ./internal/outbox/...` after a window-logic change is what
proves the calibration still holds; a sentence here could only ever assert it."*

The second half is **false as measured**: the forward-window change in B1 is a
window-logic change, and the run stays green. The first half asserts
completeness a 17-row table cannot have — and demonstrably does not have, since
its own row 2 walked into a surface it does not name.

Job 2's point was that a described list goes stale while an executable one
cannot. Replacing an enumeration with a **completeness claim** is the same
defect one round later, which is precisely how `#0347` spent three rounds.

**Remedy (transcription):** replace that sentence with

> Read the table for the trade surfaces that are currently pinned, and add a row
> when you move one. A green `go test ./internal/outbox/...` after a
> window-logic change means every surface the table pins still holds -- not that
> no unpinned surface moved; the table is a growing floor under the calibration,
> not an inventory of it.

### B4 — Fix in the same pass: give the every-identifier trade a row

Not a criterion-3 failure (mutation 2 is caught, above), but required to make
B3's rewritten pointer honest. Verified: 1 finding on `HEAD`, **0** under
nearest-only, 1 under both other mutations — it moves under exactly the intended
one. Note the bare `var _ = OrphanSweep`: no `.`-qualified call syntax, so
criterion 5's grep population is untouched (re-confirm 42/12 after adding).

```go
	// TRADE (not a false negative -- the one shape where checking every
	// identifier is load-bearing): a nearer identifier that RESOLVES must
	// not suppress a farther one in the same window that does not. #0347's
	// review (bounce 1, B1 part 2) measured that nearest-only masks #0342
	// site A, where OrphanSweep sits nearer the direction word and does
	// occur later in that file, while the false claim is about ClaimDue,
	// which occurs nowhere in it. Pinned here in-table as well as by
	// TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7,
	// which pins it against the real db9bff7 file.
	{
		name:                 "every-distinct-identifier: a nearer, resolving identifier must not mask a farther, false one",
		src:                  "package outbox\n\n// ClaimDue and OrphanSweep below claim the row.\n\nvar _ = OrphanSweep\n",
		wantClaimDueFindings: 1,
	},
```

### B5 — Fix in the same pass: the `store.go` test's fail-closed sentinel is weaker than the trade it pins

`TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping` reads the real
file from disk (criteria 2 and 6 satisfied — confirmed: `os.ReadFile("store.go")`,
no embedded copy anywhere), and it fails loudly rather than skipping if the file
is unreadable. Good. But its sentinel is `strings.Contains(src, "see SelectDue")`,
which does **not** require the direction word. Reword `store.go`'s parenthetical
to `(see SelectDue, declared earlier)` and the sentinel still matches, the
findings loop finds nothing because there is no longer a direction word, and the
test passes **while pinning nothing** — a silent fail-open in a test whose only
job is to pin a trade.

A subtlety measured before writing this: `store.go` wraps that parenthetical
across two comment lines, so a plain `"see SelectDue above"` substring check
would **not** match today's file. Use a pattern tolerant of the wrap — a regexp
matching `see SelectDue`, then any run of whitespace-or-slash characters, then
`above` — verified to match today's `internal/outbox/store.go` and to stop
matching if the direction word goes. Keep the existing fail-closed message,
adding that the *direction word* is part of what must survive.

### B6 — Fix in the same pass: two small claims in the record that are false

1. **A line-number citation survives**, in the `t.Fatalf` **string literal** at
   the top of that same test — the message names `store.go`'s parenthetical
   comment by a bare line number. `#0356`'s `TestNoCommentCitesGoFileByLineNumber`
   scans comments, so a string literal evades it — but this is the drift-prone
   form `#0352`/`#0356` settled against, and `## Fix` claims the citation was
   replaced. It was replaced in the two doc comments only. Replace the
   line-number phrase with `for ClaimRow`.
2. **`## Fix`'s "copied verbatim … not retyped from memory" is false for five of
   the rows.** Byte-compared against the sources: the four `never`/`cannot` rows
   and the `since #` rescue row **are** byte-exact from `#0347` (lines
   1142-1145). The four marker-exemption rows are not — they substitute an ASCII
   three-dot ellipsis for this issue's own U+2026 `…` and truncate `#0347`'s
   wording, which is literally the "retyped one as ASCII" bug criterion 6 names;
   the two non-positional false-positive rows add a trailing `.` the source
   lacks. **Functionally harmless** — each was instrumented and still
   exempts/flags for its named reason — so change the *claim*, not the fixtures:
   say the `never`/`cannot` rows are byte-exact extractions and the rest are
   review-table texts abbreviated for the fixture, with the abbreviation shown
   not to change the marker match. (The em dash in the clause-boundary row **is**
   a real U+2014, verified as the bytes `e2 80 94`, and is load-bearing — the
   window cuts at it.)

### What passed, and is not to be re-litigated

- **Job 2's mechanical half is done.** The four-limit prose paragraph is gone and
  nothing in the file enumerates the limits any more — confirmed by reading the
  whole "What this guard cannot do" section, not by grep. Only the replacement's
  wording (B3) is at issue.
- **`#0342` sites A and C stay pinned.**
  `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7` is green
  on `HEAD` and fails under mutations 1 and 2. The directional axis was **not**
  narrowed: the diff contains no production-logic change at all — it is a doc
  comment plus new fixtures and tests.
- **Criterion 5**: population **42 total / 12 non-exempt**, unchanged,
  re-measured by the external harness this pass;
  `TestNoClaimMachineryCallSyntaxLiteralInAnyComment` green, so no banned
  call-syntax literal entered any comment.
- **Criterion 7**: `gofmt` stable, file sha256 equals `gofmt` stdout sha256.
- **The self-inflicted-regression handling was right.** Both rephrasings preserve
  what they explain rather than deleting it — the `store.go` test's doc comment
  describes the parenthetical shape and names both identifiers instead of quoting
  the sentence, and the citation became a declaration-name reference. That is
  `#0347`'s stated convention, and the tail risk its third review named (a future
  author *deleting* the sentence, which also goes green) has not materialised
  here. B6.1 is the one place the rephrasing was left half-done.

### Reported to the orchestrator for filing (§9 — subagents report, the orchestrator allocates numbers)

**Title:** `The claim-comment directional axis ignores a capitalised "Below"/"Above" entirely`

`claimCommentGuardDirWordRe` in
`internal/outbox/claim_comment_referent_guard_test.go` matches `below|above`
case-sensitively, so a sentence-initial `Below,` or `Above,` — the single most
natural way to write a forward reference in Go prose — never enters the
directional axis at all. This is not a calibration trade: nothing was gained by
excluding it, and `#0347`'s third review probed exactly this shape believing it
was measuring the backwards-window limit, so the surface has already been
mistaken for a different one once. `#0358`'s review measured the fix as **free**:
adding the case-insensitive flag leaves `go test ./internal/outbox/` green,
leaves the whole-tree scan at 0 findings across 271 files and 30,843 comment
groups, and leaves `#0342` sites A and C caught. Left unfixed under `#0358`'s
criterion 4 (that issue may not change the shapes it pins), so it needs its own
issue: flip the regex, re-measure the whole-tree scan, and turn `#0358`'s
capitalisation row pair into a regression pin for the new behaviour.

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0347`'s second review (§9).
- 2026-08-31 — Amended by the orchestrator from `#0347`'s third review, which
  resolved that issue: a fifth false-negative row (the marker-exemption
  surface), a third false-positive row (`never`/`cannot`), a second job
  (retiring C2's prose limit list in favour of the fixture table), and the
  finding that the axis earns its keep and must not be narrowed. Title changed
  to match the widened scope.
- 2026-09-01 — Implementation (Sonnet, fresh subagent): added the calibration
  table (17 fixtures over 8 trade surfaces), the `store.go` extraction test,
  and retired the C2 prose list (commit `5ed14b6`). Proved criterion 3's
  three mutation directions by hand against a throwaway `git archive` export,
  each independently reproducing a `#0347` bounce-1 regression and confirming
  the corresponding fixture(s) fail. Found and fixed two self-inflicted
  regressions (this guard flagging its own new doc comments, and a
  line-number citation tripping `#0356`'s guard) before they reached the
  tracked tree. `ISSUE=0358 scripts/check.sh go ./internal/outbox/...
  ./internal/mailing/... ./internal/handlers/...` and `scripts/check.sh
  guards` both `VERIFICATION PASSED`, zero skips. Population unchanged (42
  total / 12 non-exempt). Left `in-progress` for review per CLAUDE.md §9.
  Token usage/cost — orchestrator to record.
- 2026-09-01 — Review (Opus, fresh subagent): **bounced**. 16 of 17 fixtures
  discriminate for their named cause, verified by instrumenting the axis per
  row; the `identifier-after-direction-word` row does not, and a
  forward-looking-window mutation leaves the whole table green (B1). Two further
  claims measured false (B2, B3), plus three same-pass fixes (B4-B6). All three
  of criterion 3's mutations independently re-run in a `git archive` export and
  each caught; criteria 5 and 7 re-verified (42/12, gofmt stable);
  `ISSUE=0358r scripts/check.sh go ...` and `scripts/check.sh guards` both
  `VERIFICATION PASSED`. **§3 decision: no escalation** — this is the first
  bounce on this issue, and every remedy above is a named replacement string
  with its measured before/after, so the next pass is transcription and stays on
  Sonnet. Token usage/cost — orchestrator to record.
- 2026-09-01 — Implementation, second pass (Sonnet, fresh subagent): applied
  all six named remedies (B1–B6) verbatim. Independently reproduced B1's
  demonstration (forward-looking-window mutation leaves the pristine table
  green) and B5's (rewording `store.go`'s parenthetical to drop "above"
  leaves the pristine sentinel test green) before writing either fix, then
  verified both replacements actually discriminate (fail under the mutation
  they pin, pass on HEAD). B1: swapped the `identifier-after-direction-word`
  row's `src` and added the case-sensitivity control/regression pair. B2:
  corrected the calibration table's doc comment about which rows mutation 1
  fails. B3: replaced the "authoritative account" pointer sentence with the
  reviewer's non-completeness-claiming version. B4: added the
  every-distinct-identifier discriminating row (verified: 1 on HEAD, 0 under
  nearest-only, unchanged under the other two mutations). B5: replaced the
  substring sentinel with a regexp requiring the direction word, tolerant of
  `store.go`'s real two-line wrap (verified against both the wrapped real
  file and the reworded negative case). B6: replaced the `t.Fatalf` string's
  remaining line-number citation with `for ClaimRow`, and corrected `##
  Fix`'s "copied verbatim" claim with a strike-and-date correction
  (`never`/`cannot` rows are byte-exact; the rest are abbreviated review-table
  texts, harmlessly so). Also independently re-confirmed, in a full-tree
  export, the review's "third catcher" finding (mutation 3 also fails the
  whole-tree `TestNoCommentClaimsClaimMachineryCallFileCodeLacks`, not just
  the two dedicated tests) and corrected `## Verification` item 3 to record
  it. Re-ran mutations 1 and 2 against the fully-edited file as a sanity
  check that the new rows don't change the existing mutation behaviour —
  confirmed unchanged. `go build`/`go vet`/`gofmt -l` clean, gofmt-stability
  hash equal. `ISSUE=0358b scripts/check.sh go ./internal/outbox/...
  ./internal/mailing/... ./internal/handlers/...` and `scripts/check.sh
  guards` both `VERIFICATION PASSED`, zero skips. Population unchanged (42
  total / 12 non-exempt); comment-group count moved 30843 → 30873 from this
  pass's own added fixture text (floor is 500, no concern). No
  production-logic file touched — diff confined to
  `internal/outbox/claim_comment_referent_guard_test.go`. Left `in-progress`
  for review per CLAUDE.md §9. Token usage/cost — orchestrator to record.
