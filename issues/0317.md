# 0317 — `source` and `import_id` can now describe different moments

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | subscribers |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |
| **Closed** | 2026-08-27 |
| **Commit** | 3c9eaf8 |

## Description

`#0129` made `RestartSignup` clear `subscribers.import_id`, so a person whose
invite import was revoked and who later signs up on the website is not mistaken
for an unaccepted invitee. That fix is right and its review confirmed all four
misfire consequences fall out of it.

**A side effect: that row keeps `source='import'` and its `source_detail` while
`import_id` goes NULL.** The two fields now describe different moments —
`source` records where the address originally came from, `import_id` records
whether the *current* subscription derives from that batch.

Neither is wrong. `#0129`'s review was explicit that clearing `import_id` moves
`Revoke` targeting in the **right** direction: a resigned-up row escapes a later
revoke, which is the same carve-out PRD §6.10.1 already makes for a confirmed
invitee. And provenance survives on `subscriber_events`.

**What is missing is the decision being written down.** A reader encountering
`source='import'` with a NULL `import_id` has no way to tell whether that is
intentional or a bug, and the natural repair — "re-link them" — would reintroduce
`#0129`'s suppression misfire.

## Resolution notes

> 🟢 Resolved 2026-08-27 — the `Subscriber` struct's provenance block
> now records, at both fields, that `Source`/`SourceDetail` are historical
> provenance fixed at INSERT while `ImportID` is a current-state link
> `RestartSignup` clears, names `#0129`, and states outright that re-linking
> them (either direction) reintroduces that issue's suppression misfire.
> Criterion 4's consumer audit was re-derived independently and is
> genuinely **empty**. No behaviour change, as the issue required.

## Acceptance criteria

1. Record, in the code at both fields, what each one means and why they can
   disagree: `source`/`source_detail` are **historical provenance**, `import_id`
   is a **current-state link** whose absence means the subscription no longer
   derives from that batch.
2. State plainly that re-linking them would reintroduce `#0129`'s defect, so the
   next reader does not "fix" it. Name `#0129` so the reasoning is findable.
3. Decide whether anything should read `source='import' AND import_id IS NULL` as
   a meaningful state — a resigned-up former invitee is arguably worth
   distinguishing in the admin UI — and record the answer either way.
4. Check every consumer of `source` for an assumption that `import_id` is set
   alongside it, and report the list even if empty. `#0311` is open on
   `imported_30d`, whose proposed fix is to key on `source = 'import'` — the two
   issues must agree, so read it before deciding.
5. No behaviour change is required by this issue. If you conclude one is needed,
   say so rather than making it silently.
6. `ISSUE=NNNN scripts/check.sh go ./internal/subscribers/... ./internal/handlers/...`
   passes with no skips.

## Relation

- Found by: [#0129](0129.md)'s review
- Caused by: [#0129](0129.md)'s `RestartSignup` clear, correctly
- Relates to: [#0311](0311.md) — whose proposed remedy keys on `source='import'`;
  these two must agree
- Relates to: [#0316](0316.md) — the other consequence of the same clear

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-08-27 | implement (batch with #0311, #0316) | claude-sonnet-5 | — | — |

Implemented as the first of a three-issue batch (`#0317` → `#0311` → `#0316`),
per the batch's own instruction that `#0317` settle semantics before `#0311`'s
predicate is written.

**Criteria 1 and 2 (record the semantics, name #0129).** Rewrote the doc
comment above `Subscriber.Source`/`SourceDetail`/`ConsentBasis`/`ImportID`/
`InvitedAt` in `internal/subscribers/store.go` (the block already covering
both fields) to state plainly: `Source`/`SourceDetail` are historical
provenance, fixed at INSERT and never rewritten (verified by grepping this
file for an UPDATE that touches `source` — there is none); `ImportID` is a
current-state link that `RestartSignup` clears (named, with a pointer to that
method's own `#0129` doc comment) when a row's current subscription no longer
derives from the batch. Spelled out explicitly that re-linking them —
whether by having `RestartSignup` leave `ImportID` set, or by having `Revoke`
key off `Source` instead of `ImportID` — reintroduces `#0129`'s suppression
misfire.

**Criterion 3 (is `source='import' AND import_id IS NULL` meaningful).**
Recorded: yes, it is a real and nameable state (a resigned-up former
invitee), distinguishable from an ordinary website signup by
`ConsentBasis == ConsentBasisDoubleOptIn` alongside it. No new admin-UI
filter or badge was built for it — no acceptance criterion here asked for
one, and `#0316` already restores the batch link at the event level, which is
the concrete "provenance stays reachable" need the two sibling issues share.

**Criterion 4 (every consumer of `source`).** Grepped every non-test read of
`.Source` / `SubscriberSourceImport` / `.ImportID` in `internal/handlers` and
`internal/subscribers`. None assume `ImportID` is set alongside `Source`:
`Confirm`, `Unsubscribe`, and `pending.go`'s invited-view all key the
"unaccepted invitation" inference off `ImportID != nil && ConsentBasis ==
nil` directly, never off `Source`. `admin_subscribers.go` and
`admin_pending.go` serialize both fields independently without cross-reading
one from the other. **List: empty** — nothing needed a change for this
criterion. Read `#0311` before deciding, per the issue's own instruction;
`#0311`'s revised predicate keys on `Source`, matching this issue's
"historical provenance" framing (see `#0311`'s own Work log for why
`Source` alone needed a `confirmed_at IS NULL` guard to stay mutually
exclusive with `confirmed_30d` — an addition, not a disagreement, with this
issue's semantics).

**Criterion 5 (no behaviour change).** None made by this issue. `#0311`'s
`Growth30Days` predicate change is that issue's own acceptance criterion, not
a side effect of this one.

## Verification

Own database (`ISSUE=0311 scripts/testdb.sh create 0311`, shared across the
batch's three issues, dropped at the end via `scripts/check.sh`'s own
cleanup). No migration — no schema change.

```
ISSUE=0311 scripts/check.sh go ./internal/subscribers/... ./internal/handlers/...
ISSUE=0311 scripts/check.sh web
```

Go: `internal/subscribers` ok (0.542s), `internal/handlers` ok (4.733s), zero
skips. Web: `svelte-check` 290 files / 0 errors / 0 warnings; `vitest` 40
files / 3986 tests passed. `gofmt -l` clean on both touched packages.

## Review notes

> 🟢 Reviewed 2026-08-27 (Opus, phase 3) — approved.

### Criteria 1 and 2 — the note is at both fields and reads as a decision

The `#0317` block sits inside the single doc comment that introduces
`Source`, `SourceDetail`, `ConsentBasis`, `ImportID`, and `InvitedAt`
(`internal/subscribers/store.go:222–271`), immediately above the field
declarations — so it is at *both* fields by construction, not duplicated in
two places that can drift apart. It reads as a settled decision rather than an
observation: it says what each field answers, states the two concrete repairs
a future reader would reach for ("having `RestartSignup` leave `ImportID`
set", "having `Revoke` key off `Source` instead of `ImportID`"), and says what
each would break, naming `#0129` and pointing at `RestartSignup`'s own doc
comment. It also says what is *not* lost — `subscriber_events` keeps the batch
link — with a forward pointer to `#0316`. A reader arriving at
`source='import'` with a NULL `import_id` cannot mistake it for a bug.

The premise the note asserts is independently verified: every `UPDATE
subscribers` statement in the tree (`store.go` ×11, `pending.go` ×2,
`imports.go` ×1) was read; none has `source` in its `SET` list, and no
migration backfills it. `RestartSignup`'s `UPDATE` names eleven columns
explicitly and `source` is not among them.

### Criterion 4 — the empty audit, re-derived

Repeated from scratch rather than taken on trust. Every non-test read in
`internal/` and `cmd/`:

**`Subscriber.Source`** — exactly one consumer:
`internal/handlers/admin_subscribers.go:288`, which serialises it into
`subscriberView.Source` as a passthrough. It infers nothing about `ImportID`.
(The other `.Source` hits are `Import.Source` / `CommitInput.Source` — the
*batch's* origin vocabulary, `luma|eventbrite|meetup|manual_csv|other`, a
different column on a different table.)

**`SubscriberSourceImport`** — two writers (`imports.go:620` and the invite
branch's INSERT) and one reader, `Growth30Days`'s new predicate. No inference.

**`Subscriber.ImportID`** — `pending.go:170`, `store.go:813` (`Confirm`'s
invite-accept), `store.go:1024` (`Unsubscribe`'s invite-decline), the
`confirmed_count` increment at `store.go:842–850`, and
`admin_subscribers.go:238`'s serialisation. Every one of the inference sites
tests `ImportID != nil && ConsentBasis == nil` directly — none reaches for
`Source`, and the `*updated.ImportID` dereference is inside the `!= nil`
guard.

**List: empty.** Confirmed. The two issues agree: `#0311` keys `imported_30d`
on `Source` precisely because it is the historical-provenance field, which is
what this issue's semantics licence.

### Criterion 3 — recording the state without building UI is the right call

`source='import' AND import_id IS NULL` is recorded as a real, nameable state
(a resigned-up former invitee, distinguishable by `ConsentBasis ==
ConsentBasisDoubleOptIn`), with the two operator questions the two fields
answer spelled out. Building an admin filter for it now would be speculative:
nothing in the tracker asks to segment on it, no operator has hit it (nothing
is live), and `#0316` restores the concrete need — reaching the originating
batch from a subscriber's detail screen — at the event level, which is where
the durable record actually is. Recording the decision is what criterion 3
asked for, and it is recorded in the code rather than only in the issue, which
is where the next reader will be.

### Criterion 5 and 6

No behaviour change: the diff for this issue is comment-only.
`ISSUE=0311r scripts/check.sh go ./internal/subscribers/... ./internal/handlers/...`
— `internal/subscribers` ok 0.684s, `internal/handlers` ok 4.828s, **no
skips**, `gofmt -l` clean (the block is a declaration doc comment, so the
gofmt smart-quote hazard in `CLAUDE.md` §8 applies — it contains no `''` or
doubled-backtick spans, and `gofmt -l` reports nothing).
