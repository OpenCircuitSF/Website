# 0465 — Production enablement for workshop image upload: directory writability and `ReadWritePaths=`

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | deploy / tooling |
| **Phase** | 9 — Reusable as a dependency |
| **First seen** | 2026-09-06 |

## Description

`#0433`'s planning pass found that the Go service **cannot write to
`/var/www/media` today, for two independent reasons** — and that fixing either
one alone changes nothing:

1. **Ownership.** The directory is `ec2-user:ec2-user 0755`; the service runs as
   `uid=990(opencircuit)` with no supplementary groups.
2. **`ProtectSystem=strict`** in `opencircuit.service` makes the whole hierarchy
   read-only **regardless of mode**. Correcting the group and mode without
   touching this leaves the write still failing, and the failure would look like
   a permissions bug that had just been fixed.

This issue is that enablement, split out of `#0433` on the planning pass's own
recommendation, so correct code is not stalled behind an approval it does not
need. `#0433`'s server and SPA work is fully provable locally with a temporary
directory.

## What it asks the user for

One question, answerable in one read:

> May we make two changes to production — give `/var/www/media` group ownership
> `opencircuit` with mode `2775` (setgid), and add `ReadWritePaths=/var/www/media`
> to `opencircuit.service`?

**Existing files, their URLs, and the `scp` workflow are unaffected**: the change
is to the directory's group and mode, not to the files' ownership or their `0644`
mode. The setgid bit is what keeps newly written files group-owned by
`opencircuit` so both paths keep working.

## Acceptance criteria

1. **Nothing runs on the box without the user's explicit approval** (`CLAUDE.md`
   §5b, §9). If approval does not come, the prepared instruction is the
   deliverable and this closes as prepared-not-run — the shape `#0447` used.
2. The exact commands, in order, with what each does and what to expect:
   the `chgrp`/`chmod`, the `ReadWritePaths=` edit, `daemon-reload`, and the
   service restart. State plainly that a restart briefly interrupts the site.
3. **Verify the existing images afterwards by hash, not by status code.** That is
   §7's standing lesson from the `/.well-known/` carve-out: a `200` proves
   something answered, not that the right bytes did. Both files' SHA-256 must be
   unchanged, and `#0417` recorded their sizes and dimensions.
4. The unit change belongs in `deploy/systemd/opencircuit.service` in the repo,
   not only on the box, so a rebuilt server gets it — the same rule `#0447`
   followed for the certbot hook.
5. **Do not weaken `ProtectSystem=strict` itself.** `ReadWritePaths=` is the
   narrow, intended escape hatch; changing the mode of the whole protection is
   not.
6. Confirm the service still starts and the site still serves before declaring
   success, and record what you ran.

## Relation

- `#0433` owns the server, SPA and `docs/media.md` work, and is not blocked by
  this — it proves against a temporary directory.
- `#0434` backs up `/var/www/media`; check whether the group change affects it.
- `#0447` is the precedent for a prepared-not-applied production change.
- `CLAUDE.md` §7 records the box; §5b requires clearing approval-gated steps in
  advance rather than stalling mid-change.

Verify any repo change with `ISSUE=NNNN scripts/check.sh go ./internal/db/... ./internal/handlers/...`. Read-only `ssh ec2` is fine to confirm current state; change nothing without approval.

## Work log

- 2026-09-08 — **Implementation pass (Sonnet), following `#0447`'s
  prepared-not-applied shape.** Confirmed `git status --porcelain` before
  starting: `#0433`'s implementation was mid-flight in `internal/handlers`,
  `internal/media/`, `internal/config`, `cmd/opencircuit`, `.env.example`,
  and `docs/configuration.md` — none of those files were touched.

  **Re-derived both blockers read-only, via `ssh ec2` (`BatchMode=yes`, no
  writes):** they hold exactly as `#0433`'s planning pass measured.
  `/var/www/media` is `ec2-user:ec2-user`, mode `755` (`drwxr-xr-x`, no
  setgid bit); `opencircuit` is `uid=990 gid=990 groups=990(opencircuit)`,
  no supplementary groups; `systemctl show opencircuit.service` reports
  `ProtectSystem=strict` and `ReadWritePaths=` empty; `df -h /` shows 9.1G
  free of 20G (55% used). Also read, for the questions below: `apache` is
  `uid=48 gid=48 groups=48(apache)`; `postgres` is `uid=26 gid=26
  groups=26(postgres)` — neither is nor will become a member of `opencircuit`,
  so both read `/var/www/media` today, and will continue to read it, purely
  through the directory's `other` bits, which `2775` leaves identical to
  `755`'s (`r-x` either way). `systemctl cat opencircuit-backup.service`
  returned "No files found" — `#0434`/`#0435`'s backup unit is not installed
  on the box at all yet, so there is nothing currently running for this
  change to disturb.

  Computed a fresh SHA-256 of both live files (read-only) rather than reuse
  `#0417`'s recorded sizes alone, since `#0417` did not record hashes:
  `programming_leds.jpg` →
  `ad298443d7d4d88ddb05c6825ace4b4c785602bb2002e2bdf313cbe841343d8f`
  (63,253 bytes), `soldering.jpg` →
  `55510a2a2ce82e98df077a2cca7451c1bf2911335dcb1968cc4099e8d03c142e`
  (89,179 bytes). Re-derived both files' dimensions independently by parsing
  each `SOF0` marker directly in `python3` (never a text search) — 600×450
  and 1200×630 respectively, matching `#0417`. Nothing on the box was
  written, renamed, or deleted at any point in this pass.

  **Criterion 4.** Added `ReadWritePaths=/var/www/media` to
  `deploy/systemd/opencircuit.service` with an inline comment explaining
  both blockers and pointing at `docs/deployment.md` §7 for the full
  sequence — so a rebuilt server gets the unit change automatically; only
  the box-side `chgrp`/`chmod`/copy/restart still needs approval and a
  human to run it.

  **Criteria 2, 3, 5.** Wrote the full approval-ready sequence into
  `docs/deployment.md` §7 under "Workshop media upload enablement
  (`#0465`) — pending approval": both read-only findings above, the
  `chgrp`/`chmod`, the unit copy, `daemon-reload`, the restart, and a
  disclosure paragraph stating plainly that `systemctl restart opencircuit`
  (not a reload — `ReadWritePaths=` requires the process to actually
  restart to pick up, unlike Apache's graceful reload in `#0447`) briefly
  interrupts the site, bounded by the unit's own `KillSignal=SIGTERM` /
  `TimeoutStopSec=30` shutdown budget (documented earlier in the same
  section), and that nothing else on the box — Apache, the two ShortLinks
  services, the database — is touched. Added the hash-based verification
  (expected SHA-256 for both files, plus their unchanged `ec2-user:ec2-user
  0644` ownership/mode) and a closing health/HTTPS check. `ReadWritePaths=`
  does not weaken `ProtectSystem=strict` itself — confirmed against
  `systemd.exec(5)`'s own description of the directive as the documented
  exemption mechanism — and the sequence tells the operator to check
  `systemctl show` reports both settings correctly afterward, not just one.
  Added a shorter pointer section to `deploy/systemd/README.md` for anyone
  who reads that file first, cross-referencing `docs/deployment.md` §7
  rather than duplicating the full sequence.

  **Answers to the four questions**, all recorded inline in
  `docs/deployment.md`: setgid's actual effect is on the `scp` path (fixing
  the `ec2-user`-vs-`opencircuit` group split for newly `scp`'d files), not
  on the service's own writes, since its primary group is already
  `opencircuit`; the group change does not affect `#0434`'s backup, because
  `postgres` reads via `other` bits that are unchanged, and the backup unit
  is not installed regardless; `ReadWritePaths=` is the documented narrow
  exemption and leaves `ProtectSystem=strict` itself untouched; the two
  existing files keep their names, ownership, mode, and `cover_image`
  values, proven by hash rather than status code.

  **Nothing was applied to the box.** No SSH write, no `chgrp`, no `chmod`,
  no unit copy, no `daemon-reload`, no restart. The commands above are the
  pending approval request, exactly as `#0447` resolved as prepared, not
  applied.

  Every command block audited as the thing a user pastes: no leading
  indentation on any fenced block (`#0464`'s review caught the opposite
  mistake in a sibling plan), no tabs, no bash-4 constructs, no unquoted
  heredocs.

  Verification:

      uptime
      15:26  up 19 days, 1:36, 29 users, load averages: 5.95 6.40 5.22

      ISSUE=0465final scripts/check.sh go ./internal/db/... ./internal/handlers/...

  **`internal/db` reports `ok` at real elapsed time (0.42–0.59s across four
  separate runs of this pass), zero skips.** `internal/handlers` reports
  `FAIL`, and this needed running down rather than accepted at face value,
  per the prompt's own warning that the shared checkout can go red from
  another agent's in-flight work and that "blame the machine" is the wrong
  instinct even under real load. Isolated with `-run`/`-v` against
  throwaway scratch databases (`scripts/testdb.sh create`/`drop`, four of
  them in sequence as `#0433` kept landing new commits mid-session — not
  the one `check.sh` itself provisions and drops), re-checking
  `git status --porcelain` each time: the failure set **shrank from three
  to two** between the first and last check as `#0433` progressed
  (`TestNoCommentCitesUnresolvedPathOrSection` cleared once `#0433` added
  the `web/src/lib/media.ts` its own new file had been citing ahead of
  time). The two still failing at the final run,
  `TestAdminMedia_DuplicateUploadsConverge` and
  `TestNoAdminFacingStringCitesInternalDocs`, are both inside files
  `git status --porcelain` shows as `#0433`'s — the untracked
  `internal/handlers/admin_media_upload_test.go` and the already-modified
  (before this pass started) `cmd/opencircuit/main.go` — not inside this
  issue's scope (`deploy/systemd/opencircuit.service`,
  `deploy/systemd/README.md`, `docs/deployment.md`, this file). None of
  this pass's changed files are Go source, so they cannot be the cause of
  a Go test failure. Left unfixed and unfiled, since `#0433`'s files are
  off limits to this pass; report only, per the task's own instruction not
  to touch them.

  **No test in either package reads `docs/deployment.md`** — stated
  plainly, per the prompt's instruction, and consistent with `#0447`'s four
  independent byte-scans. `internal/handlers` walks the whole repo tree and
  `internal/db` scans `issues/`, `PRD.md`, `CLAUDE.md`, `migrations/`,
  `docs/database.md` (`CLAUDE.md` §5) — both in scope here because this
  issue file and `CLAUDE.md`-adjacent prose changed. A clean run of
  `internal/db` confirms this issue file (including this Work log, in scope
  since `#0446`) and `deploy/systemd/opencircuit.service`'s new comment
  carry no dangling Go-file-plus-line-number citation or unresolved
  path/section reference. `internal/handlers`'s three failures, being
  entirely inside `#0433`'s in-flight files, say nothing about this issue's
  own changes either way; the substance of this issue's work rests entirely
  on the read-only `ssh ec2` measurements recorded inline, not on this Go
  run.

  Did not touch `#0433`'s files, `issues/0433.md`, or `issues/0435.md`.
  Filed no new issues (`CLAUDE.md` §9). Left the issue's status
  `in-progress` — only the reviewer sets `resolved`.
