# 0211 — The parallelism guard's message does not mention its one over-fire case

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | infra / tests |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-08-23 |
| **Commit** | `00d90fa` |

## Description

`#0209`'s AST guard fails if any `cmd/opencircuit` file calls `<expr>.Parallel()`
with zero arguments. Its phase-3 review probed twelve shapes and found the
boundary well drawn — subtest closures, helper functions, `defer`, `go`,
parenthesised receivers and `*testing.B` are all **caught**, string literals
correctly do **not** fire, and the two misses (a method value `p := t.Parallel`,
and the explicit `(*testing.T).Parallel(t)` form) are curiosities needing
`go/types` to catch.

The one real cost of not type-checking is **over-firing**. The review verified
it by adding a non-test `seedRunner` with its own zero-arg `Parallel()` method:
the guard fires on it. That trade was accepted deliberately and the doc
comment's "Match rule" paragraph anticipates it.

**But the failure message does not.** A future author who gives some unrelated
type a `Parallel()` method meets a wall of prose about `TRUNCATE`, shared
`sessions`/`users` tables, and `registerWiringTest` — none of which applies to
them. The message is otherwise accurate and genuinely teaches; this is the one
reader it misleads.

One clause closes it — something like: *if the receiver below is not a
`*testing.T`, this guard has over-fired; see its Match rule.*

**Two cosmetic slips, recorded here rather than separately.** Neither is worth a
pass on its own; fold them in if the file is open.

- The message says "reproduced empirically in `#0209`" for a measurement
  `#0165`'s review actually made. The pointer still resolves — `#0209` is where
  it is written down — but the attribution is off by one issue.
- `#0209`'s implementation notes say the guard's doc comment mentions
  `t.Parallel()` eight times; it is seven.

## Fix

One clause, inserted into `TestNoWiringTestUsesParallel`'s failure message
(`cmd/opencircuit/wiring_parallel_guard_test.go`), right before the
`Remove t.Parallel() from:` listing so it reads naturally against the
file:line entries that follow it:

> If the receiver below is not *testing.T (or *testing.B), this guard has
> over-fired -- see this file's Match rule.

Also corrected the attribution the issue flagged: "reproduced empirically in
#0209" (crediting the unregistered-fifth-test measurement to the wrong issue)
is now "reproduced empirically by #0165's phase-3 review" — matching both the
doc comment above it (which already attributed correctly: "Its phase-3 review
measured exactly how well that works") and `#0209`'s own Description ("Its
phase-3 review confirmed the hazard by reproduction... It also confirmed the
gap the guard cannot close").

The "eight times" miscount named in this issue's Notes lives in
`issues/0209.md`'s `## Implementation notes`, not in any file this issue is
scoped to touch (`wiring_parallel_guard_test.go`, message only) — the guard
file's own doc comment never states a count, so there was nothing in scope
to correct. Left `issues/0209.md` untouched per this issue's instruction not
to rewrite `#0209`'s `## Review notes` / `## Implementation notes`.

Did not reach for `go/types`. The method-value and method-expression misses,
and the untyped-match trade generally, are `#0209`'s review's settled
decision — this issue only asked for a message clause, not a behavior
change, and the acceptance criteria confirm that ("this is a message
change, not a behaviour change").

## Acceptance criteria

- [x] The failure message tells a reader whose receiver is not a `*testing.T` that the guard has over-fired, and points at the Match rule
- [x] The over-fire case still fails the build — this is a message change, not a behaviour change; the deliberate no-type-check decision stands
- [x] Adding a non-test type with a zero-arg `Parallel()` method produces the new clause — demonstrated by mutation, as `#0209`'s review did with `seedRunner`
- [x] The message stays accurate for the ordinary case: every factual claim still checks out against the code
- [x] The `#0209`/`#0165` attribution is corrected
- [x] `#0209`'s twelve boundary shapes still produce the same verdicts, and its four-probe claim-release arrangement still holds
- [x] `ISSUE=0211 scripts/check.sh go ./cmd/... ./internal/...` passes with zero skips

## Notes

Reported by `#0209`'s phase-3 review under `CLAUDE.md` §9.

The review's twelve-shape probe table is in `#0209`'s `## Review notes` and is
the regression suite for criterion 6. Worth reading before touching the message:
it records *why* each miss is acceptable, so a well-meaning attempt to close the
method-value gap with `go/types` would be re-litigating a settled decision.

Keep the message's teaching quality. `#0165`'s review established that the guard
itself is the discovery path — someone reaching for `t.Parallel()` learns the
constraint from the failure, not from a comment they were never going to read.
The fix here is one clause for a different reader, not a rewrite.

## Relation

- Found by: [#0209](0209.md) — the AST parallelism guard
- Relates to: [#0165](0165.md) — the runtime guard it complements
- Relates to: [#0181](0181.md) — the AST-walking shape both copy

## Verification

`uptime` before the run: load averages 1.65/2.06/2.16 — well under the ~4
idle ceiling, so nothing below is machine noise.

- `go build ./cmd/...`, `go vet ./cmd/...`, `gofmt -l` on the edited file —
  all clean.
- `go test ./cmd/opencircuit/... -run 'TestNoWiringTestUsesParallel|TestParallelGuard' -v`
  — 4/4 pass unchanged (the real guard plus its three synthetic-fixture
  proofs).
- **Over-fire mutation** (the acceptance criterion 3 proof): added a
  temporary non-test file defining `type seedRunner struct{}` with a
  zero-arg `Parallel()` method and one call site, mirroring `#0209`'s
  review. `TestNoWiringTestUsesParallel` failed, naming
  `zzz_0211_falsepos.go:8:2: <expr>.Parallel()` and the full message
  included the new clause verbatim: *"If the receiver below is not
  *testing.T (or *testing.B), this guard has over-fired -- see this file's
  Match rule."* File removed afterward; `git status --porcelain
  cmd/opencircuit/` clean.
- **Ordinary-case mutation** (criterion 4 proof): recorded `shasum -a 256`
  of `events_wiring_test.go`
  (`0b1aeec2772c06d1c92651143c68f1c9a8e8c24bda65b9ac7d103e0b7c9fd391`), added
  `t.Parallel()` after its `registerWiringTest(t)` call. Guard failed,
  naming `events_wiring_test.go:50:2: t.Parallel()` inside the same full
  message — the new clause is present but plainly inapplicable, since the
  receiver is `t` in a `*_wiring_test.go` file. Restored the file
  (`git checkout --`, my own edit this session) and reverified `shasum -a
  256` byte-identical.
- **Regression shapes** (criterion 6, a subset of `#0209`'s twelve): added a
  temporary test file exercising `scanFileForParallelCalls` directly against
  three synthetic sources — a `t.Run` subtest closure (**caught**, 1
  violation), a string literal containing the text `t.Parallel()`
  (**correctly not caught**, 0 violations), and a method value
  `p := t.Parallel; p()` (**still a miss**, 0 violations, matching the
  accepted trade). All three passed. File removed afterward.
- **Four-probe claim-release regression** (criterion 6, `#0165`'s
  arrangement): added four temporary tests in declaration order — A calls
  `registerWiringTest(t)` then `t.Fatal`s holding the claim, B calls it then
  `t.Skip()`s holding it, C and D call it and pass. Result:
  `--- FAIL: TestZZZProbeA`, `--- SKIP: TestZZZProbeB`,
  `--- PASS: TestZZZProbeC`, `--- PASS: TestZZZProbeD` — identical to
  `#0209`'s and `#0165`'s result; the claim still releases via `t.Cleanup`
  on failure and skip alike. File removed afterward.
- `git diff` confirms the code commit (`00d90fa`) touches exactly one file,
  one hunk: the attribution fix and the new clause in
  `TestNoWiringTestUsesParallel`'s message string.
- `ISSUE=0211 scripts/check.sh go ./cmd/... ./internal/...` — **PASSED**.
  All 16 packages reported `ok` with real elapsed times
  (0.199s–2.732s), skip audit `(no skips)`, no leftover processes.
  `opencircuit_test_0211` created and dropped by the script; confirmed
  absent from `scripts/testdb.sh list` afterward.
- `git status --porcelain` at the end: only this markdown file (working
  copy, not committed) and the one committed code file — no stray
  probe/mutation files.

## Files changed

- `cmd/opencircuit/wiring_parallel_guard_test.go` — added the over-fire
  clause to `TestNoWiringTestUsesParallel`'s failure message and corrected
  the `#0209`/`#0165` attribution for the unregistered-fifth-test
  measurement.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 64 | 4,733 | 2,249,224 | 88,788 | $1.08 |

**Total: $1.08**
