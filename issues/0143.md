# 0143 — The Announce panel says the draft targets the workshop's interests even when it targets everyone

| | |
|---|---|
| **Status** | resolved |
| **Module** | web |
| **Platform** | All |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |
| **Closed** | 2026-08-22 |
| **Commit** | `3c376d4` |

## Description

`#0056`'s announce shortcut targets the draft campaign at the workshop's interests in `any_of` mode. When the workshop carries **no** interests it falls back to audience mode `all` — every confirmed subscriber — because `any_of` with zero interests is refused store-side.

The fallback itself is correct. `#0056`'s reviewer checked the option space and found it narrower than it looks: the four modes are `all`, `any_of`, `all_of`, and `none_selected`, and `none_selected` is a *real segment* (subscribers who chose no interests), not a blank. So the only alternatives are `all` or refusing outright, and `all` is defensible because `#0047`'s send gate shows the count and a sample and requires a server-verified typed recipient count before anything goes out.

**The defect is the silence.** The panel copy says the draft is "targeted at its interests", which is false in exactly this case. An admin announcing a workshop they never tagged gets a draft aimed at the entire list while being told otherwise.

## Acceptance criteria

- [x] When the workshop has no interests, the Announce panel says plainly that the draft will target **everyone**, before the admin creates it
- [x] The copy shown in the ordinary case still describes what actually happens (`any_of` over the workshop's interests)
- [x] The interest ids are already loaded in `WorkshopEditor.svelte`, so this should stay local — no new API call
- [x] `scripts/check.sh web` passes, with a test covering both copy paths

## Root cause

`WorkshopEditor.svelte`'s Announce panel had one hardcoded sentence — "targeted
at its interests" — that assumed the workshop always has interests. `#0056`'s
`Announce` handler falls back to audience mode `all` when the workshop's saved
`interest_ids` is empty (`any_of` is refused store-side with zero interests),
so in that case the panel's claim was simply false: the admin was told the
draft targeted the workshop's interests while it actually targeted every
confirmed subscriber, with nothing surfacing the difference before the draft
was created.

## Fix

Added `announceTargetingDescription(interestIds: number[]): string` to
`web/src/lib/workshopAdmin.ts` — the copy decision lives there, not as an
inline ternary in the component, per `CLAUDE.md`'s SPA-logic convention. It
returns one of two sentences depending on whether `interestIds` is empty:

- Zero interests: `"This workshop has no interests set, so the draft will
  target everyone on the list."`
- One or more interests: `"It will be targeted at this workshop's
  interests."`

`WorkshopEditor.svelte`'s Announce panel now interpolates that sentence
between the existing "Create a draft campaign…" and "Nothing is sent…"
sentences.

**Deliberate choice on which interest ids to pass in:** the function is
driven by `workshop.interest_ids` (the last-loaded/saved value on the
`AdminWorkshop` object), **not** `fields.interestIds` (the local, possibly
unsaved checkbox buffer). `POST /admin/workshops/{id}/announce` takes no
request body — it reads the persisted row server-side — so if an admin has
toggled interest checkboxes but not yet clicked Save, the copy must still
describe what Announce will actually do (the saved state), not the pending
edit. This matches the issue's framing: the defect is the panel lying about
what actually happens, and only the saved value is what actually happens.

No targeting logic was touched — `internal/handlers/admin_workshop_announce.go`
and the `all`/`any_of` fallback are unchanged, per the issue's explicit
instruction not to undo `#0056`'s reviewer-approved fallback.

## Verification

`uptime` before starting: `19:10 up 2 days, 5:21, load averages: 1.54 1.51
1.70` — well under `CLAUDE.md` §5's ~8 threshold.

`ISSUE=0143 scripts/check.sh web`:
- `npm run check` (svelte-check) → `228 FILES 0 ERRORS 0 WARNINGS`.
- `npm test` (vitest) → `19 test files, 456 tests, all passing`. (Two other
  in-flight subagents — `#0147` on `internal/handlers/admin_campaigns.go` /
  `CampaignEditor.svelte` — had uncommitted test additions in the working
  tree at the time, which vitest also picks up; this run's own two new tests
  are confirmed below.)
- `VERIFICATION PASSED`.

Re-ran scoped to the new tests directly to confirm they executed and passed
(not just compiled):

```
npx vitest run src/lib/workshopAdmin.test.ts -t "announceTargetingDescription" --reporter=verbose
```
→
```
✓ workshopAdmin.test.ts > announceTargetingDescription > says the draft targets everyone when the workshop has no interests
✓ workshopAdmin.test.ts > announceTargetingDescription > says the draft targets this workshop's interests when it has some
Test Files  1 passed (1)
Tests  2 passed | 39 skipped (41)
```

Both copy paths are covered: `announceTargetingDescription([])` asserts
`/target everyone/i`; `announceTargetingDescription([7])` asserts
`/this workshop's interests/i` and asserts it does **not** match `/everyone/i`.

Did not exercise the live component in a browser — the copy logic is fully
covered by the unit tests above and `svelte-check` confirms the template
compiles and types correctly (`workshop.interest_ids` is `number[]` on the
narrowed non-null `AdminWorkshop`, matching `announceTargetingDescription`'s
parameter type). No new server, port, or database access was needed for this
change, so `CLAUDE.md` §8b's port/database cautions don't apply here.

## Files changed

- `web/src/lib/workshopAdmin.ts` — added `announceTargetingDescription`.
- `web/src/lib/workshopAdmin.test.ts` — added its two-case test coverage.
- `web/src/views/admin/WorkshopEditor.svelte` — imported the function and
  wired it into the Announce panel's copy, sourced from `workshop.interest_ids`.

## Notes

Filed from `#0056`'s phase-3 review, which ranked this the highest-value of its three follow-ups and explicitly declined to bounce on it, since `#0047`'s send gate stands between the draft and any real send.

## Relation

- Found by: [#0056](0056.md) — Add the announce-to-list shortcut
- Relates to: [#0047](0047.md) — the send gate that makes this survivable

## Review notes — 2026-08-22 (phase 3, PASSED)

Re-verified independently from the diff at `3c376d4`, not from the drafted
`## Verification`. `uptime` at the start `19:12 up 2 days, load averages: 1.76
1.50 1.66`, never above 2.0 throughout — far below `CLAUDE.md` §5's ~8
threshold, so nothing below is load-contaminated. `issues/0143.md` is the only
file staged.

**How this was verified.** Two other subagents (`#0055`, `#0147`) had
uncommitted work in the tree throughout, so the entire review ran in a
throwaway git worktree checked out at `3c376d4` exactly, with `web/node_modules`
symlinked from the main checkout after confirming `web/package-lock.json` is
byte-identical between the two (`md5` f8fd6847e767dc4330404edb0f798db7 both
sides) — so no install ran and no other agent's `node_modules` was mutated. The
worktree was removed afterwards. The implementer's caveat that its `456` count
was inflated by other agents' uncommitted tests turns out to be unfounded but
harmless: the clean worktree also reports **456**, because `#0147`'s in-flight
test additions are in Go (`admin_campaigns_test.go`,
`admin_workshops_test.go`), which vitest never sees.

### What was re-verified, with observed output

- `scripts/check.sh web` in the clean worktree → `VERIFICATION PASSED`.
  `svelte-check` **228 files, 0 errors, 0 warnings**; `vitest run` **19 files,
  456 tests, all passing**. (`20d540c` / `#0140` is an ancestor of `3c376d4`,
  confirmed with `git merge-base --is-ancestor`, so this `check.sh` is the
  truthful one.)
- The two new tests re-run by name → both pass, 39 skipped in the file.
- **Mutation spot-check.** Flipping the ternary's condition
  (`length === 0` → `length !== 0`), which swaps the two sentences, failed
  **both** tests: `expected 'It will be targeted at this workshop\…' to match
  /target everyone/i` and `expected 'This workshop has no interests set, s…'
  to match /this workshop's interests/i`. Neither assertion is decorative.
  Reverted.

### The saved-vs-unsaved decision — correct, and verified against the handler

The copy is driven by `workshop.interest_ids` (last saved) rather than
`fields.interestIds` (the local checkbox buffer). The implementer's stated
reason was checked against the code rather than accepted:

- `internal/handlers/admin_workshop_announce.go`'s `Announce` reads the
  persisted row — `wk, err := h.store.GetByID(r.Context(), id)` — and grep for
  `r.Body|json.NewDecoder|decodeJSON` in that file returns **nothing**. The
  route genuinely takes no request body.
- The audience decision is made purely from `wk.InterestIDs`: `mode` starts at
  `AudienceAnyOf` and drops to `AudienceAll` with `interestIDs = nil` when
  `len(interestIDs) == 0`.

So the saved value is the only input to the behavior the copy describes.
Sourcing the sentence from `fields.interestIds` would reintroduce exactly the
defect this issue exists to fix, merely pointing the other way — the panel
would promise interest targeting that Announce will not perform. The choice is
right.

Staleness is also bounded: `save()` reassigns `workshop = await
updateWorkshop(...)`, so the sentence updates the moment a save lands.

**The residual UX gap, and why it is not a bounce.** An admin who ticks
interest boxes and does not save sees checked boxes above a sentence saying
"This workshop has no interests set". The sentence is true about what the
button will do, but it reads as contradicting the visible form state. This is
not grounds to bounce: every acceptance criterion is met, the copy never lies
about the action, and this form is explicit-Save by documented design (the
component's own header comment says interest checkboxes edit the local buffer
and are written on the next Save). It is worth a small follow-up — see below.

### The other points checked

- **Targeting logic genuinely untouched.** `git diff 14bdde3 3c376d4 --
  internal/handlers/admin_workshop_announce.go internal/mailing/` is **empty**.
  `#0056`'s reviewer-approved `all`/`any_of` fallback is byte-identical.
- **The copy renders on both paths.** Not rendered in a browser — the project
  has no DOM test environment (no `jsdom`/`happy-dom`/`testing-library` in
  `web/package.json`), and installing one would have mutated the `node_modules`
  two other live agents were using. Proved two other ways instead. (1) The
  panel sits inside the `{:else if workshop}` branch, so `workshop` is narrowed
  non-null there — which is also what `svelte-check`'s 0 errors on
  `workshop.interest_ids: number[]` attests. (2) `npm run build` in the
  worktree, then grep of the emitted bundle: both sentences appear exactly once
  each, shipped as the intact ternary
  (`length===0?"This workshop has no interests set, so the draft will target
  everyone on the list.":"It will be targeted at this workshop's …"`), and the
  old hardcoded `location, targeted at its interests` string is **gone** (0
  matches). For a copy-only change with the decision extracted into a
  unit-tested pure function, that is sufficient.
- **The zero-interest copy is honest.** `internal/mailing/audience.go`'s
  `AudienceAll` adds *no* targeting predicate on top of the base eligibility
  filter `s.status = 'active' AND NOT EXISTS (SELECT 1 FROM suppressions x
  WHERE x.email = s.email)`. "Target everyone on the list" is accurate for what
  an admin means by "the list" — it excludes only `pending` (unconfirmed) and
  suppressed addresses, which no admin counts as on the list. It errs toward
  sounding broader rather than narrower, which is the safe direction for a
  warning. No overstatement that could mislead, no understatement that could
  surprise.
- **Convention.** `announceTargetingDescription` lives in
  `web/src/lib/workshopAdmin.ts` with tests, per `CLAUDE.md` §1's SPA-logic
  rule; the component gained one import and one interpolation and stayed thin.
  No new API call — the ids were already loaded, satisfying criterion 3.

### Recommended follow-up (not blocking, not filed here)

**Flag unsaved interest edits in the Announce panel.** When
`fields.interestIds` diverges from `workshop.interest_ids`, append a clause
such as "Unsaved interest changes are not included — save first." Both values
are already in scope in `WorkshopEditor.svelte`, so it is a local change of the
same size as this one. It closes the last gap between what the panel says and
what the admin is looking at, without weakening the (correct) rule that the
sentence describes the persisted state.

All four acceptance criteria are met and independently confirmed.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 64 | 4,988 | 2,255,606 | 88,304 | $1.08 |

**Total: $1.08**
