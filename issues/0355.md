# 0355 — The alias branch's `*Named`-pointer and declared-interface `continue`s are not pinned by any test

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | seo |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | — |

## Description

`scanInvalidatorSatisfiers`' package-scope-alias branch in
`internal/seo/invalidator_satisfier_guard_test.go` has **three `continue`
exclusions**. `#0353`'s review verified all three are *correct* against
`go/types` truth, and `#0354` pinned the `||` beneath them — but **two of the
three `continue`s are themselves pinned by nothing.**

Measured by `#0354`'s review:

| Mutation | Result |
|---|---|
| C4 — disable the alias-to-pointer-to-`*types.Named` `continue` | **every test in the tree stays green** |
| C5 — disable the declared-interface `continue` | **every test in the tree stays green** |

Under either, the guard begins double-counting a redundant spelling as a fresh
satisfier — the exact failure mode `#0337`'s third review's N2/N3 rulings exist
to prevent.

This is the same family as `#0349` → `#0353` → `#0354`: a check that is correct
while nothing pins the part doing the work.

**One non-obvious point, and it is the reason a naive fixture would not
work.** The pairing is not what it looks like: an alias to a *declared*
interface (`= Seam`) never reaches the interface `continue` at all, because the
`*types.Named` `continue` fires first. **Only an alias to an interface
*literal* exercises it.** A pass that adds `= Seam` and believes it has pinned
the interface case has pinned nothing.

## Acceptance criteria

1. Add to the existing synthetic source in
   `TestInvalidatorSatisfierSet_AliasToTypeLiteral`:
   `type DeclaredSeam interface{ Invalidate() }`,
   `type AliasToPtrNamed = *Site`, and
   `type AliasToIfaceLiteral = interface{ Invalidate() }`.
2. Add two **negative** assertions: `got["AliasToPtrNamed"]` and
   `got["AliasToIfaceLiteral"]` are both false.
3. **Prove the pinning discriminates** — this is the point of the issue, not a
   formality. The suite must pass unmutated, fail under C4 naming
   `AliasToPtrNamed`, and fail under C5 naming `AliasToIfaceLiteral`. Record all
   three results. `#0354`'s review already verified this remedy end to end, so
   reproduce rather than redesign.
4. The third `continue` (alias to a `*types.Named`) is already pinned by
   `#0337`'s N2 ruling — confirm that, and do not add a redundant fixture for
   it.
5. No regression: `#0353`/`#0354`'s M1–M8 still FAIL naming the right type
   (M1 by the guard's own assertion, not `[build failed]`), N1–N3 still PASS,
   and the discriminating single-disjunct mutations from `#0354` still fail
   correctly.
6. The synthetic source must remain **ordinary canonical Go**. `#0354`'s
   reviewer proved this by extracting the `src` constant to a real file and
   running `go build`, `go vet` and `gofmt -l` on it — a fixture that only
   `types.Config.Check` tolerates is a slow drift toward testing something
   else. Repeat that check after adding the three types.
7. Mutations in a `git archive` export or throwaway worktree, never the shared
   tree (§8b), with a whole-tree hash manifest diffed after each.
8. `GOFLAGS=-mod=readonly` on every loader run; `git status -- go.mod go.sum`
   checked before committing (§8b — that trap fired for real in this checkout
   on 2026-08-31).
9. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout (§8).

## Relation

- Found by: [#0354](0354.md)'s review, which resolved that issue and verified
  this remedy end to end
- Depends on: [#0354](0354.md) — the fixtures this extends
- Relates to: [#0353](0353.md) — which added the three `continue`s and verified
  they are correct
- Relates to: [#0349](0349.md) — the `go/types` oracle underneath
- Relates to: [#0337](0337.md) — whose N2/N3 rulings define the correct
  survivals these `continue`s implement
- Relates to: [#0330](0330.md) — the same lesson in the other guard family: a
  check that cannot fail is not a check

## Verification

**What the three new types are for**, worked out before writing assertions
(not transcribed from the trap warning):

- `AliasToPtrNamed = *Site` pins the alias branch's **second** `continue`
  (an alias to a `*types.Pointer` whose element is a `*types.Named` —
  `type X = *Site`). It is a new spelling of `*Site`'s own pointer form,
  already checked under the name `Site`, so it must not be double-counted.
- `AliasToIfaceLiteral = interface{ Invalidate() }` pins the alias branch's
  **third** `continue` (`target.Underlying()` is `*types.Interface`). It has
  to be an interface **literal**, not a declared interface name, because
  `types.Unalias` on an alias to a *declared* interface (e.g. `= Seam`)
  yields a `*types.Named` — the first `continue` (already pinned by
  `AliasToNamed`) fires before the interface check is ever reached.
- `DeclaredSeam interface{ Invalidate() }` is a declared (named) interface
  added specifically to make that trap concrete rather than hypothetical: it
  is not aliased anywhere in the fixture, and exists so the doc comments can
  point at a real counterexample — "aliasing to `DeclaredSeam` would take the
  `*types.Named` continue, not this one" — instead of describing an
  untested claim.

Full detail in the doc comments added to
`internal/seo/invalidator_satisfier_guard_test.go` alongside each type.

**Criterion 3 — the three discriminating results, all reproduced in a
`git archive` export of a `git stash create` snapshot under the scratchpad
(never the shared tree, §8b), `GOFLAGS=-mod=readonly` on every loader run,
whole-tree sha256 manifest of all 270 `.go` files diffed after every
mutation (identical every time — no stray files):**

| Mutation | Result |
|---|---|
| Baseline (unmutated) | `TestInvalidatorSatisfierSet` and `TestInvalidatorSatisfierSet_AliasToTypeLiteral` **PASS** |
| C4 — disable the alias-to-pointer-to-`*types.Named` `continue` (`elemIsNamed && false`) | **FAIL** — "scanInvalidatorSatisfiers reported AliasToPtrNamed … the alias-to-pointer-to-\*types.Named `continue` is missing or broken" |
| C5 — disable the declared-interface `continue` (`isInterface && false`) | **FAIL** — "scanInvalidatorSatisfiers reported AliasToIfaceLiteral … the declared-interface `continue` is missing or broken" |

**Criterion 4 — confirmed, no redundant fixture added.** Also ran C3
(disable the alias-to-`*types.Named` `continue`, the first of the three)
against the unmodified fixture: **FAIL**, naming `AliasToNamed` — already
pinned, exactly as `#0337`'s N2 ruling and `#0353`'s `AliasToNamed` fixture
predict. No fourth type was added for it.

**Criterion 5 — no regression, same export:**

- `#0354`'s two single-disjunct mutations, re-run against the now-five-type
  fixture: `||` → value-form disjunct alone **FAILs** naming
  `AliasValueEmbed` (unchanged from `#0354`'s own result); `||` → pointer-form
  disjunct alone **FAILs** naming `AliasToPointerLiteral` (unchanged) — the
  two new fixtures do not interfere with either.
- Full M1–M8/N1–N3 regression set against the real `internal/seo` package
  (M1 by renaming `Sitemap.invalidate` → `Invalidate` at both call sites in
  `sitemap.go`/`site.go`/`sitemap_test.go`; M2–M8/N1–N3 via a throwaway
  `internal/seo/zz_mut.go`, one mutation at a time, removed after each):

  | # | Mutation | Result |
  |---|---|---|
  | M1 | re-export `Sitemap.invalidate` → `Invalidate` | **FAIL** — `*Sitemap implements …: got true, want false`, by the guard's own assertion, not `[build failed]` |
  | M2 | pointer-receiver `Invalidate()` on a new type | **FAIL**, naming `MutA` |
  | M3 | value-receiver `Invalidate()` | **FAIL**, naming `MutB` |
  | M4 | named struct embedding `*Site` | **FAIL**, naming `MutC` |
  | M5 | named struct embedding an interface field | **FAIL**, naming `MutD` |
  | M6 | `MutFeed[T any]` + pointer-receiver `Invalidate()` | **FAIL**, naming `MutFeed` |
  | M7 | `MutFeed2[K comparable, V any]` two-param receiver | **FAIL**, naming `MutFeed2` |
  | M8 | `= struct{ *Site; cached []byte }` in the real package | **FAIL**, naming `MutAlias` |
  | N1 | declared `interface{ Invalidate() }` | **PASS** — correct survival |
  | N2 | `= Renderer` | **PASS** — correct survival |
  | N3 | `= *Site` | **PASS** — correct survival |

  No regressions. M1's three touched files (`sitemap.go`, `site.go`,
  `sitemap_test.go`) were restored via `git show <snapshot>:<path>` (never
  `git checkout`/`restore`, §8a — this export has no `.git`, so a plain
  re-extract from the snapshot commit was used instead) and confirmed
  byte-identical to the pre-mutation manifest.

**Criterion 6 — canonical Go, re-checked.** Extracted the `src` constant
(now 3,407 bytes, five aliased/declared types) to a standalone
`synthetic.go` in its own throwaway module: `go build ./...` OK, `go vet
./...` OK, `gofmt -l` clean, and `sha256(file) == sha256(gofmt stdout)`.
Ordinary, canonically-formatted Go — not a shape only `types.Config.Check`
tolerates.

**Criteria 7–8.** All mutation work above ran in a `git archive` export
under the scratchpad, never the shared tree or `/private/tmp/oc-live`
(untouched). `GOFLAGS=-mod=readonly` set for every `go build`/`go vet`/`go
test` invocation, including inside the export. `git status -- go.mod
go.sum` clean before and after every step in the real tree; both files
untouched.

**Criterion 9 — gofmt stability.** `sha256(file) ==
sha256(gofmt stdout)` =
`1f63a2f2c18b4f129dec1f1d6329089d1637bfd99b83b981827b7302530846b7` both
sides; `gofmt -l` reports nothing on the real-tree file.

**Full scoped suite:** `GOFLAGS=-mod=readonly ISSUE=0355 scripts/check.sh go
./internal/seo/... ./internal/handlers/... ./cmd/...` → `go build`, `go
vet`, `gofmt -l` all clean; `go test` reports `ok` for `internal/seo`
(0.705s), `internal/handlers` (6.245s), `cmd/opencircuit` (6.884s); skip
audit `(no skips)`; no leftover processes; `VERIFICATION PASSED`. `uptime`
load average was ~2.7–3.4 throughout (well under CLAUDE.md §5's ~8
threshold; load had spiked to 13.29 earlier in the session and was allowed
to drain before any timed run). Scratch database `opencircuit_test_0355`
dropped by `check.sh`'s own exit trap; `scripts/testdb.sh list` shows no
`0355` entry.

## Files changed

- `internal/seo/invalidator_satisfier_guard_test.go` — added `DeclaredSeam`,
  `AliasToPtrNamed`, and `AliasToIfaceLiteral` to the synthetic source in
  `TestInvalidatorSatisfierSet_AliasToTypeLiteral`, with two negative
  assertions (`got["AliasToPtrNamed"]` and `got["AliasToIfaceLiteral"]` both
  false) and doc comments explaining what each type pins and why the
  declared-vs-literal interface distinction matters.

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0354`'s review (§9).
- 2026-08-31 — Phase 2 (Sonnet). Added the three types and two negative
  assertions to the synthetic fixture. Proved discrimination in a `git
  archive` export (never the shared tree): C4 fails naming `AliasToPtrNamed`,
  C5 fails naming `AliasToIfaceLiteral`, and confirmed (without adding a
  redundant fixture) that the first `continue` is already pinned by the
  existing `AliasToNamed` fixture. Re-ran `#0354`'s single-disjunct
  mutations and the full M1–M8/N1–N3 regression set against the real
  `internal/seo` package — no regressions. Extracted the `src` constant to a
  standalone file and confirmed `go build`/`go vet`/`gofmt -l` all clean.
  Left status `in-progress` for review (§9 — implementer never sets
  `resolved`).
