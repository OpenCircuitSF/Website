# 0138 — Cover images are a URL field with no upload path, and the validator admits protocol-relative off-site URLs

| | |
|---|---|
| **Status** | open |
| **Module** | handlers / web |
| **Platform** | All |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |

## Description

Two related gaps in how `#0052`'s admin UI handles a workshop cover image.

**No upload path exists.** The editor accepts a path or URL as text because there is no multipart or upload route anywhere in the API — `#0052`'s reviewer confirmed that. The criterion reads "upload **or** path entry", so it is arguably satisfied as worded, but an admin currently has no way to get an image onto the server through the product.

**The validator admits off-site URLs.** `isSafeCoverImage` accepts a protocol-relative `//host/path`, which loads an asset from an arbitrary third-party host. `CLAUDE.md` §9 says the site carries no external CDNs and is self-contained by design; a cover image pulled from someone else's host is exactly that, plus a referer leak on every page view.

## Acceptance criteria

- [ ] `isSafeCoverImage` rejects protocol-relative `//host/…` URLs, and the rejection is tested
- [ ] The full set of what it accepts is written down and matches §9 — decide explicitly whether absolute `https://` URLs to other hosts are allowed at all, or whether cover images must be same-origin
- [ ] Decide whether an upload endpoint is in scope for v1. If yes, file or extend an issue for it with storage location, size and type limits, and how the stored path reaches the workshop row. If no, strike `#0052`'s criterion with a note saying path entry is the intended v1 behavior
- [ ] `#0052`'s cover-image criterion ends up ticked or struck, not floating
- [ ] The same decision covers **link destinations in Markdown body text**, not just cover images: `renderMarkdownPreview` accepts `//evil.host/x` and `\\evil.host/x`, which resolve off-site. `#0052`'s pass-2 review found this while probing the hardened scheme check and noted one ruling should settle both
- [ ] `scripts/check.sh web` passes with the new validator tests

## Notes

Filed from `#0052`'s phase-3 review. The protocol-relative hole is the part worth fixing regardless of how the upload question resolves — it is a one-line validator change plus a test, and it is the half that contradicts a stated project rule rather than merely leaving a convenience unbuilt.

## Relation

- Found by: [#0052](0052.md) — Build the workshops admin UI
- Relates to: [#0051](0051.md) — Build the workshops CRUD API
