# 0138 — Cover images are a URL field with no upload path, and the validator admits protocol-relative off-site URLs

| | |
|---|---|
| **Status** | resolved |
| **Module** | handlers / web |
| **Platform** | All |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |
| **Commit** | `84cc179`, `1d2cdd3` |
| **Closed** | 2026-08-22 |

## Description

Two related gaps in how `#0052`'s admin UI handles a workshop cover image.

**No upload path exists.** The editor accepts a path or URL as text because there is no multipart or upload route anywhere in the API — `#0052`'s reviewer confirmed that. The criterion reads "upload **or** path entry", so it is arguably satisfied as worded, but an admin currently has no way to get an image onto the server through the product.

**The validator admits off-site URLs.** `isSafeCoverImage` accepts a protocol-relative `//host/path`, which loads an asset from an arbitrary third-party host. `CLAUDE.md` §9 says the site carries no external CDNs and is self-contained by design; a cover image pulled from someone else's host is exactly that, plus a referer leak on every page view.

## Acceptance criteria

- [x] `isSafeCoverImage` rejects protocol-relative `//host/…` URLs, and the rejection is tested
- [x] The full set of what it accepts is written down and matches §9 — decide explicitly whether absolute `https://` URLs to other hosts are allowed at all, or whether cover images must be same-origin
- [x] ~~Decide whether an upload endpoint is in scope for v1. If yes, file or extend an issue for it with storage location, size and type limits, and how the stored path reaches the workshop row.~~ **Struck: no upload endpoint in v1.** No storage location, size/type limits, or serving path has ever been designed; PRD §5.2 does not ask for one; nothing else in the product uploads a file. Path entry (a site-relative path saved to `cover_image`) is the intended v1 behavior. An upload endpoint deserves its own future issue, not a rider on this one.
- [x] `#0052`'s cover-image criterion ends up ticked or struck, not floating — struck, as above (and per the note directly above it)
- [x] The same decision covers **link destinations in Markdown body text**, not just cover images: `renderMarkdownPreview` accepts `//evil.host/x` and `\\evil.host/x`, which resolve off-site. `#0052`'s pass-2 review found this while probing the hardened scheme check and noted one ruling should settle both — **Note (#0166, 2026-08-22):** `#0136` deleted `renderMarkdownPreview` and the `isSafeLinkHref` call it made over body links, moving workshop body rendering (and, unchanged, `email_campaigns.body_md`) to server-side goldmark. This criterion's *decision* still holds — body links may resolve off-site by design — but the *enforcement mechanism* it named no longer exists: body-link destinations now follow goldmark's own scheme policy (`html.IsDangerousURL`), the same policy `email_campaigns.body_md` has rendered through since `#0042`/`#0043`, rather than a dedicated `isSafeLinkHref` check. `#0166` decided not to rebuild a body-link validator: `//evil.host/x` and `///evil.host/x` both resolve (confirmed via Node's WHATWG `URL`) to `https://evil.host/x`, which this criterion already permits, so enforcing the two/three-slash form against body links grants nothing an ordinary `https://evil.host/x` link didn't already allow — `#0136`'s reviewer reached the same conclusion independently. `data:image/png|gif|jpeg|webp` link destinations also survive as live `<a href="data:…">` (goldmark allowlists those four raster types via `IsDangerousURL`); `data:image/svg+xml` remains refused (re-verified: emits `href=""`), and top-level browser navigation to a `data:` URL is blocked (re-confirmed in real Chromium: clicking an `<a href="data:image/png;base64,…">` throws `Not allowed to navigate top frame to data URL` and the page never leaves `about:blank`), so this is inert in practice, not a live gap. `cover_image` and `signup_url` are unaffected by any of this — both are still enforced by their own Go validators (`isSafeCoverImage`, `isSafeLinkHref`) at the admin API boundary, unchanged by `#0136`. To be explicit about what changed rather than only what was lost: this criterion made **two** decisions — off-site absolute `https://` links are allowed, and the protocol-relative/backslash form is closed. The first still stands; the second is **reversed** for body links by `#0166`, for the reason above. It remains in force for `cover_image` and `signup_url`.
- [x] Validate `cover_image` at the **admin API boundary**, not only in the editor: `internal/seo`'s `absoluteURL` prefixes whatever the column holds without checking it, so a garbage value yields a syntactically invalid `image` URL in the JSON-LD (`#0055`) and in `og:image` today (`#0019`). Found by `#0055`'s phase-3 review
- [x] `scripts/check.sh web` passes with the new validator tests

## Notes

Filed from `#0052`'s phase-3 review. The protocol-relative hole is the part worth fixing regardless of how the upload question resolves — it is a one-line validator change plus a test, and it is the half that contradicts a stated project rule rather than merely leaving a convenience unbuilt.

## Root cause

Three independent gaps, all shaped the same way: a check that tested for the
presence of a scheme/prefix instead of testing for "does this resolve
off-site."

1. **`isSafeCoverImage`** (`web/src/lib/workshopAdmin.ts`) was
   `value.startsWith('/') || isHttpUrl(value)`. A protocol-relative
   `//evil.host/x` starts with `/`, so it passed the first branch — the
   check never distinguished "one leading slash" (root-relative, safe) from
   "two" (network-path reference, off-site).
2. **`isSafeLinkHref`** (`web/src/lib/markdown.ts`) treated "no scheme
   detected" as sufficient for "safe": `HAS_SCHEME` requires a leading
   letter, so `//evil.host/x` (and `\\evil.host/x`, `/\evil.host/x`, which a
   browser's URL parser treats identically to `//evil.host/x` for a special
   http/https base — backslash counts as slash there) fell through to the
   unconditional `return true` for "no scheme = relative = safe." `#0054`'s
   `hasExternalSignup` (`workshopDetail.ts`) calls this same function for
   `signup_url`, so it silently inherited the hole too.
3. **`cover_image` was never checked server-side.** `internal/handlers/admin_workshops.go`'s
   `Create`/`Patch` round-tripped whatever the client sent straight into
   `workshops.CreateInput`/`UpdateInput` with no validation, so the two
   client-side checks above were the *only* defense — any other caller of
   the admin API (a script, a future import route, a bug in the editor)
   could write an arbitrary string into `cover_image`, which `internal/seo`'s
   `absoluteURL` then prefixes unconditionally into `og:image` and the
   JSON-LD `image` field.

## Fix

**Decision — cover images are same-origin only; a leading `/` and nothing
that can resolve off-site.** `CLAUDE.md` §9 ("no external CDNs ...
self-contained by design") and `PRD.md` §6.2's own schema comment
(`cover_image TEXT, -- path under /assets`) both say a cover image belongs
on this site, and an image is a load the page performs on the reader's
behalf without asking — unlike a link, which the reader chooses to follow.
So `isSafeCoverImage` no longer accepts an absolute `http(s)://` URL to
*any* host, including one that looks entirely reasonable
(`https://example.com/cover.jpg` used to pass and no longer does). It now
requires a leading `/`, then rejects a leading `//` after normalizing any
backslash to a forward slash (catching `//evil.host/x`, `\\evil.host/x`,
and `/\evil.host/x` with one rule). Mirrored server-side as
`isSafeCoverImage` in `internal/handlers/admin_workshops.go`, wired into
both `Create` and `Patch` — a bad `cover_image` now gets a 400 before it
ever reaches the store or `internal/seo`.

**Decision — Markdown link destinations (and `signup_url`, which reuses the
same helper) keep allowing an absolute `https://` URL to any host.** A link
is a navigation the reader affirmatively clicks, and a workshop's
`signup_url` is routinely an external ticketing site (Eventbrite etc.), so
restricting links to same-origin would break a legitimate use case §9
isn't actually worried about. `isSafeLinkHref` therefore only closes the
protocol-relative/backslash hole (same normalize-then-check-`//`
technique as the cover-image fix) — it does not touch the existing
http/https/mailto scheme allowlist or the "no scheme = relative, safe"
branch for a genuinely root-relative path like `/workshops/soldering-101`.

**No upload endpoint.** Struck per the acceptance criteria above — path
entry to a site-relative `cover_image` is the intended v1 behavior; an
upload route is future work with its own design questions (storage
location, size/type limits, how the stored path reaches the row) that
don't belong riding along on a validator fix.

### Fix pass 2

Addresses the bounce's two blocking findings.

**Finding 1 — interior control character.** Both `isSafeCoverImage` twins
now reject the whole C0 control range plus DEL (`\x00`–`\x1f`, `\x7f`)
*before* the prefix/backslash-normalize check runs, exactly matching the
reviewer's suggested patch:

```go
// internal/handlers/admin_workshops.go
if strings.ContainsFunc(v, func(r rune) bool { return r < 0x20 || r == 0x7f }) {
    return false
}
```

```ts
// web/src/lib/workshopAdmin.ts
const HAS_CONTROL_CHAR = /[\x00-\x1f\x7f]/;
// ...
if (HAS_CONTROL_CHAR.test(value)) return false;
```

This is the identical rule `markdown.ts`'s `isSafeLinkHref` already uses
(`HAS_CONTROL_CHAR`, from `#0052`'s pass 2) — reused verbatim rather than
writing a third variant of "safe URL", per the reviewer's instruction. Both
doc comments now explicitly cross-reference `markdown.ts`'s rule. No change
was made to `internal/seo`, `og:image`, or the JSON-LD path — the reviewer
verified those stay unaffected, and pass 2 did not re-touch them.

**Finding 2 — `#0052`'s floating criterion.** `issues/0052.md:729`'s "Cover
image upload or path entry" criterion is now struck, with a note pointing at
this issue's same-origin ruling and at `#0153`, which owns the upload
question going forward.

Code commit: `1d2cdd3` — `#0138 Fix: reject a control character disguising a
protocol-relative cover_image`. Touches `internal/handlers/admin_workshops.go`,
`internal/handlers/admin_workshops_test.go`, `web/src/lib/workshopAdmin.ts`,
`web/src/lib/workshopAdmin.test.ts`, and `issues/0052.md` (its criterion edit
rides in this commit, as this markdown change is not itself committed).

## Verification

- `ISSUE=0138 scripts/check.sh go ./internal/...` — zero skips, all
  DB-backed packages (including `internal/handlers`) reported `ok`.
- `scripts/check.sh web` — `npm run check`: 228 files, 0 errors/warnings;
  `npm test`: 19 files, 462 tests passed (includes the new cases below).
- Accept/reject proof by execution (all in the new/updated tests):
  - **Cover image, client (`workshopAdmin.test.ts`)**: rejects
    `//evil.host/x.jpg`, `\\evil.host/x.jpg`, `/\evil.host/x.jpg`, and now
    also `https://example.com/cover.jpg`; still accepts
    `/assets/cover.jpg`.
  - **Cover image, server (`admin_workshops_test.go`)**: `TestIsSafeCoverImage`
    table-tests the same cases directly; `TestAdminWorkshops_CreateRejectsUnsafeCoverImage`
    and `TestAdminWorkshops_PatchRejectsUnsafeCoverImage` prove a real
    `POST`/`PATCH` with `//evil.host/x.jpg` (etc.) gets a real 400 and the
    row is left unchanged; `TestAdminWorkshops_CreateAcceptsSafeCoverImageAndRoundTrips`
    and `TestAdminWorkshops_PatchAcceptsSafeCoverImageAndRoundTrips` prove
    `/assets/workshops/soldering.jpg` round-trips through Create and Patch
    unchanged.
  - **Markdown links (`markdown.test.ts`)**: `isSafeLinkHref` rejects
    `//evil.host/x`, `\\evil.host/x`, `/\evil.host/x`, `\/evil.host/x`;
    still accepts `/img/x.png`, `/workshops/soldering-101`,
    `https://eventbrite.com/e/soldering-101`, and
    `mailto:hello@opencircuitsf.com`. `renderMarkdownPreview` end-to-end:
    `[x](//evil.host/y)` renders as `<p>x</p>` (markup dropped), matching
    the existing `javascript:`/`data:` cases.

### Verification pass 2

Machine idle throughout: `uptime` load average 1.69–2.50 across every run
below, well under the ~8 threshold — nothing here is machine noise.

- **`ISSUE=0138b scripts/check.sh go ./internal/...`** — `ok` for every
  DB-backed package, `internal/handlers 2.248s` (real elapsed time, not an
  0.00s that never reached a database), skip audit `(no skips)`,
  `VERIFICATION PASSED`. Re-run clean after the mutation/restore below.
- **`scripts/check.sh web`** — `npm run check`: `228 FILES 0 ERRORS 0
  WARNINGS`; `npm test`: `19 passed / 464 tests passed` (462 baseline + the
  new six-payload `it` block + the Go-side count is separate). `VERIFICATION
  PASSED`.
- **All six finding-1 payloads now rejected by both twins**, proved by
  execution, not inspection:
  - Go: `TestIsSafeCoverImage` gained six subtests (`tab between slashes`,
    `LF between slashes`, `CR between slashes`, `CRLF between slashes`, `tab
    then backslash`, `LF then backslash`) — all pass. One payload
    (`/\t/evil.host/x.jpg`) also added to
    `TestAdminWorkshops_CreateRejectsUnsafeCoverImage`'s table and to
    `TestAdminWorkshops_PatchRejectsUnsafeCoverImage` as a second PATCH call
    — both prove a real 400 at the API boundary, and Patch's re-fetch proves
    the row is left unchanged.
  - TypeScript: `workshopAdmin.test.ts` gained
    `'rejects a control character disguising an off-site URL'`, looping all
    six payloads through the real exported `validateWorkshopForm`.
- **42-case accept/reject parity re-verified**, not merely assumed still
  true: reimplemented both twins verbatim in a throwaway Go program and a
  throwaway Node script (`scratchpad/parity_check.go`,
  `scratchpad/parity_check.mjs`, not committed), ran the same 42-case list
  the original bounce table enumerates (all legitimate cases — query
  string, fragment, mid-path `//`, `..`, `%5C`, `%2F%2F`, non-ASCII, bare
  `/`, a 10,012-char path, `U+200B`/`U+00A0`/space, and all the
  off-site/scheme/empty cases) plus the six new control-character payloads,
  and diffed the verdicts line-for-line: **42 of 42 matched**. Every
  previously-legitimate case still accepts; every off-site case (old and
  new) now rejects. `\x00`/`\x0b`/`\x0c`/`\x7f` flipped from ACCEPT to
  REJECT on the cover-image side as an intentional side effect of matching
  `markdown.ts`'s full C0+DEL range (documented in `## Fix pass 2` and in
  both source doc comments) — defence in depth, not a behavior change any
  legitimate cover image would trigger.
- **Proved in a real browser, not only Node.** Ground truth for *why* the
  fix matters: using this project's own `playwright-core` install
  (`web/node_modules/playwright-core`) against the cached headless Chromium
  binary at `~/Library/Caches/ms-playwright/chromium-1234`, I served a local
  page (ephemeral port, marker-verified per §8b) rendering the same
  unescaped `<img src={value}>` pattern `WorkshopDetail.svelte:121` uses,
  for each of the six finding-1 payloads, with all `evil.host` requests
  intercepted and aborted rather than sent. Result: all six attempted
  `http://evil.host/x.jpg` — reproducing the reviewer's exact Chromium
  finding. This confirms the payloads are genuinely dangerous (the reason
  the validator fix matters), and the validator-level test suite above
  (executing the real, now-hardened `isSafeCoverImage`/`validateWorkshopForm`)
  proves they can no longer reach that `<img src>` through the product: both
  `Create`/`Patch` 400 before the value reaches the store, and
  `validateWorkshopForm` errors before the editor ever submits it. I did not
  render the live `WorkshopDetail.svelte` component itself in Chromium
  (doing so would require first bypassing the now-fixed validator to get a
  malicious value into a seeded row, which defeats the point of proving the
  fix) — the browser step instead establishes the ground-truth resolution
  behavior the reviewer observed, and the test suite establishes that the
  fixed code refuses to let that value through.
- **Mutation-proved, both twins**, source restored and confirmed
  byte-identical by `shasum -a 256 -c` afterward:
  - Go: short-circuited the new control-char check
    (`if false && strings.ContainsFunc(...)`) — the tail-bounded `go test`
    output showed 5 of the 6 new `TestIsSafeCoverImage` subtests failing
    explicitly (LF/CR/CRLF-between-slashes, tab/LF-then-backslash); the 6th
    (plain tab-between-slashes) is covered independently by
    `TestAdminWorkshops_CreateRejectsUnsafeCoverImage`, which failed on
    exactly that payload with `status = 201, want 400` (cover_image echoed
    back verbatim in the response body) — so all six were proven to depend
    on the new check, between the two test functions.
    `TestAdminWorkshops_PatchRejectsUnsafeCoverImage` also failed, with
    `status = 200, want 400` plus the after-reload check showing the row
    *had* changed. Restored via `cp` from a pre-mutation copy; `shasum -a 256
    -c` confirmed byte-identical to the committed version.
  - TypeScript: same short-circuit
    (`if (false && HAS_CONTROL_CHAR.test(value)) return false;`) —
    `'rejects a control character disguising an off-site URL'` failed
    (`expected false to be true`), the other 44 tests in the file unaffected.
    Restored via `cp`; `shasum -a 256 -c` confirmed byte-identical.
- **No leftover processes or databases**: `pgrep -f 'go test|\.test '`
  empty after every run; `scripts/check.sh` dropped its own `ISSUE=0138b`
  database on exit.
- **Not re-verified in this pass** (unchanged from pass 1, no code touched
  this pass): the link-destination half (`isSafeLinkHref`) and the
  cross-language identity of the *original* 36-case set were re-run as part
  of the 42-case parity check above but not independently re-mutation-tested
  a second time — pass 1's mutation proof for those still stands and this
  pass made no edit to `markdown.ts`.

## Files changed

- `web/src/lib/workshopAdmin.ts` — `isSafeCoverImage` hardened to
  same-origin-only; header comment and the validation error message updated
  to match; upload-endpoint decision recorded in the header comment.
- `web/src/lib/workshopAdmin.test.ts` — cover-image tests updated for the
  new rule (rejects an external `https://` URL; new protocol-relative /
  backslash cases).
- `web/src/lib/markdown.ts` — `isSafeLinkHref` now rejects a leading `//`
  (after backslash normalization) in the no-scheme branch; doc comments
  updated.
- `web/src/lib/markdown.test.ts` — new `describe('protocol-relative
  off-site destinations (#0138)')` block, plus one `renderMarkdownPreview`
  end-to-end case.
- `web/src/views/admin/WorkshopEditor.svelte` — cover-image field label and
  hint text updated to describe the same-origin-only, no-upload rule.
- `internal/handlers/admin_workshops.go` — new `isSafeCoverImage` +
  `coverImageErrorMessage`; wired into `Create` and `Patch` to reject a bad
  `cover_image` with 400 before it reaches the store.
- `internal/handlers/admin_workshops_test.go` — `TestIsSafeCoverImage` plus
  four HTTP-level Create/Patch accept/reject tests (see Verification).

Commit: `84cc179` — `#0138 Fix cover-image and Markdown-link off-site URL validation`

## Gotchas

- **The cover-image and link-href rules are deliberately different, by
  design, not oversight.** A reviewer checking "did this treat both cases
  the same" should find that it didn't, on purpose — see the two
  `## Fix` decisions above. Don't flatten them to match without re-reading
  the reasoning.
- **`isHttpUrl` (workshopAdmin.ts) is untouched** — it still backs
  `signup_url`'s own separate check (not `isSafeLinkHref`) and correctly
  allows any host, matching the "links may be external" decision.
- I did not touch `internal/seo/*`, `web/index.html`, or `web/dist/index.html` —
  another subagent owns those for `#0055`'s re-review, and `git status`
  showed `issues/0055.md`/`issues/0133.md` dirty from concurrent agents at
  various points during this work; neither was staged or committed here.
- `web/dist/index.html` was never touched (no `npm run build` was run in
  this session), so the `#0141` tracked-placeholder concern didn't come up —
  confirmed via `git status --short` before committing, which showed no
  `web/dist/` entry.

## Review notes

> 🔴 **Bounced 2026-08-22.** The asymmetry is the right call and is ratified
> below; the twins agree exactly; the tests have teeth. But the cover-image
> rule does not enforce what all three of its doc comments say it enforces —
> six inputs it accepts make a **real off-site image request**, confirmed in
> Chromium. Code commit `84cc179` stays on `main`; it is an improvement, just
> not a finished one.

Machine idle throughout (`uptime` load average 1.79 at start, 1.15 and 1.21 at
the verification runs — far under the ~8 threshold, so nothing below is machine
noise). Verified in a throwaway worktree detached at `84cc179`, because two
other agents held uncommitted edits to `internal/handlers/admin_workshops.go`,
`internal/handlers/admin_workshops_test.go`, `web/src/lib/workshopAdmin.ts` and
`web/src/lib/api.ts` for the whole of this review. The worktree has been
removed; no scratch database or `go test` process was left behind.

### Ruling on the asymmetry: **correct, and ratified.**

Cover images same-origin-only while Markdown links and `signup_url` may go
anywhere is not an inconsistency to be flattened. The two cases differ in
mechanism, and §9 is about exactly the mechanism that differs:

- An `<img src>` is a **subresource the page fetches automatically**, for every
  visitor, without anyone choosing it — leaking Referer and client IP to a
  third party and creating an availability dependency on a server this project
  does not control. `CLAUDE.md` §9's "no external CDNs … self-contained by
  design" is precisely a rule about automatic third-party fetches, and PRD
  §6.2's own schema comment (`cover_image TEXT, -- path under /assets`)
  independently says the same thing about this column.
- A link is a **navigation the reader affirmatively clicks**. §9 says nothing
  about outbound links, and the product needs them: `#0054`'s
  `hasExternalSignup` renders `signup_url` with `target="_blank"
  rel="noopener noreferrer"`, which is the shape of a deliberate outbound
  link, and a workshop's registration page is routinely Eventbrite.

Dropping the previously-accepted absolute `https://` cover image is therefore
right, and **strands no data**: this is greenfield (`CLAUDE.md` §1), nothing is
live but the static placeholder, `migrations/000020_create_workshops` has no
seed rows, no `cover_image` value appears anywhere in `cmd/`, `scripts/db/` or
the migrations, and `select … from workshops` against the dev database errors
with `relation "workshops" does not exist`. There is no existing absolute
cover-image URL anywhere to be rejected on its next save.

**And the rule is not too tight.** Every legitimate shape I could think of is
accepted, verified by execution against both twins: a query string
(`/img/c.jpg?w=100&u=//evil.host`), a fragment (`/img/c.jpg#//evil.host`), a
`//` mid-path (`/a//b/c.jpg`), interior `..` (`/assets/../img/c.jpg`), an
encoded backslash (`/%5Cevil.host/x.jpg`), encoded slashes
(`/%2F%2Fevil.host/x.jpg`), a non-ASCII path (`/assets/café/über.jpg`), a bare
`/`, and a 10,012-character path. Empty and whitespace-only are treated as
"cleared", identically on both sides.

### The twins do not diverge — verified case-for-case, not assumed

The most likely defect in this change was a TypeScript/Go disagreement, so I
did not eyeball it. I drove **the real exported `validateWorkshopForm`** (which
calls the real `isSafeCoverImage`) under vitest, and **the real unexported
`internal/handlers.isSafeCoverImage`** — through `normalizeOptionalCampaignField`
first, mirroring the handler's own pipeline — under `go test`, over one shared
42-case list, and diffed the verdicts:

```
diff go-verdicts ts-verdicts   →   IDENTICAL on all 42 cases
```

That includes the boundary shapes where the two languages could plausibly
disagree: `String.prototype.trim` vs `strings.TrimSpace` on whitespace-only
input, `replace(/\\/g,'/')` vs `strings.ReplaceAll` on multi-byte input, and
`startsWith` vs `HasPrefix` on the empty string. **No divergence found.** This
part of the change is sound and does not need to be redone.

### Blocking finding 1 — an interior tab, LF or CR still reaches a third-party host

`isSafeCoverImage` (both twins) tests for a *prefix*. A browser's URL parser
**deletes every ASCII tab, LF and CR from the input before parsing it**, so a
control character placed between the two slashes reassembles into exactly the
protocol-relative URL this issue exists to reject:

| `cover_image` value | Both twins | Chromium resolves to |
|---|---|---|
| `/` `<TAB>` `/evil.host/x.jpg` | **ACCEPT** | `http://evil.host/x.jpg` |
| `/` `<LF>` `/evil.host/x.jpg` | **ACCEPT** | `http://evil.host/x.jpg` |
| `/` `<CR>` `/evil.host/x.jpg` | **ACCEPT** | `http://evil.host/x.jpg` |
| `/` `<CR><LF>` `/evil.host/x.jpg` | **ACCEPT** | `http://evil.host/x.jpg` |
| `/` `<TAB>` `\evil.host/x.jpg` | **ACCEPT** | `http://evil.host/x.jpg` |
| `/` `<LF>` `\evil.host/x.jpg` | **ACCEPT** | `http://evil.host/x.jpg` |

Confirmed in a **real browser**, not only against Node's parser: headless
Chromium was pointed at a page rendering the same `<img src={…}>` that
`WorkshopDetail.svelte:121` renders, on an ephemeral port, with server
ownership proved by a per-run marker attribute before trusting any output
(§8b), and all off-origin traffic intercepted and recorded rather than sent.
Chromium **attempted `http://evil.host/x.jpg`**. That is a third-party
subresource fetch on a public page for every visitor — the precise thing §9
forbids and the precise thing this issue was filed to stop.

Three reasons this is a bounce rather than a follow-up issue:

1. **The issue's own `## Root cause` condemns it.** It diagnoses all three
   original gaps as "a check that tested for the presence of a scheme/prefix
   instead of testing for *does this resolve off-site*." The new
   `isSafeCoverImage` makes that identical mistake a fourth time.
2. **Criterion 2 is not met.** "The full set of what it accepts is written
   down and matches §9." What is written down — in the Go doc comment, the
   TypeScript doc comment, and `workshopAdmin.ts`'s module header — is
   "a leading `/` and **nothing else that could resolve off-site**", and
   "SAME-ORIGIN ONLY". Six accepted values resolve off-site. The prose is
   not merely incomplete, it is affirmatively false.
3. **The fix was already in a file this commit edits.** `#0052`'s pass-2
   review found this exact class and armored `isSafeLinkHref` with
   `HAS_CONTROL_CHAR = /[\x00-\x1f\x7f]/`, seven lines above the branch this
   commit changed in `markdown.ts`. `isSafeLinkHref` correctly **rejects** all
   six payloads above; its cover-image sibling does not. The hardening exists,
   was read, and was not carried across.

**Suggested fix** — reject control characters before the prefix test, in both
twins, mirroring `markdown.ts`:

```go
// internal/handlers/admin_workshops.go
if strings.ContainsFunc(v, func(r rune) bool { return r < 0x20 || r == 0x7f }) {
    return false
}
```

```ts
// web/src/lib/workshopAdmin.ts
if (/[\x00-\x1f\x7f]/.test(value)) return false;
```

Add the six payloads to `TestIsSafeCoverImage` and to
`workshopAdmin.test.ts`, and at least one to the HTTP-level
`…RejectsUnsafeCoverImage` pair so the API boundary is covered too. Note that
`\x00`, `\x0b`, `\x0c` and `\x7f` do *not* resolve off-site (a browser
percent-encodes them into the path), so rejecting them is defence in depth
rather than a fix — but a denylist of "just tab/LF/CR" is the same
enumerate-the-bad-spellings trap this issue was filed against, and
`markdown.ts` already chose the whole C0+DEL range. Match it.

**Scope note, so the next pass does not over-fix:** the SEO surface is *not*
affected and needs no change. `internal/seo`'s `absoluteURL` concatenates
base + path, so `https://www.opencircuitsf.com` + `/<TAB>/evil.host/x.jpg`
parses with the authority already fixed as `www.opencircuitsf.com` and the
`//` landing harmlessly in the path — verified. `og:image` and the JSON-LD
`image` field stay same-origin for every payload above. The damage is confined
to the client-side `<img src>`.

### Blocking finding 2 — criterion 4 is ticked but was never done

Criterion 4 requires that "`#0052`'s cover-image criterion ends up ticked or
struck, **not floating**". `issues/0052.md:729` still reads:

```
- [ ] Cover image upload or path entry — path/URL entry only, no upload control; …
```

Unticked, unstruck — still floating. Neither `84cc179` nor `472da85` touched
`issues/0052.md` at all (`git show --stat` on both). The inline note is also
now stale: it says "path/URL entry", and after this change it is path entry
only. Strike that criterion in `0052.md` with a pointer to `#0138`'s ruling,
and correct the note to "path entry only, same-origin".

### What I checked that passed

- **The struck upload criterion carries a real decision record**, not a silent
  tick: criterion 3 states no storage location, size/type limit or serving
  path has ever been designed, that PRD §5.2 does not ask for an upload route,
  that nothing else in the product uploads a file, and that path entry is the
  intended v1 behavior. That is a genuine ruling and I agree with it. One gap:
  it says an upload endpoint "deserves its own future issue" and no such issue
  was filed — see the recommendation below.
- **`scripts/check.sh` verdicts, re-run by me**, truthful post-`20d540c`:
  - `ISSUE=0138rev scripts/check.sh go ./internal/handlers/...` →
    `ok … internal/handlers 2.003s`, skip audit `(no skips)`,
    `VERIFICATION PASSED`. A real 2.003s, not an 0.00s that never reached a
    database.
  - `ISSUE=0138rev scripts/check.sh web` → svelte-check
    `228 FILES 0 ERRORS 0 WARNINGS`; vitest `19 passed / 462 tests passed`.
    Matches the implementer's claimed numbers exactly.
- **The new tests have teeth, proved by mutation**, all three source files
  restored afterwards and confirmed byte-identical by `shasum -a 256 -c`:
  - Weakening the Go guard to `return true` failed `TestIsSafeCoverImage`
    (2 subtests), `TestAdminWorkshops_CreateRejectsUnsafeCoverImage`
    (observed `status = 201, want 400` with the off-site `cover_image` echoed
    in the body) and `TestAdminWorkshops_PatchRejectsUnsafeCoverImage`
    (`status = 200, want 400`). Run on a private database per §8b.
  - The same weakening in both TypeScript twins failed 4 tests, including the
    end-to-end `renderMarkdownPreview` case, which showed the concrete emitted
    markup (`<a href="//evil.host/y" rel="noope…`).
  - These are behavioural assertions on observed output, not restatements of
    the implementation.
- **The 400 path is real and §8b-clean.** Create and Patch both reject with
  400 and leave the row unchanged, against a real Postgres. The tests seed
  throwaway `zz-subtest-workshop-<nanos>` rows and target those — no literal
  or seeded id anywhere, so nothing in a shared database is at risk.
- **No other writer can reach `cover_image` unvalidated.** `CoverImage` is
  written only through `internal/workshops/store.go`'s Create/Update, which
  only `internal/handlers/admin_workshops.go` calls; there is no seeder, no
  import route, and no migration default. With finding 1 fixed, the boundary
  really would be closed.
- **The link half is sound**, and I could not break it from the new angles.
  `isSafeLinkHref` rejects all six control-character payloads, all five
  backslash/protocol-relative spellings, `data:` and `javascript:`, while
  still accepting `https://eventbrite.com/…`, `mailto:`, and root-relative
  paths. `renderMarkdownPreview` emits no `<a>` for any rejected form.
  `signup_url` is gated by `hasExternalSignup` → `isSafeLinkHref` before it
  becomes an `href`, so the asymmetry opens no XSS path.

### Payloads tried, and what each produced

Ground truth is the WHATWG URL parser resolving each value against
`https://www.opencircuitsf.com/workshops/soldering-101`. "Correct?" asks
whether the twins' verdict matches whether the value actually resolves
off-site.

| Payload | Resolves | Cover twins | Link | Correct? |
|---|---|---|---|---|
| `/assets/cover.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/img/c.jpg?w=100&u=//evil.host` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/img/c.jpg#//evil.host` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/a//b/c.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/assets/../img/c.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/%5Cevil.host/x.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/%2F%2Fevil.host/x.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/..//evil.host/x.jpg` | same-origin (`//` stays in the path) | ACCEPT | ACCEPT | ✅ |
| `/.//evil.host/x.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/assets/café/über.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/` + 10,004 chars + `.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/` U+200B `/evil.host/x.jpg` | same-origin (not stripped) | ACCEPT | ACCEPT | ✅ |
| `/` U+00A0 `/evil.host/x.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/` `<SPACE>` `/evil.host/x.jpg` | same-origin | ACCEPT | ACCEPT | ✅ |
| `/` `\x00` `/evil.host/x.jpg` | same-origin | ACCEPT | REJECT | ✅ (defence in depth on the link side) |
| `/` `\x0b` `/evil.host/x.jpg` | same-origin | ACCEPT | REJECT | ✅ |
| `/` `\x0c` `/evil.host/x.jpg` | same-origin | ACCEPT | REJECT | ✅ |
| `/` `\x7f` `/evil.host/x.jpg` | same-origin | ACCEPT | REJECT | ✅ |
| `//evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `\\evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `/\evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `\/evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `/\\evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `///evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `//` `<TAB>` `evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `<TAB>` `//evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `<SPACE>` `//evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `<LF>` `//evil.host/x.jpg` | **off-site** | REJECT | REJECT | ✅ |
| `https://example.com/cover.jpg` | **off-site** | REJECT | ACCEPT | ✅ by design (the asymmetry) |
| `HTTPS://example.com/cover.jpg` | **off-site** | REJECT | ACCEPT | ✅ |
| `http://example.com/cover.jpg` | **off-site** | REJECT | ACCEPT | ✅ |
| `data:image/png;base64,…` | non-http scheme | REJECT | REJECT | ✅ |
| `javascript:alert(1)` | non-http scheme | REJECT | REJECT | ✅ |
| `/` `<TAB>` `/evil.host/x.jpg` | **off-site** | **ACCEPT** | REJECT | ❌ **finding 1** |
| `/` `<LF>` `/evil.host/x.jpg` | **off-site** | **ACCEPT** | REJECT | ❌ **finding 1** |
| `/` `<CR>` `/evil.host/x.jpg` | **off-site** | **ACCEPT** | REJECT | ❌ **finding 1** |
| `/` `<CR><LF>` `/evil.host/x.jpg` | **off-site** | **ACCEPT** | REJECT | ❌ **finding 1** |
| `/` `<TAB>` `\evil.host/x.jpg` | **off-site** | **ACCEPT** | REJECT | ❌ **finding 1** |
| `/` `<LF>` `\evil.host/x.jpg` | **off-site** | **ACCEPT** | REJECT | ❌ **finding 1** |
| `""` | n/a | cleared | REJECT | ✅ |
| `"   "` | n/a | cleared | REJECT | ✅ |

### Worth filing separately (not blocking, not for this pass)

- **`signup_url` has no server-side validation.** `Create`/`Patch` round-trip
  it through `normalizeOptionalCampaignField` with no check, exactly as
  `cover_image` did before this change, so `javascript:alert(1)` can be
  *stored* in the column by any non-editor caller. It cannot currently be
  *rendered* — `hasExternalSignup` gates it through `isSafeLinkHref` first —
  so this is not a live XSS, but it is the same missing-boundary shape
  criterion 6 was written to close, and it leaves the client as the only
  defence. Out of scope here: criterion 6 names `cover_image` only.
- **The cover-image upload endpoint** the struck criterion 3 says "deserves
  its own future issue" — no such issue exists yet. Worth filing with the
  three open design questions criterion 3 already enumerates (storage
  location, size/type limits, how the stored path reaches the row) so the
  decision is not lost.
- **Minor, no issue needed:** the shared `opencircuit_test` database is stale —
  `select … from workshops` there fails with `relation "workshops" does not
  exist`, while a fresh `ISSUE=…` clone of the template has the table. Anyone
  running `internal/handlers` against the default database rather than a
  per-agent one will see spurious failures. `scripts/testdb.sh template`
  clears it; see `docs/obstacles.md`.

## Review notes (pass 2)

> 🟢 **Passed 2026-08-22.** Both bounce findings are closed. All six payloads
> are rejected by both twins, at the rule level and through the real
> Create/Patch and `validateWorkshopForm` pipelines. The rule is the *existing*
> `markdown.ts` one, not a third variant — proved by a codepoint sweep, not by
> reading the diff. And the claim the first pass called affirmatively false —
> "a leading `/` and nothing else that could resolve off-site" — is now true:
> across 367 payloads, **nothing either twin accepts resolves off-site**, in
> Node's WHATWG parser *or* in a real Chromium. Resolved on `84cc179` +
> `1d2cdd3`.

Machine idle throughout (`uptime` load average 1.51–2.02, far under the ~8
threshold, so nothing below is machine noise). Verified in a throwaway worktree
detached at `9ca6472` (the tip of this issue's work, whose tree at
`internal/handlers/admin_workshops.go` and both TypeScript twins is identical to
`1d2cdd3`), on its own `opencircuit_test_0138rev2` database. Worktree removed,
database dropped, `pgrep -f 'go test|\.test '` empty afterwards. `main` was
clean before and after; `#0146`'s reviewer and the `#0144`/`#0145` implementer
were both working the same tree concurrently and nothing of theirs was touched.

Also confirmed the verdict survives the tree moving underneath it: `main`
advanced to `f50bb16` mid-review, and `git diff 9ca6472..f50bb16 --
internal/handlers/admin_workshops.go` is `#0146`'s capacity work only —
`isSafeCoverImage` is byte-identical at HEAD.

### Finding 1 — closed. All six payloads rejected, by execution

Driven against the **real** functions, not reimplementations: the real
unexported `internal/handlers.isSafeCoverImage` under `go test` (both directly
and through `normalizeOptionalCampaignField`, exactly as `Create`/`Patch` call
it), and the real `web/src/lib/workshopAdmin.ts` `isSafeCoverImage` under vitest
(via an `export`-added copy of the module, `diff`-proved to differ from the
committed file by the `export` keyword and nothing else) plus the exported
`validateWorkshopForm` pipeline.

| Payload | Resolves (WHATWG) | Go rule | Go Create/Patch | TS rule | TS `validateWorkshopForm` |
|---|---|---|---|---|---|
| `/` `<TAB>` `/evil.host/x.jpg` | **off-site** | REJECT | 400 | REJECT | error |
| `/` `<LF>` `/evil.host/x.jpg` | **off-site** | REJECT | 400 | REJECT | error |
| `/` `<CR>` `/evil.host/x.jpg` | **off-site** | REJECT | 400 | REJECT | error |
| `/` `<CR><LF>` `/evil.host/x.jpg` | **off-site** | REJECT | 400 | REJECT | error |
| `/` `<TAB>` `\evil.host/x.jpg` | **off-site** | REJECT | 400 | REJECT | error |
| `/` `<LF>` `\evil.host/x.jpg` | **off-site** | REJECT | 400 | REJECT | error |

### Finding 2 — closed

`issues/0052.md:729` now reads a real strike with a real note: the original text
struck through, followed by **Struck:** *"path entry only, same-origin. `#0138`
ruled no upload endpoint ships in v1 and narrowed cover images to a site-relative
path …; the upload question now belongs to `#0153`."* That is the correction the
bounce asked for, including the stale "path/URL entry" wording. `#0153` exists,
is `open`, Phase 7, and owns the upload question with the three design questions
criterion 3 enumerated. `git show 1d2cdd3 -- issues/0052.md` is **one line
changed, one added** — only that criterion, nothing else in that resolved
issue's file.

### The three definitions of "safe URL" are one rule — proved, not asserted

The instruction was to confirm the Go side, the TypeScript cover-image side, and
`isSafeLinkHref` accept and reject *exactly the same* control characters. Rather
than compare source text, I swept every codepoint `0x00`–`0xFF` in an interior
position (`/x<CP>y.jpg`) through all three real functions:

```
go isSafeCoverImage rejects  33 codepoints: 0x00..0x1f, 0x7f
ts isSafeCoverImage rejects  33 codepoints: 0x00..0x1f, 0x7f
ts isSafeLinkHref   rejects  33 codepoints: 0x00..0x1f, 0x7f
go == ts: true    ts == link: true    set == C0+DEL: true
```

Identical, and exactly C0∪{DEL}. The Go spelling
(`strings.ContainsFunc`, `r < 0x20 || r == 0x7f`) and the TS spelling
(`/[\x00-\x1f\x7f]/`) are different expressions of the same set: Go iterates
runes, so a rune ≥ 0x80 (and `RuneError` on invalid UTF-8) can never match; JS
iterates UTF-16 code units, and no surrogate half falls in that range. There is
no third variant.

**Rule-level Go/TS parity over the whole 367-payload set: identical on all
367.** That is the stronger of the two comparisons and it is clean.

### Divergence found, non-blocking: the *pipelines* differ on leading whitespace

12 of 367 payloads get different verdicts from the two **pipelines** (not the
two rules). Every one is Go being *stricter*, and every one is a same-origin
value:

| Shape | Go `Create`/`Patch` | TS `validateWorkshopForm` |
|---|---|---|
| `"   /assets/cover.jpg"` | **400** | accept |
| `"<VT>/assets/cover.jpg"`, `"<FF>/…"` | **400** | accept |
| leading U+00A0 / U+FEFF / U+2028 / U+2029 / U+1680 / U+2000 / U+202F / U+205F / U+3000 | **400** | accept |

Cause: `normalizeOptionalCampaignField` (`admin_campaigns.go:434`) **tests**
`strings.TrimSpace(*v) == ""` but returns the *untrimmed* pointer, so the server
validates the raw string while `validateWorkshopForm` validates
`fields.coverImage.trim()`. It is unreachable through the product — the editor
sends the trimmed value — and it fails safe (the server is never the looser of
the two). Left as a note rather than a bounce or a new issue: it is pre-existing
shared behaviour of a campaign-field helper, not something `#0138` introduced,
and the only user-visible consequence is that a non-editor caller can store a
`cover_image` with *trailing* whitespace. Worth knowing if `#0152` ever
generalises the boundary-validation work.

### New angles tried, and what each produced

The C0+DEL range is now closed, so I went after characters a URL parser might
treat specially that are **not** C0. Ground truth is the WHATWG parser resolving
each value against `https://www.opencircuitsf.com/workshops/soldering-101`.

| Angle | Payload shape | Resolves | Twins | Verdict |
|---|---|---|---|---|
| U+0085 NEL | `/<NEL>/evil.host/x.jpg` | same-origin | ACCEPT | ✅ correct |
| U+00A0 NBSP | `/<NBSP>/evil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| U+200B ZWSP | `/<ZWSP>/evil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| U+FEFF BOM | `/<BOM>/evil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| U+2028 LS / U+2029 PS | `/<LS>/evil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| U+1680, U+180E, U+2000, U+202F, U+205F, U+3000 | interior | same-origin | ACCEPT | ✅ |
| same, before a backslash | `/<NBSP>\evil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| same, in leading position | `<NBSP>//evil.host/x.jpg` | same-origin | REJECT | ✅ (no leading `/`) |
| percent-encoded control | `/%09/evil.host/x.jpg`, `%0A`, `%0D` | same-origin | ACCEPT | ✅ — **the parser does not decode before parsing** |
| percent-encoded, then backslash | `/%09\evil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| double-encoded | `/%2509/evil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| lowercase encoded slashes | `/%2f%2fevil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| HTML character references | `/&#47;evil.host/x.jpg`, `&#x2f;`, `&amp;#47;` | same-origin | ACCEPT | ✅ — attribute escaping keeps them literal |
| Unicode slash lookalikes | `/／/…` (U+FF0F), `/⁄/…` (U+2044), `/∕/…` (U+2215) | same-origin | ACCEPT | ✅ — not slashes to the parser |
| authority-ish punctuation | `/:/evil.host/x.jpg`, `/@evil.host/x.jpg` | same-origin | ACCEPT | ✅ |
| trailing whitespace / NEL | `/assets/cover.jpg   `, `…<NEL>` | same-origin | ACCEPT | ✅ |
| **leading VT / FF** | `<VT>//evil.host/x.jpg`, `<FF>//…` | **off-site** | **REJECT** | ✅ — see below |

**The reviewer's own aside in pass 1 was too generous to the narrow rule.** It
said `\x0b`/`\x0c` "do *not* resolve off-site … so rejecting them is defence in
depth". That is true only in an *interior* position. In **leading** position the
WHATWG parser trims C0-and-space from the front, so `<VT>//evil.host/x.jpg`
resolves to `http://evil.host/x.jpg` — genuinely off-site. Choosing the full
C0+DEL range rather than "just tab/LF/CR" was therefore not merely defence in
depth; it closes two real bypasses the narrow denylist would have missed. The
implementer's insistence on reusing `markdown.ts`'s rule was the right call for
a better reason than either pass gave it.

### Answer to the question the new angles were asked to settle

Pass 1 noted `isSafeLinkHref` correctly *accepts* some non-C0 characters as
relative because WHATWG strips only C0-and-space plus tab/CR/LF. **That
reasoning does transfer to the same-origin image rule, and images do not need a
stricter set.** Empirically: of the 367 payloads, exactly **zero** codepoints
outside C0∪{DEL} produce an off-site resolution from a value that starts with
`/` and has no `//` after backslash normalization. The accept/reject boundary
the two twins draw is the same boundary the URL parser draws.

### Real-browser confirmation — the accepted set, not just the rejected set

Pass 1 rendered the six *rejected* payloads to show they were dangerous. The
question after a fix is the opposite one, so I rendered the **accepted** set:
all 290 payloads that either twin accepts, each into the same
`<img src={…}>` shape `WorkshopDetail.svelte:121` uses, in the project's own
headless Chromium (`chromium-1234`), on an ephemeral port whose ownership was
proved by a per-run marker before any output was trusted (§8b), with every
off-origin request intercepted and **aborted** rather than sent.

```
cases rendered: 296 (290 accepted-by-a-twin, 6 rejected controls)
ACCEPTED payloads that made an off-origin request in Chromium:  NONE.
distinct resolved origins across all accepted payloads: http://127.0.0.1:<port>
rejected controls, force-rendered: all six -> http://evil.host/x.jpg
```

Every value the hardened rule admits stays on the page's own origin in a real
browser. The six controls, force-rendered past the validator, still reach
`evil.host` — reproducing pass 1's finding and confirming the fix is guarding
something real.

### Ruling on the implementer's browser-evidence reasoning: **sound, not circular**

The implementer rendered the `<img src>` *pattern* but not the live
`WorkshopDetail.svelte`, arguing that reaching the component would require first
bypassing the validator it just fixed. That is a correct argument, not a
circular one. The claim under test decomposes into three parts, and each is
established independently:

1. *the pattern is dangerous when fed a bad value* — Chromium, ground truth,
   no reliance on the fix;
2. *the fix refuses those values* — the real validator executed at both the API
   boundary (a real 400 against real Postgres, with the row re-fetched and
   proved unchanged) and in the editor;
3. *nothing else can reach that `<img src>`* — code, re-verified here:
   `cover_image` has exactly one render site in the whole SPA
   (`WorkshopDetail.svelte:121`; `grep` across `web/src` and `internal` finds no
   other), and `CoverImage` is written only via `internal/workshops/store.go`'s
   Create/Update, which only `admin_workshops.go` calls.

Being unable to construct the negative test *because the fix works* is the proof,
not a gap in it. Seeding a malicious row behind the validator's back would have
demonstrated only that the renderer is unescaped — already known, and not what
this change claims to alter. I went one step further than the argument required
by rendering the whole accepted set instead, which tests the fix's actual claim
from the other side.

### What else I checked

- **`scripts/check.sh` verdicts, re-run by me** (truthful post-`20d540c`,
  `#0140`):
  - `ISSUE=0138rev2 scripts/check.sh go ./internal/...` → every DB-backed
    package `ok`, `internal/handlers 2.252s` (real elapsed, not an 0.00s that
    never reached a database), skip audit shows only
    `internal/testdb [no test files]` — a helper package with no tests, not a
    skipped suite — `VERIFICATION PASSED`.
  - `scripts/check.sh web` → svelte-check `228 FILES 0 ERRORS 0 WARNINGS`;
    vitest `19 passed / 464 tests passed`. Matches the implementer's numbers.
- **The new tests bite, proved by mutation**, in the private worktree against my
  own database, both files restored and confirmed byte-identical by
  `shasum -a 256 -c`:
  - Go, `if false && strings.ContainsFunc(...)` → **all six**
    `TestIsSafeCoverImage` subtests fail, plus
    `TestAdminWorkshops_CreateRejectsUnsafeCoverImage` and
    `TestAdminWorkshops_PatchRejectsUnsafeCoverImage`. (The implementer reported
    5 of 6 under the same mutation; I observe 6 of 6. Either way, teeth.)
  - TypeScript, `if (false && HAS_CONTROL_CHAR.test(value))` →
    `'rejects a control character disguising an off-site URL'` fails, the other
    44 tests in the file unaffected.
- **No legitimate case regressed.** All 20 of pass 1's legitimate shapes still
  ACCEPT on both sides — query string, fragment, mid-path `//`, interior `..`,
  `/..//`, `/.//`, `%5C`, `%2F%2F`, non-ASCII (`/assets/café/über.jpg`), a bare
  `/`, a 10,012-character path, and interior ZWSP/NBSP/space. All 14 off-site
  shapes still REJECT.
- **Criteria 2, 4 and 6 ticked.** Criterion 2's prose ("a leading `/` and
  nothing else that could resolve off-site", "SAME-ORIGIN ONLY") is what pass 1
  called affirmatively false; it is now accurate against 367 payloads under two
  independent parsers. Criterion 4 is finding 2. Criterion 6 was already
  satisfied by `84cc179`'s API-boundary check and re-proved here.
- **Pass 1's side findings are filed and need no new issue:** `#0152` owns the
  unvalidated server-side `signup_url`; `#0153` owns the upload endpoint. Both
  are `open`.

### Nothing new to file

The one new observation — `normalizeOptionalCampaignField` not actually trimming
the value it validates — is recorded above and belongs with `#0152` if that work
generalises, not in an issue of its own.

## Relation

- Found by: [#0052](0052.md) — Build the workshops admin UI
- Relates to: [#0051](0051.md) — Build the workshops CRUD API

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 108 | 6,582 | 4,766,680 | 123,050 | $1.99 |
| 2026-08-22 | review (bounced) | claude-opus-5 | 92 | 5,734 | 3,989,709 | 105,275 | $2.79 |
| 2026-08-22 | implement (pass 2) | claude-sonnet-5 | 114 | 5,926 | 5,486,395 | 113,142 | $2.16 |
| 2026-08-22 | review (pass 2) | claude-opus-5 | 104 | 11,544 | 5,369,588 | 124,045 | $3.75 |

**Total: $10.69**
