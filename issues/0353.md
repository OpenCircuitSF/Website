# 0353 — A package-scope alias to a satisfying type literal is invisible to the new `go/types` satisfier oracle

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | seo |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Closed** | 2026-08-31 |
| **Commit** | `88a780c` |

> 🟢 Resolved 2026-08-31 — the `*types.Alias` branch is correct against
> `go/types` ground truth across nineteen alias shapes, the new standing test
> is a genuine (non-circular) oracle for it, and both doc corrections plus the
> `#0319` re-attribution were independently re-verified.

## Description

`#0349` replaced the AST satisfier scan in
`internal/seo/invalidator_satisfier_guard_test.go` with `types.Implements` over
every **named** type in `internal/seo`.

Under `gotypesalias=1` (the default), a package-scope alias resolves to
`*types.Alias`, **not** `*types.Named`, so the guard's type assertion skips it
silently. Therefore:

```go
type FeedCache = struct{ *Site; cached []byte }
```

is a real satisfier of both `handlers.seoCacheInvalidator` and
`mailing.ArchiveCacheInvalidator` — `var _ interface{ Invalidate() } =
FeedCache{}` compiles — while the guard reports `ok`.

**This is a measured loss relative to the guard it replaced.** The retired
`structTypesWithEmbeddedFields` tripwire inspected any `*ast.TypeSpec` whose
`Type` was a struct literal, alias or not. `#0349`'s reviewer ran the retired
file against this identical mutation and it **fails, naming `FeedCache`**.

Not a defect against any of `#0349`'s criteria, which say "every *named* type",
and the construct is exotic — which is why it was handed forward rather than
bounced, matching how `#0346`'s review disposed of `#0350` and `#0351`.

**Note the alias case that is NOT a defect**: `type FeedCache = Renderer` and
`type FeedCache = *Site` are merely new spellings of a type the loop already
checks, and `#0337`'s third review already ruled that a legitimate survival.
Do not "fix" those into failures.

## Root cause

`#0349`'s replacement guard asserted `tn.Type().(*types.Named)` on every
package-scope name. Under `gotypesalias=1` (the Go default), a `type X = ...`
alias declaration's `Type()` returns `*types.Alias`, not `*types.Named`, so
the assertion silently `continue`d past it — including when the alias names a
type literal (`type FeedCache = struct{ *Site; cached []byte }`) that is a
real, compiling satisfier of `interface{ Invalidate() }`. The retired AST
scan caught this construct (it inspected any `*ast.TypeSpec` whose `Type` was
a struct literal, alias or not); the `go/types` replacement did not, because
it only ever asked "is this a `*types.Named`."

Separately, two sentences in the guard's doc comment stated things `#0349`'s
own review had already measured false — "export-data backed" (it
source-type-checks; an export-data-backed load of the same package costs
5.4s/240MB cold against the guard's 1.3s/6.8MB) and "there is nothing left for
a hand-reviewed allowlist to guard" (overstated by exactly this alias case) —
and a third sentence, present byte-parallel in both this file and
`sitemap.go`, overstated the half-invalidation's relationship to `#0319`.

## Fix

**Criterion 1.** Added a `*types.Alias` branch, checked before the
`*types.Named` assertion, inside a new `scanInvalidatorSatisfiers(scope
*types.Scope, invalidateOnly *types.Interface) (got map[string]bool,
concreteTypesChecked int)` helper (factored out of `TestInvalidatorSatisfierSet`
so a second, permanent test can call the identical scanning logic rather than
re-deriving it — CLAUDE.md §8's "a guard's oracle must not be the same bytes
as its subject"). The branch calls `types.Unalias(tn.Type())` and then:

- `continue`s if the result is a `*types.Named`, or a `*types.Pointer` whose
  element is a `*types.Named` — `type FeedCache = Renderer` and `type
  FeedCache = *Site` are new spellings of a type the loop already checks under
  its own name;
- `continue`s if the result's `Underlying()` is `*types.Interface` — an alias
  to a declared interface is a seam, not a concrete type joining one, mirroring
  the existing declared-interface rule for `*types.Named`;
- otherwise increments `concreteTypesChecked` and records the name in `got` if
  `types.Implements(target, invalidateOnly) ||
  types.Implements(types.NewPointer(target), invalidateOnly)` — checking both
  forms because `target` here is not a `*types.Named`, so promotion from an
  embedded *value* field lands only on the pointer form, unlike the
  `*types.Named` case where the pointer form alone is sufficient.

**Standing regression (criterion 3).** Added
`TestInvalidatorSatisfierSet_AliasToTypeLiteral`, which type-checks a small
synthetic in-memory source file (`go/types.Config.Check`, no `packages.Load`,
no dependency on `internal/seo`'s own contents) declaring a `Site` with an
`Invalidate()` method, a `FeedCache` alias to `struct{ *Site; cached []byte
}`, and an `AliasToNamed` alias to `Site` — then calls the shared
`scanInvalidatorSatisfiers` and asserts `FeedCache` is reported (the fix
holds) and `AliasToNamed` is not double-counted under its own name (the N2
survival still holds). Runs in milliseconds and is a permanent part of
`go test ./internal/seo/...`, not something exercised only in a throwaway
mutation worktree.

**Criterion 2.** Rewrote the "export-data backed" parenthetical with the
measured mechanism and numbers (source-type-checked, 1.3s/6.8MB vs.
5.4s/240MB for an export-data-backed load of the same package), and appended
a correction to the "nothing left for a hand-reviewed allowlist to guard"
sentence naming the one remaining gap this commit closes and how (structurally,
via `types.Unalias`, not an allowlist).

**Criteria 9–11 — the `#0319` attribution.** Read `#0319` in full (see
`## Verification` below for what it says). It was filed because
`AdminCampaignArchiveHandler` and the send worker's completion path
invalidated **nothing**, leaving both the sitemap and the per-path meta cache
stale, with the sitemap as the headline symptom (`#0319`'s Description leads
with "`sitemap.xml` keeps advertising a just-**withheld** campaign's URL").
The half-invalidation a bare `*Sitemap` produces is the opposite shape: it
*clears* the sitemap cache and leaves only the meta cache stale — the
meta-cache half of `#0319`'s staleness class, not the sitemap staleness
`#0319` led with, and not identical to what `#0319` was filed to fix. Replaced
the phrase "the exact failure `#0319` was filed to fix" with "the meta-cache
half of `#0319`'s staleness class, not the sitemap staleness `#0319` led
with" — byte-identical in both `invalidator_satisfier_guard_test.go` and
`sitemap.go`, preserving the files' deliberate parallelism. The `sitemap.go`
edit changes no code; verified comment-only with a purpose-built tool (see
`## Verification`).

## Verification

- **Baseline + regression:** `go test ./internal/seo/... -run
  TestInvalidatorSatisfierSet -v` — both `TestInvalidatorSatisfierSet` and the
  new `TestInvalidatorSatisfierSet_AliasToTypeLiteral` `PASS`.
- **Mutation set, in a `git archive` export (never the shared tree, §8b):**
  used `git stash create` (non-destructive — creates a commit object without
  touching the working tree or the stash ref) to snapshot the working tree
  including these uncommitted edits, `git archive`d that object into
  `$SCRATCH/0353-mutate`, confirmed both edited files byte-identical to the
  working copy first, then took a `sha256` manifest of all 270 `.go` files as
  a baseline. Ran, one at a time, each mutation from a script that writes a
  new `.go` file (or edits in place), runs `go test ./internal/seo/... -run
  'TestInvalidatorSatisfierSet$'`, removes/restores the file, and diffs the
  manifest against baseline:

  | # | Mutation | Result |
  |---|---|---|
  | M1 | re-export `Sitemap.invalidate` → `Invalidate`, **both** call sites (`site.go` and `sitemap_test.go`, so the guard's own assertion is what fails, not `[build failed]` — criterion 4's honest form) | **FAIL** — `*Sitemap implements …: got true, want false` |
  | M2 | new type + pointer-receiver `Invalidate()`, new file | **FAIL** — unexpected satisfier |
  | M3 | `func (s Sitemap) Invalidate()` value receiver, new file | **FAIL** |
  | M4 | `struct{ *Site; cached []byte }` embedded struct field | **FAIL** |
  | M5 | `struct{ cacheInvalidator; … }` embedded interface field | **FAIL** |
  | M6 | `FeedCache[T any]` + pointer-receiver `Invalidate()` | **FAIL** |
  | M7 | `FeedCache[K comparable, V any]` two-param receiver | **FAIL** |
  | N1 | `type Invalidator interface{ Invalidate() }` (declared seam) | **PASS** — correct survival |
  | N2 | `type FeedCacheAlias = Renderer` (alias to named satisfier) | **PASS** — correct survival |
  | N3 | `type FeedCacheAlias = *Site` (alias to pointer-to-named) | **PASS** — correct survival |
  | **M8** | `type AliasFeedCache = struct{ *Site; cached []byte }` — **this issue's own target** | **FAIL** — `AliasFeedCache structurally implements interface{ Invalidate() } … but is not in this guard's intended satisfier set` |

  Manifest matched baseline after every one of the eleven mutations (no stray
  files left behind), and the final unmutated re-run still `PASS`ed.

  **Before/after confirmation:** ran the identical M8 mutation against a
  second export of commit `4c649f6` (`#0349`, the pre-`#0353` state) — it
  **PASSes silently**, reproducing the exact defect this issue describes.
  Against the fix, the same mutation **FAILs** naming `AliasFeedCache`. Both
  scratch export directories removed afterward; the main tree was never
  touched (`git status --porcelain` clean before and after, `git stash list`
  empty — `git stash create` never wrote to the stash ref).

- **`GOFLAGS=-mod=readonly`** was set for every loader run (`go build`,
  `go vet`, `go test`, and every mutation run). `git status --porcelain --
  go.mod go.sum` was clean before staging and remains clean after commit — no
  `-mod=mod` promotion occurred.

- **`gofmt` stability:** `sha256(file) == sha256(gofmt stdout)` for both
  `invalidator_satisfier_guard_test.go` and `sitemap.go`; `gofmt -l` on both
  reports nothing.

- **Comment-only proof for `sitemap.go` (criteria 6/11), tool proved
  falsifiable and fail-closed first:** wrote a standalone Go program
  (`go/parser` + `go/format`, mode `0` so comments are parsed but not
  reattached, then re-printed and byte-compared) that reports
  `COMMENT-ONLY: true/false` or errors. Proved it before trusting it: an
  empty input file errors (`fail closed`, exit 2) rather than reporting a
  verdict; two files differing only in a comment report `true`; two files
  differing in a function body report `false` (exit 1) — so the tool can
  actually detect the thing it claims to detect. Run against
  `git show HEAD^:internal/seo/sitemap.go` (pre-this-commit) vs. the working
  file: `COMMENT-ONLY: true`. Run the same tool against the guard test file's
  before/after (which does change code, per criterion 1): `COMMENT-ONLY:
  false`, as expected — confirming the tool isn't trivially reporting `true`
  on everything.

- **Full scoped suite:** `ISSUE=0353 scripts/check.sh go ./internal/seo/...
  ./internal/handlers/... ./cmd/...` → `go build`, `go vet`, `gofmt -l` all
  clean; `go test` reports `ok` for `internal/seo` (0.776s), `internal/handlers`
  (6.138s), `cmd/opencircuit` (6.459s); skip audit `(no skips)`; no leftover
  processes; `VERIFICATION PASSED`. `uptime` load average was 15.50 at the
  start of this run (above CLAUDE.md §5's ~8 threshold — another
  agent/process was active); the run still passed cleanly with no timing
  failures, so per §5 this is noted, not treated as a defect requiring a
  re-run. Scratch database `opencircuit_test_0353` confirmed dropped
  afterward (`scripts/testdb.sh list` shows no `0353` entry).

## Files changed

- `internal/seo/invalidator_satisfier_guard_test.go` — added the
  `*types.Alias` branch (criterion 1), factored the scan into
  `scanInvalidatorSatisfiers`, added `TestInvalidatorSatisfierSet_AliasToTypeLiteral`
  (criterion 3's standing case), corrected the "export-data backed" and
  "nothing left for a hand-reviewed allowlist" doc-comment claims (criterion 2),
  and corrected the `#0319` attribution (criterion 9).
- `internal/seo/sitemap.go` — comment-only: corrected the same `#0319`
  attribution, byte-parallel with the guard test file (criteria 9–11). No code
  changed.

## Gotchas

- The straightforward per-mutation manifest check (`find … | xargs shasum` vs.
  a saved baseline) caught nothing wrong, but is worth naming as the check
  that would have caught a mutation script leaving a stray file behind —
  every one of the eleven restorations verified clean.
- `git stash create` (not `git stash push`/`save`) is the non-destructive form:
  it prints a commit object representing the current index+worktree without
  touching the working tree or adding to the stash ref, so it is safe to use
  even though the working tree carried the uncommitted `#0353` edits at the
  time and other sessions were live in the repo (§8a). Confirmed `git stash
  list` was empty both before and after.

## Acceptance criteria

1. In `TestInvalidatorSatisfierSet`'s scope loop, **before** the
   `*types.Named` assertion, add a `*types.Alias` branch calling
   `types.Unalias`, then:
   - `continue` if the result is a `*types.Named` or a `*types.Pointer` to one
     — a new spelling of a type the loop already checks, and `type FeedCache =
     Renderer` / `type FeedCache = *Site` must keep PASSing;
   - `continue` if its underlying is `*types.Interface` — the declared-seam
     rule, a declared interface is not a concrete type joining a seam;
   - otherwise increment `concreteTypesChecked` and record it in `got` if
     `types.Implements(target, invalidateOnly) ||
     types.Implements(types.NewPointer(target), invalidateOnly)`.
2. Two doc-comment claims that `#0349`'s review **falsified by measurement**
   are corrected in the same pass:
   - the parenthetical "**export-data backed**" is wrong. Measured:
     `GOOS=linux GOARCH=arm64 go list -export -deps ./internal/seo` costs
     **5,406 ms and 240 MB of build cache cold**, against the guard's
     **1,299 ms and 6.8 MB**. It **source**-type-checks. The phrase was
     inherited verbatim from `#0349`'s own criterion 1, so the implementation
     did not introduce it — correct it at the source.
   - "there is nothing left for a hand-reviewed allowlist to guard" is now
     overstated by exactly this alias case.
3. Re-run `#0349`'s full mutation set — the seven from `#0337`'s reviews plus
   the two correct survivals (declared interface, type alias to a named type) —
   and confirm no regression. Add the alias-to-type-literal mutation as a
   permanent standing case.
4. Mutations run in a `git archive` export or throwaway worktree, never the
   shared tree (§8b), with a whole-tree hash manifest diffed after each. Note
   `#0349`'s reviewer found that one mutation staged as described fails as
   `[build failed]` rather than by the guard's verdict — fix the companion call
   site too, so the guard's own assertion is what fails. That is the honest
   form.
5. **`GOFLAGS=-mod=readonly` on every loader run**, and
   `git status -- go.mod go.sum` checked before committing (§8b; the `-mod=mod`
   trap fired for real in this checkout on 2026-08-31).
6. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout (§8).

## Added by `#0351`'s review, 2026-08-31

That review resolved `#0351` and found a third false claim in the same doc
comment — one that must be fixed **in two files at once**.

**`"the exact failure #0319 was filed to fix"` overstates `#0319`.** The phrase
appears in both `internal/seo/invalidator_satisfier_guard_test.go` and
`internal/seo/sitemap.go`, attributing to `#0319` the
sitemap-cleared/meta-stale half-invalidation that a bare `*Sitemap` produces.

But `#0319` was filed because `AdminCampaignArchiveHandler` and the send worker
invalidated **nothing**, leaving *both* caches stale, and its headline symptom
is a stale **sitemap**. The half-invalidation *clears* the sitemap and reopens
only the **meta-cache** half. It is half of `#0319`'s staleness class — and not
the half `#0319` leads with.

The mechanism around it is correct and verified: `site.go:48`'s `Invalidate` is
`renderer.Invalidate()` + `sitemap.invalidate()`, so a bare `*Sitemap` runs only
the second. Only the `#0319` attribution is wrong.

**Both copies must change together.** They are byte-parallel by design —
`#0337` and `#0351` deliberately built that consistency, and `#0351` could not
fix this because it was barred from touching `sitemap.go` and criterion 2
required the guard's phrasing to match it. So this pass edits `sitemap.go` too.

Low severity, but this comment pair is the project's canonical record of the
`#0325` incident, and it has now been wrong in print twice.

### Extra criteria from that finding

9. The `#0319` attribution is corrected in **both**
   `internal/seo/invalidator_satisfier_guard_test.go` and
   `internal/seo/sitemap.go`, in the same pass, preserving their deliberate
   byte-parallel phrasing.
10. Read `#0319` itself before rewriting the attribution — do not restate it
    from this issue. Say what the half-invalidation actually is (the meta-cache
    half of `#0319`'s staleness class) rather than claiming identity with it.
11. Criterion 6's comment-only proof does **not** apply to this pass as a
    whole, since criterion 1 changes code. Prove the `sitemap.go` edit
    comment-only separately, and the guard file's diff by inspection against
    the intended code change.

## Review notes

**2026-08-31 — Opus, phase 3. Verdict: APPROVED, `resolved`.** Every criterion
re-verified by the reviewer's own method. All mutation work was done in two
`git archive` exports of `88a780c` and `4c649f6` under the scratchpad
(§8b — never the shared tree, never a `git worktree`), with a sha256 manifest
of all **270** `.go` files diffed after every one of the **twenty-two**
mutations; identical every time. `/private/tmp/oc-live`, another session's
worktree, was never touched. `uptime` at start: load average 6.34 / 8.91 /
6.27, draining; 2.08 by the verification run. No timing result below depends
on a deadline.

### 1. The three `continue` cases — checked against `go/types`, not reasoned about

Built a synthetic package of **nineteen** alias shapes and, for each
package-scope name, computed ground truth **independently of the guard**
(`types.Implements(T) || types.Implements(*T)` — "can a value spelled with
this name, or its address, be passed into `interface{ Invalidate() }`"), then
diffed that against `scanInvalidatorSatisfiers`' verdict. The only names where
the guard is silent on a structurally-implementing type are the three
deliberate exclusions, and each is correct:

| Alias shape | truth `Impl(T)/Impl(*T)` | guard | verdict |
|---|---|---|---|
| `= Site` | false / true | silent | correct — `Site` is reported under its own name (N2) |
| `= *Site` | true / false | silent | correct — same, one indirection further (N3) |
| `= Seam` (declared iface), `= interface{ Invalidate() }` | true / false | silent | correct — declared seam, mirrors the `*types.Named` rule |
| `= Generic[int]`, `= *Generic[int]` | — | silent | correct — `Generic` is reported uninstantiated under its own name |
| `= struct{ *Site; cached []byte }` | true / true | **reported** | the issue's target |
| `= struct{ Site; … }` (value embed, ptr receiver) | **false** / true | **reported** | |
| `= *struct{ *Site }` | true / **false** | **reported** | |
| `= *struct{ Site }` | true / **false** | **reported** | |
| `= A` where `A = struct{ *Site; … }` (alias chain) | true / true | **reported** | `types.Unalias` follows the chain |
| `= struct{ Seam }` (literal embedding an interface) | true / true | **reported** | the interface-`continue` correctly does **not** swallow it: `Underlying()` here is the struct, not the interface |
| `= *Seam`, `= []Site`, `= map[string]Site`, `= struct{ n int }` | false / false | silent | correct non-satisfiers |

**No `continue` swallows a real satisfier.** The two the brief flagged as
likely mistakes are both handled: `types.Unalias` on a chain resolves through
to the literal, and `Underlying()` on a `*types.Pointer` returns the pointer
(not the pointee), so the pointer case is decided by the explicit
`ptr.Elem().(*types.Named)` test above it rather than by the interface test —
`= *Seam` falls through to the `Implements` check and is correctly rejected on
the merits, not skipped.

**One correction to the code's own doc comment, non-blocking.** It justifies
the `||` with a single reason ("promotion from an embedded value field lands
only on the literal's pointer form"). Measured, **both** disjuncts are
load-bearing and for opposite reasons: `= struct{ Site }` needs
`Implements(*target)` (false/true), and `= *struct{ *Site }` needs
`Implements(target)` (true/false, because `**struct` has an empty method set).
The **code is right**; only its stated rationale is half the story.

### 2. Is the standing test a real oracle, or agreement with itself?

**Real oracle — proved by mutating the function it calls.** Deleting the entire
`*types.Alias` branch from `scanInvalidatorSatisfiers` (i.e. reverting to
`#0349`'s behaviour) leaves `TestInvalidatorSatisfierSet` **PASSing** — the real
`internal/seo` contains no aliases, so the guard has nothing to notice — while
`TestInvalidatorSatisfierSet_AliasToTypeLiteral` **FAILs**, naming the
regression exactly ("`#0353`'s fix has regressed and this construct is invisible
to the guard again").

That settles §8's question. The rule bars an oracle that is *the same bytes as
its subject*; here the subject is `scanInvalidatorSatisfiers` and the oracle is
two hand-written assertions about a synthetic package (`FeedCache` must be
reported, `AliasToNamed` must not) derived from Go's type rules, not copied from
the implementation. Calling the same function is what makes it a test *of* that
function rather than of a second re-derivation that could drift. And it is
currently the **only** thing in the tree keeping the alias branch alive.

### 3. Mutations — the full set re-run, plus eleven new ones

Baseline (unmutated `88a780c`): both tests **PASS**.

| # | Mutation | Result |
|---|---|---|
| M1 | re-export `Sitemap.invalidate` → `Invalidate`, both call sites (`site.go`, `sitemap_test.go`) | **FAIL** by the guard's own assertion (`*Sitemap implements …: got true, want false`), **not** `[build failed]` — criterion 4's honest form confirmed |
| M2 | new type + pointer-receiver `Invalidate()` | **FAIL** |
| M3 | `func (s Sitemap) Invalidate()` value receiver | **FAIL** |
| M4 | named `struct{ *Site; cached []byte }` | **FAIL** |
| M5 | named struct embedding an interface | **FAIL** |
| M6 | `MutFeed[T any]` + pointer receiver | **FAIL** |
| M7 | `MutFeed[K comparable, V any]` | **FAIL** |
| N1 | declared `interface{ Invalidate() }` | **PASS** — correct survival |
| N2 | `= Renderer` | **PASS** — correct survival |
| N3 | `= *Site` | **PASS** — correct survival |
| M8 | `= struct{ *Site; cached []byte }` — the issue's target | **FAIL**, naming it |

**Before/after, the claim that makes this issue worth having done: reproduced.**
The identical M8 mutation against a second export of `4c649f6` (`#0349`, the
pre-`#0353` state) **PASSes silently**. Against `88a780c` it **FAILs**. (Noting
honestly that the `_AliasToTypeLiteral` column reads `PASS` on the `4c649f6`
export only because the test does not exist there — `go test -run` with no
match exits 0.)

**Eleven new reviewer mutations, aimed at the alias branch:**

| # | Construction | Expected | Result |
|---|---|---|---|
| R1 | `= *struct{ *Site; cached []byte }` — alias to a **pointer to a literal** | FAIL | **FAIL** — and the retired AST scan would **not** have caught this (its `*ast.TypeSpec.Type` is a `*ast.StarExpr`, not an `*ast.StructType`), so the new guard is strictly better here, not merely restored |
| R2 | `= struct{ Site; cached []byte }` — **value** embed of a pointer-receiver type | FAIL | **FAIL** |
| R3 | `= A` where `A = struct{ *Site; … }` — alias chain | FAIL, both names | **FAIL**, naming `RFeedA` **and** `RFeedB` |
| R4 | `= RGen[int]` where `*RGen[T]` has `Invalidate()` | FAIL | **FAIL**, naming `RGen` under its own name |
| R5 | `= interface{ Invalidate() }` — interface literal | PASS | **PASS** — correct survival |
| R6 | `= RDecl` — declared interface | PASS | **PASS** — correct survival |
| R7 | `= struct{ RDecl; … }` — literal **embedding** an interface | FAIL | **FAIL** — the seam rule does not over-swallow |
| R8 | `= *RDecl` — pointer to a declared interface | PASS | **PASS** — rejected on the merits, not by a `continue` |
| R9 | `= struct{ *Renderer }` | FAIL | **FAIL** |
| R10 | `= handlers.RExt` — alias to an **external** named satisfier | — | **PASS** (silent). Judged correct, not a gap: an alias adds a spelling, not a member; `seo.RFeed{}` is passable exactly when `handlers.RExt{}` already is. Identical to the N2 ruling, and the retired AST scan was blind to it too |
| R11 | `= struct{ *handlers.RExt }` — literal **promoting** from an external satisfier | FAIL | **FAIL** — the promotion path across packages is caught (`#0349`'s X5, via the alias branch) |

### 4. Criterion 10 — the `#0319` rewording, read against `#0319` itself

Read `#0319` in full rather than through this issue. It was filed because
`AdminCampaignArchiveHandler` and the send worker's completion path invalidated
**nothing** (both caches stale); its Description leads with
"`sitemap.xml` keeps advertising a just-**withheld** campaign's URL", and its
criterion 3 audits every writer the **sitemap or meta-tag renderer** reads — so
the class spans both caches while the headline is the sitemap.

Mechanism re-confirmed at the source: `internal/seo/site.go:48` `Site.Invalidate`
is `s.renderer.Invalidate(); s.sitemap.invalidate()`; `Renderer.Invalidate`
(`seo.go:540`) resets the per-path meta cache, `Sitemap.invalidate`
(`sitemap.go:168`) nils the sitemap cache. A bare `*Sitemap` runs only the
second — sitemap **cleared**, meta **stale**.

So **"the meta-cache half of `#0319`'s staleness class, not the sitemap
staleness `#0319` led with" is accurate**, and it is accurate in the specific
way the old phrase was not: it no longer claims identity, it names which half,
and it names which half `#0319` led with. Third time in print, first time right.

**Byte-parallelism confirmed.** The corrected clause is byte-identical in both
files after normalising only comment markers and line wrapping (the surrounding
prose in the two doc comments was never verbatim identical, and still isn't —
only the attribution clause is shared, as before).

### 5. Criterion 2 — the two doc corrections, re-measured rather than propagated

Measured on this machine (load average 6.0, under §5's threshold), not taken
from `#0349`'s review:

| | wall | build cache |
|---|---|---|
| `GOOS=linux GOARCH=arm64 go list -export -deps ./internal/seo`, cold `GOCACHE` | **5.28 s** | **234 MB** |
| the guard's own loader (probe compiled from the guard's exact `Mode`/`Env`), cold `GOCACHE` | **1.13 s** | **7 MB** |
| the same loader, warm | **0.50 / 0.50 / 0.51 s** | — |

Direction and magnitude both reproduce; the comment's `1.3 s / 6.8 MB` vs
`5.4 s / 240 MB` is honest. The **mechanism** claim is confirmed structurally
too, not just by timing: an export-data load materialises 234 MB of
cross-compiled archives, the guard's load materialises 7 MB — it is
source-type-checking, exactly as the corrected text now says.

**The allowlist sentence is now true, not merely softened.** It reads "nothing
left for a hand-reviewed allowlist to guard **by declaration or promotion**",
then names the one remaining gap and states it is closed **structurally** via
`types.Unalias`. Both halves check out: declaration and promotion are covered
(M2–M7, R7, R9, R11), and the alias gap is closed by code, not by a list.

### 6. Criterion 11 — the `sitemap.go` comment-only proof, with my own instrument

Neither the implementer's tool nor `#0351`'s reviewer's was reused. Wrote a
`go/scanner` token-stream oracle (mode `0`, so comments are consumed and never
emitted), hashing the `(token, literal)` sequence, and **built it to a binary
first** so a build failure can never be mistaken for a verdict — the exact
fail-open §8 records and `#0351`'s review hit. It prints an explicit
`VERDICT …` line, and every check below reads that line, not the exit status.

Proved before being trusted:

- **Falsifiable, two shapes.** An identifier rename (`invalidate` →
  `invalidateX`) and a string-literal mutation each → `COMMENT-ONLY: false`,
  distinct hashes, `rc=1`.
- **Sensitive in the right direction.** A comment-only sentinel insertion →
  `COMMENT-ONLY: true`, identical hash — so it is not simply reporting "differs"
  for everything, which falsifiability alone would not rule out.
- **Fail-closed, three ways.** Empty input, a 60-byte truncation, and a missing
  file each exit **2** emitting **no verdict line at all** — a distinct code
  from the `false` verdict's `1`.

**Result:** `88a780c^` and `88a780c` copies of `internal/seo/sitemap.go` both
produce **645 tokens**, `sha 9b0f4f60dae99954…` on both sides — the edit is
comment-only. Control: the same tool on the guard test file's before/after
reports `false` (687 → 1126 tokens), which is correct, since criterion 1 changes
code there. The guard file's diff was reviewed by inspection against the
intended change and contains exactly it: the alias branch, the
`scanInvalidatorSatisfiers` extraction, the new standing test, and the three
doc-comment corrections — nothing else.

### 7. Verification

- `GOFLAGS=-mod=readonly ISSUE=0353r scripts/check.sh go ./internal/seo/...
  ./internal/handlers/... ./cmd/...` — `go build`, `go vet`, `gofmt -l` all
  clean; `internal/seo` `ok` 1.043s, `internal/handlers` `ok` 6.354s,
  `cmd/opencircuit` `ok` 6.571s; skip audit `(no skips)`; no leftover
  processes; **`VERIFICATION PASSED`**. `uptime` load average 2.08 for this run.
- Both tests observed executing: `--- PASS: TestInvalidatorSatisfierSet
  (0.66s)` and `--- PASS: TestInvalidatorSatisfierSet_AliasToTypeLiteral
  (0.00s)`.
- **`GOFLAGS=-mod=readonly` was exported on every loader run in this review**
  (the probe binary, all twenty-two mutation runs, both timing measurements,
  and `check.sh`). `git status --porcelain -- go.mod go.sum` is clean after all
  of it — no `-mod=mod` promotion.
- **`gofmt` stability:** `sha256(file) == sha256(gofmt stdout)` for both files
  (`9f231ac33b2ee313…` and `89cbb310a7f4875b…`); `gofmt -l` clean for both and
  repo-wide via `check.sh`. The only non-ASCII bytes either file gained are two
  `§` (U+00A7) in new comment lines, and neither file contains a doubled `''`
  or a doubled backtick, so §8's gofmt smart-quote rewrite has nothing to act
  on — confirmed stable by hash, not by reading.
- Scratch database `opencircuit_test_0353r` dropped on exit
  (`scripts/testdb.sh list` shows only the template). No `go test`/`.test`
  processes left.

### One finding handed to the orchestrator, not bounced

**The `||` in the alias branch is load-bearing in both directions, and the new
standing test pins neither half.** Measured: replacing
`types.Implements(target, …) || types.Implements(types.NewPointer(target), …)`
with **either** disjunct alone leaves **both** tests green, because the
synthetic `FeedCache = struct{ *Site; cached []byte }` implements in both forms.
Pointer-form-only then silently misses R1 (`= *struct{ *Site }`); value-form-only
silently misses R2 (`= struct{ Site }` with a pointer receiver). Both were
verified to reopen as real, uncaught gaps under those mutations.

Not a defect against any criterion here — criterion 1 specifies the `||` and it
is present and correct, and criterion 3 asks for *the* alias-to-type-literal
standing case, which is delivered and proved. It is the same class `#0349`'s
review handed forward as this issue and `#0346`'s handed forward as
`#0350`/`#0351`, and the same disposition is used: reported with the remedy,
which is two declarations and two assertions inside the existing synthetic
source string, not a design decision.

**§3 decision: no escalation.** This review approves on the first pass, so the
two-bounce trigger never fired. Recorded per §3's instruction that a decision
*not* to escalate earns the same line as a decision to escalate.

## Relation

- Found by: [#0349](0349.md)'s review, which resolved that issue
- Depends on: [#0349](0349.md) — the oracle this completes
- **Must not run concurrently with [#0351](0351.md)** — it rewrites part of the
  same doc comment, and its criterion 3 requires a comment-only diff, which
  this issue's code change would break. Sequence them.
- Relates to: [#0337](0337.md) — whose retired tripwire caught this construct
- Relates to: [#0350](0350.md) — the CLAUDE.md §8 entry describing the family

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0349`'s review (§9).
- 2026-08-31 — Amended by the orchestrator from `#0351`'s review, which
  resolved that issue: a third false claim in the same doc comment, the
  `#0319` attribution, present in two files that must change together.
- 2026-08-31 — Implemented (Sonnet, phase 2). Commit `88a780c`.
- 2026-08-31 — Reviewed (Opus, phase 3) and **resolved**. Every criterion
  re-verified independently: the alias branch's three `continue` cases checked
  against `go/types` ground truth across nineteen alias shapes (including
  pointer-to-literal, alias chains, generic instantiations, and a literal
  embedding an interface — none of which anyone had tried), the full mutation
  set re-run plus eleven new alias-aimed mutations in `git archive` exports
  with a 270-file sha256 manifest diffed after each, the `4c649f6` before/after
  reproduced, `#0319` read at the source to judge the rewording, the
  1.3s/6.8MB vs 5.4s/240MB claim re-measured on this machine rather than
  propagated a third time (5.28 s / 234 MB vs 1.13 s / 7 MB), and the
  `sitemap.go` comment-only status re-proved with a purpose-built
  `go/scanner` oracle shown falsifiable in two shapes, sensitive in the right
  direction, and fail-closed three ways. The standing test was proved a genuine
  oracle by deleting the alias branch it guards: the main guard stays green,
  the standing test fails naming the regression. §3: no escalation — approved
  on the first pass. One finding handed to the orchestrator rather than
  bounced: neither disjunct of the alias branch's `||` is pinned by any test.
