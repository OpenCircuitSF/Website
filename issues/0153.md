# 0153 — Decide whether workshops need a cover-image upload endpoint

| | |
|---|---|
| **Status** | resolved |
| **Module** | handlers / web |
| **Platform** | All |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-22 |
| **Closed** | 2026-08-24 |

> 🟢 Resolved 2026-08-24 — the decision (no upload endpoint in v1) is recorded
> with its reasoning and the condition that would reopen it, and `#0052`'s
> cover-image criterion is verified struck, ticked, and pointing here.

## Description

`#0138` struck its upload criterion with a decision: cover images are **path-or-URL text entry in v1**, and an upload endpoint "deserves its own future issue". This is that issue.

The current state after `#0138`: `cover_image` must be a same-origin path (`/assets/…`), validated in both the browser and the API. So an admin can reference an image only if someone has already put it on the server by some means outside the product — which today means a deploy.

## Acceptance criteria

- [x] Decide whether v1 ships an upload path at all. "No" is a legitimate answer if workshop cover images are set rarely and deploys are frequent — record the reasoning either way — **decided: no. See `## Decision`.**
- [x] ~~If yes, the design questions to settle before any code: where files are stored (filesystem beside the binary, S3, database), how they are served, size and content-type limits, what happens on duplicate filenames, and whether uploads are pruned when a workshop is deleted~~ — **moot:** the decision was no. Struck 2026-08-24 by the phase-3 review rather than left unticked forever, which is the failure mode the fourth criterion exists to prevent; the storage question is handed to `#0232` by `## Decision`
- [x] ~~If yes: uploads are admin-gated, the stored path is what lands in `cover_image`, and the result passes `#0138`'s same-origin validation without special-casing~~ — **moot:** same reason
- [x] If no: `#0052`'s cover-image criterion is struck with a note pointing here, rather than left unticked forever — already done in commit `1d2cdd3` (`#0138`'s fix pass), which struck `#0052`'s cover-image criterion with a note pointing at this issue

## Decision

**No upload endpoint in v1.** Cover images stay path-or-URL text entry against a
same-origin `/assets/…` path, exactly as `#0138` left them. Recorded by the
orchestrator on 2026-08-24 under the user's standing instruction to resolve
Phase 7 rather than stall on open questions.

The reasoning, so a later reader can weigh whether it still holds:

- **The volume does not justify the surface.** This is one San Francisco group
  running workshops; the home page shows three at a time. An upload endpoint
  brings content-type sniffing, path traversal, size limits, duplicate-name
  collision, and orphan pruning on workshop delete — five things to get right
  and keep right, for a field set a handful of times a month.
- **It would be the first mutable data directory in the deploy model.** The
  binary embeds `web/dist` via `//go:embed` and the only mutable state is
  Postgres. Filesystem uploads would add a directory that must survive deploys,
  be included in the backup scope (`#0062`, `#0229`), and be served as
  untrusted bytes from the same origin as the admin console. That is a real
  architectural change, not an added handler.
- **S3 is not available.** `CLAUDE.md` §10 item 2 records the AWS account as
  not started, and the user deferred all AWS work to deployment. Choosing the
  storage backend now would be choosing it blind.
- **Adding an image is currently a commit, and deploys are frequent.** During
  build-out that is a smaller cost than the endpoint.

**What would reopen this:** Phase 9 (`#0232` — making the platform usable as a
dependency) changes the calculus outright. A downstream consumer cannot commit
an image into *this* repository's `assets/`, so a configured storage location
becomes necessary rather than convenient. Revisit there, with the storage
question answered by whatever Phase 9 decides about deploy-time configuration.

## Review notes — phase 3, 2026-08-24

**Approved.** This issue produces a decision, not code, so the review is of
whether the decision is actually recorded and whether the one downstream edit
it claims really happened. Both check out, verified against the files rather
than against the prose.

- **The decision is recorded and is genuinely a decision** — `## Decision`
  states the outcome ("no upload endpoint in v1"), four reasons, and, unusually
  and usefully, **what would reopen it** (`#0232`, Phase 9, where a downstream
  consumer cannot commit an image into this repository). `issues/0232.md`
  exists. A decision with a stated expiry condition is worth more than one
  without.
- **`#0052`'s criterion really is struck and really does point here** — checked
  in the file, not inferred: `issues/0052.md` line 729 reads
  `- [x] ~~Cover image upload or path entry …~~ **Struck:** … the upload
  question now belongs to `#0153`.` It is ticked, struck through, and names
  this issue. Commit `1d2cdd3`'s own message confirms it ("Also strikes #0052's
  floating 'Cover image upload or path entry' criterion"), so this is not a
  later untracked edit. `#0052` is `resolved`/`Closed 2026-08-22` and was
  correctly not reopened for it.
- **The two remaining criteria were "If yes" branches** of a decision that went
  no. Struck as moot by this review rather than left `[ ]` forever — leaving
  them is precisely the failure mode the fourth criterion complains about.
- **No code, no build, no verification command applies.** Nothing in this issue
  changes the tree; the `ISSUE=0235rev scripts/check.sh` run made for `#0235`
  (14 Go packages `ok`, zero skips; `npm test` 33 files / 3,826 tests) confirms
  the tree is green around it, but that is context, not evidence for this
  issue.

**One inaccuracy carried in from `#0138`'s framing, reported not fixed.** Both
`## Description` and `## Decision` describe `cover_image` as "a same-origin path
(`/assets/…`)". The validator — `isSafeCoverImage` in
`internal/handlers/admin_workshops.go:691` and its TypeScript twin — accepts
**any** absolute same-origin path: it rejects control characters, requires a
leading `/`, and rejects `//` after normalising backslashes. `/assets/…` is a
convention, not something the code enforces. The substantive claim (same-origin
only, no upload) is correct; the parenthetical overstates the constraint. Worth
a one-line correction wherever it is repeated, and worth knowing before anyone
builds on the assumption that the prefix is validated.

## Notes

Filed from `#0138`'s decision record and its phase-3 review, which noticed the promised follow-up had not actually been filed.

The honest framing: this is a product question about how often workshops get cover images and who sets them, not a technical one. It is Phase 7 because nothing blocks on it and because the answer may reasonably be "not in v1".

## Relation

- Found by: [#0138](0138.md) — Cover images and protocol-relative URLs
- Relates to: [#0052](0052.md) — whose cover-image criterion was struck by `1d2cdd3` pointing here; re-verified 2026-08-24, it is no longer pending

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | review | claude-opus-5 | — | — | — | — | — |
