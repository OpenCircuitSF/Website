# 0375 — A calibration row can declare a second-identifier expectation that is silently skipped

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-01 |
| **Commit** | `be1f08d` |

## Description

`#0366` added a second-identifier count/reason pair to
`claimCommentGuardDirectionalCalibrationCases`, gated on `c.secondIdent != ""`.

**A future row that sets `wantSecondIdentFindings: 1` but forgets
`secondIdent` asserts nothing and passes.**

**Latent today**: no such row exists, and both assertions are live on the one
row that uses them — `#0366`'s reviewer verified the pair fires in both
directions on that row, including a reason-only mutation.

This is the family's signature failure one more time: an assertion that passes
because it never ran. `#0358` shipped a row whose fixture never entered the
axis; `#0363` shipped one whose fixture always fired; `#0364` exists because a
row can get its number for the wrong mechanism. This is the **skipped**
variant.

## The recommendation this issue carries

`#0366`'s reviewer recommended **recording, not closing**, and named the cost:
the one-`if` fix would be a **seventh in-file check** in a file `#0258`'s
caution now binds explicitly — six mechanisms already, each new one fresh
mutable surface.

**So the default deliverable is one honest sentence.** If a pass believes the
guard earns its place, it must argue why this differs from `#0330`, `#0347` and
`#0356`, which all ended by refusing exactly this trade — and be prepared for
the argument to be rejected.

There is a cheaper third option worth weighing: state the invariant in the
struct's own doc comment so the omission is visible at the point of authoring a
row, rather than adding a runtime check.

## Acceptance criteria

1. The gap is recorded where a row author meets it — on
   `claimCommentGuardDirectionalCalibrationCase`'s field documentation — saying
   plainly that `wantSecondIdentFindings` and `wantSecondIdentReason` are
   **only asserted when `secondIdent` is non-empty**, and that setting them
   without it asserts nothing.
2. The record is **re-verified, not transcribed**: construct such a row in a
   throwaway export, confirm it passes while asserting nothing, and confirm the
   existing row's pair still fires in both directions. Five issues this phase
   received a wrong identifier because prose was transcribed as though measured.
3. **If you add the check anyway**, it must discriminate: prove the
   forgotten-`secondIdent` row now fails, naming it, and prove no existing row
   changed behaviour.
4. The axis stays frozen (`#0358` criterion 4).
5. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal enters
   any Go comment; floor harness stays **42 total / 12 non-exempt** (`#0323`);
   `gofmt` stability.

## Verification

**Decision: recorded, not closed.** Took the cheaper third option the issue
names: stated the invariant on
`claimCommentGuardDirectionalCalibrationCase`'s field documentation, at the
point a row author meets it, rather than adding the one-`if` runtime check.
Considered the exception argument and concluded it does not hold any better
here than for `#0330`/`#0347`/`#0356`: the fix would be a **seventh**
in-file mechanism in a file `CLAUDE.md` §8's `#0258` caution already binds
(six already), and `#0366`'s two reviews both explicitly declared this
guard family terminated.

**Criterion 2 — both premises re-verified myself, in a throwaway `git
worktree`, never the tracked tree.**

- **Forgotten-`secondIdent` row asserts nothing (reproduced).** Inserted a
  throwaway row into `claimCommentGuardDirectionalCalibrationCases` on a
  worktree at `HEAD` (`d5563ad`): `secondIdent: ""` (left unset, its zero
  value) but `wantSecondIdentFindings: 1` and a `wantSecondIdentReason` that
  do not match the fixture's real behaviour. `go test ./internal/outbox/...
  -run TestClaimCommentGuardDirectionalAxisCalibrationTable -v`:
  `--- PASS:
  TestClaimCommentGuardDirectionalAxisCalibrationTable/#0375_probe:_forgotten_secondIdent_...`
  — passes despite declaring a false expectation, because
  `if c.secondIdent != ""` skips the whole block. Matches the issue's
  premise exactly.
- **The existing row's pair still fires (reproduced).** In the same
  worktree, mutated the real `every-distinct-identifier` row's own
  `wantSecondIdentFindings` from `0` to `1` (leaving its real `secondIdent:
  "OrphanSweep"` in place): the SAME test run now fails, naming exactly that
  row —
  `--- FAIL: .../every-distinct-identifier:...` — while the throwaway probe
  row (still `secondIdent: ""`) keeps passing in the same run. So the
  skip is specific to a forgotten `secondIdent`, not a general weakness in
  the pair; when `secondIdent` is set, its count assertion is live. (The
  reason-half discrimination was already exhaustively proved by `#0366`'s
  review — both directions, reason-only mutation included — and re-proving
  it was not necessary to confirm this issue's specific claim, which is
  about the omission shape, not the pair's general falsifiability.)

**Structural comment-only proof.** Same `tokdiff` tool built for `#0374`
(`go/scanner` mode 0, proved falsifiable/fail-closed/raw-string-aware there
— see that issue's `## Verification`). Both issues' doc-comment edits are
one combined change to the same file, verified together: `git show
HEAD:internal/outbox/claim_comment_referent_guard_test.go` (pre-edit) vs the
working tree — **`VERDICT: IDENTICAL`, 5373 tokens each side**. `git diff`
on the combined change: 61 lines added, 0 removed, every added line starts
with `//` (checked programmatically).

**Criterion 4 — axis frozen.** The same whole-file token-IDENTICAL result
above is strictly stronger than an axis-declarations-only check: it proves
zero non-comment bytes changed anywhere in the file, so
`claimCommentGuardDirWordRe`, the window computation, the marker set, and
the clamp are untouched a fortiori.

**Criterion 5 / floor harness / gofmt** — covered by the same `check.sh`
runs as `#0374` (same file, one edit): zero `.ClaimDue(` / `.OrphanSweep(` /
`.SelectDue(` occurrences; `claimKindsGuardMinPlausibleCallSiteCount`
population **42**, `claimKindsGuardMinPlausibleNonExemptCallSiteCount`
population **12** (`#0323`, unmoved); `gofmt -l` clean, file sha256 ==
`gofmt` stdout sha256
(`e9b4e4e3e0c8b874c8a01fe90a58b9d4115f5735933b06870f3fca48d2cf7a08`).

**Standard verification.**
```
ISSUE=0374b scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  # internal/outbox ok 0.809s, internal/mailing ok 4.143s (no skips)
  # VERIFICATION PASSED
ISSUE=0374b scripts/check.sh guards
  # 0 failures, VERIFICATION PASSED
```
Run once, covering both `#0374` and `#0375` since they share the one edited
file — see `#0374`'s `## Verification` for the full run detail, database
drop confirmation, `:5173` check, and worktree cleanup (`wt0375-head`
included). No new obstacle encountered specific to this issue.

## Relation

- Found by: [#0366](0366.md)'s review
- Depends on: [#0366](0366.md) — which added the gated pair
- Relates to: [#0358](0358.md), [#0363](0363.md), [#0364](0364.md) — the three
  earlier variants of an assertion that passes without running
- Relates to: [#0258](0258.md) — the six-mechanism caution
- Relates to: [#0374](0374.md) — the sibling latent gap from the same review

## Work log

- 2026-09-01 — Filed by the orchestrator from `#0366`'s review (§9).
- 2026-09-01 — Implemented (Sonnet). Decision: recorded, not closed — took
  the issue's own cheaper third option (state the invariant on
  `claimCommentGuardDirectionalCalibrationCase`'s field documentation, not a
  runtime check). Re-verified both premises myself in a throwaway `git
  worktree` before writing anything: a throwaway row with `secondIdent: ""`
  and a false `wantSecondIdentFindings: 1` passes cleanly (asserts nothing),
  and mutating the REAL `every-distinct-identifier` row's
  `wantSecondIdentFindings` (with its real `secondIdent` still set) fails
  correctly in the same run — confirming the omission, not the pair itself,
  is what's silent. Doc-comment edit and its structural comment-only proof
  (`go/scanner` mode-0 token tool) share `#0374`'s change to the same file;
  see that issue's `## Verification` for the tool's falsifiability/fail-
  closed/raw-string-fixture proof. Left `in-progress` for review per
  `issues/Issues.md` §9/critical rule.
