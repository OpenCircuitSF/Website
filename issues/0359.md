# 0359 — Bare prose line references drift silently and no guard sees them

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | handlers / tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | `64f2235` (citation_target_guard_test.go fix), `f700734` (erase.go fix) |

## Description

`#0356`'s guard is scoped to the `<file>.go:<int>` form. A positional reference
written **without** the filename escapes it entirely.

`internal/handlers/citation_target_guard_test.go` still contains:

> see that file's **line 25** for the real text

referring to `admin_workshops.go`. The guard's regex cannot match it. It is
**accurate today** — `#0356`'s reviewer checked, and the four-segment token
really is on line 25 — but it drifts on any unrelated edit above it, exactly
like the 27 citations `#0356` removed.

The reason it exists in that form is worth recording: it was written that way
**because the file's own path guard forbids spelling the token**. So one guard's
constraint produced a reference shaped to be invisible to another's.

The class is likely small, but it is invisible to **both** guard families —
`#0356`'s (needs a filename adjacent to a number) and `#0347`'s (needs a named
call identifier).

## Census and decision

**Method.** Built a standalone AST census tool (`linecensus`, Go, compiled to
a binary, `go/parser` with `ParseComments`, walking only `f.Comments` —
string literals are a different AST node kind and cannot enter the
population by construction) matching `line[s]? N`, `lines N-M`/`N–M`, `the
<word> line`, and `<N><st/nd/rd/th> line`, case-insensitive, across every
line of every comment group under `internal/` and `cmd/`.

**Proved falsifiable and fail-closed before use, per criterion 2**, on a
fixture directory (not the real tree):
- Positive control: a comment containing "line 42" alongside a **string
  literal** containing "line 99" → reported exactly the comment hit (1),
  correctly excluding the string-literal decoy — proves comments-only
  scanning is real, not incidental.
- Negative control: a file with no line-ish text → `VERDICT: OK … hits=0`
  (proves it doesn't fail open by always erroring).
- Five fail-closed probes, each producing an explicit `VERDICT: ERROR: …`
  line, never a false `OK`: nonexistent root, a file passed where a
  directory was expected, a directory with zero `.go` files, a
  syntactically-broken `.go` file (parse failure), and zero arguments.

**Real run**: `linecensus internal cmd` → `VERDICT: OK files=271 hits=38`.

**Correction 2026-09-01 (#0359 review).** Two things this Method section
should have said and didn't:

- **Coverage.** The run covered **271 of the repo's 272** `.go` files;
  `web/embed.go` sits under `web/`, outside both `internal` and `cmd`, and was
  never scanned by this tool. The review scanned it separately and found
  **zero** hits, so the omission changed nothing in outcome — but the
  original "files=271" figure should have said what it covered rather than
  reading as if it were exhaustive.
- **Pattern limit.** `numPattern` requires a digit and `ordinalWord` requires
  a literal "the" plus the **singular** "line", matched **per physical
  comment line**. That combination cannot see a spelled-out counted offset
  ("three lines above"), `L<N>`, "near the top of", or a phrase broken across
  a comment wrap onto a second physical line. The review's independent
  instruments (12 per-line pattern families plus a comment-group-joined pass
  covering 7 more) found **three** genuine sites this census missed — see
  `## Review notes` below.

**Classification of all 38 hits** (full list kept in scratchpad
`issue-0359/census_real_run.txt`):

| Class | Count | Examples |
|---|---|---|
| Idiomatic prose ("log line", "address line", "line of defense", "the equivalent line") — no digit, no external pointer | 12 | `internal/auth/mailer.go:73`, `internal/subscribers/store.go:1236`, `internal/mailing/transactional_templates.go:39,359`, `internal/mailing/worker_test.go:1261`, `internal/mailing/templates_test.go:583` |
| Prose describing a parsing/scanning **algorithm** in the abstract ("the first line", "the following line", "the raw line") — not pointing at any specific file:line | 15 | `internal/db/docs_parity_test.go:229,487,512,664`, `internal/db/issue_citation_guard_test.go:120,541`, `internal/db/prd_section_index_test.go:29,324`, `internal/handlers/campaign_progress_parity_test.go:91,332`, `internal/handlers/dangling_test_citation_guard_test.go:420,433,757`, `internal/handlers/line_citation_guard_test.go:17,59,173`, `internal/handlers/routes_parity_test.go:91` |
| Self-referential, non-drifting: a fixture's own constructed string, annotated line-by-line **in the same function**, checked programmatically against named constants (`blockOpeningLine`, `citationLine`) rather than left as unchecked prose | 9 | `internal/handlers/dangling_test_citation_guard_test.go:838-846` |
| Local **relative** reference within the same declaration ("the second line below") — not an absolute pointer into another location | 1 | `internal/handlers/audit_email_metadata_guard_test.go:1657` |
| Self-referential fixture prose about a doc string built two lines later in the same test, describing "Line 2"/"Line 4" of *that* literal | — (subset of row above's neighbours) | `internal/db/docs_parity_test.go:648,652,654,659` (recounted in row 2 above; verified non-drifting by the same self-containment argument) |
| **Real bare positional citation** — an absolute line number in one file, naming no filename, pointing at a *different* file, invalidated by any unrelated edit above the cited line | **1** | `internal/handlers/citation_target_guard_test.go:264` ("line 25", `admin_workshops.go`) |

Every non-drifting classification was checked by reading the surrounding
code, not inferred from the wording alone — in particular the
`dangling_test_citation_guard_test.go:838-846` block was traced forward to
confirm `blockOpeningLine`/`citationLine` are asserted against
programmatically (`found.pos.Line != citationLine` etc.), which is what
makes it self-checking rather than unchecked prose.

**Addendum 2026-09-01 (#0359 review) — two sites this census's patterns
structurally could not match.** `internal/subscribers/erase.go` contains the
spelled-out counted-offset phrase "three lines above" twice, which matches
none of the three patterns above (`numPattern` needs a digit; `ordinalWord`
needs literal "the" plus singular "line"). Both are real bare positional
citations, the same class as the row above, mismeasured — the only `UPDATE
email_sends` in the file is at line 218, roughly 40-48 physical lines above
each citation, not three, and `git show bace190:internal/subscribers/erase.go`
shows both were wrong since the commit that introduced them, not drifted
later:

| Class | Count | Examples |
|---|---|---|
| **Real bare positional citation** (spelled-out counted offset, outside this census's pattern coverage) — states a fact wrong by ~40-48 lines, wrong at birth | **2** | `internal/subscribers/erase.go:89` ("three lines above it in Erase"), `internal/subscribers/erase.go:265-266` ("three lines above", split across a comment wrap) |

Found and fixed by the review's wider census (12 per-line pattern families
plus a comment-group-joined pass); see `## Review notes` and the corrected
`## Fix` / `## Files changed` below. Combined with the original row, the
real count across both censuses is **three** sites, two distinct facts
(the `erase.go` pair restates the same fact twice), two files.

**Decision: do not build a guard.** ~~The census returns exactly **one** real
site — `#0359`'s own known instance, and no others~~ **corrected 2026-09-01
(#0359 review)**: this pass's narrower census returned exactly one real site;
the review's wider census (12 per-line pattern families plus a
comment-group-joined pass, versus this pass's 3 patterns) found two more —
the `erase.go` pair above — for **three** genuine sites total, two distinct
facts, two files. The review confirmed this is still squarely inside
criterion 1's stated threshold ("one or two sites, say so plainly, fix
them, and build nothing") by any reasonable reading, and confirmed the
ruling below independently. Weighing the reasons the issue names against
building anyway:

- `#0356`'s guard family cost a planning pass and three review rounds to
  reach a working exemption design, on a strictly *easier* problem (it has a
  filename to anchor exclusions on; this class does not).
- The false-positive surface here is worse, not comparable: ~~34 of the 38~~
  **corrected 2026-09-01 (#0359 review)**: 27 of the 38 (12 idiomatic + 15
  abstract-algorithm, per this section's own table) raw hits are ordinary
  English sentences about lines of code, log output, or postal addresses. A
  guard would need the same order of exemption machinery `#0356` built, to
  protect a population of one.
- §8's `#0258` lesson generalizes directly: adding a mechanism to defend
  against a single known instance, when three prior sessions (`#0330`,
  `#0347`, `#0356`) already concluded the terminating move was to stop
  claiming more than a check establishes, is the wrong shape here too.
- A guard could only ever prove a citation *exists* in some non-drifting
  form, never that it is *true* — the same limit `#0356` recorded. Fixing
  the one real site directly establishes the same true fact more cheaply.

So: ~~fix the one site~~ **corrected 2026-09-01 (#0359 review)**: fix the
three sites (two distinct facts, two files), build nothing. 372 of the
review's 375 Tier-A hits were ordinary prose, which is the same shape of
false-positive surface this decision already weighed, just measured wider.

## Fix

`internal/handlers/citation_target_guard_test.go:264` read:

> `— see that file's line 25 for the real text: a`

**Re-measured the referent myself, not from this issue's prose**:
`grep -n "internal/seo/seo.go/sitemap.go" internal/handlers/admin_workshops.go`
→ still line 25 today, inside the file's **package doc comment** (the single
comment block immediately preceding `package handlers`, lines 1-32). That
comment block is a stable, non-drifting anchor for exactly the reason §11's
PRD index and `#0352`'s identifiers are: it is unique per file (Go allows
one package clause per file) and is invalidated only by an edit to the
comment itself, never by an edit to anything else in the file above it.

Replaced the citation with that anchor:

> `— see that file's package doc comment for the real text: a`

This respects why the "line 25" phrasing existed in the first place: the
literal four-segment token (`internal`/`seo`/`seo.go`/`sitemap.go`) still
cannot be spelled out here, because this very comment is scanned by
`pathCitationIsExcluded`'s own guard below it, and spelling it would make
the guard self-referential (exactly as the surrounding paragraph already
explains for its other two examples). "Package doc comment" names the
*location* without spelling the *token*, the same way the two neighbouring
examples in this paragraph already do.

Diff is comment-only, four lines touched (`git diff` below), and re-running
`linecensus internal cmd` after the edit reports `hits=37` (down from 38)
with the `citation_target_guard_test.go:264` row gone from the output.

**Added 2026-09-01 (#0359 review, second implementation pass) —
`internal/subscribers/erase.go`'s two sites.** Both said "three lines
above"/"three lines above it in Erase", naming no filename, pointing at the
file's sole `UPDATE email_sends` statement (line 218) — actually ~40-48
physical lines above each citation, not three, and wrong since the commit
that introduced them (`git show bace190:internal/subscribers/erase.go`
places both at identical positions to today). Re-measured directly against
the working tree before editing, per the same rule as the `admin_workshops.go`
fix above, not transcribed from the review's line numbers.

Replaced both with anchors on the unique statement name rather than a
position — `grep -c "UPDATE email_sends" internal/subscribers/erase.go` = 1,
so the phrase is unambiguous and survives any edit that doesn't touch that
statement, the same `#0352` property the "package doc comment" anchor above
relies on:

- `erase.go:89` — "three lines above it in Erase" → "built inline by Erase's
  own UPDATE email_sends statement".
- `erase.go:265-266` — "the email_sends redaction three lines above" → "the
  UPDATE email_sends redaction earlier in this method".

Both diffs are comment-only, line count preserved (4→4, 5→5), no backticks
introduced (this file's own path guards don't forbid them here, but the
gofmt smart-quote hazard in CLAUDE.md §8 is avoided regardless). Verified
comment-only structurally: a purpose-built tool (`commentonly`, `go/parser`
mode 0 — comments never enter the AST at all, so they cannot affect the
comparison by construction) parses HEAD's and the working tree's
`erase.go` and prints each with `go/printer` in `RawFormat`, discarding
comments; the two canonical outputs are byte-identical
(`VERDICT: OK comment-only=true decls=5 bytes=4202`). Proved falsifiable
first (a fixture with a genuine code change was correctly flagged
`comment-only=false`) and fail-closed on six probes (missing file, zero
args, one arg, empty file, syntactically broken file, directory-as-file —
all `VERDICT: ERROR: …`, never a false OK) before trusting it against the
real file.

## Acceptance criteria

1. **Census first, and decide afterwards.** Count the bare positional
   references — "line N", "lines N–M", "the Nth line" and near neighbours —
   across Go comments, and classify each as a real positional citation versus
   prose that legitimately discusses a line number. **A guard is not
   automatically the answer**: if the census comes back at one or two sites, say
   so and fix them without building anything. That judgement is part of this
   issue's deliverable, not a preamble to it.
2. The census tool scans only `f.Comments` from a `ParseComments` walk, so
   string literals cannot enter the population by construction, and is proved
   falsifiable and fail-closed **before** use. Seven agents found genuine
   fail-opens in their own instruments on 2026-08-31; build to a binary and read
   an explicit verdict line, not an exit code.
3. `citation_target_guard_test.go`'s "line 25" reference is replaced with
   something that does not drift — and the replacement must respect **why** it
   was written that way, since that file's own path guard forbids spelling the
   token it points at. Re-measure the referent yourself rather than trusting
   this issue.
4. If a guard is built, it must state the false-positive class it accepts and
   pin its calibration in fixtures rather than prose — see `#0358`, which exists
   because `#0347` described its window limits in a sentence that went stale.
5. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal enters
   any Go comment, and the floor harness population stays **42 total / 12
   non-exempt** (`#0323`'s trap).
6. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout (§8).

## Verification

- **Criterion 1** — census method and results in `## Census and decision`
  above. Real census tool run: `linecensus internal cmd` →
  `VERDICT: OK files=271 hits=38` before the fix, `hits=37` after, with the
  one real citation's row gone. Decision: no guard built, one site fixed.
- **Criterion 2** — `linecensus.go` built to a binary
  (`/private/tmp/.../scratchpad/issue-0359/linecensus`), scans only
  `f.Comments` from a `go/parser.ParseFile(..., ParseComments)` walk.
  Falsifiability: comment-hit-plus-string-literal-decoy fixture correctly
  reports the comment hit only (`hits=1`, not 2). Fail-closed: five distinct
  bad-input probes (nonexistent root, file-not-directory, zero-.go-file
  directory, syntactically broken `.go` file, zero arguments) each produced
  an explicit `VERDICT: ERROR: <reason>` line; a negative-hit real file
  produced `VERDICT: OK … hits=0`, proving the tool distinguishes "found
  nothing" from "could not run" rather than defaulting to one or the other.
- **Criterion 3** — the referent was re-measured directly against the
  working tree (`grep -n` on `admin_workshops.go`, not transcribed from this
  issue), confirmed still accurate at line 25 today, and replaced with
  "package doc comment" — a stable anchor that respects the original
  constraint (the file's own guard forbids spelling the literal token).
- **Criterion 4** — no guard built, so no calibration to pin; not
  applicable.
- **Criterion 5**. `grep -c '\.ClaimDue(\|\.OrphanSweep(\|\.SelectDue('
  internal/handlers/citation_target_guard_test.go` → `0`, both before and
  after. `bash scripts/go_file_visit_floor_guard_test.sh` (via
  `ISSUE=0359 scripts/check.sh guards`) reports "all guards hold (0
  failures)"; the outbox section's externally-measured populations are
  unchanged: `42` total / `12` non-exempt. Confirmed `scripts/go_file_visit_floor_guard_test.sh`
  was not touched this session (`git status --porcelain` on it was empty
  before any edit).
- **Criterion 6 (gofmt stability)**. `shasum -a 256
  internal/handlers/citation_target_guard_test.go` equals `gofmt
  internal/handlers/citation_target_guard_test.go | shasum -a 256`
  (`73f80e43…8ae7303` both sides); `gofmt -l` reports nothing.
- `uptime` checked before every run (load averages 1.4–2.2, well under the
  ~8 threshold); `:5173` confirmed free (`lsof`, not `pkill -f`) before the
  `guards` run.
- `ISSUE=0359 scripts/check.sh go ./internal/handlers/... ./internal/outbox/...
  ./internal/mailing/... ./internal/seo/...` — build/vet/`gofmt -l` clean;
  all four packages `ok` (handlers 6.904s, outbox 0.834s, mailing 9.535s,
  seo 0.880s), zero skips, `VERIFICATION PASSED`.
- `ISSUE=0359 scripts/check.sh guards` — all guard suites, including the
  `#0356` line-citation-guard shape section and
  `go_file_visit_floor_guard_test.sh`, `VERIFICATION PASSED`.
- Own test database `opencircuit_test_0359`, dropped automatically by
  `scripts/check.sh` on exit (`scripts/testdb.sh drop 0359` afterward
  confirmed "does not exist"). No leftover `go test`/`.test` processes
  (`pgrep`). Did not touch `internal/outbox/claim_comment_referent_guard_test.go`,
  `issues/0358.md`, `scripts/testdb.sh`, or any subscribers/growth file —
  confirmed by `git diff --cached --name-only` before committing.

**Second implementation pass (2026-09-01), applying the reviewer's exact
remedy for the two `erase.go` sites:**

- `git status --porcelain -- internal/subscribers/erase.go` was empty
  immediately before editing and again immediately before committing
  (§8a — that file is another session's territory, `#0336`).
- Re-verified the anchor myself: `grep -c "UPDATE email_sends"
  internal/subscribers/erase.go` → `1`, at line 218 both today and at
  `bace190` (`git show bace190:internal/subscribers/erase.go`), and both
  citations at identical positions at `bace190` — wrong at birth, not
  drifted.
- `ISSUE=0359b scripts/check.sh go ./internal/subscribers/...
  ./internal/handlers/... ./internal/outbox/... ./internal/mailing/...
  ./internal/seo/...` — build/vet/`gofmt -l` clean; all five packages `ok`
  (subscribers 0.611s, handlers 6.440s, outbox 6.281s, mailing 3.813s, seo
  0.876s), zero skips, `VERIFICATION PASSED`. (The run's per-agent test
  database template fell back to a full migration build rather than the
  usual ~0.2s clone — this is `#0360`'s known stale-template issue, not a
  regression from this change.)
- `ISSUE=0359b scripts/check.sh guards` — all guard suites
  `VERIFICATION PASSED`, `go_file_visit_floor_guard_test.sh: all guards
  hold (0 failures)`; floor populations externally re-measured unchanged at
  `42` total / `12` non-exempt.
- `shasum -a 256 internal/subscribers/erase.go` equals `gofmt
  internal/subscribers/erase.go | shasum -a 256`
  (`35125d19…6510ac806` both sides); `gofmt -l internal cmd web` silent.
- `grep -rn 'lines above\|lines below\|lines up\|lines down' internal cmd
  web --include='*.go'` → only `campaign_progress_test.go`'s "a few lines
  up" and `shutdown_wiring_test.go`'s "a few lines down" remain, both
  deliberately approximate and out of scope, as the review predicted.
- Comment-only, proved structurally: `commentonly` (`go/parser` mode 0 —
  comments never parsed, so cannot enter the comparison by construction),
  parses HEAD's and the working tree's `erase.go`, prints each via
  `go/printer` `RawFormat` with comments stripped, and diffs the canonical
  output plus per-declaration signatures. `VERDICT: OK comment-only=true
  decls=5 bytes=4202`. Proved falsifiable (a fixture with a real code
  change correctly reported `comment-only=false`) and fail-closed on six
  probes (missing file, zero args, one arg, empty file, syntactically
  broken file, directory-as-file — each an explicit `VERDICT: ERROR: …`,
  never a false `OK`) before trusting it.
- `uptime` 1.8–2.1 throughout, well under ~8. `:5173` confirmed free with
  `lsof` before the guards run, never `pkill -f`. Own database
  `opencircuit_test_0359b`, dropped by `scripts/check.sh` on exit. Did not
  touch `citation_target_guard_test.go`, `scripts/go_file_visit_floor_guard_test.sh`,
  `internal/outbox/claim_comment_referent_guard_test.go`, `issues/0358.md`,
  or `scripts/testdb.sh`.

## Files changed

- `internal/handlers/citation_target_guard_test.go` — replaced the bare
  "line 25" positional reference with a stable "package doc comment"
  anchor. Comment-only, 4 lines changed.
- `internal/subscribers/erase.go` — replaced both "three lines above"
  positional references with an anchor on the file's sole `UPDATE
  email_sends` statement. Comment-only, 5 lines changed across two blocks
  (4 lines at 86-89, unchanged count; 5 lines at 264-268, unchanged count).

## Relation

- Found by: [#0356](0356.md)'s review
- Depends on: [#0356](0356.md) — the guard whose scope this sits just outside
- Relates to: [#0347](0347.md) — the other guard family that cannot see it
- Relates to: [#0352](0352.md) — where "stable identifiers, not corrected
  numbers" was settled
- Relates to: [#0358](0358.md) — the precedent for pinning calibration in
  fixtures instead of prose
- Relates to: [#0113](0113.md) — the PRD index made heading-anchored for this
  same reason

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0356`'s review (§9).
- 2026-09-01 — Implementation (Sonnet, phase 2). Built a falsifiable,
  fail-closed AST census tool (`f.Comments`-only, `ParseComments`),
  proved it on fixtures before trusting it, ran it against `internal/`
  and `cmd/` (38 hits), classified all 38 by hand against the surrounding
  code (not the wording alone), and found exactly one real bare positional
  citation — the issue's own known instance. Per criterion 1's stated
  threshold, built no guard; fixed the one site by replacing "line 25"
  with a re-measured, non-drifting "package doc comment" anchor that
  respects why the literal token cannot be spelled there. `ISSUE=0359
  scripts/check.sh go/guards` both green; floor population unchanged at
  42/12; gofmt-stable. Status left `in-progress` for review.
- 2026-09-01 — Review (Opus, phase 3). **Bounced.** The decision is right and
  the one fix is right; the census the decision rests on is not wide enough,
  and the class it structurally cannot match holds two live sites. §3
  escalation decision: **stay on Sonnet.** This is the first bounce, and the
  remedy below carries the exact replacement text, the verified-unique
  anchors, the exact strike-and-correct wording, and the verification
  commands — it is transcription, not design.
- 2026-09-01 — Implementation, second pass (Sonnet, phase 2). Applied the
  reviewer's exact remedy: verified `internal/subscribers/erase.go` clean
  before editing and again before committing (§8a), independently confirmed
  the `UPDATE email_sends` anchor (line 218, `grep -c` = 1) and that both
  citations were wrong at birth (`bace190` shows identical positions to
  today), replaced both "three lines above" citations with anchors on that
  unique statement, and made the two `## Census and decision` corrections
  per `Issues.md`'s strike-and-date convention (34→27, plus the 271/272
  coverage and pattern-limit notes). Built and proved (falsifiable,
  fail-closed on six probes) a standalone `commentonly` tool to establish
  the `erase.go` diff is comment-only structurally, not by inspection alone.
  `ISSUE=0359b scripts/check.sh go/guards` both green across five packages,
  zero skips; floor population unchanged at 42/12; gofmt-stable. Did not
  build a guard, did not touch `citation_target_guard_test.go` or any file
  another session owns. Status left `in-progress` for review.

## Review notes

> 🔴 **Bounced** 2026-09-01 (review, `claude-opus-5`) at `64f2235` / `3f717f3`.
> The **judgement is correct and I confirm it independently**: with a census
> far wider than the implementation pass's, the tree still holds no absolute
> cross-file `line N` citation after the fix, and building a guard for this
> class would be wrong. The fix to `citation_target_guard_test.go` is correct,
> its referent re-measured, and criteria 2, 3, 5 and 6 all hold.
>
> It is bounced on **criterion 1 alone.** The census instrument's three
> patterns cannot match a plural, spelled-out, counted offset — and exactly
> that shape occurs **twice in `internal/subscribers/erase.go`**, stating one
> fact whose literal reading is **wrong by ~45 lines**, and wrong since the
> commit that introduced it. Criterion 1 is "census, classify, then fix them";
> two sites were never surfaced, so never classified and never fixed.

### What I verified independently, and what held

**My own instruments, proved before use (two of them, deliberately different
from theirs).** Both are compiled Go binaries over `go/parser` +
`parser.ParseComments`, walking only `f.Comments`, and both print an explicit
`VERDICT:` line that I read rather than an exit code.

- `linescan` matches **per physical comment line** against **twelve** pattern
  families in two tiers: `line[s] N` (with optional `~`/`#`/`:`), `N(st|nd|rd|th)
  line`, `L<N>`, `<file>.<ext>:<N>` for eleven extensions, spelled-out and
  ordinal words before `line[s]`, `line[s] above/below/up/down/earlier/later`,
  `(a few|several|two…ten|<N>) lines (down|up|below|above|later|earlier|back)`,
  `near/at/toward the (top|bottom|start|end|beginning) of`, `the line[s]`,
  `see/at/on/in/per/cf. line[s]`, `line number|lineno|offset N|column N`, and
  `first/second/…/last <entry|row|item|paragraph|clause|block|…>`. Tier A is the
  widest possible net: **any** comment line mentioning `line`/`lines` at all.
- `linejoin` exists because per-line matching cannot see a phrase broken across
  a comment wrap — which is a real limitation of the implementation pass's tool
  too. It joins each comment **group** into one whitespace-collapsed text before
  matching. This is what surfaced `erase.go:257-294`, where "three" ends one
  physical line and "lines above" begins the next.

**Falsifiability, proved before use.** A fixture carrying a `//` citation, a
`/* */` block citation, a TAB-indented `L1234`, and **string-literal and
raw-string decoys** holding `line 99`, `L555`, `near the top of bar.go`,
`line 88`, `the second line`: both tools reported the comment hits and **none**
of the literal decoys. A negative-control file reported `VERDICT: OK … hits=0`,
so "found nothing" is distinguishable from "could not run".

**Fail-closed, proved before use.** Six probes on `linescan` and five on
`linejoin` — zero args, nonexistent root, a file where a directory was
expected, a directory with zero `.go` files, a syntactically broken `.go` file,
and (for `linescan`) an unreadable file — each printed an explicit
`VERDICT: ERROR: …`. Neither tool can report a census it did not perform. A
third fail-closed arm is internal: both refuse to report if the AST walk
yielded zero comment lines / zero comment groups.

**My census, against the working tree at `3f717f3`:**

| | Implementation pass | This review |
|---|---|---|
| Roots | `internal cmd` | `internal cmd web` |
| `.go` files | **271** | **272** |
| Comment lines / groups | — | 31,317 lines · 5,051 groups |
| Pattern families | 3 | 12 (per-line) + 7 (group-joined) |
| Raw hits | 38 | **375** Tier A · **144** Tier B · **57** group-joined |
| Genuine drifting sites found | 1 | **3** (in 2 distinct facts, 2 files) |

**Absolute cross-file `line N` citations: zero remain.** Post-fix, every
`line-N` hit my group-joined pass reports is either
`dangling_test_citation_guard_test.go:838-846` (the fixture's own
`// line 1`…`// line 9` annotations) or prose. The
`citation_target_guard_test.go:264` row is gone. That half of the census
reproduces exactly.

**Their classification, spot-checked at the boundary rather than the obvious
end, holds everywhere I looked.** I re-read the code around, not just the
wording of:

- `dangling_test_citation_guard_test.go:838-846` — the `// line N` annotations
  are on `src`'s own constructed string; `blockOpeningLine = 3` really is the
  `/*` line and `citationLine = 6` really is the planted citation, and both are
  asserted programmatically. Self-checking. ✓
- `docs_parity_test.go:648,652,654,659` — "Line 2" / "Line 4" describe the `doc`
  literal built at `:667-671` in the same function. Line 2 of that literal
  really is the 4-column-indented ```` ``` ````; line 4 really is
  ``` ``` bogus`info ```. Accurate and self-contained. ✓
- `audit_email_metadata_guard_test.go:1657` — "the second line below" is the
  fixture `src` built immediately underneath. Local. ✓
- `issue_citation_guard_test.go:479` — "The line #0268 draws" is *line* as
  boundary. Pure idiom. ✓
- `imports.go:628`, `testdb.go:178`, `shutdown_wiring_test.go:145`,
  `campaign_progress_test.go:101` — same-declaration, deliberately approximate
  ("a few lines up/down"), or about **runtime stderr output** rather than
  source position. None asserts a checkable positional fact. ✓

**Criterion 2 — met.** `linecensus.go` genuinely walks only `f.Comments` from a
`ParseComments` parse, so string literals cannot enter by construction; I read
the source, and the fixture proofs are real. The instrument is honest. Its
*coverage* is the problem, not its integrity.

**Criterion 3 — met, referent re-measured from the tree, not the issue.**
`internal/handlers/admin_workshops.go` lines **1–35** are one comment block
sitting immediately above `package handlers` at line 36 — so it *is* the
package doc comment — and the four-segment token
`internal/seo/seo.go/sitemap.go` sits inside it at line 25. The new sentence is
true. "Package doc comment" is genuinely unique per file (Go permits one
package clause per file) and is invalidated only by an edit to that comment,
never by an edit above it — the §11 / `#0352` property. It also respects the
original constraint: it names the *location* without spelling the slash-joined
token, so `pathCitationIsExcluded`'s own guard, which scans this very comment,
is not made self-referential. `internal/handlers` is `ok`.

**Criterion 5 — met, re-run.** `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(`
call syntax: zero in the diff and zero in the file.
`TestNoClaimMachineryCallSyntaxLiteralInAnyComment` passes repo-wide. Floor
harness populations unchanged and externally re-measured: **42 total / 12
non-exempt** (`claimKindsGuardMinPlausibleCallSiteCount=5` against 42,
`…NonExemptCallSiteCount=8` against 12). `scripts/go_file_visit_floor_guard_test.sh`
was **clean in `git status` before and after** my run — I did not touch it.

**Criterion 6 — met.** `shasum -a 256 internal/handlers/citation_target_guard_test.go`
equals `gofmt … | shasum -a 256`, both `73f80e43…8ae7303`; `gofmt -l internal cmd web`
is silent.

**Verification runs.** `ISSUE=0359r scripts/check.sh go ./internal/handlers/...
./internal/outbox/... ./internal/mailing/... ./internal/seo/...` —
`VERIFICATION PASSED`, all four `ok` (handlers 6.549s, outbox 6.974s, mailing
3.791s, seo 0.872s), **zero skips**, `gofmt -l` clean.
`ISSUE=0359r scripts/check.sh guards` — `VERIFICATION PASSED`,
`all guards hold (0 failures)`. `uptime` 1.99–2.16 throughout, well under ~8.
`:5173` confirmed free with `lsof` before the guards run, never cleared with
`pkill`. Own database `opencircuit_test_0359r`, dropped by `check.sh` on exit.
No leftover `go test` / `.test` processes. Every fixture and binary lived in a
run-unique scratchpad subdirectory; the shared tree was never mutated.

### Why this is bounced — the census cannot see the shape it missed

The instrument's whole pattern set is three regexes:

```go
numPattern     = regexp.MustCompile(`(?i)\bline[s]?\s+[0-9]+(\s*[-–]\s*[0-9]+)?\b`)
ordinalWord    = regexp.MustCompile(`(?i)\bthe\s+[a-z]+(-[a-z]+)?\s+line\b`)
ordinalNumeric = regexp.MustCompile(`(?i)\b[0-9]+(st|nd|rd|th)\s+line\b`)
```

`ordinalWord` requires a literal `the` and the **singular** `line`. `numPattern`
requires a **digit**. So a counted offset written the ordinary English way —
*"three lines above"* — matches none of the three, and neither do `L25`,
`line ~25`, `a few lines down`, or `near the top of`. Criterion 1 asked for
"`line N`, `lines N–M`, `the Nth line` **and near neighbours**"; the near
neighbours were not covered, and this is not a hypothetical gap.

**The two sites it missed, both in `internal/subscribers/erase.go`.** I
confirmed `erase.go` appears **nowhere** in the pass's own
`census_real_run.txt`, so these were never classified.

1. **`erase.go:88-89`** (doc comment of `erasedEventPlaceholder`):
   *"…unlike the per-row email_sends placeholder **three lines above it** in
   Erase"* — "it" being Erase's redaction comment, which begins at line 257.
2. **`erase.go:265-266`** (inside `Erase`):
   *"…deliberately different from the email_sends redaction **three lines
   above**"* — the phrase split across the wrap, which is why only the
   group-joined pass sees it whole.

**Measured, not argued.** The only `UPDATE email_sends` in the file is at line
**218** (its own comment at 210–216); it is the sole statement either sentence
can mean. From the citation at 265–266 that is **~48 physical lines above**;
from the redaction comment at 257 it is **~40**. Not three. And this did not
drift: `git show bace190:internal/subscribers/erase.go` puts `UPDATE
email_sends` at 218 and the citation at 265–266 — **identical positions**. It
was wrong at birth, the same shape as §11's §14 row.

The one charitable reading is "three *statement blocks* above" (the
addSuppression block, the suppression-EXISTS block, the email_sends block).
That reading is defensible and it is *not what the words say* — and a reader
who follows the words lands on
`return ErasureResult{}, fmt.Errorf("subscribers: adding erasure suppression…`
and finds nothing. That is precisely the failure `#0359` exists to remove, and
it is worse than the site that was fixed: `#0356`'s reviewer had at least
confirmed "line 25" was *accurate* on the day.

**Two smaller record defects, same family as this issue's subject.**

- `## Census and decision` asserts "**34** of the 38 raw hits are ordinary
  English sentences". Its own classification table says 12 (idiomatic) + 15
  (abstract algorithm) = **27**. The table's five counted rows do sum correctly
  to 38 (12+15+9+1+1); the sixth row is an explicitly-double-counted note, and
  the "34" is a number the classification does not support. An issue about
  prose asserting more than a check establishes should not do it itself.
- The census scanned **271** `.go` files; the repo has **272**. `web/embed.go`
  was outside both roots. Harmless in outcome — I scanned it and it has **zero**
  hits in either tier — but the coverage claim should state what it covered.

### The build/don't-build ruling — I agree, and my wider census strengthens it

**Do not build a guard.** Explicitly, so it is not reopened:

1. **The population is still tiny after widening 3 patterns to 19.** Three
   genuine sites, two distinct facts, two files — inside criterion 1's "one or
   two sites, fix them and build nothing" by any reasonable reading, and the
   remedy below takes it to zero.
2. **The false-positive surface is worse than `#0356`'s, and I measured how
   much worse.** `#0356`'s class had a filename adjacent to a number to anchor
   on and still cost a planning pass and three review rounds. This class has no
   anchor at all: of my 375 Tier-A hits, **372** are ordinary English about
   lines of code, log output, markdown parsing, or postal addresses. A guard
   would need exemption machinery at least the size of `#0356`'s to protect a
   population that fits in a sentence.
3. **A textual guard could not decide the case that actually matters.** The
   `erase.go` sentences are *grammatically identical* to
   `campaign_progress_test.go:101`'s "a few lines up" and
   `shutdown_wiring_test.go:145`'s "a few lines down", which are fine. What
   separates them is whether the count is *true*, and no regex can evaluate
   that — the same limit `#0356` recorded and `#0330`/`#0347` terminated on.
   An exemption list would be `#0337`'s "syntactic exemption for a semantic
   property" again.
4. **`#0258` generalises exactly.** Six passes were spent defending one file
   against decoys added to that file. Standing up new machinery for a known
   population of three, in a repo that has already ruled three times that the
   terminating move is to stop claiming more than a check establishes, buys a
   seventh bounce.

**What would change my mind** (recorded so it need not be re-derived): a census
returning a double-digit population of *counted* offsets, or a demonstrated
recurrence — someone writing a fresh one after these are fixed. Neither is true
today.

### Remedy — apply exactly this. It is transcription; stay on Sonnet.

**0. Before touching `internal/subscribers/erase.go`, run
`git status --porcelain -- internal/subscribers/erase.go`.** Another session
owns the subscribers/growth code (`#0336`). If it is **not** empty, stop and
report rather than editing (§8a). It was clean at `3f717f3`. Re-read the region
before editing — the line numbers below are from `3f717f3` and another commit
may have moved them; match on the **text**, not the numbers.

`erase.go` is scanned by none of the name-forbidding guards (`#0347`'s is about
`.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call syntax, `#0356`'s about
`<file>.go:<int>`), so the wording below is unconstrained. **Use no backticks**
in the replacement text — §8's gofmt doc-comment rewriting is not a risk worth
running for no gain.

**1. `internal/subscribers/erase.go`, block A (lines 86–89 at `3f717f3`).**
Replace exactly this:

```go
// erasedEventPlaceholder builds the subscriber_events email placeholder for
// a given subscriber id — see Erase's redaction comment for why this is
// keyed by the SUBSCRIBER id (unlike the per-row email_sends placeholder
// three lines above it in Erase).
```

with exactly this:

```go
// erasedEventPlaceholder builds the subscriber_events email placeholder for
// a given subscriber id — see Erase's redaction comment for why this is
// keyed by the SUBSCRIBER id (unlike the per-row email_sends placeholder
// built inline by Erase's own UPDATE email_sends statement).
```

**2. `internal/subscribers/erase.go`, block B (lines 264–268 at `3f717f3`).**
Replace exactly this:

```go
	// The placeholder is derived from the SUBSCRIBER's id, not each row's
	// own id — deliberately different from the email_sends redaction three
	// lines above, whose placeholder is per-row so two erased subscribers'
	// email_sends rows can never be correlated. Here the point is the
	// opposite: once subscriber_id is NULL, the placeholder is the only
```

with exactly this:

```go
	// The placeholder is derived from the SUBSCRIBER's id, not each row's
	// own id — deliberately different from the UPDATE email_sends redaction
	// earlier in this method, whose placeholder is per-row so two erased
	// subscribers' email_sends rows can never be correlated. Here the point
	// is the opposite: once subscriber_id is NULL, the placeholder is the only
```

Line count is unchanged (5 → 5) and the continuation into the next line
("`// key left, and an unlinked pile of rows…`") still reads correctly.

**Why this anchor.** `UPDATE email_sends` occurs **exactly once** in the file
(verified: `grep -c` = 1, at line 218). It names a unique statement rather than
a position, so it survives any edit that does not touch that statement — the
`#0352` property, and the same property the `admin_workshops.go` fix relies on.
Both diffs are comment-only.

**3. `issues/0359.md` — correct the record, per `Issues.md`'s
strike-and-date convention.**

- In `## Census and decision`, strike the "34" figure and correct it. Strike the
  **original bytes** only, `~~` around them and nothing else, so the struck span
  with `~~` removed is byte-identical to the original:
  ~~34 of the 38~~ → **corrected 2026-09-01 (#0359 review)**: 27 of the 38
  (12 idiomatic + 15 abstract-algorithm, per this section's own table).
- In `## Census and decision` **Method** and `## Verification` criterion 1, add
  the coverage and the limit, both measured by the review:
  - the run covered **271** of the repo's **272** `.go` files; `web/embed.go`
    lies under `web/`, outside both roots, and has **zero** hits — so the
    omission changed nothing, but the claim should say what it covered;
  - the three patterns require either a digit or a literal "the" plus the
    singular "line", and match **per physical comment line**, so they cannot see
    a spelled-out counted offset ("three lines above"), `L<N>`, `near the top
    of`, or any phrase broken across a comment wrap. The review's group-joined
    pass over 12 + 7 pattern families found **three** genuine sites, not one.
- Add the two `erase.go` sites to the classification, and the fix to `## Fix`
  and `## Files changed`.
- Leave the **decision** — do not re-derive it. Record that the review's wider
  census confirmed it: three genuine sites, two distinct facts, two files, and
  372 of 375 Tier-A hits ordinary prose.

**4. Verify.** Then, and only then:

```bash
ISSUE=<yours> scripts/check.sh go ./internal/subscribers/... ./internal/handlers/... \
  ./internal/outbox/... ./internal/mailing/... ./internal/seo/...
ISSUE=<yours> scripts/check.sh guards
shasum -a 256 internal/subscribers/erase.go
gofmt internal/subscribers/erase.go | shasum -a 256   # must be equal
gofmt -l internal cmd web                             # must be silent
```

and confirm the phrase is gone, both whole and split:

```bash
grep -rn 'lines above\|lines below\|lines up\|lines down' internal cmd web --include='*.go'
```

Expect only `campaign_progress_test.go`'s "a few lines up" and
`shutdown_wiring_test.go`'s "a few lines down" — both deliberately approximate,
same-declaration, and correctly out of scope.

**Nothing else changes.** Do not touch `citation_target_guard_test.go` (its fix
is right), do not build a guard, do not add fixtures or floors, and do not
touch `scripts/go_file_visit_floor_guard_test.sh`.

### Reported to the orchestrator, not filed here (§9)

- **`internal/subscribers/erase.go`'s two sites** are **not** a follow-up issue.
  They are inside `#0359`'s own scope and its own criterion 1, which is why this
  is a bounce rather than a resolve-plus-file.
- **Observation, out of scope, no issue proposed:** this census covered Go
  comments only, as criterion 1 scopes it. Bare positional prose in `scripts/`,
  `docs/`, and `issues/` was not examined and is invisible to every guard family
  in this repo.
