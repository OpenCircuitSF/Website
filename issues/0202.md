# 0202 — The walk-coverage test's doc comment justifies its invariant backwards

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | handlers |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |
| **Commit** | 944a562 |
| **Closed** | 2026-08-23 |

## Description

`#0201` made the walk-coverage test's ground truth exclude vendored trees by
anchored path prefix. Its phase-3 review verified the behaviour exhaustively and
passed it — then found the doc comment explaining *why* the invariant holds is
wrong.

The comment says `onDisk` "is a superset of that same walk **(it additionally
counts the two vendored prefixes `onDisk` would otherwise miss)**".

The ground truth does **not** count those prefixes — excluding them is the
entire change `#0201` made. That clause describes the pre-`#0201` unrestricted
walk, and survived the edit.

What `onDisk` actually counts in addition is **name-matched directories outside
the anchored prefixes**: `internal/dist`, `internal/node_modules`,
`web/src/node_modules`. That is precisely the set the two mechanisms disagree
about, and precisely why the test can still catch the shipped walk narrowing.

The conclusion the comment draws — `collected ⊆ onDisk` — is correct, and the
review confirmed it by execution. Only the justification is backwards.

**This is worth fixing despite being one clause**, because this test's value
lives almost entirely in that comment. The test is a set comparison; what makes
it meaningful rather than circular is the argument for why the two enumerations
differ where they do. A reader who trusts the current clause would conclude the
ground truth still walks `node_modules` — the exact thing `#0201` removed — and
could "simplify" the anchoring away on that basis.

## Acceptance criteria

- [x] The doc comment states what `onDisk` actually counts in addition: name-matched directories outside the anchored vendored prefixes
- [x] It names the concrete cases (`internal/dist`, `internal/node_modules`, `web/src/node_modules`) so the disagreement set is legible
- [x] The `collected ⊆ onDisk` conclusion is retained — it is correct
- [x] No behaviour change: `#0201`'s four plants still produce the same results, including the adversarial `internal/node_modules/` case
- [x] `ISSUE=0202 scripts/check.sh go ./internal/...` passes with zero skips

## Implementation notes

Reworded the parenthetical inside `TestScanDirForCitationsCollectsExactlyDiskFiles`'s
doc comment (`internal/handlers/citation_guard_test.go`, the sentence right
before the "#0200's review tried every candidate cause" paragraph). New text:

> "on disk" is a superset of that same walk (it additionally counts
> name-matched directories that lie outside the two anchored prefixes —
> internal/dist, internal/node_modules, and web/src/node_modules — since the
> anchoring does not exclude them), so collected is always a subset of
> onDisk.

Comment-only change; the anchoring logic (`path == prefix ||
strings.HasPrefix(path, prefix+string(filepath.Separator))`) was not touched.
No other file was modified — checked `git status` before starting and again
before committing; `internal/handlers/dangling_test_citation_guard_test.go`
(`#0199`'s file) was left alone throughout.

## Verification

Re-ran `#0201`'s four plants by hand (created, tested, removed; `git status
--porcelain` confirmed clean after each cleanup):

| # | Plant | Expected | Observed |
|---|---|---|---|
| 1 | `internal/dist/proof.go` | FAIL | **FAIL** — named `internal/dist/proof.go` under "present on disk but NOT collected" |
| 2 | `web/node_modules/zz0202vendored/vendored.go` | PASS | **PASS** |
| 3 | `internal/handlers/zz0202_ordinary.go` | PASS | **PASS** |
| 4 | `internal/node_modules/zz0202/adversary/adversary.go` and `web/src/node_modules/zz0202/adversary/adversary.go` | FAIL | **FAIL** — both named individually under "present on disk but NOT collected" |

All plants (including `internal/dist/`, `internal/node_modules/`, and
`web/src/node_modules/`) removed afterward; `git status --porcelain` showed
only the intended comment edit throughout.

95/95 baseline unchanged (`find internal cmd web -name '*.go' ! -name
'*_test.go' ! -path '*/node_modules/*' ! -path '*/dist/*' | wc -l` → 95).
`#0181`'s guard (`TestNoAdminFacingStringCitesInternalDocs`) and `#0196`'s
guard (all four `TestDanglingTestCitationPattern*` tests in
`dangling_test_citation_guard_test.go`) pass.

`uptime` before the run: `1.63 2.08 2.49` (well under the ~8 threshold).
`ISSUE=0202 scripts/check.sh go ./internal/...`: `go build` and `go vet`
clean, all 15 packages `ok` with real elapsed times (`handlers` 3.070s,
`interests` 2.963s, lowest `devstore` 0.134s — no package at 0.00s), skip
audit `(no skips)`, leftover processes `(none)`, `VERIFICATION PASSED`.
Database `opencircuit_test_0202` dropped on exit (script cleanup confirmed
it no longer exists).

## Notes

Reported by `#0201`'s phase-3 review under `CLAUDE.md` §9.

This is a comment-only change. Do not touch the anchoring logic — the review
mutation-tested it: reducing `path == prefix || HasPrefix(path, prefix+sep)` to
a bare `HasPrefix(path, prefix)` makes a planted `web/node_modules_extra/`
sibling fail, so the separator guard is load-bearing and not incidental.

`#0201`'s `## Review notes` carries the full plant table, including two
adversarial cases worth keeping in mind while rewording: `internal/node_modules/`
and the nested npm-workspaces shape `web/src/node_modules/`, both correctly
caught.

## Relation

- Found by: [#0201](0201.md) — the anchored vendored exclusion
- Relates to: [#0200](0200.md) — the walk-coverage invariant itself
- Relates to: [#0194](0194.md) — the name-based skip the ground truth must stay independent of

## Review notes — 2026-08-23 (phase 3, **passes**)

Reviewed code commit `944a562` (`internal/handlers/citation_guard_test.go` only,
+5/−3). All verification ran in a throwaway `git worktree` detached at
`944a562`, removed afterwards, so no plant ever touched the shared checkout
while `#0199`'s re-review was live. Nothing outside `issues/` has changed
between `944a562` and the current tip (`git diff --stat 944a562 HEAD` lists
`issues/0199.md` and `issues/0202.md` only), so these results stand at tip.

**The diff is genuinely comment-only.** `git diff 944a562^ 944a562` touches
five contiguous comment lines inside the doc comment. `excludedPrefixes`, the
`excluded` predicate (`path == prefix || strings.HasPrefix(path,
prefix+string(filepath.Separator))`), both `WalkDir` bodies, the set
difference, and every failure message are byte-identical. `#0201`'s mutation
proof that the separator guard is load-bearing is therefore untouched and was
not re-run.

### The new text, claim by claim

> "on disk" is a superset of that same walk (it additionally counts
> name-matched directories that lie outside the two anchored prefixes —
> internal/dist, internal/node_modules, and web/src/node_modules — since the
> anchoring does not exclude them), so collected is always a subset of onDisk.

| Claim | Verdict |
|---|---|
| `onDisk ⊇ collected` — superset stated in the right direction | **True.** `onDisk` excludes only paths under `web/node_modules` and `web/dist`; both basenames are keys in `skipVendoredDirNames`, so everything `onDisk` drops the shipped walk drops too. Nothing can be collected-but-not-on-disk for a static tree. |
| Grammar says what it means | **Yes.** "it" attaches unambiguously to `"on disk"`, and "additionally counts … since the anchoring does not exclude them" is the direction the code actually implements. The old clause's failure — describing the pre-`#0201` unrestricted walk — is gone. |
| Naming `internal/dist`, `internal/node_modules`, `web/src/node_modules` | **Correct as instances, and correctly framed.** The general rule ("name-matched directories that lie outside the two anchored prefixes") is stated *before* the list, so the em-dash list reads as apposition to that rule rather than as a closed set. |
| The list is not exhaustive — proved by execution | **Confirmed.** See below. |
| `collected ⊆ onDisk` conclusion retained | **Yes**, unchanged and correct. |

### Planting a name-matched directory outside the three named

The characterisation had to be true as a *rule*, not just for three instances,
so two name-matched directories that appear nowhere in the comment were planted
in the worktree:

| Plant | Result |
|---|---|
| `cmd/node_modules/zz0202rev/out.go` | **FAIL** — named `cmd/node_modules/zz0202rev/out.go` under "present on disk but NOT collected" |
| `internal/handlers/dist/zz0202rev_out.go` | **FAIL** — named `internal/handlers/dist/zz0202rev_out.go` under the same heading |

Both fire, so the rule the comment states is the operative one and the three
named directories are illustrations of it. The comment is right.

### `#0201`'s four plants, re-run

Each created, tested, removed; `git status --porcelain` showed 0 dirty after
every cleanup.

| # | Plant | Expected | Observed |
|---|---|---|---|
| 1 | `internal/dist/proof.go` | FAIL | **FAIL** — named under "present on disk but NOT collected" |
| 2 | `web/node_modules/zz0202rev/vendored.go` | PASS | **PASS** |
| 2b | `web/dist/zz0202rev_build.go` (added — the second anchored prefix) | PASS | **PASS** |
| 3 | `internal/handlers/zz0202rev_ordinary.go` | PASS | **PASS** (both guards) |
| 4 | `internal/node_modules/…` and `web/src/node_modules/…` | FAIL | **FAIL** — both named individually |

Identical to `#0201`'s table. Plant 2b is new and confirms the other anchored
prefix is excluded from both sides symmetrically.

### The rest

- **95/95 baseline unchanged** — `find internal cmd web -name '*.go' ! -name
  '*_test.go' ! -path '*/node_modules/*' ! -path '*/dist/*' | wc -l` → **95**
  in the worktree at `944a562`.
- **`#0181`'s guard** (`TestNoAdminFacingStringCitesInternalDocs`) and
  **`#0196`'s guard** (all four `TestDanglingTestCitationPattern*` tests) both
  **pass**, tested at `944a562` — which already contains `#0199`'s fix
  (`d05c107`). `internal/handlers/dangling_test_citation_guard_test.go` is
  byte-identical between `944a562` and the current tip, so that result holds
  after `#0199` resolved.
- **`ISSUE=0202rev scripts/check.sh go ./internal/...`** — `uptime` first,
  2.23/2.21/2.44 (well under the ~8 threshold); `go build` and `go vet` clean;
  all **15** packages `ok` with real elapsed times (`handlers` 2.678s,
  `interests` 2.439s, lowest `devstore` 0.205s — no package at 0.00s); skip
  audit `(no skips)`; leftover processes `(none)`; `VERIFICATION PASSED`.

Cleanup confirmed: worktree removed and pruned, working tree 0 dirty,
`pg_database` has no `%0202rev%` row, `pgrep -f 'go test|\.test '` empty.

### Two notes for the orchestrator (reported, not filed — `CLAUDE.md` §9)

1. **A dangling cross-reference one paragraph up, pre-existing from `#0201` and
   outside this diff.** Line 448 sends the reader to "that map's own doc comment
   on internal/dist as the live example", but `skipVendoredDirNames`' doc
   comment (lines 163–178) never mentions `internal/dist` — it discusses only
   `web/node_modules` and `web/dist`. `internal/dist` also does not exist on
   disk, so "the live example" overstates a hypothetical. Same class of defect
   as this issue, in the same comment block; a one-clause fix.
2. **Cosmetic, not worth an issue.** `onDisk` is a set of file paths, so
   "additionally counts name-matched directories" is shorthand for counting the
   non-test `.go` files inside them; and an "e.g." before the three names would
   put the illustrative-not-exhaustive reading beyond doubt. Neither misleads a
   reader in context, and both are inside the sentence this issue just rewrote —
   folding them into item 1's fix would be cheaper than a separate pass.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 40 | 2,358 | 1,185,061 | 49,340 | $0.58 |
| 2026-08-23 | review | claude-opus-5 | 40 | 2,239 | 1,170,747 | 58,071 | $1.00 |

**Total: $1.58**
