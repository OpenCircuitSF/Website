# 0174 — The cancel dialog says "never published" to someone who published and then unpublished, and one sitemap case lost its test

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | All |
| **Module** | web / seo |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |
| **Commit** | 202a380 |

## Description

Two small residues of `#0171`, both found by its phase-3 review.

**1. The confirm copy is false on one path.** `#0171` gave `cancelConfirmMessage` a `wasPublished` flag so the dialog stops promising a draft "stays visible". The never-published branch says **"It was never published"** — which is false on the `published → unpublished → canceled` path. That workshop *was* published; unpublishing cleared `published_at`.

The operative claim ("so it stays private") is **true**, and the behavior is right — `#0171`'s review proved that path stays private on all three surfaces. This is copy accuracy, not a repeat of the defect. But it is the same species of error `#0171` exists to fix: a sentence that presumes a fact the case does not have.

*"It is not currently published, so it stays private"* is true in both sub-cases.

The call site itself is correct and was checked: `!!workshop.published_at`, `omitempty` on the wire, optional in `types.ts`, and `WorkshopEditor.svelte:323` replaces `workshop` with the PATCH response after every transition, so the value is never stale.

**2. A sitemap case lost its coverage.** `sitemap_test.go:70`'s `canceled-1` fixture now defaults to `Published: false`, so **no unit test covers "a *was-published* canceled workshop is excluded from the sitemap"** — the case that actually exercises the gate. Nothing is broken (the sitemap gate is status-only, and `#0171`'s reviewer proved that case by execution), but the assertion that would catch a regression is gone.

One added `Published: true` restores it.

## Acceptance criteria

- [x] The cancel dialog's not-currently-published copy is true on both sub-cases — never published, and published-then-unpublished
- [x] The published-then-canceled branch is unchanged; `#0051`'s "canceled stays visible" ruling is not in question here
- [x] `sitemap_test.go`'s canceled fixture covers a **was-published** canceled workshop, so the exclusion is asserted against the case that exercises the gate
- [x] Both changes proved by mutation — flip the copy branch and the fixture, and confirm a test notices
- [x] `scripts/check.sh web` and `ISSUE=0174 scripts/check.sh go ./internal/...` both pass

## Root cause

**1.** `cancelConfirmMessage`'s not-currently-published branch hardcoded "It
was never published" as if that were the only way `wasPublished` could be
`false`. But `wasPublished` is `!!workshop.published_at` at confirm time, and
`published_at` also reads `NULL` on the `published → unpublished → canceled`
path — a workshop the unpublish step genuinely had made public once. The
sentence presumed a fact ("never published") that only one of the two
`false` sub-cases actually has.

**2.** `#0171` added a `Published` field to `seo.Workshop` for the JSON-LD /
meta-tag gate, which does key off it. `sitemap_test.go`'s `canceled-1`
fixture predates that field and was left with its zero value
(`Published: false`), which silently turned it from "an arbitrary canceled
workshop" into specifically "a *never-published* canceled workshop" —
without anyone deciding that on purpose. `sitemap.go`'s own exclusion gate
is status-only (`w.Status != WorkshopPublished`, `sitemap.go:86`) and never
reads `Published`, so the fixture's meaning drifted out from under the test
without breaking it: nothing exercised the specific claim "a workshop that
really was published, then canceled, is still excluded from the sitemap on
status alone" — the case where a status-vs-published mixup would actually
show up.

## Fix

**1.** Reworded the not-currently-published branch of
`cancelConfirmMessage` (`web/src/lib/workshopAdmin.ts:147`) from "It was
never published, so it stays private" to "It is not currently published, so
it stays private" — true in both sub-cases (never published, and
published-then-unpublished), reads consistently alongside the unchanged
published branch ("It stays visible on the public site, marked canceled,
rather than disappearing"), and the JSDoc above the function needed no
change (it documents the boolean's meaning, not the exact wording of either
returned string, and was already accurate). Updated the matching vitest
assertion in `workshopAdmin.test.ts` to expect `/not currently published/i`
and explicitly assert `/never published/i` no longer matches, with a comment
explaining why.

**2.** Added a second sitemap fixture, `canceled-2`
(`Status: WorkshopCanceled, Published: true`), alongside the existing
`canceled-1` (left as `Published: false`, i.e. never-published), and added
`canceled-2` to the excluded-slugs assertion loop. Chose *add a fixture*
over *change the existing one* (the issue offered both options) so the test
keeps asserting both sub-cases — never-published and was-published-then-
canceled — rather than trading one covered case for the other.

## Verification

`uptime` before every run stayed in the 2–4 range (well under the ~8
threshold `CLAUDE.md` §5 calls out), so no result here is load-contaminated.

- **`scripts/check.sh web`** — `npm run check`: 230 files, 0 errors, 0
  warnings. `npm test`: 20 files / **3611 tests passed** (matches the stated
  baseline exactly — both edits changed existing tests, no new test cases
  were added).
- **`ISSUE=0174 scripts/check.sh go ./internal/...`** — own database
  (`opencircuit_test_0174`), `go build` clean, `go vet` clean, all 15
  packages reported `ok` with real non-zero elapsed times
  (`internal/seo 0.226s`, `internal/handlers 2.904s`, `internal/interests
  2.737s`, …), skip audit reported **(no skips)**.
- **Mutation 1 (copy)** — temporarily reverted the returned string back to
  "It was never published, so it stays private…" and ran
  `npx vitest run src/lib/workshopAdmin.test.ts`. Observed failure:
  `AssertionError: expected 'Cancel "X"? It was never published, s…' to
  match /not currently published/i`, 1 failed / 72 passed. Restored from the
  copy made before editing; diff against the restored file was clean and the
  suite was re-run green (72/72 in that file, 3611/3611 overall).
- **Mutation 2 (sitemap fixture)** — temporarily mutated `sitemap.go:86`'s
  gate from `if w.Status != WorkshopPublished` to
  `if w.Status != WorkshopPublished && !w.Published` (the plausible-but-wrong
  "key off Published instead of Status" regression) and ran
  `go test ./internal/seo/... -run TestSitemap_ExcludesUnpublishedWorkshops
  -v`. Observed failure: `sitemap contains excluded (non-published) workshop
  "canceled-2"` — `canceled-1` and every other fixture still passed under
  the mutated gate, which is exactly the point: only the was-published
  fixture (`canceled-2`) exercises this class of regression. Restored
  `sitemap.go` from the copy made before mutating; diff was clean and
  `TestSitemap_ExcludesUnpublishedWorkshops` re-ran green.
- `pgrep -f 'go test|\.test '` was empty and `scripts/testdb.sh list` showed
  only `opencircuit_test_template` after the run — no leftover processes or
  scratch databases.

## Files changed

- `web/src/lib/workshopAdmin.ts` — `cancelConfirmMessage`'s
  not-currently-published branch: "It was never published" → "It is not
  currently published".
- `web/src/lib/workshopAdmin.test.ts` — updated the matching assertion to
  `/not currently published/i` (and assert `/never published/i` no longer
  matches), with a comment explaining the `#0174` rationale.
- `internal/seo/sitemap_test.go` — added a `canceled-2` fixture
  (`Published: true`) to `TestSitemap_ExcludesUnpublishedWorkshops` and its
  excluded-slugs loop, alongside the existing never-published `canceled-1`.

## Notes

Reported by `#0171`'s phase-3 review under `CLAUDE.md` §9. Both are one-line changes; they are filed together because they are the same issue's residue and would otherwise be two tracker entries for two lines.

Worth noting what the review demonstrated while finding these: it mutated `ListVisible` and `GetBySlug` **separately** and showed neither surface rides on the other's guard — dropping the index guard left the detail route still returning 404, and vice versa. That is the property that makes the fix trustworthy, and it is why the `canceled → draft` row matters most: its `published_at` is genuinely non-NULL, and it stays private on the status check alone.

## Relation

- Found by: [#0171](0171.md) — Canceling a never-published draft makes it public
- Relates to: [#0055](0055.md) — the JSON-LD gate the same review re-verified

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 62 | 5,588 | 2,442,691 | 77,618 | $1.10 |

**Total: $1.10**
