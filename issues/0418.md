# 0418 — The SES region and MAIL FROM subdomain are wrong in the docs and the PRD's DNS table

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | docs |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-03 |
| **Commit** | — |

## Description

Production's SES region is **`us-east-1`**, proved three independent ways by
`#0057`'s planning pass: the instance metadata's `placement/region`,
`AWS_REGION=us-east-1` in `/etc/opencircuit/config.env`, and the live custom
MAIL FROM record `10 feedback-smtp.us-east-1.amazonses.com`.

Two places still say `us-west-2`:

- **`docs/email-setup.md` — six occurrences.** Confirmed by count.
- **`PRD.md` §10.2's DNS table**, which is wrong on **two** independent facts:

	| `mail.opencircuitsf.com`  | MX | `10 feedback-smtp.us-west-2.amazonses.com` |
	| `lists.opencircuitsf.com` | MX | `10 inbound-smtp.us-west-2.amazonaws.com`  |

  Wrong region, **and** wrong subdomain — production's custom MAIL FROM is
  `bounce.mailing.opencircuitsf.com`, not `mail.opencircuitsf.com`.

`CLAUDE.md` §7 already records `us-east-1` correctly, and notes that this
contradicted what the PRD assumed. That correction never reached §10.2 or the
runbook.

## Why it matters

`#0057` is about to add DNS records and SES receipt rules. A runbook naming the
wrong region would have an operator create inbound infrastructure in a region
where the sending identity does not exist — silently, since SES will happily
verify an identity in any region.

This is distinct from `#0414`, which covers the wrong `From:` **address** in the
same file. Same class of drift, different fact, and both live in
`docs/email-setup.md`.

## Acceptance criteria

1. `docs/email-setup.md` names `us-east-1` everywhere it currently names
   `us-west-2`. **Re-derive production's region yourself** rather than copying it
   from this issue — that is the failure mode this whole family exhibits.
2. `PRD.md` §10.2's DNS table matches production on **both** the region and the
   MAIL FROM subdomain. Read the live Route 53 zone rather than reasoning about
   what it should be.
3. **PRD §10.3 covers the SES region choice** — check whether it also names
   `us-west-2`, and whether its stated *reasoning* still holds now that the
   answer is `us-east-1`. A rationale for the wrong answer is worse than a stale
   value.
4. Follow `#0072`'s dated-note convention where §9/§10 blocks are corrected in
   place, and **leave genuine decision-log history intact** — `#0271`'s `## Scope`
   explains why rewriting history is the wrong move. Say which occurrences you
   judged to be which.
5. Editing `PRD.md` means running `./internal/db/...`; if any `##` heading moves,
   §11's section-index table and its parity guard must stay correct. Editing
   `CLAUDE.md` means **both** `./internal/db/...` and `./internal/handlers/...`
   (§5's repo-wide-guard table). Per §5 name the specific tests that ran.
6. **No mechanism** (`#0330`/`#0347`/`#0356`/`#0381`).

## Relation

- Found by: [#0057](0057.md)'s planning pass, which proved the region three ways
- Relates to: [#0414](0414.md) — the wrong `From:` address in the same runbook;
  same drift class, different fact
- Relates to: [#0057](0057.md) — which will add DNS records in the correct region
  and would be misled by these
- Relates to: [#0072](0072.md) — the dated-note convention for correcting
  maintained config blocks in place

## Work log

- 2026-09-03 — Filed by the orchestrator from `#0057`'s planning pass (§9). The
  six-occurrence count and §10.2's two wrong facts were confirmed independently
  before filing.
