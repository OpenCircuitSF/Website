# 0205 — Nothing pins the coupled codepoint lists' lengths, so shrinking one is silent

| | |
|---|---|
| **Status** | open |
| **Platform** | All |
| **Module** | handlers / web |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |

## Description

`#0198` coupled the two citation guards' whitespace classes behaviourally: each
file asserts its own shipped `citationPattern` against a shared canonical
codepoint list, in its own real engine. Its phase-3 review proved the coupling
works in **both** directions and that the reject half bites too.

Then it found the way out of the coupling.

**1. Removing a codepoint from one file's *list* is completely silent.** The
review deleted `0x2007` from Go's `whitespaceClassMustMatch` and `0x200b` from
web's `WHITESPACE_MUST_NOT_MATCH`, one file each, and **both suites stayed
green**. No file asserts its own list length; nothing cross-checks the counts.

This matters because of the repair path it invites. When the coupling test
fails, it fails *naming a codepoint* — and the lazy fix is to delete that
codepoint from the list rather than repair the pattern. After that the guards
have genuinely drifted and **nothing fails anywhere**. The escape hatch is
exactly the shape of the failure message.

The fix is about four lines. `#0187`'s matrix, in these same two files, already
carries precisely this meta-assertion (`MATRIX_COUNTS`) as its precedent: 25 and
10 on each side.

**2. `internal/handlers/citation_guard_test.go:413` lost a token.** The comment
reads "spelled differently — `\x{00A0}` here, ␣ there", with the web spelling
simply **absent** — byte-verified: there is no invisible character in the gap,
the text is missing. Its web mirror is intact.

This is the fingerprint of the NBSP incident `#0198`'s implementer documented:
the same trap was repaired by *deletion* on the Go side and by *escaping* on the
web side. `#0203` is the precedent for filing a doc-comment defect in this
family.

**3. A pre-existing clause in the same file overstates, and `#0203`'s review
asked that it be folded in here rather than given a fourth standalone pass.**

`internal/handlers/citation_guard_test.go`'s disk-comparison doc comment says a
name-matched directory "is swallowed by the shipped walk today with nothing
failing". The review measured **both readings**: "the shipped citation walk
silently skips it and its guard stays green" is **true** (proved by planting a
citation-bearing file under `internal/dist` and watching `#0181`'s guard stay
green); "nothing in the tree fails" is **false** — that same plant fails
`TestScanDirForCitationsCollectsExactlyDiskFiles`, which is precisely `#0200`'s
purpose.

The clause is verbatim `#0201` prose, byte-unchanged by `#0202` and `#0203`, and
the paragraph as a whole is true — the next two sentences do not survive the
false reading. So this is a tightening, not a defect, and it is only worth doing
because this issue is already editing this file.

Cosmetic, same block: `#0203`'s edit did not reflow its paragraph, leaving a
44-column line among ~70-column neighbours.

## Acceptance criteria

- [x] Each of the four lists asserts its own length — 25 for the accept set, 10 for the near-miss set, on both sides
- [x] Deleting a codepoint from any one list makes that side's suite fail — demonstrated by mutation on at least one accept list and one reject list
- [x] The failure message says the list was shortened rather than merely reporting a count, so the repair path is obvious
- [x] `citation_guard_test.go:413`'s comment names both spellings, with the web spelling as literal escape text
- [x] A byte census of both files finds only `§` and em-dash above ASCII
- [x] `#0198`'s two mutation directions still fail as measured, and the reject half still bites
- [x] The "with nothing failing" clause distinguishes the shipped citation walk staying green from the walk-coverage test failing, so neither reading misleads
- [x] That paragraph is reflowed to match its neighbours
- [x] `#0203`'s six plants still produce the same results
- [x] `ISSUE=0205 scripts/check.sh go ./internal/...` and `scripts/check.sh web` both pass

## Implementation notes

Four edits, two per file, all inside `internal/handlers/citation_guard_test.go`
and `web/src/lib/citationGuard.test.ts`. No matching behaviour changed —
`citationPattern` itself is untouched in both files; every edit is either a
new test/constant or a comment.

**Item 1 — length guards.** Added `whitespaceMustMatchCount = 25` /
`whitespaceMustNotMatchCount = 10` (Go, a `const` block) and
`WHITESPACE_LIST_COUNTS = { mustMatch: 25, mustNotMatch: 10 }` (web, mirroring
`#0187`'s `MATRIX_COUNTS` in the same file), then a dedicated test on each
side — Go's `TestWhitespaceClassCanonicalListsHaveNotBeenShortened`, web's
`it('has not had either canonical list shortened (#0205)', ...)` as the first
case in the existing `#0198` coupling `describe` block. Each failure message
names the list, both counts, and says explicitly that the list "has been
shortened," e.g. (Go): `whitespaceClassMustMatch has 24 entries, want 25 —
the accept list has been shortened; restore the deleted codepoint instead of
deleting the next one the coupling test names`. Deliberately a separate
assertion from the existing coupling test rather than folded into its loops,
so a shrinkage failure and a drift failure never share one message.

**Item 2 — `citation_guard_test.go:413`.** The web spelling `\u00A0` was
missing from the parenthetical; restored so it reads "`\x{00A0}` here,
`\u00A0` there," matching the web file's mirror ("`\u00A0` here, `\x{00A0}`
there"), each file naming its own spelling first.

**Item 3 — the `internal/dist` clause.** "is swallowed by the shipped walk
today with nothing failing" was ambiguous between "the shipped citation guard
stays green" (true) and "nothing in the tree fails" (false —
`TestScanDirForCitationsCollectsExactlyDiskFiles`, `#0200`'s purpose, does).
Reworded to name both facts: "...is swallowed by the shipped walk today, and
the citation guard above (`TestNoAdminFacingStringCitesInternalDocs`) stays
green over it — this test, `#0200`'s walk-coverage check, is what actually
catches the gap." The whole paragraph (`"on disk" is a second, independent
enumeration...` through `...no longer fails this test.`) was reflowed with
`textwrap.fill(width=75)`, which also fixes the 44-column outlier line
`#0203` left behind.

All new/changed text was built in a Python script using `chr(92)` for
backslashes and `chr(0x2014)` for the em dash, never typed directly, per
`CLAUDE.md` §8 — the same trap `#0198`'s reviewer and `#0203`'s reviewer both
hit while drafting review notes.

`git status --porcelain` throughout showed only these two files plus another
subagent's unrelated in-flight `web/` changes (`#0204`'s
`CampaignSendDialog.*`), left untouched.

## Verification

**Byte census** (raw bytes, decoded, every codepoint above 127 counted):

- `internal/handlers/citation_guard_test.go` (33,356 B): `—` U+2014 ×59, `§`
  U+00A7 ×21 — nothing else.
- `web/src/lib/citationGuard.test.ts` (45,437 B): `§` U+00A7 ×62, `—` U+2014
  ×57 — nothing else.

**Shrinkage mutations** (`cp` to scratchpad first, restored with `cp`,
`shasum -a 256` matched before/after both times):

- Go accept list: deleted the `0x2007 // FIGURE SPACE` entry from
  `whitespaceClassMustMatch`. `TestWhitespaceClassCanonicalListsHaveNotBeenShortened`
  **FAIL**ed: `whitespaceClassMustMatch has 24 entries, want 25 — the accept
  list has been shortened; restore the deleted codepoint instead of deleting
  the next one the coupling test names`. `TestCitationPatternWhitespaceClassStaysCoupledWithWebGuard`
  stayed green — reproducing `#0198`'s review finding that shrinkage alone
  doesn't trip the coupling test.
- Web reject list: deleted `['ZERO WIDTH SPACE', 0x200b]` from
  `WHITESPACE_MUST_NOT_MATCH`. `has not had either canonical list shortened
  (#0205)` **FAIL**ed: `WHITESPACE_MUST_NOT_MATCH has 9 entries, want 10 —
  the reject list has been shortened; restore the deleted codepoint instead
  of deleting the next one the coupling test names`.

**`#0198`'s original one-sided mutation reproduced** (Go `citationPattern`
itself, not the list): removed `\v` from the shipped whitespace class.
`TestCitationPatternWhitespaceClassStaysCoupledWithWebGuard` **FAIL**ed with
the exact message the `#0198` review recorded: `citationPattern did not match
PRD + U+000B + section — ... the two guards have drifted`. Restored, hash
matched, both new/old tests green afterward.

**`#0187`'s 34-case matrix** (`-t "shape matrix"`): `36 passed | 25 skipped`
(61 total now, vs. 60 before this issue's new test) — same 36 active cases as
`#0198`'s review recorded.

**`#0193`'s 17 whitespace shapes** (`-t "whitespace shape"`): `1 passed | 60
skipped` — unchanged. Go's `TestCitationPatternMatchesWebWhitespaceShapes`
`PASS`.

**`#0203`'s six plants**, reproduced against `TestScanDirForCitationsCollectsExactlyDiskFiles`,
each created, tested, removed, `git status --porcelain` clean after every one:

| # | Plant | Expected | Observed |
|---|---|---|---|
| 1 | `internal/dist/proof.go` | FAIL, naming it | **FAIL** — named under "present on disk but NOT collected" |
| 2 | `web/node_modules/zz0205/vendored.go` | PASS | **PASS** |
| 2b | `web/dist/zz0205_build.go` (tracked `web/dist/index.html` left untouched — added/removed only the planted file, never `rm -rf web/dist`) | PASS | **PASS** |
| 3 | `internal/zz0205proof/proof.go` (ordinary directory, own package) | PASS | **PASS** |
| 4 | `internal/node_modules/zz0205/vendored.go` | FAIL, naming it | **FAIL** — named individually |
| 5 | `web/src/node_modules/zz0205/vendored.go` | FAIL, naming it | **FAIL** — named individually |
| 6 | `cmd/node_modules/zz0205/out.go` and `internal/handlers/dist/zz0205_out.go` | FAIL, naming both | **FAIL** — both named individually |

Identical to `#0203`'s table. 95/95 baseline unchanged (`find internal cmd web
-name '*.go' ! -name '*_test.go' ! -path '*/node_modules/*' ! -path
'*/dist/*' | wc -l` → **95**).

`uptime` before the run: `1.77 2.28 2.81`, well under the ~8 threshold; stayed
under 2.4 throughout.

`ISSUE=0205 scripts/check.sh go ./internal/...`: `go build` and `go vet`
clean; all 15 packages `ok` with real elapsed times (`handlers` 2.788s,
`interests` 2.663s, lowest `testdb` 0.190s — no package at 0.00s); skip audit
`(no skips)`; leftover processes `(none)`; `VERIFICATION PASSED`. Database
`opencircuit_test_0205` dropped on exit (`scripts/testdb.sh drop 0205`
reported "does not exist" afterward). `pgrep -f 'go test|\.test |vitest'`
empty.

`scripts/check.sh web`: `npm run check` — `234 FILES 0 ERRORS 0 WARNINGS`;
`npm test` — `23 passed (23)` / `3730 passed (3730)` (3728 baseline + 2 new
tests, one per file); `VERIFICATION PASSED`. Guard file alone: `61 passed
(61)` (60 baseline + 1).

`gofmt -l internal/handlers/citation_guard_test.go` produced no output
(clean).

## Notes

Reported by `#0198`'s phase-3 review under `CLAUDE.md` §9.

The review's verdict on the duplicated list is worth preserving, because it is
the reason this issue is a length check and **not** a shared source of truth:
the two lists are not each other's oracle. Both are pinned to ECMA-262
`WhiteSpace` ∪ `LineTerminator`, which `#0193` proved equals V8's native `\s`
across all of Unicode — so the *spec* arbitrates, and an edit moving both
patterns the same wrong way still fails both. A third JSON file read by two
languages would be untyped, uncompiled, and — decisively — **would not fix the
shrinkage failure mode at all**: deleting from a shared file removes the
assertion from both sides at once, which is worse than today.

`CLAUDE.md` §8's typed-escape trap is live in this exact work. `#0198`'s
reviewer disclosed that drafting its own notes produced four real NBSPs and one
real U+2060 from typed escapes, caught by byte census before writing. Build any
new text with `chr(92)` and read the file back as bytes.

## Relation

- Found by: [#0198](0198.md) — the behavioural coupling
- Relates to: [#0193](0193.md) — the exhaustive sweep the lists are derived from
- Relates to: [#0187](0187.md) — `MATRIX_COUNTS`, the precedent for criterion 1
- Relates to: [#0203](0203.md) — the doc-comment defect precedent

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 150 | 4,205 | 7,333,370 | 133,960 | $2.77 |

**Total: $2.77**
