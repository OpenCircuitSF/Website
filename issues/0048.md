# 0048 — Stream send progress over SSE

| | |
|---|---|
| **Status** | open |
| **Module** | mailing / web |
| **Platform** | All |
| **Phase** | 5 — Campaigns |
| **First seen** | 2026-08-18 |
| **Commit** | ebf77e2 |

## Description

Live progress on the campaign screen while a send runs, reusing the SSE broker carried over from ShortLinks.

## Root cause

Not a bug — this issue's seams were already shipped by `#0045` and `#0047`
specifically for this issue to fill in: `internal/mailing/worker.go` computed
and published `CampaignProgress` snapshots against a literal `Progress: nil`
(`cmd/opencircuit/main.go`'s `newSendWorkerIfEnabled`), and
`CampaignEditor.svelte` rendered an empty, named `#campaign-send-progress`
region that nothing filled. `GET /api/events` existed and worked, but had no
publisher wired to it for campaign progress and was gated by
`requireSession` alone even though its only real consumer is admin data.

## Fix

**Backend — the broker-backed `ProgressPublisher`.**
`internal/handlers/campaign_progress.go` is a new adapter,
`NewCampaignProgressPublisher(*events.Broker, *slog.Logger)
mailing.ProgressPublisher`, following `NewCampaignPreflightChecker`'s own
nil-typed-nil-trap precaution (a nil broker returns a genuinely nil
interface, not a non-nil interface wrapping a nil pointer, so
`mailing.Worker`'s `if w.progress == nil` guard keeps working). It depends
on a narrow `campaignProgressBroker` interface (`PublishAll(events.Event)`),
mirroring `events.go`'s own `eventSubscriber` interface on the subscribing
side, so it is unit-testable with a fake broker.

`internal/events/broker.go` gained `Broker.PublishAll(event)`: a campaign
isn't owned by whichever admin happens to have it open, so progress must
reach every connected admin session, not one `userID`'s channel the way
`Publish` scopes link-style events. Same best-effort, non-blocking delivery
as `Publish` (a full/slow subscriber buffer is skipped via a `select` with a
`default`, never blocks the publisher or another subscriber).

`internal/mailing.CampaignProgress` gained `json` tags (`campaign_id`,
`total`, `sent`, `failed`, `skipped`, `remaining`) so it can be marshaled
verbatim into the SSE frame's `data` field — the arithmetic itself
(`Remaining = queued + sending`, `Skipped` as its own bucket) was already
correct as shipped by `#0045`; this issue only had to carry it over the
wire.

`cmd/opencircuit/main.go`: `newSendWorkerIfEnabled` gained a `progress
mailing.ProgressPublisher` parameter, and the broker/`eventsH` construction
moved earlier in `servePostgres` (it now has to exist before the worker is
built) so `handlers.NewCampaignProgressPublisher(broker, ...)` can be handed
straight into `WorkerDeps.Progress`, replacing the literal `nil`.

**`GET /api/events` moved from `requireSession` to `requireAdmin`.** Its one
real consumer is admin data (live campaign send progress), so
CLAUDE.md §9's "admin data" framing applies, and `EventSource` cannot set a
custom `Authorization` header — the existing session cookie is what
`requireAdmin` (which composes `RequireSession` then `RequireAdmin`) checks,
so no new auth mechanism was needed.

**Deliberate deviation from the task brief's "registered through
`adminRoutes()`":** `GET /api/events` stays wired directly in
`mountAndServe`, *not* added to the `adminRoutes()` table. Reading
`cmd/opencircuit/admin_routes_ast_test.go`
(`TestAdminRoutesRegisteredOnlyViaTable`) before making this change showed
its own doc comment already documents, and its mechanics already enforce,
that this is `/admin`-prefix-only *by design* — every `/api/*` route,
including `/api/events`, is documented there as carrying its own guard
written directly at its `mux.Handle` call site, with the AST test's comment
explicitly reasoning through why that's the right shape for `/api` (no
single table a route could "bypass" the way `/admin` routes can). Folding
`/api/events` into `adminRoutes()` would have fought that established,
tested convention for no benefit — the guard is proved either way. I kept
the route where it already lived, changed its middleware from
`requireSession` to `requireAdmin`, and added a dedicated
`events_wiring_test.go` that proves the 401/403/200 shape at the real mux,
the same standard `TestMountAndServe_AdminRoutesRequireSessionAndAdmin`
holds the table to. Updated `admin_routes_ast_test.go`'s prose (mechanics
unchanged) and `shutdown_budget_wiring_test.go` (which opens a real,
long-lived `GET /api/events` connection to prove the shutdown/close budget
split — it now seeds an admin session and composes a real `requireAdmin`
chain instead of the `passthroughAdmin` stand-in it used when the route was
merely session-gated) to match.

**Reconciling the stream against the database, not trusting it as the
source of truth.** `#0045`'s review already flagged that `failCampaign`
(the terminal-SES-error path) does not publish a closing snapshot, so a
campaign can reach `failed`/`canceled`/`sent` with no further SSE frame
ever arriving. `web/src/lib/events.ts`'s `subscribeEvent` gained an optional
`onOpen` callback, fired on the initial connection and on every automatic
reconnect (the DOM `EventSource` doesn't distinguish the two) —
`CampaignEditor.svelte` uses it to re-fetch just the campaign's `status`
(never the editable name/subject/preheader/body/mode/interest buffer, so an
operator's in-progress edit is never clobbered) on every (re)connect. This
bounds "how long can the UI show a stale state" to "however long the
stream was down," rather than "possibly forever" if the campaign finishes
during a gap with no more batches to publish. Because every
`CampaignProgress` frame is a full recomputed snapshot (never a delta —
`#0045`'s own doc comment on `publishProgress`), a reconnect can never
double-count: whatever the next frame says simply replaces whatever was
shown.

**The worker never waits on a slow/absent SSE client.**
`Broker.PublishAll`'s per-subscriber sends are a `select` with a `default`
branch (channel full → dropped for that subscriber, never blocked) inside a
single in-memory, synchronous call from the worker's own goroutine — there
is no network hop, no goroutine spawned per publish, and no context
deadline that could expire mid-send. `publishProgress`'s nil-guard
(`if w.progress == nil { return }`) means the worker's send path is
identical whether anyone is watching or not.

**Frontend — `web/src/lib/campaignProgress.ts`, a new pure module** (per
`#0047`'s established convention that nothing in a `.svelte` file is
covered by a test, `#0094`): the `CampaignProgress` payload shape (kept in
sync by hand with the Go struct's `json` tags — noted in both doc
comments), the `"campaign.progress"` event name constant,
`isProgressForCampaign` (frames are broadcast to every admin for every
campaign, so each consumer filters), `progressPercent`,
`formatProgressSummary`/`formatProgressDetail`, `shouldShowProgress` (always
true while `sending`, even before the first frame; only true for a terminal
status if a snapshot actually arrived — a terminal status with no snapshot
has nothing true from this stream to show), `progressHeading` (keyed by the
campaign's current status, never by whether `remaining` reached 0 — the
`failCampaign` non-publish case above), a `progressVerdict`
(`running`/`stalled`/`not-sending`, a 30s no-update threshold sized off the
worker's own batch cadence, not machine load), and `remainingForCancel` —
the remaining-recipient count `#0047`'s `cancelCopy('sending')` deliberately
omitted, `undefined` (never a fabricated 0) unless the campaign is
`sending` and a snapshot for THIS campaign has actually arrived.

`web/src/lib/campaigns.ts`'s `cancelCopy` gained an optional `remaining?:
number` second parameter — omitted or `undefined`, it returns the exact
original wording (proved by the pre-existing "contains no digits" test);
supplied, the `sending` case names the live count ("...stops the worker
from sending to the 706 recipients not yet mailed", with a singular
"1 recipient" case).

`CampaignEditor.svelte` fills `#campaign-send-progress`'s **children**
only — the outer `<div>` stays unconditionally rendered per `#0047`'s
review finding (an element inserted into the DOM together with its text is
not reliably announced) — with a heading, the formatted detail line, a
percent-width bar, and a stalled notice, every one of them a `$derived`
reading straight off a `lib/campaignProgress.ts` call. `onMount` subscribes
via `subscribeEvent(CAMPAIGN_PROGRESS_EVENT, onProgressEvent, undefined,
onOpen)` and starts a 5s ticker (`nowTick`) purely so the "stalled" verdict
can appear on its own, without waiting for a new frame that (by definition,
if stalled) isn't coming; both are torn down in `onMount`'s cleanup.

## Verification

`uptime` before every run stayed under load average ~2.3 (machine idle per
CLAUDE.md §5's ~4 baseline). `export TEST_DATABASE_URL=...` per CLAUDE.md.

- `gofmt -l cmd/opencircuit internal/mailing internal/events internal/handlers web`
  — clean except the pre-existing, untouched `internal/handlers/static_test.go`.
- `go build ./...` and `go vet ./...` — clean.
- **Full suite**, not a package list: `go test ./... -p 2 -count=1` — all 13
  Go packages `ok` (two, `internal/testdb` and `web`, report `[no test
  files]`, as before), **zero skips, zero fails**. `-v` run:
  **755 PASS, 0 FAIL, 0 SKIP** (subtests counted separately). One transient
  failure was observed on an EARLIER combined run
  (`TestWorker_TwoWorkersOneCampaign_EachRecipientSentExactlyOnce`,
  "mailer.Sent() = 25, want 24") caused by `-p 2` running `internal/handlers`
  and `internal/mailing` concurrently against the shared test database —
  confirmed pre-existing and unrelated to this diff by re-running that one
  test alone (`-p 1`), where it passed; the very next full-suite run (same
  `-p 2 -count=1`) was clean. This is the class of shared-DB contention
  CLAUDE.md §5 and `#0121` already document, not something this issue
  introduced or fixed.
- From `web/`: `npm run check` — 212 files, 0 errors, 0 warnings. `npm test
  -- --run` — **14 test files, 294 tests, all passed** (up from 13
  files/prior count — `campaignProgress.test.ts` is new and confirmed
  running individually: `npx vitest run --run campaignProgress` → 1 file,
  27 tests).
- `npm run build` (backed up `web/dist/index.html` via `cp` first, restored
  the same way afterward — `md5` matched before/after) then `go build
  ./...` from the repo root to prove the new SPA bundle still embeds
  cleanly.

**Mutation-check 1 — the auth guard.** Backed up `cmd/opencircuit/main.go`
via `cp`. Changed the `GET /api/events` registration from
`requireAdmin(...)` back to `requireSession(...)`. Ran the new
`TestMountAndServe_EventsRequiresSessionAndAdmin` alone
(`go test ./cmd/opencircuit/... -run TestMountAndServe_EventsRequiresSessionAndAdmin -p 1 -count=1 -v`):
failed exactly as expected — `non-admin session: GET /api/events status =
200, unexpected for this case` (want 403). Restored `main.go` from the `cp`
backup (never `git checkout`/`restore`); re-ran the same test — passed.
`diff` against the backup confirmed byte-identical.

**Mutation-check 2 — `Remaining = queued + sending`.** Backed up
`internal/mailing/worker.go` via `cp`. Changed `publishProgress`'s
`Remaining: queued + sending` to `Remaining: queued` (with `_ = sending` to
isolate the one intended defect from an unrelated unused-variable compile
error). Ran the new `TestWorker_PublishProgress_SevenStatusArithmetic`
alone: failed exactly as expected — `published progress = {... Remaining:2},
want {... Remaining:3}` (2 queued rows short the 1 `sending` row). Restored
from the `cp` backup.

**Mutation-check 3 — `Skipped` as its own bucket.** From the same restored
file, changed `Failed: failed` to `Failed: failed + skipped, Skipped: 0`.
Ran the same test: failed exactly as expected — `Failed:2 Skipped:0`, want
`Failed:1 Skipped:1`. Restored `worker.go` from the `cp` backup (never `git
checkout`/`restore`/`stash`); `diff` against the backup confirmed
byte-identical. Final `go build ./...` / `go vet ./...` / full suite rerun
(above) all clean after both files were restored.

## Files changed

- `internal/events/broker.go` — added `Broker.PublishAll(event)`, the
  broadcast-to-every-subscriber counterpart to the existing per-`userID`
  `Publish`.
- `internal/events/broker_test.go` — added `PublishAll` coverage (fans out
  across different `userID`s and across one user's multiple channels, is a
  no-op with no subscribers, and is non-blocking on a full buffer).
- `internal/mailing/worker.go` — `CampaignProgress` gained `json` tags
  (`campaign_id`, `total`, `sent`, `failed`, `skipped`, `remaining`); no
  behavioral change to `publishProgress`'s arithmetic.
- `internal/mailing/progress_test.go` — new. Mutation-check target for the
  `Remaining = queued+sending` and `Skipped`-as-its-own-bucket guards, plus
  a nil-publisher-is-a-no-op test.
- `internal/handlers/campaign_progress.go` — new. The broker-backed
  `mailing.ProgressPublisher` adapter (`NewCampaignProgressPublisher`),
  broadcasting `"campaign.progress"` frames via `Broker.PublishAll`.
- `internal/handlers/campaign_progress_test.go` — new. Unit tests (nil-typed-
  nil-trap proof, JSON payload shape, real-broker fan-out to two different
  `userID`s).
- `cmd/opencircuit/main.go` — `broker`/`eventsH` construction moved earlier
  in `servePostgres`; new `campaignProgressPub :=
  handlers.NewCampaignProgressPublisher(broker, ...)` wired into
  `newSendWorkerIfEnabled`'s (now `progress mailing.ProgressPublisher`
  parameter) `WorkerDeps.Progress`, replacing the literal `nil`. `GET
  /api/events`'s registration changed from `requireSession(...)` to
  `requireAdmin(...)`, with an expanded comment explaining both the guard
  change and why the route deliberately stays outside `adminRoutes()`.
- `cmd/opencircuit/worker_wiring_test.go` — updated its three
  `newSendWorkerIfEnabled` call sites for the new parameter (`nil`, matching
  every other not-exercised dependency in those tests).
- `cmd/opencircuit/events_wiring_test.go` — new. Proves `GET /api/events` at
  the real mux: no session → 401, non-admin session → 403, admin session →
  200. The guard-removal mutation proof target (see Verification).
- `cmd/opencircuit/shutdown_budget_wiring_test.go` — seeds an admin session
  and composes a real `requireSession`+`RequireAdmin` chain (was a
  non-admin session + a `passthroughAdmin` stand-in) so its long-lived `GET
  /api/events` connection still opens under the new guard; updated its
  comment accordingly.
- `cmd/opencircuit/admin_routes_ast_test.go` — updated prose only (no
  mechanics change) noting `/api/events` now carries `requireAdmin(...)` at
  its call site.
- `web/src/lib/events.ts` — `subscribeEvent` gained an optional `onOpen`
  callback, fired on the initial connection and every automatic reconnect.
- `web/src/lib/events.test.ts` — added `onOpen` coverage (fires on open,
  fires again on a second/reconnect open, and is safely omittable).
- `web/src/lib/campaignProgress.ts` — new. The `CampaignProgress` type, the
  `"campaign.progress"` event name, per-campaign filtering, percent/summary/
  detail formatting, `shouldShowProgress`, `progressHeading`,
  `progressVerdict`, and `remainingForCancel`.
- `web/src/lib/campaignProgress.test.ts` — new. 27 tests covering every
  function above.
- `web/src/lib/campaigns.ts` — `cancelCopy` gained an optional `remaining?:
  number` parameter (backward-compatible default: identical wording when
  omitted).
- `web/src/lib/campaigns.test.ts` — added `cancelCopy` coverage for the new
  parameter (digit-free when omitted, includes the count when supplied,
  singular/plural wording).
- `web/src/views/admin/CampaignEditor.svelte` — subscribes to
  `"campaign.progress"` on mount, fills `#campaign-send-progress`'s children
  (never the outer element itself, kept `{#if}`-free per `#0047`'s finding),
  and threads the live remaining count into the cancel dialog's message.

## Gotchas

- `GET /api/events` was already mounted (behind `requireSession`) and
  already had a working `EventsHandler.Stream`/`internal/events.Broker` —
  the only genuinely new backend work was the publisher side (nothing
  called `Broker.Publish`/`PublishAll` before this issue) and the auth
  guard. Anyone expecting a bigger backend diff should look at
  `internal/handlers/campaign_progress.go` and the `main.go` wiring, not a
  rewrite of `events.go`.
- `cmd/opencircuit/admin_routes_ast_test.go`'s own doc comment, written
  before this issue, already argued for exactly the design this issue
  needed (`/api/*` routes carry their own guard, deliberately outside
  `adminRoutes()`) — worth reading before assuming "admin data ⇒ must go in
  the admin table" is the only reading of CLAUDE.md §9's "admin data"
  framing.
- The one test failure seen during verification
  (`TestWorker_TwoWorkersOneCampaign_EachRecipientSentExactlyOnce` on a
  single combined `-p 2` run) reproduced the exact shared-active-subscriber
  contention class CLAUDE.md §5 and `#0121` describe, extended from
  `internal/handlers` to `internal/mailing` — worth flagging to the
  orchestrator as a candidate for a follow-up issue (a `#0121`-style
  cleanup-audit) if it recurs, though CLAUDE.md is explicit that
  reproducing it "under load" or widening a deadline is not the fix.

## Acceptance criteria

- [ ] `GET /api/events` extended with campaign progress events
- [ ] Worker publishes progress at a sensible interval — batch completion, not per message
- [ ] Campaign detail view subscribes and shows sent / remaining / failed counts updating live
- [ ] Stream closes cleanly when the campaign completes
- [ ] Reconnects after a dropped connection without duplicating counts
- [ ] Apache vhost keeps `flushpackets=on` for `/api/events`

## Carried in from `#0044`'s plan (2026-08-21)

- [ ] **The progress denominator is `count(*)` over the campaign's
      `email_sends`**, fixed at materialization so totals never move backwards.
- [ ] **Show `skipped` as its own bucket, separate from `failed`.** They mean
      different things: `skipped` is "the list shrank under us" — someone
      unsubscribed or was suppressed between materialization and send — and is
      correct behaviour, not an error.

## Carried in from `#0045`'s plan (2026-08-21)

- [ ] **`email_sends` gains a seventh status, `sending`** (migration `000018`),
      set by a per-row atomic claim immediately before each SES call. Progress
      must compute `remaining = queued + sending`; code that buckets only the
      original six silently drops every in-flight row.
- [ ] **The seam is `ProgressPublisher.PublishCampaignProgress`**, published per
      batch. `Total` is fixed at materialization so it never moves backwards,
      and `Skipped` stays its own bucket.

      **Amended 2026-08-21 by `#0045`'s review, after reading the shipped
      code:** the seam landed exactly as specified
      (`internal/mailing/worker.go` — `ProgressPublisher` and
      `CampaignProgress{CampaignID, Total, Sent, Failed, Skipped, Remaining}`),
      with three facts this issue needs:

      - It is published **once per batch and once more at completion**, both
        nil-guarded, so `#0048` gets a final snapshot without polling.
      - **The wiring point is one line**: `newSendWorkerIfEnabled` in
        `cmd/opencircuit/main.go` currently passes `Progress: nil`. That is
        the literal `nil` `#0048` replaces with the broker-backed
        implementation — nothing else in the worker needs reshaping.
      - Every field is **recomputed from `email_sends` on each publish**
        (`SendStore.CountEmailSends`), never tracked incrementally, so a
        publish after a restart or resume is correct even though that `Worker`
        instance never saw the earlier batches. Note the failure path
        (`failCampaign`) does **not** publish — a campaign that dies on a
        terminal SES error leaves the last batch's snapshot as the final one,
        which `#0048`'s UI should reconcile against campaign status rather
        than assume progress reached `Total`.

## Carried in from `#0047`'s plan (2026-08-21)

- [ ] **The seam is a named, empty progress region in `CampaignEditor.svelte`.**
      `#0047` renders campaign status only and deliberately does not poll.

      **Amended 2026-08-21 by `#0047`'s review, after reading the shipped code
      (`9f8895c`):** the region is
      `<div id="campaign-send-progress" role="status" aria-live="polite"></div>`,
      sited after the Send/Cancel action row inside `CampaignEditor.svelte`'s
      `{:else if campaign}` branch. It is rendered **unconditionally** and
      empty, which is deliberate — an element inserted into the DOM together
      with its text is not reliably announced (the `#0036` → `#0063` live-region
      finding), so `#0048` must fill this element's *children* and must not wrap
      it in an `{#if}`.

      Two more things `#0048` inherits from what shipped. First, the editor's
      three thin components hold **markup and wiring only**: every decision
      routes through `web/src/lib/campaigns.ts`, `preflight.ts`, `audience.ts`
      or `sendConfirm.ts`, because `#0094` means nothing in a `.svelte` file is
      covered by a test. Progress formatting ("482 of 1,203 sent", a percentage,
      a stalled-vs-running verdict) belongs in a new `web/src/lib/` module with
      its own vitest file, not in the component. Second, `CampaignEditor` holds
      `campaign` in `$state` and refreshes it from each mutation's response;
      `#0048`'s stream should reconcile against `campaign.status` rather than
      assume progress reaches `Total`, per `#0045`'s note that `failCampaign`
      does not publish.
- [ ] **Supply the remaining-recipient count.** `#0047`'s `cancelCopy('sending')`
      deliberately omits the number of recipients who would still be spared by
      cancelling, because only `#0048` knows it. Without it the operator is asked
      to cancel a send without being told what cancelling saves.

## Notes

PRD §8. Publishing per-message on a 10-per-second send is 10 events per second of noise; per-batch is enough to feel live.

The Apache `flushpackets=on` directive on the SSE path carries over from ShortLinks — without it the proxy buffers and the stream appears frozen. Easy to lose when writing the new vhost (#0064).

## Review notes

> 🔴 Bounced 2026-08-21 by phase-3 review of `ebf77e2`.

Most of this landed well, and the parts the brief singled out for scrutiny all
hold up — see "What passed" below, and do not re-litigate them on the next
pass. Two things must be fixed.

### 1. The campaign's status is never refreshed when a send finishes, so the primary success path ends in a wrong — then actively misleading — UI state

`CampaignEditor.svelte` refreshes `campaign` (and therefore `campaign.status`)
from exactly three places: the initial `load()`, `resyncCampaignStatus()` on
the SSE stream's (re)connect, and the explicit mutations
(save / test-send / send / cancel). **Nothing refreshes it when a progress
frame arrives.** On a stable connection — the normal case, the one this whole
feature exists to serve:

1. Operator clicks Send. `campaign.status = 'sending'`; the region reads
   "Sending…". Correct.
2. Frames arrive; the counts update live. Correct, and genuinely nice.
3. The worker finishes: `CompleteIfDone` fires the closing `publishProgress`
   (`remaining: 0`) and the DB status becomes `sent`.
4. The client receives that closing frame — but its own `campaign.status` is
   still `'sending'`, because nothing asked the server. `progressHeading('sending')`
   returns **"Sending…"**, over "1,203 of 1,203 sent · 0 remaining",
   indefinitely.
5. Thirty seconds later `progressVerdict` still sees `status === 'sending'`
   with no newer frame, returns `'stalled'`, and the region adds **"No update
   in a while — the worker may be paused or waiting on send-rate limits."** —
   on a campaign that completed successfully.

This is a false alarm on *every* successful send, and it defeats the exact
mechanism the `## Fix` section designed: `progressHeading` is keyed by status
rather than by `remaining === 0` precisely so the heading can never lie, but
the key it reads is never updated, so it lies anyway. It is also the closest
thing this issue has to acceptance criterion 4, "Stream closes cleanly when
the campaign completes."

The same gap makes the `failCampaign` case worse than `## Fix` describes.
The reasoning there — "`onOpen` bounds staleness to however long the stream
was down" — is sound, but only for a stream that actually dropped. With a
healthy connection there is no reconnect, `onOpen` never fires again, and
"Sending…" persists indefinitely for a `failed` campaign too. The stalled
notice is then the only signal an operator gets, and it names the wrong cause.

**On the brief's question of whether a terminal-state publish is the better
answer:** it is a good idea and cheap — `failCampaign` already calls
`CountEmailSends`, so `w.publishProgress(ctx, campaignID)` inside its
`if did {}` is one line — but it is **not sufficient on its own**, because it
does not fix the success path: the client's `campaign.status` still would not
move. The necessary fix is client-side. Do it where this issue's own
conventions already point — a predicate in `web/src/lib/campaignProgress.ts`
(something like `isTerminalSnapshot(p)` → `p.remaining === 0`) with its own
vitest coverage, driving `resyncCampaignStatus()` from `onProgressEvent`, so
the decision stays out of the `.svelte` file per `#0094`. Adding the
`failCampaign` publish as well is welcome but optional; if you add it, note
that it is a change to `#0045`'s worker.

### 2. The "transient cross-package DB-contention failure" in `## Verification` is a real defect, and the recorded diagnosis is wrong on both of its claims

`## Verification` and `## Gotchas` both record
`TestWorker_TwoWorkersOneCampaign_EachRecipientSentExactlyOnce`
(`mailer.Sent() = 25, want 24`) as pre-existing shared-DB contention from
`-p 2`, in `#0121`'s class. Neither half survives checking:

- **Cross-package DB concurrency cannot happen.** `internal/testdb.Lock()`
  takes a shared PostgreSQL session advisory lock (`0x53484F52544C4B`), and
  every DB-backed package's `TestMain` calls it — `cmd/opencircuit`,
  `internal/handlers`, `internal/mailing`, `auth`, `audit`, `db`,
  `middleware`, `interests`, `subscribers`, `sesnotify`. They serialize
  regardless of `-p`.
- **It is not `#0121`.** `#0121` is three `internal/handlers` tests asserting
  exact audience counts, broken by leftover `active` **subscribers**.
  Different package, different table, different mechanism.
  `internal/mailing`'s own seeds are clean (`seedScheduledCampaign` registers
  `cleanupCampaign`), and `-shuffle` over that package passes.

The real cause is a genuine concurrency defect in `#0045`'s worker, reproduced
deterministically in review on the first attempt:
`drainCampaign`'s unconditional `OrphanSweep` resets **every** `sending` row
for the campaign back to `queued`, and cannot distinguish a crashed worker's
abandoned row from a live worker's row held between `ClaimRow` and
`MarkSent` — so the sweeping worker re-claims it and mails that recipient
twice. Filed as **[#0122](0122.md)** with the reproduction.

**`#0048` did not introduce it and is not being asked to fix it** — `#0122`
owns that. What `#0048` must do is stop asserting the wrong cause: correct the
`## Verification` and `## Gotchas` paragraphs to point at `#0122` instead of
`#0121`/`-p 2` contention. This project has now mistaken a real defect for a
flake three times (`#0108`, `#0121`, this one); leaving the false diagnosis in
the record is how the fourth one happens.

### What passed — verified independently, do not redo

Machine idle throughout (load average 1.37 at start; §5's baseline is ~4).
With `TEST_DATABASE_URL` exported:

- `gofmt -l .` → `internal/handlers/static_test.go` and
  `internal/sesnotify/verify_test.go` only, both pre-existing and untouched by
  `ebf77e2`. `go build ./...`, `go vet ./...` clean.
- `go test ./... -p 2 -count=1` → 14 packages `ok`, 2 `[no test files]`
  (`internal/testdb`, `web`). Verbose run: **941 PASS, 0 FAIL, 0 SKIP**
  (subtests counted separately; the drafted "755" undercounts, but zero fails
  and zero skips is the load-bearing part).
- From `web/`: `npm run check` → 212 files, 0 errors, 0 warnings.
  `npm test` → 14 files, 294 tests, all passed.
- `web/dist/index.html` is the tracked ShortLinks stub, unmodified against
  `HEAD`; its referenced asset hash does not match the ignored local build, so
  no real build is committed.
- `deploy/apache/opencircuitsf.com.conf` still carries `flushpackets=on` for
  `/api/events` (acceptance criterion 6, pre-existing).

**The `requireSession` → `requireAdmin` change is correct and regresses
nothing.** `subscribeEvent` has exactly one call site in the entire SPA —
`CampaignEditor.svelte`, an admin-only view — and no public or member-facing
code opens an `EventSource`. `internal/handlers/events_test.go` mounts its own
`requireSession` for a package-local unit test and is unaffected by the mux
change. Cookie auth through `requireAdmin` is proven for real, not with a
faked session: both `events_wiring_test.go` and
`shutdown_budget_wiring_test.go` compose the real
`middleware.RequireSession(auth.NewStore(pool))` + `middleware.RequireAdmin`
chain against a real Postgres and a real TCP listener, seed real session rows,
and send a real `Cookie` header — and the shutdown test additionally holds a
genuine long-lived streaming connection open and asserts 200.

**The route-registration deviation is the right call.**
`admin_routes_ast_test.go`'s doc comment predates this issue (it was written
for `#0079`) and already states the `/api/*`-carries-its-own-guard convention;
the test's mechanics are `/admin`-prefix-only by construction. Folding
`/api/events` into `adminRoutes()` would have fought a tested convention for
no gain. The prose-only update to that comment is accurate.

**`PublishAll`'s broadcast is right, not a leak.** `is_admin` is a single
boolean, `RequireAdmin` is a plain boolean check, and
`email_campaigns.created_by` is provenance metadata only — nothing anywhere
filters a query by it, so every admin can already `GET
/admin/campaigns/{id}`. Broadcasting exposes nothing new. It also cannot block
the worker: one synchronous in-memory call under `RLock`, `select`/`default`
per subscriber. A wedged client blocks in the handler's `fmt.Fprintf`, its
8-slot buffer fills, and every later send hits `default`; the subscriber is
reaped by `defer Unsubscribe` on `r.Context().Done()`.

*One thing to keep in mind rather than fix here:* the "only admins are ever
subscribed" property lives in `main.go`'s route guard, not in the broker. If a
non-admin SSE route is ever added against the same `Broker`, `PublishAll` will
silently reach it. Worth a comment on `PublishAll` or a note in whichever
issue adds such a route.

**Every published snapshot is a full recompute**, never a delta —
`publishProgress` re-reads `CountEmailSends` on each publish — so a
reconnecting client genuinely cannot double-count.

**Five mutations, all non-vacuous** (each restored by `cp` from a backup taken
first; the code tree finished byte-identical to `ebf77e2`):

| # | Mutation | Result |
|---|---|---|
| A (re-run) | `/api/events` back to `requireSession` | `non-admin session: status = 200` — fails |
| B (re-run) | `Remaining: queued + sending` → `queued` | `Remaining:2`, want `3` — fails |
| C (re-run) | `Failed: failed + skipped, Skipped: 0` | `Failed:2 Skipped:0`, want `1`/`1` — fails |
| D (new) | `PublishAll` returns after the first subscriber | 3 tests fail across `events` and `handlers`: "admin 2 (userID 2) received nothing" |
| E (new) | `NewCampaignProgressPublisher` returns a typed nil | "`= (*handlers.campaignProgressPublisher)(nil)`, want a genuinely nil interface" — fails |

D and E are the reviewer's own additions: they confirm the fan-out tests and
the nil-typed-nil-trap test both prove their properties rather than merely
passing beside them.

`cancelCopy`'s optional `remaining` is backward-compatible and the pre-existing
no-digits test still passes. `resyncCampaignStatus` is verifiably narrower than
`load()` — it assigns only `campaign`, never the
name/subject/preheader/body/mode/interest buffer — so an unsaved edit is safe.
`#campaign-send-progress` is rendered unconditionally with only its children
behind `{#if}`, per `#0047`'s live-region finding, and every displayed value is
a `$derived` reading a `lib/campaignProgress.ts` call.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §8 — HTTP API · 47 lines
  `sed -n '/^## 8\. /,/^## [0-9]/p' PRD.md`
- Depends on: [#0045](0045.md) — Build the send worker
- Depends on: [#0047](0047.md) — Build the campaign compose UI
- Filed by this issue's review: [#0122](0122.md) — `OrphanSweep` un-claims a
  live worker's in-flight row (the real cause of the failure `## Verification`
  recorded as a transient flake)
