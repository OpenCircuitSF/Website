# 0100 — The suppression list has no admin surface, and clear-complaint removes suppressions reason-blind

| | |
|---|---|
| **Status** | open |
| **Module** | subscribers / admin / web |
| **Platform** | All |
| **Phase** | 4 — Unsubscribe and list hygiene |
| **First seen** | 2026-08-20 |

## Description

`#0033` built the `suppressions` table and `subscribers.SuppressionStore`
(`Add`/`AddTx`/`IsSuppressed`/`List`/`Remove`), but the only admin surface over
it is a side effect of two subscriber-row actions. An admin can *create* a
manual suppression and cannot *reverse* one; `List` has no caller at all; and
the one removal path deletes a suppression without regard to why it was added.
None of this blocked `#0033` — its acceptance criteria are store-level and are
met — but the gaps compound as soon as `#0038` starts writing `hard_bounce` and
`complaint` rows.

## Plan

Phase-1 planning pass (Opus, 2026-08-20). Status stays `open`; no code was
written. Read this together with `issues/0033.md` (the store this builds on,
resolved `ad6cafc`) and `issues/0038.md` §4/§5/§7 (the next issue in the queue,
which writes into this table).

### 0. What exists today (verified against the tree at `ffce334`)

- `migrations/000012_create_suppressions.{up,down}.sql` — `suppressions` with
  `email TEXT PRIMARY KEY`, `reason` CHECK-constrained to the four values,
  `note`, `created_at`, plus `suppressions_email_normalized`. **Highest
  migration on disk is `000012`.**
- `internal/subscribers/suppressions.go` — `Add`/`AddTx` (idempotent
  `ON CONFLICT (email)`, first-writer-wins), `IsSuppressed(email)`,
  `List()` (newest first, no caller outside tests), `Remove(email)`
  (reason-blind `DELETE`).
- `internal/handlers/admin_subscribers.go` — `Suppress` writes
  `reason=manual`; `ClearComplaint` calls `Remove(after.Email)`. These are the
  **only two** production call sites of the suppression store.
- `internal/handlers/subscribe.go` — `SuppressionChecker` /`NoSuppressions`
  doc comments are stale (criterion 4). `cmd/opencircuit/main.go:164` wires the
  real `subscribers.NewSuppressionStore(pool)`.
- `web/src/views/Admin.svelte` — five subtabs held in local `$state`
  (`section`), *not* router routes; `openSuppress`/`submitSuppress` and the
  `canClearComplaint`-gated clear-complaint row action. No suppression list.
- Production serves the static placeholder (CLAUDE.md §7): the Go service has
  never run there, so **no production database has ever had `000012` applied**.
  §2 below does not depend on that, but it is worth knowing.

### 1. Decision — reason-scoped rows. Primary key becomes `(email, reason)`

**Chosen: reason-scoped rows.** One `suppressions` row per (address, reason);
an address that both hard-bounced and complained carries two rows.

**Why the other two candidates fail.** Both keep one row per address, and under
`Add`'s first-writer-wins contract that row records only the *first* reason.
Walk the two orderings of the case criterion 2 names:

| Order of events | Stored row after both | `Remove(email,"complaint")` | Result |
|---|---|---|---|
| hard bounce, then complaint | `hard_bounce` (the complaint `Add` no-ops) | no reason match | hard bounce survives — correct, by luck |
| complaint, then hard bounce | `complaint` (the bounce `Add` no-ops) | reason matches, row deleted | **hard-bounce suppression destroyed** |

The second row is the whole defect, and *neither* **reason-scoped removal**
(`Remove(email, reason)` deleting only on a match) nor **a removal that
refuses on mismatch** prevents it: in that ordering the stored reason genuinely
*is* `complaint`, so both designs delete and both are correct-by-their-own-rules
while mail resumes to a permanently-failing address. The information is
destroyed at **write** time by the single-row model, not at removal time, so no
removal-side rule can recover it. They differ from each other only in which
error a mismatch returns — a UX distinction, not a correctness one. Rejected.

Reason-scoped rows is the only candidate that can represent "this address has
two independent reasons to be blocked", which is the fact the send gate needs.
It also matches PRD §6.5's state machine, where four different events each
"insert into `suppressions`" as independent facts.

**What it costs:**

- One new migration (§2) and a `Remove` signature change with exactly two call
  sites plus tests.
- `Add`'s idempotency key becomes `(email, reason)`, so the "first reason
  recorded for an address is the one that persists" sentence in its doc comment
  becomes false and must be rewritten. A second, *different* reason now inserts
  rather than no-ops. This is the intended behavior change.
- The admin list shows one row per (email, reason), so an address can appear
  more than once. §5 orders by email so those rows are adjacent.
- Removing a suppression is no longer "unblock this address" — it is "retire
  one reason". Every piece of admin copy and audit metadata has to say which
  (§4, §5).

`IsSuppressed` is deliberately **not** reason-scoped — see §3.

### 2. Migration `000013_scope_suppressions_by_reason`

Append-only (CLAUDE.md §1): a new pair, `000012` is never edited.

```sql
-- 000013_scope_suppressions_by_reason.up.sql
-- #0100: an address can have more than one independent reason to be blocked.
-- Keying on email alone made the first reason recorded the only one kept, so a
-- later admin "clear complaint" could delete a hard-bounce suppression and let
-- mail resume to a permanently-failing address. Key on (email, reason) so each
-- reason is its own removable fact. IsSuppressed stays reason-blind.
ALTER TABLE suppressions DROP CONSTRAINT suppressions_pkey;
ALTER TABLE suppressions ADD CONSTRAINT suppressions_pkey PRIMARY KEY (email, reason);
```

```sql
-- 000013_scope_suppressions_by_reason.down.sql
-- This DOWN INTENTIONALLY FAILS if any address carries more than one reason.
-- The only way to narrow the key back to (email) is to discard suppressions,
-- and silently discarding a suppression is the exact failure #0100 exists to
-- prevent. Resolve the duplicates deliberately before rolling back.
ALTER TABLE suppressions DROP CONSTRAINT suppressions_pkey;
ALTER TABLE suppressions ADD CONSTRAINT suppressions_pkey PRIMARY KEY (email);
```

**Existing rows.** Nothing to migrate and no dedupe step: the old `(email)`
uniqueness *implies* `(email, reason)` uniqueness, so the widened key can never
conflict with data already present. This is true independently of how many rows
exist — do not justify it with "the table is empty in production". (It is:
production still serves the placeholder, §0. That is a convenience, not the
argument.)

**No new indexes.** The composite PK's btree leads with `email`, so every
`WHERE email = …` lookup (`IsSuppressed`, `ListByEmail`, `Remove`) is already
served. `List` is an unfiltered scan of an internal table with no expected
scale; an index for its `ORDER BY` would be premature.

**Doc parity.** `internal/db`'s `TestDatabaseDocMigrationParity` fails unless
both are updated in the same commit:
- add a `000013_scope_suppressions_by_reason` row to `docs/database.md`'s
  migration table (and amend the `000012` row's description to say the key was
  later widened);
- change `docs/database.md:4` and `CLAUDE.md` §1's repository-layout row from
  `000001`–`000012` to `000001`–`000013`.

### 3. `IsSuppressed` does not change, and neither does the subscribe path

**Semantics: any row of any reason blocks.** `IsSuppressed(ctx, email)` keeps
its exact signature, its exact SQL, and its single round trip:

```sql
SELECT EXISTS (SELECT 1 FROM suppressions WHERE email = lower(trim($1)))
```

Do **not** add a reason parameter, a second query, an `IN` list, or any
branch. Three reasons, in order of severity:

1. **CLAUDE.md §9 / `#0026` / `#0088`.** `POST /api/subscribe` must return a
   byte-identical uniform `202` across every branch, and `#0088` closed a
   100%-accurate *timing* oracle by making every branch do exactly one
   `IsSuppressed` plus one `FindByEmail`, always, in that fixed order. Any
   change that makes the suppressed branch cheaper or costlier reopens it.
   **`internal/handlers/subscribe.go`'s request path must not be touched by
   this issue at all** beyond the two doc-comment edits in §7.
2. The send gate's question is "may we mail this address", which is
   reason-independent by definition.
3. It keeps the blast radius of the key change inside the store and the admin
   surface.

`TestSubscribe_UniformResponseAcrossBranches` is the regression gate; it must
pass untouched.

### 4. Store API (`internal/subscribers/suppressions.go`)

SQL stays in this package (CLAUDE.md §1). Changes:

| Method | Change |
|---|---|
| `Add` / `AddTx` | `ON CONFLICT (email, reason) DO UPDATE SET email = suppressions.email RETURNING …`. The no-op-update trick that keeps `RETURNING` alive is unchanged and still load-bearing. Contract becomes: **idempotent per (email, reason)** — a repeat of the same reason returns the original row (original `note`, original `created_at`); a *different* reason inserts a new row. Rewrite the doc comment's "the first reason recorded for an address is the one that persists" paragraph accordingly. |
| `IsSuppressed` | unchanged (§3). Add one sentence to its doc: reason-blind by design — any row of any reason blocks. |
| `Remove` | **`Remove(ctx, email, reason string) (Suppression, error)`.** Validates `reason` against `suppressionReasons` → `ErrInvalidSuppressionReason`. `DELETE FROM suppressions WHERE email = lower(trim($1)) AND reason = $2 RETURNING email, reason, note, created_at`. `pgx.ErrNoRows` → `ErrSuppressionNotFound`. Returns the deleted row because the row is being destroyed and the audit entry is the only remaining record of its `note`/`created_at`. **There is deliberately no reason-less removal** — the reason-blind delete must be unreachable, not merely discouraged. |
| `ListByEmail` (new) | `ListByEmail(ctx, email string) ([]Suppression, error)` — every row for one normalized address, `ORDER BY created_at DESC`. Callers: `ClearComplaint` (to report survivors) and the removal handler. |
| `List` | returns `[]SuppressionListItem` (new type: the four columns plus `SubscriberStatus *string`) via `LEFT JOIN subscribers ON subscribers.email = suppressions.email`, `ORDER BY suppressions.email ASC, suppressions.created_at DESC`. See §5 for why the join and why the order changed from newest-first. `Suppression` itself is left alone so `Add`/`Remove` keep their simple shape. `List` has no caller outside tests today, so the signature change is free. |

`SuppressionReason*` constants keep their current names.

### 5. Admin surface

#### 5a. `ClearComplaint` (`internal/handlers/admin_subscribers.go`)

- Call `Remove(ctx, after.Email, subscribers.SuppressionReasonComplaint)`.
  `ErrSuppressionNotFound` stays non-fatal (unchanged); any other error stays a
  500 (unchanged).
- Then `ListByEmail(ctx, after.Email)` for the survivors.
- `adminSuppressionWriter` grows `Remove(ctx, email, reason string) (subscribers.Suppression, error)` and `ListByEmail`. It stays a narrow
  handler-local interface (CLAUDE.md §1); `*SuppressionStore` satisfies it.
- **Confirmation copy must stop over-claiming.** Today it says "The matching
  suppression-list entry was also removed, so a future resignup is no longer
  blocked at the send gate" — a statement that becomes *false* the moment a
  `hard_bounce` row survives. Four cases:

  | removed | survivors | message suffix |
  |---|---|---|
  | yes | none | ` The "complaint" suppression-list entry was removed, and no other suppression remains for this address.` |
  | yes | some | ` The "complaint" suppression-list entry was removed, but this address is still suppressed for: hard_bounce. It remains blocked at the send gate.` |
  | no | none | ` No "complaint" suppression-list entry existed for this address.` |
  | no | some | ` No "complaint" suppression-list entry existed. This address is still suppressed for: hard_bounce.` |

  Build the reason list from `ListByEmail`, never hardcode `hard_bounce`.
- **Audit metadata.** Replace the single `suppression_removed` bool with:
  `suppression_removed` (bool), `suppression_removed_reason` (`"complaint"`),
  `suppression_removed_note`, `suppression_removed_created_at`, and
  `suppressions_remaining` (`[]string` of surviving reasons, `[]` when none).
  A later reader must be able to tell which suppression was destroyed and which
  survived without re-deriving it. Keep the existing
  `previous_unsubscribed_at` / `overwrites_note` fields.
- The subscriber's `status` handling is untouched: `AdminClearComplaint`
  remains the sole exit from `complained` (CLAUDE.md §9).

#### 5b. New handler `internal/handlers/admin_suppressions.go`

A separate handler and file, not more methods on `AdminSubscribersHandler`: a
suppression is keyed by email and can exist with **no subscribers row at all**
(hard deletion, `#0060`), so it is not a subscriber sub-resource.

```
GET  /admin/suppressions          → { "suppressions": [ … ], "count": n }
POST /admin/suppressions/remove   → body { email, reason, note }
```

`POST …/remove` rather than `DELETE /admin/suppressions/{email}`: email
addresses make poor path segments (`+`, `%`, `.`, percent-encoding), the action
requires a justification note in a body, and every other mutating admin action
in this codebase is a `POST`. Follows `Suppress`'s shape deliberately.

**Store seams** (narrow, handler-local): `suppressionLister` (`List`,
`ListByEmail`, `Remove`) and `suppressionSubscriberLookup` (`FindByEmail`
only, for the one decision in the next bullet).

**Removal rules:**

- `note` required, 400 on empty-after-trim — mirrors `Suppress`.
- `reason` must be one of the four; 400 otherwise.
- `reason == "complaint"` **and** a subscribers row exists for the address →
  **409**, message directing the admin to Subscribers → Clear complaint. This
  keeps exactly one audited path that touches a complaint (CLAUDE.md §9's
  concentration of authority), rather than opening a second one.
- `reason == "complaint"` **and no subscribers row exists** (the `#0060` orphan)
  → allowed. Otherwise an orphaned complaint suppression would be permanently
  unremovable, since `ClearComplaint` is keyed by subscriber id and unreachable.
- `ErrSuppressionNotFound` → **404** here (unlike in `ClearComplaint`, where it
  is incidental; here it is the whole request).
- **This endpoint never touches the `subscribers` table.** See §6.
- Response: `{ removed: {email, reason, note, created_at}, remaining: ["hard_bounce"], message }`, with a message that names the surviving reasons the
  same way §5a does.

**Audit.** New constant in `internal/audit/actions.go`:
`ActionSuppressionRemoved = "subscriber.suppression_removed"`, written with
`TargetType: audit.TargetSubscriber` and `TargetID` = the subscriber id when
one exists, `nil` when orphaned. Metadata: `email`, `reason`, `note` (the
admin's justification), `removed_note`, `removed_created_at`,
`suppressions_remaining`, `subscriber_status` (or `null`),
`subscriber_status_unchanged: true`. Doc-comment the constant to the same
standard as its neighbours.

**Wiring** (`cmd/opencircuit/main.go`): construct `adminSuppressionsH` next to
`adminSubscribersH` from the same `suppressionsStore` + `subscribersStore`;
declare it `nil` on the dev/`STORAGE=json` path exactly as
`adminSubscribersH` is (line ~362); add both routes **inside `adminRoutes()`**
under an `if adminSuppressionsH != nil` block. Do not register them anywhere
else — `admin_routes_ast_test.go` fails the build if you do, and
`admin_wiring_test.go` enumerates `adminRoutes(...)` so the new routes get
their RequireSession+RequireAdmin coverage automatically once the parameter is
threaded through both call sites (main.go and the test).

#### 5c. `web/` — a sixth Admin subtab

- `web/src/lib/api.ts`: `listSuppressions()`, `removeSuppression(email, reason, note)`, plus a `Suppression` type in `types.ts` mirroring the server view
  (`email`, `reason`, `note`, `created_at`, `subscriber_status`).
- `web/src/lib/admin.ts` (unit-testable TS, CLAUDE.md §1): `SUPPRESSION_REASONS`
  + `suppressionReasonLabel(reason)`, `validateSuppressionNote(note)` (reuse or
  mirror `validateSuppressNote`), and
  `suppressionRemovalBlocked(reason, hasSubscriber): string | null` returning
  the "use Clear complaint instead" explanation — so the UI pre-disables the
  button rather than discovering the 409 after a failed request, matching the
  established disabled-Delete pattern in the Interests section.
- `web/src/views/Admin.svelte`: add `'suppressions'` to the `Section` union and
  a sixth `.subtab`; a table with **Email · Reason · Note · Added · Subscriber
  status** and a per-row Remove button opening a confirm modal with a required
  note. Reason rendered as a labelled badge. Empty state: "No addresses are
  suppressed." Keep the component thin — validation and the blocked-reason
  decision live in `admin.ts`.
- The **Subscriber status** column is the one deliberate addition beyond
  criterion 3's four columns. It is what makes the two-layer picture legible
  ("is this address blocked at the suppression list, the subscriber row, or
  both?") and it is what lets the client pre-disable complaint removals. It is
  also why `List` uses a `LEFT JOIN` (§4) instead of an N+1, and why the order
  changed to `email ASC, created_at DESC` — adjacency of an address's several
  rows is precisely what this issue exists to make visible.
- Two existing strings become wrong and must change:
  - the suppress modal's "removing it requires a separate admin action" →
    name where: "…can be removed later from Admin → Suppressions."
  - the clear-complaint `confirm()` at `Admin.svelte:648`'s "removes any
    matching suppression-list entry" → "removes only the `complaint`
    suppression-list entry; any other suppression (for example a hard bounce)
    stays in force."
- No `router.ts` change: admin sections are local `$state`, not routes, so
  `routes_parity_test.go` is unaffected.

**Out of scope, on purpose:** pagination, filtering, CSV export, adding a
suppression directly from this screen (Suppress on the subscriber row is the
entry point), and surfacing suppressions on the subscriber detail view.

### 6. Reversing a manual suppression, and what it does to `status`

Criterion 1's flow: Admin → Suppressions → find the `manual` row → Remove with
a note.

**Removal changes no subscriber status, ever.** A manual suppress leaves the
row `unsubscribed`; un-suppressing leaves it `unsubscribed`. The address is no
longer blocked at the send gate, but it is still not a subscriber — it must come
back through double opt-in (`restartSignup` already handles `unsubscribed`).
Silently flipping anything to `active` would manufacture consent that was never
given. The response message must say this in as many words:
"…this does not resubscribe the address; it must opt in again."

**Interaction with CLAUDE.md §9.** Removing a `hard_bounce` or `manual`
suppression from a subscriber who is `complained` leaves the status
`complained`, so the address still cannot resubscribe — `#0026`'s complained
branch sends nothing. Nothing in this issue can auto-resubscribe a complained
subscriber, and the `reason == "complaint"` refusal in §5b means this endpoint
cannot even be used as an oblique route toward it. `AdminClearComplaint`
remains the only thing that clears `complained`.

### 7. The doc-comment sweep (criterion 4)

`internal/handlers/subscribe.go`, prose only — do not change any type,
signature, or behavior:

1. **`SuppressionChecker`** (~line 228). Delete "the `suppressions` table added
   by #0033, **which has not landed yet** (#0026 depends on #0025/#0027, not
   #0033; the two issues are independent branches of the tracker)" and "and
   #0033 only has to provide a real implementation — swapped in at the single
   call site in cmd/opencircuit/main.go — for production enforcement to switch
   on." Replace with: the table exists (`migrations/000012`, widened by
   `000013`); `*subscribers.SuppressionStore` satisfies this interface and is
   wired at the single call site in `cmd/opencircuit/main.go`. Keep the seam's
   rationale (interface, not concrete type) and add one line: reason-blind —
   any suppression row of any reason blocks (§3).
2. **`NoSuppressions`** (~line 241). Delete "the SuppressionChecker wired in
   cmd/opencircuit/main.go **until #0033 lands**". Replace with: the
   nil-default `SuppressionChecker` (`NewSubscribeHandler` substitutes it when
   `suppression` is nil) and the stand-in used by tests; production wires
   `*subscribers.SuppressionStore`. **Keep the type and the method** — both are
   still used.

Two more that go stale in this commit and should be corrected while you are
there (not in criterion 4, but they are the same falsehood):

3. `internal/handlers/admin_subscribers.go`'s package doc: "it removes **any
   matching** suppressions row alongside `subscribers.Store.AdminClearComplaint`"
   → "the `complaint` suppression row only; any other reason survives (#0100)".
4. `internal/subscribers/suppressions.go`'s `Add` doc — mandatory, not
   optional: its first-reason-persists paragraph is exactly what stops being
   true (§4).

Leave `subscribe.go:91` and `:942` alone; `:942`'s "#0033/#0038 … will refine
this" may optionally be narrowed to `#0038`.

### 8. Tests

DB-backed. Export `TEST_DATABASE_URL` first and confirm zero skips (CLAUDE.md
§5). Every test seeds its own throwaway address/row and targets that — **never a
literal or seeded id** (CLAUDE.md §8b). No guard is disabled anywhere here, so
no private scratch database is needed.

`internal/subscribers/suppressions_test.go`:

1. `TestSuppressionStore_Add_TwoReasonsForOneEmailCoexist` — `hard_bounce` then
   `complaint`; `ListByEmail` returns two rows.
2. `TestSuppressionStore_Add_IdempotentPerReason` — rewrite of the existing
   `_IdempotentKeepsOriginalRow`: a repeat of the same reason returns the
   original `note`/`created_at` and adds no row.
3. **`TestSuppressionStore_Remove_ScopedByReason_LeavesHardBounceInForce`** —
   *the case criterion 2 names.* Seed `hard_bounce` **and** `complaint` for one
   throwaway address, `Remove(email, "complaint")`, then assert
   `IsSuppressed == true` and `ListByEmail == [hard_bounce]`. Run it in **both
   insertion orders** — the single-row model only failed in one of them, and a
   test that happens to pick the lucky order proves nothing.
4. `TestSuppressionStore_Remove_NotFoundWhenReasonDoesNotMatch` — seed
   `hard_bounce` only; removing `complaint` returns `ErrSuppressionNotFound`
   and the row survives.
5. `TestSuppressionStore_Remove_InvalidReasonRejected` — `ErrInvalidSuppressionReason`, nothing deleted.
6. `TestSuppressionStore_Remove_ReturnsDeletedRow` — note and `created_at`
   come back for the audit entry.
7. `TestSuppressionStore_IsSuppressed_TrueForEveryReason`.
8. `TestSuppressionStore_ListByEmail_NormalizesEmail`.
9. `TestSuppressionStore_List_JoinsSubscriberStatus` — one suppression with a
   subscriber, one orphaned; assert the non-null and null branches and the
   `email ASC` grouping.
10. The existing `AddTx` rollback/commit pair must still pass unchanged —
    `#0038`'s hard dependency.

`internal/handlers/admin_subscribers_test.go`:

11. `TestAdminSubscribers_ClearComplaint_RemovesOnlyComplaintSuppression` — the
    end-to-end form of test 3: both rows present, only `complaint` removed,
    response message names the surviving `hard_bounce`, audit metadata carries
    `suppressions_remaining: ["hard_bounce"]`.
12. `TestAdminSubscribers_ClearComplaint_MessageWhenNothingRemains`.

New `internal/handlers/admin_suppressions_test.go`:

13. `TestAdminSuppressions_List_ReturnsRowsWithSubscriberStatus`.
14. `TestAdminSuppressions_Remove_RequiresNote` — 400.
15. **`TestAdminSuppressions_Remove_ManualSuppressionIsReversible`** —
    criterion 1's round trip: `POST /admin/subscribers/{id}/suppress`, then
    `POST /admin/suppressions/remove`; assert `IsSuppressed == false` **and the
    subscriber's status is still `unsubscribed`** (§6).
16. `TestAdminSuppressions_Remove_ComplaintRefusedWhenSubscriberExists` — 409,
    row survives; and `_AllowedWhenOrphaned` — no subscribers row, 200.
17. `TestAdminSuppressions_Remove_NotFound` — 404.
18. `TestAdminSuppressions_Remove_WritesAuditWithRemovedAndRemaining`.

Regression gates (must pass untouched, name them in `## Verification`):
`TestSubscribe_UniformResponseAcrossBranches` (§3),
`TestDatabaseDocMigrationParity` (§2), `cmd/opencircuit`'s
`admin_wiring_test.go` and `admin_routes_ast_test.go` (§5b).

`web/`: `npm run check` and `npm test`, with new cases in
`web/src/lib/admin.test.ts` for `suppressionReasonLabel`,
`validateSuppressionNote`, and `suppressionRemovalBlocked`.

Scope the Go run to what changed (CLAUDE.md §5a):
`go test ./internal/subscribers/... ./internal/handlers/... ./internal/db/... ./cmd/opencircuit/... -p 2`.

### 9. Files

| File | Change |
|---|---|
| `migrations/000013_scope_suppressions_by_reason.up.sql` | new (§2) |
| `migrations/000013_scope_suppressions_by_reason.down.sql` | new (§2) |
| `internal/subscribers/suppressions.go` | `Add` conflict target, `Remove` signature, new `ListByEmail`, `List` join + new item type, doc rewrites (§4) |
| `internal/subscribers/suppressions_test.go` | tests 1–10 |
| `internal/handlers/admin_suppressions.go` | new handler (§5b) |
| `internal/handlers/admin_suppressions_test.go` | new, tests 13–18 |
| `internal/handlers/admin_subscribers.go` | `adminSuppressionWriter`, `ClearComplaint` reason-scoped removal + copy + audit metadata, package-doc fix (§5a, §7.3) |
| `internal/handlers/admin_subscribers_test.go` | tests 11–12 |
| `internal/handlers/subscribe.go` | **two doc comments only** (§7) |
| `internal/audit/actions.go` | `ActionSuppressionRemoved` (§5b) |
| `cmd/opencircuit/main.go` | construct + nil-in-dev + two routes in `adminRoutes` (§5b) |
| `cmd/opencircuit/admin_wiring_test.go` | thread the new handler into the `adminRoutes(...)` call |
| `web/src/lib/api.ts`, `types.ts`, `admin.ts`, `admin.test.ts` | client + testable logic (§5c) |
| `web/src/views/Admin.svelte` | sixth subtab, table, modal, two copy fixes (§5c) |
| `docs/database.md`, `CLAUDE.md` | migration table row + range `000001`–`000013` (§2) |

Suggested order: migration → store + store tests → `ClearComplaint` → new
handler + wiring → web → doc sweep → docs/parity.

### 10. What `#0038` must be told (report to the orchestrator; do not edit `issues/0038.md`)

`#0038` is next in the queue and writes into this table. Four points:

1. **`AddTx`'s signature is unchanged** — `#0038`'s single-transaction design
   (its plan §5) is unaffected. Continue to pass a `pgx.Tx`.
2. **Its plan §7 item 2 ("Idempotent by email … must not overwrite the original
   reason") is superseded.** The key is now `(email, reason)`. A `Complaint`
   arriving for an address already suppressed for `hard_bounce` now inserts a
   **second row** instead of no-oping — that is the point of `#0100`. `#0038`
   must call `AddTx` unconditionally per its own §4 mapping and must **not**
   check "is this address already suppressed" and skip.
3. **Its plan §6 migration number is taken twice over.** `000012` is
   `create_suppressions` and `#0100` takes `000013`, so `#0038`'s
   `email_events` migration becomes **`000014_create_email_events`**.
4. Its §7 item 4 names the reason constants `ReasonHardBounce` etc.; the
   shipped names are `subscribers.SuppressionReasonHardBounce`,
   `…SuppressionReasonComplaint`, `…SuppressionReasonRepeatedSoftBounce`,
   `…SuppressionReasonManual`.

`issues/0038.md` should carry a short note recording points 2–4 before it is
dispatched. Whether `#0100` lands before `#0038` or after: **before** is
strongly preferable — after, the same migration has to be written anyway and
`#0038`'s freshly-written `complaint` rows will already have overwritten
nothing but will have silently *failed to record* second reasons in the
meantime.

## Acceptance criteria

- [ ] **An admin can reverse a manual suppression through the UI.**
      `SuppressionStore.Remove` is reachable today only from
      `AdminSubscribersHandler.ClearComplaint`, which `Admin.svelte` offers only
      on `complained` rows (`canClearComplaint`). A subscriber suppressed via the
      **Suppress** button ends up `unsubscribed`, not `complained` — so a
      mis-click is currently reversible only by direct database access. The
      suppress modal already tells the admin that "removing it requires a
      separate admin action"; that action needs to exist.
- [ ] **Scope suppression removal by reason, or make it explicit.** `Remove`
      deletes by email alone. Once `#0038` lands, an address that both
      hard-bounced and complained carries one row; clearing the complaint would
      silently delete the `hard_bounce` suppression and let mail flow to a
      permanently-failing address. Decide the model — reason-scoped rows,
      reason-scoped removal, or a removal that refuses when the stored reason
      isn't the one being cleared — and make `ClearComplaint`'s confirmation and
      audit metadata say which happened.
- [ ] **Give `SuppressionStore.List` a caller.** It is correct and tested but
      currently unreachable from any route or view; an admin suppression-list
      screen (email, reason, note, created_at) is the natural home, and it is
      also what makes the two criteria above discoverable.
- [ ] **Sweep the two stale doc comments in `internal/handlers/subscribe.go`.**
      `SuppressionChecker`'s comment still reads "the `suppressions` table added
      by #0033, which has not landed yet", and `NoSuppressions`' still reads
      "the SuppressionChecker wired in cmd/opencircuit/main.go until #0033
      lands". Both are false as of `ad6cafc` — `main.go` wires the real
      `subscribers.NewSuppressionStore(pool)`. `NoSuppressions` itself is still
      legitimately used as the nil-default and by tests, so keep the type and fix
      only the prose.

## Notes

PRD §6.2, §6.5. Filed by `#0033`'s review pass (2026-08-20), which approved that
issue on its own criteria — store operations add/check/list/remove, all present,
`Remove` audited at its one call site behind `RequireAdmin` — and split these
four items out rather than widening `#0033`'s scope after the fact.

The second criterion is the load-bearing one: it is a correctness problem that
only becomes reachable when `#0038` writes non-`manual` reasons, so it should be
settled before or with `#0038` rather than after.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §6.2 — Database schema · 158 lines
  `sed -n '/^### 6\.2 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0033](0033.md) — Create the suppressions migration and store
- Related: [#0032](0032.md) — Build the admin subscribers screen
- Related: [#0038](0038.md) — Ingest SES bounce and complaint events
