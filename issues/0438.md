# 0438 — The stale `/assets` example survives in the validation error, twenty lines below the hint that corrects it

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Web |
| **Module** | web / docs |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |
| **Commit** | — |

## Description

`#0432` corrected the workshop editor's cover-image hint to name `/media/`.
**The superseded `/assets/workshops/soldering.jpg` example survives in the
client-side validation error message**, which renders as a `role="alert"`
paragraph in that same editor, roughly twenty lines below the corrected hint.

	Cover image must be a site-relative path starting with "/"
	(e.g. "/assets/workshops/soldering.jpg") — an external URL is not accepted.

So an operator who mistypes a path gets the **old** example at the exact point
of entry, contradicting the hint immediately above it. The fix is undermined by
the error that fires when someone needs guidance most.

Confirmed by the orchestrator in `web/src/lib/workshopAdmin.ts` and pinned by an
identical string in `web/src/lib/workshopAdmin.test.ts`.

**This is a copy change, not a validation change.** The Go
`coverImageErrorMessage` carries no example at all, so the two messages already
differ and **no parity fixture constrains the wording** —
`url_validator_fixture_test.go` pins the *predicate*, not the prose. Do not
touch `isSafeCoverImage` or its TypeScript twin's logic; `#0432` verified both
byte-unchanged and `docs/media.md` explains why their breadth is deliberate.

## Two more instances of the same thread

**1. `workshopAdmin.ts` also quotes the erroneous migration comment.** `#0432`
put a correction paragraph in the Go `isSafeCoverImage`'s doc comment, reasoning
it was *"the one place in the source that already repeats the wrong string
verbatim."* **There are two** — the TypeScript twin carries the identical quote
and got no correction. The rationale was sound; the inventory was not.

**2. `PRD.md` §6.2 carries `-- path under /assets` and is NOT frozen.** The new
Go correction attributes the wrong quote to "PRD §6.2's own schema comment" and
then explains only that the *migration* cannot be edited — leaving its own
antecedent uncorrected in a file that **is** editable. `#0432` left it because
`#0425` owned `PRD.md` at the time; that is no longer a constraint.

Confirmed present at `PRD.md` line 702.

## Also fold in
A typography nit from the same review: the new hint uses ASCII `--` where
rendered copy across `web/src` uses an em dash on 121 lines without exception.

## Acceptance criteria

1. The validation error's example matches the hint above it — `/media/…`.
2. The assertion in `workshopAdmin.test.ts` pinning that string is updated, and
   **still pins it**. A test edited into vacuity is this phase's recurring
   defect.
3. `workshopAdmin.ts`'s doc comment gets the same correction the Go side has, or
   a stated reason why one place suffices.
4. `PRD.md` §6.2's `-- path under /assets` is corrected, since it is editable.
   **§11's line counts must stay exact** — §6.2 is currently **355** lines and
   the file **1,871**; verify with the row's own `sed`, not a green test
   (`#0113`).
5. **No logic changes.** `isSafeCoverImage` and its twin stay byte-identical —
   prove it by hash. `docs/media.md` explains why their breadth is deliberate.
6. **Sweep for `/assets` as a cover-image example anywhere else** — this is the
   third surface found after two passes looked. Report what you find.
7. `(cd web && npm run check)` and `(cd web && npm test)`; run
   `./internal/handlers/...` and `./internal/db/...`. Per §5 name the specific
   tests that ran — real function names, not a `-run` prefix. No
   Go-file-plus-line-number citation (`#0352`), scanned with `python3` bytes.

## Relation

- Found by: [#0432](0432.md)'s review, which called the error message its
  highest-value finding
- Relates to: [#0432](0432.md) — whose hint fix this completes
- Relates to: [#0425](0425.md) — which held `PRD.md` when this was found
- Relates to: `docs/media.md` — the authority for the `/media/` convention

## Work log

- 2026-09-05 — Filed by the orchestrator from `#0432`'s review (§9). The error
  string, its test pin, and `PRD.md` §6.2's comment were all confirmed directly
  before filing.
- 2026-09-05 — Implemented. `web/src/lib/workshopAdmin.ts`'s validation-error
  example and its `workshopAdmin.test.ts` pin now read `/media/soldering-101.jpg`,
  matching the corrected hint above them (AC1-2). Added a `Correction (#0438)`
  paragraph to `workshopAdmin.ts`'s header comment mirroring the Go twin's
  `Correction (#0432)` note, since the same stale-quote doc comment existed on
  both sides (AC3). Corrected `PRD.md` §6.2's `cover_image` schema comment from
  `-- path under /assets` to `-- path under /media`, since it (unlike the frozen
  migration) is editable; verified `wc -l PRD.md` (1871, unchanged) and the
  `sed -n '/^### 6\.2 /,/^#\{2,3\} [0-9]/p'` extraction (356 raw / 355 per the
  guard's `raw-1`, unchanged) (AC4). Proved `isSafeCoverImage` and its Go twin
  byte-identical before/after by SHA-256 of the function body in each language
  (AC5). Also normalized the second, syntactically-valid `/assets/cover.jpg`
  example in `workshopAdmin.test.ts` (an accepted-input test, not the stale
  error string) to `/media/cover.jpg` for the same reason.
  Swept for further `/assets` cover-image examples (AC6): found
  `internal/handlers/admin_workshops_test.go` (four occurrences, all Go test
  fixtures exercising the identical accepted-path shape, not user-facing copy)
  left untouched as out of this issue's scope (web/src/lib only); reported for
  a filing decision. `docs/media.md`'s own quote of the (frozen, unfixable)
  migration comment is accurate as documentation of the migration and needs no
  change. `internal/handlers/admin_workshops.go`'s `Correction (#0432)` note
  quotes PRD §6.2's *former* text verbatim as history — now that `PRD.md` no
  longer says that, the quote reads as literal history rather than current
  fact; left untouched since editing Go source was outside this issue's given
  scope, reported for a filing decision. The em-dash typography nit named
  under "Also fold in" lives in `WorkshopEditor.svelte`'s cover-hint paragraph
  (ASCII `--`, not the `/assets` string), a file outside this issue's scope;
  left untouched, reported for a filing decision.
