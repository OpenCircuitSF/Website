# 0175 — Admin copy says "the send worker" in five places, and mixes `--` with em dashes

| | |
|---|---|
| **Status** | resolved |
| **Platform** | All |
| **Module** | web |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |
| **Commit** | `104ba3f` |
| **Closed** | 2026-08-23 |

## Description

`#0172` removed tracker citations from admin-facing copy. Its reviewer swept on a different axis — reading every rendered string rather than grepping for citation patterns — and found the same family of problem with no citation to grep for.

**1. "The send worker" is an implementation detail, rendered to admins in five places:**

- `Admin.svelte`'s physical-address hint
- `campaigns.ts`'s returned-to-draft text
- `campaigns.ts`'s two cancel confirmations
- `Campaigns.svelte`'s "Returned to draft — worker refused it" badge
- `CampaignEditor.svelte`'s stall hint

An admin has no model of a "worker". The facts behind these strings are real and worth telling them — a send was refused, a campaign went back to draft, something stalled — but the *reason* should be phrased in terms of the product ("sending stopped because no physical address is set") rather than the process that enforced it.

**2. User-visible copy mixes `--` and em dashes.** Two strings in `WorkshopEditor.svelte` and two messages in `workshopAdmin.ts` use a double hyphen where the rest of the console uses `—`.

## Acceptance criteria

- [x] The five "worker" strings describe what happened and why in the admin's terms, without naming the worker. Keep the diagnostic value — an admin must still learn *why* a campaign returned to draft
- [x] The physical-address hint in particular should say what to do about it, since that one is actionable (`#0045` refuses to send without it — CAN-SPAM §7704)
- [x] Dashes are consistent across user-visible copy; pick the convention the console already uses and apply it — see `## Fix` for a scope note on `workshopAdmin.ts`
- [x] **Sweep the way this was found**: read the rendered strings, do not grep for a word. `#0172`'s reviewer stripped `<script>`/`<style>`/comments from the admin views and read the text nodes plus `placeholder`/`title`/`aria-label`/`alt` — that method is what turned this up, and it is worth repeating rather than searching for "worker"
- [x] Judged fine by that same review and **not** to be changed: the audit log's own displayed values (`${entry.target_type} #${entry.target_id}`, the `e.g. email_campaign` placeholder, `#0114`), `api.ts`'s `HTTP ${status}` fallback, and product vocabulary an admin genuinely meets ("slug", "double opt-in", "draft", "Markdown")
- [x] `scripts/check.sh web` passes

## Root cause

Five rendered strings across the campaign-admin UI named the background
send worker (`internal/mailing.Worker`, `#0045`) directly — "the send
worker refused it", "stops the worker from sending", "the worker may be
paused" — an implementation detail with no meaning to an admin who has
never seen the Go source. Separately, `WorkshopEditor.svelte` had two
strings using `--` where the rest of the console's user-visible copy uses
`—`, left over from copy that predates the em-dash convention settling.

## Fix

Rewrote each of the five strings in product terms, keeping what actually
happened and (for the actionable one) what to do about it, without
naming the worker:

- `web/src/views/Admin.svelte` — the physical-address setting hint now
  reads "…every commercial message — campaigns cannot be sent until an
  address is set here." (was "…the send worker refuses to start a
  campaign while this is empty.") — actionable: it tells the admin
  exactly what unblocks sending.
- `web/src/lib/campaigns.ts`'s `demotionExplanation` now reads "…but
  sending was refused and it was returned to draft…" (was "…but the
  send worker refused it and returned it to draft…"). The function's
  doc comment was updated to match ("does not restate WHY sending was
  refused").
- `web/src/lib/campaigns.ts`'s `cancelCopy` (both branches) now reads
  "Cancelling stops sending to …" instead of "Cancelling stops the
  worker from sending to …".
- `web/src/views/admin/Campaigns.svelte`'s demoted-row badge now reads
  "Returned to draft — sending was refused" instead of "Returned to
  draft — worker refused it".
- `web/src/views/admin/CampaignEditor.svelte`'s stall hint now reads
  "No update in a while — sending may be paused or waiting on
  send-rate limits." instead of naming the worker.

Also fixed the two `--` strings the issue calls out in
`WorkshopEditor.svelte` (the slug hint and the delete-conflict
message) to use `—`, matching the em dash used throughout the rest of
the admin console (confirmed by checking existing usage in the same
file and in `campaigns.ts`).

**Scope note on `workshopAdmin.ts`:** the issue's dash criterion also
names two messages in `web/src/lib/workshopAdmin.ts` (the cancel
confirmation at line 147 and the cover-image validation message at
line 588 — both still use `--`). The orchestrator's dispatch for this
issue explicitly listed that file as owned by `#0174`'s reviewer for
this work session ("stay out of anything dirty that is not yours"),
so it was left untouched here even though it was not dirty at the
time. Reporting this back rather than editing it or filing a new
issue, per `CLAUDE.md` §9 (subagents report, they don't file).

**Sweep beyond the five strings:** stripped `<script>`/`<style>`/comments
from every file under `web/src/views/admin/` plus `Admin.svelte` and read
every remaining text node and `placeholder`/`title`/`aria-label`/`alt`
value (method matching `#0172`'s reviewer, not a grep for "worker").
Found nothing else naming the worker or otherwise off — the rest of the
sweep matched what the issue already judged fine (slug/draft/Markdown
vocabulary, the audit log's own values). Also checked
`campaignProgress.ts`, `admin.ts`, and `sendConfirm.ts`'s returned
strings directly (no `.svelte` wrapper to strip) — clean, no `worker`
mentions and no `--` dashes.

## Verification

- `uptime` before starting: load averages 1.63 2.08 2.97 — well under the
  ~8 threshold, so no timing readings are suspect.
- `scripts/check.sh web`:
  - `npm run check` (svelte-check) — `230 FILES 0 ERRORS 0 WARNINGS
    0 FILES_WITH_PROBLEMS`.
  - `npm test` (vitest) — `Test Files 20 passed (20)`, `Tests 3611 passed
    (3611)`, matching the stated baseline exactly (no test asserted the
    old wording, so the count is unchanged rather than increased).
- Built the SPA to a scratch `--outDir` outside the repo and grepped the
  minified bundle: confirmed `worker refused it`, `send worker refuses`,
  `worker may be paused`, and `stops the worker` are all **absent**; confirmed
  `sending was refused`, `campaigns cannot be sent until an address is set
  here`, `sending may be paused`, `Cancelling stops sending to`, and both
  em-dash replacements (`creation — the server`, `instead — it stays`) are
  **present**. Scratch build directory removed after the check.
- `git status --porcelain` before staging showed only the five files below
  dirty; `git diff --cached --name-only` after `git add` confirmed the same
  five and nothing else, before running `git commit` with an explicit
  pathspec matching them.

## Files changed

- `web/src/views/Admin.svelte` — physical-address setting hint rewritten to
  say what to do, without naming the worker.
- `web/src/lib/campaigns.ts` — `demotionExplanation`'s returned string and
  its doc comment; both `cancelCopy` branches.
- `web/src/views/admin/Campaigns.svelte` — the demoted-row badge text.
- `web/src/views/admin/CampaignEditor.svelte` — the send-progress stall
  hint.
- `web/src/views/admin/WorkshopEditor.svelte` — two `--` → `—` fixes (slug
  hint, delete-conflict message).

## Notes

Reported by `#0172`'s phase-3 review under `CLAUDE.md` §9, explicitly outside that issue's acceptance criteria.

Worth noting the pattern this is the fourth instance of: a targeted search confirms what you already suspect, and reading finds what nobody named. `#0163`, `#0170`, `#0172` and now this all surfaced that way.

## Relation

- Found by: [#0172](0172.md) — Admin-facing hint text quoting issue numbers
- Relates to: [#0045](0045.md) — the physical-address refusal one of these strings explains
- Relates to: [#0047](0047.md) — the campaign send UI where most of them live

## Review notes — 2026-08-23 (phase 3, **passes**)

Re-verified independently at `104ba3f` in a throwaway worktree — the main
checkout was dirty with `#0177`'s in-flight edits to `workshopAdmin.ts`,
`workshopAdmin.test.ts` and `WorkshopEditor.svelte`, one of which this issue
also touched, so reading the working copy would have measured someone else's
work. `uptime` 1.44 at the start; nothing here is a timing reading anyway.

### Accuracy of the five rewrites

Each was checked against the code that produces the condition it describes,
not just against the old wording.

1. **The physical-address hint** (`Admin.svelte`) — "campaigns cannot be sent
   until an address is set here." **Literally true, at three independent
   points**: `admin_campaigns.go`'s send transition runs `mailing.Preflight`
   and answers 409 `{"unmet":[…]}` on `physical_address_missing`;
   `worker.go`'s gate re-runs the same `Preflight` and calls `DemoteToDraft`
   plus a `send_refused` audit row before the start claim; and `drainLoop`
   re-reads `physical_address` fresh at drain time and `failCampaign`s on a
   blank one, so an address blanked mid-send stops the send too. "Cannot be
   sent" is therefore accurate at schedule time, at claim time, and mid-send.
   It is also **better** on `CLAUDE.md` §9 than the string it replaced: the
   old text ("the send worker refuses to start a campaign while this is
   empty") attributed the refusal to one named component, which invites the
   reading that some other path might not refuse. The new text states the
   property, not the enforcer, and "…set here" points at the textarea
   directly below it. No §9 concern; if anything the §9 posture improved.
2. **`demotionExplanation`** — "sending was refused and it was returned to
   draft" describes the real path exactly: the worker's `Preflight` gate runs
   **before** `ClaimStart`, so nothing was sent, `started_at` stays NULL (which
   is what `wasDemotedAfterScheduling` keys on) and `DemoteToDraft` moves
   `scheduled` → `draft`. The agent of the refusal is dropped, which is the
   point of the issue; the sentence that follows ("The checks below are its
   current state") is what makes it actionable, and the live pre-send panel
   below it names the specific unmet requirement. Diagnostic value intact.
   Note `wasDemotedAfterScheduling`'s own doc comment still says "the send
   worker refused" — correctly left alone: that is a comment for developers,
   where the worker is the accurate referent, not rendered copy.
3. **`cancelCopy`, both branches** — `sending`: cancel is
   `scheduled|sending → canceled` (`admin_campaigns.go`'s `Cancel`), the drain
   stops on the status change and already-sent messages are unrecallable, so
   both sentences hold; the count branch and the no-digits fallback are
   unchanged in structure and still covered by seven existing tests, which
   assert semantically (digits, singular/plural, scheduled ≠ sending) rather
   than pinning a phrase — they survived the reword, correctly. `scheduled`
   branch is untouched and never named the worker.
4. **The demoted-row badge** ("Returned to draft — sending was refused") and
   **the stall hint** ("No update in a while — sending may be paused or
   waiting on send-rate limits") — both still true and still diagnostic. The
   stall hint's two named causes are the two real ones: a stopped/paused
   worker loop, and `effectiveSendRate`'s throttle.

### Third sweep axis: strings the server sends that the admin reads

`#0172`'s reviewer read the admin views; this issue's implementer repeated
that method. Both swept **`web/`**. So the axis tried here was the surface
neither covered: **strings authored in Go that are rendered verbatim in the
admin console.** `lib/preflight.ts` deliberately owns no code→message table —
`parseUnmet` passes the server's `message` through untouched, and its doc
comment says so — which makes `internal/mailing/preflight.go`'s failure
messages admin-facing copy. Also read the public views and every non-test
`lib/*.ts` returned string, as two cheaper axes.

- **Public views** (`views/*.svelte` minus `Admin.svelte`, plus `Header`,
  `Footer`, `SubscribeForm`, `WorkshopCard`): text nodes and
  `placeholder`/`title`/`aria-label`/`alt`/`aria-live` values all read in
  product terms. 23 em dashes, zero double hyphens, no implementation
  vocabulary. `PrivacyPolicy.svelte`'s `--` occurrences are all in comments.
  Clean.
- **Non-test `lib/*.ts` toast and error strings** (`account.ts`, `admin.ts`,
  `audience.ts`, `campaignProgress.ts`, `campaignStats.ts`, `sendConfirm.ts`,
  `subscribe.ts`, `unsubscribe.ts`, `workshops.ts`): clean. `admin.ts`'s
  `${entry.target_type} #${entry.target_id}` and `api.ts`'s `HTTP ${status}`
  are the two this issue already judged fine, and they still are.
  `campaignProgress.ts`'s "Send stopped" is terse for a terminal failure but
  it sits next to the pre-send panel that names the reason.
- **The Go side — one real finding, reported not filed** (`CLAUDE.md` §9).
  Three server-authored strings that render on admin screens cite this
  repo's own internal documents, which is exactly `#0172`'s defect one layer
  down:
  - `internal/mailing/preflight.go:171` — "Reply-To address is not configured
    (EMAIL_REPLY_TO). **PRD §6.6** requires a monitored reply address, not
    noreply@." Rendered verbatim in the pre-send checks panel.
  - `internal/handlers/admin_subscribers.go:553` — "This subscriber has
    complained, and **CLAUDE.md §9** refuses to let unsubscribe touch a
    complained row — no status change." A `message` field in the suppress
    response.
  - `internal/handlers/admin_subscribers.go:728` — "This address has
    previously complained and **CLAUDE.md §9** refuses to auto-resubscribe
    it." `manualAddMessage`, whose own comment says it "renders a
    human-readable outcome for the admin screen".

  An admin can look up CAN-SPAM §7704 (so the physical-address message's
  citation is fine); they cannot look up our PRD or our `CLAUDE.md`. The
  `EMAIL_REPLY_TO` / `EMAIL_LIST_DOMAIN` env-var names in the same messages
  are defensible — `preflight.ts` classifies those codes as deploy-only
  configuration with no in-SPA affordance, so the variable name *is* the
  actionable fact.

  This is outside `#0175`'s acceptance criteria, which scope the sweep to the
  rendered strings of the admin views. It does not bounce this issue.

### The dash convention, and the two deferred strings

Confirmed by census over rendered copy only (comments stripped), at
`104ba3f`: **22** spaced em dashes in the admin views, **23** in the public
views, and **zero** ` -- ` anywhere in either. So the em dash is the
convention, not merely the more common of two habits — after this commit the
only rendered double hyphens left in the whole SPA are the two the
implementer deferred:

- `workshopAdmin.ts:145` — `cancelConfirmMessage`'s not-currently-published
  branch;
- `workshopAdmin.ts:577` — the cover-image validation message.

(The issue text cites lines 147 and 588; the strings themselves are at 145
and 577 at this commit. The `#0177` criterion says "around", and names the
strings, so it is unambiguous.) `git show --stat 104ba3f` lists five files and
`workshopAdmin.ts` is not among them — `git log -- web/src/lib/workshopAdmin.ts`
shows `202a380` (`#0174`) as its last touch. It genuinely stayed out.

### Reproduced verification

- `scripts/check.sh web` in the worktree: svelte-check `230 FILES 0 ERRORS
  0 WARNINGS`, vitest `20 passed (20)` / `3611 passed (3611)` — the stated
  baseline, exactly.
- Bundle: built with `npx vite build --outDir <scratch>` outside the repo
  (the tracked `web/dist/index.html` was never written — `git status --
  web/dist` clean, `#0141`). `worker refused it`, `send worker refuses`,
  `worker may be paused`, `stops the worker` and the bare phrase `the send
  worker` are **all absent**; `sending was refused`, `campaigns cannot be sent
  until an address is set here`, `sending may be paused or waiting on
  send-rate limits`, both `Cancelling stops sending to …` forms, `Returned to
  draft — sending was refused`, and both em-dash fixes are **present**.
- Worktree removed, scratch build removed, no stray processes.

### Should any of this copy be test-pinned?

Partly, and one gap is worth naming.

- The **physical-address** hint should **not** be pinned verbatim. What
  `CLAUDE.md` §9 protects is the refusal, and the refusal is already pinned
  hard on the Go side — `Preflight`'s `physical_address_missing` branch, the
  worker's demote-and-audit gate, and `drainLoop`'s re-read all have tests.
  Pinning the sentence would only assert that a sentence is a sentence, and
  `#0174` was filed precisely because a phrase-match test masqueraded as a
  behavior guard. A semantic assertion — the hint names the setting and does
  not promise an exception — would be defensible; a string equality would not.
- `cancelCopy` is **already** pinned the right way and needed no change.
- **The real gap: `demotionExplanation` has no test at all.** It is not
  imported by any `*.test.ts`. It has a branch (`scheduled_at` present versus
  the `'an earlier time'` fallback) and it is the only sentence an admin gets
  explaining why a campaign left `scheduled`. A semantic test — mentions the
  scheduled time when there is one, falls back when there is not, says the
  campaign is back in draft — would have flagged this rewrite for a human to
  read, and would guard the branch. Worth filing; reported here rather than
  filed, per `CLAUDE.md` §9.
- The three `.svelte` strings are not unit-pinnable under this project's
  convention (logic in `lib/*.ts`, components thin) and should stay that way.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 108 | 2,754 | 4,310,737 | 91,731 | $1.68 |
| 2026-08-23 | review | claude-opus-5 | 88 | 1,545 | 3,841,807 | 107,421 | $2.63 |

**Total: $4.31**

