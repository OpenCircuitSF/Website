# 0386 — `TestOutbox_Counts` passes only because earlier tests leave rows behind

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-02 |
| **Closed** | 2026-09-02 |
| **Commit** | `3ab323c` |

## Resolution notes

> 🟢 Resolved 2026-09-02 — `TestOutbox_Counts` now seeds both states it names,
> each under its own `distinctKind`, claims only the row bound for `Sent`, and
> asserts an exact before/after delta of +1 on each side. The reviewer
> reproduced the run-alone failure against `3ab323c^` on a fresh scratch
> database (`Queued = 0, want at least 1 (id 1)`) and the pass at `3ab323c` on
> another fresh one, then proved the delta is not vacuous by mutating `Counts`
> itself: three of four mutants are killed, including the all-zeros one that
> vacuity would let through. An independent residue survey — every one of the
> package's 41 tests run **alone**, which `TestMain`'s entry `TRUNCATE` makes a
> genuinely pristine start — finds exactly one failure at `3ab323c^`
> (`TestOutbox_Counts`) and **zero** at `3ab323c`, confirming criterion 4's
> report by a method of the reviewer's own.

## Description

`TestOutbox_Counts` enqueues two rows, claims **both** with
`ClaimDue(ctx, 10, AllKinds)`, marks one sent, then asserts
`counts.Queued >= 1`.

Both rows are claimed, so this test leaves **nothing** queued. The assertion
holds only because **earlier tests in the package leave queued rows behind**.

**Run alone it fails**: `Queued = 0, want at least 1` — measured by
`#0378`/`#0380`'s review.

Pre-existing; `d7ed2ed` was a pure addition to that file.

## Why this is worth pinning

`#0378`'s review was asked to judge whether a **delta-based** assertion could
pass spuriously. It concluded the delta form was sound — its failure mode is a
false FAIL, not a false pass — and then found that the *absolute* form, right
next to it, is the one that is wrong.

That inversion is the point: an assertion reading `>= 1` looks stricter than a
delta and is in fact weaker, because it silently depends on state no one
declared.

## Acceptance criteria

1. `TestOutbox_Counts` passes when run **alone** — `go test ./internal/outbox/
   -run '^TestOutbox_Counts$'`. Demonstrate that, since it is the failing case.
2. It asserts what it actually establishes. If the test means to check that
   `Counts` partitions correctly, seed each state it names rather than leaning
   on residue; if it means to check a delta, say so and measure one.
3. **Do not weaken it to pass.** Loosening the assertion further, or deleting
   it, would trade a fragile check for a vacuous one — `#0354`, `#0358` and
   `#0363` each shipped a check that could not fail, and `#0364` exists because
   an assertion can pass for the wrong reason.
4. **Survey the package for the same shape** and report what you find: any
   assertion whose truth depends on rows another test left behind. Fix only
   `TestOutbox_Counts`; **report the rest** rather than widening scope.
5. Note `testdb.Lock()` serialises packages against one database and there are
   **zero live `t.Parallel()` calls** in `internal/outbox` — so ordering is
   deterministic today, which is *why* the residue is reliable enough to hide
   this. Do not rely on that; it is the bug, not the mitigation.
6. §8b: mutations use **your own database**, never a literal or seeded id. Per
   §5 name the specific tests that ran. `gofmt` stability. Run
   `./internal/outbox/...` and `./internal/handlers/...` (`#0381`).

## Relation

- Found by: [#0378](0378.md)/[#0380](0380.md)'s review, while judging the
  opposite fragility
- Relates to: [#0364](0364.md) — an assertion passing for the wrong reason
- Relates to: [#0354](0354.md), [#0358](0358.md), [#0363](0363.md) — checks
  that could not fail
- Relates to: [#0126](0126.md) — the durable outbound queue

## Fix

`internal/outbox/store_test.go` (commit `3ab323c`):

- **`TestOutbox_Counts` rewritten (criteria 1, 2, 3).** Each of the two rows
  is now enqueued under its own `Kind` unique to this test run
  (`distinctKind`, `#0285`) instead of the shared, real `KindConfirmation`.
  `ClaimDue` is called scoped to only the `sentKind` row
  (`store.ClaimDue(ctx, 10, []Kind{sentKind})`), asserting it claimed
  exactly the one row — so it cannot touch the row left queued, which the
  previous version's `ClaimDue(ctx, 10, AllKinds)` did (claiming every due
  row up to its limit, both of this test's own rows included). The
  assertion is a before/after **delta** on `Counts()` around this test's own
  two rows (matching the sibling
  `TestOutbox_Counts_ReportsSkippedDistinctlyFromAbandoned`'s existing
  idiom), not an absolute floor — it needs no residue from any other test to
  pass, and it does not merely restate a weaker check: it seeds the queued
  state and the sent state each row's own name claims, and asserts each
  moved by exactly one.

## Verification

**Criterion 1 — run alone, before and after, demonstrated (not merely
argued).**

Before, against a `git archive HEAD` export (the pre-fix file) in its own
scratch database (§8b — never the shared or tracked database):

```
go test ./internal/outbox/... -run '^TestOutbox_Counts$' -v
    store_test.go:962: Queued = 0, want at least 1 (id 1)
    --- FAIL: TestOutbox_Counts (0.00s)
```

matching the issue's own stated failure exactly. After, against the fixed
file, its own scratch database:

```
ISSUE=0386demo scripts/check.sh go ./internal/outbox/... -run '^TestOutbox_Counts$'
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/outbox	0.337s
    VERIFICATION PASSED
```

**Criterion 4 — package survey, scoped to `internal/outbox` as the issue
asks (report, do not widen scope).** Read every test in
`internal/outbox/store_test.go` and `select_due_claim_row_test.go` that
calls `Counts`, `AbandonedCountByKind`, `SelectDue`, or `ClaimDue` against a
shared assertion. Found:

- `TestOutbox_Counts` — the one genuine hit, fixed above.
- `TestOutbox_Counts_ReportsSkippedDistinctlyFromAbandoned` — already a
  before/after delta scoped to its own `distinctKind`, immune to residue.
  Not the same shape.
- `TestOutbox_AbandonedCountByKind_ScopedToKind` — asserts an absolute `0`
  and an absolute `before+1`, but both are scoped to a freshly-minted
  `distinctKind`, so no other test's rows can ever contribute to either
  number. Not the same shape (residue-proof by construction, not by luck).
- Every other test in both files (`SelectDue`/`ClaimRow`/`ClaimDue`/
  `OrphanSweep` behavior tests) scopes its own assertions to a
  `distinctKind` and/or a specific enqueued id, per the file's own
  established convention (`distinctKind`'s doc comment, `#0285`).

**No other assertion in `internal/outbox` depends on rows another test left
behind.** `TestOutbox_Counts` was the only instance; nothing else in the
package needs fixing or filing. (`internal/mailing` has several `>= 1`/`< 1`
style assertions of its own — out of this issue's scope, package boundary
respected, not surveyed.)

**Criterion 5 — noted, not relied on.** This fix does not depend on test
ordering or on `testdb.Lock()`'s serialisation; the delta is measured around
this test's own `distinctKind`-scoped rows regardless of what else is or
isn't in the table at the time it runs.

```
uptime                                       # load 2.0-2.6, not saturated
gofmt -l internal/outbox/store_test.go       # clean
sha256(file) == sha256(gofmt stdout)         # equal — gofmt stable
python3 byte scan for ".ClaimDue(" / ".OrphanSweep(" / ".SelectDue(" in any
  comment: zero (the new doc comment mentions `ClaimDue(ctx, 10, AllKinds)`
  and "ClaimDue cannot touch", neither preceded by a dot, so neither matches
  the banned `.ClaimDue(` literal)
ISSUE=0382 scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
  # internal/outbox ok 0.863s, internal/mailing ok 7.743s, internal/handlers ok 12.107s
  # zero skips, VERIFICATION PASSED
scripts/check.sh guards
  # 0 failures, VERIFICATION PASSED
```

Own scratch databases (`opencircuit_test_0386demo`, the manually-provisioned
`opencircuit_test_0386before` used for the pre-fix reproduction, and
`opencircuit_test_0382` from the combined `check.sh` run) all dropped —
confirmed absent afterward. `pgrep -f 'go test|\.test '` empty. `:5173`
confirmed free before the guards run; no `pkill -f` used. §8b: the pre-fix
reproduction targeted no literal or seeded id — the enqueued row's id
(printed above, `id 1`) is whatever the fresh scratch database assigned it,
not a hardcoded value the test steered toward.

## Review notes

**Verdict: approved (2026-09-02, Opus phase-3 review).** Re-verified
independently; nothing accepted from the drafted `## Verification`.

**Criterion 1 — run alone, before and after, on separate fresh databases.**
Both runs used scratch databases created for this review (`§8b`), never the
shared one, and `internal/outbox`'s `TestMain` `TRUNCATE`s `outbound_queue` at
process entry, so each is a pristine table.

```
# 3ab323c^ (pre-fix), TEST_DATABASE_URL=opencircuit_test_0382rb
go test ./internal/outbox/ -run '^TestOutbox_Counts$' -v -p 2
    store_test.go:962: Queued = 0, want at least 1 (id 1)
    --- FAIL: TestOutbox_Counts (0.01s)

# 3ab323c (fixed), TEST_DATABASE_URL=opencircuit_test_0382ra (fresh)
go test ./internal/outbox/ -run '^TestOutbox_Counts$' -v -p 2
    --- PASS: TestOutbox_Counts (0.01s)
# and again on the same database, now carrying its own prior run's rows: PASS
```

**Criterion 3 — the delta is not vacuous, proved by mutating `Counts`.** Four
mutations of `outbox.Store.Counts`, each on a throwaway copy of a `git archive`
export with its own scratch database, run against the fixed test alone:

| Mutation of `Counts` | Result |
|---|---|
| returns `Counts{}` unconditionally (all zeros) | **FAIL** — `Queued = 0, want 1` |
| `Queued` filters on `'sending'` instead of `'queued'` | **FAIL** — `Queued = 0, want 1` |
| `Queued` drops its `FILTER` (counts every row) | **FAIL** — `Queued = 2, want 1` |
| `Queued` and `Sent` swapped | **passes** |

Three of four killed, including the all-zeros mutant — a broken `Counts` does
not survive this assertion, so the delta is a real check and not a restatement.
It is also strictly stronger than what it replaced: the dropped-`FILTER` mutant
above would have satisfied the old `counts.Queued >= 1` floor.

The surviving mutant is a genuine gap, but a **pre-existing** one and not
something this fix introduced: the old `>= 1` / `>= 1` pair was equally blind to
a symmetric relabelling. Measured further — the **whole `internal/outbox`
package** passes with `Counts` swapping `Queued` and `Sent`. Nothing in the
package pins the two labels to their meanings against each other. Reported to
the orchestrator for filing rather than widened into this issue (criterion 4's
own instruction, and CLAUDE.md §9 — subagents report, they do not file).

**Criterion 4 — independent residue survey, by a different method.** Rather
than reading for the shape, the reviewer ran **every** `Test*` function in
`internal/outbox` **alone** (41 of them, enumerated from `^func Test`), which
`TestMain`'s entry `TRUNCATE` makes a pristine-table run each time — the
reliable way to expose an assertion that depends on another test's rows.

```
3ab323c^ : 1 run-alone failure  — TestOutbox_Counts (store_test.go:962)
3ab323c  : 0 run-alone failures out of 41
```

The survey is therefore falsifiable — it finds the known bug at the pre-fix
commit — and it independently confirms the implementer's report that
`TestOutbox_Counts` was the only instance. Cross-checked by reading the two
tests criterion 4 names: `TestOutbox_Counts_ReportsSkippedDistinctlyFromAbandoned`
is already a `distinctKind`-scoped before/after delta (the fixed test now
matches its idiom exactly), and `TestOutbox_AbandonedCountByKind_ScopedToKind`
scopes both absolutes to a freshly-minted kind. Three further full-package runs
under `-shuffle=on` all passed, so no ordering dependence remains.

**Criterion 2.** The test now establishes what it asserts: two rows, each under
its own per-call-unique `distinctKind` (`testdb.Unique()`), `ClaimDue` scoped to
`[]Kind{sentKind}` with an assertion that it claimed exactly that one row — so
it structurally cannot touch the row left queued — and an exact `+1` on each of
`Queued` and `Sent`. Criterion 5 is respected: nothing here depends on test
ordering or on `testdb.Lock()`'s serialisation.

**Criterion 6.**

```
uptime                                                  # load 1.7-6.1, not saturated
ISSUE=0382r scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
  # ok internal/outbox 0.848s | ok internal/mailing 4.978s | ok internal/handlers 9.131s
  # gofmt -l clean; skip audit (no skips) — VERIFICATION PASSED
scripts/check.sh guards                                 # 0 failures — VERIFICATION PASSED
gofmt -l internal/outbox/store_test.go                  # clean
sha256(file) == sha256(gofmt file)                      # equal — gofmt stable
```

Named tests: `TestOutbox_Counts` (alone, before and after, and under four
`Counts` mutants), `TestOutbox_Counts_ReportsSkippedDistinctlyFromAbandoned`
and `TestOutbox_AbandonedCountByKind_ScopedToKind` (alone), and all 41
`internal/outbox` tests individually plus three shuffled full-package runs.
`.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` scanned for as **bytes** in
`python3` over `go/scanner`-extracted comment tokens — never BSD `grep -P`
(CLAUDE.md §8) — zero in any comment of `store_test.go` before or after; zero
`<file>.go:<int>` citations likewise. No mutation targeted a literal or seeded
id: every row was one the test enqueued into its own scratch database. All four
scratch databases dropped and confirmed absent.

**CLAUDE.md §3 decision: no escalation.** Approved on the first review pass, so
the two-bounce trigger never arose. Implementation stays on Sonnet.

## Work log

- 2026-09-02 — Filed by the orchestrator from `#0378`/`#0380`'s review (§9).
- 2026-09-02 — Implementation (Sonnet, fresh subagent, batched with `#0382`).
  Reproduced the run-alone failure first, against a `git archive HEAD`
  export in its own scratch database (`Queued = 0, want at least 1`,
  matching the issue exactly), then rewrote `TestOutbox_Counts` to scope
  each row to its own `distinctKind`, claim only the row bound for `Sent`,
  and assert a before/after delta rather than an absolute floor. Surveyed
  the rest of `internal/outbox` for the same shape (criterion 4): found no
  other instance — `TestOutbox_Counts` was the only test whose truth
  depended on residue from another test. `ISSUE=0386demo scripts/check.sh go
  ./internal/outbox/... -run '^TestOutbox_Counts$'` passes standalone;
  `ISSUE=0382 scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` and `scripts/check.sh guards` both
  `VERIFICATION PASSED`. `gofmt` stable. Left `in-progress` for review per
  CLAUDE.md §9. Token usage/cost — orchestrator to record.
- 2026-09-02 — Review (Opus, fresh subagent, batched with `#0382`). Reproduced
  the run-alone failure at `3ab323c^` and the pass at `3ab323c` on separate
  fresh scratch databases; proved the new delta is not vacuous by mutating
  `outbox.Store.Counts` four ways (three killed, including all-zeros); ran an
  independent residue survey by executing all 41 `internal/outbox` tests alone
  (1 failure pre-fix, 0 post-fix) plus three `-shuffle=on` package runs.
  Recorded one pre-existing gap for the orchestrator to file: a `Counts` that
  swaps `Queued` and `Sent` passes the entire package. `ISSUE=0382r
  scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` and `scripts/check.sh guards` both
  `VERIFICATION PASSED`. Approved; status `resolved`. §3: no escalation — first
  pass approved. Token usage/cost — orchestrator to record.
