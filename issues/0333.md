# 0333 — `testdb.sh drop template` still drops the real shared template

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-28 |
| **Commit** | — |

## Description

`#0327` made `gc --all` refuse to drop `opencircuit_test_template`. `drop` was
not changed, and it still will: `name_for template` resolves to
`opencircuit_test_template` (and, since `#0208`, so does `name_for TEMPLATE`).

So `scripts/testdb.sh drop template` — which reads like "drop my scratch
database called template", the same shape as `drop 0324` — destroys the shared
template every agent clones from. Rebuilding it is `scripts/testdb.sh template`,
so this is recoverable rather than catastrophic, but it stops every concurrent
agent's `create` until someone notices and rebuilds.

The defect is **pre-existing**, not introduced by `#0327`. What `#0327` created
is the *inconsistency*: two commands with the same blast radius, one now
guarded and one not. An agent that reads `#0327` and concludes the template is
protected is wrong for exactly one command.

This is the same class as `#0207` (`db-reset.sh` refusing without `--force`)
and `#0150` (`gc` refusing without `--all`): a destructive default on a shared
resource, cured by requiring an explicit second signal.

## Acceptance criteria

1. `scripts/testdb.sh drop template` (and `drop TEMPLATE`) refuses by default,
   naming what it would destroy and how to rebuild it
   (`scripts/testdb.sh template`).
2. An explicit override flag performs the drop, consistent in spelling with the
   existing `--all` / `--force` conventions in `testdb.sh` and `db-reset.sh`.
3. Dropping an ordinary per-agent scratch database (`drop 0324`) is unaffected
   and still needs no flag.
4. The refusal reports live connections the way `#0150`'s `gc` guard does, so a
   template in use is never dropped out from under a running agent.
5. Covered by an assertion in `scripts/testdb_gc_guard_test.sh` (or a sibling
   external harness), following §8: mutate a COPY, assert exit code and output
   from OUTSIDE, prove the assertion is sensitive by mutation, fail closed on an
   empty extraction. Never run the destructive path against the real pool.

## Relation

- Found by: [#0327](0327.md)/[#0328](0328.md)'s review
- Relates to: [#0327](0327.md) — which guarded `gc` and left `drop` inconsistent
- Relates to: [#0332](0332.md) — the other half of the same blast radius
- Relates to: [#0150](0150.md) — `gc`'s refusal, the model for this one
- Relates to: [#0207](0207.md) — `db-reset.sh`'s `--force`, the same pattern
- Relates to: [#0208](0208.md) — which made `name_for TEMPLATE` resolve here

## Fix

`drop`'s dispatch case now:

1. Parses `--force` in any position among the remaining arguments (matching
   `#0250`'s fix for `db-reset.sh` — `drop template --force` reads naturally
   with the flag last).
2. After the existing existence check, tests whether the resolved database
   name equals `$TEMPLATE` **or** `$DEFAULT_TEMPLATE` — covering both "the
   currently-selected template (which `TEMPLATE_DB` may have overridden)"
   and "the real default," the same two-name shape `#0327` already added to
   `gc`.
3. If it matches and `--force` was not given, refuses: names the database,
   explains the blast radius (stalls every concurrent agent's next
   `create`), and points at `scripts/testdb.sh template` to rebuild instead.
4. If `--force` was given, still checks `pg_stat_activity` for live
   connections on that database (the same query shape `gc --all`'s
   per-database skip already uses) and refuses — reporting the connection
   count — if any exist. `--force` means "I know this is the shared
   template and I mean to drop it," not "drop it even out from under a
   running agent's clone." Only once both checks pass does the `DROP
   DATABASE` actually run.

An ordinary `drop <id>` never matches `$TEMPLATE`/`$DEFAULT_TEMPLATE`, so
none of this changes its behavior (criterion 3) — no flag is consulted or
needed.

## Verification

Ran `uptime` before each step; load average 1.8–3.9 throughout (shared
machine, never treated as a defect per §5).

**Every destructive path was proven against a private, prefix-scoped copy
of the real script, following `#0327`'s own technique — never the real
pool.** Read-only refusal calls (which by construction never issue `DROP
DATABASE`) were additionally verified end-to-end against the real,
unscoped `scripts/testdb.sh` and the real shared template, with the
template's continued existence confirmed by `psql` immediately after each
call:

```
$ psql -tAc "select datname from pg_database where datname like 'opencircuit%'"
opencircuit_test opencircuit_live opencircuit opencircuit_test_template
$ scripts/testdb.sh drop template          # criterion 1
refusing: 'opencircuit_test_template' is the shared template every 'scripts/testdb.sh create'
clones from. ... If you really mean to drop it, pass --force.
$ echo $?
1
$ scripts/testdb.sh drop TEMPLATE          # criterion 1, uppercase (#0208 folds it)
refusing: ... (identical)
$ echo $?
1
$ psql -tAc "select datname from pg_database where datname like 'opencircuit%'"
opencircuit_test opencircuit_live opencircuit opencircuit_test_template   # unchanged
```

**Criterion 3** (ordinary drop unaffected), against the real script with a
private id, dropped immediately after:
```
$ scripts/testdb.sh create 0333verify   # provisions (template digest predates #0320, unrelated)
$ scripts/testdb.sh drop 0333verify
dropped opencircuit_test_0333verify
$ echo $?
0
```

**Criteria 1, 2, 4 — the destructive `--force` paths — proven on a
`TESTPREFIX`-scoped copy** (`sed`-repointed `PREFIX`, same technique
`testdb_gc_guard_test.sh` already uses for `gc`), with a synthetic
`opencircuit_test_0333manualgt_template` I created and owned:

- `drop template` (no `--force`) on the scoped copy: refused, template
  survived.
- `drop template --force` **while a background `pg_sleep(15)` held a live
  connection** open on it: refused (`'...' has 1 active connection(s) —
  someone ... is using it right now. Wait for it to finish, then retry.`),
  template survived — criterion 4.
- Backend terminated server-side (`pg_terminate_backend`, waited for
  `pg_stat_activity` to clear — `#0223`'s lesson: killing the local `psql`
  client does not stop a backend blocked in `pg_sleep`), then `drop
  template --force` again: dropped it — criterion 2.
- Also checked under a `TEMPLATE_DB` override (`drop other` where
  `TEMPLATE_DB=...other`): refuses without `--force`, drops with it —
  confirming the `$TEMPLATE`-equals check (not just `$DEFAULT_TEMPLATE`)
  works too.

**Criterion 5 — automated external guard**, `scripts/testdb_gc_guard_test.sh`
Part 6 (new; old Part 6 "leak census" renumbered to Part 7), built on the
same `$SCOPED` copy Parts 2–5 already establish and scope-check before use:

- Bare `drop template`/`drop TEMPLATE` refuse (exit ≠ 0), template
  survives, message names `--force` and mentions rebuilding.
- `drop template --force` with a live connection (background `pg_sleep`,
  polled onto `pg_stat_activity` before proceeding, terminated
  server-side and waited-for afterward — mirroring the existing Part 2
  pattern) still refuses and still survives; message mentions the
  connection.
- `drop template --force` with no connection succeeds.
- An ordinary `drop <id>` (no flag) still works, unaffected.
- **Mutation proof**: `sed -e 's/if \[ "\$force" != "1" \]; then/if false; then/'`
  neuters the refusal condition specifically (verified by `grep`-confirming
  the mutation took effect before running it, §8's fail-closed-on-empty-
  extraction rule); against the mutant, bare `drop template` (no `--force`)
  **does** drop it — proving the assertions above are sensitive to the
  #0333 regression, not vacuously true.
- Part 7's leak census and the byte-identity check (tracked
  `scripts/testdb.sh` unchanged across the whole run) both pass.

`ISSUE=0333gt` and `ISSUE=0333gt2` — two full runs of
`testdb_gc_guard_test.sh`, 0 failures each.

**Unrelated regression check**: `scripts/db_reset_guard_test.sh`
(`ISSUE=0333dbr`) also run directly (not `check.sh guards`, per instructions
— `dev_guard_test.sh` binds `:5173` and has caused false failures this
session) — all 13 parts pass, confirming `db-reset.sh`'s own `--force`
pattern (the model this issue follows) is untouched.

**`shellcheck -S warning`** and **`bash -n`**: clean on `scripts/testdb.sh`
and `scripts/testdb_gc_guard_test.sh`.

**Pool census**, before and after all work across `#0331`–`#0333`:
`opencircuit`, `opencircuit_live`, `opencircuit_test`,
`opencircuit_test_template` — unchanged. (Another agent's
`opencircuit_test_0336b`, present at the start of this session, and a
transient `opencircuit<pid>gt`-shaped database from a concurrently-running
`db_reset_guard_test.sh` elsewhere, were both observed and gone again later
— neither created nor touched by this work; verified by inspecting the
commands run, which never invoke the real, unscoped script's destructive
`gc --all` or a `--force`'d `drop` against the real prefix.)

## Work log

- 2026-08-28 — Filed by the orchestrator from the `#0327`/`#0328` review.
- 2026-08-31 — Implemented: `drop` now refuses to drop `$TEMPLATE`/
  `$DEFAULT_TEMPLATE` without `--force`, and even with `--force` refuses
  while the target has a live connection. Guard coverage added as Part 6 of
  `scripts/testdb_gc_guard_test.sh`. See `## Verification`.
