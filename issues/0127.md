# 0127 — Send a welcome email when a subscriber confirms

| | |
|---|---|
| **Status** | in-progress |
| **Module** | mailing / subscribers |
| **Platform** | All |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-21 |
| **Commit** | `41993c0` |

## Description

Nothing is sent after a successful double opt-in confirmation. The welcome message is the highest-engagement email the list will ever send and the natural place to set expectations about frequency and show the way out — both of which measurably reduce complaints later.

## Implementation notes

Phase 2 (Sonnet), 2026-08-24. No separate phase-1 planning pass ran; design
decisions were made and disclosed inline (code comments + this section)
rather than in a `## Plan`.

**`internal/subscribers.Store.Confirm`** enqueues `outbox.KindWelcome`
inside its own confirmation transaction, immediately after recording the
`confirmed` event — mirroring #0126's "committed signup can never have an
unsent confirmation" one step later in the flow: a committed confirmation
can never have an unsent welcome. Two guards run first, inside the same
transaction:

- **Suppressed addresses.** Checked via a direct `EXISTS (SELECT 1 FROM
  suppressions WHERE email = $1)` inline in `Confirm` rather than going
  through `SuppressionStore` — same package, same transaction, no second
  round trip. The subscriber still confirms normally (status → active);
  only the welcome enqueue is skipped.
- **Complained subscribers.** `Confirm` already refuses with
  `ErrComplainedLocked` before any of this runs, so a complained subscriber
  never reaches the enqueue at all — proved explicitly rather than left
  implicit (`TestConfirm_ComplainedSubscriberNeverGetsWelcome`).

**"Sent exactly once per subscriber; a second confirmation does not
re-send"** is true by construction, not by a separate check: `confirm_token`
is single-use (cleared to `NULL` on activation), so a retried `Confirm` call
with the same, now-stale token returns `ErrTokenInvalid` before reaching the
enqueue.

**Selected interest names** are read inside `Confirm`'s transaction via a
new `selectedInterestNamesTx` — a raw join against `interests` (no import of
`internal/interests`, keeping this package a leaf per its own package doc
comment) — and stored in the queue payload. Read AT CONFIRM TIME, not
re-read by the worker at send time, so a later preference change can't
retroactively rewrite what the email already said.

**`internal/mailing.BuildWelcomeEmail`** is a seventh template. Content:
what Open Circuit SF is (paraphrased from `Home.svelte`'s own "What this
is" copy), the subscriber's selected interests (or a general-announcements
fallback when none), a "roughly once a month" cadence line (flagged the
same way `About.svelte` flags it — sourced from PRD §4.1's own reference
layout, not invented), a CTA to `/workshops`, and the standard
manage/unsubscribe footer + physical address via `ShowListFooter`.

**Deliberate, disclosed departure:** unlike every other template in this
file, `BuildWelcomeEmail` carries the RFC 8058 `List-Unsubscribe` /
`List-Unsubscribe-Post` / `List-Id` header set (via the same
`CampaignHeaders` helper campaign mail uses) — historically "campaign mail
only" in this package's own doc comments. #0127's acceptance criteria name
the headers explicitly for this message, and the privacy policy's "every
campaign email carries a one-click unsubscribe link" is a floor commitment,
not an exclusivity claim, so this does not contradict it. Both package doc
comments (`templates.go`, `transactional_templates.go`) and
`TestNoTransactionalMessageCarriesCampaignHeaders` are updated to state and
prove this positively for "welcome" going forward, the same way they already
did for "campaign".

**No `/archive` link**, per this issue's own Notes: #0127 depends on #0123
(the public campaign archive), which had not landed when this was
implemented. Pinned by `TestBuildWelcomeEmail_NoArchiveLink`. Add the link
once #0123 lands.

**`internal/mailing.OutboxWorker`** renders `KindWelcome` from a new
`welcomePayload` (mirrors `internal/subscribers.welcomePayload`
field-for-field, matched by JSON field name per this package's existing
producer/consumer convention) and writes `welcome_sent` on `MarkSent` — the
exact `confirmation_sent` precedent (#0126's plan §6: written when the
message actually LEAVES the queue, not at enqueue time). `listDomain`
(`config.Config.EmailListDomain`) is threaded through
`OutboxWorkerDeps`/`OutboxWorker` and wired in `main.go`'s
`newOutboxWorker`, since `BuildWelcomeEmail` needs it for the header set.

**Disclosed design call (flagged for the reviewer, not resolved
unilaterally):** a subscriber who unsubscribes and later resubscribes
(`RestartSignup`) gets a fresh `confirm_token` and, on that later `Confirm`,
a SECOND welcome email. The acceptance criterion's "second confirmation of
an already-confirmed address does not re-send" reads as token-replay
protection, not a lifetime cap across a genuine resubscribe — but this is a
judgment call, not a certainty.

**Closed-set guard updated.** `#0126`'s
`TestSubscriberEventActions_EveryConstantHasCallSiteOrOwner` named `#0127`
as `ActionWelcomeSent`'s pending owner. That entry moved from
`ownedByLaterIssue` to `hasCallSite`. Verified the guard is load-bearing by
manually removing the entry (leaving `welcome_sent` with neither a call
site nor an owner) and observing the exact failure ("has neither a call
site nor a recorded owner issue — the closed set has rotted"), then
restoring the file and confirming the restored content hashes identical to
the pre-mutation copy (`shasum -a 256`).

## Verification

Every claim below rests on Postgres (`TEST_DATABASE_URL`) and
`mailing.RecordingMailer` — no live SES call anywhere (CLAUDE.md §10 item
2).

```
ISSUE=0127 scripts/check.sh go ./internal/outbox/... ./internal/subscribers/... \
    ./internal/mailing/... ./internal/handlers/... ./internal/auth/... \
    ./internal/db/... ./cmd/opencircuit/...
```
→ build/vet/gofmt clean; `ok` for all seven packages; zero skips.

```
ISSUE=0127 scripts/check.sh
```
→ full Go suite (17 packages) all `ok`, zero skips; `npm run check` — 274
files / 0 errors / 0 warnings; `npm test` — 33 test files / **3,832** tests
passed (unchanged from #0126's baseline — this issue touched no `web/`
files, so the count did not move).

**Named tests proving the load-bearing properties:**

- `TestConfirm_EnqueuesWelcomeAtomicallyWithActivation` — a committed
  `Confirm` always has both the subscriber active AND a `welcome` row
  queued; a retried `Confirm` with the same (now-stale) token fails and
  does not create a second welcome row ("not sent twice on retry").
- `TestConfirm_SuppressedAddressNeverGetsWelcome` — a subscriber suppressed
  after `Create` but before confirming still activates, with zero welcome
  rows.
- `TestConfirm_ComplainedSubscriberNeverGetsWelcome` — `Confirm` fails
  outright for a complained subscriber; zero welcome rows follow.
- `TestConfirm_WelcomePayloadCarriesSelectedInterestNames` /
  `TestConfirm_NoInterestsSelected_WelcomePayloadHasEmptyInterestList` —
  the payload (read before any worker drains it, so unscrubbed) actually
  carries the real selection, or an empty list when none.
- `TestOutboxWorker_DrainsWelcomeRow` — end-to-end: claim, render with the
  real payload, send through `RecordingMailer`, `MarkSent` scrubs the
  payload to `{}`, the rendered message carries a `List-Unsubscribe`
  header.
- `TestOutboxWorker_MarkSent_RecordsWelcomeSent` — `welcome_sent` is
  written on send, not enqueue. Deliberately does NOT go through
  `subscribers.Store.Confirm` to produce the row (see the test's own
  comment): `internal/mailing`'s `TestMain` truncates its shared test
  database once per package run, not per test, and several
  `AudienceAll`-scoped preflight tests elsewhere in this package (
  `worker_store_test.go`) assert "no subscribers seeded" — an
  `ACTIVE` subscriber left behind by going through `Confirm` here would
  silently pollute those counts depending on file-execution order. This
  test enqueues the welcome row directly against a subscriber left
  `pending`, proving the identical `OutboxWorker.sendOne` wiring without
  that side effect. `Confirm`'s own atomicity is proved separately, in
  `internal/subscribers` where it doesn't have this shared-fixture
  conflict.
- `TestBuildWelcomeEmail_Golden` (CRLF `.txt` fixture, confirmed by byte
  count), `TestBuildWelcomeEmail_ListsSelectedInterests`,
  `TestBuildWelcomeEmail_NoInterestsFallsBackToGeneralAnnouncement`,
  `TestBuildWelcomeEmail_NoArchiveLink`,
  `TestBuildWelcomeEmail_ListUnsubscribeHeadersMatchCampaignHeaders`,
  `TestWelcomeEmail_CarriesFooterLink`.
- `TestNoTransactionalMessageCarriesCampaignHeaders` (updated) — six
  purely-transactional templates still carry zero custom headers;
  "campaign" and "welcome" both positively carry the RFC 8058 set.
- `TestAllMessagesEnumeratesEveryBuildEmailFunc` (existing AST guard,
  unmodified) — passes with `BuildWelcomeEmail` added to `allMessages()`,
  proving the new builder didn't slip past that guard.
- `TestSubscriberEventActions_EveryConstantHasCallSiteOrOwner` — passes
  with `ActionWelcomeSent` moved to `hasCallSite`; mutation-proven to fail
  when that entry is removed (see Implementation notes).

**Not independently re-verified by a fresh reviewer pass** — this is the
implementer's own report. Status stays `in-progress`.

**One ambient note, not a defect in this work:** a `scripts/check.sh` run
briefly reported load average 10.05 (other agents' concurrent work,
including `#0126`'s bounced-back review fix); all packages still reported
`ok` and the run was not re-run to "wait out" the load, per CLAUDE.md §5.

## Acceptance criteria

- [ ] `BuildWelcomeEmail` added to `internal/mailing/transactional_templates.go`, HTML and plain text, matching the existing template family's structure and brand tokens
- [ ] Enqueued via `#0126`'s outbound queue with `kind = 'welcome'`, inside the confirmation transaction
- [ ] Sent exactly once per subscriber. A second confirmation of an already-confirmed address does not re-send
- [ ] Content: what Open Circuit SF is, which interests they selected, roughly how often to expect mail, a link to `/archive` for past mailings, a link to the preference center, and a working unsubscribe link
- [ ] Carries `List-Unsubscribe` and `List-Unsubscribe-Post` headers (`#0035`)
- [ ] Includes the `physical_address` setting, like every other outbound message
- [ ] Writes a `welcome_sent` subscriber event (`#0126`)
- [ ] Golden-file tests for HTML and text, committed with **CRLF** line endings per `#0078`
- [ ] Imported subscribers (`#0125`) do **not** receive it — they never confirmed here, and a welcome to an imported address reads as unsolicited
- [ ] Addresses that accept an import invitation (`#0129`) do **not** receive it either — the invitation was their introduction
- [ ] A suppressed address never receives it, even if a confirmation somehow lands

## Notes

PRD §6.3 covers the subscription flow this hooks into; §6.8 is the archive it links to.

The `/archive` link is the reason this issue depends on `#0123` — a welcome email pointing at an empty or nonexistent archive is worse than one that does not link at all. If `#0123` slips, ship the welcome without the archive link rather than with a dead one.

`#0076` fixed two email-copy defects the template re-theme left behind; read it before writing new copy so the same mistakes are not reintroduced.

Golden files in `internal/mailing/testdata` must be CRLF — `#0078` cost a day when four tests failed on a clean checkout because git normalized them to LF.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §6.3 — Subscription flow (double opt-in) · 56 lines
  `sed -n '/^### 6\.3 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0028](0028.md) — Write the transactional email templates
- Depends on: [#0030](0030.md) — Build the confirmation flow
- Depends on: [#0123](0123.md) — Public campaign archive (the link target)
- Depends on: [#0126](0126.md) — Durable outbound queue
- Relates to: [#0076](0076.md), [#0078](0078.md), [#0129](0129.md)

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Phase 2 only — no separate phase-1 planning pass ran; design decisions were
made and disclosed inline (see Implementation notes). Status stays
`in-progress`: this is the implementer's own report, not an independently
re-verified phase-3 review. The orchestrator fills in the token counts.
