# 0233 — Composite a live terminal screen onto the hero CRT photograph

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Web |
| **Module** | web / design |
| **Phase** | 8 — Polish and follow-ups |
| **First seen** | 2026-08-24 |

## Description

`#0231` put a photograph of a green-phosphor Zenith monitor in the home hero.
The screen in that photograph is a **still image**: it says `> Open Circuit SF _`
and always will.

The user supplied a reference implementation —
`../open-circuit-sf-perspective-crt.html` — that makes the screen **live**: a
canvas is projectively warped onto the four corners of the photographed glass,
so real text can be typed, scrolled and re-rendered while still appearing to sit
on a monitor photographed at an angle. It is a genuine perspective mapping, not
a rotate/skew, so the illusion survives the ~15° yaw of the photograph.

The user asked for a **static-HTML proof of concept matching the current home
page design first**, before any of it touches `web/src/views/Home.svelte`. That
prototype is the deliverable of this issue; integration is a separate one.

## What has been built

`prototypes/crt-live-screen/` — `index.src.html` is the source, `build.py`
inlines every asset into a self-contained `index.html`. It reproduces the real hero (design tokens copied from
`web/src/app.css`, the `TerminalPanel` chrome, the real headline, status list
and command line, the theme toggle's three states) and composites a live screen
onto the real hero photographs, referenced by relative path out of
`web/public/` so there is exactly one copy of each asset in the tree.

**The technique**, carried over from the reference and rewritten to this
project's conventions:

1. Four corners of the photographed raster are stored as **percentages of the
   photograph**, so the fit survives any resize.
2. A projective homography is solved from the unit rectangle to those four
   corners — eight unknowns, Gauss-Jordan with partial pivoting.
3. Its coefficients are packed into a CSS `matrix3d()` (column-major, `z = 0`)
   on a plane whose `transform-origin` is `0 0`.
4. Canvas draws the terminal; a second, blurred canvas composited with
   `mix-blend-mode: screen` supplies the phosphor bloom; scanline and glass
   gradients sit on top.

**The canvases are cleared, not filled.** The reference implementation paints an
opaque `#03200c` ground, which replaces the photographed screen with a flat
rectangle — losing its texture, curvature, corner falloff and the glow pooling
at the top, and leaving a visibly flat panel sitting on a curved tube. Clearing
instead composites only the glyphs, so all of that survives. The bloom canvas
must be cleared too: it is `mix-blend-mode: screen`, and screening against a
filled ground would lift the whole photographed screen rather than just the
text. The scanline and glass overlays were cut to roughly a third of their
previous strength as a consequence — the photograph already supplies both, and
at the old values they doubled up into moire and crushed corners.

## Blanking the photographed screen

Transparency exposed a problem the opaque fill had been hiding: the
photographs have `> Open Circuit SF _` **baked into the screen**, so live text
would land on top of it.

`tools/blank-screen.py` reconstructs both photographs without it. Two
approaches failed first, and both are recorded in that script so nobody repeats
them:

- **Median-filtering the bright pixels** removes the glyph strokes and leaves
  the bloom — the glow around the text is several times wider than the strokes,
  so the result is a row of bright smears.
- **Flagging "brighter than the column median" and interpolating** also catches
  the pool of glow at the top of the screen, which is likewise brighter than
  the median, producing tall vertical streaks down the whole tube.

What works is discriminating on **horizontal high-frequency detail** rather
than brightness: glyphs have sharp vertical edges, the glow is smooth, so
`|G − horizontal_median(G, 21)|` is large only on text and locates the band
precisely. The band is then rebuilt per column by interpolating between the
clean rows immediately above and below, preserving the screen's own vertical
gradient rather than flattening it, and re-grained with light noise so it does
not read as a blurred patch against real texture.

The prototype has a toggle to put the original photograph back, so the reason
these derived assets exist is visible rather than asserted.

## How the corners were measured

Worth recording, because **two automatic approaches were tried and both
failed**, and the failures look like successes if not checked visually.

- **Thresholding the phosphor green finds the glow, not the raster.** Light
  pools on the bezel and inner surround, so a green mask reaches well outside
  the glass. Eroding it hard and taking extremes of `x±y` gave a quadrilateral
  covering roughly half the screen.
- **Half-maximum crossing per scanline is worse.** The brightest point in most
  columns *is* that glow, so the reported bottom edge jumped between 23% and
  75% of image height depending on the column.

**What worked:** fill the warped plane with flat magenta, screenshot it over the
photograph via `safaridriver`, and compare footprints. Over- and under-shoot at
all four corners are visible at once, which a text render does not show. Two
refinement passes converged on:

```
tl {x:23.03, y:28.84}   tr {x:68.38, y:23.03}
br {x:67.68, y:77.51}   bl {x:23.00, y:72.30}
```

Those are **the user's own hand-dragged values**, which superseded the
screenshot-fitted ones (`tl 22.5/24.0`, `tr 69.8/18.0`, `br 68.2/72.4`,
`bl 23.0/72.3`). They sit lower and their bottom edge slopes. The measurement
was not wrong so much as measuring the wrong edge: the magenta-fill comparison
matched the *bright raster*, while the glass a viewer reads as "the screen"
carries on past it into the dimmer border where the phosphor falls off. A
person dragging a handle onto what looks like the corner beats a threshold
fitted to what the sensor recorded. `tools/blank-screen.py` uses the same
numbers, so the blanking mask and the live plane cannot drift apart.

**One calibration serves both themes.** `#0231` ships a dark and a light
photograph; their registration was checked against the red power LED — an
unambiguous landmark present in both — whose centroids agree to within **2px in
x and 5px in y on a 1402×1122 image** (0.14% and 0.45%). Verified by rendering,
not inferred: the same corners were screenshotted against both photographs.

## Acceptance criteria

- [x] A standalone static HTML prototype exists that matches the home page's design — real tokens, real panel chrome, real hero copy, working theme toggle
- [x] The prototype opens from the Finder with no server — verified zero external subresources at runtime
- [x] The folder zips and works for someone without the repository — verified by unzipping outside the tree and rendering it
- [x] The live screen is projectively warped onto the photographed glass and stays registered when the stage is resized
- [x] The same corner calibration is proved correct against both the dark and the light photograph
- [x] Calibration is adjustable in the prototype — draggable handles, numeric fields, and JSON output to paste into the component
- [x] `prefers-reduced-motion` renders a static finished frame rather than a blank screen or an animation
- [x] A decision is recorded on what the live screen should *say* on the real home page — see `## Decision`
- [x] The performance cost is measured, not assumed: repaint rate, CPU while idle, and behaviour when the hero scrolls out of view — see `## Measured cost`; the scroll case is **unimplemented**, which is a finding rather than a result
- [x] Integration into `web/src/views/Home.svelte` is filed as its own issue and does not happen here — filed as `#0270`

## Decision

**The screen stays illustrative — no API.** Recorded by the orchestrator on
2026-08-25.

The eight-command session is static brand copy. Only the first command shows
workshop dates, and wiring it to the real API buys little and costs several
things:

- It duplicates the "Next up" section immediately below the hero, so the same
  three workshops appear twice on one screen.
- It makes a decorative element depend on a network call, which must then have
  a loading state, an error state, and an empty state — for a canvas most
  visitors scroll past.
- **It changes the accessibility answer.** The stage is `aria-hidden` because
  the screen repeats what the adjacent headline and command line already say.
  Real dates are information, and hiding information is wrong — so an API-fed
  screen would need a text alternative, which then duplicates "Next up" a third
  time for screen-reader users.

Illustrative copy keeps the element decorative, which is what makes the whole
design defensible. The dates shown are plainly examples; if that ever reads as
a real schedule, change the copy rather than the plumbing.

**Reopen if** the hero stops being accompanied by "Next up", at which point the
duplication argument disappears.

## Measured cost

Measured on the prototype in Safari via `safaridriver`, counting real
`clearRect` calls on the terminal canvas:

| | paints | rate |
|---|---|---|
| Tab visible, 12s | **131** | ~10.9/s |
| Tab hidden, 12s | **0** | — |

So the idle cost is roughly eleven canvas repaints a second while visible, and
**exactly nothing when the tab is hidden** — the `document.hidden` early return
works completely, not partially.

**The scroll-out-of-view case is not implemented**, and that is the honest
result rather than a measurement: a visitor who scrolls past the hero and reads
the rest of the page keeps paying ~11 repaints a second for a canvas they
cannot see. `#0270` carries it as a blocking criterion.

**LCP was not obtained.** The observer returned no entries in this harness and I
did not spend further budget chasing it. `#0231` established the adjacent fact —
a CSS background is never an LCP candidate in WebKit — but that does not settle
what a canvas does, and open question 3 asked for a measurement rather than an
inference. It stays open, and `#0270` must answer it against the real home page,
which is the only place the answer matters.

## Open questions

These need answering before integration, and none is settled by the prototype:

**Question 1 is now answered — see `## Decision`. Questions 2–5 pass to
`#0270`.**

1. **What does it say?** *(answered — illustrative, no API.)* The prototype runs a continuous eight-command
   session — workshops, tools, topics, venues, skill level, subscription
   interests — typing each command, printing its output, holding 3s, then
   moving on and looping. Only the first duplicates the "Next up" section
   below the hero, and only that one would need the real API; the rest are
   static brand copy that cannot go stale. The decision left is whether the
   workshop dates come from the API or stay illustrative.
2. **What does it cost?** The reference implementation repaints on a 100ms
   timer *plus* a 520ms cursor blink *plus* a 720ms line timer, all
   unconditionally and forever. The prototype already drops the opacity jitter
   and stops the cursor under `prefers-reduced-motion`, but nothing yet pauses
   the canvas when the hero is scrolled out of view or the tab is hidden. On a
   marketing home page that is a battery cost paid by every visitor for
   something most will scroll past in seconds.
3. **Does it become the LCP element?** `#0231` measured the still photo taking
   LCP from the headline. A canvas that starts empty and types in may push LCP
   later, or may not count at all. Must be measured, not reasoned about.
4. **What happens with JavaScript off, or before hydration?** The still
   photograph is currently correct on its own. The live version must degrade to
   exactly that rather than to a blank rectangle.
5. **Accessibility.** The prototype marks the whole stage `aria-hidden` on the
   grounds that the screen repeats the adjacent headline. That holds only while
   the content stays decorative — a screen showing real workshop dates is
   information, and hiding it would be wrong.

## Notes

Filed at the user's request on 2026-08-24, along with the prototype itself.

**The first version did not open from the Finder**, and the user hit it
immediately. It referenced the photographs and webfonts by relative path out of
`web/public/` — correct paths, which resolve fine over HTTP — but under
`file://` a browser will not reliably fetch subresources from outside the
page's own directory, so the CRT rendered as an empty box and the fonts fell
back. That failure mode looks like a broken stylesheet, not a blocked fetch,
which is what makes it worth writing down. `CLAUDE.md` §8 already records the
sibling case (CSS masks failing silently under `file://`); this is the same
hazard with a different symptom.

Fixed in two steps. First `build.py` was added to inline the assets as `data:`
URIs into the `index.html` people actually open — 183 KB generated, zero
external subresources, confirmed at runtime via the Resource Timing API rather
than assumed.

That fixed the generated file but not the folder: `index.src.html` and
`build.py` still reached up into `web/public/`, so a zip of `prototypes/` was
only half portable. On the user's instruction ("it should be self contained
even if it keeps a local copy of the light and dark CRT images") each prototype
now carries its own `assets/`, and nothing references a path that climbs out of
its own directory.

**The duplication is checked, not trusted.** `build.py` compares `assets/`
against `web/public/` by SHA-256 and exits non-zero on any difference; `--sync`
copies upstream over. Proved by appending one byte to a local copy: the build
reported `hero-crt-760.webp: DIFFERS` and exited 1, `--sync` restored it, and
the hash matched the pre-mutation backup. Outside the repository the check
reports that there is nothing to compare against and carries on.

Verified end to end by actually zipping the folder, unzipping it outside the
tree, rebuilding there (`upstream web/public/ not present — skipping drift
check`), and rendering both `index.html` and `index.src.html` from that copy.
240 KB zipped.

The reference file is 2.4 MB because it embeds its photograph as a base64 data
URI, and its photograph is a **different crop** (1365×1097) from this project's
asset (1402×1122) — so its own corner defaults do not transfer, which is why
this issue re-measures them.

Not to be confused with `#0231`, which placed the still photograph. This issue
only makes the screen live.

## Relation

- Depends on: [#0231](0231.md) — the hero CRT photograph and its two-theme pinning
- Relates to: [#0232](0232.md) — Phase 9, where a configurable hero would need this to be optional
- Relates to: [#0015](0015.md) — the Home view this would eventually change

## Resolution

**Accepted by the user, 2026-08-25** — *"#0233 is done… It looks great."*

All ten acceptance criteria were met and ticked before this. The prototype
stands as the record of the technique: the projective warp, the corner
calibration (the user's own hand-dragged values), the two failed automatic
detection approaches, the blanking of the photographed screen, and the measured
repaint cost. `#0270` carried it into the real hero.
