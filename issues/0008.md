# 0008 — Verify the passkey auth stack end to end after the port

| | |
|---|---|
| **Status** | resolved |
| **Module** | auth |
| **Platform** | All |
| **Phase** | 1 — Auth carried over |
| **First seen** | 2026-08-18 |
| **Closed** | 2026-08-18 |
| **Commit** | 0e154d5 (first pass), 0cce767 (second pass — suffix assertion + honest naming + rate-limit/audit-rollback tests) |

## Description

Prove that registration, login, recovery, session handling, and credential management all still work under the new module path and domain — before any new feature depends on them.

## Resolution notes

> 🟢 Resolved 2026-08-18 — The bounce is fixed: `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW` now carries a real registrable-domain suffix assertion that the reviewer independently re-mutated and confirmed bites, the overstated test name is honest, and the two overstated criteria are backed by real tests — but **acceptance criteria 1 and 2 remain unticked and await human browser verification**, so `resolved` here means "everything automatable is proven", not "fully verified".

**Reviewer's independent re-verification (second review pass, `TEST_DATABASE_URL` exported).**
`go build ./...` clean · `go vet ./...` clean · `go test ./... -count=1` — every package `ok`, zero skips, real elapsed time on every DB-backed package (`cmd/opencircuit` 0.265s, `internal/audit` 3.005s, `internal/auth` 6.439s, `internal/config` 1.178s, `internal/db` 6.445s, `internal/devstore` 0.353s, `internal/events` 0.566s, `internal/handlers` 9.211s, `internal/middleware` 9.592s) · `go test ./internal/auth/... ./internal/audit/... ./cmd/opencircuit/... -count=1 -v` — 55 top-level `--- PASS`, 0 FAIL, 0 SKIP, including `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW`, `TestCeremonySucceedsWhenRPIDDiffersFromOriginHost`, `TestWriteTx_RollsBackWithTransaction`, `TestMountAndServe_RateLimitsAuthLoginStart` · `npm run check` 171 files / 0 errors / 0 warnings · `npm test` 4 files / 61 tests passed.

**Suffix assertion re-mutated independently in a throwaway worktree at `0cce767`.** Both rows of the original bounce's mutation table now fail on the suffix assertion (`relying_party_test.go:116`): `opencircuitsf.com` / `https://www.completely-different.test` → `--- FAIL`, and `totally-unrelated.invalid` / `https://nonsense.example.test` → `--- FAIL`. With the hardcoded literal-equality assertions temporarily disabled, the assertion still behaves structurally rather than riding on the `opencircuitsf.com` literal: `example.com` / `https://www.example.com` → `--- PASS`; `totally-unrelated.invalid` / `https://nonsense.example.test` → `--- FAIL`; and both near-misses fail correctly — `example.com` / `https://wwwexample.com` (no dot) → `--- FAIL`, `example.com` / `https://notexample.com` → `--- FAIL`.

**Rate-limit wiring test re-mutated.** `TestMountAndServe_RateLimitsAuthLoginStart` calls the real unexported `mountAndServe` (same signature and the same `nil` `outerMiddleware` production passes) over a live listener, not a hand-built mux. Removing the limiter from `GET /auth/login/start` in `main.go` makes it fail with `request 11: status = 200, want 429`; wiring the wrong limiter (`registerLimiter`, burst 3) makes it fail with `request 4: got 429 before exhausting the burst of 10`. Both mutations were reverted.

**Naming, manual procedure, and scope all check out.** `TestCeremonySucceedsWhenRPIDDiffersFromOriginHost` now names exactly what it proves and its doc comment states plainly what it does *not* prove. The manual procedure exports `DATABASE_URL` before `migrate up`, adds the one-time superuser `psql -U postgres -f scripts/db/create.sql` step (matching `docs/database.md`), exports exactly the eight variables `internal/config` requires, and states explicitly that `localhost` puts RP ID and origin on the same host so the browser run does not cover apex/www. `0cce767` touches three `_test.go` files and no production code; nothing strays into `#0068`'s territory.

**One follow-up the reviewer noted but did not bounce on.** The narrowed "audit rows are written inside the ceremony transaction" criterion is honestly scoped and accurate as written for `FinishRegistration`/`FinishLogin` — those genuinely have no post-audit-write failure point a test can force without new production hooks. But an existing seam does cover one auth action: `LoginService.commit` (`internal/auth/login.go:75`, a function field that exists solely for tests) is already used by `TestLogoutAll_NoMailWhenCommitFails` (`internal/auth/logout_all_test.go:320`) to fail `LogoutAll` at commit *after* its `auditor.WriteTx` at `login.go:358`. That test asserts the session delete rolled back but not that the `session.revoked_all` audit row is absent. Adding that one assertion would prove audit rollback on a real in-flight auth action with zero production-code change — a cheap strengthening for whoever next touches `internal/auth`, not a defect in this pass.

## Root cause

Nothing in the auth port itself was broken: `internal/auth`, `internal/middleware`, and `internal/handlers` already had a large, passing test suite covering registration, login, recovery, credential rename/revoke, session cookie attributes, `RequireSession`/`RequireAdmin`, the rate limiter, and ceremony-transaction audit writes. The real gap was that the suite's proof of *which relying party* it was exercising was hollow. `#0066` pointed the ceremony tests at this project's RP values, but `testRPID`/`testRPOrigin` (`internal/auth/registration_test.go`) feed **both** the server's `webauthn.Config` (via `newService`/`newLoginService`) **and** the `virtualwebauthn.RelyingParty` the fake authenticator signs with. Both sides are built from the same two Go constants, so they are self-consistent by construction and never cross-checked against an external oracle — the suite would stay green if those constants were set to any other single (wrong-but-consistent) domain pair, and it would also stay green under a same-shaped apex/www swap (RP_ID = `www.opencircuitsf.com`, RP_ORIGIN = `https://opencircuitsf.com`), since nothing in the ceremony itself distinguishes "correct" from "internally consistent." A green `internal/auth` suite was therefore not evidence the relying party matched `opencircuitsf.com` / `https://www.opencircuitsf.com`.

## Fix

**First pass (`0e154d5`).** Added `internal/auth/relying_party_test.go` with four tests anchored to their own literal constants (`prodRPID = "opencircuitsf.com"`, `prodRPOrigin = "https://www.opencircuitsf.com"`), never reusing `testRPID`/`testRPOrigin`:

- `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW` — builds a `config.Config` from the literals, calls `NewWebAuthn`, and asserts the constructed `*webauthn.WebAuthn`'s `Config.RPID` and `Config.RPOrigins` equal the apex and the `www` origin exactly (read off the constructed instance, not off the test's own constants), plus an explicit assertion that `RPID != RPOrigins[0]`.
- `TestFinishRegistration_RejectsOriginOutsideConfiguredScope` and `TestFinishLogin_RejectsOriginOutsideConfiguredScope` — drive real ceremonies (start → verify → finish) with a `virtualwebauthn.RelyingParty` whose `Origin` is `https://evil.example` (RP ID unchanged, isolating the check to the origin), asserting `FinishRegistration`/`FinishLogin` return an error and, for registration, that no `passkey_credentials` row lands.
- `TestCredentialRegisteredUnderApexRPID_ValidForWWWOrigin` — originally described (inaccurately, per the review bounce) as "the direct pin for the apex/www relationship". Registers and then logs in with the same credential end-to-end using RP ID = the bare apex and origin = the `www` host.

**Second pass (this commit) — the review-bounce fix.** Two changes to `internal/auth/relying_party_test.go`, no other files:

1. **Added the suffix assertion** into `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW`, exactly as specified in `## Review notes`: parses `wa.Config.RPOrigins[0]`, takes `u.Hostname()`, and requires it to equal `wa.Config.RPID` or have `"." + wa.Config.RPID` as a suffix. This is the actual pin for "one passkey covers apex and www" (CLAUDE.md §7) — the property neither go-webauthn nor virtualwebauthn enforces, and the one thing the file was missing. Confirmed in `## Verification` to fail under both unrelated RP ID/origin pairs from the review's mutation table and to pass for a valid (including a non-hardcoded) apex/www pair.
2. **Renamed `TestCredentialRegisteredUnderApexRPID_ValidForWWWOrigin` → `TestCeremonySucceedsWhenRPIDDiffersFromOriginHost`** and rewrote its doc comment to state plainly what it proves (a ceremony succeeds end-to-end when RP ID and origin host are not identical strings) and what it does not (it is not, by itself, an apex/www pin — it would pass unchanged for any unrelated pair). The old name and the old `## Fix` wording ("the direct pin for the apex/www relationship") overstated the test; that's exactly what the bounce was about.

Also this pass, in packages outside `internal/auth` (see `## Files changed`): added a real route-wiring test for the rate limiter and a rollback test for `audit.Logger.WriteTx`, addressing the two overstated criteria the review flagged — see the note under `## Acceptance criteria` below for why tests were added rather than the wording narrowed.

No production code changed in either pass — every finding was in test coverage, exactly as the issue's Notes anticipated ("little or no auth code changes").

## Verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
go build ./...                                                                          # clean
go vet ./...                                                                             # clean
go test ./... -count=1                                                                  # all ok, real elapsed time on every DB-backed package
go test ./internal/auth/... ./internal/middleware/... ./internal/handlers/... -count=1 -v
cd web && npm run check && npm test
```

Results:

- `go build ./...` — clean.
- `go vet ./...` — clean.
- `go test ./... -count=1` — every package `ok`, zero skips: `cmd/opencircuit` 0.513s, `internal/audit` 1.720s, **`internal/auth` 1.658s**, `internal/config` 0.264s, `internal/db` 2.642s, `internal/devstore` 0.879s, `internal/events` 2.350s, **`internal/handlers` 2.639s**, **`internal/middleware` 2.804s** — all real elapsed time, none `0.00s`/`[no test files]`.
- `go test ./internal/auth/... ./internal/middleware/... ./internal/handlers/... -count=1 -v` — every relevant test `--- PASS`, `--- FAIL` × 0, `--- SKIP` × 0 (`internal/auth` alone: 56 top-level `--- PASS`, 0 FAIL, 0 SKIP). Tests confirmed by name in `=== RUN` / `--- PASS`:
  - `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW` — PASS (0.00s), now including the suffix assertion
  - `TestFinishRegistration_RejectsOriginOutsideConfiguredScope` — PASS
  - `TestFinishLogin_RejectsOriginOutsideConfiguredScope` — PASS, logs the expected go-webauthn origin-mismatch warning (`Error validating origin ... Expected Values: [https://www.opencircuitsf.com], Received: https://evil.example`)
  - `TestCeremonySucceedsWhenRPIDDiffersFromOriginHost` — PASS (renamed this pass from `TestCredentialRegisteredUnderApexRPID_ValidForWWWOrigin`)
  - `TestWriteTx_RollsBackWithTransaction` (`internal/audit`, new this pass) — PASS
  - `TestMountAndServe_RateLimitsAuthLoginStart` (`cmd/opencircuit`, new this pass) — PASS
  - Pre-existing, still green and directly relevant to the acceptance criteria: `TestRecoveryEndToEnd_AddsCredentialToExistingUser`, `TestCredentialsRevoke_LastRefused` (409 `cannot_revoke_last_credential`), `TestRegisterFinish_Success`/`TestLoginFinish_Success`/`TestRecoverFinish_Success` (cookie `HttpOnly`/`Secure`/`SameSite=Strict` assertions), `TestRequireSession_*`/`TestRequireAdmin_*` (401/403), `TestRateLimiterBurstThenReject` and siblings, `TestAudit_RegistrationAndLoginSeams`/`TestAudit_SettingsUpdated` (audit rows visible after a successful ceremony).
- `npm run check` (web/) — `svelte-check`: 171 files, 0 errors, 0 warnings (unchanged; no frontend files touched).
- `npm test` (web/) — vitest: 4 files, 61 tests, all passed (unchanged).

**First-pass transposition proof (`0e154d5`, kept for the record)**: twice temporarily edited the `prodRPID`/`prodRPOrigin` literals, re-ran the four-test set, then reverted:

1. **Scheme-swapped transposition** (`prodRPID = "https://www.opencircuitsf.com"`, `prodRPOrigin = "opencircuitsf.com"`): all four tests `--- FAIL` — `NewWebAuthn` itself rejects the config (`field 'RPID' is not a valid domain string: the scheme component must be empty`).
2. **Same-shape apex/www transposition** (`prodRPID = "www.opencircuitsf.com"`, `prodRPOrigin = "https://opencircuitsf.com"`): only `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW` `--- FAIL`ed; the ceremony-level tests stayed green, which is precisely the gap this pass's suffix assertion closes.

**Second-pass suffix-assertion proof (this commit) — the review's required mutation table, re-run independently:**

| `prodRPID` | `prodRPOrigin` | `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW` result |
|---|---|---|
| `opencircuitsf.com` | `https://www.completely-different.test` | **`--- FAIL`** — `relying_party_test.go:116: origin host "www.completely-different.test" is not the RP ID "opencircuitsf.com" nor a subdomain of it; ...` |
| `totally-unrelated.invalid` | `https://nonsense.example.test` | **`--- FAIL`** — `relying_party_test.go:116: origin host "nonsense.example.test" is not the RP ID "totally-unrelated.invalid" nor a subdomain of it; ...` |

Both rows are exactly the two the review's mutation table showed passing under the old (pre-fix) test; both now fail. To confirm the new suffix assertion is doing the work independently of the pre-existing literal-value checks (`RPID != "opencircuitsf.com"`, `RPOrigins[0] != "https://www.opencircuitsf.com"`), the literal checks were additionally, temporarily commented out and the suite re-run:

- With the literal checks disabled and `prodRPID = "totally-unrelated.invalid"` / `prodRPOrigin = "https://nonsense.example.test"`: `--- FAIL`, with only the suffix-assertion line firing.
- With the literal checks disabled and a genuinely different, non-hardcoded valid apex/www pair (`prodRPID = "example.com"`, `prodRPOrigin = "https://www.example.com"` — not `opencircuitsf.com` at all): `--- PASS`. This shows the suffix assertion is a structural check on the registrable-domain relationship, not a check that happens to piggyback on the hardcoded `opencircuitsf.com` literal.

The file was then restored to its committed state (diffed against a pre-mutation copy to confirm byte-identical) and the full auth suite re-run green before committing.

## Files changed

**First pass (`0e154d5`):**
- `internal/auth/relying_party_test.go` (new) — four tests pinning the relying party against literal apex/www constants, plus the `newServiceForRP`, `registerWithAuthenticatorRP`, `driveLoginRP`, and `countCredentialsByEmail` test helpers they use.

**Second pass (this commit):**
- `internal/auth/relying_party_test.go` (modified) — added the suffix assertion to `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW` (`net/url`, `strings` now imported); renamed `TestCredentialRegisteredUnderApexRPID_ValidForWWWOrigin` → `TestCeremonySucceedsWhenRPIDDiffersFromOriginHost` and rewrote both doc comments for accuracy. No other symbols changed.
- `internal/audit/audit_test.go` (modified) — added `TestWriteTx_RollsBackWithTransaction`, proving `Logger.WriteTx`'s row rolls back with its transaction and commits with it, addressing the "audit rows are written inside the ceremony transaction" criterion.
- `cmd/opencircuit/ratelimit_wiring_test.go` (new) — added `TestMountAndServe_RateLimitsAuthLoginStart`, which calls the real (unexported) `mountAndServe` over a live HTTP listener and drives `GET /auth/login/start` past `loginLimiter`'s burst of 10, addressing the "rate limiter still applies to the auth endpoints" criterion. Also adds the `doLoginStart`/`waitForHealthy` test helpers it uses.
- `issues/0008.md` (not committed with the code, per the workflow) — this pass's `## Fix`/`## Verification`/`## Gotchas`/`## Manual verification required`/`## Files changed` updates.

No production code changed in `cmd/opencircuit/main.go`, `internal/middleware/`, or `internal/audit/actions.go` — the new tests call existing, unexported/exported functions as-is.

## Gotchas

- **`ValidateRPID` accidentally catches one but not both transposition shapes.** A scheme-bearing RPID (e.g. accidentally assigning the origin string to `WEBAUTHN_RP_ID`) fails fast at `NewWebAuthn` with a clear error. A same-shaped swap (RP_ID = a bare `www.` host, RP_ORIGIN = the bare apex with a scheme) is syntactically valid on both sides and passes `NewWebAuthn` silently — only the literal-value assertion (and, as of this pass, the suffix assertion) in `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW` catches it. Don't rely on config validation alone to catch a real-world apex/www misconfiguration.
- **go-webauthn does not itself enforce "RPID must be a registrable-domain suffix of the origin" — but that does not mean a Go test can't.** (Corrected in this pass: the previous conclusion here was wrong.) That check is a browser/platform-authenticator responsibility before the ceremony ever reaches the server; go-webauthn only checks (a) the assertion's RP-ID hash equals `SHA256(configured RPID)` and (b) the assertion's origin is a member of the configured `RPOrigins` list. `virtualwebauthn` mirrors this — it will happily sign an attestation for any RP ID/origin combination given to it, valid or not per real browser rules. That is exactly *why* the ceremony-level tests alone (`TestCeremonySucceedsWhenRPIDDiffersFromOriginHost` et al.) cannot pin the suffix relationship — they pass for any unrelated RP ID/origin pair, not just a genuine apex/www pair. But the server can still assert the relationship directly against its own constructed config: `TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW` now parses `wa.Config.RPOrigins[0]`, takes `u.Hostname()`, and asserts it equals `RPID` or has `"." + RPID` as a suffix. That assertion fails under an unrelated RP ID/origin pair (confirmed in `## Verification`) and is a real, independent pin — it does not depend on go-webauthn or virtualwebauthn enforcing anything. What genuinely cannot be verified without a browser is the *client-side* half: that a real platform authenticator, given a real `navigator.credentials.get()/.create()` call on `www.opencircuitsf.com`, actually accepts a passkey scoped to RP ID `opencircuitsf.com` — that enforcement lives entirely in the browser/OS and is one more reason the two "verified in a browser" acceptance criteria genuinely need a human.

## Manual verification required

The two acceptance criteria that name "verified in a browser" cannot be driven from this environment — there is no way to trigger a real WebAuthn platform authenticator prompt (Touch ID / Windows Hello / a security key) from a Go test or a headless agent. They are left **unticked** above and require a human with a real browser and a real authenticator. Run this against the **Postgres** path, not `STORAGE=json` dev mode — dev mode bypasses the ceremony entirely via `internal/devstore.DevRegistrar`/`DevRecoverer` and `middleware.DevAutoLogin`, so it proves nothing about the real flow.

**Production mailer note:** `servePostgres` in `cmd/opencircuit/main.go` wires `auth.NoOpMailer{BaseURL: cfg.BaseURL}` (see `internal/auth/mailer.go`). It does **not** silently drop mail — every call `log.Printf`s the full magic-link URL to the server's stdout, e.g. `NoOpMailer: verification email to alice@example.com: http://localhost:8080/register/verify?token=...`. Read the link from the terminal running `opencircuit serve`.

Setup:

```bash
# 1. As a Postgres superuser, once: create the opencircuit role/database.
#    (Not the opencircuit_test one — this is the manual-run database.)
psql -U postgres -f scripts/db/create.sql

# 2. Start the server against Postgres, RP values matched to localhost, mailer to stdout.
export STORAGE=              # unset/empty — NOT "json" — so servePostgres runs, not dev mode
export DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit?sslmode=disable'
export BASE_URL='http://localhost:8080'
export WEBAUTHN_RP_ID='localhost'
export WEBAUTHN_RP_ORIGIN='http://localhost:8080'
export SESSION_SECRET="$(openssl rand -hex 32)"
export ADMIN_EMAIL='you@example.com'          # use the email you'll register with
export AWS_REGION='us-west-2'
export EMAIL_FROM='Open Circuit SF <hello@opencircuitsf.com>'

# 3. Now that $DATABASE_URL is exported, migrate.
migrate -path migrations -database "$DATABASE_URL" up

cd web && npm run build && cd ..
go run ./cmd/opencircuit serve
```

**This procedure does not exercise the apex/www relationship.** `WEBAUTHN_RP_ID=localhost`
and `WEBAUTHN_RP_ORIGIN=http://localhost:8080` put the RP ID and the origin on
the *same host* (`localhost`) — there is no apex/www split here at all. A
green run of this procedure proves the ceremony machinery works end to end in
a real browser (registration, login, conditional-UI autofill), which is
genuinely valuable and is the reason the two unticked criteria below require
it. It proves **nothing** about the production RP configuration
(`WEBAUTHN_RP_ID=opencircuitsf.com` / `WEBAUTHN_RP_ORIGIN=https://www.opencircuitsf.com`,
CLAUDE.md §7). Do not read a successful manual run as covering that
relationship. The apex/www suffix relationship is pinned by
`TestNewWebAuthn_ConfiguredRPPinsProductionApexAndWWW`'s suffix assertion (see
`## Fix`) and, ultimately, is only observable end-to-end against the real
`www.opencircuitsf.com` deployment.

**Registration (email → magic link → `navigator.credentials.create()` → account + session):**

1. Open `http://localhost:8080` in a real browser (Chrome/Safari with a platform authenticator, or a browser with a security key attached) and go to the registration/sign-up view.
2. Submit the email matching `ADMIN_EMAIL` above.
3. In the terminal running `opencircuit serve`, find the line `NoOpMailer: verification email to <email>: http://localhost:8080/register/verify?token=<token>` and copy that URL.
4. Paste the URL into the browser. The SPA should load `/register/verify`, call `GET /auth/register/verify?token=...`, and trigger the browser's passkey creation prompt (`navigator.credentials.create()`).
5. Complete the platform/security-key prompt. Expect: redirect into the authenticated account view, a `sessions` row created, and (first user) `is_admin = true`.
6. Confirm in Postgres: `SELECT email, is_admin FROM users;` and `SELECT COUNT(*) FROM passkey_credentials;` — both should reflect the new account and one credential.

**Login — explicit sign-in button:**

1. Log out (or open a fresh browser profile), go to the login view, click the explicit "Sign in" button (not autofill), and complete the passkey prompt.
2. Expect: a new `sessions` row, redirect into the account view.

**Login — conditional-UI autofill path:**

1. On the login view, click into the email/username `<input>` field **without** clicking any sign-in button. Chrome/Safari should show the OS-level passkey autofill suggestion (the browser's native picker) if the field is wired with `autocomplete="webauthn"` and the page called `navigator.credentials.get()` with `mediation: "conditional"`.
2. Select the passkey from the autofill suggestion and complete the prompt.
3. Expect: same result as the explicit path — a session is issued without ever touching a submit button. This is the one path that has **no non-browser proxy at all**: the mediation behavior is entirely a browser/OS UI feature.

Record the outcome (pass/fail, browser + OS + authenticator used) directly on this issue before the reviewer sets `resolved`, since both criteria are gating and neither is covered by the automated suite.

## Acceptance criteria

- [ ] Registration: email → magic link → `navigator.credentials.create()` → account + session, verified in a browser — **awaits manual browser verification, see `## Manual verification required`**
- [ ] Login: both the conditional-UI autofill path and the explicit sign-in button issue a session — the explicit-button ceremony is automated and passing; **conditional-UI autofill awaits manual browser verification**, see `## Manual verification required`
- [x] Recovery: magic link adds a new credential to an existing account without creating a duplicate user
- [x] Credential rename and revoke work, and revoking the last credential is refused with `409 cannot_revoke_last_credential`
- [x] Session cookie is `HttpOnly; Secure; SameSite=Strict` with the 30-day sliding window intact
- [x] `RequireSession` and `RequireAdmin` reject correctly (401 / 403)
- [x] Rate limiter still applies to the auth endpoints — proven by `TestMountAndServe_RateLimitsAuthLoginStart` (`cmd/opencircuit/ratelimit_wiring_test.go`), which drives `GET /auth/login/start` through the real `mountAndServe` route table over a live HTTP listener and gets `429` on the 11th request from one IP within `loginLimiter`'s burst of 10. Previously this was supported only by reading the route wiring in `main.go`; now it is also exercised end-to-end.
- [x] Audit rows are written inside the ceremony transaction for every auth action — supported by (a) code inspection: every ceremony seam (`registration.go`, `login.go`, `recovery.go`, `users.go`) calls `auditor.WriteTx(ctx, tx, entry)` with the same `tx` it later `Commit`s or (via `defer tx.Rollback(ctx)`) rolls back on any earlier error; (b) `TestAudit_RegistrationAndLoginSeams`/`TestAudit_SettingsUpdated`, which prove the rows exist after a *successful* ceremony; and (c) the new `TestWriteTx_RollsBackWithTransaction` (`internal/audit/audit_test.go`), which proves the underlying `WriteTx` primitive genuinely rolls back with its transaction and commits with it. This pass did not add a test that fails a real ceremony *after* its audit write specifically and checks the row is gone — no natural, non-flaky failure point exists there without adding fault-injection hooks to `registration.go`/`login.go` (the remaining steps after the audit write — `DeletePendingRegistration`, `CreateSession`, `Commit` — have no deterministic way to fail from a test without corrupting shared state or predicting a randomly-generated session token), and that's production-code surface out of scope for this test-only pass. (b)+(c) together are the practical substitute: (b) proves committed ceremony rows exist, (c) proves the shared write primitive they use is genuinely transactional. **Update (`#0069`, 2026-08-18):** narrower than stated even for (b)+(c) — neither proved an audit row is actually absent after a *real* auth action's transaction rolls back; (c) only exercised `WriteTx` in isolation. `#0069` closed that gap for the one auth action with a genuine test-forceable failure point: `TestLogoutAll_NoMailWhenCommitFails` (`internal/auth/logout_all_test.go`) now also asserts zero `session.revoked_all` rows survive `LogoutAll`'s forced commit failure, using the pre-existing `commit` test seam — a production-code-free addition, mutation-proven to bite (moving the `WriteTx` call outside the transaction made the new assertion fail, as expected). `FinishRegistration`/`FinishLogin` remain exactly as narrowed above — still no forceable post-audit-write failure point without new fault-injection hooks, which `#0069` explicitly did not add.
- [x] `go test ./internal/auth/... ./internal/middleware/... ./internal/handlers/...` passes and the test names are recorded in `## Verification`

## Notes

PRD §3.1. This is a verification issue, not a rewrite — the expectation is that little or no auth code changes. If something is broken, it is almost certainly the module path rewrite or a route registration lost in #0002.

`WEBAUTHN_RP_ID` is the apex `opencircuitsf.com` so a passkey stays valid across apex and `www`. **`WEBAUTHN_RP_ORIGIN` is `https://www.opencircuitsf.com`** — www is the canonical host as of the 2026-08-18 deploy, and the origin must match the host the browser is actually on. Locally both are `localhost`. A mismatch between `WEBAUTHN_RP_ORIGIN` and the browser's actual origin fails the ceremony with an opaque error — check this first when a ceremony fails.

The `descope/virtualwebauthn` dependency drives the ceremony tests without a browser; keep it.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §3.1 — Copy unchanged (rename module path + branding strings only) · 19 lines
  `sed -n '/^### 3\.1 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0001](0001.md) — Scaffold the repository from the ShortLinks skeleton
- Depends on: [#0004](0004.md) — Renumber and apply the auth migrations
- Depends on: [#0005](0005.md) — Adapt the dev script and in-memory dev store
- Depends on: [#0007](0007.md) — Extend the config loader for the new environment variables
- Blocks: [#0009](0009.md) — Port the admin users, settings, and audit views

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-08-18 | implement | claude-sonnet-5 | 160,506 | — |
| 2026-08-18 | review (bounced) | claude-opus-5 | 87,776 | — |
| 2026-08-18 | implement (2nd pass) | claude-sonnet-5 | 165,810 | — |
| 2026-08-18 | review | claude-opus-5 | 97,496 | — |

One bounce: the first pass's apex/www test was tautological. Harness reports
aggregate subagent totals only, so cost is left uncomputed rather than estimated.
