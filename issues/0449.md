# 0449 — `staticRoutesBlockRe` matches the doc comment first, and only luck makes that harmless

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | handlers / guards |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |

## Description

`TestRouteTableParity_StaticRoutes` (`internal/handlers`, from `#0071`) reads
`web/src/lib/router.ts` as text and extracts its route table with a regex whose
literal anchor is the declaration's own spelling.

Since `#0440`, **`router.ts` contains that exact text twice** — once in the new
doc comment and once in the declaration — and the regex matches the comment
first. Measured with `python3` over raw bytes by `#0440`'s reviewer.

This is `CLAUDE.md` §8's recorded "a regex that hunts for a tag finds the one
inside a comment" shape, live in this repo for the first time since `#0064` hit
it in `web/index.html`.

## Why it is not currently a defect, and why that is not reassuring

The match still yields the right fifteen entries, for a reason nobody chose:
the pattern's `[^=]*` cannot cross an `=`, so the capture falls forward to the
declaration's own `= {`. `#0440`'s reviewer probed four variants in a throwaway
worktree:

| Probe | Result |
|---|---|
| control | PASS |
| `=` added to the doc comment | PASS — falls forward to the real declaration |
| `= {` added to the doc comment | **FAIL loudly** — a parse error, not a silent partial parse |
| regex re-anchored to `(?sm)^const` | **FAIL loudly** |

So the guard **cannot be silently disabled** by this — `#0071`'s per-line
strictness turns any wrong path into a loud failure. But nothing pins the
property that makes it safe, and the anchored variant would reject the file
outright. The `export` keyword `#0440` added survived for the same unpinned
reason: the pattern happens to match a substring rather than a line.

**A guard that works by luck is a guard nobody can safely edit.** The next
person to tighten that regex, or to write another sentence in that doc comment,
has no way to know which change is fatal.

## Acceptance criteria

1. Assert `staticRoutesBlockRe` matches **exactly once** in `router.ts`. That
   single assertion names the doc-comment occurrence and removes the luck.
2. Fail with a message that says what to do — the fix is to reword the comment
   or anchor the pattern, and the reader will not infer that from a count.
3. **One assertion, no redesign.** Do not re-anchor the regex and add the count
   check; do not rewrite `#0071`'s parser. `CLAUDE.md` §8 records `#0258`
   spending six passes stacking mechanisms, each addition buying a bounce.
   `#0440`'s reviewer reported this as a follow-up precisely because folding it
   in would have been new mechanism inside a passing issue.
4. Prove it by mutation: add a second occurrence to a **copy** of `router.ts`,
   confirm the assertion fails naming it, restore, and prove the restore by
   hash. Never `git stash` or `git checkout --` (§8a).
5. Decide whether the same one-match pin belongs on any sibling text-scraping
   guard, and record the answer. Do **not** build them speculatively — name them
   and stop.

## Relation

- `#0071` built the parity guard.
- `#0440` added the doc comment and the `export`, and its review measured this.
- `#0064` is the precedent: a CSP hash computed twice from a span that began at
  a `<script>`-shaped string inside an HTML comment.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/handlers/... ./internal/db/...` and `scripts/check.sh web`. Scan bytes with `python3`, never BSD `grep -P`.

## Work log

- 2026-09-05 — **Implemented (phase 2, Sonnet).** Added
  `TestStaticRoutesBlockAnchorIsUnambiguous` to
  `internal/handlers/routes_parity_test.go`, plus a small
  `staticRoutesBlockAnchorText` constant (`"const STATIC_ROUTES:"`) it
  measures against. It is the one assertion criterion 1/3 call for:

  ```go
  func TestStaticRoutesBlockAnchorIsUnambiguous(t *testing.T) {
      src := readRouterTS(t)
      n := strings.Count(src, staticRoutesBlockAnchorText)
      if n != 1 {
          t.Fatalf(
              "%s contains %d occurrence(s) of %q, but staticRoutesBlockRe anchors "+
                  "on the first one and relies on being the only match -- "+
                  "reword whichever occurrence is not the STATIC_ROUTES declaration "+
                  "itself (commonly a doc comment quoting the anchor text, as #0449 "+
                  "found), or re-anchor staticRoutesBlockRe so no other text in the "+
                  "file can match its start",
              routerTSPath, n, staticRoutesBlockAnchorText,
          )
      }
  }
  ```

  **Why counting the literal anchor text, not `FindAllString` on
  `staticRoutesBlockRe` itself (criterion 1's literal wording).** Measured
  before writing anything: `staticRoutesBlockRe.FindAllString` on the current
  (pre-fix) `router.ts` returns exactly **one** match today, comment-and-all,
  because the pattern's `[^=]*` falls forward from the *first* occurrence of
  the anchor text all the way to the real `= {` and consumes both — there is
  only one possible closing `\n};` for it to stop at, so no edit that leaves
  the parse succeeding can ever produce a second, separate non-overlapping
  match for `FindAll` to count. A `len(FindAllString(...)) != 1` assertion
  would therefore pass on the exact file this issue is about, silently
  proving nothing. `staticRoutesBlockAnchorText` isolates the one part of the
  pattern where the ambiguity is actually visible — its literal starting
  anchor — and counts occurrences of *that* directly, independent of how far
  the rest of the pattern falls forward. Confirmed empirically with `python3`
  (`re.findall` over the tracked file, `(?s)const STATIC_ROUTES:[^=]*=\s*\{(.*?)\n\};`
  → 1 match starting at the comment's offset; `data.count(b"const STATIC_ROUTES:")`
  → 2) before writing the Go test, not assumed.

  **Criterion 2** — the failure message above names the count, quotes the
  offending text, and states both remedies (reword the comment, or re-anchor
  the pattern) so a reader is not left to infer either from a bare number.

  **Criterion 3 — one assertion, no redesign.** Exactly one new test function
  and one small unexported constant it reads; `staticRoutesBlockRe` itself,
  `staticRoutesEntryRe`, and `parseStaticRoutesFromRouterTS` are untouched
  (`git diff` confirms — the only hunks in
  `internal/handlers/routes_parity_test.go` are the new constant and the new
  test, both additive). No second parsing mechanism, no count check bolted
  onto the existing regex.

  **The fix also required touching `web/src/lib/router.ts`** — noted per the
  issue prompt's instruction to say why. The new assertion is a *pin*, not a
  standalone repair: as written, it fails against the file exactly as `#0440`
  left it (two occurrences — the doc comment's quoted `"const STATIC_ROUTES:"`
  and the real declaration). Reworking the assertion instead of the comment
  was not an option that makes both criterion 1 and a green suite true at
  once, so the doc comment was reworded to *name the shape without writing
  it* — the same move `CLAUDE.md` §8 already records for citation guards
  (`#0384`, `#0347`): it no longer spells `const STATIC_ROUTES:` verbatim,
  describing instead that `staticRoutesBlockRe` "anchors on this
  declaration's own `const` keyword, name, and colon -- not spelled out
  verbatim here on purpose, see #0449". `staticRoutesBlockRe`,
  `staticRoutesEntryRe`, and `parseStaticRoutesFromRouterTS` were not
  touched; `STATIC_ROUTES`'s entries and `router.test.ts` were not touched.

  **Criterion 4 — mutation proof, in a throwaway worktree (`git worktree add
  -d <scratchpad>/wt-0449 HEAD`), never on the shared tracked tree** (`docs/deployment.md`
  and `placeholder/README.md` were dirty from other agents at the time; only
  `router.ts` and `routes_parity_test.go` were mine). Copied both edited files
  into the worktree (the worktree's checkout predates this session's commits),
  confirmed the copied `router.ts` byte-identical to the tracked one by hash
  first, then:

  | Step | Result |
  |---|---|
  | Control (worktree copy, unmutated) | `TestStaticRoutesBlockAnchorIsUnambiguous` PASS, `TestRouteTableParity_StaticRoutes` PASS |
  | Mutated the worktree copy of `router.ts`: added a parenthetical `(e.g. \`const STATIC_ROUTES:\`)` to the reworded doc comment, reintroducing a second occurrence | `TestRouteTableParity_StaticRoutes` still PASSES (unaffected — it's the fall-forward behavior this issue is about); `TestStaticRoutesBlockAnchorIsUnambiguous` **FAILS**, its `t.Fatalf` message reading `"...router.ts contains 2 occurrence(s) of \"const STATIC_ROUTES:\", ..."` — naming the count and both remedies (test source location omitted here on purpose, per `CLAUDE.md`'s Go-file-plus-line-number rule) |
  | Restored the worktree's `router.ts` from the tracked copy | `shasum -a 256` byte-identical to the tracked file (`a745f02e3b991206fcebcf5e16bbefad4173019f3c4b6088831a38f7f10dda0`); both tests PASS again |

  Worktree removed (`git worktree remove --force`) after use; the tracked
  `web/src/lib/router.ts` was never mutated — confirmed by re-running
  `data.count(b"const STATIC_ROUTES:")` on it (`1`) and `git diff` (only the
  intended reword hunk) immediately after.

  **Criterion 5 — sibling text-scraping guards, named and not touched.**
  Surveyed every `internal/*` test file that both `regexp.MustCompile`s a
  pattern and `os.ReadFile`s an external source file. Two share
  `staticRoutesBlockRe`'s exact hazard shape — a fixed literal anchor
  spelling a declaration's own syntax, extracting a block from a TypeScript
  file that carries its own doc comments:

  - `internal/handlers/campaign_progress_parity_test.go`'s
    `campaignProgressInterfaceBlockRe` (anchor `"export interface
    CampaignProgress {"`, reading `web/src/lib/campaignProgress.ts`) — its own
    doc comment says it anchors "the way `staticRoutesBlockRe` anchors on
    `STATIC_ROUTES`'s type annotation," i.e. it already knows it's this
    guard's sibling.
  - `internal/handlers/complained_copy_parity_test.go`'s `parseTSStringConst`,
    whose per-call `declRe` is built as `` `export const ` + name + `...` ``
    against `web/src/lib/subscribe.ts`, called for
    `COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD`, `_TAIL`, and
    `COMPLAINED_CONTACT_EMAIL`.

  Checked both against the live files with `python3` (`.count()` over raw
  bytes, not `grep -P`): `campaignProgress.ts` contains
  `"export interface CampaignProgress {"` exactly **once**; `subscribe.ts`'s
  doc comments reference the three constant names but never reproduce
  `"export const <NAME>"` as a contiguous substring (they write bare names
  like "see `COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD`"), so each of the three
  anchors also occurs exactly **once**. **Decision: the same one-match pin
  does not belong on either today** — criterion 5 says name them and stop,
  not build speculatively, and neither is currently ambiguous the way
  `router.ts` was. Worth remembering if either target file grows a doc
  comment that quotes its own anchor text verbatim, the same way `#0440` did
  here; not filing a new issue for a guard that is not currently broken.

  Did not touch `docs/deployment.md`, `PRD.md`, `web/src/views/admin/`, or
  `issues/0417.md`, `issues/0427.md`, `issues/0448.md` (other agents' files,
  confirmed dirty-but-untouched via `git diff` scoped to only my two paths).

  `uptime` before the web run: `7.71 9.29 10.12`; before the Go run:
  `7.37 9.16 10.07` — recorded, not reasoned from.
  `gofmt -w internal/handlers/routes_parity_test.go` run twice; hash
  (`d716c1755106f9c37d4171ec5eee319d2d96be76e6ab37579856264e985f389a`) stable
  across both runs and `gofmt -l` reported it clean both before and after.

  `scripts/check.sh web`: `npm run check` (291 files, 0 errors, 0 warnings)
  and `npm test` (41 files, 4016 tests) both green.
  `ISSUE=0449 scripts/check.sh go ./internal/handlers/... ./internal/db/...`:
  `go build`, `go vet`, `gofmt -l` clean; `go test` reported
  `ok  github.com/brennanMKE/OpenCircuitSF/internal/handlers  8.092s` and
  `ok  github.com/brennanMKE/OpenCircuitSF/internal/db  8.322s` — real
  elapsed time, zero skips reported by the skip audit. Named test functions
  actually run and observed passing:
  `TestStaticRoutesBlockAnchorIsUnambiguous` and
  `TestRouteTableParity_StaticRoutes` (both, directly, `-run` against the
  worktree copy for the mutation proof and again in the full scoped run
  above). `check.sh` dropped `opencircuit_test_0449` on exit per its own
  design (the shared template is at migration 28 against this tree's 27, so
  the scoped run provisioned the scratch database directly from
  `migrations/` rather than cloning the template — expected, not an
  environment defect, per `CLAUDE.md` §5a and `#0315`'s precedent; did not
  run `scripts/testdb.sh template` or `scripts/db-reset.sh`).

  Did not file new issues (§9); `#0451` remains the highest allocated.
  Nothing further found worth filing beyond the criterion-5 note above.
  Status set to `in-progress`; not marked `resolved` (§9, reviewer's call).
