# 0417 — One workshop cover is 600×450 and carries iPhone EXIF, so it unfurls poorly and leaks capture metadata

| | |
|---|---|
| **Status** | open |
| **Platform** | Web |
| **Module** | web / assets |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-03 |
| **Commit** | — |

## Description

`#0416` committed the two live workshop cover images, which had existed only on
the server. Validating them surfaced two differences between them:

| File | Dimensions | Notes |
|---|---|---|
| `web/public/soldering.jpg` | **1200×630** | correct Open Graph card size, no EXIF payload |
| `web/public/programming_leds.jpg` | **600×450** | wrong aspect and half the width; carries Exif |

### The dimensions

1200×630 is the Open Graph card size, and it is what `soldering.jpg` uses.
`programming_leds.jpg` is 600×450 — a 4:3 photo rather than a 1.91:1 card.
Unfurlers letterbox or centre-crop it, and 600 px wide is below the width most
treat as qualifying for `summary_large_image`, which is the card type this site
declares. So the workshop using it gets a visibly worse preview than the one
next to it.

### The metadata

`programming_leds.jpg` carries an Exif block naming the capture device
(`Apple`, `iPhone 16`), the software version, and the capture timestamp
`2026:08:31 20:10:43`. It is served publicly at
`https://www.opencircuitsf.com/programming_leds.jpg`.

Nothing here is dangerous — no GPS tags were present in the header dump — but
device and timestamp metadata is conventionally stripped from published web
images, and this site is otherwise deliberate about not leaking incidental data
(§9 forbids third-party analytics and trackers for the same reason).

**Confirmed by the orchestrator** with `file(1)` against the committed bytes,
which are byte-identical to production by sha256.

## Scope changed 2026-09-04 — this is now server-side work

`#0416` has been reversed on the user's instruction: workshop photos are
deliberately **not** in source control, and `web/public/*.jpg` is gitignored
(`43f4ee4`).

So **neither file is in the repository any more.** Both findings below are still
real — `programming_leds.jpg` is 600×450 against its sibling's 1200×630, and it
carries iPhone Exif — but the fix is a **re-export and replace on the server**,
not a commit.

That changes the criteria's shape:

- There is nothing to commit, so nothing for a repo-side test to verify.
- The work is an operator action, and **`CLAUDE.md` §9 and the confirmation
  rules mean it needs the user's go-ahead** before anything is written to the
  box — this issue must not be implemented by an agent acting alone.
- Criterion 5's "byte-reproducible from the repository" is **void**. `#0416`
  records that the user has knowingly accepted that a clean rebuild does not
  reproduce these images.

**The open question the user has not yet answered** is whether a
higher-resolution original of `programming_leds.jpg` exists. A 600 px source
cannot reach 1200 px without upscaling (soft) or cropping (loses content), and
that is a content decision, not a technical one.

## Resolved by decision and by action, 2026-09-05

### Dimensions — the user's decision: leave at 600×450

> "The current resolution is fine. I can provide better images in the future.
> We do not need 1200px. It is normally seen on a mobile phone anyway."

So criterion 1 is **withdrawn**, not deferred. The reasoning is sound and worth
recording so nobody re-files it: link previews are predominantly rendered on
phones, where a 600 px source is adequate, and better originals may arrive
later. The asymmetry with `soldering.jpg`'s 1200×630 stands as a known,
accepted difference.

### EXIF — stripped

Done directly at the user's instruction ("Yes, strip the EXIF data").

The box has neither `exiftool` nor Pillow, so this was **lossless JPEG segment
surgery** in `python3` rather than a re-encode: drop the `APP1` (Exif/XMP) and
`APP13` (IPTC) segments, keep `APP0` (JFIF) and all image data untouched.

| | Before | After |
|---|---|---|
| Bytes | 66,301 | **63,253** |
| `Exif` marker | present | **absent** |
| `iPhone` string | present | **absent** |
| Dimensions | 600×450 | **600×450** |
| Valid SOI/EOI | yes | **yes** |

Removed: `APP1` 2,988 bytes, `APP13` 56 bytes. No GPS tags were present at any
point. The original is kept at `/tmp/programming_leds.jpg.bak` on the box.

Verified served: `200`, `Content-Length: 63253`, `Content-Type: image/jpeg`.

**One consequence worth knowing.** `/media/` carries `Cache-Control: max-age=604800`,
so a client that already fetched the file keeps the EXIF-bearing copy for up to
a week. `docs/media.md` records that renaming is how that cache is busted; that
was judged not worth doing for metadata removal, since nothing sensitive was
present — no GPS — and the file is small.

### Remaining scope

**None.** Criterion 1 is withdrawn by decision; criterion 2 is done; criteria 3
and 5 were already void per the `#0416` reversal. `soldering.jpg` was checked
and carries no `APP1`.

## Re-scoped 2026-09-05 — the premise moved twice and the user has decided

**Everything below the Description was written against a world that no longer
exists.** Three changes since it was filed, in order:

1. `#0416` — which committed the two covers to `web/public/` — was **reverted**.
   The user's instruction was explicit: *"We do not want those files in source
   control."* The images were placed on the server deliberately.
2. `#0432` removed the `.gitignore` rules that assumed otherwise, since nothing
   workshop-related belongs in `web/public/` at all.
3. The covers now live at **`/var/www/media`**, served by an Apache carve-out at
   `/media/` outside the Go binary (`docs/media.md`).
   `https://www.opencircuitsf.com/media/programming_leds.jpg` is live.

**The user has ruled on the dimensions** (2026-09-05): *"The current resolution
is fine. I can provide better images in the future. We do not need 1200px. It is
normally seen on a mobile phone anyway."* Criterion 1 is therefore withdrawn —
600×450 stands, and re-exporting to 1200×630 is not wanted.

**The EXIF strip has already been performed** on the live file, by the
orchestrator on the user's instruction, before this re-scoping. No `exiftool` or
Pillow exists on the box, so it was done as lossless JPEG segment surgery in
`python3`: `APP1` (2,988 bytes) and `APP13` (56 bytes) dropped, 66,301 → 63,253
bytes, dimensions unchanged at 600×450, **no GPS present** in the original. A
backup of the pre-strip file was left at `/tmp/programming_leds.jpg.bak` on the
box.

So what remains is **verification, not work**.

## Acceptance criteria (replaced)

1. Fetch the live image over HTTPS and confirm **no `APP1`/Exif and no `APP13`**
   segment remains. Do this by parsing JPEG segment markers in `python3` over
   the raw bytes — not with a text search, and never with BSD `grep -P`, which
   matches nothing here silently (`CLAUDE.md` §8).
2. Confirm the dimensions are unchanged at 600×450 and the image still decodes.
3. Check **every other file** the `/media/` carve-out serves the same way, not
   only this one. The original criterion's instinct was right even though its
   location was wrong.
4. Confirm the workshop's `cover_image` value still resolves to a live URL.
5. **Do not re-export, re-encode, or resize anything.** The user has ruled.
6. If a file *does* still carry Exif, report it — **do not fix it on the box**.
   A change to production needs the user's approval (§5b, §9), and the stripping
   procedure is recorded above.

**Withdrawn criteria, recorded so nobody restores them:** the old criterion 1
(1200×630) is overruled by the user; the old criterion 5 (byte-reproducible from
a clean checkout plus `npm run build`) is void, because the images are
deliberately not in the repository and `npm run build` never sees them.

**Not this issue's:** stripping location and iPhone model data from images
uploaded *in future* is `#0433`'s scope — the admin-console upload path. Do not
absorb it.

## Relation

- Found by: the orchestrator while validating `#0416`'s committed images
- Depends on: [#0416](0416.md) — which put these files under version control at
  all
- Relates to: [#0273](0273.md) — the generated per-workshop card, which is what a
  workshop gets when it has **no** cover; these two have covers, so they take the
  override path instead
- Relates to: [#0153](0153.md) — the no-upload decision that put cover images
  outside the product in the first place

## Work log

- 2026-09-03 — Filed by the orchestrator after `#0416` brought both files under
  version control. Dimensions and Exif read from the committed bytes.

- 2026-09-04 — **Orchestrator: scope changed.** `#0416`'s reversal removed both
  files from the repository, so this is now a server-side re-export requiring
  the user's approval rather than a repo change. Criterion 5 is void. The
  higher-resolution-original question is still open with the user.

- 2026-09-05 — **Orchestrator: both halves closed.** The user withdrew the
  dimensions requirement with reasoning (mobile previews, better originals
  later) and authorised the EXIF strip. Stripped losslessly by JPEG segment
  removal, since the box has no imaging tooling; verified byte counts, markers,
  dimensions and the served response. Left for a reviewer to confirm rather than
  self-resolving (§9).
