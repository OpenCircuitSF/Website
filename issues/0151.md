# 0151 — The Announce panel's targeting sentence contradicts the checkboxes an admin has ticked but not saved

| | |
|---|---|
| **Status** | resolved |
| **Module** | web |
| **Platform** | All |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |
| **Closed** | 2026-08-22 |
| **Commit** | `8c23db0` |

## Description

`#0143` made the Announce panel say plainly when a draft will target everyone rather than the workshop's interests. The sentence reads the **saved** `workshop.interest_ids`, which is correct: `POST /admin/workshops/{id}/announce` takes no body and derives its audience from the persisted row, so the copy describes what the server will actually do.

That leaves one confusing state. An admin who ticks interest checkboxes, does **not** save, and opens Announce reads *"This workshop has no interests set, so the draft will target everyone on the list"* while looking at checked boxes. The sentence is true about the action and contradictory about the form.

`#0143`'s reviewer verified the underlying behavior against the handler and ruled this a non-blocking follow-up rather than a defect: every acceptance criterion is met, the copy never lies, the form is explicit-Save by design, and staleness is bounded — `save()` reassigns `workshop`, so the sentence corrects itself the instant a save lands.

## Acceptance criteria

- [x] When `fields.interestIds` diverges from `workshop.interest_ids`, the panel adds a line to the effect of "Unsaved interest changes are not included — save first"
- [x] Both values are already in scope in `WorkshopEditor.svelte`; this should need no new API call
- [x] The comparison is order-insensitive — the two arrays may hold the same ids in different orders
- [x] The existing two copy paths from `#0143` are unchanged when there is no divergence
- [x] The new branch is unit-tested in `web/src/lib/`, per `CLAUDE.md`, and `scripts/check.sh web` passes

## Root cause

`#0143` correctly drives the Announce panel's targeting sentence from the
*saved* `workshop.interest_ids` — the only value
`POST /admin/workshops/{id}/announce` (no request body) can see. But nothing
told the admin when that saved value disagreed with the checkboxes currently
visible on screen (`fields.interestIds`, the local, unsaved buffer). An admin
who ticked boxes and did not save read a sentence that was true about the
action ("no interests set") while looking at a form that said otherwise.

## Fix

Added two pure functions to `web/src/lib/workshopAdmin.ts`, next to
`announceTargetingDescription`:

- `interestIdsDiverge(a, b)` — set-equality between two `number[] | undefined`
  arrays. Builds a `Set` from each side (`undefined` and `[]` both become the
  empty set), compares sizes, then checks membership — order never enters the
  comparison, which is what makes it safe against a naive `.join()` false
  positive.
- `announceUnsavedInterestsHint(formInterestIds, savedInterestIds)` — returns
  `' Unsaved interest changes are not included — save first.'` when
  `interestIdsDiverge` is true, `''` otherwise.

`WorkshopEditor.svelte`'s Announce panel now interpolates
`announceUnsavedInterestsHint(fields.interestIds, workshop.interest_ids)`
immediately after the existing `announceTargetingDescription(workshop.interest_ids)`
call. `announceTargetingDescription` itself is untouched — it still reads only
the saved value, so the sentence it produces never changes; the hint is a
separate, additive clause that appends nothing when the two arrays match.

## Verification

- `uptime` at start: `21:09  up 2 days, 7:19, load averages: 1.77 1.71 1.70` —
  far below CLAUDE.md §5's ~8 threshold; nothing here is load-contaminated.
- `ISSUE=0151 scripts/check.sh web` → `VERIFICATION PASSED`. `svelte-check`:
  **228 files, 0 errors, 0 warnings**. `vitest run`: **19 files, 477 tests,
  all passing** (up from 456 at `#0143`'s review; 13 of the 21 new tests here
  are `#0151`'s — `interestIdsDiverge` (8 cases) and
  `announceUnsavedInterestsHint` (5 cases) in `workshopAdmin.test.ts`).
- Targeted re-run: `npx vitest run src/lib/workshopAdmin.test.ts -t "0151"` →
  **13 passed**, including same-order-different-array (no divergence),
  identical-order (no divergence), both-empty and undefined-vs-empty (no
  divergence), an added id, a removed id, and a same-length swap (all
  divergence) — the order-insensitivity requirement is directly exercised,
  not just implied by the implementation.
- `git diff -- web/dist/index.html` is empty — no build was run, so the
  tracked placeholder is untouched.
- **Not rendered in a browser.** Relied on unit tests plus type-checking, the
  same standard `#0143`'s reviewer accepted for this same panel: the project
  has no DOM test environment (no jsdom/happy-dom/testing-library), and the
  interpolation sits inside the same `{:else if workshop}`-narrowed branch
  `#0143`'s sentence already lives in, so `workshop.interest_ids` and
  `fields.interestIds` are both non-null there per `svelte-check`'s 0 errors.
  A reviewer wanting stronger evidence should build and grep the bundle for
  the new hint string, the way `#0143`'s review did.

## Files changed

- `web/src/lib/workshopAdmin.ts` — added `interestIdsDiverge` and
  `announceUnsavedInterestsHint`.
- `web/src/lib/workshopAdmin.test.ts` — 13 new unit tests covering both
  functions, including explicit order-insensitivity and undefined/empty
  cases.
- `web/src/views/admin/WorkshopEditor.svelte` — imports
  `announceUnsavedInterestsHint` and interpolates it into the Announce
  panel's paragraph, right after the existing `announceTargetingDescription`
  call.

Commit: `8c23db0` — "#0151 Add a divergence hint when unsaved interest
checkboxes disagree with the saved workshop"

## Notes

Recommended in `#0143`'s phase-3 review notes rather than filed by it, following the precedent `#0056`'s reviewer set for non-blocking UI suggestions.

The alternative — sourcing the sentence from the unsaved buffer — is **wrong** and should not be attempted: it would reintroduce `#0143`'s original defect pointing the other way, promising interest targeting that Announce will not perform.

## Relation

- Found by: [#0143](0143.md) — The Announce panel's copy when a workshop has no interests
- Relates to: [#0056](0056.md) — Add the announce-to-list shortcut

## Review notes — 2026-08-22 (phase 3, PASSED)

Re-verified independently from the diff at `8c23db0`, not from the drafted
`## Verification`. `uptime` at the start `21:11 up 2 days, load averages: 2.53
2.11 1.86`, and `1.75 1.98 1.83` when `check.sh` ran — far below `CLAUDE.md`
§5's ~8 threshold, so nothing below is load-contaminated. Run in the main
checkout, not a worktree: the only dirt in the tree was `#0156`'s reviewer's
staged `issues/0156.md` (since committed at `5c709e7`/`6f7c518`), and nothing
under `web/` was in flight, so the working copy of every file this issue
touches was byte-identical to `8c23db0`. `issues/0151.md` is the only file
staged for this commit.

### What was re-verified, with observed output

- `ISSUE=0151 scripts/check.sh web` → `VERIFICATION PASSED`. `svelte-check`
  **228 files, 0 errors, 0 warnings**; `vitest run` **19 files, 477 tests, all
  passing** — 21 more than `#0143`'s review saw, 13 of them this issue's. Both
  commands ran; both counts are real. (`20d540c` / `#0140` is an ancestor, so
  this `check.sh` is the truthful one.)
- The 13 new tests re-run by name (`npx vitest run src/lib/workshopAdmin.test.ts
  -t "0151"`) → **13 passed, 45 skipped**.
- **Mutation spot-check — three mutations, all caught.** (1) Inverting the
  hint's ternary (return `''` on divergence, the warning on a match) fails
  **all 5** `announceUnsavedInterestsHint` tests. (2) Replacing the set
  comparison with an order-*sensitive* `.join(',')` comparison fails **2**
  tests — precisely the two order-insensitivity assertions, which is the
  criterion-3 guard doing its job. (3) Deleting the `as.size !== bs.size`
  early return fails **2** tests (the removal cases, which the membership loop
  alone cannot see, since a subset passes it). No assertion here is
  decorative. Each mutation was applied by anchored string replacement with an
  `assert` that the anchor matched, and the file restored from a scratch copy
  afterwards, never with `git checkout --` (§8a).
- Worth recording as a near-miss: a first attempt at mutation (1) used a
  `perl -0pi` substitution whose anchor silently did not match. The tests
  "passed", which looked like weak assertions and was in fact an unapplied
  mutation. A mutation probe that does not verify the mutation landed proves
  nothing — hence the `assert` in the rerun.

### The `#0143` copy paths are byte-unchanged

`announceTargetingDescription` does not appear in the diff at all — the whole
change to `workshopAdmin.ts` is 43 appended lines below it. Both sentences
were then exercised directly against the real module (node
`--experimental-strip-types` importing `workshopAdmin.ts`) and come back
exactly as `#0143` shipped them:

- no interests, nothing unsaved → `This workshop has no interests set, so the
  draft will target everyone on the list.` + `''`
- interests set, nothing unsaved → `It will be targeted at this workshop's
  interests.` + `''`

The `''` return is a true no-op on the rendered text, confirmed at the bundle
level rather than assumed: the compiled paragraph is
`…location.\n      ${desc}${hint} Nothing is sent — review and send the draft
from the Campaigns tab.` — the hint slots between the description and the
space that already preceded "Nothing", so with an empty hint the rendered
sentence is character-for-character `#0143`'s. With a divergence it reads
`…target everyone on the list. Unsaved interest changes are not included —
save first. Nothing is sent — …`: one space before, sentence-terminated, no
double space.

### The direction is still right — `#0143`'s decision is intact

The targeting sentence still reads the **saved** value. In the compiled
bundle the paragraph's two thunks are
`()=>hm(r(a).interest_ids)` and `()=>mm(r(d).interestIds,r(a).interest_ids)`
— `a` is the `workshop` rune, `d` is `fields`. The description takes only
`workshop.interest_ids`; the unsaved buffer reaches the hint and nothing else.
That is exactly the separation `#0143`'s review settled on, and the hint is
also the literal follow-up its "Recommended follow-up" section described,
wording included. Nothing is undone.

### Edge cases probed beyond the committed tests

Against the real module, not a re-implementation:

| Input | Result | Verdict |
|---|---|---|
| `[1,1,2]` vs `[1,2]` | no divergence | Correct. Interest ids are a *set*; a duplicate in one array is not a targeting difference, and warning here would be a false alarm an admin could not act on. The checkbox path cannot produce duplicates anyway — `toggleInterestId` is `includes`-guarded. |
| `[1,1]` vs `[1,2]` | divergence | Correct — genuinely different sets. |
| `undefined` vs `[]`, `undefined` vs `undefined` | no divergence | Correct; `?? []` collapses both to the empty set. |
| `[0]` vs `[]` / `undefined` | divergence | Correct, and the interesting one: a `.filter(Boolean)` or truthiness-based implementation would have dropped id `0` and *suppressed* a warning that should appear. `Set` membership does not. (Ids are `BIGSERIAL` from 1, so this is defensive, not live.) |
| `[-1,-2]` vs `[-2,-1]`, `[1.5]` vs `[1.5]` | no divergence | Order-insensitive for negatives and non-integers too; `[1.5]` vs `[1]` diverges. |
| `[NaN]` vs `[NaN]` | no divergence | `Set` uses SameValueZero, so `NaN` matches itself — the sane answer. `0` vs `-0` likewise. Neither is reachable from the API. |
| 50 000 ids, reversed | no divergence, 8 ms for both probes | O(n); no pathological case. |
| shared array reference (`workshopToFormFields` assigns `interestIds: w.interest_ids`) | no divergence after load; divergence after one toggle; `workshop.interest_ids` unmutated | The aliasing is safe **because** `toggleInterestId` returns a new array. Had it spliced in place, `fields.interestIds` and `workshop.interest_ids` would stay the same object and the hint could never fire. Checked, not assumed. |

No probed case produces a warning that would confuse an admin, and none
suppresses one that should appear.

### The hint fires only where it should

Traced every write to `fields` and `workshop` in `WorkshopEditor.svelte`:

- **First load** (`load()`): `workshop = w; fields = workshopToFormFields(w)`
  — `interestIds` is literally `w.interest_ids`. No hint. ✓
- **After a successful save** (`save()`): `workshop = await updateWorkshop(…);
  fields = workshopToFormFields(workshop)` — both resynced from the server
  response in the same tick. The hint clears the instant a save lands. ✓
- **Editing any other field**: every other write is
  `fields = { ...fields, <field> }`, which carries `interestIds` through
  untouched. No hint. ✓
- **Ticking a checkbox without saving**: hint appears. ✓
- **A status transition with unsaved interest edits**: `openTransition`'s
  handler reassigns `workshop = await updateWorkshop(workshopId, { status })`
  and deliberately does *not* resync `fields`. The hint therefore survives a
  publish/unpublish/cancel — which is right: the interest edits really are
  still unsaved.

### Evidence standard

Settled on the stronger one — unit tests plus `svelte-check` plus a bundle
grep, matching `#0143`'s review rather than the implementer's stated
tests-and-types-only standard. For a change whose whole payload is a string
reaching a specific position in one paragraph, "the pure function returns the
right string" leaves the interpolation itself unverified, and that is the half
that could have gone wrong. The build ran as `npx vite build --outDir
<scratchpad>` inside the main checkout, so the tracked `web/dist/index.html`
(`#0141`) was never written — `git status` is clean after it. The new string
appears in the emitted chunk, both `#0143` sentences still appear, and the
compiled concatenation is the one quoted above. Still not rendered in a
browser: the project has no DOM test environment, and `#0143`'s review already
accepted this standard for this same paragraph.

### Non-blocking observations (reported, not filed)

- The hint inherits `class="text-muted"` from the paragraph it joins, so a
  warning renders in the same de-emphasised grey as the surrounding
  explanatory copy. It is the sentence in that paragraph most likely to change
  an admin's next action. Giving it its own `<span>` with warning styling, or
  a `role="status"` line beneath, would suit it better. Purely cosmetic;
  `#0143`'s sentence has the same treatment.
- Criterion 1 says "adds a line"; the implementation appends a clause to the
  existing paragraph. Read as satisfied — the criterion says "to the effect
  of", and `#0143`'s review recommended exactly "append a clause such as …".

All five acceptance criteria are met and independently confirmed.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 48 | 4,267 | 1,470,115 | 52,112 | $0.71 |

**Total: $0.71**
