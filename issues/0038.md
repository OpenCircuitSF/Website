# 0038 — Ingest SES bounce and complaint events

| | |
|---|---|
| **Status** | resolved |
| **Module** | sesnotify |
| **Platform** | All |
| **Phase** | 4 — Unsubscribe and list hygiene |
| **First seen** | 2026-08-18 |
| **Commit** | 42d5104, d9e6a9c |
| **Closed** | 2026-08-20 |

> 🟢 Resolved 2026-08-20 — Second-pass review. The crash-window gap is closed:
> `TestSESNotifications_CrashWindowEquivalence_RetryLandsAllWrites` runs every
> write for one SNS message for real against Postgres inside a `pgx.Tx` whose
> only subverted method is `Commit`, then asserts all three effects — the
> `email_events` row, the `suppressions` row and the subscriber status change —
> are absent afterward, and that an identical retry lands all three. Re-verified
> in review by re-running the first review's own mutation
> (`AddTx`/`MarkBouncedTx`/`MarkComplainedTx` moved off the transaction onto the
> pool) against a private database: the new test is the **only** test in the SES
> group that turns red, on exactly the two assertions proving the suppression
> and the status change had survived independently of a rolled-back
> `email_events` insert. Correction 5's new `subscribers.Store.FindByEmailTx`
> follows the same unexported-`querier` idiom as `AddTx`/`setStatusTx` and
> changes no behaviour; corrections 3 and 4 are fixed. Six packages `ok`, 453
> `RUN` lines, **zero** skips; no SPA file is touched by either commit.

## Description

The SNS webhook that turns SES delivery events into suppressions and subscriber status changes.

## Review notes

> 🔴 Bounced 2026-08-20 — the single-transaction guarantee this issue exists
> to establish has **no test at all**, proven by mutation; and the `## Gotchas`
> "flake" is a reproducible defect, not a flake.

The code in `42d5104` is, as far as I can tell, **correct**. Every behavioural
property I checked holds: signature verification precedes all parsing,
storage and dispatch; `FetchSubscribeURL` reuses the same-package
`validateCertURL` with the same client, no-redirect policy and timeout;
`AddTx` is called unconditionally per `#0100`; `complained` stays terminal on
every path including the new transactional ones; classification is on
`bounceType`/`bounceSubType` only; migration `000014` is append-only with
`000012`/`000013` untouched and `idx_email_events_soft_bounce` present; and
the `ErrCertUnavailable`→500 / everything-else→403 mapping is a single
`errors.Is` with no special case, exactly as `#0107`'s carried-in note
requires. `go build` and `go vet` are clean.

What must be fixed is the **evidence**, not the behaviour. Two blockers and
three corrections.

### Blocker 1 — plan §12 test 9 is missing, and its absence is material

The plan's required case 9 ("crash-window equivalence: run the transaction,
roll it back, re-post → all three writes land on the retry") was not written,
and case 7's "**and assert no partial rows**" half was not implemented either
— `TestSESNotifications_StorageFailure_Returns500` injects a `failingBeginner`
so the transaction never starts, which is the one storage failure under which
partial rows are impossible by construction. Nothing anywhere exercises a
failure *between* the `email_events` insert and the suppression insert, which
is the exact window `#0033` shipped `AddTx` to close.

Mutation-tested in review, against a private database
(`CREATE DATABASE opencircuit_0038_rev`, migrated to `000014`, dropped
afterward — CLAUDE.md §8b), with `internal/handlers/ses_notifications.go`
backed up by `cp` first and restored from that copy:

```
h.suppr.AddTx(ctx, tx, …)          ->  h.suppr.Add(ctx, …)          // pool
h.subs.MarkBouncedTx(ctx, tx, …)   ->  h.subs.MarkBounced(ctx, …)   // pool
h.subs.MarkComplainedTx(ctx, tx,…) ->  h.subs.MarkComplained(ctx,…) // pool
```

That mutation **fully dismantles the transaction** — the suppression row and
the subscriber status change now commit independently of the `email_events`
row, reopening precisely the crash window described in this file's `## Fix`,
in `ses_notifications.go`'s package doc comment, and in the commit message.
Result:

```
ok  internal/handlers    1.379s
ok  internal/sesnotify   0.491s
ok  cmd/opencircuit      1.461s
```

Green. Twenty-five handler tests, two wiring tests, seven store tests, and not
one of them notices that the issue's headline guarantee is gone. That is the
definition of an untested property, and on this particular property the
failure mode is silent and permanent: the retry is deduped away by
`email_events`' own `UNIQUE` constraint, so a hard-bounced address keeps
receiving mail with nothing to show why.

`TestStore_InsertTx_RollsBackWithTransaction` does not cover this — it proves
`pgx` rolls back, not that the handler's three writes share one `tx`.

**What the next pass must add:** a handler-level test that forces a failure
*after* at least one `email_events` row is inserted and *before* the commit,
and asserts that the `email_events` row, the `suppressions` row and the
subscriber status change are **all** absent afterward — then that a re-post of
the identical signed envelope lands all three. A `sesTxBeginner` returning a
`pgx.Tx` wrapper whose `Commit` fails, or one whose `QueryRow` fails on the
Nth call, both work; the point is that the mutation above must turn that test
red.

For the avoidance of doubt: **do not change the handler.** The transaction is
already written correctly. This is a test-only gap.

### Blocker 2 — the `## Gotchas` "flake" is a real, reproducible defect

`TestSuppressionStore_List_JoinsSubscriberStatus` is recorded here as an
unreproduced flake citing CLAUDE.md §5. §5 does not apply: the entry itself
records load average 1.4–3.8, far under the ~8 threshold, and §5's own point
is that a failure on an **idle** machine is not the machine.

It reproduces. My independent re-verification run failed on it at load average
2.98, and 5 of 11 whole-package runs of `internal/subscribers` failed —
identically against a private database, which rules out shared-database
interference and the `-p 2` isolation hypothesis (every DB-backed package
already serialises through `testdb.Lock`'s advisory lock, so no two of them
ever run concurrently).

Root cause: `uniqueSuppressionEmail` is not unique. macOS's wall clock advances
in 1 µs steps, and the two adjacent calls at
`internal/subscribers/suppressions_test.go:387-388` land in the same tick
**62 %** of the time — measured in situ inside that package's own test binary.
When they collide, `Add(orphan, manual)` conflicts with `Add(withSub, manual)`
on `(email, reason)` and no-ops, and the classifying `switch` routes every row
to `case withSub`, so `withSubRows` is still 2 and `orphanRows` is 0 —
`orphan rows = 0, want 1`, exactly as reported. It passes 100 % of the time
under `-run TestSuppressionStore_List_JoinsSubscriberStatus`, because running
it alone leaves the process cold and the first `fmt.Sprintf`/`t.Helper()` pair
costs more than a tick — which is why re-running "just that test" looked like
confirmation of a flake.

This is **pre-existing and not this issue's fault**: `42d5104` does not touch
`suppressions_test.go` and adds no tests to `internal/subscribers`. It is
filed as **[#0108](0108.md)**. What this issue must fix is its own record:
replace the `## Gotchas` bullet with a pointer to `#0108`, and drop the
"`internal/handlers`'" mislabel (the test lives in `internal/subscribers`).

Until `#0108` lands, the six-package verification run is red about half the
time; say so in `## Verification` rather than reporting a green run.

### Correction 3 — `## Verification` overstates coverage

It claims the handler tests "cover plan §12 tests 1–15 by name". Test 9 is not
covered (blocker 1) and test 7 is covered only in part. Restate it accurately.

### Correction 4 — a stale doc comment makes a false claim

`internal/handlers/ses_notifications.go:61` ends the "held concretely, not
behind narrow interfaces" argument with *"subscriberLookup below stays a real
interface because FindByEmail takes no querier"*. There is no
`subscriberLookup` in the file — `subs` is held as a concrete
`*subscribers.Store`. Either introduce the interface the comment describes or
delete the sentence. The rest of that comment is a careful, correct argument
and it is worth not undermining it.

### Correction 5 — advisory, not blocking: a pool-acquisition hazard

`applyRecipient` calls `h.subs.FindByEmail`, which runs on the **pool**, while
the handler is holding a `tx` checked out of that same pool — so each request
needs a second connection per recipient while already holding one. This
endpoint deliberately carries no rate limiter (correctly — see the call-site
comment in `mountAndServe`) and a campaign produces bursts of thousands of
events, so under a burst every pooled connection can be held by an open
transaction with every request waiting for a second one, and nothing recovers
until contexts cancel. The same pattern already exists in
`internal/auth/registration.go`, so this is not a regression, and there is no
performance requirement here (CLAUDE.md §5) — but a `FindByEmailTx` over the
existing `querier` seam would remove the hazard *and* make the read see the
transaction's own writes, which `#0039` will want anyway. Your call whether to
take it now or leave it for `#0039`.

### Verification I ran

`uptime` 1.41–2.98 throughout — idle, so nothing below is attributable to load.

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
psql -c 'select * from schema_migrations'   # version 14, dirty = f
go build ./...   # clean
go vet ./...     # clean
go test ./internal/sesnotify/... ./internal/subscribers/... ./internal/handlers/...         ./internal/db/... ./internal/audit/... ./cmd/opencircuit/... -p 2 -count=1
```

Five of six packages `ok` with real elapsed time (`sesnotify` 0.49s,
`handlers` 0.97s, `db` 0.88s, `audit` 0.29s, `cmd/opencircuit` 1.34s);
`internal/subscribers` **FAIL** on
`TestSuppressionStore_List_JoinsSubscriberStatus` (blocker 2). A `-v` pass
confirms **zero skips** across all six. No SPA file is touched by `42d5104`,
so `npm run check` / `npm test` were correctly not run.

Mutation checks I ran myself, both against the private
`opencircuit_0038_rev` database, both restored by `cp` from copies taken
first (never `git checkout`/`restore`/`stash`), with `git diff 42d5104` empty
for every code file afterward:

1. **Transaction atomicity** (blocker 1) — suite stays green. **Not tested.**
2. **`complained` terminality** — replaced `setStatusTx`'s guarded
   `CASE WHEN status = $4` with an unconditional write.
   `TestSESNotifications_ComplainedIsNotErasedByLaterBounce`,
   `TestSESNotifications_RepeatComplaint_DoesNotBumpUpdatedAt` and
   `TestMarkBounced_ComplainedStaysComplained` all FAIL. **Well tested** —
   the guard is load-bearing on both the pool path and the new Tx path.

The private database was dropped; `pgrep -f 'go test|\.test '` is empty.

### Also done in this review pass

- Filed **[#0108](0108.md)** for the `uniqueSuppressionEmail` defect.
- Amended **[#0039](0039.md)** with a `## Carried in from #0038's
  implementation` section recording the seam — the `BounceTypeTransient` arm,
  the in-transaction `CountRecentTransientBounces` read, the
  `AddTx(ReasonRepeatedSoftBounce)` + `MarkBouncedTx` pair, and that
  `idx_email_events_soft_bounce` is keyed on `received_at` (not the nullable
  `event_at`), so `#0039` needs no migration. `#0039` carried none of this
  before; it was only in this file's `## Fix`.

### Second review (2026-08-20) — approved

The bounce asked for evidence, not a behaviour change, and that is what
`d9e6a9c` delivers. Re-verified independently:

- **The mutation is now caught.** Re-ran the first review's own mutation —
  `h.suppr.AddTx(ctx, tx, …)` → `Add(ctx, …)`,
  `h.subs.MarkBouncedTx(ctx, tx, …)` → `MarkBounced(ctx, …)`,
  `h.subs.MarkComplainedTx(ctx, tx, …)` → `MarkComplained(ctx, …)` — against a
  private database (`createdb opencircuit_0038_rev2`, migrated to `000014`,
  dropped afterward; CLAUDE.md §8b), with `ses_notifications.go` copied aside
  by `cp` first and restored from that copy (never `git checkout`/`restore`/
  `stash`; md5 of the restored file matched the backup and `git diff d9e6a9c`
  is empty for every code file). Unmutated the new test passes on that
  database; mutated it **FAILS**, and it is the **only** failure in the whole
  `-run TestSESNotifications` group:

  ```
  --- FAIL: TestSESNotifications_CrashWindowEquivalence_RetryLandsAllWrites
      IsSuppressed = true after a rolled-back commit, want false …
      subscriber status after rolled-back commit = "bounced", want unchanged "active"
  ```

  Exactly where the first review's mutation left the suite green.

- **`commitFailingTx` is honest.** It embeds a real `pgx.Tx` from the real
  pool and overrides only `Commit`; every statement runs against Postgres.
  The mutation run proves this rather than assuming it — under the mutation
  the suppression and status writes (moved to the pool) *survived*, which is
  only possible if the writes genuinely executed; and unmutated the retry half
  lands all three rows, which is only possible if the fake was not swallowing
  statements. The absences therefore come from the real `Rollback` the
  handler's own `defer` performs.

- **All three absences and a real retry.** The test asserts zero
  `email_events` rows (`ByMessageID`), `IsSuppressed == false`, and
  `Status == StatusActive` after the commit failure; then, after re-posting
  the identical signed envelope, exactly one `email_events` row,
  `IsSuppressed == true`, and `Status == StatusBounced`. `commitFailingBeginner`
  fails only the first `Begin`, so the retry commits for real. This also
  supplies plan §12 test 7's missing "assert no partial rows" half.

- **Correction 5 reviewed as production code.** `FindByEmailTx(ctx, q querier,
  email)` is the same `…Tx`-over-unexported-`querier` split this package
  already uses for `AddTx`/`setStatusTx`/`MarkBouncedTx`, with `FindByEmail`
  and it sharing one `findByEmail` implementation, so the pool and
  transaction paths cannot drift. Reading through `tx` is safe here: the
  handler never branches on `sub.Status` (only `sub.ID` and
  `sub.ConfirmedAt`, neither of which this transaction writes), so the only
  self-read reachable at all is a duplicate address inside one envelope's
  recipient list, where re-reading a status this transaction just set changes
  nothing. `pgx` returns `ErrNoRows` without aborting the transaction, and
  that path is covered live by
  `TestSESNotifications_NoSubscriberRow_StillRecordsAndSuppresses`, which
  still records and suppresses after a not-found lookup inside the same `tx`.
  Both branches of `FindByEmailTx` are exercised by the handler suite; there
  is no dedicated store-level test, and none is possible for the
  tx-vs-pool routing itself, which is deliberately behaviour-neutral.

- **Nothing regressed from the first pass.** `d9e6a9c` touches three files
  (`ses_notifications.go` — doc comment plus the one `FindByEmailTx` call
  site; `ses_notifications_test.go` — additions only; `subscribers/store.go` —
  the `findByEmail` factoring). Re-confirmed after it: verification precedes
  all inner-payload parsing and dispatch; `FetchSubscribeURL` still reuses
  `validateCertURL`; `NewVerifierForTesting` still has no non-test caller;
  classification is still on `bounceType`/`bounceSubType`; `AddTx` is still
  unconditional per `#0100`; migration `000014` is untouched and append-only;
  the `ErrCertUnavailable`→500 / everything-else→403 mapping is still one
  `errors.Is` with no special case; and `complained` stays terminal on every
  path, including through the new in-transaction lookup.

- **Corrections 3 and 4 are fixed.** No `subscriberLookup` remains anywhere in
  the tree, and the doc comment now describes the `FindByEmailTx` call that
  actually exists. The coverage claim is restated accurately (`## Fix`'s range
  corrected to 1–8 and 10–15 in this commit, 16–17 being the wiring tests).

- **`#0039`'s carried-in seam** was re-read against the code after `d9e6a9c`
  and nothing in it is falsified — the `BounceTypeTransient` arm, the
  in-transaction read requirement, the index note and the staleness-guard note
  all still hold. Amended anyway in this commit, minimally: the provenance now
  names both commits, and the transaction bullet points at
  `subscribers.Store.FindByEmailTx` as the existing in-transaction-read
  precedent for `CountRecentTransientBounces` to copy.

### Verification I ran

`uptime` 1.44–1.74 throughout — idle, so nothing here is attributable to load.

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
go build ./...   # clean
go vet ./...     # clean
go test ./internal/sesnotify/... ./internal/subscribers/... ./internal/handlers/... \
        ./internal/db/... ./internal/audit/... ./cmd/opencircuit/... -p 2 -count=1
```

All six packages `ok` with real elapsed time (`sesnotify` 0.40s,
`subscribers` 0.58s, `handlers` 1.19s, `db` 1.10s, `audit` 0.38s,
`cmd/opencircuit` 1.44s). A `-v` pass over the same six: **453 `RUN`, 0
`SKIP`, 0 `FAIL`**, with
`TestSESNotifications_CrashWindowEquivalence_RetryLandsAllWrites` present and
passing, and its `commit failed (crash window …)` ERROR line in the output
proving the commit-failing path actually ran. `internal/subscribers` is green
now that `#0108` (`8305a0f`) has landed. No SPA file is touched by `42d5104`
or `d9e6a9c`, so `npm run check` / `npm test` were correctly not run.

The private database was dropped (`dropdb opencircuit_0038_rev2`) and
`pgrep -f 'go test|\.test '` is empty.

## Root cause

Not a bug fix — this issue builds the last piece PRD §6.5's state machine needs:
turning a verified SNS/SES delivery event into `email_events` evidence,
`suppressions` rows, and subscriber status transitions. `#0037` built the
trust boundary (signature + `TopicArn`); `#0033`/`#0100` built the
suppression store with the transaction seam and the reason-scoped key this
issue depends on. Nothing existed yet to connect them.

## Fix

Followed the `## Implementation plan` and all four `## Carried in from …`
sections as written; the notable decisions and one deliberate scope
addition are below.

- **`migrations/000014_create_email_events.{up,down}.sql`** — new table,
  confirmed as the next free number against `migrations/` on disk (000012 =
  `#0033`, 000013 = `#0100`). `UNIQUE (sns_message_id, recipient)` is the
  dedupe key (§2 of the plan): SNS's at-least-once delivery repeats
  `sns_message_id`, and a single `Bounce`/`Complaint` carries an array of
  recipients, so the key has to be composite. `ses_message_id` is a plain
  indexed column (SES's id for the outbound email — `#0049`'s future
  reconciliation key), `recipient` is `NOT NULL DEFAULT ''` with a
  `lower(trim(...))` CHECK matching `subscribers`/`suppressions`, and
  `event_type` is deliberately unconstrained (SES adds event types over
  time). The soft-bounce partial index `#0039` needs is added here so that
  issue is a pure read.
- **`internal/sesnotify/event.go`** — `SESEvent` (parses `eventType`,
  falling back to `notificationType`), `Recipients()` (Bounce →
  `bouncedRecipients`, Complaint → `complainedRecipients`, Delivery →
  `recipients`, else `mail.destination`, else a single `""` so the event is
  still recorded), and `ParseEventTimestamp` (RFC3339Nano then RFC3339,
  `ok=false` on anything unparseable rather than erroring).
- **`internal/sesnotify/store.go`** — `Store.InsertTx(ctx, q querier, …)`
  over the same unexported `querier` idiom `audit`/`suppressions` already
  establish; `ON CONFLICT (sns_message_id, recipient) DO NOTHING` plus an
  `inserted bool` return is how the handler recognizes a pure redelivery.
  `ByMessageID` is a pool-backed read for tests (no production caller yet,
  as the plan anticipated).
- **`internal/handlers/ses_notifications.go`** — `SESNotificationsHandler`.
  One `pgx.Tx` per SNS message (`sesTxBeginner`, satisfied by
  `*pgxpool.Pool`) spans every recipient's `email_events` insert, then —
  only when at least one row was newly inserted (i.e., not a pure
  redelivery) — the §4 state mapping per recipient
  (`applyRecipient`/`applyBounce`/`applyComplaint`), then commit. Status
  codes follow §5/§6 exactly: malformed body or unrecognized `Type` → 200;
  `errors.Is(err, sesnotify.ErrCertUnavailable)` → 500; every other `Verify`
  failure → 403; a transaction failure on an otherwise-trustworthy message →
  500 (the one place "always 200" is wrong). `SubscriptionConfirmation` is
  auto-confirmed only after `Verify` passes (signature + `TopicArn`) and
  after `SubscribeURL` clears the same host allowlist as `SigningCertURL`;
  `UnsubscribeConfirmation` never fetches anything, only logs at ERROR and
  audits. The raw inner payload is always recorded as valid JSONB —
  `rawPayloadJSON` wraps a non-JSON `Message` string as `{"raw": "..."}`
  rather than dropping it, so PRD §6.7 item 3 ("record before interpreting")
  holds even for a payload this handler can't parse.
- **`internal/subscribers/store.go`** — `setStatus` renamed `setStatusTx`
  and now takes `q querier` instead of the pool; `MarkBounced`/
  `MarkComplained` become thin pool-backed wrappers, and `MarkBouncedTx`/
  `MarkComplainedTx` are the new transaction-scoped twins this handler
  calls. The `statusLockedFromNonAdmin` guard lives in the one shared
  implementation, so a Tx-taking caller can't bypass it — matching how
  `AddTx`/`Add` are already split in this same package.
- **`internal/sesnotify/verify.go`** — two additions, no change to
  reviewed code. `FetchSubscribeURL` (exported): validates a
  `SubscriptionConfirmation`'s `SubscribeURL` with the same unexported
  `validateCertURL` `SigningCertURL` uses, then fetches via the new
  unexported `fetchSubscribeURLReal` — this satisfies the carried-in
  criterion "reuse `validateCertURL`… no new export is needed" for the
  *validation* logic while still giving `internal/handlers` an exported
  entry point, since that package cannot reach an unexported method.
  `NewVerifierForTesting` (exported): builds a `*Verifier` with the
  certificate fetcher overridden, mirroring this package's own
  `newTestVerifier` test helper — added because `#0038`'s handler tests
  need to exercise the *real* `Verify` (signature, canonical string,
  `TopicArn` pin) from a different package, and the fetcher field is
  otherwise unexported.
- **Suppression is reason-scoped and unconditional (#0100's correction).**
  `applyBounce`/`applyComplaint` call `suppr.AddTx` every time their branch
  is reached — never gated on "is this address already suppressed". Proven
  by a dedicated test and mutation (see Verification) rather than assumed
  from reading the code.
- **`docs/database.md`, `CLAUDE.md`** — new `000014` table row and
  `000001`–`000014` range (`TestDatabaseDocMigrationParity`).
  **`docs/email-setup.md`** — correction 2's runbook: the
  `aws sesv2 put-account-suppression-attributes` invocation, what it
  does/doesn't cover, and a note that `internal/sesnotify`/
  `internal/handlers` are no longer "not yet built". **`CLAUDE.md` §10** —
  new open item 7 recording the account-level suppression list as not yet
  enabled (gated on item 2, the SES account not existing yet).

**Deviations from the plan, both already anticipated by it:**

- Corrections 1–4 (§9 of the plan) are followed as written: criterion 6 is
  satisfied read-side (no `email_sends` write — that table is `#0040`,
  Phase 5), criterion 8 is satisfied by the `docs/` runbook rather than
  code, and this issue cannot be verified end-to-end against real SNS
  (`SES_EVENTS_TOPIC_ARN` is unset in every environment that exists today).
- `FetchSubscribeURL`/`NewVerifierForTesting` are additions to
  `internal/sesnotify` beyond §11's Files table, which listed only this
  issue's own new files for that package. Both are small, additive, and
  necessary for the reasons stated above; no existing `verify.go` behavior
  was changed.

**Not built here, and left as the seam #0039 needs (its own boundary,
§8 of the plan):** `applyBounce`'s `BounceTypeTransient` case does no
counting — it records the row (already inserted above) and returns `nil`,
with a comment naming `#0039` as the owner of the
`CountRecentTransientBounces` call and the `ReasonRepeatedSoftBounce` +
`MarkBouncedTx` pair that belongs inside that same `case`, inside the same
transaction. The partial index `#0039`'s query needs
(`idx_email_events_soft_bounce`) already exists (migration `000014`), so
`#0039` is a pure read with no new migration.

### Second pass (bounce fix), commit `d9e6a9c`

The bounce judged `42d5104`'s behavior correct and asked for evidence, not a
behavior change. Followed that exactly — the transaction was not touched.

- **Blocker 1 — plan §12 test 9.** Added
  `TestSESNotifications_CrashWindowEquivalence_RetryLandsAllWrites` in
  `internal/handlers/ses_notifications_test.go`. The seam the review named
  already existed and needed no handler change: a `sesTxBeginner` (the
  interface `Notify` already depends on) can wrap a *real* `pgx.Tx` — every
  write in it runs for real — and fail only `Commit`. `commitFailingTx`
  embeds `pgx.Tx` and overrides only `Commit`; `commitFailingBeginner` wraps
  the first `Begin` call this way and lets the retry's `Begin` return the
  real, unwrapped `Tx`. The test asserts all three writes (`email_events`,
  the suppression row, the subscriber status) are absent after the
  commit-failing post, then that an identical retry lands all three. This
  is a genuine crash-window test, not a rename of the existing
  `_StorageFailure_Returns500` case: that one injects a `failingBeginner`
  so `Begin` itself never succeeds, which is the one storage failure under
  which partial rows are impossible by construction — it cannot exercise a
  failure *between* the `email_events` insert and commit, which is exactly
  what this test does instead.

  Proved load-bearing by reproducing the review's own mutation — see
  `## Verification`.

- **Correction 4 — stale doc comment.** `ses_notifications.go`'s package
  doc comment named a `subscriberLookup` interface that has never existed
  in this file. Rather than introduce an unused interface just to make the
  sentence true, the sentence is rewritten to describe what the file
  actually does — including the new `FindByEmailTx` call below, which now
  also takes the same unexported `querier` for the same reason `AddTx`/
  `MarkBouncedTx`/`MarkComplainedTx` do.

- **Correction 5 — pool-acquisition hazard, taken now rather than left for
  `#0039`.** The review left this as a judgment call. Taken: `applyRecipient`
  was calling the pool-backed `h.subs.FindByEmail` while already holding
  `tx` checked out of that same pool, so every request in a burst held one
  connection open for its transaction while also contending the pool for a
  second one just for this lookup — on an endpoint deliberately built with
  no rate limiter, because a campaign produces thousands of events in a
  burst. Added `subscribers.Store.FindByEmailTx(ctx, q querier, email)` as
  `FindByEmail`'s transaction-scoped twin (same `querier`/`…Tx` split
  `AddTx`/`MarkBouncedTx` already use in this same file) and switched
  `applyRecipient` to call it with `tx`. This was small, mirrors an
  established pattern, and closes a real hazard already present in code
  this pass was touching — not deferred to `#0039`, which the review's own
  wording left as the fallback rather than the default. As a side effect,
  the lookup now also sees the transaction's own uncommitted writes, which
  `#0039`'s soft-bounce counting will want.

- **`## Gotchas`' flake bullet.** Replaced below with a pointer to
  `#0108` (now resolved, `8305a0f`) and the `internal/handlers` mislabel
  dropped — see that bullet.

- **Correction 3 — overstated coverage claim.** Restated below in
  `## Verification`: the handler suite covered plan §12 tests 1–8 and
  10–15 by name (16–17 are `cmd/opencircuit`'s wiring tests), and test 7
  only in part; test 9 was entirely missing until this pass.

Not touched this pass: `#0108`'s `uniqueSuppressionEmail` defect (already
filed and independently resolved) and `#0039`'s seam (still exactly as
`42d5104` left it — no counting, no new migration needed).

## Verification

`uptime` before starting: load average ~1.4–1.9 throughout (well under
CLAUDE.md §5's ~8 caution threshold; one incidental spike to 3.8 during a
run, still far under threshold — no result here is attributable to machine
load).

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
migrate -path migrations -database "$TEST_DATABASE_URL" up   # applied 000014 cleanly
go build ./...   # clean
go vet ./...     # clean
go test ./internal/sesnotify/... ./internal/subscribers/... ./internal/handlers/... \
        ./internal/db/... ./internal/audit/... ./cmd/opencircuit/... -p 2 -count=1
```

All six packages `ok` with real, non-zero elapsed time (`internal/sesnotify`
~0.5s, `internal/handlers` ~1.1s, `cmd/opencircuit` ~1.3–1.6s); a `-v` pass
confirmed **zero `SKIP` lines**, so no DB-backed suite silently opted out.

New tests, all named and confirmed passing in the verbose run:

- `internal/sesnotify/event_test.go` (15 tests, pure) —
  `TestSESEvent_Type_*`, `TestParseSESEvent_*` (both payload shapes, plus
  `_InvalidJSON`), `TestSESEvent_Recipients_*` (Bounce/Complaint/Delivery/
  mail.destination-fallback/single-empty-fallback/zero-value), `TestSESEvent_ComplaintFeedbackType_NotSpamConstant`,
  `TestParseEventTimestamp_*`.
- `internal/sesnotify/store_test.go` (7 tests, DB-backed) —
  `TestStore_InsertTx_InsertsRowAndReturnsInserted`,
  `_EmptyOptionalFieldsMapToNull`, `_DuplicateMessageAndRecipientIsNoOp`
  (redelivery), `_DifferentRecipientsSameMessageBothInsert` (composite
  key), `_RollsBackWithTransaction`/`_CommitsWithTransaction` (mirrors
  `internal/audit`'s `TestWriteTx_*` pair), `TestStore_ByMessageID_ReturnsNewestFirst`.
- `internal/sesnotify/verify_test.go` (4 new tests, no DB) —
  `TestFetchSubscribeURLReal_FetchesAndDiscardsBody`,
  `_NonOKStatusIsError`, `_RefusesRedirect` (all against a local
  `httptest` server, bypassing `validateCertURL` exactly like the existing
  `TestFetchCertReal_*` trio does — its host allowlist admits no local
  server), `TestVerifier_FetchSubscribeURL_HostileURLRejectedBeforeFetch`.
- `internal/handlers/ses_notifications_test.go` (26 tests, DB-backed,
  real `Verifier` via `NewVerifierForTesting` with only the certificate
  fetcher faked — signature/canonical-string/`TopicArn` logic all run for
  real) — as first written, this covered plan §12 tests 1–8 and 10–15 by
  name, and test 7 only in part (its "assert no partial rows" half was not
  implemented, and test 9 was missing entirely — see **Correction 3** and
  the **Second pass** section below, both filed by the review, and the
  crash-window mutation evidence at the end of this section, added in the
  second pass to close that gap)
  (`TestSESNotifications_PermanentBounce_SuppressesAndMarksBounced`,
  `_TransientBounce_RecordsOnlyNoSuppression`,
  `_Complaint_SuppressesAndMarksComplained`, `_ComplaintNotSpam_NoSuppression`,
  `_Delivery_RecordsOnlyNothingElseChanges`,
  `_MalformedOuterBody_Returns200NoRows`,
  `_UnparseableInnerMessage_Returns200RecordsRawPayload`,
  `_UnknownEventType_Returns200RecordsRowNoStateChange`,
  `_StorageFailure_Returns500`,
  `_CrashWindowEquivalence_RetryLandsAllWrites` (added second pass — plan
  §12 test 9),
  `_Redelivery_ExactlyOneRowAndOneSuppression`,
  `_MultiRecipientBounce_BothSuppressedAndBounced`,
  `_NoSubscriberRow_StillRecordsAndSuppresses`,
  `_StaleEventGuard_PredatesConfirmedAt`,
  `_ComplainedIsNotErasedByLaterBounce`,
  `_RepeatComplaint_DoesNotBumpUpdatedAt`, `_CaseWhitespaceNormalization`),
  plus `_SecondReasonForAlreadySuppressedAddressCoexists` (the #0100
  unconditional-`AddTx` property), `_BadSignature_Returns403ZeroRows`,
  `_TopicArnMismatch_Returns403ZeroRows`, `_CertUnavailable_Returns500`,
  the two `SubscriptionConfirmation_*` dispatch tests (a fake `sesVerifier`
  for determinism — see the test file's comment on why a real network
  fetch isn't attempted), `_HostileSubscribeURLNeverFetched`, and
  `_UnsubscribeConfirmation_NeverActsAndReturns200`.
- `cmd/opencircuit/ses_notifications_wiring_test.go` (2 tests, DB-backed,
  real `mountAndServe`) — plan §12 tests 16–17:
  `TestMountAndServe_SESNotifications_UnauthenticatedReachability` (a bad
  signature 403s and a genuinely valid signed envelope 200s, both with **no
  session cookie**, against the real `requireSession`/`requireAdmin`
  middleware — not no-op stand-ins, per `#0103`'s carried-in lesson in
  `unsubscribe_wiring_test.go`) and
  `TestMountAndServe_SESNotifications_TopicArnMismatch` (403, zero
  `email_events` rows).

**Mutation evidence** (CLAUDE.md's instruction given this session's history
of vacuous guard tests), all against `internal/handlers/ses_notifications.go`,
restored by `cp` from a backup taken first — never `git checkout`/`restore`/
`stash` — and confirmed byte-identical to the backup afterward:

1. Disabled the §3 staleness guard (`if false && hasSub && …`).
   `TestSESNotifications_StaleEventGuard_PredatesConfirmedAt` fails on both
   assertions (`IsSuppressed = true, want false`; `status = "bounced", want
   "active"`) — the guard is load-bearing, not vacuous.
2. Disabled the `not-spam` complaint exclusion (`if false && ev.ComplaintFeedbackType() == …`).
   `TestSESNotifications_ComplaintNotSpam_NoSuppression` fails identically
   (suppressed=true, status flips to `complained`) — confirms a feedback-loop
   "rescued from spam" report would otherwise unsubscribe the address.
3. Reintroduced the superseded "skip `AddTx` if already suppressed" check
   `#0100` explicitly forbids (`if already, _ := h.suppr.IsSuppressed(...); !already { … }`).
   The new `TestSESNotifications_SecondReasonForAlreadySuppressedAddressCoexists`
   test fails (`suppression row count = 1, want 2`) — proves this specific
   regression, which the carried-in plan section calls out by name, is
   actually caught by the suite rather than merely documented.

No leftover `go test`/`.test` processes after the run
(`pgrep -f 'go test|\.test '` empty). No scratch database was created —
every mutation above ran against the shared `opencircuit_test` database and
disabled a guard *inside this handler's own code*, not a store-level
authorization check reachable by a hardcoded id (CLAUDE.md §8b); every test
row is seeded under a unique `zz-0038-…`/`zz-subtest-…` prefix and cleaned
up via `t.Cleanup`.

No SPA change — `npm run check`/`npm test` were not run, as the plan's §12
anticipated.

**Not verified, per correction 3:** end-to-end delivery from a real AWS SNS
topic. `SES_EVENTS_TOPIC_ARN` is unset everywhere (the topic doesn't exist —
CLAUDE.md §10 item 2), so `Verify` correctly 403s every real delivery today;
that live-wiring verification is explicitly out of scope for this phase.

### Second pass verification (commit `d9e6a9c`)

`uptime` before starting: 1.73–2.98 throughout (well under CLAUDE.md §5's
~8 threshold; nothing below is attributable to machine load).

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
go build ./...   # clean
go vet ./...     # clean
go test ./internal/sesnotify/... ./internal/subscribers/... ./internal/handlers/... \
        ./internal/db/... ./internal/audit/... ./cmd/opencircuit/... -p 2 -count=1
```

All six packages `ok` with real, non-zero elapsed time (`sesnotify` 0.27s,
`subscribers` 0.42s, `handlers` 1.05s, `db` 0.99s, `audit` 0.22s,
`cmd/opencircuit` 1.25s). A `-v` pass over the same six packages confirmed
**zero `SKIP` lines** across 453 `RUN` lines. `#0108` is resolved
(`8305a0f`), so `TestSuppressionStore_List_JoinsSubscriberStatus` no longer
needs a caveat here — it ran clean.

**Crash-window mutation evidence (plan §12 test 9), against a private
database**, per CLAUDE.md §8b — this mutation dismantles the transaction
that is the whole point of `AddTx`/`MarkBouncedTx`/`MarkComplainedTx`, so
it ran against `opencircuit_0038_pass2` (`createdb` as the local superuser,
migrated to `000014`, dropped afterward), never the shared
`opencircuit_test`:

```
h.suppr.AddTx(ctx, tx, …)          ->  h.suppr.Add(ctx, …)          // pool
h.subs.MarkBouncedTx(ctx, tx, …)   ->  h.subs.MarkBounced(ctx, …)   // pool
h.subs.MarkComplainedTx(ctx, tx,…) ->  h.subs.MarkComplained(ctx,…) // pool
```

`internal/handlers/ses_notifications.go` was backed up with `cp` before the
edit (never `git checkout`/`restore`/`stash`). Result, run against the
private database with the mutation applied:

```
--- FAIL: TestSESNotifications_CrashWindowEquivalence_RetryLandsAllWrites (0.08s)
    ses_notifications_test.go:682: IsSuppressed = true after a rolled-back commit, want false — the suppression write must not have survived independently of email_events
    ses_notifications_test.go:689: subscriber status after rolled-back commit = "bounced", want unchanged "active"
FAIL
```

**Red**, exactly where the review's own mutation left the first pass's
suite green — the new test catches precisely the regression the review
demonstrated. Restored by `cp` from the pre-mutation backup; `md5` of the
restored file matched the backup, and `git diff` against the working tree
showed only this pass's intended changes (`FindByEmailTx`, the doc-comment
fix, and the new test) — none of `42d5104`'s original code. The private
database was dropped (`dropdb opencircuit_0038_pass2`) and
`pgrep -f 'go test|\.test '` was empty afterward.

The rest of `## Fix`'s second-pass section (correction 4's doc comment,
correction 5's `FindByEmailTx`) is covered by the ordinary green run above —
no separate mutation was needed for either since neither introduces a new
guard.

No SPA change — `npm run check`/`npm test` were not run.

## Files changed

- `migrations/000014_create_email_events.up.sql` / `.down.sql` — new
- `internal/sesnotify/event.go`, `event_test.go` — new: `SESEvent`,
  `Recipients()`, `ParseEventTimestamp`
- `internal/sesnotify/store.go`, `store_test.go` — new: `Store.InsertTx`,
  `ByMessageID`
- `internal/sesnotify/verify.go` — edit: `FetchSubscribeURL`,
  `fetchSubscribeURLReal`, `NewVerifierForTesting` (additions only; no
  change to `Verify`/`validateCertURL`/`getCert`/`fetchCertReal`)
- `internal/sesnotify/verify_test.go` — edit: `TestFetchSubscribeURLReal_*`,
  `TestVerifier_FetchSubscribeURL_HostileURLRejectedBeforeFetch`
- `internal/sesnotify/main_test.go` — new: shared `TestMain`/`testDBPool`
  for this package's first DB-backed tests
- `internal/handlers/ses_notifications.go`, `ses_notifications_test.go` —
  new: `SESNotificationsHandler`, `Notify`, message-type dispatch, the
  transaction, the §4 mapping
- `internal/subscribers/store.go` — edit: `setStatus` → `setStatusTx(ctx, q
  querier, …)`; new `MarkBouncedTx`/`MarkComplainedTx`
- `cmd/opencircuit/main.go` — edit: constructs `sesEventsStore`/
  `sesVerifier`/`sesNotifyH`, registers `POST /api/ses/notifications`
  unguarded with the §10-style call-site comment
- `cmd/opencircuit/ses_notifications_wiring_test.go` — new: unauthenticated
  reachability + `TopicArn` mismatch, against real `mountAndServe`
- `cmd/opencircuit/{admin,ratelimit,shutdown,shutdown_budget,subscribe,unsubscribe}_wiring_test.go`
  — edit: threaded the new `sesNotifyH` nil argument through each
  `mountAndServe` call site (signature change, no behavior change)
- `internal/audit/actions.go` — new `ActionSubscriberBounced`,
  `ActionSubscriberComplained`, `ActionSESSubscriptionConfirmed`,
  `ActionSESUnsubscribeConfirmation`, `TargetSNSTopic`
- `docs/database.md`, `CLAUDE.md` — `000014` table row, `000001`–`000014`
  range, new CLAUDE.md §10 open item 7
- `docs/email-setup.md` — SES account-level suppression runbook
  (correction 2); "not yet built" → accurate status for `internal/sesnotify`

**Second pass, commit `d9e6a9c`:**

- `internal/handlers/ses_notifications.go` — edit: doc comment correction
  (correction 4); `applyRecipient` calls `h.subs.FindByEmailTx(ctx, tx, …)`
  instead of the pool-backed `FindByEmail` (correction 5)
- `internal/handlers/ses_notifications_test.go` — new:
  `commitFailingTx`/`commitFailingBeginner`,
  `TestSESNotifications_CrashWindowEquivalence_RetryLandsAllWrites` (plan
  §12 test 9, blocker 1)
- `internal/subscribers/store.go` — edit: `FindByEmail` factored into
  `findByEmail(ctx, q querier, email)`; new `FindByEmailTx(ctx, q querier,
  email)` twin (correction 5)

## Gotchas

- **The RFC3339-truncation trap in test fixtures.** Early test runs failed
  the Permanent-bounce/Complaint tests because `time.Now().UTC().Format(time.RFC3339)`
  drops sub-second precision, and a `mail.timestamp` generated within the
  same wall-clock second as `seedTestSubscriber`'s `confirmed_at = now()`
  (which keeps microsecond precision from Postgres) can format to a value
  that compares *before* `confirmed_at` even though the two events were
  practically simultaneous — silently tripping the §3 staleness guard in
  every non-staleness test. Fixed by giving every non-staleness fixture's
  `mail.timestamp` a `+1h` offset; the dedicated staleness test already
  used an unambiguous `-24h` offset and was unaffected. Not a production
  concern (real bounce/complaint events arrive minutes to days after
  confirmation, never within the same second), but worth knowing for
  anyone writing more fixtures against this guard.
- **`sesnotify.Verifier.validateCertURL`'s host allowlist blocks every
  local-server test technique** for anything that calls it — including the
  new `FetchSubscribeURL`. There is no way to point a `SubscribeURL` (or a
  `SigningCertURL`) at an `httptest` server and have it pass validation;
  the only way to test the *fetch* mechanics is to bypass validation
  entirely and call the unexported real-fetch method directly (the
  existing `fetchCertReal` pattern, now mirrored by
  `fetchSubscribeURLReal`). A "valid host, successful fetch" test for
  `FetchSubscribeURL` specifically is therefore not attempted — it would
  require a real outbound call to AWS.
- **`TestSuppressionStore_List_JoinsSubscriberStatus`** (in
  `internal/subscribers`, owned by `#0100`, not touched by this issue) was
  recorded here in the first pass as an unreproduced flake, citing CLAUDE.md
  §5 and mislabeling the package as `internal/handlers`. It is neither: the
  review reproduced it on an idle machine (load average 1.4–3.8, `uptime`
  recorded at the time) and traced it to a real defect —
  `uniqueSuppressionEmail` in `suppressions_test.go` was not unique against
  macOS's 1 µs wall-clock tick, so two adjacent fixture calls could collide
  62% of the time. Filed as **[#0108](0108.md)**, since resolved
  (`8305a0f`). Nothing in `42d5104` or this pass touches
  `suppressions_test.go`.

## Acceptance criteria

- [ ] `POST /api/ses/notifications` accepts SNS payloads, signature-verified by #0037
- [ ] Raw payload inserted into `email_events` **before** interpretation
- [ ] `Bounce` with `bounceType == "Permanent"` → status `bounced` + suppression `hard_bounce`
- [ ] `Bounce` with `bounceType == "Transient"` counts toward the soft-bounce threshold (#0039)
- [ ] `Complaint` → status `complained` + suppression `complaint`; never auto-resubscribes
- [ ] Events reconciled to `email_sends` by `ses_message_id`; an event with no matching send row is still recorded and still suppresses
- [ ] Returns 200 even for payloads that cannot be interpreted — logged, not retried
- [ ] SES account-level suppression list enabled as a second layer
- [ ] `email_events` migration created with indexes on message id and recipient
- [ ] Tests cover permanent bounce, transient bounce, complaint, delivery, and an uninterpretable payload

## Carried in from `#0025`'s review (2026-08-19)

- [ ] **`MarkBounced` and `MarkComplained` return `(Subscriber, nil)` with the
      row unchanged when it is already `complained`.** That is success, not
      failure — do not treat it as an error and skip the `email_events` insert or
      the suppression insert. Both are separate steps and must still run.
- [ ] **A repeat `MarkComplained` no longer bumps `updated_at`.** `email_events`
      is the authoritative timeline; do not infer recency from the subscriber row.

## Carried in from `#0037`'s review (2026-08-20)

`#0037` shipped the verifier only — `Verify` (signature + `TopicArn` pin) and
the unexported `validateCertURL`, with no HTTP handler and no database
dependency, per its plan §7 and §11. Its review confirmed that split is
correct, but two of *its* acceptance criteria describe message-type **dispatch**
and therefore land here, where the handler lives. They appeared nowhere in this
issue before, so they are written out in full rather than cross-referenced:

- [ ] **`SubscriptionConfirmation` is auto-confirmed, but only after the
      `TopicArn` pin passes.** Fetch `SubscribeURL` once with `GET`, ignore the
      body, log at INFO, write an audit row (`ses.subscription_confirmed`),
      respond `200`. `Verify` already refuses any message whose `TopicArn` is
      not the configured one, so calling it first is what makes
      auto-confirmation safe — without that, any AWS account could subscribe
      this endpoint to a topic of their own and then publish
      genuinely-signed forged bounce events through it.
- [ ] **`SubscribeURL` is host-validated before it is fetched.** It is
      attacker-influenced in exactly the same way `SigningCertURL` is. Reuse
      `sesnotify`'s existing unexported `validateCertURL` — this handler is in
      the same package, so no new export is needed — with the same client, the
      same no-redirect policy, and the same timeout. Do not hand-roll a second
      check.
- [ ] **`UnsubscribeConfirmation` alerts and never acts.** Never fetch
      anything. Log at ERROR and write an audit row
      (`ses.unsubscribe_confirmation`); respond `200`, since there is nothing
      for SNS to retry. This message means our endpoint has been unsubscribed
      from the SES events topic — bounces and complaints stop arriving and
      nothing else in the system notices.
- [ ] **Map `sesnotify` errors to statuses per `#0037`'s plan §6.**
      `errors.Is(err, sesnotify.ErrCertUnavailable)` → `500` (verification could
      not be *performed*; SNS retries and the event survives). Every other
      `Verify` error → `403` (verification *failed*; an attacker must not be
      able to induce a retry). Unparseable or unknown-`Type` bodies → `200`.
      Wrap the body in `http.MaxBytesReader(w, r.Body, 512<<10)`.
- [ ] **Read `Type` and `TopicArn` from the JSON body, never from the
      `x-amz-sns-message-type` / `x-amz-sns-topic-arn` headers.** Headers are
      not covered by the signature. `sesnotify.Message` has no header-reading
      code at all; keep it that way in the handler.

Note that `SES_EVENTS_TOPIC_ARN` is deliberately not a required config
variable, and an empty value makes `Verify` reject **every** message. Until the
topic is provisioned (CLAUDE.md §10 item 2) this route correctly 403s
everything; that is the intended fail-closed state, not a bug to work around.

## Carried in from `#0100`'s plan (2026-08-20)

`#0100` widens `suppressions`' primary key to `(email, reason)` so one address
can carry several independent reasons to be blocked. It lands **before** this
issue. Four things in this file are superseded by that:

- [ ] **`AddTx`'s signature is unchanged**, and its single-transaction design —
      the hard block `#0033` shipped for this issue — is unaffected. Keep
      writing the event row and the suppression row in one transaction.
- [ ] **§7 item 2 is superseded.** It says the store is "idempotent by email,
      must not overwrite the original reason". The key is now `(email, reason)`,
      so a complaint arriving after a hard bounce inserts a **second row**. Call
      `AddTx` unconditionally per this issue's own §4 mapping; do **not** skip on
      "already suppressed". That skip is precisely the write-time data loss
      `#0100` exists to fix — the address must end up carrying both reasons.
- [ ] **The migration number changes.** §6 names `000012_create_email_events`,
      but `000012` is `create_suppressions` and `#0100` takes `000013`. This
      issue's migration is **`000014_create_email_events`**. Migrations are
      append-only (`CLAUDE.md` §1) — confirm the highest applied number before
      writing it rather than trusting this note.
- [ ] **The constant names differ.** §7 item 4 names `ReasonHardBounce` and
      siblings; the shipped names are `subscribers.SuppressionReasonHardBounce`
      and its siblings.

## Carried in from `#0107`'s review (2026-08-20)

`#0107` (resolved, `a3ae0c2`) reordered `Verify`: the `TopicArn` pin now runs
between `validateCertURL` and the certificate fetch, not after
`rsa.VerifyPKCS1v15`. `#0037`'s host-validation-before-fetch property is
unchanged, and no criterion above is invalidated — but three consequences
land here, where the handler and its tests live:

- [ ] **The `ErrCertUnavailable` → `500` / everything-else → `403` mapping above
      is unchanged and still correct.** One input now crosses that line, in the
      intended direction: a message with a non-allowlisted or unconfigured
      `TopicArn` *and* an unreachable `SigningCertURL` used to return
      `ErrCertUnavailable` (a `500`, so SNS retried a message that could never
      be accepted) and now returns the plain topic-rejection error (a terminal
      `403`). Do not add a special case for it — the existing mapping already
      produces the right answer.
- The unprovisioned-topic note above is now unconditionally true. With
  `SES_EVENTS_TOPIC_ARN` empty this route `403`s every message *without making
  any outbound request*; previously it fetched the signing certificate first
  and would have `500`d if that fetch failed. The fail-closed state is now
  genuinely free.
- [ ] **Set `TopicArn` deliberately in every negative-path fixture.** The pin
      fires *before* signature verification, so a fixture carrying both a wrong
      `TopicArn` and a bad signature returns the topic error, not
      `"signature verification failed"`. §12 test 16 distinguishes
      guard-`403` from bad-signature-`403` by response body, and test 17
      asserts the topic-mismatch `403` — keep those two fixtures' `TopicArn`
      values distinct so each proves its own property rather than both
      tripping the pin.

## Notes

PRD §6.7. Returning non-2xx on a parse failure is the trap: SNS retries aggressively, so a parsing bug becomes a retry storm that buries the real events. Log and 200.

Recording the raw payload before interpreting it means a bug in the interpretation logic is recoverable — the evidence is still in `email_events`.

Enable SES account-level suppression too. Our list is authoritative for our sending decisions; SES's protects the account reputation if ours ever has a bug.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §6.7 — SES event ingestion · 33 lines
  `sed -n '/^### 6\.7 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0027](0027.md) — Replace the SMTP mailer with the SES v2 API mailer
- Depends on: [#0033](0033.md) — Create the suppressions migration and store
- Depends on: [#0037](0037.md) — Verify SNS message signatures
- Blocks: [#0039](0039.md) — Suppress addresses after repeated soft bounces
- Blocks: [#0049](0049.md) — Build the campaign stats view
- Related: [#0107](0107.md) — Hoist the topic-ARN pre-filter before the certificate fetch (resolved, `a3ae0c2`)

## Implementation plan

Phase-1 planning pass (Opus, 2026-08-19). Planned jointly with
[#0037](0037.md), which carries the signature-verification half — **read
#0037's plan first**; §1–§6 there (fetcher seam, host allowlist, topic-ARN
pin, status-code table) are prerequisites this plan assumes and does not
repeat. Status stays `open`.

### 0. What exists today (verified)

- `internal/sesnotify` does not exist yet; #0037 creates it (verifier only, no
  DB dependency). This issue adds `store.go` and the handler.
- **`email_events` does not exist in any migration.** Highest migration is
  `000011_add_subscribers_already_subscribed_sent_at`. This issue writes
  `000012`.
- **`email_sends` does not exist either, and is created by #0040 (Phase 5).**
  See correction 1 in §9.
- `suppressions` does not exist; #0033 creates it and is unimplemented — §7
  states what this issue needs from it.
- `subscribers.Store` already has `MarkBounced`, `MarkComplained`,
  `FindByEmail`, `GetByID`, and a `setStatus` that preserves `complained`
  (`statusLockedFromNonAdmin`) — CLAUDE.md §9 is already enforced in the store,
  so this handler cannot violate it even by accident.
- `internal/audit` has the `querier` / `Write` / `WriteTx` pair that §5's
  transaction design copies.
- Route wiring lives in `mountAndServe` (`cmd/opencircuit/main.go`), with
  `adminRoutes()` as the single admin table and an AST test
  (`admin_routes_ast_test.go`) that fires only on literal patterns beginning
  `/admin`.

### 1. Decision — payload shape: parse `eventType`, accept `notificationType`

PRD §6.7 wires a **Configuration Set event destination**, whose payload is the
*event publishing* format:

```json
{"eventType":"Bounce","mail":{...},"bounce":{...}}
```

The older *identity notification* format uses `"notificationType"` instead, and
AWS's docs for the two sit side by side — an implementer can easily read the
wrong page. Parse `eventType` first and fall back to `notificationType`; treat
whichever is non-empty as the event type. Both formats agree on the `mail`,
`bounce`, `complaint`, and `delivery` sub-objects, so nothing else changes.

**The SNS `Message` field is a JSON string, not a nested object.** It must be
unmarshalled a second time. This is the single most common bug in SNS/SES
ingestion code and deserves a comment.

**Open/Click events must not be enabled on the configuration set** (CLAUDE.md
§9: no email open-tracking pixels). PRD §6.7 already lists only Bounce,
Complaint, Delivery, Reject, RenderingFailure. The handler should still
tolerate an unexpected `eventType` by recording it and returning 200 — it must
not be the thing that enforces the tracking policy, but it must not crash if
the configuration set is ever misconfigured.

### 2. Decision — the dedupe key is the SNS `MessageId`, and the unique
constraint is `(sns_message_id, recipient)`

This is the decision the brief flags as most likely to be got wrong, so the
reasoning is spelled out.

- **SNS `MessageId`** identifies one *delivery attempt group*: SNS's
  at-least-once guarantee means the same `MessageId` arrives repeatedly. This
  is the retry-dedupe key.
- **SES `mail.messageId`** identifies one *outbound email*. One email produces
  *many* events (Send, Delivery, then possibly Bounce, then possibly
  Complaint). It is **not** a dedupe key; it is the reconciliation key, and it
  is what #0049 joins on and what #0039 does *not* use.

So: store both. `sns_message_id` carries the uniqueness; `ses_message_id` is a
plain indexed column.

**Why the constraint is composite.** A single `Bounce` event carries
`bounce.bouncedRecipients[]` — an array. `email_events.recipient` is one
column, so one SNS message can legitimately produce several rows. A `UNIQUE
(sns_message_id)` would make that impossible; picking "the first recipient" and
discarding the rest silently drops suppressions. Insert **one row per
recipient**, all inside the one transaction, with `UNIQUE (sns_message_id,
recipient)`.

`recipient` must therefore be `NOT NULL DEFAULT ''` — in Postgres, `NULL`s do
not conflict, so a nullable `recipient` would defeat the dedupe entirely for
events that have no recipient list. Recipient extraction, in order:

| Event | Source |
|---|---|
| Bounce | `bounce.bouncedRecipients[].emailAddress` |
| Complaint | `complaint.complainedRecipients[].emailAddress` |
| Delivery | `delivery.recipients[]` (plain strings) |
| anything else | `mail.destination[]` |
| nothing found | one row with `recipient = ''` |

Normalise every extracted address with the same `lower(trim(…))` the
`subscribers` and `suppressions` tables enforce (see §7) — SES returns the
address as the sender supplied it, and a case mismatch means a suppression that
silently fails to match, which is worse than no suppression at all.

### 3. Decision — ordering: stale events must not overwrite newer state

At-least-once and out-of-order delivery, handled in three layers:

1. **Terminal states are already monotone in the store.**
   `subscribers.setStatus` preserves `complained` against every non-admin
   write, so a late `Bounce` cannot erase a `Complaint` regardless of arrival
   order. Nothing in this handler needs to re-implement that, and nothing in
   this handler may bypass it — `AdminClearComplaint` is the only method that
   can, and this handler must never call it.
2. **`Delivery`, `Reject`, `RenderingFailure`, and `Send` never change
   subscriber status at all.** They are recorded and nothing else. This removes
   the entire class of "a late Delivery un-bounces someone".
3. **The real remaining hazard** is a genuinely old event arriving after the
   address has been legitimately restored (admin clears the complaint or the
   suppression, the person re-signs up, re-confirms, and *then* the retry of a
   week-old bounce lands). Guard it explicitly: **skip the status transition
   and the suppression insert when the event's `mail.timestamp` is older than
   the subscriber's `confirmed_at`.** The `email_events` row is still written —
   the evidence is never discarded, only the state change is suppressed. This
   is cheap (one comparison against data already loaded) and it is the only
   place where "newer state" is at risk. It needs its own test.

Note this guard applies only when a subscriber row exists. An event for an
address with no subscriber still records and still suppresses (criterion 6).

### 4. Decision — event → state mapping

| `eventType` | `bounce.bounceType` | Extra condition | `email_events` | `suppressions` | subscriber status |
|---|---|---|---|---|---|
| `Bounce` | `Permanent` | any subType (`General`, `NoEmail`, `Suppressed`, `OnAccountSuppressionList`) | row | `hard_bounce` | → `bounced` |
| `Bounce` | `Transient` | — | row | **none** — #0039's job | unchanged |
| `Bounce` | `Undetermined` | — | row | **none** | unchanged |
| `Complaint` | — | `complaintFeedbackType != "not-spam"` | row | `complaint` | → `complained` |
| `Complaint` | — | `complaintFeedbackType == "not-spam"` | row | **none** | **unchanged** |
| `Delivery` | — | — | row | none | unchanged |
| `Reject` | — | — | row | none | unchanged |
| `RenderingFailure` | — | — | row + log at ERROR | none | unchanged |
| `DeliveryDelay`, `Send`, unknown | — | — | row | none | unchanged |

Two traps worth naming:

- **`not-spam` is not a complaint.** It is a feedback-loop report that a
  message was moved *out* of a spam folder. Suppressing on it unsubscribes
  people for the crime of rescuing our mail. Explicitly exclude it and test it.
- **`Permanent/Suppressed` and `Permanent/OnAccountSuppressionList`** mean SES's
  own account-level suppression list blocked the send. Still a hard bounce from
  our point of view — suppress.

Per the carried-in criteria from #0025's review: when `MarkBounced` /
`MarkComplained` return the row unchanged because it is already `complained`,
that is **success**. Do not branch on it, do not skip the `email_events` insert
or the suppression insert. With the single-transaction design in §5 this falls
out naturally — there is no early return to get wrong.

### 5. Decision — one transaction, and where the 200/500 line actually falls

**All writes for one SNS message happen in a single `pgx.Tx` owned by the
handler.** Copy `internal/audit`'s established idiom: each store package
exposes a `…Tx` variant over a `querier` interface that both `*pgxpool.Pool`
and `pgx.Tx` satisfy. That keeps SQL in the package that owns each table
(CLAUDE.md §1) while letting one caller make them atomic.

```
BEGIN
  for each recipient:
      INSERT INTO email_events (...) ON CONFLICT (sns_message_id, recipient) DO NOTHING
  if zero rows inserted across all recipients → COMMIT (no-op), return 200   ← redelivery
  for each recipient:
      resolve subscriber by normalised email (may be absent)
      apply §3 staleness guard
      apply §4 mapping: suppressions.AddTx(...) and subscribers.MarkBouncedTx/MarkComplainedTx(...)
  audit.WriteTx(...)
COMMIT
```

**Atomicity is what makes the dedupe short-circuit correct.** If the three
writes were separate, a crash between the `email_events` insert and the
suppression insert would leave the event recorded but unsuppressed — and SNS's
retry would then be *deduped away* by the already-present `email_events` row,
so the suppression would never happen and nothing would ever notice. Inside one
transaction, either the whole effect landed or none of it did, so "the event
row exists" is a sound proof that "the event was fully processed".

Status codes extend #0037's table with the one case that matters here:

- Payload parses but the `eventType` is unrecognised, or the JSON is malformed
  → **200**, logged. A parse bug is not fixed by retrying, and SNS's retry
  policy would turn it into a storm that buries real events. This is
  criterion 7 and PRD §6.7 item 5.
- Payload is valid but the **transaction fails** (DB down, constraint error,
  serialisation failure) → **500**. Criterion 7 is about *interpretation*, not
  *storage*: if the raw payload could not be recorded there is nothing to
  recover from later, and SNS's retry is the only thing standing between us and
  a lost bounce. Returning 200 here would discard events silently during a
  database outage. **This distinction must be a comment in the handler and must
  have a test** — it is the one place where "always 200" is wrong.

### 6. Migration `000012_create_email_events`

```sql
CREATE TABLE email_events (
    id             BIGSERIAL PRIMARY KEY,
    sns_message_id TEXT NOT NULL,
    ses_message_id TEXT,
    event_type     TEXT NOT NULL,            -- Bounce | Complaint | Delivery | Reject | ...
    bounce_type    TEXT,                     -- Permanent | Transient | Undetermined
    bounce_subtype TEXT,
    recipient      TEXT NOT NULL DEFAULT '', -- normalised lower(trim(...)); '' when the event has none
    event_at       TIMESTAMPTZ,              -- mail.timestamp / event timestamp from the payload
    payload        JSONB NOT NULL,
    received_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (sns_message_id, recipient)
);
CREATE INDEX idx_email_events_message_id ON email_events (ses_message_id);
CREATE INDEX idx_email_events_recipient  ON email_events (recipient);
-- #0039 counts transient bounces per recipient in a sliding window; this is
-- the index that query needs, added here rather than in #0039 so #0039 is a
-- pure read.
CREATE INDEX idx_email_events_soft_bounce
    ON email_events (recipient, received_at DESC)
    WHERE event_type = 'Bounce' AND bounce_type = 'Transient';
```

Add a `CHECK (recipient = lower(trim(recipient)))` to match the `subscribers`
migration's belt-and-suspenders normalisation constraint, and a `CHECK` on
`event_type` **only if** the set is genuinely closed — it is not (SES adds
event types), so leave `event_type` unconstrained and say so in a comment. This
is the opposite of `subscribers.status`, which *is* closed; the asymmetry is
deliberate and worth one line of SQL comment.

**Deviations from PRD §6.2's `email_events` definition, deliberate:**
`sns_message_id` added (there is no dedupe key in the PRD schema);
`bounce_subtype` added (#0039 needs it, and PRD §6.5's own note about
out-of-office replies depends on subType); `recipient` made `NOT NULL DEFAULT
''` and joined into the unique constraint; `event_at` added. These are
additions, not removals — every PRD column survives. Since this issue *creates*
the table, this is not an append-only violation (CLAUDE.md §1). Record the
deviation in `## Fix` so the PRD can be reconciled later.

The `.down.sql` drops the table.

### 7. What #0033 must provide (build the suppressions store to fit)

`#0033` is unimplemented. State these requirements there before or while
building it, so the mismatch is not discovered during #0038's implementation:

1. **A transaction-accepting variant.** `AddTx(ctx context.Context, q querier,
   email, reason, note string, now time.Time) error` alongside the pool-backed
   `Add`. Without it, §5's single transaction is impossible and the crash
   window in §5 reopens. Model it on `audit.Logger.WriteTx` / `audit.insert(ctx,
   q querier, …)` — that precedent already exists in this codebase, so this is
   not a new pattern.
2. **Idempotent by email.** `INSERT … ON CONFLICT (email) DO NOTHING`. A repeat
   suppression must not error and must **not** overwrite the original reason —
   the first reason is the true one, and `email_events` is the authoritative
   timeline for everything after it. Returning "already present" is success.
3. **No foreign key to `subscribers`.** Criterion 6 requires that an event with
   no matching subscriber still suppresses; #0033's own notes require the row
   to survive hard deletion (#0060). Key on email only.
4. **Exported reason constants**: `ReasonHardBounce = "hard_bounce"`,
   `ReasonComplaint = "complaint"`, `ReasonRepeatedSoftBounce =
   "repeated_soft_bounce"`, `ReasonManual = "manual"`. #0038 must never write
   these as string literals.
5. **`email` normalised `lower(trim(email))` with the same CHECK as
   `subscribers.email`** — already a carried-in criterion on #0033, and #0038
   depends on it being true.

#0038 additionally needs, in `internal/subscribers`: `MarkBouncedTx` and
`MarkComplainedTx` taking a `querier`, refactored out of the existing
`setStatus` (which becomes `setStatusTx(ctx, q, …)` with the pool-backed
methods as thin wrappers). **The `statusLockedFromNonAdmin` guard must live in
the shared implementation, not the wrapper**, so the Tx path cannot skip it.
That refactor belongs to this issue, not #0033.

### 8. The boundary with #0039 — stated so the two do not overlap

**#0038 does no counting whatsoever.** It does not read a window, does not read
`settings`, does not look at prior events. Its entire responsibility for a
`Transient` bounce is: write the `email_events` row with `bounce_type =
'Transient'` and `bounce_subtype` faithfully recorded, and change nothing else.

**#0039 adds exactly one call at exactly one place.** #0038 must structure the
per-recipient interpretation as a single named function — suggested
`applyBounce(ctx, tx, ev, sub)` — with a `case "Transient":` arm carrying a
comment naming #0039 as its owner. #0039 inserts, inside that arm and inside
the same transaction, a `CountRecentTransientBounces(ctx, q, recipient, window)`
call against `email_events` (the partial index in §6 is already there for it)
and, on threshold, performs the same `AddTx(ReasonRepeatedSoftBounce)` +
`MarkBouncedTx` pair `Permanent` already uses. Nothing else in #0038 changes.

Two boundary questions #0038 must **record but not decide**:

- Whether `Undetermined` counts toward the soft-bounce threshold. #0038 records
  the value faithfully so #0039 can choose either way. Recommendation to
  #0039: count it — an unclassifiable repeated failure is a dead address — but
  it is #0039's call.
- The threshold values (5 in 30 days) live in `settings` per #0039's criteria.
  #0038 must not hardcode them anywhere, because it must not read them at all.

### 9. Corrections to the acceptance criteria

Recorded here rather than edited into the checkbox list, per the planning brief.

**Correction 1 — criterion 6 ("Events reconciled to `email_sends` by
`ses_message_id`") cannot be implemented as a write, and should not be.**
`email_sends` is created by **#0040**, which is Phase 5, and #0038 is Phase 4
and does not list #0040 in `Depends on` — correctly, since a Phase 4 issue
gated on Phase 5 would deadlock the phase order. There is no table to reconcile
*to* when this issue is built.

Recommended restatement: *"`email_events` records `ses_message_id` on every
event, indexed, so that #0049 can reconcile campaign stats by joining
`email_sends` on it. #0038 writes no `email_sends` rows."* The second half of
the criterion — "an event with no matching send row is still recorded and still
suppresses" — survives verbatim and is genuinely testable now; it is the
important half.

This restatement is also the *better* design independently of the ordering
problem: a write-side reconciliation would need a monotone-by-severity guard on
`email_sends.status` to stop a late `Delivery` overwriting `bounced`, which is
exactly the kind of ordering bug §3 exists to avoid. A read-side join has no
such hazard.

**Correction 2 — criterion 8 ("SES account-level suppression list enabled as a
second layer") is an AWS console/CLI action, not code, and cannot be verified
by `go test`.** The SES account itself does not exist yet: CLAUDE.md §10 open
item 2 records SES domain verification as *not started*. Recommended
resolution: satisfy the criterion with a documented runbook step in `docs/`
(the `aws sesv2 put-account-suppression-attributes --suppressed-reasons BOUNCE
COMPLAINT` invocation, plus what it does and does not cover) and a line in
CLAUDE.md §10's open-items table, rather than by claiming a code change.

**Correction 3 — a consequence worth stating plainly:** because the SNS topic,
the configuration set, and the SES identity do not exist yet, **#0038 cannot be
verified end-to-end against real SNS during this phase.** Everything in this
plan is unit- and DB-testable with synthesised payloads and #0037's keypair
fixture, and that is the standard the reviewer should apply. The live wiring is
a separate, later verification gated on CLAUDE.md §10 open item 2 — it should
not be claimed as done in `## Verification`.

**Correction 4 — missing criterion: topic-ARN allowlisting.** See #0037's plan
§5 and correction 3. It belongs to #0037 but #0038 inherits the consequence:
this endpoint must reject any message whose `TopicArn` is not the configured
one, before any row is written.

### 10. Route, wiring, and the admin-guard invariant

Route: **`POST /api/ses/notifications`** (PRD §6.7, criterion 1).

- Registered **directly in `mountAndServe`**, not via `adminRoutes()`. It is
  `/api`, not `/admin`, so `admin_routes_ast_test.go` does not fire on it —
  that test matches only literal patterns whose path begins `/admin`, and
  #0079's recorded decision deliberately scopes it that way.
- **It must not sit behind `requireAdmin` or `requireSession`.** SNS posts with
  no bearer credential of any kind; the signature *is* the authentication.
  Wrapping it in either guard would return 403 to SNS, which retries, backs
  off, and eventually disables the subscription — bounces stop arriving and
  nothing surfaces an error.
- #0079's recorded invariant is that **every `/api` and `/account` registration
  carries its guard, or a comment explaining its deliberate absence, at the
  call site**. This route needs that comment, in the same style as
  `/api/subscribe` and `/api/preferences`: state that it is public by design,
  that `sesnotify.Verifier` plus the topic-ARN pin is the authentication, and
  that adding a session guard here breaks event ingestion silently.
- Follow the existing nil-guard convention (`if sesNotifyH != nil { … }`) so
  dev mode (`STORAGE=json`, no Postgres) still boots — `internal/devstore` has
  no `email_events` backing and none should be added.
- **No rate limiter.** Every other public route here has one; this one must
  not. A campaign send produces thousands of events in a burst from many SNS
  source IPs, and a per-IP token bucket would reject exactly the traffic the
  system exists to receive. Cost is bounded instead by
  `http.MaxBytesReader` (#0037 §6) and by the cert cache. Put that reasoning in
  the call-site comment too, since its absence would otherwise read as an
  oversight.
- `knownStaticRoutes` (`internal/handlers/routes.go`) is the **SPA** route
  table and must not gain an `/api/...` entry; `routes_parity_test.go` is
  unaffected.

### 11. Files

| File | Action |
|---|---|
| `migrations/000012_create_email_events.up.sql` / `.down.sql` | new — §6 |
| `internal/sesnotify/store.go` | new — `Store` over `email_events`: `InsertTx`, `ByMessageID`; SQL lives here (CLAUDE.md §1) |
| `internal/sesnotify/event.go` | new — SES event payload types, `eventType`/`notificationType` parsing, recipient extraction, normalisation |
| `internal/sesnotify/store_test.go`, `event_test.go` | new |
| `internal/handlers/ses_notifications.go` | new — `SESNotificationsHandler`, narrow store interfaces, the transaction, the status-code policy |
| `internal/handlers/ses_notifications_test.go` | new |
| `internal/subscribers/store.go` | edit — `setStatusTx`/`MarkBouncedTx`/`MarkComplainedTx` over a `querier`; guard moves into the shared impl |
| `internal/suppressions/…` | **#0033's** — this issue depends on §7's shape |
| `cmd/opencircuit/main.go` | edit — construct the handler on the Postgres path, register the route with the §10 comment |
| `cmd/opencircuit/*_wiring_test.go` | edit — assert the route is registered and reachable **unauthenticated** |
| `docs/` | new/edit — SES account-level suppression runbook (correction 2) |

Handler dependencies are narrow interfaces declared in
`internal/handlers/ses_notifications.go`, matching `confirmSubscriberStore` /
`confirmInterestStore` in `confirm.go` — never the concrete stores.

### 12. Test strategy

DB-backed; **`TEST_DATABASE_URL` must be exported** and the run must show zero
skips (CLAUDE.md §5). Fixtures: synthesised SNS envelopes signed with the
test keypair from #0037's helper, so a handler test exercises the real
verifier rather than stubbing it out — the verifier's fetcher seam is the only
thing injected.

Required cases:

1. **Permanent bounce** → `email_events` row, `suppressions` row
   (`hard_bounce`), subscriber `bounced`, 200.
2. **Transient bounce** → `email_events` row with `bounce_type='Transient'`,
   **no** suppression, subscriber status unchanged, 200.
3. **Complaint** → `email_events`, `suppressions` (`complaint`), subscriber
   `complained`, 200.
4. **Complaint with `complaintFeedbackType: "not-spam"`** → `email_events`
   only, no suppression, status unchanged.
5. **Delivery** → `email_events` only, nothing else changes.
6. **Uninterpretable payload** (not JSON, and separately: valid JSON with an
   unknown `eventType`) → 200, logged, no state change.
7. **Storage failure → 500, not 200.** The important negative case. Close the
   pool or point the handler at a cancelled context; assert 500 and assert no
   partial rows.
8. **Redelivery**: post the identical signed envelope twice → exactly one
   `email_events` row per recipient, exactly one suppression row, second call
   200.
9. **Crash-window equivalence**: run the transaction, roll it back, re-post →
   all three writes land on the retry. This proves §5's atomicity claim rather
   than assuming it.
10. **Multi-recipient bounce**: two `bouncedRecipients` → two `email_events`
    rows, two suppressions, both subscribers `bounced`.
11. **Event for an address with no subscriber row** → `email_events` row and
    suppression row still written, 200 (criterion 6's surviving half).
12. **Stale event guard (§3)**: a bounce whose `mail.timestamp` predates the
    subscriber's `confirmed_at` → event recorded, **no** suppression, status
    unchanged.
13. **`complained` is not erased**: subscriber already `complained`, a
    `Permanent` bounce arrives → status stays `complained`, **and** the
    `email_events` row and the `hard_bounce` suppression are both still
    written (the carried-in criteria from #0025's review).
14. **Repeat `MarkComplained` does not bump `updated_at`** — assert the
    timestamp is unchanged across a second complaint (carried-in criterion 2).
15. **Case/whitespace normalisation**: SES reports `" Foo@EXAMPLE.com "` for a
    subscriber stored as `foo@example.com` → matched and suppressed.
16. **Unauthenticated reachability**: a wiring test in `cmd/opencircuit`
    asserting the route answers *without* a session (403 for a bad signature,
    not 403-from-the-guard). Distinguish the two by the response body or by
    posting a *valid* signature and expecting 200.
17. **Topic-ARN mismatch** → 403, zero rows written.

Discipline required by CLAUDE.md §8b:

- **Seed throwaway rows; never target a literal or seeded id.** Use a unique
  per-test email prefix (`zz-0038-<random>@example.test`) and derive ids from
  what you inserted.
- If any test proves a guard by removing it (e.g. deleting the topic-ARN check
  to show the mutation is caught), **create a private database for it**
  (`CREATE DATABASE opencircuit_0038`, drop it after). Do not run guard-removal
  mutations against the shared `opencircuit_test`.
- Bound all verification output with `tail -40`.

No SPA change, so no `npm run check` / `npm test` requirement — but say so
explicitly in `## Verification` rather than leaving it ambiguous.

### 13. Suggested order

1. Migration `000012` + `sesnotify.Store` (`InsertTx`, `ByMessageID`) +
   store tests. Nothing above it can be tested until rows can be written.
2. `event.go`: payload types, `eventType`/`notificationType` fallback, the
   double-unmarshal of `Message`, recipient extraction, normalisation. Pure
   functions, table-driven tests, no DB.
3. `internal/subscribers` `…Tx` refactor, with the existing subscriber suite
   re-run to prove the guard did not move.
4. **Confirm #0033 has landed with §7's shape.** If it has not, this is the
   blocking point — do not stub a local suppressions table to get past it;
   report back instead.
5. `internal/handlers/ses_notifications.go`: the transaction, the mapping, the
   status-code policy. Tests 1–15.
6. Route wiring in `mountAndServe` with the §10 comment; wiring tests 16–17.
7. `docs/` runbook for SES account-level suppression (correction 2).
8. `go build ./... && go vet ./...`, then
   `go test ./... -count=1` with `TEST_DATABASE_URL` exported; confirm zero
   skips and that `internal/sesnotify`, `internal/handlers`,
   `internal/subscribers` and `cmd/opencircuit` all report real durations.

### 14. Left open for the implementer

- Whether `sesnotify.Store` also gets a `CountRecentTransientBounces` stub now
  or #0039 adds it. Recommendation: leave it out; #0039 owns it and an unused
  method is dead code the reviewer will (rightly) ask about.
- Audit action names. Suggested: `subscriber.bounced` and
  `subscriber.complained`, with `source: "ses"` in metadata, plus
  `ses.event_unhandled` for the 200-on-uninterpretable path. Keep the
  `TargetType` consistent with whatever #0037 chose for its two SNS audit rows.
- Whether one audit row is written per SNS message or per recipient. Per
  recipient is more useful and matches `email_events`; per message is cheaper.
  Recommendation: per recipient, for the state changes only — not for
  `Delivery`, which would flood the audit log during a campaign.

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-08-20 | implement | claude-sonnet-5 | 487,900 | — |
| 2026-08-20 | review (bounced) | claude-opus-5 | 185,800 | — |

Bounced for missing evidence, not a wrong fix: the single-transaction guarantee
had no test, proven by mutation. Review filed #0108. Harness reports aggregate
subagent totals only.
