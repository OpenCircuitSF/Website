# 0224 — The fence *opener* has no indentation cap, and it still masks a phantom row

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | docs / db |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-23 |

## Description

`#0092` fixed the two illegal fence **closers** the docs guard accepted. Its
phase-3 review verified the fix against `commonmark.js` 0.31.2 as an oracle —
21 closer shapes, post-fix agreeing 21/21 where pre-fix disagreed on 7 — and
then found the same defect class on the **opposite branch**.

**CommonMark caps a fence opener at 3 columns of indentation too.** The opener
branch checks only `leadingRunLength(trimmed, ch) >= 3`, so `trimmed` discards
indentation exactly as the closer branch used to. Three divergences remain at
`f74cd6f`:

- an opener indented 4 spaces,
- an opener indented 1 tab (4 columns under CommonMark's tab-stop rule),
- a backtick inside a backtick fence's info string (`` ``` foo`bar ``), which
  CommonMark forbids.

**Individually each fails in the safe, loud direction — but the review
reproduced a still-silent mask post-fix.** A spurious open from an indented
`` ``` `` flips fence parity, and a later info-string fence flips it back,
letting a phantom table row past while the guard reports `ok`. That is the same
failure mode `#0092` was filed for, reached through the other branch.

The review confirmed by oracle that such a document is honest markdown — so this
is not a contrived shape, it is a shape a documentation author could write.

**A second item, from `#0089` and still open:** the probe forms both `#0092`
and this review constructed by hand should be committed as `cases` subtests. A
per-shape entry would have caught this round *and* the last one. Three passes
have now built the same probe set and thrown it away.

## Acceptance criteria

- [ ] An opener indented 4+ columns does not open a fence, matching CommonMark
- [ ] A tab-indented opener is treated as 4 columns, using the same `leadingIndentWidth` column semantics the closer branch now uses
- [ ] A backtick in a backtick fence's info string does not open a fence
- [ ] The parity-flip masking scenario the review built is reproduced and now fails loudly — this is the criterion that matters
- [ ] Each divergence is proved by mutation: drop the condition, confirm exactly its own subtest fails
- [ ] The probe shapes are committed as `cases` subtests rather than reconstructed, covering both opener and closer forms
- [ ] `#0092`'s 16 leaf subtests still pass, `TestDatabaseDocMigrationParity`'s 2 still pass, and `#0113`'s `TestPRDSectionIndexParity` is untouched
- [ ] `ISSUE=0224 scripts/check.sh go ./internal/...` passes with zero skips

## Implementation notes

All changes are in `internal/db/docs_parity_test.go` (the guard is itself a
test file, per this codebase's existing pattern — no production package
owns `docTableRowLines`).

**The opener branch (`fenceChar == 0`) now mirrors the closer branch:**

```go
indented := leadingIndentWidth(line) > 3
switch {
case !indented && leadingRunLength(trimmed, '`') >= 3 &&
	!strings.Contains(trimmed[leadingRunLength(trimmed, '`'):], "`"):
	fenceChar = '`'
	fenceLen = leadingRunLength(trimmed, '`')
	continue
case !indented && leadingRunLength(trimmed, '~') >= 3:
	fenceChar = '~'
	fenceLen = leadingRunLength(trimmed, '~')
	continue
}
```

- Indentation cap: `leadingIndentWidth(line) > 3` reuses the exact helper and
  threshold the closer branch has used since `#0092` — a tab is 4 columns
  under its `col += 4 - (col % 4)` tab-stop arithmetic, so it's caught the
  same way a 4-space indent is, with no separate tab-handling code.
- Info-string check: for a backtick-run opener only, `trimmed` is sliced past
  the run and checked for any remaining backtick. A tilde-run opener has no
  such check, matching CommonMark (a backtick can't be confused with a `~~~`
  marker, so there's nothing to forbid there).
- An opener that fails to open (either reason) falls through to the existing
  indented-code-block check (`leadingIndentWidth(line) >= 4`) or is treated
  as ordinary prose — it does not silently vanish, it's handled exactly like
  any other line that was never a fence marker.

**Composed masking scenario (criterion 4).** Added
`TestDocTableRowLinesParityFlipMaskingRegression`, a standalone test asserting
`docTableRowLines` returns all three rows of a document where a 4-indented
`` ``` `` (opener residual 1) is immediately followed by a doc row naming a
migration that doesn't exist on disk, followed by `` ``` bogus`info `` (opener
residual 2), followed by a second real row. Pre-fix, the indented line
spuriously opens a fence; the info-string line, evaluated by the
*already-correct* closer branch (since state is wrongly non-zero), has
trailing text and so correctly refuses to close — meaning the fence never
closes for the rest of the document and every subsequent line, including the
stale/phantom row, is masked out of `rowLines` entirely. That row never
reaches `verifyDocRowsHaveMigration`'s reverse check, so the parity guard
would report `ok` on a document that actually names a nonexistent migration.
See `## Verification` for the direct before/after measurement.

**Probe forms committed as `cases` subtests (criterion 6, closing #0089's
open item):** three new cases in the existing "accepts" table (opener
indented 4 spaces, tab-indented opener, backtick-in-info-string opener, each
asserting the fence does *not* open and the following row *is* counted), one
new case in "rejects" (a tilde opener with a backtick in its info string,
asserting the fence *does* still open — proves the fix didn't over-correct).

**Doc comment** above `docTableRowLines` extended with a `#0224` paragraph
describing both residuals and the composition, in the same style as the
existing `#0083`/`#0086`/`#0089`/`#0092` paragraphs.

## Verification

`go vet ./internal/db/...` — clean.

`go test ./internal/db/... -run 'TestDocTableRowLines|TestDatabaseDocMigrationParity' -v`
— all pass: `TestDocTableRowLines` (16 pre-existing leaf subtests unchanged,
plus 4 new: 1 under "rejects", 3 under "accepts"),
`TestDocTableRowLinesParityFlipMaskingRegression` (new),
`TestDatabaseDocMigrationParity` (both `docs/database.md` and `CLAUDE.md`
cases, unchanged).

**Mutation proof, per divergence (criterion 5).** Copied the fixed file aside
(`cp internal/db/docs_parity_test.go /tmp/docs_parity_test.go.fixed-0224`),
mutated one condition at a time in the working file, ran the targeted tests,
then restored from the copy and confirmed `diff` was empty before the next
mutation:

- Disabled the indent cap (`indented := false`) — `TestDocTableRowLines/accepts/opener_indented_4+_spaces_does_not_open_a_fence`,
  `.../tab-indented_opener_does_not_open_a_fence`, and
  `TestDocTableRowLinesParityFlipMaskingRegression` failed; every other
  subtest (including the info-string and tilde-info-string cases) still
  passed.
- Restored, confirmed clean via `diff`, then disabled only the info-string
  check (dropped the `!strings.Contains(...)` clause) —
  `TestDocTableRowLines/accepts/backtick_in_backtick_fence_info_string_does_not_open_a_fence`
  failed; every other subtest, including both indent cases, still passed.
- Restored, confirmed clean via `diff` again.

**Composed-scenario before/after, against the actual pre-fix code (criterion
4).** Per this task's guidance, did not `git stash`. Instead: `git show
HEAD:internal/db/docs_parity_test.go` (unmodified at the time, i.e. the
pre-#0224 code) into a scratch directory, extracted `docTableRowLines`,
`leadingRunLength`, and `leadingIndentWidth` verbatim into a standalone
`go run` program, and ran it against the exact document
`TestDocTableRowLinesParityFlipMaskingRegression` uses. Pre-fix output:

```
OLD (pre-#0224) rows = 1
  "| `000003_add_x` | real row one |"
```

— confirming the phantom row and the second real row are both silently
masked pre-fix. The fixed `docTableRowLines` (in the actual test file)
returns all 3 rows for the same document, asserted by the new regression
test.

**Full-suite run:** `ISSUE=0224 scripts/check.sh go ./internal/...` —
`go build`, `go vet`, and `gofmt -l` clean; every package reported `ok`
(15 packages including `internal/db`); skip audit found zero skips
(`TEST_DATABASE_URL` was exported). Machine load average 2.0–2.7 throughout
(idle).

## Notes

Reported by `#0092`'s phase-3 review under `CLAUDE.md` §9.

Criterion 4 is the one that decides whether this is worth doing. Each divergence
alone is loud; the composition is silent, and silence is what `#0083`, `#0086`,
`#0089`, and `#0092` were all filed about.

**Use the reference implementation.** `#0092`'s review ran `commonmark.js`
0.31.2 against 21 shapes and used the agreement count as its verdict. That is a
far better oracle than reading the spec prose, and it is what turned "these two
forms are wrong" into "post-fix agrees 21/21, pre-fix disagreed on 7".

Reuse `leadingIndentWidth` — it already implements CommonMark's column
semantics (`col += 4 - (col % 4)`), verified by that review, and the closer
branch now depends on it.

## Relation

- Found by: [#0092](0092.md) — the two illegal closers
- Relates to: [#0089](0089.md) — where committing the probe forms was first suggested
- Relates to: [#0086](0086.md) — `fenceLen` and the closer-length rule
- Relates to: [#0083](0083.md) — the original silent-row-masking class

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — (not reported to this subagent) | — |
