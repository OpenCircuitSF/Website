# 0400 — `TestTokenGatedKindsPartitionEveryMailKind` checks membership but not the flag, so a kind can be silently ungated

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | mailing / outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-03 |
| **Commit** | `8007d19` |

## Description

`#0340` added `tokenGatedKinds` as a `map[Kind]bool` and
`TestTokenGatedKindsPartitionEveryMailKind` to keep the enumeration a live
artifact. **The guard checks map *membership* but never the `bool` value.**

So an entry flipped from `true` to `false` is silently ungated while the guard
still reports that a decision was made for that kind — the enumeration looks
complete and the gate does nothing.

**Measured, not inferred.** `#0340`'s review ran exactly this mutation: setting
`tokenGatedKinds[KindImportInvite]` to `false` failed the send-path tests that
depend on the gate, **and `TestTokenGatedKindsPartitionEveryMailKind` passed.**
The partition guard did not notice the kind it names had been disarmed.

The model it was built from, `#0365`'s `gatedKinds`, is a `map[Kind]string` with
**no off state** — membership there genuinely is the decision. Copying the shape
to a `map[Kind]bool` silently introduced one.

This is the phase's recurring defect once more — an assertion passing for a
reason that does not distinguish what it names (`#0364`, `#0389`, `#0390`,
`#0394`) — and specifically the sub-family where the check *cannot fail* for the
thing it claims to cover (`#0354`, `#0358`, `#0363`).

## The remedy, from the review that found it

One loop:

	for k, on := range tokenGatedKinds {
		if !on {
			t.Errorf(...)
		}
	}

Consider instead whether `map[Kind]bool` is the wrong type — if there is no
legitimate `false`, a set type (`map[Kind]struct{}`) makes the defect
unrepresentable rather than merely detected. Say which you chose and why.

## The second half — an untested stale-row site

`#0340`'s plan named **four** sites that leave a deliverable stale row.
Three are covered by tests in the tree; **`RestartSignup` is not.**

It is the sharpest of the four: it moves a subscriber's status *back* to
`pending`, re-opening `#0365`'s status gate on a queued row still carrying the
pre-restart token. The token gate is what catches it, and nothing in the tree
proves that.

`#0340`'s reviewer wrote that test and confirmed it lands `skipped` on the fixed
tree and **delivered** against a gate-disabled mutant, but did not land it. It
is preserved at:

	<scratchpad>/0340-review-restart-signup-test.go.txt

**That path is session-scoped and will not survive.** Recover the file early, or
rewrite the case from its description: create pending → enqueue `import_invite`
with the live token → `Unsubscribe` → `RestartSignup` → run the worker → assert
`skipped`.

## Acceptance criteria

1. The partition guard fails when any `tokenGatedKinds` entry is `false` — or
   the type no longer admits a `false`. State which approach you took.
2. **Prove it by mutation**: set one entry to `false`, confirm the partition
   guard now fails **naming that kind**, and confirm an unmutated control
   **passes**. Score by `go test` exit code, not by grepping for `ok`.
3. The `RestartSignup` case lands as a test in the tree, and is proved by
   mutation to fail against a gate-disabled build.
4. **Both halves of any new assertion must be load-bearing** — each fires under
   some mutation, neither fires on unmutated code.
5. `#0340`'s existing coverage must not regress: M1, M2 and M3 still behave, and
   the three `physical_address` defer pins still pass **unmodified**. §9's
   refusal must remain non-bypassable — `#0045`'s rule is restricted and a
   design that lets a send through is wrong.
6. Run `./internal/mailing/...`, `./internal/subscribers/...`,
   `./internal/outbox/...`, `./internal/handlers/...` and `./internal/db/...`
   (§5's repo-wide-guard table). Per §5 name the specific tests that ran.
   `gofmt` stability. No `<file>.go:<int>` citation (`#0352`), scanned with
   `python3` bytes rather than BSD `grep -P`.

## Verification

**Both reported defects independently reproduced before any fix, in a
`git archive HEAD` export (never the tracked tree), own scratch database
(`opencircuit_test_0400repro`, dropped after) — not taken on the prior
review's word:**

- Flipping `tokenGatedKinds[outbox.KindImportInvite]` from `true` to `false`
  (key left present) left `TestTokenGatedKindsPartitionEveryMailKind`
  **passing** (`go test` exit 0), while
  `TestOutboxWorker_SkipsDeferredImportInviteWhoseConfirmTokenWasSwept`
  **failed** (exit 1: row delivered `sent` instead of `skipped`) — the
  partition guard cannot see a disarmed value, confirmed by exit code, not
  by reading `ok`.
- `RestartSignup` had zero references anywhere under `internal/mailing`'s
  `*_test.go` files before this issue (`grep -rn "RestartSignup"
  internal/mailing/*_test.go` returned nothing).

**Fix — criterion 1, `map[outbox.Kind]bool` → `map[outbox.Kind]struct{}`.**
Chosen over the value-assertion loop the issue also offered: a set type
makes the disarmed-but-present state unrepresentable at the type level
rather than merely caught by an assertion someone has to remember to write
and could as easily delete. Proved unrepresentable directly: writing
`outbox.KindImportInvite: false` into the new map literal fails to compile
(`cannot use false (untyped bool constant) as struct{} value in map
literal`), in the same `internal/mailing/outbox_worker.go`.
`sendGate`'s call site changed from `if !tokenGatedKinds[row.Kind]` to
`if _, tokenGated := tokenGatedKinds[row.Kind]; !tokenGated` — same
membership semantics for both present kinds, no behavior change on the
send path.

**Criterion 2 mutation proof, `rsync` copy of the fixed tree (never `git
stash`/`checkout` — CLAUDE.md §8a), own scratch database
(none needed — this test has no DB dependency), scored by `go test` exit
code:**

- Control (unmutated fixed tree): `TestTokenGatedKindsPartitionEveryMailKind`
  — **PASS**, exit 0.
- Mutated (the type-change equivalent of "silently disarm one kind": delete
  `outbox.KindImportInvite: {}` from `tokenGatedKinds` only, leaving it in
  neither `tokenGatedKinds` nor `tokenUngatedKinds`):
  `TestTokenGatedKindsPartitionEveryMailKind` — **FAIL**, exit 1, naming it:
  `outbox.Kind "import_invite" (in mailKinds) is in NEITHER tokenGatedKinds
  nor tokenUngatedKinds — a token-rotation recheck decision was never made
  for it`.

**Criterion 3 — `TestOutboxWorker_SkipsImportInviteAfterRestartSignup`,
added to `internal/mailing/outbox_worker_test.go`, recovered and verified
from the reviewer's preserved draft (`#0340`'s review), proved by mutation
in a `git archive` export of a `git stash create` snapshot (working tree
never touched — confirmed via `git status --porcelain` unchanged before and
after), own scratch databases (`opencircuit_test_0400`,
`opencircuit_test_0400gate`, `opencircuit_test_0400v`, all dropped after):**

- Control (fixed tree, own database `opencircuit_test_0400`):
  `TestOutboxWorker_SkipsImportInviteAfterRestartSignup` — **PASS**, exit 0.
- Mutated (gate-disabled build — `sendGate`'s `return w.tokenGate(row,
  elig)` replaced with an unconditional `return false, "", nil`, own
  database `opencircuit_test_0400gate`): the same test — **FAIL**, exit 1:
  `import_invite row status = "sent", want "skipped" — RestartSignup
  re-opened the status gate on a row still carrying the PRE-restart
  confirm_token`.

**Criterion 4** — both halves load-bearing, shown above: the partition
guard's membership check fires under the delete-the-key mutation and stays
silent unmutated; the new `RestartSignup` test fires under the
gate-disabled mutation and stays silent unmutated.

**Criterion 5 — regression, one combined run** (own database
`opencircuit_test_0400v`, dropped after), all nine named tests passing
together, `go test -v` exit 0:
`TestOutboxWorker_DefersWelcome_MissingPhysicalAddress`,
`TestOutboxWorker_DefersImportInvite_MissingPhysicalAddress`,
`TestAdminResendInvitation_PhysicalAddressGateNotBypassable` (the three
physical-address-defer pins, byte-unmodified — confirmed by `git diff`
touching only `outbox_worker.go`, `outbox_worker_test.go`, and this issue
file), `TestGatedKindsPartitionEveryMailKind`,
`TestOutboxWorker_SkipsDeferredImportInviteWhoseConfirmTokenWasSwept` (M1),
`TestOutboxWorker_SkipsConfirmationWhoseConfirmTokenWasRotated` (M2),
`TestOutboxWorker_TokenGateDoesNotShortCircuitPhysicalAddressDefer` (M3),
`TestOutboxWorker_SkipsImportInviteAfterRestartSignup` (new),
`TestTokenGatedKindsPartitionEveryMailKind`. §9 holds: `sendGate`/`tokenGate`
still return only `(skip bool, reason string, err error)` and the type
change touches only how membership is tested, not the send/withhold
decision structure — `#0045`'s refusal remains unreachable by any new path.

**Criterion 6 — repo-wide-guard table, `ISSUE=0400 scripts/check.sh go
./internal/mailing/... ./internal/subscribers/... ./internal/outbox/...
./internal/handlers/... ./internal/db/...`:** all five `ok`, zero skips,
real elapsed time on every DB-backed package (`5.084s`, `5.363s`, `0.933s`,
`6.904s`, `6.631s` — none `0.00s`), `gofmt -l` clean, machine load 2.80
(§5's "check `uptime` first"). `gofmt` stability separately confirmed
non-destructively: `gofmt <file> | shasum -a 256` matches the on-disk hash
for both `internal/mailing/outbox_worker.go` and
`internal/mailing/outbox_worker_test.go`. No `<file>.go:<int>` citation
anywhere in this issue file's own text, and no literal call syntax for
the claim-machinery identifiers (`ClaimDue`, `OrphanSweep`, `SelectDue`)
introduced in any Go comment — scanned with `python3` reading bytes over
this issue's own diff (pre-existing legitimate call sites elsewhere in
`outbox_worker.go`/`outbox_worker_test.go` are ordinary code, not
comments, and were excluded by scoping the scan to added lines).

## Relation

- Found by: [#0340](0340.md)'s review, which resolved that issue
- Relates to: [#0340](0340.md) — the token gate whose enumeration this guards
- Relates to: [#0365](0365.md) — the `gatedKinds` model whose `map[Kind]string`
  shape has no off state, which is how the defect entered
- Relates to: [#0354](0354.md), [#0358](0358.md), [#0363](0363.md) — checks that
  could not fail
- Relates to: [#0364](0364.md), [#0389](0389.md), [#0390](0390.md),
  [#0394](0394.md) — assertions passing for a reason that does not distinguish

## Work log

- 2026-09-03 — Filed by the orchestrator from `#0340`'s review (§9). The
  mutation result is the reviewer's measurement, not independently reproduced.
- 2026-09-03 — Implemented (Sonnet). Reproduced both reported defects
  independently before touching any code (see `## Verification`). Chose the
  type change (`map[outbox.Kind]bool` → `map[outbox.Kind]struct{}`) over the
  issue's value-assertion-loop alternative: a set type makes the disarmed-
  but-present state unrepresentable — proved by showing the exact original
  mutation (`KindImportInvite: false`) no longer compiles against the new
  type, rather than merely being caught by a check that itself could be
  skipped or deleted later (the same "one more thing to maintain" hazard
  CLAUDE.md's in-file-guard entries warn about). Recovered `#0340`'s
  reviewer-authored `RestartSignup` test from the preserved scratchpad file,
  verified every identifier and signature against the current tree (none had
  drifted), and landed it as
  `TestOutboxWorker_SkipsImportInviteAfterRestartSignup`. All mutation proofs
  used `git archive` exports (one from `HEAD`, one from a `git stash create`
  snapshot of the uncommitted fix — working tree confirmed untouched by
  `git status --porcelain` before and after) against private scratch
  databases, never the tracked tree or its shared database. Left `in-progress`
  for review (§9); did not mark `resolved`.
