# 0425 — `resolve`'s own doc comment omits the archive branch it implements, and two prose slips remain

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | seo / docs |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-04 |
| **Closed** | 2026-09-05 |
| **Commit** | `b85874c` |

> 🟢 Resolved 2026-09-05 — `marketingRoutes`' doc comment and its slice now
> name the same six routes, verified member by member as separate byte ranges,
> and an independently-run sweep over `internal/`, `cmd/`, `web/src`, `docs/`,
> `PRD.md` and `CLAUDE.md` found no seventh stale prose account of the route
> set.

## Description

`#0419` fixed `docs/seo.md` and `#0422` fixed `PRD.md` §7.4, so both documents
now describe `internal/seo` accurately. `#0422`'s review then found the drift one
layer down:

**`internal/seo/seo.go`'s package doc (step 2) and `resolve`'s own numbered
priority list both describe four branches. `resolve` implements five** — the
`/archive/{slug}` branch (`archiveRouteMeta`, cache key `archive:` + slug) is
absent from both.

So after two rounds of documentation correction, **the least accurate account of
`resolve` left in the tree is the comment attached to `resolve`.**

Confirmed by the orchestrator: the archive branch is present in the function
body and returns `"archive:" + slug`.

## Two smaller prose slips, recorded rather than bounced by `#0422`'s review

Both were judged not bounce-worthy at the time; exact replacement wording is in
`#0422`'s `## Review notes`.

1. **`PRD.md` §7.4's `og.png` sentence** says the card is served "for a published
   workshop with no `cover_image`". Eligibility is `Status == WorkshopPublished
   && Published` and **ignores `cover_image` entirely** — the route serves a card
   for a workshop that has one too; `cover_image` only wins in the `og:image`
   meta tag. Defensible as a statement of purpose, which is why it was not a
   bounce, but it is not what the code does.
2. **`docs/seo.md`'s "(`PRD.md`'s own convention, §11)"** sends a reader to
   PRD §11, which is *Security and Compliance*. **`CLAUDE.md` §11** is meant.

## Acceptance criteria

1. `resolve`'s doc comment and the package doc's step 2 both describe every
   branch `resolve` implements. **Enumerate them from the function body**, not
   from either document — both documents were wrong in the same way, so agreement
   between them proves nothing.
2. The two prose slips are corrected using the wording in `#0422`'s
   `## Review notes`, or with a stated reason for differing.
3. **Check whether the same omission appears anywhere else.** Two independent
   comments dropped the same branch, which suggests it was added without a sweep
   of the prose around it — look for other accounts of `resolve`, the cache keys,
   or the route set.
4. **No mechanism** (`#0330`/`#0347`/`#0356`/`#0381`). This is comments and prose.
   If you find yourself wanting a guard that comments match code, note that
   `TestNoDocCommentNamesADifferentDeclarationInSameFile` already exists and does
   not cover this, and say what you concluded rather than building.
5. **gofmt stability** — these are doc comments, and §8 records gofmt rewriting
   them, collapsing doubled backticks and `''`. Prove the file hash is unchanged
   across `gofmt -w`, using the non-destructive stdout comparison.
6. Editing `PRD.md` means `./internal/db/...` and **§11's line counts must stay
   exact** — §7.4 is currently 34 lines and the total 1,870; verify with each
   row's own `sed` rather than trusting the guard (`#0113`). Run
   `./internal/seo/...` and `./internal/handlers/...` too. Per §5 name the
   specific tests that ran. No Go-file-plus-line-number citation (`#0352`),
   scanned with `python3` bytes rather than BSD `grep -P`.

## Relation

- Found by: [#0422](0422.md)'s review, after both documents above it were fixed
- Relates to: [#0419](0419.md), [#0422](0422.md) — the two documentation layers
  already corrected
- Relates to: [#0123](0123.md) — which added the archive branch the comments miss
- Relates to: [#0407](0407.md), [#0414](0414.md), [#0418](0418.md),
  [#0420](0420.md), [#0421](0421.md) — the same drift family

## Verification

`uptime` before the run: `load averages: 1.83 1.72 1.81` — well under this
laptop's idle baseline, so no timing failure would be attributable to load.

The shared template (`opencircuit_test_template`, migration 28) is ahead of
this working tree (migration 27), so `check.sh` provisioned
`opencircuit_test_0425` directly from `migrations/` rather than cloning the
template — expected per CLAUDE.md §5a/#0315, not an environment defect.

```
ISSUE=0425 scripts/check.sh go ./internal/seo/... ./internal/db/... ./internal/handlers/...
```

```
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	1.495s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.639s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	5.666s
```

`gofmt -l` clean, skip audit `(no skips)`, `VERIFICATION PASSED`. All three
report real elapsed time, not 0.00s. Database dropped on exit per `check.sh`.

Re-created `opencircuit_test_0425` by hand afterward (`scripts/testdb.sh
create 0425`) to name and run the four specific guard functions this issue
touches, then dropped it again:

```
go test ./internal/db/... -run '^(TestPRDSectionIndexParity|TestPRDSectionIndexTotalMatchesFile)$' -v
--- PASS: TestPRDSectionIndexParity (0.00s)
--- PASS: TestPRDSectionIndexTotalMatchesFile (0.00s)

go test ./internal/handlers/... -run '^(TestNoCommentCitesUnresolvedPathOrSection|TestNoDocCommentNamesADifferentDeclarationInSameFile)$' -v
--- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.14s)
--- PASS: TestNoDocCommentNamesADifferentDeclarationInSameFile (0.05s)
```

**§11's line counts, re-measured against the current tree, not the issue's
stated 1,870/34** (both had already moved by the time this issue was
filed — `#0422`/`#0423` both touched `PRD.md` the same day):

```
wc -l PRD.md                                                  → 1871
sed -n '/^### 7\.4 /,/^#\{2,3\} [0-9]/p' PRD.md | wc -l        → 35 raw → 34
```

Both match `CLAUDE.md`'s section-index table (`1,871` total, `34` for §7.4)
before and after this issue's edit — the wording change inside §7.4's closing
paragraph was rewrapped to hold the same 6-line count, so neither number
moved.

**gofmt stability (§8), proven non-destructively** on `internal/seo/seo.go`:

```
before (on-disk SHA-256):  3de5567405e756c306c84bb2799fbb6cff3d4a43169f3c63a09feec3ec698298
after  (gofmt stdout SHA-256): 3de5567405e756c306c84bb2799fbb6cff3d4a43169f3c63a09feec3ec698298
gofmt -l internal/seo/seo.go → (silent, clean)
```

Identical hash — no doubled backticks or `''` in the new prose to collapse.

Byte-level scans in this pass used `python3` reading bytes, never BSD
`grep -P`. No `scripts/db-reset.sh`, no `scripts/testdb.sh template`. No
production access.

## Review notes

**Bounced 2026-09-05.** Criterion 1 and criterion 2 are correct and verified
independently; **criterion 3's sweep is incomplete**, and it missed an instance
in `internal/seo/seo.go` itself — the file this issue is about — that the
implementer's own grep terms should have surfaced. One additional defect was
*introduced* by the commit. The code commit `356735a` stays in place; the next
pass adds to it.

### What passed

**Criterion 1 — enumerated from the body, independently.** `resolve` has five
top-level branches and seven return points:

1. `r.static[normalizedPath]` hit → key is `normalizedPath` itself.
2. `handlers.WorkshopDetailSlug` matches → `workshopRouteMeta` ok gives
   `"workshop:" + slug`; not-ok returns `cacheKeyFallback` (an early return, it
   does not fall through to branch 3).
3. `handlers.ArchiveDetailSlug` matches → `archiveRouteMeta` ok gives
   `"archive:" + slug`; not-ok returns `cacheKeyFallback`, same early return.
4. `handlers.IsKnownRoute` → `cacheKeyFallback`, `r.fallback`.
5. default → `cacheKeyNotFound`, `r.notFound`.

Five, with those two key prefixes, exactly as the implementer reported.
`resolve`'s rewritten doc comment and the package doc's step 2 both now match
this. `maxCacheEntries`' widened keyspace description is also correct and was a
genuine find.

**Criterion 2 — both prose slips, re-checked against the code.**
`workshopIsCardable` is `Status == WorkshopPublished && Published` and does not
read `CoverImage`, so §7.4's new wording ("for a published workshop, used as its
`og:image` when it has no `cover_image`") is accurate: `CoverImage` only selects
which URL lands in `OGImage`, it does not gate the route. `docs/seo.md` now
reads "`CLAUDE.md` §11 is the index of those sections". Both correct.

**Criterion 5 — gofmt stability, re-proved non-destructively.**
`shasum -a 256 internal/seo/seo.go` and `gofmt internal/seo/seo.go | shasum -a 256`
both give
`3de5567405e756c306c84bb2799fbb6cff3d4a43169f3c63a09feec3ec698298`;
`gofmt -l` is silent. Matches the implementer's claim.

**Criterion 6 — §11's line counts, re-measured with each row's own command.**
`sed -n '/^### 7\.4 /,/^#\{2,3\} [0-9]/p' PRD.md | wc -l` → `35` raw → `34`;
`wc -l PRD.md` → `1871`. `CLAUDE.md`'s table carries `34` for §7.4 and `1,871`
for the total. The rewrap held its line count, as claimed.

### What must be fixed

**1. `defaultStaticRouteMeta`'s doc comment omits `/archive` — in `internal/seo/seo.go`, and it is this exact drift family.**

That doc comment names the route table as "`/`, `/about`, `/workshops`, and
`/subscribe`" per `#0019`'s criteria, then adds a sentence recording that
`/privacy` was added at `#0070`. It never records that `/archive` was added at
`#0123` — yet `/archive` sits in the map directly below it, carrying its own
inline `#0123, PRD §6.8` comment. So the comment accounts for five routes and
the map holds six, and the missing one is the same `#0123` addition, dropped for
the same reason, that this entire issue exists to correct.

Criterion 3 names "the route set" in as many words, and the implementer's own
reported grep terms included "route table" — which is the literal phrase this
doc comment opens with. It was read past.

**Remedy** (comment only, no code change): after the existing `/privacy`
sentence, record the `#0123` addition in the same style the `/privacy` sentence
uses — for example, "`/archive` was added at `#0123` (PRD §6.8) for the same
reason: the archive index is the site's only recurring indexable page."

**2. The commit introduced a new stale claim: "nil until `#0123` lands".**

The package doc's step 2 now reads "the archive source for `/archive/{slug}`
(nil until `#0123` lands -- same fallback)". `#0123` is `resolved`, and
`servePostgres` wires a real `campaignArchiveSEOSource` into `buildSEOSite`;
only `serveDevMode` passes nil, and its own comment says so. So the parenthetical
asserts a future condition that is already past, in a commit whose entire purpose
is removing exactly that kind of claim.

It mirrors the pre-existing "(nil until `#0051`/`#0054` land ...)" clause beside
it, which is stale in the identical way — `#0051` and `#0054` are both
`resolved` too, and the same phrasing appears again in `NewSite`'s doc comment.
That is mitigation for how it happened, not a reason to leave it.

**Remedy**: state the real condition rather than an issue that has landed — the
source is nil only under `STORAGE=json`, where `internal/devstore` has no
`email_campaigns` backing. Correcting the adjacent `WorkshopSource` clause in
the same edit is **explicitly in scope and preferred**, since fixing one half of
a parallel pair and leaving the other is worse than fixing neither. `NewSite`'s
copy of the phrase is also fair game in the same pass.

**3. The `seo_test.go` call was judged wrong — it is partly this family.**

The implementer reported `TestRender_StaticRoutesGetDistinctTitles`' doc comment
as a stale count from `#0070`'s `/privacy`, a different drift family, and left
it. Reading it against the route table shows it is stale **twice**: it names
"`/`, `/about`, `/workshops`, `/subscribe`" and "the four", while the test's own
`paths` slice has five (it adds `/privacy`) and the compiled-in table has six
(it adds `/archive`). The `/privacy` half is indeed `#0070`'s family. **The
`/archive` half is `#0123`'s — this issue's.** So the call was half right, and
the half it missed is the half in scope.

The same shape appears a third time:
`TestSitemap_ContainsStaticRoutesWithLastmod`'s doc comment says "each of the
four static marketing routes" while `marketingRoutes` holds six.

**Remedy** (doc comments only): correct both doc comments to describe the route
sets their subjects actually cover. **Do not change either test body** — the
fact that neither test exercises `/archive` is a coverage gap, not prose, and is
out of scope for a comments-and-prose issue. It is reported below for filing
instead.

### Criterion 4 — agreed, no mechanism

The implementer's conclusion is right and is the one this review reached
independently. `TestNoDocCommentNamesADifferentDeclarationInSameFile` detects a
doc comment attached to the wrong declaration, not one gone stale against its
own declaration's body. A guard for the latter would have to compare a
hand-maintained enumeration against an AST branch count in `resolve`, which is
more machinery than the prose justifies — and, per `CLAUDE.md` §8's rule about
where a guard's proof belongs, its oracle would be a copy of the answer stored
next to the question. Recorded, not built.

### For the orchestrator to file (§9 — not filed here)

- **Neither static-route test covers `/archive`.**
  `TestRender_StaticRoutesGetDistinctTitles`' `paths` slice and
  `TestSitemap_ContainsStaticRoutesWithLastmod`'s `want` map each omit
  `/archive`, so `#0123`'s static route entry and its sitemap line are
  unasserted — a distinct-title collision or a dropped sitemap `<loc>` for
  `/archive` would pass both. This is the coverage half of finding 3, deliberately
  left out of this issue's scope.
- **The "nil until `#NNNN` lands" idiom has gone stale across `internal/seo`.**
  Finding 2 requires the instances in `internal/seo/seo.go`'s package doc; the
  copy in `NewSite`'s doc comment is the same defect and is offered to this pass,
  but a sweep for the idiom across the package may be worth its own issue if more
  turn up.

### Review verification

`uptime` before the run: `load averages: 2.36 2.63 2.28` — under this laptop's
idle baseline, so nothing timing-related here is attributable to load.

```
ISSUE=0425r scripts/check.sh go ./internal/seo/... ./internal/db/... ./internal/handlers/...
```

```
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	1.530s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.533s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	5.312s
```

`go build`, `go vet` and `gofmt -l` clean; skip audit `(no skips)`; all three
packages report real elapsed time, not 0.00s; `VERIFICATION PASSED`. The
database was dropped by `check.sh` on exit, re-created by hand with
`scripts/testdb.sh create 0425r` to name individual tests, and dropped again.

**Specific tests named, run individually rather than by prefix.** Written after
this section was drafted and re-run against it, since both issue-prose guards
scan only `open`/`in-progress` files and this file is `open` — so these results
cover the notes above, not a pre-edit tree:

- `internal/db` — `TestPRDSectionIndexParity`,
  `TestPRDSectionIndexTotalMatchesFile` (criterion 6's two counts),
  `TestNoOpenIssueEvidenceCitesGoFileByLineNumber` and
  `TestNoOpenIssueVerificationCitesUndefinedTestFunction` (this file's own prose,
  `#0352`/`#0384`), all PASS.
- `internal/handlers` — `TestNoCommentCitesUnresolvedPathOrSection`,
  `TestNoCommentCitesGoFileByLineNumber`,
  `TestNoDocCommentNamesADifferentDeclarationInSameFile`, all PASS.
- `internal/seo` — `TestRender_StaticRoutesGetDistinctTitles`,
  `TestSitemap_ContainsStaticRoutesWithLastmod`,
  `TestRender_JSONLD_DistinctCanceledWorkshopsDoNotShareCacheEntry`, all PASS.
  The first two passing while their doc comments are wrong is precisely finding
  3: they assert nothing about `/archive`.

Byte-level scans in this pass used `python3` reading bytes, never BSD `grep -P`.
No `scripts/db-reset.sh`, no `scripts/testdb.sh template`, no production access,
and no file outside `issues/0425.md` was touched — `docs/deployment.md`
(`#0431`) and `issues/0432.md` (`#0432`) were dirty in the shared tree
throughout and were left alone.

## Verification (second implementation pass, 2026-09-05)

`uptime` before the run: `load averages: 3.53 2.94 2.61` — under this
laptop's idle baseline, so nothing timing-related here is attributable to
load.

```
ISSUE=0425b scripts/check.sh go ./internal/seo/... ./internal/db/... ./internal/handlers/...
```

```
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	1.454s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.657s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	5.660s
```

`go build`, `go vet`, and `gofmt -l` clean; skip audit `(no skips)`; all
three packages report real elapsed time, not 0.00s; `VERIFICATION PASSED`.
Database dropped by `check.sh` on exit.

Re-created `opencircuit_test_0425b` by hand (`scripts/testdb.sh create
0425b`) to name and run the specific tests this pass's four corrections
touch, then dropped it again:

```
go test ./internal/seo/... -run '^(TestRender_StaticRoutesGetDistinctTitles|TestSitemap_ContainsStaticRoutesWithLastmod|TestSite_EmittedURLsShareConfiguredHost)$' -v
--- PASS: TestRender_StaticRoutesGetDistinctTitles (0.00s)
--- PASS: TestSite_EmittedURLsShareConfiguredHost (0.01s)
--- PASS: TestSitemap_ContainsStaticRoutesWithLastmod (0.00s)

go test ./internal/handlers/... -run '^(TestNoCommentCitesUnresolvedPathOrSection|TestNoDocCommentNamesADifferentDeclarationInSameFile)$' -v
--- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.13s)
--- PASS: TestNoDocCommentNamesADifferentDeclarationInSameFile (0.05s)

go test ./internal/db/... -run '^(TestPRDSectionIndexParity|TestPRDSectionIndexTotalMatchesFile)$' -v -count=1
--- PASS: TestPRDSectionIndexParity (0.00s)
--- PASS: TestPRDSectionIndexTotalMatchesFile (0.00s)
```

`internal/db`'s two tests are included for parity with the prior pass even
though this pass touched no PRD.md prose (only `.go` doc comments) — §11's
counts are therefore unchanged (34 for §7.4, 1,871 total) and were not
re-measured, since nothing that determines them moved.

**gofmt stability (§8), proven non-destructively** on all four edited files:

```
internal/seo/seo.go          before/after SHA-256: 1ee480d618362444f5d119bb6534ee5622cf616326fdc7fae57aa5ff5f1527a2 (match)
internal/seo/site.go         before/after SHA-256: 474c128cd92229d7a2d1b27b78c88bfbda4b00d11cfec7cb952683e2e1828e22 (match)
internal/seo/seo_test.go     before/after SHA-256: 3b5807646b267042ef8bcb0d177eb5ed8be3bcde8f4ee3f37f40c56987201267 (match)
internal/seo/sitemap_test.go before/after SHA-256: 63873632e4080cb008716f4e622f58154fec859eb48f6021efdfbffdc7283431 (match)
```

`gofmt -l` on all four is silent. `internal/seo/seo.go`'s hash changed from
the prior pass's `3de5567405e756c306c84bb2799fbb6cff3d4a43169f3c63a09feec3ec698298`
because this pass's edits landed in that file (the package doc's step 2 and
`defaultStaticRouteMeta`'s doc comment); the before/after comparison within
this pass is what proves gofmt stability, not equality with the prior hash.

Byte-level scans in this pass used `python3`, never BSD `grep -P`. No
`scripts/db-reset.sh`, no `scripts/testdb.sh template`. No production
access. No file outside this pass's four edited `.go` files and
`issues/0425.md` was touched; `issues/0431.md` (`#0431`) was dirty in the
shared tree throughout and was left alone.

**Re-run after writing this file's own prose**, since both issue-prose
guards scan only `open`/`in-progress` files and this file is `in-progress`
as of this edit -- so this covers the notes above, not a pre-edit tree
(`scripts/testdb.sh create 0425c` / `drop 0425c`):

```
go test ./internal/db/... -run '^(TestNoOpenIssueEvidenceCitesGoFileByLineNumber|TestNoOpenIssueVerificationCitesUndefinedTestFunction)$' -v -count=1
--- PASS: TestNoOpenIssueVerificationCitesUndefinedTestFunction (0.25s)
--- PASS: TestNoOpenIssueEvidenceCitesGoFileByLineNumber (0.01s)

go test ./internal/handlers/... -run '^(TestNoCommentCitesUnresolvedPathOrSection|TestNoCommentCitesGoFileByLineNumber|TestNoDocCommentNamesADifferentDeclarationInSameFile)$' -v -count=1
--- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.15s)
--- PASS: TestNoCommentCitesGoFileByLineNumber (0.09s)
--- PASS: TestNoDocCommentNamesADifferentDeclarationInSameFile (0.05s)
```

## Review notes (second review, 2026-09-05)

**Bounced 2026-09-05.** All four remedies from the first review are correctly
applied and independently verified, and the beyond-remedy `STORAGE=json` rewrite
is right on the facts. But **criterion 3 is still not satisfied**: a further
account of the same route set, in `internal/seo/sitemap.go`, still omits
`/archive`. That is the **sixth** wrong account this one route set has produced,
and it is the direct counterpart of the first review's finding 1. The code commit
`442a4e0` stays in place; the next pass adds one sentence to it.

### What passed, re-verified from the code rather than from either document

**Remedy 1 — `defaultStaticRouteMeta`'s doc comment.** Enumerated the map's keys
from the function body: `/`, `/about`, `/privacy`, `/workshops`, `/subscribe`,
`/archive` — six. The doc comment now records `/archive`'s addition at `#0123`
(PRD §6.8) in the same style as the `/privacy`/`#0070` sentence. Correct.

**Remedy 2 — the `nil until #NNNN lands` clauses, and the beyond-remedy
rewrite.** The pass went past what was prescribed: instead of refreshing the
issue references it replaced them with the actual condition, reusing the wording
`buildSEOSite`'s own doc comment already carries. **The new claim was checked and
is true**, end to end:

- `config.DevMode()` returns `c.Storage == "json"`, and `Storage` is read
  straight from `os.Getenv("STORAGE")` — so "under `STORAGE=json`" is exact, not
  approximate.
- `serve` dispatches on `DevMode()` alone; its own comment records that dev mode
  is engaged ONLY by an explicit `STORAGE=json`.
- `servePostgres` passes `workshopSEOSource{store: workshopsStore}` and
  `campaignArchiveSEOSource{store: campaignsStore}` — both struct values, so both
  interfaces are non-nil unconditionally on that path, independent of whether the
  underlying stores are.
- `serveDevMode` is the only other caller of `buildSEOSite`, and passes
  `nil, nil`.
- `buildSEOSite` is the only non-test constructor of a `seo.Site`.
- The two backing tables named are the right ones: `workshops.Store` reads
  `workshops`; `mailing.CampaignStore`'s `GetBySlug`/`ListArchived`, which
  `campaignArchiveSEOSource` wraps, read `email_campaigns`. `internal/devstore`
  implements neither (its own package doc records campaign and workshop fakes as
  not yet added), matching CLAUDE.md §5's note that `STORAGE=json` has no mailing
  list.

So "nil only under `STORAGE=json`" is accurate for both sources. **The rewrite is
endorsed over the remedy that was actually prescribed**: a condition cannot go
stale the way an issue reference can, which is the defect class this whole issue
exists to close, and it now agrees with `buildSEOSite`'s wording instead of
competing with it. Going beyond a remedy is normally a scope problem; here it
removed the recurrence rather than deferring it, and it was disclosed clearly.

One nuance, recorded and deliberately **not** bounced: `NewSite`'s parameters are
exported seam arguments that tests pass `nil` to freely, so "nil only under
`STORAGE=json`" is a statement about the deploy wiring, not about the
constructor's contract. The doc comment already carries the contract half — it
calls both arguments "optional" and says the `ArchiveSource` "mirrors
WorkshopSource's own nil-tolerant contract" — so the two halves are both present
and a reader is not misled. No change wanted.

**Remedies 3 and 4 — the two test doc comments, and the test bodies.** Both are
accurate against their own fixtures:

- `TestRender_StaticRoutesGetDistinctTitles`' `paths` slice holds exactly `/`,
  `/about`, `/privacy`, `/workshops`, `/subscribe` — five, matching the comment's
  "all five", and the route table's sixth entry `/archive` is indeed not in it.
- `TestSitemap_ContainsStaticRoutesWithLastmod`' `want` map holds the same five
  keys, and `marketingRoutes` does hold a sixth, `/archive`.

**Both test bodies are provably untouched.** Every added and removed line in
`442a4e0` across all four files is a comment line — filtering the commit's diff
to changed lines that are not `//` comments yields the empty set. The coverage
gap stays `#0437`'s.

**gofmt stability, re-proved non-destructively** (`gofmt` stdout hashed against
on-disk bytes; nothing rewritten, since other agents are working):

```
internal/seo/seo.go          1ee480d618362444f5d119bb6534ee5622cf616326fdc7fae57aa5ff5f1527a2  match
internal/seo/site.go         474c128cd92229d7a2d1b27b78c88bfbda4b00d11cfec7cb952683e2e1828e22  match
internal/seo/seo_test.go     3b5807646b267042ef8bcb0d177eb5ed8be3bcde8f4ee3f37f40c56987201267  match
internal/seo/sitemap_test.go 63873632e4080cb008716f4e622f58154fec859eb48f6021efdfbffdc7283431  match
```

All four match the implementer's reported values; `gofmt -l internal/seo/` is
silent.

### What must be fixed

**`marketingRoutes`' own doc comment omits `/archive` — `internal/seo/sitemap.go`.**

The doc comment attached to `marketingRoutes` describes the sitemap's curated
static-route portion as `#0020`'s acceptance criteria — `"/"`, `"/about"`,
`"/workshops"`, `"/subscribe"` — then records `"/privacy"` as added at `#0070`
"for the same reason". It never records `/archive`. The declaration it documents,
on the line immediately below it, is a six-element slice ending in `"/archive"`.
Verified by reading the comment span and the declaration line as separate byte
ranges: the comment contains no `/archive`, the declaration does.

This is the same defect as the first review's finding 1, in the counterpart
declaration. `defaultStaticRouteMeta` (the renderer's route table) and
`marketingRoutes` (the sitemap's) are the parallel pair; the first review caught
the renderer half and this pass fixed it, while the sitemap half was left. The
first review's own reasoning against half-fixing a parallel pair applies here
verbatim.

It is also squarely inside criterion 3, which names "the route set" in as many
words — and inside the previous pass's own stated scope, since that pass wrote
"`marketingRoutes` also holds a sixth entry, `/archive` (`#0123`)" into
`sitemap_test.go`'s doc comment in this very commit. **The correct fact was
written into the test file while the declaration's own comment continued to state
the wrong one, in the same package, in the same pass.**

Why both sweeps missed it, which matters for the next one: this comment contains
**no `#0123`** — that absence *is* the defect — and **no count word**; it
enumerates the routes as literal quoted strings and never says "four". The
previous pass swept on exactly those two axes (`#0123` occurrences, and
"four"-shaped counts), so both were blind to it by construction. Despite being
described as read-based, that sweep still chose what to read by matching. The net
that catches this class is to search for a **member of the set** that any
enumeration must contain — `/subscribe` works, being in every version of the list
since `#0019` — and then read every comment that mentions three or more of the
routes. Run that way, the surviving stale enumeration in `internal/seo` is this
one and only this one.

**Remedy** (comment only, no code change): in `marketingRoutes`' doc comment,
after the existing `"/privacy"`/`#0070` clause, record the `#0123` addition in the
same style — for example, `"/archive" was added at #0123 (PRD §6.8), the archive
index being a real, indexable public page for the same reason.` Do not touch the
declaration, and do not touch any test body.

### Sweep result — the rest of the family is clean

Re-run independently, netting on `/subscribe` and then reading every comment
window naming three or more marketing routes, across `internal/seo`,
`cmd/opencircuit`, `internal/handlers`, `docs/seo.md`, and `PRD.md`. Besides
`marketingRoutes` above, every other account is correct: `defaultStaticRouteMeta`
and both test doc comments (fixed by this commit), `docs/seo.md`'s two
enumerations (both list `/archive`), and PRD §7.4. `defaultFallbackMeta`'s comment
lists the Phase 1 auth routes, a different set, and is accurate.
`TestSite_EmittedURLsShareConfiguredHost`'s "all four" counts emission points, not
routes — correctly stated, as the previous pass said.

### Criterion 4 — agreed, no mechanism

Third independent agreement. `TestNoDocCommentNamesADifferentDeclarationInSameFile`
catches a doc comment attached to the wrong declaration, not one stale against its
own declaration's contents. Worth recording, though, that the finding above is a
*harder* case than it first looks and is the reason not to reach for a guard here:
a checker for it would have to compare a hand-maintained enumeration in prose
against a slice literal, and per CLAUDE.md §8's rule on where a guard's proof
belongs, its oracle would be a copy of the answer stored next to the question.
Recorded, not built.

### For the orchestrator to file (§9 — not filed here)

- **The stale `nil until #NNNN` idiom in `internal/seo`, now with two concrete
  instances.** The previous reviewer said this "may be worth its own issue if more
  turn up". More turned up, so the branch it named has been taken. `NewRenderer`'s
  doc comment (`internal/seo/seo.go`) still says the source may be nil "until
  #0051/#0054 wire a real workshop store", and — not reported by the previous
  pass, and the worse of the two — the `WorkshopSource` interface's own doc comment
  (`internal/seo/workshop.go`) states that `#0051` "has not landed yet" and that
  `#0054` "is expected to wire the real implementation in". Both `#0051` and
  `#0054` are `resolved`, and `workshopSEOSource` is the real implementation
  `#0054` wired. These are not merely stale references: they assert a state of the
  world that is false, and `NewRenderer`'s now contradicts the package doc twelve
  declarations above it in the same file. Two quotations of `#0020`'s Notes
  ("Until #0051 lands, the workshop portion of the sitemap can be empty"), in
  `internal/seo/workshop.go` and `internal/seo/sitemap_test.go`, are **correct as
  attributed quotations** of an issue's Notes and describe behaviour that still
  holds — they should be left alone.
- **`TestRender_JSONLD_StaticRoutesHaveNoBlock` claims a completeness it does not
  have.** Its doc comment says the token is removed "for every static marketing
  route" while its loop covers five of the six plus `/admin`, omitting `/archive`.
  Reported here rather than made a remedy because the honest fix is to add
  `/archive` to the slice — a test-body change, which is `#0437`'s coverage
  territory, not this comments-and-prose issue's. Worth folding into `#0437`
  rather than filing separately.

### Escalation (CLAUDE.md §3) — not warranted

This is the second bounce, but not two bounces on the same design problem, and
§3's test is whether the next pass must **decide** or merely **apply**. The remedy
above is one sentence, its exact wording is supplied, the file and declaration are
named, and the verification is already proved green. That is transcription.
**Stay on Sonnet.** Recorded per §3's requirement that a decision not to escalate
be logged like a decision to escalate.

Noting for the record what the two bounces have in common, since it is a scope
signal rather than a capability one: both are criterion 3 sweep misses, and both
were missed the same way — by choosing what to read with a matcher keyed to the
very token whose absence is the defect. The remedy is the sweep method named
above, not a larger model.

### Review verification

`uptime` before the run: `load averages: 3.11 3.37 2.91` — under this laptop's
idle baseline, so nothing timing-related here is attributable to load.

```
ISSUE=0425r2 scripts/check.sh go ./internal/seo/... ./internal/db/... ./internal/handlers/...
```

```
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	1.325s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.517s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	6.422s
```

`go build`, `go vet` and `gofmt -l` clean; skip audit `(no skips)`; all three
packages report real elapsed time, not 0.00s; `VERIFICATION PASSED`. The database
`opencircuit_test_0425r2` was provisioned from `migrations/` (the shared template
is at 28, ahead of this tree's 27 — expected, not a defect) and dropped by
`check.sh` on exit.

No `PRD.md` prose changed in `442a4e0` or in this review, so §11's counts cannot
have moved; `TestPRDSectionIndexParity` and `TestPRDSectionIndexTotalMatchesFile`
were run anyway and pass.

Byte-level scans in this pass used `python3` reading bytes, never BSD `grep -P`.
No `scripts/db-reset.sh`, no `scripts/testdb.sh template`, no production access.
No file outside `issues/0425.md` was touched — `issues/0431.md` (`#0431`) was
dirty in the shared tree throughout and was left alone.

## Verification (third implementation pass, 2026-09-05)

`uptime` before the run: `load averages: 3.29 3.24 2.98` — under this laptop's
idle baseline, so nothing timing-related here is attributable to load.

**Sweep, done the way the second review's notes prescribed**: grepped for the
set member `/subscribe` (a member of every version of this route set since
`#0019`) across `internal/seo/*.go`, then read every comment window naming
three or more marketing routes, rather than matching on `#0123` or a count
word. Hits: `internal/seo/seo.go` (`defaultStaticRouteMeta`'s doc comment and
the map literal itself — already correct, fixed by the prior pass),
`internal/seo/seo_test.go` (three doc comments/literals — already correct or
not route-set enumerations: `TestRender_StaticRoutesGetDistinctTitles` and the
package const are correct; `TestRender_JSONLD_StaticRoutesHaveNoBlock` and
`TestRender_AllTokensSubstituted` use literal path slices with no enumerating
doc comment and no count claim, so they are not this drift family — the
`/archive` gap in the former is `#0437`'s coverage territory, not prose),
`internal/seo/sitemap.go` (`marketingRoutes`' doc comment — **the one
remaining defect**, fixed below), `internal/seo/sitemap_test.go` (already
correct, fixed by the prior pass). No other file in `internal/seo` mentions
`/subscribe`. Result: **one hit, exactly the one the review named**; nothing
else in the family is stale.

```
ISSUE=0425c scripts/check.sh go ./internal/seo/... ./internal/db/... ./internal/handlers/...
```

First run failed on an unrelated timing-sensitive test,
`TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch` (from `#0297`, a
different subsystem entirely — subscribe-intake claiming, not SEO), with
`blockingSuppressionChecker.IsSuppressed was never called`. `uptime` at the
time was `2.49–3.29` across the run, well under this laptop's ~8 busy
threshold, so §5's "check the machine" rule does not explain it either; it
reproduced as a pass in isolation
(`go test ./internal/handlers/... -run
'^TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch$' -v` →
`--- PASS (5.06s)`, single execution, not a `-count=N` retry) and as a pass
again when the full three-package command was re-run once more:

```
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	1.208s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.375s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	5.573s
```

`go build`, `go vet`, `gofmt -l` clean; skip audit `(no skips)`; all three
report real elapsed time, not 0.00s; `VERIFICATION PASSED`. Database
`opencircuit_test_0425c` dropped by `check.sh` on exit (per this pass's
required suffix).

Re-created `opencircuit_test_0425c` by hand to name and run the specific
tests this pass's one-sentence correction touches, then dropped it again:

```
go test ./internal/seo/... -run '^(TestSitemap_MarketingRoutesAreKnownRoutes|TestSitemap_ContainsStaticRoutesWithLastmod|TestRender_StaticRoutesGetDistinctTitles)$' -v -count=1
--- PASS: TestRender_StaticRoutesGetDistinctTitles (0.00s)
--- PASS: TestSitemap_MarketingRoutesAreKnownRoutes (0.00s)
--- PASS: TestSitemap_ContainsStaticRoutesWithLastmod (0.00s)

go test ./internal/handlers/... -run '^(TestNoCommentCitesUnresolvedPathOrSection|TestNoDocCommentNamesADifferentDeclarationInSameFile)$' -v -count=1
--- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.15s)
--- PASS: TestNoDocCommentNamesADifferentDeclarationInSameFile (0.05s)

go test ./internal/db/... -run '^(TestPRDSectionIndexParity|TestPRDSectionIndexTotalMatchesFile)$' -v -count=1
--- PASS: TestPRDSectionIndexParity (0.00s)
--- PASS: TestPRDSectionIndexTotalMatchesFile (0.00s)
```

No `PRD.md` prose touched this pass, so §11's counts (34 for §7.4, 1,871
total) are unchanged and `internal/db`'s two tests are run for parity only,
same as the prior two passes.

**gofmt stability (§8), proven non-destructively** — `internal/seo/sitemap.go`
had no prior baseline in this issue's history, so it was established first:

```
sitemap.go before-edit (on-disk SHA-256):    89cbb310a7f4875b7ff276e18515e25547c347fca2cb5f6eab44a944276ffd49
sitemap.go before-edit (gofmt stdout SHA-256): 89cbb310a7f4875b7ff276e18515e25547c347fca2cb5f6eab44a944276ffd49  (match, pre-edit baseline)
sitemap.go after-edit  (on-disk SHA-256):    562855f72e601ab1588f85f4261ce097ad3494cd3524c3e6efa7610c72d16abf
sitemap.go after-edit  (gofmt stdout SHA-256): 562855f72e601ab1588f85f4261ce097ad3494cd3524c3e6efa7610c72d16abf  (match)
```

`gofmt -l internal/seo/sitemap.go` silent both before and after. The one
inserted sentence used no doubled backticks or `''`.

**Test bodies untouched** — `git show b85874c` touches only
`internal/seo/sitemap.go`, and every changed line in it is a `//` comment
line (3 insertions, 1 deletion, confirmed via `git show --stat HEAD`).

Byte-level scans in this pass used `python3` reading bytes, never BSD
`grep -P`. No `scripts/db-reset.sh`, no `scripts/testdb.sh template`, no
production access. No file outside `internal/seo/sitemap.go` (committed) and
this issue file (left uncommitted) was touched — `docs/deployment.md`
(`#0431`) was dirty in the shared tree throughout this pass and was left
alone.

## Review notes (third review, 2026-09-05)

**Passed 2026-09-05.** `b85874c` fixes exactly the one thing the second review
bounced, introduces no new claim that is false, and touches nothing else. Every
finding below was re-derived from the code rather than taken from the
implementer's report or from the brief.

### The comment against the slice, member by member

Read as two separate byte ranges — the contiguous `//` block immediately above
the declaration, and the declaration line itself — and compared as sets rather
than by eye:

| Member of the slice | Named in the doc comment | Attributed to |
|---|---|---|
| `"/"` | yes | `#0020`'s acceptance criteria |
| `"/about"` | yes | `#0020`'s acceptance criteria |
| `"/privacy"` | yes | `#0070` |
| `"/workshops"` | yes | `#0020`'s acceptance criteria |
| `"/subscribe"` | yes | `#0020`'s acceptance criteria |
| `"/archive"` | **yes — the fix** | `#0123` (PRD §6.8) |

Six in the slice, six in the comment, and the set difference is empty in **both**
directions — nothing is named in the prose that is absent from the slice either,
which is the failure mode a one-way check would miss. The comment's ordering
(the four `#0020` routes, then `/privacy`, then `/archive`) differs from the
slice's, but the comment is organised by provenance rather than by position, so
that is not a discrepancy.

**The new sentence's claim was checked, not assumed.** "the archive index is the
site's only recurring indexable page" is PRD §6.8's own first reason for the
archive existing ("It is the only recurring indexable content the site will
have"), and it is the identical clause `defaultStaticRouteMeta`'s doc comment
already carries for the same route — so the pass's "same style" claim is literal,
not approximate. That matters given the second bounce: this pass had the standing
opportunity to introduce a seventh wrong account and did not.

**The rest of the same comment re-checked while I was in it.** Its second
paragraph claims the excluded Phase 1 routes are `/login`, `/account`, `/admin`,
`/confirm`, `/preferences`, `/unsubscribe`, `/register/verify`, `/recover/verify`
and `/subscribe/thanks`. Computed `knownStaticRoutes` minus `marketingRoutes`
from the two literals: exactly those nine, no more and no fewer. Its closing
claim that every entry is a `handlers.IsKnownRoute` member also holds — all six
are keys of `knownStaticRoutes`, and `TestSitemap_MarketingRoutesAreKnownRoutes`
asserts it.

### My own sweep — run wider than the previous three, and one hit they all missed

The previous two sweeps were blind by construction (matching on `#0123`, then on
count words). The third pass used the review's prescribed net — grep a set member
that every enumeration must contain, then read every comment window naming three
or more routes — but scoped it to `internal/seo`. I ran a **superset** of both:
netting on the bytes `/subscribe` (122 files) *and*, independently, a
six-line-window scan flagging any window naming three or more of
`/about`, `/privacy`, `/workshops`, `/subscribe`, `/archive`, over 427 files
across `internal/`, `cmd/`, `web/src/`, `docs/`, `PRD.md` and `CLAUDE.md`. The
window scan does not depend on any single member being present, so it also
catches an enumeration that drops `/subscribe` itself. 18 windows; each read.

**`marketingRoutes` was indeed the only survivor in `internal/seo`** — the third
pass's report is confirmed. Everything else there is correct:
`defaultStaticRouteMeta` and both static-route test doc comments (fixed by
`442a4e0`), and `TestRender_AllTokensSubstituted`, whose path slice carries no
enumerating doc comment and so is not this family. Outside `internal/seo`,
`docs/seo.md`'s two enumerations, PRD §5.1 and §7.4, `knownStaticRoutes`,
`router.ts`'s `STATIC_ROUTES` and `router.test.ts`'s case table all name
`/archive` correctly; `Header.svelte`'s `NAV_LINKS` is the nav set, a different
and smaller one, with no completeness claim attached.

**One hit outside `internal/seo` that all three prior sweeps missed**, reported
rather than bounced, for the reason given below: `TestIsKnownRoute_StaticRoutes`
in `internal/handlers/routes_test.go`. Its doc comment says it covers "every
static path in PRD §5.1 plus the Phase 1 auth routes"; §5.1's static (non-slug)
paths are `/`, `/workshops`, `/archive`, `/about`, `/subscribe`,
`/subscribe/thanks`, `/confirm`, `/preferences`, `/unsubscribe`, `/login`,
`/register/verify`, `/recover/verify`, and the test's `known` slice omits exactly
one of them — `/archive`, `#0123`'s addition. So it is this drift family, in
another package.

**Why that is not a third bounce.** It is byte-for-byte the same shape as
`TestRender_JSONLD_StaticRoutesHaveNoBlock`, which the second review examined and
deliberately routed to `#0437` instead of making it a remedy: the honest fix is
adding `/archive` to the slice, a **test-body change**, and both prior reviews
carved test bodies out of this comments-and-prose issue in as many words. Editing
the doc comment instead would record the coverage gap as intended behaviour,
which is worse than leaving it. Applying that precedent consistently is what the
issue's own scope requires. Reported below for `#0437`.

**One borderline case examined and deliberately not called.**
`TestStaticRoutes_ContainsMarketingPaths`' doc comment says the exported route
list "includes the four marketing paths those issues curate a sitemap subset
from". Its `want` slice holds exactly those four, so the comment is accurate
about its own subject; the count is attributed to `#0019`/`#0020`, at which point
the curated set genuinely was four. That places it with the attributed
quotations of `#0020`'s Notes the second review ruled correct and to be left
alone, not with `TestSitemap_ContainsStaticRoutesWithLastmod`'s original "the
four", which contradicted its own five-entry `want` map and was rightly fixed.
The distinguishing test both earlier reviews used — does the comment describe the
set its own subject actually covers — is satisfied here. No change wanted.

### Out-of-scope groups, confirmed untouched

`b85874c` touches one file, `internal/seo/sitemap.go`, 3 insertions and 1
deletion, and every changed line is a `//` comment line. Confirmed by hashing the
other four files this issue has edited across its three passes against their
`442a4e0` blobs: `internal/seo/seo.go`, `internal/seo/site.go`,
`internal/seo/seo_test.go` and `internal/seo/sitemap_test.go` are all
byte-identical. Therefore:

- **`#0439`'s territory is intact.** `NewRenderer`'s doc comment still reads
  "until `#0051`/`#0054` wire a real workshop store", and the `WorkshopSource`
  interface's doc comment still asserts `#0051` "has not landed yet" and that
  `#0054` "is expected to wire the real implementation in". Both false, both
  deliberately left.
- **`#0437`'s territory is intact.** `TestRender_StaticRoutesGetDistinctTitles`'
  `paths` slice and `TestSitemap_ContainsStaticRoutesWithLastmod`' `want` map
  each still hold five entries and omit `/archive`. No test body was touched.
- **Both attributed quotations of `#0020`'s Notes are untouched** — the literal
  one in `internal/seo/workshop.go` ("Until #0051 lands, the workshop portion of
  the sitemap can be empty") and the paraphrase in `internal/seo/sitemap_test.go`
  ("until #0051 lands, the workshop portion can be empty"). Both remain correct
  as attributions and describe behaviour that still holds.

### Criterion 4 — agreed, no mechanism

Fourth independent agreement, and nothing about this pass changes the reasoning:
a checker would compare a hand-maintained prose enumeration against a slice
literal, and its oracle would be a copy of the answer stored next to the question
(`CLAUDE.md` §8). Not built. Worth recording that the sweep above is the cheaper
substitute and found the remaining instance in seconds.

### For the orchestrator to file (§9 — not filed here)

- **Fold `TestIsKnownRoute_StaticRoutes` into `#0437`.** Its `known` slice omits
  `/archive` while its doc comment claims to cover every static path in PRD §5.1.
  This is the third member of `#0437`'s set, alongside
  `TestRender_StaticRoutesGetDistinctTitles`,
  `TestSitemap_ContainsStaticRoutesWithLastmod` and the
  `TestRender_JSONLD_StaticRoutesHaveNoBlock` item the second review already
  routed there. It is in `internal/handlers`, not `internal/seo`, which is why
  three sweeps scoped to `internal/seo` did not see it — worth saying in `#0437`
  so its implementer widens the package scope rather than repeating the miss.
- Nothing else. `#0439` already covers the stale `nil until #NNNN` idiom, and no
  new instance turned up in this sweep.

### Escalation (CLAUDE.md §3) — moot, and would not have been warranted

This pass passed, so no third bounce and no escalation decision is forced.
Recording the judgement anyway, since the brief asked for it explicitly: had a
bounce been warranted it would still not have justified escalating to Opus. Both
prior bounces were a **method** fault, not a capability one — each sweep chose
what to read with a matcher keyed to the very token whose absence was the defect
— and both remedies were a single supplied sentence, which is §3's definition of
transcription. The third pass applied the corrected method and got the
`internal/seo` result exactly right; its only residual gap was the package scope
of the sweep, which is again a method parameter and is now recorded above for
`#0437`. Sonnet was the right tool for all three passes.

### Review verification

`uptime` before the run: `load averages: 4.52 4.00 3.38` — under this laptop's
~8 busy threshold per `CLAUDE.md` §5, so nothing timing-related here is
attributable to load. The `#0297` subscribe-intake failure the third
implementation pass saw did **not** recur: the first run of the command below
was clean, first time.

```
ISSUE=0425r3 scripts/check.sh go ./internal/seo/... ./internal/db/... ./internal/handlers/...
```

```
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	1.357s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.642s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	5.642s
```

`go build`, `go vet` and `gofmt -l` clean; skip audit `(no skips)`; all three
packages report real elapsed time, not 0.00s; `VERIFICATION PASSED`. The
database `opencircuit_test_0425r3` was provisioned from `migrations/` (the
shared template is at 28, ahead of this tree's 27 — expected per §5a, not a
defect) and dropped by `check.sh` on exit.

**Specific test functions, named and run individually rather than by prefix**,
against a hand-created `opencircuit_test_0425r3` dropped again afterwards:

- `internal/seo` — `TestSitemap_ContainsStaticRoutesWithLastmod`,
  `TestSitemap_MarketingRoutesAreKnownRoutes` (the assertion that every
  `marketingRoutes` member is a `handlers.IsKnownRoute`, which is what makes the
  comment's closing claim checkable), `TestRender_StaticRoutesGetDistinctTitles`.
- `internal/handlers` — `TestNoCommentCitesUnresolvedPathOrSection`,
  `TestNoCommentCitesGoFileByLineNumber`,
  `TestNoDocCommentNamesADifferentDeclarationInSameFile`,
  `TestIsKnownRoute_StaticRoutes`, `TestStaticRoutes_ContainsMarketingPaths`
  (the two whose doc comments this review examined above).
- `internal/db` — `TestPRDSectionIndexParity`,
  `TestPRDSectionIndexTotalMatchesFile`,
  `TestNoOpenIssueEvidenceCitesGoFileByLineNumber`,
  `TestNoOpenIssueVerificationCitesUndefinedTestFunction`.

Results:

```
go test ./internal/db/... -run '^(TestNoOpenIssueEvidenceCitesGoFileByLineNumber|TestNoOpenIssueVerificationCitesUndefinedTestFunction|TestPRDSectionIndexParity|TestPRDSectionIndexTotalMatchesFile)$' -v -count=1
--- PASS: TestNoOpenIssueVerificationCitesUndefinedTestFunction (0.12s)
--- PASS: TestNoOpenIssueEvidenceCitesGoFileByLineNumber (0.01s)
--- PASS: TestPRDSectionIndexParity (0.00s)
--- PASS: TestPRDSectionIndexTotalMatchesFile (0.00s)

go test ./internal/handlers/... -run '^(TestNoCommentCitesUnresolvedPathOrSection|TestNoCommentCitesGoFileByLineNumber|TestNoDocCommentNamesADifferentDeclarationInSameFile|TestIsKnownRoute_StaticRoutes|TestStaticRoutes_ContainsMarketingPaths)$' -v -count=1
--- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.14s)
--- PASS: TestNoCommentCitesGoFileByLineNumber (0.10s)
--- PASS: TestNoDocCommentNamesADifferentDeclarationInSameFile (0.05s)
--- PASS: TestIsKnownRoute_StaticRoutes (0.00s)
--- PASS: TestStaticRoutes_ContainsMarketingPaths (0.00s)

go test ./internal/seo/... -run '^(TestSitemap_ContainsStaticRoutesWithLastmod|TestSitemap_MarketingRoutesAreKnownRoutes|TestRender_StaticRoutesGetDistinctTitles)$' -v -count=1
--- PASS: TestRender_StaticRoutesGetDistinctTitles (0.00s)
--- PASS: TestSitemap_MarketingRoutesAreKnownRoutes (0.00s)
--- PASS: TestSitemap_ContainsStaticRoutesWithLastmod (0.00s)
```

This block was written before the run above, and the two issue-prose guards were
re-run against this file afterwards while its status was still `in-progress`,
since both skip `resolved` files — so their pass covers these notes, not a
pre-edit tree. The status flip to `resolved` and the `Closed` row were the only
changes made after that final guard run, and neither is prose.

**§11's line counts.** No `PRD.md` prose changed in `b85874c` or in this review,
so they cannot have moved; re-measured with each row's own command anyway rather
than trusting the guard (`#0113`): `wc -l PRD.md` → `1871`, and
`sed -n '/^### 7\.4 /,/^#\{2,3\} [0-9]/p' PRD.md | wc -l` → `35` raw → `34`.
`CLAUDE.md`'s table carries `1,871` and `34`. Match.

**gofmt stability (§8), re-proved non-destructively** — `gofmt` stdout hashed
against the on-disk bytes, nothing rewritten, since other agents are working:

```
internal/seo/sitemap.go on-disk       SHA-256: 562855f72e601ab1588f85f4261ce097ad3494cd3524c3e6efa7610c72d16abf
internal/seo/sitemap.go gofmt stdout  SHA-256: 562855f72e601ab1588f85f4261ce097ad3494cd3524c3e6efa7610c72d16abf  (match)
gofmt -l internal/seo/sitemap.go → (silent)
```

This is the third pass's reported post-edit value, independently reproduced. Its
pre-edit baseline (`89cbb310…ffd49`) was not re-derived here, since the commit's
parent blob is in git and the diff is three comment lines; the post-edit hash is
what proves the tree as it stands is gofmt-stable.

Byte-level scans in this review used `python3` reading bytes, never BSD
`grep -P`. No `scripts/db-reset.sh`, no `scripts/testdb.sh template`, no
production access. No file outside `issues/0425.md` was touched —
`issues/0431.md` (`#0431`) was dirty in the shared tree throughout and was left
alone, as were `docs/deployment.md` and everything else under `#0431`.


## Work log

- 2026-09-04 — Filed by the orchestrator from `#0422`'s review (§9), with the
  archive branch confirmed present in `resolve`'s body before filing.
- 2026-09-04 — Implemented (phase 2). Enumerated `resolve`'s branches
  directly from `internal/seo/seo.go`'s body (not from `docs/seo.md` or
  `PRD.md`, which agreeing proves nothing per criterion 1): (1) exact
  static-route entry, keyed by path; (2) `/workshops/{slug}` match via
  `WorkshopSource`, keyed `"workshop:" + slug`, falling back to
  `cacheKeyFallback` on no match; (3) `/archive/{slug}` match via
  `ArchiveSource` (`#0123`), keyed `"archive:" + slug`, same fallback on no
  match; (4) `handlers.IsKnownRoute` fallback, keyed `cacheKeyFallback`; (5)
  not-found default, keyed `cacheKeyNotFound`. Rewrote the package doc's
  step 2 and `resolve`'s own doc comment to name all five and the two cache
  key prefixes. Folded in both prose slips using #0422's review-notes
  wording verbatim (PRD.md §7.4's og.png sentence; docs/seo.md's PRD §11 →
  CLAUDE.md §11 pointer).

  **Criterion 3 sweep result.** Found one more instance of the identical
  omission: `maxCacheEntries`' doc comment (seo.go, just above `resolve`)
  describes "the realistic keyspace" as static routes + fallback bucket +
  not-found bucket + one entry per published/canceled workshop slug — no
  mention of archive slugs, which also get their own cache key
  (`"archive:" + slug`) per the fix above. This is squarely "the cache
  keys" the acceptance criteria named, so fixed it in the same commit
  rather than only reporting it: added "and one entry per distinct
  published archive slug actually requested (#0123, same bound
  reasoning)."

  Also noticed, judged out of scope: `seo_test.go`'s
  `TestRender_StaticRoutesGetDistinctTitles` doc comment still says the
  static route table covers "/, /about, /workshops, /subscribe, and the
  four must be genuinely different" while the test itself checks five
  paths (`/privacy` included, from `#0070`, predating `#0123`). Same shape
  (a stale count in a doc comment) but a different cause and a different
  drift family — not the archive-branch omission this issue tracks — so
  left it for whoever picks it up; not filed per §9 (subagents report,
  they don't file).

  Checked `cmd/opencircuit/*.go` and the rest of `internal/seo/*.go` for
  any other independent account of `resolve`'s branches, the route set, or
  the cache-key scheme (`grep` for "resolve", "priority order", "route
  table", "static marketing", "IsKnownRoute" across both trees): no other
  hits describe the branch list or keyspace independently — `docs/seo.md`
  and `PRD.md` §7.4 (both `#0419`/`#0422`) already list all five
  correctly, and every other `resolve`/`IsKnownRoute` mention in
  `cmd/opencircuit` and the rest of `internal/seo` is either an unrelated
  identifier (`resolveCallee`, `resolveAdminRoutePath`) or a passing
  reference, not an enumeration.

  Criterion 4: no mechanism built.
  `TestNoDocCommentNamesADifferentDeclarationInSameFile` was checked and,
  as the issue predicted, does not cover this class (it catches a doc
  comment attached to the wrong declaration, not one that is stale
  relative to its own declaration's body) — comments-vs-code drift of this
  shape would need something like a hand-maintained enumeration compared
  against the AST count of `if` branches in `resolve`, which is more
  machinery than five lines of prose justify. Recorded rather than built.

  Set to `in-progress`; commit `356735a` covers `PRD.md`, `docs/seo.md`,
  `internal/seo/seo.go` only, staged and verified via `git diff --cached
  --name-only` before committing and `git show --stat HEAD` after — three
  files, matching this pass's edits exactly. `issues/0425.md` left
  uncommitted for review per phase-2 convention. Not marked `resolved`
  (§9, reviewer's call). Did not file `#0426`+ — reported the
  `TestRender_StaticRoutesGetDistinctTitles` drift above instead; `#0432`
  is still the highest allocated as of this pass.

- 2026-09-05 — Reviewed (phase 3). **Bounced.** Criteria 1, 2, 5 and 6 verified
  independently and pass: `resolve`'s five branches enumerated from the body
  (static / `"workshop:"` / `"archive:"` / `cacheKeyFallback` /
  `cacheKeyNotFound`), both rewritten comments matching it, `workshopIsCardable`
  confirming §7.4's new `og.png` wording, `docs/seo.md` pointing at `CLAUDE.md`
  §11, the gofmt hash identical across `gofmt` stdout, and §7.4/total still
  34/1,871 by each row's own `sed`. Criterion 3's sweep is incomplete: three
  further accounts of `#0123`'s `/archive` addition are stale, one of them
  `defaultStaticRouteMeta`'s own doc comment in `internal/seo/seo.go`, and the
  commit introduced a fresh "nil until `#0123` lands" claim for an issue that has
  landed. Exact remedies in `## Review notes`; code commit `356735a` left in
  place. Two items reported for the orchestrator to file (§9): the `/archive`
  static-route test-coverage gap, and the stale "nil until `#NNNN` lands" idiom.
  `#0435` was the highest allocated at review time.

- 2026-09-05 — Implemented (second pass, phase 2). §3 escalation
  (`CLAUDE.md` §3) was considered and declined: this is the second
  implementation attempt on this issue, but the reviewer's bounce
  specified every remedy precisely -- the exact sentence to add, the exact
  condition to state in place of each stale "nil until #NNNN lands"
  clause, and which two test doc comments to correct and how -- so the
  work is transcription against a proved-green verification, not a design
  decision. Per `CLAUDE.md` §3's own test ("decide" vs. "apply"), this
  pass applies; it does not decide. Stayed on Sonnet.

  Made all four corrections named in the review notes, each verified
  against the current code rather than trusted from any document:

  1. `defaultStaticRouteMeta`'s doc comment (`internal/seo/seo.go`) now
     records `/archive`'s addition at `#0123` (PRD §6.8) in the same style
     as the existing `/privacy`/`#0070` sentence, matching the inline
     `#0123, PRD §6.8` comment already on the map entry itself.
  2. The package doc's step 2 (`internal/seo/seo.go`) and `NewSite`'s doc
     comment (`internal/seo/site.go`) both had their "nil until #0051/#0054
     land" and "nil until #0123 lands" clauses replaced with the real,
     present-tense condition -- nil only under `STORAGE=json`, which has
     no `workshops`-table / `email_campaigns`-table backing respectively --
     matching the wording `buildSEOSite`'s own doc comment
     (`cmd/opencircuit/main.go`) already uses for the identical fact.
     Fixed both the `#0123` clause the prior commit introduced and the
     adjacent pre-existing `#0051`/`#0054` clause beside it, in both
     locations, as the review notes asked.
  3. `TestRender_StaticRoutesGetDistinctTitles`'s doc comment
     (`internal/seo/seo_test.go`) now says five routes match its own
     `paths` slice (`/`, `/about`, `/workshops`, `/subscribe`, `/privacy`
     -- `#0070`) and separately notes the route table's sixth entry,
     `/archive` (`#0123`), is not exercised by that slice. Test body left
     unchanged -- the coverage gap is out of scope per the review notes.
  4. `TestSitemap_ContainsStaticRoutesWithLastmod`'s doc comment
     (`internal/seo/sitemap_test.go`) now names the five routes actually in
     its `want` map and separately notes `marketingRoutes`' sixth entry,
     `/archive` (`#0123`), is not exercised by that map. Test body
     unchanged, same reasoning.

  **Own re-read sweep, done by reading rather than only grepping** (the
  prior pass's grep for "route table" should have hit finding 1 and did
  not, per the review notes' own point). Read every `#0123` occurrence in
  `internal/seo/*.go`, `cmd/opencircuit/*.go`, `docs/seo.md`, and `PRD.md`
  (grep as a first pass, then read each hit in context) and every "the
  four"/"four static"/"four marketing" occurrence in `internal/seo/*.go`.
  Found no fifth instance: `internal/seo/site_test.go`'s
  `TestSite_EmittedURLsShareConfiguredHost` doc comment also says "all
  four", but that "four" counts *emission points* (og:url, og:image,
  sitemap `<loc>`s, robots.txt's `Sitemap:` line), not routes -- a
  different fact, correctly stated, not part of this drift family. Every
  other `#0123` reference in `docs/seo.md`, `PRD.md` §7.4, and the rest of
  `cmd/opencircuit`/`internal/seo` already accounts for `/archive`
  correctly (both were fixed by `#0419`/`#0422` and re-verified here, not
  just trusted).

  Confirmed the `#0051`/`#0054` pair is now fixed in both locations named
  by the review notes: the package doc's step 2 (`internal/seo/seo.go`)
  and `NewSite`'s doc comment (`internal/seo/site.go`). Left
  `NewRenderer`'s doc comment (`internal/seo/seo.go`, "source may be nil
  -- until #0051/#0054 wire a real workshop store ... archive (#0123) may
  likewise be nil") untouched -- it carries the identical idiom but was not
  named in the review notes, and the review notes explicitly listed "a
  package-wide sweep of the idiom beyond the instances named" as out of
  scope for this pass.

  No mechanism built (criterion 4): agreed independently with both the
  implementer's and reviewer's prior conclusion that
  `TestNoDocCommentNamesADifferentDeclarationInSameFile` does not cover
  this class, and that a guard comparing a hand-maintained enumeration
  against `resolve`'s AST branch count would store a copy of the answer
  next to the question (`CLAUDE.md` §8). Not built.

  Verified with `ISSUE=0425b scripts/check.sh go ./internal/seo/...
  ./internal/db/... ./internal/handlers/...` (zero skips, real elapsed
  times, `VERIFICATION PASSED`), plus the seven specific tests named in
  `## Verification` above run individually via `scripts/testdb.sh create
  0425b` / `drop 0425b`. gofmt stability proven non-destructively on all
  four edited files; hashes recorded above.

  Set to `in-progress` (was bounced back to `open`). Committed
  `internal/seo/seo.go`, `internal/seo/seo_test.go`, `internal/seo/site.go`,
  `internal/seo/sitemap_test.go` only -- staged narrowly and verified via
  `git diff --cached --name-only` before committing and `git show --stat
  HEAD` after. `issues/0425.md` left uncommitted for review per phase-2
  convention. Not marked `resolved` (§9, reviewer's call). Did not file
  new issues (§9); no new drift found worth filing beyond the two items
  the reviewer already listed for the orchestrator. `#0438` was the
  highest allocated as of this pass (re-checked at commit time, since
  another session files concurrently); `issues/0431.md` (`#0431`) and any
  concurrent `#0432` work were dirty/settling in the shared tree
  throughout and were left alone.

- 2026-09-05 — Reviewed (second review, phase 3). **Bounced.** All four remedies
  from the first review verified independently and pass, including the
  beyond-remedy `STORAGE=json` rewrite, whose claim was checked against
  `config.DevMode`, `serve`, `servePostgres`, `serveDevMode`, and the two backing
  stores and found true; both test bodies confirmed untouched (the commit's
  non-comment changed-line set is empty); gofmt stable on all four files.
  Criterion 3 still fails: `marketingRoutes`' own doc comment
  (`internal/seo/sitemap.go`) enumerates five routes and omits `/archive` above a
  six-element slice — the sixth wrong account of this route set and the
  counterpart of the first review's finding 1. Both prior sweeps were blind to it
  by construction, having matched on `#0123` and on count words, neither of which
  it contains; a net keyed on `/subscribe` finds it. Exact remedy in
  `## Review notes (second review, 2026-09-05)`; code commit `442a4e0` left in
  place. §3 escalation considered and declined — the remedy is one supplied
  sentence, so the next pass applies rather than decides; stay on Sonnet. Judged
  the pass's decision to leave `NewRenderer`'s `nil until #NNNN` instance correct
  scope discipline, since the prior review named a package-wide idiom sweep as its
  own issue "if more turn up" and more did; reported that for filing along with a
  second, worse instance in `internal/seo/workshop.go` the pass did not report,
  and one item to fold into `#0437`. Did not file (§9). `#0438` was the highest
  allocated at review time.

- 2026-09-05 — Implemented (third pass, phase 2). §3 escalation considered
  and declined. This is the third implementation attempt, but the second
  review supplied the exact remedy sentence, named the exact file and
  declaration, and left a proved-green verification -- transcription, not
  design, by §3's own "decide" vs. "apply" test. The two bounces on this
  issue share a *method* fault, not a capability one: both prior sweeps
  keyed their matcher on the token whose absence was the defect itself
  (`#0123` occurrences, then count words), so each was blind by
  construction to a comment containing neither. A bigger model would not
  have avoided that trap any more than Sonnet did once shown the right net
  (grep a set member, `/subscribe`, then read every comment naming three
  or more routes) -- the fix was a sweep-method correction, not a
  reasoning-depth one. Stayed on Sonnet.

  Verified the claim against `internal/seo/sitemap.go` directly rather
  than trusting the review: `marketingRoutes`' doc comment enumerated
  `"/"`, `"/about"`, `"/workshops"`, `"/subscribe"`, plus the `/privacy`
  (`#0070`) sentence -- five -- while the slice on the next line holds six,
  ending `"/archive"`. Added one sentence after the existing `/privacy`
  clause, in the same style, recording `/archive`'s addition at `#0123`
  (PRD §6.8). No other line touched.

  **Own sweep, run the way the review's diagnosis prescribed**: grepped
  `internal/seo/*.go` for the set member `/subscribe` (present in every
  version of this route set since `#0019`), then read every comment window
  naming three or more of the routes rather than matching on `#0123` or a
  count word. `marketingRoutes` was the only survivor; every other hit
  (`defaultStaticRouteMeta` and both test doc comments, all fixed by the
  second pass; `sitemap.go`'s own "deliberately NOT `handlers.StaticRoutes()`"
  paragraph, which lists the *excluded* Phase 1 routes, a different set) was
  already correct. Confirmed the three out-of-scope items untouched:
  `internal/seo/seo.go`'s `NewRenderer` doc comment and
  `internal/seo/workshop.go`'s `WorkshopSource` doc comment both still carry
  their stale "`#0051`/`#0054`" language (`#0439`'s territory, not edited);
  neither static-route test's body was touched (`#0437`'s territory); the two
  attributed quotations of `#0020`'s Notes in `workshop.go` and
  `sitemap_test.go` are untouched.

  No mechanism built (criterion 4): agreed for the third time, independently,
  that a guard here would compare a hand-maintained prose enumeration against
  a slice literal -- an oracle that is a copy of the answer next to the
  question (`CLAUDE.md` §8) -- and is not worth building for one doc comment.

  Verified with `ISSUE=0425c scripts/check.sh go ./internal/seo/...
  ./internal/db/... ./internal/handlers/...`: the first run failed on
  `TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch`, a `#0297`
  subscribe-intake test wholly unrelated to this pass's `internal/seo`
  comment edit; `uptime` at the time (`2.49`-`3.29`) ruled out §5's load
  explanation, so it was checked directly -- passed alone
  (`go test ./internal/handlers/... -run
  '^TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch$'`, a single
  execution, not a `-count=N` retry) and the full three-package command
  passed clean on a second, single re-run (zero skips, real elapsed times,
  `VERIFICATION PASSED`). Plus the six specific tests named in
  `## Verification (third implementation pass, 2026-09-05)` above, run
  individually via `scripts/testdb.sh create 0425c` / `drop 0425c`. gofmt
  stability proven non-destructively, with `sitemap.go`'s baseline
  established first since no prior pass had hashed this file; hashes
  recorded above.

  Set to `in-progress` (was bounced back to `open`). Committed
  `internal/seo/sitemap.go` only -- staged narrowly, confirmed via
  `git diff --cached --name-only` (one file, mine) before committing with a
  pathspec, and via `git show --stat HEAD` after (1 file, 3 insertions, 1
  deletion, matching this pass's edit exactly). `issues/0425.md` left
  uncommitted for review per phase-2 convention. Not marked `resolved` (§9,
  reviewer's call). Did not file new issues (§9); nothing new found to
  report -- the sweep confirmed the family is otherwise clean. `#0439` was
  the highest allocated as of this pass (re-checked at commit time; it is
  also the concurrent out-of-scope issue named in this pass's brief).
  `docs/deployment.md` (`#0431`) was dirty in the shared tree throughout and
  was left alone; no production access.

  **Re-run after writing this file's own prose**, since both issue-prose
  guards scan only `open`/`in-progress` files and this file is
  `in-progress` as of this edit (`scripts/testdb.sh create 0425c` /
  `drop 0425c`):

  ```
  go test ./internal/db/... -run '^(TestNoOpenIssueEvidenceCitesGoFileByLineNumber|TestNoOpenIssueVerificationCitesUndefinedTestFunction)$' -v -count=1
  --- PASS: TestNoOpenIssueVerificationCitesUndefinedTestFunction (0.13s)
  --- PASS: TestNoOpenIssueEvidenceCitesGoFileByLineNumber (0.02s)

  go test ./internal/handlers/... -run '^(TestNoCommentCitesUnresolvedPathOrSection|TestNoCommentCitesGoFileByLineNumber|TestNoDocCommentNamesADifferentDeclarationInSameFile)$' -v -count=1
  --- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.14s)
  --- PASS: TestNoCommentCitesGoFileByLineNumber (0.11s)
  --- PASS: TestNoDocCommentNamesADifferentDeclarationInSameFile (0.06s)
  ```

- 2026-09-05 — Reviewed (third review, phase 3). **Passed; set to `resolved`.**
  `b85874c` fixes exactly what the second review bounced and nothing else.
  Compared `marketingRoutes`' doc comment against its slice as two separate byte
  ranges: six members each, set difference empty in both directions, and the new
  `/archive` sentence's claim traced to PRD §6.8's own first reason and to the
  identical clause already in `defaultStaticRouteMeta`'s comment — so no seventh
  wrong account was introduced. Also re-derived the same comment's
  excluded-routes list (`knownStaticRoutes` minus `marketingRoutes` = exactly the
  nine it names) and its `IsKnownRoute` membership claim. Ran a superset of the
  prescribed sweep — the `/subscribe` net plus an independent scan for any
  six-line window naming three or more marketing routes, over 427 files across
  `internal/`, `cmd/`, `web/src/`, `docs/`, `PRD.md` and `CLAUDE.md`, so it does
  not depend on any one member being present. 18 windows, all read.
  `marketingRoutes` was the only stale one in `internal/seo`, confirming the
  pass's report. One further instance found outside it — the doc comment on
  `TestIsKnownRoute_StaticRoutes` claims to cover every static path in PRD §5.1
  while its slice omits `/archive` — reported for `#0437` rather than bounced,
  since its honest fix is a test-body change and both prior reviews carved test
  bodies out of this issue; `TestStaticRoutes_ContainsMarketingPaths`' "the four"
  was examined and judged accurate about its own subject. Confirmed all three
  out-of-scope groups untouched by hashing the four previously-edited
  `internal/seo` files against their `442a4e0` blobs (all identical): `#0439`'s
  two stale `#0051`/`#0054` claims, `#0437`'s two test bodies, and both
  attributed quotations of `#0020`'s Notes. `ISSUE=0425r3 scripts/check.sh`
  passed clean on the first run — the `#0297` subscribe-intake failure the
  implementation pass saw did not recur — with zero skips, real elapsed times and
  `VERIFICATION PASSED`; twelve named test functions also run individually; §11's
  counts re-measured at 1,871/34; `sitemap.go`'s gofmt hash reproduced
  independently. Escalation (§3) moot and, recorded per the brief, would not have
  been warranted: both prior bounces were a sweep-**method** fault with a
  one-sentence supplied remedy, which is §3's definition of transcription.
  Did not file (§9); one item reported for the orchestrator to fold into `#0437`.
  `#0439` was the highest allocated at review time.
