# 0435 — The backup timer was never installed, so this project has never been backed up

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | deploy / backups |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-04 |
| **Commit** | — |

## Description

**`opencircuit` has no backups and never has.** Measured on the box:

	systemctl is-enabled opencircuit-backup.timer
	→ Failed to get unit file state … No such file or directory

`/etc/systemd/system/` contains **only** `opencircuit.service`. `systemctl
list-timers` reports **0 timers**. The only dumps on disk belong to ShortLinks
(`/var/backups/postgres/shortlinks/`, newest 2026-08-17) plus a one-off
`pgdumpall-2026-08-14.sql.gz` from the AL2023 upgrade.

**Everything needed already exists and is correct.** `scripts/db/backup.sh`,
`restore.sh`, `pull-backups.sh` and `backup-alert.sh` are all present in the
repo *and* deployed to `/opt/opencircuit/scripts/db/`. All three unit files —
`opencircuit-backup.service`, `.timer`, and `-alert.service` — are in
`deploy/systemd/`. The service correctly sets `BACKUP_DATABASES=opencircuit`.

**They were written and never installed.** Only `opencircuit.service` was.

## Why this is the most urgent open issue

Production holds real, irreplaceable data: subscribers, the interest taxonomy,
audit rows, and the user's first newsletter draft. **None of it has ever been
dumped.**

And a **migration is about to run against it**. `#0404`'s deploy sequence says
"back up before `migrate up`" — a step that cannot currently be performed by any
installed mechanism.

## Acceptance criteria

1. The timer is installed, enabled and running, per `deploy/systemd/README.md`'s
   documented procedure. **Follow that document**; do not invent an install.
2. **A real dump exists and is verified** — not merely a green unit. Confirm the
   file, its size, and that it is a valid archive.
3. **A restore is proved.** `scripts/db/restore.sh` exists and is untested here.
   Restore into a **scratch database, never over `opencircuit`**, and compare
   row counts for `subscribers`, `email_campaigns`, `workshops` and
   `interests` against the source. §8b: a mutation that touches real data must
   run against a database you created.
4. Retention and disk are checked: `backup.sh`'s prune keeps the volume bounded
   on a `t4g.nano` (§7 — 418 MB RAM, and disk is not unlimited either).
5. `backup-alert.sh`'s path is considered — it reads
   `EnvironmentFile=-/etc/opencircuit/backup-alert.env`, which has **no
   committed template** and is optional. Say whether alerting should be wired
   now or deliberately left.
6. `docs/deployment.md` and `CLAUDE.md` §7 record the real state afterwards.
   **Note `#0431` is concurrently sweeping that file** for pre-deploy claims —
   coordinate rather than colliding.
7. **Installing units and enabling a timer changes production.** Do not do it
   without the user's explicit go-ahead; prepare and verify everything that can
   be verified without it, and say exactly what remains.

## Relation

- Blocks: the deploy — `#0404`'s sequence has a backup step with nothing behind it
- Relates to: [#0434](0434.md) — adding `/var/www/media` to whatever runs
- Relates to: [#0229](0229.md), [#0430](0430.md) — the S3 offsite leg deferred
  separately; this is about the local dumps that were supposed to exist meanwhile
- Relates to: PRD §10.6

## Work log

- 2026-09-04 — Filed by the orchestrator after the user asked whether backups
  run. The absent unit, the empty timer list, the ShortLinks-only dumps, and the
  presence of every script and unit file in both the repo and the deploy
  checkout were all confirmed directly.

- 2026-09-08 — **Implementation pass (Sonnet).** A prior attempt was killed by
  a machine-load spike before it wrote anything; nothing of it survived, so
  this pass re-derived every fact from scratch rather than trusting the
  filing. **Nothing was applied to the box** — every command below is
  prepared and pending the user's approval (`CLAUDE.md` §5b, §9).

  **Re-derived read-only, via `ssh ec2` (load 0.01/0.05/0.02 — idle,
  `CLAUDE.md` §5):**
  - `/etc/systemd/system/` contains only `opencircuit.service`; no
    `opencircuit-backup*` unit exists. `systemctl is-enabled
    opencircuit-backup.timer` fails "No such file or directory".
    `systemctl list-timers --all` lists 8 unrelated system timers, none of
    this project's.
  - **The recorded blocker holds, confirmed by `stat` rather than trusted:**
    `/var/backups/postgres` is `root:root 0700`. Its only content,
    `shortlinks/` (also `root:root 0700`, two dump files dated 2026-08-17),
    was populated by a one-off manual `sudo` run — no cron, timer, or
    running service on this box depends on the parent directory's current
    ownership, confirmed by checking for a `shortlinks` cron/timer/unit
    (none exists) and the root/postgres crontabs (both empty).
  - Disk: 9.1G free of 20G (55% used). Memory: 418Mi total, tight but the
    production `opencircuit` database is 9639 kB (6 subscribers, 1 email
    campaign, 3 workshops, 12 interests) and `/var/www/media` is 152K, so
    `pg_dump`/`tar` for either has no meaningful footprint on either
    resource. Peer auth confirmed working: `sudo -u postgres psql -d
    opencircuit` connects; `postgres` role is superuser with `CREATEDB`;
    `opencircuit` app role exists (needed for `restore.sh`'s ownership
    reassignment step).
  - **A second, independent blocker, not in the original filing:**
    `/opt/opencircuit`'s git checkout is `ef0a58f`, 711 commits behind this
    repo's current commit and specifically from before `#0434`.
    `scripts/db/backup-media.sh` and `scripts/db/restore-media.sh` do not
    exist there, and its copy of `opencircuit-backup.service` is the
    pre-`#0434` single-`ExecStart=` version. `sha256sum` on both ends
    confirmed `backup.sh`, `restore.sh`, `pull-backups.sh`,
    `backup-alert.sh`, `opencircuit-backup.timer`, and
    `opencircuit-backup-alert.service` are already byte-identical between
    the box and this repo's current commit, so only those three files need
    updating — not a full redeploy. Installing today's committed unit file
    without this fix would succeed on the database leg and then fail the
    media leg with "no such file", marking the whole run failed and firing
    the alert every night despite the database dump having worked.

  **Produced:** an approval-ready sequence in `deploy/systemd/README.md`'s
  "Backup timer and failure alert" section (new subsections: fix the
  permission blocker, bring the on-box checkout current for the media leg,
  install/enable, verify the dump is real and prove a restore into a scratch
  database), and a matching "Re-derived on the real box" write-up plus a
  corrected summary-table row in `docs/deployment.md`'s Backups section.
  Also corrected two now-stale claims found in passing while editing that
  same text: `deploy/systemd/README.md` previously said this project's own
  EC2 host was not yet provisioned (`CLAUDE.md` §10 item 6 has recorded it as
  provisioned and live since 2026-08-25), and `docs/deployment.md` said
  `/opt/opencircuit` was an unconfirmed placeholder path (`#0435`
  re-verified directly that it is the real, current checkout location — the
  real blocker was ownership, not path correctness). Did not touch
  `backup.sh`, `restore.sh`, `pull-backups.sh`, `backup-alert.sh`, or any
  unit file's logic — only prose in `deploy/systemd/README.md` and
  `docs/deployment.md`.

  **Four questions, answered (full reasoning in
  `deploy/systemd/README.md`):**
  1. `chown postgres:postgres` (non-recursive, top-level directory only) is
     right, not a mode change — nothing live depends on the current root
     ownership, and a mode-only fix would require opening the directory to
     "other" (it holds session tokens, passkey credentials, and the audit
     log per `backup.sh`'s own header), which is strictly worse. The
     sibling `shortlinks/` directory is untouched by a non-recursive chown.
  2. Dumps land at
     `/var/backups/postgres/opencircuit/opencircuit-<timestamp>.dump` (+
     `-latest.dump`) and `/var/backups/postgres/media/media-<timestamp>.tar.gz`
     (+ manifest/`-latest`). At today's data volume (9.6 MB database, 152 KB
     media) the compressed footprint is negligible against 9.1 GB free; a
     full disk fails `pg_dump`'s temp-file write, which `backup.sh` already
     detects (`rc≠0` or an empty `$tmp`), removes the partial file, leaves
     every prior good dump untouched, exits non-zero, and fires the alert —
     it does not free space itself.
  3. Retention is local-only, `BACKUP_RETENTION_DAYS` (default 14). The
     offsite pull (`pull-backups.sh` to the user's "joe" machine) is treated
     as **out of scope** for this issue — joe's hostname and reachability
     for an unattended pull are not recorded anywhere in this repo, and per
     the task's instruction this is a question for the user, not a default
     to invent.
  4. First run behaves correctly: re-confirmed `#0445`'s control-flow finding
     still holds (unchanged bytes, `sha256sum` verified) — the prune step is
     reachable only in the same loop iteration immediately after a fresh
     publish, so a first run with no prior dump writes one file (age 0) and
     the `-mtime +14 -delete` prune matches nothing.

  **Disclosure paragraph (`#0447`'s style — every command in the sequence
  audited as something the user pastes, per that issue's own lesson about an
  unscoped command doing more than its write-up admitted):** the sequence
  touches one directory's ownership (non-recursive `chown`, confirmed to
  leave `shortlinks/` untouched), copies three files onto the box via `scp`
  (content only — no execution, no service restart), copies three unit files
  into `/etc/systemd/system/` and runs `daemon-reload` (re-reads every unit
  on the host, a standard momentary operation that does not stop or restart
  `opencircuit.service` or either ShortLinks service sharing this box),
  enables a timer that will very likely queue an immediate first run
  (`Persistent=true`, never triggered before) in addition to its nightly
  07:30 UTC schedule, runs one `pg_dump` against the live `opencircuit`
  database (ACCESS SHARE locks only, sub-second at this data volume) and one
  `tar` of `/var/www/media`, writes two small files under `/var/backups/`,
  and — for the restore proof — creates and drops one scratch database
  (`opencircuit_restore_drill`), never touching `opencircuit` itself. No
  webhook, no external notification, and no change to `BACKUP_ALERT_WEBHOOK_URL`
  (left deliberately unconfigured — no such channel exists anywhere in this
  project yet).

  **Verification.**

      uptime
      # up 24 days, load average 0.01, 0.05, 0.02 — idle
      ISSUE=0435 scripts/check.sh go ./internal/db/... ./internal/handlers/...

      ok  	github.com/brennanMKE/OpenCircuitSF/internal/db        (real elapsed time)
      ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers  (real elapsed time)

  Zero skips; both DB-backed packages ran for real, not `0.00s`. **What this
  run confirms and does not:** these are the repo-wide citation and metadata
  guards (`CLAUDE.md` §5) — `internal/handlers` walks the whole repo tree,
  `internal/db` scans `issues/`, `PRD.md`, `CLAUDE.md`, `migrations/`,
  `docs/database.md` — so a green run confirms this issue file and its Work
  log carry no dangling citation or unresolved path/section reference, and
  that nothing else in the tree regressed. **No test in either package reads
  `docs/deployment.md`**, confirmed by byte-scanning both packages'
  `_test.go` files for that string before writing this — so this run says
  nothing about whether the backup sequence itself is correct. That rests
  entirely on the read-only box measurements, the `sha256sum` comparisons,
  and `#0445`'s already-reviewed control-flow finding, all recorded above —
  none of which this Go suite exercises. `scripts/check.sh` dropped the
  `opencircuit_test_0435` database on exit, as documented.

  **Left for the reviewer / the user:** nothing has been applied to the box.
  Everything in `deploy/systemd/README.md`'s new subsections is the exact
  sequence to run, in order, once approved. Not filed as new issues (none
  found worth filing beyond what is already recorded here — the stale
  `/opt/opencircuit` provisioning claim and the stale placeholder-path claim
  were corrected in place rather than tracked separately, since both were
  one-sentence factual corrections adjacent to text this pass was already
  editing).

- 2026-09-08 — **`#0466` implementation pass, reviewing this issue against
  the missing-precondition defect found in `#0447` and `#0465`.** This
  issue's own instruction already states the precondition explicitly, before
  the step that consumes it — the "Bring the on-box checkout current for the
  media leg" subsection in `deploy/systemd/README.md`, `scp`-ing
  `backup-media.sh`, `restore-media.sh`, and `opencircuit-backup.service`
  with hash verification, ahead of "Install and enable the timer". No change
  needed here. Re-examined the `scp`-versus-`git pull` choice alongside the
  other two instructions rather than leaving it unquestioned: still `scp` —
  a `git pull` on this box forces a `go build`/`npm run build` on 418 MB of
  RAM (`CLAUDE.md` §7) to update three files that need neither, and
  `deploy/systemd/README.md` already makes this same trade-off explicit in
  its own text ("A full redeploy … would also fix this … but that rebuilds
  the whole Go binary and SPA … and is `#0404`'s job at the next real
  deploy, not a prerequisite for turning backups on today"). Full details
  and the sweep of the other two instructions are in `issues/0466.md`.
  Status stays `in-progress`.

## Review notes

> 🔴 Bounced 2026-09-08 (review, Opus) — every fact this pass measured on the
> box is correct, including both blockers and the exact-3-file claim, which I
> re-derived independently rather than trusting. The restore drill is sound and
> is the strongest part of the deliverable. The defect is in the one artifact
> the user pastes: **two of its own verification commands cannot produce the
> output it tells the reader to expect** (proved against the live box, below),
> and a **stale bolded directive four lines above this pass's own correction**
> tells the reader to hand-edit the very unit file the new sequence then
> `scp`s and hash-verifies. Nothing was applied to the box, by this pass or by
> this review. `1110fec` stays in place; the fix is prose in
> `deploy/systemd/README.md`.

### What passed — re-derived read-only, not trusted

Box load `0.05 0.02 0.00` — idle. Everything below is a read; nothing was
changed, and no read was refused.

**Blocker 1 holds.** `stat` reports `/var/backups/postgres` is `root:root 700`.
Its only content is `shortlinks/` (`root:root 700`) holding two dumps dated
2026-08-17 plus a `-latest` symlink. No `opencircuit-backup*` unit exists under
`/etc/systemd/system/`; `systemctl list-timers --all` lists 8 timers, all OS
ones (`refresh-policy-routes@ens5`, `sysstat-collect`, `systemd-tmpfiles-clean`,
`certbot-renew`, `logrotate`, `sysstat-summary`, `update-motd`, `fstrim`).

**Blocker 2 holds, and the 3-file claim is exact.** `/opt/opencircuit` is at
`ef0a58f` with one pre-existing local modification (`M web/dist/index.html`).
`backup-media.sh` and `restore-media.sh` are absent from its `scripts/db/`.
Hashing both ends: all six files this pass called already-identical are
byte-identical — `backup.sh` `6696b86e…`, `restore.sh` `0edf1129…`,
`pull-backups.sh` `15773421…`, `backup-alert.sh` `1b908d74…`, the `.timer`
`f627bea9…`, the alert `.service` `200b8ef9…`. The box's
`opencircuit-backup.service` is `f57bcf50…` against this repo's `edf5aa0b…`.
So exactly three files differ, and the three hashes printed in the instruction
match this repo's files exactly. **The `scp` step's scope is correct.**

**The `chown` is safe, checked independently rather than accepted.**
`sudo grep -rIl "/var/backups" /etc /usr/local` returns **zero hits** — nothing
in either tree names the path. There is no crontab for `root`, `postgres`, or
`ec2-user`; `/etc/cron.d` holds only `0hourly` and `cron.daily` is empty; none
of the 8 timers touch it. Two further points the write-up did not make and that
strengthen it: the `chown` is non-recursive so `shortlinks/` keeps `root:root
700`, and `root` bypasses mode bits regardless, so a future manual root-run
ShortLinks dump — the only thing that has ever written there — is unaffected in
both directions.

**Every measured number in the write-up reproduces.** Database 9639 kB; row
counts exactly `subscribers 6`, `email_campaigns 1`, `workshops 3`,
`interests 12`; `/var/www/media` 152K holding exactly `programming_leds.jpg`
and `soldering.jpg`; 9.1G free of 20G. `/etc/opencircuit/` holds only
`config.env` — `backup-alert.env` is confirmed absent.

**No scratch-name collision.** The cluster holds `opencircuit`, `postgres`,
`shortlinks`, `shortlinks_ocsf`, `template0`, `template1`. There is no
`opencircuit_restore_drill`.

**The drill's leading environment assignments are valid on this box.**
`sudo -l` reports `(ALL) NOPASSWD: ALL`, which implies `SETENV` even under
`env_reset`, so `sudo -u postgres VAR=val …` is permitted; confirmed
empirically with a command that only runs `echo`.

### The restore drill — audited hardest, and it holds

- **It never targets `opencircuit`.** `restore.sh` is invoked with the literal
  scratch name, and `scripts/db/restore.sh` never drops a database at all (its
  own header says so, and the code has no `dropdb` outside the caller's hands).
  The only statement aimed at `opencircuit` is a read-only `count(*)`.
- **It cannot silently overwrite.** `RESTORE_CREATE=1` calls `createdb`, which
  fails if the name already exists, and the script runs under `set -euo
  pipefail` — so a stale scratch database aborts the drill instead of being
  restored over.
- **The evidence is content, not an exit code — and that is load-bearing
  here, not decorative.** `pg_restore` without `--exit-on-error` continues past
  failures and still exits 0, so `restore.sh` returning success proves little.
  The row-count comparison is what actually decides it, which is exactly the
  answer `#0434`'s review asked for.
- **It would fail on a bad restore, not pass vacuously.** A table that failed
  to restore makes `count(*)` raise `relation … does not exist`, so that
  database's block prints an error instead of four numbers; a structurally
  complete but empty restore prints `0` against `6/1/3/12`. Every count is
  label-prefixed (`subscribers|6`), so the two blocks stay comparable even if
  `UNION ALL` branch order ever differed. I could not construct a corruption
  that leaves the two blocks matching.

One gap, non-blocking (N4 below): if the restore aborts partway the scratch
database is left behind, and the `dropdb` is a later step the reader may never
reach.

### Verbatim-paste audit

No heredocs and no tab-indented lines anywhere in `deploy/systemd/README.md`,
so `#0464`'s half-run hazard class does not apply. Extracted every fenced block
programmatically rather than retyping: all fences terminate, and every shell
block this issue added passes `bash -n`. The one block that does not parse is
the `BACKUP_SSH_HOST=…` line, which is **pre-existing and unchanged by
`1110fec`** and is a `<placeholder>` line rather than a paste target.

Three blocks do not survive a verbatim paste for other reasons. Those are the
bounce.

### Required fixes

**F1 — two verification commands cannot produce their stated output.** Proved
against the live box just now, as `ec2-user`:

    $ stat -c '%U:%G %a' /var/backups/postgres /var/backups/postgres/shortlinks
    root:root 700
    stat: cannot statx '/var/backups/postgres/shortlinks': Permission denied
    rc=1

    $ ls -la /var/backups/postgres/
    ls: cannot open directory '/var/backups/postgres/': Permission denied
    rc=2

`ec2-user`'s groups are `ec2-user, adm, wheel, systemd-journal` — **not**
`postgres`. The directory is mode `700` before the `chown` and mode `700`
after it, so `ec2-user` has no traverse bit either way, and `backup.sh` itself
re-asserts `chmod 0700` on `BACKUP_ROOT` every run. The instruction tells the
reader to "Expect `postgres:postgres 700` on the first line and `root:root
700`, unchanged, on the second" — **the second line is unobtainable without
`sudo`, before or after the fix.** A reader sees a permission error in the
first thing they check after chowning a production backup directory, which
reads exactly like the `chown` broke something. The same applies to the
`ls -la` opening the dump-verification block; the two lines under it already
carry `sudo -u postgres` and are correct.

Remedy — in "Fix the `/var/backups/postgres` permission blocker", the
verification block becomes:

```bash
sudo stat -c '%U:%G %a' /var/backups/postgres /var/backups/postgres/shortlinks
```

and in "Verify the dump is real, and prove a restore", step 1, the block's
first line becomes:

```bash
sudo ls -la /var/backups/postgres/opencircuit/
```

**F2 — a stale bolded directive that contradicts the new sequence.** The
opening paragraph of "Backup timer and failure alert (`#0229`)" still reads
`/opt/opencircuit` as "a placeholder … until the real server layout is captured
(`CLAUDE.md` §10 item 6, still undocumented)" and then instructs, in bold,
"**Edit the unit file to match the real path before installing it.**" This pass
corrected that exact claim in `docs/deployment.md` and corrected the
neighbouring "not yet provisioned" claim in this same section, but left this
one four lines above its own correction. `CLAUDE.md` §10 item 6 has been done
since 2026-08-25, and this pass re-verified the checkout is really there. The
directive is now actively harmful: a reader who edits the unit file first will
`scp` an edited file and then fail the `sha256sum` check that expects
`edf5aa0b…`, and will read that as the copy having gone wrong.

Remedy — replace that paragraph in full with:

```
`opencircuit-backup.service` assumes the repo is checked out at
`/opt/opencircuit` (`WorkingDirectory=` and `ExecStart=` both reference it).
**Corrected (`#0435`, 2026-09-08): this is the real path, not a placeholder.**
It was written as one, mirroring ShortLinks' `/opt/shortlinks` convention,
while `CLAUDE.md` §10 item 6 still recorded the server layout as uncaptured;
that item has been done since 2026-08-25, and `#0435` re-verified directly that
the checkout lives at `/opt/opencircuit`. Do **not** edit the unit file's paths
before installing it — the sequence below copies that file byte for byte and
verifies its `sha256sum`, which an edit would fail.
```

The same stale sentence survives as a comment inside
`deploy/systemd/opencircuit-backup.service` itself, which this sequence copies
onto the box. Correcting it there too is welcome but optional — it is a
comment, it changes the file's hash, and the three expected hashes in the
instruction would then need recomputing. If it is left alone, say so, so the
next reader does not treat it as an oversight.

**F3 — the install block switches machines without saying so.** "Bring the
on-box checkout current" ends with a block explicitly labelled "Run from the
local machine, **not** on the box". The next block opens with three bare
relative filenames — `sudo cp opencircuit-backup.service
opencircuit-backup.timer opencircuit-backup-alert.service
/etc/systemd/system/` — with no `ssh` and no `cd`. Pasted into the shell the
reader is demonstrably still in, it reads the local checkout and, on macOS,
fails on the missing target directory. Fail-loud rather than dangerous, but
this section is the artifact the issue exists to produce, and the file has just
established that it marks machine context when it matters.

Remedy — precede that block with the sentence "Everything from here on runs
**on the box** (`ssh ec2`)." and make the block:

```bash
cd /opt/opencircuit/deploy/systemd
sudo cp opencircuit-backup.service opencircuit-backup.timer opencircuit-backup-alert.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now opencircuit-backup.timer
```

### Accepted as written, recorded so they are not re-litigated

- **N1** — `sudo -u postgres psql` invoked from `ec2-user`'s home prints
  `could not change directory to "/home/ec2-user": Permission denied` on every
  call (seen on all of mine). Harmless, but the drill's blocks will emit it and
  the instruction does not warn. One sentence would close it; not blocking.
- **N2** — `deploy/systemd/README.md` carries **two** near-identical
  `## Create the system user` sections. Pre-existing (present in `1110fec~1`),
  not this issue's doing, and out of scope. Reported for filing.
- **N3** — the `Persistent=true` first-enable catch-up claim is hedged ("very
  likely", "do not be surprised"), which is the honest framing for something
  unapplied. I could not settle systemd's first-enable behaviour without
  applying it, and the sequence's own manual `systemctl start` makes the
  observable outcome the same either way. Accepted.
- **N4** — say that the `dropdb` should be run even if the restore aborted, so
  a failed drill does not leave a scratch database behind.

### The two judgment calls

**Alerting — the recommendation is right, the gap it leaves is real.**
`/etc/opencircuit/backup-alert.env` is confirmed absent and no
Slack/Discord/Mattermost/healthchecks channel exists anywhere in this project,
so wiring `BACKUP_ALERT_WEBHOOK_URL` would mean inventing a destination with
nothing behind it — worse than none, and the unit's `EnvironmentFile=-` prefix
already makes the journal path work without it. But the interim signal is
weaker than the write-up implies: `journalctl -p err` and `systemctl --failed`
are both **pull-based, and nothing and nobody is scheduled to pull them**. A
silent nightly failure would stay silent in exactly the way this issue exists
to fix. That is a genuine gap, and it belongs in its own tracked issue rather
than a paragraph inside this one. Recommended for filing, not a blocker here.

**Deferring `pull-backups.sh` to "joe" — correct, and stated plainly enough.**
The hostname genuinely is not recorded anywhere in this repo, inventing one is
how `#0245` previously pointed a pull at another project's production host, and
the deferral appears in three places (this Work log, `deploy/systemd/README.md`,
and `docs/deployment.md`), each naming exactly what is missing — hostname,
static address or VPN, whether it stays powered on — and each saying it is a
question for the user. Nothing to add.

### Verification

    uptime
    # 15:43 up 19 days, load average 5.33 5.77 5.45 — busy but well under the
    # ~8 threshold (CLAUDE.md §5); no timing-sensitive assertion involved here
    ISSUE=0435r scripts/check.sh go ./internal/db/... ./internal/handlers/...

    === go test ./internal/db/... ./internal/handlers/... -p 2
    ok  github.com/brennanMKE/OpenCircuitSF/internal/db          0.428s
    ok  github.com/brennanMKE/OpenCircuitSF/internal/handlers    6.918s
    === skip audit ... (no skips)
    VERIFICATION PASSED

Zero skips; both DB-backed packages report real elapsed time, not `0.00s`.
`gofmt -l` clean, `go build` and `go vet` clean. No failure named any file, so
the `#0433` interference this review was warned about did not occur.
`scripts/check.sh` dropped `opencircuit_test_0435r` on exit.

**What this run does and does not confirm.** These are the repo-wide citation
and metadata guards — `internal/handlers` walks the whole repo tree and
`internal/db` scans `issues/`, `PRD.md`, `CLAUDE.md`, `migrations/`, and
`docs/database.md` — so a green run confirms this issue file, including this
section and the Work log above it (`#0446`), carries no dangling citation or
unresolved path or section reference. **No test in either package reads
`docs/deployment.md`** — I re-confirmed this myself by scanning both packages'
`_test.go` files for that string, finding none; the only `deploy/systemd`
mentions anywhere in them are two doc comments in
`internal/handlers/subscribe_test.go` naming `opencircuit.service`, which the
citation guard resolves as a path and nothing more. So this suite says nothing
whatever about whether the backup sequence is correct. That rests entirely on
the read-only box measurements recorded above.

### Status

Stays `in-progress`. First bounce, and every remedy above is written out
verbatim — the next pass applies text rather than deciding anything, so it
stays on Sonnet (`CLAUDE.md` §3). Nothing has been applied to the box by
either the implementation pass or this review; **this project still has no
backups.**
