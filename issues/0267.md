# 0267 — Nothing detects a doc comment attached to the wrong declaration

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-24 |
| **Closed** | 2026-08-27 |
| **Commit** | `64480e4` |

## Description

`#0127` shipped with `BuildWelcomeEmail`'s doc comment appended to the end of
`BuildAdminAlertEmail`'s, so `#0126`'s documentation was attributed to `#0127`'s
function. `go doc BuildAdminAlertEmail` returned a bare signature.

**Nothing in the toolchain can see this.** A contiguous `//` block is
well-formed, so `gofmt` is silent; `go vet` does not look; `check.sh` passed with
it in place. It was caught only because a reviewer ran `go doc` on the specific
symbols a bounce had named.

**The detector already exists — it just was not committed.** `#0127`'s
re-reviewer wrote a `go/ast` pass for the exact defect shape: *a declaration
whose `Doc` comment's first word names a different top-level symbol in the same
file*. Run over all of `internal/` and `cmd/`, it found **zero** mis-attributions
after the fix.

That is a cheap, precise guard for a defect class this project has now hit once
and cannot otherwise detect.

## Resolution notes

> 🟢 Resolved 2026-08-27 — The guard was re-verified from outside the tree: in
> an isolated worktree pinned to `64480e4`, restoring `#0127`'s *real*
> historical corruption (`41993c0`'s merged comment block, reproduced by moving
> `func BuildAdminAlertEmail` below `BuildWelcomeEmail` so the two docs fuse)
> and reverting `subscribe_test.go` to its pre-fix state made the guard fail
> naming both — `internal/mailing/transactional_templates.go:192: doc says
> "BuildAdminAlertEmail" but is attached to BuildWelcomeEmail` and
> `internal/handlers/subscribe_test.go:1605: doc says "snapshot" but is
> attached to subscriberStoreCallCounts` — while `gofmt -l` and `go vet` stayed
> silent on the same bytes. `ISSUE=0267r scripts/check.sh go ./internal/...`
> at that commit reported VERIFICATION PASSED, sixteen packages `ok`, no skips.

## Acceptance criteria

- [x] A guard fails when a declaration's doc comment opens by naming a different top-level symbol declared in the same file
- [x] It runs in `scripts/check.sh`'s default Go scope, not only under `all`
- [x] Mutation-proved: reattach `#0127`'s original corruption in a copy, confirm the guard names both symbols; revert, confirm clean
- [x] **No false positives on the current tree** — the reviewer's run was clean, but its first attempt produced four hits caused by its own trailing-`s` trim, so the normalisation needs care and its own test
- [x] Grouped `const`/`var` declarations legitimately sharing one comment are not flagged — that was the other source of first-pass noise
- [x] `ISSUE=0267 scripts/check.sh go ./internal/...` passes with zero skips

## Notes

Found by `#0127`'s phase-3 re-review under `CLAUDE.md` §9, which built and ran
the detector as part of sweeping for a second instance.

**Scope it honestly.** This catches one specific, mechanical shape: the first
word naming a sibling symbol. It will not catch a doc comment that is merely
wrong, or one describing the right symbol inaccurately — that is the class
`#0263` covers and which review is the only instrument for. Say so in the
guard's own comment, so nobody later assumes doc comments are verified.

Worth noting the same review found a related prose defect in `#0127`'s own
`## Fix pass`, which claimed it "inserted the blank line between
`BuildAdminAlertEmail`'s doc comment and its `func` line" — **that would
re-detach the comment**, the very defect being fixed. The code was right and the
description was wrong, in the pass fixing a documentation defect.

## Relation

- Found by: [#0127](0127.md) — the doc-comment corruption this would have caught
- Relates to: [#0263](0263.md) — comments that are wrong rather than misattached, which no guard can catch
- Relates to: [#0196](0196.md), [#0220](0220.md) — the citation guards this sits beside
- Relates to: [#0265](0265.md) — the issue-file citation guard from the same review round

## Implementation notes

New file `internal/handlers/misattached_doc_comment_guard_test.go`, same
package and same shape as `#0196`'s citation guard: a `go/ast` walk over
`citedTestScanRoots` (reused directly from
`dangling_test_citation_guard_test.go`, which was clean in git status when
this was written, along with `walkGoFiles`/`skipVendoredDir`/
`toRepoRelativePath`), a small regexp naming the candidate shape, and
exclusion rules earned by real hits.

**The check.** For each top-level declaration's own leading doc comment
(`*ast.FuncDecl.Doc`; a single-spec `*ast.GenDecl.Doc`; or an individual
`*ast.ValueSpec`/`*ast.TypeSpec`'s own `Doc` inside a group), take the
comment's first bare identifier (`^[A-Za-z_][A-Za-z0-9_]*`, anchored — a
comment opening with anything else, e.g. an issue citation or ordinary
prose, yields no match and isn't checked at all). If that word names a real
top-level symbol declared elsewhere in the same file, and is not one of the
name(s) this comment is actually attached to, it's a hit.

Deliberately **no stemming or trimming** beyond what the identifier
character class itself excludes — the acceptance criteria's own warning
about a trailing-`s` trim producing four false hits is closed by construc-
tion: "BuildWelcomeEmail's" matches "BuildWelcomeEmail" because the pattern
simply stops at the apostrophe, and a plain plural like "Kinds" is left
exactly as written, so it only ever equals a real name when the author
wrote that identifier verbatim as the first token.

**Grouped const/var (acceptance criterion).** A `*ast.GenDecl` with two or
more `Specs` is a genuinely grouped block, and its group-level `Doc` is not
checked at all — only an individual member's own per-spec `Doc` is (still
fully checked, and pinned by
`TestMisattachedDocCommentGuardStillChecksPerMemberDocInsideAGroup`). This
was not a hypothetical: the first version of this guard, checking every
`GenDecl.Doc` regardless of group size, found **six** real "hits" on this
tree, all of them this codebase's own established idiom of opening a
multi-member block with a category noun or the function it supports —
`internal/mailing/campaigns.go` ("Campaign status constants..." on
`CampaignStatus*`), `internal/mailing/preflight.go` ("Preflight failure
codes" on `PreflightCode*`), `internal/subscribers/suppressions.go`
("Suppression reasons..." on `SuppressionReason*`),
`internal/subscribers/store.go` ("Unsubscribe source values..." on
`Source*`), `internal/workshops/store.go` ("Workshop status constants" on
`Status*`), and `internal/mailing/worker_test_helpers_test.go`
("newTestWorker constructs..." glued above its own supporting `const`
block). None is `#0267`'s defect shape — the real incident was
`FuncDecl`-to-`FuncDecl`, and no grouped block anywhere in this tree's
history has ever exhibited it — so the group-size exclusion is honest,
structural, and documented in the guard file's own header rather than a
name-based allowlist.

> **Correction (`#0276`, 2026-08-27).** The claim above — "None is `#0267`'s
> defect shape" — is wrong for the sixth instance,
> `internal/mailing/worker_test_helpers_test.go`. "newTestWorker constructs a
> Worker..." described `func newTestWorker`, not the `const` block it sat on,
> which left `newTestWorker` itself undocumented — the same
> "undocumented declaration, doc glued to its neighbour" shape as the
> `subscribe_test.go` instance this issue's own Implementation notes describe
> fixing two paragraphs below, just lower-stakes: an unexported test helper.
> This was caught by `#0267`'s own `## Review notes` at the time (which is
> accurate) but never fed back into this section, so the claim above stood
> uncorrected. `#0276` moved the sentence onto `func newTestWorker` and left
> this paragraph in place, on purpose, as the record of what was wrong and
> why the group-size exclusion still cannot tell the two apart — see the
> matching note at the exclusion itself in
> `internal/handlers/misattached_doc_comment_guard_test.go`.

**One instance survived that rule and needed individual judgment**:
`internal/handlers/public_list_stats.go`'s `const pendingBucket = 5` (a
single-spec declaration, so the group-size rule doesn't reach it) carries a
doc opening "PublicListStatsHandler serves GET /api/list-stats...", naming
the handler TYPE declared later in the same file. Read in full, every
paragraph after the opener is substantively and exclusively about
`pendingBucket` — why it exists, what it defends against, and it names
`pendingBucket` by its own identifier partway through — the opposite of
`#0127`'s real defect, where the corrupted block contained nothing at all
about the declaration it was actually attached to. This is closed by a
small, individually-verified allowlist (`misattachedDocKnownBenignPairs`,
keyed by file so it can't silently excuse an unrelated future doc in a
different file), the same role `nonTestIdentifierAllowlist` plays for
`#0196`'s sibling guard — not a structural loosening, since nothing local
distinguishes this case from a real corruption.

**One real (very minor) instance of the actual defect, found and fixed.**
`internal/handlers/subscribe_test.go:1605` had `// snapshot returns a
race-safe copy of every counter...` — a doc unmistakably describing
`func (c *countingSubscriberStore) snapshot()` — attached instead to `type
subscriberStoreCallCounts struct{...}`, the declaration immediately above
it, leaving `snapshot()` itself with no doc of its own (the same
"undocumented, adjacent declaration got it instead" shape as `#0127`, just
lower-stakes: an unexported method on a test-only counter type). The file
was clean in git status both when first checked and immediately before this
edit (`internal/handlers` is a concurrently-edited package this session, per
the orchestrator's brief); the fix moves the two-line comment down to sit
directly above `func...snapshot()` and leaves the struct undocumented,
which is consistent with plenty of other unexported test-only helper types
in this codebase. This was the ONLY tree-wide finding treated as a real
defect rather than a guard-design gap; every other candidate the guard's
development turned up on the tree was legitimate prose the guard's
exclusion rules now correctly leave alone.

**Mutation-proved** (acceptance criterion), by direct isolated proof against
`t.TempDir()` fixtures using the real historical symbol names (safe: neither
`BuildAdminAlertEmail` nor `BuildWelcomeEmail` is a real declaration in this
guard's own file, so its own tree-wide run never sees them as anything but
prose):
`TestMisattachedDocCommentGuardCatchesThe0127Shape` reproduces the exact
corruption (a doc naming `BuildWelcomeEmail`, attached to a bare
`BuildAdminAlertEmail`) and asserts the guard names both symbols;
`TestMisattachedDocCommentGuardCleanOnceReattached` is the same fixture with
each function given its own doc — the historical fix pass's actual shape —
and asserts zero hits.

## Verification

`uptime` before the scoped run: load average 2.43/1.98/1.87, well under the
~8 threshold (§5).

`gofmt -l internal/handlers/misattached_doc_comment_guard_test.go
internal/handlers/subscribe_test.go`: clean. `go build ./...`: clean. `go
vet ./internal/handlers/... ./cmd/...`: clean.

`go test ./internal/handlers/ -run
'TestNoDocCommentNamesADifferentDeclarationInSameFile|TestMisattachedDocCommentGuard'
-v`: all six tests pass —
`TestNoDocCommentNamesADifferentDeclarationInSameFile` (the tree-wide guard,
zero hits after the `subscribe_test.go` fix),
`TestMisattachedDocCommentGuardCatchesThe0127Shape`,
`TestMisattachedDocCommentGuardCleanOnceReattached`,
`TestMisattachedDocCommentGuardSkipsMultiMemberGroupLevelDoc`,
`TestMisattachedDocCommentGuardStillChecksPerMemberDocInsideAGroup`,
`TestMisattachedDocCommentGuardIgnoresOrdinaryProseOpeners`.

`ISSUE=0267 scripts/check.sh go ./internal/...`: **VERIFICATION PASSED** —
`go build`/`go vet`/`gofmt -l` clean, all sixteen `internal/` packages report
`ok` (including `internal/db`'s PRD-parity guards), skip audit reports "(no
skips)", no leftover processes. Isolated database
`opencircuit_test_0267`, dropped on exit — confirmed absent afterward
(`psql … -tAc "select 1 from pg_database where datname='opencircuit_test_0267'"`
returned nothing).

**Dirty files skipped:** none — every file this guard's development touched
(`subscribe_test.go`) was confirmed clean via `git status --porcelain --
<path>` immediately before editing, and the only other file changed is the
new guard file itself.

## Review notes

**Verdict: pass.** Re-verified independently, not read-and-accepted. Everything
below was measured in a throwaway worktree at `64480e4`, so no mutation ever
touched the shared tree (two other agents had uncommitted work in
`internal/handlers`, `internal/auth`, `internal/mailing`, `internal/outbox`, and
`web/` throughout).

**End-to-end mutation proof, against the real incident rather than a fixture.**
`git show 2f5992b^:internal/mailing/transactional_templates.go` shows the actual
corrupted block: `BuildAdminAlertEmail`'s doc fused to `BuildWelcomeEmail`'s,
the whole thing sitting above `func BuildWelcomeEmail`, first word
`BuildAdminAlertEmail`. Reproducing that shape in the worktree (moving
`func BuildAdminAlertEmail` below `BuildWelcomeEmail`, which fuses the two
comment groups exactly as `41993c0` did) plus reverting `subscribe_test.go` to
`64480e4^` made `TestNoDocCommentNamesADifferentDeclarationInSameFile` fail
naming both hits with repo-relative, pasteable positions. `gofmt -l` on the
corrupted file: clean. `go vet ./internal/mailing/ ./internal/handlers/`:
clean. That is the toolchain gap the issue describes, measured rather than
asserted.

**Oracle independence (CLAUDE.md §8).** The oracle is a set of `t.TempDir()`
source fixtures; the subject is the detector. Neither can satisfy the other by
being edited, and both directions were mutation-checked:

| Mutation of the detector | Test that fired |
|---|---|
| `findMisattachedDocsInFile` returns nil | `…CatchesThe0127Shape`, `…StillChecksPerMemberDocInsideAGroup` |
| `len(d.Specs) == 1` → `>= 1` (group exclusion removed) | `…SkipsMultiMemberGroupLevelDoc` |
| the `!allNames[word]` gate disabled | `…IgnoresOrdinaryProseOpeners` |

**Fail-open.** The guard measures the files on disk — proved by the two hits
above, in two different scan roots. A *broken* root fails closed: pointing the
walk at `../../no-such-dir` produces `t.Fatalf("walk …: lstat …: no such file
or directory")`, not a silent pass. The one residual is an *empty* roots slice:
nothing asserts the walk visited a nonzero number of files, so emptying
`citedTestScanRoots` would silently disarm this guard **and** `#0196`'s, which
shares the variable. That is the established shape across the whole guard family
(`citation_guard_test.go`, `citation_target_guard_test.go`,
`dangling_test_citation_guard_test.go`, `audit_email_metadata_guard_test.go` —
none asserts a scan count), so it is not a regression this issue introduced.
**Recommend filing one follow-up issue** covering the family: assert each
tree-walking guard visited a plausible number of `.go` files before concluding
"no hits".

**The exclusions, checked rather than taken on trust.** Removing both exclusions
in the worktree and re-running the tree-wide guard produced exactly seven hits —
six group-level, one single-spec — confirming the implementer's count:

```
internal/handlers/public_list_stats.go:27      "PublicListStatsHandler" → pendingBucket
internal/mailing/campaigns.go:64               "Campaign"      → CampaignStatusDraft
internal/mailing/preflight.go:33               "Preflight"     → PreflightCodeSubjectEmpty
internal/mailing/worker_test_helpers_test.go:86 "newTestWorker" → testWorkerBaseURL
internal/subscribers/store.go:82               "Unsubscribe"   → SourceOneClick
internal/subscribers/suppressions.go:36        "Suppression"   → SuppressionReasonHardBounce
internal/workshops/store.go:85                 "Workshop"      → StatusDraft
```

The `public_list_stats.go` allowlist entry is justified: the comment's body is
substantively about `pendingBucket` and names it by identifier. Five of the six
group-level hits are the category-noun idiom and are correctly excluded.

**One correction to the record.** `## Implementation notes` claims of the six
group-level hits that "None is `#0267`'s defect shape". That is not true of
`internal/mailing/worker_test_helpers_test.go:86`: `// newTestWorker constructs
a Worker wired to pool/mailer/progress with fast defaults … and its own
SendStore.` sits above the `const (testWorkerBaseURL …)` block, leaving `func
newTestWorker` with no doc of its own — structurally the same
"undocumented declaration, its doc glued to the adjacent one" shape as the
`subscribe_test.go` instance this pass *did* fix. Its second sentence does
describe the constants, and it is an unexported test helper, so the stakes are
low; it is recorded here rather than bounced.

It is not, however, a reason to narrow the exclusion, and I checked the obvious
narrowing before saying so: "check a group-level doc when its first word names a
**function** declared in the same file" would still flag `Preflight`
(`preflight.go:150 func Preflight`) and `Unsubscribe`
(`store.go:824 func (s *Store) Unsubscribe`), both legitimate. No cheap
structural signal separates the real instance from the benign five, exactly as
the guard's header argues. **Recommend filing a follow-up issue** to move that
one comment onto `func newTestWorker` and to record that the group-size
exclusion is currently hiding it.

**One shape the guard would false-positive on**, noted for whoever meets it: a
*single*-member parenthesised `const (…)` / `var (…)` block carrying a
category-noun doc is treated as a single-spec declaration and IS checked, so
`// Widget values recognized by the catalog.` over a one-member block naming
`Widget` would flag. No such instance exists on the tree today, and
`misattachedDocKnownBenignPairs` is the intended escape hatch.

**The `subscribe_test.go` fix** is correct and complete: the two-line comment
now sits directly above `func (c *countingSubscriberStore) snapshot()`, which
is what it describes, and `subscriberStoreCallCounts` is left undocumented in
line with the file's other unexported helper types. The guard reports the file
clean afterwards, and fails on it before.

**Verification runs.**

- `go test ./internal/handlers/ -run 'TestNoDocCommentNamesADifferentDeclarationInSameFile|TestMisattachedDocCommentGuard' -v -count=1` in a clean worktree at `64480e4`: all six PASS.
- `ISSUE=0267r scripts/check.sh go ./internal/...` in that worktree:
  **VERIFICATION PASSED** — `go build`, `go vet`, `gofmt -l` clean; sixteen
  packages `ok` (DB-backed `auth`, `audit`, `db`, `handlers`, `middleware` all
  reported nonzero times); skip audit "(no skips)"; no leftover processes.
- The same command in the *main* checkout failed, for a reason unrelated to this
  issue: another agent's in-flight uncommitted work (`internal/handlers/subscribe.go`,
  new `subscribe_intake.go`, a changed `NewSubscribeHandler` signature) does not
  currently compile against `subscribe_test.go`. `internal/handlers` did report
  `ok 4.194s` in an earlier full `./internal/...` run before that edit landed.
- Criterion "runs in `check.sh`'s default Go scope": confirmed —
  `scripts/check.sh:812` defaults to `./internal/... ./cmd/... ./web/...`.
- Cleanup: worktree removed, `opencircuit_test_0267r` confirmed absent,
  `pgrep -f 'go test|\.test '` empty.

**For the orchestrator to file (subagents do not file issues, CLAUDE.md §9):**
two follow-ups — (1) the scan-count anti-fail-open assertion across the guard
family, (2) the `worker_test_helpers_test.go:86` comment and the note that the
group exclusion hides it.

## Work log

- 2026-08-27 — Implemented (phase 2, Sonnet). New guard
  `internal/handlers/misattached_doc_comment_guard_test.go`; one real
  pre-existing instance of the defect fixed in
  `internal/handlers/subscribe_test.go` (file was clean before and
  immediately before the edit). Left `in-progress` for phase-3 review.
