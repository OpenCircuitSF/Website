# 0348 — Two comments twelve lines apart give opposite answers about claim order

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | mailing |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | — |

## Description

`internal/mailing/outbox_worker_test.go:774-784` — the checkpoint comment
inside `TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued` — says which row gets
claimed first is not guaranteed.

That was true under `ClaimDue`, whose `UPDATE … RETURNING` gave no ordering
guarantee. It is false under `SelectDue`, which `#0297` substituted: a plain
`SELECT … FOR UPDATE SKIP LOCKED` with `ORDER BY next_attempt_at, id`, whose
result the caller then iterates **in order**. Claim order is now deterministic.

The result is two comments twelve lines apart in the same test giving opposite
answers to the same question — the neighbouring text, corrected by `#0342`'s
third pass, states that `id2` is deterministically never claimed.

**Only the reasoning is stale.** The test's pair-based assertion is still the
right assertion and must stay: asserting a specific row by name would pin
behaviour the test does not exist to pin, and a later legitimate ordering
change should not have to rewrite it.

Out of scope for `#0342`: `ClaimRow` is not in that issue's criterion 3 keyword
list, and rewriting this reasoning is judgement rather than the transcription
its remaining pass was scoped to.

## Acceptance criteria

1. The checkpoint comment states the current guarantee: `SelectDue`'s
   `ORDER BY next_attempt_at, id` plus in-order iteration makes claim order
   deterministic, attributed to `#0297`.
2. It does not contradict the neighbouring comments in the same test, which
   say `id2` is never claimed. Read all of them together and make them
   consistent.
3. **The pair-based assertion is unchanged.** This is a comment-only issue; if
   the implementation finds itself editing an assertion, it has left scope and
   should stop and report.
4. Comment-only, proved structurally — parse both revisions without comments
   and compare bytes, with the tool proved falsifiable first and failing closed
   on empty input (§8).
5. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal is
   introduced into any Go comment (see `#0342` criterion 5); re-run
   `scripts/go_file_visit_floor_guard_test.sh` and record the population.
6. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout (§8).

## Relation

- Found by: [#0342](0342.md)'s second review, which scoped it out deliberately
- Depends on: [#0342](0342.md) — whose third pass rewrites the neighbouring
  comment this must agree with; land that first
- Relates to: [#0297](0297.md) — the `ClaimDue` → `SelectDue` substitution that
  made the ordering deterministic
- Relates to: [#0303](0303.md) — which removed the variadic `ClaimDue` form
- Relates to: [#0347](0347.md) — the guard that would catch this class

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0342`'s second review (§9).
- 2026-08-31 — Implemented. Confirmed `#0342`'s third pass
  (`dc3d7aa`) is landed: the neighbouring comment at
  `internal/mailing/outbox_worker_test.go:743-745` already reads "id2 is
  deliberately left 'queued' … so id2 is never claimed at all (#0297)".
  Read `pass` in `internal/mailing/outbox_worker.go` to re-verify the
  underlying claim rather than trust the issue text: `SelectDue` (called
  once per pass, `ORDER BY next_attempt_at, id`) returns `ids`, and the
  very next loop is a plain `for _, id := range ids` — no reordering, no
  concurrency — so which row is claimed first is deterministic given
  `SelectDue`'s ordering; it is not a race. That makes the checkpoint
  comment's "NOT guaranteed to be id1" claim (the old text) stale, exactly
  as the issue describes, and consistent with the now-correct neighbouring
  comment rather than contradicting it.

  Edited only the checkpoint comment at `outbox_worker_test.go:772-786`
  (line numbers shifted slightly by the edit itself). Replaced the
  "NOT guaranteed to be id1 … pass's own loop order … is what determines
  which gets claimed first" clause with a statement that claim order is now
  deterministic under `#0297`'s `SelectDue` ordering plus in-order
  iteration, contrasted with the pre-`#0297` `ClaimDue`-based pass whose
  `UPDATE … RETURNING` gave no ordering guarantee at all — matching the
  issue's own description of the two regimes. Kept the rationale for the
  pair-based assertion (criterion 3): it does not pin a specific id because
  that would encode the loop's current iteration order rather than the
  property the test exists to check, and a later legitimate change to claim
  order should not force a rewrite. Left the `#0264`-review historical
  sentence in place, since it describes a past defect (pre-`#0297`, when
  order genuinely was not guaranteed) and is not part of what this issue
  flags as stale.

  **The pair-based assertion itself
  (`sendingBefore != 1 || queuedBefore != 1` and its post-Stop counterpart)
  was not touched** — confirmed by the structural proof below, which shows
  the only difference between the two revisions is comment text.

  Comment-only, proved structurally with the same tool built for `#0345`
  (standalone `go/ast`-fingerprint binary: `parser.ParseFile` mode `0` to
  drop comments, `go/format`, sha256; proved falsifiable against a
  deliberate literal-value edit and fail-closed on an empty file before
  trusting it). Fingerprint of the pre-edit and post-edit
  `outbox_worker_test.go` is identical.

  Confirmed no `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax
  literal was introduced (checked the `+` lines of `git diff` against the
  harness's own `\.(ClaimDue|OrphanSweep|SelectDue)\(` pattern) and re-ran
  `scripts/go_file_visit_floor_guard_test.sh`: population 42 (total),
  non-exempt population 12 — both unchanged by this edit.

  `gofmt` stability: `sha256(file) == sha256(gofmt stdout)`, confirmed.

## Verification

```
ISSUE=0345 scripts/check.sh go ./internal/mailing/... ./internal/outbox/... ./internal/handlers/...
```
(run once, covering both `#0345` and `#0348` in the same batch) —
`go build`, `go vet`, `gofmt -l` clean, and `go test` for all three
packages: `internal/mailing` ok, `internal/outbox` ok, `internal/handlers`
ok, zero skips (fresh per-issue database `opencircuit_test_0345`). Also ran
scoped, in its own database:

```
ISSUE=0348 scripts/check.sh go ./internal/mailing/... -run TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued
```
— `internal/mailing` ok, confirming
`TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued` itself passed, unchanged in
behaviour since only its comment moved. `scripts/check.sh guards` also run
and passed in full. Both test databases dropped afterward; confirmed absent.
No leftover `go test`/`.test` processes.
