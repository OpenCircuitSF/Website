# 0231 — Place the green-phosphor CRT image in the home hero

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Web |
| **Module** | web / design |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-24 |

## Description

The user supplied `CRT.png` — a photograph of a Zenith Data Systems green-phosphor
monitor displaying `> Open Circuit SF _` — and wants it in the **top section of
the home page, on the right**, where the hero currently has empty space, with
the image's background blending into the page.

It is a good fit for the brand: the site's whole visual language is a terminal
motif, and this is the physical object that motif refers to. The screen even
shows the site's own name at its own prompt.

**Three things make this more than dropping in an `<img>`.** Each is measured,
not guessed.

**1. The hero's LCP element is currently the headline, by construction.**
`web/src/views/Home.svelte:10-11` says so explicitly: *"The headline is the
page's LCP element by construction: no images in the hero, just type — see PRD
§7.6's LCP budget (<2.0s on 4G)."* An above-the-fold hero image becomes the LCP
element. **The source file is 1,355 KB** — roughly five times the entire
JS bundle (239 KB raw, 75 KB gzipped). Shipping it as-is would blow the budget
outright.

**2. The site has a light theme, and the image's background is baked in.**
`web/src/app.css:21` sets `--bg-page: #F4F6F3` for light and `#0A0D0B` for dark
(`:117`, `:154`), switched by both `prefers-color-scheme` and an explicit
toggle. Sampling `CRT.png`'s edges gives a uniform **`#181d17`** at all four
corners and all three mid-edges.

That is very close to dark mode's `--bg-subtle` (`#182018`) — so on dark it will
blend almost perfectly. **On light it will be a dark rectangle in the middle of
a near-white page.** This is the same trap `CLAUDE.md` §8 records for the logo:
*"Transparency does not make a logo theme-adaptive"* — brand green measures
14.8:1 on the dark ground but 1.32:1 on white.

**3. It is a photograph of a real product.** The Zenith wordmark and "data
systems" lettering are legible on the bezel. Worth a moment's thought about
whether that matters for a marketing page; if it does not, say so and move on.

## Acceptance criteria

- [x] The CRT image appears in the home hero's right-hand space, at desktop widths — inside the terminal panel, text left, photo right, per the user's correction
- [x] Its background blends into the page ground rather than reading as a pasted rectangle — **verified in both light and dark themes**, not only the one the developer happens to be using — each theme pins to its own photograph's decoded backdrop (`#191C17` / `#E8EEE5`); review confirmed border-pixel mode matches the pinned CSS exactly, and all four theme states render correctly including the two cross cases
- [x] The narrow-viewport layout stays sound: the image either reflows sensibly or is dropped below a breakpoint, and the page never scrolls horizontally at 390 px — stacks below the text at 240px; review measured `scrollWidth === clientWidth === 390`
- [x] Total added weight is stated in the issue and fits PRD §7.6 — the 1,355 KB source must come down substantially — 3,786 / 10,500 bytes (dark) and 6,752 / 19,354 (light), one asset per theme per load; **6.6 KB at 1x**, down from 1,355 KB
- [x] LCP on the home route is measured before and after, with the number recorded. If the image becomes the LCP element, say so and show it still meets the budget — it does **not** become the LCP element; see the Budget check above
- [x] The image is marked up honestly for assistive technology — decorative (`alt=""`) or described, whichever it actually is — it is a CSS background carrying no text alternative, which is correct: it repeats what the adjacent headline and command line already say
- [x] `Home.svelte`'s "no images in the hero" comment is corrected, since it will no longer be true — review confirmed it is gone
- [x] `scripts/check.sh web` passes — 274 files / 0 errors, 33 test files / 3,829 tests

## Notes

**On blending, the honest options, in rough order of durability:**

- **Cut the monitor out** to a transparent PNG or WebP. Works on any ground, survives a future palette change, and is the only option that is genuinely theme-independent. Costs the most effort and risks a halo on the CRT's soft glow.
- **A radial or linear fade** to transparent at the edges, so the baked background dissolves into whatever is behind it. Cheap, and good enough given `#181d17` is already within a hair of `--bg-subtle` on dark — but it still darkens a light page.
- **Two assets**, one per theme, swapped with `<picture>` + `prefers-color-scheme` or the existing theme attribute. Honest, doubles the bytes.
- **Confine the hero to a dark surface regardless of theme**, so only one ground exists. A design decision rather than an asset one — record it as such if chosen.

**Do not** simply set the image's background colour to a token and hope; the
photograph has a soft vignette, so a flat fill will band visibly against it.

`CLAUDE.md` §8 also records that **headless Chromium enforces a ~485 px minimum
window width**, which silently clips narrower renders. Safari via `safaridriver`
does not have that limit — the session that produced this project's screenshots
captured 390 px cleanly — so use it for the narrow-viewport check.

The source image is at `../CRT.png` relative to the repo root. Static assets
live in `web/public/`, which is where the logo and icon set already sit.

## Relation

- Relates to: [#0015](0015.md) — the Home view, whose LCP note this changes
- Relates to: [#0011](0011.md), [#0013](0013.md) — the design tokens and terminal motifs this must sit inside
- Relates to: [#0063](0063.md) — the accessibility audit, which will look at this image

## Implementation notes

**Do not mark this issue `resolved` from this section** — that is the
reviewer's call.

**Placement.** The image lives *inside* `TerminalPanel`'s own body, not
beside it. `TerminalPanel` renders `{@render children()}` inside one
`.terminal-body`; the hero's children are now a single `.hero-row` flex
container with `.hero-text` (the existing Prompt/headline/StatusList/
CommandLine block) on the left and `.hero-crt` (the photo) on the right, so
both sit inside the same bordered, titled panel. An earlier draft of this
work put the image as a sibling of `<TerminalPanel>` — a second box next to
the terminal rather than inside it — and was corrected after the user
looked at the rendered page.

**Blending approach — a hybrid, not any one of the Notes' four options.**
`CRT.png`'s backdrop is unusually uniform (measured: every corner and
mid-edge sample is within a few values of `rgb(25,30,23)` / `#181d17`), so
rather than a manual cutout or a geometric vignette, the alpha matte was
built by **color-distance keying against that measured backdrop colour**:
pixels close to it fade toward transparent, pixels far from it (the green
screen glow, the white bezel highlights, the red power LED) stay opaque,
with a Gaussian blur on the alpha channel and a geometric edge feather
(clamped to the crop rectangle) so there is no hard cutout edge and no
banding. This is effort-cheap (a ~30-line Pillow/numpy script, no manual
masking) but gets close to the option-1 "cut the monitor out" quality,
because the photo's own soft vignette already does most of the blending
work for a color key.

That alpha-matted cutout is then composited into `.crt-frame`, a small
panel with a **fixed** background (`#181d17`, the same measured value) and
the ordinary `--border` token for its edge — i.e. option 4 from the Notes,
"confine to a dark surface regardless of theme," but scoped to a small
frame around just the photo rather than the whole hero.

**Why a fixed frame instead of the panel's own background, and why not two
theme assets.** Before deciding, I composited the cutout over three real
backgrounds to check by eye rather than assume:
- `--bg-page` light (`#F4F6F3`) and `--bg-panel` light (`#FFFFFF`) — both
  ghost badly. The photo's own dark tones (screen bezel, cabinet) are close
  enough to its own backdrop colour that the color-key alpha thins them out
  too, so on a light ground the monitor doesn't blend, it *washes out* —
  a translucent grey ghost, worse than a flat pasted rectangle would have
  been. This confirms the issue's prediction and rules out placing the
  cutout directly on any light surface.
- `--bg-panel` dark (`#121712`) — no visible seam. Close enough to the
  photo's own `#181d17` backdrop that this would have worked without a
  frame at all in dark mode specifically.
- `--bg-page` dark (`#0A0D0B`, darker still) — also no visible seam.

So dark mode's own surfaces would have worked unframed, and the "one
colour" simplification the correction asked me to check for is real **for
dark mode alone** — but light mode has no surface in this design system the
cutout can sit on directly. Rather than branch the treatment (one photo
placement for dark, a framed one for light), `.crt-frame`'s fixed
`#181d17` is used in both themes: one asset, one background value, verified
seamless in dark and by design in light (screenshots below). The cost:
in light mode the frame is a visibly dark box inside a white terminal
panel — not "invisible," but read as a deliberate screen-in-an-alcove
element, consistent with the terminal panel's own bordered-card language,
not a mistake. `.crt-frame`'s background is a literal hex value rather than
a design token *on purpose* — the whole point is that it must not change
with the theme the way every other token does.

**Responsive behaviour and the `alt=""`/`src` fix.** The first draft used
`<picture><source media="(min-width:860px)">` with an `<img>` carrying no
`src`, intending to skip the fetch entirely below 860px. The user's
correction flagged this as broken markup (an `<img>` with no fallback
`src`) and asked for a real fix. The corrected markup gives `<img
class="crt-photo" src="/hero-crt-380.webp">` a real, valid `src` (the 1x
desktop asset, reused as the sub-860px asset too) and keeps `<source
media="(min-width: 860px)">` only to offer the 2x asset to retina desktops
via `srcset="/hero-crt-380.webp 1x, /hero-crt-760.webp 2x"`. Consequence:
the image is **never removed from the DOM** at any width and its bytes are
paid regardless of viewport, so `.hero-row` reflows instead of hiding —
`flex-direction: column` under 860px (text first, photo stacked below at
220px) and `row` at 860px+ (text left, 380px-wide photo right). Since the
download happens either way, showing a smaller version on narrow viewports
wastes nothing that hiding it would have saved.

**Accessibility — `alt=""`.** The correction asked me to state this
explicitly rather than assert it, since the screen does read "> Open
Circuit SF _", which is arguably content. I kept `alt=""` because that
exact information — the org name and the terminal-prompt framing — is
already present as real, adjacent text in the same panel: `<Prompt
text="open_circuit // san_francisco">`, the `h1` headline, and
`<CommandLine command="opencircuitsf.com">` all sit in `.hero-text`,
inches from the photo. A screen-reader user reaches the org name and the
prompt motif through those actual text nodes; the photo adds a physical,
photographic reinforcement of the terminal motif that a sighted user gets
and a screen-reader user doesn't need repeated. Describing it (e.g. `alt="A
green-phosphor CRT monitor displaying > Open Circuit SF _"`) would mostly
duplicate `.hero-text` word-for-word for AT users, which is worse than a
clean decorative skip. Marked `decoding="async"`, no `aria-hidden` needed
since `alt=""` on an `<img>` (not a background image) is itself the
decorative signal.

**Weight.** Source `CRT.png` is 1,402×1,122, 1,387,611 bytes (RGB, no
alpha). Processing (crop to content + padding, alpha-matte per above,
Lanczos resize, WebP encode at quality 82):
- `web/public/hero-crt-380.webp` — 380×354, **37,198 bytes** (~36 KB) — the
  asset every viewport downloads (desktop 1x and all narrow widths, per the
  `src` fallback above)
- `web/public/hero-crt-760.webp` — 760×707, **100,010 bytes** (~98 KB) —
  desktop retina only, via the `<source>`'s `2x` descriptor

So the deliberate cap: **every viewport pays at most ~36 KB**, and only
non-narrow retina displays pay the extra ~62 KB for the 2x asset. Combined
JS+CSS for the whole SPA is currently 246.42 KB raw / 77.30 KB gzip (from
`npm run build`), so the 1x image (~36 KB) is a modest addition and well
inside PRD §7.6's budget on any of the standard levers (modern format,
real intrinsic dimensions, `width`/`height` reserved via attributes,
responsive `srcset`).

**LCP — measured before and after, via `safaridriver` (not Chromium — see
the CLAUDE.md §8 width-limit note) against the built `web/dist/`, served
unthrottled over local HTTP** (`python3 -m http.server`, no network
throttling available in this environment — the absolute times below are
not "4G," but the *relative* comparison and the element identification are
real):

| | Before | After |
|---|---|---|
| 1440px LCP element | `h1.headline` (the headline) | **`h1.headline`** — unchanged; see Fix pass 2 |
| 1440px LCP entry size (px²) | 78,739 | 65,727 (re-measured on the shipped build) |
| 1440px LCP time (unthrottled) | ~52 ms | ~28–52 ms (two runs, noise-level difference) |
| 390px LCP element | `h1.headline` | **`h1.headline`** — unchanged; see Fix pass 2 |

**The headline remains the LCP element at every width measured** — `h1.headline`
at 65,727 px² (1440) and 19,089 px² (390), with `.crt-frame` never
appearing as a candidate. Measured on the shipped build with a buffered
`largest-contentful-paint` observer.

**An earlier version of this paragraph asserted the opposite**, and said so in
bold directly beneath a table that had already been corrected. It survived two
review passes. Its reasoning was not silly — the frame's box (115,554 px² at
1440) genuinely does exceed the headline's (88,452 px²), so *if* a CSS
background were a candidate it would win. It is not one in WebKit, which is why
the conclusion was wrong while the arithmetic was right. Recorded rather than
deleted, because "sound reasoning, wrong premise" is the harder error to catch
in review than a plain miscalculation.

**Budget check.** PRD §7.6 wants LCP under 2.0s on 4G. Since the photo is *not*
the LCP resource, it does not sit on that path at all — it competes for
bandwidth rather than gating the metric. Added weight is **6.6 KB at 1x**
(light; 3.7 KB dark) and **18.9 KB at 2x** (light; 10.3 KB dark), one asset per
theme per load, verified by Resource Timing. At a conservative Slow-4G profile
(~1.6 Mbps) that is ~33 ms and ~95 ms of transfer respectively, against an
existing critical path of ~77 KB gzipped JS/CSS. The budget holds with room to
spare — and by a wider margin than the superseded figures suggested, since those
were taken against the 37 KB / 100 KB keyed-cutout assets that no longer exist.

Caveat, unchanged and real: background-image LCP candidacy is engine-specific,
and every figure here is from WebKit only. No other engine has been measured.

**Verified in `docs/obstacles.md` terms:** no privilege/tool obstacles hit.
`safaridriver` at `/System/Cryptexes/App/usr/bin/safaridriver`, Pillow
11.3.0 already available via the system `python3`, no ImageMagick/`cwebp`
needed. Ports used for verification (8931/8932, then 8941/8942 after the
placement fix) were freed on exit; no leftover `go test`/`.test` processes.

## Verification

- `uptime` before every run — machine idle (load average 1.5–5.4 across
  runs, well under the ~8 threshold CLAUDE.md §5 calls out).
- `(cd web && npm run check)` — `svelte-check`, 269 files, **0 errors, 0
  warnings** (both before and after the placement fix).
- `ISSUE=0231 scripts/check.sh web` — ran three times (initial pass, and
  again after the placement/markup fix) — **29 test files / 3,779 tests
  passed** every time, identical count to the pre-#0231 baseline (confirmed
  by stashing this issue's changes and re-running `npm test`: also 29/3,779).
  This issue added no new unit tests — the change is presentational markup
  and a static asset with no new branchable logic — so the identical count
  is expected, not a gap.
- `npm run build` (`vite build`) succeeded before and after; `web/dist/`
  build artifacts were reverted with `git restore -- web/dist/index.html`
  after each manual verification build, since that file's committed
  content is a deliberate placeholder (`.gitignore` / `web/.gitignore`
  comment) — see this repo's convention that only `web/dist/index.html` is
  tracked, not the rest of `dist/`.
- Visual verification via `safaridriver`, driven with `execute/sync` and
  `execute/async` (not the flaky element-click/Actions APIs), against the
  production build served over local HTTP — screenshots in `issues/0231/`:
  - `issues/0231/1440-dark.png` — 1440px, dark theme
  - `issues/0231/1440-light.png` — 1440px, light theme
  - `issues/0231/390-narrow.png` — 390px, confirms `document.documentElement.scrollWidth === clientWidth === 390` (no horizontal scroll) and that the `crt-photo` `<img>` loaded the 380w asset with `complete: true`
- All test servers and the `safaridriver` session were torn down; confirmed
  no process left listening on the ports used and no leftover `go
  test`/`.test` processes (`pgrep -f 'go test|\.test '` empty).

## Review notes — phase 3, 2026-08-24

**Bounced.** Commits reviewed: `cc670fa` (initial placement), `742eb8e` and
`ca65cad` (revision 1), `5b01342` (revision 2 — the shipped state). First
review of this issue; revisions 1 and 2 were written
directly by the orchestrator during live iteration and had never been read by
anyone else, which its own `## Work log` says. Most of it is very good and the
three things that work log asked me to scrutinise were the right three — but
**one of them is genuinely wrong**, and a comment in the shipped file asserts
the opposite of what the browser does. Two more comments describe an element
that no longer exists, and two acceptance criteria record evidence for an
implementation that was replaced twice.

Nothing here is a security or data problem, and the page is correct on every
engine anyone is likely to be using. It is bounced because the file now
contains claims that are false, and because a reviewer who lets a false
comment stand has left a trap for the next person.

### Blocking — 1. The `image-set` fallback does not fall back

`Home.svelte`'s `.crt-frame` comment says:

> The 1x/2x pair is selected by `image-set`; the plain `url()` above it is the
> fallback for engines that do not support `image-set`, **which get the 1x
> asset rather than nothing.**

They get nothing. The two-declaration fallback idiom works for *literal*
values, but **not through `var()`**. Per CSS Variables §3.2, a property
containing `var()` is assumed valid at parse time and only syntax-checked
after substitution; if the substituted value does not parse, the declaration
is *invalid at computed-value time*, and the property becomes **unset — the
initial value (`background-image: none`)**, rather than falling back to the
earlier declaration. The earlier `background-image: var(--crt-photo)` is
overridden and never used.

Measured in Chromium rather than argued from the spec — two probes in the same
page, one through `var()` and one with literal values, both with a deliberately
unparseable second declaration:

```
viaVar   (background-image: var(--x); background-image: var(--y))  -> "none"
viaPlain (background-image: url(…);   background-image: notafn(1)) -> "url(…)"
```

So on an engine with custom properties but no unprefixed `image-set()` —
Safari ≤ 16 and Chrome < 113 are the real populations — the hero shows an
empty 380×304 box, not the 1x photo. It degrades quietly (the box is the same
colour as the panel behind it) which is why nobody would notice, and it is
exactly the kind of thing that only gets caught by testing the claim instead
of reading it.

**What the next pass must do:** either make the fallback real — an
`@supports (background-image: image-set(url(x) 1x))` block around the
`image-set` declaration, or set `background-image` directly in the two theme
rules instead of routing it through a custom property — or delete
`--crt-photo` and rewrite the comment to say plainly that engines without
`image-set` get no photo, which is a defensible choice. What must not survive
is a comment asserting a fallback that does not happen. Whichever is chosen,
prove it the way this finding was proved, not by reasoning.

### Blocking — 2. Two comments describe an `<img>` that no longer exists

- The `.hero-row` CSS comment: *"It is never removed from the DOM at any
  width, **because the `<img>` always carries a real `src`** (see the script
  comment)"*. There is no `<img>`; revision 2 replaced it with a CSS
  background on an empty `<div class="crt-frame">`. The conclusion is still
  true, but the stated reason is not, and it points at a script comment that
  now says the opposite.
- The `<script>` comment: *"At >=860px that split is a **two-column grid**"*.
  It is `display: flex` with `flex-direction: row`; there is no grid in
  `.hero-row`.

Both are cheap to fix and both will mislead the next reader of this file.

### Non-blocking, but fix while you are in here — stale recorded evidence

Two acceptance criteria have evidence recorded against the deleted `<img>`
implementation, so they cannot be ticked as they stand:

- **Weight.** `## Implementation notes` still states `37,198` and `100,010`
  bytes. The shipped assets are `hero-crt-380.webp` **3,786 B**,
  `hero-crt-760.webp` **10,500 B**, `hero-crt-light-380.webp` **6,752 B**,
  `hero-crt-light-760.webp` **19,354 B** (measured). Revision 1 records the
  3,786 figure in passing; the light pair's weight is stated nowhere. The real
  number is excellent — **≤ 6.6 KB fetched per page view at 1x** — and
  deserves to be the one written down.
- **LCP.** The table identifies `img.crt-photo` as the LCP element. That
  element does not exist; the LCP candidate is now a CSS background, which is
  a different kind of LCP entry. Re-measure or re-scope the claim.

### What was re-verified and does hold — all of it in a real browser

Chromium via Playwright against a **real `vite build`** written to a scratch
`--outDir` (never the shared `web/dist/`; `web/dist/index.html`'s SHA-256 was
identical before and after, `git status --porcelain -- web/dist/` empty), on a
private HTTP server bound with `allow_reuse_address = False` and identified by
a marker string asserted present before any measurement (`CLAUDE.md` §8b).
Both servers were shut down; `pgrep -f 'go test|\.test '` empty.

- **The two-theme swap answers to both signals**, which is the whole reason the
  `<picture>` was abandoned — and the two *cross* cases are the ones that
  matter, since a `<picture>` would get them wrong:

  | OS `prefers-color-scheme` | `data-theme` | asset fetched | `--bg-panel` |
  |---|---|---|---|
  | light | (absent) | `hero-crt-light-380.webp` | `#e8eee5` |
  | dark | (absent) | `hero-crt-380.webp` | `#191c17` |
  | **light** | **dark** | `hero-crt-380.webp` | `#191c17` |
  | **dark** | **light** | `hero-crt-light-380.webp` | `#e8eee5` |

- **Exactly one asset per load**, confirmed by Resource Timing rather than by
  inspection: each row above fetched **one** `hero-crt*` resource. At
  `devicePixelRatio: 2` the single fetch was `hero-crt-760.webp` — the 1x is
  not also pulled.
- **`aspect-ratio` fully replaces the `<img>`'s intrinsic-size CLS
  protection — the work log's third question, answered yes.** With every
  `hero-crt*.webp` request hung indefinitely by a route interceptor, the frame
  still measured **380 × 304.09** at DOM-ready. A fixed `width` plus
  `aspect-ratio` is *more* deterministic than an `<img>`, which needs
  `width`/`height` attributes and still depends on layout to resolve a fluid
  width. Declared `1402 / 1122` (= 1.24955) against the assets' true
  `380 × 304` (= 1.25000) stretches the photo by 0.09 px vertically — 0.04%,
  not worth changing.
- **The decoded-vs-source colour reasoning is exactly right**, and this is the
  claim I most expected to find loose. Measured as the mode of the border
  pixels of the decoded WebP:

  | asset | border mode | modal share | pinned in CSS |
  |---|---|---|---|
  | `hero-crt-380.webp` | `#191C17` | 90.1% | `--bg-panel: #191c17` ✓ |
  | `hero-crt-760.webp` | `#191C17` | 85.7% | ✓ |
  | `hero-crt-light-380.webp` | `#E8EEE5` | 95.0% | `--bg-panel: #e8eee5` ✓ |
  | `hero-crt-light-760.webp` | `#E8EEE5` | 98.5% | ✓ |

  And the source PNGs confirm the shift the comment describes:
  `CRT-light.png`'s border is `#E9EEE6` at **100%** and decodes to `#E8EEE5`.
  (`CRT.png`'s dark border modes at only 28.5% — a genuinely noisy field — so
  its stated `#181D17` is approximate; the load-bearing half, the *decoded*
  value, is exact.) Both sources are `1402 × 1122`, as the comment says.
- **No seam, proven numerically as well as by eye.** Sampling a row across the
  frame boundary in a rendered screenshot: the panel ground reads `#E8EEE5`
  (light) / `#191C17` (dark) at x=600 and x=650 — outside the photo — and the
  identical value at the far right margin. The only step on that row is the
  monitor's own bezel. Screenshots at 1440 px in both themes show a monitor
  floating on the panel with no rectangle, border or halo.
- **No horizontal overflow at 390 px**: `document.documentElement.scrollWidth
  === clientWidth === 390`, `document.body.scrollWidth === 390`. The row stacks
  and the frame renders 240 × 192.
- **`scripts/check.sh web`** (as part of `ISSUE=0235rev scripts/check.sh`):
  `npm run check` **274 files, 0 errors, 0 warnings**; `npm test` **33 files /
  3,826 tests passed**. (The counts differ from the `29 / 3,779` in
  `## Verification` because other issues have landed tests since; nothing here
  regressed.) Grep confirms no test anywhere references `hero-crt`,
  `crt-frame` or `crt-photo`, so the suite says nothing about this change
  either way — which is honest, but it is also why the browser work above was
  the actual review.
- **The "no images in the hero" LCP comment is gone** from `Home.svelte`;
  `grep` finds it nowhere in `web/`.

So six of the eight acceptance criteria are verifiably met and two carry stale
evidence. Fix the three comment/fallback items and re-record the two numbers,
and this is a clean pass.


## Work log

**2026-08-24 — implementation (Sonnet subagent).** Placed the photo, keyed a
transparent cutout, measured the LCP shift. Everything above this heading is
that pass's own record; parts of it describe an `img.crt-photo` element that no
longer exists — see the two revisions below.

**2026-08-24 — revision 1, blend (orchestrator, direct).** User: the photo was
outside the terminal panel, and then carried a visible border. Two keying
attempts failed — a tolerance sweep (8→46) and a morphological close/open —
because the monitor's dark right flank and bottom shadow are indistinguishable
from its backdrop in the source photograph. Inverted the approach instead:
**drop the alpha channel entirely and pin the panel's background to the photo's
own backdrop**, so the two are the same surface and no cutout is needed. Cut
the asset from 37,198 to 3,786 bytes as a side effect. Commits `742eb8e`,
`ca65cad`.

**2026-08-24 — revision 2, light mode (orchestrator, direct).** Pinning one
dark photo forced the whole hero to stay dark in light mode — one dark band
across the top of an otherwise pale page, which revision 1 dressed with an
ambient shadow rather than fixing. The user had a second photograph produced
against a light backdrop (prompt at `../CRT-light-prompt.md`, kept for the
record); each theme now pins to its own photo.

Three things worth knowing if this is touched again:

- **The `<img>` became a CSS background.** A `<picture>` cannot do this job:
  `<source media="(prefers-color-scheme: dark)">` reads only the OS preference,
  so the header's explicit light/dark toggle (`lib/theme.ts` writes
  `data-theme` on `<html>`) would leave a dark photo on a light panel. Two
  `<img>`s toggled by `display` would fetch both assets. A background driven by
  a custom property answers to both signals and fetches one — **verified by
  Resource Timing on a real load: dark fetched only `hero-crt-380.webp`, light
  only `hero-crt-light-380.webp`.** The photo is decorative (it repeats what
  the adjacent headline and command line already say), so it carried `alt=""`
  as an `<img>` and needs no text alternative as a background.
- **The pinned colors are the WebP's *decoded* values, not the source PNG's.**
  Lossy encoding shifts a flat field by a step or two: `#E9EEE6` decodes to
  `#E8EEE5`, `#181D17` to `#191C17`, and it is the decoded value the browser
  paints beside the panel background. Measured as the mode of the border pixels
  (95–98% of them for the light asset). Lossless holds the source values
  exactly but costs 262 KB against 19 KB at 2x — not worth one 255th.
  Re-measure if either asset is re-encoded.
- **Panel styling now goes through TerminalPanel's own custom properties**
  (`--bg-panel`, `--bg-header`, `--border`) rather than `:global()` overrides of
  its internals, and text tokens are no longer overridden at all — each theme's
  palette already contrasts correctly against its own photo backdrop. Revision
  1's light-mode ambient shadow is gone with the dark band that motivated it.

**Verification of revision 2.** `npm run check` — 269 files, 0 errors, 0
warnings. `npm test` — 29 files / 3,779 tests passed, unchanged from baseline
(no test referenced `.crt-photo`, `.crt-frame`, or `hero-crt`; confirmed by
grep across `web/src`, `cmd`, and `internal` before the markup change).
`npm run build` succeeded and the built CSS was checked to contain both
resolved asset URL pairs and both theme branches. Visual verification via
`safaridriver` at 1440px against the production build over local HTTP, driving
the app's real theme mechanism (`data-theme` on `<html>`) rather than an OS
setting: `/tmp/crt-light-mode.png` and `/tmp/crt-dark-mode.png` both show the
monitor seamless on the panel ground with no border, seam, or halo.
`web/dist/index.html` was restored after building (`#0141`'s tracked
placeholder). safaridriver session deleted and the driver killed.

**Not yet reviewed.** Revisions 1 and 2 were written directly by the
orchestrator during live iteration with the user rather than dispatched to an
implementer, so no phase-3 review has run against them. A review should
scrutinise that, and specifically: the decoded-vs-source color reasoning, the
`image-set` fallback path, and whether `aspect-ratio` on a background element
fully replaces the `<img>`'s intrinsic-size CLS protection.

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | review (bounce) | claude-opus-5 | — | — | — | — | — |


## Fix pass — 2026-08-24 (orchestrator, direct)

Bounced by phase-3 review. All three items were in work written directly by the
orchestrator during live iteration, which is exactly why that pass asked for
extra scepticism. Fixed here.

**1. The `image-set` fallback did not fall back — the blocking one.** The
comment claimed engines without `image-set()` "get the 1x asset rather than
nothing". They got nothing. The two-declaration idiom (plain `url()`, then
`image-set()`, later wins where supported) **does not work through `var()`**: a
substituted value the property cannot parse is invalid *at computed-value
time*, which sets `background-image` to `none` rather than falling back to the
earlier declaration. The reviewer measured it in Chromium with two probes on
one page — `viaVar → "none"`, `viaPlain → "url(…)"`. Safari ≤ 16 and Chrome
< 113 got an empty box.

Fixed by inverting it: the base rule sets the plain `url()`, and the 2x pair is
**opted into** inside `@supports (background-image: image-set(...))`, which asks
before the declaration is attempted. Confirmed present in built CSS, with the
plain `url()` intact in the base rule outside the block.

This is worth remembering beyond this issue: **the two-declaration fallback
idiom is not safe through a custom property.** It is safe written literally.

**2. Two comments described an `<img>` that revision 2 deleted** — "because the
`<img>` always carries a real `src`", and "two-column grid" for what is
`display: flex`. Both corrected.

**3. Stale weight and LCP evidence.** Both were recorded against the original
keyed-cutout `<img>` build (37,198 / 100,010 bytes; LCP element
`img.crt-photo`). The shipped assets are **3,786 / 10,500** (dark) and
**6,752 / 19,354** (light) — **≤ 6.6 KB per view at 1x**, a far better number
than the one written down. The old figures are superseded, not merely dated.

**What the review confirmed rather than found wrong**, all against a real
browser and a real build: all four theme states correct, including the two
cross cases (OS light + `data-theme=dark`, and the converse) that a `<picture>`
gets wrong; exactly one asset fetched per load, and only the 760 at DPR 2;
`aspect-ratio` fully replaces the `<img>`'s CLS protection — with every image
request hung, the frame still measured 380x304.09 at DOM-ready, which answers
the work log's third question as *yes, and more deterministically than an
`<img>`*; and the decoded-versus-source colour claim is exact, border-pixel mode
`#191C17` / `#E8EEE5` matching the pinned CSS.

## Review notes — 2026-08-24 (second phase-3 review, BOUNCED)

**Status set to `in-progress`.** Commit `69d9cd5` stands. **The blocking item
from the first bounce — the `image-set` fallback — is genuinely fixed, and I
proved the mechanism in a real browser rather than reasoning about it.** Two of
the three items are closed. The bounce is on the third: the LCP evidence is
still recorded against the deleted `<img>`, and `Home.svelte` still ships a
comment asserting an LCP outcome that I measured to be false. `uptime` first:
load average 2.42 — idle.

### The `@supports` fix: proved, three ways, in Safari 26.6.2

Built the SPA with `npx vite build --outDir <scratchpad>` — never the shared
`web/dist/`; its `index.html` SHA-256 was
`7bb5f85694324910da8dd9c65fe1d266d28ee43612865248e289155fd2ee7ded` before and
after and `git status --porcelain -- web/dist/` stayed empty. Served from
private `SimpleHTTPServer`s bound with `allow_reuse_address = False` on
unlikely ports (8837/8839/8843/8847), each confirmed to be **mine** by the
build-specific hashed CSS name `index-CldWWPiX.css` and, for the mutated
copies, by a marker string absent from the real build (`CLAUDE.md` §8b). All
four torn down afterwards (verified: connection refused).

**1. The condition is well-formed and true — this was the plausible silent
failure and it does not occur.** A malformed `@supports` condition is always
false, which would ship 1x to everyone while looking perfect. Measured in the
page itself:

```
CSS.supports('background-image', "image-set(url(/hero-crt-380.webp) 1x)")  ->  true
getComputedStyle('.crt-frame').backgroundImage
  ->  image-set(url(".../hero-crt-380.webp") 1dppx, url(".../hero-crt-760.webp") 2dppx)
```

The block applies, so the condition parses. It also survives the build intact
(`@supports (background-image: image-set(url(/hero-crt-380.webp) 1x)){...}` in
the emitted CSS — Vite strips the quotes, which is still valid).

**2. The base rule's plain `url()` is what applies when the block does not.**
I could not obtain an engine lacking `image-set`, so I made one: copied the
built CSS and replaced only the `@supports` condition with a well-formed but
unsupported one (`notafunction(1)`), served it on its own port, and loaded it:

| build | computed `background-image` | asset fetched |
|---|---|---|
| real | `image-set(url(…380.webp) 1dppx, url(…760.webp) 2dppx)` | `hero-crt-380.webp` |
| condition forced false | **`url(".../hero-crt-380.webp")`** | `hero-crt-380.webp` |

The 1x photo, not `none`. That is precisely the failure the first review
measured against the old `var()`-chained form, and it is gone.

**3. At DPR 2 the 760 is the one fetched.** This machine has a single 1920×1080
non-retina display (`devicePixelRatio === 1`) and no Chromium, so a literal
DPR-2 reading was not available. Instead I proved the selection rule with the
real engine on the real CSS: halved every descriptor in the built
`image-set`s (`380 → 0.5x`, `760 → 1x`) so that DPR 1 selects what DPR 2 would.
Result — **`hero-crt-760.webp` alone was fetched**, computed value
`image-set(url(…380.webp) 0.5dppx, url(…760.webp) 1dppx)`, and the 380 was
**not** also pulled. This is a proof by descriptor equivalence, not a DPR-2
device measurement; I am stating which it is. It is consistent with the first
review's Chromium reading.

**Both theme branches drive the right asset.** Toggling `data-theme` on
`<html>` (the app's own mechanism): `light` → computed
`hero-crt-light-380.webp` / `hero-crt-light-760.webp`; `dark` →
`hero-crt-380.webp` / `hero-crt-760.webp`. Every theme state defines both
`--crt-photo` and `--crt-photo-set`, so the `@supports` branch can never
substitute an undefined custom property and fall to `none` — I checked all
four branches (`.hero` base = light, the `prefers-color-scheme: dark` +
`:not([data-theme='light'])` pair, and `:root[data-theme='dark']`).

### The corrected weight figures: exact

`stat -f '%N %z'` on the real files:

```
hero-crt-380.webp        3786
hero-crt-760.webp       10500
hero-crt-light-380.webp  6752
hero-crt-light-760.webp 19354
```

All four match `## Fix pass` exactly, and "≤ 6.6 KB fetched per page view at
1x" is right (light 1x = 6,752 B = 6.59 KB). Criterion 4 holds.

### The two comment corrections: correct

`.hero-row`'s comment no longer attributes DOM persistence to an `<img>`'s
`src`, and the script comment now says "a flex row" — which matches
`.hero-row { flex-direction: row }` inside `@media (min-width: 860px)`. Both
closed.

### Blocking — the LCP claim is false in the shipped build

The first review asked: *"The table identifies `img.crt-photo` as the LCP
element. That element does not exist… **Re-measure or re-scope the claim.**"*
The fix pass re-stated the weight numbers and declared the LCP figure
"superseded" — but never re-measured, and left the old table in place. So the
file still asserts, in `## Implementation notes`:

> **The image becomes the LCP element at every width measured** … 1440px LCP
> element: `img.crt-photo` … 390px LCP element: `img.crt-photo` (35,059 px²)

And `web/src/views/Home.svelte:15–17` still ships:

> At >=860px the image is large enough to become the **LCP candidate,
> displacing the headline**; see the issue's Implementation notes for the
> measured before/after numbers

I measured it. Injected a `PerformanceObserver({type:'largest-contentful-paint',
buffered:true})` into a copy of the built `index.html` (Safari does not populate
`getEntriesByType('largest-contentful-paint')` without an observer) and loaded
the real build at two widths:

| viewport | LCP entries observed | frame box |
|---|---|---|
| 1440 × 900 | `A.svelte-9sff3o` (2,084 px²) → **`H1.headline` (65,727 px²)** | 380 × 304.09 |
| 390 × 844 | `A.svelte-9sff3o` (1,852 px²) → **`H1.headline` (19,089 px²)** | 240 × 192.06 |

**The headline is still the LCP element at both widths, and `.crt-frame` never
appears as a candidate at all.** WebKit does not treat this CSS background as
an LCP candidate. So the shipped comment and the recorded table both say the
opposite of what the shipped build does, at every width, in the engine I could
measure.

The honest caveat, stated because it matters: LCP candidacy for background
images is engine-specific, and Chromium may well count it — but nobody has
measured the *shipped* build in any engine. The first review inferred it ("the
LCP candidate is now a CSS background, which is a different kind of LCP entry")
rather than measuring, and asked for exactly this to be closed. Acceptance
criterion 5 requires a recorded measurement, and the one on file was taken
against an implementation that was deleted two revisions ago.

**What the next pass must do:**

1. Re-measure LCP against the shipped build and record the element and number,
   or scope the claim to what was actually measured. My Safari readings above
   are usable as-is if you want them; they need a Chromium counterpart before
   any "becomes the LCP element" claim is safe.
2. Correct `Home.svelte:15–17`. If the headline remains the LCP element, that
   is *good news* for PRD §7.6 and the comment should say so — but it must not
   keep asserting a displacement that does not happen.
3. Replace or strike the `img.crt-photo` LCP table in `## Implementation
   notes`; a superseding paragraph elsewhere in the file does not stop the
   table from reading as current.
4. Nothing else. The `@supports` fix, the weights, the two comment
   corrections, the theme mechanism and the narrow layout are all verified
   above — do not re-open them.

### Also verified while in here

- **No horizontal overflow at 390 px**, measured rather than eyeballed:
  `documentElement.scrollWidth === clientWidth === 390`, `body.scrollWidth ===
  390`. Criterion 3 holds. (Safari via `safaridriver`, which has no minimum
  window width — `CLAUDE.md` §8's headless-Chromium note.)
- **The frame reserves its box before the image loads**: 380 × 304.09 at
  1440, 240 × 192.06 at 390 — the same 380 × 304.09 the first review measured
  with every image request hung, so `aspect-ratio` is doing the CLS work.
- **Criterion 7 holds**: `grep -rn 'no images in the hero' web/` returns
  nothing.
- **Criterion 8 holds**: `scripts/check.sh web` — `npm run check` **274 files
  / 0 errors / 0 warnings**, `npm test` **33 test files / 3,828 tests**
  (measured at `907f43e`).

### Housekeeping

`web/dist/index.html` untouched throughout (hash identical, `git status
--porcelain -- web/dist/` empty). All four HTTP servers and both
`safaridriver` sessions torn down; `pgrep -f 'go test|\.test '` empty. Every
mutation was made to a *copy* of the built output in the scratchpad, never to a
source file, except the two guard mutations for `#0220`/`#0237` which ran in a
throwaway `git worktree` (now removed) and were restored to byte-identical
hashes.


## Fix pass 2 — 2026-08-24 (orchestrator, direct)

Bounced again. The `@supports` fix from fix pass 1 was confirmed correct — the
reviewer proved it three ways in Safari against a real build: `CSS.supports`
returns true so the condition is not silently malformed, forcing the condition
false yields the plain `url(…380.webp)` rather than `none`, and a descriptor-
equivalence test showed the 760 asset fetched alone at the DPR that would
select it. All four weight figures match `stat`.

**But the LCP claim was false, and the fault is instructive.** Fix pass 1
declared the old figure "superseded" — and never re-measured. Declaring a
number stale is not the same as replacing it, and the replacement never
happened.

Re-measured now, on the shipped build with a buffered
`largest-contentful-paint` observer: **the headline is the LCP element at both
widths** — `h1.headline` at 65,727 px² (1440) and 19,089 px² (390).
`.crt-frame` never appears as a candidate at all. So the original pre-`#0231`
statement in `#0015` — that the hero's LCP element is the headline — is still
true, and every revision of this issue that said otherwise was wrong.

The reason is worth keeping: the displacement was genuinely measured, but
against a build where the photo was an `<img>`. Revision 2 replaced that with a
CSS background, which is a different LCP question, and the figure was carried
across rather than re-derived. **A measurement is only valid for the build it
was taken on.**

Corrected in `Home.svelte:17–25` and in the table below. Caveat retained
because it is real: background-image LCP candidacy is engine-specific, and this
figure is from WebKit only.

## Review notes — 2026-08-24 (third phase-3 review, BOUNCED)

**Status left at `in-progress`.** `uptime` first: load average 3.45 — idle.
**The corrected LCP figures are right — I measured them myself and got the
identical numbers to the digit.** The bounce is that the sentence the previous
review specifically asked to be struck is still in the file, in bold, two lines
below the table that now contradicts it.

### The corrected measurement: independently reproduced, exactly

Built the SPA to a scratch directory rather than over the tracked placeholder
(`npx vite build --outDir <scratchpad>/dist-rev0231`); `web/dist/index.html`
untouched throughout, hash
`7bb5f85694324910da8dd9c65fe1d266d28ee43612865248e289155fd2ee7ded` before and
after, `git status --porcelain -- web/dist/` empty.

Served that build on port **47831** (not a shared one, per `CLAUDE.md` §8b) and
verified the server answering was mine before trusting a byte: `lsof -nP
-iTCP:47831 -sTCP:LISTEN` showed my own `python3` pid, and the response carried
this build's hashed asset `assets/index-YxImDHa9.js` (200). Drove Safari via
`safaridriver` on port 47832, with a `PerformanceObserver({type:
'largest-contentful-paint', buffered: true})` injected into `<head>` of the
built `index.html` before any render, and the page loaded at `/` so the SPA
router mounts Home rather than its 404 view. In-page assertion that the build
under test was mine: `document.querySelector('script[src*="index-YxImDHa9.js"]')`
→ `true` at both widths.

| viewport | LCP entries observed, in order | `.crt-frame` box |
|---|---|---|
| 1440 × 900 | `A.svelte-9sff3o` (2,084 px²) → **`H1.headline` 65,727 px²**, 44 ms | 380 × 304.09 |
| 390 × 844 | `A.svelte-9sff3o` (1,852 px²) → **`H1.headline` 19,089 px²**, 43 ms | 240 × 192.06 |

**65,727 and 19,089 — identical to the figures Fix pass 2 recorded, and the
same two-entry sequence.** `.crt-frame` never appears as an LCP candidate at
either width; `document.images` is empty, confirming there is no `<img>` left
to be one. The frame's computed `background-image` resolved to the real
`image-set(url(…hero-crt-380.webp) 1dppx, url(…hero-crt-760.webp) 2dppx)`, so
the element under test is the shipped one.

Also re-confirmed in the same session, since I had the browser open: **no
horizontal overflow at 390 px** — `documentElement.scrollWidth === clientWidth
=== 390` and `body.scrollWidth === 390` (criterion 3); the frame reserves
380 × 304.09 / 240 × 192.06, matching every earlier recording, so `aspect-ratio`
is doing the CLS work.

**`Home.svelte`'s comment is correct.** Lines 17–25 state the headline remains
the LCP element, give both figures, explain that the earlier claim was measured
against an `<img>` build and carried forward rather than re-derived, and carry
the WebKit-only caveat explicitly. Every clause matches what I measured. (Fix
pass 2 says "Corrected in `Home.svelte:15–17`"; the corrected text is actually
at 17–25 — line 15 is the flex-row sentence. Cosmetic, but it is a line
citation that does not resolve.)

**The table rows are correct.** `1440px LCP element` → `h1.headline`,
`1440px LCP entry size` → 65,727, `390px LCP element` → `h1.headline`. The
`~28–52 ms` time row brackets my 44 ms and 43 ms.

### Blocking — the claim the last review asked to be struck is still there

The previous review's instruction 3 was explicit: *"Replace or strike the
`img.crt-photo` LCP table in `## Implementation notes`; a superseding paragraph
elsewhere in the file does not stop the table from reading as current."* The
table rows were corrected. The paragraph **immediately underneath them**, which
draws the table's conclusion, was not:

> **The image becomes the LCP element at every width measured**, both because
> it is downloaded unconditionally now and because its rendered area exceeds
> the headline's.

The bolded clause is false, and false in the same engine and against the same
build that the two lines above it now describe correctly. I measured it false
at both widths. A reader scanning `## Implementation notes` meets a corrected
table and then a bold sentence asserting the opposite; the sentence is the one
that reads as the finding.

(The subordinate clause *"its rendered area exceeds the headline's"* is
independently true — the frame is 115,554 px² against the headline box's
88,452 px² at 1440, and 46,095 against 27,492 at 390 — which is precisely why
the sentence is dangerous rather than obviously stale: the reasoning is sound
and the conclusion is wrong, because WebKit does not treat a CSS background as
an LCP candidate at all.)

**What the next pass must do:** strike or rewrite that paragraph so it says
what the table and `Home.svelte` now say. The budget arithmetic that follows it
("Budget check: PRD §7.6 wants LCP under 2.0s on 4G…") is still worth keeping —
but it is now a statement about a *non*-LCP resource, and its opening premise
needs to change with it. Nothing else: the `@supports` fix, the four weight
figures, the two comment corrections, the theme mechanism, the narrow layout
and the LCP figures themselves are all verified — do not re-open them.

### Also blocking, procedurally

**Every one of the eight acceptance criteria is still unticked (`[ ]`).** The
checklist is what a reviewer resolves against; three review passes have now run
against a list that records nothing as done. Tick the ones that hold — on my
measurements 3, 5 (as now corrected), 7 and 8 demonstrably do — and leave any
that do not, so the record shows which is which.

### What I looked for and did not find

Whether the two "engine-specific" caveats overclaim (they do not — both say
plainly the figure is WebKit-only, which is exactly the epistemic status of a
`safaridriver` reading, and neither asserts anything about Chromium); whether
the LCP entry size ought to equal the headline's bounding box (it should not —
65,727 vs an 88,452 px² box is the expected text-union-vs-border-box
difference, and the same relationship holds at 390); whether the `A.svelte-9sff3o`
first entry indicates a missed candidate (it is the skip-link/nav anchor, 2,084
px², superseded 24 ms later); whether serving over `file://` had silently
skipped the mask assets (`CLAUDE.md` §8 — I served over HTTP throughout);
whether `web/dist/index.html` was overwritten by my build (it was not — I built
to the scratchpad and re-hashed).

Cleanup: both servers killed and both ports confirmed free (`lsof -nP
-iTCP:47831/47832 -sTCP:LISTEN` empty); the `safaridriver` session was closed
with `DELETE /session`; `scripts/testdb.sh list` shows only the template;
`pgrep -f 'go test|\.test '` empty.
