# 0399 — The path-citation guard fails open whenever an agent worktree exists

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | handlers / tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-03 |
| **Commit** | `ff3b678` |
| **Closed** | 2026-09-03 |

## Resolution notes

> 🟢 Resolved 2026-09-03 — reproduced both directions with a worktree
> built for this review rather than the ambient one, and measured the corpus
> effect on the real tree: pruning removes 1027 paths and changes zero real
> citations' outcomes.

## Description

`internal/handlers`'s `TestNoCommentCitesUnresolvedPathOrSection` resolves a
path cited in a Go comment by walking the repository tree and matching. Two
properties combine into a fail-open:

1. `citationTargetSkipDirs` prunes only `.git` and `node_modules`. It does
   **not** prune `.claude`, so `collectRepoPaths` walks
   `.claude/worktrees/<agent>/` — a **full second copy of the repository**.
2. `pathCitationResolves` matches by **suffix**, so a path found anywhere in
   the walked set satisfies a citation.

Together: a comment citing a file that has been **deleted or renamed in the live
tree** still resolves, because the worktree's copy of it is still on disk and
still matches by suffix. The guard reports clean and the citation is dead.

**Confirmed by the orchestrator**, independently of the plan that found it:
`git worktree list` shows a live `.claude/worktrees/agent-a16d8f8a64eadf32f`
containing **281** `.go` files alongside `CLAUDE.md`, `cmd/`, `docs/`, `go.mod`
and the rest of the tree. `citationTargetSkipDirs` is `{".git", "node_modules"}`
and the suffix comparison is a `strings.HasSuffix` pair.

### Why it matters

`CLAUDE.md` §5a **actively encourages worktrees** — "Use a worktree when two
agents would otherwise edit the same files" — so the condition is not rare, it
is the documented workflow. The window is also invisible: worktrees are
gitignored, so nothing in `git status` hints that the guard's corpus has
silently doubled.

The failure is exactly the one this guard exists to prevent. A rename that
leaves a stale citation behind passes review, and surfaces later as a dangling
reference — the drift family of `#0352`, `#0356`, `#0370` and `#0384`.

**Direction of the bug matters.** This is a **fail-open** (a stale citation
wrongly resolves), not a false alarm. It cannot produce a spurious failure, so
nobody will trip over it; it will simply let something through. That is the
harder kind to notice and the reason to close it rather than record it.

## The fix the plan specified

Add `".claude"` to `citationTargetSkipDirs`. One line.

`.claude/` holds agent worktrees and session scratch state; nothing in it is a
legitimate citation target, and pruning it cannot make a real citation
unresolvable — unlike `dist`, which the existing doc comment explains at length
must **not** be pruned because real comments cite `web/dist/index.html`.

## Acceptance criteria

1. `.claude` is pruned from the repo-path walk, and the existing doc comment's
   reasoning is extended to say why — matching the care already taken there to
   explain why `dist` is deliberately **not** pruned.
2. **Prove the fail-open is closed, and prove it discriminates.** With a
   worktree present: a comment citing a path that exists **only** in the
   worktree must fail after the fix, and must (demonstrably) pass before it.
   Construct the worktree copy yourself in a throwaway location rather than
   relying on whichever agent worktree happens to exist when you run.
3. **Do not break the legitimate cases.** `web/dist/index.html`, `dist/index.html`
   and every other real citation in the tree must still resolve. The corpus must
   come back with the same result as before the change apart from the closed
   hole — a fix that quiets the guard is worse than the fail-open.
4. **Consider whether suffix matching is itself the deeper defect** and say what
   you conclude. Pruning `.claude` closes the instance; matching by suffix means
   any future in-tree copy reopens it. If you judge tightening the match to be
   out of scope, say so with a reason — that judgement is part of the
   deliverable. Per `#0330`/`#0347`/`#0356`, recording a limit is a legitimate
   outcome; leaving it unmentioned is not.
5. **No new mechanism beyond what criterion 1 and your criterion-4 conclusion
   require.** §8's `#0258` is six passes of adding checks to defend checks.
6. Run `./internal/handlers/...` and `./internal/db/...` (`#0381`). Per §5 name
   the specific tests that ran. `gofmt` stability. No `<file>.go:<int>` citation
   anywhere (`#0352`).

## Decision — criterion 4, suffix matching

**Judged out of scope for this issue, recorded rather than built**, matching
the `#0330`/`#0347`/`#0356` precedent this issue itself cites.

Pruning `.claude` closes the *instance*: the fail-open required a second full
copy of the repository sitting somewhere inside `repoRoot` for `collectRepoPaths`
to walk into, and `.claude/worktrees/<agent>/` is the one place this project's
own documented convention (`CLAUDE.md` §5a, "use a worktree when two agents
would otherwise edit the same files") puts one. It is also worth noting the walk
is rooted at `repoRoot` and goes no further up or sideways — a worktree created
*outside* the repo tree (as `git worktree list` shows this session's own
`/private/tmp/oc-live` is) was never reachable by this guard at all, pruning or
not, because `filepath.WalkDir` never visits it.

Matching by `strings.HasSuffix` is a separate, broader property: it means *any*
future in-tree second copy — not only `.claude` — would reopen the same shape of
hole, since `pathExistsDirectly` accepts any path on disk ending in the cited
suffix rather than checking the cited path is the unique, canonical one. That is
a real, correctly-identified structural point, and I considered tightening it
(e.g. requiring an exact repo-relative match, or resolving to a single
canonical file before accepting a suffix hit).

**I conclude tightening it is out of scope here, for a reason beyond "it's a
bigger change":** suffix matching is not an accident of this guard, it is a
deliberately designed and tested feature. `pathCitationResolves`'s own doc
comment (`internal/handlers/citation_target_guard_test.go`) explains two
non-exact-match shapes it exists to resolve — a shortened citation that drops a
leading path segment (`handlers/auth.go` for `internal/handlers/auth.go`,
`internal/handlers/logout_all_test.go`'s real `TestLogoutAll_HTTP_Transports`
comment) and a leading bare word before an independently-resolvable remainder
(`WorkshopCard/lib/workshops.ts`) — and `TestPathCitationResolvesShortenedAndLeadingWordForms`
in that same file asserts both against the real tree, using the real historical
citations that motivated each rule. Removing or narrowing suffix matching would
break those real, currently-legitimate citations — precisely what criterion 3
forbids ("a fix that quiets the guard is worse than the fail-open" cuts both
ways: turning real citations into false failures is not better than leaving a
false pass). Redesigning the resolution rule to keep the shortened-citation
cases while closing the general second-copy risk is a real design task with its
own acceptance criteria, not a one-line addition, and criterion 5 asks for
nothing beyond what criterion 1 and this conclusion require.

**The residual, named rather than acted on:** if some future in-tree location
other than `.claude` ever comes to hold a nested copy of repo-relative paths
(a build artifact tree that mirrors source layout, a different agent-tooling
directory than `.claude`), suffix matching would resolve citations against it
the same way it did against `.claude/worktrees/` before this fix. Nothing in
this repo's current conventions creates such a location today — `.claude` is
the one this project's own §5a documents and the one this issue measured. That
is a limit worth a future reader knowing, not a mechanism worth building now.

## Relation

- Found by: [#0387](0387.md)'s planning pass, while deriving the repo-wide-guard
  package set
- Relates to: [#0381](0381.md) — the §5 table naming this guard's scan root as
  the whole repo tree
- Relates to: [#0352](0352.md), [#0356](0356.md), [#0370](0370.md),
  [#0384](0384.md) — the silent-citation-drift family this guard belongs to
- Relates to: [#0392](0392.md) — writing about citation guards produces text
  those guards reject; expect that hazard while working this

## Verification

Reproduced the fail-open before touching the fix: constructed a throwaway `git
worktree add -b proof-0399-branch .claude/worktrees/proof-0399 HEAD` (a second,
disposable worktree alongside whatever agent worktree happens to already exist
— not relying on that one), placed a marker file only inside it
(`internal/handlers/proof_only_in_worktree_0399_ZQXK.go`, absent from the live
tree), and drove `collectRepoPaths`/`pathCitationResolves` directly from an
uncommitted, throwaway `_test.go` file (deleted before this commit, never part
of the diff): citing that marker's repo-relative path resolved `true` before
the fix and `false` after — the discrimination criterion 2 asks for, both
directions, against a worktree built for this proof rather than an ambient one.
Also confirmed from the same throwaway file that `dist/index.html` and
`web/dist/index.html` still resolve after the fix. The throwaway worktree and
its branch were removed (`git worktree remove --force`, `git branch -D`)
before this commit; `git worktree list` after removal shows only this
checkout and the pre-existing agent worktree that was here before this issue
started — nothing added by this work was left behind.

`uptime` before the scoped run: `load averages: 2.97 2.69 2.70`. `ISSUE=0399
scripts/check.sh go ./internal/handlers/... ./internal/db/...` (`#0381`) — `go
build`, `go vet` clean; `gofmt -l` clean, and non-destructively (hashing
`gofmt`'s stdout against the on-disk bytes, since another agent has files
dirty in this tree) confirmed stable. `internal/handlers` (7.061s) and
`internal/db` (7.273s) both `ok`, zero skips — real elapsed time, not `0.00s`,
so both packages actually exercised their database-backed suites. Scratch
database `opencircuit_test_0399` dropped by `check.sh`'s own exit cleanup; no
leftover `go test`/`.test` processes. `python3` byte-scan of the diff for
`\.go:\d+` found none (`#0352`). The single real guard this change targets,
run by name directly: the whole-suite run above includes
`TestNoCommentCitesUnresolvedPathOrSection`, which passed clean both against
the corpus as edited and — separately, from the throwaway file above — against
the constructed worktree citation in both directions.

## Review notes

**Approved.** Every claim re-derived here rather than read; the diff is exactly
the one-line prune plus the doc-comment extension, and nothing else.

**Criterion 2 — reproduced both directions, with a worktree built for this
review.** I did not use the ambient agent worktree. From the main checkout I
created two throwaway detached worktrees: a run environment at
`/private/tmp/oc-rev0399` pinned to `ff3b678`, and a second full repository copy
nested inside it at `/private/tmp/oc-rev0399/.claude/worktrees/proof-rev0399`.
Into the nested copy alone I placed a marker Go file whose repo-relative path
exists nowhere in the live tree (confirmed absent from both the outer worktree
and the main checkout by `find`). An uncommitted throwaway probe test in the
outer worktree drove `collectRepoPaths` and `pathCitationResolves` directly.
Running everything inside `/private/tmp` kept the concurrent `#0400` agent's
`internal/handlers` runs untouched.

| | corpus paths | paths under `.claude/` | marker citation resolves |
|---|---|---|---|
| pre-fix (`.claude` line removed from the worktree copy) | 2046 | 1023 | **true** — the fail-open |
| post-fix (as committed) | 1023 | 0 | **false** |

The 1023-path delta is exactly the nested copy. In the same runs
`dist/index.html`, `web/dist/index.html`, `handlers/auth.go`,
`WorkshopCard/lib/workshops.ts`, `internal/handlers/citation_guard_test.go` and
`docs/README` all resolved `true` in **both** states, and `web/HANDOFF` resolved
`false` in both — so the change moved exactly the one citation it was aimed at.
`TestNoCommentCitesUnresolvedPathOrSection` and
`TestPathCitationResolvesShortenedAndLeadingWordForms` passed in both states
too, which is the point: the fail-open is silent, and only the direct probe
exposes it.

**Criterion 3 — the guard was not quieted, measured on the real corpus.** A
second throwaway probe pointed `repoRoot` and the scan roots at the **main
checkout**, with the ambient agent worktree present, and ran
`collectCitationTargetHits` twice over the identical citation corpus — once with
`.claude` pruned, once without:

- paths pruned 1048, unpruned 2075 (the ambient worktree contributes 1027);
- Go files visited 275 in both — the same corpus, not a narrowed one;
- **hits 0 in both**, and **0 citations became unresolved because of the
  prune**.

So no real citation in this tree was resolving only through
`.claude/worktrees/`, and the corpus outcome is unchanged apart from the closed
hole. The prune removes ~half the walked path set and costs nothing.

**Criterion 4 — I agree with recording rather than closing, and the load-bearing
claim measures out stronger than the issue states it.** All three supporting
claims check out:

- *The walk cannot reach an out-of-tree worktree.* `collectRepoPaths` calls
  `filepath.WalkDir` rooted at `repoRoot` and never ascends, so this session's
  own `/private/tmp/oc-live` was never in the corpus, pruning or not. Confirmed
  by reading the walk.
- *Suffix matching is a designed, separately tested feature.*
  `TestPathCitationResolvesShortenedAndLeadingWordForms` exists and asserts the
  shortened and leading-bare-word shapes against the real tree.
- *Real citations depend on it.* This is the one worth measuring rather than
  taking on the test's word, so I narrowed `pathExistsDirectly` to exact-match
  only in the throwaway worktree and re-ran the differential over the main
  checkout: **23 citation sites covering 15 distinct cited paths turn into
  failures** — `dist/index.html` (5 sites), `handlers/auth.go` (3),
  `WorkshopCard/lib/workshops.ts` (2), `subscribers/store.go` (2), and single
  sites for `mailing/campaigns.go`, `mailing/preflight.go`,
  `mailing/worker_store.go`, `mailing/outbox_worker.go`,
  `subscribers/imports_test.go`, `interests/store.go`, `interests/store_test.go`,
  `lib/workshops.ts`, `lib/workshopAdmin.ts`, `admin/Campaigns.svelte` and
  `testdata/README.md`.

That last number settles it. Narrowing the match is not a tightening, it is a
redesign that must keep 15 real citation shapes working while closing the
general second-copy risk — a design task with its own acceptance criteria, and
exactly what criterion 3 forbids doing carelessly, since turning 23 real
citations into false failures is worse than the fail-open it would close. There
is a neat symmetry the `## Decision` does not point out but which supports it:
`dist/index.html` is both the reason `dist` must not be pruned *and* the
heaviest single consumer of suffix matching. Recording the residual matches the
`#0330`/`#0347`/`#0356` precedent, and the residual as written names the right
condition (a future in-tree location holding a nested copy of repo-relative
paths).

**Criterion 1 — the doc comment is accurate.** Checked as a set of factual
claims rather than read as prose. The `CLAUDE.md` §5a quotation is verbatim
against `CLAUDE.md` modulo comment line-wrapping (whitespace-normalised byte
comparison), and it does sit in §5a. `.claude/worktrees/` is gitignored at
`.gitignore` line 44, so "`git status` never shows it" is correct. The
mechanism sentence — suffix matching over a stale second copy — is the mechanism
I reproduced above. Crucially the comment claims only that pruning `.claude`
cannot break a real citation; it makes no claim that suffix matching itself is
now safe, so it does not imply the residual is closed.

One forward-looking caveat, not a defect: "nothing under `.claude` is ever a
legitimate citation target" is an absolute about the future, and the prune
matches by directory *name* at any depth, mirroring how `.git` and
`node_modules` are matched. If this project ever commits agent or skill
definitions under `.claude`, a comment citing one would fail. That failure would
be loud and immediate, which is the direction this repo prefers, and the fix
would be obvious from the comment itself — so it is worth knowing, not worth
changing.

**Criterion 6 — nothing under `.claude/` is citable today.** Checked rather than
assumed. There is exactly one directory named `.claude` in the tree; it holds
`settings.local.json` and `worktrees/`. **Nothing under it is git-tracked** —
`git ls-files` returns empty, `worktrees/` is ignored by this repo's own
`.gitignore` and `settings.local.json` by the user's global one. A path that
does not exist in a fresh clone is not a legitimate citation target, so the
comment's claim holds for the right reason. The only two Go comments in the tree
that mention `.claude/worktrees/` are prose *about* the directory, not citations
of a file inside it, and neither is flagged (the 0-hit measurement above covers
them).

**Criterion 5 — no new mechanism.** `git show --stat ff3b678` is two files:
`internal/handlers/citation_target_guard_test.go` (+1 map entry, the rest
comment) and `issues/0399.md`. No new test, no new helper, no new constant. Per
§8's `#0258` entry that is the right call — this is a data change to an existing
prune list, and it needs no defender.

**Verification (criterion 6 of the issue).** `uptime` before the run: `load
averages: 1.91 2.30 2.52` — well under §5's threshold. `ISSUE=0399r
scripts/check.sh go ./internal/handlers/... ./internal/db/...` (`#0381`, both
packages) → `VERIFICATION PASSED`: `go build`, `go vet`, `gofmt -l` clean;
`internal/handlers` `ok` 5.760s and `internal/db` `ok` 0.609s, skip audit
reports no skips, both real elapsed time rather than `0.00s`. Scratch database
`opencircuit_test_0399r` dropped by `check.sh` on exit (confirmed absent from
`pg_database`). Named guard run separately and passing:
`TestNoCommentCitesUnresolvedPathOrSection`,
`TestPathCitationResolvesShortenedAndLeadingWordForms` and
`TestCitationTargetPathPatternCatchesSyntheticExample`. `gofmt` stability
checked **non-destructively** — SHA-256 of `gofmt`'s stdout for the changed file
equals the SHA-256 of its on-disk bytes, so nothing was rewritten. A `python3`
byte-scan of the changed file and this issue file found no `.go`-plus-line-number
citation (`#0352`); scanned as bytes, not with BSD `grep -P` (§8).

**Cleanup.** Both throwaway worktrees and their nested copy were removed with
`git worktree remove --force`; neither used a branch (both detached), so no
branch was left either. `git worktree list` now shows exactly the three entries
that existed before this review — the main checkout, the pre-existing prunable
`/private/tmp/oc-live`, and the pre-existing agent worktree. No stray `go test`
or `.test` processes. Nothing under `internal/mailing/` or `issues/0400.md` was
read, written, reverted or stashed.

**Nothing worth filing.** The suffix-matching residual is recorded in
`## Decision` above, which is the outcome the `#0330`/`#0347`/`#0356` precedent
prescribes; the `.claude`-becomes-tracked caveat is a loud-failure hypothetical
with no current trigger.

## Work log

- 2026-09-03 — Filed by the orchestrator from `#0387`'s planning pass (§9), with
  both halves — the unpruned `.claude` walk and the suffix match — confirmed
  independently against the live tree before filing.
- 2026-09-03 — implement (claude-sonnet-5, phase 2 subagent): re-verified both
  halves of the defect directly against source before changing anything —
  `citationTargetSkipDirs` was exactly `{".git": true, "node_modules": true}`,
  and `pathExistsDirectly` resolves by `strings.HasSuffix`. Reproduced the
  fail-open with a throwaway `git worktree add` at
  `.claude/worktrees/proof-0399` (deliberately separate from whatever agent
  worktree already existed) plus a marker file only inside it, driven by an
  uncommitted, throwaway `_test.go` file calling `collectRepoPaths`/
  `pathCitationResolves` directly: resolved `true` before the fix. Added
  `".claude"` to `citationTargetSkipDirs` (one line) and extended the existing
  doc comment to explain why, mirroring the care already given to explaining
  why `dist` is deliberately not pruned — same throwaway proof re-run showed
  `false` after. Confirmed `dist/index.html` and `web/dist/index.html` still
  resolve post-fix, and confirmed the real guard
  (`TestNoCommentCitesUnresolvedPathOrSection`) still passes clean against the
  unmodified corpus — the fix does not quiet it (criterion 3). Removed the
  throwaway worktree, its branch, and the throwaway test file before
  committing; `git worktree list` afterward shows nothing added by this work.
  **Criterion 4 conclusion**: suffix matching is a separate, broader property
  — pruning `.claude` closes the one in-tree second-copy location this
  project's own `CLAUDE.md` §5a convention creates, but any future in-tree
  copy would reopen the same shape of hole under suffix matching. Judged
  tightening the match itself out of scope: it is a deliberately designed,
  separately tested feature (`TestPathCitationResolvesShortenedAndLeadingWordForms`)
  that real citations in this tree depend on (`handlers/auth.go`,
  `WorkshopCard/lib/workshops.ts`), so narrowing it risks exactly what
  criterion 3 forbids — turning real citations into false failures — and is a
  real design task, not a one-line fix. Recorded as a residual limit rather
  than built, per the `#0330`/`#0347`/`#0356` precedent; full reasoning in
  `## Decision` above. No mechanism added beyond the one-line prune
  (criterion 5). Added nothing beyond the doc-comment extension the fix
  itself requires. Careful of `#0392`'s hazard while writing this and the
  `## Verification`/`## Decision` sections above: no fabricated `Test…`
  identifier is cited in an evidence-asserting section — the two throwaway
  proof-function names are described in prose rather than named, since they
  no longer exist in the tree once the throwaway file was deleted; every
  `Test…` identifier actually cited above
  (`TestNoCommentCitesUnresolvedPathOrSection`,
  `TestPathCitationResolvesShortenedAndLeadingWordForms`,
  `TestLogoutAll_HTTP_Transports`) is real and currently defined. Re-ran
  `ISSUE=0399 scripts/check.sh go ./internal/handlers/... ./internal/db/...`
  after writing these issue-file edits (editing `issues/0399.md` is inside
  `internal/db`'s own scan root per `#0381`'s table) — `VERIFICATION PASSED`,
  zero skips, both packages `ok` with real elapsed time. Full details in
  `## Verification` above. `git status --porcelain` before staging showed two
  other files dirty (`internal/mailing/outbox_worker.go`, `issues/0400.md`) —
  neither touched, both belong to the concurrent `#0400` session per this
  issue's own briefing. Staged only `internal/handlers/citation_target_guard_test.go`
  and `issues/0399.md` by explicit pathspec, confirmed with `git diff --cached
  --name-only` before committing and `git show --stat HEAD` after. Left status
  `in-progress` for review (§9 — only the reviewer sets `resolved`). No new
  issue filed (§9); nothing else surfaced worth filing. Usage/cost not
  recorded here — no transcript token accounting available to this subagent.
- 2026-09-03 — review (claude-opus-5, phase 3 subagent): **approved**, status
  `resolved`. Reproduced both directions with two throwaway detached worktrees
  built for this review (a run environment plus a nested second repository copy
  under its own `.claude/worktrees/`), deliberately not the ambient agent
  worktree: marker citation resolved `true` pre-fix and `false` post-fix, corpus
  1023 vs 2046 paths. Measured the criterion-3 question on the **real** main
  checkout with the ambient worktree present — identical citation corpus (275 Go
  files visited both ways), zero hits both ways, zero citations newly
  unresolved, so the prune drops 1027 paths and costs nothing. Verified the
  criterion-4 reasoning by measurement rather than reading: narrowing
  `pathExistsDirectly` to exact matches turns 23 real citation sites across 15
  distinct cited paths into failures, so suffix matching is load-bearing and
  recording the residual is right per `#0330`/`#0347`/`#0356`. Doc comment
  checked claim by claim, including a whitespace-normalised byte comparison of
  its `CLAUDE.md` §5a quotation. Confirmed nothing under `.claude/` is
  git-tracked, so no legitimate citation target exists there today. Diff is the
  one-line prune plus comment and nothing else. `ISSUE=0399r scripts/check.sh go
  ./internal/handlers/... ./internal/db/...` passed with zero skips; both
  throwaway worktrees removed and `git worktree list` restored to its
  pre-review three entries. No new issue filed (§9); nothing surfaced worth
  filing.
