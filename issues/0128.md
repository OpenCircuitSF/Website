# 0128 — Build the pending-subscriber admin screen

| | |
|---|---|
| **Status** | in-progress |
| **Module** | admin / subscribers / web |
| **Platform** | All |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-21 |
| **Commit** | `567db7d` |

## Description

An admin cannot see who signed up but never confirmed, cannot tell how long they have been waiting, and cannot resend a confirmation. When a confirmation email fails to arrive — SES outage, a spam folder, a typo'd address — nobody finds out.

## Implementation notes

Phase 2 (Sonnet), 2026-08-24. No separate phase-1 planning pass ran; design
decisions were made and disclosed inline (code comments + this section).

**`internal/subscribers/pending.go`** (new file) adds three methods:

- `ListPending(ctx, oldestFirst bool) ([]Subscriber, error)` — every
  `status='pending'`, `synthetic=false` row, sorted by `confirm_sent_at`.
  Reuses the existing `Subscriber` type rather than a narrower
  `PendingSubscriber` type, matching `List`'s own convention.
- `AdminResendConfirmation(ctx, id, now, cooldown, ttl) (ResendResult, error)`
  — mints a genuinely NEW `confirm_token` (unlike the public
  `ClaimAndEnqueueConfirmation`, which reuses the existing one), extends
  `confirm_expires_at`, stamps `confirm_sent_at`, and enqueues the new
  confirmation — all inside one transaction, so a committed resend can
  never leave the new token unqueued. Guarded, in order, inside the same
  transaction (`SELECT ... FOR UPDATE`): exists and not synthetic
  (`ErrPendingSubscriberNotFound`), `status='pending'` (`ErrNotPending`),
  not suppressed (`ErrResendSuppressed`), and the same
  cooldown-in-the-WHERE-clause shape `ClaimAndEnqueueConfirmation` uses
  (`ErrResendCooldownActive`) — the criterion's "rate-limited per
  subscriber so the screen cannot be used to mail-bomb an address".
- `ExpirePendingSweep(ctx, now) (int64, error)` — clears `confirm_token`
  (only) and writes one `confirmation_expired` subscriber_events row per
  address for every pending row past `confirm_expires_at`; the row stays
  `status='pending'`, per the criterion. Idempotent by construction: the
  WHERE clause requires `confirm_token IS NOT NULL`, so a repeat sweep
  over an already-cleared row is a no-op.

**Where the sweep runs — a disclosed, deliberate choice.** #0128's
criterion just says "a sweep expires pending signups". Rather than stand up
a fourth dedicated ticker in `cmd/opencircuit/main.go` (alongside the send
worker, the outbox worker, and the orphan sweep already inside it),
`ExpirePendingSweep` is called once per `OutboxWorker.pass()` — the same
poll loop that already sweeps orphaned claims and drains the queue. It is
cheap (one indexed `UPDATE ... RETURNING`, near-zero rows in the steady
state), and that worker already holds the one `*subscribers.Store` this
package is given. See `outbox_worker.go`'s `pass` doc comment for the
argument in full; a reviewer who wants a separate ticker instead should say
so, this was a judgment call under time pressure, not a forced design.

**`internal/outbox/store.go`** gains two read methods used only by this
issue: `LatestByRecipients(ctx, kind, recipients) (map[string]Row, error)`
— one query for every pending address's confirmation-mail queue state
(`DISTINCT ON (recipient) ... ORDER BY recipient, created_at DESC`), not one
per row — and `AbandonedCountByKind(ctx, kind) (int64, error)`, scoped to
one kind rather than `Counts`' account-wide total.

**`internal/handlers/admin_pending.go`** (new file, new
`AdminPendingHandler`) serves both routes, registered through
`adminRoutes()` in `cmd/opencircuit/main.go` — `#0079`'s AST guard
(`TestAdminRoutesRegisteredOnlyViaTable`) therefore covers them
structurally, and `TestMountAndServe_AdminRoutesRequireSessionAndAdmin`
(`admin_wiring_test.go`, extended to construct a real `adminPendingH`)
proves the session/admin guard end-to-end the same way it does for every
other admin route. `age_seconds`/`expired` are computed **server-side, at
response time** — the client renders them, it does not recompute a "now" of
its own. Error mapping: 404 not found, 409 not pending, 409 suppressed
(with a distinct, named reason in the response body), 429 cooldown active.

**Audit, without the address in Metadata.** A new
`audit.ActionSubscriberResendConfirmation` constant
(`internal/audit/actions.go`) is written on every successful resend.
Metadata carries only `previous_confirm_sent_at` (RFC3339, omitted for a
first-ever send) — deliberately no `email` key, so this new
`audit.Entry{}` call site does not need an entry in
`internal/handlers/audit_email_metadata_guard_test.go`'s
`auditEmailMetadataKnownSites` fixture (that file was flagged as held by
another agent at dispatch time and was not touched; confirmed still
passing unchanged in every scoped run).

**`#0071`/router.ts parity — not applicable here, and said so rather than
skipped silently.** The "screen" is a new `Section` value (`'pending'`) and
subtab inside the existing single-page `/admin` route (`web/src/views/
Admin.svelte`), the same shape every other admin sub-screen already uses
(Subscribers, Suppressions, Campaigns) — none of them has its own
`router.ts`/`knownStaticRoutes` entry, because there is no new top-level SPA
URL to register. `#0128`'s Notes citing `#0071` reads as a generic caution
for issues that DO add a route; this one doesn't.

**No `PrivacyPolicy.svelte` change.** Nothing in this issue adds a new store
of subscriber email that survives erasure — `ListPending`/
`AdminResendConfirmation`/`ExpirePendingSweep` all operate on the existing
`subscribers` table.

**Dashboard (`#0061`).** `pending` count needed no new plumbing — it was
already part of `dashboardSubscriberCounts` before this issue. Added
`abandoned_confirmations` to `dashboardOutboundQueueView` (Go),
`DashboardOutboundQueue` (TypeScript), and `Dashboard.svelte`'s render, in
the same commit, per `worker.go`'s own `CampaignProgress` comment on why
that three-file agreement is never enforced mechanically.

**Frontend.** `web/src/lib/pending.ts` (new): age formatting, signup-source
summary, queue-state/expired badge classes — no DOM, unit-tested in
isolation (CLAUDE.md §1). `web/src/views/admin/Pending.svelte` (new,
mirroring `Dashboard.svelte`'s "markup and wiring only" convention): loads
the list, a click-to-resort age header, per-row resend with independent
per-row loading/error/notice state (no bulk resend, per the criterion).
Wired into `Admin.svelte` as a new `'pending'` subtab between Subscribers
and Suppressions.

## Verification

Every claim rests on Postgres (`TEST_DATABASE_URL`) and
`mailing.RecordingMailer` — no live SES call anywhere (CLAUDE.md §10 item
2). Frontend component-mount claims rest on **jsdom, not a real browser**
(CLAUDE.md §1); the admin console cannot be driven in a real browser in
this sandbox (no WebAuthn ceremony), so a jsdom mount plus static reading
of `Admin.svelte`'s wiring is the honest ceiling here — layout, focus
order, and screen-reader behavior are NOT verified by this pass.

```
ISSUE=0128 scripts/check.sh
```
→ full Go suite (17 packages) all `ok`, zero skips; `npm run check` — 278
files / 0 errors / 0 warnings; `npm test` — 35 test files / **3,852** tests
passed (up from #0127's 3,832: +14 in `lib/pending.test.ts`, +6 in
`Pending.behavior.test.ts`).

**Named tests, backend:**

- `TestAdminResendConfirmation_Success` /
  `TestAdminResendConfirmation_CooldownActive` /
  `TestAdminResendConfirmation_SuppressedRefused` /
  `TestAdminResendConfirmation_NotPending` /
  `TestAdminResendConfirmation_NotFound` (`internal/subscribers`) — the
  store-level atomicity and every guard, including that a cooldown refusal
  leaves the queue count unchanged (mail-bomb guard actually holds).
- `TestExpirePendingSweep_ExpiresPastTTL_LeavesRowPending` /
  `TestExpirePendingSweep_NotYetExpired_LeavesRowUntouched` /
  `TestExpirePendingSweep_IsIdempotent` (`internal/subscribers`).
- `TestOutboxWorker_Pass_ExpiresPendingSignups` (`internal/mailing`) — the
  sweep actually rides the worker's own poll loop, not just the standalone
  store method. Deliberately inserts its subscriber row via raw SQL rather
  than `Store.Create`, disclosed in its own doc comment: `Create` also
  enqueues a `confirmation` row, and `internal/mailing`'s shared test
  database is truncated once per package run, not per test — an
  undrained row would sit `queued` and could be opportunistically claimed
  by a LATER, unrelated test's worker. This is not hypothetical: an earlier
  version of this test did exactly that and corrupted
  `TestOutbox_Enqueue_AdminAlert_DrainsAndSends`'s `mailer.Sent()`
  assertion in the same run; caught immediately, fixed before committing.
- `TestOutbox_LatestByRecipients_ReturnsMostRecentPerRecipient` /
  `_AbsentRecipientOmittedNotErrored` / `_ScopedToKind` /
  `_EmptyRecipientsReturnsEmptyMapWithoutQuery`, and
  `TestOutbox_AbandonedCountByKind_ScopedToKind` (`internal/outbox`).
- `TestAdminPending_List_RequiresSession`,
  `TestAdminPending_List_MarksExpiredAndComputesAge`,
  `TestAdminPending_List_ReflectsRealOutboundQueueState`,
  `TestAdminPending_List_SortOrder`,
  `TestAdminPending_Resend_RequiresSession`,
  `TestAdminPending_Resend_Success_MintsFreshTokenAndAudits` (also asserts
  the audit metadata does NOT contain the address),
  `TestAdminPending_Resend_CooldownReturns429`,
  `TestAdminPending_Resend_SuppressedReturns409`,
  `TestAdminPending_Resend_NotPendingReturns409`,
  `TestAdminPending_Resend_NotFoundReturns404` (`internal/handlers`) —
  real HTTP round trips through `requireSession`/`requireAdmin`.
- `TestAdminDashboardOverview_AbandonedConfirmationsScopedToKind`
  (`internal/handlers`) — abandoning a `registration`-kind row does not
  move `abandoned_confirmations`; abandoning a `confirmation`-kind row
  moves it by exactly 1.
- `cmd/opencircuit`'s existing `TestAdminRoutesRegisteredOnlyViaTable` and
  `TestMountAndServe_AdminRoutesRequireSessionAndAdmin` — both pass
  unchanged in shape, now also covering the two new routes (the latter
  extended to construct a real `adminPendingH`, per this issue).

**Named tests, frontend (jsdom):**

- `web/src/lib/pending.test.ts` (14 cases) — `formatPendingAge`,
  `pendingSignupSource`, `queueStateLabel`/`queueStateBadgeClass`,
  `pendingExpiredBadgeClass`, `sortPendingByAge` — pure logic, no DOM.
- `web/src/views/admin/Pending.behavior.test.ts` (6 cases, jsdom-mounted)
  — loading/empty/error states, populated-row rendering (email, expired
  badge, queue-state badge, UTM-vs-"Direct" source), the sort-header
  click re-fetching with the flipped argument, a resend that shows a
  per-row notice and reloads **without disabling the other row's
  button**, and that a cooldown/suppression error surfaces the server's
  own message verbatim rather than invented copy.

**Not independently re-verified by a fresh reviewer pass** — this is the
implementer's own report. Status stays `in-progress`.

**One disclosed defect discovered and fixed during this pass, not shipped:**
`internal/handlers/admin_pending_test.go`'s cleanup originally issued
`DELETE FROM subscriber_events` to tidy up test residue, which
`TestSubscriberEvents_AppendOnlyGuard` (`internal/subscribers`) correctly
flagged — that guard scans ALL of `internal/`, including `_test.go` files,
for any `DELETE`/`UPDATE` against `subscriber_events` outside `erase.go`.
Fixed by removing that line: deleting the parent `subscribers` row already
leaves any of its `subscriber_events` rows in place with `subscriber_id`
nulled (the FK's `ON DELETE SET NULL`), the same harmless residue every
other test in this package already leaves behind.

## Acceptance criteria

- [ ] `GET /admin/subscribers/pending` lists unconfirmed subscribers: email, age since `confirm_sent_at`, `confirm_expires_at`, whether the token has expired, signup source, UTM attribution
- [ ] Sortable by age; default oldest first
- [ ] `POST /admin/subscribers/{id}/resend-confirmation` mints a fresh token, extends `confirm_expires_at`, enqueues via `#0126`'s queue, stamps `confirm_sent_at`, and is audited
- [ ] Resend is rate-limited per subscriber so the screen cannot be used to mail-bomb an address
- [ ] Resending to a suppressed address is refused with a clear reason
- [ ] Expired pending signups are visually distinct from live ones
- [ ] A sweep expires pending signups past `confirm_expires_at`: `confirm_token` cleared, a `confirmation_expired` subscriber event written, the row left `pending` rather than deleted
- [ ] The screen shows the outbound queue state for each pending address — queued, sent, failed, abandoned — so a delivery failure is visible as a delivery failure rather than as an unexplained non-confirmation
- [ ] Pending count and the count of confirmations abandoned in the queue appear on the admin overview (`#0061`)
- [ ] `synthetic` rows (`migrations/000019`) are excluded
- [ ] Registered through `adminRoutes()` so `#0079`'s guard covers it; Go and TypeScript route tables stay in parity (`#0071`)

## Notes

PRD §5.2 and §8 carry the routes; §6.3 is the flow.

`confirm_expires_at` and `idx_subscribers_confirm_expires` already exist from `migrations/000010`, so the expiry data is there — nothing reads it and nothing acts on it.

**Why this pairs with `#0126`.** Without the durable queue, "why didn't they confirm?" has no answer: the send happened on a goroutine that left no trace. With it, the pending screen can show whether the confirmation was even delivered to SES, which turns a mystery into a fact. Build them in that order.

**Do not add a bulk resend.** A button that re-mails every pending address at once is a spam complaint generator, and pending addresses are by definition the ones least likely to want mail. Per-subscriber resend only.

**Registering outside `adminRoutes()` ships an unguarded admin route that passes every test** — `#0079` is the issue that established this. New admin endpoints go in the table.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §5.2 — Admin routes · 19 lines
  `sed -n '/^### 5\.2 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0032](0032.md) — Build the subscribers admin screen
- Depends on: [#0126](0126.md) — Durable outbound queue and subscriber activity log
- Relates to: [#0061](0061.md), [#0071](0071.md), [#0079](0079.md)

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Phase 2 only — no separate phase-1 planning pass ran; design decisions were
made and disclosed inline (see Implementation notes, especially the
"where the sweep runs" call). Status stays `in-progress`: this is the
implementer's own report, not an independently re-verified phase-3 review.
The orchestrator fills in the token counts.
