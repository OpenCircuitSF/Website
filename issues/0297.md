# 0297 — Re-stamp `claimed_at` per row so an orphan window can be honest and small

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | internal/outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |
| **Closed** | 2026-08-28 |
| **Commit** | 724728c |

> 🟢 Resolved 2026-08-28 — second review. All four bounce defects were
> re-verified independently, each against the code rather than against the
> implementer's account: the defect-1 mutation was re-run from scratch in this
> reviewer's own throwaway worktree and scratch database and fails on the
> substantive assertion; the new regression pin is race-free (deterministic by
> construction, race-detector clean, stable across four test orderings); the
> 30s window follows from the package's own constants with nothing measured off
> the machine; and its oracle fires against the exact zero-margin value defect 3
> bounced on. One residual comment inaccuracy remains and is reported for
> filing rather than bounced on — see the second review's notes below.

## Description

Three orphan windows now have the same shape of problem, because `ClaimDue` and
`ClaimBatch` stamp **one `claimed_at` per batch** and then the rows are
processed serially:

| Window | Where | Honest batch-derived value |
|---|---|---|
| `outboxOrphanStaleAfter` | `internal/mailing/outbox_worker.go` | **700s** (`#0284`) |
| ~~`orphanStaleAfter`~~ | ~~`internal/mailing/worker.go`~~ | **not affected — see the correction below** |
| `intakeOrphanStaleAfter` | `internal/handlers/subscribe_intake.go` | ~200s (`#0294`) |

Each is being corrected to a value derived from `batchSize × per-row bound`,
because that is what the current claim model actually permits. The values are
honest and the derivations are right. They are also **large**, and the reason is
structural rather than necessary: the last row of a batch is treated as having
been claimed since before the first row was even attempted.

**The alternative, named by `#0284`'s reviewer: re-stamp `claimed_at` on each
row as processing of that row begins.**

> **Correction (#0295, 2026-08-27).** `internal/mailing/worker.go` is **not** one
> of the three, and **already implements exactly this design** — so the pattern
> below is not hypothetical, it has a working reference in this codebase. Read
> it first. `SendStore.ClaimBatch` never touches `claimed_at` or status; it only
> selects and rechecks eligibility, leaving rows `'queued'`. The atomic claim
> (`status='sending', claimed_at=now()`) is `SendStore.ClaimRow`, issued as the
> **first statement inside `sendOne`**, per recipient, at the moment that
> recipient's send begins. Batch size therefore never enters its bound, which is
> why its 70s window carries 35s of real margin rather than the near-zero the
> outbox worker had. The row above naming `worker.go` was wrong, and `#0295`
> resolved by correcting the premise rather than changing a constant. A row's claim age would then measure its
own processing rather than its predecessors', and every window collapses to a
per-row bound — roughly 35s for the outbox worker instead of 700s.

## Why this is worth doing, and why it is not urgent

The cost of a large window is **recovery latency on the crash path only**.
`Stop` already releases claims on graceful shutdown, so the window only governs
how long a row stays stuck after a hard kill. Nothing is incorrect today.

The cost of getting it *wrong* in the other direction is a duplicate send, which
is `#0254`'s defect — a duplicate confirmation link or recovery magic link.

So: not urgent, and deliberately not folded into `#0284`, `#0294`, or `#0295`,
each of which correctly fixed its derivation under the existing claim model
rather than changing the model inside a bounce fix.

## Acceptance criteria

1. `claimed_at` is re-stamped per row at the point processing of that row
   begins, in the shared `internal/outbox` claim path, so all three consumers
   benefit rather than one.
2. The re-stamp must not create a window where a row is claimed by nobody — the
   transition from batch claim to per-row claim has to be safe against a sweep
   running between them. Enumerate the states and prove it.
3. Each of the three windows is then re-derived from the per-row bound, and each
   doc comment states the new model. `#0284`, `#0294`, and `#0295` must be
   resolved before this starts, or this will conflict with all three.
4. **Do not assert a bound the code does not have.** `#0284` bounced for exactly
   that; its review notes carry the reasoning about `rate.Limiter` regenerating
   tokens during a send, which this change is what would finally make irrelevant.
5. The invariant tests from `#0284`/`#0294`/`#0295` are updated, not deleted, and
   each must still fail when its fix is reverted. Note `#0284`'s reviewer found
   one test that passed against full reversion — check for that failure mode
   explicitly.
6. A DB-backed test drives the crash path: kill processing mid-batch, and assert
   both that unprocessed rows are reclaimable promptly and that a row still
   being processed is not swept.
7. Not a load or performance exercise (CLAUDE.md §5). The justification is model
   correctness and recovery latency, expressed in the code's own constants.
8. `ISSUE=NNNN scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/... ./cmd/opencircuit/...`
   passes with no skips.

## Relation

- Named by: [#0284](0284.md)'s review as the alternative to a large window, and
  recommended for filing by its implementer
- Depends on: [#0284](0284.md), [#0294](0294.md), [#0295](0295.md) — all three
  must land first
- Relates to: [#0254](0254.md) — whose duplicate-send defect is the failure mode
  every one of these windows guards against
- Relates to: [#0126](0126.md) — which built the queue and its claim model

## Work log

**2026-08-28, phase-2 implementation.**

Read #0284, #0294, #0295 (all resolved) and #0283/#0288 for context before
touching anything, per the dispatch instruction. Confirmed `worker.go`'s
`SendStore.ClaimBatch`/`ClaimRow` split (#0295's reference implementation)
before designing anything, rather than inventing a new shape.

**Criterion 1 — re-stamp per row, in the shared claim path.** Added two new
methods to `internal/outbox.Store` (`internal/outbox/store.go`), mirroring
`SendStore.ClaimBatch`/`ClaimRow` exactly:

- `SelectDue(ctx, limit, kinds...)` — `SELECT ... FOR UPDATE SKIP LOCKED`
  inside a transaction that commits immediately after selecting, claiming
  NOTHING (status/claimed_at untouched).
- `ClaimRow(ctx, id)` — the atomic per-row claim (`status='sending',
  attempts+1, claimed_at=now() WHERE status='queued'`), meant to be called
  immediately before that row's own processing begins.

`ClaimDue` itself was deliberately left unchanged (still a whole-batch
atomic claim) — it remains correct and is still what this package's own
tests and a few cross-package test-setup call sites want (see its doc
comment for the reasoning). Only the two PRODUCTION consumers were moved
onto the new pair:

- `internal/mailing/outbox_worker.go`'s `pass`: `SelectDue` up front, then
  per-id in the send loop — `limiter.Wait` → `ClaimRow` (its own detached,
  bounded `context.WithTimeout(ctx, writeStatusTimeout)`) → `sendOne`.
  `trackClaimed`/`untrackClaimed` now track at most one row at a time
  (doc comments updated).
- `internal/handlers/subscribe_intake.go`'s `intakePass`: `SelectDue` up
  front, then per-id `ClaimRow` (its own `context.WithTimeout(h.sendCtx,
  intakeRowTimeout)`) → `processIntakeRow`.

**Criterion 2 — enumerated the states, proved safety.** A row SelectDue
returns and ClaimRow has not yet reached stays plain `'queued'` the entire
time it waits its turn — never `'sending'`, so OrphanSweep (which only
ever touches `'sending'` rows) cannot touch it. There is no window where a
row is "claimed by nobody": it is either genuinely unclaimed (`'queued'`,
safe — anyone's `ClaimRow` can pick it up) or genuinely claimed
(`'sending'`, protected by staleAfter). Two concurrent `SelectDue` calls
returning the same id is safe by construction: `ClaimRow`'s own `WHERE
status = 'queued'` is the actual exclusivity, not `SelectDue`'s
`FOR UPDATE SKIP LOCKED` (free insurance only, released at that
transaction's commit — same relationship `ClaimBatch`/`ClaimRow` already
document). Proved with a DB-backed test (criterion 6, below) rather than
asserted.

**Criteria 3/4 — each of the three windows re-derived, no bound asserted
the code doesn't have.**

- `outboxOrphanStaleAfter` (`internal/mailing/outbox_worker.go`): the
  batch term drops out entirely (only one row is ever `'sending'` under
  this worker at a time now). True worst case, including the newly-bounded
  `ClaimRow` round-trip residual: `sendMessageTimeout + 2*writeStatusTimeout`
  = 40s. Set to `2 * (sendMessageTimeout + writeStatusTimeout)` = 70s — the
  SAME expression and value as `worker.go`'s own `orphanStaleAfter` — 30s
  of margin over the 40s true worst case. Doc comment rewritten in full
  (package doc comment's "orphanStaleAfter is recomputed here" section,
  and the var's own comment) to state the new model and name the residual
  that remains (SelectDue/outboxEffectiveSendRate still run on the
  worker's unbounded ambient `ctx`, but neither holds a row's claim open,
  so a hang there delays the NEXT claim rather than falsifying an existing
  one).
- `intakeOrphanStaleAfter` (`internal/handlers/subscribe_intake.go`): same
  shape. True worst case (ClaimRow round trip + processIntakeRow's own
  bound, both `intakeRowTimeout`) = 20s. Set to `2 * intakeRowTimeout` =
  20s exactly (no extra margin beyond the doubling itself, matching the
  two terms' actual composition). Down from 210s.
- `orphanStaleAfter` (`internal/mailing/worker.go`) — untouched here. It
  already had the per-row model (#0295); its own remaining residual
  (`ClaimRow`/`MarkFailedRow`/`MarkRetryOrFailed` on the undeadlined
  ambient context) is #0302's territory, done next in this same batch.

**Criterion 5 — invariant tests updated, not deleted, checked against the
"passes on full reversion" failure mode `#0284`'s reviewer found.**
Rewrote (renamed, not left as dead code citing a deleted test — the guard
`TestNoCommentCitesUndefinedTestFunction` catches exactly that) both
batch-derived tests into single-row ones:

- `TestOutboxOrphanStaleAfterCoversFullBatch` →
  `TestOutboxOrphanStaleAfterCoversSingleRowHold`
  (`internal/mailing/outbox_orphan_stale_after_test.go`), modeled on
  `worker.go`'s own `TestOrphanStaleAfterCoversLegitimateSingleRowHold`
  (#0295): oracle is `sendMessageTimeout + writeStatusTimeout`, a
  DIFFERENT expression from the subject's `2 * (...)`, asserted with `<=`
  as the failure condition (CLAUDE.md's "an oracle must not be the same
  bytes as its subject", #0258).
- `TestIntakeOrphanStaleAfterCoversFullBatch` →
  `TestIntakeOrphanStaleAfterCoversSingleRowHold`
  (`internal/handlers/subscribe_intake_orphan_stale_after_test.go`): oracle
  is `intakeRowTimeout` alone, subject is `2 * intakeRowTimeout`.

Checked explicitly for the exact failure mode named: pointing either
`got` at its PRE-#0297 batch-derived value against the NEW oracle still
passes trivially (both old values comfortably exceed the new, smaller
single-row bound) — the old batch-derived tests are what actually caught
THAT regression; they are superseded, not silently redundant. Proved both
new tests genuinely fail on an under-sized value, in the live tree (own
files only, restored and hash-verified afterward — not a throwaway
worktree, since this was a one-line var-initializer edit with an
immediate revert, no risk to any other agent's files):

- `outboxOrphanStaleAfter = sendMessageTimeout` (30s, dropping the
  doubling) → `TestOutboxOrphanStaleAfterCoversSingleRowHold` FAILED:
  "outboxOrphanStaleAfter (30s) does not exceed ... (35s)".
- `intakeOrphanStaleAfter = intakeRowTimeout` (10s) →
  `TestIntakeOrphanStaleAfterCoversSingleRowHold` FAILED:
  "intakeOrphanStaleAfter (10s) does not exceed ... (10s)".

Restored both files afterward; sha256 confirmed byte-identical to the
pre-mutation copies before re-running the suite.

Also rewrote `TestOutboxWorker_Stop_ReleasesClaimedRow` →
`TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`
(`internal/mailing/outbox_worker_test.go`): under the new per-row model,
`Stop`'s `releaseAll` provably never has anything to release in a
graceful shutdown (a row not yet reached stays `'queued'`, never claimed;
the one row genuinely mid-send always runs to completion before `Run` can
return, since `sendOne` has no internal `stopCh` check) — documented this
explicitly rather than silently weakening the test, kept `releaseAll` and
its machinery as a defensive safety net (still exercised directly by
`TestOutboxWorker_ReleaseAll_ConcurrentCallsDoNotRace`, #0266, against a
fabricated `w.claimed`), and rewrote the test's assertions to the new,
still-meaningful observable outcome: exactly one row sent, one left
`'queued'` (never claimed — not released).

**Criterion 6 — DB-backed crash-path test.**
`TestOutbox_SelectThenClaimRow_CrashMidBatch`
(`internal/outbox/select_due_claim_row_test.go`): `SelectDue`s a batch of
3, `ClaimRow`s only the first (simulating a crash mid-batch), then proves
both directions — the two unprocessed rows are immediately reclaimable
(never having left `'queued'`) and the claimed row is NOT swept while
genuinely fresh (`OrphanSweep` with a generous window) but IS swept once
its claim is treated as stale (`OrphanSweep` with a zero-width window) —
so the mechanism is shown to actually discriminate, not just "never
sweep." Also added `TestOutbox_SelectDue_LeavesRowsQueuedUntilClaimRow`
(the architectural proof, mirroring #0295's
`TestSendStore_ClaimBatch_LeavesRowsQueuedUntilClaimRow`) and
`TestOutbox_ClaimRow_LosingRaceReturnsClaimedFalse`.

**Not a load exercise** (criterion 7): every quantity in every derivation
above is one of the package's own constants; nothing was sized against
machine timing.

Files changed: `internal/outbox/store.go` (new `SelectDue`/`ClaimRow`,
`ClaimDue` doc comment), `internal/outbox/select_due_claim_row_test.go`
(new), `internal/mailing/outbox_worker.go` (`pass` rewritten,
`outboxOrphanStaleAfter` re-derived, doc comments), `internal/mailing/
outbox_worker_test.go` (test renamed/rewritten, stale citations fixed),
`internal/mailing/outbox_orphan_stale_after_test.go` (rewritten),
`internal/handlers/subscribe_intake.go` (`intakePass` rewritten,
`intakeOrphanStaleAfter` re-derived, doc comments),
`internal/handlers/subscribe_intake_test.go` (one stale-mechanism comment
corrected), `internal/handlers/subscribe_intake_orphan_stale_after_test.go`
(rewritten).

**To report to the orchestrator:** none beyond what #0302/#0303 (this
same batch) already cover.

**2026-08-28, phase-2 re-implementation (review remedy, first bounce).**
Read the bounce's four defects in full before touching anything. Code
commit `4b369c9` left in place, as instructed; this is additive only.

- **Defect 1 (decisive).** Added
  `TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch`
  (`internal/handlers/subscribe_intake_test.go`), modeled on
  `TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`'s shape but using this
  package's own natural blocking seam — `blockingSuppressionChecker`, a
  `SuppressionChecker` whose `IsSuppressed` blocks the FIRST call (a
  `sync.Once`) until released, standing in for `blockingMailer` since
  `processIntakeRow` has no `Mailer` to block but calls `IsSuppressed` just
  as early and unconditionally. Seeds two due `KindSubscribeIntake` rows,
  waits for the real background poller (started by `NewSubscribeHandler`)
  to block inside the first row's processing, and asserts on the PAIR:
  exactly one row `'sending'` (`claimed_at` non-NULL, `attempts=1`) and one
  still `'queued'` (`claimed_at` NULL, `attempts=0`) — a whole-batch claim
  cannot satisfy that, since its one UPDATE stamps every row in the batch
  before either row's own processing begins.

  **Mutation proof**, in a throwaway worktree
  (`git worktree add`, own scratch database `opencircuit_test_0297cmut`,
  never the shared or main-tree database): copied the current (uncommitted)
  working-tree files into the worktree — the worktree started at `0acd57c`
  and does not itself contain this pass's edits, so the new test had to be
  carried in — then reverted ONLY the worktree copy's `intakePass` to a
  whole-batch `outbox.Store.ClaimDue` call (the exact pre-#0297 shape),
  leaving every other file (including `intakeOrphanStaleAfter`) untouched.
  Ran `TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch` alone in
  the worktree: **FAILED**, for the intended reason —
  `status while blocked = (id1="sending", id2="sending"), want exactly one
  "sending" ... and one "queued"` — both rows were already `'sending'` the
  instant the batch-wide claim committed, before either row's own
  processing had started. Removed the worktree and its branch
  (`git worktree remove --force`, `git branch -D`) and dropped the scratch
  database afterward; `git status` and `git diff --stat` in the MAIN tree
  confirmed throughout and after that none of this pass's real files were
  ever touched by the mutation — the proof ran entirely in the worktree's
  own copies.

- **Defect 2.** Reworded the three sites the review named
  (`outbox_worker.go` lines then-122 and then-534, `subscribe_intake.go`
  line then-125) from "detached" to "bounded", each with one added
  sentence explaining why the claim call is deliberately bounded FROM the
  caller's ambient context rather than detached from it (a claim that
  cannot complete before shutdown should not be taken at all) — contrasted
  explicitly against the genuinely detached `context.Background()` writes
  elsewhere in the same files, which keep the word. Also corrected two
  comments the review did not enumerate but which shared the exact same
  staleness (`OutboxWorker.claimed`'s field comment still said "ClaimDue"
  and "anything still present when Stop reads this set is exactly the set
  Stop's doc comment promises to release" — a promise this same pass just
  removed from `Stop`'s own comment, so leaving the field comment
  unchanged would have reintroduced an inconsistency of the identical
  kind).

- **Defect 3.** `intakeOrphanStaleAfter` raised from `2 * intakeRowTimeout`
  (20s, equal to its own derived worst case, zero margin) to
  `3 * intakeRowTimeout` (30s) — one additional `intakeRowTimeout` of
  margin on top of the two worst-case terms, matching the "one extra term
  of margin" posture `outboxOrphanStaleAfter` (30s over a 40s worst case)
  and `orphanStaleAfter` (35s over a 35s worst case) reduce to once
  expressed in their own constants. Derived from the package's own
  constants only, never from measured timing (CLAUDE.md §5).
  `TestIntakeOrphanStaleAfterCoversSingleRowHold`'s oracle raised from
  `intakeRowTimeout` alone (10s — half the actually-derived bound) to
  `2 * intakeRowTimeout` (20s, the full derived worst case), so the test
  now enforces the invariant the doc comment actually derives rather than
  a weaker one — while staying a different expression from the subject's
  `3 * intakeRowTimeout` initializer (#0258).

- **Defect 4.** `Stop`'s doc comment (`outbox_worker.go`) rewritten to
  state what is now true: it no longer promises to release any row, since
  `releaseAll` provably has nothing to release on every path that reaches
  it under the per-row claim model (a row not yet reached is still
  `'queued'`; the one row genuinely mid-send always runs to completion
  first, since `sendOne` has no internal `stopCh` check) — `releaseAll` is
  kept only as a defensive net, matching what
  `TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`'s own comment already
  said. `trackClaimed`'s doc comment corrected — `Stop` blocks on
  `<-w.doneCh`, which cannot close mid-send, so it was never actually true
  that "a Stop landing during the send still sees it in `w.claimed`"; the
  corrected comment explains tracking is still done for `releaseAll`'s
  defensive-net role, not because `Stop` observes it mid-send. Both stale
  `ClaimDue` references in `subscribe_intake.go` (lines then-50 and
  then-292) corrected to `SelectDue`/`ClaimRow` respectively, matching the
  eight other references in that same file this pass's first round already
  updated.

All four fixes verified together: `go build`/`go vet`/`gofmt -l` clean;
every edited file's `gofmt` stdout sha256-matched the on-disk file
(byte-stable, no doc-comment rewriting — CLAUDE.md's gofmt/backtick
gotcha); grepped the full tree for `//.*\.ClaimDue(`, `//.*\.OrphanSweep(`,
`//.*\.SelectDue(` — zero matches, tree stays clean of the literal-call
pattern a shell harness measures.

## Verification

```
ISSUE=0297 scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/... ./cmd/opencircuit/...
```

`uptime` at start: load averages ~2.0–3.6 (idle-ish; §5's no-performance-
requirement rule followed throughout). Result: `go build`/`go vet`/`gofmt
-l` all clean; `internal/outbox` ok (0.29s), `internal/mailing` ok
(3.2s), `internal/handlers` ok (8.4s), `cmd/opencircuit` ok (6.7s) — zero
skips, no package reported `0.00s`. `VERIFICATION PASSED`. Also
independently confirmed by name:
`TestOutbox_SelectDue_LeavesRowsQueuedUntilClaimRow`,
`TestOutbox_ClaimRow_LosingRaceReturnsClaimedFalse`,
`TestOutbox_SelectThenClaimRow_CrashMidBatch`,
`TestOutboxOrphanStaleAfterCoversSingleRowHold`,
`TestIntakeOrphanStaleAfterCoversSingleRowHold`, and the existing
`claim_kinds_call_site_guard_test.go` suite (`TestNoUnscopedOutboxClaim
CallOutsidePackage`, `TestNonExemptFloorCatchesScanRootsNarrowedToSelf`,
`TestClaimKindsGuardFiresOnFixtureWithNoKinds`) — all pass; the
call-site floor (8) is unaffected by this issue (`SelectDue`/`ClaimRow`
are new method names the guard doesn't match at all; it only tracks
`ClaimDue`/`OrphanSweep`, both of which this issue's two production call
sites moved away from, at most reducing — never inflating — the count
that guard measures). Scratch databases `opencircuit_test_0297` and
friends dropped; `pgrep -f 'go test|\.test '` clean at the end of this
session.

## Verification — second pass (review remedy, first bounce)

```
ISSUE=0297c scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/... ./cmd/opencircuit/...
```

`uptime` at start: load averages ~1.9–2.3 (idle; §5's no-performance rule
followed throughout — no constant here was sized against machine timing).
Result: `go build`/`go vet`/`gofmt -l` all clean; `internal/outbox` ok
(0.43s), `internal/mailing` ok (3.6s), `internal/handlers` ok (9.6s),
`cmd/opencircuit` ok (7.9s) — zero skips, no package reported `0.00s`.
`VERIFICATION PASSED`. `scripts/check.sh guards` also run in full:
`VERIFICATION PASSED`, 0 failures, including the outbox claim-kinds
call-site floor guards (unaffected by this pass — no call sites added or
removed).

Independently confirmed by name (own scratch database
`opencircuit_test_0297c`):
`TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch` (defect 1's
new pin — pass in the real tree),
`TestIntakeOrphanStaleAfterCoversSingleRowHold` (defect 3's corrected
oracle), `TestNoCommentCitesUnresolvedPathOrSection`,
`TestNoCommentCitesUndefinedTestFunction`,
`TestNoDocCommentNamesADifferentDeclarationInSameFile`,
`TestNoAdminFacingStringCitesInternalDocs` (the AST citation/doc-comment
guards — confirm the new test's own doc comment and this pass's rewritten
comments cite nothing unresolved). Mutation proof for defect 1 (revert
`intakePass` to whole-batch `ClaimDue`, throwaway worktree, own scratch
database `opencircuit_test_0297cmut`) FAILED as intended — see the Work
log entry above for the exact failure output — then the worktree, its
branch, and its scratch database were all removed; `git status`/
`git diff --stat` in the main tree confirmed clean of that mutation
throughout. `gofmt` stdout sha256-matched the on-disk file for every
edited file (`internal/mailing/outbox_worker.go`,
`internal/handlers/subscribe_intake.go`,
`internal/handlers/subscribe_intake_test.go`,
`internal/handlers/subscribe_intake_orphan_stale_after_test.go`) — no
doc-comment rewriting. Scratch databases `opencircuit_test_0297c` and
`opencircuit_test_0297cmut` dropped; `pgrep -f 'go test|\.test '` clean at
the end of this session.

## Review notes

**Verdict: bounced.** The claim model is right, both windows follow correctly
from it, and every named test passes — but the change is incomplete against its
own criteria 3, 4 and 5, and the gap is concentrated on the intake half. The
code commit `4b369c9` stays in place; the work below is additive.

### What was verified and is correct

**The core safety claim holds.** `ClaimRow` is genuinely atomic — a single
conditional `UPDATE ... WHERE id = $1 AND status = $3` with `RETURNING`, not a
read-then-write, so `pgx.ErrNoRows` → `claimed=false` is the real exclusivity
gate and `SelectDue`'s `FOR UPDATE SKIP LOCKED` is correctly described as free
insurance rather than the guarantee. Walked the three states the criterion asks
for:

- *Crash between `SelectDue` and `ClaimRow`* — the row never left `'queued'`,
  so it is immediately reclaimable and `OrphanSweep` (which only touches
  `'sending'`) never sees it. Confirmed by
  `TestOutbox_SelectThenClaimRow_CrashMidBatch`, which also proves the sweep
  still *does* discriminate (fresh claim not swept, zero-width window swept) —
  a genuine two-directional test, not a "never sweep" assertion.
- *Two workers racing `ClaimRow`* — `WHERE status = 'queued'` means at most one
  wins; the loser gets `claimed=false` and skips. Covered by
  `TestOutbox_ClaimRow_LosingRaceReturnsClaimedFalse`.
- *`Stop` mid-loop* — `pass`'s `stopCh` check sits before each `ClaimRow`, so
  rows not yet reached are still `'queued'`.

**`releaseAll` is a no-op, and the proof is stronger than claimed.** `Stop`
calls it only on the `<-w.doneCh` branch, i.e. only after `Run` has fully
returned; and in `pass` there is no `return` between `trackClaimed` and
`untrackClaimed` (`sendOne` has no internal `stopCh` check and there is no
`recover` in this file). So `w.claimed` is empty on *every* path that reaches
`releaseAll`, not merely the graceful one. The ungraceful path does not need it
either — a hard kill runs no Go code at all, and the orphan sweep is what
recovers there. Keeping it as a defensive net is a reasonable call and
`TestOutboxWorker_ReleaseAll_ConcurrentCallsDoNotRace` still covers it.

**Both windows re-derived independently by this review, from the code's own
constants, with nothing measured off the machine:**

*`outboxOrphanStaleAfter`.* `claimed_at` is stamped server-side when
`ClaimRow`'s `UPDATE` commits, at T. The client learns within
`writeStatusTimeout` (5s — `claimCtx`'s deadline; if it expires the call errors
and the row is not processed at all). `sendOne` then bounds `render` +
`mailer.Send` together in one `sendCtx` = `sendMessageTimeout` (30s). The
terminal write — `MarkSent`, or `finishFailed`, or
`deferMissingPhysicalAddress` — each runs on one `writeCtx` of
`writeStatusTimeout` (5s) and moves the row out of `'sending'`; checked that
`finishFailed`'s *two* round trips (`effectiveMaxRetries` then
`MarkRetryOrAbandon`) share a single 5s deadline rather than getting one each.
`RecordEvent` runs after the transition. `limiter.Wait` runs *before*
`ClaimRow`, so the rate limiter is out of the bound — which is exactly what
`#0284` could not achieve under the batch model. Batch size is not a term:
at most one row is `'sending'` under this worker at a time.
**Worst case = 30 + 2×5 = 40s. Window = 2 × (30 + 5) = 70s. Margin 30s.**
Matches the implementer's figures.

*`intakeOrphanStaleAfter`.* Same shape. Round-trip residual < `intakeRowTimeout`
(10s), then `processIntakeRow` bounds unmarshal, `IsSuppressed`, `FindByEmail`,
`dispatchMutation` (synchronous — it calls `newSignup`/`existingSignup` on the
same ctx, it does not enqueue) and `MarkSent` inside one further
`intakeRowTimeout` (10s). Batch size drops out.
**Worst case < 10 + 10 = 20s. Window = 2 × 10 = 20s.** Also matches — and this
is where the first defect is (defect 3, below). Sanity check on both: the old
values fall straight out of the same formulas with batch size restored — 20 ×
(30 + 2×5) = 800 and (20+1) × 10 = 210 — so the new numbers are the old
derivations with the batch term removed, which is precisely what the change
earns. Nothing here was sized against load; `uptime` at the start of this review
was 1.39.

---

### Defect 1 (the substantive one) — the intake half of the change has no
### regression pin, and a partial revert passes the entire suite

Criterion 5 asks specifically for the failure mode `#0284`'s reviewer found: a
test that still passes against reversion. The implementer checked one direction
— reverting the *constant* — and correctly reported that the old batch-derived
values pass the new oracles trivially. It did not check the other direction,
which is the dangerous one: **reverting the claim model while keeping the new,
much smaller window.**

That direction was tested by this review, in a throwaway worktree, by rewriting
`intakePass` back to a whole-batch `ClaimDue` and leaving
`intakeOrphanStaleAfter` at 20s — the exact configuration that reinstates
`#0254`'s failure mode with a window 10.5× smaller than the honest
batch-derived bound:

```
ok  internal/handlers   4.709s
ok  internal/outbox     0.297s
ok  internal/mailing    4.968s
ok  internal/db         5.074s
ok  cmd/opencircuit     1.803s
```

**Green everywhere.** Nothing in the tree detects it.

The outbox half is not exposed this way, and it is worth seeing why — it is the
model for the fix. The same mutation applied to `OutboxWorker.pass` fails
immediately:

```
--- FAIL: TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued
    outbox_worker_test.go:802: status before Stop = (id1="sending", id2="sending"),
        want exactly one "sending" (blocked in sendOne) and one "queued"
```

That pre-`Stop` checkpoint — *exactly one row `'sending'`, exactly one still
`'queued'` while the worker is mid-send* — is a direct observation of the
per-row claim model, and it is the thing the 70s window rests on. The intake
poller has no equivalent. Its three existing tests cover single-row recovery
and kind-scoping only.

**Remedy.** Add a DB-backed test to `internal/handlers/subscribe_intake_test.go`
that enqueues **two** due `KindSubscribeIntake` rows, blocks the poller inside
the first row's processing (the existing `blockingMailer` pattern in
`internal/mailing/outbox_worker_test.go` is the model; here the natural seam is
a hook or a blocked dependency inside `processIntakeRow`'s path), and asserts
while it is blocked that **exactly one row is `'sending'` and the other is still
`'queued'`** — with `claimed_at IS NULL` and `attempts = 0` on the queued one,
so a whole-batch claim cannot satisfy it. Confirm it fails against the
`ClaimDue` shape before landing it.

### Defect 2 — three doc comments call a non-detached context "detached"

This is criterion 4's class ("do not assert a bound the code does not have"),
and it sits in the derivation paragraph itself. The bound is not falsified — a
`context.WithTimeout(parent, d)` is capped at `d` whichever parent it has — but
the word *detached* has a specific, load-bearing meaning in these two files
(`context.Background()`, so the write survives ambient cancellation), and
`#0302` in this same batch uses it correctly for exactly that. Three new sites
use it for the opposite thing:

| Site | Comment says | Code is |
|---|---|---|
| `outbox_worker.go:122` | "wraps it in a detached `context.WithTimeout(ctx, writeStatusTimeout)`" | self-contradictory in one sentence |
| `outbox_worker.go:534` | "A detached, bounded context (**not the caller's ambient ctx**)" | `context.WithTimeout(ctx, …)` at line 538 — it *is* the caller's ambient ctx |
| `subscribe_intake.go:125` | "wrapped in a detached `context.WithTimeout(h.sendCtx, intakeRowTimeout)`" | derived from `h.sendCtx` |

**Remedy.** Do not change the code — deriving the claim from the ambient
context is defensible here (it stops a new claim being taken during shutdown,
which is the opposite of `#0302`'s failure-write case, where the write *must*
land). Change the three comments to say "bounded" rather than "detached", and
say in one sentence why this call is deliberately *not* detached while
`sendOne`'s writes are. `outbox_worker.go` lines 115/116/567/573 use "detached"
correctly for the `context.Background()` cases, so the file already sets the
meaning these three break.

### Defect 3 — `intakeOrphanStaleAfter` has exactly zero margin, and its test
### does not enforce the derivation

The window (20s) equals the worst case the doc comment itself computes (20s).
It is not *false* — `OrphanSweep`'s predicate is strict (`claimed_at < now() -
$3`) and a processed row's round-trip residual is strictly under 10s — but it
is the only one of the three windows with no engineered slack, against
`outboxOrphanStaleAfter`'s 30s and `orphanStaleAfter`'s 30s. And
`TestIntakeOrphanStaleAfterCoversSingleRowHold`'s oracle is `intakeRowTimeout`
alone (10s), i.e. **half** the derived bound, so the test would still pass at
11s — well inside the 20s worst case. The invariant that was derived is not the
invariant that is checked.

**Remedy.** Either raise the window to `3 * intakeRowTimeout` (30s), giving the
same 10s-of-margin-per-term posture the other two carry, or state explicitly in
the doc comment that this window is deliberately equal to its worst case and
why that is acceptable. Either way, raise the test's oracle to the derived
bound (`2 * intakeRowTimeout`) so it actually enforces the derivation — keeping
it a different *expression* from the subject, per `#0258`.

### Defect 4 — stale doc comments the change itself invalidated

Criterion 3 asks that each doc comment state the new model. Four were missed,
and three of them are in production code rather than tests:

- **`outbox_worker.go:353`, `Stop`'s own doc comment** — still says `Stop`
  "releases any row this process currently holds claimed-but-unsent back to
  `'queued'` (so a restart … picks it up immediately instead of waiting out
  `outboxOrphanStaleAfter`)". Under the new model that is unreachable, as this
  pass itself established. It also still says "whatever ids remain are exactly
  the rows `pass`'s `stopCh` check left claimed" — `pass`'s `stopCh` check now
  leaves none. `#0297` documented the no-op in the *test's* comment but left
  `Stop`'s own contract asserting the old behaviour.
- **`outbox_worker.go:419`, `trackClaimed`** — "so a `Stop` landing during the
  send still sees it in `w.claimed`". `Stop` blocks on `doneCh` and therefore
  cannot observe it mid-send.
- **`subscribe_intake.go:292`, `processIntakeRow`** — "this call's own
  `ClaimDue` having put it there". It is `ClaimRow` that puts it there now.
- **`subscribe_intake.go:50`** — "`ClaimDue`'s `WHERE next_attempt_at <= now()`
  excludes it". That predicate is `SelectDue`'s for this poller now.

The last two are clearly misses rather than deliberate history: `#0297` rewrote
eight other `ClaimDue` references in that same file.

**Remedy.** Update all four. For `Stop`, say what is now true — it stops `Run`
and waits for it; `releaseAll` is retained as a defensive net that the current
`pass` provably never gives work to — which is a better comment than the one
there now, and matches what the test's doc comment already says.

---

### Not defects, checked and dismissed

- **`ClaimDue` left unchanged** is right. It remains correct for a caller with
  no serial drain, its doc comment now explains the split, and both production
  consumers moved off it.
- **The renamed test** (`TestOutboxWorker_Stop_ReleasesClaimedRow` →
  `TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`) is a good rename — the new
  name describes what the test now observes, and the test is genuinely stronger
  than a rename-and-weaken would be, as defect 1's mutation shows. The dangling
  citation it created in `issues/0258.md` was repaired by `#0303` in this same
  batch; that repair is correct and stands.
- **The `internal/testdb` failure is unrelated.** `git diff --name-only
  4b369c9~1 80925b3` touches neither `internal/testdb` nor `migrations/`, and
  this batch adds no migration (tree is at `000001`–`000025`, all pre-existing;
  CLAUDE.md §1's frozen `000001`–`000022` are untouched). Filed as `#0339`.

### Verification re-run by this reviewer

`ISSUE=0297b scripts/check.sh go ./internal/... ./cmd/...` — `go build`,
`go vet`, `gofmt -l` clean; **zero skips**; every package `ok` except `#0339`.
`scripts/check.sh guards` — `VERIFICATION PASSED`, 0 failures. Named tests
observed passing: `TestOutbox_SelectDue_LeavesRowsQueuedUntilClaimRow`,
`TestOutbox_ClaimRow_LosingRaceReturnsClaimedFalse`,
`TestOutbox_SelectThenClaimRow_CrashMidBatch`,
`TestOutboxOrphanStaleAfterCoversSingleRowHold`,
`TestIntakeOrphanStaleAfterCoversSingleRowHold`,
`TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`,
`TestOutboxWorker_ReleaseAll_ConcurrentCallsDoNotRace`,
`TestOrphanStaleAfterCoversLegitimateSingleRowHold`,
`TestSubscribeIntakeWorker_RecoversRowTheFastPathNeverProcessed`,
`TestSubscribeIntakeWorker_OrphanSweepDoesNotTouchOtherKinds`.

Every mutation in this review ran in a throwaway worktree
(`git worktree add`, own scratch database `opencircuit_test_0297b`); the main
tree was never modified, and the worktree's `git status` was clean after each
restore.

### Note on model policy (CLAUDE.md §3)

This is `#0297`'s **first** bounce, and every remedy above names the exact file,
the exact replacement, and the exact test shape to add. Nothing here requires a
design decision — it is transcription. **Stay on Sonnet.**

---

## Review notes — second pass (2026-08-28)

**Verdict: resolved.** Every one of the four bounce defects is genuinely fixed,
and each was re-verified against the code rather than against the implementer's
account. All eight acceptance criteria hold. One residual comment inaccuracy
remains, of the same class as defect 4 but at a site the first review did not
enumerate; it is a two-sentence correction with no design content, so it is
**reported for filing** rather than bounced on — a second bounce would escalate
the next implementation pass to Opus (CLAUDE.md §3) for a comment edit, which is
the wrong trade under that section's own cost rule.

### Defect 1 — the decisive one. Independently re-mutated, and race-free

`TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch` is a real pin, not
a restatement.

**Mutation re-run by this reviewer**, not taken on trust: a fresh
`git worktree add … --detach` at `95ec252`, its own scratch database
(`opencircuit_test_0297dmut`, created and dropped by `scripts/testdb.sh`), and
`intakePass`'s `SelectDue` + per-id `ClaimRow` loop rewritten to a single
whole-batch `h.intake.ClaimDue(claimCtx, intakeBatchSize, …)` with a plain range
over the returned rows — the exact pre-`#0297` shape, nothing else touched.
Result:

```
--- FAIL: TestSubscribeIntakeWorker_ClaimsRowsIndividuallyNotAsBatch (10.02s)
    subscribe_intake_test.go:435: status while blocked = (id1="sending", id2="sending"),
        want exactly one "sending" … and one "queued"
```

It fails on the substantive assertion — both rows already `'sending'` before
either row's processing began — not on a timeout and not on a blocking-seam
artifact. The main tree was never touched: `git status --porcelain` was empty
before and after, and `internal/handlers/subscribe_intake.go` hashed
`fd2540f2…` unchanged.

**Race-freedom, checked three ways** — this mattered because `sync.Once` plus a
real background poller is a concurrency construct:

- *By construction.* `intakePass` is a strictly serial `for _, id := range ids`
  loop and `processIntakeRow` is called synchronously inside it, so the first
  row's blocked `IsSuppressed` stalls the whole loop; `runIntakeWorker` is a
  single goroutine (`NewSubscribeHandler` starts exactly one), and
  `subscribeMux`'s `t.Cleanup` calls `h.Close`, so no earlier test's poller is
  still alive to claim these rows. `close(started)` happens *before*
  `<-release` inside the `once.Do`, so when the test wakes the poller is
  provably parked and cannot advance until the test closes `release`. There is
  no scheduling race to win.
- *Race detector.* `go test ./internal/handlers/ -race -run
  'TestSubscribeIntakeWorker_'` — clean, and the new test runs in 0.06s, far
  inside its `intakePollInterval*2 + 5s` bound.
- *Order-independence.* `-shuffle=on`, `-shuffle=12345`, `-shuffle=777` on
  `internal/handlers`, plus `-shuffle=on` on `internal/mailing` and
  `internal/outbox` — all `ok`. No `-count=N` was used (CLAUDE.md §5a).

One benign consequence of the seam, noted not as a defect: if the pair assertion
fails, `release` is never closed, so the parked poller goroutine outlives the
test and `subscribeMux`'s cleanup reports `h.Close: context deadline exceeded`
alongside the real failure. That is failure-path noise on an already-failing
test, visible in the mutation output above.

The one theoretical hole in the *mutation* direction (not in the passing
direction): if `SelectDue`/`ClaimDue` were to land in the sub-millisecond window
between the two `Enqueue` calls, the batch would contain one row and the
mutation would pass. Against a 5s poll interval that is negligible, and it
cannot make the test fail spuriously in the correct tree.

### Defect 2 — the three rewordings are true, and the real "detached" sites survive

Checked every occurrence of the word in both files rather than only the three
named. `outbox_worker.go:123` and `:551` and `subscribe_intake.go:126` now say
*bounded … not detached*, and each names the reason (a claim that cannot
complete before shutdown should not be taken at all). Verified against the code:
both claim contexts are `context.WithTimeout(ctx, …)` / `(h.sendCtx, …)`.

The surviving "detached" uses are all genuine `context.Background()`:
`outbox_worker.go:115/116/585` describe `sendOne`'s `sendCtx`, `writeCtx`,
`eventCtx` and `finishFailed`'s `writeCtx` — each read and confirmed as
`context.WithTimeout(context.Background(), …)`. `#0302`'s writes in `worker.go`
are untouched by this commit and their descriptions still stand.

The unnamed fifth site the implementer fixed on its own initiative —
`OutboxWorker.claimed`'s field comment — was a correct catch: leaving it would
have re-created the same contradiction with `Stop`'s rewritten contract.

### Defect 3 — the 30s derivation, checked under §5

Re-derived from the code, not from the doc comment. After `ClaimRow`'s `UPDATE`
commits server-side at T:

1. the client learns the outcome within the remaining budget of
   `claimCtx = context.WithTimeout(h.sendCtx, intakeRowTimeout)` → **< 10s** of
   real claim age before processing even starts;
2. `processIntakeRow` bounds unmarshal, `IsSuppressed`, `FindByEmail`,
   `dispatchMutation` (synchronous — it does not enqueue) and the terminal
   `MarkSent` inside one further `context.WithTimeout(h.sendCtx,
   intakeRowTimeout)` → **≤ 10s**, and `MarkSent` moves the row out of
   `'sending'`.

Worst case < 20s; window `3 * intakeRowTimeout` = 30s, i.e. the derived worst
case plus one further `intakeRowTimeout` of margin. Every term is
`intakeRowTimeout`, a package constant; `intakeBatchSize` is not a term.
**Nothing here is sized against anything observed** — no measured latency, no
load average, no wall-clock figure appears in the derivation. The posture now
matches `outboxOrphanStaleAfter` (70s over a 40s worst case) and
`orphanStaleAfter` (70s over a 35s worst case): one extra processing term of
slack. Raising the window is also strictly conservative for the sweep, which is
kind-filtered to `KindSubscribeIntake` regardless.

**The oracle is independent of the subject (`#0258`).** Confirmed by mutation in
a second throwaway worktree: setting `var intakeOrphanStaleAfter = 2 *
intakeRowTimeout` — the exact zero-margin value defect 3 bounced on — makes the
test fail:

```
intakeOrphanStaleAfter (20s) does not exceed the legitimate worst-case hold time
of a single live row (20s)
```

So `want` (`2 * intakeRowTimeout`) does not move when `got`'s margin term is
edited away. It is a different expression, not a copy of the answer.

### Defect 4 — three of the four are right; `trackClaimed`'s new claim is true

- **`Stop`'s contract** now says only that it stops `Run` and waits, and that
  `releaseAll` is a retained defensive net. True: `Stop` calls `releaseAll` only
  on the `<-w.doneCh` branch, i.e. after `Run` returned; on the `ctx.Done()`
  branch it returns `ctx.Err()` without releasing.
- **`trackClaimed`'s "`Stop` blocks on `doneCh`, which cannot close mid-send"** —
  verified true. `Run`'s `defer close(w.doneCh)` fires only after the `for` loop
  exits, `pass` is called synchronously, and in `pass` there is no `return`
  between `trackClaimed` and `untrackClaimed` (`sendOne` has no internal
  `stopCh` check and there is no `recover` in the file). So on every path that
  reaches `releaseAll`, `w.claimed` is already empty.
- **`subscribe_intake.go:50`** (`SelectDue`'s `WHERE next_attempt_at <= now()`)
  and **`:305`** (`ClaimRow` … having put it there) are both now accurate.

### Residual — report for filing, not a bounce

**`internal/mailing/outbox_worker.go:572-576`**, the inline comment at `pass`'s
`trackClaimed` call site, still reads:

> Recorded the instant the claim succeeds, before this row is sent, so a `Stop`
> landing anywhere in `sendOne` still sees it as outstanding — see
> `trackClaimed`'s doc comment.

That is the identical falsehood defect 4 required be removed from
`trackClaimed`'s own doc comment, 150 lines away in the same file — and the
sentence now points the reader at the doc comment that refutes it. `w.claimed`
does hold the id throughout `sendOne`, but `Stop` is not a reader that can
observe it there. The first review enumerated four sites and this was not one of
them; the implementer went beyond the four to fix a fifth, and missed this
sixth. The correct text is a restatement of what `trackClaimed`'s corrected doc
comment already says.

Second, smaller: **`internal/handlers/subscribe_intake_test.go:184-186`** still
says "`#0294` has since raised `intakeOrphanStaleAfter` to
`(intakeBatchSize+1)*intakeRowTimeout` = 210s and `#0284` raised
`outboxOrphanStaleAfter` to 800s". Both were superseded by this issue (30s and
70s). The paragraph does label its numbers historical and points at the live doc
comment, so it misleads less than the first, but the "has since" framing is now
wrong.

Neither affects behaviour, a bound, or a test.

### Also checked

- **Self-referential trap.** `grep -rn --include="*.go" -E
  '^[[:space:]]*(//|\*).*\.(ClaimDue|OrphanSweep|SelectDue)\(' .` → zero
  matches. The remaining `ClaimDue` mentions in comments are prose (`ClaimDue
  call`, `ClaimDue already does`) with no `.Name(` literal. `scripts/check.sh
  guards` passes with 0 failures and the claim-kinds guard's externally-measured
  populations are intact (42 total, 12 non-exempt, against floors 5 and 8).
- **gofmt stability (§8).** For each of `internal/mailing/outbox_worker.go`,
  `internal/handlers/subscribe_intake.go`,
  `internal/handlers/subscribe_intake_test.go` and
  `internal/handlers/subscribe_intake_orphan_stale_after_test.go`, the on-disk
  sha256 equals the sha256 of `gofmt`'s stdout. `gofmt -l ./internal ./cmd` is
  empty.
- **Siblings undisturbed.** `724728c` touches `internal/mailing/outbox_worker.go`
  only within `internal/mailing`; `worker.go` (`#0302`) and
  `internal/outbox/store.go` (`#0303`) are not in the diff. Neither
  `internal/subscribers/` nor `web/src/lib/` (another agent's `#0336`) is
  touched.

### Verification re-run by this reviewer

```
ISSUE=0297d scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/... ./cmd/opencircuit/...
```

`uptime` at start: load average 1.45 (idle — no timing result here is a load
artifact). `go build`, `go vet`, `gofmt -l` clean; `internal/outbox` ok (0.547s),
`internal/mailing` ok (3.744s), `internal/handlers` ok (8.770s),
`cmd/opencircuit` ok (6.787s); **zero skips**, no package at 0.00s.
`VERIFICATION PASSED`. `scripts/check.sh guards` — `VERIFICATION PASSED`, 0
failures. Named tests observed passing: the four AST citation/doc-comment
guards, `TestIntakeOrphanStaleAfterCoversSingleRowHold`,
`TestOutboxOrphanStaleAfterCoversSingleRowHold`,
`TestOrphanStaleAfterCoversLegitimateSingleRowHold`,
`TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`,
`TestOutboxWorker_ReleaseAll_ConcurrentCallsDoNotRace`, and all three
`TestSubscribeIntakeWorker_*`.

Both mutations ran in throwaway worktrees with their own scratch databases; both
worktrees and both databases were removed, `git worktree list` shows only the
main checkout, `git status --porcelain` in the main tree carries only another
agent's in-flight `#0336` files, and `pgrep -f 'go test|\.test '` is clean.

`internal/testdb`'s `TestDiagnoseLockHolders_IgnoresLocksInOtherDatabase` was
out of scope for this run and is tracked as `#0339`.
