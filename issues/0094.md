# 0094 — Svelte components have no test infrastructure, so conditional rendering is unverifiable

| | |
|---|---|
| **Status** | resolved |
| **Module** | web |
| **Platform** | All |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-19 |
| **Commit** | `ddbde2b` |
| **Closed** | 2026-08-23 |

## Description

No test in the repository imports a `.svelte` file. There is no jsdom or happy-dom, no
`@testing-library/svelte`, and no component harness. The house convention — logic in
plain TypeScript under `web/src/lib/`, components kept thin — is what makes this
tolerable, and it is a good convention. But it leaves the wiring *between* a tested
function and its template completely uncovered.

`#0090`'s review demonstrated the gap concretely. It deleted the entire `{#if}` wrapper
that suppresses the fake "Subscribe again" button for complained subscribers —
restoring the exact dead end `#0090` was filed to fix — and **all 152 tests stayed
green**. Only mutating `subscribe.ts` fails anything.

This is not held against `#0090`: that fix followed the house convention exactly, and
its pure-function half is properly mutation-proven. The exposure is project-wide.

## Acceptance criteria

- [x] Decide explicitly whether component tests are wanted, and record the decision —
      "no, and here is why the pure-function convention is sufficient" is a legitimate
      outcome and should be written down rather than left implicit
- [x] If yes: add a DOM environment and component-testing library, wired into the
      existing `npm test` run
- [ ] If yes: cover the conditional renders that gate user-visible affordances, with
      `#0090`'s `{#if}` wrapper as the first case — deleting the wrapper must fail a test
      — **substituted**: the dispatching orchestrator's brief for this implementation
      pass directed proving the harness on `#0120`'s Escape-closes-modal wiring instead
      (four issues this session — `#0120`, `#0197`, `#0216`, `#0195` — had explicitly
      left behavior "unverified by execution", making that the higher-value first case).
      `#0090`'s original wrapper is not yet covered by a mounted test; see Implementation
      notes' "newly-testable surface" for it as follow-up scope.
- [x] If yes: the performance budget in PRD §7.6 is unaffected (test-only dependencies —
      `@testing-library/svelte` and `jsdom` are `devDependencies`, not imported by any
      `web/dist` entry point)

## Implementation notes

**What existed vs. what was missing.** `web/` already ran Vitest and several tests
(`modalEscapeGuard.test.ts`, `CampaignSendDialog.structuralGuard.test.ts`,
`citationGuard.test.ts`) parsed `.svelte` source with `svelte/compiler`'s `parse()` —
so the gap was never "no test runner." It was that nothing imported a `.svelte` file as
a *component*: no DOM environment (no `jsdom`/`happy-dom` dependency at all), no
`@testing-library/svelte`, and `vite.config.ts` had no `test` block. Every existing
`.svelte`-adjacent test proves its claim either by walking the component's own AST or
by re-deriving its formulas side by side in plain TypeScript — never by mounting the
component and driving it.

**Harness chosen: `@testing-library/svelte@5.4.2` + `jsdom@30.0.1`**, scoped per test
file with a `// @vitest-environment jsdom` docblock rather than switching Vitest's
global `test.environment` — the other 25 existing test files stay on the fast default
(no environment = effectively synchronous), and only files that opt in pay jsdom's
setup cost.

Rejected Vitest browser mode: heavier install (a real browser binary + a
Playwright/WebDriver provider) and heavier CI weight, for no answer to the decisive
question that jsdom couldn't also give directly — see below.

One resolution wrinkle, not obvious in advance: Vite's default module resolution
handed `@testing-library/svelte`'s `render()` Svelte's **server** build even though the
test ran under `jsdom`, so the first attempt failed with `Svelte error:
lifecycle_function_unavailable — mount(...) is not available on the server` (`render()`
calls Svelte's client `mount()`). Fixed by adding `resolve: { conditions: ['browser'] }`
to `vite.config.ts`, gated on `process.env.VITEST` so `vite build`/`vite dev` are
untouched.

**Runes/`$effect` evidence — the decisive question.** `CampaignSendDialog.svelte`'s
`$effect(() => { void tick().then(() => dialogEl?.focus()); })` is the exact
`tick()`-then-`focus()` shape `#0120`/`#0047 §8` established and several deferred
verifications depend on. Under jsdom + `@testing-library/svelte`, `document
.activeElement` reliably became the dialog panel after `waitFor()`, across every test
in the new file. `$derived` also verifiably ran real component logic, not a stub: the
"does NOT close while sending" test only passes because `sending = $derived(guard.kind
=== 'sending')` actually evaluates `sendGuardState({ inFlight: true, ... })` — a stub
or no-op derived would have left the assertion vacuously true or false at random.

**Deferred verification proved.** `#0120`'s claim ("Escape closes the modal") was
proven on `CampaignSendDialog.svelte` — `#0120`'s own fix site where, instead of
rewriting the panel's `onkeydown` to call `isModalEscape` (the shape the other seven
sites use), the fix bound the backdrop's pre-existing `handleKeydown` to the panel too,
deliberately, so `#0197`'s AST guard keeps reading the same expression shape off the
backdrop unchanged. New file:
`web/src/views/admin/CampaignSendDialog.behavior.test.ts` — mounts the real component,
waits for the real `tick()`-then-`focus()` effect to move focus into the dialog panel,
dispatches a real bubbling DOM `keydown` event with `key: 'Escape'`, and asserts
`onClose` fires; a parallel case with `inFlight: true` asserts it does **not** fire
(the `#0195` sending-wins-over-Escape rule); a third asserts a non-Escape key is a
no-op.

**Mutation-proved, not just written and trusted.** Temporarily reintroduced the exact
pre-`#0120` regression — `onkeydown={(e) => e.stopPropagation()}` on the panel — and
reran the new test file: 2 of 5 tests failed (0 calls where 2 were expected). Reverted
immediately (`git diff` on `CampaignSendDialog.svelte` is clean).

**A genuine finding, only visible by mounting, not by inspection.** Because
`handleKeydown` is bound to *both* the backdrop's and the panel's `onkeydown` (same
function reference, and nothing in the file calls `stopPropagation` in the keydown
path — deliberate, per that component's own doc comment), a real Escape keypress on
the focused panel bubbles and fires `handleKeydown` **twice**, so `onClose()` is
called twice per Escape. This is real production behavior (confirmed: DOM `keydown`
bubbles by default, and `fireEvent.keyDown` dispatches with `bubbles: true`), not a
jsdom artifact. Harmless today — closing an already-closing dialog is idempotent from
`CampaignEditor.svelte`'s side. `#0120`'s own review had already reasoned this through
and recorded it as known-and-harmless, and `#0052` had browser-verified the same
double-fire shape earlier; what this harness adds is not the finding but the evidence
class — `expect(onClose).toHaveBeenCalledTimes(2)` is executed proof where `#0120` had
DOM-bubbling reasoning. That is worth recording precisely, since it would matter if
`onClose` ever grew a non-idempotent side effect (e.g. an audit-log write keyed on the
close action). **Worth a follow-up issue**; not fixed here since #0094 is test
infrastructure, not a behavior change, and the double-fire is currently harmless.

**What this harness does NOT establish.** `jsdom` is not a browser. Its focus, layout,
and `aria` semantics are approximations — real layout/visibility, IME composition, and
some focus-order edge cases are not modeled faithfully. A passing test here is weaker
evidence than a real-browser test (Vitest browser mode / Playwright) would be. It DOES
faithfully establish that Svelte 5 runes (`$state`/`$derived`/`$effect`) execute
correctly under it, that DOM event bubbling and `document.activeElement` tracking work
as modeled, and that the `tick()`-then-`focus()` pattern several deferred
verifications need is reproducible outside a real browser.

**Guards not removed.** `modalEscapeGuard.test.ts`, `modalKeydown.test.ts`,
`CampaignSendDialog.structuralGuard.test.ts`, `CampaignSendDialog.guard.test.ts`, and
`citationGuard.test.ts` are all unchanged. They catch a different class of drift (a
component's AST shape vs. what a test claims about it, repo-wide) that one
props-fixed behavioral test does not replace — e.g. a *different* future modal built
by copying old markup would trip the AST guard immediately but is not covered by this
one behavioral test until someone deliberately extends coverage to it.

**Newly-testable surface, left for follow-up issues:** `Admin.svelte`'s five modals,
`CampaignEditor.svelte`'s modal, `Campaigns.svelte`'s "New campaign" modal (note: its
`onMount` calls `listCampaigns()` over the network — mounting it under jsdom will need
either a mocked `fetch` or accepting the `loadError` catch path), `WorkshopEditor
.svelte` / `Workshops.svelte`'s pre-`#0120` modals, and `#0090`'s original `{#if}`
wrapper suppressing the "Subscribe again" button for complained subscribers — the
issue's own originally-named first case, still untested by a mounted component.

**Install and run-time cost.** Two new direct `devDependencies`
(`@testing-library/svelte@5.4.2`, `jsdom@30.0.1`), 53 packages added transitively,
~11.6MB added to `node_modules` (`jsdom` 8.3M + `@testing-library` 3.3M, measured
directly; `package-lock.json` diff is +722 lines, purely additive). All
`devDependencies` — no effect on `web/dist` or the production bundle; nothing here is
reachable from any app entry point, so PRD §7.6's performance budget is unaffected.
Run time: `npm test` went from 25 files / 3744 tests / 1.03s to 26 files / 3749 tests /
1.16s — about 130ms slower, of which Vitest's own reporting attributes ~270ms to
environment setup (jsdom only spins up for the one file that opts in via the
`// @vitest-environment jsdom` pragma; the other 25 files keep running under the fast
default). Small next to `#0207`'s ~48s `dev_guard_test.sh` benchmark, and it will grow
roughly linearly as more component tests are added — worth watching once several more
land.

## Verification

- `ISSUE=0094 scripts/check.sh web` — **PASSED**. `npm run check`: 264 files, 0 errors,
  0 warnings. `npm test`: 26 test files, 3749 tests passed.
- Before this change: `npm test` — 25 test files, 3744 tests, 1.03s.
- After: `npm test` — 26 test files, 3749 tests (+5), 1.16s.
- Mutation-proof (not left in the tree): reintroduced the exact pre-`#0120`
  `onkeydown={(e) => e.stopPropagation()}` on `CampaignSendDialog.svelte`'s panel; 2 of
  the 5 new tests failed as expected (0 calls where 2 were asserted); reverted, and
  `git diff -- web/src/views/admin/CampaignSendDialog.svelte` is clean.
- `uptime` before the check.sh run: `load averages: 2.74 3.59 2.88` — normal, not
  saturated.

## Notes

Found during `#0090`'s review, by mutation rather than by inspection.

The cost is real and so is the gap; this issue exists to force the choice, not to
presume the answer. Worth weighing against how many user-visible affordances are
currently gated by an untested `{#if}` — that count is the actual input to the decision.

## Review notes

Phase-3 review, `ddbde2b`. **Pass.** `uptime` at start: `15:55 … load averages:
3.05 3.09 2.78` — idle, so nothing below is load-contaminated. Every claim was
re-derived by execution rather than read back, and every probe below was run in
a throwaway detached worktree (`git worktree add --detach`, `node_modules`
symlinked from the main checkout, removed afterwards) so the shared tree was
never mutated — a concurrent agent held uncommitted work in `internal/` for
`#0059` throughout.

**The runes claim holds, and it is stronger than the implementer stated.**
Wrote a throwaway probe component (`$state` + a two-level `$derived` chain +
an `$effect` with a cleanup return) and drove it under jsdom. Results, each
confirmed by breaking it and watching the probe fail:

| Probe | Result | Negative control |
|---|---|---|
| `$effect` runs on mount | yes | — |
| `$effect` **re-runs** on dependency change | yes | effect stopped reading `n` → *re-runs on dependency change* failed |
| `$effect` **cleanup runs on unmount** | **yes** | deleted the `return () => …` → *cleanup runs on UNMOUNT* failed (`['effect:run:7']` vs `['effect:run:7','effect:cleanup:7']`) |
| `$effect` cleanup runs before each re-run | yes | same mutation → failed |
| two-level `$derived` chain settles | yes | — |
| ...**with no `await` at all** | **yes** | a probe with zero awaits read `c=21` for `n=10` correctly |

The last row answers the question that was actually asked. `$derived` under
jsdom is **synchronous** — it settles during `render()`, so no assertion in the
new file depends on an incidental `await tick()`. The `waitFor()` calls are
needed for exactly one thing, and it is genuinely asynchronous: the component's
own `void tick().then(() => dialogEl?.focus())` microtask. Nothing here is
"passing by accident because something else awaited first."

`$effect` cleanup on unmount was the specific jsdom weak spot worth probing,
and it works. That matters beyond this issue: `afterEach(cleanup)` in the new
file is therefore real teardown, not decoration, so future component tests will
not leak effects into one another.

**Four mutations against the real component — the focus assertion is not
incidentally satisfied.**

| Mutation | Failed | Reading |
|---|---|---|
| A — `void tick().then(() => dialogEl?.focus())` → `void tick()` | **5 of 5** | `document.activeElement` stayed `<body>`. The focus assertion is driven by the real `$effect` and nothing else |
| B — pre-`#0120` `onkeydown={(e) => e.stopPropagation()}` on the panel | **2 of 5** | "expected vi.fn() to be called 2 times, got 0" and "…4 times, got 0". Exactly the implementer's figure |
| C — `sending = $derived(guard.kind === 'sending')` → `$derived(false)` | **1 of 5** | only the `#0195` sending test, "expected not to be called, been called 2 times". The `$derived` really evaluates `sendGuardState`; a stub does not pass |
| D — `bind:this={dialogEl}` deleted | **5 of 5** | — |

Mutation D is the point of the whole issue. `#0120`'s own review closed with:
"no proof the new `$effect`s move focus in practice (a `bind:this` typo would be
caught only by `svelte-check`, which is green)". That gap is now closed by
execution — a dropped `bind:this` fails five tests instead of passing silently.
The tree was restored from a backup copy after every mutation and
`git diff -- CampaignSendDialog.svelte` was confirmed empty each time.

**No flakiness.** Ran the new file three consecutive times: 5/5 passed each,
839ms / 839ms / 847ms. No ordering sensitivity, no timing sensitivity.

**The `resolve.conditions` gate holds — the production bundle is byte-identical.**
Built twice in the worktree against the *same* `node_modules`, so the only
variable was `vite.config.ts`: once at `ddbde2b` (with
`resolve: process.env.VITEST ? { conditions: ['browser'] } : undefined`) and once
with `ddbde2b^`'s config substituted (no `resolve` key at all). `VITEST` was
confirmed unset at build time. All **20** files under `dist/` hash identically,
including the content-hashed entry points — `index-po6PP459.js`
(`9706bd2d…a90b3`) and `index-TVYljy6F.css` (`ddee1616…91fe4`) — which would be
the first thing to move if resolution had shifted. 202 modules transformed in
both. `#0141`'s class of regression is not present.

Worth recording for the next agent: **`web/dist/index.html` is a tracked file**,
so `npm run build` in the shared checkout dirties git state. Building in a
worktree was not just politeness.

**Which description of the shipped code is correct: `#0120`'s, not `#0094`'s.**
`CampaignSendDialog.svelte` binds `onkeydown={handleKeydown}` on **both** the
backdrop (line 146) and the panel (line 156); `bind:this={dialogEl}` and
`tabindex="-1"` are on the same panel node. Nothing was "deleted outright" —
that is the abandoned first attempt, the one that tripped
`a11y_click_events_have_key_events`. `#0094`'s `## Implementation notes` repeat
the stale claim because they trusted `modalEscapeGuard.test.ts`'s header.
Already tracked: `#0216` covers it and its acceptance criteria explicitly name
`issues/0094.md` as needing the same correction, and `6d2468a` recorded the
propagation. Not re-litigated here, and not a bounce — the code is right, the
prose about it is not, and the fix belongs to `#0216`.

**The two double-invocation accounts agree.** `#0120`'s review: "`closeX` is
invoked twice per Escape; harmless, because every close function is idempotent
… the same double-invocation `#0052` already had browser-verified."  `#0094`:
same mechanism (panel handler, then the unstopped event bubbles to the
backdrop's identical handler), same verdict (harmless because `onClose` is
idempotent), same caveat (it would matter if `onClose` grew a non-idempotent
side effect). No contradiction on mechanism, count, or severity.

What is genuinely new is the **evidence class**, not the finding:
`expect(onClose).toHaveBeenCalledTimes(2)` is executed proof where `#0120` had
reasoning about DOM bubbling. That is a real upgrade and the reason this issue
was worth doing. But `## Implementation notes` calls it "a genuinely new fact
this issue's harness surfaced" and "previously unverified by anything, AST or
otherwise" — that overstates it by one notch, since `#0120`'s review had already
recorded it as known-and-harmless and `#0052` had browser-verified the same
double-fire. Reported, **not filed** (`CLAUDE.md` §9); it is a wording
correction of the same family as `#0216`'s three, and belongs with them.

**The documented limits are honest, and they are recorded where a reader will
meet them.** Not only in a report: the caveat lives in `## What this harness
does NOT establish` in this file **and** in the new test file's own header
comment ("It does NOT establish that a real browser's focus/tab order,
CSS-driven visibility, or screen-reader announcement behave the same way").
Both name real jsdom weaknesses rather than hedging vaguely, and the split
between what it *does* establish (runes execute, bubbling works,
`activeElement` tracks, `tick()`-then-`focus()` reproduces) and what it does
not is accurate against everything I measured. Given that four issues deferred
verification to this harness, a reader arriving at either file learns its
ceiling before trusting it. That is the right outcome.

Two blemishes on that record, reported for a follow-up, **not filed**:

1. The test file's header cites **`web/HANDOFF`** — no such file exists
   (`web/` contains no `.md` at all). Dangling.
2. The same header reads "see this repo's `CLAUDE.md` §… discussion" — a
   literal unfilled ellipsis where a section number belongs. There is no
   `CLAUDE.md` section on jsdom's limits; the nearest relevant text is §5's
   verification rules.

Both are the exact defect class `#0216` was filed for, in the commit that
`#0216`'s own description points at — and this file is meant to be the
trustworthy evidence base for four deferred verifications, so its citations
should resolve. Neither overstates what is *proved* (unlike `#0216`'s item 1),
so neither is a bounce. Natural home: fold into `#0216`, which is already open
and already scoped to `#0094`.

A third, larger gap, also reported rather than filed: **nothing outside this
issue records that a component harness now exists.** `CLAUDE.md` §1 still says
only "SPA logic goes in plain TypeScript modules under `web/src/lib/` so it is
unit-testable without a DOM; Svelte components stay thin", and `docs/` has no
entry for it (`grep` finds `jsdom`/`testing-library` nowhere but
`docs/obstacles.md`). The convention is not wrong, but it is now incomplete: a
future agent reading `CLAUDE.md` would not learn that mounting a component is
available, what the `// @vitest-environment jsdom` pragma is for, or why
`vite.config.ts` has a `VITEST`-gated `resolve` block. Worth a short
`docs/` note or a `CLAUDE.md` §1 sentence.

**Acceptance criteria.** Ticked 1, 2, and 4 stand — verified. Criterion 3 is
**correctly left unticked**: `#0090`'s `{#if}` wrapper is genuinely not covered
by a mounted test, and the substitution note explains why `#0120`'s Escape
wiring was proven instead. Leaving the box unticked while explaining the
substitution is the honest record, and it keeps the original first case visible
as follow-up scope rather than quietly absorbing it.

**Measured deltas, re-derived — the implementer's figures are right.** Full
suite in a detached worktree at `ddbde2b^` (pristine, same `node_modules`):
**25 files / 3744 passed / 1.01s**, `environment 1ms`. Main tree at `ddbde2b`:
**26 files / 3749 passed / 1.15s**, `environment 270ms`. Delta **+1 file, +5
tests, ~140ms**, of which the 269ms `environment` jump is jsdom spinning up for
the single opt-in file — the per-file pragma is doing its job, and the other 25
suites still report `environment 1ms`.

**AST guards: untouched and green.** `git diff ddbde2b^ ddbde2b` is **empty**
for all five (`modalEscapeGuard.test.ts`, `modalKeydown.test.ts`,
`CampaignSendDialog.structuralGuard.test.ts`, `CampaignSendDialog.guard.test.ts`,
`citationGuard.test.ts`). Run together on the current tree: **5 files / 109
passed**. The commit touches only `web/package.json`, `package-lock.json`,
`vite.config.ts`, and the new test file — 892 insertions, **zero deletions**,
so nothing was traded away for the new coverage.

**`scripts/check.sh web`: VERIFICATION PASSED** — `npm run check` 264 files, 0
errors, **0 warnings**; `npm test` 26 files, **3749 passed**, zero skips; "(none)"
for leftover processes. Confirmed independently afterwards: no stray
`vitest`/`go test` processes, both worktrees removed, `git status` on the main
tree clean, `web/dist/` untouched.

## Relation

- Depends on: [#0090](0090.md)

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 100 | 7,514 | 4,236,359 | 117,432 | $1.82 |
| 2026-08-23 | review | claude-opus-5 | 46 | 526 | 1,562,451 | 78,032 | $1.28 |

**Total: $3.10**
