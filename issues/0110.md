# 0110 — `types.ts` still documents `soft_bounce_count` as a count of Transient-bounce rows

| | |
|---|---|
| **Status** | resolved |
| **Module** | web |
| **Platform** | All |
| **Phase** | 4 — Unsubscribe and list hygiene |
| **First seen** | 2026-08-20 |
| **Commit** | `896e61c` |
| **Closed** | 2026-08-20 |

> 🟢 Resolved 2026-08-20 — the new comment was checked claim-by-claim against
> the code rather than against the text it replaced, and every claim holds.
> `CountRecentSoftBounces`' SQL (`internal/sesnotify/store.go:172–179`) reads
> `bounce_type IN ('Transient', 'Undetermined')` and
> `bounce_subtype IS NULL OR bounce_subtype NOT IN ('MessageTooLarge',
> 'ContentRejected', 'AttachmentRejected')` — so "Transient- and
> Undetermined-bounce rows, excluding the sender-fault Transient subtypes" is
> exact, and the exclusion list matches the handler's counted arm
> (`ses_notifications.go:456`) verbatim. "Currently configured window" is true:
> `admin_subscribers.go:424–425` resolves the window through
> `softBounceThreshold(ctx, h.settings, nil)` and passes
> `h.now().Add(-window)` as `since`. The inherited "present together or absent
> together" clause was verified for the first time rather than taken on trust —
> all three fields are set in one `if h.bounces != nil && h.settings != nil`
> block whose only other exit is an error return, and all three are `*int` with
> `omitempty`, which omits nil but keeps a zero count, so a `0` count still
> renders as "0 of N" rather than vanishing. Terminology matches
> `admin.ts`'s `softBounceSummary` ("soft bounce"), closing the drift.
> `npm run check` 198 files / 0 errors and `npm test` 177 passed, both
> unchanged. Scope is exactly `web/src/lib/types.ts`, comment-only.
>
> One correction to the Notes below, applied in this commit: types.ts was
> **not** the only remaining stale description. The Go-side declaration of the
> same three fields (`internal/handlers/admin_subscribers.go:190–197`) still
> carries the identical pre-`#0109` sentence, and `soft_bounce.go:3` still
> says "a Transient bounce". Out of this issue's scope (`Module: web`, and
> every acceptance criterion names `types.ts`), so `#0110` is approved on its
> own terms; filed as [#0111](0111.md).

## Description

`web/src/lib/types.ts`'s doc comment on the `Subscriber` interface describes
`soft_bounce_count` as "the count of **Transient-bounce** `email_events` rows
within the currently configured window". After `#0109` (`8f900fe`) that is
wrong in both directions: the count also includes `Undetermined` bounces, and
it excludes the sender-fault `Transient` subtypes (`MessageTooLarge`,
`ContentRejected`, `AttachmentRejected`).

Documentation only — no behaviour is affected, and **this does not block Phase
4's deployable gate.** Filed because it is the same stale-label problem
`#0109` was itself filed to correct, one file over from the one `#0109` fixed.

## Root cause

`#0109` deliberately reworded the *user-facing* copy in
`web/src/lib/admin.ts` (`softBounceSummary`: "N transient bounce(s)" → "N soft
bounce(s)") and added a comment there explaining why, but did not sweep the
sibling type declaration that documents the same three fields. `types.ts` was
not in `#0109`'s touched-file set.

Current text, `web/src/lib/types.ts` lines 95–100:

```
 * `soft_bounce_count`/`soft_bounce_threshold`/`soft_bounce_window_days`
 * (#0039) are likewise populated by the detail endpoint only: the count of
 * Transient-bounce `email_events` rows within the currently configured
 * window, plus the threshold/window themselves so the screen can render
 * "N of THRESHOLD in the last WINDOW days" rather than a bare number. All
 * three are present together or absent together.
```

## Acceptance criteria

- [x] `types.ts`'s description of `soft_bounce_count` matches what
      `Store.CountRecentSoftBounces` actually counts: `Transient` **and**
      `Undetermined` bounces, excluding the sender-fault `Transient` subtypes
- [x] The `(#0039)` attribution picks up `#0109` as the issue that widened it,
      matching the convention `admin.ts` and `store.go` already follow
- [x] `npm run check` and `npm test` still clean (comment-only change, so no
      assertion should move)

## Fix

Reworded the `Subscriber` doc comment in `web/src/lib/types.ts` (lines
95–101) to match what `Store.CountRecentSoftBounces` actually counts and the
terminology `admin.ts`'s `softBounceSummary` comment already settled on:

Before:

```
 * `soft_bounce_count`/`soft_bounce_threshold`/`soft_bounce_window_days`
 * (#0039) are likewise populated by the detail endpoint only: the count of
 * Transient-bounce `email_events` rows within the currently configured
 * window, plus the threshold/window themselves so the screen can render
 * "N of THRESHOLD in the last WINDOW days" rather than a bare number. All
 * three are present together or absent together.
```

After:

```
 * `soft_bounce_count`/`soft_bounce_threshold`/`soft_bounce_window_days`
 * (#0039, widened by #0109) are likewise populated by the detail endpoint
 * only: the count of Transient- and Undetermined-bounce `email_events` rows
 * within the currently configured window, excluding the sender-fault
 * Transient subtypes (MessageTooLarge, ContentRejected, AttachmentRejected),
 * plus the threshold/window themselves so the screen can render "N of
 * THRESHOLD in the last WINDOW days" rather than a bare number. All three
 * are present together or absent together.
```

No code paths changed — this is a doc-comment-only edit.

## Verification

`uptime` before starting: load averages 1.58 1.73 1.70 — machine idle,
nothing to account for.

Comment-only change touching only the SPA, so only the frontend checks apply
(no Go suite, no database needed — stated explicitly rather than skipped
silently):

```
npm run check 2>&1 | tail -20   # 198 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
npm test      2>&1 | tail -20   # Test Files 9 passed (9); Tests 177 passed (177)
```

Both clean and unchanged from baseline, as expected for a comment edit. Did
not run `npm run build` (would regenerate the checked-in placeholder
`web/dist/index.html`).

## Files changed

- `web/src/lib/types.ts` — reworded the `soft_bounce_count` doc comment;
  no interface shape or behavior change.

## Notes

This is a **one-line comment fix** and does not warrant its own
plan → implement → review dispatch. Batch it into the next issue that touches
`web/src/lib/` or the soft-bounce subsystem.

`#0109`'s review recorded this as the *only* remaining stale description of
the widened count. **That was wrong, and review of this issue corrected it:**
the sweep covered `web/` and `docs/` but not `internal/handlers/`, where
`admin_subscribers.go:190–197` documents the same three fields with the same
pre-`#0109` sentence. Filed as [#0111](0111.md). Accurate as surveyed:
`web/src/lib/admin.ts`, `docs/unsubscribe.md` (which quotes PRD §6.5's "5 soft
bounces" correctly), and `docs/database.md`'s `000014`/`000016` rows.
`migrations/000014`'s `-- #0039 needs this` column comment on
`bounce_subtype` is left alone deliberately — migrations are append-only
(`CLAUDE.md` §1), and `#0109` made that comment substantively true by giving
the soft-bounce count a real read of `bounce_subtype`.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §6.5 — Unsubscribe — three paths, all required · 93 lines
  `sed -n '/^### 6\.5 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Follows: [#0109](0109.md) — An address that only ever produces `Undetermined` bounces is never suppressed
- Follows: [#0039](0039.md) — Suppress addresses after repeated soft bounces
- Followed by: [#0111](0111.md) — `admin_subscribers.go` still documents the soft-bounce fields as a count of Transient-bounce rows

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-08-20 | implement | claude-sonnet-5 | 56,257 | — |
| 2026-08-20 | review | claude-opus-5 | 63,191 | — |

No bounce. Review found the same stale sentence in the authoritative Go source
and filed #0111. Harness reports aggregate subagent totals only.
