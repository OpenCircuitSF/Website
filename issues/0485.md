# 0485 — `npm run check` type-checks nothing in `web/vite.config.ts`, and that file now carries logic

| | |
|---|---|
| **Status** | open |
| **Platform** | Web |
| **Module** | tooling / build |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-09-09 |
| **Commit** | ea8daca |

## Description

`web/tsconfig.json`'s `include` is exactly:

	"include": ["src/**/*.ts", "src/**/*.svelte"]

`web/vite.config.ts` is not under `src/`, so it is outside that glob and no
other tsconfig picks it up. **`npm run check` type-checks nothing in it.**

That was harmless while the file was declarative. It is not declarative any
more: `#0408` added `spaNavigationBypass`, a predicate with a `GET`/`HEAD`
gate, a path allowlist, query-string stripping, and header inspection, whose
correctness decides whether a browser navigation reaches Vite or Go. A silent
mistake there produces a **blank page** — the exact symptom `#0408` existed to
fix, and one that reads as a broken build rather than a type error.

## Root cause

`web/tsconfig.json`'s `include` names only `src/**/*.ts` and `src/**/*.svelte`,
and `npm run check` (`svelte-check --tsconfig ./tsconfig.json`) only resolves
files inside a tsconfig's own `include`. `web/vite.config.ts` sits at the
project root, outside that glob, so it was never a candidate for
`svelte-check` to look at regardless of what it contained.

## Fix

Widening `tsconfig.json`'s own `include` to reach `vite.config.ts` was
rejected rather than attempted: the file calls `process.env` and imports
`IncomingMessage` from `node:http`, and neither resolves without
`@types/node`. A quick `tsc --noEmit` against the file with no config changes
confirmed this directly — it reported `Cannot find module 'node:http'` and
`Cannot find name 'process'` before any fix landed. Adding `@types/node` to
`tsconfig.json`'s `types` array to fix that would hand every file under
`src/**` the ambient Node globals (`process`, `Buffer`, the `NodeJS`
namespace) that `@types/node` installs, even though everything under `src/`
runs in the browser and none of those globals exist there — exactly the
"different lib/types requirement" this issue's first criterion warned a naive
widening could cause.

Instead, added `web/tsconfig.node.json` — a second, narrow tsconfig scoped to
`vite.config.ts` alone, with its own `types: ["node"]` and no `extends` from
`@tsconfig/svelte`, since the file doesn't touch Svelte. This is the shape
Vite's own scaffolded templates use to separate build-time config from
application source. Added `@types/node` as a `devDependency` so the new
config's `node:http` and `process` references resolve, and changed the
`check` script to `tsc -p tsconfig.node.json --noEmit && svelte-check
--tsconfig ./tsconfig.json` — the new pass runs first and is cheap, and
`tsconfig.json`'s own `types` array (`["svelte", "vite/client"]`) is
unchanged, so `@types/node`'s globals never become visible to anything under
`src/`.

Proved the file is now actually checked, per criterion 2: recorded its
SHA-256, changed the tail of `spaNavigationBypass` from `return undefined;`
to `return 42;` — a value that violates its declared `string | undefined`
return type — and re-ran `npm run check`. It failed at exit code 2, naming
`vite.config.ts(64,3)` and `Type 'number' is not assignable to type
'string'.`, with `svelte-check` never reached because the new `tsc` step
short-circuited the `&&` first. Reverted the one-line change and re-hashed the
file: the SHA-256 matched the pre-edit value exactly
(`c83949fc...c98ee3e`), and `git status --porcelain -- web/vite.config.ts`
showed no diff at all afterward.

Confirmed criterion 3 by running `npm run check` again after the fix: the
`svelte-check` pass still reports the same 293 files, 0 errors, 0 warnings it
reported before this change, since `tsconfig.json`'s `include` was never
touched. No pre-existing errors surfaced anywhere, because nothing new was
added to that config's scope — the whole point of the second-tsconfig design
was to avoid widening it at all, so there was no glob expansion left to
surface a defect in some other unchecked file.

**Siblings (criterion 4), left unfixed:** `web/svelte.config.js` is the other
root-level config file outside any tsconfig's `include`. It carries a single
call to `vitePreprocess()` with no arguments and no branching logic — nothing
like `spaNavigationBypass`'s predicate — so leaving it unchecked does not
carry the same risk this issue was filed over. It is also a plain `.js` file
rather than `.ts`, so bringing it under type checking would additionally mean
deciding whether to rename it or turn on `allowJs`/`checkJs` for a second
config, which is more than this pass's scope justifies for a file with no
logic to get wrong. There is no separate Vitest config file to check either —
this project has no `vitest.config.ts`; Vitest reads its settings out of
`vite.config.ts` itself, the same file this issue already brings under
checking. `web/embed.go` is the one other root-level file under `web/`, but
it is Go source checked by `go vet`/`gofmt`, not by `npm run check`, so it was
never a candidate here.

## It has now mattered twice

- Flagged once on the abandoned worktree branch during `#0408`'s original
  review there, and left untracked.
- Flagged again by `#0408`'s review on `main`, which verified the gap itself
  rather than repeating the earlier claim.

Both times it was noticed while reviewing changes *to that very file*. That is
the tell: the only thing catching mistakes in it is human attention, applied at
exactly the moments when the file is being changed and least stable.

## What makes it worse than an ordinary uncovered file

`#0408`'s predicate is exercised only by `vite dev`. It is absent from the
production build — the built SPA is served by Go from one origin with no proxy —
so **no test in the suite runs this code**, and `vite build` output was
measured byte-identical before and after the change. Type-checking is the only
static signal available, and it is switched off.

## Acceptance criteria

1. **Bring `web/vite.config.ts` under type checking.** Options include adding it
   to `include`, adding a second tsconfig for build-time files, or a
   `tsconfig.node.json` of the shape Vite's own templates use. Pick one and say
   why.
2. **Prove it is actually checked afterwards.** Introduce a deliberate type
   error in that file, confirm `npm run check` fails and names it, then remove
   it and confirm the file is byte-identical (SHA-256 before and after). A check
   never seen to fail is not a check (`CLAUDE.md` §8).
3. **Do not break the existing check.** `npm run check` currently reports 293
   files with 0 errors and 0 warnings; whatever you change must leave it clean.
   If widening the glob surfaces pre-existing errors in other unchecked files,
   report them rather than silencing them — and say whether fixing them belongs
   here or in a separate issue.
4. **Check for siblings.** Other root-level config files (`svelte.config.js`,
   `vitest` setup, any script under `web/`) may be in the same position. Name
   them; do not necessarily fix them in this pass.
5. **Do not run `npm run build`** (`CLAUDE.md` §8b) — it overwrites the tracked
   `web/dist/index.html` placeholder, which three agents have already hit. If
   you must, restore with `git show HEAD:web/dist/index.html > web/dist/index.html`,
   never `git checkout` (§8a).

## Verification

Ran with `uptime` showing a load average of 3.35/3.02/3.06 before starting
and 2.78/2.83/2.96 during the full run below, both well under the ~8
threshold.

- **The deliberate-error proof (criterion 2)**, run directly: `npm run check`
  against the mutated file exited 2 and printed exactly one line —
  `vite.config.ts(64,3): error TS2322: Type 'number' is not assignable to
  type 'string'.` — with `svelte-check` never invoked, confirmed by its own
  `START`/`COMPLETED` banner being absent from that run's output. SHA-256 of
  `web/vite.config.ts` was `c83949fc...c98ee3e` before the mutation and the
  same value again after reverting it.
- **`scripts/check.sh web`** — `npm run check` reported `293 FILES 0 ERRORS 0
  WARNINGS 0 FILES_WITH_PROBLEMS`, unchanged from the count this issue's
  description cites. `npm test` reported `Test Files 42 passed (42)` and
  `Tests 4035 passed (4035)`. The script's own self-checks and its final
  `VERIFICATION PASSED` line both passed.
- `git status --porcelain -- web/dist/` printed nothing before, during, and
  after this pass — `npm run build` was never run, per criterion 5.
- `git status --porcelain -- web/vite.config.ts` printed nothing after the
  revert, confirming the file the mutation targeted carries no diff in the
  landed commit.

`ISSUE=0485 scripts/check.sh go ./internal/db/...` is recorded separately
below, re-run against this section per the current phase-2 step 8.

## Files changed

- `web/tsconfig.node.json` — new file; a narrow tsconfig scoped to
  `vite.config.ts` alone, with `types: ["node"]` and no dependency on
  `@tsconfig/svelte`.
- `web/package.json` — added `@types/node` as a `devDependency`, and changed
  the `check` script to run `tsc -p tsconfig.node.json --noEmit` before
  `svelte-check`.
- `web/package-lock.json` — lockfile update from the `@types/node` install.

## Gotchas

- **`npm install --save-dev @types/node` with no version pin did not install
  the newest published version.** The registry's `latest` dist-tag for
  `@types/node` resolves to `22.20.2`, well behind the `26.x` versions that
  also exist — DefinitelyTyped tags `latest` by TypeScript-version
  compatibility, not by recency. `npm view @types/node dist-tags` is what
  showed this, and `22.20.2` matches this project's documented minimum
  runtime (`docs/deployment.md`'s `node --version # v20.x or newer`) more
  comfortably than a version tied to whatever Node happens to be on this
  laptop, so the default resolution was kept rather than pinned to something
  newer.
- **Confirming the widening question needed a throwaway `tsc` run before
  touching any config.** Running `npx tsc --noEmit` directly against
  `vite.config.ts` with no project file, before writing
  `web/tsconfig.node.json`, is what surfaced the `node:http`/`process`
  resolution failures described in `## Fix` above — that measurement is what
  ruled out the plain `include`-widening option rather than the reasoning
  alone.

## Review notes — 2026-09-09 (phase 3, bounced)

The fix is the right design and every acceptance criterion was independently
re-verified below. It is bounced for one thing it left behind: installing
`@types/node` falsified three standing comments under `web/src/lib/` that
state, in the present tense, that this project carries no such dependency.

### What was verified and stands — do not redo this

- **The Node-globals leak claim is the crux, and it is real, measured in both
  directions.** A throwaway probe module declaring `process`, `Buffer`, and
  `NodeJS.Timeout` was compiled twice under otherwise identical options: under
  this project's actual `types: ["svelte", "vite/client"]` it failed with
  `TS2591` on both `process` and `Buffer` and `TS2503` on the `NodeJS`
  namespace, and under the same options with `"node"` appended to `types` it
  exited 0. The rejected one-line `include`-widening really would have handed
  every browser-only file the ambient Node globals, so the extra config file
  earns its place.
- **The separation holds after the change.** A throwaway module placed under
  `web/src/lib/` referencing `process` and `Buffer` still fails `npm run
  check` with the same two `Cannot find name` errors, reported at 294 files
  against the usual 293. Node's globals reach nothing under `src/`, and
  `web/tsconfig.json`'s own `types` array is byte-unchanged by this commit.
- **The deliberate-error proof reproduces exactly.** Changing the final
  `return undefined;` of `spaNavigationBypass` to `return 42;` made `npm run
  check` exit 2 printing the single line `vite.config.ts(64,3): error TS2322:
  Type 'number' is not assignable to type 'string'.`, with `svelte-check`'s
  `START`/`COMPLETED` banner absent because the `&&` short-circuited first.
  Reverting restored SHA-256 `c83949fc…c98ee3e`, the pre-mutation value, and
  `web/` reports no diff at all.
- **The `&&` ordering is right, deliberately.** The cheap `tsc` pass gates the
  expensive `svelte-check` pass, and the file it checks is the one that
  configures the build, so a broken build config is the correct thing to
  report before anything downstream of it. The cost is real and was seen in
  both directions during this review: while the probe module under `src/`
  was present, `tsc` passed and `svelte-check` still ran and reported both
  errors, so the short-circuit only ever hides Svelte errors behind a genuine
  config error, never the reverse.
- **The new devDependency ships nothing.** `@types/node` and its single
  transitive dependency `undici-types` both carry `"dev": true` in the lock
  file and contain no runtime JavaScript whatsoever, `web/package.json`'s
  `dependencies` object is empty, and no module under `src/` imports either.
  Nothing can reach the built bundle, so the self-contained-site rule in
  `CLAUDE.md` §9 is untouched.
- **The version is appropriate.** The resolved `22.20.2` is what
  DefinitelyTyped currently tags `latest`, and it type-checks cleanly against
  this repo's TypeScript 5.9.3, which is what the passing runs below
  demonstrate.
- **The lock file grew by exactly the types package and its own dependency.**
  The eighteen added lines are the root `devDependencies` entry, the
  `@types/node` package record, and the `undici-types` package record —
  nothing unrelated was swept in.
- **The sibling assessment is correct, including the negative claim.** There
  is no separate Vitest configuration file anywhere outside `node_modules`,
  no Vitest workspace file, and no test setup file; Vitest reads the
  `VITEST`-gated `resolve` block out of the same `web/vite.config.ts` this
  change now covers.
- **Criterion 5 was honoured.** `git status --porcelain -- web/dist/` printed
  nothing before, during, and after this review, so `npm run build` was never
  run by either pass.

### What must change

Three comments now assert the opposite of what this commit did. They are not
historical notes — each is written in the present tense about the current
dependency tree, and one of them offers a reproducible measurement that today
returns the opposite of what it reports.

1. **`web/src/lib/nodeFsShim.d.ts`** is the primary site and the one the other
   two forward readers to. Its doc comment says `this project deliberately
   does not depend on it`, reports that `` `npm ls --all` confirms @types/node
   is not installed here -- only present, unmet, as an OPTIONAL peer of
   vite/vitest ``, and frames the shim as keeping that precedent intact with
   `zero new devDependencies`. All three statements are now false: `npm ls
   @types/node` reports `@types/node@22.20.2` as a direct dependency with
   vite's and vitest's formerly unmet optional peers now satisfied and
   deduped against it. Rewrite the passage to say what is now true — that the
   package is installed as a devDependency for the build-time config's sake,
   that `web/tsconfig.json`'s `types` array still excludes it so nothing under
   `src/` sees Node's ambients, and that the shim therefore remains the way
   this file's `node:*` imports are typed rather than being made redundant.
2. **`web/src/lib/liveRegionGuard.structuralGuard.test.ts`** describes
   `node:path` as something `this project deliberately does without` and
   forwards the reader to the shim's comment `on why @types/node was
   removed`. The narrow claim that this file avoids importing `node:path` is
   still true and should stay; the project-wide claim wrapped around it is
   not, and the forwarding sentence must match whatever the shim's comment
   ends up saying.
3. **`web/src/lib/urlValidatorFixture.test.ts`** contains the parenthetical
   `this project carries no @types/node dependency, per web/tsconfig.json's
   types: ["svelte", "vite/client"]`. The reason it cites is still correct
   and the import style it justifies is unaffected; only the flat assertion
   of no dependency is wrong, so narrow it to what the `types` array actually
   guarantees — that this file's program cannot see Node's types — rather
   than deleting the explanation.

Two nearby comments were checked and are **fine as they stand**; leave them
alone. `web/src/lib/audience.test.ts` says the SPA's tsconfig deliberately
excludes `@types/node`, which remains exactly true of the `types` array. And
`web/src/lib/citationGuard.test.ts` describes its own earlier pass reverting
an `@types/node` dependency, which is accurate history rather than a claim
about today.

### Why this is a bounce rather than a follow-up issue

The staleness was created by the commit under review, so it is that commit's
own debt. It also points a future agent the wrong way: a comment stating
`zero new devDependencies` as a precedent to preserve is an instruction, and
acting on it by removing the package breaks the very `tsc` step this issue
added. The remedy above is three comment edits and one `scripts/check.sh web`
run, fully specified, so the next pass stays on Sonnet per `CLAUDE.md` §3.

The design decision itself should be **recorded, not reversed**. Reversing
that precedent is defensible here and this review endorses it: the earlier
pass avoided `@types/node` because `import.meta.glob` gave a browser-safe
alternative for a file under `src/`, and `web/vite.config.ts` has no such
alternative, since it genuinely runs in Node. What was missing is that
`## Fix` never acknowledged the precedent it was overturning. Say so there
too.

### Review verification

- `uptime` at the start of the review reported load averages 4.10, 3.33, 3.13,
  and 3.88, 3.18, 3.08 before the suite run — both under the ~8 threshold in
  `CLAUDE.md` §5.
- `scripts/check.sh web` — `npm run check` reported `293 FILES 0 ERRORS 0
  WARNINGS 0 FILES_WITH_PROBLEMS`, matching criterion 3's stated baseline
  exactly. `npm test` reported `Test Files 42 passed (42)` and `Tests 4035
  passed (4035)`. The script's own self-checks and its `VERIFICATION PASSED`
  line both passed.
- `ISSUE=0485r scripts/check.sh go ./internal/db/...` is recorded with this
  bounce, run against these notes.

## Relation

- `#0408` put real logic into the unchecked file; both its reviews flagged this.
- `CLAUDE.md` §1 records that SPA logic belongs in `web/src/lib/` so it is
  unit-testable — this file is the case where that is not possible, since the
  predicate only exists under `vite dev`.

Verify with `scripts/check.sh web`, plus
`ISSUE=NNNN scripts/check.sh go ./internal/db/...` for the issue text.
