# 0249 — The restore drill leaks its target database on every run

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | infra / db |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-24 |
| **Closed** | 2026-08-24 |
| **Commit** | `e2f6a8d` |

## Description

Step 9 of `docs/deployment.md`'s restore drill cannot drop the database it
created. Every run leaves one behind.

```
scripts/testdb.sh drop "${ISSUE}dst"
ERROR: must be owner of database
```

**Deterministic, not intermittent** — reproduced identically in both of
`#0248`'s runs and in `#0239`'s review before it. The cause: `restore.sh` with
`RESTORE_CREATE=1` under `BACKUP_RUN_AS=""` calls `createdb` as the **local OS
user**, so that user owns the database — while `testdb.sh drop` connects as
`opencircuit`, which does not.

The drill documents the failure, so a follower is not surprised. But
documenting a leak is not the same as not leaking:

- **Every drill run costs a manual cleanup** that the drill itself cannot
  perform, by a person who has to work out which role actually owns the thing.
- **`#0247` just namespaced the drill so it can be run concurrently.** Its
  reward for being run more often is more orphans, each under a different
  `$$`-derived name, and nothing sweeps them.
- `scripts/testdb.sh gc --all` will not help: `#0150` deliberately made it
  refuse databases with live connections and require an explicit flag, and it
  operates on the `opencircuit_test_*` prefix rather than the drill's names.

This is the same class as `#0223` — a cleanup path that reports success while
leaving a database behind — with the difference that here the failure is
visible and simply tolerated.

## Implementation notes

**Root cause confirmed to be exactly what the issue describes.** `restore.sh`'s
`RESTORE_CREATE=1` path called plain `createdb "$TARGET"`, so the *database
object itself* ended up owned by whichever role connected to run it — the
local OS user under `BACKUP_RUN_AS=""`, `postgres` in production. The
existing `reassign_ownership` step (`#0228`, `#0235`) only ever reassigns
what lives *inside* the database — its tables, sequences, views, schemas —
never the database object's own `pg_database.datdba`, which is exactly what
`DROP DATABASE` (and therefore `testdb.sh drop`, which always connects as
`opencircuit`) checks.

**Fix, per criterion 2's first option**: `restore.sh` now creates the
database already owned by `RESTORE_OWNER` when one is set —
`createdb -O "$RESTORE_OWNER" "$TARGET"` instead of plain `createdb`. This
works on both documented paths because `-O` to a role other than the
connecting one requires the connecting role to be a superuser (or a member
of the target role): `BACKUP_RUN_AS=postgres` in production is a superuser,
and on this dev machine the local OS role (`brennan`) is also a superuser
(`select rolsuper from pg_roles where rolname=current_user` → `t`) —
confirmed directly, not assumed. `RESTORE_OWNER=""` skips the `-O` flag
entirely and falls back to plain `createdb`, so the opt-out still opts out
of database ownership, matching its existing behavior for the objects
inside it (criterion 3).

Chose this over step 9 dropping as the actually-owning role (criterion 2's
second option) because that would have meant either hardcoding "the OS user
who ran the drill" into `testdb.sh drop` — which has no way to know that
role in the concurrent, multi-agent case — or adding a second drop path
just for this drill. Fixing `restore.sh` means every caller of
`RESTORE_CREATE=1`, not just this drill, gets a database `testdb.sh drop`
(or any other tool connecting as `opencircuit`) can actually drop.

**Interaction with `#0235` checked, not assumed.** `-O` only sets the
*database's* owner at creation time; it has no effect on the tables,
sequences, views, or the `public` schema itself that `pg_restore`/`psql`
and then `reassign_ownership` create and touch afterward — those are a
separate mechanism (`ALTER TABLE/SEQUENCE/VIEW/SCHEMA … OWNER TO`) working
on objects that don't exist yet when `createdb` runs. Ran the **full**
drill, not just step 9, to prove this isn't hand-waved: seeded a source
database, rolled it back one migration (step 1b), backed it up, restored it
with the fixed script, then ran a real `migrate -path migrations -database
"$DST_DSN" up` (step 6) as the `opencircuit` role. It applied migration 20
cleanly (`20/u create_workshops`) and a subsequent `select count(*) from
workshops` **connected as `opencircuit`** succeeded — the exact check
`#0235`'s own regression proof relies on. `#0235`'s fix is intact.

**Orphan census (criterion 5).** Checked before starting any work and again
right before finishing — both times the only databases on the machine were
the long-lived ones (`opencircuit`, `opencircuit_live`, `opencircuit_test`,
`opencircuit_test_template`, `shortlinks`, `shortlinks_test`), none matching
the drill's `opencircuit_test_*src`/`*dst` naming. **No orphans found to
enumerate.** Per the issue's own caution, no sweep was attempted regardless
— this was a read-only `select datname, pg_get_userbyid(datdba) from
pg_database`, nothing dropped that this session didn't create itself.

**`#0248`'s exit-status prose (criterion 6).** `docs/deployment.md`'s "no
`set -e`" paragraph named two legitimate non-zero exits in the drill's code
block — step 7's expected constraint-count `diff` and step 9's
now-fixed `testdb.sh drop`. Updated it in this commit: step 9 is no longer
one of them, so only step 7's remains. **`issues/0248.md` itself carries its
own copy of the same reasoning in its working tree, uncommitted at the time
of this pass** (last modified alongside `docs/deployment.md` commit
`3e2a4af`, apparently not yet folded into a markdown commit) — that copy is
now stale in the same way and needs the same correction, but editing another
in-flight file's uncommitted content was out of scope here per `CLAUDE.md`
§8a; flagged to the orchestrator instead of touched directly.

## Acceptance criteria

- [x] The drill drops its own target database, as part of the drill, with no manual step
- [x] The fix addresses the ownership mismatch at its cause — either `restore.sh` creates the database owned by `RESTORE_OWNER`, or step 9 drops as the role that actually owns it — rather than instructing the follower to sort it out
- [x] `RESTORE_OWNER=""`'s opt-out still works, and is not quietly broken by whatever creates the database
- [x] Proved by running the drill twice concurrently to completion and confirming **zero** leftover databases afterwards, by census — not by the absence of an error
- [x] Any orphans already accumulated from previous runs are enumerated so they can be cleared deliberately; do not sweep them automatically, since another agent may be mid-run
- [x] `#0248`'s prose about exit status not being the pass signal is updated if step 9 stops failing (in `docs/deployment.md`; `issues/0248.md`'s own uncommitted copy still needs it — see above)

## Verification

All against a real local PostgreSQL 16 cluster (Homebrew) — nothing here
rests on reasoning alone or on infrastructure not yet standing (`CLAUDE.md`
§10); every claim below was actually run.

**Mutation proof (old script leaks, fixed script doesn't).** Checked out the
pre-fix `restore.sh` to a scratch path (`git show HEAD:scripts/db/restore.sh`
before this commit), ran a real backup/restore with it:

```
=== census (OLD script) ===
opencircuit_test_0249mutdst|brennan
=== attempt testdb.sh drop (expect ERROR: must be owner of database) ===
ERROR:  must be owner of database opencircuit_test_0249mutdst
error: failed to drop opencircuit_test_0249mutdst (see the psql error above — commonly: you are not its owner)
exit=1
```

Same dump, same target name, restored with the fixed script:

```
=== census (FIXED script) ===
opencircuit_test_0249mutdst|opencircuit
=== testdb.sh drop ===
dropped opencircuit_test_0249mutdst
exit=0
=== census after drop ===
(expect empty)
```
(empty — matched.)

**`RESTORE_OWNER=""` opt-out (criterion 3).** Restored with
`RESTORE_OWNER=""` set: database created plain (no `-O`), and both the
database and its first table stayed owned by the restoring role (`brennan`),
exactly as before this fix:

```
=== census (RESTORE_OWNER='') ===
opencircuit_test_0249mutdst2|brennan
=== table ownership check (should remain as restoring role, opt-out) ===
brennan
```

**Full drill, single run, including `#0235`'s regression check (step 6).**
Ran every step — source seed, `migrate down 1`, backup, restore with the
fixed script, then a real `migrate -path migrations -database "$DST_DSN" up`
as `opencircuit`:

```
20/u create_workshops (13.114584ms)
exit=0
 version | dirty
---------+-------
      20 | f

=== step 8b: connect as opencircuit (app role) and query ===
 count
-------
     1        (users, pre-existing table)

 count
-------
     0        (workshops, migration 20's new table)
```

Both queries succeeded connected **as `opencircuit`**, confirming `#0235`'s
schema-ownership fix is unaffected. Step 9 then dropped both databases
cleanly with no manual step.

**The load-bearing proof (criterion 4): full drill, twice, concurrently.**
Two independent runs (`ISSUE=0249ca`, `ISSUE=0249cb`) launched as background
jobs and waited on:

```
run A exit=0  run B exit=0
=== A output ===
20/d create_workshops (3.789375ms)
INSERT 0 1
20/u create_workshops (4.805875ms)
dropped opencircuit_test_0249casrc
dropped opencircuit_test_0249cadst
RUN 0249ca DONE
=== B output ===
20/d create_workshops (3.659375ms)
INSERT 0 1
20/u create_workshops (4.789541ms)
dropped opencircuit_test_0249cbsrc
dropped opencircuit_test_0249cbdst
RUN 0249cb DONE
```

Census immediately after, by query rather than by absence of an error:

```
select datname, pg_get_userbyid(datdba) from pg_database
  where datname like 'opencircuit_test_0249c%';
(0 rows)
```

Full-machine census, before this pass and after both concurrent runs,
identical both times:

```
opencircuit|opencircuit
opencircuit_live|opencircuit
opencircuit_test|opencircuit
opencircuit_test_template|opencircuit
shortlinks|shortlinks
shortlinks_test|shortlinks
```

**Go suite**: `ISSUE=0249 scripts/check.sh go ./internal/...` — 15 packages
`ok`, zero skips, own database (`opencircuit_test_0249`), no leftover
processes, `VERIFICATION PASSED`. `bash -n scripts/db/restore.sh` and
`shellcheck scripts/db/restore.sh` both clean.

Every database created during this verification (`0249mut*`, `0249full*`,
`0249ca*`, `0249cb*`) was dropped by the end of the corresponding step; none
were left for the next agent, and `shortlinks`/`shortlinks_test` were never
touched.

## Notes

Found by `#0248` while running the current drill for real, and independently
seen by `#0239`'s review. Reported under `CLAUDE.md` §9.

**Check the interaction with `#0235` before changing who creates the database.**
That issue made `restore.sh` reassign ownership of every object *and* the
`public` schema to `RESTORE_OWNER` after restoring, precisely so the app role
can migrate afterwards. Whatever fixes database ownership must not undo that,
and the drill's step 6 — a real `migrate up` as the app role — is the test that
catches it if it does.

The fourth criterion is the real one. This defect has been observed at least
three times across three passes and tolerated each time because the error
message is honest about what happened.

## Relation

- Found by: [#0248](0248.md) — the drill-outcome corrections, during a real run
- Relates to: [#0247](0247.md) — the namespacing that makes the leak multiply
- Relates to: [#0235](0235.md) — the ownership reassignment this must not undo
- Relates to: [#0223](0223.md) — the same class: cleanup reporting success while leaking
- Relates to: [#0150](0150.md) — why `testdb.sh gc` will not sweep these

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

**Total: —** (token counts unavailable to this subagent; orchestrator to fill in from the transcript per `Issues.md`'s "Getting exact token counts")

## Review notes — 2026-08-24 (phase 3, RESOLVED)

> 🟢 Resolved 2026-08-24 — `createdb -O` fixes the leak at its root; I
> reproduced the leak against the pre-fix script, then proved zero leftovers by
> `pg_database` census across two concurrent full drills, and confirmed `#0235`
> and the `RESTORE_OWNER=""` opt-out both still behave.

**Status set to `resolved`.** `uptime` first: load average 1.34-2.06 — idle.
Every command below ran from a **throwaway `git worktree` pinned at `172f438`**
(removed afterwards), never the shared checkout — another agent held
`scripts/db-reset.sh` / `scripts/db_reset_guard_test.sh` dirty in the main tree
(`#0250`/`#0251`). `git cat-file -t e2f6a8d` → `commit`; the `**Commit**` row is
real. `shortlinks` was never targeted; the only database I did not create and
did not touch was another agent's `opencircuit_test_0212wt`.

### The leak, reproduced against the pre-fix script

`git show e2f6a8d^:scripts/db/restore.sh > /tmp/rev4-restore-prefix.sh` — a
**different path**, never `git checkout --` on the shared tree (`CLAUDE.md`
§8a). Ran a real drill through it (source at version 19 after `migrate down 1`,
seeded, backed up with the current `backup.sh`):

```
  creating database 'opencircuit_test_0248rvdstold'
  Ownership reassigned to 'opencircuit' (schema and objects).
--- census: opencircuit_test_0248rvdstold|brennan
--- scripts/testdb.sh drop 0248rvdstold
ERROR:  must be owner of database opencircuit_test_0248rvdstold
error: failed to drop opencircuit_test_0248rvdstold (…)
exit=1
```

Exactly the reported defect, and note the ownership reassignment *did* run —
which is the point: it fixes what is inside the database, never `datdba`.
Dropped that orphan by hand as its actual owner.

### The fix, on the same dump

```
  creating database 'opencircuit_test_0248rvdst' (owner 'opencircuit')
--- census: opencircuit_test_0248rvdst|opencircuit
--- step 9:  dropped opencircuit_test_0248rvsrc   src exit=0
             dropped opencircuit_test_0248rvdst   dst exit=0
```

### Criterion 4 — by census, not by absence of an error

Two independent full drills (`0249ra`, `0249rb`) launched as concurrent
background jobs and waited on; both exit 0, both step-9 drops exit 0. Full
`select datname, pg_get_userbyid(datdba) from pg_database` taken **before**
launching and **after** both finished, then `diff`ed:

```
opencircuit|opencircuit          opencircuit_test_template|opencircuit
opencircuit_live|opencircuit     postgres|brennan
opencircuit_test|opencircuit     shortlinks|shortlinks
                                 shortlinks_test|shortlinks
IDENTICAL — zero leaked databases
```

### Criterion 3 — the opt-out still opts out

`RESTORE_OWNER="" RESTORE_CREATE=1`: the script printed
`creating database 'opencircuit_test_0249optdst'` with **no** `(owner …)`
clause and `RESTORE_OWNER is empty — skipping post-restore ownership
reassignment (schema included).` Census: `opencircuit_test_0249optdst|brennan`;
`pg_tables.tableowner` for `subscribers` also `brennan`. `testdb.sh drop` then
failed with `must be owner of database`, exit 1 — which is exactly what
`docs/deployment.md`'s step 9 now documents as the one deliberate exception.
Dropped by hand as its owner.

### `#0235` is intact — the check that catches an ownership fix undoing the earlier one

Ran the **full** drill, not just step 9. Step 6's real
`migrate -path migrations -database "$DST_DSN" up` as the app role printed
`20/u create_workshops (16.3435ms)`, exit 0, `select version, dirty` → `20|f`.
Step 8 inserted into `workshops` (migration 20's brand-new table) → `id=1`.
Step 8b connected as the app role (`select current_user` → `opencircuit`) and
both `select count(*) from subscribers` (4) and `select count(*) from
workshops` (1) succeeded — no `permission denied for schema public`, no
`permission denied for table`. Repeated in both concurrent runs and in a
plain-format (`BACKUP_FORMAT=plain`) run. `-O` sets `datdba` at creation and
touches nothing `reassign_ownership` later does, exactly as the commit claims.

### Criteria 1, 2, 5, 6

1, 2 — step 9 now drops both databases inside the drill, with no manual step,
because the cause was fixed in `restore.sh` rather than worked around in
`testdb.sh`. Read the diff: it is the minimal change (`createdb -O
"$RESTORE_OWNER"` guarded by `[ -n "$RESTORE_OWNER" ]`), plus header docs.

5 — my own census before starting found no drill orphans to enumerate either
(only the long-lived databases plus another agent's `opencircuit_test_0212wt`,
which is not a drill artefact and which I left alone). Nothing was swept.

6 — `docs/deployment.md`'s `set -e` rationale now names **one** legitimate
non-zero exit (step 7's constraint `diff`) and explains that step 9's used to
be the second and no longer is. I confirmed both halves by running: step 7's
constraint diff exits 1 printing exactly `< email_campaigns|12` /
`> email_campaigns|13`, and step 9's two drops exit 0. `issues/0248.md`'s own
copy of that rationale is annotated to match (committed in `afcd8d8`) — the
two records agree, so the cross-check `#0249` flagged to the orchestrator is
closed.

### What I looked for and did not find

Whether `-O` could break the production path (it cannot in the shape that
matters here: `-O` to another role needs superuser or membership, and both
documented paths have it — `BACKUP_RUN_AS=postgres` is a superuser and this
machine's OS role reports `rolsuper=t`; **this rests on the local run plus the
documented production path, not on a real server** per `CLAUDE.md` §10);
whether the fix silently changed object-level ownership (it did not — the
`RESTORE_OWNER=""` run leaves table ownership exactly as before);
whether the plain format regressed (it did not — a full `BACKUP_FORMAT=plain`
drill produced identical step-7 results and clean drops); whether anything of
mine was left behind (nothing — final census identical to baseline, no
`/tmp/backup-drill-*` directories, `pgrep -f 'go test|\.test |vitest'` empty).
