# 0278 — SESMailer.SendVerification and SendRecovery are production-dead

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | auth / mailing |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |
| **Commit** | 58ad750 |
| **Closed** | 2026-08-27 |

## Resolution notes

> 🟢 Resolved 2026-08-27 — decision recorded in the code: the two methods
> are retained on `auth.Mailer`, with each implementation documenting that
> it has no production caller since `#0260`.

## Description

`#0260` routed registration and recovery mail through `outbound_queue` instead
of calling the mailer directly. `SESMailer.SendVerification` and
`SESMailer.SendRecovery` therefore have no production callers left. Only
`SendSessionsRevoked` is still live on that interface.

They remain on the `Mailer` interface, so every implementation must still
provide them and a reader cannot tell from the interface which methods are real.

Low priority — nothing is broken. It is a tidiness and legibility item, filed so
the dead surface is a recorded decision rather than something the next agent has
to re-derive.

## Acceptance criteria

1. Decide explicitly between narrowing the `Mailer` interface to what is live
   and retaining the methods, and record the decision in the code.
2. If narrowed: `SendVerification`/`SendRecovery` come off the interface and off
   every implementation that only had them to satisfy it. `SendSessionsRevoked`
   stays.
3. If retained: a doc comment on each says it has no production caller and why
   it is kept, so the next reader does not go looking for one.
4. Whichever is chosen, `internal/auth`'s no-op and test mailers stay consistent
   with it — do not leave an implementation carrying a method the interface no
   longer names.
5. Note the interaction with [#0277](0277.md), which may delete
   `auth.NoOpMailer`'s equivalents. Sequence the two rather than having both
   edit the same declarations.
6. `ISSUE=NNNN scripts/check.sh go ./internal/... ./cmd/...` passes with no
   skips.

## Relation

- Found by: [#0254](0254.md)'s review
- Caused by: [#0260](0260.md) — which removed the production callers
- Relates to: [#0277](0277.md) — the other half of the same dead-mailer surface,
  and the one that is an actual defect

## Fix

**Decision: RETAIN, not narrow.** `SendVerification`/`SendRecovery` stay on
`auth.Mailer`, implemented by both `SESMailer` and `NoOpMailer`, unchanged in
behavior — only doc comments were added.

Considered narrowing the interface to `SendSessionsRevoked` alone (the only
method with a live production caller). Chose retain instead, for reasons
specific to this codebase rather than a general preference:

- Four test files build fake mailers whose whole purpose is to assert
  `SendVerification`/`SendRecovery` are **not** called by the current
  start-flow: `internal/auth/registration_test.go`'s `recordingMailer`,
  `internal/auth/recovery_test.go`'s `recoveryRecordingMailer`,
  `internal/handlers/logout_all_test.go`'s `logoutAllRecordingMailer`, and
  `internal/handlers/settings_test.go`'s `gateMailer`. Narrowing the
  interface would not have made those assertions wrong, but it would have
  meant rewriting or restructuring all four fakes to drop methods the
  interface no longer named — real surface for a change this issue itself
  describes as "low priority... a tidiness and legibility item."
- `SESMailer.SendVerification`/`SendRecovery` are not merely stubs — they
  correctly enqueue onto `outbound_queue` and are exercised by
  `TestSESMailer_SendVerification_Enqueues`/`_SendRecovery_Enqueues`
  (`internal/auth/ses_mailer_test.go`). Deleting live, tested, correct code
  because its current caller moved elsewhere is a bigger amputation than
  "dead code cleanup" for a one-paragraph tidiness item.
- Retaining costs nothing at compile time or runtime: every implementation
  already had these methods before `#0260`, so "retain" is the zero-diff
  option modulo documentation.

What changed, recorded in the code itself (criterion 1):

- `internal/auth/mailer.go`: the `Mailer` interface doc comment gained a
  `# #0278: SendVerification and SendRecovery are production-dead` section
  naming the decision and why; `SendVerification`'s and `SendRecovery`'s own
  doc comments each got a one-line pointer back to it (criterion 3).
- `internal/auth/ses_mailer.go`: `SESMailer.SendVerification`/`SendRecovery`
  doc comments note no production caller since `#0260` and point back to the
  interface's doc comment.
- `internal/auth/mailer.go`: `NoOpMailer.SendVerification`/`SendRecovery`
  (the pair `#0277` also touches) documented the same way, for consistency
  (criterion 4) — `#0277` retained rather than deleted these, matching this
  decision rather than working at cross purposes with it.
- No implementation lost a method the interface still names, and no
  interface method lost an implementation — criteria 2 and 4 are vacuous
  under "retain" (there is nothing to narrow off, nothing to keep
  consistent by removal) but are satisfied by construction: every
  `Mailer`-implementing type in `internal/auth` and its test files (grepped:
  `recordingMailer`, `recoveryRecordingMailer`, `logoutAllRecordingMailer`,
  `gateMailer`, `NoOpMailer`, `SESMailer`) still implements exactly the same
  three methods the interface still names — none were touched.
- Sequenced after `#0277` per criterion 5 (both touch
  `internal/auth/mailer.go`'s declarations) — `#0277`'s change there
  (documenting `NoOpMailer`'s two methods) landed first in the same session,
  and this issue's edits were made on top of it rather than concurrently.

## Verification

- `ISSUE=0278 scripts/check.sh go ./internal/... ./cmd/...` (criterion 6,
  run verbatim) — `go build`, `go vet`, `gofmt -l` clean, every package
  `ok` with nonzero timings, zero skips, `VERIFICATION PASSED`. (First
  attempt at this broader scope failed on an unrelated pre-existing guard,
  `internal/seo`'s `TestDistIndexPlaceholder` — caused by `#0277`'s manual
  end-to-end check running a real `npm run build` via `./scripts/dev.sh
  --built`, which overwrote `web/dist/index.html`'s guarded placeholder.
  Restored (`git show HEAD:web/dist/index.html > web/dist/index.html`,
  confirmed clean via `git status --porcelain -- web/dist`) before
  re-running; the re-run is the one reported above, and no changes here
  touch `internal/seo` or `web/dist`.)
- `pgrep -f 'go test|\.test '` clean before finishing.

## Review notes

Reviewed 2026-08-27 against commit `58ad750`. **Approved.**

**The change is comments-only, verified rather than assumed.** Stripping `//`
lines from `internal/auth/mailer.go` and `internal/auth/ses_mailer.go` at
`58ad750^` and `58ad750` yields identical hashes (`170553c7…` and `6e078aee…`).
No declaration, signature, or body moved.

**Criterion 1 — an explicit decision, in the code.** `mailer.go`'s `Mailer` doc
comment carries a `# #0278: SendVerification and SendRecovery are
production-dead` section that states the decision in those terms ("#0278 decided
to RETAIN both methods on the interface rather than narrow it") and gives the
reasons. That is a decision a later reader can act on, not a description of the
status quo.

**The reasoning holds up.** The "production-dead" claim is accurate: grepping
`SendVerification|SendRecovery` across all non-test Go files returns only
declarations and comments — no call site anywhere. And the cost of narrowing is
real and slightly understated: fake mailers implementing the interface live in
`internal/auth/registration_test.go`, `internal/auth/recovery_test.go`,
`internal/handlers/logout_all_test.go`, `internal/handlers/settings_test.go` and
also `internal/handlers/logging_test.go` — five files, several of which exist
specifically to assert these methods are *not* called
(`registration_test.go:448`). Set against a self-described "low priority…
tidiness and legibility item", retain is the defensible call. The counter-case
(an interface method with no caller is exactly what narrowing is for) is not
foreclosed: the doc comments now make a later narrowing cheap and explicit
rather than a rediscovery.

**Criterion 3 — every method carries the note, not just the interface.** Six doc
comments in total: `Mailer.SendVerification`, `Mailer.SendRecovery`,
`SESMailer.SendVerification`, `SESMailer.SendRecovery`,
`NoOpMailer.SendVerification`, `NoOpMailer.SendRecovery`. Each says it has had no
production caller since `#0260` and points back to the interface comment for why
it is kept.

**Criterion 4 — nothing drifted.** Nothing was narrowed, and the comments-only
hash check above proves no implementation gained or lost a method. `go build` and
`go vet` are clean, which is itself the compile-time proof that every
`Mailer` implementation still satisfies the unchanged interface.

**Criterion 5 — sequencing respected.** `#0277` and `#0278` landed in one commit
touching `internal/auth/mailer.go` once, with `NoOpMailer`'s methods retained
rather than deleted — the two issues agree rather than fight over the same
declarations.

**Independent verification.** `ISSUE=0277r scripts/check.sh go
./internal/auth/... ./internal/mailing/... ./internal/handlers/...
./cmd/opencircuit/...` — build, vet, and `gofmt -l` clean (the `# #0278` heading
form survives gofmt's doc-comment pass unchanged, per CLAUDE.md §8's warning);
all four packages `ok` with nonzero timings; no skips; `VERIFICATION PASSED`.
Criterion 6's broader `./internal/... ./cmd/...` scope was run by the
implementer; this review's narrower scope covers every package the diff touches.

**One note, not blocking.** The implementer's prose was written under
`## Work log`, which `Issues.md` reserves for the token/cost table. Renamed to
`## Fix` (heading only, no content changed) so the orchestrator's usage rows have
a clean home.
