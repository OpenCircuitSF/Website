# 0321 — The floor harness's outbox section documents claims that are not true

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-27 |

## Description

`#0304` extended `scripts/go_file_visit_floor_guard_test.sh` with an outbox
section. The mechanism works and its review re-proved every criterion. **Two
statements in its own documentation are false**, both measured by that review.

### 1. "Setting either to 0 leaves `go test` green" — half wrong

True for `claimKindsGuardMinPlausibleCallSiteCount`. **False for
`claimKindsGuardMinPlausibleNonExemptCallSiteCount`**: zeroing it makes
`TestNonExemptFloorCatchesScanRootsNarrowedToSelf` **fail** on `0 >= 0`.

Worse, measured across all four combinations: **neither oracle rejects a floor of
`1`.** So the harness's added protection for this family is exactly the single
value `0` — which the standing Go test already covers. The redundancy is
harmless and `1` still catches the `{"."}` narrowing, but the justification as
written is not a fact.

### 2. The margin comment justifies itself on the wrong number

It says call-site populations "CAN be dominated by one file (worst case 3, in
`worker_store_test.go`)". **3 of 12 is 25%, not domination.** That claim is true
of the *total* population (`store_test.go` is 20 of 35) and false of the
non-exempt one the rule actually governs.

**The real justification is unstated and stronger:** `6` is exactly `12/2`, so
`floor_plausible` trips after **two** new call sites — against roughly 44 files
of headroom for the handlers family. That is why this family needs a different
margin rule, and it should say so.

## Acceptance criteria

1. The section header states accurately which floors `go test` can and cannot
   pin, that the standing Go test already pins the non-exempt floor against `0`,
   and that **neither oracle rejects a floor of `1`**.
2. It states the harness's real value for this family: surviving an edit to the
   file it guards, which no in-file check can do (CLAUDE.md §8).
3. The margin comment justifies `0 < floor <= population` on the headroom
   argument — two new call sites versus ~44 files — not on a domination claim
   that is false for the population it governs.
4. Every number in the corrected comments is **re-measured**, not copied from
   this issue. `#0310` exists because a comment advertised itself as measured
   while carrying a count that was not.
5. Comments only — `git diff` filtered to non-comment lines must be empty.
6. `ISSUE=NNNN scripts/check.sh guards` passes, run at least twice.

## Relation

- Found by: [#0304](0304.md)'s review, which measured all four floor/oracle
  combinations
- Relates to: [#0300](0300.md) — the harness this section extends
- Relates to: [#0322](0322.md) — the behavioural question in the same section
- Relates to: [#0310](0310.md), [#0263](0263.md) — comments asserting more than
  the code supports

## Fix

Rewrote two comment blocks in `scripts/go_file_visit_floor_guard_test.sh`'s
outbox section — no code, only `#` comment text (verified: `git diff`
filtered to lines not starting with `#` is empty, criterion 5):

1. **The section header** (criterion 1) now states, separately for each
   floor: zeroing `claimKindsGuardMinPlausibleCallSiteCount` (the total
   floor) leaves `go test` green (unchanged, true); zeroing
   `claimKindsGuardMinPlausibleNonExemptCallSiteCount` (the non-exempt
   floor) does NOT, because `TestNonExemptFloorCatchesScanRootsNarrowedToSelf`'s
   own permanent regression assertion degenerates to the always-true `0 >=
   0` and its `REGRESSION` `Fatalf` fires — re-verified directly against the
   real file (see Verification). States plainly that **neither oracle
   rejects a floor of 1** (roots intact, both floors mutated to 1: `go
   test` stays green and the harness judges PLAUSIBLE — re-verified).
2. **Criterion 2** — added a paragraph naming the harness's actual value for
   this family: it survives an edit to the file it guards, including an
   edit that disables `TestNonExemptFloorCatchesScanRootsNarrowedToSelf`
   itself, because disabling the floors now requires touching two files
   (`internal/outbox/claim_kinds_call_site_guard_test.go` **and**
   `scripts/go_file_visit_floor_guard_test.sh`) and `scripts/check.sh
   guards` is what still catches a single-file edit — the same shape
   `#0300`'s review recorded under "On the regress §8 names".
3. **The margin comment** (`outbox_floor_plausible`'s doc, criterion 3) no
   longer justifies the weaker `0 < floor <= population` bound on the false
   "population CAN be dominated by one file (worst case 3 … )" claim — 3 of
   the 12 non-exempt sites is 25%, not domination, and the real
   file-dominated population (~20 of 32) belongs to the *total* floor, not
   this one. Replaced with the headroom argument: 6 is exactly `12/2`, so
   reusing `floor_plausible()`'s `>= population/2` bound verbatim would put
   this floor at its own failure boundary today and trip after **two** new
   non-exempt call sites (12 → 14), against the handlers family's own
   `floor_plausible()` margin, which — re-measured, not copied from this
   issue — tolerates roughly **35** files of growth today (population 267,
   floor 150, fails at population 302) versus `#0300`'s own historical
   measurement of ~44 files of headroom against a smaller population (258)
   three commits earlier — the same margin, re-measured at a later point,
   not a discrepancy.
4. **Criterion 4** — every number above was re-measured this session, not
   copied from the issue: re-ran the harness against the live tree (total
   population 35, non-exempt 12, both matching the issue's own figures
   incidentally because the tree hasn't changed those specific counts since
   `#0304`), and separately re-measured the handlers family's headroom
   arithmetic by hand (see Verification).

No change to `internal/outbox/claim_kinds_call_site_guard_test.go` or any
other file — this issue's scope is the shell harness's own documentation.

## Verification

- `git diff -- scripts/go_file_visit_floor_guard_test.sh | grep -E '^[+-]'
  | grep -v '^[+-]#' | grep -v '^+++' | grep -v '^---'` — empty (criterion
  5: comments only).
- `bash -n scripts/go_file_visit_floor_guard_test.sh` — clean.
- `ISSUE=0321 scripts/check.sh guards` — run **twice**, both
  `VERIFICATION PASSED`, both reporting `claimKindsGuardMinPlausibleCallSiteCount=5`
  plausible against population 35 and
  `claimKindsGuardMinPlausibleNonExemptCallSiteCount=6` plausible against
  population 12, both zeroed-copy mutations correctly judged IMPLAUSIBLE,
  and both tracked guard files byte-identical before/after.
- Re-verified the "neither oracle rejects a floor of 1" and "the standing Go
  test already pins the non-exempt floor against 0" claims directly: added
  a throwaway `internal/outbox/zzz_scratch_count_test.go` to re-derive the
  AST-side populations (visited=267, total=32, nonExempt=12 today, matching
  `#0304`'s figures), then deleted it and confirmed `git status --porcelain
  internal/outbox/` was empty again before committing anything.
- `uptime` before the run: load average 1.59–2.36 — idle, no timing
  concerns.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-27 | implement | claude-sonnet-5 | — | — | — | — | — (subagent cannot self-measure totals; orchestrator fills in from this session's transcript) |

Left `in-progress` for review, per the standard pipeline — implementation
subagents do not set `resolved`.
