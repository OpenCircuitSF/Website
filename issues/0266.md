# 0266 — Four small residuals in the outbox worker and erasure tests

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | mailing / subscribers |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-24 |
| **Commit** | `ac1a49d` |

## Description

Four items from `#0126`'s re-review. None blocked that issue — it resolved — and
none is a behavioural defect today. They are grouped because they all sit in the
same two files and are cheap to close together.

**1. `#0126`'s erasure test does not exercise the normalisation its fix depends
on.** The fix widened the redaction predicate to
`WHERE subscriber_id = $1 OR email = lower(trim($3))`, and the reviewer
established the fix is *correct* — every write site stores the Postgres-normalised
`RETURNING` value, no `UPDATE` touches the column, so `lower(trim(…))` is
idempotent over it. But the shipped test uses `uniqueEmail()`, which is already
lower-cased and unpadded, **so it never exercises the normalisation at all**. The
reviewer's own mixed-case, whitespace-padded probe is the test that should exist.

That matters because the predicate's correctness rests entirely on a property of
the write path. If a future write site stores caller input verbatim, the
predicate silently stops matching — and the current test would not notice.

**2. `releaseAll(ctx)` never uses its `ctx`.** It builds its own bounded context
by design. A dead parameter that `go vet` will not flag, and a reader may pass a
cancelled context expecting it to matter.

**3. `claimedMu`'s comment understates it.** It calls itself *"belt-and-braces
rather than load-bearing"* — but `Stop` promises it is safe to call more than
once, and two concurrent `Stop`s both reach `releaseAll`. **The mutex is what
makes that promise true.** The code is right and the comment is modest, which is
the direction that gets a guard deleted as redundant — the same class as
`#0263`'s `FOR UPDATE`.

**4. Pre-existing: rows marked `'sending'` by a `ClaimDue` that then errors are
never tracked**, so `releaseAll` misses them. The orphan sweep covers them after
its interval, so nothing is lost. Unchanged by `#0126` and not introduced by it —
recorded so it is not rediscovered as new.

## Acceptance criteria

- [x] The erasure test uses a mixed-case, whitespace-padded address and proves no plaintext survives by exact, case-insensitive **and** substring search — the reviewer's probe, committed
- [x] `releaseAll`'s signature matches what it uses, or the parameter's purpose is documented if kept for interface reasons
- [x] `claimedMu`'s comment says what it actually guarantees, and names `Stop`'s repeat-call promise as the reason
- [x] Item 4 is either fixed or recorded in the worker's own comments as a known, sweep-covered gap — not left silent
- [x] `ISSUE=0266 scripts/check.sh go ./internal/mailing/... ./internal/subscribers/...` passes with zero skips

## Implementation notes

**Obstacle check, per this issue's own note.** `git status --porcelain` was
run first, before any edit: `internal/mailing/outbox_worker.go`,
`outbox_worker_test.go`, `transactional_templates.go` and
`internal/subscribers/pending.go` (the files `#0127`/`#0263`/`#0264` were
mid-edit on) were **clean** by the time this pass started — `#0127`,
`#0263` and `#0264` had each landed a commit (`2f5992b`, `a423a40`,
`611915b` plus their `Fix pass` follow-ups). Re-checked immediately before
every edit in this session; still clean throughout. `internal/subscribers/erase.go`
and `erase_test.go` were never in that protected list.

**Item 1.** Added
`TestErase_RedactsSubscriberEvents_NormalizesMixedCaseWhitespacePaddedAddress`
to `internal/subscribers/erase_test.go`, committing `#0126`'s phase-3
reviewer's own probe: a raw address (`"  ZZ-Review-<n>@Example.COM  "`,
never pre-normalized by the test) submitted at every entry point that can
write a NULL-`subscriber_id` `subscriber_events` row —
`SuppressionStore.Add`, `SuppressionStore.Remove` (both action types),
`Store.Create` — then `Erase`, then a leak hunt three ways (exact match,
`lower(email) = lower($1)`, `email ILIKE '%'||$1||'%'` on the local part)
plus a direct census of the `detail` JSONB column. `Store.Create`'s
`RETURNING`-normalized value is asserted against by hand
(`sub.Email != normalized`) before the leak hunt runs, so a future write
site that stopped normalizing would fail this test at that assertion, not
silently pass the leak hunt for the wrong reason.

**Items 2–4**, all in `internal/mailing/outbox_worker.go`:

- **Item 2.** `releaseAll` took `ctx context.Context` and never read it —
  every `store.Release` call already builds its own bounded context,
  independent of the caller's. Dropped the parameter; updated the one call
  site in `Stop`; documented why in `releaseAll`'s doc comment rather than
  leaving the drop unexplained.
- **Item 3.** Rewrote `claimedMu`'s doc comment (previously: "belt-and-braces
  rather than load-bearing"). It understated the mutex: `Stop` enforces no
  first-caller-wins ordering beyond making `close(w.stopCh)` idempotent via
  `stopOnce` — two goroutines calling `Stop` concurrently both block on the
  same `<-w.doneCh` and, once it closes (a `close()` broadcasts to every
  waiting receiver), both proceed into `releaseAll` at the same instant.
  Without the mutex that is a genuine data race on `w.claimed` — proved,
  not asserted (see `## Verification`). The mutex is exactly what makes
  `Stop`'s "safe to call more than once" promise hold under concurrent
  calls, the same undersold-guard shape as `#0263`'s `FOR UPDATE`. Updated
  `Stop`'s and `releaseAll`'s own doc comments to point at `claimedMu`'s
  for this rather than duplicating it three times.
- **Item 4.** Did not attempt a structural fix — `ClaimDue`
  (`internal/outbox/store.go`) is a single `UPDATE ... RETURNING` that
  commits server-side before the Go code scans a single result row; if
  scanning then fails, the rows are genuinely `'sending'` in the database
  but `w.trackClaimed` is never called for them, because `ClaimDue` returns
  `(nil, err)`. A real fix means wrapping the UPDATE and the scan in one
  explicit transaction `ClaimDue` can still roll back — a change to
  `internal/outbox`'s shared claim path, out of this tidying issue's scope,
  and risky to make without its own dedicated review. Documented the gap in
  `pass`, right at the `ClaimDue` call, naming the mechanism and the orphan
  sweep as the existing safety net — matching the issue's own framing
  ("sweep-covered, nothing is lost").

## Verification

`uptime` before every run: load average 1.3–2.8, well under the ~8 threshold
(§5) — nothing here is a timing reading.

**Item 1**, executed against a real database (`opencircuit_test`, then
independently via `ISSUE=0266 scripts/check.sh`'s own
`opencircuit_test_0266`):

```
go test ./internal/subscribers/ -run TestErase_RedactsSubscriberEvents_NormalizesMixedCaseWhitespacePaddedAddress -v -count=1
```

→ PASS. **Mutation-proved**, not merely executed: copied
`internal/subscribers/erase.go` aside (`cp`, `shasum -a 256`) and created a
dedicated scratch database (`scripts/testdb.sh create 0266mut`, per
`CLAUDE.md` §8b — this reverts the redaction predicate, a safety-relevant
guard, so it does not run against the shared `opencircuit_test`). Reverted
the predicate to its pre-`#0126` shape
(`WHERE subscriber_id = $1`, dropping the `OR email = lower(trim($3))`
half). Both the new test and the existing
`TestErase_RedactsSubscriberEvents_PreservesRows` failed under the mutation:

```
--- FAIL: TestErase_RedactsSubscriberEvents_PreservesRows
    erase_test.go:399: subscriber_events rows still holding the real address after erase = 2, want 0 (defect 1 regression)
--- FAIL: TestErase_RedactsSubscriberEvents_NormalizesMixedCaseWhitespacePaddedAddress
    erase_test.go:537: exact match: found 4 row(s) still holding the address, want 0
```

Restored `erase.go` from the `cp` copy; `shasum -a 256` matched the
pre-mutation hash exactly; `git diff -- internal/subscribers/erase.go`
showed nothing afterward. Dropped `opencircuit_test_0266mut`
(`scripts/testdb.sh drop 0266mut`).

**Item 3**, mutation-proved the same way. Added
`TestOutboxWorker_ReleaseAll_ConcurrentCallsDoNotRace`
(`internal/mailing/outbox_worker_test.go`): two goroutines call
`w.releaseAll()` at the same instant against a populated `w.claimed`. Ran
clean under the mutex:

```
go test ./internal/mailing/ -run TestOutboxWorker_ReleaseAll_ConcurrentCallsDoNotRace -race -v -count=1
```

→ PASS. Copied `outbox_worker.go` aside, removed `claimedMu`'s
`Lock()`/`Unlock()` from `releaseAll` only, re-ran the identical command:
**failed** with `WARNING: DATA RACE`, both stacks pointing at
`releaseAll`'s own `range w.claimed` (read) and `w.claimed = nil` (write) —
exactly the mechanism the corrected comment describes. Restored the file
from the `cp` copy; `shasum -a 256` matched the pre-mutation hash exactly;
`git diff -- internal/mailing/outbox_worker.go` afterward showed only this
issue's intended changes (items 2–4), confirmed by re-diffing against the
hash-verified restore point.

**Full scoped suite**, twice — once directly, once through `check.sh`'s own
isolated database:

```
go test ./internal/subscribers/ -v -count=1   # includes both erase tests above
go test ./internal/mailing/ -run TestOutboxWorker_Stop_ReleasesClaimedRow -v -count=1
```

Both PASS — the existing single-caller `Stop` release test (item 3's
neighbor) is unaffected by the comment/signature changes.

```
ISSUE=0266 scripts/check.sh go ./internal/mailing/... ./internal/subscribers/...
```

— build and vet clean, `gofmt -l` clean, both packages report `ok` with
real elapsed times (no DB-backed package at 0.00s), skip audit `(no
skips)`, `VERIFICATION PASSED`. Own database `opencircuit_test_0266`
dropped by `check.sh` on exit; `pgrep -f 'go test|\.test '` empty
afterward.

## Notes

Reported by `#0126`'s phase-3 re-review under `CLAUDE.md` §9.

**Item 1 is the one worth doing properly.** The other three are tidying; that one
is a test that passes for a reason unrelated to what it claims to prove. This
project has been bitten repeatedly by tests that hold for the wrong reason
(`#0216`, `#0219`, `#0222`), and the reviewer had to write the real probe itself
to be satisfied.

Note another agent has been editing `internal/mailing/outbox_worker.go` for
`#0263`/`#0264` — check `git status` before starting, and rebase on current HEAD
rather than on `#0126`'s commits.

## Relation

- Found by: [#0126](0126.md) — the durable queue whose re-review surfaced all four
- Relates to: [#0263](0263.md) — the same "comment understates a load-bearing guard" class as item 3
- Relates to: [#0265](0265.md) — the citation-accuracy issue from the same review round

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

No subagent transcript file was available to this session to sum exact
token counts — recording model and phase per `issues/Issues.md`'s "no
transcript available" fallback rather than fabricating a number. The
orchestrator should replace this row with exact figures if it can locate
this session's transcript. This session's `git status --porcelain` check at
the start found `internal/mailing/outbox_worker.go`,
`outbox_worker_test.go`, `transactional_templates.go` and
`internal/subscribers/pending.go` clean (`#0127`/`#0263`/`#0264` had each
already landed a commit), so this issue was not blocked as its own Notes
anticipated it might be.

**Total: —**
