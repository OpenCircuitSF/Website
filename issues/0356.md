# 0356 — Line-number citations in Go doc comments drift silently repo-wide

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | tooling / docs |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | — |

## Description

`#0352` removed the two `<file>.go:<int>` citations from
`internal/outbox/claim_kinds_call_site_guard_test.go`, replacing them with
stable identifiers. **The class is repo-wide.**

A census of Go comments by `#0352`'s reviewer found **30 such citations across
7 files**:

| File | count |
|---|---|
| `internal/handlers/audit_email_metadata_guard_test.go` | 11 |
| `internal/handlers/citation_target_guard_test.go` | 11 |
| ~~`internal/outbox/outbox_kind_detection_test.go`~~ | 3 |
| ~~`internal/…/logging_test.go`~~ | 2 |
| `internal/handlers/citation_guard_test.go` | 1 |
| `internal/mailing/templates_test.go` | 1 |
| `internal/seo/sitemap_test.go` | 1 |

**Correction (#0356 planning pass, 2026-08-31).** Two rows above name paths
that do not exist. The struck rows read, verbatim,
`internal/outbox/outbox_kind_detection_test.go` and
`internal/…/logging_test.go`. Re-measured by AST census (see `## Plan`), the
real files are **`internal/mailing/outbox_kind_detection_test.go`** and
**`internal/handlers/logging_test.go`**; `internal/outbox/` contains no file
of that name. `#0352`'s review had the mailing path right and the
transcription into this table did not. Both counts (3 and 2) are correct.

**Two spot-checked at random were both already stale:**

- `internal/seo/sitemap_test.go:80` says "`sitemap.go:86` checks only
  `w.Status`" — that line is `workshops, err := s.source.Workshops()`.
- `internal/mailing/templates_test.go:404` cites `mailer.go:105`, which is a
  **blank line**.

A line number is invalidated by edits to *unrelated* parts of the cited file,
which is what makes the drift silent; an identifier is invalidated only by an
edit to the cited thing itself. §11's PRD index was deliberately rebuilt
heading-anchored for exactly this reason.

## Why this needs a planning pass first

`#0352`'s reviewer endorsed a **Phase 1 (Opus) planning pass** under §3's
"real design latitude" test, and named four decisions where two implementers
would build different things:

1. **Scan scope** — comments only, or all text? (A comment-scoped scan excludes
   runtime `%s:%d` format strings for free, since those live in string
   literals.)
2. **Citation versus fixture.** `outbox_kind_detection_test.go` cites *itself*
   inside an indented block that is a rendered `go test` failure transcript.
   `citation_target_guard_test.go`'s 11 are the citation guard's own prose
   *about* citations. Neither is a stale citation; both match any naive
   pattern.
3. **Fix all 30, or grandfather behind an allowlist?** An allowlist here is
   precisely §8's "a copy of the answer stored next to the question" hazard —
   and `#0337`'s `embeddingReviewed` allowlist, keyed on a name, was proved not
   to stay true.
4. **Where the oracle belongs**, per §8's direction-of-comparison rule: in Go,
   or in an external `scripts/check.sh guards` harness.

The existing `internal/handlers` citation-guard family — which already catches
comments citing unresolvable paths and undefined test functions — is **2,173
lines across three files** and took five issues of design iteration. That is the
scale being extended, and the reason this is not a mechanical pass.

## Plan

### 1. The census, re-measured by this planning pass

**Method — an AST tool, not a grep.** `census0356.go` (scratchpad
`plan0356/`) parses every `.go` file under the repo with
`parser.ParseComments`, pruning `node_modules`, `dist`, and `.git`, and scans
**only the tokens in `f.Comments`** — the file's comment groups. String
literals cannot enter the population by construction. The pattern is
deliberately **wider** than `<file>.go:<int>`:
`[A-Za-z0-9_./\-]+\.[A-Za-z]{1,8}:[0-9]+`, so `.md`, `.ts`, `.svelte` forms
are counted too and can be subtracted, rather than assumed absent.

**Proved falsifiable and fail-closed before it was trusted** (§8 — three
agents found genuine fail-opens in their own instruments this session). On a
fixture carrying one citation in a `//` comment, one in a `/* */` block, and
one inside a string literal next to a `%s:%d` format verb, it reported
**exactly the two comment citations and not the literal**. Zero files
visited, an empty root, and a missing root each produce an explicit
`VERDICT: ERROR` line and exit 1 — a zero census is never reported as
agreement.

**Result: 270 files parsed · 37 citations of any extension · 30 of them
`.go` · 7 files.** The 7 non-`.go` ones subtract cleanly — `PRD.md:671`
(`internal/db/prd_index_parity_test.go`), `citationGuard.test.ts:76` ×3 and
`WorkshopsIndex.svelte:7` ×2 and `docs/frontend.md:16`
(`citation_target_guard_test.go`) — leaving **30 across 7 files**, and the
per-file counts reproduce the reported table exactly: 11 / 11 / 3 / 2 / 1 /
1 / 1. The reported figure is confirmed; two of its **paths** are not (see
the correction under `## Description`).

Each citation was then resolved against the tree — target file located by
repo-relative path, then by sibling directory, then by basename — and the
current content of the cited line read.

### 2. Classification of all 30

**14 stale · 13 real but currently accurate · 3 legitimate fixtures.**

#### `internal/handlers/audit_email_metadata_guard_test.go` — 11 (9 stale, 2 accurate)

| At | Cites | Line actually holds | Real site | Verdict |
|---|---|---|---|---|
| `:104` | `admin_subscribers.go:694` | a comment about `#0100`'s DELETE scoping | `metadata := map[string]any{` at `:733`, in `AdminSubscribersHandler.ClearComplaint` | **stale** |
| `:105` | `admin_subscribers_export.go:327` | a comment about anti-exfiltration | `metadata := map[string]any{` at `:314`, in `AdminSubscribersHandler.Export` | **stale** |
| `:237` | `internal/audit/read.go:159` | `rec.Metadata = json.RawMessage(metaRaw)`, in `Reader.ListAuditLog` | — | accurate |
| `:413` | `ses_notifications.go:443` (and a bare `:503`) | `note = fmt.Sprintf("%s (%s)", note, st)` | the two `audit.ActionSubscriberBounced` entries at `:468` and `:569`, both in `SESNotificationsHandler.applyBounce` | **stale** |
| `:1121` | `admin_campaign_preview.go:419` | `if err != nil {` | `Metadata: map[string]any{` at `:432` (`"recipient_email"` at `:435`), in `AdminCampaignPreviewHandler.Test` | **stale** |
| `:1132` | `admin_subscribers_export.go:312` | a comment naming the `"query"` token | `"filter_query"` at `:318` | **stale** |
| `:1144` | `admin_campaign_preview.go:419` | `if err != nil {` | `"to": actor.Email` at `:433` | **stale** |
| `:1160` | `admin_campaign_preview.go:419` | `if err != nil {` | same as above | **stale** |
| `:1908` | `admin_suppressions.go:274` | `h.auditor.Record(ctx, audit.Entry{` — the `ActionSuppressionRemoved` entry, `AdminSuppressionsHandler.Remove` | — | accurate |
| `:1911` | `admin_subscribers.go:694` | a comment | traced local at `:733`, conditional index assignments at `:742`–`:743` | **stale** |
| `:1919` | `admin_subscribers.go:694` | a comment | same as above | **stale** |

The `:413` entry is the one to read out loud. Its own paragraph explains
that known sites are identified "by (repo-relative file, Action constant
name) **rather than by line number**: a line number shifts on any unrelated
edit above it in the same file" — and then cites two line numbers, both now
25 and 66 lines off. The convention this issue proposes is one this file
already articulated and did not apply to itself.

#### `internal/handlers/citation_target_guard_test.go` — 11 (all currently accurate)

`:150` and `:256` → `campaign_markdown.go:14`; `:202` → `request.go:25`;
`:204` → `testutil_test.go:7`; `:252` and `:708` →
`worker_test_helpers_test.go:2`; `:257`, `:286`, `:296` →
`admin_workshops.go:25`; `:318` and `:741` → `logout_all_test.go:285`. Every
one resolves and the cited text is present at (or within the comment group
spanning) the cited line. **Zero stale, zero fixtures.**

**These are not fixtures**, and this corrects the framing in the "Why this
needs a planning pass first" section above. They are ordinary positional
citations of real sites in other files that simply happen to be correct
today, and every one of them will drift on the next unrelated edit to its
target. Two carry an extra hazard: `logout_all_test.go` is an **ambiguous
basename** — `internal/handlers/logout_all_test.go:285` is the intended
target, while `internal/auth/logout_all_test.go:285` is unrelated code that
resolves just as well.

#### `internal/mailing/outbox_kind_detection_test.go` — 3 (all legitimate fixtures)

`:30` → `:107`, `:157` → `:192`, `:216` → `:251`. Each sits inside a
`//` + **TAB** indented preformatted block that is a **verbatim captured
`go test` failure transcript**, introduced by prose saying so ("captured
before the fix was applied, not reconstructed afterward") and containing
`=== RUN` and `--- FAIL:` lines.

**The load-bearing measurement for decision 2:** the transcript at `:30`
cites `:107`, and this file's real `t.Fatalf` is now at **`:109`**. So one of
the three fixtures is already "inaccurate" — and is *correct as it stands*,
because rewriting a captured transcript to match today's line numbers would
falsify the record it exists to preserve. The other two (`:192`, `:251`)
currently coincide with their `t.Fatalf` lines by accident of not having been
edited.

#### `internal/handlers/logging_test.go` — 2 (both stale, and worse)

`:263` cites `registration.go:111` and `recovery.go:106`, asserting both are
"bare `return s.mailer.SendVerification(...)` / `return
s.mailer.SendRecovery(...)`, no wrapping frame in between".
`internal/auth/registration.go:111` is a comment about
`account.registration_started`; `internal/auth/recovery.go:106` is
`s.auditor.Record(ctx, audit.Entry{`. **Neither call exists anywhere in the
tree**: `grep -rn "return s.mailer.SendVerification\|return
s.mailer.SendRecovery" internal/auth` returns nothing, and
`registration.go:72` and `recovery.go:65` both say in as many words that
`#0260` removed them ("this used to end with … so this method no longer calls
the mailer itself").

This pair is why the plan below is honest about the guard's reach. A
line-number guard flags them — correctly, but for the wrong reason. The
remedy is not a re-anchored citation; it is deleting a paragraph that asserts
a call `#0260` deleted.

#### The three singletons

- `internal/handlers/citation_guard_test.go:40` → `preflight.go:171`'s "PRD
  §6.6 citation". `internal/mailing/preflight.go:170` is
  `// PRD §6.6 requires a monitored reply address, not noreply@.`; `:171` is
  `fail(PreflightCodeReplyToMissing,`. **Stale, off by one.**
- `internal/mailing/templates_test.go:404` → `mailer.go:105`.
  `internal/mailing/mailer.go` is **49 lines long**, so the position does not
  exist; and `SESMailer` is not in that file at all. The real "only rejects
  neither being set" check is `if msg.HTMLBody == "" && msg.TextBody == ""` at
  **`internal/mailing/ses_mailer.go:125`**, inside `SESMailer.Send`. **Stale,
  unresolvable.**

  `#0352`'s review described this as "a **blank line**". That is not
  reproducible: `internal/mailing/mailer.go:105` is past EOF, and
  `internal/auth/mailer.go:105` — the only other `mailer.go` in the tree — is
  `func (m NoOpMailer) SendSessionsRevoked(…)`. The verdict (stale) stands;
  the characterisation does not, and an implementer must not transcribe it.
- `internal/seo/sitemap_test.go:80` → `sitemap.go:86` "checks only
  `w.Status`". `internal/seo/sitemap.go:86` is
  `workshops, err := s.source.Workshops()`; the real gate is
  `if w.Status != WorkshopPublished` at **`:91`**, in `Sitemap.Build`.
  **Stale.**

**Every location in the tables above was measured by this planning pass on
2026-08-31 and is a fact about the tree at commit `6a5b718`. It is still not
licence to transcribe.** Four wrong identifiers were written into issues on
this date alone, in every case because illustrative phrasing was copied into a
criterion as though measured. Criterion 3 below requires re-measurement.

### 3. Decision 1 — scan comments only

**Comments only.** Three reasons, in ascending weight:

1. It is what the census tool already does, and the fixture proves the
   exclusion works: a `bar.go:99` inside a string literal was not reported.
2. The false-positive class it removes is real and present. Beyond the
   `%s:%d` format verbs `#0352` named, the `t.Fatalf` calls in
   `outbox_kind_detection_test.go` at `:192` and `:251` *emit* `<file>:<int>`
   text at runtime; an all-text scan would have to reason about the
   difference between a citation and a diagnostic that produces one.
3. It is what makes the guard's own population stable. A comments-only census
   cannot be moved by a code edit, so `got` changes only when a comment
   changes — which is the property decision 4 depends on.

### 4. Decision 2 — fixture versus citation, by structure, never by accuracy

**Accuracy cannot be the discriminator, and this is measured, not argued.**
The `:30` transcript cites `:107` where the live `t.Fatalf` is at `:109` — an
accuracy test flags a fixture that is correct. All 11 in
`citation_target_guard_test.go` are accurate — an accuracy test clears 11
citations that are exactly as drift-prone as the 14 stale ones.

**The rule: exempt a citation only when all three of these hold**, each a
property of the text rather than a name in a list:

1. **The comment line is a gofmt preformatted line** — `//` followed by a
   **TAB** (§8's indented-block form, the only shape gofmt leaves alone).
   Measured: `indented=true` on exactly the 3 fixtures and `false` on all 27
   others.
2. **The citation is a self-citation** — the cited path resolves to the file
   the comment is in.
3. **The enclosing preformatted block carries a `go test` transcript
   marker** — a line matching `^\t=== RUN ` or `^\t--- (FAIL|PASS|SKIP): `.

All three are required. Any one alone is too weak: (1) exempts any indented
block; (2) exempts a stale self-citation; (3) is spoofable from an
unindented comment. Together they describe "a captured test transcript" and
nothing else in this tree.

**Why this is a rule and not `#0337`'s allowlist.** `embeddingReviewed` was
keyed on a *type name*, so it kept saying "reviewed" after the thing it named
changed underneath it — the answer stored next to the question. These three
predicates are read off the comment itself at scan time: a block that stops
being an indented self-citing transcript stops being exempt on the same edit,
with no list to update. There is nothing to decay.

### 5. Decision 3 — fix all 27, grandfather nothing

**Fix all 14 stale and all 13 currently-accurate citations. Exempt only the 3
fixtures, structurally.** The guard then asserts **zero non-exempt
citations**, which is the only floor with nothing to keep true.

The 13 accurate ones are the argument's real test, and they should still go.
They are not wrong today; they are wrong *tomorrow*, silently, for reasons
unrelated to them — which is the entire defect this issue names. Leaving them
means the guard needs a population to compare against, and a population is an
allowlist wearing a number.

**Replace with an identifier, never with the quoted text.** This matters
specifically in `citation_target_guard_test.go`, whose comments are scanned
by *its own* path-citation guard, and which deliberately does not spell out
`admin_workshops.go:25`'s four-segment token for exactly that reason (see its
`:257`–`:262` and `:290`–`:296`). Substituting the literal cited string would
inflate that guard's own population — `#0323`'s trap, in the one file where it
is guaranteed to fire.

### 6. Decision 4 — the oracle goes in both places, split by role

§8's test is the direction of the comparison and what a mutation leaves
falsifiable, and it gives different answers for the two things that can break.

- **The population assertion belongs in Go.** The mutation it exists to catch
  is *adding a positional citation to a Go comment*. That mutation moves
  `got` from 0 to 1 and the assertion `got == 0` fires. The in-package test is
  a legitimate, non-circular oracle: it measures comment text across 269 files
  and cannot be satisfied by a copy of its own answer.
- **The guard's own shape belongs in the external harness.** The other
  failure mode is "the checker was edited" — loosening the pattern, weakening
  the three-predicate exemption to one predicate, or deleting the assertion.
  §8 is explicit that a guard living in the file it guards is new mutable
  surface, and `#0258` spent six passes proving it. This project already has
  the right home: **`scripts/go_file_visit_floor_guard_test.sh`**, which
  exists precisely to pin `internal/handlers` guard constants from outside,
  already holds `citationGuardMinPlausibleFileCount`,
  `auditEmailMetadataMinPlausibleFileCount`, and
  `citedTestScanRootsMinPlausibleFileCount`, and is already wired into
  `scripts/check.sh guards`.

§8 says the two homes are not exclusive, and here they are complementary
rather than redundant: the Go test cannot detect that it was disarmed, and the
shell harness cannot count citations.

The harness's oracle must be a **regex over the guard file**, not a heredoc
copy of its text — `#0258`'s `FAILED=1` lesson, where one `sed` rewrote both
the definition and the expected copy and the guard agreed with itself.

### 7. The honest limit — this guard is narrower than the problem

`#0330` spent three passes discovering that the terminating move was
**deleting an overclaiming comment**, not adding another check, because a grep
cannot establish that a Go assertion is live. The analogous limit here is
sharper, and this pass measured it:

**A citation guard cannot establish that a citation is true.** It establishes
only that a positional citation *exists*. Telling the 14 stale from the 13
accurate took reading every target file, and for `logging_test.go` it took
knowing that `#0260` deleted the calls the comment describes — which no
pattern over the citing file could have told anyone.

So the guard's value is not detection of falsehood. It is that it enforces a
**convention** — no positional citations in Go comments — and a convention is
mechanically checkable where truth is not. The convention is worth having
because it converts a silently-drifting citation into a citation that can only
be invalidated by an edit to the thing it names (§11's heading-anchored
reasoning, and `#0352`'s review's principled version of it).

**Do not let this guard's greenness be read as "the comments are accurate."**
It says only "no comment states a line number." `#0347` carries the same
warning for its own family, and `logging_test.go:263` is the case that proves
neither family reaches the whole class: it is a stale citation (this guard
sees it) whose real defect is a false behaviour claim about deleted calls in a
*different package* (`#0347`'s file-local census does not see it, and its own
amendment already records that behaviour claims are invisible to that family).

### 8. One guard or two — recommend **two guards, one shared walker**

**Two.** They ask different questions, and — decisively — their exemption
models are **incompatible**:

- `#0347` must allow correctly-framed **history** (its criterion 3, confirmed
  by `#0342`'s sweep: historical references are common and legitimate).
- `#0356` must **not**. A historical positional citation drifts exactly as
  silently as a present-tense one, and the three fixtures here are exempt for
  a structural reason — a captured transcript — not a historical one.

Fusing them would import `#0347`'s history allowance into this class and
reopen it. They also differ in what they must parse: `#0347` needs a mode-`0`
code census of the *same file* to answer "does this file call it"; `#0356`
needs no code census at all.

What they should share is the **walk**, not the assertion: one
`ParseComments` traversal of `internal/` + `cmd/` with the family's existing
`goFileVisitCountImplausible` floor discipline. Two guards, two failure
messages that each name their own rule, one helper.

**Sequencing:** `#0347` is already filed with criteria and an amendment, and
its census helper is the shared piece — land it first. **Do not run `#0347`
and `#0356` concurrently**; both edit the `internal/handlers` guard family.

## Obstacles

| Obstacle | Avoidable? | Clearing command / request |
|---|---|---|
| Toolchain | yes — none missing | Verified by this pass: `command -v go psql npm migrate shellcheck` all resolve (`go1.26.3 darwin/arm64`). Nothing to request. |
| **`scripts/check.sh guards` binds `:5173` and costs ~48s** (`dev_guard_test.sh`) | no, but collision is | Confirm the port is free and yours *before* the run: `lsof -nP -iTCP:5173 -sTCP:LISTEN`. Never `pkill -f` to clear it — §5a: that reaches past its owner, and it has already orphaned another agent's `dev.sh`/vite pair on this project. If it is bound, wait. |
| Own test database | yes | `ISSUE=0356 scripts/check.sh go ./internal/handlers/... ./internal/mailing/... ./internal/seo/... ./internal/auth/...`. The new guard is a pure AST test with no DB, but `internal/handlers` and `internal/auth` are DB-backed and skip silently without `TEST_DATABASE_URL` (§5). Confirm `CREATEDB` if `testdb.sh` complains: `psql -tAc "select rolcreatedb from pg_roles where rolname=current_user"` (the grant is already applied). |
| **Live agents on adjacent files** | yes, by ordering | `#0355` is live in `internal/seo/invalidator_satisfier_guard_test.go` — this issue edits `internal/seo/sitemap_test.go`, a different file in the same package, so a `go test ./internal/seo/...` run may see its in-flight state. `#0336` owns the subscribers/growth code, which this issue **cites but must not edit**. `#0331`–`#0333` own `scripts/testdb.sh`; `#0343`/`#0344` are also live. The worktree at `/private/tmp/oc-live` belongs to another session — do not touch it. |
| **`#0347` overlaps this work** | yes | Do not dispatch both at once; both edit `internal/handlers`' citation guard family. Land `#0347` first (§8 of the plan). |
| §8a shared-index / shared-tree race | no — must be handled | It fired **twice on 2026-08-31**. Stage narrowly, run `git diff --cached --name-only` **immediately** before committing, pick the pathspec form only after reading the index, and verify every commit with `git show --stat HEAD` — line counts, not just filenames. |
| **gofmt smart-quote rewrite** (§8) | yes | ~27 declaration doc comments are being rewritten. Any doubled `'` or doubled backtick collapses on the next `gofmt -w`. Prove stability: `shasum -a 256 <file>` must equal `gofmt <file> \| shasum -a 256`, and `gofmt -l` must be silent. |
| **`#0323` population inflation** | yes | `scripts/go_file_visit_floor_guard_test.sh` greps file **text**, comments included, for `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(`. No replacement text may introduce that call syntax into a comment. Separately, `citation_target_guard_test.go`'s own comments are scanned by its own path guard — see plan §5. |
| BSD `grep -P` matches nothing, silently (§8) | yes | The exemption rule turns on a literal **TAB** after `//`. Do not verify its presence with `grep -P`; read the bytes (`python3 -c "print(open(P,'rb').read())"`). |
| **The agent shell is zsh; the scripts are bash** (§8) | yes | This planning pass hit it: `set -- $var` inside a `for` loop does not word-split under zsh and silently produced empty arguments. Run every script check as `bash -c '…'` or by invoking the script. |
| SES / credentials / migrations | n/a | None needed. No migration, no outward-facing or destructive step, nothing approval-gated. |

## Acceptance criteria

1. **Re-run the census yourself before changing anything**, with an
   instrument you built and proved. It must parse with `ParseComments` and
   scan only `f.Comments` (not `grep` over raw text), be shown falsifiable on
   a fixture whose string literal contains a citation the tool must *not*
   report, and fail closed — an explicit error, never a zero count — on zero
   files visited, an empty file, and a missing path. Record the method and the
   counts. The planning pass measured **270 files · 37 citations of any
   extension · 30 `.go` · 7 files**; report yours and reconcile any
   difference rather than adopting these numbers.

2. **Every one of the 30 is either removed or exempted, and nothing is left
   undecided.** Concretely: the **14 stale** and the **13 currently accurate**
   citations are replaced with stable identifiers; the **3 fixtures** in
   `internal/mailing/outbox_kind_detection_test.go` are left byte-identical.
   A pass that corrects a line number to a newer line number does not satisfy
   this criterion — that is `#0352`'s settled rule.

3. **Re-measure every replacement identifier against the tree.** Do not copy
   an identifier, a line number, or a "real site" out of this issue,
   `#0352`, or any comment. The plan's tables are dated facts, not licence:
   four wrong identifiers were written into issues on 2026-08-31 by exactly
   that transcription. State the method (an AST census, not a grep) and show
   that each replacement resolves to exactly one declaration.

4. **`internal/handlers/logging_test.go:263` is rewritten, not re-anchored.**
   Its claim — that `registration.go` and `recovery.go` end in bare `return
   s.mailer.SendVerification(...)` / `SendRecovery(...)` calls — is false in
   substance: `#0260` removed those calls, as
   `internal/auth/registration.go:72` and `internal/auth/recovery.go:65` both
   state. Verify that independently, then fix the paragraph's *claim*, not
   just its citation.

5. **`internal/mailing/templates_test.go:404` names the right file.** Its
   `mailer.go:105` does not exist (`internal/mailing/mailer.go` is 49 lines)
   and `SESMailer` is not in that file. Re-measure and cite the real check by
   identifier. Do **not** repeat `#0352`'s review's "blank line"
   characterisation, which this planning pass could not reproduce.

6. **A Go guard test fails when a `<file>.go:<int>` citation appears in a Go
   comment under `internal/` or `cmd/`,** with the exemption rule of plan §4
   — all three of: the comment line is `//` + **TAB**; the citation resolves
   to the file the comment is in; and the enclosing preformatted block
   contains a line matching `^\t=== RUN ` or `^\t--- (FAIL|PASS|SKIP): `.
   The assertion is **zero non-exempt citations**. No allowlist keyed on a
   file name, type name, or identifier appears anywhere in it (§8; `#0337`).

7. **The guard is proved falsifiable in both directions, against real bytes.**
   Pointed at a scratch copy of the tree at `HEAD` before this issue's fix, it
   reports the 27 non-exempt citations; against the fixed tree it reports
   zero. Separately, a synthetic fixture that is an indented self-citing block
   **without** a transcript marker must be flagged, and one **with** all three
   properties must not — proving the exemption is the conjunction, not any one
   predicate. Do not mutate the shared tree to do this: compare `git show`
   copies in your own scratchpad (§8a).

8. **It fails closed on a thin census.** Adopt the family's existing
   discipline: a `…MinPlausibleFileCount` const checked through
   `goFileVisitCountImplausible` before an empty findings slice is trusted, so
   a narrowed or emptied scan-roots list cannot report "no problems found".
   The population under `internal/` + `cmd/` is **269 `.go` files** as
   measured on 2026-08-31; choose a floor well below it and say why.

9. **The guard's own shape is pinned from outside**, in
   `scripts/go_file_visit_floor_guard_test.sh` — the existing external oracle
   for this family's constants, already wired into `scripts/check.sh guards`.
   It must mutate a **copy** of the guard file and assert failure from
   outside for at least: the new floor const set to `0`; the exemption
   predicate weakened from three conjuncts to one; and the citation regex
   loosened so it matches nothing. Its oracle must be a **regex over the
   file**, never a heredoc holding a copy of the guard's text (`#0258`: one
   `sed` rewrote both and the guard agreed with itself). Assert any marker
   block it relies on appears **exactly once**.

10. **The guard's own doc comment contains no `<file>.go:<int>` token**, and
    the doc comment records, in prose, (a) why the population assertion lives
    in Go and the shape assertion lives in the shell harness, per §8's
    direction-of-comparison rule, and (b) **the limit of plan §7**: this guard
    proves a *convention*, not the *truth* of any comment, and its greenness
    must not be read as "the comments are accurate."

11. **No replacement text introduces `.ClaimDue(` / `.OrphanSweep(` /
    `.SelectDue(` call syntax into any Go comment** (`#0323`'s trap), and no
    replacement in `internal/handlers/citation_target_guard_test.go`
    substitutes a literal slash-joined path token for a line number — that
    file's comments are scanned by its own path-citation guard and it
    deliberately avoids spelling those tokens. Re-run
    `scripts/go_file_visit_floor_guard_test.sh` and record the population
    (currently **42 total / 12 non-exempt**).

12. **`gofmt` stability on every file touched**: `shasum -a 256 <file>` equals
    `gofmt <file> | shasum -a 256`, and `gofmt -l` is silent. These are
    declaration doc comments — §8's smart-quote rewrite applies. Scan the
    added lines for doubled `'` and doubled backtick and report zero of each.

13. **Verification, per §5**, naming the runs and confirming zero skips:
    `ISSUE=0356 scripts/check.sh go ./internal/handlers/... ./internal/mailing/...
    ./internal/seo/... ./internal/auth/... ./internal/audit/...`, then
    `ISSUE=0356 scripts/check.sh guards` — after confirming `:5173` is free and
    that `uptime` is under ~8. Drop your database and kill your own processes
    by pid (§5a).

14. **Scope.** This issue changes Go comments, adds one Go guard test, and
    extends one shell harness. It does **not** change any production code
    path, any assertion in the guards whose comments it edits, or any
    constant. Prove the comment-only property for the 7 citation-bearing files
    structurally — parse with comments excluded, compare canonical bytes, tool
    proved falsifiable and fail-closed first (`#0352`'s criterion 6 is the
    worked example). The new guard file and the harness edit are, of course,
    exempt from that comparison; say so explicitly rather than quietly.

## Verification

**Census re-derived independently (criterion 1).** Built a fresh AST tool
(`census`, `go/parser` + `parser.ParseComments`, scanning only `f.Comments`,
never string literals), proved falsifiable on a fixture (one `//` citation,
one `/* */` citation, one string-literal `%s:%d` citation — reported exactly
the two comment citations) and fail-closed (missing root, empty root, no
args each print `VERDICT: ERROR` and exit 1; never a zero count read as
agreement). Result against `internal/` + `cmd/`: **270 files parsed · 42
citations of any extension · 35 `.go` · 8 files** — reconciled against the
plan's **270 · 37 · 30 · 7**: the extra 5 `.go` citations, in an 8th file
(`internal/outbox/claim_comment_referent_guard_test.go`), did not exist at
planning time — they were added by `#0347`'s concurrently-running third
implementation pass on this same file, live-owned and off-limits to this
issue. Excluding that file, my census reproduces the plan's 30-across-7
figure exactly, line-for-line: same 11/11/3/2/1/1/1 split, same line
numbers, same paths.

**Every replacement identifier re-measured against the tree myself, not
copied from the issue or `#0352`'s tables (criterion 3).** Read every cited
target's current content directly (`sed -n`/`grep -n` against the real
files) before writing a replacement; confirmed each replacement identifier
resolves to exactly one declaration (`grep -rn 'func (h \*...) ...('` etc.
for every named method; `find . -name '<basename>.go'` for every
bare-filename reference, confirming uniqueness or, for
`logout_all_test.go`, confirming the ambiguity the plan named and
disambiguating with the package-qualified path). One drift already caught
in the act: the plan's own table cited `admin_subscribers.go`'s traced-local
assignments at `:742`–`:743`; re-measured today they are one line further
down, at `:743`–`:744` — the exact silent-drift class this issue exists to
fix, reproduced during its own fix.

**All 27 (14 stale + 13 accurate) replaced with stable identifiers — method
and function names, never corrected line numbers** — across
`internal/handlers/audit_email_metadata_guard_test.go` (11),
`citation_guard_test.go` (1), `citation_target_guard_test.go` (11),
`logging_test.go` (2), `internal/mailing/templates_test.go` (1),
`internal/seo/sitemap_test.go` (1). `internal/mailing/outbox_kind_detection_test.go`'s
3 fixtures are byte-identical (confirmed: `git diff --stat` shows no entry
for that file).

**`logging_test.go:263` rewritten, not re-anchored (criterion 4).**
Independently confirmed `#0260`'s removal:
`internal/auth/registration.go:72`'s and `internal/auth/recovery.go:65`'s
own doc comments state it, and `grep -rn "return s.mailer.SendVerification\|return s.mailer.SendRecovery" internal/auth`
returns nothing. Further confirmed *where* the real enqueue now happens —
`Store.CreatePendingRegistration` / `Store.CreateRecoveryToken`
(`internal/auth/store.go`) call `s.outbox.EnqueueTx` directly, never the
mailer — and that `SESMailer.SendVerification`/`SendRecovery`
(`internal/auth/ses_mailer.go`) have had no production caller since `#0260`
(confirmed by that method's own doc comment, citing `#0278`). Rewrote the
paragraph's claim to state this, not merely swap the citation.

**`templates_test.go:404` re-measured and corrected (criterion 5).**
`internal/mailing/mailer.go` is 49 lines and defines no `SESMailer`; the
real "only rejects neither being set" check is
`if msg.HTMLBody == "" && msg.TextBody == ""` at
`internal/mailing/ses_mailer.go:125`, inside `SESMailer.Send` — confirmed
directly, `#0352`'s review's "blank line" characterisation not repeated.

**New guard: `internal/handlers/line_citation_guard_test.go`,
`TestNoCommentCitesGoFileByLineNumber`.** Population assertion in Go
(scans `citedTestScanRoots` — `internal/`+`cmd/`+`web/`, test files
included, reusing `walkGoFiles`/`toRepoRelativePath`/
`assertGoFileVisitCountPlausible` from `dangling_test_citation_guard_test.go`,
untouched and clean throughout); exemption is the plan's three-conjunct
structural rule (`//`+TAB indentation; self-citation; enclosing comment
group carries a `=== RUN `/`--- FAIL|PASS|SKIP: ` marker), proved as a
conjunction two ways: `TestLineCitationIsExemptRequiresAllThreePredicates`
(pure boolean table, all 8 combinations) and
`TestCollectLineCitationHitsFixtureExemption` (real `go/parser` source,
one synthetic fixture with all three properties — not flagged — and one
missing only the transcript marker — flagged). Floor
(`lineCitationGuardMinPlausibleFileCount = 150`) is a fresh, guard-specific
constant rather than a reuse of the family's shared one, specifically so
the external harness can zero *this* guard's floor independent of its
three siblings. `TestLineCitationPatternExcludesNonGoExtensions` proves the
regex is narrower than the census pattern (`.md`/`.ts`/`.svelte` excluded
by construction, not merely by observation).

**Trap caught and fixed in my own file.** First draft of
`lineCitationIsSelfCitation`'s doc comment quoted
`"outbox_kind_detection_test.go:107"` literally — which the guard's own
regex matched, since doc comments are `f.Comments` too. Caught by running
the guard against itself (`go test -run TestNoCommentCitesGoFileByLineNumber`
reported 6 hits, one inside `line_citation_guard_test.go`); rewritten to
describe the shape without spelling a matching example, the same
self-referential care `citation_target_guard_test.go` already takes
(`#0323`).

**Guard shape pinned externally (criterion 9),
`scripts/go_file_visit_floor_guard_test.sh`.** Floor-to-0: folded into the
existing `FLOOR_NAMES` mutate-and-recheck loop (same shape as its three
siblings, since this guard shares their exact walk). Two new,
guard-specific sub-checks, neither expressible as a `const = 0` mutation:
(a) exemption weakened from three conjuncts to one — the oracle counts
`&&` in `lineCitationIsExempt`'s own `return` line via `grep -o`, mutates a
copy to a single-predicate return, and re-derives the count from the copy
(no hardcoded "2" — a copy of the answer stored next to the question);
(b) citation regex loosened to match nothing — the oracle extracts
`lineCitationPattern`'s raw string from the source and hands it to
**Python's `re` module** (a different regex engine from Go's RE2, so one
edit to the Go source cannot satisfy both the guard and this oracle),
tested against a real fixture citation (`internal/mailing/worker.go:625`);
a mutant with the pattern replaced by a non-matching literal is confirmed
to no longer match. Ran the whole script standalone
(`bash scripts/go_file_visit_floor_guard_test.sh`) and via
`ISSUE=0356 scripts/check.sh guards`: **all guards hold (0 failures)**,
`VERIFICATION PASSED`. Sha256 of every mutated file confirmed unchanged
across the run (mutations happened only in `$WORKDIR`).

**Comment-only, proved structurally (criterion 14).** Built a second,
independent tool (`commentdiff`: `go/parser` **without** `ParseComments`,
re-rendered with `go/printer`, SHA-256 compared) — a different mechanism
from `#0352`'s own `commentdiff`, written fresh for this pass. Proved
falsifiable first (a code-changing pair renders `CODE-DIFFERS` with
distinct hashes; a comment-only pair with identical code renders
`COMMENT-ONLY`) and fail-closed (missing file, wrong arg count both
`VERDICT: ERROR`, never `COMMENT-ONLY`). Ran `HEAD` vs. working tree for
all 6 edited files: **all 6 report `VERDICT: COMMENT-ONLY`.** The new
guard file and the harness edit are new code, not comment-only, and are
correctly excluded from this comparison — stated explicitly rather than
silently skipped.

**gofmt stability (criterion 12).** All 7 touched/added Go files:
`shasum -a 256 <file>` equals `gofmt <file> | shasum -a 256`, and
`gofmt -l` reports nothing for any of them. Scanned every added (`+`) line
for doubled `'` and doubled backtick (§8's smart-quote rewrite hazard):
zero of either. Scanned for `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call
syntax (`#0323`'s trap) and for a literal slash-joined path token
substituted into `citation_target_guard_test.go`'s own comments (its
distinct trap, named in the plan): zero of either.

**`ISSUE=0356 scripts/check.sh go ./internal/handlers/... ./internal/mailing/...
./internal/seo/... ./internal/outbox/...`** — `go build`/`go vet`/`gofmt -l`
clean; `internal/mailing` ok (9.5s), `internal/seo` ok (0.9s),
`internal/outbox` ok (2.4s), zero skips. **`internal/handlers` FAILS —
see "Known open item" below; every other test in that package passes**
(confirmed with `go test ./internal/handlers/... -v`: the only
`--- FAIL` line in the entire package is
`TestNoCommentCitesGoFileByLineNumber`).
`uptime` checked before every run (1.6–2.5, well under ~8). Own database
`opencircuit_test_0356`, dropped via `scripts/testdb.sh drop 0356` after
use (confirmed "dropped"). No leftover `go test`/`.test` processes
(`pgrep` empty). `:5173` confirmed free before `scripts/check.sh guards`
and released after.

### Known open item — resolved by a follow-up implementation pass, 2026-08-31

`TestNoCommentCitesGoFileByLineNumber` correctly found **5 non-exempt
`<file>.go:<int>` citations**, all in
`internal/outbox/claim_comment_referent_guard_test.go` (lines 90, 241,
628, 985, 1022 — e.g. `db9bff7:internal/mailing/outbox_worker.go:165`),
none of which this issue's original implementation pass touched or could
touch: that file is `#0347`'s, and `#0347` **resolved and committed**
(`b297a48`) partway through this issue's implementation pass — confirmed
by `git log`: the file went from `M` (dirty, live) to clean between two
census runs, and the same 5 citations were present at the new `HEAD`.

This was not a defect in the original fix, and left `main` (and
`go test ./internal/handlers/...`) **red** on exactly
`TestNoCommentCitesGoFileByLineNumber` after `#0356`'s implementation pass
landed. Three options were left for whoever picked this up next: (a) a
narrow follow-up converting `#0347`'s 5 citations to identifiers, the same
treatment given to this issue's own 27; (b) a structurally-distinct
exemption in `#0356`'s guard for a leading commit-hash-anchor
(`^[0-9a-f]{7,40}:` immediately before the path); or (c) a standing, named
exception tracked in a follow-up issue.

**Resolved via option (a)** — a dedicated follow-up implementation pass
rewrote `#0347`'s 5 citations as stable identifiers, applying this issue's
own decision 3 (fix all, grandfather nothing) to the population that
arrived after the original pass's census ran. No exemption was added to
the guard (option b was explicitly declined — see below) and no allowlist
was filed (option c was not needed once (a) landed cleanly).

**Each of the 5 replacements was re-verified against the actual
historical commit, not transcribed from this issue's prose**, following
the same discipline as the rest of this issue's own tables:

- 4 of the 5 cite `db9bff7:internal/mailing/outbox_worker.go:165`, and in
  three of those four the same clause also names `:471` in shorthand
  (`"...:165 and :471"`). `git show db9bff7:internal/mailing/outbox_worker.go`
  confirms `:165` opens `mailKinds`' doc comment (the var itself is
  declared at `:185`) and `:471` is the `#0254`-review-bounce inline
  comment immediately preceding the `w.store.OrphanSweep(...)` call inside
  `OutboxWorker.pass` — matching this guard's own file's existing "site A"
  / "site C" labels for the same two `#0342` defects. Replaced with:
  "`mailKinds`' doc comment ... at commit `db9bff7`" for site A, and "the
  inline comment preceding the `OrphanSweep` call in `OutboxWorker.pass`"
  for site C, both naming `internal/mailing/outbox_worker.go` explicitly.
- The 5th cites `ffef8cf:internal/mailing/outbox_worker_test.go:746`.
  `git show ffef8cf:internal/mailing/outbox_worker_test.go` confirms `:746`
  sits inside the `t.Cleanup` preamble comment of
  `TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`. Replaced with "the
  `t.Cleanup` preamble comment in
  `TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`".

**Comment-only, proved structurally**: a fresh `go/parser` (mode 0) +
`go/printer` render-and-hash tool (independent of every prior
`commentdiff` built for `#0352`/`#0356`'s original pass), proved
falsifiable (flags a real code change between two fixtures) and
fail-closed (errors on a missing or empty file, never a silent "equal")
before being trusted, run `HEAD` vs. the follow-up pass's working tree for
this one file — reported `COMMENT-ONLY`.

**`#0347`'s guard left untouched in every other respect**: its regexes,
marker sets, fixtures, floors, and test names are unchanged, and all of
its tests still pass, including both falsifiability regressions against
the real `db9bff7`/`ffef8cf` fixtures
(`TestClaimCommentGuardFlagsKnownStaleSiteFromFfef8cf`,
`TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`) and
the negative fixture (`TestClaimCommentGuardIsCleanOnKnownNegativeFixture`).
`#0356`'s own guard now reports zero non-exempt citations, and
`scripts/go_file_visit_floor_guard_test.sh`'s population is unchanged at
**42 total / 12 non-exempt**.

**Design question for the record, deliberately left unimplemented**: this
collision is a genuine, previously-undiscovered interaction between two
independently-settled designs — `#0347`'s guard must allow correctly-framed
historical citations (its own criterion 3), while `#0356`'s guard must not,
because a historical positional citation drifts exactly as silently as a
present-tense one; the commit hash pins *when* a claim was true, not
*where* the line is today. Option (b) above — a narrow, structural
exemption in `#0356`'s guard for a leading commit-hash-anchor immediately
before the path (`^[0-9a-f]{7,40}:`) — is arguably the more principled fix
for this whole *class*, since a commit-pinned citation genuinely cannot
drift the way a present-tense one can, and would have meant `#0347`'s file
never needed touching. It was **not implemented here**: `#0356`'s plan §8
decision 8 explicitly considered and rejected exempting history from this
guard, and weakening that settled rule is a design decision this follow-up
pass was not authorized to make unilaterally, particularly under time
pressure to restore a green `main`. Whether to file a new issue proposing
that structural exemption (which would let `#0347`'s file, and any future
commit-pinned citation, cite by line number without triggering `#0356`'s
guard) is left to whoever reviews this resolution.

## Relation

- Found by: [#0352](0352.md)'s review, which resolved that issue and ran the
  census
- Relates to: [#0352](0352.md) — the same defect, fixed in one file
- Relates to: [#0347](0347.md) — the adjacent class: comments naming a call
  their file's code does not make. Consider whether one guard should cover both
- Relates to: [#0113](0113.md) — the PRD index made heading-anchored for
  exactly this reason
- Relates to: [#0337](0337.md) — the name-keyed allowlist proved not to stay
  true, relevant to decision 3

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0352`'s review (§9), which
  explicitly endorsed a planning pass before implementation.
- 2026-08-31 — Planning (Opus, phase 1). Re-ran the census with a
  purpose-built `go/parser` + `f.Comments` instrument, proved falsifiable and
  fail-closed before use rather than the reported grep: **270 files parsed ·
  37 citations of any extension · 30 `.go` · 7 files** — reproducing the
  reported figure exactly, and correcting two wrong paths in this issue's own
  table. Classified all 30 (**14 stale · 13 real but currently accurate · 3
  legitimate fixtures**); both reported spot-checks confirmed stale, one not
  for the stated reason. Settled the four decisions, recommended two guards
  sharing one walker rather than merging with `#0347`, recorded the guard's
  structural limit (it proves a convention, not truth), and wrote
  `## Obstacles`. No code written; status stays `open` for phase 2.
- 2026-08-31 — Implementation (Sonnet, phase 2). **The orchestrator's
  dispatch note corrected the plan's own §8 sequencing claim**: `#0347`
  landed in `internal/outbox` (not `internal/handlers`) and shares no
  infrastructure with this issue, so it is neither a blocker nor a
  dependency — recorded here because the plan itself said the opposite.
  That correction held for the *file-overlap* reason it was given; it did
  not anticipate a *content* collision, which turned up anyway (see
  "Known open item" in `## Verification`): `#0347` completed its own third
  implementation pass and resolved (`b297a48`) while this pass was
  in-flight, landing 5 `<file>.go:<int>` citations this guard's own,
  plan-settled exemption rule does not cover. Re-ran the independent
  census myself rather than trusting the plan's table (built a fresh
  `go/parser`-based tool, proved falsifiable and fail-closed before use);
  it reproduced the plan's 30-across-7 figure exactly once `#0347`'s
  8th, out-of-scope file was excluded. Re-measured every one of the 27
  replacement identifiers against the tree directly (not copied from this
  issue or `#0352`) and replaced all 27 (14 stale, 13 accurate) with
  identifiers, rewriting `logging_test.go:263`'s claim rather than merely
  re-anchoring it (independently confirmed `#0260`'s and `#0278`'s
  history). Left the 3 `outbox_kind_detection_test.go` fixtures
  byte-identical. Added `internal/handlers/line_citation_guard_test.go`
  (`TestNoCommentCitesGoFileByLineNumber`, plus a boolean-table exemption
  test and a real-source fixture exemption test) and extended
  `scripts/go_file_visit_floor_guard_test.sh` with this guard's floor
  (folded into the existing shared loop) and two new, guard-specific
  external checks (exemption-weakening, regex-loosening — both oracled
  independently of Go, one via a second, independent regex engine).
  Checked `scripts/go_file_visit_floor_guard_test.sh`'s git status before
  every edit to it; found it clean both times I touched it. Proved the
  6-file comment-only property with a fresh, independently-built
  `commentdiff` tool (different mechanism from `#0352`'s own). `ISSUE=0356
  scripts/check.sh guards` — `VERIFICATION PASSED`. `ISSUE=0356
  scripts/check.sh go ./internal/handlers/... ./internal/mailing/...
  ./internal/seo/... ./internal/outbox/...` — green except
  `internal/handlers`, which fails on exactly one test
  (`TestNoCommentCitesGoFileByLineNumber`) for the reason recorded in
  `## Verification`'s "Known open item" — a real, out-of-scope collision
  with `#0347`'s now-resolved work, not a defect in this pass. Did not
  weaken the guard, grandfather the 5 citations, or touch `#0347`'s file to
  force a green run. Status left `in-progress` for review; not marking
  `resolved` per `CLAUDE.md` §9 in any case, and this open item is an
  additional, explicit reason review is needed before anyone reads this as
  done.
- 2026-08-31 — Follow-up implementation pass (Sonnet), dispatched
  specifically to clear the "Known open item" above and restore a green
  `main`. Rewrote `#0347`'s 5 commit-pinned citations in
  `internal/outbox/claim_comment_referent_guard_test.go` (lines 90, 241,
  628, 985, 1022) as stable identifiers, re-verifying each against the
  actual historical commit (`git show db9bff7:...` /
  `git show ffef8cf:...`) rather than transcribing this issue's own
  tables — see the rewritten "Known open item" section above for the exact
  replacements and evidence. Did not weaken `#0356`'s guard, did not add
  an exemption for commit-pinned citations, and did not touch `#0347`'s
  guard logic/regexes/floors — only its comments' citation text, proved
  comment-only with a fresh `go/parser`+`go/printer` tool proved
  falsifiable and fail-closed before use. `ISSUE=0356b scripts/check.sh go
  ./internal/handlers/... ./internal/outbox/... ./internal/mailing/...
  ./internal/seo/...` — all four packages `ok`, zero skips. `ISSUE=0356b
  scripts/check.sh guards` — `VERIFICATION PASSED`, including the new
  `line citation guard shape (#0356)` and `outbox claim-comment-referent
  guard floors (#0347 B3)` sections; `go_file_visit_floor_guard_test.sh`
  population unchanged at 42 total / 12 non-exempt. Committed separately
  from this issue-file edit (`8560fda`, 1 file, +15/-7, verified via
  `git show --stat HEAD`). Recorded, but did not implement, the design
  question of whether `#0356`'s guard should carry a structural exemption
  for a leading commit-hash-anchor — that remains a decision for review,
  not settled by this pass. Status left `in-progress`; the reviewer
  resolves.
- 2026-08-31 — Review (Opus, phase 3). **Bounced.** Criteria 1–8 and 10–14
  all independently re-verified and met, in full, against my own
  instruments; criterion 9's final requirement — "assert any marker block
  it relies on appears **exactly once**" — is not implemented, and I
  demonstrated two concrete fail-opens it leaves in
  `scripts/go_file_visit_floor_guard_test.sh` (see `## Review notes`).
  **`CLAUDE.md` §3 decision: no escalation — implementation stays on
  Sonnet.** This is the issue's first bounce, the failure is one
  specifiable gap in an external harness rather than a design problem, and
  the remedy below is written as exact code to transcribe, which is
  precisely the condition §3 names for keeping the next pass on Sonnet.

## Review notes

> 🔴 Bounced 2026-08-31 (review, `claude-opus-5`) at `b46fa45` / `8560fda`.
> The citation work itself is correct and I could not fault it: I
> re-derived the census with my own AST instrument, re-measured **all 32**
> replacements (27 + the 5 in the collision fix) against the tree and
> against the historical blobs, and every single one is true. The fix is
> bounced on **criterion 9 alone** — the external harness does not assert
> its marker block appears exactly once, and I reproduced the two
> disarmaments that omission permits, one of which leaves the guard
> completely dead with `scripts/check.sh guards` reporting
> `all guards hold (0 failures)`.

### What I verified independently, and what held

**Census (criterion 1) — my own instrument, proved before use.** Built a
fresh `go/parser` + `parser.ParseComments` tool scanning **only**
`f.Comments` (string literals cannot enter by construction), with a
deliberately wider pattern than `<file>.go:<int>` so non-`.go` forms are
subtracted rather than assumed absent. Proved falsifiable on a fixture
carrying a `//` citation, a `/* */` citation, a TAB-indented citation and a
`fmt.Sprintf("%s:%d delta.go:44", …)` **string literal** — it reported the
three comment citations and not the literal. Proved fail-closed: no args,
a missing root, and an empty directory each print `VERDICT: ERROR` and
exit 1; a zero census is never reported as agreement.

Result against the working tree: **272 `.go` files parsed · 13 citations of
any extension · exactly 3 `.go` · 1 file.** All 3 are the
`internal/mailing/outbox_kind_detection_test.go` transcript fixtures, all
correctly exempt. **Zero non-exempt `<file>.go:<int>` citations remain**,
and a repo-wide run (every root, not just `internal/`+`cmd/`+`web/`) finds
the same 272 files and the same 3 hits — there is nothing outside the
guard's scan roots. The fixture file is **byte-identical** to `243fc6d`
(`git diff --stat` shows no entry for it), and I read its bytes directly
rather than through `grep -P` (§8): all three sit under `//` + a real TAB,
inside groups carrying `//\t=== RUN ` and `//\t--- FAIL: `.

The guard's own doc-comment claim of "272 real .go files … measured
directly" is **true** (`find internal cmd web -name '*.go' … | wc -l` = 272).

**Falsifiability against real bytes (criterion 7).** I exported `243fc6d`
with `git archive` into my own scratchpad, dropped in **only**
`line_citation_guard_test.go`, and ran the guard against that pre-fix tree:
it reports **32** citations — the planned **27**, split exactly
11/1/11/2/1/1 across the six files the plan named, **plus** the 5 that
`#0347` landed. Against the fixed tree it reports zero. That is the
criterion's both-directions proof, measured rather than argued.

**Every replacement is TRUE (criterion 3 — the substance).** I re-measured
all 32 against the tree and the history, copying nothing from the issue:

- `AdminSubscribersHandler.ClearComplaint` / `.Export` — both are real
  `metadata := map[string]any{` locals, at `admin_subscribers.go` and
  `admin_subscribers_export.go`. ✓
- `Reader.ListAuditLog`'s `rec.Metadata = json.RawMessage(metaRaw)` — the
  single tree-wide `.Metadata =` hit, in `internal/audit/read.go`. ✓
- The two `ActionSubscriberBounced` entries are indeed the
  `BounceTypePermanent` case and the repeated-soft-bounce case, **both
  inside `SESNotificationsHandler.applyBounce`**. ✓
- `AdminCampaignPreviewHandler.Test`'s `Metadata:` literal really does
  carry `"to": actor.Email` **and** `"recipient_email"`. ✓
- `AdminSuppressionsHandler.Remove`'s `ActionSuppressionRemoved` entry
  really is an **inline** composite literal with `"email"` written directly
  in it, and `ClearComplaint` really is the traced local whose conditional
  index assignments are `"suppression_removed_note"` /
  `"suppression_removed_created_at"` and never `"email"` — the exact
  contrast the paragraph draws. ✓
- `preflight.go`'s `PreflightCodeReplyToMissing` check, `SESMailer.Send`'s
  `msg.HTMLBody == "" && msg.TextBody == ""` guard (the only such check in
  `internal/mailing`), and `Sitemap.Build`'s
  `w.Status != WorkshopPublished` gate — all present, all unique. ✓
- All 11 in `citation_target_guard_test.go`:
  `campaign_markdown.go`'s **Raw-HTML-decision** comment does carry
  `PRD/CLAUDE.md §9`; `clientIP`'s doc comment carries the `#0002`
  deletion note; `itoa`'s carries the `url_filters_test.go` note;
  `worker_test_helpers_test.go`'s **file-header** comment carries
  `campaigns_test.go/audience_test.go`; `admin_workshops.go`'s
  **package-header** comment carries the four-segment
  `internal/seo/seo.go/sitemap.go`; and the `handlers/auth.go` citation
  is inside `internal/handlers/logout_all_test.go`'s
  `TestLogoutAll_HTTP_Transports` (lines 232–340) — with
  `internal/auth/logout_all_test.go` genuinely existing, so the added
  disambiguation is both correct and load-bearing. ✓

**`logging_test.go` (criterion 4) — the rewrite is right, and it is a
rewrite.** I confirmed independently that `RegistrationService.StartRegistration`
and `RecoveryService.StartRecovery` no longer touch the mailer (their own
`#0260` notes say so and no `s.mailer` call survives in either file), that
`Store.CreatePendingRegistration` and `Store.CreateRecoveryToken` each call
`s.outbox.EnqueueTx` **inside the same `tx` that inserts the token row**,
that `#0278`'s retain-on-the-interface decision is real
(`internal/auth/mailer.go`), and that the test body does call
`mailer.SendVerification` / `SendRecovery` directly on a live
`*auth.SESMailer` with a cancelled context. The deleted claim was false;
the replacement is true.

**The 5 commit-pinned replacements (the collision fix) — verified against
the blobs, not the prose.** `git show db9bff7:internal/mailing/outbox_worker.go`
line **165** is the first line of `mailKinds`' doc comment (the `var` is at
`:185`), and its clause is "so this worker never claims OR sweeps a row it
cannot render" — site A, exactly as written. Line **471** is the
`#0254`-review-bounce comment immediately preceding
`w.store.OrphanSweep(…)` at `:477`, inside `func (w *OutboxWorker) pass`
declared at `:470`, and its clause is "so this sweep can never release a
live claim" — site C, exactly as written.
`git show ffef8cf:internal/mailing/outbox_worker_test.go` line **746** is
inside the comment preamble immediately above `t.Cleanup(` at `:750`,
within `TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued` declared at
`:718`, and that line is the one containing "OutboxWorker.ClaimDue call".
All five identifiers resolve uniquely and correctly.

**The 6th citation-shaped string — the implementer's judgement is
correct.** `outbox_worker_test.go:743` at
`internal/outbox/claim_comment_referent_guard_test.go:953` lives inside the
**raw string literal** `claimCommentGuardStaleFixtureSrc`, not in a comment
of the enclosing file. My AST census does not report it and the guard does
not flag it, both correctly. It must stay: the fixture is a byte-for-byte
reproduction of the `ffef8cf` text that
`TestClaimCommentGuardFlagsKnownStaleSiteFromFfef8cf` depends on, and
`:743-749` is itself accurate against the blob.

**The three-predicate exemption is a genuine conjunction (criterion 6/7).**
I did not take the boolean table's word for it. In an exported copy I
mutated the **real fixture file's real bytes**, one predicate at a time,
and ran the **real guard**:

| Mutation to `outbox_kind_detection_test.go:30` | Guard |
|---|---|
| drop the TAB (`//\t` → `// `), keep self-citation + markers | **flagged** ✓ |
| keep TAB + markers, change the cited path to `worker.go:107` | **flagged** ✓ |
| keep TAB + self-citation, blank the `=== RUN` / `--- FAIL` lines | **flagged** ✓ |
| all three intact | not flagged ✓ |

Each predicate is load-bearing; the file was restored byte-identically
(sha verified) after each. And an ordinary stale citation cannot satisfy
it: an injected plain `// internal/mailing/worker.go:99999` in
`internal/seo/sitemap.go` is caught, because it is not TAB-indented.

**Comment-only (criterion 14) — my own prover.** Built an independent
`go/parser` mode-`0` + `go/printer` render-and-hash tool (comments never
enter the AST), proved falsifiable (a `const N = 1` → `2` pair renders
`CODE-DIFFERS` with distinct hashes; a comments-differ pair renders
`COMMENT-ONLY`) and fail-closed (empty file, missing file, wrong argc all
`VERDICT: ERROR`, never `COMMENT-ONLY`). All **7** edited `.go` files
report `VERDICT: COMMENT-ONLY` from `243fc6d` to `HEAD`, including
`#0347`'s file. `line_citation_guard_test.go` is new code and correctly
excluded.

**Everything else.** `gofmt -l` silent and `shasum` equals
`gofmt | shasum` on all 8 touched/added Go files; zero doubled `'` and
zero doubled backtick in the added lines; no `.ClaimDue(` /
`.OrphanSweep(` / `.SelectDue(` call syntax in any added comment (§0323);
`go_file_visit_floor_guard_test.sh` population unchanged at **42 total /
12 non-exempt**. `ISSUE=0356r scripts/check.sh go ./internal/handlers/...
./internal/outbox/... ./internal/mailing/... ./internal/seo/...` —
`VERIFICATION PASSED`, all four `ok`, **zero skips**. `ISSUE=0356r
scripts/check.sh guards` — `VERIFICATION PASSED`. **`main` is green now**;
I confirmed it was red only between `b46fa45` and `8560fda` by running the
guard against the `243fc6d` export. All 7 of `#0347`'s tests pass, and all
4 of `#0356`'s. `uptime` 1.2–2.1 before every run; `:5173` confirmed free
with `lsof` before the guards run and released after; test database
`opencircuit_test_0356r` dropped; no leftover `go test` processes. Every
mutation happened in my own `git archive` export, never the shared tree
(§8a/§8b).

### Why this is bounced — criterion 9's "exactly once" is missing, and it matters

Criterion 9 ends: "**Assert any marker block it relies on appears exactly
once.**" `scripts/go_file_visit_floor_guard_test.sh` does not. Both new
sub-checks locate their subject with a single `grep` and then judge the
first/only thing it finds. I reproduced two disarmaments this permits,
externally, in an exported copy.

**Fail-open A — the exemption oracle is shadowable (`#0258`/`GUARD-0208`
shape).** Weaken the real function to `return commentIsIndented`, and add
anywhere else in the same file:

```go
func lineCitationIsExemptLegacy(commentIsIndented, isSelfCitation, groupHasTranscriptMarker bool) bool {
	return commentIsIndented && isSelfCitation && groupHasTranscriptMarker
}
```

`grep -E "$EXEMPT_RETURN_PATTERN"` finds the decoy, counts its 2 `&&`, and
the harness prints
`go_file_visit_floor_guard_test.sh: all guards hold (0 failures)` — **exit
0** — with the real exemption reduced to one predicate. Measured, not
argued. (The in-file boolean table does catch this one, but it lives in the
file it guards, which is exactly the surface §8 says the harness exists to
cover.)

**Fail-open B — the regex oracle has exactly one fixture, so a pattern
narrowed to that fixture satisfies it.** Replace the pattern with
``regexp.MustCompile(`worker\.go:625`)``. The Python oracle still reports
`MATCH` on its sole fixture `internal/mailing/worker.go:625`, so the
harness prints `all guards hold (0 failures)`, **and**
`TestNoCommentCitesGoFileByLineNumber` **passes** — I injected a real
`// probe: internal/mailing/preflight.go:99999` into `internal/seo/sitemap.go`
and the guard did not catch it. The guard is **completely dead** with the
external harness green. The single fixture is "a copy of the answer stored
next to the question" one level up: the only thing the pattern must satisfy
is the one string the oracle tests.

**One thing that is right and should not be changed while fixing this.** I
applied a *single* `sed` to the guard **and** the harness together (the
`#0258` test) — the harness still failed, because `REAL_AMP_COUNT` is
compared against a literal `2` that lives in a different file from its
subject. That hardcoded `2` is the protection.

### Remedy — apply exactly this

**1. Assert exactly-once before judging the exemption.** In
`scripts/go_file_visit_floor_guard_test.sh`, in the `#0356` section,
replace the `REAL_RETURN_LINE=` line and the `if [ -z "$REAL_RETURN_LINE" ]`
block's opening with a count check first (the script is `set -uo pipefail`,
not `-e`, and `grep -c` returning 1 on zero matches is safe; keep the
`|| true` anyway):

```bash
EXEMPT_RETURN_COUNT="$(grep -cE "$EXEMPT_RETURN_PATTERN" "$LINE_GUARD_SRC" || true)"
if [ "$EXEMPT_RETURN_COUNT" -ne 1 ]; then
  fail "REGRESSION #0356: lineCitationIsExempt's three-conjunct return statement appears ${EXEMPT_RETURN_COUNT} time(s) in $LINE_GUARD_FILE, not exactly 1 -- 0 means it was weakened or renamed; >1 means a decoy copy elsewhere in the file shadows the real (possibly weakened) definition for this oracle's grep, which is CLAUDE.md §8's GUARD-0208 begin/end-count rule and #0356 criterion 9's 'appears exactly once'."
else
  REAL_RETURN_LINE="$(grep -E "$EXEMPT_RETURN_PATTERN" "$LINE_GUARD_SRC")"
  ... existing REAL_AMP_COUNT logic, unchanged ...
fi
```

Do the same for the pattern extraction: assert
`grep -cE '^var lineCitationPattern = regexp\.MustCompile\(`' "$LINE_GUARD_SRC"`
is exactly `1` before extracting.

**2. Give the regex oracle a fixture SET, not one fixture.** Replace the
single `FIXTURE_CITATION` with two arrays and loop `python_regex_matches`
over both, `fail`ing on any disagreement. Use exactly these — I verified
each against the tracked pattern under Python:

```bash
LINE_CITATION_MUST_MATCH=("internal/mailing/worker.go:625" "sitemap.go:86" "a.go:1" "x_test.go:1234")
LINE_CITATION_MUST_NOT_MATCH=("PRD.md:671" "citationGuard.test.ts:76" "WorkshopsIndex.svelte:7")
```

A pattern narrowed to any one member then fails on the others, and the
must-not-match arm pins the `.go`-only narrowness the census relied on.
Keep the existing "loosened to a non-matching literal" mutant check as is.

**3. Correct two false statements about this harness's own mechanism.**
The `#0356` section header says the exemption oracle "recomputes a `&&`
count from the tracked file itself (**not a hardcoded "2"**)" and sub-check
1's comment repeats it ("rather than hardcoding '2'"). Both are false —
`[ "$REAL_AMP_COUNT" -eq 2 ]` hardcodes it, twice — and the claim is
backwards about which mechanism provides the protection. Replace with what
is actually true and load-bearing: *the count is re-derived from the
tracked file, and compared against a literal `2` that lives in a different
file from its subject, so a single `sed` applied to both the guard and this
harness still fails (verified).* The identical false claim is in this
issue's `## Verification` ("no hardcoded \"2\" — a copy of the answer
stored next to the question") — correct it there too, per `Issues.md`'s
strike-and-date convention, since this issue is *about* guard comments
that assert things that are not so.

**4. While you are in `internal/handlers/logging_test.go`** (small, same
class): the rewritten paragraph says `SendVerification` and `SendRecovery`
"are still **the only place** that produces `errEnqueueFailed`'s fixed,
sanitized shape". `SendSessionsRevoked` produces it too — all three go
through `logEnqueueFailure` (`internal/auth/ses_mailer.go`). Scope the
claim to the registration/recovery paths, which is what the sentence's own
"rather than going through RegistrationService/RecoveryService" contrast
actually means.

Re-run `ISSUE=<yours> scripts/check.sh guards` and confirm both new
assertions fail against a decoy-shadowed copy and a narrowed-pattern copy
in your own export, not the shared tree.

### Ruling: commit-pinned citations should NOT be an exempt class

Asked for explicitly, and settled here so it is not reopened. **Keep zero
tolerance. Do not add the `^[0-9a-f]{7,40}:` exemption option (b).**

1. **It buys nothing.** The immutability argument is about the *target*,
   but the citation's job is to tell a reader what is being discussed, and
   `db9bff7:…outbox_worker.go:165` only does that after checking out a
   historical blob and counting lines. "`mailKinds`' doc comment at commit
   `db9bff7`" answers it directly **and** is immutable. I verified all five
   rewrites resolve unambiguously against the real blobs, and none was
   harder to write or less precise than the number it replaced.
2. **It is a syntactic exemption for a semantic property — `#0337` again.**
   Nothing in the guard can check that the hex string is a real commit,
   that the commit contains that file, or that the line says what the prose
   claims. Meanwhile any ordinary stale present-tense citation becomes
   exempt by prefixing seven hex characters, and no reviewer would notice.
   That is an allowlist with a seven-character entry fee.
3. **New surface for a population of zero.** Post-fix the tree contains no
   commit-pinned citations. The exemption would add a fourth branch to
   `lineCitationIsExempt` that nothing exercises, which the external
   harness would then also have to pin — on the very guard whose in-file
   surface §8 already names as the hazard, and while that harness has the
   gaps this review just bounced it for. Decision 3's reasoning holds
   exactly: a retained shape with no members is an allowlist wearing a
   regex.
4. **The transcript exemption is not a precedent for it.** The three
   fixtures are exempt because they are **verbatim artifacts** whose value
   is being unaltered — rewriting one would falsify the record it exists to
   preserve. A commit-pinned prose citation is the author's own sentence,
   freely rewritable at no cost. That is where the line belongs, and the
   guard's doc comment already draws it correctly ("the commit hash or
   issue number pins WHEN the claim was true, not WHERE the line is
   today").
5. **The collision recurring argues the other way.** Its cost was one small
   follow-up commit. The guard now catches this shape the moment it is
   written, which is cheaper than discovering it mid-pass. The exemption's
   cost is permanent.

No follow-up issue for this; the question is answered.
