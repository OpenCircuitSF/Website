# 0228 — A restore leaves every table owned by the wrong role, and the app cannot read them

| | |
|---|---|
| **Status** | resolved |
| **Platform** | All |
| **Module** | infra / deploy |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-24 |
| **Commit** | `a34dcef`, `e3d7920` (the `docs/deployment.md` half — see `## Review notes`) |
| **Closed** | 2026-08-24 |

## Description

`#0062` verified the backup/restore round trip exhaustively — row counts,
sequence positions, `schema_migrations`, every constraint definition and index
definition, both dump formats, all matching. Its phase-3 review reproduced that
independently and found the one thing the comparison did not cover.

**Table ownership is not preserved.** Source tables owned by `opencircuit`;
after restore, owned by whoever ran the restore. `backup.sh` dumps with
`--no-owner --no-privileges`, so ownership and grants do not travel.

Locally this is invisible — the restoring user happens to be able to read
everything. **On the documented production path it is not.** That path is
`sudo -u postgres pg_restore`, which would leave every table owned by
`postgres` with grants stripped — so the application role gets
`permission denied` on its first query after a restore that otherwise reported
complete success.

This is the failure mode a backup exists to prevent, arriving at the worst
moment: you restore during an incident, the restore says it worked, every row is
present, and the site stays down.

**`docs/deployment.md` currently asserts the opposite** — a parenthetical claims
`tableowner` was confirmed identical. That is true only because the local drill
runs as the owning role, and it is wrong in the general case.

**Three smaller defects in the same territory**, all found by the same review:

1. **The drill's step 1 is broken**: it calls `scripts/testdb.sh create` twice —
   and `create` drops first — then `-f seed.sql`s a file that does not exist.
2. **The drill's step 7 fails silently.** `testdb.sh drop` against a
   `restore.sh`-created database hits `ERROR: must be owner`, then prints "does
   not exist" and **exits 0**, leaving a stray database. That is a `testdb.sh`
   bug, outside `#0062`'s diff.
3. **Two undocumented behaviours worth stating**, both benign: a custom-format
   re-restore is idempotent, and a plain `.sql.gz` into a non-empty database
   aborts at the first `CREATE TABLE` with exit 3 — it fails safe.

## Acceptance criteria

- [x] After a restore run as a role other than `opencircuit`, every table, sequence, and index is owned by `opencircuit` and the app role can read and write — demonstrated by connecting **as the app role** and issuing a real query, not by inspecting catalog rows
- [x] The restore path does this itself, or the runbook makes it an explicit, gated step that cannot be skipped by someone following the happy path
- [x] `docs/deployment.md`'s `tableowner` claim is corrected — it is currently false in the general case
- [x] The drill's step 1 runs as written: one `create`, and no reference to a seed file that does not exist
- [x] `testdb.sh drop` reports failure when it fails — a foreign-owned database must not produce "does not exist" and exit 0
- [x] The two undocumented behaviours (idempotent custom re-restore; plain-format abort into a non-empty database) are stated in the runbook
- [x] `#0062`'s full round-trip comparison still passes for both formats
- [x] `ISSUE=0228 scripts/check.sh go ./internal/...` passes with zero skips — see `## Verification`; note the one caveat recorded there about an unrelated concurrent file

> 🟢 **Resolved 2026-08-24** — the ownership defect was reproduced end to end
> on the reviewer's own scratch databases against the genuine pre-fix script,
> and the fix re-proved by connecting **as `opencircuit`** and issuing real
> queries in both dump formats.

## Review notes

Independent phase-3 re-verification of `a34dcef`. This issue's claim is
operational, so it was re-run rather than read: a real `backup.sh` →
`restore.sh` round trip on scratch databases created for this review
(`oc0228rev_*`), never the shared `opencircuit_test` and never a seeded id
(`CLAUDE.md` §8b). The pre-fix scripts were obtained with
`git show a34dcef^:scripts/db/restore.sh` into the scratchpad — no
`git stash`, no `git checkout --` on the shared tree, which mattered because
two other agents had uncommitted work in `internal/` throughout.

**Machine.** `uptime` load averages `1.57 1.63 1.66` at the start. Idle.
PostgreSQL 16.14 (Homebrew). Roles present: `brennan` (superuser),
`opencircuit` (not superuser, `CREATEDB`).

**Syntax.** `bash -n` clean on `scripts/db/restore.sh`, `scripts/db/backup.sh`,
`scripts/testdb.sh`, and on the pre-fix copies.

### Criterion 1 — reproduced, then re-proved

Source database `oc0228rev_src` created **owned by `opencircuit`** with a
`widgets` table (bigserial → a sequence), two rows, and a view. Dumped with
the real `scripts/db/backup.sh` in both formats (`BACKUP_RUN_AS=""`).

**Before the fix** (`restore-prefix.sh`, run as `brennan` ≠ `opencircuit`,
`RESTORE_CREATE=1` — the shape of production's `sudo -u postgres` path):

```
widgets|brennan
widgets_id_seq|brennan
$ psql "postgres://opencircuit@localhost/oc0228rev_before" -c 'select count(*) from widgets'
ERROR:  permission denied for table widgets
```

The restore printed `Done.` and exited 0 while the app role was locked out —
exactly the failure mode described, reproduced first-hand rather than assumed.

**After the fix**, same conditions:

```
Reassigning ownership of every table/sequence/view in 'oc0228rev_after' to 'opencircuit' ...
widgets=opencircuit   widgets_id_seq=opencircuit   widgets_v=opencircuit
select count(*)  → 2          (as opencircuit)
insert … returning id → 3     (as opencircuit)
select count(*) from widgets_v → 3
```

A real `SELECT` **and** a real `INSERT` issued **as the application role**,
not a catalog inspection — which is the method the issue insists on, because
the catalog check is the one that passed while the defect was present.

### Criterion 1 — the plain format too, and the opt-out

- **Plain `.sql.gz`**: same result — `widgets=opencircuit`, app role reads.
  Both branches of the `case` are covered because `reassign_ownership` sits
  after it, at one call site.
- **`RESTORE_OWNER=""` genuinely opts out**, checked directly rather than
  inferred from the `${VAR-default}` form: the run printed
  `RESTORE_OWNER is empty — skipping post-restore ownership reassignment.`
  and left `widgets=brennan`. The unset-only default is doing what
  `backup.sh`'s matching `BACKUP_RUN_AS` comment promises.
- **Identifier validation is real**: `RESTORE_OWNER='opencircuit"; DROP
  DATABASE x; --'` was refused with
  `ERROR: RESTORE_OWNER='…' is not a plain identifier` and `RESTORE_OWNER='bad;name'`
  exited **2**. Combined with `quote_ident()` inside the `DO` block, the
  bash-into-heredoc splice is safe.

### Criterion 5 — `testdb.sh drop`, both sides

A database `opencircuit_test_0228revdrop` was created owned by `brennan`, so
`psql_admin` (which connects as `opencircuit`) cannot drop it:

```
# pre-fix
ERROR:  must be owner of database opencircuit_test_0228revdrop
opencircuit_test_0228revdrop does not exist
exit=0                       ← and the database was still there

# fixed
ERROR:  must be owner of database opencircuit_test_0228revdrop
error: failed to drop … (see the psql error above — commonly: you are not its owner)
exit=1
```

And the genuinely-absent case still exits 0 with `… does not exist`, so the
fix did not turn a normal no-op into a failure.

### Criterion 7 — the round trip still holds, both formats

The source sequence was deliberately advanced past `max(id)`
(`setval('widgets_id_seq', 99)`) — the case the original drill flagged as
"the one that actually bites people" — then dumped and restored in both
formats:

| | rows | `last_value` | table owner | app-role `INSERT` |
|---|---|---|---|---|
| source | 2 | 99 | opencircuit | — |
| custom `.dump` | 2 | 99 | opencircuit | id `100` |
| plain `.sql.gz` | 2 | 99 | opencircuit | id `100` |

No naive `max(id)` reset, no id collision, ownership correct in both.

### Criteria 2, 3, 4, 6 — the documentation half

Present and accurate in `docs/deployment.md`: the drill's step 1 now captures
the DSN once and states plainly that `seed.sql` is a throwaway file you write
(not a repo file); step 6b demonstrates criterion 1's method — connect as the
app role, not the restoring role — inside the copy-pasteable drill; step 7
explains the remaining database-level ownership mismatch and that
`testdb.sh drop` now surfaces it loudly; and the two behaviours (idempotent
custom re-restore, plain-format abort at exit 3 into a non-empty target) are
their own bullets. The old **`tableowner` parenthetical is gone**, replaced by
"**This section previously claimed that matched `opencircuit` before and
after — that was true only because the local drill happened to run as the
`opencircuit` role, and it is false in general**", which is precisely what
the reproduction above shows.

### Two things the reviewer found that the issue did not

**1. A restore leaves the app role unable to run migrations.** Ownership of
the *objects* is fixed, but the `public` **schema** is not — it resolves to
`pg_database_owner`, i.e. the role that ran `createdb`. Measured on the
restored database:

```
public owner=pg_database_owner
$ psql "postgres://opencircuit@localhost/oc0228rev_plain" -c 'create table probe(id int)'
ERROR:  permission denied for schema public
```

The same probe succeeds on a database created normally with
`createdb -O opencircuit`. `SELECT`/`INSERT`/`UPDATE`/`DELETE` on restored
tables all work (PostgreSQL grants `USAGE` on `public` to `PUBLIC`), so the
issue's own criterion 1 — "the app role can read and write" — genuinely
holds and this does not block resolution. But the first `migrate up` after
an incident restore would fail, and the deploy runbook's redeploy procedure
runs migrations. The issue's `## Implementation notes` say "the app only
needs to read/write the *objects*, which the fix now guarantees" — that is
true of steady-state serving and not true of the deploy path. Worth its own
issue: either `ALTER DATABASE … OWNER TO` (needs the new owner to hold
`CREATEDB`) or an `ALTER SCHEMA public OWNER TO`/`GRANT CREATE ON SCHEMA
public` in `reassign_ownership`. Reported to the orchestrator rather than
filed here, per `CLAUDE.md` §9.

**2. The `docs/deployment.md` half of this issue is not in `a34dcef`.** The
`## Implementation notes` describe updating `docs/deployment.md` (step 1,
step 6b, the `tableowner` correction, step 7, the two behaviours) but
`git show a34dcef --stat` lists only `issues/0228.md`, `scripts/db/restore.sh`
and `scripts/testdb.sh`. Those doc edits were left unstaged and were swept
into `#0229`'s commit `e3d7920` instead — confirmed with
`git show e3d7920 -- docs/deployment.md`, which contains `6b. Prove ownership
the way that actually catches the defect (#0228)` verbatim. **All the content
is present and correct in the tree**, so nothing is lost and this is not a
functional defect; the attribution is wrong, which is exactly the shared-index
hazard `CLAUDE.md` §8a describes. The **Commit** row above names both hashes
rather than pretending one commit carries the change.

**Minor, not blocking.** `reassign_ownership` covers tables, sequences and
views but not materialized views, types, or functions — none of which
`migrations/` creates (`grep` for `CREATE TYPE|MATERIALIZED|FUNCTION|EXTENSION`
in `migrations/` returns nothing), so the omission is currently unreachable.
Also, `RESTORE_OWNER` validation happens inside `reassign_ownership`, i.e.
*after* the restore has already run, so a malformed value costs a full
restore before exiting 2; validating at the top of the script would be
cheaper. And `testdb.sh reset` now inherits `drop`'s non-zero exit under
`set -e`, so a reset of a foreign-owned database stops instead of silently
recreating — a behaviour change, and the correct one.

**Cleanup.** Every scratch database created by this review was dropped
(`oc0228rev_src`, `_before`, `_after`, `_plain`, `_noown`, `_rt_dump`,
`_rt_sql`, `opencircuit_test_0228revdrop`). Final census of
`opencircuit_test_*`: only `opencircuit_test_template` and another agent's
`opencircuit_test_0227b`, which was left alone. `pgrep -fl 'go test|\.test
|pg_dump|pg_restore'`: none.

## Notes

Reported by `#0062`'s phase-3 review under `CLAUDE.md` §9.

Criterion 1's method is the point. Checking `pg_tables.tableowner` after a local
restore is exactly the check that passed while the defect was present, because
the local drill runs as the owning role. **Connect as the application role and
issue a query** — that is the only test that would have failed.

Note the review deliberately left `#0062` resolved with its S3 criteria unticked
(see `#0229`), so this issue is about the path that *was* verified, not the one
that was not.

`#0062`'s `## Review notes` carry the full snapshot-diff method — row counts,
`pg_sequences` including `is_called`, every `pg_get_constraintdef()` and
`indexdef` — which is the regression suite for criterion 7.

## Implementation notes

**Root cause confirmed exactly as described.** `backup.sh` dumps with
`pg_dump --no-owner --no-privileges`; `restore.sh` restored with no
ownership-fixing step of its own, so every object ended up owned by whichever
role ran `pg_restore`/`psql` — `opencircuit` locally (invisible), `postgres`
on the documented production path (`sudo -u postgres`, per `restore.sh`'s
`BACKUP_RUN_AS` default) — with the app role locked out.

**Fix — `scripts/db/restore.sh`:** after the restore completes (both the
custom-format `pg_restore` branch and the plain-format `gunzip | psql`
branch — one call site, after the `case`), a new `reassign_ownership()`
function walks every table, sequence, and view in every non-system schema of
the target database and `ALTER … OWNER TO`s each one to a new `RESTORE_OWNER`
variable (default `opencircuit`, matching `scripts/db/create.sql`'s bootstrap
role; unset-only default via `${RESTORE_OWNER-opencircuit}`, same pattern as
the existing `BACKUP_RUN_AS` fix from `#0062`, so `RESTORE_OWNER=""`
genuinely skips the step rather than silently falling back). Because
ownership implies full privileges on an object in PostgreSQL, this also
restores what `--no-privileges` stripped, with no separate `GRANT` needed.
`RESTORE_OWNER` is validated against `[A-Za-z0-9_]+` before use (rejecting
anything else with exit 2) and additionally passed through SQL's
`quote_ident()` before being used in `ALTER … OWNER TO`, so it is safe even
though it is spliced into generated SQL text.

**A real implementation snag, worth recording:** the first version of
`reassign_ownership()` tried to pass `RESTORE_OWNER` into the `DO $$ … $$`
block via `psql -v owner=... ` and referenced it as `:'owner'` inside the
block body. That does not work — verified empirically, not assumed: psql's
`:'var'` interpolation does **not** reach inside a `$$`-quoted string (its
lexer treats the dollar-quoted body as opaque text it does not scan for
`:name` tokens), so every attempt raised `syntax error at or near ":"`. The
fix uses an *unquoted* heredoc with bash substituting the (already-validated)
`$owner` directly into the SQL text, with `$$` escaped to `\$\$` so bash
doesn't read it as this shell's PID.

**Fix — `scripts/testdb.sh drop`:** the old `db_exists && DROP DATABASE &&
echo dropped || echo "does not exist"` chain fell into the same `||` branch
whenever the `DROP DATABASE` failed for *any* reason — not just
non-existence — so a database that exists but is owned by a different role
(exactly what a `restore.sh RESTORE_CREATE=1` run produces at the *database*
level; see below) printed the misleading "does not exist" and exited `0`,
leaving the stray database behind. Existence and drop-success are now checked
separately; a real drop failure prints the psql error, an explanatory
message, and exits `1`.

**One more thing worth naming (not required by the acceptance criteria, but
adjacent):** `RESTORE_OWNER` reassigns ownership of the tables/sequences/views
*inside* the target database, not the database object itself. A database
`restore.sh` creates via `RESTORE_CREATE=1` is still owned, at the database
level, by whoever ran `createdb` (the `BACKUP_RUN_AS` role) — reassigning
that too would need the new owner to hold `CREATEDB` (or superuser), which
`opencircuit` is not granted in `scripts/db/create.sql`. Left alone
deliberately: the app only needs to read/write the *objects*, which the fix
now guarantees; `testdb.sh drop`'s honest failure (above) is what makes the
remaining database-level mismatch visible instead of silently lying.

**`docs/deployment.md` updated:** the drill's step 1 (double `create` call,
phantom `seed.sql` reference) is fixed to capture the DSN once; the
`tableowner` claim's parenthetical is corrected and replaced with the general
case, the fix, and how it was proven; a new step 6b demonstrates criterion
1's method directly in the copy-pasteable drill (connect as `opencircuit`,
not the restoring role); step 7's comment explains why `testdb.sh drop` may
still fail on a `RESTORE_CREATE=1` database's *database*-level ownership, and
that this is now surfaced loudly instead of silently; both undocumented
behaviours (idempotent custom re-restore; plain-format abort at exit 3 into a
non-empty target) are now stated as their own bullets rather than buried in
a sequences aside.

## Verification

Local PostgreSQL 16.14 (Homebrew). `uptime` before starting: `load averages:
2.63 2.58 2.66` (idle). This claim rests on **local Postgres**, not production
infrastructure — see the caveats below for what is not yet proven on the real
`sudo -u postgres` path (`CLAUDE.md` §10).

**Criterion 1, proved by a real dump/restore round trip against a scratch
database, not asserted:**

1. Created `opencircuit_0228src` owned by `opencircuit` (matching
   `create.sql`'s bootstrap), with a throwaway `widgets` table + rows.
2. Backed it up with the real `scripts/db/backup.sh` (`BACKUP_RUN_AS=""`,
   custom format).
3. **Before fix**: restored with the pre-fix `restore.sh` (`git show
   HEAD:scripts/db/restore.sh`, run from a scratch copy — the tracked file
   was never reverted) as `brennan` (a role distinct from `opencircuit`,
   standing in for production's `postgres`, `BACKUP_RUN_AS=""`,
   `RESTORE_CREATE=1`). Result: `tableowner` = `brennan`; connecting **as
   `opencircuit`** and running `select count(*) from widgets` returned
   `ERROR: permission denied for table widgets` — the exact failure mode the
   issue describes, reproduced, not assumed.
4. **After fix**: repeated into a fresh target with the fixed `restore.sh`.
   Result: table and sequence owner = `opencircuit`; connecting as
   `opencircuit` both `SELECT`ed (`count = 2`) and `INSERT`ed
   (`RETURNING id` succeeded) — a real query, not a catalog inspection,
   satisfying criterion 1's stated method exactly.
5. Repeated the "after" case with a **plain-format** (`.sql.gz`) dump —
   same result: owner reassigned, app role reads and writes.
6. Confirmed `RESTORE_OWNER=""` opts out cleanly: ownership stayed with the
   restoring role, no reassignment attempted, no error.
7. Confirmed the two "undocumented behaviours" still hold with the fix in
   place: a second custom-format restore over the same target came back to
   the dump's row count (idempotent, `--clean --if-exists`) with ownership
   correct again; a plain-format restore into that same now-populated target
   aborted with `ERROR: relation "widgets" already exists`, exit `3`, target
   row count unchanged (fails safe).
8. `testdb.sh drop` bug reproduced and fixed: created
   `opencircuit_test_0228dropbug` owned by `brennan`, ran the pre-fix
   `testdb.sh drop 0228dropbug` — printed "does not exist" and exited `0`
   despite the database still existing (confirmed via a follow-up catalog
   query). Ran the fixed `testdb.sh drop 0228dropbug` — printed the real
   `ERROR: must be owner of database` plus an explanatory message and exited
   `1`.
9. Cleanup: every scratch database created this session
   (`opencircuit_0228src`, `…dst_before`, `…dst_after`, `…plain_after`,
   `…noowner`, `opencircuit_test_0228dropbug`) dropped by hand as their
   actual owner (`brennan`, a local superuser). Final census: only
   `opencircuit_test_template` remains among `opencircuit_test_*`/`*0228*`
   names. `pgrep -fl 'pg_dump|pg_restore|psql'` at the end: none.

**Commands:** `bash -n` and `shellcheck` clean (exit 0) on
`scripts/db/restore.sh` and `scripts/testdb.sh` (the latter's two pre-existing
`shellcheck` style notes, lines 48 and 160, are outside the `drop` case this
issue touched and were not introduced by this change).

**`ISSUE=0228 scripts/check.sh go ./internal/...`:** the `internal/db` package
(which owns the `CLAUDE.md` §11 / PRD.md guard this issue's PRD edit — done
under `#0229` — must satisfy) passed clean: `go test ./internal/db/... -p 2`
→ `ok`, zero skips, both `TestPRDSectionIndexParity` and
`TestPRDSectionIndexTotalMatchesFile` green. **The whole-repo `go vet ./...`
step inside `check.sh` failed**, but not from anything in this diff: the
failure is `internal/handlers/citation_target_guard_test.go:4:2: "go/ast"
imported and not used` — an **untracked file from a different, concurrently
running agent** (confirmed via `git status --porcelain` before and after;
that file is not part of this issue's diff and `internal/handlers` is
explicitly out of this issue's scope per the dispatch instructions).
Confirmed the narrower, unaffected signal directly: `go vet
./internal/db/...` and `go build ./...` both pass with no output. Scratch
test database `opencircuit_test_0228` (created by `check.sh`) did not
survive the run — `scripts/testdb.sh drop 0228` reported "does not exist"
afterward, consistent with `check.sh`'s own on-exit cleanup.

**Not verified here — rests on real infrastructure that does not exist yet
(`CLAUDE.md` §10):**

- The actual production restore path, `sudo -u postgres pg_restore`. This
  drill used a distinct non-superuser local role (`brennan`, who is a local
  superuser via Homebrew's peer-auth default) standing in for `postgres` to
  reproduce the *shape* of the defect (restoring role ≠ app role), not a real
  `sudo -u postgres` invocation on Amazon Linux. `#0062`'s runbook already
  flagged this specific path as "read, not executed"; that remains true.
- Whether `opencircuit`'s actual production password/connection details will
  behave identically — this drill's "app role" checks connected as the real
  `opencircuit` role locally with its real local password.

## Relation

- Found by: [#0062](0062.md) — the backup/restore verification
- Relates to: [#0064](0064.md) — the deployment runbook this feeds
- Relates to: [#0229](0229.md) — the S3 criteria left unmet

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Token/cache/cost columns left for the orchestrator to fill from this
subagent's transcript, per `issues/Issues.md`'s "Usage is recorded per
phase" — this subagent does not have access to its own dispatch-level usage
accounting.
