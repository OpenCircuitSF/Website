# 0014 — Add a History API path router

| | |
|---|---|
| **Status** | open |
| **Module** | web |
| **Platform** | All |
| **Phase** | 2 — Brand and marketing pages |
| **First seen** | 2026-08-18 |
| **Commit** | 2ab9597, f8d1f09 |

## Description

Replace the `currentView` store navigation with real URL routing. Public pages get shared and indexed, so `/workshops/soldering-101` has to be an actual address.

## Acceptance criteria

- [x] `web/src/lib/router.ts` parses `window.location.pathname` into a typed `Route` union — *verified at review 2026-08-18*
- [x] Same-origin `<a>` clicks are intercepted and pushed with `pushState`; modified clicks (cmd/ctrl/shift, `target=_blank`, external hosts) fall through to the browser — *verified at review: a plain nav click produced zero navigation requests and changed the address bar; a cmd-click opened a second tab and left the current page's URL alone*
- [x] `popstate` restores the correct route on back/forward — *verified at review: back from `/workshops` re-rendered About at `/about`, forward re-rendered Workshops, both without a document load*
- [x] One path parameter supported: `/workshops/:slug` — *verified at review*
- [x] Unmatched paths resolve to a `not-found` route — *verified at review*
- [x] Query parameters are parsed and exposed (needed by `/confirm?token=`, `/preferences?token=`) — *verified at review: `/confirm?token=abc123&x=1` routed to `confirm` with the query preserved*
- [ ] Scroll position resets to top on navigation, except on back/forward where it restores
      — *partially met by `f8d1f09` and re-verified at review 2026-08-18 in a
      real headless Chromium (1280×800) against the Go binary serving the
      production build: the reset-to-top half and the **Back** half both work,
      including three history entries deep (Back restored 300 then 400 from a
      Home→About→Workshops trail). **Forward** does not restore, and the
      `pagehide` path saves an offset nothing ever reads — see
      `## Review notes` for the transcripts and the suggested fix.*
- [x] No routing library added — the implementation is plain TypeScript, roughly 60 lines — *verified at review: no dependency added; `router.ts` is 235 lines of which ~85 are code, the rest rationale comments*
- [x] Unit tests cover path parsing, param extraction, and the click-interception predicate

## Root cause

Not a bug fix — this is the deliberate architectural departure from ShortLinks
that PRD §3.4 calls for. ShortLinks' `currentView` store had no concept of a
URL at all; a signed-in dashboard doesn't need one, but marketing pages that
get shared on Discord/social and need to be indexable absolutely do.

## Fix

Added `web/src/lib/router.ts`: `parsePath(pathname, search)` parses a path into
a typed `Route` (`{ name, path, params, query }`) against a static route table
mirroring PRD §5.1 plus the Phase 1 auth routes, with one dynamic pattern
(`/workshops/:slug`). `shouldIntercept(candidate)` is the click-interception
predicate, factored out as a pure function over a plain `InterceptCandidate`
object (button/modifier keys/target/download/href/origin) rather than a real
`MouseEvent`, so it is unit-testable without a DOM. `navigate(path, opts)`
push/replace-state's, updates the `currentRoute` store, and resets scroll.
`initRouter()` is the only DOM-touching piece — it wires a document-level click
listener and a `popstate` listener and is idempotent.

`currentView` and its `View` type were removed from `stores.ts` entirely (not
just unused) — the issue calls for retiring it as a navigation mechanism, and
leaving it in place as dead code some file could still import would recreate
exactly the divergence the issue is fixing. `pendingVerifyToken` and
`currentUser` stay, since those are genuinely transient UI/session state, not
navigation.

Every existing consumer of `currentView` was migrated in the same commit
(broader than a narrow reading of the acceptance criteria, but retiring the
store while five views still imported it would fail `npm run check`/`vite
build` on the missing export): `App.svelte`'s onMount now reads the
already-parsed `currentRoute` to detect the
register-verify/recover-verify magic-link landings (same behavior as before:
capture `?token=`, strip it via a replace-navigation, skip the `/api/me`
probe) and otherwise calls `getMe()` unconditionally to populate `currentUser`
for every other route. `Login.svelte`, `Account.svelte`, `Admin.svelte`,
`RegisterVerify.svelte`, and `RecoverVerify.svelte` had their
`currentView.set(...)` navigation calls replaced with `navigate('/...')` and
their `$currentView === 'login'` read replaced with `$currentRoute.name ===
'login'`.

Public marketing routes (home/about/workshops/subscribe/confirm/preferences/
unsubscribe) currently render a one-line placeholder naming the route in
`App.svelte`'s catch-all branch — intentional for this commit, so the router
is independently buildable/testable before `Home`/`About`/`NotFound` exist.
`#0017` adds the header/footer shell and `#0015`/`#0016`/`#0022` replace the
placeholder with real views for `home`/`about`/`not-found` in the following
commits; the remaining routes stay placeholders until their own later-phase
issues (`#0029`–`#0036`, `#0053`–`#0054`).

### `f8d1f09` — scroll restoration (bounce fix)

Reviewed at 2026-08-18 (phase 3) and bounced: the reset-to-top half worked but
back/forward did not restore scroll, because the browser's own
`history.scrollRestoration = 'auto'` fires on `popstate` before Svelte has
re-rendered the restored view, so it restores against the *outgoing* view's
(shorter) DOM and clamps to 0. Fixed exactly per the reviewer's prescription
in `## Review notes`:

1. `initRouter()` now sets `history.scrollRestoration = 'manual'`
   (feature-detected via `'scrollRestoration' in window.history`) so the
   browser never attempts — and loses — that race in the first place.
2. `navigate()` calls a new `saveScrollToCurrentEntry()` helper, which
   `replaceState`s the *outgoing* history entry's state with
   `{ scrollY: window.scrollY }` immediately before every `pushState`/
   `replaceState` it performs. The new entry itself is stamped
   `{ scrollY: 0 }`, matching the reset-to-top behavior for forward
   navigation. The same helper is wired to `pagehide` in `initRouter()`, to
   cover leaving the SPA without going through `navigate()` at all (a
   same-tab navigation away and back).
3. The `popstate` handler reads `event.state.scrollY` (defaulting to `0` if
   absent, e.g. the very first history entry) and applies it with
   `window.scrollTo(0, saved)` only after `await tick()` — giving Svelte's
   reactive `{#if $currentRoute.name === ...}` block in `App.svelte` a chance
   to swap in the restored view's markup first, so the page actually has the
   height to scroll to.

No routing library added, no change to `parsePath`/`shouldIntercept`/the
route table — additive to `navigate()`/`initRouter()` only, as scoped.

## Verification

```
cd web && npm run check 2>&1 | tail -20   # svelte-check: 187 files, 0 errors, 0 warnings, 0 files with problems
npm test 2>&1 | tail -20                  # vitest: 7 files, 103 tests passed
npm run build 2>&1 | tail -10             # vite build: 155 modules, succeeded
cd .. && go build ./... 2>&1 | tail -20   # exit 0, no output
```

(`go build` only, per this pass's brief — the concurrent `#0019`/`#0020` work
in `internal/` was mid-flight and out of this pass's lane; `internal/seo/` was
untracked at the time.)

`npm run build` regenerates the real `web/dist/index.html`/`assets/*`, which
overwrites the committed placeholder — restored afterward by rewriting the
file back to `git show HEAD:web/dist/index.html`'s exact content (not `git
checkout --`, per `CLAUDE.md` §8a); `git status --porcelain
web/dist/index.html` confirmed empty afterward. Twice, actually: the
concurrent `#0019` agent's own `npm run build` runs also touch this same
shared file mid-pass (it now carries their `%%OC_*%%` SEO-injection template
tokens in `web/index.html`, the vite source, which is their change and out of
this pass's scope) — each time the placeholder needed restoring again before
this issue's own build/commit steps.

**Real-browser proof (this is the part that actually discharges the bounce).**
jsdom has no layout and reports `scrollY` as `0` unconditionally, so a
vitest-only test cannot prove this; `router.test.ts` is unchanged by this
pass; the fix is proved with an actual headless Chromium (`playwright-core`
driving the cached "Chrome for Testing" binary at
`~/Library/Caches/ms-playwright/chromium-1234`, launched with `newPage({
viewport: { width: 1280, height: 800 } })`, never `--window-size`, per
`CLAUDE.md` §8) against the real Go binary serving the real `npm run build`
output (`STORAGE=json`, port 8099):

```
Home body.scrollHeight = 1346
After scrolling Home, window.scrollY = 546
After clicking About, window.scrollY = 0 (expect 0 -- scroll reset)
Document-load requests to /about during click nav: 0 (expect 0 -- SPA nav, no reload)
After Back, location.pathname = /
After Back, window.scrollY = 546 (expect close to 546 )
RESULT: PASS -- scroll restored on Back
After Forward, location.pathname = /about window.scrollY = 0 (expect 0, About was never scrolled)
```

One deliberate deviation from a literal `page.click()`: Playwright's
synthetic click auto-scrolls its target into view before dispatching, and
since `Header.svelte`'s nav is not `position: sticky`/`fixed`, that auto-scroll
itself reset `window.scrollY` to `0` *before* `router.ts`'s click handler ever
ran, contaminating the measurement (this was caught by an intermediate debug
run that instrumented `history.replaceState`/`pushState` and found the very
first `replaceState` call already saw `scrollY: 0`). The proof instead
dispatches the click via `el.dispatchEvent(new MouseEvent('click', {
bubbles: true, cancelable: true, button: 0 }))` in-page — still a real
Chromium DOM event through the real `document.addEventListener('click', ...)`
listener `initRouter()` registers, just without Playwright's viewport-homing
side effect.

**Mutation-tested, not just green — against the fix that discharges the
bounce, not the previously-passing parts.** Commented out the
`window.scrollTo(0, saved)` call inside the `popstate` handler's
`tick().then(...)`, rebuilt (`npm run build`, `go build`), reran the exact
same browser proof against a freshly started server:

```
After Back, location.pathname = /
After Back, window.scrollY = 0 (expect close to 546 )
RESULT: FAIL -- scroll NOT restored on Back
```

This is precisely the reviewer's reproduction from the bounce (`Home
re-renders, scrollY is 0, not ~600`). Reverted the mutation, rebuilt, reran —
green again (`window.scrollY = 546`, `PASS`), matching the transcript above.

**What this does and does not cover.** Covered, live in a real browser:
`scrollRestoration = 'manual'` taking effect, the outgoing entry's scroll
offset being captured via `replaceState` before a `pushState` navigation, the
`popstate` handler reading it back off `event.state` and applying it after
`tick()`, and the reset-to-0-on-forward-push behavior (unchanged from before
this pass). **Not covered by this pass:** the `pagehide` listener (leaving the
SPA without calling `navigate()` — e.g. closing the tab or a same-tab
navigation to an external URL and back) was not exercised in a live browser,
only by code inspection; multiple consecutive Back/Forward presses across
more than two entries; and scroll restoration interacting with the still-
placeholder public routes (workshops/subscribe/confirm/preferences/
unsubscribe), which currently render too little content to scroll at all so
there was nothing meaningful to prove there yet. `initRouter()`'s click
interception and `parsePath`/`shouldIntercept` unit-test coverage from the
original pass (`router.test.ts`, 22 tests) is untouched by this fix and still
holds — see the original mutation test below.

**Original pass's mutation test (unchanged, still valid).** Broke the
`/workshops/:slug` regex (`WORKSHOP_DETAIL`) to require an extra literal
segment; `router.test.ts` failed 3 of 22 (`parsePath('/workshops/x').name`
expected `'workshop-detail'`, got `'not-found'`, plus the slug-extraction and
decode tests). Reverted and re-ran green. This proves the dynamic-parameter
test actually exercises the regex rather than a tautology.

**What is verified automatically:** `parsePath` against every static route in
PRD §5.1 plus the auth routes, the `/workshops/:slug` extraction and
URL-decoding, trailing-slash normalization, root-path handling, query-string
parsing (single and multiple params, and the empty case), and every branch of
`shouldIntercept` (button, all four modifier keys, `target=_blank` vs.
`_self`, `download`, cross-origin, `mailto:`/`tel:`, already-prevented,
same-origin relative href).

**What is verified by rendering, not by unit test:** `initRouter()` itself —
the real `document`/`window` click and `popstate` listeners, `pushState`
actually changing the address bar, and scroll reset/restore behavior (now
covered live, see above). **Deep links on first load**: confirmed by
inspecting `currentRoute`'s initializer, which calls
`parsePath(window.location.pathname, window.location.search)` at module load
time — the same function the unit tests cover — so a hard refresh on
`/workshops/some-slug` parses correctly before any click ever happens. Not
confirmed with a live hard-refresh in a browser in this pass; that is exactly
what `internal/handlers`'s `SPAHandler` (unchanged by this issue, addressed by
`#0022`) needs to keep serving `index.html` for.

## Files changed

- `web/src/lib/router.ts` — new; the router itself (see Fix)
- `web/src/lib/router.test.ts` — new; 22 unit tests
- `web/src/lib/stores.ts` — removed `currentView`/`View`; kept `currentUser`, `pendingVerifyToken`
- `web/src/App.svelte` — routes via `currentRoute`/`initRouter`/`navigate` instead of `currentView`; ported the magic-link token-capture logic onto the route-driven flow
- `web/src/views/Login.svelte` — `currentView.set('account')` → `navigate('/account')`; `$currentView === 'login'` → `$currentRoute.name === 'login'`
- `web/src/views/Account.svelte` — `currentView.set(...)` → `navigate(...)` at all four call sites (auth-error redirect, admin-tab `go()`, sign-out, sign-out-everywhere)
- `web/src/views/Admin.svelte` — same pattern, three call sites (auth-error redirect, account-tab `go()`, sign-out)
- `web/src/views/RegisterVerify.svelte` — `history.replaceState(...); currentView.set('account')` → `navigate('/account')`; `goToLogin()` → `navigate('/login')`
- `web/src/views/RecoverVerify.svelte` — same as RegisterVerify

`f8d1f09` (this pass, scroll-restoration bounce fix):

- `web/src/lib/router.ts` — `initRouter()` sets `history.scrollRestoration =
  'manual'` and adds a `pagehide` listener; `navigate()` calls a new
  `saveScrollToCurrentEntry()` helper before every push/replace; the
  `popstate` handler restores the saved offset after `tick()`. No other file
  touched — additive to this one file, as scoped by the bounce.

## Gotchas

`npm run build` overwrote the committed placeholder `web/dist/index.html`
(tracked deliberately per `.gitignore`'s `web/dist/*` / `!web/dist/index.html`
so `go build ./...` has something to embed pre-deploy). Restored it by
rewriting the file back to its exact committed content (`git show
HEAD:web/dist/index.html`) rather than `git checkout --`, per `CLAUDE.md` §8a.
`git status` confirmed zero diff afterward.

Migrating the five Phase 1 auth views off `currentView` was not explicitly
required by this issue's acceptance criteria, but was necessary: the issue's
own Notes say `currentView` "is retired as a navigation mechanism," and
leaving those five files importing a deleted export fails `npm run check`
immediately. This is flagged here rather than silently expanded scope.

**(This pass, scroll-restoration bounce fix.)** `npm run build` overwrote
`web/dist/index.html` again, same as the original pass, and had to be
restored the same way (rewrite to `git show HEAD:...`'s content, never `git
checkout --`). This time there was a wrinkle: the concurrent `#0019`/`#0020`
agent working `internal/seo/`/`internal/handlers/` is also running `npm run
build` against `web/index.html` (its own in-progress edit, adding
`%%OC_TITLE%%`-style SEO injection tokens) — every so often `web/dist/
index.html` on disk carried their build output instead of mine, or theirs
again after I'd just restored the placeholder. Handled by re-reading the file
before each rewrite and re-checking `git status --porcelain
web/dist/index.html` is empty afterward, rather than assuming my last write
stuck; this is a preexisting shared-file hazard from the dist/index.html
placeholder-tracking convention (`#0071`-adjacent, not something this pass's
fix changes), not a defect in the fix itself.

Playwright's `page.click()` was the wrong tool for the real-browser proof:
its actionability check auto-scrolls the click target into view first, and
since the header nav is not sticky, that silently zeroed `window.scrollY`
before `router.ts`'s own click handler ran, producing a false failure that
looked exactly like the original bug. Diagnosed by instrumenting
`history.replaceState`/`pushState` via `page.addInitScript` and finding
`scrollY: 0` already on the very first `replaceState` call. Fixed by
dispatching the click in-page (`element.dispatchEvent(new MouseEvent(...))`)
instead — still exercises the real listener, just without Playwright's
viewport-homing side effect. Worth remembering for any future browser proof
against a page with a non-sticky header and content taller than the
viewport.

## Notes

PRD §3.4, §7.2. ShortLinks writes to a `currentView` store with no URL router; that is fine for a signed-in dashboard and wrong for a marketing site.

The magic-link landing behaviour from `App.svelte` must survive the rewrite: `/register/verify` and `/recover/verify` read `?token=`, stash it in `pendingVerifyToken`, strip it from the URL with `history.replaceState`, and route to the passkey view *without* calling `/api/me` first. A user opening an email link has no session; bouncing them to login loses the token.

The `currentView` store is retired as a navigation mechanism but stores stay for transient UI state.

## Review notes — 2026-08-18 (phase 3, bounced a second time)

`f8d1f09` fixes the Back direction — that half is real, reproduced, and
mutation-tested. It does not fix the Forward direction, and the `pagehide`
listener it adds is write-only. The acceptance criterion names both
directions, so it stays unticked.

**How this was re-verified.** Real headless Chromium ("Chrome for Testing"
1234) via `playwright-core`, `newPage({ viewport: { width: 1280, height:
800 } })` — a viewport override, never `--window-size`, per `CLAUDE.md` §8 —
driving the real Go binary serving the real `npm run build` output on
`:8099` (`STORAGE=json`). Clicks are dispatched in-page
(`el.dispatchEvent(new MouseEvent('click', …))`) rather than via
`page.click()`, because Playwright's actionability auto-scroll zeroes
`window.scrollY` before the router's listener runs on a page with a
non-sticky header — the trap the implementer documented under `## Gotchas`,
confirmed here and avoided the same way. The entire build/serve/mutate cycle
ran in a throwaway `git worktree` checked out at `f8d1f09`, so the
concurrent `#0019`/`#0020` agent's in-flight `internal/`, `web/index.html`
and `web/dist/index.html` were never built into, restored, or otherwise
touched.

### Defect 1 — Forward does not restore scroll (blocking)

`saveScrollToCurrentEntry()` is called from exactly two places: inside
`navigate()`, and on `pagehide`. Neither fires when the user leaves an entry
via a **history traversal**. So an entry entered by `pushState` keeps the
`{ scrollY: 0 }` it was stamped with at push time for as long as the user
only ever leaves it with Back/Forward, and returning to it replays that
stale `0`.

The everyday gesture that exposes it — scroll a page, Back, Forward — is a
first-class browser interaction, and it lands the user at the top of a page
they were halfway down. Live transcript against the shipped, unmutated
build:

```
1. load /                      scrollY=0    state=null
2. click About (push)          path=/about  scrollY=0    state={"scrollY":0}
3. scroll About to             scrollY=500  state={"scrollY":0}   <-- entry state NOT updated
4. Back                        path=/       scrollY=0    state={"scrollY":0}
5. Forward                     path=/about  scrollY=0    state={"scrollY":0}   <-- expected 500
   >>> FAIL -- forward navigation does not restore scroll
```

The same root cause produces a second visible symptom: Back into an entry,
scroll it, Forward, then Back again — the second Back replays the offset
saved by the last `navigate()`, not where the user actually was.

```
Back to /, scrollY = 200
re-scrolled / to 546 without navigating
Forward to /about
Back to / again, scrollY = 200   <-- expected 546
```

Note that `f8d1f09`'s own proof, and this reviewer's first cut of it, both
passed the Forward check only because About had never been scrolled —
`expect 0` is trivially satisfied by a handler that restores nothing. Any
Forward assertion has to scroll the forward entry first or it proves
nothing.

**Suggested fix.** Stamp the current entry continuously instead of only on
the way out: a passive, rAF- or timeout-throttled `scroll` listener in
`initRouter()` calling `saveScrollToCurrentEntry()`. Stamping inside the
`popstate` handler does not work — by the time it fires the traversal has
already committed and `history.replaceState` would write to the *incoming*
entry. A throttled scroll listener subsumes the `pagehide` listener as well.

### Defect 2 — the `pagehide` save is write-only (blocking, and a small regression)

`initRouter()` sets `history.scrollRestoration = 'manual'` unconditionally,
which correctly stops the browser losing the race on same-document
`popstate`. But it also switches off the browser's native restoration for
**cross-document** history traversal — leaving the site and pressing Back —
and nothing in the router replaces it, because restoration only ever happens
in the `popstate` handler, and `popstate` does not fire on a fresh document
load. The `pagehide` listener dutifully writes the offset into the entry and
no code path ever reads it back:

```
/ scrolled to 546
history.state before leaving: null
full-load to /about, scrollY = 0
Back to /, scrollY = 0            <-- user left it at 546
history.state after Back: {"scrollY":546}    <-- the value was saved...
history.scrollRestoration now: manual        <-- ...and nothing reads it
```

Before `f8d1f09`, `scrollRestoration` was `'auto'` and the browser handled
this path natively, so on this one path the fix is a net regression. Real
Chrome/Safari will usually mask it via bfcache (which preserves scroll
independently of `scrollRestoration`); bfcache is not guaranteed, and
headless CDP disables it, which is how it surfaced here.

**Suggested fix.** In `initRouter()`, after the `scrollRestoration = 'manual'`
line, read `window.history.state?.scrollY` and — if it is a number — apply it
after `tick()`, the same way the `popstate` handler does. That is what makes
the `pagehide` write meaningful. The implementer listed this path as
"verified by code inspection only"; the inspection was optimistic.

### What was verified and holds — do not re-litigate these next pass

- **Back restores, including three-deep.** Home scrolled to 400 → About
  scrolled to 300 → Workshops, then two consecutive Backs restored 300 and
  then 400 exactly. The `replaceState` scheme does *not* degrade across more
  than two entries; this was the reviewer's main worry about the approach and
  it is unfounded.
- **Mutation-tested.** Commenting out `window.scrollTo(0, saved)` in the
  `popstate` handler, rebuilding and re-running turned 4 checks red
  (`after Back … got 0, expected 546`; `Back #1 … got 0, expected 300`;
  `Back #2 … got 0, expected 400`; `Forward #1 … got 0, expected 300`) and
  reverting turned them green again. The Back-direction proof is not
  tautological.
- **No collateral damage from `replaceState`.** `router.ts` is the only
  writer of `history.state` anywhere in `web/src` (`grep -rn "history\."`
  returns hits in no other file), so stamping `{ scrollY }` cannot clobber
  state another module keeps. `history.length` advances by exactly 1 per
  `pushState` navigation and by 0 for a replace-navigation — no spurious
  entries.
- **`#0022` is undisturbed.** `/definitely-not-a-route` still returns HTTP
  404 with the NotFound view rendered, `/workshops/soldering-101` still
  returns 200 with the path preserved, and a client-side navigation to an
  unknown path renders NotFound and correctly saves/restores the scroll of
  the entry it came from.
- **SPA navigation is still document-load-free** (0 document requests during
  a nav click), and scroll still resets to 0 on forward `pushState`
  navigation.
- **Suites re-run at review:** `npm run check` → 187 files, 0 errors, 0
  warnings; `npm test` → 7 files, 103 tests passed; `router.test.ts` alone →
  22 passed.
- **Scope is clean.** `f8d1f09` touches `web/src/lib/router.ts` and nothing
  else — no `internal/`, no `web/index.html`, no other agent's issue files.

### Note for Phase 6 (`#0054`, workshop detail routes)

Restoration is applied after a single `await tick()`. That is sufficient for
the current views, which render synchronously from data already in the
bundle. A view that fetches its own content — a workshop detail page loading
a workshop — will still be short when `tick()` resolves, so the restore will
clamp against a document that has not grown yet, and Back into it will land
at the top. Whoever builds `/workshops/:slug` should either reserve the
final height before paint or re-apply the offset once the fetch settles.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §3.4 — Deviations from ShortLinks' architecture · 20 lines
  `sed -n '/^### 3\.4 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0003](0003.md) — Strip the shortener views and helpers from the SPA
- Depends on: [#0011](0011.md) — Define the terminal design tokens in app.css
- Blocks: [#0015](0015.md) — Build the Home view
- Blocks: [#0016](0016.md) — Build the About view
- Blocks: [#0017](0017.md) — Build the site header, navigation, and footer
- Blocks: [#0019](0019.md) — Inject per-route meta tags server-side
- Blocks: [#0020](0020.md) — Serve sitemap.xml and robots.txt
- Blocks: [#0022](0022.md) — Build the 404 Not Found view
- Blocks: [#0029](0029.md) — Build the SubscribeForm component
- Blocks: [#0030](0030.md) — Build the confirmation flow
- Blocks: [#0031](0031.md) — Build the preference center
- Blocks: [#0034](0034.md) — Build the one-click unsubscribe endpoint
- Blocks: [#0036](0036.md) — Build the unsubscribe views
- Blocks: [#0053](0053.md) — Build the public workshops index
- Blocks: [#0054](0054.md) — Build the workshop detail view
