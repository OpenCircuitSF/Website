# 0383 — `TestAdminImports_Revoke_RequiresReason` cannot distinguish the handler's check from the store's

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | handlers |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-01 |
| **Commit** | `69c8c75` |
| **Closed** | 2026-09-02 |

## Resolution notes

> 🟢 Resolved 2026-09-02 — option 2 was the right call, and the corrected
> doc comment is both true and complete. Re-derived the two-layer premise by
> mutation on a private database: removing the handler's `reason == ""` check
> alone leaves `TestAdminImports_Revoke_RequiresReason` — and the whole
> `internal/handlers` package — green; removing the store's check as well turns
> it 404, so the behaviour is genuinely pinned and the layer genuinely is not.

## Description

`AdminImportsHandler.Revoke`'s `reason == ""` 400 and
`subscribers.ImportStore.Revoke`'s `ErrRevokeReasonRequired` produce an
**identical 400 body**. So removing the handler-level check leaves the whole
suite green.

Proved by mutation during `#0376`'s review: removing the handler check alone →
the test **passes**; removing both → it fails. The store layer is pinned
separately at `internal/subscribers/imports_test.go`; **the handler layer is
pinned by nothing.**

**No behavioural risk today** — both return the same status and message, and the
store's check runs before any database work (`imports.go:756`, first statement,
before `pool.Begin`). This is about **assertion honesty**, not a defect.

The live claim: `#0376` added a doc comment asserting the handler returns 400
*"before it ever calls `h.store.Revoke`"* — **a boundary the test cannot
demonstrate.** Five issues bounced this phase for doc comments claiming more
than the code delivers.

## The choice this issue must make

1. **Pin the handler layer specifically** — e.g. a fake store recording whether
   `Revoke` was called at all, so the test proves the boundary its comment
   asserts.
2. **Correct the doc comment** to say the test pins the *behaviour*, not the
   layer.

Option 2 is cheaper and removes a false claim rather than adding a mechanism —
the move `#0330`, `#0347` and `#0356` each landed on. Option 1 is more work but
makes the assertion match the intent. **Choose deliberately and record why**; do
not default.

## Acceptance criteria

1. The choice is made explicitly, with its reason recorded **in the test's own
   doc comment**, not only in this issue.
2. If option 1: prove the new assertion discriminates — remove the handler check
   alone and confirm the test now fails. **A check that cannot fail pins
   nothing**; `#0354`, `#0358` and `#0363` each shipped exactly that this phase.
3. If option 2: the corrected comment must not overstate in the other direction
   either — the behaviour *is* pinned, and the store layer *is* separately
   pinned; say both.
4. **Re-verify the two-layer premise yourself**: confirm both checks exist,
   produce identical bodies, and that removing only the handler's leaves the
   suite green. **Seven unsupported details reached issues this phase**, several
   written by the orchestrator.
5. §8b: mutations use **your own database** and never target a literal or seeded
   id. Note `#0376` retargeted this very test to a sentinel
   (`/admin/imports/999999999/revoke`) for exactly that reason — do not
   reintroduce a small id.
6. Per §5 name the specific tests that ran; `gofmt` stability; and run
   `./internal/handlers/...` so `#0356`'s repo-wide guard is in scope
   (see `#0381`).

## Fix

**Chose option 2** — corrected the doc comment rather than adding a
handler-layer-specific pinning mechanism (a fake store recording whether
`Revoke` was called). Reasoning, recorded in this decision and in the
test's own doc comment:

- Re-verified the two-layer premise directly against the code before
  deciding anything (criterion 4): `AdminImportsHandler.Revoke`'s
  `reason == ""` block returns `writeError(w, http.StatusBadRequest,
  "reason is required")` before calling `h.store.Revoke`;
  `subscribers.ImportStore.Revoke`'s first two statements are the same
  `reason = strings.TrimSpace(reason)` / `if reason == ""` check, returning
  `ErrRevokeReasonRequired`, which the handler's `switch` maps to the
  identical `writeError(w, http.StatusBadRequest, "reason is required")`.
  Both checks exist today; both bodies are byte-identical; the store's
  check runs before `tx, err := s.pool.Begin(ctx)`, i.e. before any
  database work.
- No behavioural risk today — both checks currently agree, so there is
  nothing broken to fix. The only live problem was the doc comment's
  overclaimed boundary.
- Option 1 (a fake store proving the handler-level check specifically)
  would add a mechanism to pin a distinction with zero behavioural
  consequence, for a boundary the store already independently enforces.
  Option 2 removes the false claim instead — cheaper, and the precedent
  this phase has repeatedly landed on (`#0330`, `#0347`, `#0356`).
- The corrected comment satisfies acceptance criterion 3: it states the
  test pins the *behaviour* (empty reason → 400) and explicitly says the
  store layer is *separately* pinned by
  `internal/subscribers/imports_test.go`'s `ErrRevokeReasonRequired`
  assertion — neither side is overstated.

No `#0383`-caused discrimination proof is needed under option 2, since no
new assertion was added; the decision itself, and why it does not need
one, is recorded here and in the test's doc comment.

## Verification

`ISSUE=0383 scripts/check.sh go ./internal/handlers/... ./internal/subscribers/... ./internal/db/... ./internal/outbox/... ./internal/mailing/...`
→ `go build`/`go vet` clean, `gofmt -l` clean, all five packages `ok`
(`handlers` 6.740s, `subscribers` 6.850s, `db` 0.284s, `outbox` 0.734s,
`mailing` 4.206s), zero skips, `VERIFICATION PASSED`.

Named test re-run individually with `-v` on a fresh scratch database
(`opencircuit_test_0383v`, dropped after):
`TestAdminImports_Revoke_RequiresReason` — PASS. Also ran
`TestNoCommentCitesGoFileByLineNumber` (the repo-wide line-citation guard
`#0381` records `internal/handlers` as owning) — PASS, confirming the new
doc comment cites no `<file>.go:<int>`.

`gofmt` stability proved by hash, not `gofmt -l` alone: `shasum(file) ==
shasum(gofmt file)` for `internal/handlers/admin_subscribers_import_test.go`.

§8b: no mutation testing was performed for this issue (option 2 needs
none); no database mutation, no literal/seeded id touched. Scratch
database `opencircuit_test_0383v` created and dropped; shared
`opencircuit_test` untouched; no leftover `go test` processes.

## Review notes — 2026-09-02 (Opus, phase 3)

**Verdict: resolved.** All six acceptance criteria independently re-verified.

**The option-2 choice was correct on its merits, not merely cheaper.** Option 1
would have pinned a distinction with no behavioural consequence: both checks
return the byte-identical `writeError(w, http.StatusBadRequest, "reason is
required")`, and the store's check is the first statement of
`ImportStore.Revoke`, ahead of `pool.Begin`, so neither ordering nor cost nor
data reachability differs between the two paths. A fake store recording whether
`Revoke` was called would have pinned an implementation detail that no caller
can observe — the thing this phase keeps bouncing comments for asserting. Adding
a mechanism to defend a claim, rather than retracting the claim, is also what
`#0330`, `#0347` and `#0356` each declined to do.

**Criterion 4, re-derived rather than trusted.** Mutations ran in a
`git archive HEAD` export under the scratchpad, against my own database
`opencircuit_test_0383r` — the tracked tree was never modified (`git status`
clean throughout):

| Mutation | `TestAdminImports_Revoke_RequiresReason` |
|---|---|
| none (baseline) | PASS |
| handler `reason == ""` block removed | **PASS** — and whole-package `go test ./internal/handlers/` still `ok` |
| handler *and* store checks removed | **FAIL** — `status = 404, want 400; body: {"error":"import not found"}` |

So the premise holds exactly as the issue states it, and the corrected comment's
central claim — "removing the handler-level check alone leaves this test green"
— is measured, not asserted. `TestImportStore_Revoke_RequiresReason` was run
under the second mutation and does fail, confirming the comment's other claim
that it, not this test, is what catches the store check going missing.

**Criterion 3 — the comment overstates in neither direction.** It says the test
pins the observable behaviour (`POST .../revoke` with an empty reason returns
400), says it cannot discriminate which layer produced that 400, and names
`internal/subscribers/imports_test.go`'s `ErrRevokeReasonRequired` assertion as
the separate, specific pin on the store layer. All three are true. It claims no
boundary the test cannot demonstrate — which was the whole defect `#0376`'s
review found.

**Criterion 5 — the sentinel survived.** Both `/admin/imports/…/revoke` targets
in the file are still `999999999`; no small id was reintroduced. Checked against
my database: zero `subscriber_imports` rows with that id, so the guard-removal
mutation above could not reach a real import.

**Wording note, not a defect.** The second paragraph's "so the sentinel is
reachable regardless of whether the target id resolves to a real row" is vague
where the text it replaced was precise (the empty-reason check fires *before*
lookup). It overstates nothing and the paragraph that follows makes the real
point exactly, so it does not warrant a pass; noted only so a later editor
tightens rather than re-derives it.

**The change is comment-only, proved structurally.** A standalone `tokdiff`
binary (`go/scanner`, run as its own process) compared `69c8c75~1` against
`69c8c75`: 3816 non-comment tokens identical, comment count 60 -> 72, for
`internal/handlers/admin_subscribers_import_test.go`. The instrument was proved
falsifiable first — it reports DIFFERENT on a one-identifier code mutation,
and **errors** (never "DIFFERENT") on a file that does not scan, on empty input,
and on a pair with no comments at all.

**Verification re-run independently.** `ISSUE=0383r scripts/check.sh go
./internal/handlers/... ./internal/subscribers/... ./internal/db/...
./internal/outbox/... ./internal/mailing/...` — `go build`, `go vet`,
`gofmt -l` all clean; `handlers` 6.710s `ok`, `subscribers` 6.968s `ok`,
`outbox` 0.839s `ok`, `mailing` 4.734s `ok`; zero skips. Named tests re-run by
name: `TestAdminImports_Revoke_RequiresReason` PASS,
`TestImportStore_Revoke_RequiresReason` PASS,
`TestNoCommentCitesGoFileByLineNumber` PASS (`#0352` — the new comment cites no
`<file>.go:<int>`), `TestAdminResendInvitation_PhysicalAddressGateNotBypassable`
PASS, `TestSubscribe_UniformResponseAcrossBranches` PASS,
`TestSubscribe_SuppressionCheckError_FailsSafeToUniform202` PASS (§9 anchors
undisturbed — guaranteed anyway by the comment-only proof above). `gofmt`
stability by hash: `shasum(file) == shasum(gofmt file)` on all three touched
files.

**One failure in that run, not attributable to this batch.**
`internal/db`'s `TestNoOpenIssueVerificationCitesUndefinedTestFunction` fails on
`issues/0382.md`'s citation of an undefined instrumentation-test name — one the
concurrent `#0382`/`#0386` work ran in an export and never committed. Proved
attributable: in a clean `git archive HEAD` export with that one line deleted
and nothing else changed, `go test ./internal/db/` returns `ok`. It arrived in
`e724a51`, after `69c8c75`.

## Relation

- Found by: [#0376](0376.md)'s review, which resolved that issue
- Depends on: [#0376](0376.md) — which added the doc comment in question
- Relates to: [#0372](0372.md) — the sibling hardcoded-id fix
- Relates to: [#0330](0330.md), [#0347](0347.md), [#0356](0356.md) — the
  correct-the-claim-rather-than-add-a-mechanism precedent

## Work log

- 2026-09-01 — Filed by the orchestrator from `#0376`'s review (§9).
- 2026-09-02 — Phase 2 (implementation, Sonnet). Chose option 2: corrected
  `TestAdminImports_Revoke_RequiresReason`'s doc comment to state it pins
  the observable behaviour, not the handler layer specifically, and that
  the store layer is separately pinned by `imports_test.go`. Re-verified
  the two-layer premise (identical bodies, store check pre-`Begin`)
  directly against the code. Commit `69c8c75`. Verification: `ISSUE=0383
  scripts/check.sh go ./internal/handlers/... ./internal/subscribers/...
  ./internal/db/... ./internal/outbox/... ./internal/mailing/...` — all
  five packages `ok`, zero skips, `gofmt` clean and stable by hash. Left
  `in-progress` for review, not `resolved` (§9).
- 2026-09-02 — Phase 3 (review, Opus). **Resolved.** Re-derived the two-layer
  premise by mutation on a private database (`opencircuit_test_0383r`) in a
  `git archive HEAD` export: handler check removed alone -> test and whole
  package green; both checks removed -> 404, test fails. Confirmed the corrected
  comment is true and complete in both directions (criterion 3), that the
  `999999999` sentinel survived (criterion 5), and that the commit is
  comment-only via a standalone `go/scanner` binary proved falsifiable first.
  No §3 escalation question arose — approved on the first review pass.
