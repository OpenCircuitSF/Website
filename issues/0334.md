# 0334 — `claimKindsGuardMinPlausibleCallSiteCount`'s doc comment misattributes the worst single file

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-28 |
| **Commit** | — |

## Description

`claimKindsGuardMinPlausibleCallSiteCount`'s doc comment in
`internal/outbox/claim_kinds_call_site_guard_test.go` says a single-file
narrowing yields "at most 3, in `internal/handlers/subscribe_intake.go` or
`internal/mailing/outbox_worker.go`".

Both of those files hold **2** sites each. The only non-exempt file holding
**3** is `internal/mailing/worker_store_test.go`.

The **bound itself (3) is correct**; only the attribution is wrong. So this is
a documentation defect, not a live guard failure — nothing computes a floor
from the named files.

It was **born wrong at `270ebf9`**, not drifted: both named files already held
2 sites at that commit. It sits in the *other* constant's comment — `#0322`
corrected `claimKindsGuardMinPlausibleNonExemptCallSiteCount`, and this one was
never in scope for either of `#0322`'s two review passes, which is why it
survived them.

### Why it is worth fixing rather than leaving

`#0322` was bounced precisely because a number in this file was copied rather
than re-measured, and that number had propagated through `#0304` → `#0322` →
`#0330` before anyone checked it. A wrong file attribution sitting in the
adjacent constant's comment is the same hazard with a fresh fuse: the next
agent reasoning about single-file narrowings will read it, and it names the two
files most likely to be reasoned about.

## Acceptance criteria

1. The doc comment names `internal/mailing/worker_store_test.go` as the
   worst-case single non-exempt file, with its measured count of 3.
2. The counts for `internal/handlers/subscribe_intake.go` and
   `internal/mailing/outbox_worker.go` are stated correctly (2 each) if they
   are mentioned at all.
3. Every figure in the corrected comment is **re-measured**, not copied from
   this issue, from `#0304`, or from `#0322`. State the method used.
4. The bound `3` is left unchanged unless measurement shows it wrong — the
   defect is attribution only.
5. No `.ClaimDue(`/`.OrphanSweep(` call-syntax literal is introduced into any
   Go file. A shell harness greps for those literals, and comment text
   containing them inflates the population it measures — this fired twice in
   this file (`#0323`, `#0322`). Re-run the harness and confirm the total
   population is unchanged at 35.
6. `gofmt` stability proved: file sha256 equals the sha256 of `gofmt` stdout
   (§8 — gofmt rewrites doc comments, and doubled `''` or backticks collapse
   into curly quotes).
7. The change is comment-only, proved structurally rather than by
   `grep -v '^[+-]#'` — parse both revisions without comments and compare.

## Relation

- Found by: [#0322](0322.md)'s second review
- Relates to: [#0322](0322.md) — the sibling constant, corrected there
- Relates to: [#0330](0330.md) — the structural fix that removes reliance on
  these counts entirely
- Relates to: [#0304](0304.md) — origin of the propagated miscount, corrected
- Relates to: [#0275](0275.md), [#0323](0323.md) — the same guard family

## Method

Re-measured directly (criterion 3), not copied from this issue, `#0304`, or
`#0322`: added a temporary in-package test
(`internal/outbox/zzz_temp_measure_test.go`, removed before committing —
never landed in the tree) that calls this file's own
`walkClaimKindsGuardFiles` + `findOutboxCallSitesInFile` helpers — the exact
functions `TestNoUnscopedOutboxClaimCallOutsidePackage` uses — over
`claimKindsGuardScanRoots`, and logs the resulting `allSites` grouped by
`file` and by `!inOwnPkg` (non-exempt). Result:

```
visited=269 totalSites=36
ALL    2  internal/handlers/admin_dashboard_test.go
ALL    1  internal/handlers/admin_pending_test.go
ALL    2  internal/handlers/subscribe_intake.go
ALL    2  internal/mailing/outbox_worker.go
ALL    1  internal/mailing/outbox_worker_test.go
ALL    1  internal/mailing/worker.go
ALL    3  internal/mailing/worker_store_test.go
ALL    4  internal/outbox/select_due_claim_row_test.go
ALL   20  internal/outbox/store_test.go
---- non-exempt only (matches the 12 the doc comment already cites) ----
NONEXEMPT  2  internal/handlers/admin_dashboard_test.go
NONEXEMPT  1  internal/handlers/admin_pending_test.go
NONEXEMPT  2  internal/handlers/subscribe_intake.go
NONEXEMPT  2  internal/mailing/outbox_worker.go
NONEXEMPT  1  internal/mailing/outbox_worker_test.go
NONEXEMPT  1  internal/mailing/worker.go
NONEXEMPT  3  internal/mailing/worker_store_test.go
```

`internal/mailing/worker_store_test.go` is confirmed the worst single
non-exempt file at **3** (its three `mailing.SendStore.OrphanSweep` call
sites — a different `OrphanSweep` than this package's own, but
`nameMatchesGuardedMethod` matches by method name only, so the guard's scan
counts them too, exactly as `nameMatchesGuardedMethod`'s own doc comment
already says). `internal/handlers/subscribe_intake.go` and
`internal/mailing/outbox_worker.go` each hold **2** (one `OrphanSweep` call
plus one `SelectDue` call), not the 4 the doc comment (as it stood pre-fix,
after `#0303`'s edit) attributed to them. Total 36 and non-exempt 12 both
match the figures the surrounding paragraphs of the same doc comment
already state, confirming the measurement method agrees with what the rest
of the comment already asserts.

`scripts/go_file_visit_floor_guard_test.sh`'s independent grep-based oracle
(a different measurement method entirely — CLAUDE.md §8's "an oracle must
not share its method with its subject") separately confirms
`internal/mailing/worker_store_test.go` as the worst-case single file at 3,
in its own comment (`scripts/go_file_visit_floor_guard_test.sh:489`) — that
file did not need correcting; it was already right.

## Verification

Comment-only, proved structurally (criterion 7): parsed `HEAD` and the
working-tree revision of `internal/outbox/claim_kinds_call_site_guard_test.go`
with `go/parser` (`parser.ParseComments`), stripped every
`ast.File.Comments` entry plus every declaration's `.Doc`/`.Comment` field,
and printed both through `go/format`. The stripped output is
byte-identical — confirmed with `diff`, not `grep -v '^[+-]#'`.

`gofmt` stability (criterion 6): `sha256(file) == sha256(gofmt stdout)` —
confirmed equal.

No `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call-syntax literal introduced
(criterion 5): re-ran `scripts/go_file_visit_floor_guard_test.sh` — its
grep-based oracle still measures the total population at **42**, unchanged
(the doc comment's own prose names 42 as the pre-existing grep figure and
36 as the AST figure — both stayed unchanged by this fix, which touched
only prose, not code). `internal/mailing/worker_store_test.go` — the file
the corrected comment now cites for its `SendStore.OrphanSweep` call
sites — was itself untouched by this issue.

`scripts/check.sh guards` and a standalone
`bash scripts/go_file_visit_floor_guard_test.sh` both report
`claimKindsGuardMinPlausibleCallSiteCount=5` plausible against population 42,
`claimKindsGuardMinPlausibleNonExemptCallSiteCount=8` plausible against
population 12, and the tracked file unchanged across the run (byte-identity
check).

```
uptime            # 15:14, load averages 3.31 2.21 1.95 — not saturated
ISSUE=0342 scripts/check.sh go ./internal/mailing/... ./internal/handlers/... ./internal/outbox/...
scripts/check.sh guards
```

`internal/outbox` ok (6.105s) — zero skips. `VERIFICATION PASSED`. (Run
jointly with `#0342`'s implementation in the same session, same scoped
database, since both touch overlapping packages — see `#0342`'s own
`## Verification` for the full transcript notes, including the guards
self-check's transient first-run failure and clean rerun, unrelated to
either issue's diff.)

## Work log

- 2026-08-28 — Filed by the orchestrator from `#0322`'s second review.
  Reviewers report; the orchestrator allocates numbers (§9).
- 2026-08-31 — implement (`claude-sonnet-5`): re-measured every figure fresh
  (see `## Method` above) rather than trusting this issue's own quoted "3" —
  the tree had drifted further since filing (`#0303` landed the same day
  this issue was filed and changed the doc comment's wrong number from 3 to
  4, still wrong, still misattributed). Corrected the doc comment to name
  `internal/mailing/worker_store_test.go` (3) as the worst-case single
  non-exempt file and to state `internal/handlers/subscribe_intake.go` and
  `internal/mailing/outbox_worker.go`'s counts correctly (2 each). Checked
  `scripts/go_file_visit_floor_guard_test.sh` for the same misattribution
  named in this issue's title — it was already correct, naming
  `worker_store_test.go` and 3; no change needed there. Token usage not
  captured by this subagent; left for the orchestrator's usual
  per-transcript accounting. Code commit `ffef8cf` (shared with `#0342`,
  batched in the same commit — both are comment-only doc-comment
  corrections to overlapping files). Left `in-progress` for review (§9 —
  the implementer never sets `resolved`).
