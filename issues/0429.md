# 0429 — Two `.env.example` lines carry inline comments, so a verbatim `cp` install refuses to boot

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | config / deploy |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-04 |
| **Commit** | — |

## Description

`.env.example` carries trailing `#` comments **on the value line** for two
variables:

	MAX_SEND_RATE=10          # messages/second, keep below the SES quota
	SEND_WORKER_ENABLED=true  # false on a second instance to avoid double-sending

`deploy/systemd/opencircuit.service` loads that file with `EnvironmentFile=`.
**systemd treats `#` as a comment only at the start of a line** — an unquoted
value runs to end of line. So `MAX_SEND_RATE` becomes the whole string including
the comment, `getInt` hands it to `strconv.Atoi`, that fails, the loader appends
a validation error, and **the service refuses to start.**

Confirmed by the orchestrator: both lines are as shown, `opencircuit.service`
does use `EnvironmentFile=`, and production's own `config.env` carries both
variables **without** the comments — so what was installed was never a literal
copy of the template.

## Why this is now a defect rather than a wart

Both lines are **pre-existing**. What changed is `#0423`, which established that
`.env.example` **is the production template** — because `docs/deployment.md`'s
install step is a literal `sudo cp .env.example /etc/opencircuit/config.env`.

That decision converts a cosmetic inconsistency into a **boot failure on the
documented happy path**. An operator following the runbook exactly gets a
service that will not start.

**And no local workflow can reproduce it**: `scripts/dev.sh` loads env through
`godotenv`, which strips inline comments. So the failure appears only on a real
systemd host, which is the worst place to discover it.

## The fix

Move both comments to their own preceding lines. That is all — but check the
whole file for the same shape rather than only these two.

## Acceptance criteria

1. No value line in `.env.example` carries a trailing comment. **Sweep the whole
   file**; do not fix only the two named.
2. **Prove the failure and the fix**, rather than reasoning about systemd's
   parsing. Options: parse the file the way systemd does in a throwaway harness,
   or — better — construct the resulting environment and confirm `config.Load`
   rejects the pre-fix value and accepts the post-fix one. **Do not test on the
   box, and do not restart anything.**
3. The comments' content survives — they carry real operational guidance
   (`MAX_SEND_RATE` below the SES quota; `SEND_WORKER_ENABLED=false` on a second
   instance to avoid double-sending). Relocating must not lose them.
4. **Check the other loaded-verbatim surfaces for the same hazard.** Anything
   else `EnvironmentFile=` reads, or any doc telling an operator to copy a file
   into place, is in scope for the sweep even if it needs no change.
5. Consider whether `#0423`'s new `TestEnvExampleCoversLoaderVariables` should
   also assert **parseability** — a file that is copied verbatim could be checked
   for systemd-safety. Per `#0330`/`#0347`/`#0356`/`#0381` recording is
   legitimate, and `#0428` already proposes touching that guard, so **say what
   you conclude and coordinate with `#0428` rather than both editing it.**
6. Run `./internal/config/...`, `./internal/db/...` and `./internal/handlers/...`
   (§5's repo-wide-guard table). Per §5 name the specific tests that ran.
   `gofmt` stability if any Go file changes. No Go-file-plus-line-number
   citation (`#0352`), scanned with `python3` bytes rather than BSD `grep -P`.

## Relation

- Found by: [#0423](0423.md)'s second review, which called it the highest-value
  finding of that pass
- Depends on: [#0423](0423.md) — whose reference-versus-template decision is what
  makes this a defect
- Relates to: [#0428](0428.md) — the other open question about that guard; do not
  let both edit it independently
- Relates to: [#0057](0057.md) — which will add `SES_INBOUND_BUCKET` to the same
  file

## Work log

- 2026-09-04 — Filed by the orchestrator from `#0423`'s second review (§9). Both
  comment lines, the `EnvironmentFile=` directive, and production's comment-free
  values were confirmed independently before filing.
