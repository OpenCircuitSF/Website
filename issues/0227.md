# 0227 — The dashboard warns at Gmail's complaint threshold, not the one that re-sandboxes the account

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | admin / mailing |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-24 |

## Description

`#0061`'s dashboard warns when the account-wide complaint rate crosses **0.3%**,
which is what its acceptance criterion specified and what `PRD.md` §11 records
as the Gmail/Yahoo bulk-sender threshold. Its phase-3 review verified the
arithmetic exactly — 1/333 warns, 1/334 does not — and then noticed the number
may be the wrong one to warn on.

**`PRD.md` §6.9 records that AWS enforces 0.1% account-wide, and that crossing
it re-sandboxes the account.** So a rate of 0.2% sits inside AWS-review
territory with **no warning on the screen whose entire job is that warning** —
and the consequence there is not degraded inbox placement, it is sending
stopping altogether.

The implementation is correct against its criterion. What needs deciding is
whether the criterion is right. Two bands is the obvious answer — an amber at
0.1% ("AWS may review this account") and a red at 0.3% ("Gmail and Yahoo will
start filtering") — but that is a product call, not a bug fix.

**Three smaller items from the same review**, none blocking:

1. **Two comments cite a constant that does not exist.** `admin_dashboard.go:50`
   and `campaign_stats.go:133` both point at `send_health_complaint_pct` "in
   `worker.go` at 0.1". It lives nowhere — PRD §6.9's circuit breaker is not
   implemented, and `#0124` is the issue that would build it. The citations
   should point there. (This is the citation-drift class `#0220` is open about.)
2. **`AccountComplaintRate`'s numerator lacks the denominator's
   `s.status='sent'` filter.** Unreachable today, since only `MarkSent` writes a
   `ses_message_id` — but a rate above 100% is representable if that ever
   changes, and `AND s.status='sent'` costs nothing.
3. **Copy nit:** "SES is still in sandbox mode" is stated as observed fact for
   what is an operator-set environment flag with no live AWS call behind it. The
   default is the harmless direction, so this is polish.

## Acceptance criteria

- [ ] The dashboard's complaint warning reflects both thresholds that matter, or records in writing why one band is correct — with the AWS re-sandboxing consequence stated either way — **decided: two bands. See `## Decision`.**
- [ ] Whatever bands ship, each is proved at its boundary the way `#0061`'s review proved 0.3%: one send either side of the threshold, asserting the warning flips
- [ ] The two stale comments cite `#0124` rather than a constant that does not exist
- [ ] `AccountComplaintRate`'s numerator carries the same `s.status='sent'` filter as its denominator
- [ ] The sandbox copy does not present an operator-set flag as a detected fact
- [ ] `#0061`'s verified figures still agree with hand-written SQL, and the query count stays fixed at 8
- [ ] `ISSUE=0227 scripts/check.sh go ./internal/... ./cmd/...` and `scripts/check.sh web` both pass

## Decision

**Two bands: amber at 0.1%, red at 0.3%.** Recorded by the orchestrator on
2026-08-24 under the user's standing instruction to resolve Phase 7 rather than
stall on open questions.

The asymmetry of consequences decides it. Crossing Gmail/Yahoo's 0.3% degrades
inbox placement — bad, gradual, recoverable by sending better mail. Crossing
AWS's 0.1% re-sandboxes the account, and sending stops **altogether**. A screen
whose entire job is to warn about deliverability must not be silent through the
band where the more severe failure becomes possible. A 0.2% rate showing no
warning at all is the specific defect this issue names.

The counter-argument — a quieter dashboard — does not survive the comparison:
an amber that occasionally reads as noise costs attention, while a missing
warning costs the ability to send.

**Required wording distinction**, since the two bands mean genuinely different
things and a single escalating colour would blur them:

| Band | Threshold | Says |
|---|---|---|
| Amber | ≥ 0.1% | AWS may review this account and return it to the sandbox — sending would stop |
| Red | ≥ 0.3% | Gmail and Yahoo bulk-sender limits exceeded — mail will be filtered |

Both boundaries get the treatment `#0061`'s review gave 0.3%: one send either
side, asserting the band flips. Note the amber boundary is the arithmetically
harder one — 1/1000 warns, 1/1001 does not — so it is the one most worth
proving rather than assuming.

**Not in scope here:** actually enforcing either threshold. The circuit breaker
that would stop sending is `#0124`, unimplemented. This issue makes the
dashboard tell the truth; it does not make the system act on it.

## Notes

Reported by `#0061`'s phase-3 review under `CLAUDE.md` §9.

The first criterion is a decision, not an implementation — worth putting to the
user rather than choosing silently, since it trades a quieter dashboard against
an earlier warning about the failure mode that stops sending entirely.

`#0061`'s `## Review notes` carry the boundary proofs and the per-figure SQL
comparison; they are the regression suite for criterion 6. Note the review also
corrected the implementer's query count from 7 to **8** — `CampaignStore.List`
issues two statements — and proved it fixed by growing the fixture to 32
campaigns and 200 `campaign_interests` with the count unchanged.

## Relation

- Found by: [#0061](0061.md) — the admin overview dashboard
- Relates to: [#0124](0124.md) — the bounce/complaint circuit breaker the stale comments should cite
- Relates to: [#0220](0220.md) — the citation-drift guard that would have caught item 1

## Implementation notes

Implements the `## Decision`'s two independent bands exactly as tabled:
amber ≥ 0.1% (AWS account-wide review/re-sandboxing), red ≥ 0.3%
(Gmail/Yahoo bulk-sender filtering). Both use `>=`, matching the Decision
table's own "≥" notation for both rows — this is a small behavior change for
the red band, which previously used strict `>` (harmless: no automated test
pinned the exact-0.3% case, and the two already-verified boundary fractions
1/333 and 1/334 don't land exactly on 0.3% either way).

- `internal/handlers/admin_dashboard.go`: added
  `dashboardComplaintReviewThresholdPct = 0.1` alongside the existing
  `dashboardComplaintRateThresholdPct = 0.3`; added `ComplaintRateReview
  bool` (json `complaint_rate_review`) to `dashboardWarnings` next to the
  existing `ComplaintRateHigh`; `buildWarnings` sets both independently from
  the same `pct`. Rewrote both constants' doc comments to stop citing
  `send_health_complaint_pct` as if it exists in `worker.go` — it doesn't;
  the per-campaign circuit breaker is `#0124`, unimplemented, and the
  comments now say so.
- `internal/mailing/campaign_stats.go`: `AccountComplaintRate`'s numerator
  FILTER now carries `AND s.status = 'sent'`, matching the denominator's own
  predicate (item 2). Rewrote the doc comment to drop the same stale
  `send_health_complaint_pct` citation and to explain the new predicate.
- `web/src/lib/types.ts` / `web/src/lib/dashboard.ts`: added
  `complaint_rate_review` to `DashboardWarnings`; `buildWarnings` renders it
  as its own row (key `complaint-rate-review`, before the existing
  `complaint-rate-high` row per the Decision table's ordering) with wording
  distinct from the red row — "AWS may put this account under review and
  return it to the sandbox, which would stop sending altogether" vs. "this
  domain risks throttling or blocking." Both rows can render together (a
  rate above 0.3% is also above 0.1%) — this is deliberate, not a bug: both
  consequences are true at once, and collapsing them into one row keyed off
  the higher band would silently drop the re-sandboxing warning exactly when
  it matters most (item "the two bands must read as different things, not
  one escalating colour").
- Sandbox copy nit (item 3): "SES is still in sandbox mode" → "This
  environment is configured for SES sandbox mode" — states the operator-set
  flag as configuration, not an observed fact.
- Updated four frontend test files' `DashboardWarnings` fixtures to include
  the new required field, and rewrote the dashboard warnings tests/
  assertions for the two-row shape (see `## Verification`).

## Verification

`uptime` first: load average 2.05–2.50 across the session — idle, nothing
below is a load artifact.

- `ISSUE=0227 scripts/check.sh go ./internal/... ./cmd/...` — **passed**: 16
  packages `ok`, skip audit `(no skips)`, `gofmt -l` clean.
- `go test ./internal/handlers/... -run TestAdminDashboardBuildWarnings_ComplaintRateBandBoundaries -v` —
  **9/9 subtests passed**, including both required boundaries:
  `1/1000 = 0.1000...%` warns amber, `1/1001 = 0.0999...%` does not; the
  existing `1/333`/`1/334` red pair still flips as before, plus a fresh
  exact-boundary case (`3/1000 = 0.3000...%` warns red, `2999/1000000 =
  0.2999%` does not). The 1/1000 vs 0.1 float64 equality was confirmed by a
  standalone `go run` before writing the test (both round to the identical
  bit pattern `0.10000000000000000555`), not assumed — see the test's own
  doc comment.
- `go test ./internal/mailing/... -run TestAccountComplaintRate -v` —
  **3/3 passed**, including the new
  `TestAccountComplaintRate_NumeratorExcludesNonSentStatus`.
- **Mutation-tested the SQL fix**: reverted the numerator's `AND s.status =
  'sent'` filter (backed up via `cp`, not `git stash`, per CLAUDE.md §8a),
  re-ran the new test — it failed exactly as expected
  (`complained = 1, want 0`), proving the test catches the defect. Restored
  from the backup and confirmed byte-identical with `shasum -a 256`.
- **Criterion 6, checked independently rather than taken on trust**: built a
  throwaway `cmd/probe0227` in a detached worktree (removed afterward, main
  checkout never touched) with a `pgx.QueryTracer`, wired the real
  `AdminDashboardHandler` over a real pool against my own test database, and
  called `Overview` directly. Against an empty database it issued only 7
  queries — `CampaignStore.List`'s second query
  (`campaign_interests` batch load) short-circuits on zero ids
  (`interestIDsByCampaign`'s own `len(ids) == 0` early return), which #0061's
  review didn't hit because its fixture already had campaigns seeded. Seeding
  one throwaway campaign row (`zz-probe-campaign`, deleted immediately after)
  reproduced the reviewer's own figure exactly: **8 queries**, with
  `AccountComplaintRate` appearing as the same single statement it always
  was — the `AND s.status = 'sent'` addition is inside that one query, not a
  new one. Both counts logged and reviewed by eye against the SQL text.
- `scripts/check.sh web` — **passed**: `svelte-check` 274 files / 0 errors
  (matches baseline exactly), `vitest` 33 files / **3825** tests (baseline
  3823 + 2 new: the amber-row test and the both-bands-together test).

## Files changed

- `internal/handlers/admin_dashboard.go` — amber threshold + field, doc
  comment fixes (items 1 and the Decision)
- `internal/handlers/admin_dashboard_test.go` — new boundary-proof test
- `internal/mailing/campaign_stats.go` — numerator filter fix (item 2), doc
  comment fix
- `internal/mailing/campaign_stats_test.go` — new numerator-exclusion
  regression test
- `web/src/lib/types.ts` — `complaint_rate_review` field
- `web/src/lib/dashboard.ts` — amber row, sandbox copy fix (item 3)
- `web/src/lib/dashboard.test.ts` — new/updated unit tests for the two-row
  shape
- `web/src/views/admin/Dashboard.behavior.test.ts`,
  `web/src/views/Admin.behavior.test.ts` — fixture updates for the new
  required field, updated assertions for the renamed keys and copy

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Token counts not available in this environment (no subagent transcript file
to sum); the orchestrator should fill this row in from its own accounting
per `issues/Issues.md`'s "Getting exact token counts".
