# 0405 — Campaign slugs are subject-derived; the newsletter default should be the `MM-YYYY` template

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | mailing |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-03 |
| **Commit** | 1dcb3fa, e5c5d2d |
| **Closed** | 2026-09-03 |

## Resolution notes

> 🟢 Resolved 2026-09-03 — second review. Both bounce defects re-verified
> independently against the code rather than against the implementer's
> account: the widened announce guard was re-proved with this reviewer's own
> three-way mutation matrix in a fresh throwaway worktree and scratch
> database, **including the row that matters** — the same mutation against the
> old narrow regex still passes, so the widening closed a real hole rather
> than being cosmetic — and `newsletterSlugFormat` was proved load-bearing by
> mutating its value, not merely by reading its declaration. `campaigns.go`
> and both pre-existing slug-test files hash identically before and after this
> issue, so `#0404`'s production row and the savepoint collision loop are
> untouched.

## Description

`CampaignStore.Create` mints a slug by lowercasing the **subject** and replacing
every non-alphanumeric run with a hyphen (`slugifyCampaign`), falling back to
`campaign` when that leaves nothing.

So the archive URL is whatever the subject happens to be. The user's first real
newsletter, subject *"Open Circuits SF: September 2026"*, would yield
`open-circuits-sf-september-2026`.

## Root cause

`CampaignStore.Create` had exactly one source for a campaign's slug —
`slugifyCampaign(in.Subject)` — with no way for a caller to say "this one is a
monthly newsletter, mint its slug from the calendar instead." Nothing in the
schema distinguished a newsletter from any other campaign (`## Plan` §0), so
there was no seam to opt into a different minting rule even if a caller wanted
one.

## Fix

Implemented the plan exactly as specified, **with one correction made in a
second implementation pass** (see below) — the first pass's "no deviation"
claim was itself inaccurate and is corrected here rather than restated.

`CampaignInput` gains one field, `NewsletterMonth *NewsletterMonth` (nil by
default, so every existing call site is unaffected). `Create`'s single line

    base := slugifyCampaign(in.Subject)

became

    base := slugifyCampaign(in.Subject)
    if in.NewsletterMonth != nil {
        if !in.NewsletterMonth.valid() {
            return Campaign{}, ErrInvalidNewsletterMonth
        }
        base = in.NewsletterMonth.Slug()
    }

Everything downstream of `base` — the savepoint-per-candidate collision retry,
the `-2`/`-3` suffix, the `UNIQUE` contention, `maxSlugAttempts`, the INSERT —
is untouched, so the template inherits collision handling rather than
reimplementing it.

New `internal/mailing/newsletter_month.go` adds `NewsletterMonth` (unexported
`year`/`month` fields), `ParseNewsletterMonth("YYYY-MM")`, `.Slug()` (returns
`"MM-YYYY"`), and `ErrInvalidNewsletterMonth`. The wire format is `YYYY-MM`
(what `<input type="month">` produces); the slug is `MM-YYYY`, the user's own
decision. The parser anchors four digits first, so a value in the transposed
slug shape can never parse as a wire-format month — it is rejected outright,
never silently misread — and `TestParseNewsletterMonth_Rejects` pins that
property directly (using `"03-2031"`, the transposed form of this package's
own test month, rather than any real production slug — see below).

`POST /admin/campaigns`'s `createCampaignRequest` gains an optional
`newsletter_month *string` (`omitempty`); when present it is trimmed and
parsed, with a parse failure answered `400`. `PATCH` is untouched, exactly as
the plan required — a slug template field on create only, not a second way to
change a minted slug.

The "New campaign" modal in `Campaigns.svelte` gains a checkbox — "Monthly
newsletter (archive URL is MM-YYYY)" — and, shown only when checked, a native
`<input type="month">` defaulted to the current month via a new pure
`currentMonthValue(now: Date): string` helper in `lib/campaigns.ts`. The
client never formats or previews the slug itself (the plan's own
anti-drift reasoning): the field is sent only when the operator opts in, and
the real minted slug is what `CampaignEditor.svelte`'s existing copyable
archive-URL field shows once the draft exists.

**One correction against the plan's own premises**, caught before it landed
in code: `CampaignDraftFields` lives in `web/src/lib/api.ts`, not
`web/src/lib/types.ts` as the plan's §5 stated — verified by grep before
editing, and the field was added to the correct file.

**One self-correction during implementation:** an early draft of the modal's
help text hardcoded `/archive/09-2026` as a worked example. That is exactly
the literal `## Plan` §6 forbids anywhere under `internal/` or `web/src/` (it
belongs to `#0404`'s production migration and to issue prose only), so it was
rewritten to name the shape in prose (two-digit month, hyphen, four-digit
year) with no concrete digits. The same self-check caught the literal
appearing in three Go doc comments (illustrating the transposed-format safety
property) and in one pure test's fixture data (`"2026-09"` → `"09-2026"` as a
worked example, and `"09-2026"` as the literal transposed-rejection input) —
all rewritten to describe the property in prose, or to use the package's own
`2031-03`/`03-2031` test month, before any of it was committed. A `python3`
byte scan of every file under `internal/` and `web/src/` for the literal
`09-2026` confirms zero occurrences (see `## Verification`).

**Second implementation pass (phase 2, Sonnet), commit `e5c5d2d` — two small
additive fixes, both written out exactly in commit `21d6d0c`'s `## Review
notes`, neither changing the design:**

1. `internal/mailing/newsletter_month.go`'s own doc comment asserted
   `newsletterSlugFormat (campaigns.go)` — a constant that did not exist
   anywhere in the tree; `Slug()` used an inline `"%02d-%04d"` literal
   instead. Added `const newsletterSlugFormat = "%02d-%04d"` in
   `newsletter_month.go` (the file that actually owns it — `Slug()` is its
   only consumer, and it was never going to belong in `campaigns.go`), wired
   `Slug()` to use it, and corrected the doc comment's parenthetical from
   `(campaigns.go)` to `(this file)`.
2. `internal/handlers/admin_campaigns_newsletter_slug_test.go`'s
   `newsletterSlugShape` was `^[0-9]{2}-[0-9]{4}$`, which does not match a
   collision-suffixed month slug (`03-2031-2`), so a workshop announcement
   that consumed the newsletter's own month **and** got suffixed would slip
   past the non-newsletter guard undetected. Widened to
   `^[0-9]{2}-[0-9]{4}(-[0-9]+)?$` — one character class, no new test
   needed, per the review's exact prescribed remedy.

`internal/mailing/campaigns.go` was not touched in this pass — confirmed by
an empty `git diff --stat -- internal/mailing/campaigns.go` — so `Create`'s
collision handling and `#0404`'s already-backfilled production row are
undisturbed. §3 escalation to Opus was considered per `CLAUDE.md` §3 and
declined: both remedies were prescribed precisely enough (the exact constant
name, the exact widened pattern) that this pass was transcription, not
design — see `## Work log`.

## Verification

Machine load before each run was checked and stayed within CLAUDE.md §5's
idle baseline (`uptime` peaked around 6.6 on the one-minute figure across the
session, never near the ~8 threshold).

    ISSUE=0405 scripts/check.sh go ./internal/mailing/... ./internal/handlers/... ./internal/db/... ./internal/seo/...

Ran clean end to end (`VERIFICATION PASSED`), twice — once mid-implementation
and once as the final pass after a doc-comment correction:

    ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	5.531s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	13.161s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	7.859s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	0.864s

Zero skips (the audit step reported none); every package reported real
non-zero elapsed time, so each one reached its database. `gofmt -l` reported
clean on every touched file, non-destructively (no `-w` sweep).

**Every new or modified test individually confirmed passing, by name** — the
one-time `TAIL`/`-v` scoping needed to see per-test output cleanly is why this
was a supplementary, narrowly-scoped run rather than folded into the main one
(CLAUDE.md's own guidance: "re-run a single failing package unbounded ...
only when you need the detail" — here, naming the specific tests):

    ISSUE=0405 scripts/check.sh go ./internal/mailing/... -run Newsletter -v

    --- PASS: TestCampaignStore_Create_NewsletterMonthMintsTemplateSlug (0.00s)
    --- PASS: TestCampaignStore_Create_SecondNewsletterInSameMonthGetsSuffix (0.00s)
    --- PASS: TestCampaignStore_Create_NewsletterMonthIgnoresPunctuationOnlySubject (0.00s)
    --- PASS: TestCampaignStore_Create_WithoutNewsletterMonthStillMintsFromSubject (0.00s)
    --- PASS: TestCampaignStore_Create_RejectsInvalidNewsletterMonth (0.00s)
    --- PASS: TestParseNewsletterMonth_Valid (0.00s)
    --- PASS: TestParseNewsletterMonth_Rejects (0.00s)
    --- PASS: TestNewsletterMonth_ZeroValueIsInvalid (0.00s)
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	0.378s

    ISSUE=0405 scripts/check.sh go ./internal/handlers/... -run Newsletter -v

    --- PASS: TestAdminCampaigns_Create_NewsletterMonthMintsTemplateSlug (0.02s)
    --- PASS: TestAdminCampaigns_Create_OmittedNewsletterMonthMintsFromSubject (0.02s)
    --- PASS: TestAdminCampaigns_Create_InvalidNewsletterMonthReturns400 (0.02s)
    --- PASS: TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug (0.02s)
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	0.369s

**The non-newsletter proof (`## Plan` §7 test 12, criterion 6's sharpest
requirement) passed**: `TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug`
creates a `2031-03` newsletter (asserts `slug == "03-2031"`), *then* announces
a workshop, then asserts both halves — the announcement's slug does not match
`^[0-9]{2}-[0-9]{4}$`, **and** the newsletter's slug, re-fetched by `GET`, is
still exactly `03-2031`, unsuffixed.

**Criterion 4 — the three existing slug tests are unedited, proved
mechanically:**

    git diff HEAD --stat -- internal/mailing/campaign_archive_test.go internal/handlers/admin_campaigns_test.go

produced empty output (confirmed both by the diff itself and by
`git status --porcelain` on the same two paths). Both files' full test suites
also passed inside the two full-package runs above — `TestCampaignStore_Create_MintsSlugFromSubject`,
`TestCampaignStore_Create_UniquifiesSlugOnCollision`,
`TestCampaignStore_Create_EmptySubjectFallsBackToPlaceholderSlug`,
`TestCampaignStore_Update_SlugEditableWhileDraft`,
`TestCampaignStore_Update_SlugImmutableOnceScheduled`,
`TestCampaignStore_Update_SlugCollisionReturnsTypedError`,
`TestCampaignStore_Update_ZeroValueSlugKeepsCurrent`,
`TestCampaignStore_GetBySlug_Found`, `TestCampaignStore_GetBySlug_NotFound`,
`TestAdminCampaigns_Patch_RoundTripsSlugOnOrdinaryEdit`,
`TestAdminCampaigns_Patch_ScheduledCampaignSlugChangeReturns409`,
`TestAdminCampaigns_Patch_SlugCollisionReturns409` among them.

**§6's mechanical proof — the literal never appears:**

    python3 - <<'EOF'
    import os
    target = b"09-2026"
    hits = []
    for root in ["internal", "web/src"]:
        for dirpath, _, filenames in os.walk(root):
            for fn in filenames:
                p = os.path.join(dirpath, fn)
                with open(p, "rb") as f:
                    if target in f.read():
                        hits.append(p)
    print("HITS:", hits)
    EOF

`HITS: []`, run after every edit that could have reintroduced it (including
the self-caught mistake described in `## Fix`) and re-run as the final check
before committing.

**No Go-file-plus-line-number citation (`#0352`)**, scanned with the same
`python3`-over-bytes approach against every new/changed Go file — zero
matches.

**Web:**

    scripts/check.sh web

    > svelte-check --tsconfig ./tsconfig.json
    1788467980886 START ... COMPLETED 290 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
    Test Files  40 passed (40)
         Tests  3996 passed (3996)

and, individually by name:

    cd web && npx vitest run src/lib/campaigns.test.ts -t "currentMonthValue" --reporter=verbose

    ✓ currentMonthValue > zero-pads a single-digit month, constructed with local calendar fields
    ✓ currentMonthValue > zero-pads January (month index 0)
    ✓ currentMonthValue > does not zero-pad a two-digit month

`cmd/opencircuit` was **not** run, per `## Plan` §8: no wiring, route table,
or `*seo.Site` construction changed, and the plan explicitly argues it out at
~8x the cost of a small package.

### Second pass (phase 2, Sonnet), commit `e5c5d2d`

Machine load checked before each run and stayed well within CLAUDE.md §5's
idle baseline (`uptime` one-minute figure 2.0–3.1 across the session).

Real verification, in the tracked tree, database dropped on exit:

    ISSUE=0405b scripts/check.sh go ./internal/mailing/... ./internal/handlers/... ./internal/db/... ./internal/seo/...

    ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	5.158s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	11.648s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	7.048s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	0.851s
    VERIFICATION PASSED

Zero skips; every DB-backed package reported real non-zero elapsed time.
`gofmt -l` reported clean, non-destructively (no `-w` sweep).

    scripts/check.sh web

    290 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
    Test Files  40 passed (40)
         Tests  3996 passed (3996)
    VERIFICATION PASSED

**Defect-2 proof — the widened guard actually catches the hole it was
written to close.** Neither the tracked tree nor the shared database was
used for this; it ran in a throwaway `git worktree` (`git worktree add
--detach`) against its own scratch database (`opencircuit_test_0405b`,
created via `scripts/testdb.sh create 0405b`, dropped via `scripts/testdb.sh
drop 0405b` when done — same suffix as the real run, never the shared
`opencircuit_test`), targeting only
`TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug`:

1. **Control (unmutated code, widened regex): PASS.**
   `go test ./internal/handlers/... -run
   TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug -v` →
   `--- PASS ... (0.03s)`.
2. **Mutation applied**: `admin_workshop_announce.go`'s `CampaignInput{}`
   literal was edited to also set `NewsletterMonth` to the newsletter's own
   test month (`2031-03`, via `mailing.ParseNewsletterMonth`), simulating an
   announce path that consumes the newsletter's month and collides.
3. **Mutated code, widened regex `^[0-9]{2}-[0-9]{4}(-[0-9]+)?$`: FAIL, as
   intended.** Same command → `--- FAIL ...`, with the assertion message
   naming the exact suffixed slug the collision retry produced:
   `announced campaign's slug = "03-2031-2", matches the MM-YYYY template
   shape — it must not consume a newsletter month`.
4. **Same mutation, old narrow regex `^[0-9]{2}-[0-9]{4}$` (temporarily
   substituted to prove the widening was necessary): PASS — incorrectly.**
   Same command → `--- PASS ... (0.03s)`, confirming the pre-fix guard would
   have let this exact defect through silently.

All mutated files were then restored to the two real fixes only (verified
with `git diff --stat` inside the worktree matching the tracked tree's own
two-file diff), the worktree was removed
(`git worktree remove --force`), and the scratch database was dropped. This
is a mutation proof, not a claim about production traffic — no mock/real-SES
distinction applies here, since nothing in either fix touches sending.

**Byte scans, re-run after the second pass's edits:** the `python3`-over-bytes
scan for the literal `09-2026` across every file under `internal/` and
`web/src/` returned `HITS: []`. The same `python3`-over-bytes scan for a
Go-file-plus-line-number citation pattern (`#0352`) against both changed
files returned no matches. Both used `python3`, never BSD `grep -P`.

**Collision handling and `#0404`'s row, re-confirmed untouched:**
`git diff --stat -- internal/mailing/campaigns.go` is empty for this pass —
the file was not opened. `TestCampaignStore_Create_SecondNewsletterInSameMonthGetsSuffix`
passed unchanged, in both the tracked-tree run and the worktree's control
run, exercising the exact same collision-retry code the first pass verified
byte-identical.

## Files changed

- `internal/mailing/campaigns.go` — `CampaignInput` gains `NewsletterMonth
  *NewsletterMonth`; `Create`'s slug-base line gains the four-line template
  branch. Nothing else in the file changed.
- `internal/mailing/newsletter_month.go` (new; amended second pass, commit
  `e5c5d2d`) — `NewsletterMonth`, `ParseNewsletterMonth`, `.Slug()`,
  `ErrInvalidNewsletterMonth`. Second pass added the `newsletterSlugFormat`
  constant `Slug()` uses and corrected the doc comment that had wrongly
  claimed it already existed in `campaigns.go`.
- `internal/mailing/newsletter_month_test.go` (new) — pure parse/reject/zero-value tests.
- `internal/mailing/campaign_newsletter_slug_test.go` (new) — store-level
  Create tests for the template, the second-in-a-month suffix, the
  punctuation-only-subject override, the nil-field default, and the
  reject-with-no-row-inserted case.
- `internal/handlers/admin_campaigns.go` — `createCampaignRequest` gains
  `NewsletterMonth *string` (`json:"newsletter_month,omitempty"`); `Create`
  parses it and passes it through, answering `400` on a parse failure.
- `internal/handlers/admin_campaigns_newsletter_slug_test.go` (new; amended
  second pass, commit `e5c5d2d`) — handler-level Create tests for the
  field, the omitted-field default, the `400` on an invalid month, and the
  non-newsletter proof against the workshop-announce shortcut. Second pass
  widened `newsletterSlugShape` to also match a collision-suffixed month
  slug (`^[0-9]{2}-[0-9]{4}(-[0-9]+)?$`), closing a gap where a suffixed
  announcement slug could slip past the guard undetected.
- `web/src/lib/api.ts` — `CampaignDraftFields` gains `newsletter_month?:
  string`.
- `web/src/lib/campaigns.ts` — new `currentMonthValue(now: Date): string`.
- `web/src/lib/campaigns.test.ts` — three new tests for `currentMonthValue`.
- `web/src/views/admin/Campaigns.svelte` — the "New campaign" modal gains a
  newsletter checkbox and, conditionally, a native month input; `submitCreate`
  sends `newsletter_month` only when checked; a `.checkbox-label` style was
  added to this file's own `<style>` block (it existed only in sibling
  components before).

## Gotchas

An early draft of the modal's help text, and several doc comments explaining
the transposed-format safety property, used the real value `09-2026` as a
concrete illustration before it was caught and rewritten — see the
self-correction paragraph in `## Fix`. Nothing incorrect ever reached a
commit, but it is worth flagging for the reviewer as the one place this issue
came closest to violating its own hardest constraint: any future edit to
these files that adds a "for example, September 2026 becomes ..." aside
should reach for a description of the *shape*, not the digits, or use the
package's own `2031-03`/`03-2031` test month if a concrete example is really
needed.

## The decision — made by the user, not open for re-litigation

**The default newsletter slug is `MM-YYYY`.** September 2026 is `09-2026`, giving
`/archive/09-2026`. The user's words: *"It can be short and specific."*

Zero-padded two-digit month, hyphen, four-digit year. Implement that; do not
substitute a variant you prefer.

One consideration recorded rather than raised again: a date-only slug carries no
keywords, so **search discovery rests on the archive index page, the sitemap, and
each entry's title and heading — not on the slug.** All three of those already
exist and already carry the subject, so the trade-off is already covered.

### What is already in place, and is NOT part of this issue

Verified against the code, so the work here stays narrow:

- **Collision handling is done and is robust.** Create uniquifies with a numeric
  suffix (`-2`, `-3`, …), and each candidate is a **real INSERT** fought out
  against the `UNIQUE(slug)` constraint inside its own savepoint — never a
  check-then-insert race, so two concurrent Creates cannot both win a slug.
- **Overrides are done.** `PATCH` accepts a `slug`, rejecting a taken one and
  refusing edits once the campaign is no longer editable. This is how an
  exception to the template is expressed.
- **Minting happens at draft time, not send time** — PRD §6.8 calls this
  load-bearing, because promotion has to be written against a URL that already
  exists.
- **The archive index, its footer link, and sitemap coverage all exist** — see
  the note at the end of this issue.

## The questions still to settle

1. **Which campaigns get the template?** Not every campaign is a monthly
   newsletter — a workshop announcement slugged `09-2026` would be actively
   wrong, and would also consume that month's slug so the real newsletter falls
   back to `09-2026-2`. **That failure is silent and permanent once sent**, so
   this is the question that matters most. Decide whether the template is
   unconditional, opt-in, or inferred from campaign kind, and say how an author
   chooses.
2. **Where does the month come from?** Creation time is the obvious source, but a
   newsletter drafted on 31 August for September would be misnamed `08-2026`.
   Consider letting the author supply it, defaulting to now.
3. **Second newsletter in one month.** The existing suffix yields `09-2026-2`.
   Confirm that is acceptable rather than an error worth surfacing to the author.
4. **Configurable or constant?** The project has a settings table, but this is a
   one-organisation site. A constant is likely right; argue whichever you choose.

## Interaction with #0404 — read that first

[#0404](0404.md) is a **deploy blocker** and it will **backfill a slug onto the
one existing production row** — the September 2026 newsletter this template is
being defined for.

**That row's slug should be `09-2026`.** Either `#0404` waits for this issue, or
it backfills `09-2026` directly. Say which, and coordinate — if they land
inconsistently the first real newsletter carries a subject-derived slug while
every later one uses the template.

## Plan

**Phase 1 (planning) pass, 2026-09-03. Opus. No production code was written,
no schema was changed, and nothing was applied to any database.** Every claim
below was read out of the tree rather than carried over from the dispatch —
including the dispatch's own assertions, three of which are corrected or
sharpened in §1.

### 0. What was measured, and where

| Fact | Measured how |
|---|---|
| `email_campaigns` has **no campaign-kind column** | `campaignColumns` in `internal/mailing/campaigns.go` lists all 18 selected columns: `id, name, subject, preheader, body_md, status, audience_mode, workshop_id, scheduled_at, started_at, completed_at, test_sent_at, created_by, created_at, updated_at, slug, archive_status, archived_at`. PRD §6.8 describes no kind either |
| **Exactly two** production call sites construct a campaign | `grep -rn 'CampaignInput{' --include=*.go .` excluding tests → `internal/handlers/admin_campaigns.go` (`POST /admin/campaigns`) and `internal/handlers/admin_workshop_announce.go` (`POST /admin/workshops/{id}/announce`) |
| `workshop_id` is set **only** by the announce shortcut | Same grep — `WorkshopID:` appears in exactly one of the two |
| The announce path falls back to `audience_mode = 'all'` for an interest-less workshop | `internal/handlers/admin_workshop_announce.go`, the `mode := mailing.AudienceAnyOf` / `if len(interestIDs) == 0` block |
| The SPA's "New campaign" modal hardcodes `audience_mode: 'all'` for **every** draft it creates | `web/src/views/admin/Campaigns.svelte`, `submitCreate` |
| `POST /admin/campaigns` accepts only `name, subject, preheader, body_md, audience_mode, interest_ids` | `createCampaignRequest` in `internal/handlers/admin_campaigns.go` |
| The slug override is **API-only — there is no admin UI for it** | `PATCH` accepts `slug` (`patchCampaignRequest`), but `CampaignDraftFields` in `web/src/lib/types.ts` has no slug field and no `.svelte` file under `web/src/views/admin/` binds one. `CampaignEditor.svelte` renders the archive URL read-only, with a copy button |
| **There is no `DELETE /admin/campaigns/{id}`** | No delete route in the campaigns handler; the only `DELETE FROM email_campaigns` statements in the tree are two tests' own cleanup |
| Collision handling is a savepoint-per-candidate INSERT race, bounded at `maxSlugAttempts = 1000` | `CampaignStore.Create`'s retry loop |
| The existing slug tests live in `internal/mailing/campaign_archive_test.go`, not `campaigns_test.go` | `grep '^func Test' internal/mailing/campaign_archive_test.go` |
| `## Plan` and `## Acceptance criteria` are **excluded** from `internal/db`'s issue-citation guard | `issueCitationSectionHeaders` and its doc comment — "A planned or required test may legitimately not exist yet". So the test names proposed in §7 may be named here while this issue is `open` |
| Working tree clean; highest allocated issue is **`#0408`**, not `#0407` | `git status --porcelain` (empty), `ls issues/*.md` |

Two corrections to the dispatch, both minor and neither changing the work:

- **`#0407` is not the highest allocated number — `#0408` is** (`vite dev` proxying
  `/admin` wholesale, `in-progress`). Nothing here needs a number, but a later
  pass that files should start from 409 and re-check.
- The dispatch says overrides are "done". They are done **in the API and the
  store**; they are **not reachable from the admin console**. That distinction
  is load-bearing for question 1's escape hatch and for question 3, and it is
  the one thing this plan recommends reporting for a follow-up issue (§10).

### 1. Question 1 — which campaigns get the template

**Answer: opt-in, chosen per campaign at compose time. Never inferred, never
unconditional.**

#### What actually distinguishes a newsletter in the schema: nothing

There is no `kind`, `type`, or `is_newsletter` column, and PRD §6.8 defines
none. The three columns that could plausibly stand in for one were each
checked against the real call sites, and all three fail:

| Candidate rule | Why it fails |
|---|---|
| `workshop_id IS NULL` → newsletter | Sound in one direction only. `workshop_id IS NOT NULL` really does mean "workshop announcement" (only the announce shortcut ever sets it). But `workshop_id IS NULL` covers **every hand-composed campaign** — the September newsletter *and* a one-off "the space is closed this weekend" note. Those are exactly the two things that must not share a slug rule |
| `audience_mode = 'all'` → newsletter | Measured, and it is unconditional in disguise. The SPA's "New campaign" modal sends `audience_mode: 'all'` for **every** draft it creates, and the announce shortcut *also* falls back to `'all'` whenever the workshop carries no interests. So this rule would hand the month's slug to a workshop announcement on the most ordinary path there is |
| Subject or name matching (`/newsletter/i`, a month name) | Guessing at prose. Silent when it guesses wrong, in both directions, and it makes a URL depend on wording the author is free to edit |

#### Why unconditional is worse than it looks

It is not merely that an announcement would take `09-2026`. If **every**
campaign uses the month, then every campaign in a month collides, and the
archive fills with `09-2026`, `09-2026-2`, `09-2026-3` — slugs that identify
nothing and cannot be told apart. The template only carries meaning while it
is scarce.

#### Why opt-in is the right shape, in one sentence

Opt-in is the only option whose failure mode is **loud and reversible**:
forget to tick the box and you get `open-circuits-sf-september-2026`, visible
in the editor immediately, fixable by `PATCH` while the campaign is still a
draft. Inference fails the other way — silently, and permanently once sent,
which is the exact failure this issue exists to prevent.

#### How an author chooses

One optional field on the create path, carrying the opt-in **and** the month
together, so there is no invalid combination to validate:

- **API:** `POST /admin/campaigns` gains one optional body field,
  `"newsletter_month": "2026-09"`. Absent → today's behaviour exactly
  (subject-derived). Present → `MM-YYYY`.
- **Admin UI:** the "New campaign" modal in `web/src/views/admin/Campaigns.svelte`
  gains a checkbox — *"Monthly newsletter (archive URL is MM-YYYY)"* — and,
  shown only when it is ticked, a native `<input type="month">` defaulted to
  the current month. `type="month"`'s value is literally `YYYY-MM`, so the
  client sends the field verbatim with no formatting logic of its own (§5
  explains why keeping the format knowledge server-side matters).
- **Announce shortcut:** excluded **by construction** — it builds its own
  `CampaignInput` and simply never sets the field. §7 pins that with a
  behavioural test rather than leaving it to inspection.

Rejected, deliberately: a separate boolean plus a separate month (two fields,
one invalid state — `newsletter: false` with a month set — for no gain), and
adding `newsletter_month` to `PATCH` (after creation the slug already exists
and `slug` is the override; two ways to set one thing is how they drift).

### 2. Question 2 — where the month comes from

**Answer: the author supplies it explicitly; the compose form defaults the
input to the current month. `CampaignStore.Create` never reads a clock.**

Three reasons, in order:

1. **It fixes the bug the question names.** A newsletter drafted 31 August for
   September is the *normal* case, not an edge case, and any server-side
   `time.Now()` default gets it wrong with no way for the author to see or
   correct it before the slug is permanent.
2. **It keeps `Create` clock-free.** `Create` today has no time dependency at
   all — `created_at` is a column default. Introducing `time.Now()` would
   mean either injecting a clock seam for tests or writing tests that are
   correct only until a month boundary. Neither is worth it when the caller
   already knows the answer.
3. **The added UI surface is one checkbox and one native input**, and the
   default is computed where the author can see and change it. `<input
   type="month">` is a real browser control with a month picker; nothing has
   to be invented.

The one cost — a non-browser caller (curl) must spell the month out — is
acceptable and arguably correct: an API client asserting "this is the
September newsletter" should say September.

### 3. Question 3 — a second newsletter in one month

**Answer: `09-2026-2` is the right outcome, and the collision algorithm must
not change. But the likely trigger is not two newsletters.**

Confirmed as desired, on three grounds: the suffix is unique, permanent and
honest; a genuine second newsletter in one month is rare under a monthly
cadence; and turning a collision into an error is not available anyway —
acceptance criterion 4 pins the savepoint-based `UNIQUE` contention, and any
"refuse if taken" behaviour is precisely the pre-check that criterion forbids.

**The real hazard is different and worth stating plainly.** Two measurements
combine badly:

- there is **no `DELETE /admin/campaigns/{id}`**, so a draft created by
  mistake exists forever; and
- the slug override has **no admin UI**.

So an author who clicks "New campaign" twice with the newsletter box ticked
permanently parks `09-2026` on a stray draft, and the real newsletter becomes
`09-2026-2` — with no way to repair it from the console. That is a genuine
trap, but it is a *reachability* gap in the override, not a defect in the
minting rule, and the fix is a slug field in `CampaignEditor.svelte`. Reported
in §10 rather than built here, so this issue stays narrow.

Nothing else is needed to surface the collision: `CampaignEditor.svelte`
already displays the full archive URL as a copyable field the moment the draft
is created, so an author who ends up with `09-2026-2` sees it immediately.

### 4. Question 4 — constant, not a setting

**Answer: an unexported format constant in `internal/mailing`.**

For the setting: the `settings` table exists and is already used for
operator-tunable numbers.

Against, and it decides it:

1. **Every existing `settings` row is something the send worker reads at
   runtime and an operator legitimately tunes** — send rate, soft-bounce
   thresholds, the physical postal address, queue retry limits. A URL format
   is not operational tuning; it is a published contract.
2. **A settings row invites a false expectation.** Changing it would not — and
   must not — alter slugs already minted, so the setting would silently mean
   "the format for campaigns created from now on". A constant carries no such
   implication.
3. **The real cost is not the constant.** A settings key needs a seeding
   migration (the `000008` pattern: the row must exist before `PATCH
   /admin/settings` can touch it), validation on the patch path, and a UI —
   for one organisation with one cadence and no second format in prospect.
4. **Per-campaign deviation is already expressible**, via the `slug` override.
   That is the right granularity for an exception; a global format switch is
   not.

Concretely: `const newsletterSlugFormat = "%02d-%04d"` in `internal/mailing`,
with a doc comment recording that it is `#0405`'s user-made decision and that
changing it does not retroactively change minted slugs.

### 5. The change, precisely

Four files, plus tests.

**`internal/mailing/campaigns.go`** — `CampaignInput` gains one field:

    // NewsletterMonth opts this campaign into #0405's MM-YYYY archive-slug
    // template ... nil (the default) leaves slug minting exactly as it was:
    // slugifyCampaign(Subject).
    NewsletterMonth *NewsletterMonth

and `Create`'s single existing line

    base := slugifyCampaign(in.Subject)

becomes

    base := slugifyCampaign(in.Subject)
    if in.NewsletterMonth != nil {
        if !in.NewsletterMonth.valid() {
            return Campaign{}, ErrInvalidNewsletterMonth
        }
        base = in.NewsletterMonth.Slug()
    }

**That is the whole store change.** Everything downstream of `base` — the
savepoint-per-candidate loop, the `-2`/`-3` suffix, the `UNIQUE` contention,
the `INSERT`, `maxSlugAttempts` — is untouched, so the template inherits
collision handling rather than reimplementing it.

**The fallback when the template does not apply is the current behaviour,
unchanged**, including `slugifyCampaign`'s `"campaign"` result for a subject
that is empty or entirely punctuation. That is what lets
`TestCampaignStore_Create_MintsSlugFromSubject`,
`TestCampaignStore_Create_UniquifiesSlugOnCollision` and
`TestCampaignStore_Create_EmptySubjectFallsBackToPlaceholderSlug` pass
**byte-for-byte unmodified** (criterion 4).

**`internal/mailing/newsletter_month.go`** (new, small):

    type NewsletterMonth struct { year int; month time.Month }   // fields unexported
    func ParseNewsletterMonth(s string) (NewsletterMonth, error) // "YYYY-MM"
    func (m NewsletterMonth) Slug() string                       // "MM-YYYY"

plus `ErrInvalidNewsletterMonth`. Unexported fields mean the only way to build
a valid value outside the package is `ParseNewsletterMonth`, so the range rule
lives in exactly one place; `Create`'s `valid()` check above is the
belt-and-braces backstop for a caller that constructs a zero value anyway —
the same shape `normalizeAudience` already uses behind the database's CHECK.

**The wire format is `YYYY-MM` and the slug is `MM-YYYY`, deliberately
inverted, and the inversion is safe rather than a footgun.** `YYYY-MM` is what
`<input type="month">` produces; `MM-YYYY` is what the user decided the URL
should read. Because the accepted pattern anchors four digits first
(`^\d{4}-(0[1-9]|1[0-2])$`), a transposed `"09-2026"` cannot parse as anything
— it is rejected outright, never silently misread. §7 pins that property.

**`internal/handlers/admin_campaigns.go`** — `createCampaignRequest` gains
a `NewsletterMonth *string` field tagged `json:"newsletter_month,omitempty"`; when non-nil
it is trimmed and passed to `mailing.ParseNewsletterMonth`, with a parse
failure answered `400` ("newsletter_month must be YYYY-MM"). `PATCH` is
untouched. The audit metadata written on create is untouched too — no new key,
so `internal/handlers`' audit-metadata guard is not in play.

**`web/`** — `CampaignDraftFields` (`web/src/lib/types.ts`) gains
`newsletter_month?: string`; `web/src/lib/campaigns.ts` gains a pure
`currentMonthValue(now: Date): string` helper returning `YYYY-MM` from local
calendar fields; `web/src/views/admin/Campaigns.svelte`'s modal gains the
checkbox and the month input.

**The client deliberately does not format the slug.** It was tempting to
preview `/archive/09-2026` in the modal, but that would put the `MM-YYYY`
ordering in two languages and create a drift surface the repo would then need
a parity guard for (the `campaign_progress_parity_test.go` /
`routes_parity_test.go` pattern). Instead the modal's help text names the
shape in prose only, and the **real** URL appears in `CampaignEditor.svelte`'s
existing copyable field the instant the draft is created, straight from the
server's response. One formatter, one source of truth.

### 6. The existing production row — nothing re-derives it

**Explicitly: nothing in this change reads, re-derives, or overwrites an
existing `email_campaigns.slug`.** The only code path touched is the value of
`base` inside `Create`, which runs only on `INSERT`. `Update` is not touched;
`GetBySlug`, `ListArchived` and `SetArchiveStatus` are not touched.

So production's id 1 keeps the `'09-2026'` that `#0404`'s migration backfills,
and it keeps it for the same reason it has it: because a migration wrote a
literal, not because a rule recomputes it. `#0405` and `#0404` agree by
construction rather than by coincidence.

Two things the implementer must therefore **not** do, stated so a reviewer can
check them mechanically:

- do not add any backfill, re-mint, or "fix up existing slugs" step; and
- do not let the literal `09-2026` appear anywhere under `internal/` or
  `web/src/` — it belongs to `migrations/000025` and to issue prose. Tests use
  a distinct far-future month (§7) so a test can never contend with it.

### 7. Test design

New tests go in **new files** so `git diff --stat` proves the existing slug
tests were not edited (criterion 4's "prove they were not edited"):
`internal/mailing/campaign_newsletter_slug_test.go`,
`internal/mailing/newsletter_month_test.go`,
`internal/handlers/admin_campaigns_newsletter_slug_test.go`.

All database-backed tests use the month **2031-03** (`03-2031`) — far from any
real campaign and from `#0404`'s `09-2026` — and clean up with the package's
existing `cleanupCampaign` helper.

**Pure, no database** (`newsletter_month_test.go`):

1. `TestParseNewsletterMonth_Valid` — `"2026-09"` → `Slug() == "09-2026"`;
   `"2026-12"` → `"12-2026"`; `"2026-01"` → `"01-2026"` (zero padding is the
   whole point of the format).
2. `TestParseNewsletterMonth_Rejects` — `""`, `"2026-1"`, `"2026-00"`,
   `"2026-13"`, `"2026-9x"`, and — the property worth pinning —
   **`"09-2026"`, the transposed form, which must be rejected rather than
   parsed**, so the two formats can never be confused silently.
3. `TestNewsletterMonth_ZeroValueIsInvalid` — a bare `NewsletterMonth{}` does
   not pass `valid()`.

**Store, database-backed** (`campaign_newsletter_slug_test.go`):

4. `TestCampaignStore_Create_NewsletterMonthMintsTemplateSlug` — **the
   default.** Month 2031-03, ordinary subject → `Slug == "03-2031"`.
5. `TestCampaignStore_Create_SecondNewsletterInSameMonthGetsSuffix` — **the
   second-in-a-month case.** Two creates, same month, different subjects →
   `second.Slug == first.Slug + "-2"`, asserted relatively, mirroring the
   existing collision test's shape rather than hardcoding the suffix twice.
6. `TestCampaignStore_Create_NewsletterMonthIgnoresPunctuationOnlySubject` —
   **the empty/all-punctuation subject.** Subject `"!!!"` **with** a month →
   `"03-2031"`, not `"campaign"`: the template replaces the subject entirely,
   including the fallback.
7. `TestCampaignStore_Create_WithoutNewsletterMonthStillMintsFromSubject` —
   the opt-in is off by default; a nil field behaves exactly as before.
8. `TestCampaignStore_Create_RejectsInvalidNewsletterMonth` — a zero-value
   `NewsletterMonth` returns `ErrInvalidNewsletterMonth` **and inserts no
   row** (assert the count, not just the error).

**Handler** (`admin_campaigns_newsletter_slug_test.go`):

9. `TestAdminCampaigns_Create_NewsletterMonthMintsTemplateSlug` — `POST` with
   `"newsletter_month":"2031-03"` → `201`, `slug == "03-2031"`.
10. `TestAdminCampaigns_Create_OmittedNewsletterMonthMintsFromSubject` — the
    field absent → subject-derived, proving the wire default is unchanged.
11. `TestAdminCampaigns_Create_InvalidNewsletterMonthReturns400` — `"2031-13"`
    → `400`, and no campaign row created.

**The non-newsletter proof** (criterion 6's sharpest requirement), in
`internal/handlers`:

12. `TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug` — create
    a newsletter for 2031-03 (slug `03-2031`), **then** `POST
    /admin/workshops/{id}/announce`, then assert **both** halves: the
    announcement's slug is its own subject-derived value and does not match
    `^[0-9]{2}-[0-9]{4}$`, **and** the newsletter's slug is still exactly
    `03-2031` (not suffixed). Asserting only the first half would miss the
    failure this issue is actually about — an announcement *consuming* the
    month.

**Web** (`web/src/lib/campaigns.test.ts`):

13. `currentMonthValue` returns `'2026-09'` for a local `new Date(2026, 8, 15)`
    and `'2026-01'` for `new Date(2026, 0, 5)` — zero padding and the
    month-index-is-zero-based trap, in one cheap unit test. Construct dates
    with local components, not `Date.UTC`, since `<input type="month">` is a
    local-calendar control.

Existing tests to confirm still pass **unmodified**, named so the verification
can list them: `TestCampaignStore_Create_MintsSlugFromSubject`,
`TestCampaignStore_Create_UniquifiesSlugOnCollision`,
`TestCampaignStore_Create_EmptySubjectFallsBackToPlaceholderSlug`,
`TestCampaignStore_Update_SlugEditableWhileDraft`,
`TestCampaignStore_Update_SlugImmutableOnceScheduled`,
`TestCampaignStore_Update_SlugCollisionReturnsTypedError`,
`TestCampaignStore_Update_ZeroValueSlugKeepsCurrent`,
`TestCampaignStore_GetBySlug_Found`, `TestCampaignStore_GetBySlug_NotFound`,
`TestAdminCampaigns_Patch_RoundTripsSlugOnOrdinaryEdit`,
`TestAdminCampaigns_Patch_ScheduledCampaignSlugChangeReturns409`,
`TestAdminCampaigns_Patch_SlugCollisionReturns409` (criterion 3).

### 8. Verification (§5)

Run `uptime` first and read it before blaming any timing failure on the code.

    ISSUE=0405 scripts/check.sh go ./internal/mailing/... ./internal/handlers/... \
        ./internal/db/... ./internal/seo/...
    scripts/check.sh web

`scripts/check.sh` creates the isolated database itself when `ISSUE` is set
and drops it on exit — do **not** also run `scripts/testdb.sh create/template`
by hand (§Obstacles 2).

Why each package, against §5's repo-wide-guard table:

| Package | Why it is in scope |
|---|---|
| `internal/mailing` | the change itself |
| `internal/handlers` | the handler change, **and** the repo-wide comment-citation guards — mandatory because this pass adds Go doc comments |
| `internal/db` | its guards scan `issues/`, so an edit to `issues/0405.md` is in its blast radius |
| `internal/seo` | builds sitemap entries and `/archive/{slug}` route metadata from archive slugs; no code change is expected here, so this is a regression check and its passing is the evidence |

`cmd/opencircuit` is **not** required: no wiring, route table, or `*seo.Site`
construction changes. It costs ~8x a small package, and §5 is explicit that the
scoped run is what keeps concurrent agents workable.

Also required:

- **Zero skips**, and every named package reporting `ok` in non-zero elapsed
  time — a package reporting `ok` in 0.00s did not reach a database.
- `gofmt -l .`, read but **not** `-w`-swept across files you did not edit
  (§8a); format your own files individually.
- Name the specific tests that ran in `## Verification` (criterion 8).
- **No Go-file-plus-line-number citation** anywhere added (`#0352`), scanned
  with `python3` over raw bytes — never BSD `grep -P`, which matches nothing
  here and reports it as absence (§8).
- Prove criterion 4 mechanically: `git diff HEAD --stat --
  internal/mailing/campaign_archive_test.go
  internal/handlers/admin_campaigns_test.go` must be **empty**.
- Prove §6 mechanically: `09-2026` must not appear under `internal/` or
  `web/src/`.

### 9. Migration: none

**No migration is needed, and none should be written.** The month is an *input
to minting*, not durable state: nothing new is persisted, because the only
output — the slug — is already a column. `PRD` §6.2's `email_campaigns`
listing is therefore unchanged, and `prd_index_parity_test.go` (which matches
column names per table) has nothing to see.

Recorded for the reviewer, since the dispatch asked: **`000028` is taken** by
another session's uncommitted `000028_create_crt_commands.{up,down}.sql`, and
`internal/db`'s `verifyContiguous` forbids skipping to `000029`. And per
`#0407`, "frozen" is `CLAUDE.md` §1's *rule* — "never edit a migration that
has been applied to a database anyone cares about" — not the literal range
some prose states. Neither fact bites here, because no migration is added.

No `docs/database.md` change either, both because no migration changes and
because a concurrent agent is working `#0407` in that file (§Obstacles 3).
No `CLAUDE.md` change, for the same reason plus the ~8x cost of dragging
`internal/handlers` in for no gain.

### 10. Reported, not filed (§9)

Two things this pass found that are out of scope here and worth a number.
**Do not file them from a subagent** — report them.

1. **The campaign slug override is unreachable from the admin console.**
   `PATCH /admin/campaigns/{id}` accepts `slug` and enforces every rule
   correctly, but no SPA view sends it: `CampaignDraftFields` has no slug
   field and `CampaignEditor.svelte` renders the archive URL read-only. So
   "the override is how an exception to the template is expressed" is true of
   the API and false of the product. Combined with (2) this is a real trap.
   Fix: an editable slug field in `CampaignEditor.svelte`, enabled only while
   `status == 'draft'`, mapping `409` to the existing messages.
2. **There is no way to delete a campaign draft.** No `DELETE
   /admin/campaigns/{id}` exists, so a draft created by mistake holds its slug
   forever. Harmless while slugs are subject-derived and disambiguated by
   wording; sharper once a slug is a scarce, meaningful `MM-YYYY`.

## Obstacles

Each was checked rather than assumed. Four are cleared; three need the
implementer to route around them; none needs the user before implementation
can start.

| # | Obstacle | State | What clears it |
|---|---|---|---|
| 1 | `CREATEDB` on role `opencircuit`, needed for the isolated test database | **Cleared** | Measured: `select rolcreatedb from pg_roles where rolname=current_user` → `t` |
| 2 | **The shared test template is at version 28**, ahead of this tree's `migrations/` (27), because another session's uncommitted `000028_create_crt_commands` is applied to it and to the shared dev database. Measured read-only: `opencircuit` 28, `opencircuit_test_template` 28, `opencircuit_test` 27, all clean | **Do not touch** | Nothing to clear. `ISSUE=0405 scripts/check.sh …` calls `scripts/testdb.sh create` itself, which detects the version/digest mismatch and provisions directly from `migrations/` (~2s instead of ~0.2s). **Never run `scripts/testdb.sh template`** — it would rebuild from this tree and strip `crt_commands` out from under `#0331`–`#0333`. **Never run `scripts/db-reset.sh`** either |
| 3 | **A concurrent agent may be working `#0407` in `CLAUDE.md` and `docs/database.md`** | Active | Stay out of both files. Nothing in this plan needs either (§9). Before staging, `git status --porcelain` and look |
| 4 | **`000028` is taken** by another session's uncommitted `000028_create_crt_commands.{up,down}.sql`, and `verifyContiguous` forbids skipping to `000029` | Not binding | This plan adds no migration (§9). If a reviewer ever bounces toward one, the number must be coordinated with that session first |
| 5 | Local tooling — `go`, `npm`, `psql`, `migrate` | **Cleared** | All four resolve under `/opt/homebrew/bin/` |
| 6 | **`scripts/check.sh` manages its own database when `ISSUE` is set** — a hand-rolled `scripts/testdb.sh create` alongside it wastes a database and invites a stray leftover | **Cleared, by knowing it** | Use `ISSUE=0405 scripts/check.sh go …` and nothing else; it drops the database on exit |
| 7 | **Editing an issue file drags `internal/db` into scope**, and adding Go doc comments drags `internal/handlers` in (§5's repo-wide-guard table) | Cost, not a blocker | Both are already in §8's package list. Budget for `internal/handlers` being the slowest package in the run (~13–17s under normal load) |
| 8 | **The shared git index** — four shared-index incidents this session (§8a) | Active discipline | `git add <your paths>`, then `git diff --cached --name-only` and **look**; only yours → bare commit, anyone else's staged or dirty → pathspec; then `git show --stat HEAD` and check the **line counts**, not just the filenames |

Two things checked because they easily could have been obstacles and are not:

- **Nothing here needs production, and nothing here needs `#0404`'s deploy to
  have happened.** The template governs campaigns created from now on; the
  September row already has its slug from `#0404`'s migration, whether or not
  that migration has been applied yet (§6).
- **`STORAGE=json` is not in play.** `internal/devstore` implements no campaign
  storage at all, so there is no second implementation of `Create` to keep in
  step. Verified: no campaign type in that package.

**Machine load when this plan was written: `load averages: 2.57 4.57 5.27`** —
at or below §5's ~4 idle baseline on the one-minute figure, well under the ~8
threshold. Recorded as context, explicitly **not** as a baseline for sizing
anything.

## Acceptance criteria

1. A newsletter's default slug is `MM-YYYY`, zero-padded, per the decision above.
2. Non-newsletter campaigns do not silently receive a date-based slug, per your
   answer to question 1.
3. A per-campaign override still works, still rejects a taken slug, and still
   refuses edits once the campaign is past the editable state.
4. Collision behaviour is unchanged — the savepoint-based `UNIQUE` contention
   must not be replaced by a pre-check, and its existing tests must pass
   **unmodified**. Prove they were not edited.
5. Slugs remain non-empty and unique, and minting stays at **draft time**.
6. **Prove the default by test**, including: the second campaign in one month;
   a campaign whose subject is empty or entirely punctuation; and a
   non-newsletter campaign, showing it does not take the month's slug.
7. `#0123`'s archive behaviour is unaffected — the link renders in both HTML and
   text, and the public archive route still serves the slug.
8. Run `./internal/mailing/...`, `./internal/handlers/...`, `./internal/db/...`
   and `./internal/seo/...` (§5's repo-wide-guard table; `internal/seo` builds
   sitemap entries from archive slugs). Per §5 name the specific tests that ran.
   `gofmt` stability. No Go-file-plus-line-number citation (`#0352`), scanned
   with `python3` bytes rather than BSD `grep -P`.

## Review notes

**Phase 3 (review, Opus), 2026-09-03, against commit `1dcb3fa`. Bounced to
`open`.** The code commit is left in place — the design is right, the
end-to-end outcome is correct, and the two defects below are small, additive
fixes to it. Nothing needs redesigning and nothing needs reverting.

Machine load read before every run (`load averages: 3.08 3.84 4.07` at the
start, never above ~4.6 on the one-minute figure) — recorded as context, not
as a baseline for sizing anything.

### What was independently re-verified, and passed

    ISSUE=0405r scripts/check.sh go ./internal/mailing/... ./internal/handlers/... \
        ./internal/db/... ./internal/seo/...

    ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	5.120s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	12.258s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	7.265s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	0.776s

`VERIFICATION PASSED`, `(no skips)` from the audit step, every package in real
non-zero elapsed time, and `gofmt -l` clean by the non-destructive check (no
`-w` sweep). `scripts/check.sh web` also passed. All mutation work below ran
in a **throwaway git worktree** at `HEAD`, never in the shared tree (§8a).

**The user's actual outcome, traced end to end rather than inferred.** A
throwaway handler test (worktree only, never committed) posted the exact wire
value the modal produces for September 2026 and read the minted slug back out
of `email_campaigns`:

    E2E OK: wire 2026-09 -> slug "09-2026" -> /archive/09-2026

The full chain checks out at every link: `openCreate()` resets
`newNewsletterMonth = currentMonthValue(new Date())`, so the month input opens
on the current month zero-padded by `padStart` (the `getMonth()`-is-zero-based
trap is closed, and `getFullYear`/`getMonth` are the local-calendar readers a
`<input type="month">` needs); `submitCreate` spreads the key in **only** when
the checkbox is ticked, so the absent-means-subject-derived default stays
reachable; the handler trims and parses; `Create` swaps `base`; the INSERT
writes it. The inversion is handled correctly — `%02d-%04d` over
`(month, year)`.

**Criterion 4 — collision handling byte-identical, proved by hash, not by
reading.** `Create`'s region from `tx, err := s.pool.Begin(ctx)` to the end of
the function is **1803 bytes with an identical SHA-256** before and after
`1dcb3fa`, as is the function's prologue up to the `base :=` line. The diff
inside `Create` is **six added lines and zero removed**. The two existing
slug-test files hash identically at `1dcb3fa~1` and `HEAD`:
`campaign_archive_test.go` `ffd1b6eb1e7878eb`,
`admin_campaigns_test.go` `df17df4e1ca8b48e`.

**The template inherits collision handling**, confirmed behaviourally rather
than structurally, including the case the plan never considered: a campaign
whose *subject* slugifies to the same `MM-YYYY` shape, created first, makes
the later newsletter for that month `07-2033-2` — degraded gracefully, no
error, no pre-check.

**#0404's row cannot be disturbed.** `Create` runs on INSERT only. The single
statement that writes `slug` on an existing row is `Update`'s
`UPDATE email_campaigns … slug = $7`, and it takes the value from `in.Slug`
with an empty-value fallback to `current.Slug` — it never re-derives from the
subject. `SetArchiveStatus` writes `archive_status` only. Nothing else in
`internal/` writes the column. There is no backfill or re-mint step anywhere
in the commit.

**The `09-2026` prohibition holds.** Re-ran the byte scan independently over
every file under `internal/` and `web/src/` with `python3` reading bytes:
`HITS: []`. The `#0352` Go-file-plus-line-number scan over all ten changed
files also found zero matches.

**The transposed-input property holds**, including the ambiguous months. Every
`MM-YYYY` slug fed back in as a wire value is rejected outright rather than
misread — `01-2026`, `11-2026`, `12-2026`, `03-2031`, `11-1112` all rejected.
So do `2026-13`, `2026-00`, `2026-1`, `2026-009`, `0000-01`, `10000-01`,
`999-01`, `2026-09x`, `2026-09-01`, embedded `\n` and `\x00`, and full-width
and Arabic-Indic digits (RE2's `\d` is ASCII-only). Whitespace-padded input is
rejected by the parser and tolerated by the handler, which trims first.

### Defect 1 — a doc comment names a constant that does not exist

`internal/mailing/newsletter_month.go`, in the *"Why this decision does not
retroactively change minted slugs"* section of the package doc comment:

> `newsletterSlugFormat` (campaigns.go) is a constant, not a settings row

**There is no `newsletterSlugFormat` anywhere in the tree.** The only two
occurrences of that identifier in the whole repository are this comment and
`## Plan` §4, which *prescribed* it. The format string is instead inlined as a
literal in `Slug()`. So the comment is wrong twice over: it asserts a
declaration that does not exist, and it sends a reader to the wrong file for
it — the format lives in `newsletter_month.go` itself, not `campaigns.go`.

No guard catches this. `TestNoDocCommentNamesADifferentDeclarationInSameFile`
compares a doc comment's **first word** against the declaration it is attached
to; a mid-comment reference to a non-existent identifier is outside its reach.
That is why it needs a human-noticed fix rather than a test.

It also falsifies two claims in this issue's own prose: `## Fix`'s
"Implemented the plan exactly as specified — no deviation", and `## Files
changed`, which records no deviation either. `## Plan` §4 was the *answer to
question 4* — "an unexported format constant in `internal/mailing`", argued
against a settings row — so this is a departure from a decided point, not an
incidental wording slip.

**The remedy, precisely.** Introduce the constant the comment already claims
exists, in `newsletter_month.go` (not `campaigns.go`), and use it:

    // newsletterSlugFormat is #0405's user-decided MM-YYYY archive-slug
    // template: zero-padded two-digit month, hyphen, four-digit year.
    // A constant rather than a settings row — issues/0405.md §4 records
    // the reasoning. Changing it in a future commit does not, and must
    // not, alter a slug already written to email_campaigns.slug; Create
    // is the only writer and it runs once, at INSERT time.
    const newsletterSlugFormat = "%02d-%04d"

`Slug()` then becomes
`return fmt.Sprintf(newsletterSlugFormat, int(m.month), m.year)`, and the
package doc comment's sentence must drop its "(campaigns.go)" parenthetical,
since the constant now lives in the same file. Correct `## Fix` and add the
constant to `## Files changed`.

(Rewriting the comment to describe the inline literal instead would also make
it true, but it loses `## Plan` §4's decision — a named constant is the thing
a future reader greps for. Prefer the constant.)

### Defect 2 — the non-newsletter guarantee misses the suffixed case

`TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug` **can fail** —
that much was proved, not assumed. Mutating `admin_workshop_announce.go` to
set a `NewsletterMonth` on its own `CampaignInput` produced:

    admin_campaigns_newsletter_slug_test.go: announced campaign's slug = "07-2031",
      matches the MM-YYYY template shape — it must not consume a newsletter month
    --- FAIL: TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug

So the guarantee is not vacuous, and the realistic regression — a clock-based
inference on the announce path, which in this test would infer the *current*
month against the test's far-future `2031-03` newsletter — is caught.

**But the same mutation with the newsletter's own month passes.** Setting the
announce path's month to `2031-03` instead of `2031-07`:

    --- PASS: TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug

The announcement gets `03-2031-2`, and `newsletterSlugShape`
(`^[0-9]{2}-[0-9]{4}$`) does not match it because of the trailing suffix — so
an announcement that *did* take a month-template slug slips through the very
assertion written to catch it. The second half also passes, because the
newsletter is created first and therefore keeps the unsuffixed slug.

This is a narrow gap rather than a hole in the feature: the implementation is
correct (the announce path never sets the field, by construction), and the
regression shape that would realistically reintroduce the bug is the one that
*is* caught. But the assertion's own error message says "it must not consume a
newsletter month", and `03-2031-2` is a consumed newsletter month.

**The remedy, precisely.** Widen the shape regex to admit the collision suffix,
so any `MM-YYYY`-derived slug is rejected however it was uniquified:

    // newsletterSlugShape matches #0405's MM-YYYY template INCLUDING the
    // collision suffix Create may append (-2, -3, …): an announcement that
    // took a month-template slug and was pushed to a suffix has still
    // consumed that month, so the bare-shape pattern would let exactly the
    // regression this test exists to catch slip through.
    var newsletterSlugShape = regexp.MustCompile(`^[0-9]{2}-[0-9]{4}(-[0-9]+)?$`)

That is the whole change — one character class, no new test, no new fixture.
Re-run `./internal/handlers/...` and confirm the test still passes on the
unmutated tree.

### Mutations run, and what each showed

| Mutation | Result |
|---|---|
| Announce path sets a `NewsletterMonth` for a **different** month | **Caught** — the test fails naming the slug |
| Announce path sets a `NewsletterMonth` for the **newsletter's own** month | **Missed** — defect 2 |
| `Slug()` drops zero-padding (`%d-%d`) | **Caught** — six tests fail across both packages |
| Parse pattern loosened to a 2–4-digit year and optional month padding | **Caught** — `TestParseNewsletterMonth_Rejects` fails |
| `valid()` backstop removed from `Create` | **Caught** — `TestCampaignStore_Create_RejectsInvalidNewsletterMonth` fails |
| Wire value transposed (`MM-YYYY` fed in as `YYYY-MM`), 5 months incl. `11-2026`/`12-2026` | Rejected outright in every case |
| 13th month, month `00`, year `0000`, 5-digit year, 3-digit year, unicode digits, embedded NUL/newline | All rejected |
| Subject-derived slug already holding the template's value | Degrades to `-2` through the existing loop; no error |

### Not defects — recorded so the next pass does not "fix" them

- **`0001-01` and `9999-12` parse**, giving slugs like `01-0001`. `valid()`
  enforces only `year >= 1`, so there is no plausible-range floor. Harmless:
  the operator has to type it deliberately into a month picker, the result is
  visible immediately in the editor's archive-URL field, and no plan criterion
  asked for a range. Leave it.
- **Checking the box and clearing the month input** sends `""`, which the
  handler answers `400 newsletter_month must be YYYY-MM`, surfaced in the
  modal's `createError`. Loud and correct.
- The `web/src/lib/api.ts` comment's claim about `decodeJSON`'s
  `DisallowUnknownFields` was checked and is accurate (`auth.go`'s `decodeJSON`
  sets it), so `newsletter_month` really would be rejected rather than ignored
  on the PATCH path.
- `#0410` (no admin UI for the slug override) and `#0411` (no campaign DELETE)
  remain out of scope and untouched, as dispatched.

### Nothing new to file

No new issue is warranted. Both defects above belong to `#0405` itself and are
fixed inside it. `#0413` remains the highest allocated number as far as this
pass could see.

### Second review (phase 3, Opus), 2026-09-03, against commit `e5c5d2d`. Approved.

Deliberately narrow: `21d6d0c`'s pass verified the substance exhaustively and
none of it was re-litigated. This pass checked the two prescribed remedies, and
checked them by mutation rather than by reading.

Machine load read before every run — `load averages: 1.55 2.22 2.94` at the
start, never above 2.0 on the one-minute figure. Recorded as context, not as a
baseline for sizing anything.

**The three-way proof, reproduced from scratch.** A throwaway
`git worktree add --detach` at `e5c5d2d`, its own scratch database
(`opencircuit_test_0405r2m`, created and dropped by hand, never the shared
`opencircuit_test`), scored by `go test`'s **exit code**, not by reading its
prose:

| # | Code | `newsletterSlugShape` | Exit | Result |
|---|---|---|---|---|
| 1 | unmutated | widened `^[0-9]{2}-[0-9]{4}(-[0-9]+)?$` | `0` | **PASS** — control |
| 2 | announce path sets `NewsletterMonth` to the newsletter's own month | widened | `1` | **FAIL**, naming `announced campaign's slug = "03-2031-2"` |
| 3 | *same* mutation | old narrow `^[0-9]{2}-[0-9]{4}$` | `0` | **PASS — incorrectly** |

Row 3 is the one that decides it: the identical defect slips through the old
pattern silently, so the widening closed a real hole. The mutation was applied
to `internal/handlers/admin_workshop_announce.go`'s own `CampaignInput` literal
(`ParseNewsletterMonth("2031-03")`, the newsletter's own month), which is the
exact regression shape the guard exists to catch.

**`newsletterSlugFormat` is load-bearing, proved by mutating its value.**
Declaring a constant and then not using it would have satisfied the letter of
defect 1 while leaving the doc comment as decorative as before. It does not:
`Slug()`'s body is `fmt.Sprintf(newsletterSlugFormat, int(m.month), m.year)`,
and changing the constant to `"%d-%d"` in the worktree failed **six** tests
across both packages — `TestCampaignStore_Create_NewsletterMonthMintsTemplateSlug`,
`TestCampaignStore_Create_SecondNewsletterInSameMonthGetsSuffix`,
`TestCampaignStore_Create_NewsletterMonthIgnoresPunctuationOnlySubject`,
`TestParseNewsletterMonth_Valid`,
`TestAdminCampaigns_Create_NewsletterMonthMintsTemplateSlug`,
`TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug`. A
`python3`-over-bytes scan of the whole tree finds the identifier in exactly one
Go file, four times: the package doc comment, the constant's own doc comment,
the declaration, and `Slug()`'s single use.

On whether the comment can drift again: only halfway. A *deletion* of the
constant is now a compile error, because `Slug()` references it — that is the
whole difference from the state `21d6d0c` bounced. A *rename* would still leave
the prose naming an identifier that no longer exists, and no guard reaches a
mid-comment reference (`TestNoDocCommentNamesADifferentDeclarationInSameFile`
compares a doc comment's first word against its own declaration). Not worth a
guard for one identifier; recorded so a future rename knows to carry the two
comment mentions with it.

**Nothing else moved, proved by hash rather than by `git diff --stat`.**
`internal/mailing/campaigns.go` is byte-identical (SHA-256 `76597f926c4c8a58…`)
at `1dcb3fa`, `21d6d0c`, `e5c5d2d` and in the working tree — so `Create`'s
savepoint-per-candidate collision loop is exactly what `21d6d0c` verified, and
`#0404`'s backfilled production row is untouched by this pass by construction.
The two pre-existing slug-test files hash identically at `1dcb3fa~1` (before
this issue began), `1dcb3fa`, `e5c5d2d` and in the working tree —
`campaign_archive_test.go` `ffd1b6eb1e7878eb…`, `admin_campaigns_test.go`
`df17df4e1ca8b48e…`, matching the values `21d6d0c` recorded. Criterion 4 holds
across both implementation passes, not just the first.

**The `09-2026` scan, re-run independently** with `python3` reading bytes over
every file under `internal/` and `web/src/`: `HITS: []`. The `#0352`
Go-file-plus-line-number scan over both files `e5c5d2d` changed also returns
zero matches. Neither used BSD `grep -P`.

**The two recorded non-defects were left alone, verified in the code.**
`valid()` is still `m.year >= 1 && m.month >= time.January && m.month <=
time.December` — no plausible-year floor was added, so `0001-01` still parses,
deliberately. And the handler still answers a cleared month `400
newsletter_month must be YYYY-MM` via `ParseNewsletterMonth` on the trimmed
value; loud, as recorded.

**`## Fix`'s falsified claim is corrected**, not restated: it now opens
"Implemented the plan exactly as specified, **with one correction made in a
second implementation pass**", and `## Files changed` records the constant
against `newsletter_month.go`.

Verification, in the tracked tree, database dropped on exit:

    ISSUE=0405r2 scripts/check.sh go ./internal/mailing/... ./internal/handlers/... \
        ./internal/db/... ./internal/seo/...

    gofmt -l  (clean)
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	5.197s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	11.939s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	6.902s
    ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	0.932s
    (no skips)   VERIFICATION PASSED

    scripts/check.sh web
    290 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
    Test Files  40 passed (40)   Tests  3996 passed (3996)
    VERIFICATION PASSED

Every DB-backed package reported real non-zero elapsed time; `gofmt -l` was
read, never `-w`-swept. Named tests, run under `TAIL=200 ISSUE=0405r2
scripts/check.sh go ./internal/mailing/... ./internal/handlers/... -run
Newsletter -v` and the same with `-run Slug -v` — all twelve new tests
(`TestCampaignStore_Create_NewsletterMonthMintsTemplateSlug`,
`_SecondNewsletterInSameMonthGetsSuffix`,
`_NewsletterMonthIgnoresPunctuationOnlySubject`,
`_WithoutNewsletterMonthStillMintsFromSubject`,
`_RejectsInvalidNewsletterMonth`, `TestParseNewsletterMonth_Valid`,
`TestParseNewsletterMonth_Rejects`, `TestNewsletterMonth_ZeroValueIsInvalid`,
`TestAdminCampaigns_Create_NewsletterMonthMintsTemplateSlug`,
`_OmittedNewsletterMonthMintsFromSubject`,
`_InvalidNewsletterMonthReturns400`,
`TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug`) and every
pre-existing slug test criterion 3 names passed, plus criterion 7's
`TestRenderCampaign_ArchiveLinkInBothPartsWhenSlugSet`,
`TestRenderCampaign_NoArchiveLinkWhenSlugEmpty` and
`TestPublicArchive_GetBySlug_VisibilityByStatus` — so the archive link and the
public archive route are unaffected.

**Escalation: not warranted, and the §3 record is that this was the second
bounce.** Both bounces were on small additive fixes with the remedy written out
verbatim, not on the same design problem twice. `CLAUDE.md` §3's trigger is two
bounces on one *design* problem, and the decide-versus-apply test puts this
pass squarely on "apply" — the second implementation pass had the constant
name, the placement, and the exact widened pattern handed to it, and landed
both in one commit of 18 insertions and 5 deletions.

**Nothing new to file.** `#0410` and `#0411` remain the two known, accepted
gaps and were not touched. `#0415` is still the highest allocated number as far
as this pass can see. Nothing was applied to any database beyond the two
scratch databases (`opencircuit_test_0405r2m`, dropped by hand;
`opencircuit_test_0405r2`, dropped by `scripts/check.sh` on exit), no process
this pass did not start was signalled, and production was not touched.

## Relation

- Relates to: [#0404](0404.md) — the deploy blocker that backfills a slug onto
  the existing production row; these two must agree on `09-2026`
- Relates to: [#0123](0123.md) — which built the archive, the slug, and the
  compose-time minting whose default this changes
- Relates to: PRD §6.8 — the public campaign archive

## Work log

- 2026-09-03 — **Second review pass (phase 3, Opus). Approved; status
  `resolved`.** Deliberately narrow — `21d6d0c` had already verified the
  substance exhaustively, and none of it was re-litigated. **Reproduced the
  three-way mutation proof from scratch** in this reviewer's own throwaway
  worktree and scratch database, scored by `go test` exit code: control
  passes (`0`), the announce path mutated to take the newsletter's own month
  fails against the widened regex (`1`, naming `03-2031-2`), and the *same*
  mutation against the old narrow `^[0-9]{2}-[0-9]{4}$` passes incorrectly
  (`0`) — the row that proves the widening closed a real hole rather than
  being cosmetic. **Proved `newsletterSlugFormat` load-bearing by mutating its
  value**, not by reading its declaration: `"%d-%d"` fails six tests across
  both packages, and the identifier appears in exactly one Go file with
  `Slug()` as its only use, so a deletion is now a compile error (a rename
  would still drift the two comment mentions — recorded, not guarded).
  **Confirmed nothing else moved, by hash:** `internal/mailing/campaigns.go`
  is byte-identical across `1dcb3fa`, `21d6d0c`, `e5c5d2d` and the working
  tree, and both pre-existing slug-test files hash identically at `1dcb3fa~1`
  through the working tree, matching `21d6d0c`'s recorded values — so
  criterion 4 holds across both implementation passes and `#0404`'s row is
  untouched. Re-ran the `09-2026` byte scan (`HITS: []`) and the `#0352`
  citation scan (zero matches) independently with `python3` over bytes.
  Verified the two recorded non-defects were left alone (`valid()` still has
  no plausible-year floor; a cleared month still answers `400`) and that
  `## Fix` no longer carries the falsified "no deviation" claim.
  `ISSUE=0405r2 scripts/check.sh go ./internal/mailing/...
  ./internal/handlers/... ./internal/db/... ./internal/seo/...` and
  `scripts/check.sh web` both `VERIFICATION PASSED`, zero skips, real non-zero
  elapsed times, `gofmt -l` clean non-destructively. **Escalation not
  warranted** — both bounces were small additive fixes with the remedy written
  out verbatim, not the same design problem twice (`CLAUDE.md` §3's
  decide-versus-apply test). No new issues filed or warranted; `#0415` remains
  the highest allocated. Both scratch databases dropped; production untouched.

- 2026-09-03 — **Review pass (phase 3, Opus). Bounced to `open`.** Re-ran
  `ISSUE=0405r scripts/check.sh go ./internal/mailing/... ./internal/handlers/...
  ./internal/db/... ./internal/seo/...` and `scripts/check.sh web` independently;
  both `VERIFICATION PASSED`, zero skips, real elapsed times, `gofmt -l` clean
  non-destructively. **The user's outcome was traced end to end rather than
  inferred** — a throwaway worktree test posted the modal's own wire value for
  September 2026 and read `/archive/09-2026` back out of `email_campaigns`.
  Collision handling proved byte-identical by SHA-256 over `Create`'s
  post-`base` region (1803 bytes, same hash) with six lines added and none
  removed; both named existing slug-test files hash identically across the
  commit; the template inherits the savepoint loop, including against a
  subject-derived slug that already holds the same value. Nothing re-derives an
  existing slug, so #0404's row is untouched by construction. The `09-2026` and
  #0352 byte scans were re-run independently and are clean, and the
  transposed-input property holds for every month tried including `11-2026` and
  `12-2026`. **Eight mutation classes run in a throwaway git worktree**, six
  caught. **Two defects, both small and additive, with exact remedies written
  in `## Review notes`:** (1) the package doc comment in `newsletter_month.go`
  asserts a constant `newsletterSlugFormat` in `campaigns.go` that **does not
  exist anywhere in the tree** — the format is an inline literal in `Slug()`,
  which also falsifies `## Fix`'s "no deviation" claim against `## Plan` §4's
  decided answer to question 4; (2)
  `TestAdminWorkshopAnnounce_DoesNotConsumeTheNewsletterMonthSlug` **can** fail
  (proved by mutation) but its `^[0-9]{2}-[0-9]{4}$` shape does not match a
  collision-suffixed `MM-YYYY` slug, so the same mutation using the
  newsletter's own month passes. Code commit `1dcb3fa` left in place. No new
  issues filed or warranted; nothing was applied to any database beyond the
  scratch `opencircuit_test_0405r`, which `check.sh` dropped on exit, and
  production was not touched.

- 2026-09-03 — **Planning pass (phase 1, Opus).** Verified every claim in the
  dispatch and in this issue against the tree rather than transcribing it; the
  measurements are tabulated in `## Plan` §0. Answers: **(1) opt-in, never
  inferred** — the schema carries no campaign-kind column, and all three
  candidate inference rules fail against the real call sites (`workshop_id IS
  NULL` also covers every hand-composed one-off; `audience_mode = 'all'` is
  what the SPA's "New campaign" modal sends for *every* draft **and** what the
  workshop-announce shortcut falls back to for an interest-less workshop, so
  it is unconditional in disguise; subject matching is guessing at prose).
  Opt-in is the only shape whose failure mode is loud and reversible.
  **(2) the author supplies the month**, defaulted to the current month in the
  compose form, so `CampaignStore.Create` never reads a clock and a newsletter
  drafted 31 August for September is not misnamed. **(3) `09-2026-2` stands** —
  it is unique, honest, and rare, and criterion 4 forbids replacing the
  collision algorithm anyway; but the likelier trigger is a stray duplicate
  draft, and there is **no `DELETE /admin/campaigns/{id}`** and **no admin UI
  for the slug override**, which is the real trap (reported, not filed).
  **(4) a constant**, not a settings row: every existing settings key is
  operator-tunable runtime behaviour, a URL format is a published contract, and
  a setting would falsely imply it could be changed retroactively. The change
  is two lines inside `Create` plus one optional `CampaignInput` field, one
  small `NewsletterMonth` type, one optional `newsletter_month` request field,
  and a checkbox plus native month input in the compose modal. **Nothing
  re-derives or overwrites an existing slug**, so `#0404`'s backfilled
  `09-2026` on production's id 1 is untouched by construction. **No migration**
  — the month is an input to minting, not persisted state. Nothing was applied
  to any database; production was not touched at all this pass. Status stays
  `open`.

- 2026-09-03 — Filed by the orchestrator at the user's request, after checking
  which of four asks were already covered. **Three already exist**: the archive
  index page at `/archive`, its link in the site footer (and a second link on the
  home page), and sitemap coverage — `/archive` sits in the sitemap's marketing
  routes and every published entry is emitted as `/archive/{slug}`. Only the slug
  template was missing. The user then specified `MM-YYYY` directly, which this
  issue records as decided.

- 2026-09-03 — **Implementation pass (phase 2, Sonnet), commit `1dcb3fa`.**
  Followed `## Plan` exactly: `CampaignInput.NewsletterMonth`, the four-line
  branch in `Create`, `newsletter_month.go`, the handler's optional field, and
  the compose modal's checkbox plus native month input. One premise corrected
  before it reached code: `CampaignDraftFields` lives in `web/src/lib/api.ts`,
  not `types.ts` as `## Plan` §5 stated. One mistake self-caught before
  committing: an early draft used the literal `09-2026` in the modal's help
  text and in three Go doc comments/one test fixture — all rewritten to
  describe the shape in prose or use the package's own `2031-03` test month;
  a `python3` byte scan over every file under `internal/` and `web/src/`
  confirms zero occurrences of the literal in the committed tree. All three
  named-unmodified slug tests confirmed byte-identical via an empty `git diff
  --stat`; all 8 new mailing tests and all 4 new handler tests (including the
  non-newsletter proof against the workshop-announce shortcut) individually
  confirmed passing by name. `ISSUE=0405 scripts/check.sh go
  ./internal/mailing/... ./internal/handlers/... ./internal/db/...
  ./internal/seo/...` and `scripts/check.sh web` both report
  `VERIFICATION PASSED`, zero skips, real non-zero elapsed times. Full detail
  in `## Verification`. Status left `in-progress` for review; not marking
  `resolved` (§9 — reviewer's call).

  **Reported, not filed (§9 — do not file from a subagent):** nothing new
  beyond `## Plan` §10's two already-reported gaps (`#0410` the missing
  admin-UI slug override, `#0411` the missing campaign DELETE) — both
  confirmed still out of this issue's scope and not touched here. Re-checked
  before finishing: `#0412` and `#0413` are now also allocated (by other
  concurrent work — `internal/db`'s PRD-index-parity guard's closing
  sentence, and a word-boundary gap in `docRangePattern`, neither related to
  this issue), so `#0413` is the highest allocated as of this pass, not
  `#0411`. Not filed here regardless — this pass has nothing new to report.

- 2026-09-03 — **Second implementation pass (phase 2, Sonnet), commit
  `e5c5d2d`.** Fixed both defects `21d6d0c`'s review named, exactly as
  prescribed. **§3 escalation to Opus was considered and declined**: the
  reviewer's remedies named the exact constant (`newsletterSlugFormat =
  "%02d-%04d"`, placed in `newsletter_month.go` where `Slug()` is its only
  consumer) and the exact widened pattern
  (`^[0-9]{2}-[0-9]{4}(-[0-9]+)?$`) — this pass had nothing to decide, only
  to apply, which is squarely Sonnet's job per CLAUDE.md §3's
  decide-vs-apply test. Verified the constant is actually referenced by
  `Slug()` (not just declared) before considering the first defect closed.
  Proved the widened regex closes the second defect's hole with a mutation
  test in a throwaway `git worktree` against its own scratch database
  (`opencircuit_test_0405b`, dropped after): a mutated announce handler that
  sets `NewsletterMonth` to the newsletter's own month now fails the guard
  test (caught, with the exact suffixed slug `03-2031-2` in the failure
  message), where the same mutation against the old narrow regex passed
  silently — proving the widening was necessary, not just plausible. Control
  (unmutated, widened regex) passes. `internal/mailing/campaigns.go` was not
  touched — confirmed by an empty `git diff --stat` — so `Create`'s
  collision handling and `#0404`'s already-backfilled production row are
  undisturbed by this pass. Full run: `ISSUE=0405b scripts/check.sh go
  ./internal/mailing/... ./internal/handlers/... ./internal/db/...
  ./internal/seo/...` and `scripts/check.sh web`, both `VERIFICATION
  PASSED`, zero skips, real non-zero elapsed times, `gofmt -l` clean
  non-destructively. Re-ran the `09-2026` byte scan and the `#0352`
  line-number-citation scan over both changed files after the edits — both
  clean, both via `python3` over bytes, never `grep -P`. Committed only the
  two code files (`internal/mailing/newsletter_month.go`,
  `internal/handlers/admin_campaigns_newsletter_slug_test.go`) with a
  pathspec, after confirming the staged index held only those two files;
  `git show --stat HEAD` confirmed 2 files changed, 18 insertions(+), 5
  deletions(-) — matching the pre-commit diff exactly. Status left
  `in-progress` for review; not marking `resolved` (§9 — reviewer's call).
  **Reported, not filed (§9):** nothing new beyond what earlier passes
  already reported (`#0410`, `#0411`, both still out of scope and
  untouched). Re-checked before finishing: `#0414` and `#0415` are now also
  allocated, by another concurrent session (`#0414`/`#0415`, the
  sending-identity doc-drift and stale-SES-sandbox-status issues, neither
  related to this one), so `#0415` is the highest allocated as of this pass.
  `#0271` (flagged as possibly concurrent in the dispatch) was already
  resolved and committed (`f93e94f`) before this pass started, and its file
  was never touched here.
