# 0390 — `Counts` cannot tell `Sending` from `Abandoned`: swapping them passes the whole repo

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-02 |
| **Commit** | — |

## Description

`#0389`'s review swept **all ten** two-column swaps in `outbox.Store.Counts`'
five-column `count(*) FILTER (WHERE status = $n)` query. **Nine are detected.
One survives.**

Swapping `StatusSending` and `StatusAbandoned` in the positional argument list
leaves `internal/outbox` green **and** `internal/handlers` green (`ok`, 5.590s
under the mutation), so **nothing in the tree detects it.**

**The cause is precise**: `Counts.Sending` is asserted by **no test at all**,
and `Counts.Abandoned` only ever as *unchanged* — in
`TestOutbox_Counts_ReportsSkippedDistinctlyFromAbandoned`'s "a skip must never
count as a delivery-health failure" check. **Two columns that are only ever
both-zero-delta are mutually indistinguishable.**

Pre-existing: it passes at `c921ba9^` too, so `#0389` neither caused nor widened
it. That issue closed `Queued`/`Sent` and confirmed `Abandoned`/`Skipped` was
already closed by an existing asymmetric assertion.

**This is `#0364`'s finding a third time** — an assertion passing for a reason
that does not distinguish the thing it names.

## The remedy's shape, from the review that found it

Same as `#0389`'s: **a moment where the two must move by different amounts.**
Two candidates:

- claim a row without finishing it — `Sending +1`, `Abandoned` unchanged;
- drive one to `abandoned` via the retry path past `queue_max_retries` and
  assert the other stayed put.

**`internal/handlers` cannot help**: the dashboard reaches `Counts` through an
interface, so no handler test exercises the real SQL. The assertion must live in
`internal/outbox`.

## Acceptance criteria

1. `TestOutbox_Counts` (or a sibling) fails under the `Sending`/`Abandoned`
   swap. **Prove it by running that exact mutation** in a `git archive` export
   with your own database (§8b), never the tracked tree.
2. **Re-sweep all ten two-column swaps** and report which are detected. Nine
   were before this change; all ten must be after. A fix closing one while
   opening another is a net loss — `#0389` checked exactly this and it is cheap
   to repeat.
3. The three `#0386` mutants must still fail: all-zeros; `Queued` filtering
   `'sending'`; `Queued` dropping its `FILTER`. And `#0389`'s `mid.Queued` /
   `mid.Sent` assertions must still fire — its reviewer needed a separate probe
   for `mid.Sent`, since everything else fatals first.
4. **Both halves of any new assertion must be load-bearing.** Show each fires
   under some mutation and neither fires on unmutated code. `#0354`, `#0358` and
   `#0363` each shipped a check that could not fail.
5. `TestOutbox_Counts` must still pass **run alone**, and the package must keep
   **zero run-alone failures** across all 39 tests (`go test -list` says 39; a
   textual `^func Test` scan says 42, the extra three being `TestMain` and two
   matches inside raw-string fixtures).
6. Per §5 name the specific tests that ran; the mailing subsystem needs
   Postgres. `gofmt` stability. Run `./internal/outbox/...` and
   `./internal/handlers/...` (`#0381`). No `.ClaimDue(` / `.OrphanSweep(` /
   `.SelectDue(` literal in any comment (`#0323`), scanned with `python3` bytes
   rather than BSD `grep -P`. No `<file>.go:<int>` citation (`#0352`).
7. **Measure any population figure yourself** — `44 / 12` at filing, and the
   orchestrator propagated a stale `42 / 12` nine times this phase.

## A caution from the review that found this

Its own first mutation sweep used `${3:+-run "$3"}`, which expands to a
**single** argument; the binary rejected it and the runner — scoring by grepping
for `ok` — marked **every** mutant FAIL, the expected answer for an unrelated
reason. It was caught only because the **unmutated control row also read FAIL**.

**Include an unmutated control in your sweep, and check it passes.** That is the
cheapest available protection against measuring your own harness instead of the
code, and it is the same defect this issue is about.

## Relation

- Found by: [#0389](0389.md)'s review, sweeping the full swap space
- Relates to: [#0389](0389.md) — which closed `Queued`/`Sent`
- Relates to: [#0386](0386.md) — which made the test residue-proof
- Relates to: [#0364](0364.md) — an assertion passing for the wrong reason, the
  third instance
- Relates to: [#0380](0380.md) — which added `Skipped` to this query

## Work log

- 2026-09-02 — Filed by the orchestrator from `#0389`'s review (§9).
