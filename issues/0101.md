# 0101 — Link the Luma events calendar alongside the Discord invite

| | |
|---|---|
| **Status** | resolved |
| **Module** | web |
| **Platform** | All |
| **Phase** | 2 — Brand and marketing pages |
| **First seen** | 2026-08-20 |
| **Commit** | b3cbd1c |
| **Closed** | 2026-08-21 |

> 🟢 Resolved 2026-08-21 — every claim in `## Fix` was re-checked against the
> diff rather than taken on trust. **The §9 constraint holds:** `git show
> b3cbd1c` is five files and 26 added lines with no `<script>`, no `iframe`,
> and no embed; a production build of the SPA references exactly four external
> hosts in its entire bundle — `w3.org` (SVG namespace), `svelte.dev` (a
> framework error URL), `discord.gg`, and `luma.com` — so nothing third-party
> loads. **The URL really is defined once:** `grep -rn discord.gg web/src`
> returns only `links.ts` and the router test, confirming both hand-copied
> `const DISCORD_URL` lines were removed rather than one being left behind.
> **The router test is not vacuous** — mutation-checked by replacing
> `if (url.origin !== c.currentOrigin) return false;` in `router.ts` with a
> comment, which failed both the new Luma case and the existing cross-origin
> case; the file was restored from a copy taken first (`CLAUDE.md` §8a) and
> `git diff b3cbd1c` is empty for all code files. The case is meaningfully
> distinct from the Discord one: `candidate()` leaves `target` null, so the
> assertion exercises the cross-origin branch itself rather than short-
> circuiting on the `target="_blank"` the real anchors carry. **The privacy
> sentence is modest** — "does not cover third-party services like Discord or
> Luma, which have their own privacy policies" asserts only that the policy
> does not cover them, making no claim about what Luma collects, which is the
> failure mode that bounced `#0070` twice. The footer link inherits the
> `@media (max-width: 480px)` ≥40 px rule because that rule targets
> `.footer-links a` generically; About's copy no longer says Discord is where
> workshops get announced. Nothing was built toward `#0053`, no Go file was
> touched, and `web/dist/` still carries only the checked-in placeholder.
> Verified independently on an idle machine (load 1.19): `npm run check` →
> 199 files, 0 errors/0 warnings; `npm test` → 9 files, 178 tests passed,
> including `shouldIntercept > does not intercept the Luma calendar link`
> confirmed by name under `--reporter=verbose`.
>
> One gap this issue correctly did not cover is filed as
> [#0115](0115.md): production still serves `placeholder/index.html`, whose
> only call to action is the Discord invite, so the calendar the user added to
> promote events is not yet visible on the live site.

## Description

Open Circuit SF now has a Luma calendar at <https://luma.com/opencircuitsf>,
which is where upcoming events get promoted. The site links Discord in two
places and the calendar in none, so the one surface built for "what's coming up"
is invisible to anyone who lands on the site.

## Acceptance criteria

- [x] **The calendar is linked in the footer, beside the Discord link.**
      `web/src/lib/Footer.svelte` renders a `Calendar` (or `Events`) link in
      `.footer-links`, with the same `target="_blank" rel="noopener noreferrer"`
      treatment, a `·` separator, and the `@media (max-width: 480px)` ≥40 px
      tap-target rule the existing footer links already get.
- [x] **The calendar is linked from About's "Get involved" section.**
      `web/src/views/About.svelte` currently says "The easiest way in is
      Discord — that's where workshops get announced". That is now only half
      true. Name the calendar as where upcoming sessions are listed and RSVP'd,
      and update the `StatusList` item `'announced through Discord and the
      mailing list'` to include it.
- [x] **The URL is defined once, not copy-pasted.** `DISCORD_URL` is already
      duplicated verbatim in `Footer.svelte` and `About.svelte`; a second
      hand-copied constant would make three. Put both external links in a shared
      module (a new `web/src/lib/links.ts` alongside `branding.ts` — note
      `branding.ts`'s own comment says not to use it for functional values) and
      import from there.
- [x] **It is a plain link — no Luma embed, widget, script, or iframe.**
      `CLAUDE.md` §9 forbids third-party scripts, external CDNs, and trackers;
      the site is self-contained by design. Luma's embed loads their JS. An
      `<a href>` is the whole feature.
- [x] **The router does not intercept it.** `shouldIntercept` must return
      `false` for the Luma URL the same way it does for the Discord invite. Add
      a case to `web/src/lib/router.test.ts` beside the existing
      `does not intercept a cross-origin href` test rather than assuming the
      cross-origin branch covers it.
- [x] **The privacy policy names Luma.** `web/src/views/PrivacyPolicy.svelte`
      says the policy "does not cover third-party services like Discord, which
      has its own privacy policy." Someone who RSVPs on Luma hands their details
      to a third party the same way. Add Luma to that sentence. Do not make any
      claim about what Luma collects — the sentence's job is to say the policy
      does not cover it.
- [x] **Confirm the canonical URL form before hardcoding it.** Luma serves both
      `luma.com` and the shorter `lu.ma`. Use whichever the calendar actually
      canonicalizes to rather than guessing, and note in `## Gotchas` which one
      it was.
- [x] `npm run check` and `npm test` pass from `web/`.

## Notes

The user added the calendar on 2026-08-20 and gave the URL as
`https://luma.com/opencircuitsf`.

**Scope: two link sites plus the privacy sentence.** The natural temptation is
to also surface the calendar on the public workshops index — that page does not
exist yet ([#0053](0053.md)), and deciding how a Luma calendar relates to the
site's own `workshops` table is that issue's problem, not this one. This issue
adds the link where Discord already is and stops there.

**No component test will cover the markup.** [#0094](0094.md) records that the
Svelte components have no test infrastructure, so the footer and About changes
are verified by `npm run check` plus review of the rendered page. The shared
links module and the router case *are* unit-testable, which is part of why the
URL belongs in a module rather than inline in two components.

This lands outside the Phase 2 gate, which is already complete. It is not
blocked by, and does not block, the Phase 4 work in flight — it can be picked up
whenever.

## Relation

- Specified by: user instruction, 2026-08-20 (no PRD section — the Luma calendar
  postdates the PRD, which records only "Discord integration | Manual
  cross-posting for now." at §2)
- Depends on: [#0016](0016.md) — Build the About view
- Depends on: [#0017](0017.md) — Build the site header, navigation, and footer
- Depends on: [#0070](0070.md) — Build the privacy policy page
- Related: [#0014](0014.md) — Add a History API path router (the `shouldIntercept` case)
- Related: [#0053](0053.md) — Build the public workshops index (deliberately out of scope here)
- Related: [#0094](0094.md) — Svelte components have no test infrastructure

## Root cause

Not a bug — a new external surface (the Luma calendar) that postdates the
PRD and had no link anywhere on the site. Discord had two hand-copied
occurrences of its URL already; adding a Luma constant the same way would
have made three copies of two URLs across two files.

## Fix

- Added `web/src/lib/links.ts` exporting `DISCORD_URL` and `LUMA_URL` as a
  sibling module to `branding.ts` (per that file's own comment against using
  it for functional values). `Footer.svelte` and `About.svelte` both now
  import `DISCORD_URL` from here too, removing the duplicate local consts.
- `Footer.svelte`: added a `Calendar` link (`LUMA_URL`, `target="_blank"
  rel="noopener noreferrer"`) beside Discord in `.footer-links`, with a `·`
  separator. The existing `@media (max-width: 480px)` rule targets
  `.footer-links a` generically, so the new link already gets the ≥40px tap
  target with no separate rule needed.
- `About.svelte`: "Get involved" now says Discord is where locations get
  shared and people organize between sessions, and that upcoming sessions
  are listed on the Luma calendar where you can RSVP directly. Added a
  "View the calendar" link next to "Join the Discord". Updated the
  `StatusList` item to `'announced through Discord, the Luma calendar, and
  the mailing list'`.
- `PrivacyPolicy.svelte`: the "does not cover third-party services like
  Discord, which has its own privacy policy" sentence now reads "...like
  Discord or Luma, which have their own privacy policies." No claim is made
  about what Luma collects.
- `router.test.ts`: added `does not intercept the Luma calendar link`
  beside the existing cross-origin-href test, asserting `shouldIntercept`
  is `false` for `https://luma.com/opencircuitsf`.
- No embed/widget/script/iframe anywhere — plain `<a href>` in both
  locations, consistent with CLAUDE.md §9.

## Verification

Machine load at start: `load averages: 1.21 1.80 2.08` (idle; no load
caveat applies).

From `web/`:
- `npm run check` → `COMPLETED 199 FILES 0 ERRORS 0 WARNINGS 0
  FILES_WITH_PROBLEMS`
- `npm test` → `Test Files 9 passed (9)`, `Tests 178 passed (178)`
- `npx vitest run src/lib/router.test.ts --reporter=verbose` confirms by
  name: `✓ src/lib/router.test.ts > shouldIntercept > does not intercept
  the Luma calendar link`

No dev server or long-running process was started; nothing to kill.

## Files changed

- `web/src/lib/links.ts` (new)
- `web/src/lib/Footer.svelte`
- `web/src/views/About.svelte`
- `web/src/views/PrivacyPolicy.svelte`
- `web/src/lib/router.test.ts`

Commit: `b3cbd1c` — "#0101 Link the Luma events calendar alongside the
Discord invite"

## Gotchas

- **Canonical URL confirmed as `luma.com`, not `lu.ma`.**
  `curl -sI https://luma.com/opencircuitsf` returns `HTTP/2 200` directly.
  `curl -sI https://lu.ma/opencircuitsf` returns `HTTP/2 301` with
  `location: https://luma.com/opencircuitsf`. So `lu.ma` is the redirecting
  shortener form and `luma.com` is canonical — matches the URL the user
  originally gave, no change needed. `LUMA_URL` in `links.ts` uses the
  `luma.com` form; do not swap in `lu.ma`.
- Left the issue's checkboxes checked based on this implementation pass;
  status remains `in-progress` for review per CLAUDE.md/Issues.md — this
  subagent does not set `resolved`.

## Review notes

Reviewed 2026-08-21 (Opus, phase 3) — **approved**.

Re-ran verification independently rather than trusting `## Verification`;
`uptime` first showed `load averages: 1.19 1.58 1.94`, so no load caveat
applies and any failure would have been a real defect.

| Check | Result |
|---|---|
| `npm run check` | `COMPLETED 199 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS` |
| `npm test` | `Test Files 9 passed (9)`, `Tests 178 passed (178)` |
| New test by name | `✓ shouldIntercept > does not intercept the Luma calendar link` |
| Mutation check | breaking the cross-origin branch fails the new case; restored, `git diff b3cbd1c` clean for code files |
| Go suite | **not run, deliberately** — the commit touches zero `.go` files |
| Built-bundle external hosts | `w3.org`, `svelte.dev`, `discord.gg`, `luma.com` only; no CDN, script, iframe, or pixel |

The build was done with `npx vite build --outDir <scratch> --emptyOutDir` into a
private scratch directory rather than `web/dist/`, since another agent's build
was sitting there and `CLAUDE.md` §8b treats it as a shared mutable resource.
`git status web/` was clean afterward. No server was left listening and
`pgrep -f 'go test|\.test |vitest'` returns nothing.

Two observations recorded, neither blocking:

- `placeholder/index.html` hardcodes the Discord URL a third time, outside
  `web/src`. It is a standalone static file that cannot import a TypeScript
  module, so this is not a violation of the "defined once" criterion — but the
  page it belongs to is what production actually serves today, and it has no
  calendar link at all. Filed as [#0115](0115.md).
- The `StatusList` item now reads "announced through Discord, the Luma
  calendar, and the mailing list" while the mailing list is not open yet. That
  wording predates this issue and About's own copy says the list "is on the
  way", so it is not newly misleading.
