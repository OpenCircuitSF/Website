# 0090 — The preference centre steers a complained subscriber into a dead end

| | |
|---|---|
| **Status** | open |
| **Module** | web |
| **Platform** | All |
| **Phase** | 3 — Mailing list capture |
| **First seen** | 2026-08-19 |
| **Commit** | e76b385 |

## Description

`#0031` correctly stops a non-`active` subscriber's save from claiming success, and shows a status panel with a "Subscribe again" affordance instead. For four of the five statuses that is right. For `complained` it is a dead end the UI knowingly steers into.

`goToSubscribe()` is a client-side `navigate('/subscribe')` — it performs **no mutation**, so the guard is not undone and the row cannot leave `complained` by any route reachable from this UI. That part is sound. But the person then submits the public form and receives `#0026`'s uniform `202` — *"Check your email to confirm."* — for a mail that `existingSignup`'s `StatusComplained` branch never sends.

Meanwhile `inactiveStatusMessage('complained')` returns the vague *"This address isn't currently subscribed."*, while the **same component's** unsubscribe path already has honest copy for exactly this case:

> …complained addresses can't be unsubscribed or resubscribed from this page. Contact us…

## Why this is a residual and not a bounce

`#0031`'s reviewer judged it so, and I agree: nothing is falsely *stated*, no guard weakens, no record is corrupted, and the same dead end exists site-wide by `CLAUDE.md` §9's design — the uniform `202` is what makes it invisible, deliberately. This is about not walking someone into it from a page that already knows their status.

## Acceptance criteria

- [x] The "Subscribe again" affordance is suppressed for `complained`
- [x] `inactiveStatusMessage('complained')` reuses the honest copy the unsubscribe path already returns, including how to reach a human — **bounced twice. 1st (2026-08-19): the copy said "Contact us" and never said how. 2nd (2026-08-19, `5aa9cd5`): the address now reaches both surfaces, but the commit moved the server's sentence and froze the client's mirror one revision behind it, then added a test forbidding it from catching up. See Review notes.** Third pass (2026-08-19, escalated to Opus): the constant is split at the clause boundary either side of the address, so the client renders the server's sentence — the whole of it, address included — as one sentence with the anchor inside it. The forbidding test is inverted into a composition assertion. See *Bounce-fix completion (third pass)*.
- [x] The other four statuses keep the affordance — `pending`, `unsubscribed` and `bounced` can legitimately resubscribe, and `active` never sees the panel
- [x] A test asserts the affordance is absent for `complained` and present for the others. **Mutation-proven** by re-enabling it and watching the test fail
- [x] No server-side change: the store guard is already correct and must not be touched
- [x] `npm run check`, `npm test`, `npm run build` pass; `go test ./... -count=1` green with `TEST_DATABASE_URL`, zero skips

## Notes

Two smaller items from the same review, both belonging here rather than anywhere else:

- `no_op` is tagged `json:"no_op,omitempty"` while `#0032`'s equivalent is plain `json:"no_op"`, so the field vanishes on the success path. Harmless today — the TS type declares it optional and the SPA branches on `unsubscribed` — but the two conventions should match.
- `maskEmail` fully reveals a **two-rune** local part (`ab@` → `a•••••b@`). Pre-existing, not introduced by `#0031`, and only worth changing if the mask is ever tightened.

**Decisions on both (2026-08-19), recorded rather than acted on:** both live
in `internal/handlers/preferences.go`, which is explicitly out of lane for
this batch — a separate subagent is concurrently reviewing `#0079`/`#0084`/
`#0087` in `internal/handlers/`, and `#0090`'s own dispatch instructions
name that directory off-limits. That alone is reason enough to leave both
untouched here. On the merits, independent of the lane conflict:
  - `no_op`'s tag: a genuine one-line fix (`json:"no_op,omitempty"` →
    `json:"no_op"`) worth doing to match `#0032`'s convention, but it is
    cosmetic (the TS type already declares the field optional and the SPA
    branches on `unsubscribed`, never on `no_op`'s mere presence) and
    touches a handler file mid-review by someone else. Left for a follow-up
    issue or the next pass through `internal/handlers/preferences.go`.
  - `maskEmail`'s two-rune reveal: the issue's own text already answers
    this — "only worth changing if the mask is ever tightened" — so the
    decision on the merits is *no change now* regardless of the lane
    question. Recorded here as confirmed-not-a-bug, not deferred.

## Review notes

**Bounced 2026-08-19 — the replacement path is inert: "Contact us" never says
how.**

Everything else in this fix is correct and was independently re-proven. The
affordance suppression is right and complete: `showSubscribeAgainAffordance`
returns `false` for `complained` and `true` for `pending`, `unsubscribed`,
`bounced`, `active` and an unrecognized status, and all three reported
mutations reproduced exactly — forcing the predicate to `true` fails only
*is suppressed for complained*, forcing it to `false` fails only *is present
for every other status the panel can show*, and reverting the copy fails only
the two message assertions, 25 tests each time. The claim that the other two
`goToSubscribe` call sites needed no change holds on inspection: the `invalid`
panel has no subscriber status at all (the token was never resolved), and the
`unsubscribed` panel is reachable only after a *successful* unsubscribe PATCH,
which a complained row can never produce — its PATCH routes to the separate
`unsubscribeNoOp` panel, which has no resubscribe button. No server file was
touched: `e76b385` is three files, all under `web/src/`, and the store guard is
untouched. `npm run check` reports 195 files / 0 errors, `npm test` 8 files /
152 tests, `npm run build` succeeds (run in an isolated worktree), and
`web/dist/index.html` in the main tree is byte-identical to the committed
placeholder. The constant matches the server string word for word apart from
the deliberately dropped `"No change: "` lead-in and the resulting
capitalisation — that judgement was right, and the sentence does read naturally
on page load rather than as a reply to an action.

### 1. Why this bounces

The acceptance criterion is not "reuse the copy"; it is "reuse the honest copy
… **including how to reach a human**." The shipped sentence ends:

> Contact us if you believe this is a mistake.

There is no address and no link. That names *that* a human exists, not *how* to
reach one, so the criterion is unmet and was checked in error.

This matters more here than it would anywhere else on the site, because of what
this fix just did. Before it, a `complained` visitor had a button — a fake path,
but a path. Now the button is correctly gone and this sentence is the **only**
remaining path offered. Making it the sole route and leaving it inert
reconstructs the defect the issue exists to close, one shape over: the visitor
is still told to do something and still given nothing to act on.

`#0075` published `hello@opencircuitsf.com` for exactly this class of request,
and the app already treats naming it as the house convention — `Footer.svelte`,
`About.svelte` and `PrivacyPolicy.svelte` each hold a `CONTACT_EMAIL` constant
and render it as `<a href="mailto:{CONTACT_EMAIL}">`, and `PrivacyPolicy.svelte`
goes out of its way to write "contact us **at the address below**." This panel
is the one place that says "contact us" and stops.

The footer does carry the address site-wide, so it is not strictly
unreachable — but requiring the visitor to infer that, scroll, and connect it to
the sentence is not the same as offering it, and a person who has just been told
they are marked as a complainer is the least likely visitor to go hunting.

### 2. One design tension to resolve while fixing it

Adding the address to `COMPLAINED_NO_RESUBSCRIBE_MESSAGE` would break the
"matching, word for word, the no-op message" guarantee its own doc comment
makes, since `internal/handlers/preferences.go:250` has the same inert
"Contact us" and is out of lane for this batch. Two clean options:

- Render the mailto link in `PreferenceCenter.svelte` beside the message (a
  sibling `<p>` or a trailing anchor), leaving the constant a faithful mirror.
  This keeps the change entirely in-lane and is probably the smaller edit.
- Or add the address to both strings and update the doc comment. This needs
  `internal/handlers/preferences.go`, so it must wait for that file to be free.

Either way the server's own message has the same gap and should be tracked —
**this belongs in a new issue** against `patchUnsubscribe`'s no-op copy.

### 3. Two further findings, neither a bounce reason

**a. The `{#if}` wiring is untested.** The new tests assert on the pure
predicate, never on the rendered panel. Deleting the `{#if}` wrapper from
`PreferenceCenter.svelte` entirely — restoring the exact dead end this issue
closes — leaves all 152 tests green; only mutating `subscribe.ts` fails
anything. So the one line that actually delivers the user-visible fix carries no
regression protection. This is **not** held against the fix: the project has no
component-test infrastructure at all (no jsdom/happy-dom, no
`@testing-library/svelte`, and no test anywhere imports a `.svelte` file), every
suite is a pure-logic test under `src/lib/`, and this fix followed that
convention exactly by extracting the decision into a testable function. The
exposure is project-wide rather than specific to `#0090`. **Belongs in a new
issue** if component coverage is ever wanted.

**b. Stale cross-reference.** `web/src/lib/subscribe.ts:184` points the reader
at `COMPLAINED_UNSUBSCRIBE_NO_OP_MESSAGE`; no such symbol exists — the constant
is `COMPLAINED_NO_RESUBSCRIBE_MESSAGE` at line 219. Worth correcting in the same
pass.

### 4. The two deferred decisions

**`no_op`'s `,omitempty` — deferral correct, "cosmetic" is the wrong word.**
Leaving it alone was right: `internal/handlers/preferences.go` is explicitly out
of lane and under concurrent review, and editing another agent's file mid-review
is what `CLAUDE.md` §8 warns against. But the characterisation understates it.
This is not a formatting matter; it is two handlers serialising the same field
name under two different schemas. `omitempty` on a `bool` erases the difference
between `false` and absent, and in JS `undefined !== false`, so a consumer
writing `result.no_op === false` or `'no_op' in result` gets different answers
from `/preferences` than from `/admin/subscribers/{id}/suppress` — which is
exactly why `api.ts:426` types the admin field as required `no_op: boolean`
while `types.ts:175` types this one optional. The codebase already documents the
intended convention and breaks it one line later: the comment at
`preferences.go:120` says the field "mirrors AdminSubscribersHandler.Suppress's
no_op field, #0032", and line 121 does not. Call it latent contract drift —
harmless only because no caller reads it today, which is a fact about today's
callers, not about the field. "Left for a follow-up issue or the next pass" is a
wish rather than a tracked item; **this needs a new issue** against
`internal/handlers/preferences.go`.

**`maskEmail`'s two-rune reveal — declined correctly.** Agreed on the merits.
`ab@x.com` → `a•••••b@x.com` does reveal the whole local part while the bullets
imply hidden characters that are not there, so the mask mildly over-promises.
But it is not a security boundary — the caller already holds the row's token —
the affected input class is negligible, and tightening it means inventing a
policy (mask fully below N runes) that nobody has specified. Recording it as
confirmed-not-a-bug is right. Worth noting only that the two labels ended up
swapped: the genuinely cosmetic item was called not-a-bug, and the contract item
was called cosmetic.

---

**Bounced 2026-08-19 (second pass, commit `5aa9cd5`) — the address landed, but
the commit re-authored the very false claim it says it fixed, and moved the
server's copy out from under the client's mirror.**

Everything I could re-prove, I re-proved. The address is present and clickable,
the tests are real, and I re-ran every mutation myself. Two things stop this.

### What holds up (independently re-verified)

- **The Go test is genuine and §8b-clean.** It seeds its own throwaway row via
  `journeyUniqueEmail(t)` + `Create` → `Confirm` → `MarkComplained` and targets
  `created.ManageToken` — no literal id, no seeded taxonomy row, nothing
  destructive. Passes in 5.85s targeted.
- **Go mutation 1** — dropped `" at " + complainedContactEmail` from the no-op
  message:

  ```
  --- FAIL: TestPreferencesHandler_Patch_UnsubscribeEverything_ComplainedNoOpMessageNamesContactAddress (4.14s)
      preferences_test.go:418: … want it to contain the contact address …
      preferences_test.go:421: … want the address inline in the "Contact us at …" clause …
  ```

  Both assertions failed, nothing else.
- **Go mutation 2 (mine, not the implementer's)** — I checked whether the
  second assertion is redundant with the first by putting the address in the
  message but *outside* the clause (`Contact us (hello@…) if you believe`).
  Only line 421 failed:

  ```
  preferences_test.go:421: Message = "… Contact us (hello@opencircuitsf.com) if you believe …", want the address inline in the "Contact us at …" clause, not merely present somewhere in the string
  ```

  So both assertions are independently load-bearing. Good test.
- **TS mutation A** — `COMPLAINED_CONTACT_EMAIL = 'not-an-address'`: 2 failed /
  26 passed. **TS mutation B** — inlined the address into
  `COMPLAINED_NO_RESUBSCRIBE_MESSAGE`: exactly the boundary assertion failed,
  1 failed / 27 passed. The boundary guard is not trivially satisfiable; it
  catches the drift it claims to.
- Every mutated file was restored from a copy taken beforehand (never
  `git checkout --`), each verified byte-identical by `shasum`. Working tree is
  clean; nothing scratch left behind.
- `go build ./...`, `go vet ./internal/handlers/...` clean. `npm run check`
  195 files / 0 errors, `npm test` 8 files / 155 tests. Per dispatch,
  `go test ./internal/handlers/...` (whole package) was **not** run — `#0091`.
- **The `{:else}` branch cannot be reached by an unexpected status.**
  `showSubscribeAgainAffordance` is `status !== 'complained'`, so the else arm
  is reachable **iff** `status === 'complained'` — an unknown status, `''` or
  `undefined` all take the `{#if}` arm and get the button. No leak.

### 1. The corrected comment is true. Two others in the same commit are not.

I compared the two strings character by character. There are exactly two
divergence regions:

```
replace  server[0:12]='No change: t'          client[0:1]='T'
delete   server[169:196]='at hello@opencircuitsf.com '   client → ''
```

So `subscribe.ts`'s corrected doc comment — "it differs from the server string
in two places, not one … the leading 'No change: ' clause … AND the server's
inline 'at hello@opencircuitsf.com'" — is **accurate**, not merely less false.
(It does not mention the `t`→`T` recapitalisation entailed by dropping the
lead-in; that is implied, and I do not hold it against the comment.) Credit
where due: the implementer found a real drift and fixed it correctly.

But it fixed **one** of the three places that state the claim, and the place it
left is the worst one — and was **newly written by this same commit**.
`web/src/views/PreferenceCenter.svelte`, in the comment block added at
`5aa9cd5`:

> Deliberately NOT fixed by baking the address into that constant (**it's a
> word-for-word mirror of preferences.go's no-op message and must stay one**);
> instead the mailto link renders here…

That sentence is false as of the commit that wrote it, and it is false in the
exact words the implementer struck out of `subscribe.ts` twenty lines away.
It is not a stray adjective either: it is the **stated justification for the
entire split design**, and it justifies the split by an invariant the code does
not have. A maintainer reading only this component is told an untrue guarantee
and told it "must stay" that way.

Third site, weaker but same family — `COMPLAINED_CONTACT_EMAIL`'s own doc
comment in `subscribe.ts`: "so the message above stays **a faithful mirror** of
preferences.go's no-op message." It is not a faithful mirror; it drops the
lead-in as well.

The work log says the comment "was overstating a guarantee the code no longer
(fully) provides — exactly the kind of silent drift the constant exists to
prevent." That diagnosis is right. The audit behind it was one file wide.

### 2. The client no longer reuses the copy the criterion points at

Acceptance criterion 2 is *"reuses the honest copy the unsubscribe path
**already returns**."* This commit changed what the unsubscribe path returns —
it now returns "…Contact us at hello@opencircuitsf.com if you believe this is a
mistake." — and simultaneously froze the client constant one revision behind it,
then added a test (`does not appear inside COMPLAINED_NO_RESUBSCRIBE_MESSAGE`)
that **forbids the client from ever catching up**. On the letter of the
criterion the client is now further from the server string than it was before
the fix, not closer.

The first review did offer "render the mailto beside the message, leaving the
constant a faithful mirror" as a clean option, and I am not re-litigating that.
But it offered it because *"this keeps the change entirely in-lane"* — the
server file was out of lane for that batch, and option 2 "must wait for that
file to be free." This pass **edited `preferences.go` anyway** (correctly — it
is in lane now, and fixing the server's identical gap here rather than deferring
it to a new issue is a genuine improvement). Once that constraint was gone, the
reason to prefer the split evaporated, and the pass shipped option 1's client
shape on top of option 2's server edit — paying option 2's cost without getting
its benefit.

### 3. How it reads to the person receiving it

```
This address is marked as having complained about a previous email, and
complained addresses can't be unsubscribed or resubscribed from this page.
Contact us if you believe this is a mistake.

Reach us at hello@opencircuitsf.com.
```

Read aloud, that is two authors. One instruction ("get in touch") is issued
twice, in two different verbs — *Contact us* … *Reach us at* — split across a
paragraph break, with the reader expected to carry the intent from the first to
the second. Nothing here is wrong or misleading, and it is unambiguously better
than the inert dead end it replaces. But it is worse than the single sentence
the server already writes, and worse for no reason a reader could infer.

Two smaller things in the same breath:

- The sole remaining path is rendered `class="text-muted"`, i.e. deliberately
  de-emphasised, in the slot a `link-button` used to occupy. The one actionable
  element on the panel is now the quietest thing on it.
- The message `<p>` carries `role="status"`; the mailto `<p>` sits outside that
  live region, so a screen-reader user who hears the status announced does not
  hear the address that answers it.

And the first review's observation stands unaddressed: "can't be unsubscribed or
resubscribed" still answers a question nobody asked on page load. That one is
inherited from the criterion's own "reuse the copy" instruction and I am **not**
bouncing on it — noting it so it is not lost.

### What to change

Small, and it resolves 1, 2 and 3 together. Split the constant at the clause
boundary so the address can live *inside* the sentence on both surfaces:

```ts
export const COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD =
  "This address is marked as having complained about a previous email, and complained addresses " +
  "can't be unsubscribed or resubscribed from this page. Contact us at ";
export const COMPLAINED_NO_RESUBSCRIBE_MESSAGE_TAIL = " if you believe this is a mistake.";
export const COMPLAINED_NO_RESUBSCRIBE_MESSAGE =
  COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD + COMPLAINED_CONTACT_EMAIL + COMPLAINED_NO_RESUBSCRIBE_MESSAGE_TAIL;
```

`inactiveStatusMessage('complained')` keeps returning the whole string (still a
pure, DOM-free, unit-testable value — `CLAUDE.md` §1 is satisfied), the mirror
is restored to the single documented divergence ("No change: "), and
`PreferenceCenter.svelte` renders one sentence with a real anchor in the middle
of it:

```svelte
<p role="status">
  {COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD}<a href="mailto:{COMPLAINED_CONTACT_EMAIL}"
  >{COMPLAINED_CONTACT_EMAIL}</a>{COMPLAINED_NO_RESUBSCRIBE_MESSAGE_TAIL}
</p>
```

Note this **inverts** the new `does not appear inside
COMPLAINED_NO_RESUBSCRIBE_MESSAGE` test — that is intended, not a regression.
Replace it with the composition assertion (`MESSAGE === LEAD + EMAIL + TAIL`),
which guards the same drift from the useful direction. Then delete the
now-false "word-for-word mirror" sentence from `PreferenceCenter.svelte` and
soften "faithful mirror" in `COMPLAINED_CONTACT_EMAIL`'s doc.

If the split is genuinely not wanted, the alternative is acceptable: keep the
two-element rendering but **rewrite the second line so it is not a second
imperative** — e.g. drop "Contact us if you believe this is a mistake." from the
constant's tail and let the link carry the whole ask. What cannot stand is the
current pairing plus a comment asserting a mirror that does not exist.

### 4. On whether a comment is enough — it is not, and this project already
built the answer twice

I was asked to form a view. Mine: a comment is the wrong instrument here, and
the evidence is this commit. The mirror survived exactly one edit before drifting,
the drift was invisible to every test, and it was caught only because a human
reviewer read both strings. Nothing about the next edit will be different.

This codebase already has the pattern, twice: `internal/db/docs_parity_test.go`
(#0082/#0083) and `internal/handlers/routes_parity_test.go` (#0071) — both
filesystem-only Go tests that read a *different* file and assert parity, with
no database, riding the `go test` run every backend pass already does. #0083's
history is the precedent that matters: that doc table drifted **three times**
before a mechanical guard existed, and the guard's own reviewer then found four
more drift modes by probing it. Same shape, same failure mode, one file type
over.

Once the constant is split as above, the guard becomes almost trivial: read
`web/src/lib/subscribe.ts`, extract `COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD`,
`_TAIL` and `COMPLAINED_CONTACT_EMAIL`, and assert `patchUnsubscribe`'s no-op
message equals `"No change: " + lowerFirst(LEAD) + EMAIL + TAIL`. That turns the
doc comment's prose promise into the one divergence it can actually name.

**I am not making the guard a condition of this bounce** — it is new scope and a
separate concern from the two defects above. File it as its own issue against
the client/server copy pair and let it be scheduled. But it should be filed, not
wished for; "the constant is what keeps this mechanical" is, today, not true —
the constant keeps *one side* mechanical and a comment keeps the other.

### 5. Not held against this fix

Confirmed still true and already tracked elsewhere; recorded so the next pass
does not spend time re-deriving them:

- **The client-side user-visible fix has zero regression protection.** I deleted
  the entire `{:else}` mailto block from `PreferenceCenter.svelte` — restoring
  the exact inert dead end this bounce exists to close — and got `8 passed /
  155 tests` green plus `npm run check` at 0 errors, with the now-unused import
  unflagged. Identical to the first review's finding 3a and to `#0094`; the
  project has no component-test infrastructure at all, and this fix followed the
  existing convention correctly. **Not a bounce reason.**
- `no_op`'s `,omitempty` mismatch — `#0093`. Untouched here, correctly.
- Docs-guard fence residuals — `#0092`. `maskEmail` — declined, correct.

## Relation

- Depends on: [#0031](0031.md) — Build the preference center
- Related: [#0026](0026.md) — Build the subscribe endpoint with anti-abuse controls

## Fix

**1. Affordance suppressed for `complained`.** Added
`showSubscribeAgainAffordance(status: string): boolean` to
`web/src/lib/subscribe.ts` — `status !== 'complained'` — and wrapped the
"Subscribe again" `<button>` in the `!isActive` branch of
`PreferenceCenter.svelte` in `{#if showSubscribeAgainAffordance(status)}`.
Only that one panel changed; the two other places `goToSubscribe` is wired
(the `invalid` token panel and the `unsubscribed`-after-a-real-unsubscribe
success panel) are unrelated to a subscriber's current `status` — a
complained row's own unsubscribe attempt routes to the separate
`unsubscribeNoOp` panel, which has no resubscribe button at all — so neither
needed a change.

**2. Honest copy for `complained`.** Added the exported constant
`COMPLAINED_NO_RESUBSCRIBE_MESSAGE` to `subscribe.ts`, carrying the same
substantive sentence `internal/handlers/preferences.go`'s `patchUnsubscribe`
already returns for a complained row's no-op PATCH: *"This address is
marked as having complained about a previous email, and complained
addresses can't be unsubscribed or resubscribed from this page. Contact us
if you believe this is a mistake."* Deliberately drops only that server
message's leading `"No change: "` clause, which describes a PATCH that was
just attempted — not applicable when this copy is shown on page load,
before anyone has clicked anything. `inactiveStatusMessage('complained')`
now returns this constant instead of the generic `"This address isn't
currently subscribed."` every other unrecognized/default status still gets.
One exported constant, not two independently-typed strings, so the two call
sites cannot quietly drift apart on a future edit — though note this is
copy *duplication* across a Go string literal and a TS constant, not a
single shared source; a future edit to either still needs a human to update
the other by hand.

## Verification

Mutation 1 — reverted `showSubscribeAgainAffordance` to always return
`true`, ran `npx vitest run src/lib/subscribe.test.ts`:

```
 ❯ src/lib/subscribe.test.ts (25 tests | 1 failed)
     × showSubscribeAgainAffordance (#0090 — the dead end) > is suppressed for complained
   AssertionError: expected true to be false
     - false
     + true
 Test Files  1 failed (1)
      Tests  1 failed | 24 passed (25)
```

Only that one test failed. Reverted; suite back to 25/25.

Mutation 2 — reverted `showSubscribeAgainAffordance` to always return
`false` (the opposite mutation, covering the "present for the others"
assertion), same command:

```
 ❯ src/lib/subscribe.test.ts (25 tests | 1 failed)
     × showSubscribeAgainAffordance (#0090 — the dead end) > is present for every other status the panel can show
   AssertionError: expected false to be true
 Test Files  1 failed (1)
      Tests  1 failed | 24 passed (25)
```

Again exactly one test failed. Reverted; suite back to 25/25.

Mutation 3 — reverted `inactiveStatusMessage('complained')` to the old
vague string, same command:

```
 ❯ src/lib/subscribe.test.ts (25 tests | 2 failed)
     × inactiveStatusMessage (#0031 review finding 1) > gives complained the same honest copy the unsubscribe path already returns, not the vague default
   AssertionError: expected 'This address isn't currently subscri…' to be 'This address is marked as having comp…'
     × inactiveStatusMessage (#0031 review finding 1) > complained's copy names how to reach a human
   AssertionError: expected 'This address isn't currently subscri…' to match /contact us/i
 Test Files  1 failed (1)
      Tests  2 failed | 23 passed (25)
```

Both message-specific assertions failed, nothing else. Reverted; suite back
to 25/25, confirmed byte-identical to the pre-mutation file via `diff`.

Full suite:

```
$ npm run check
1787151969445 START ... COMPLETED 195 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS

$ npm test
 Test Files  8 passed (8)
      Tests  152 passed (152)

$ npm run build
✓ 168 modules transformed. ✓ built in 2.95s
```

`web/dist/index.html` (the repo's committed minimal placeholder, not the
real built output — see `.gitignore`'s comment above `web/dist/*`) was
overwritten by `npm run build` and restored via `git show HEAD:web/dist/index.html
> web/dist/index.html`; confirmed byte-identical to `HEAD` afterward (`git
status` showed nothing under `web/dist/`).

Go side — no server file was touched, so this is confirmatory rather than
load-bearing:

```
$ export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
$ go build ./...                       # clean
$ go test ./internal/db/... -count=1
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.997s
```

## Files changed

- `web/src/lib/subscribe.ts` — added `showSubscribeAgainAffordance`,
  `COMPLAINED_NO_RESUBSCRIBE_MESSAGE`; `inactiveStatusMessage('complained')`
  now returns the constant instead of the vague default.
- `web/src/lib/subscribe.test.ts` — new `describe` blocks for both, plus one
  added case to the existing `inactiveStatusMessage` suite.
- `web/src/views/PreferenceCenter.svelte` — the `!isActive` panel's
  "Subscribe again" button is now wrapped in
  `{#if showSubscribeAgainAffordance(status)}`.

`internal/handlers/preferences.go` and `internal/handlers/preferences_test.go`
were read (to confirm the exact server copy to reuse and to locate the
`no_op` tag) but not edited — both decisions above are recorded, not acted
on, and that file is out of lane for this batch regardless.

## Gotchas

- The server's no-op message starts with `"No change: "`, which reads
  correctly only in the context of a PATCH that was just attempted. Reusing
  it verbatim in `inactiveStatusMessage` (shown on page load, no action
  taken yet) would have been confusing, so the constant is the same
  sentence minus that clause — "reuse the copy" was interpreted as "reuse
  the substantive honest sentence," not "byte-for-byte reuse the whole
  server string including context-specific framing."
- `web/node_modules` doesn't exist in a fresh `git worktree` (it's
  gitignored, not part of the tree) — `npm run check`/`test`/`build` all
  fail with `command not found` until something provides it. Symlinked to
  the main tree's `web/node_modules` rather than running `npm install`
  again in the worktree; removed no files, left the main tree's
  `node_modules` untouched, and the symlink itself is gitignored so it
  never shows up in `git status` as anything to clean up.
- Confirmed by reading `internal/handlers/preferences.go` around line 121
  that the `no_op` tag mismatch (`#0032`'s `admin_subscribers.go:368` is
  plain `json:"no_op"`) is real and exactly as described — not re-verified
  by a mutation test since the fix itself was deliberately not made.

## Bounce-fix completion (2026-08-19)

A previous implementer wrote the correct code for the bounce reason above but
died to an infrastructure error before writing tests, this log entry, or
committing — the changes sat uncommitted in the tree. This pass verified,
completed, and committed that work rather than redoing it; none of the
already-good code was rewritten.

**What was already correct on inspection (adopted as-is):**

- `internal/handlers/preferences.go` — new `complainedContactEmail =
  "hello@opencircuitsf.com"` constant, inlined into the no-op message so it
  reads "…Contact us at hello@opencircuitsf.com if you believe…". Correct
  choice: this response is plain-text JSON with nowhere to put a clickable
  link, so inlining the address is the only option server-side.
- `web/src/lib/subscribe.ts` — new exported `COMPLAINED_CONTACT_EMAIL =
  'hello@opencircuitsf.com'`, kept as its own constant rather than folded
  into `COMPLAINED_NO_RESUBSCRIBE_MESSAGE`, plus the stale
  `COMPLAINED_UNSUBSCRIBE_NO_OP_MESSAGE` cross-reference fixed to point at
  the real symbol name.
- `web/src/views/PreferenceCenter.svelte` — imports the constant and, in the
  `{:else}` branch beside the no-op message, renders `Reach us at <a
  href="mailto:{COMPLAINED_CONTACT_EMAIL}">{COMPLAINED_CONTACT_EMAIL}</a>.`

**Design decision (kept, not re-litigated):** the server inlines the address
directly into its plain-text message body because it has no way to carry a
link. The client instead keeps `COMPLAINED_NO_RESUBSCRIBE_MESSAGE` as a
close-but-not-identical mirror of that sentence (deliberately omitting the
address) and renders a real `mailto:` anchor as a sibling element next to it.
Same underlying fact — a human is reachable, here is how — delivered two ways
because the two surfaces have different capabilities. This is sound and was
not changed.

**What this pass found and fixed:** `subscribe.ts`'s doc comment on
`COMPLAINED_NO_RESUBSCRIBE_MESSAGE` claimed the constant matches the server's
no-op string "word for word" modulo only the server's leading `"No change:
"` clause. That was true before this bounce fix; it stopped being true the
moment the server's sentence gained `"at hello@opencircuitsf.com"` in the
middle, since the client constant deliberately does not carry the address.
The comment was overstating a guarantee the code no longer (fully) provides —
exactly the kind of silent drift the constant exists to prevent, just one
level up in the comment describing it rather than in the code itself.
Corrected the comment (`web/src/lib/subscribe.ts`, doc block above
`COMPLAINED_NO_RESUBSCRIBE_MESSAGE`) to name both deliberate divergences
explicitly: the dropped `"No change: "` lead-in, and the dropped inline
address (with a pointer to `COMPLAINED_CONTACT_EMAIL`'s own doc comment for
why). No string values were changed, only the comment's claim about them.

**Tests added:**

- Go — `TestPreferencesHandler_Patch_UnsubscribeEverything_ComplainedNoOpMessageNamesContactAddress`
  in `internal/handlers/preferences_test.go`: seeds a confirmed subscriber,
  marks it `complained` via `MarkComplained`, PATCHes `{unsubscribe: true}`,
  and asserts the response's no-op `Message` contains
  `"Contact us at hello@opencircuitsf.com"` — not just that the address
  appears somewhere in the string.
- TypeScript — new `describe('COMPLAINED_CONTACT_EMAIL (#0090 bounce fix)')`
  block in `web/src/lib/subscribe.test.ts`: asserts the constant's exact
  value, that it's a plausible mailto target, and — the boundary this split
  design depends on — that the address does **not** appear inside
  `COMPLAINED_NO_RESUBSCRIBE_MESSAGE` (guards against a future edit
  re-merging the two and silently breaking the "message mirrors the server
  minus two named things" claim the corrected comment now makes).

**Mutation proof — Go**, dropped `" at " + complainedContactEmail` from the
no-op message, reran the new test:

```
--- FAIL: TestPreferencesHandler_Patch_UnsubscribeEverything_ComplainedNoOpMessageNamesContactAddress (3.19s)
    preferences_test.go:418: Message = "No change: this address is marked as having complained about a previous email, and complained addresses can't be unsubscribed or resubscribed from this page. Contact us if you believe this is a mistake.", want it to contain the contact address (#0090 bounce fix: naming that a human exists isn't enough, the message must say how to reach one)
    preferences_test.go:421: Message = "No change: this address is marked as having complained about a previous email, and complained addresses can't be unsubscribed or resubscribed from this page. Contact us if you believe this is a mistake.", want the address inline in the "Contact us at …" clause, not merely present somewhere in the string
FAIL
```

Both assertions failed as expected, nothing else. Reverted; `git diff` on
`internal/handlers/preferences.go` afterward matched the pre-mutation diff
exactly (confirmed byte-for-byte), and the test passes again.

**Mutation proof — TypeScript**, two separate mutations, each reverted before
the next:

1. Changed `COMPLAINED_CONTACT_EMAIL` to `'not-an-address'`:

```
FAIL  src/lib/subscribe.test.ts > COMPLAINED_CONTACT_EMAIL (#0090 bounce fix) > is the published house contact address (#0075)
AssertionError: expected 'not-an-address' to be 'hello@opencircuitsf.com'
FAIL  src/lib/subscribe.test.ts > COMPLAINED_CONTACT_EMAIL (#0090 bounce fix) > is a plausible mailto target
AssertionError: expected 'not-an-address' to match /^[^\s@]+@[^\s@]+\.[^\s@]+$/
 Test Files  1 failed (1)
      Tests  2 failed | 26 passed (28)
```

2. Inlined `" at hello@opencircuitsf.com"` into
   `COMPLAINED_NO_RESUBSCRIBE_MESSAGE` (simulating the drift the boundary
   test exists to catch):

```
FAIL  src/lib/subscribe.test.ts > COMPLAINED_CONTACT_EMAIL (#0090 bounce fix) > does not appear inside COMPLAINED_NO_RESUBSCRIBE_MESSAGE
AssertionError: expected 'This address is marked as having comp…' not to contain 'hello@opencircuitsf.com'
 Test Files  1 failed (1)
      Tests  1 failed | 27 passed (28)
```

Exactly the targeted assertion(s) failed each time; both mutations reverted,
suite back to 28/28.

**Verification:**

```
$ export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
$ go build ./...                                                    # clean
$ go vet ./internal/handlers/...                                    # clean
$ go test ./internal/handlers/ -run 'TestPreferencesHandler_Patch_UnsubscribeEverything_ComplainedNoOpMessageNamesContactAddress' -count=1 -v
--- PASS (3.44s)

$ npm run check    # 195 files, 0 errors, 0 warnings
$ npm test         # 8 files, 155 tests passed (was 152; +3 new)
$ git diff --stat web/dist/index.html   # empty — no rebuild artifact leaked
```

Per this issue's own dispatch instructions, `go test ./internal/handlers/...`
(the whole package) was deliberately **not** run — it currently takes ~622s
and exceeds Go's default 10-minute test timeout (tracked separately as
`#0091`). Only the new test was run targeted, per instructions.

Status left `open`: setting `resolved` is the reviewer's call per
`CLAUDE.md` §9.

## Bounce-fix completion (third pass, 2026-08-19 — escalated to Opus)

Code commit: `953a9d4`.

**Escalation recorded per `CLAUDE.md` §3:** implementation is Sonnet by
default; this issue has now been bounced twice on the same acceptance
criterion, so this pass ran on Opus.

### The split

`web/src/lib/subscribe.ts` now exports the sentence in three parts, in the
shape the second review prescribed:

```ts
export const COMPLAINED_CONTACT_EMAIL = 'hello@opencircuitsf.com';
export const COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD = '…Contact us at ';
export const COMPLAINED_NO_RESUBSCRIBE_MESSAGE_TAIL = ' if you believe this is a mistake.';
export const COMPLAINED_NO_RESUBSCRIBE_MESSAGE =
  COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD + COMPLAINED_CONTACT_EMAIL + COMPLAINED_NO_RESUBSCRIBE_MESSAGE_TAIL;
```

`COMPLAINED_CONTACT_EMAIL` had to move *above* the message block — a
top-level `const` cannot be referenced by an initializer that runs earlier in
the module (temporal dead zone), and the address is now part of the
composition rather than a sibling of it.

`inactiveStatusMessage('complained')` still returns the whole composed string,
still pure and DOM-free. What changed is what the string *is*: it now carries
the address, so the client is no longer a revision behind the server. Verified
character for character —

```
server === "No change: " + lowerFirst(COMPLAINED_NO_RESUBSCRIBE_MESSAGE)
```

is exactly true, restoring the mirror to the one divergence the doc comments
have always claimed (the `"No change: "` lead-in, plus the recapitalisation
dropping it entails).

`PreferenceCenter.svelte`'s `!isActive` panel renders one sentence with the
anchor inside it, replacing the message + trailing second sentence:

```svelte
{#if showSubscribeAgainAffordance(status)}
  <p role="status">{inactiveStatusMessage(status)}</p>
  <button type="button" class="link-button" onclick={goToSubscribe}>Subscribe again</button>
{:else}
  <p role="status">
    {COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD}<a
      href="mailto:{COMPLAINED_CONTACT_EMAIL}">{COMPLAINED_CONTACT_EMAIL}</a
    >{COMPLAINED_NO_RESUBSCRIBE_MESSAGE_TAIL}
  </p>
{/if}
```

The status `<p>` moved inside the `{#if}` so the complained arm can compose
its own; the affordance predicate and its two tests are unchanged. Confirmed
the compiled markup has no stray whitespace nodes at the seam by compiling the
component with `svelte/compiler` (`generate: 'server'`) and reading the emitted
push:

```
<p role="status" …>${$.escape(COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD)}<a${$.attr('href',
  `mailto:${$.stringify(COMPLAINED_CONTACT_EMAIL)}`)}>${$.escape(COMPLAINED_CONTACT_EMAIL)}</a
  >${$.escape(COMPLAINED_NO_RESUBSCRIBE_MESSAGE_TAIL)}</p>
```

0 compiler warnings. The reader now gets:

> This address is marked as having complained about a previous email, and
> complained addresses can't be unsubscribed or resubscribed from this page.
> Contact us at [hello@opencircuitsf.com](mailto:hello@opencircuitsf.com) if
> you believe this is a mistake.

One instruction, one verb, one sentence — the same one the server writes.

### The three comment sites

The second review's point was that the mirror claim was stated in three places
and audited in one. All three were rewritten; each now states something true of
the code as it stands.

1. **`subscribe.ts`, doc block above the message constants** — previously
   "differs from the server string in two places, not one … AND the server's
   inline `at hello@opencircuitsf.com`". The address is no longer dropped, so
   that is now false. It now says the split exists because the two surfaces
   have different capabilities (plain-text JSON vs. an anchor), that the
   composed whole has *exactly one* divergence from `preferences.go`'s no-op
   message — the `"No change: "` lead-in and the recapitalisation it entails —
   spells the parity out as `server === "No change: " + lowerFirst(MESSAGE)`,
   and states plainly that **nothing mechanical enforces it today**: "this
   comment is a promise, and an earlier edit to the server string broke the
   promise an earlier version of this comment made. #0095 tracks replacing it
   with a test that reads both files."
2. **`subscribe.ts`, `COMPLAINED_CONTACT_EMAIL`'s doc** — previously "so the
   message above stays a faithful mirror", which it was not (it dropped the
   lead-in too). The "faithful mirror" claim is gone. It now says the address
   is its own constant so the sentence can be *composed around* it, which is
   what lets the component put the anchor where the address falls without
   either surface owning a second copy of the wording.
3. **`PreferenceCenter.svelte`, the `!isActive` comment block** — previously
   "Deliberately NOT fixed by baking the address into that constant (it's a
   word-for-word mirror of preferences.go's no-op message and must stay one)",
   which was false as of the commit that wrote it and was the stated
   justification for the whole design. Deleted. It now describes what the
   branch actually does (composes LEAD + address + TAIL so the address is a
   real anchor mid-sentence), why the anchor sits inside `role="status"` and is
   not `text-muted`, and that parity with the server is unguarded until #0095.

A fourth site was found in the same audit and fixed: `inactiveStatusMessage`'s
own doc comment said the component "renders the actual contact-us affordance (a
mailto: link …) **beside** it", which the split makes wrong in the same way. It
now says the component renders that same sentence from its LEAD/TAIL parts so
the address inside it is a real anchor rather than plain text.

`internal/handlers/preferences.go`'s `complainedContactEmail` comment was
checked and makes no mirror claim — it says the address matches
`Footer.svelte`/`About.svelte`/`PrivacyPolicy.svelte`'s `CONTACT_EMAIL` and
that the response is plain-text JSON with nowhere to put a link. Both still
true; left alone.

### Accessibility

Both defects the second review named are fixed by the same restructuring, not
by a separate patch:

- **The sole actionable element is no longer the quietest thing on the panel.**
  The `class="text-muted"` paragraph is gone. The anchor inherits `app.css`'s
  global `a { color: var(--accent) }` — the same token `.link-button` uses, so
  it renders exactly as loud as the "Subscribe again" affordance whose slot it
  occupies, and carries the UA underline.
- **The anchor is inside the live region.** It sits within the same
  `<p role="status">` as the sentence explaining the problem, so a screen
  reader announces the problem and the address that answers it as one
  announcement, rather than announcing the problem and leaving the answer
  outside the region entirely.

### Test change and mutation proof

Removed: `does not appear inside COMPLAINED_NO_RESUBSCRIBE_MESSAGE` — the
assertion that forbade the client from ever catching up to the server.

Added, in its place:

- **`composes with the LEAD/TAIL parts into exactly the whole message`** — the
  composition assertion. **Note a trap here:** asserting
  `LEAD + EMAIL + TAIL === COMPLAINED_NO_RESUBSCRIBE_MESSAGE` *alone* asserts
  nothing, because the message is now **defined** as that concatenation, so the
  comparison can never fail no matter what any part says. The test therefore
  compares the composition against `EXPECTED_COMPLAINED_SENTENCE`, the sentence
  written out once independently in the test file, and then additionally
  against the constant and against `inactiveStatusMessage('complained')`. Only
  the independent literal makes it load-bearing; mutation 1 below is the proof.
- **`splits the sentence at the clause boundary around the address`** — pins
  the seam itself (`LEAD` ends `/contact us at $/i`, `TAIL` is the exact
  trailing clause, neither part contains the address), because a composition
  that is correct as a whole can still put the anchor in the wrong place.
- **`drops the server no-op message's "No change: " lead-in and
  recapitalises`** — asserts the client half of the one documented divergence.

Also strengthened: `complained's copy names how to reach a human` now asserts
`toContain(COMPLAINED_CONTACT_EMAIL)`, not just `/contact us/i` — the exact
inertness the first bounce was about.

Three mutations, each reverted by file copy from a pre-mutation backup and each
confirmed byte-identical afterwards by `shasum` (never `git checkout --`):

**Mutation 1 — break the seam.** `LEAD` ends `"Contact us "` instead of
`"Contact us at "`:

```
 ❯ src/lib/subscribe.test.ts (30 tests | 2 failed)
     × composes with the LEAD/TAIL parts into exactly the whole message
AssertionError: expected 'This address is marked as having comp…' to be 'This address is marked as having comp…'
Expected: "… from this page. Contact us at hello@opencircuitsf.com if you believe this is a mistake."
Received: "… from this page. Contact us hello@opencircuitsf.com if you believe this is a mistake."
     × splits the sentence at the clause boundary around the address
AssertionError: expected 'This address is marked as having comp…' to match /contact us at $/i
 Tests  2 failed | 28 passed (30)
```

This is the mutation that proves the composition test is not tautological: the
constant tracked the mutated `LEAD` exactly, and the test still failed, because
it compares against the independent literal.

**Mutation 2 — freeze the client behind the server again.** Redefined
`COMPLAINED_NO_RESUBSCRIBE_MESSAGE` as the address-free literal the second pass
shipped (i.e. reconstructed the exact state this bounce is about):

```
 ❯ src/lib/subscribe.test.ts (30 tests | 2 failed)
     × complained's copy names how to reach a human
AssertionError: expected 'This address is marked as having comp…' to contain 'hello@opencircuitsf.com'
     × composes with the LEAD/TAIL parts into exactly the whole message
AssertionError: expected 'This address is marked as having comp…' to be 'This address is marked as having comp…'
Expected: "… Contact us if you believe this is a mistake."
Received: "… Contact us at hello@opencircuitsf.com if you believe this is a mistake."
 Tests  2 failed | 28 passed (30)
```

The drift the deleted test used to *forbid* is now the drift the replacement
test *catches* — from the useful direction.

**Mutation 3 — reuse the server string verbatim, lead-in and all.** Prefixed
`LEAD` with `"No change: "`:

```
 ❯ src/lib/subscribe.test.ts (30 tests | 2 failed)
     × composes with the LEAD/TAIL parts into exactly the whole message
AssertionError: expected 'No change: this address is marked as …' to be 'This address is marked as having comp…'
     × drops the server no-op message's "No change: " lead-in and recapitalises
 Tests  2 failed | 28 passed (30)
```

Each mutation was reverted immediately; the suite returned to 30/30 with the
file's `shasum` matching the backup exactly each time.

The `{#if}`/`{:else}` wiring itself still carries no automated coverage — the
project has no component-test infrastructure (`#0094`), unchanged by this pass.
The SSR-compile check above is the substitute: it reads the markup the compiler
actually emits for that branch.

### Verification

Frontend (from `web/`):

```
$ npm run check
COMPLETED 195 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS

$ npm test
 Test Files  8 passed (8)
      Tests  157 passed (157)      # was 155: −1 removed, +3 added

$ npm run build
✓ 168 modules transformed. ✓ built in 14.43s
```

The built bundle carries the split parts and the composition
(`Contact us at ",iu=" if you believe this is a mistake.",kh=su+Ti+iu`), i.e.
the shipped artifact is the composed sentence, not a stale literal.
`web/dist/index.html` was restored from a copy of `HEAD`'s placeholder taken
before the build; `git diff --stat web/dist/index.html` is empty.

Backend — **no Go file was changed by this pass**, so the Go runs are
confirmatory. Against a private database per `CLAUDE.md` §8b
(`opencircuit_0090`, created, migrated to `000011`, dropped afterwards):

```
$ go build ./...                                  # clean
$ go vet ./internal/handlers/...                  # clean
$ go test ./internal/handlers/ -run 'Preferences' -count=1
ok  github.com/brennanMKE/OpenCircuitSF/internal/handlers  17.298s
```

**`go test ./...` did not come back clean on this box, for an environmental
reason worth recording.** Two runs (one default, one `-p 1`) failed 7 tests,
every one of them with the identical error and none of them in a package this
change touches:

```
--- FAIL: TestMe_AdminUser (5.00s)
    me_test.go:46: truncate tables: timeout: context deadline exceeded
```

All seven pass when run targeted (`TestMe_AdminUser`, `TestMe_NormalUser`,
`TestAdminSubscribers_ClearComplaint_Success`,
`TestAdminUsers_DeactivateOtherRequiresNote` → 4/4 PASS, 18.6s). The cause is
machine load, not code: two other agents were running their own suites
concurrently and the box was at **load average 307** (`uptime`), which is
enough to starve `testdb`'s 5-second `TRUNCATE` deadline. Package wall times in
that run were absurd for the same reason — `internal/interests` 558s,
`internal/subscribers` 596s, against `#0091`'s measured 5m57s for the whole
suite. Zero skips in either run. Flagging rather than hiding it: a reviewer
running the full suite on a quiet box should see it green, and if they do not,
it is a `#0091`-adjacent flake and not this change, which touches only
`web/src/`.

`opencircuit_0090` dropped after the run; confirmed absent from `\l`.

### Files changed

- `web/src/lib/subscribe.ts` — `COMPLAINED_CONTACT_EMAIL` moved above the
  message block; `COMPLAINED_NO_RESUBSCRIBE_MESSAGE_LEAD` / `_TAIL` added;
  `COMPLAINED_NO_RESUBSCRIBE_MESSAGE` is now their composition with the
  address; three doc comments rewritten (two here, one in the component).
- `web/src/lib/subscribe.test.ts` — the forbidding assertion replaced by the
  composition assertion plus the seam and lead-in assertions; the independent
  `EXPECTED_COMPLAINED_SENTENCE` literal added; the "names how to reach a
  human" test strengthened to assert the address.
- `web/src/views/PreferenceCenter.svelte` — the complained arm renders one
  sentence with the anchor inside the `role="status"` region; the false
  "word-for-word mirror … must stay one" justification deleted.

No server file changed. `#0092`, `#0093`, `#0094`, `#0095` and `maskEmail`
were left alone as directed; `#0095` in particular depends on this split
landing and is deliberately not built here.

Status left `open`: `resolved` is the reviewer's call per `CLAUDE.md` §9.

## Work log

**2026-08-19 — implementation escalated to Opus.** `CLAUDE.md` §3 puts
implementation on Sonnet and escalates to Opus "only after the reviewer has
bounced the same issue twice, and record the escalation in `## Work log`."
Acceptance criterion 2 was bounced on 2026-08-19 (inert "Contact us") and again
on 2026-08-19 (`5aa9cd5`: the false mirror claim re-authored in a second file,
and the client frozen a revision behind the server by a test forbidding it from
catching up). This third implementation pass therefore ran on Opus. Planning
(phase 1) was not re-run: the second review prescribed the fix concretely
(split at the clause boundary, invert the boundary test into a composition
assertion), so a planning pass would have restated it.

Token/cost rows are the orchestrator's to append.
