# 0381 — A scoped verification cannot see the repo-wide guards that scan the edited file

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | tooling / docs |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-01 |
| **Commit** | — |

## Description

Several guards in this family **scan the whole tree from a package other than
the one being edited**. `#0356`'s `TestNoCommentCitesGoFileByLineNumber` lives
in `internal/handlers` and scans every Go comment in the repo.

So `ISSUE=NNNN scripts/check.sh go ./internal/outbox/...` — a correctly scoped
run under §5a — **cannot see a violation an `internal/outbox` edit introduces.**

**This left HEAD red for roughly 18 minutes.** `be1f08d` added 61 comment lines
to `internal/outbox/claim_comment_referent_guard_test.go` recording two guard
limits (`#0374`, `#0375`). Two of them cited `store.go:463` and
`internal/mailing/outbox_worker.go:475` by line number. The implementer's scoped
run passed; `#0376`'s reviewer found the failure while verifying an unrelated
diff in `internal/handlers`.

**The irony is the point**: the records were doc comments *about a guard*, and
they violated the rule `#0352` and `#0356` settled — replace a line citation
with a stable identifier, never a corrected number.

**And the citation was already wrong.** `#0365`'s work moved that site from line
475 to line **517** within the hour, which `#0374`/`#0375`'s reviewer noted as
empirical vindication.

## Acceptance criteria

1. Record, where an agent choosing a verification scope will meet it — CLAUDE.md
   §5, and/or each repo-wide guard's own doc comment — that **an edit to a file
   within a repo-wide guard's scan roots must also run that guard's owning
   package**.
2. **Name which packages own repo-wide guards**, measured rather than assumed.
   `#0356`'s line-citation guard in `internal/handlers` is one; enumerate the
   rest by finding the guards whose scan roots extend beyond their own package.
   Report the list.
3. **Do not build a mechanism** unless the enumeration shows it is warranted.
   §8's `#0258` case is six passes of adding checks, and `#0330`, `#0347` and
   `#0356` each ended by recording a limit rather than closing it. A sentence in
   §5 naming the owning packages may be the whole deliverable — if you think it
   is, say so and stop.
4. If you do consider a mechanism, weigh the obvious one honestly: making
   `scripts/check.sh` always include repo-wide guard packages would slow every
   scoped run, and §5a's whole point is that scoped runs are the default. Say
   what it would cost.
5. Any figure or file path you state is **re-measured**, not transcribed —
   **seven unsupported details reached issues this phase**, several written by
   the orchestrator.
6. Documentation only unless criterion 3's exception applies. `gofmt` stability
   if any Go file is touched.

## Relation

- Found by: [#0374](0374.md)/[#0375](0375.md)'s review, which fixed the red HEAD
- Relates to: [#0356](0356.md) — the repo-wide line-citation guard
- Relates to: [#0352](0352.md) — where "stable identifiers, not corrected
  numbers" was settled
- Relates to: [#0374](0374.md) — whose records tripped it
- Relates to: [#0330](0330.md), [#0347](0347.md) — the record-rather-than-build
  precedent criterion 3 rests on

## Enumeration (criterion 2) — measured, not assumed

Method: found every guard test file whose walk root is not its own package
directory, by grepping for `filepath.Walk`/`filepath.WalkDir` in `_test.go`
files repo-wide, then reading each file's own root/scan-roots variable:

```
$ grep -rln "filepath.Walk\|filepath.WalkDir" --include='*_test.go' .
cmd/opencircuit/wiring_parallel_guard_test.go
internal/outbox/claim_kinds_call_site_guard_test.go
internal/db/issue_citation_guard_test.go
internal/subscribers/events_append_only_guard_test.go
internal/handlers/dangling_test_citation_guard_test.go
internal/handlers/citation_target_guard_test.go
internal/handlers/citation_guard_test.go
```

(`internal/handlers/line_citation_guard_test.go` — `#0356`'s
`TestNoCommentCitesGoFileByLineNumber` — does not call `filepath.Walk`
itself; it reuses `citedTestScanRoots`, defined in
`dangling_test_citation_guard_test.go`, so it did not surface in the grep and
was found instead by `grep -rln TestNoCommentCitesGoFileByLineNumber .`)

Reading each file's root:

| File (package) | Root variable / value | Reaches beyond own package? |
|---|---|---|
| `cmd/opencircuit/wiring_parallel_guard_test.go` (`cmd/opencircuit`) | `baseDir := filepath.Dir(thisFile)` — its own directory only | **No** |
| `internal/outbox/claim_kinds_call_site_guard_test.go` (`internal/outbox`) | `claimKindsGuardScanRoots = []string{"..", "../../cmd"}` — `..` is `internal/` (every sibling package) | **Yes** |
| `internal/db/issue_citation_guard_test.go` (`internal/db`) | `issueGuardScanRoots = []string{"../.."}` — the repo root | **Yes** |
| `internal/subscribers/events_append_only_guard_test.go` (`internal/subscribers`) | `repoRootForEventsGuard(t)` then `filepath.Join(root, "internal")` — all of `internal/` | **Yes** |
| `internal/handlers/dangling_test_citation_guard_test.go`, `citation_target_guard_test.go`, `citation_guard_test.go`, `line_citation_guard_test.go` (`internal/handlers`) | `citedTestScanRoots`/`scanGoRoots` = `["..", "../../cmd", "../../web"]`, or (`citation_target_guard_test.go`) the full repo root | **Yes** |

**This "four packages" claim was wrong — see `## Review notes` below.** The
`filepath.Walk`/`WalkDir` grep this section's method relied on cannot see a
guard that reaches out by any other means: `os.ReadDir`, `parser.ParseDir`,
`parser.ParseFile`, `packages.Load`, or a bare `os.ReadFile`/`filepath.Join`
of a relative path never calls `Walk` or `WalkDir` and so never appears in
that grep's output. Three packages reach outside their own directory by one
of those means and were missed: `internal/seo` (`os.ReadFile` of two
literal paths), `cmd/opencircuit` (`os.ReadDir` + `parser.ParseFile` over
every import `servePostgres` resolves to — present in the same directory as
the file the grep *did* surface, `wiring_parallel_guard_test.go`, which is
why the package was seen and then wrongly ruled out on that one file's
evidence), and `internal/mailing` (`parser.ParseDir` of a sibling package).

**The corrected count is seven, not four**, re-derived this pass (the second
implementation pass, after the review bounce) by a method deliberately
different from both the first implementation pass's grep above and the
reviewer's own sweep: a Python scan of every `_test.go` file's double-quoted
string literals, resolving each one containing `..` and `/` relative to its
*package* directory (the directory `go test` sets as the process's working
directory, not the source file's own path) and flagging any resolved path
that falls outside that package directory — cross-checked with targeted
`grep` for `os.ReadDir`, `filepath.Glob`, `parser.ParseDir`, `packages.Load`,
and `runtime.Caller` to catch reaches built via `filepath.Join` on separate
literal arguments (invisible to the single-string-literal scan — this is how
`internal/handlers`'s `testdata/url_validators.json` reach and
`internal/mailing`'s `../outbox` reach were confirmed) rather than a single
literal containing `../`. Every broad-looking hit (e.g. `internal/db`,
`internal/audit`, `internal/workshops`, `internal/testdb`, `internal/auth`,
`internal/config` files that merely *mention* `CLAUDE.md`, `PRD.md`,
`docs/`, `migrations/`, or `issues/` inside a doc comment or error string)
was individually opened and confirmed to be prose, not a file read, before
being excluded — `internal/seo/invalidator_satisfier_guard_test.go`'s
`packages.Load(cfg, ".")` was checked the same way and excluded: it loads
only its own package. No eighth package was found.

Guards owned by each, by name:

- `internal/handlers`: `TestNoAdminFacingStringCitesInternalDocs`,
  `TestNoCommentCitesUndefinedTestFunction`,
  `TestNoCommentCitesGoFileByLineNumber` (`#0356`),
  `TestNoDocCommentNamesADifferentDeclarationInSameFile`
  (`misattached_doc_comment_guard_test.go`, uses `citedTestScanRoots`), and
  `TestNoCommentCitesUnresolvedPathOrSection`, whose root is **both** the
  repo root (via `collectRepoPaths`) **and** `CLAUDE.md`'s own `##` section
  headings (via `loadClaudeMDSections`, read fresh off disk each run) — plus
  `TestNoAdminAlertCitesInternalDocs`-family checks in
  `audit_email_metadata_guard_test.go`
  (`auditEmailMetadataScanRoots = ["..", "../../cmd"]`) and
  `TestResetSettings_PreservesFutureMigrationSeed` /
  `TestSettingsSeedStmtRe_MatchesIndentedInsert` in `settings_test.go`
  (`os.ReadDir("../../migrations")`) — together reaching the whole repo
  tree, `CLAUDE.md`'s section headings, `internal/`, `cmd/`, `web/`,
  `migrations/`, and `testdata/url_validators.json`. The three
  `web/src/lib/*.ts` reads (`router.ts`, `subscribe.ts`,
  `campaignProgress.ts`) do **not** belong to this citation-guard family —
  they are owned by `TestRouteTableParity_StaticRoutes`,
  `TestComplainedCopyParity_ContactEmail`, and
  `TestCampaignProgressParity_KeySet` respectively.
- `internal/db`: `TestNoOpenIssueVerificationCitesUndefinedTestFunction`
  (`issueGuardScanRoots = ["../.."]`, the repo root, for `issues/*.md`
  citations), `TestPRDWorkshopAndMailingIndexParity` /
  `TestPRDWorkshopAndMailingColumnParity` / `TestPRDSectionIndexParity` /
  `TestPRDSectionIndexTotalMatchesFile` (`prdPath = "../../PRD.md"`), and
  `TestDatabaseDocMigrationParity` (`docsPath = "../../docs/database.md"`,
  `claudeMDPath = "../../CLAUDE.md"`, plus `os.ReadDir("../../migrations")`)
  — together reaching `issues/`, `PRD.md`, `CLAUDE.md`, `docs/database.md`,
  and `migrations/`.
- `internal/outbox`: `TestNoUnscopedOutboxClaimCallOutsidePackage`
  (`claimKindsGuardScanRoots = ["..", "../../cmd"]`) and
  `TestNoCommentClaimsClaimMachineryCallFileCodeLacks` /
  `TestNoClaimMachineryCallSyntaxLiteralInAnyComment`
  (`claimCommentGuardScanRoots = ["..", "../../cmd"]`, the guard whose edit in
  `be1f08d` caused this issue) — together reaching all of `internal/` plus
  `cmd/`.
- `internal/subscribers`: `TestSubscriberEvents_AppendOnlyGuard` —
  scanning all of `internal/` for SQL text naming `subscriber_events` in an
  `UPDATE`/`DELETE`.
- `internal/seo`: `TestDistIndexPlaceholder` (`#0141`,
  `distIndexPath = "../../web/dist/index.html"`) and
  `TestSourceTemplate_EachTokenAppearsExactlyOnce`
  (`templatePath = "../../web/index.html"`).
- `cmd/opencircuit`: `TestServePostgres_OneSEOSiteFlowsToEveryConsumer`
  (`servepostgres_seo_instance_guard_test.go`) — resolves every module-local
  import in `main.go` to `filepath.Join("..", "..", rel)`, then
  `os.ReadDir`s and `parser.ParseFile`s that directory to check `*seo.Site`
  parameter flow through `servePostgres`.
- `internal/mailing`: `TestMailKindsCoversEveryOutboxKind`
  (`outbox_worker_kinds_guard_test.go`) — `parser.ParseDir(fset, "../outbox",
  …)`.

## Decision (criteria 3, 4) — recorded, nothing built

**Recording only; no mechanism built.** Per criterion 3's stop condition, and
matching the precedent it names (`#0330`, `#0347`, `#0356` each ended by
recording a limit rather than closing it): the deliverable is the sentence
added to `CLAUDE.md` §5 (see below) naming the seven owning packages and
their scan roots, plus this issue's own enumeration as the measured backing
for it. (Corrected from "four" to "seven" on the second implementation pass
— see `## Enumeration` above and the `## Review notes` bounce; this
correction does not touch the judgment below, which the review independently
verified and endorsed.)

**The obvious mechanism, weighed honestly (criterion 4).** Making
`scripts/check.sh`'s default `go` scope always append all seven owning
packages (`./internal/handlers/... ./internal/db/... ./internal/outbox/...
./internal/subscribers/... ./internal/seo/... ./cmd/...
./internal/mailing/...`) to every scoped run would guarantee the guards run,
but at a real cost: `internal/handlers` alone took **7.7s** in the first
pass's own `ISSUE=0377 scripts/check.sh` run (`cmd/opencircuit` took
**8.5s**; the review's independent re-measurement found **6.443s** and
**7.241s** respectively) against `internal/db`'s 0.3s–0.476s and
`internal/subscribers`'s 0.8s–0.937s — so a scoped run on an unrelated single
small package (say, `internal/audit`) would roughly double or more in wall
time to buy protection it usually does not need, on every run, forever.
§5a's whole point is that scoped runs are cheap and are the default;
automatically widening every one of them to include seven packages (two of
them slow) trades that away for a failure mode that a one-sentence lookup in
§5 already prevents an agent from hitting, provided the agent reads it. This
mirrors §8's `#0258` lesson the other direction: not every guard gap needs a
guard.

**Where it's recorded.** `CLAUDE.md` §5, immediately after "Always set
`ISSUE=NNNN`" — the sentence an agent choosing a verification scope will
meet right where it decides that scope (criterion 1's placement instruction).
No repo-wide guard's own doc comment was also edited; the issue's "and/or"
permits recording in one place, and duplicating the same seven-package list
into seven files is the "copy of the answer stored next to the question"
`#0356`'s planning pass and CLAUDE.md §8 both warn against — one is enough
for an agent to find via `grep -n "repo-wide guard" CLAUDE.md`.

## Verification

**Criterion 5** — every figure above was re-measured this pass: the
`filepath.Walk`/`WalkDir` grep, each file's root variable read directly, the
guard names read from each file's own `func Test…` declarations, and the
7.7s/8.5s/0.3s/0.8s timings read from this pass's own `ISSUE=0377
scripts/check.sh` transcript (not from a prior issue).

**No `.go` file touched** (criterion 6's default): `git diff --cached
--name-only` before commit shows only `CLAUDE.md` and `PRD.md` (the latter
from the batched `#0377`); `gofmt -l` is therefore not applicable to this
issue's own change.

`uptime` before the scoped run: `load averages: 6.27 3.43 2.59`. `ISSUE=0377
scripts/check.sh go ./internal/db/... ./internal/subscribers/...
./internal/handlers/... ./cmd/...` — deliberately including
`internal/handlers`, the very package whose guard could not see a violation
from a narrower scope — `go build`, `go vet`, `gofmt -l` clean;
`internal/db`, `internal/subscribers`, `internal/handlers`,
`cmd/opencircuit` all `ok`, zero skips, `VERIFICATION PASSED`.
`scripts/check.sh guards` (`:5173` confirmed free via `lsof` first, run
alone) → all guard families held, `VERIFICATION PASSED`, including the
`lineCitationGuardMinPlausibleFileCount` (#0356) and outbox claim-kinds
floors that back two of the four packages named above. Scratch database
`opencircuit_test_0377` dropped by `check.sh`'s own exit cleanup; no
leftover `go test`/`.test` processes.

(The second implementation pass's re-verification, after the bounce below,
is recorded in its own `## Work log` entry.)

## Review notes

**Verdict: BOUNCED** (review, claude-opus-5, phase 3 subagent). Criterion 3's
judgement — record, do not build — is **correct and I endorse it**. Criterion 2's
enumeration, which is the actual deliverable, is **incomplete**, and the
incompleteness is now a false statement in `CLAUDE.md` §5. Criterion 2 said
"measured rather than assumed"; the measurement had a blind spot that hid three
packages, one of which the pass explicitly ruled *out*.

### The enumeration is wrong — seven packages, not four

I re-derived the list by a deliberately different method: instead of grepping for
`filepath.Walk`/`WalkDir`, I swept for (a) every `_test.go` containing a path
literal escaping its own package (`"../`), (b) `runtime.Caller` (the repo-root
idiom), and (c) non-`Walk` directory enumeration — `os.ReadDir`, `filepath.Glob`,
`parser.ParseDir`, `packages.Load` — then read each hit's root.

**The `filepath.Walk` grep cannot see a guard that reaches out by any other
means, and three do.** Measured at `HEAD`:

| Package | Reaches beyond own package via | Pass's list |
|---|---|---|
| `internal/handlers` | `citedTestScanRoots`/`scanGoRoots` = `["..", "../../cmd", "../../web"]`; `auditEmailMetadataScanRoots` = `["..", "../../cmd"]`; `"../../migrations"`; `"../../web/src/lib/router.ts"`; `"../../web/src/lib/campaignProgress.ts"`; `"../../web/src/lib/subscribe.ts"`; `"../../testdata/url_validators.json"` | ✅ listed |
| `internal/db` | `["../.."]`, `"../../issues"`, `"../../PRD.md"`, `"../../CLAUDE.md"`, `"../../migrations"`, `"../../docs/database.md"` | ✅ listed |
| `internal/outbox` | `claimKindsGuardScanRoots` **and** `claimCommentGuardScanRoots`, both `["..", "../../cmd"]` | ✅ listed |
| `internal/subscribers` | `repoRootForEventsGuard` → `internal/` | ✅ listed |
| **`internal/seo`** | `distIndexPath = "../../web/dist/index.html"`; `seo_test.go`'s `"../../web/index.html"` | ❌ **missing** |
| **`cmd/opencircuit`** | `servepostgres_seo_instance_guard_test.go`'s `importDirs` maps every `github.com/brennanMKE/OpenCircuitSF/…` import to `filepath.Join("..", "..", rel)` and `os.ReadDir`s + parses it | ❌ **ruled out, wrongly** |
| **`internal/mailing`** | `parser.ParseDir(fset, "../outbox", …)` | ❌ **missing** |

Three findings, in descending severity:

1. **`cmd/opencircuit` was ruled out on the wrong file.** The pass wrote that
   `TestNoWiringTestUsesParallel` "scans only its own directory and is **not**
   repo-wide … I did not include it on the strength of the grep hit alone." That
   reasoning is sound *about that file* — and `wiring_parallel_guard_test.go` is
   the only file in `cmd/opencircuit` the `filepath.WalkDir` grep surfaced. But
   its sibling `servepostgres_seo_instance_guard_test.go` reads **other packages'
   source**: `TestServePostgres_OneSEOSiteFlowsToEveryConsumer` resolves each of
   `servePostgres`'s module-local imports to `../../<pkg>` and parses those
   directories to check `*seo.Site` parameter flow. It uses `os.ReadDir`, so the
   grep never saw it. A signature change in `internal/seo` or any package
   `servePostgres` calls can therefore break a test in `cmd/opencircuit` — the
   exact failure this issue exists to prevent, in the one package the pass told
   future agents they may safely omit.

2. **`internal/seo` is missing, and it guards the file `CLAUDE.md` §8b warns about
   most.** `TestDistIndexPlaceholder` (`#0141`) reads `../../web/dist/index.html`
   and fails if a real `npm run build` has been committed over the tracked
   placeholder. §8b records that three agents have already overwritten that file
   with `./scripts/dev.sh --built`. An agent consulting the committed §5 paragraph
   after doing so is told `internal/handlers` covers `web/` — so it runs
   `internal/handlers`, which does not carry this guard, and concludes it is
   clean. **The paragraph as written actively misdirects for this case.**

3. **`internal/db`'s and `internal/outbox`'s guard lists are each missing the
   entry that matters most to the work in front of them.** For `internal/db` the
   pass named only `TestNoOpenIssueVerificationCitesUndefinedTestFunction`, omitting
   `TestPRDSectionIndexParity` / `TestPRDSectionIndexTotalMatchesFile`
   (`../../PRD.md`, `../../CLAUDE.md`) and `docs_parity_test.go` — i.e. the guard
   that `#0377`, this pass's own batch partner, exists to satisfy. For
   `internal/outbox` it named only `TestNoUnscopedOutboxClaimCallOutsidePackage`,
   omitting `claim_comment_referent_guard_test.go`'s two guards — the file whose
   edit in `be1f08d` **caused this issue**. Both packages are on the list, so
   `CLAUDE.md`'s actionable content survives for them; the enumeration reported
   under criterion 2 does not.

The stakes are the ones the issue itself names: this is documentation that tells
future agents which packages they must run, so an incomplete list is worse than
none. Criterion 5 ("any figure or file path you state is re-measured — seven
unsupported details reached issues this phase") binds the enumeration too, and
"four" is now the eighth.

### Criterion 3/4 — recording rather than building was right

I verified the timings independently in my own `ISSUE=0377r scripts/check.sh` run:
`internal/db` **0.476s**, `internal/subscribers` **0.937s**, `internal/handlers`
**6.443s**, `cmd/opencircuit` **7.241s**. The pass reported 0.3s / 0.8s / 7.7s /
8.5s — different absolute numbers on a differently-loaded machine, but the ratio
that carries the argument is reproduced exactly: the two slow packages cost
roughly **8×** the two fast ones.

The trade is a real one and the pass called it correctly. Appending the guard
packages to every scoped run would add ~15s to a run of a package like
`internal/audit` that finishes in well under a second — not a doubling, closer to
an order of magnitude — on every run, forever, to buy protection that is needed
only when the edit lands inside a scan root. §5a's cheap-scoped-run default is
load-bearing for the three-agent concurrency ceiling, and spending it on this
would be the `#0258` shape: adding mechanism where the gap is a knowledge gap.
`#0330`, `#0347` and `#0356` each ended the same way, and this case does **not**
differ from them. Keep the decision; it survives re-implementation unchanged.

**This bounce is about the enumeration only.** Nothing about criteria 3 or 4
needs revisiting, and no mechanism should be built.

### The remedy, exactly

Replace the paragraph added to `CLAUDE.md` §5 (currently beginning "**A scoped run
can miss a repo-wide guard's own package (`#0381`).** Four packages own guards…")
with, verbatim:

> **A scoped run can miss a repo-wide guard's own package (`#0381`).** Seven
> packages own tests whose scan roots reach beyond their own directory, so an
> edit inside one of those roots must add that package to the scoped run:
>
> | Package | Reaches into |
> |---|---|
> | `internal/handlers` | `internal/`, `cmd/`, `web/` (the citation-guard family and the audit-metadata guard), plus `migrations/`, `testdata/`, and three `web/src/lib/*.ts` parity fixtures |
> | `internal/db` | the repo root — `issues/`, `PRD.md`, `CLAUDE.md`, `migrations/`, `docs/database.md` |
> | `internal/outbox` | `internal/` and `cmd/` (both the claim-kinds and claim-comment guards) |
> | `internal/subscribers` | all of `internal/` |
> | `internal/seo` | `web/index.html` and `web/dist/index.html` (`#0141`'s placeholder guard) |
> | `cmd/opencircuit` | every `internal/*` package `servePostgres` imports, parsed for `*seo.Site` flow |
> | `internal/mailing` | `internal/outbox` |
>
> Editing `PRD.md` or `CLAUDE.md` means running `internal/db`; editing
> `web/dist/index.html` means running `internal/seo`; editing a Go comment
> anywhere means running `internal/handlers`. `scripts/check.sh` is deliberately
> **not** widened to do this automatically — `internal/handlers` and
> `cmd/opencircuit` each cost ~8× a small package, and §5a's cheap scoped run is
> what makes three concurrent agents workable.

Then correct this issue's `## Enumeration` section to match: state the method's
blind spot (a `filepath.Walk` grep cannot see `os.ReadDir`, `parser.ParseDir`,
`packages.Load`, or a bare `os.ReadFile` of a relative path), list all seven
packages with the guard names each owns, and replace the "Four packages own
repo-wide guards" claim and the `cmd/opencircuit` ruling-out paragraph. Re-run
`ISSUE=NNNN scripts/check.sh go ./internal/db/...` afterwards — editing
`CLAUDE.md` is itself an edit inside `internal/db`'s scan roots, which is the
rule this issue is about.

### Second review (2026-09-02) — BOUNCED again, on `internal/handlers`'s stated root

**The package set is now correct and I could not break it.** I re-derived it a
fourth time by a method none of the three previous derivations used — all three
of those were *static* scans of test source, and a package that reaches out
through a path assembled at run time would be invisible to every one of them
identically. This derivation is **dynamic**: it observes what the test binaries
actually open.

Method, and why it is independent:

1. `git archive HEAD | tar -x` into a throwaway copy outside the repo (nothing
   in the live tree was touched at any point).
2. `go test -c` **every** test package first, so that compile-time source reads
   are finished before any measurement begins — this is what separates "the
   compiler read it" from "the test read it", and it is why the result is not
   just a re-run of the static scans.
3. Reset every file's and directory's access time to 2000-01-01, drop a marker
   file, then run one package's test binary **from its own package directory**
   (reproducing `go test`'s working directory exactly), then
   `find -neweram <marker>` for everything whose atime advanced.
4. Repeat for all 17 test packages, with `TEST_DATABASE_URL` pointed at a
   private scratch database so that DB-backed tests actually ran rather than
   skipping — an earlier attempt without one missed `internal/handlers`'s
   `migrations/` reads entirely, because they live in a DB-backed test.

Two calibration notes, both of which had to be fixed before the result meant
anything. This volume behaves like `relatime`: atime advances only when it is
already older than mtime, so without step 3's reset the first run detected
**nothing at all** and looked like a clean bill of health. And directories had
to be measured as well as files, because `os.ReadDir` touches the directory,
not the files in it. A control — reset, read `PRD.md`, scan — reported exactly
`PRD.md` and nothing else.

**Result: the same seven packages, and the other ten read nothing whatsoever
outside their own directory.**

| Package | Out-of-package paths opened at run time |
|---|---|
| `internal/db` | 726 |
| `internal/outbox` | 285 |
| `internal/subscribers` | 251 |
| `internal/handlers` | 250 |
| `internal/seo` | 117 |
| `cmd/opencircuit` | 115 |
| `internal/mailing` | 2 |
| `internal/audit`, `internal/auth`, `internal/config`, `internal/devstore`, `internal/events`, `internal/interests`, `internal/middleware`, `internal/sesnotify`, `internal/testdb`, `internal/workshops` | **0 each** |

The zeroes are the part the static methods could not give: they are positive
evidence of absence, measured, for every package not on the list. There is no
eighth package. Criterion 2's headline claim is now settled.

One false positive the method produced, adjudicated by reading the code rather
than by trusting the measurement: `internal/seo` opened source in twelve other
`internal/*` packages. That is `invalidator_satisfier_guard_test.go`'s
`packages.Load(cfg, ".")` running with `packages.NeedDeps` and a cross-compile
`Env`, type-checking `internal/seo`'s own import graph — and `internal/seo`
imports `internal/handlers`, whose transitive deps are exactly the twelve. Those
are compile dependencies, which `go build ./...` already covers on every run,
not a guard scanning a subject. The second implementation pass excluded this
same guard for substantially the same reason, and was right to.

### What is still wrong: `internal/handlers`'s row understates its root, and the prose sends `CLAUDE.md` edits to the wrong package

This is the same defect shape the first review bounced on, one level down. The
first review's words were that an incomplete list "actively misdirects" — an
agent editing `web/dist/index.html` was told to run `internal/handlers` when
`internal/seo` owns that guard. The committed paragraph now does the same thing
for the single most common non-Go edit in this repo: editing `CLAUDE.md`.

The paragraph says, in prose: "Editing `PRD.md` or `CLAUDE.md` means running
`internal/db`". Its `internal/handlers` row lists `internal/`, `cmd/`, `web/`,
`migrations/`, `testdata/`, and three `web/src/lib/*.ts` fixtures — **`CLAUDE.md`
does not appear in it, and neither does the repo-root walk.**

Both are wrong. `citation_target_guard_test.go`'s `collectRepoPaths` walks the
**entire repository root** to build the set of paths a Go comment may cite, and
`loadClaudeMDSections` reads `CLAUDE.md` off disk and parses its own `##`
numbered headings as the ground truth for `CLAUDE.md §N` citations.

Measured, not argued. In the throwaway copy I renumbered exactly one heading —
`## 8b.` to `## 8z.` — changing nothing else in the tree, and ran both packages:

- `internal/handlers` `TestNoCommentCitesUnresolvedPathOrSection` **FAILED**,
  naming 17+ now-unresolvable `CLAUDE.md §8b` citations across
  `cmd/opencircuit` and `internal/handlers`.
- `internal/db`, the package the paragraph tells you to run for a `CLAUDE.md`
  edit, ran its full suite and **PASSED**.

So an agent who renumbers or deletes a `CLAUDE.md` section, follows the
paragraph, runs `internal/db`, and sees green has left `HEAD` red — which is
precisely the 18-minute red `HEAD` in this issue's own `## Description`. The
same guard fails on renaming or deleting any repository file that a Go comment
cites, which the row's `internal/`, `cmd/`, `web/` framing also fails to convey.

Everything else in the table checked out against the dynamic measurement:
`internal/db` "the repo root" is right (it walks the whole tree);
`internal/outbox` `internal/` + `cmd/` is right; `internal/subscribers` all of
`internal/` and not `cmd/` is right; `cmd/opencircuit` reads 13 `internal/*`
packages, right; `internal/mailing` reads `internal/outbox/store.go`, right;
`internal/seo`'s `web/index.html` and `web/dist/index.html` are right. The
three `web/src/lib/*.ts` fixtures are exactly `router.ts`, `subscribe.ts` and
`campaignProgress.ts`, and `testdata/url_validators.json` was observed too.

Secondary, same class as the first review's third finding: the per-package guard
list for `internal/handlers` omits
`TestNoDocCommentNamesADifferentDeclarationInSameFile`
(`misattached_doc_comment_guard_test.go`), which uses `citedTestScanRoots` and
is therefore a repo-wide guard, and it attributes the three `web/src/lib/*.ts`
reads to the citation-guard family when they belong to
`TestRouteTableParity_StaticRoutes`, `TestComplainedCopyParity_ContactEmail`
and `TestCampaignProgressParity_KeySet`.

### Criteria 3 and 4 — endorsed again, untouched

The `## Decision` section preserved the record-don't-build judgement intact,
including both sets of timings, and correctly marked the four-to-seven
correction as not disturbing it. Nothing there needs revisiting and no mechanism
should be built.

### The remedy, exactly — transcription only

**One.** In `CLAUDE.md` §5, replace the `internal/handlers` table row verbatim
with:

> | `internal/handlers` | **the whole repo tree** — `TestNoCommentCitesUnresolvedPathOrSection` walks the repo root for the set of citable paths and reads `CLAUDE.md`'s own `##` section headings; the rest of the citation-guard family, `TestNoDocCommentNamesADifferentDeclarationInSameFile`, and the audit-metadata guard scan `internal/`, `cmd/`, `web/`; plus `migrations/`, `testdata/url_validators.json`, and three `web/src/lib/*.ts` parity fixtures |

**Two.** In the same paragraph, replace the sentence beginning "Editing
`PRD.md` or `CLAUDE.md` means running `internal/db`" verbatim with:

> Editing `PRD.md` means running `internal/db`. Editing `CLAUDE.md` means
> running **both** `internal/db` and `internal/handlers` — renumbering or
> deleting a `##` section, or renaming any file a Go comment cites, fails
> `internal/handlers`'s `TestNoCommentCitesUnresolvedPathOrSection` while
> `internal/db` passes clean. Editing `web/dist/index.html` means running
> `internal/seo`; editing a Go comment anywhere means running
> `internal/handlers`.

**Three.** In this issue's `## Enumeration`, in the `internal/handlers` bullet:
add `TestNoDocCommentNamesADifferentDeclarationInSameFile` to the named guards,
say that `TestNoCommentCitesUnresolvedPathOrSection`'s root is the repo root
**and** `CLAUDE.md`'s section headings, and attribute the three
`web/src/lib/*.ts` reads to `TestRouteTableParity_StaticRoutes`,
`TestComplainedCopyParity_ContactEmail` and
`TestCampaignProgressParity_KeySet` rather than to the citation-guard family.

**Change nothing else.** The seven-package set is confirmed and must not move.
Criteria 3 and 4 are settled. Then re-run `ISSUE=NNNN scripts/check.sh go
./internal/db/... ./internal/handlers/...` — which is now the rule the
paragraph itself states, applied to the edit you just made.


## Work log

- 2026-09-01 — Filed by the orchestrator from `#0374`/`#0375`'s review (§9).
- 2026-09-02 — implement (claude-sonnet-5, phase 2 subagent, batched with
  `#0377`): enumerated the four packages owning repo-wide guards
  (`internal/handlers`, `internal/db`, `internal/outbox`,
  `internal/subscribers`) by grepping for `filepath.Walk`/`WalkDir` in
  `_test.go` files and reading each file's own scan-root variable, then
  ruling out `cmd/opencircuit`'s `TestNoWiringTestUsesParallel` (walks only
  its own directory). Decided, per criterion 3's stop condition and the
  `#0330`/`#0347`/`#0356` precedent, to record rather than build: added one
  paragraph to `CLAUDE.md` §5 naming the four packages and their scan roots,
  placed immediately after "Always set `ISSUE=NNNN`". Weighed the "always
  widen `check.sh`'s scope" mechanism honestly against this pass's own
  measured timings (`internal/handlers` 7.7s, `cmd/opencircuit` 8.5s vs.
  `internal/db` 0.3s and `internal/subscribers` 0.8s) and declined to build
  it. No `.go` file in the diff. Verified with `ISSUE=0377 scripts/check.sh
  go ./internal/db/... ./internal/subscribers/... ./internal/handlers/...
  ./cmd/...` and `scripts/check.sh guards`, both `VERIFICATION PASSED`, zero
  skips. Left status `in-progress` for review (§9 — only the reviewer sets
  `resolved`). Usage/cost not recorded here — no transcript token accounting
  available to this subagent; the orchestrator should append the usage row
  per `issues/Issues.md`'s `## Work log` convention.
- 2026-09-02 — review (claude-opus-5, phase 3 subagent, reviewed independently of
  the batched `#0377`): **bounced on criterion 2.** Re-derived the enumeration by
  a different method (escaping path literals, `runtime.Caller`, and non-`Walk`
  directory enumeration — `os.ReadDir`/`parser.ParseDir`/`packages.Load`) and
  measured **seven** packages, not four: `internal/seo` and `internal/mailing`
  were missing, and `cmd/opencircuit` was ruled out on `wiring_parallel_guard_test.go`
  while its sibling `servepostgres_seo_instance_guard_test.go` parses every
  module-local import's directory via `os.ReadDir`. The `filepath.Walk` grep was
  the blind spot. `internal/db`'s and `internal/outbox`'s per-guard lists also
  omit, respectively, the PRD/CLAUDE index guards and the claim-comment guard
  whose edit caused this issue. Endorsed criteria 3 and 4 unchanged — verified
  the timing ratio independently (`internal/handlers` 6.443s and `cmd/opencircuit`
  7.241s against `internal/db` 0.476s and `internal/subscribers` 0.937s, ~8×) and
  agree that recording beats building here, matching `#0330`/`#0347`/`#0356`; no
  mechanism should be built. §3 decision: **no escalation** — first bounce, and
  the remedy is written out verbatim above (the replacement `CLAUDE.md` paragraph
  and the enumeration correction), so the next pass is transcription and stays on
  Sonnet.
- 2026-09-02 — implement, second pass (claude-sonnet-5, phase 2 subagent):
  transcribed the reviewer's verbatim replacement paragraph into `CLAUDE.md`
  §5 (same location, immediately after "Always set `ISSUE=NNNN`"), listing
  all seven packages. Before applying it, independently re-derived the
  enumeration by a third method — distinct from both the first pass's
  `filepath.Walk`/`WalkDir` grep and the reviewer's escaped-literal/
  `runtime.Caller`/non-`Walk`-enumeration sweep: a Python scan resolving
  every `_test.go` file's double-quoted string literals containing `..`
  against that file's *package* directory (the `go test` working directory),
  flagging any that resolve outside it, cross-checked with targeted `grep`
  for `os.ReadDir`, `filepath.Glob`, `parser.ParseDir`, `packages.Load`, and
  `runtime.Caller` to catch reaches built via `filepath.Join` on separate
  literal arguments (invisible to the single-literal scan). Read every hit
  individually rather than trusting the grep, including the ones that turned
  out to be prose mentions (`internal/audit`, `internal/workshops`,
  `internal/testdb`, `internal/auth`, `internal/config`) and one guard that
  loads only its own package (`internal/seo`'s `packages.Load(cfg, ".")` in
  `invalidator_satisfier_guard_test.go`) and so does not belong on the list.
  Result: the same seven packages the reviewer found —
  `internal/handlers`, `internal/db`, `internal/outbox`,
  `internal/subscribers`, `internal/seo`, `cmd/opencircuit`,
  `internal/mailing` — with no eighth found. Corrected this issue's own
  `## Enumeration` and `## Decision` sections from "four" to "seven",
  documented the `filepath.Walk`-grep blind spot, and named every guard each
  package owns (also naming `internal/db`'s PRD/CLAUDE.md index guards and
  `internal/outbox`'s claim-comment guard, the two per-package omissions the
  review flagged separately from the missing-package finding). Left criteria
  3 and 4's judgment (record, don't build) untouched per the review's
  explicit endorsement. No `.go` file touched;
  gofmt not applicable. `uptime` before the run: `load averages: 2.78 2.67
  2.85`. Verified with `ISSUE=0381b scripts/check.sh go ./internal/db/...
  ./internal/handlers/... ./internal/seo/... ./cmd/...` — `go build`,
  `go vet`, `gofmt -l` clean; `internal/db` (0.400s), `internal/handlers`
  (6.188s), `internal/seo` (0.858s), `cmd/opencircuit` (5.602s) all `ok`,
  zero skips, `VERIFICATION PASSED`. (The template rebuild noted by
  `check.sh` at the start of the run is `#0360`'s pre-existing, expected
  condition, not a defect.) Scratch database `opencircuit_test_0381b`
  dropped by `check.sh`'s own exit cleanup; confirmed no leftover
  `go test`/`.test` processes and `:5173` free. Confirmed via `git diff
  CLAUDE.md` that only §5's single paragraph changed and §11's PRD index
  table is untouched, and confirmed via `grep -nE '\.go:[0-9]+'` on the diff
  that no `<file>.go:<line>` citation entered the text. Left status
  `in-progress` for review (§9). Usage/cost not recorded here — no
  transcript token accounting available to this subagent.
- 2026-09-02 — review, second pass (claude-opus-5, phase 3 subagent):
  **bounced again, on criterion 1/2's `internal/handlers` entry.** Re-derived
  the package set a fourth time by a **dynamic** method — precompiled every
  test binary, reset all access times, ran each binary from its own package
  directory against a private scratch database, and collected every path whose
  atime advanced — and confirmed the seven-package set exactly, with a measured
  **zero** out-of-package reads for each of the other ten test packages. No
  eighth package exists. Re-ran the full scoped verification over all seven
  (`ISSUE=0381r2 scripts/check.sh go ./internal/db/... ./internal/handlers/...
  ./internal/seo/... ./internal/mailing/... ./internal/outbox/...
  ./internal/subscribers/... ./cmd/...`): `go build`, `go vet`, `gofmt -l`
  clean, all seven `ok`, zero skips, `VERIFICATION PASSED`. Confirmed the
  `CLAUDE.md` diff is a single hunk in §5, that §11's PRD index table is
  untouched, and that no `<file>.go:<int>` citation entered either commit
  (`#0352`). Confirmed `## Decision` preserved criteria 3 and 4's endorsed
  judgement unchanged; endorsed it again. **The defect:** the paragraph's
  `internal/handlers` row omits the repo-root walk and `CLAUDE.md`, and its
  prose sends a `CLAUDE.md` edit to `internal/db` alone — proved wrong by
  mutating a throwaway copy's `CLAUDE.md` heading `## 8b.` to `## 8z.` and
  nothing else, whereupon `internal/handlers`'s
  `TestNoCommentCitesUnresolvedPathOrSection` failed on 17+ citations while
  `internal/db` passed its whole suite. That is the same "actively misdirects"
  shape as the first bounce, one level down. §3 decision: **no escalation, stays
  on Sonnet.** The two bounces are not two passes at one design problem — the
  next pass has nothing to decide: the replacement table row and the replacement
  sentence are both written out verbatim above, the seven-package set is fixed
  and must not move, and criteria 3 and 4 are settled. That is transcription,
  which §3 puts on Sonnet at a fraction of the cost. Nothing in the live working
  tree was modified by this review's measurements; the scratch copy and scratch
  database were dropped. Usage/cost not recorded here — no transcript token
  accounting available to this subagent.
- 2026-09-02 — implement, third pass (claude-sonnet-5, phase 2 subagent):
  transcribed the second review's verbatim remedy — the orchestrator's
  briefing flagged this pass as transcription, not design, and independent
  verification confirmed that framing before applying anything. Read
  `collectRepoPaths` (`internal/handlers/citation_target_guard_test.go`) and
  confirmed it `filepath.WalkDir`s the whole repo root for the set of citable
  paths; read `loadClaudeMDSections` in the same file and confirmed it reads
  `CLAUDE.md` fresh off disk each run and parses its own `##\s+(\d+[A-Za-z]?)\.`
  headings — both exactly as the review stated. Confirmed
  `TestNoDocCommentNamesADifferentDeclarationInSameFile`
  (`misattached_doc_comment_guard_test.go`) uses `citedTestScanRoots`, making
  it repo-wide. Confirmed by direct grep that the three `web/src/lib/*.ts`
  reads belong to `TestRouteTableParity_StaticRoutes` (`routerTSPath` in
  `routes_parity_test.go`), `TestComplainedCopyParity_ContactEmail`
  (`complainedCopyTSPath` in `complained_copy_parity_test.go`), and
  `TestCampaignProgressParity_KeySet` (`campaignProgressTSPath` in
  `campaign_progress_parity_test.go`) — not to the citation-guard family as
  the committed table implied. Applied the review's verbatim replacement
  `internal/handlers` table row and replacement sentence to `CLAUDE.md` §5
  (same location, immediately after "Always set `ISSUE=NNNN`"), and corrected
  this issue's own `## Enumeration` `internal/handlers` bullet to add
  `TestNoDocCommentNamesADifferentDeclarationInSameFile`, state
  `TestNoCommentCitesUnresolvedPathOrSection`'s root as both the repo root and
  `CLAUDE.md`'s section headings, and attribute the three `.ts` reads to the
  three parity tests above rather than the citation-guard family. Left `##
  Decision` (criteria 3, 4) untouched — both bounces endorsed it and neither
  is revisited. No `.go` file touched; `gofmt` not applicable. Confirmed via
  `git diff -- CLAUDE.md` that only the two named spots inside the existing §5
  paragraph changed (2 hunks in git's unified-diff output, both within that
  one paragraph — no other part of the file touched) and via `grep -n "^##
  11\." CLAUDE.md` plus a diff scan that §11's PRD index table is untouched.
  Confirmed via `grep -nE '\.go:[0-9]+'` on the `CLAUDE.md` diff that no
  `<file>.go:<int>` citation entered the text (`#0352`). `uptime` before the
  run: `load averages: 1.62 1.79 2.12`. Verified with `ISSUE=0381c
  scripts/check.sh go ./internal/db/... ./internal/handlers/...` — `go build`,
  `go vet`, `gofmt -l` clean; `internal/db` (0.407s) and `internal/handlers`
  (5.239s) both `ok`, zero skips, `VERIFICATION PASSED`. (The template rebuild
  `check.sh` reported at the start is `#0360`'s pre-existing, expected
  condition.) Additionally ran the specific guard the mutation in the second
  review exercised — `go test ./internal/handlers/... -run
  'TestNoCommentCitesUnresolvedPathOrSection' -v` against a fresh
  `scripts/testdb.sh create 0381c` database — and it **PASSED** in 0.14s
  against the corrected `CLAUDE.md`. Scratch database `opencircuit_test_0381c`
  dropped (once by `check.sh`'s own exit cleanup, once by hand via
  `scripts/testdb.sh drop 0381c` after the standalone `-run`); confirmed no
  leftover `go test`/`.test` processes and `:5173` free. Committed `CLAUDE.md`
  and this issue file as two separate commits, each staged and committed by
  pathspec, each verified with `git show --stat HEAD` immediately after.
  `git status --porcelain` before staging showed only these two files dirty —
  no other agent's work was in the tree or the index. Left status
  `in-progress` for review (§9 — only the reviewer sets `resolved`).
  Usage/cost not recorded here — no transcript token accounting available to
  this subagent.
