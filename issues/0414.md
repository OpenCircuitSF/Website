# 0414 — The runbooks document a `From:` address production does not use, and contradict the reference doc

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | docs |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-03 |
| **Closed** | 2026-09-03 |
| **Commit** | `4a1c652` |

> 🟢 Resolved 2026-09-03 — I re-read the address off the box myself rather than
> off the diff: `/etc/opencircuit/config.env` gives
> `EMAIL_FROM="Open Circuit SF <contact@mailing.opencircuitsf.com>"` and
> `EMAIL_REPLY_TO=contact@opencircuitsf.com`, matching every corrected line. All
> three named runbook occurrences, `PRD.md` §9's block, and all three softer
> stragglers are fixed; §14's decision log and the `issues/*.md` records are
> untouched, byte-verified.

## Description

`#0271` moved the public contact address to `contact@`. Production now sends as
`contact@mailing.opencircuitsf.com` with `Reply-To: contact@opencircuitsf.com`,
verified read-only on the box.

**Three runbook lines still document the old local part**, and one commit wrote
both spellings at once:

| Where | Says | Should be |
|---|---|---|
| `docs/email-setup.md` (two places) | `hello@mailing.opencircuitsf.com` | `contact@mailing.opencircuitsf.com` |
| `docs/aws-iam-setup.md` | `hello@mailing.opencircuitsf.com` | same |

Commit `049dd4d` wrote those lines **and** wrote the correct
`contact@mailing.opencircuitsf.com` into `docs/configuration.md` in the same
change. So the runbooks contradict the reference doc, inside one commit.

### The PRD's configuration block is separately stale, and doubly so

`PRD.md` §9's environment block still reads:

	EMAIL_FROM=Open Circuit SF <hello@opencircuitsf.com>
	EMAIL_REPLY_TO=hello@opencircuitsf.com

Wrong local part **and** wrong domain — production's `From:` sits on the
`mailing.` subdomain because that is the verified SES identity. The apex form
cannot send.

The same block reads `AWS_REGION=us-west-2`; production is **`us-east-1`**
(`CLAUDE.md` §7). So two independent facts in one block are wrong.

**This block is maintained, not frozen.** `#0271`'s `## Scope` exempts the PRD's
*decision log* — `PRD.md` §14's Q2 row is correctly left alone — but §9's env
block was corrected in place by `#0072` with a dated note, precisely because a
stale value there is a defect. `#0271`'s reviewer reached the same verdict.

### Three softer stragglers, same cause

- `docs/deployment.md` open item 4 still says the identity is "undecided…
  defaults to `hello@`". Its items 2, 5, 6 and 7 are stale for the same reason —
  it is an unupdated mirror of `CLAUDE.md` §10's table.
- `docs/obstacles.md` says there is "no decision on who reads
  `hello@opencircuitsf.com`". Both halves are settled.
- `scripts/dev.sh` carries a parenthetical claiming production is `hello@`.

## Why this matters now

The user is about to send their first real newsletter. These are the documents
an operator reads while doing that, and the two that matter most — the SES
runbook and the IAM runbook — name an address that **cannot send**, because the
identity verified in SES is the `contact@` one.

## Acceptance criteria

1. `docs/email-setup.md` and `docs/aws-iam-setup.md` name the address production
   actually uses. **Verify it against the box, read-only, rather than copying it
   from this issue** — the whole failure class here is a documented value nobody
   re-measured.
2. `PRD.md` §9's env block matches production for **both** the sending identity
   and the region, following `#0072`'s dated-note convention for that block.
3. The three softer stragglers are corrected or explicitly left with a reason.
4. **Historical records stay intact** — `PRD.md` §14's decision-log row and the
   `issues/*.md` files quoting the old address must not be rewritten. `#0271`'s
   `## Scope` explains why: rewriting them falsifies the history the citation
   guards exist to protect. If you think §14's row should get the
   struck-plus-resolved-date convention some sibling rows use, say so rather
   than doing it silently.
5. **No mechanism** (`#0330`/`#0347`/`#0356`/`#0381`).
6. If any Go comment or a file cited by one changes, run `./internal/handlers/...`;
   editing `CLAUDE.md` means **both** that and `./internal/db/...` (§5's
   repo-wide-guard table). Editing `PRD.md` means `./internal/db/...`. Per §5
   name the specific tests that ran. No Go-file-plus-line-number citation
   (`#0352`), scanned with `python3` bytes rather than BSD `grep -P`.

## Relation

- Found by: [#0271](0271.md)'s review, which took a byte-level census of the old
  address across the tree
- Relates to: [#0271](0271.md) — which changed the address and deliberately
  scoped historical records out
- Relates to: [#0072](0072.md) — the precedent that `PRD.md` §9's env block is
  maintained in place with a dated note
- Relates to: [#0415](0415.md) — the SES status in the same `CLAUDE.md` §10 table

## Verification

**Re-measured, not copied from this issue.** `ssh ec2 "sudo grep -E
'AWS_REGION|EMAIL_FROM|EMAIL_REPLY_TO' /etc/opencircuit/config.env"` returned,
read-only:

```
AWS_REGION=us-east-1
EMAIL_FROM="Open Circuit SF <contact@mailing.opencircuitsf.com>"
EMAIL_REPLY_TO=contact@opencircuitsf.com
```

This matches the issue's premise exactly — no disagreement to report.

**Occurrences corrected** (commit `4a1c652`):
- `docs/email-setup.md` — the `From:` table row and the "sending identity
  question" line under **Open items**, both `hello@mailing.…` →
  `contact@mailing.…`.
- `docs/aws-iam-setup.md` — the `From:` table row.
- `PRD.md` §9's env block — `EMAIL_FROM`/`EMAIL_REPLY_TO` corrected in place
  with an inline dated note, following `#0072`'s convention (kept on the same
  line so the section's line count, and `CLAUDE.md` §11's guarded stated
  count, do not move).
- `docs/deployment.md`'s "Open items" mirror, item 4 — struck the original
  "undecided… defaults to `hello@`" verbatim and added a dated correction,
  since this list reads as a live status tracker (mirroring `CLAUDE.md` §10),
  not a decision log. Items 2, 5, 6, 7 got the same status-note treatment for
  the same reason (item 2 also carries `#0418`'s region fix).
- `docs/obstacles.md` — struck the "no decision on who reads `hello@…`"
  sentence verbatim (plus its now-stale "SES has never been set up" lead,
  found in the same paragraph) and added a dated correction, same convention.
- `scripts/dev.sh` — the comment's "production is `hello@…`" corrected to
  `contact@mailing…`.

**Beyond the issue's named "Where" table, judged in-scope because they are
the same fact, directly falsified, in a file already being edited for this
issue:** `docs/aws-iam-setup.md`'s IAM policy JSON example (two ARNs) and its
quoted `AccessDeniedException` message both embedded the stale region
(`#0418`'s fact, not this issue's) alongside the address fix — left uncorrected
they would have sat next to a corrected line while still being wrong, so both
were corrected too. Recorded under `#0418` below since the fact is region, not
address.

**Left alone, with reasons:**
- `PRD.md` §14 Q2 — the decision-log row. `#0271`'s `## Scope` is explicit that
  this stays, and this issue's criterion 4 agrees. Not struck or annotated;
  §14 is decision-log history, not a live tracker.
- `issues/*.md` files quoting the old address (`#0070`, `#0075`, `#0045`,
  `#0090`, etc.) and synthetic test fixtures (`hello@opencircuitsf.com` in
  `internal/mailing`, `internal/handlers`, `internal/config`, `web/src/lib`
  tests) — all untouched, per `#0271`'s scope: they are either historical
  issue records or deliberately-synthetic, not-production addresses.
- `HANDOFF.md:72` still names `us-west-2` for SES verification — a region
  fact, not an address one, and not named by either issue; reported below
  rather than fixed.

**No `§14` row got the struck-plus-resolved-date treatment**, per this issue's
criterion 4's explicit ask to say so rather than do it silently: it wasn't
touched at all, since `#0271`'s scope already settled that only `CLAUDE.md`
§10 item 4's *status* — not the PRD decision log — gets updated as the
identity became decided.

**Tests run:** `ISSUE=0414 scripts/check.sh go ./internal/db/... ./internal/handlers/...`
— zero skips, both DB-backed packages reported real elapsed time
(`internal/db` `ok 0.458s`, `internal/handlers` `ok 5.365s`), `gofmt -l`
clean, and `scripts/check.sh` dropped `opencircuit_test_0414` on exit
(confirmed absent from `psql -l` afterward). No Go source was touched by this
issue, so this run is `internal/db`'s guard on `PRD.md` (§5's repo-wide-guard
table) plus `internal/handlers`'s citation guard, as directed — not evidence
of a code change, since there isn't one (`#0330`/`#0347`/`#0356`/`#0381`).

## Work log

- 2026-09-03 — Filed by the orchestrator from `#0271`'s review (§9), with the
  contradictory lines and the PRD block confirmed independently before filing.
- 2026-09-03 — implement — claude-sonnet-5 — batched with `#0418` in one
  subagent pass (both correct the same two files). Re-verified production's
  address and region read-only (`ssh ec2`, `config.env`); corrected 6 files;
  committed doc changes only (`4a1c652`); left this markdown uncommitted for
  review, per `issues/Issues.md`'s phase-2 convention.

## Review notes

> 🟢 Approved 2026-09-03 — claude-opus-5.

**Ground truth re-measured, not read off the diff.** Read-only `ssh ec2` against
`/etc/opencircuit/config.env`:

```
EMAIL_FROM="Open Circuit SF <contact@mailing.opencircuitsf.com>"
EMAIL_REPLY_TO=contact@opencircuitsf.com
AWS_REGION=us-east-1
```

All three match what the implementer reported and what the commit wrote. Nothing
was written to the box, DNS, or any AWS resource.

**Criterion by criterion.**

1. *The runbooks name the address production uses.* Met. A byte-level scan with
   `python3` (not BSD `grep -P`, per `CLAUDE.md` §8) counts `hello@` **zero**
   times in `docs/email-setup.md` and `docs/aws-iam-setup.md`. The `From:` rows
   in both, and `email-setup.md`'s "sending identity question" line under **Open
   items**, all read `contact@mailing.opencircuitsf.com`.
2. *`PRD.md` §9's block matches production on both facts, with `#0072`'s dated
   note.* Met, and the split of attribution is right: the two address lines carry
   `(#0414)` and the `AWS_REGION` line carries `(#0418)`. Both notes are
   **same-line**, which is what keeps §9's line count still — see the §11 finding
   below.
3. *The three softer stragglers.* All three corrected, none merely excused:
   `docs/deployment.md`'s open item 4 (original struck verbatim, dated correction
   appended — the convention `Issues.md` requires for live text, and the verbatim
   half is what `#0293` had to reconstruct once), `docs/obstacles.md` §10 (same
   treatment, and it correctly caught the now-stale "SES has never been set up"
   lead in the same paragraph), and `scripts/dev.sh`'s parenthetical.
4. *Historical records intact.* Met, verified rather than assumed. `PRD.md` §14
   Q2 still reads `hello@opencircuitsf.com` verbatim (byte-checked), and
   `git status` shows no `issues/*.md` modified but the two under review. The
   implementer also did what the criterion asked in words — it said explicitly
   that it was *not* applying the struck-plus-resolved-date treatment to §14,
   rather than deciding silently.
5. *No mechanism.* Met — the commit adds no test, no guard, no abstraction.
6. *Guards.* Ran `ISSUE=0414r scripts/check.sh go ./internal/db/...
   ./internal/handlers/...` — `internal/db` `ok 0.398s`, `internal/handlers`
   `ok 6.145s`, **zero skips**, `gofmt -l` clean, scratch database
   `opencircuit_test_0414r` dropped on exit. No Go source changed, so this is the
   `PRD.md`/`CLAUDE.md` doc-parity guard plus the citation guard, exactly as §5's
   repo-wide-guard table requires. No Go-file-plus-line-number citation appears
   anywhere in these notes (`#0352`).

**On `scripts/dev.sh`.** The commit changed one thing the issue did not name: the
`AWS_REGION` default itself, `us-west-2` → `us-east-1`. I judge that correct and
in fact required — the surrounding comment claims the value is "left at the real
SES region", so the comment could not be made true by editing prose alone.
`scripts/dev_guard_test.sh` asserts nothing about `AWS_REGION`, `EMAIL_FROM`, or
this comment (it is entirely `#0117` port-safety), so `scripts/check.sh guards`
is genuinely unaffected and I did not pay its ~48s `:5173` run.

**Reported, not filed** (§9 — subagents do not file):

- `.env.example:34` still ships `AWS_REGION=us-west-2` in a **copyable template**.
  This is the one occurrence with real blast radius left, and it was neither
  fixed nor reported. Note `docs/configuration.md`'s `EMAIL_FROM` row already
  flags the equivalent divergence in prose ("`.env.example` shows the apex form
  for readability; do not copy it…"); `AWS_REGION` has no such flag.
- `scripts/db-reset.sh:240` carries the byte-identical
  `AWS_REGION="${AWS_REGION:-us-west-2}"` default that `dev.sh` just lost, so the
  two dev seeds now disagree. Reported by the implementer; worth closing together
  with the item above.
- `docs/email-setup.md`'s "Production access" section still reads "**Still in the
  sandbox as of 2026-08-25**" while production's `config.env` sets
  `SES_SANDBOX=false`. Those measure different things (an app flag vs. the AWS
  account state, which `CLAUDE.md` §10 item 2 still records as sandboxed), so
  this is not necessarily wrong — but it is `#0415`'s territory and it should
  look at it.
