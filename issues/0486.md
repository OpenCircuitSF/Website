# 0486 — `scripts/db-reset.sh` is advised as routine in four places without saying it destroys the dev database

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | tooling / docs |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-09-09 |
| **Commit** | `b435b38` (first pass), `b36b49a` (second pass — the sixth and seventh sites) |
| **Closed** | 2026-09-09 |

> 🟢 Resolved 2026-09-09 — second review. All seven sites that point a reader
> at `scripts/db-reset.sh` now name the drop before the rebuild, including the
> two the first pass missed: the `Database management` block in
> `issues/Issues.md`, which had left the two authoritative documents
> disagreeing, and `docs/dev.md`, which the issue had wrongly held up as the
> model. A third search found no eighth site, and settled three previously
> unexamined error strings in `internal/testdb` as already correct — they lead
> with "drop and recreate". `#0207`'s refusal and its guard are byte-unchanged
> and still green.

## Description

`scripts/db-reset.sh` **drops and recreates `opencircuit`** — which is not a
scratch database. `CLAUDE.md` §5a says so plainly: it targets the two databases
every agent falls back to sharing, "and `opencircuit` is also the user's dev
database."

It is nonetheless offered as ordinary advice, unqualified, in several places:

- `CLAUDE.md` §5's command block — `# rebuild the local dev DB from migrations + seed admin`
- `docs/obstacles.md`'s **Clear** column, as the remedy for a missing schema
- `docs/configuration.md`'s `DEV_ADMIN_LOGIN` row, as the way to get an admin row
- `cmd/opencircuit`'s own startup error, which names it when `ADMIN_EMAIL` has
  no active row

Each phrasing describes the *outcome* — rebuilt, seeded, cleared — and none
mentions that everything currently in that database is deleted first. An agent
or developer reaching for it to fix a missing admin row is not told they are
about to discard their own local data.

`docs/dev.md` was filed here as the exception that shows the right shape,
because it says the script refuses when another process holds the database.
**That premise was false**, and this issue's own review measured it rather
than accepting it: that refusal is `#0207`'s protection for other agents, not
for the solo developer this issue is about, and the sentence that actually
recommends the script was outcome-only like all the rest. `docs/dev.md` was a
seventh site, and was fixed as one.

## Where this came from

Found by the audit of the abandoned `#0402` branch. That branch's **first
review bounce** was precisely this: `scripts/dev.sh`'s banner advised a
database-destroying command as routine. The fix landed there, in commit
`8f289c3`, and **never reached `main`** — so the finding is real, was reviewed,
and the correction was stranded along with the rest of that branch.

`main` has since acquired more copies of the same unqualified advice, not fewer.

## Why the existing guard is not the answer

`#0207` already made the script refuse without `--force` when another
connection is live, and `scripts/db_reset_guard_test.sh` pins that. **That
protects other agents, not the person running it.** A developer alone on their
own machine — the common case — passes the guard and loses their data with no
warning at all.

## Acceptance criteria

1. **Say what it destroys, at each site that recommends it.** Outcome-only
   phrasing ("rebuild", "clear", "seed") is what makes it read as safe. One
   clause naming the drop is enough.
2. **Include the startup error.** `cmd/opencircuit` names the script when
   `ADMIN_EMAIL` has no active row, and that is the single most likely moment
   someone runs it without thinking. Note it already offers `opencircuit seed`
   as the non-destructive alternative — lead with that one.
3. **Check for sites this issue missed** rather than trusting the list above;
   re-derive it with a search. This lineage has had several false premises
   corrected by measurement.
4. **Do not weaken `#0207`'s refusal** or its guard. This is additive: a warning
   for the solo case, not a change to the multi-agent one.
5. Consider whether the script itself should say what it is about to do before
   doing it. Decide yes or no with a reason — a prompt is not obviously right
   for something agents invoke non-interactively, and `CLAUDE.md` §5a's
   concurrency rules are the constraint.

## Relation

- `#0207` made the script refuse when other connections are live, and added
  `scripts/db_reset_guard_test.sh`.
- `#0402`'s abandoned-branch review found this first; its fix was stranded.
- `CLAUDE.md` §5a is where the "also the user's dev database" fact is recorded.
- `#0485` is the sibling filed from the same audit.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/db/... ./internal/handlers/...`
(both, since `CLAUDE.md` and Go comments may change), plus
`scripts/db_reset_guard_test.sh` directly. Note `scripts/check.sh guards` binds
`:5173`, currently held by orphaned processes.

## Implementation notes

**Criterion 3 — re-derived the sites by search, not by trusting the issue's
list.** `grep -rn "db-reset" --include="*.go" --include="*.md" --include="*.sh"`
over the whole tree, with `issues/` excluded (291 hits, nearly all historical
issue prose), turned up five non-`issues/` sites that recommend running the
script rather than merely mentioning it as a fact:

1. `CLAUDE.md` §5's command block (matches the issue's list).
2. `docs/obstacles.md`'s **Clear** column (matches the issue's list).
3. `docs/configuration.md`'s `DEV_ADMIN_LOGIN` row (matches the issue's list).
4. `cmd/opencircuit`'s startup error in `newDevAdminAutoLogin` (matches the
   issue's list).
5. **`scripts/dev.sh`'s own header comment** — not on the issue's list. Its
   `--postgres/-p` description says "Requires a local database already
   migrated and seeded with an admin row — scripts/db-reset.sh does both,"
   which is exactly the outcome-only phrasing the issue describes, recommending
   the script to satisfy a prerequisite without naming what it destroys first.

Sites checked and left alone: `CLAUDE.md`'s §1 repository-layout table cell
(`db-reset.sh (rebuild a local DB)`) is part of a row where every script gets
a two-to-four-word gloss, not "advice offered as routine" the way the other
sites are — it names the tool, it does not tell a stuck reader to run it to
fix something. `docs/deployment.md`'s handful of mentions are either already
correctly scoped ("Local dev-database reset (never production)") or describe
what was already exercised locally, not advice pointing someone at the
command as a fix. `docs/dev.md` was already the right shape per the issue and
needed no change. `CLAUDE.md` §5a's own `db-reset.sh` bullet (added by
`#0207`) already fully describes the drop-and-recreate behavior and its
guard, so it was left as-is.

**Criterion 1 — the wording added at each site**, one clause per site, no
essay:

- `CLAUDE.md` §5: `scripts/db-reset.sh          # rebuild the local dev DB…`
  → `# drops and rebuilds the local dev DB from migrations + seed admin`.
- `docs/obstacles.md`: `` `scripts/db-reset.sh` (rebuild + seed) `` →
  `` `scripts/db-reset.sh` (drops the database, then rebuilds and seeds it —
  destructive) ``, and named `migrate ... up` as the non-destructive
  alternative in the same cell for contrast.
- `docs/configuration.md`'s `DEV_ADMIN_LOGIN` row: `` (`opencircuit seed` /
  `scripts/db-reset.sh`) `` → `` (`opencircuit seed`, which only creates the
  missing row, or `scripts/db-reset.sh`, which drops and rebuilds the whole
  database) ``.
- `scripts/dev.sh`'s header: added "but drops and rebuilds the target
  database first, so it is not a safe thing to run reflexively if that
  database already holds data you want."

**Criterion 2 — the startup error**, `cmd/opencircuit/main.go`'s
`newDevAdminAutoLogin`, now reads:

    opencircuit: DEV_ADMIN_LOGIN=true but ADMIN_EMAIL %q has no users row —
    run `opencircuit seed` to create just that row, or scripts/db-reset.sh if
    you want to drop and rebuild the whole database first

It leads with `opencircuit seed` (the non-destructive fix for the actual
problem the error describes — a missing row) and names the destructive
alternative second, with what it destroys spelled out rather than left
implicit. Checked against `TestNoAdminFacingStringCitesInternalDocs`: the new
text cites no `CLAUDE.md`/PRD §/`#NNNN`/`docs/*.md`/`issues/NNNN.md` — it
passed the same way the original string did, not by exemption.

**Criterion 4 — `#0207` untouched.** `scripts/db-reset.sh` itself was not
edited; its sha256 is unchanged (`scripts/db_reset_guard_test.sh`'s own
byte-identity check confirms this every run, including this one). No
assertion in `scripts/db_reset_guard_test.sh` was weakened, relaxed, or
removed.

**Criterion 5 — decided: no, the script should not print an announcement of
what it's about to do before doing it, in this pass or in general.**

- A blocking confirmation prompt is ruled out directly by `CLAUDE.md` §5a:
  agents invoke this non-interactively, sometimes as part of a scoped
  `check.sh` flow, and a prompt would hang rather than protect anyone.
- A non-blocking banner (print-then-proceed) doesn't add real protection
  either, and this issue's own framing shows why: the gap it identifies is
  that a developer reaches for the command *without having been told* what it
  does, and by the time the script is running, that decision is already made
  — a line of stdout after the decision point doesn't undo it, and an agent
  invoking the script programmatically doesn't "read" a banner at all. The
  leverage point is upstream, at the documentation and error-message layer,
  where a reader decides *whether* to run it — which is what this issue's
  other four criteria actually fix.
- The one case that *does* warrant an at-the-moment warning — another
  connection is live, i.e. the multi-agent case — already gets exactly that,
  from `#0207`'s refusal message naming the holder. That is the right model:
  say something when there is a decision left to make (`--force` or not),
  not when there is none (a bare invocation with no live connections has
  already resolved to "proceed").
- This pass is documentation-scoped per its own dispatch and was told
  explicitly not to change the script's behavior, which a bare-invocation
  announcement would be, however small. Nothing above is a workaround of that
  constraint — the reasoning stands independent of it.

## Verification

`ISSUE=0486 scripts/check.sh go ./internal/db/... ./internal/handlers/...` —
**VERIFICATION PASSED**: `go build`, `go vet`, `gofmt -l` clean, both packages
`ok` (`internal/db` 0.438s, `internal/handlers` 6.026s), skip audit shows
`(no skips)`, no leftover processes.

`scripts/db_reset_guard_test.sh`, run directly (not via `scripts/check.sh
guards`, which also binds `:5173`): **all guards hold (0 failures)**, every
one of its ~35 `PASS` lines including the mutation proof (Part 3), the
per-agent-name-needs-`--force` check (Part 5), the unquoted-`$DB` static
check (Part 6), and the final byte-identity check confirming the tracked
`scripts/db-reset.sh` was never modified (sha256 identical before/after).
20.5s wall.

`gofmt -l` also run directly against every file this issue touched
(`cmd/opencircuit/main.go`, `internal/handlers`, `internal/db`): clean.

Machine load at verification time: `uptime` showed load averages 1.81 2.50
2.79 — well under the ~8 threshold, so no timing result here needed
discounting.

No `##` heading in `CLAUDE.md` was added, removed, or renumbered — only
prose inside existing §5 was edited.

`scripts/db-reset.sh` itself was never run, with or without `--force`, per
this issue's explicit instruction; its behavior was exercised only through
`scripts/db_reset_guard_test.sh`'s existing sandboxed private-copy technique.

## Files changed

- `CLAUDE.md` — §5's command block now says `db-reset.sh` drops and rebuilds.
- `docs/obstacles.md` — the **Clear** column now names the drop and contrasts
  it with the non-destructive `migrate ... up` alternative.
- `docs/configuration.md` — `DEV_ADMIN_LOGIN` row distinguishes `opencircuit
  seed` (creates the row) from `scripts/db-reset.sh` (drops and rebuilds
  everything).
- `cmd/opencircuit/main.go` — the `DEV_ADMIN_LOGIN` startup error leads with
  `opencircuit seed` and names what `scripts/db-reset.sh` does before
  suggesting it.
- `scripts/dev.sh` — the `--postgres` header comment now says
  `db-reset.sh` drops and rebuilds the target database rather than only
  describing the outcome.

## Review notes

**Bounced.** The code commit `b435b38` stays in place — everything in it is
correct, and none of it needs redoing. What it is missing is criterion 3: the
re-derived site list stopped one short, and one of the four sites it
deliberately left alone was left alone on a premise this issue's own
`## Description` contradicts.

### What the review confirmed

- **`scripts/db-reset.sh` is byte-unchanged.** Its sha256 is
  `1776b5643e782df4fcf7930433467544a048e591f980c7cfd48f613a96137157` both at
  `b435b38^` and in the working tree, and `scripts/db_reset_guard_test.sh` is
  unchanged in the same commit. Run directly rather than through
  `scripts/check.sh guards` — which also binds `:5173`, currently held by
  orphaned processes — the guard reports `all guards hold (0 failures)` in
  19.9s, including its own byte-identity check. Criterion 4 holds.
- **The startup error leads correctly and cites nothing banned.** It offers
  `opencircuit seed` first, as the fix for the problem the error actually
  describes, and names the drop before naming the script. It carries no
  `docs/*.md` path, no `CLAUDE.md` reference, no PRD section, and no issue
  number, so `TestNoAdminFacingStringCitesInternalDocs` passes for the right
  reason rather than by exemption. Its prefix and em-dash-then-remedy shape
  match the two sibling errors `#0402` introduced in the same function.
- **No `##` heading in `CLAUDE.md` moved.** The heading set and its line
  numbers are identical to `b435b38^`.
- **Verification re-run independently.**
  `ISSUE=0486r scripts/check.sh go ./internal/db/... ./internal/handlers/...`
  reports `VERIFICATION PASSED` — `go build`, `go vet` and `gofmt -l` clean,
  `internal/db` `ok` in 0.451s, `internal/handlers` `ok` in 7.231s, skip audit
  `(no skips)`, no leftover processes. `uptime` read load averages
  `2.55 2.53 2.72` before that run and `2.56 2.55 2.72` before the guard run,
  both well under the ~8 threshold in `CLAUDE.md` §5.
- **The wording at all five changed sites reads naturally in place** and names
  the drop rather than gesturing at it. Seven insertions across five files is
  proportionate to one clause per site.

### What the next pass must fix

**1. The "Database management" block in `issues/Issues.md` is a sixth site, and
it is the same sentence that was fixed in `CLAUDE.md` §5.** The re-derivation
excluded all of `issues/` on the grounds that its hits are historical issue
prose. That is right for the `NNNN.md` files and wrong for `Issues.md`, which
`CLAUDE.md`'s authoritative-sources table names as the issue-workflow
authority and which every subagent loads. Its block still reads
`scripts/db-reset.sh             # rebuild the local dev DB from migrations +
seed admin` — verbatim the pre-fix `CLAUDE.md` line — so the two authoritative
documents now disagree about what the script does.

Remedy, verbatim: in `issues/Issues.md`, in the fenced block under
`Database management:`, replace

    scripts/db-reset.sh             # rebuild the local dev DB from migrations + seed admin

with

    scripts/db-reset.sh             # drops and rebuilds the local dev DB from migrations + seed admin

**2. `docs/dev.md` is not already the right shape.** This issue's own
`## Description` calls it the exception, and the implementation notes accepted
that rather than measuring it — exactly the false-premise pattern criterion 3
exists to catch. What `docs/dev.md` actually says is that the prerequisite is a
database that exists, is migrated, and has an admin row, and that "One command
does all three: `scripts/db-reset.sh`". That is outcome-only phrasing offered
as the primary instruction. The only qualification that follows is the
refusal-when-another-process-holds-it, which is `#0207`'s protection — and this
issue's `## Why the existing guard is not the answer` section states plainly
that that protects other agents, not the solo developer. So the one site the
issue held up as the model is the site whose reader is most likely to be the
person this issue is about.

Remedy, verbatim: in `docs/dev.md`, in the "Running against Postgres"
prerequisite paragraph, replace

    three:

with

    three, though it drops and rebuilds the database before doing so:

so the sentence reads "One command does all three, though it drops and rebuilds
the database before doing so:".

### Judgement on the four sites deliberately left alone

- **`CLAUDE.md` §1's repository-layout table cell — call upheld, narrowly.**
  The row glosses nine scripts in two-to-four words each; it identifies what
  lives in `scripts/`, it does not tell a stuck reader to run anything, so it
  is not a site that recommends the script. Worth recording that the cheapest
  possible improvement was available here — `(rebuild a local DB)` →
  `(drop and rebuild a local DB)` is two words and strictly better, since
  `rebuild` is one of the three outcome-only verbs criterion 1 names. Not
  required, and not a reason to hold the issue open on its own; fold it in if
  the next pass is in the file anyway.
- **`docs/deployment.md` — call upheld, and measured rather than accepted.**
  Its five mentions are a file inventory, two historical what-was-exercised
  notes, a row reading "Local dev-database reset (never production)", and one
  that already spells the behaviour out as "(drop, recreate, migrate, seed)".
  None is advice pointing a reader at the command as a fix, and the last one
  already satisfies criterion 1 without being touched.
- **`CLAUDE.md` §5a's own bullet — call upheld.** It already says the script
  drops and recreates the database and that `opencircuit` is the user's dev
  database. Nothing to add.
- **`docs/dev.md` — call overturned**, per item 2 above.

### Criterion 5 — the reasoned "no" is accepted, with one qualification

The argument holds on its two strongest legs. A blocking prompt is genuinely
ruled out by `CLAUDE.md` §5a's non-interactive invocation, and the case that
does warrant an at-the-moment warning — a live connection, meaning a decision
about `--force` is still open — already gets one from `#0207`.

The middle leg is weaker than it is written. "An agent would not read a banner"
is true and does not generalise: the developer running the command
interactively is precisely the person this issue was filed about, and they
would read it. The reason to still decline is narrower than "a banner does not
help" — it is that a banner after the decision point buys less than the same
sentence placed where the decision is made, and the six documentation sites are
where it is made. Recorded as a decision not to change the script, with the
reasoning corrected rather than the verdict.

### Not escalated

This is the first bounce, and the remedy above is two verbatim replacements
with no design latitude, so the next pass stays on Sonnet per `CLAUDE.md` §3.

## Second implementation pass

Applied both verbatim remedies from `## Review notes` — transcription, no
design latitude exercised.

1. **`issues/Issues.md`'s Database management block.** Replaced
   `scripts/db-reset.sh             # rebuild the local dev DB from
   migrations + seed admin` with `scripts/db-reset.sh             # drops
   and rebuilds the local dev DB from migrations + seed admin`, matching the
   already-fixed `CLAUDE.md` §5 wording so the two authoritative documents
   agree again.
2. **`docs/dev.md`'s "Running against Postgres" prerequisite paragraph.**
   Replaced the sentence-ending `three:` with `three, though it drops and
   rebuilds the database before doing so:`, so the site the first pass wrongly
   held up as an exemplar now names the drop itself.

Also took the optional, cost-free tweak the review flagged: `CLAUDE.md` §1's
repository-layout cell now reads `` `db-reset.sh` (drop and rebuild a local
DB) `` instead of `(rebuild a local DB)` — two words, no `##` heading touched.

Left alone, per the review's explicit instruction: `scripts/db-reset.sh`
itself (unedited — sha256 `1776b5643e782df4fcf7930433467544a048e591f980c7c
fd48f613a96137157`, identical to the value recorded in the first pass's
review), `scripts/db_reset_guard_test.sh` and its assertions, `#0207`'s
refusal behavior, and the criterion-5 "no announcement" decision (reasoning
already corrected by the review, verdict not reopened).

### Verification

`ISSUE=0486b scripts/check.sh go ./internal/db/... ./internal/handlers/...`
— **VERIFICATION PASSED**: `go build`, `go vet`, `gofmt -l` clean, both
packages `ok` (`internal/db` 0.626s, `internal/handlers` 5.712s), skip audit
`(no skips)`, no leftover processes.

`scripts/db_reset_guard_test.sh`, run directly (not via `scripts/check.sh
guards`, which also binds `:5173`, held by orphaned pids unrelated to this
issue): `all guards hold (0 failures)`, including the final byte-identity
check confirming `scripts/db-reset.sh` was never modified.

`uptime` before the run: load averages `3.06 3.47 3.14` — well under the
~8 threshold in `CLAUDE.md` §5.

No `##` heading in `CLAUDE.md` was added, removed, or renumbered —
confirmed via `git diff CLAUDE.md | grep '^[+-]##'` returning nothing.

Commit `b36b49a`: `CLAUDE.md`, `docs/dev.md`, `issues/Issues.md` — 3 files
changed, 3 insertions(+), 3 deletions(-), one line per file.

Leaving this issue `open` for the reviewer, per `CLAUDE.md` §9 — only the
reviewer sets `resolved`.

## Review notes — second review

**Approved.** Both verbatim remedies from the first bounce were applied
exactly as written, `b36b49a` is three one-line changes across three files,
and nothing in `b435b38` needed redoing. One thing the bounce asked for was
*not* done as asked, and this pass did it rather than bouncing again — see
"The Description correction" below.

### The two remedies, confirmed in place

- **The sixth site.** The fenced block under `Database management:` in
  `issues/Issues.md` now reads `scripts/db-reset.sh             # drops and
  rebuilds the local dev DB from migrations + seed admin`. Compared against
  the corrected line in `CLAUDE.md` §5 by splitting each at its `#` and
  comparing the remainder, the two comments are byte-identical:
  `drops and rebuilds the local dev DB from migrations + seed admin`. The
  divergence between the two authoritative documents, which was the whole
  point of the bounce, is closed.
- **The seventh site.** The prerequisite paragraph in `docs/dev.md` now
  reads "One command does all three, though it drops and rebuilds the
  database before doing so:" immediately above the fenced
  `scripts/db-reset.sh`. It reads naturally, and it names the destruction at
  the point of the primary instruction rather than leaving it to the
  refusal-when-held sentence that follows — which is `#0207`'s protection for
  other agents, not for the solo reader this issue is about.
- **The `CLAUDE.md` §1 tweak was taken.** The repository-layout cell now
  reads `db-reset.sh` (drop and rebuild a local DB), replacing the
  outcome-only `rebuild`. Two words, and it removes the last instance of one
  of the three verbs criterion 1 names.

### The Description correction — appending did not suffice

The first bounce established by measurement that `docs/dev.md` was a seventh
site rather than the model. The implementer recorded that finding in a new
`## Second implementation pass` section but left the `## Description`
sentence claiming `docs/dev.md` "is the exception and shows the right shape"
standing unaltered.

That is not enough, and the reason is specific rather than stylistic. **The
sentence was not merely inaccurate; it was load-bearing, and it already
caused the defect it describes.** The first implementation pass read it,
accepted it instead of measuring, and skipped the site — which is exactly
what criterion 3 was written to prevent. A correction that lives 300 lines
below the claim leaves the next reader meeting the claim first, in the
section that reads as the issue's statement of fact, with nothing marking it
false at the point of reading. This project has spent repeated passes on
defects of precisely that shape.

The precedent in this repository is to correct such a premise **in place and
carry the correction note**, not to append a rebuttal elsewhere. `#0064`'s
inverted vhost criterion was corrected in the issue itself. `#0293` corrected
the stale greenfield language where it stood rather than annotating around
it.

So this review made the edit rather than spending a second bounce on two
lines of markdown in the file it was already writing to. The replacement
sentence is recorded here verbatim so the change is auditable. Was:

    `docs/dev.md` is the exception and shows the right shape: it says the script
    refuses when another process holds the database.

Now:

    `docs/dev.md` was filed here as the exception that shows the right shape,
    because it says the script refuses when another process holds the database.
    **That premise was false**, and this issue's own review measured it rather
    than accepting it: that refusal is `#0207`'s protection for other agents, not
    for the solo developer this issue is about, and the sentence that actually
    recommends the script was outcome-only like all the rest. `docs/dev.md` was a
    seventh site, and was fixed as one.

The claim is preserved rather than deleted, so a later reader can still see
what was believed at filing time and why it was wrong. The bulleted list of
four sites above it needed no similar treatment: criterion 3 already frames
that list as provisional and instructs the implementer not to trust it.

**The title still says "four places" and was deliberately left.** A title
names the defect, which is unchanged in kind, and the count was provisional
by the issue's own construction. Rewriting a `#` heading mid-flight would
also break any cross-reference quoting it, for no gain the corrected
Description does not already deliver.

### Criterion 3 — searched a third time, and one candidate neither pass saw

Re-derived independently rather than trusting either prior list, widening the
include set beyond the earlier `*.go`/`*.md`/`*.sh` to cover `*.ts`, `*.json`,
`*.yml`, `*.sql`, and config files. Everything outside `issues/NNNN.md` was
enumerated and classified.

**No eighth site requiring a change** — but the search was not redundant,
because it turned up three occurrences that neither the first pass nor the
first review examined at all:
`internal/testdb`'s `classifyVersion` and its phantom-DDL census both end
their error strings with "Remedy: drop and recreate — `scripts/testdb.sh
create <id>`, or `scripts/db-reset.sh --force %s` for a shared database".

Those are genuine recommending sites — error messages naming the script at a
moment of frustration, the same shape as criterion 2's startup error — and
they were never assessed. **Call: no change needed, and for the right
reason.** The remedy clause is led by the words "drop and recreate", so the
destruction is named before the script is, which is precisely what criterion
1 asks for; the same call the first review made about the already-correct
"(drop, recreate, migrate, seed)" phrasing in `docs/deployment.md`. The
`--force` these strings tell the reader to pass is itself the deliberate
override, so nothing here reads as routine.

Everything else classified and left: the `db-reset.sh` mentions in
`internal/middleware` describe the script as a *cause* of a vanished row, not
as advice; two in `scripts/testdb.sh` and two more in `scripts/dev.sh` are
historical cross-references to flag-parsing lessons and a shared default DSN;
the doc comment on `newDevAdminAutoLogin` in `cmd/opencircuit` describes what
the startup error says and remains true of the corrected string; the five in
`docs/deployment.md` were measured by the first review and re-confirmed; the
bullet in `CLAUDE.md` §5a already spells the behaviour out in full. The two
entries in `.claude/settings.local.json` are permission-allowlist patterns,
not prose.

### Criterion 4 — `#0207` untouched, proved rather than asserted

`git diff --name-only b435b38^ HEAD` restricted to `scripts/db-reset.sh` and
`scripts/db_reset_guard_test.sh` returns nothing, so neither file appears in
either commit. The sha256 of `scripts/db-reset.sh` is
`1776b5643e782df4fcf7930433467544a048e591f980c7cfd48f613a96137157` in the
working tree, identical to its value at `b435b38^`. Run directly rather than
through `scripts/check.sh guards` — which also runs the dev guard and binds
`:5173`, held by orphaned processes belonging to no one in this pipeline —
`scripts/db_reset_guard_test.sh` reports `all guards hold (0 failures)`,
including its own closing byte-identity check. The script was never executed
by this review, with or without `--force`.

### No `##` heading moved

The numbered listing of every `^## ` line in `CLAUDE.md` is identical, in
both text and line number, to the same listing at `b435b38^`. The §1 edit
changed a table cell and the §5 edit changed a comment inside a fenced block,
neither of which is a heading.

### Verification

`ISSUE=0486s scripts/check.sh go ./internal/db/... ./internal/handlers/...`,
run twice per this file's phase 3 step 5 — once with Status at `in-progress`
so the citation guards can see these notes, and again after the flip to
`resolved` so `TestResolvedOrClosedIssueCarriesClosedRow` sees the
`**Closed**` row. Both runs report VERIFICATION PASSED with `go build`,
`go vet` and `gofmt -l` clean, both packages `ok` on real elapsed time, and
the skip audit reporting no skips. `uptime` was read before each run and
before the guard run; every reading was far below the ~8 threshold in
`CLAUDE.md` §5, so no timing result here needed discounting.

`scripts/go_file_visit_floor_guard_test.sh` was not run: it fails on `HEAD`
for reasons filed as `#0489`, unrelated to this issue.

### Not escalated, and no second bounce

Recorded per `CLAUDE.md` §3, which asks for the decision either way. There is
no second bounce: the two remedies were transcribed correctly, and the one
outstanding item was a single prose sentence in the issue file this review
already owns. Had it been bounced it would have been transcription rather
than design, so it would not have triggered escalation regardless.

### Criterion 5 — verdict undisturbed

The reasoned "no announcement in the script" stands as the first review left
it, with the middle leg of its argument already corrected there. Nothing in
this pass reopened it.
