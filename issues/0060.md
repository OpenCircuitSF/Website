# 0060 — Handle GDPR erasure requests

| | |
|---|---|
| **Status** | resolved |
| **Module** | subscribers / admin |
| **Platform** | All |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-18 |
| **Commit** | `e718b73`, `bd4b422` |
| **Closed** | 2026-08-23 |

## Description

A hard delete that removes a subscriber's personal data while keeping the address suppressed so it cannot silently return.

## Acceptance criteria

- [x] `DELETE /admin/subscribers/{id}` hard-deletes the subscriber and cascades `subscriber_interests`
- [x] A `suppressions` row with reason `manual` is left in place so the address is not re-added
- [x] `email_sends` rows are anonymized rather than deleted, preserving campaign counts
- [x] `email_events` retains the raw payload for deliverability forensics but the linkage is severed
- [x] The action requires a typed confirmation and is audited with the requester's identity
- [x] A data-export-on-request path exists (reuse #0059 filtered to one subscriber)
- [x] Privacy policy documents what is retained after erasure and why
- [x] Tests confirm the subscriber is gone, the suppression remains, and counts are intact

## Notes

PRD §11. The tension here is real: erasure must be genuine, but keeping a suppression entry is what prevents the address being re-added by a later import or signup — which is itself a privacy protection. Documenting that in the privacy policy is what makes it defensible.

Anonymizing rather than deleting `email_sends` preserves the historical campaign metrics that would otherwise silently change.

## Implementation notes

**Erasure semantics — hard delete, not anonymize-in-place.** `subscribers.Store.Erase`
(`internal/subscribers/erase.go`) runs one transaction: `SELECT ... FOR UPDATE`
locks the subscriber, refuses (`ErrHasPendingSends`, 409) if any `email_sends`
row for it is `queued`/`sending`, anonymizes that subscriber's `email_sends`
rows in place, adds an idempotent `manual`-reason `suppressions` row for the
address, then `DELETE FROM subscribers`. The row and its `subscriber_interests`
rows are genuinely gone — this is not a soft delete or a status flag.

**Resolving the `complained`-never-auto-resubscribes tension (CLAUDE.md §9).**
The two are aligned, not in conflict, because `suppressions` already has no
foreign key to `subscribers` (`suppressions.go`'s package doc comment
anticipated exactly this issue) — a suppression outlives the row it was
written for. Erase always adds a `manual` row, on top of whatever the address
already carries (a `complaint` row from #0038's SES ingestion survives
untouched). A previously-complained address that gets erased ends up with
**two** independent suppression reasons, never fewer. Proved by execution:
`TestErase_PreservesAndAddsToExistingSuppressions` seeds a `complaint` row,
erases, and asserts both rows survive and `IsSuppressed` is true;
`TestAdminSubscribers_Erase_ThenAddressStaysBlockedFromResubscribing` goes
further and drives the real public `POST /api/subscribe` handler (with a real
`SuppressionStore`, not the `NoSuppressions` stub) against the erased address
afterward — 202 (uniform), no new `subscribers` row.

**What survives per table:**
| Table | After erasure |
|---|---|
| `subscribers` | row deleted |
| `subscriber_interests` | rows deleted (CASCADE via `subscribers(id)`, migration 000010 — unchanged) |
| `suppressions` | a `manual` row added (idempotent); any pre-existing rows (any reason) untouched |
| `email_sends` | rows survive, `email` overwritten with a per-row `erased-<send-id>@erased.invalid` placeholder, `subscriber_id` set NULL |
| `email_events` | untouched — no FK to `subscribers` ever existed; forensics rely on the raw payload/`recipient`, and the join chain back to a name is severed once `email_sends.email` is anonymized |
| `audit_log` | a new `subscriber.erased` row; existing rows for this subscriber are never touched (append-only) |

**Migration change (greenfield, edited in place per CLAUDE.md §1).**
`migrations/000017_create_campaigns.up.sql`'s `email_sends.subscriber_id` FK
changed from `NOT NULL ... ON DELETE CASCADE` to nullable `... ON DELETE SET
NULL`. The old CASCADE would have destroyed exactly the rows this issue's
acceptance criteria require preserving. `docs/database.md`'s row for
`000017` was updated to match; `PRD.md` §6.2's schema block was left
as-is (the one-directional migrations→PRD parity guard,
`internal/db/prd_index_parity_test.go`, only checks column names/indexes,
never nullability or `ON DELETE` behavior, and still passes).

**Why a pending/in-flight send blocks erasure.** `internal/mailing.AudienceStore.RecheckEligibleTx`
(`audience.go`) INNER JOINs `email_sends` to `subscribers` on `subscriber_id`
immediately before a send. A row whose `subscriber_id` has gone NULL (or
whose `subscribers` row is gone) matches neither side of that join, so it
would never reappear in either its `recipients` or `skipped` return — and
`MarkSkippedTx` is only ever called on rows that function returns. Erasing a
subscriber with a queued/sending row would leave it stuck at `queued`
forever with no code path to clear it. `Erase` refuses instead
(`ErrHasPendingSends`, mapped to 409) rather than reaching into
`internal/mailing` to fix that join — reported below as a follow-up, not
built here (out of this issue's `internal/subscribers`/`internal/handlers`
scope).

**Typed confirmation.** `DELETE /admin/subscribers/{id}` requires a JSON body
`{"confirm_email": "<address>"}` matching (case/whitespace-insensitively)
the target subscriber's own address — the "type the resource's name"
pattern, mirroring #0047's `confirm_count` reasoning for campaign sends. The
handler loads the subscriber and compares BEFORE calling `Erase`, so a
mismatched confirmation mutates nothing.

**Audit.** New `audit.ActionSubscriberErased` (`subscriber.erased`).
`TargetID` is the erased subscriber's former id — `audit_log.target_id`
carries no FK to `subscribers`, so the row is never orphaned by the delete
it records. Metadata: `email`, `previous_status`, `interests_removed`,
`email_sends_anonymized`, `suppression_reason` (always `"manual"`),
`suppression_preexisted`. Deliberately omits `signup_ip`/`signup_user_agent`/
UTM fields/tokens — the audit row proves the action happened and lets an
operator reconstruct its scope, not become a second plaintext copy of what
was just erased.

**Data-export-on-request.** Reuses #0059 as specified: `GET
/admin/subscribers/export?q=<email>` — `StreamExport`'s `Query` filter is
already a case-insensitive substring match against `email`, so the exact
address returns exactly one row. No new export code was written; the
`Erase` handler's doc comment documents this reuse explicitly.

**Privacy policy.** Already documents this design precisely
(`web/src/views/PrivacyPolicy.svelte`'s "Erasure" paragraph, written ahead of
this issue landing — its own comments cite `#0060` by number): the
permanent suppression entry, anonymized send-history rows, and raw
deliverability events "kept without the link back to your identity." No
change needed.

**Admin UI.** Not built in this pass — matches #0059's own sibling
precedent (that issue also shipped API-only, its Gotchas explicitly
deferring a UI affordance to "whichever issue/agent owns that screen
next"). The acceptance criteria are phrased at the API level
(`DELETE /admin/subscribers/{id}`); reported as a follow-up below.

**Files changed:** `migrations/000017_create_campaigns.up.sql`,
`docs/database.md`, `internal/subscribers/erase.go` (new),
`internal/subscribers/erase_test.go` (new), `internal/audit/actions.go`,
`internal/handlers/admin_subscribers.go`, `internal/handlers/admin_subscribers_test.go`
(mux/case-table wiring only), `internal/handlers/admin_subscribers_erase_test.go`
(new), `cmd/opencircuit/main.go`.

**Worth filing (reported, not filed — CLAUDE.md §9):**
1. `internal/mailing.AudienceStore.RecheckEligibleTx`'s `INNER JOIN subscribers`
   means a send row whose subscriber has been erased (or, in principle, any
   row whose `subscriber_id` is ever NULL) is silently invisible to the
   recheck rather than explicitly skipped — this issue's `Erase` works
   around it by refusing to erase while a send is pending, but the
   underlying join is still a latent trap for any future code path that
   might leave `email_sends.subscriber_id` NULL on a `queued` row.
2. No admin-console UI (button + confirmation modal) exists yet for either
   `DELETE /admin/subscribers/{id}` or #0059's export — both are reachable
   only by direct API call today.

## Bounce fix (2026-08-23, claude-sonnet-5)

Addressed the review's single defect: the privacy policy's closed "three
things" retention list was factually wrong — a real subscribe → confirm →
erase journey leaves the address (and the signup IP) in `audit_log` too, and
the page never disclosed it. Per the reviewer's instructions, the code
(`e718b73`), the migration's schema, the handler, and the tests were **not**
touched.

**`web/src/views/PrivacyPolicy.svelte`.** Added a fourth `StatusList` item to
the "Erasure" paragraph under "How to leave", changed "three things" to "four
things", and updated the header comment (was lines 38–42) to name the fourth
item and warn future editors not to re-close the count. New item text:

> an internal admin audit log entry recording earlier actions on your account
> (signup, confirmation, and the erasure itself) — this includes your email
> address and your signup IP address, kept so we can prove a request was
> honored and to investigate abuse; it is not exposed publicly and is not
> used to re-add or re-contact you

**`PRD.md` §6.2 (line 541 in the reviewer's numbering; 542 in the current
file).** `subscriber_id BIGINT NOT NULL REFERENCES subscribers(id) ON DELETE
CASCADE` still contradicted the shipped schema (nullable, `ON DELETE SET
NULL`, changed by this issue's own implement pass under CLAUDE.md §1's
greenfield exception). Corrected the column definition and added two short
comments (mirroring the migration's own rationale) explaining the
nullable/SET NULL choice and the UNIQUE-key NULL carve-out. This grew §6.2
from 326 to 335 lines and §6 (its parent, a bold row) from 933 to 942, and
grew `PRD.md` overall from 1,798 to 1,807 lines — all three rows in
`CLAUDE.md` §11's index were updated to match, and
`TestPRDSectionIndexParity`/`TestPRDSectionIndexTotalMatchesFile`
(`internal/db/prd_section_index_test.go`) both pass. (Both had already been
passing at 326/933/1,798 before this pass touched them — i.e. some earlier,
unrelated commit had already updated §11 to values ahead of what
CLAUDE.md's snapshot at the top of this session's prompt showed; this pass's
diff is the widening from those already-current numbers, not from the
stale-looking 296/1798 figures the task description cited.)

**`migrations/000017_create_campaigns.up.sql`.** Added a note to the
`email_sends` header comment: `UNIQUE (campaign_id, subscriber_id)` no longer
constrains anonymized rows once `subscriber_id` is NULL (Postgres treats
NULLs as distinct), and explained why that's harmless (an erased subscriber
can never be re-materialized). Comment-only change — no `.down.sql` change
needed, no template rebuild needed (schema unchanged).

**Execution proof the new policy text is true.** A throwaway probe test
(`internal/handlers/zz0060fix_probe_test.go`, deleted before commit, run
against a private scratch database `opencircuit_test_0060fixb`) drove the
real journey: `POST /api/subscribe` (from IP `203.0.113.77`) → real
`POST /api/subscribe/confirm` → real `DELETE /admin/subscribers/{id}`. After
erasure:
- `subscribers` row: gone (by id and by email).
- `audit_log` (filtered by `target_type='subscriber' AND target_id=<the
  erased id>`, not by a metadata text match, because `subscriber.signup`'s
  metadata has no email — only its `ip_address` column does): three rows —
  `subscriber.signup` (`ip_address=203.0.113.77/32`, confirming the policy's
  "signup IP" claim), `subscriber.confirmed` (`metadata={"email":…}`),
  `subscriber.erased` (`metadata={"email":…,"previous_status":"active",…}`).
- A full grep of every `text`/`varchar`/`jsonb` column in the `public` schema
  for the address found it recoverable in exactly two places:
  `suppressions.email` (policy item 1) and `audit_log.metadata` (policy item
  4). Nothing outside the four now-named categories. (`email_sends` and
  `email_events` were not exercised by this simple journey — no campaign
  send occurred — but their anonymization/retention was already proved by
  the implement pass's own `internal/subscribers` tests and is unchanged by
  this fix.)

Probe file deleted, scratch database `opencircuit_test_0060fixb` dropped and
confirmed absent, no leftover `go test`/`.test` processes.

**Worth filing (reported, not filed — CLAUDE.md §9):** nothing new. The two
items the review already reported (`RecheckEligibleTx`'s INNER JOIN; no
admin-console UI) are unchanged and still open.

## Verification

`uptime` before starting: load average ~1.7–2.0 (idle, well under the ~8
threshold). Own database throughout (`ISSUE=0060 scripts/check.sh`, which
creates and drops `opencircuit_test_0060`; the test-template database was
rebuilt via `scripts/testdb.sh template` after editing migration 000017, and
a second scratch database `opencircuit_test_0060g` was used for the
guard-removal mutation proof, both dropped afterward). Every seeded row uses
the existing `zz-test-`/`zz-subtest-`/`zz-erase-test-` prefixes already
established in this package's test suites; no literal or seeded id was ever
targeted (CLAUDE.md §8b) — the not-found tests re-target a real id this
test itself created and erased.

- `ISSUE=0060 scripts/check.sh go ./internal/subscribers/...` — **PASSED**,
  `internal/subscribers` `ok` at real elapsed time (0.24–0.42s across runs),
  zero skips. Covers `TestErase_HardDeletesAndCascadesInterests`,
  `TestErase_AnonymizesEmailSendsPreservingCountAndStatus`,
  `TestErase_RefusesWhilePendingSend`, `TestErase_NotFound`,
  `TestErase_PreservesAndAddsToExistingSuppressions`,
  `TestErase_RepeatManualSuppressionIsIdempotent`.
- `ISSUE=0060x scripts/check.sh go ./internal/handlers/... -run 'TestAdminSubscribers_Erase\|TestAdminSubscribers_NonAdminForbidden' -v`
  — all 6 tests pass: `TestAdminSubscribers_Erase_RequiresMatchingConfirmEmail`,
  `TestAdminSubscribers_Erase_Success`, `TestAdminSubscribers_Erase_NotFound`,
  `TestAdminSubscribers_Erase_ConflictsOnPendingSend`,
  `TestAdminSubscribers_Erase_ThenAddressStaysBlockedFromResubscribing`
  (drives the real public subscribe handler against a real `SuppressionStore`
  — the end-to-end proof that the address stays blocked), and
  `TestAdminSubscribers_NonAdminForbidden` (extended with the new DELETE
  route).
- `ISSUE=0060 scripts/check.sh go ./internal/... ./cmd/...` — **VERIFICATION
  PASSED**, zero skips, `gofmt` clean, every DB-backed package `ok` with real
  elapsed times (`internal/handlers 2.938s`, `internal/subscribers 0.417s`,
  `internal/db 0.210s` — confirms the migration/docs edits didn't break the
  PRD/docs parity guards, `cmd/opencircuit 1.382s`, …), no `[no test files]`.
- **Admin-guard mutation proof**: backed up `internal/handlers/admin_subscribers_test.go`
  (`shasum -a 256`), changed the test mux's `DELETE /admin/subscribers/{id}`
  registration from `requireAdmin` to `requireSession` only, ran
  `ISSUE=0060g scripts/check.sh go ./internal/handlers/... -run TestAdminSubscribers_NonAdminForbidden -v`:
  **FAILED** as required — `DELETE /admin/subscribers/1 with non-admin
  session: status = 400, want 403` (a non-admin session reached the handler
  and only then failed on the empty JSON body, proving `requireAdmin`'s
  removal is exactly what let it through). Restored the file from the
  backup and confirmed byte-identical via `shasum -a 256` before re-running
  the suite green (CLAUDE.md §8a).
- **Execution proof of what remains/doesn't**: `TestErase_HardDeletesAndCascadesInterests`
  and `TestErase_AnonymizesEmailSendsPreservingCountAndStatus` seed a real
  subscriber with interests and two campaigns' worth of `email_sends` rows,
  erase, and assert directly against the database: the subscriber row is
  gone (`GetByID` → `ErrNotFound`), `subscriber_interests` count is 0, a
  `manual` suppression row exists, and both `email_sends` rows survive with
  their original `status` unchanged but `email` overwritten and
  `subscriber_id` NULL — row counts unchanged (1/1), not 0.
- **Property proof**: `TestAdminSubscribers_Erase_ThenAddressStaysBlockedFromResubscribing`
  erases via the real HTTP handler, then submits the same address to the
  real `POST /api/subscribe` handler wired to a real `SuppressionStore` over
  the same database (not the `NoSuppressions` stub other tests in this
  package use) — 202 (uniform, per CLAUDE.md §9), and no new `subscribers`
  row is ever created for the address; `IsSuppressed` is confirmed true
  before and after.
- Databases dropped (`opencircuit_test_0060`, `opencircuit_test_0060x`,
  `opencircuit_test_0060g` all confirmed absent via `psql ... pg_database`
  query); no leftover `go test`/`.test` processes (`pgrep -fl` empty).
- Did not run `scripts/check.sh web` — no `web/` files were touched (the
  privacy policy already covered this design; nothing to change).

### Bounce-fix verification (2026-08-23)

`uptime` before starting: load average ~1.9–2.5 (idle, well under the ~8
threshold).

- `ISSUE=0060fix scripts/check.sh go ./internal/... ./cmd/...` —
  **VERIFICATION PASSED**, zero skips, `gofmt` clean, every DB-backed
  package `ok` with real elapsed times (`internal/handlers 3.095s`,
  `internal/subscribers 0.427s`, `internal/db 0.178s`, `internal/mailing
  0.879s`, `internal/auth 1.254s`, …), no `[no test files]`. Own database
  (`opencircuit_test_0060fix`), created and dropped by `check.sh`.
- `scripts/check.sh web` — **VERIFICATION PASSED**: `npm run check` — 265
  files, 0 errors, 0 warnings; `npm test` — 27 test files, 3760 tests
  passed, real elapsed (~1.19s reported duration).
- `go test ./internal/db/... -run TestPRDSectionIndex -v` — both
  `TestPRDSectionIndexParity` and `TestPRDSectionIndexTotalMatchesFile`
  **PASS** after the `PRD.md`/`CLAUDE.md` §11 edits (see Bounce fix notes
  above for the exact line-count changes).
- Execution proof against a private scratch database
  (`opencircuit_test_0060fixb`, created via `scripts/testdb.sh create
  0060fixb`, dropped afterward and confirmed absent) — see "Bounce fix"
  section above for the full census. Throwaway probe test file deleted
  before commit; no leftover `go test`/`.test` processes (`pgrep -fl`
  empty).

## Review notes

**Phase 3 review, 2026-08-23 (Opus). Bounced.** Re-verified `e718b73`
independently. `uptime` at start: load average **2.16** (2.95 / 2.67) — idle,
well under the ~8 threshold, so nothing below is a load artifact. All probing
ran against my own databases (`opencircuit_test_0060rev` via `ISSUE=0060rev
scripts/check.sh`; a second scratch database `opencircuit_test_0060revb` via
`scripts/testdb.sh create 0060revb` for the guard-removal mutations and the
behavioural probes; a third, `opencircuit_test_0060rev_mig`, created from
scratch for the migration round-trip). All three dropped. The probes lived in a
throwaway `internal/handlers/zz0060rev_probe_test.go`, deleted before this
commit; every seeded row used a `zz-0060rev-` prefix and no literal or seeded
id was ever targeted (CLAUDE.md §8b).

`ISSUE=0060rev scripts/check.sh go ./internal/... ./cmd/...` — **VERIFICATION
PASSED**, zero skips, `gofmt` clean, every DB-backed package `ok` with real
elapsed times (`internal/handlers 3.278s`, `internal/interests 3.117s`,
`cmd/opencircuit 2.099s`, `internal/mailing 0.966s`, `internal/middleware
0.925s`, `internal/auth 0.858s`, `internal/subscribers 0.521s`, `internal/audit
0.373s`, `internal/db 0.265s`), no `[no test files]`.

**Almost all of this is right, and the two properties that matter most hold up
under execution.** One acceptance criterion does not, and it is a criterion the
implementer resolved by assertion rather than by checking what the code
actually leaves behind. The fix is small and in scope — hence a bounce rather
than a resolve-plus-follow-up. **Keep the code commit; only the privacy policy
needs to change.**

### The migration edit — clean, and the greenfield exception genuinely still applies

**Greenfield confirmed, explicitly.** CLAUDE.md §1's exception expires at the
first deploy that applies a migration to production. It has not: CLAUDE.md §7
records the placeholder as what is "Currently served"; `HANDOFF.md` §5 says the
live host serves the static placeholder and that nobody has even written down
whether the box's PostgreSQL is the target; `#0064` (the deploy issue) is still
open; PRD.md:720's greenfield note is unamended. No migration has ever been
applied to a database anyone cares about, so editing `000017` in place is
legitimate here.

**Round-trip, on a database created from scratch** (`CREATE DATABASE` → `migrate
up` → `migrate down -all` → `migrate up`):

| Step | Result |
|---|---|
| `up` #1 | clean through `20/u create_workshops`; `email_sends.subscriber_id` `is_nullable = YES`, `confdeltype = 'n'` (SET NULL) |
| `down -all` | clean through `1/d create_users`; one table left in `public` — `schema_migrations` |
| `up` #2 | clean, identical column state (`YES` / `'n'`), `migrate version` → `20`, not dirty |

So the migration is both reversible and re-runnable. `000017`'s `down.sql`
needed no change (it drops the whole table).

**`docs/database.md` matches** — its `000017` row now states the widening, and
`TestDatabaseDocMigrationParity` passes (`internal/db ok 0.265s`).

**The PRD parity guard genuinely does not check nullability.** Read both
guards in `internal/db/prd_index_parity_test.go`:
`TestPRDWorkshopAndMailingColumnParity` builds `prdColumns` from a *blanked*
view of §6.2's SQL (`prepareSQLText(sqlOnly)` — "column names only") and the
only assertion in its loop is `if _, ok := prdCols[col]; !ok` — column-name
presence, one direction, migrations → PRD. Nothing reads a type, a `NOT NULL`,
or an `ON DELETE` clause anywhere in either guard. The implementer's claim is
accurate and this is **not** a bounce reason. It does leave PRD.md:541 still
saying `subscriber_id BIGINT NOT NULL REFERENCES subscribers(id) ON DELETE
CASCADE` — reported below.

### The property that matters most — verified end to end, including the `complained` case

**An erased address cannot re-subscribe into a weaker state.** Driven through
the real public `POST /api/subscribe` with a real `*subscribers.SuppressionStore`
(not the `NoSuppressions` stub), against the same database:

```
erase response: {"erased":true,...,"previous_status":"active",...}
re-subscribe after erasure: status=202 body={"message":"Check your email to confirm."}
no subscribers row exists for the address (checked by email, not just by id)
```

**The `complained` probe (CLAUDE.md §9) — the design's central claim, proved.**
Seeded a subscriber at `status='complained'` with a `complaint` suppression row
(the exact state `#0038`'s SES ingestion leaves), erased it, then re-subscribed:

```
BEFORE erase: status=complained, suppression reasons=[complaint]
AFTER  erase: suppression reasons=[complaint manual]
re-subscribe after erasure: status=202, no subscribers row created
```

I pushed one step past the shipped test to check the two reasons are genuinely
*independent* rather than one masking the other: removing the `manual` row
afterwards left `IsSuppressed` **true** on the surviving `complaint` row alone,
and a further re-subscribe still created no row. So an erased-while-complained
address ends with two orphan-safe blocking reasons, and the complaint one is
load-bearing on its own. §9's rule is preserved, not merely not-violated.

### The uniform 202 — byte-identical across every branch, erasure included

Compared bodies **and** full header sets (minus `Date`), the way
`TestSubscribe_UniformResponseAcrossBranches` does, adding two branches that
test does not have:

| Branch | Status | Body | Headers |
|---|---|---|---|
| never-seen address | 202 | `{"message":"Check your email to confirm."}\n` | `Content-Type: application/json` |
| ordinary `pending` | 202 | byte-identical | identical |
| **erased** | 202 | byte-identical | identical |
| **erased, previously `complained`** | 202 | byte-identical | identical |

No enumeration oracle. An erased address is indistinguishable from one the
service has never seen.

### Full post-erasure census — what survives, counted rather than described

Seeded one subscriber with an interest, two `email_sends` rows (`sent` and
`failed`, different campaigns, with `ses_message_id`/`error`/`attempts` set),
one `email_events` row whose raw payload carries the address, then erased it
through the real HTTP endpoint:

| Table | Before → after | Detail |
|---|---|---|
| `subscribers` | 1 → **0** | gone by id *and* by email |
| `subscriber_interests` | 1 → **0** | CASCADE via `subscribers(id)` |
| `email_sends` | 2 → 2 rows **still present by primary key**; 2 → 0 by `subscriber_id` | both `subscriber_id IS NULL`; `status` `sent`/`failed` **unchanged**; `attempts` 1/3, `ses_message_id`, `error` all unchanged; `email` overwritten to `erased-1@erased.invalid` / `erased-2@erased.invalid` |
| `email_events` | 1 → 1 | untouched, by design |
| `suppressions` | 0 → 1 | `reason="manual" note="GDPR/CCPA erasure request"` |
| `audit_log` (this target) | 0 → 1 | `subscriber.erased` added; nothing removed |

Campaign counts and statuses do not move — criterion 3 holds.

**Then I grepped the address across every column of every table in the public
schema** (`information_schema.columns` → `SELECT count(*) … WHERE col::text
ILIKE '%addr%'`, plus a local-part-only pass to catch partial retention). Four
places still hold it:

```
RECOVERABLE  audit_log.metadata (jsonb)     — 1 row
RECOVERABLE  email_events.payload (jsonb)   — 1 row
RECOVERABLE  email_events.recipient (text)  — 1 row
RECOVERABLE  suppressions.email (text)      — 1 row
```

Three of those four are required or disclosed: `suppressions.email` is
criterion 2 and the policy names it; `email_events.recipient`/`payload` are
criterion 4 and the policy names them. `audit_log.metadata` is the fourth, and
it is the defect below.

I then ran the **real production journey** — real `POST /api/subscribe` →
real `POST /api/subscribe/confirm` → erase — rather than the store-level
shortcut, which is what surfaced the full picture:

```
BEFORE ERASE — audit_log rows for the subscriber:
    subscriber.signup     meta={"kind": "new"}                          ip="192.0.2.1"
    subscriber.confirmed  meta={"email": "<the address>"}               ip="192.0.2.1"
AFTER ERASE:
    subscriber.signup     meta={"kind": "new"}                          ip="192.0.2.1"
    subscriber.confirmed  meta={"email": "<the address>"}               ip="192.0.2.1"
    subscriber.erased     meta={"email": "<the address>", "previous_status": "active", …}  ip="127.0.0.1"
CENSUS: RECOVERABLE audit_log.metadata (jsonb) — 2 rows
        RECOVERABLE suppressions.email (text) — 1 row
```

### The 409 refusal, and the check-to-delete window — no race

`Erase` takes `SELECT … FOR UPDATE` on the `subscribers` row **before** the
pending-send `EXISTS` check and holds it through the `DELETE`, all in one
transaction. I verified by execution that this genuinely closes the window
rather than merely looking like it does. Transaction A held the row exactly as
`Erase` does; transaction B then attempted the send worker's materializing
`INSERT INTO email_sends (campaign_id, subscriber_id, email, status)`:

```
OK: the concurrent INSERT blocked on Erase's SELECT ... FOR UPDATE for >1.5s
    (the FK's implicit FOR KEY SHARE on the parent conflicts with FOR UPDATE)
after Erase committed, the concurrent INSERT returned:
    ERROR: insert or update on table "email_sends" violates foreign key
    constraint "email_sends_subscriber_id_fkey" (SQLSTATE 23503)
ghost email_sends rows for the erased address after the race: 0
```

So a send cannot be materialized between the check and the delete: it blocks,
and is then rejected by the FK. Ordering the other way, B commits first and A's
`FOR UPDATE` waits, then sees the new `queued` row in a fresh read-committed
statement and returns `ErrHasPendingSends` → 409. The refusal itself is proved
by execution too (`TestAdminSubscribers_Erase_ConflictsOnPendingSend`, re-run
green), and the refusal path mutates nothing.

### `RecheckEligibleTx` — the reported trap is real; the workaround is sufficient today

Verified the claim directly rather than by reading the SQL. Same `email_sends`
row, same call, before and after NULLing `subscriber_id`:

```
with subscriber_id set:  eligible=1 skipped=0 err=<nil>
with subscriber_id NULL: eligible=0 skipped=0 err=<nil>   ← invisible
the row's status remains "queued" with no code path to change it
```

Confirmed: `audience.go`'s `JOIN subscribers s ON s.id = es.subscriber_id` is an
INNER JOIN, so a NULL-`subscriber_id` row is returned in **neither** slice, and
`MarkSkippedTx` only ever runs on rows the function returns. The row would sit
at `queued` forever.

**Verdict: the 409 workaround is sufficient for erasure specifically, and the
join should still be fixed.** Sufficient, because the lock ordering above makes
"erased with a pending send" genuinely unreachable, and I confirmed the only
two Go read sites for `email_sends.subscriber_id` are safe against NULL
(`audience.go:380` is `RETURNING` from an `INSERT`, never NULL; `audience.go:494`
is behind the INNER JOIN itself) — so nothing panics or mis-scans. Still worth
fixing, because the join *fails silently*: nothing in the codebase enforces the
invariant "a `queued` row always has a non-NULL `subscriber_id`", and the
failure mode of breaking it is an invisible stuck row rather than an error. A
`LEFT JOIN` that returns the orphan as an explicit `SkippedSend` would make the
invariant self-enforcing. Reported below, not filed (CLAUDE.md §9).

### The admin guard — proved by two mutations, both against my own database

Both files backed up and restored byte-exact (`shasum -a 256` matched before and
after; the suite re-ran green on the restored tree):

| Mutation | Result |
|---|---|
| `adminSubscribersMux`: `DELETE /admin/subscribers/{id}` `requireAdmin` → `requireSession` | `TestAdminSubscribers_NonAdminForbidden` **FAILED** — `DELETE /admin/subscribers/1 with non-admin session: status = 400, want 403` |
| `mountAndServe`'s `adminRoutes` loop: `requireAdmin(r.handler)` → `requireSession(r.handler)` for the DELETE path only | `TestMountAndServe_AdminRoutesRequireSessionAndAdmin` **FAILED** — `non-admin session: DELETE /admin/subscribers/1 status = 400, unexpected for this case` |

(The `/1` in both messages is a subscriber each test seeded itself, which
happened to take id 1 in an empty scratch database — not a hardcoded literal.)
`TestAdminRoutesRegisteredOnlyViaTable` continues to block registering the
route outside the table. The guard is real at both layers.

The typed confirmation is also real and ordered correctly: `GetByID` and the
comparison run **before** `Erase`, so a mistyped confirmation mutates nothing —
confirmed by re-running `TestAdminSubscribers_Erase_RequiresMatchingConfirmEmail`
and by the subscriber still existing after both 400s.

---

### Defect — criterion 7 is not met: the privacy policy's retention list is now factually wrong

`web/src/views/PrivacyPolicy.svelte` says, under "How to leave":

> An erasure request **hard-deletes your subscriber record — your email,
> interests, and signup details** — but **three things** are deliberately
> retained rather than deleted:

…and then enumerates exactly three: the suppression entry, the anonymized send
history, the raw deliverability events. The implementation notes conclude from
this that the page "Already documents this design precisely… No change needed."

It does not. As the real-journey census above shows, an erasure leaves a
**fourth** category the page never mentions and which its closed "three things"
enumeration actively excludes: **`audit_log` retains the subscriber's email
address in plaintext, and their signup IP.**

- `subscriber.confirmed` keeps `metadata = {"email": "<the address>"}`
  (`internal/handlers/confirm.go:156`) — not written by this change, but not
  removed by it either, and never disclosed anywhere.
- `subscriber.signup` keeps `ip_address` = the signup IP
  (`internal/handlers/subscribe.go:1104`) — which is precisely one of the
  "signup details" the same sentence promises are hard-deleted, and which the
  page's own "What we collect" list names explicitly.
- The **new** `subscriber.erased` row this change adds keeps
  `metadata = {"email": …, "previous_status": …}`. `previous_status` is a fact
  about the person (`complained`, `bounced`) that exists in no other surviving
  row.
- `subscriber.complained` / `subscriber.bounced` keep `{"email": recipient, …}`
  too (`ses_notifications.go:562`, `:444`, `:504`) for any address that ever
  bounced or complained.

So after an "erasure", one table still joins this person's email address to
their signup IP and their former subscriber state, and the published policy
tells them the opposite. That is criterion 7 ("Privacy policy documents what is
retained after erasure and why") unmet as a matter of fact, not of wording, and
it is squarely this issue's scope — "what is retained after erasure" is exactly
what this issue defines.

The audit design itself is right and should not change: `ActionSubscriberErased`
is correctly justified, `target_id` correctly carries no FK, and an erasure that
left no trace of itself would be worse. `audit_log` is append-only, so scrubbing
the pre-existing rows is not the answer either. **The page is what is wrong.**

### What the next implementation pass must do

1. **Fix the erasure paragraph in `web/src/views/PrivacyPolicy.svelte`.** Add a
   fourth `StatusList` item covering the admin audit log — that an internal
   record of the erasure itself, and of earlier actions on the account
   (signup, confirmation, bounce/complaint handling), is retained, that it
   includes the address and the signup IP, and why (proving the request was
   honored, and abuse forensics). Change "three things" to match, and do **not**
   replace it with another closed count unless the fourth item is genuinely the
   last — the file's own header comment already warns against closed
   enumerations for exactly this reason.
2. **Update that header comment** (lines 38–42), which currently instructs
   future editors that the paragraph "must keep naming all three things #0060
   retains". It is now four, and the comment will otherwise re-introduce the
   defect.
3. **Run `scripts/check.sh web`** — `npm run check` and `npm test`. The
   implement pass skipped web verification on the (then-believed) grounds that
   no `web/` file needed to change; that is no longer true.
4. Nothing in Go, SQL, or `docs/` needs to change. Leave `e718b73` in place.

### Worth filing (reported, not filed — CLAUDE.md §9)

1. **`internal/mailing.AudienceStore.RecheckEligibleTx`'s INNER JOIN silently
   hides a NULL-`subscriber_id` row** — verified above: neither eligible nor
   skipped, status stuck at `queued` with no code path out. `#0060` works
   around it by refusing to erase while a send is pending, and that workaround
   is airtight for erasure, but the join is a latent trap for any future path
   that leaves `subscriber_id` NULL on a `queued` row, and it fails silently
   rather than loudly. A `LEFT JOIN` returning the orphan as an explicit
   `SkippedSend` would make the invariant self-enforcing.
2. **`PRD.md` §6.2 (line 541) still specifies `subscriber_id BIGINT NOT NULL
   REFERENCES subscribers(id) ON DELETE CASCADE`**, the exact opposite of what
   shipped and of what this issue depends on. The parity guards are
   names-only, so nothing catches it, and a future reader of the spec would
   conclude that deleting a subscriber cascades away their send history. This
   is not "the spec leading the implementation" (the case
   `prd_index_parity_test.go` deliberately tolerates) — it is the spec
   contradicting it. A one-line currency edit, the same kind `#0113` made.
3. **No admin-console UI** exists for `DELETE /admin/subscribers/{id}` or for
   `#0059`'s export — both are reachable only by direct API call. (`#0219`
   already covers the export half; this is the erasure half, and it needs the
   typed-confirmation modal.)
4. **`UNIQUE (campaign_id, subscriber_id)` no longer constrains anonymized
   rows**, since PostgreSQL treats NULLs as distinct. Harmless today — an
   erased subscriber can never be re-materialized — but the migration comment
   still describes that unique key as "the idempotency guarantee" without
   noting the NULL carve-out.

---

## Review notes — second pass (re-review of the bounce fix)

**Phase 3 re-review, 2026-08-23 (Opus). Resolved.** Re-verified `bd4b422`
independently, against my own database throughout (`ISSUE=0060rr`, i.e.
`opencircuit_test_0060rr`, created by `scripts/testdb.sh create 0060rr` for the
probes and re-created/dropped by `check.sh` for the suite run — dropped, and
`scripts/testdb.sh list` afterwards shows only `opencircuit_test_template`,
which is not mine). `uptime` at start: load average **2.00** (2.04 / 2.24) —
idle, well under the ~8 threshold, so nothing below is a load artifact. The
probes lived in a throwaway `internal/handlers/zz0060rr_probe_test.go`, deleted
before this commit; every seeded row used a `zz-0060rr-` prefix and no literal
or seeded id was ever targeted (CLAUDE.md §8b). No leftover `go test`/`.test`
processes.

**Scope of `bd4b422` confirmed to be exactly what the bounce asked for.** Four
files: `web/src/views/PrivacyPolicy.svelte`, `PRD.md`, `CLAUDE.md`,
`migrations/000017_create_campaigns.up.sql`. `internal/subscribers/erase.go`,
`internal/handlers/admin_subscribers.go`, and every `_test.go` are untouched,
and the migration diff is **comment-only** — every added line is `--`-prefixed
(`git show bd4b422 -- migrations/… | grep -vE '^[+-]--'` returns nothing), so
the schema is byte-identical to what passed the first review. The erasure code
is genuinely not under re-review and I did not re-litigate it.

### The only question that mattered: is the new text true?

**I ran the real journey myself, not the store-level shortcut.** Real `POST
/api/subscribe` (signup IP `203.0.113.77`, delivered as `X-Forwarded-For`
behind a loopback peer, exactly as Apache delivers it — `middleware.ClientIP`
only trusts XFF from a loopback peer, so this is the production path, not a
spoof) → real `POST /api/subscribe/confirm` with the live `confirm_token` →
real `DELETE /admin/subscribers/{id}` through `adminSubscribersMux` with a real
admin session and the typed confirmation.

```
BEFORE ERASE — audit_log rows for the subscriber:
    subscriber.signup      meta={"kind": "new"}                 ip=203.0.113.77
    subscriber.confirmed   meta={"email": "<the address>"}      ip=203.0.113.77
AFTER ERASE:
    subscriber.signup      meta={"kind": "new"}                 ip=203.0.113.77
    subscriber.confirmed   meta={"email": "<the address>"}      ip=203.0.113.77
    subscriber.erased      meta={"email": …, "previous_status": "active",
                                 "interests_removed": 0, "suppression_reason": "manual",
                                 "email_sends_anonymized": 0,
                                 "suppression_preexisted": false}   ip=127.0.0.1
subscribers row exists (checked by id AND by email): false
```

**Then the full census.** I enumerated every column in the `public` schema of
`text`/`varchar`/`char`/`jsonb`/`json`/`inet`/`cidr` type from
`information_schema.columns` (restricted to `BASE TABLE`) and ran
`col::text ILIKE '%needle%'` against each, for the full address, for the
local-part alone (to catch partial retention), and for the IP:

```
ADDRESS "zz-0060rr-…@example.com":
    audit_log.metadata (jsonb)     — 2 rows      ← policy item 4
    suppressions.email (text)      — 1 row       ← policy item 1
LOCAL PART "zz-0060rr-…"  (partial-retention pass):
    audit_log.metadata (jsonb)     — 2 rows
    suppressions.email (text)      — 1 row
SIGNUP IP "203.0.113.77":
    audit_log.ip_address (inet)    — 2 rows      ← policy item 4
```

The address survives in **exactly two** places and the IP in **exactly one**
column — matching the fix pass's report precisely. I also checked the type
coverage was complete rather than assuming it: the only other column types in
`public` are `bigint`, `boolean`, `integer`, `timestamptz`, `uuid`, and
`bytea`, and every `uuid`/`bytea` column belongs to `passkey_credentials` or
`webauthn_challenges` (admin WebAuthn material, never subscriber PII). Nothing
survives outside the four categories the page now names.

**Nothing named over-promises either.** Categories 2 and 3 were not exercised
by the bare journey (no campaign send, no SES event), so I proved them
separately rather than inheriting them: seeding two `email_sends` rows on two
campaigns and erasing showed `2 → 2` rows by primary key, `status` `sent`/
`failed` and `attempts` `1`/`3` unchanged, `subscriber_id` NULL, `email`
rewritten to `erased-N@erased.invalid`. `email_events` retention was proved
under execution by the first pass against unchanged code.

**The two promises in the new item's tail check out.** "Not exposed publicly":
the only reader of `audit_log` in the whole tree is `internal/audit/read.go`,
reached solely by `GET /admin/audit`, which sits in `adminRoutes` and is
therefore wrapped by `requireAdmin` (`TestAdminRoutesRegisteredOnlyViaTable`
prevents registering an admin route outside that table). "Not used to re-add or
re-contact you": nothing in the mailing subsystem reads `audit_log` at all.

### Reading it as a person

The rest of the page sets a plain-language standard, and the new item meets it.
It leads with *what* ("an internal admin audit log entry recording earlier
actions on your account"), states *what of mine is in it* in concrete terms
("your email address and your signup IP address" — not "certain identifiers"),
gives *why* in two reasons a reader can evaluate ("prove a request was honored",
"investigate abuse"), and closes the two questions a reader actually has ("not
exposed publicly", "not used to re-add or re-contact you"). It is longer than
the other three items, which is the right trade: this is the one the reader
would not have guessed. The header-comment rewrite is the more valuable half of
the change — it now records *why* the count moved and instructs future editors
to update the number rather than re-close it, which is what would have
prevented the original defect.

Two things in it are narrower than reality. Neither is a false statement and
neither is a second bounce; both are reported below as follow-ups:

- the parenthetical `(signup, confirmation, and the erasure itself)` omits
  bounce/complaint handling, which also writes `subscriber.bounced` /
  `subscriber.complained` rows carrying `{"email": recipient, …}`
  (`ses_notifications.go:447`, `:507`, `:565`). The reader is not left
  misinformed — item 3 already discloses that bounces and complaints logged
  against the address are retained — so this is a completeness gap inside a
  disclosed category, not the closed-enumeration defect the first pass bounced;
- `audit_log.ip_address` is populated on *every* subscriber-driven request, not
  only signup: the confirming click (proved above — `subscriber.confirmed`
  carries an IP), `subscriber.unsubscribed` from both the preference center and
  the one-click path, and `subscriber.preferences_updated`
  (`preferences.go:257`, `:320`, `unsubscribe.go:246`). "Your signup IP
  address" is true; it is not the whole set.

### PRD §6.2 and §11 — recomputed, not taken on trust

`PRD.md` §6.2 now reads `subscriber_id  BIGINT REFERENCES subscribers(id) ON
DELETE SET NULL`, byte-matching migration `000017`'s shipped column, and the
spec-contradicts-implementation defect the first pass reported is gone.

I recomputed **every** row of CLAUDE.md §11 by running that row's own `sed`
command, applying §11's documented convention (the range is inclusive of the
following section's heading, so a row's count is the raw `wc -l` minus one
whenever a terminator matched; §14, the last section, has no terminator and is
raw). Every one of the 48 rows matches — including the three the fix pass
moved:

| Row | Raw `sed \| wc -l` | Convention-adjusted | CLAUDE.md §11 |
|---|---|---|---|
| §6.2 Database schema | 336 | **335** | 335 ✓ |
| §6 Mailing List (parent) | 943 | **942** | 942 ✓ |
| `PRD.md` total (`wc -l`) | — | **1,807** | 1,807 ✓ |
| §14 Open Questions (no terminator) | 14 | 14 | 14 ✓ |

`go test ./internal/db/... -run 'TestPRDSectionIndex|TestPRDWorkshopAndMailingColumnParity|TestDatabaseDocMigrationParity' -v`
— `TestPRDSectionIndexParity`, `TestPRDSectionIndexTotalMatchesFile`,
`TestPRDWorkshopAndMailingColumnParity`, and `TestDatabaseDocMigrationParity`
(both its `docs/database.md` and `CLAUDE.md` subtests) all **PASS**.

**The fix pass's stale-snapshot observation is correct, and I confirmed it
rather than assuming.** `git log -S'1,769 lines' -- CLAUDE.md` shows commit
`2e55a78` ("Correct CLAUDE.md §7's stale #0064 warning…"), which predates all
of `#0060`'s work, is what moved §11 from 1,769 to 1,798. So the file on disk
was already at 1,798/933/326 before `bd4b422` touched it, and the 1,769/296
figures quoted in the session's CLAUDE.md snapshot were stale context, not a
number the fix pass failed to update. `bd4b422`'s diff is the widening from the
already-current values.

### The migration comment — true in Postgres, and it does not overstate

Proved by execution rather than by reasoning about the manual. Two
`email_sends` rows inserted for the **same** `campaign_id` with
`subscriber_id = NULL` were **both accepted**, ids returned — so the
`UNIQUE (campaign_id, subscriber_id)` key genuinely does not constrain
anonymized rows against each other, exactly as the comment says. The constraint
itself is still present and still binding for live rows
(`email_sends_campaign_id_subscriber_id_key`).

The comment's "harmless — an erased subscriber can never be re-materialized"
also follows from the erasure design, on two independent grounds: `subscribers`
is hard-deleted and `id` is `BIGSERIAL`, so the erased id is never reissued;
and even if the same *address* re-subscribed it would take a fresh id, which
the UNIQUE key constrains normally. The carve-out therefore only ever relaxes
the key *between rows whose subscriber no longer exists*, where there is
nothing left to double-deliver to. Not overstated.

### Regression bar — the first review's three properties, re-run, all hold

| Property | Result |
|---|---|
| A complained address stays blocked after erasure | seeded `status='complained'` + a `complaint` suppression row (`#0038`'s exact end state), erased through the real HTTP endpoint → suppression reasons `[complaint manual]`, both surviving independently; the real `POST /api/subscribe` afterwards created **no** `subscribers` row. CLAUDE.md §9 preserved. |
| The 202 is byte-uniform across branches | never-seen / erased / erased-was-complained all returned `202`, body byte-identical `{"message":"Check your email to confirm."}\n`, and identical full header sets (`Content-Type: application/json`, `Date` excluded). No enumeration oracle. |
| `email_sends` survive with counts and statuses unchanged | `2 → 2` rows by primary key, `[sent failed]` before and after, `attempts` `1`/`3` intact, `subscriber_id` NULL, `email` anonymized. Criterion 3 holds. |

### Verification

- `ISSUE=0060rr scripts/check.sh go ./internal/... ./cmd/...` — **VERIFICATION
  PASSED**, `gofmt` clean, **zero skips** ("(no skips)"), no `[no test files]`,
  every DB-backed package `ok` at a real elapsed time: `internal/handlers
  3.124s`, `internal/interests 2.727s`, `cmd/opencircuit 1.635s`,
  `internal/mailing 1.012s`, `internal/middleware 0.977s`, `internal/auth
  0.834s`, `internal/subscribers 0.416s`, `internal/audit 0.365s`,
  `internal/sesnotify 0.322s`, `internal/db 0.175s`.
- `scripts/check.sh web` — **VERIFICATION PASSED**: `npm run check` — 265
  files, **0 errors, 0 warnings**; `npm test` — 27 files, **3760 tests
  passed**, 1.17s. This is the run the implement pass skipped and the bounce
  required.
- `go test ./internal/db/... -run 'TestPRDSectionIndex|…' -v` — all four parity
  guards pass (above).
- Probe file deleted; `opencircuit_test_0060rr` dropped and confirmed absent;
  `pgrep -f 'go test|\.test '` empty.

### Worth filing (reported, not filed — CLAUDE.md §9)

Items 1–3 below are the first pass's follow-ups, still open and unchanged by
`bd4b422`; items 4–6 are new from this pass. (The first pass's fourth item —
the `UNIQUE` NULL carve-out — is now **addressed** by `bd4b422`'s migration
comment and needs no issue.)

1. **`internal/mailing.AudienceStore.RecheckEligibleTx`'s INNER JOIN silently
   hides a NULL-`subscriber_id` row** — neither eligible nor skipped, status
   stuck at `queued` with no code path out. `#0060`'s 409 refusal is an
   airtight workaround *for erasure*, but the join is a latent trap for any
   future path leaving `subscriber_id` NULL on a `queued` row, and it fails
   silently. A `LEFT JOIN` returning the orphan as an explicit `SkippedSend`
   would make the invariant self-enforcing.
2. **No admin-console UI** for `DELETE /admin/subscribers/{id}` — reachable
   only by direct API call, and it needs the typed-confirmation modal.
   (`#0219` already covers `#0059`'s export half.)
3. *(the PRD §6.2 currency defect from the first pass is **fixed** by
   `bd4b422`; recorded here only so the numbering below is unambiguous.)*
4. **The privacy policy's audit-log item names only the signup IP, but
   `audit_log.ip_address` is populated on every subscriber-driven request** —
   the confirming click, one-click and preference-center unsubscribes, and
   preference updates all record the requester's IP against a
   `target_type='subscriber'` row that survives erasure. Either widen the
   wording ("the IP address of each of those actions") or stop recording IPs on
   the subscriber-driven audit rows that do not need them. Note this also sits
   slightly awkwardly beside the page's Consent section, which says the
   confirming click records "its own timestamp — not a second IP address" —
   true of the `subscribers` schema, but the audit log does hold one.
5. **The same item's parenthetical omits bounce/complaint handling**, which
   writes `subscriber.bounced`/`subscriber.complained` audit rows carrying the
   address (`ses_notifications.go:447`, `:507`, `:565`) and which survive
   erasure. Item 3 covers the substance, so this is a wording completeness gap,
   not a misrepresentation.
6. **Nothing mechanically ties the privacy policy's retention enumeration to
   what erasure actually leaves behind.** The only guard today is a prose
   comment at the top of `PrivacyPolicy.svelte`, and that comment is exactly
   what was wrong when this issue bounced — it asserted "all three things" while
   the code retained four. A small `web/` unit test asserting the erasure
   `StatusList` has one item per retained category (and mentions the audit log)
   would turn the next drift into a failing test rather than a review finding.
   This is the general shape of `#0113`: an invariant maintained by hand across
   two files drifts on the first edit that does not think about both.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §11 — Security and Compliance · 35 lines
  `sed -n '/^## 11\. /,/^## [0-9]/p' PRD.md`
- Depends on: [#0033](0033.md) — Create the suppressions migration and store
- Depends on: [#0032](0032.md) — Build the subscribers admin screen
- Depends on: [#0059](0059.md) — Export subscribers as CSV

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 306 | 11,124 | 28,824,541 | 268,001 | $9.82 |
| 2026-08-23 | review (bounced) | claude-opus-5 | 108 | 22,433 | 6,220,387 | 156,052 | $4.65 |
| 2026-08-23 | fix | claude-sonnet-5 | 122 | 7,629 | 5,225,437 | 92,528 | $2.03 |

**Total: $16.50**
