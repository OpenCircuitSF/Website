# 0200 — The citation guard's 95/95 walk-coverage invariant is still re-derived and deleted every pass

| | |
|---|---|
| **Status** | open |
| **Platform** | All |
| **Module** | handlers |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |

## Description

`#0194` committed the 34-case shape matrix precisely so three successive passes
would stop rebuilding and deleting the same evidence. Its own phase-3 review
noticed the issue's Part 1 has the identical problem, untouched.

**Nothing in the tree asserts that the Go walk reaches every non-test `.go`
file.** The 95-collected-equals-95-on-disk measurement has now been derived by
scratch probe and thrown away by `#0181`'s pass-2 review, `#0187`'s
implementation, `#0187`'s review, `#0194`'s implementation, and `#0194`'s
review — five times.

It matters more here than for the matrix, because that invariant is **the only
thing that would ever detect `#0194`'s name-based `SkipDir` swallowing a real
directory.** `skipVendoredDirNames` matches any directory called `node_modules`
or `dist` at any depth. The tree has exactly one of each today, both genuinely
vendored, and the doc comment states the choice — but if someone adds
`internal/dist/` the walk silently stops covering it, and every guard built on
that walk quietly narrows with no test failing.

A dozen-line committed test closes both halves: it pins the invariant and makes
the skip's bluntness detectable.

## Acceptance criteria

- [ ] A committed test asserts the shipped walk collects exactly the set of non-test `.go` files on disk under the three roots, set-identical in both directions
- [ ] It runs against the shipped `scanDirForCitations` (or its collection step), not a replica of the walk predicate — see `#0197` on this tracker's replica history
- [ ] Adding a non-test `.go` file under a directory the skip swallows makes the test fail — demonstrated by planting one under a new `internal/dist/`, then removing it
- [ ] Adding an ordinary non-test `.go` file anywhere in the three roots does **not** fail it
- [ ] The test's failure message names the files present-but-uncollected and collected-but-absent, so the direction of the drift is obvious
- [ ] `#0194`'s three-way skip proof still holds, and `#0187`'s 34-case matrix still passes
- [ ] `ISSUE=0200 scripts/check.sh go ./internal/...` passes with zero skips

## Notes

Reported by `#0194`'s phase-3 review under `CLAUDE.md` §9.

The review instrumented the shipped `scanDirForCitations` rather than
re-implementing its predicate, and measured 95 collected, no duplicates, against
95 from `find` — and 95 again excluding `node_modules`/`dist`. That
instrumentation is the test, nearly written.

A second, smaller point from the same review, worth folding in only if it is
cheap: two of the matrix's seven "acceptable misses" (`docs` as prose,
`docs.google.com`) are arguably **boundary checks** — strings that were never
citations at all — which is why their notes read thinner than the other five.
The matrix is faithful to `#0187`'s grouping and should stay that way unless
regrouping is a deliberate decision, so do not silently recategorise them.

Also noted there, not worth its own issue: `[further-shape] {#if} block test
expression` survives a fully broken fragment walk, because its fixture also puts
the citation in `<script>`. Still a valid catch assertion — just not an
isolating one.

## Implementation notes

`internal/handlers/citation_guard_test.go` gained one new test,
`TestScanDirForCitationsCollectsExactlyDiskFiles`, plus the minimal
instrumentation needed for it to run against the shipped walk rather than a
replica:

- `scanDirForCitations` now returns `(violations []citationViolation,
  collected []string)` instead of just violations — same `WalkDir` call, same
  `skipVendoredDirNames` check, same suffix filter; the only change is
  recording each collected path before parsing it. Its one call site
  (`TestNoAdminFacingStringCitesInternalDocs`) was updated to discard the new
  second value.
- The new test enumerates the same three `scanGoRoots` a second, independent
  way: an unrestricted `filepath.WalkDir` with **no** skip logic at all — not
  even a reuse of `skipVendoredDirNames` — because reusing that map would make
  the "ground truth" blind to exactly the bug this test exists to catch (a
  directory swallowed by name that isn't actually vendored). It then diffs the
  two file sets and reports both directions separately: present-on-disk-but-
  uncollected vs. collected-but-not-on-disk.

Commit: `bce1ad8` — `internal/handlers/citation_guard_test.go` only.
Not committed: `issues/0200.md` (this file, per instructions).

## Verification

- `go build ./...` and `go vet ./internal/handlers/...` clean.
- Planted `internal/dist/proof.go` (a real, non-vendored `.go` file) —
  `TestScanDirForCitationsCollectsExactlyDiskFiles` failed, reporting it under
  "present on disk but NOT collected (skipVendoredDirNames may be swallowing a
  real directory)" with the exact path. Removed the file (`git status`
  confirmed only my own tracked-file edit remained, nothing else to restore —
  no `cp`/`shasum` was needed since nothing was copied aside, only a new
  untracked file was created and deleted), reran: green again.
- Added an ordinary throwaway non-test `.go` file directly under
  `internal/handlers/` (not under any skipped directory) — the test did not
  fail. Removed it, confirmed `git status` clean.
- Re-derived `#0194`'s three-way skip proof by hand: planted a `.go` file
  citing "PRD §6.6" under `web/node_modules/some-dep-with-go/`, guard stayed
  green (`TestNoAdminFacingStringCitesInternalDocs`); copied the identical
  file one level up to `web/tmp0200proof/` (not vendored), guard failed
  naming that file and line. Removed both, guard green again, `git status`
  clean of both artifacts.
- `#0187`'s 34-case matrix: `cd web && npm test -- citationGuard` → 58/58
  passed (the matrix is a subset of that file's suite).
- Self-reference hazard: confirmed `#0181`'s guard
  (`TestNoAdminFacingStringCitesInternalDocs`, which excludes `_test.go`
  files from its own walk by construction) and `#0196`'s dangling-test-
  citation guard (`TestNoCommentCitesUndefinedTestFunction`, which does scan
  this file's comments) both still pass with the new test's doc comments
  committed — every `Test...`-shaped token those comments name (e.g.
  `TestScanDirForCitationsCollectsExactlyDiskFiles`,
  `TestNoAdminFacingStringCitesInternalDocs`) is a real function defined in
  this same file, and every `#0NNN` token names a real issue.
- **Shared-tree note:** `internal/handlers/dangling_test_citation_guard_test.go`
  had uncommitted, in-progress edits from another subagent (`#0199`) for the
  entire session — never touched. One interim run of
  `ISSUE=0200 scripts/check.sh go ./internal/...` in the main tree failed on
  `TestDanglingTestCitationPatternExcludesDiscountedShapes`, a test in that
  other file, unrelated to anything this issue's diff touches. Isolated this
  by `git worktree add --detach` from HEAD (the last **committed** state,
  i.e. without that agent's in-flight edits), applying only this issue's diff
  there, and rerunning — `go build`, `go vet`, and the full
  `ISSUE=0200 scripts/check.sh go ./internal/...` all passed clean (zero
  skips, real elapsed times, `internal/handlers` at 2.758s). This confirms
  the interim failure was the other agent's WIP, not a regression from this
  change. Worktree and its scratch database (`opencircuit_test_0200`) were
  removed afterward; no leftover `go test` processes (`pgrep -f 'go test|\.test '`
  came back empty in both the worktree and the main tree).
- Load average throughout: ~1.8–2.6 (well under the ~8 threshold — machine
  was not saturated).

## Relation

- Found by: [#0194](0194.md) — the vendored-directory skip and the committed matrix
- Relates to: [#0187](0187.md) — where the walk gained its third root
- Relates to: [#0181](0181.md) — the citation guard itself
- Relates to: [#0197](0197.md) — the replica trap this must avoid

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 78 | 9,695 | 3,083,853 | 80,867 | $1.37 |

**Total: $1.37**
