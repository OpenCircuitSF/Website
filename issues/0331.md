# 0331 — `template_state()`'s safety rests on call-site shape, not its own control flow

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-28 |
| **Commit** | — |

## Description

`#0328` collapsed `template_version()` and `template_digest()` into a single
`template_state()` that reads version and digest in one `psql` call. Doing so
correctly removed a redundant `db_exists` probe — and with it the
`|| echo "none"` fallback that had been absorbing a failing `psql`.

The result is an unguarded failing assignment:

```
row="$(psql "$PGHOST_URL/$TEMPLATE" … )"
```

Under `set -euo pipefail`, a direct call with the template absent kills the
shell **silently**. `#0327`/`#0328`'s reviewer reproduced this.

**It does not bite today**, verified end to end: `TEMPLATE_DB=…_nope create`
still prints the friendly message and provisions, exit 0. It survives for two
incidental reasons, neither of which is the function's own doing:

1. `template_matches_disk` calls it bare, but only ever as an `if` condition,
   where `set -e` is suspended.
2. `warn_template_mismatch`'s `tv="$(template_state)"` is saved by a bash 3.2
   quirk the reviewer isolated: `row="$(exit 2)"` aborts on a direct call but
   **not** when reached from inside a command substitution.

So the code comment's "no existence probe needed" reasoning reaches the right
result by the wrong mechanism. Any future caller that invokes `template_state`
directly — not as an `if` condition, not inside a command substitution —
inherits a silent shell death with no message.

This is the shape `CLAUDE.md` §8 keeps warning about: a property that holds by
accident of context rather than by construction, on a bash 3.2 where the
failure is quiet.

## Acceptance criteria

1. `template_state()` cannot silently kill the shell when the template is
   absent, regardless of how it is called — the fix is a one-token
   `2>/dev/null || true` on the assignment, or equivalent.
2. The friendly-message path for an absent template still works: a
   `TEMPLATE_DB=<nonexistent> create` provisions and exits 0.
3. A direct (non-`if`, non-command-substitution) call with the template absent
   is proven not to abort — demonstrated under `/bin/bash`, not zsh (§8: the
   agent shell is zsh, the scripts are bash 3.2.57).
4. The stale code comment claiming no existence probe is needed is corrected to
   state why the function is safe on its own terms.
5. `shellcheck -S warning` stays clean.

## Relation

- Found by: [#0327](0327.md)/[#0328](0328.md)'s review (Finding A)
- Depends on: [#0328](0328.md) — introduced by its `template_state()` collapse
- Relates to: [#0320](0320.md) — the digest mechanism being read here

## Fix

`template_state()`'s `row="$(psql … 2>/dev/null)"` assignment now ends in
`|| true`. A failing `psql` (nonexistent `$TEMPLATE`) leaves `row` empty
instead of propagating its exit status to the enclosing simple command; the
existing `[ -n "$TSTATE_VERSION" ] || TSTATE_VERSION="none"` lines (and the
digest equivalent) already turned an empty `row` into the friendly
`"none"`/`"none"` pair — they just never got the chance to run under
`set -e` on a direct call. The stale comment claiming "no up-front existence
probe is needed" (correct about the outcome, silent about the mechanism) is
replaced with one that names the `|| true` as what actually makes that true,
regardless of call-site shape.

## Verification

**Reproduced the silent abort against the pre-fix function first.** Extracted
just `template_state()`'s body from `git show HEAD:scripts/testdb.sh` (i.e.
the code before this fix) via `sed -n '/^template_state() {/,/^}/p'`, `eval`'d
it into a throwaway `bash` script (never sourced into the agent's zsh — §8:
"the agent shell is zsh, the scripts are bash 3.2.57"), and called
`template_state` directly (not inside `if`, not inside `$(...)`) with
`$TEMPLATE` pointing at a nonexistent database:

```
$ bash /private/tmp/.../0331_pre_fix_repro.sh
before direct call
$ echo $?
2
```

No "after direct call" line, no message — the shell died silently at
`template_state`, confirming Finding A's claim (criterion 3's "prove it fails"
implicit in a criterion this specific).

**Same script against the fixed function**, same throwaway-file technique,
run with `bash /private/tmp/.../0331_direct_call_test.sh`:

```
before direct call
after direct call: version=[none] digest=[none]
SURVIVED (no abort)
exit code: 0
```

Criterion 1 (cannot silently kill the shell) and criterion 3 (direct,
non-`if`, non-command-substitution call proven safe under `/bin/bash`) both
hold.

**Criterion 2** — the friendly-message path still works end to end against
the real script and a private id (`0331verify`, dropped immediately after):

```
$ TEMPLATE_DB="opencircuit_test_0331_nope_$$" scripts/testdb.sh create 0331verify
note: template opencircuit_test_0331_nope_66993 does not exist yet — provisioning opencircuit_test_0331verify directly from
migrations/ (this working tree is at 25). This does not touch the shared
template; build it once, explicitly, to get the fast clone path for everyone:
    scripts/testdb.sh template
[25 migrations applied]
postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test_0331verify?sslmode=disable
$ echo $?
0
$ scripts/testdb.sh drop 0331verify
dropped opencircuit_test_0331verify
```

**Criterion 4** — comment corrected in `scripts/testdb.sh` above
`template_state()`; it now names the `|| true` as the mechanism, not the
call-site shape.

**Criterion 5** — `shellcheck -S warning scripts/testdb.sh` and `bash -n
scripts/testdb.sh` both clean.

**Guard suite**: `ISSUE=0331gt bash scripts/testdb_gc_guard_test.sh` — all
5 parts pass, 0 failures, byte-identity check confirms the tracked script was
not further mutated mid-run.

**Pool census unchanged**: `psql -tAc "select datname from pg_database where
datname like 'opencircuit%'"` before and after this work:
`opencircuit`, `opencircuit_live`, `opencircuit_test`,
`opencircuit_test_0336b` (another agent's, untouched), `opencircuit_test_template`.
All work in this issue ran on a nonexistent-by-construction `$TEMPLATE` name
(`opencircuit_test_0331_nope_$$`/`opencircuit_test_0331_nonexistent_$$`) or a
private scratch id (`0331verify`), dropped after use. `gc --all`/`drop
template` were never run against the real pool for this issue.

## Work log

- 2026-08-28 — Filed by the orchestrator from the `#0327`/`#0328` review.
- 2026-08-31 — Implemented: `template_state()`'s assignment now ends `|| true`
  (`scripts/testdb.sh`); comment corrected. See `## Verification`.
  Reviewers report; the orchestrator allocates numbers (§9).
