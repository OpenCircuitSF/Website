# 0439 — `WorkshopSource`'s interface doc asserts a false state of the world, not just a stale reference

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | seo |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |
| **Commit** | — |

## Description

`internal/seo` carries an idiom — *"nil until `#NNNN` lands"* — written when
those issues were open. `#0425` replaced two instances with the **real**
condition (nil only under `STORAGE=json`), which a review verified end to end
and endorsed, because a condition cannot go stale the way an issue reference
can.

**Two instances remain, and one is worse than stale.**

1. **`NewRenderer`'s doc comment** (`internal/seo/seo.go`) carries the same
   idiom. Left deliberately by `#0425`'s third pass — out of its prescribed
   scope, correct discipline.
2. **`WorkshopSource`'s interface doc** (`internal/seo/workshop.go`) states that
   `#0051` **"has not landed yet"** and that `#0054` **"is expected to wire the
   real implementation in"**.

Both issues are `resolved`, and **`workshopSEOSource` is what `#0054` wired**.

### Why the second is a different category

A stale issue reference is a pointer that has rotted. This is a **claim about
the present state of the system that is false** — an interface's own contract
telling a reader the implementation does not exist, when it does and is in the
same repository.

A reader trusting it would conclude the workshop source is unimplemented and
either duplicate it or work around a nil that never occurs in the Postgres path.

Confirmed by the orchestrator: `workshop.go` carries "`#0054` is expected to
wire the real implementation in."

## The fix, and the precedent to follow

`#0425` established the shape: **replace the issue-relative claim with the
condition it was standing in for.** `buildSEOSite`'s doc comment
(`cmd/opencircuit/main.go`) already carries the accurate form for the same fact,
and a review verified it: `DevMode()` is literally `Storage == "json"`;
`servePostgres` passes struct values, so non-nil unconditionally; `serveDevMode`
is the only other caller and passes `nil, nil`.

**Do not simply update the issue numbers.** That reproduces the defect with a
later expiry date.

## Leave these alone
The two **quotations** of `#0020`'s Notes — in `workshop.go` and
`sitemap_test.go` — are correct **as attributed quotations** of what that issue
said at the time. `#0425`'s reviewer confirmed this specifically. Rewriting a
quotation to match present reality would falsify a record, which is `#0271`'s
`## Scope` rule.

Distinguishing them from the defect above is part of the work.

## Acceptance criteria

1. Neither remaining instance asserts a state of the world that is false.
2. **The replacement states a condition, not an issue reference**, following
   `#0425`'s endorsed pattern — and **verify the condition yourself** rather
   than copying it from here.
3. The `#0020` quotations are untouched, and your notes say how you told them
   apart from the defect.
4. **Sweep the package for the idiom** using the technique `#0425`'s second
   review established, since two sweeps there missed an instance by keying on
   the token whose absence *was* the defect: net on a **set member or phrase any
   instance must contain**, then read the windows. Report the result even if
   empty.
5. **No mechanism** (`#0330`/`#0347`/`#0356`/`#0381`). Two reviews independently
   concluded a comment-versus-code guard here would store a copy of the answer
   next to the question (§8).
6. **gofmt stability** — these are doc comments, and §8 records gofmt rewriting
   them and collapsing doubled backticks, with backtick spans offering no
   protection. Prove it non-destructively, hashing `gofmt`'s stdout against
   on-disk bytes.
7. Run `./internal/seo/...`, `./internal/db/...` and `./internal/handlers/...`
   (§5's repo-wide-guard table). Per §5 name the specific tests that ran — real
   function names, not a `-run` prefix. No Go-file-plus-line-number citation
   (`#0352`), scanned with `python3` bytes rather than BSD `grep -P`.

## Relation

- Found by: [#0425](0425.md)'s second review, which named it the branch to take
  "if more turn up"
- Relates to: [#0425](0425.md) — which established the condition-not-reference
  pattern and endorsed it
- Relates to: [#0051](0051.md), [#0054](0054.md) — resolved, and the subject of
  the false claims
- Relates to: [#0437](0437.md) — the coverage half of the same area

## Work log

- 2026-09-05 — Filed by the orchestrator from `#0425`'s second review (§9), with
  the `workshop.go` claim confirmed directly before filing.
- 2026-09-05 — Implemented. `NewRenderer`'s doc comment (`internal/seo/seo.go`)
  and `WorkshopSource`'s interface doc (`internal/seo/workshop.go`) both
  replaced the "until #0051/#0054 land"/"has not landed yet"/"is expected to
  wire ... in" idiom with the real condition, matching `#0425`'s endorsed
  pattern verbatim: nil only under `STORAGE=json`, which has no
  workshops-table (and, for `archive`, no `email_campaigns`-table) backing.
  Verified against `cmd/opencircuit/main.go` directly rather than trusting the
  issue's framing: `Config.DevMode()` is literally `Storage == "json"`;
  `main()`'s backend-selection block calls `serveDevMode` only when
  `cfg.DevMode()`; `servePostgres` passes `buildSEOSite` a non-nil
  `workshopSEOSource{...}` struct value unconditionally; `serveDevMode` is the
  only other caller and passes literal `nil, nil`.

  The `#0020` quotation inside `WorkshopSource`'s doc ("Until #0051 lands, the
  workshop portion of the sitemap can be empty") is untouched verbatim, per
  the issue's own instruction and `#0425`'s reviewer's prior confirmation that
  it is correct as an attributed quotation. Told apart from the defect by
  what each sentence asserts: the two removed sentences ("#0051 ... has not
  landed yet", "#0054 is expected to wire the real implementation in") state
  the *current* condition of the system in the writer's own voice, and that
  statement is now false; the quoted sentence is explicitly attributed to
  `#0020`'s Notes as something that document said, and a record of what a
  past document said does not go stale the way a live assertion does (`#0271`'s
  `## Scope` rule). Added one word to the surrounding prose -- "a quotation of
  #0020's Notes as originally written, describing the then-unimplemented
  state" -- to make that attribution explicit for a future reader, without
  altering the quoted text itself.

  Swept `internal/seo` for the idiom with `python3`, netting on
  `has not landed`, `not landed yet`, `is expected to`, `lands -- `,
  ``until #\d{4}``, `nil until`, `expected to wire` (the technique `#0425`'s
  second review established, after two earlier sweeps there missed an
  instance by keying on a token whose absence was itself the defect), then
  read every window by hand. Result: the two instances above (now fixed);
  `internal/seo/sitemap_test.go`'s own paraphrase of the same `#0020` quote
  ("until #0051 lands, the workshop portion can be empty") -- left alone per
  the issue's explicit instruction, out of scope as a second correct
  quotation; and one near-miss in `internal/seo/archive.go`'s `ArchiveSource`
  doc ("#0123's real store is expected to be wired in from the start (unlike
  WorkshopSource's staged #0051/#0054 rollout)") -- read closely and judged
  NOT an instance of the defect: it is a permanent design-contrast statement
  (archive's real store was never staged behind a nil placeholder the way
  WorkshopSource's was) rather than a forward-looking promise about something
  still unbuilt, and it remains true regardless of #0051/#0054/#0123's
  resolution status. Left unchanged. No other hits in the package (the
  remaining matches, in `seo.go`'s cache-miss comments, `workshop.go`'s
  `StartsAt` field doc, `card_test.go`'s doc comment, `site.go`'s and
  `invalidator_satisfier_guard_test.go`'s already-correct past-tense rename
  notes, were unrelated to this idiom on inspection).

  No mechanism added (criterion 5): considered whether the new "nil only
  under STORAGE=json" claim could be pinned by a test. The "nil source
  degrades gracefully" half is already exercised in-package (e.g.
  `internal/seo/site_test.go`'s and `sitemap_test.go`'s fixtures construct
  `NewSite`/`NewSitemap` with a literal `nil` source and assert the fallback
  behavior). The "nil ONLY under STORAGE=json" half is a cross-package wiring
  fact about `cmd/opencircuit`'s two call sites, not something `internal/seo`
  can observe about itself, and no existing test anywhere asserts it -- adding
  one would mean introducing new test surface in `cmd/opencircuit` for a
  single doc-comment claim, which is exactly the shape two prior reviews
  (cited by this issue's own criterion 5) already rejected for this area, and
  it exceeds this issue's file scope (`internal/seo/` and
  `cmd/opencircuit/main.go` itself, not its test files). Verified the claim
  by direct source reading instead (above), matching how `#0425`'s second
  pass verified and shipped the identical rewrite with no accompanying test.

  gofmt stability proven non-destructively (`gofmt <file> | shasum` against
  the on-disk `shasum`, no `-w`): both `internal/seo/seo.go` and
  `internal/seo/workshop.go` hash identical before and after --
  `3e7654d1aeb76ca2c5079e5beef2fede363a8010d29badab4fe615b2290fb681` and
  `1a5790cc54b165f027097b28c7820f3d9eca62d89bbbb3f035f4adbafcfb43fd`
  respectively. Confirmed by `python3` byte scan that neither file contains a
  doubled `''`, a doubled backtick, or a `.go:` line-number citation.

  `uptime` before the full run: `load averages: 7.78 6.18 4.57` -- under the
  ~8 threshold, so no timing result here is attributable to load.

  ```
  ISSUE=0439 scripts/check.sh go ./internal/seo/... ./internal/handlers/... ./internal/db/... ./cmd/opencircuit/...
  ```

  `go build`/`go vet` clean, `gofmt -l` clean, all four packages `ok` with
  real elapsed time (`internal/seo` 1.324s, `internal/handlers` 7.677s,
  `internal/db` 6.703s, `cmd/opencircuit` 1.765s), skip audit `(no skips)`,
  `VERIFICATION PASSED`. `check.sh` dropped `opencircuit_test_0439` on exit
  per its normal behavior; recreated it by hand (`scripts/testdb.sh create
  0439`) to name and run the specific functions this issue touches, then
  dropped it again:

  ```
  go test ./internal/handlers/... -run '^(TestNoCommentCitesUnresolvedPathOrSection|TestNoDocCommentNamesADifferentDeclarationInSameFile)$' -v
  --- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.14s)
  --- PASS: TestNoDocCommentNamesADifferentDeclarationInSameFile (0.06s)

  go test ./internal/db/... -run '^TestPRDSectionIndexParity$' -v
  --- PASS: TestPRDSectionIndexParity (0.00s)

  go test ./internal/seo/... -run '^(TestMiddleware_PreservesUpstreamStatus|TestSitemap_ExcludesUnpublishedWorkshops)$' -v
  --- PASS: TestMiddleware_PreservesUpstreamStatus (0.00s)
  --- PASS: TestSitemap_ExcludesUnpublishedWorkshops (0.00s)

  go test ./cmd/opencircuit/... -run '^TestServePostgres_OneSEOSiteFlowsToEveryConsumer$' -v
  --- PASS: TestServePostgres_OneSEOSiteFlowsToEveryConsumer (0.33s)
  ```

  No `scripts/db-reset.sh`, no `scripts/testdb.sh template`. No production
  access. Nothing worth filing found.
