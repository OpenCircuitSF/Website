# 0184 — `sendGuardState`'s branch order is still unpinned at two adjacencies

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | All |
| **Module** | web |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |

## Description

`#0183` pinned one adjacency in `sendGuardState` — `unmet` versus `audienceCount === 0`. `#0183`'s review then swept the **input space** rather than each export's branches, and found the other two are still unpinned:

- swapping `status !== 'draft'` and `inFlight`
- swapping `inFlight` and `unmet`

Both pass the full suite. **This is the same defect as `#0183`'s own criterion 2, one pair higher** — and it sits on `#0047`'s send gate, whose doc comment states the branches "must stay exactly as enumerated."

Three smaller `normalizeCountInput` gaps from the same sweep, all surviving mutations:

- an **ASCII space is accepted as a separator**, though the contract admits only `,` and U+2009
- the **exact `Number.MAX_SAFE_INTEGER`** boundary is unasserted
- a **leading `+`** is accepted

One further survivor — `confirmMatches` with `&& count > 0` — is benign and should be left: `empty-audience` returns first, so the cell is unreachable. Say so rather than "fixing" it.

## Acceptance criteria

- [x] Two cases pin `sendGuardState`'s `status`/`inFlight` and `inFlight`/`unmet` orderings — each must fail when that pair is swapped
- [x] `normalizeCountInput` asserts that an ASCII space is **rejected**, that `9007199254740991` is **accepted**, and that `'+482'` is **rejected** — or, if any of those three is actually desired behaviour rather than a gap, change the contract deliberately and say so
- [x] Every new case is proved by the mutation that currently survives — restore it, watch the new case fail, restore byte-identically (§8a)
- [x] The benign `confirmMatches` survivor is recorded as unreachable rather than papered over
- [x] `scripts/check.sh web` passes

## Root cause

Same shape as `#0183`: five branches/inputs in `web/src/lib/sendConfirm.ts`
that no test could distinguish from their mutated counterpart.

1. `sendGuardState` checks `status !== 'draft'`, then `inFlight`, then
   `unmet.length > 0`, in that order — but no existing case set
   `status !== 'draft'` **and** `inFlight` together, nor `inFlight` **and**
   `unmet` non-empty together, so both adjacent swaps were invisible to the
   suite.
2. `normalizeCountInput` already implements the documented contract
   correctly (separator class is `/[, ]/g` — comma or U+2009 only, not
   ASCII space; digit regex is `/^\d+$/`, no leading sign; the safe-integer
   guard is `Number.isSafeInteger`, which is true exactly at the boundary)
   — but nothing asserted any of the three behaviors, so a mutation at any
   of them passed the whole suite.
3. `confirmMatches`'s hypothetical `&& count > 0` mutation cannot be
   observed at all: `sendGuardState` only ever calls `confirmMatches` after
   its own `audienceCount === 0` check has already returned
   `empty-audience`, so `count` (i.e. `audienceCount`) is always `> 0` by
   the time `confirmMatches` runs from that call site. The cell is dead,
   not undertested.

## Fix

Added five new cases to `web/src/lib/sendConfirm.test.ts`, no production
source changes:

- `sendGuardState`: two cases, each setting the two conditions of one
  adjacency simultaneously (`status: 'sending', inFlight: true` →
  `unavailable`; `inFlight: true, unmet: oneUnmet` → `sending`), pinning
  branch order the same way `#0183`'s `unmet`/`audienceCount` case did.
- `normalizeCountInput`: `'1 234'` (ASCII space, distinct from the existing
  U+2009 case) added to the `rejects` table; `String(Number.MAX_SAFE_INTEGER)`
  added to the `parses` table, asserting the exact boundary is accepted;
  `'+482'` added to the `rejects` table.

**Contract decision: no change.** All three `normalizeCountInput` behaviors
were verified by direct inspection of the compiled regex bytes (the
separator class is genuinely ` `, not an ASCII space — confirmed with
`xxd`) and by evaluating the real function against each input in isolation
before writing any test. The implementation already matches the doc
comment's stated contract in all three cases; only the test coverage was
missing. An admin pasting a spreadsheet count with an ASCII space would get
a "doesn't match" hint rather than silent misacceptance, which is
acceptable — `confirmHint` still names the expected count, so the input
isn't silently swallowed, just not auto-normalized. Changing the contract to
accept ASCII space was considered and rejected: broadening the accepted
separator set is a real behavior change that deserves its own issue and
review, not a side effect of a coverage pass.

**`confirmMatches`'s `&& count > 0` survivor is left alone**, as directed.
Recorded here rather than "fixed": `sendGuardState`'s call to
`confirmMatches(input.confirmRaw, input.audienceCount)` is only reached
after the `audienceCount === 0` branch has already returned, so
`count > 0` always holds at that call site — the mutated cell is
unreachable, not a coverage gap.

## Verification

`uptime` before and after: load average ~1.5–1.9 throughout (§5, well under
the ~8 threshold).

`ISSUE=0184 scripts/check.sh web`: `npm run check` — 358 files, 0 errors,
0 warnings; `npm test` — 21 files, **3,645 passed** (baseline 3,640 + 5 new).
Targeted run (`npx vitest run src/lib/sendConfirm.test.ts --reporter=verbose`)
confirms all five new cases individually: `parses "9007199254740991"`,
`rejects "1 234"`, `rejects "+482"`, `sendGuardState > sending wins over
blocked when inFlight is true and unmet is non-empty`, `sendGuardState >
unavailable wins over sending when status is not draft and inFlight is
true`.

### All five mutations reproduce, each failing exactly its own new case

Baseline `web/src/lib/sendConfirm.ts` md5 `41330f9ecd290d0785aecbad83c0f10d`
(matches `#0183`'s review baseline) — verified unchanged before, and restored
to this checksum after, every probe below. A pristine copy was kept aside in
the scratchpad and restored by `cp`, never a destructive git command (§8a).

| # | mutation | result |
|---|---|---|
| 1 | `sendGuardState`: swap the `status !== 'draft'` and `inFlight` branches | **1 failed** — only *unavailable wins over sending when status is not draft and inFlight is true* |
| 2 | `sendGuardState`: swap the `inFlight` and `unmet.length > 0` branches | **1 failed** — only *sending wins over blocked when inFlight is true and unmet is non-empty* |
| 3 | `normalizeCountInput`: add ASCII space to the separator class (`/[, ]/` → `/[,  ]/`) | **1 failed** — only *rejects "1 234"* |
| 4 | `normalizeCountInput`: narrow the safe-integer guard to `n < Number.MAX_SAFE_INTEGER` (excludes the boundary) | **1 failed** — only *parses "9007199254740991"* |
| 5 | `normalizeCountInput`: allow a leading `+` (`/^\d+$/` → `/^\+?\d+$/`) | **1 failed** — only *rejects "+482"* |

`confirmMatches`'s `&& count > 0` mutation was not restored/re-proved — per
the issue and the acceptance criteria, it is recorded as unreachable, not
exercised.

`pgrep -f 'go test|\.test '` empty at the end; no scratch database created
(this issue only touches `web/`, so `ISSUE=0184 scripts/check.sh web`'s test
database was unused); no `git checkout --` / `restore` / `stash` / `reset`
/ `clean` ran anywhere.

## Files changed

- `web/src/lib/sendConfirm.test.ts` — five new cases (commit `372aae4`)

## Notes

Reported by `#0183`'s phase-3 review under `CLAUDE.md` §9. It was explicit that this is not a bounce: `#0183`'s criterion 5 asked about un-asserted **branches** and was answered honestly; these are un-asserted **inputs**, which an every-export branch sweep structurally cannot find.

That distinction is the durable lesson from this thread. `#0183` existed because `cancelConfirmMessage` was tested with `null` where production sends `undefined` — a question of *which inputs the tests use*. The same review confirmed that class is otherwise clean: `toDatetimeLocalValue` and `isPreviewStale` are already asserted with `undefined`, `null`, and `''`, so `cancelConfirmMessage` was the singular gap.

**This should be the last pass of its kind here.** The parsing three are genuinely low severity; the two `sendGuardState` orderings are the ones worth the trip, because that function is what stands between an admin and a send to the whole list.

## Relation

- Found by: [#0183](0183.md) — Four one-line test gaps
- Relates to: [#0047](0047.md) — the send gate `sendGuardState` implements
- Relates to: [#0179](0179.md) — where `confirmHint`'s coverage started this thread

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 98 | 3,107 | 3,862,629 | 86,825 | $1.53 |

**Total: $1.53**
