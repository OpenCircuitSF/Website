# 0098 — The deferral guard catches wholesale un-deferring, not selective un-deferring

| | |
|---|---|
| **Status** | in-progress |
| **Module** | handlers |
| **Platform** | All |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-19 |

## Description

`#0088` closed a timing oracle by moving every branch-dependent write off the request
path onto an async queue, and `TestSubscribe_MutationDeferredDoesNotBlockResponse`
guards that. The guard blocks `Create` and asserts the response returns while `Create`
is still blocked — which catches any change that un-defers the whole job.

**It does not catch un-deferring one branch.** `#0088`'s second reviewer proved this
rather than speculating. Dispatching only the existing-subscriber branches inline:

```go
if findErr == nil { runMutationSynchronously(job) } else { enqueueMutation(job) }
```

left the **entire package green**. The guard blocks `Create`, which is reached only on
`ErrNotFound` — so it never observes `RestartSignup` or `ClaimAlreadySubscribedSend`.

That shape would put the `active` and `pending` member branches back on the request
path, which is exactly the separation `#0088` exists to enforce, and exactly the pair
whose timings the original oracle used: `already active` at 0.63 ms median against
`brand new` at 1.67 ms.

**This is not a defect in shipped code.** The current single `enqueueMutation` call site
hands over the whole job, and the failing shape requires deliberately adding a
conditional dispatch that a diff reviewer would see. It is a gap in the guard, filed so
the guard's limit is written down rather than assumed away.

## Acceptance criteria

- [ ] A second subtest blocks `RestartSignup` for a **seeded pending subscriber** and
      asserts the response returns while it is still blocked — the existing
      `blockingSubscriberStore` already delegates that method, so this is small
- [ ] Consider the same for `ClaimAlreadySubscribedSend` on an already-active
      subscriber, or state why one member-branch case is sufficient
- [ ] **Mutation-proven with the selective revert above** — apply exactly that
      conditional dispatch, watch the new subtest fail, quote the output, revert by
      file copy
- [ ] The existing wholesale-deferral subtest still passes and still fails under the
      original inline reversion — do not trade one coverage shape for the other

## Notes

From `#0088`'s second review, which approved that issue and recommended this follow-up.

One coupling recorded there and worth preserving here: the guard's 2 s bound must stay
**below `mutateJobTimeout`** (`10 * time.Second`, `internal/handlers/subscribe.go:551`).
Under a synchronous revert, `Create` unblocks when that timeout cancels the job context,
so lowering `mutateJobTimeout` below 2 s would make the test quietly vacuous rather than
failing. 5× margin today. Any new subtest inherits the same constraint.

Measured context, so nobody re-derives it: the guard passes **35/35** under load average
291–307, worst sample 0.40 s against the 2 s bound, clean under `-race`.

## Relation

- Depends on: [#0088](0088.md) — The subscribe endpoint is still a 100%-accurate enumeration oracle by timing
- Related: [#0081](0081.md) — the async send queue whose `blockingMailer` this guard mirrors

## Implementation notes

Generalized `blockingSubscriberStore` (`internal/handlers/subscribe_test.go`)
from a Create-only blocker to one that blocks a caller-chosen method, via a
new `which string` field and a shared `awaitReleaseOrCancel` helper. Every
other method still delegates straight to `inner`, unchanged.
`newBlockingSubscriberStore` (unqualified) still means "block Create," so no
existing call site needed to change; a new `newBlockingSubscriberStoreOn`
takes the method name explicitly.

Restructured `TestSubscribe_MutationDeferredDoesNotBlockResponse` from one
case into a table of three subtests, one per store method
`processMutateJob`'s dispatch can reach:

- **brand new signup (Create)** — the original case, unseeded, `findErr`
  is `ErrNotFound`.
- **unsubscribed restart (RestartSignup)** — seeds a `StatusUnsubscribed`
  row via the existing `seedSubscriberRow` helper (bounced routes through
  the identical `restartSignup` path per `existingSignup`'s switch, so one
  case covers both — the acceptance criteria's "or state why one
  member-branch case is sufficient" for THIS specific pair; bounced was not
  additionally covered since it is provably the same code path, not a
  judgment call).
- **already active resend (ClaimAlreadySubscribedSend)** — seeds a
  `StatusActive` row, covering the acceptance criteria's second item (not
  skipped — both were cheap given the generalized blocking store).

Each subtest constructs its own `blockingSubscriberStore` blocking only its
one target method, drives the request directly over `ServeHTTP` (not
`doSubscribe`, which would wait for the blocked call to finish), and asserts
the response returns within the same 2s hang-guard the original test used,
then that the blocked method really was entered.

## Fix

No production code changed — `internal/handlers/subscribe.go` is
byte-identical to before this issue (confirmed by `shasum -a 256` after the
mutation-test revert below). This issue is purely a test-coverage gap in the
existing `#0088` guard, as its own Description states ("not a defect in
shipped code").

## Verification

`uptime` first: load average 2.00–2.50 across the session — idle, nothing
below is a load artifact.

- `go build ./...` — clean.
- `go test ./internal/handlers/... -run TestSubscribe_MutationDeferredDoesNotBlockResponse -v` —
  **3/3 subtests passed** against the real (unmutated) `subscribe.go`.
- `go test ./internal/handlers/... -p 2` — full package **ok** (2.7–2.9s),
  no regressions from the `blockingSubscriberStore` generalization.
- `go test ./internal/handlers/... -run 'TestSubscribe_MutationDeferredDoesNotBlockResponse|TestSubscribe_AsyncSendDoesNotBlockResponse' -race -v` —
  clean under `-race`.
- `ISSUE=0098 scripts/check.sh go ./internal/... ./cmd/...` — **passed**: 16
  packages `ok`, skip audit `(no skips)`, `gofmt -l` clean.

**Mutation-proven, exactly as the acceptance criteria specify (criterion 3),
by editing `internal/handlers/subscribe.go` directly and reverting by file
copy per CLAUDE.md §8a** (backed up with `cp` to the scratchpad before
touching the file, never `git stash`):

1. Applied the selective revert from the issue's own illustrative code —
   `Subscribe`'s single `h.enqueueMutation(job)` call site replaced with:
   ```go
   if findErr == nil {
       h.mutateWG.Add(1)
       h.processMutateJob(job)
   } else {
       h.enqueueMutation(job)
   }
   ```
   Ran the three subtests. Output:
   ```
   --- PASS: .../brand_new_signup_(Create) (0.01s)
   --- FAIL: .../unsubscribed_restart_(RestartSignup) (2.01s)
       subscribe_test.go:1845: Subscribe did not return within 2s while
       RestartSignup is still blocked — processMutateJob appears to be
       running on the request goroutine, which would reopen #0088's timing
       oracle
   --- FAIL: .../already_active_resend_(ClaimAlreadySubscribedSend) (2.00s)
       subscribe_test.go:1845: Subscribe did not return within 2s while
       ClaimAlreadySubscribedSend is still blocked — ...
   ```
   Confirms the exact gap the issue describes: the pre-existing Create
   subtest alone would NOT have caught this defect (it kept passing,
   since new signups still take the `findErr != nil` async branch), and
   the two new subtests do catch it.
2. **Criterion 4, checked as an extra step**: also tried the *original*
   wholesale revert (`processMutateJob` called synchronously,
   unconditionally, for every job — no `if`) and confirmed all three
   subtests fail, including the pre-existing Create one:
   ```
   --- FAIL: .../brand_new_signup_(Create) (2.00s)
   --- FAIL: .../unsubscribed_restart_(RestartSignup) (2.01s)
   --- FAIL: .../already_active_resend_(ClaimAlreadySubscribedSend) (2.00s)
   ```
   Confirms the widened test did not trade away the coverage shape the
   original guard already had.
3. Restored `subscribe.go` from the `cp` backup taken before step 1 and
   confirmed byte-identical: `shasum -a 256` on both files produced the
   same digest
   (`fadd4d2230a1c99017a2729524bc9891e09eb7ab4c8fa30b0665c9eb018cac5b`).
   `go build ./...` and the full `internal/handlers` suite passed again
   afterward.

## Files changed

- `internal/handlers/subscribe_test.go` — generalized
  `blockingSubscriberStore` to block a caller-chosen method; restructured
  `TestSubscribe_MutationDeferredDoesNotBlockResponse` into the
  three-subtest table described above. No other file changed.

## Gotchas

- The mutation edits to `subscribe.go` were never committed and never left
  uncommitted between steps — each was applied, tested, and reverted before
  moving to the next, so `git status` on that file was clean at every point
  a git operation could have observed it.
- The dangling-test-citation guard (`internal/handlers/dangling_test_citation_guard_test.go`,
  #0190/#0192/#0196) caught a first-draft doc comment that forward-referenced
  a test function name I hadn't written (I'd planned a permanent
  `..._CaughtBySelectiveRevertMutation` test, then decided the mutation proof
  belongs in this Verification section instead, as a one-time proof rather
  than a permanent test — a mutation test that stays in the tree pointing at
  already-reverted code doesn't guard anything ongoing). Fixed by rewording
  the comment to reference this file's `## Verification` instead of a
  nonexistent function; worth recording since the guard caught it
  immediately and correctly.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Token counts not available in this environment (no subagent transcript file
to sum); the orchestrator should fill this row in from its own accounting
per `issues/Issues.md`'s "Getting exact token counts".
