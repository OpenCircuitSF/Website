# 0076 — Two email-copy defects the template re-theme left behind

| | |
|---|---|
| **Status** | resolved |
| **Module** | mailing / auth |
| **Platform** | All |
| **Phase** | 3 — Mailing list capture |
| **First seen** | 2026-08-19 |
| **Commit** | a2387a1, 3876382 |
| **Closed** | 2026-08-19 |

## Description

Two small, real defects found by `#0028`'s review. Neither blocked that issue; neither has a natural owner among the remaining Phase 3 work.

> 🟢 Resolved 2026-08-19 — The security notice's "Sign in" button and its copy-this-link fallback now both point at `https://www.opencircuitsf.com/login`, and the reviewer confirmed that destination the way a recipient would: built the SPA, served the embedded binary on an unlikely port, proved ownership of the listener by PID *and* by a build-unique marker echoed back in the response, and rendered the page headless — `GET /login` returns **200** and paints the real Login view (heading "Open Circuit SF", tagline "Sign in with your passkey", the `autocomplete="username webauthn"` email field, a green **Sign in** submit, plus Register and Recover affordances), while an unmatched path still 404s, so the 200 is `main.go`'s deliberate SPA fallback rather than a catch-all that answers anything. The one error on that page — an `internal server error` under the Sign in button — is `devstore: passkey login not available (use dev auth bypass)` from `GET /auth/login/start`, a `STORAGE=json` limitation of the dev store and not a property of the shipped route. Both fixtures were read end to end as the recipient rather than diffed: the `.txt` reads naturally from `$ opencircuit/security` through "Click the **link** below" to `Sign in: https://www.opencircuitsf.com/login` and the lost-device and "if you did not do this" lines, with no unsubscribe footer and no physical address; the `.html` contains exactly two `href`s and both are the `/login` URL, matching the visible fallback text, so the button and the "copy this link into your browser" line can no longer disagree. All three new or strengthened mutations reproduce — a `{{cta}}` planted in `BuildConfirmationEmail`'s `Subject` trips `TestNoRenderedBodyContainsCTAToken` on both `Subject` and `HTMLBody`; flipping `sessions_revoked`'s `ShowListFooter` trips the no-list-footer test; and, most usefully, replacing the registration CTA sentence with prose while **leaving** the "If the button above doesn't work…" and "This link expires in 5 minutes." boilerplate that made the old assertion vacuous fails `TestHTMLAndTextCTAWordingDiffer` on both parts while `TestNoTextBodyContainsButton` still passes — the two tests are demonstrably not redundant. Every emailContent field that can reach a recipient (`Preheader`, `Eyebrow`, `Heading`, `ButtonText`, `ButtonURL`, the footer URLs) funnels through `renderHTML`/`renderText` into the three fields the property test checks, and no `Build*` sets `Headers`, so there is no live `{{cta}}` escape route today. `#0008`'s `NoOpMailer` verification line is byte-identical in-tree and in a fresh clone at `3876382` (`3876382` touches `internal/auth/mailer.go` in comments only), that clone reports `CRLF line terminators` for all five `.txt` fixtures with every fixture byte-identical to the working tree, and the corrected `## Gotchas` scope note now states the facts accurately and schedules no follow-up that can never fire. `go build ./...`, `go vet ./...`, `go test ./internal/mailing/... ./internal/auth/... -count=1` (84 passes, **zero** skips, zero failures) and a full `go test ./... -count=1` are all green in isolation, and `3876382` touches only its six files.

## Actual behavior

**1. The plain-text registration and recovery emails tell the reader to click a button that does not exist there.** The shared renderer emits "Click the button below…" into both the HTML and text parts. The HTML has a button; the text part has only `Verify email: https://…`. The originals said "click the **link** below", so the re-theme in `#0028` regressed the copy specifically for text readers — the audience `CLAUDE.md` and PRD §6.6 care about, since a text alternative is not optional and "some recipients read mail that way".

**2. `SendSessionsRevoked` is still text-only and un-themed.** `#0028` re-themed three of the four `internal/auth` mails into HTML+text pairs and deliberately scoped this one out. That was a defensible line for that issue, but it leaves one security email visibly unlike its siblings — the same account holder receives two differently-branded messages from the same system.

## Acceptance criteria

- [x] The text part of every template refers to a **link**, not a button; the HTML part may still say button. Prove the two parts can differ, so a future edit cannot re-couple them
- [x] A test asserts no text body contains the word "button" — mutation-proven by reintroducing it
- [x] `SendSessionsRevoked` renders an HTML + text pair consistent with the other three, or a written decision in `## Gotchas` explaining why it stays plain text
- [x] Golden `testdata` files regenerated and **read as a recipient**, not merely diffed
- [x] `go build ./...`, `go vet ./...`, `go test ./... -count=1` green with `TEST_DATABASE_URL` exported and zero skips

## Notes

`#0028`'s reviewer read the golden files as a recipient rather than diffing them, which is the only reason the button/link mismatch surfaced — every assertion about those files passed. Worth repeating that habit here: golden files lock in whatever was rendered, including mistakes.

Do not add `List-Unsubscribe` headers to any of these. They are transactional, not campaign mail, and the published privacy policy is narrowed to "every campaign email" precisely because of that distinction.

## Carried in from `#0078`'s review (2026-08-19)

- [x] **Assert the CRLF contract in Go, not only through git configuration.**
      `#0078` fixed the fixtures, but the reviewer established that those four
      golden files are now the **only** assertion of the RFC 5322 `\r\n` contract
      in the entire repo — `grep -rn '\r' internal/mailing/*_test.go
      internal/auth/*_test.go` returns nothing, and `ses_mailer.go` hands
      `TextBody` straight to SES without re-adding CR.

      So the contract rests on a `.gitattributes` line. A one-line
      `strings.HasSuffix(msg.TextBody, "\r\n")` assertion (or equivalent) would
      make a `renderText()` regression fail in Go, where the failure is legible,
      rather than only via a checkout on someone else's machine.

      **Done.** See `## Fix` item 3 and `TestAllTemplates_TextBodyUsesCRLF` in
      `internal/mailing/templates_test.go`.

## Fix

**1. Text/HTML can now differ on the one word that must differ.** The four
`Build*Email` functions used to write the literal word "button" straight into
`IntroParagraphs` entries — copy shared verbatim by both `renderHTML` and
`renderText` (that sharing is the whole point of `emailContent`: one place
decides what the copy says). Because the string was identical, the two
renderers could never independently say different things about it, which is
exactly how the text part ended up telling readers to click a button it
never renders.

The fix is a literal placeholder, `{{cta}}` (`ctaToken` in
`internal/mailing/templates.go`), that authors put in paragraph copy wherever
the sentence needs to name the primary-action affordance.
`resolveCTA(s, word)` substitutes it: `renderHTML`'s two paragraph loops
resolve it to `"button"`, `renderText`'s two paragraph loops resolve it to
`"link"`. `BuildRegistrationEmail` and `BuildRecoveryEmail` now write "Click
the `{{cta}}` below to…" instead of the hard-coded word. This isn't just a
one-off string fix — the mechanism makes the two parts *structurally able* to
differ, and a future paragraph that reuses `{{cta}}` gets the correct word in
both formats for free, with no way to accidentally recouple them short of
someone hard-coding "button" into a paragraph directly (which
`TestNoTextBodyContainsButton` below catches).

**2. `SendSessionsRevoked` is now an HTML+text pair.** Added
`mailing.BuildSessionsRevokedEmail(to, baseURL string, at time.Time) Message`
in `internal/mailing/transactional_templates.go`, built from the same
`emailContent`/`renderHTML`/`renderText` machinery as the other four
templates — terminal-styled card, `$ opencircuit/security` eyebrow, a "Sign
in" button/link pointing at `baseURL` (there's no token or magic link here,
so the CTA just points at the site itself), and the same three paragraphs of
copy the old plain-text version had, reusing the `{{cta}}` mechanism from
fix 1 for its own "Click the button/link below to sign in…" sentence.
`ShowListFooter` stays `false`, matching registration/recovery: this is
account security email, not mailing-list email, so no manage/unsubscribe
footer or physical address. `internal/auth/ses_mailer.go`'s
`SendSessionsRevoked` now just calls the new builder and hands the result to
`sendMessage`; the old hand-rolled `fmt.Sprintf` + private `send` helper are
gone.

**3. The CRLF contract now has a Go assertion.** Added
`TestAllTemplates_TextBodyUsesCRLF` to `templates_test.go`: for every
built message it asserts `TextBody` ends with `"\r\n"` and that stripping
every `"\r\n"` pair leaves no bare `"\n"` behind (i.e. every line break is
`\r\n`, not just the last one). This runs in Go on every `go test`, so a
`renderText()` regression fails with a legible Go test failure instead of
surfacing only as a `.gitattributes`-dependent diff on someone else's
checkout.

## Fix — re-implementation after the 2026-08-19 review bounce (commit `3876382`)

**4. `SendSessionsRevoked`'s CTA now points at `/login`, not `baseURL`.**
`BuildSessionsRevokedEmail`'s `ButtonURL` is now `baseURL + "/login"` — the
same construction `BuildRegistrationEmail`/`BuildRecoveryEmail` use for their
own links. `/login` is a real, existing route (PRD §5.1;
`web/src/lib/router.ts:69` → `web/src/views/Login.svelte`) and is already the
canonical post-passkey landing page: both `RegisterVerify.svelte:130` and
`RecoverVerify.svelte:130` `navigate('/login')` after enrollment. The
original `## Gotchas` scope note claiming there was no sign-in path "in this
codebase as of this issue" was factually wrong — see the corrected note
below — and has been removed.

**5. `TestNoRenderedBodyContainsCTAToken`** (new, `templates_test.go`)
asserts none of the five templates' `Subject`, `HTMLBody`, or `TextBody`
contains the literal `ctaToken` (`"{{cta}}"`). This guards the review's
finding that `resolveCTA` only walks `IntroParagraphs`/`NoteParagraphs` — a
`{{cta}}` written into any other `emailContent` field (as the reviewer's
probe did, into `Subject`/`Preheader`/`Eyebrow`/`Heading`/`ButtonText`/
`ButtonURL`) ships the raw token verbatim, undercutting fix 1's "cannot
recouple by accident" claim. No template does this today, so the test is a
property guard against a future one doing so, not a fix to `resolveCTA`
itself (there is nothing for `resolveCTA` to resolve in a `Subject` or
`ButtonText` — none of the five templates need `{{cta}}` there).

**6. `TestHTMLAndTextCTAWordingDiffer` now asserts the resolved sentence,
not a bare substring.** It previously checked `HTMLBody` for `"button"` and
`TextBody` for `"link"` as independent substrings, which passed for
`registration`/`recovery` regardless of whether `resolveCTA` ran at all: the
unconditional "If the button above doesn't work…" fallback line satisfies
the HTML check, and "This link expires in N minutes." satisfies the text
check, for every template with a `ButtonURL` and a TTL. It now asserts the
actual resolved sentence — `"Click the button below"` in HTML,
`"Click the link below"` in text — which only exists if `resolveCTA` ran
over the paragraph that contains it.

**7. `TestRegistrationAndRecovery_NoListFooter` now covers
`sessions_revoked`.** Added to the same name list as `registration` and
`recovery`; the "no unsubscribe link on a security notice" property was
previously golden-locked only for this template.

**8. `testdata/README.md`** now says "the five golden tests" (was "four").

**9. `internal/auth/mailer.go`'s `Mailer` interface comment** no longer
calls `SendSessionsRevoked` "a plain notification" — reworded to describe
what stays true (no credentials, no single-use token) and note explicitly
that the sentence is about payload, not formatting, now that the method is
themed. `NoOpMailer`'s log lines were **not** touched (see the `#0008` check
in `## Verification` below).

## Verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
go build ./...                                                    # clean
go vet ./...                                                      # clean
go test ./internal/mailing/... ./internal/auth/... -count=1 -v    # all PASS, ok
```

Both packages in this issue's lane report `ok` (not `[no test files]` or a
skip notice): `internal/mailing` in ~0.2s, `internal/auth` in ~1.1–1.3s
(`internal/auth` is DB-backed — non-zero, real time against Postgres).

**`go test ./... -count=1`** (full repo, run from the working tree) also
came back clean for every package except `cmd/opencircuit` and
`internal/handlers`, which failed on timing (`context deadline exceeded`,
`did not return within 2s`) — both packages are explicitly out of this
issue's lane (owned by the concurrent `#0081` subagent this session, per
`CLAUDE.md` §8b's concurrent-agent caution) and neither imports anything this
issue changed. `internal/mailing` and `internal/auth` were unaffected in that
same run.

**Mutation proofs** (each applied to a scratch copy of
`internal/mailing/templates.go`, verified to fail, then reverted byte-for-byte
before re-running the full suite green):

- Changed `renderText`'s `resolveCTA(p, "link")` call (for `IntroParagraphs`)
  back to `resolveCTA(p, "button")` → `TestNoTextBodyContainsButton` failed on
  3 of 5 templates (`registration`, `recovery`, `sessions_revoked` — the only
  three that use `{{cta}}`), printing the full offending text body.
- Changed `renderText`'s `strings.Join(lines, "\r\n") + "\r\n"` to
  `strings.Join(lines, "\n") + "\n"` → `TestAllTemplates_TextBodyUsesCRLF`
  failed on all 5 templates ("TextBody does not end with \r\n"); the four
  pre-existing golden tests also failed on the same mutation, so the CRLF
  contract is now backstopped twice.

**Golden files — read as a recipient.** Regenerated all five templates'
`testdata/*.{html,txt}` with `UPDATE_GOLDEN=1` and read every `.txt` file
top to bottom as the person receiving it, not just diffed:

- `registration.txt` / `recovery.txt` now read "Click the **link** below to
  verify your email…" / "…register a new passkey." followed by
  `Verify email: https://…` / `Recover account: https://…` — the instruction
  now matches what's actually on the page.
- `sessions_revoked.txt` (new) reads naturally end to end: who was signed
  out and when, "Click the link below to sign in with your existing
  passkey…" followed by `Sign in: https://www.opencircuitsf.com`, the
  lost-device follow-up, and the "if you did not do this" line — nothing
  about it reads like a template artifact.
- `confirmation.txt` / `already_subscribed.txt` were untouched by this
  issue's changes (they don't use `{{cta}}` — their copy already said "Use
  the link below") and read the same as before.
- Read the matching `.html` files too: `registration.html` / `recovery.html`
  / `sessions_revoked.html` correctly still say "button" ("Click the
  **button** below to…", `<a>…Sign in</a>`), so the split is real — HTML
  readers see a button because there is one, text readers see "link" because
  there isn't.

**Fresh-clone check** (the `#0078` failure mode: fixtures that only pass on
the authoring machine). `git clone` of this working tree into a scratch
directory, confirmed all five `internal/mailing/testdata/*.txt` files report
`CRLF line terminators` via `file`, then ran
`go test ./internal/mailing/... ./internal/auth/... -count=1 -v` inside the
clone: all tests passed, including the new golden test and both new
mutation-proof tests. Scratch clone removed afterward.

**`NoOpMailer` / `#0008` byte-identity check.** `internal/auth/mailer.go`
(the `NoOpMailer` type) was not touched by this issue — only
`internal/auth/ses_mailer.go` and the `mailing` package changed. Ran
`TestNoOpMailer` and confirmed the logged line is unchanged:
`NoOpMailer: verification email to a@example.com: https://opencircuitsf.com/register/verify?token=t`,
which matches the format `issues/0008.md` documents verbatim
(`NoOpMailer: verification email to <email>: http://localhost:8080/register/verify?token=<token>`).
`SendSessionsRevoked`'s log line changed in wording only
(`NoOpMailer: sessions-revoked notice to %s at %s`, itself untouched by this
issue) — `#0008`'s manual workflow never reads that line, only the
verification-email line, so this is not a regression against it.

### Re-verification after the bounce (commit `3876382`)

- **`go build ./...`, `go vet ./...`** — both clean.
- **`go test ./internal/mailing/... ./internal/auth/... -count=1 -v`** with
  `TEST_DATABASE_URL` exported — both `ok`, zero skips, zero failures
  (`internal/mailing` ~0.17–0.27s, `internal/auth` ~1.05–1.13s).
- **`go test ./... -count=1`** — every package `ok`, including
  `cmd/opencircuit` and `internal/handlers` (the packages `#0084` flags as
  flaky under load; they were green this run).

**Regenerated `sessions_revoked` fixtures, read as a recipient.** Ran
`UPDATE_GOLDEN=1 go test ./internal/mailing/... -run
TestBuildSessionsRevokedEmail_Golden`; the diff against the prior fixtures is
exactly the `/login` path added to the button `href`, the fallback-link
`href` and its visible text, and the `Sign in:` line in the `.txt` — nothing
else moved. Read `sessions_revoked.txt` top to bottom as the recipient:

```
$ opencircuit/security

All sessions signed out

All sessions for alice@example.com were signed out on Thu, 06 Aug 2026 15:04:05 +0000.

Click the link below to sign in with your existing passkey — it still works and nothing about your account has changed.

Sign in: https://www.opencircuitsf.com/login

If you did this because a device was lost or is no longer yours, also open Account settings after signing in and revoke that device's passkey. You can enroll a replacement from the same screen.
If you did not do this, sign in and revoke your passkeys immediately.

--
Open Circuit SF - opencircuitsf.com
```

The "Sign in" link now goes to an actual sign-in page — the promise and the
destination match. Considered also linking "Account settings" in the
lost-device paragraph to `/account`, but `NoteParagraphs` are plain strings
rendered through `html.EscapeString` in both formats — there's no per-word
markup hook to hang a link on without a bigger change to `emailContent`, and
the reviewer flagged it as a "consider," not a blocker; left as prose.

**Mutation proofs** (each applied to a scratch copy, verified to fail, then
reverted byte-for-byte via `diff` against a saved original before re-running
green):

- Added `{{cta}}` to `BuildConfirmationEmail`'s `Subject` (a field
  `resolveCTA` never touches) → `TestNoRenderedBodyContainsCTAToken` failed,
  naming both the leaked field and the literal token:
  `confirmation: HTMLBody contains unresolved "{{cta}}"` and
  `confirmation: Subject contains unresolved "{{cta}}"`.
- Replaced `BuildRegistrationEmail`'s `{{cta}}` paragraph with prose that
  contains no CTA sentence at all (while leaving the "expires in"/fallback
  lines that made the old test pass regardless) →
  `TestHTMLAndTextCTAWordingDiffer` failed on both the HTML and text
  assertions for `registration`; `TestNoTextBodyContainsButton` still
  passed, confirming the new test catches something the sibling test does
  not.
- Flipped `BuildSessionsRevokedEmail`'s `ShowListFooter` to `true` →
  `TestRegistrationAndRecovery_NoListFooter` failed:
  `sessions_revoked: HTML unexpectedly carries the mailing-list footer`.
- Re-ran the `#0078`/`a2387a1` CRLF mutation (`renderText`'s
  `strings.Join(lines, "\r\n")` → `"\n"`) — still fails twice over: all five
  `TestBuild*Email_Golden` tests and `TestAllTemplates_TextBodyUsesCRLF`.

**`#0008` / `NoOpMailer` byte-identity check.** `internal/auth/mailer.go` was
touched this pass (the interface *comment* only — see fix 9), so re-checked
`TestNoOpMailer`'s logged lines directly rather than assuming untouched:
`NoOpMailer: verification email to a@example.com: https://opencircuitsf.com/register/verify?token=t`
is byte-identical to the format `issues/0008.md` documents. The
`SendSessionsRevoked` log line/method body is unchanged (only the doc
comment above the interface method changed); `#0008`'s manual workflow never
reads that line, only the verification-email line.

**Fresh-clone check.** `git clone` of this tree at `3876382` into a scratch
directory: `file` reports `CRLF line terminators` for all five
`internal/mailing/testdata/*.txt` files, and
`go test ./internal/mailing/... ./internal/auth/... -count=1 -v` passed
completely inside the clone, including `TestNoOpMailer`'s log line. Scratch
clone removed afterward.

**Scope.** `3876382` touches six files: `internal/auth/mailer.go`,
`internal/mailing/templates_test.go`, `internal/mailing/testdata/README.md`,
`internal/mailing/testdata/sessions_revoked.{html,txt}`, and
`internal/mailing/transactional_templates.go`. Nothing in
`internal/handlers/`, `cmd/opencircuit/`, `issues/0081.md`, or
`issues/0026.md` — those stayed in the concurrent `#0081` subagent's lane per
`CLAUDE.md` §8b.

## Files changed

- `internal/mailing/templates.go` — added `ctaToken`/`resolveCTA`;
  `renderHTML`'s and `renderText`'s `IntroParagraphs`/`NoteParagraphs` loops
  now resolve `{{cta}}` to `"button"` / `"link"` respectively.
- `internal/mailing/transactional_templates.go` — `BuildRegistrationEmail`
  and `BuildRecoveryEmail` now write `{{cta}}` instead of the literal word
  "button"; added `BuildSessionsRevokedEmail`.
- `internal/mailing/templates_test.go` — renamed `allFourMessages` to
  `allMessages` (now returns five), added `TestBuildSessionsRevokedEmail_Golden`,
  `TestNoTextBodyContainsButton`, `TestHTMLAndTextCTAWordingDiffer`,
  `TestAllTemplates_TextBodyUsesCRLF`, and `testRevokedAt`.
- `internal/mailing/testdata/registration.txt`,
  `internal/mailing/testdata/recovery.txt` — regenerated ("button" → "link").
- `internal/mailing/testdata/sessions_revoked.html`,
  `internal/mailing/testdata/sessions_revoked.txt` — new golden fixtures.
- `internal/auth/ses_mailer.go` — `SendSessionsRevoked` now builds via
  `mailing.BuildSessionsRevokedEmail` and calls `sendMessage` directly; the
  hand-rolled `fmt.Sprintf` text and the private `send` helper were removed.
- `internal/auth/ses_mailer_test.go` —
  `TestSESMailer_SendSessionsRevoked_InjectedSender` now also asserts
  `HTMLBody` is non-empty and contains "existing passkey".

(`internal/mailing/testdata/confirmation.{html,txt}` and
`already_subscribed.{html,txt}` were regenerated byte-identical — no diff —
since neither template uses `{{cta}}`.)

### Files changed — re-implementation (commit `3876382`)

- `internal/mailing/transactional_templates.go` — `BuildSessionsRevokedEmail`'s
  `ButtonURL` is now `baseURL + "/login"` (was `baseURL`); updated the
  function's doc comment accordingly.
- `internal/mailing/templates_test.go` — `TestHTMLAndTextCTAWordingDiffer`
  now asserts the resolved CTA sentence instead of a bare substring; added
  `TestNoRenderedBodyContainsCTAToken`; added `"sessions_revoked"` to
  `TestRegistrationAndRecovery_NoListFooter`'s name list.
- `internal/mailing/testdata/sessions_revoked.html`,
  `internal/mailing/testdata/sessions_revoked.txt` — regenerated
  (`baseURL` → `baseURL + "/login"` in the button href, fallback link, and
  `Sign in:` line; nothing else changed).
- `internal/mailing/testdata/README.md` — "four golden tests" → "five".
- `internal/auth/mailer.go` — reworded the `Mailer` interface's
  `SendSessionsRevoked` doc comment away from "a plain notification" (stale
  next to a themed HTML+text pair); `NoOpMailer` itself, including its log
  lines, is unchanged.

## Gotchas

**Decision: `SendSessionsRevoked` was themed, not left plain.** Both options
in the acceptance criteria were defensible; themed it because leaving it
plain no longer had a good reason once looked at closely. The email is one of
four things this account-holder-facing surface sends, and the other three
already look like Open Circuit SF (`$ opencircuit/…` eyebrow, terminal card,
green accent). A recipient who gets a themed confirmation, a themed recovery
link, and then a bare-text security notice has a real reason to wonder if the
plain one is legitimate — inconsistent branding is itself a phishing
tell, and this message concerns an actual "did you do this?" security event,
where trust signals matter more, not less, than on the other three. `#0028`
scoping it out was reasonable for that issue (it wasn't one of the four
templates in scope, and adding a fifth mid-review would have been scope
creep); revisiting it here, with the `{{cta}}` mechanism from fix 1 already
in hand, cost one more `Build*` function and one golden fixture pair. The
counter-argument — "it's a bare notification, terminal styling adds nothing
functional" — is real but weaker: `ShowListFooter` stays `false` and the
copy is otherwise unchanged, so nothing about the *content* changed, only
whether it looks like it came from the same sender as everything else.

**Why `{{cta}}` and not, say, a `CTALabel` struct field per format.** A
tempting alternative was giving `emailContent` two separate fields
(`ButtonTextHTML`/`ButtonTextText` or similar) so each `Build*` function
states both words explicitly. Rejected: that pushes the "don't say button in
text" discipline back onto every future caller's judgment — exactly the
failure mode that produced this issue in the first place, since #0028's
authors presumably also "knew" text shouldn't say button and wrote it anyway
under time pressure. A shared token with a hard-coded substitution table
(one word per format, defined once in `templates.go`) makes the two renderers
differ *by construction* for every paragraph that opts in, with no per-call
decision to get wrong.

**Scope note — corrected 2026-08-19 after the review bounce.** This
originally claimed `SendSessionsRevoked`'s CTA pointed at bare `baseURL`
because "there wasn't [a `/login` or `/account` path] to route to in this
codebase as of this issue." **That was factually wrong**, and the reviewer
caught it: `/login` already existed (PRD §5.1; `web/src/lib/router.ts:69` →
`web/src/views/Login.svelte`), `cmd/opencircuit/main.go`'s SPA-fallback
serves it exactly like `/register/verify` (a path this same package already
emails), and it was already the canonical post-passkey landing page —
`RegisterVerify.svelte:130` and `RecoverVerify.svelte:130` both
`navigate('/login')` after enrollment. As shipped in `a2387a1`, the email
said "Click the link below to sign in with your existing passkey," labelled
the button **Sign in**, and landed the reader on the marketing homepage —
hero, workshop list, subscribe form — with no visible sign-in affordance
except a footer link. That is the same species of defect this whole issue
was filed to fix (copy promising an affordance the destination doesn't
provide), and worse here: this is the one message where the recipient is
already asking "was this me?", and a vague destination is precisely the
shape of a phishing lure, undercutting the trust-signal argument the theming
decision above rests on.

Fixed in `3876382`: `BuildSessionsRevokedEmail`'s `ButtonURL` is now
`baseURL + "/login"`, built the same way
`BuildRegistrationEmail`/`BuildRecoveryEmail` build theirs. There is no
"if a dedicated sign-in path is added later" follow-up to leave behind —
the path already exists and always did.

## Relation

- Depends on: [#0028](0028.md) — Write the transactional email templates

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-08-19 | implement | claude-sonnet-5 | 151,034 | — |
| 2026-08-19 | review (bounced) | claude-opus-5 | 108,870 | — |
| 2026-08-19 | implement (2nd) | claude-sonnet-5 | 111,948 | — |
| 2026-08-19 | review | claude-opus-5 | 121,053 | — |

One bounce: the security-notice CTA pointed at the marketing homepage while
`/login` existed and was live, on the one message whose recipient is asking
"was this me?". Harness reports aggregate subagent totals only.
