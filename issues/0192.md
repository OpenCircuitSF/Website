# 0192 — The discriminator's second moved row is still unguarded, and a comment cites a test that does not exist

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | mailing |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |

## Description

`#0190` closed the gap where reverting `errors.As(err, &opErr)` to
`strings.HasPrefix` left the whole package green. Its phase-3 review confirmed
the mutation now turns exactly one subtest red, on both the returned string and
the log assertion. It then measured something the issue's own criteria did not
ask for.

**1. The delta is two rows wide; the tree guards one.** The reviewer rebuilt all
21 of `#0188`'s probes independently and re-ran the mutation: **probes 07 and 21
both fail**. The tree covers 07. Every `OperationError` row in the committed
table carries `sesWrapPrefix`, so none can distinguish the two implementations
on row 21 — a **bare, unprefixed `*smithy.OperationError`**.

The measured consequence: the partial mutation
`errors.As(…) && strings.HasPrefix(…)` leaves **the entire package green**. That
shape is exactly what a well-meaning "belt and braces" edit looks like, and
nothing would catch it.

This is not `#0190` failing — criterion 1 asked for "at least one test red", and
criteria 2–3 named precisely the two rows the implementer built. The gap was in
the criteria I wrote. Row 21 is the case the discriminator change was *made
for*: a non-`SESMailer` mailer returning a bare `*smithy.OperationError` now
classifies correctly, and that improvement is asserted by nothing.

This is the **fifth** appearance of the unguarded-neighbour shape in this
subsystem — `#0171`, `#0182`, `#0185`, `#0190`.

**2. A new comment cites a test that does not exist.** The row added by `#0190`
carries a comment claiming "this row and `TestWorker_TransportDiscriminatorMutationGuard`
below are the only things in the tree that would catch it." `grep` finds that
name only inside the comment. It overstates coverage by exactly the amount item
1 is short — and it is the same comment-claims-what-the-code-does-not-build
defect `#0190` was filed to fix, reintroduced one block away.

## Acceptance criteria

- [ ] A table row builds a bare, unprefixed `*smithy.OperationError` — want `adminSendUnreachableMessage`, `wantLog: false`
- [ ] The **partial** mutation `errors.As(…) && strings.HasPrefix(…)` turns at least one test red, demonstrated by mutation, not by inspection
- [ ] The full mutation (`errors.As` → `strings.HasPrefix`) still turns a test red
- [ ] The stale comment either names a test that exists or stops naming one; no comment in the file claims coverage the file does not provide
- [ ] `#0188`'s 21 probes still land where established
- [ ] `ISSUE=0192 scripts/check.sh go ./internal/...` passes with zero skips

## Implementation notes

Scope held to `internal/mailing/worker_test.go` only; `worker.go` was touched
only transiently, for the two mutations below, and restored (verified
byte-identical, `260f72deb85582982cf638500d280e519e6357751d76566ec039cc59e8b8ddaf`,
matching `#0190`'s own recorded baseline).

- **New row.** Added to `TestAdminSendErrorMessage_DiscriminatesPrefixedFromOwnErrors`:
  a bare, **unprefixed** `*smithy.OperationError` (no `sesWrapPrefix` at all),
  `want: adminSendUnreachableMessage`, `wantLog: false`. This is `#0188`'s
  headline "improved" row — a non-`SESMailer` `Mailer`'s transport failure,
  which the old `strings.HasPrefix` discriminator could never classify
  correctly (no prefix, so it always fell through to the catch-all) and the
  new `errors.As` discriminator now does.
- **Partial-mutation proof.** Took a copy of `worker.go` (§8a), changed the
  transport branch to
  `errors.As(err, &opErr) && strings.HasPrefix(err.Error(), sesWrapPrefix)`,
  and ran the package. Before this issue's row existed, `#0190`'s review
  measured that shape leaving the entire package green. With the new row in
  place, exactly one test failed —
  `TestAdminSendErrorMessage_DiscriminatesPrefixedFromOwnErrors`, exactly one
  subtest — `bare,_unprefixed_*smithy.OperationError:_no_sesWrapPrefix_at_all_—_the_case_the_type-based_discriminator_was_made_for`
  — on both axes: the returned string (`"...could not be built..."` instead
  of the wanted `"...could not reach SES at all..."`) and the log assertion
  (`logged = true`, want `false`, since the partial mutation's row falls back
  to the catch-all rather than classifying).
- **Full-mutation proof.** Reverted the branch fully to
  `strings.HasPrefix(err.Error(), sesWrapPrefix)` (removing the `opErr`
  discriminator entirely) and re-ran — the same test still failed, confirming
  the full mutation (already guarded by the pre-existing row) remains caught.
- **Restore.** Copied the original `worker.go` back over both mutations and
  confirmed `shasum -a 256` matched the pre-mutation hash; `git diff` on the
  file was empty throughout (it was never staged or committed).
- **Stale comment.** The row `#0190` added claimed "this row and
  `TestWorker_TransportDiscriminatorMutationGuard` below are the only things
  in the tree that would catch it." `grep -rn
  TestWorker_TransportDiscriminatorMutationGuard internal/` returns only that
  one comment line — no such test exists anywhere in the tree. Replaced the
  clause with "this row is what catches it," naming nothing that doesn't
  exist.
- **Third-instance check.** Grepped every `Test[A-Za-z_]*` token appearing in
  a comment across `worker_test.go` and `worker.go` (not just the one
  `#0190`/`#0192` flagged) and confirmed each named test actually exists via
  `func <name>(` — `TestSendStore_OrphanSweep_ResetsOnlySendingRows`
  (`worker_store_test.go`), `TestSESMailer_Send_MultipartWithCustomHeaders`
  (`ses_mailer_test.go`), `TestPreflight_FailureOrderIsStable`
  (`preflight_test.go`), `TestWorker_Run_ResumeWithLiveOrphan_DoesNotBusyLoop`,
  `TestWorker_SendStartedAuditWrittenOnlyOnFirstClaim`,
  `TestWorker_AbortBetweenSESAcceptAndStatusWrite_ResumesWithOneDuplicate`,
  `TestWorker_AbortBeforeSESCall_ResumesWithNoDuplicate`,
  `TestWorker_ThreeRetryableFailuresMarkRowFailed` (all `worker_test.go`). No
  third instance of the comment-claims-nonexistent-coverage shape was found.

## Verification

`ISSUE=0192 scripts/check.sh go ./internal/...` — `uptime` 1.67/1.77/1.83 at
the top of the run, well under the ~8 threshold. Build and vet clean, **all
15 packages `ok`** with real elapsed times (`handlers` 2.634s, `interests`
2.469s, `mailing` 0.827s, `auth` 0.775s, `middleware` 0.795s, `audit` 0.320s —
no DB-backed package at 0.00s), skip audit `(no skips)`, `VERIFICATION
PASSED`. Own database `opencircuit_test_0192`, dropped by `check.sh` on exit
(confirmed absent afterward); `pgrep -f 'go test|\.test '` empty.

- `#0181`'s citation guard: `TestNoAdminFacingStringCitesInternalDocs` PASS
  (run individually, in addition to the full-package run above).
- `#0188`'s 21 probes: unchanged rows all still pass in the full-suite run;
  `TestAdminSendErrorMessage_DiscriminatesPrefixedFromOwnErrors` now carries
  15 subtests (14 pre-existing + this issue's new row), all PASS at rest.

## Notes

Reported by `#0190`'s phase-3 review under `CLAUDE.md` §9.

The partial-mutation criterion is the one that matters, and it is the right
proof to demand: the full mutation is already caught, so only the partial form
exercises the row this issue adds.

`#0190`'s `## Review notes` carries the reviewer's independently rebuilt 21-probe
table and the observed unwrap chains. Read it before touching the table — and
note its warning that `%T` renders both `awshttp.ResponseError` and
`smithyhttp.ResponseError` as `*http.ResponseError`, so print `PkgPath` when
identifying a nesting.

## Relation

- Found by: [#0190](0190.md) — the unguarded transport discriminator
- Relates to: [#0188](0188.md) — the 21-probe table
- Relates to: [#0185](0185.md), [#0182](0182.md), [#0171](0171.md) — earlier instances of the same shape

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 84 | 2,737 | 3,318,585 | 77,685 | $1.33 |

**Total: $1.33**
