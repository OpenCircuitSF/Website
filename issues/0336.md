# 0336 — `Growth30Days` counts a restarted import row as an arrival while it is merely `pending`

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | subscribers / admin |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-28 |
| **Commit** | — |

## Description

`#0324` fixed two arrival/departure asymmetries in `Growth30Days` and its
review re-derived the whole truth table — 18 scenarios run through the real
store methods — to prove it. Sixteen cells are now correct. **Two are not**,
and they are scenarios neither `#0324`'s tables nor `#0305`'s named:

| Row | Scenario | Before | After |
|---|---|---|---|
| **O** | invite accepted → unsubscribe → `RestartSignup` | 0 | **+1** |
| **P** | `prior_consent` import → unsubscribe → `RestartSignup` | 0 | **+1** |

Both are the *interaction* of `#0324`'s two halves, which is why neither half
looks wrong alone. `RestartSignup` now withdraws the departure (correctly — that
was `#0324` item 2), while the `imported_30d` arrival cannot be withdrawn. The
row is left counted as +1 while sitting in `pending`, and it stays there
persistently if the confirmation is simply abandoned.

Row **O** additionally contradicts both doc comments, which define the bucket as
"placed on the list with its own standing consent". An accepted invitation that
was later unsubscribed and restarted is *pending*, and holds no standing
consent.

### The remedy is verified

The review measured it: changing `imported_30d`'s predicate from

```
consent_basis IS NOT NULL
```

to

```
consent_basis = ConsentBasisImportedPriorConsent
```

changes **exactly one cell** — row O goes +1 → 0 — and leaves the other
seventeen byte-identical. Row P needs its own consideration; confirm whether the
same predicate covers it or whether the arrival must be withdrawn on
`RestartSignup`.

This was reported rather than bounced because it clears the same bar `#0324`
itself was filed under (from `#0311`'s review): one row per person, four
sequential actions, erring by +1.

## Acceptance criteria

1. Rows O and P both net 0 across the full sequence, and the fix is proved by
   re-running the review's 18-scenario truth table, not by argument.
2. The other sixteen cells are unchanged — demonstrated, not assumed.
3. `imported_30d`'s doc comments state what the bucket means precisely enough
   that row O is not a contradiction: an accepted-then-restarted invitation is
   `pending` and holds no standing consent.
4. The predicate change is measured against every state an imported row can
   reach, including `ImportStore.Commit`'s two shapes and the invite-mode path.
5. `consent_basis`'s append-only property is re-confirmed, not assumed —
   `#0324`'s review found `Confirm`'s `COALESCE` is its only writer tree-wide,
   and the fix depends on that staying true.
6. Tests use real `time.Now()` for every `Create`/`Commit`; no fixed-epoch or
   forward-dated timestamps (`#0324` caused a cross-test flake exactly that way,
   and `#0285` is the same shape). Prove order-independence with `-shuffle`,
   never `-count=N` (§5a).

## Relation

- Found by: [#0324](0324.md)'s review, which built the truth table that exposed
  it
- Depends on: [#0324](0324.md) — the interaction of whose two halves creates it
- Relates to: [#0305](0305.md) — the decision that an unaccepted invitation is
  not growth
- Relates to: [#0311](0311.md) — whose review filed `#0324` under this same bar
- Relates to: [#0285](0285.md) — the cross-test-flake shape criterion 6 guards

## Work log

- 2026-08-28 — Filed by the orchestrator from `#0324`'s review (§9).
