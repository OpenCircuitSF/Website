# 0256 — The restore drill's findings are pinned to migration 20 and drift with every new one

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | infra / docs |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-24 |
| **Commit** | `32992cf` |
| **Closed** | 2026-08-24 |

> 🟢 Resolved 2026-08-24 — the drill's expectations are genuinely derived at run time: it was followed end to end twice, at HEAD and with a throwaway migration stacked on top, against the identical document text with no edits.

## Description

`#0248` corrected `docs/deployment.md`'s drill findings against a real run and
was resolved. **`#0126`'s migrations `000021`/`000022` landed roughly twenty
minutes later and invalidated them again.** Measured at HEAD by `#0248`'s own
reviewer:

| Documented | Actual at HEAD |
|---|---|
| step 1b lands at `19\|f` | `21\|f` |
| step 6 applies `20/u create_workshops` | `22/u create_subscriber_events` |
| constraint diff: one line, `email_campaigns` | `subscriber_events\|8` |
| sequence diff: empty ("non-empty is a real failure, full stop") | `subscriber_events_id_seq` |
| index diff: empty | `subscriber_events\|4` |
| `settings\|6` | **`settings\|7`** — `000021` seeds `queue_max_retries` |

That last row is the figure `#0248` was **bounced over twice**. It was 7, then
6, and is 7 again — not because anyone was careless, but because it was never a
constant.

**The defect is not the numbers. It is that they are numbers.** Every value in
`### What the drill found` is pinned to whatever migration was HEAD when someone
last ran it, and this project adds migrations routinely. Three passes have now
corrected this section, each correctly, and each was invalidated by ordinary
work. A fourth correction would last until migration `000023`.

**Re-pinning is the wrong fix.** The expectations must be *derived* — from HEAD
and HEAD-1 at run time — so the drill states what it is comparing rather than
what someone once observed. Where a figure genuinely cannot be derived, the
drill should print it and ask the follower to sanity-check it, not assert it.

## Acceptance criteria

- [ ] The drill's expectations are derived at run time from the current migration set, not written down
- [ ] The one genuinely-invariant claim — the constraint delta is exactly one table, caused by the rolled-back migration's own `down.sql` — is stated as the rule it is, rather than as `email_campaigns` specifically
- [ ] `settings` is not asserted as a fixed count anywhere; it changes whenever a migration seeds a key
- [ ] Proved by running the drill at HEAD **and** with one further migration added on top, and confirming both pass without editing the document
- [ ] `### What the drill found` either derives its figures or is removed — a section of recorded observations cannot survive an append-only migration set
- [ ] The `set -e` rationale stays accurate: step 7's diff is still a legitimate non-zero exit

## Notes

Found by `#0248`'s third phase-3 review under `CLAUDE.md` §9, while verifying at
HEAD. That reviewer resolved `#0248` rather than bouncing it a third time — the
right call, since the pass genuinely did what it was asked and the drift is a
different defect.

**Worth stating for the record**, because this issue is the fourth attempt at
the same section: `#0239` fixed a drill that passed while testing nothing;
`#0248` fixed a section reporting a drill that no longer existed; `#0249` fixed
the leak those runs exposed. Each was correct. The section kept going stale
because the underlying design — recording observations about a moving schema —
cannot be correct for long.

**Practical note for whoever takes this:** `scripts/testdb.sh` will refuse to
run while the template is behind (`template … is at migration 22 but
migrations/ is at 20`), so re-run `scripts/testdb.sh template` first. That
refusal is working as intended and is how the reviewer noticed.

## Relation

- Found by: [#0248](0248.md) — the third correction of this section
- Caused by: [#0126](0126.md) — whose migrations invalidated it, entirely legitimately
- Relates to: [#0239](0239.md), [#0249](0249.md) — the drill's other two defects
- Relates to: [#0064](0064.md) — the runbook this lives in

## Root cause

`### What the drill found` recorded one specific run's observations —
migration numbers, table names excluded from comparisons, and row counts —
as prose. Every one of those facts is a function of whatever migration
happens to be HEAD, and this project adds migrations routinely
(`CLAUDE.md` §1's greenfield note notwithstanding — the schema itself keeps
moving). Three passes corrected the section and each was invalidated by
ordinary, unrelated work landing afterward, because the design itself —
recording an observation about a moving target — cannot stay correct for
long. `#0126`'s `000021`/`000022` are the fourth invalidation, twenty
minutes after `#0248`'s fix landed.

## Fix

Replaced hand-written numbers with values derived from `migrations/` at run
time:

- **Step 1a** (new) derives `HEAD_VERSION` (highest-numbered
  `migrations/*.up.sql`), `NEW_TABLES` (tables HEAD's migration
  `CREATE TABLE`s outright — comments stripped first, since `000020`'s own
  prose contains the literal phrase "attached with an ALTER TABLE" and would
  otherwise read as a second `ALTER TABLE` statement), and `ALTERED_TABLES`
  (pre-existing tables HEAD's migration modifies via `ALTER TABLE` — the
  only tables the constraint-count diff may legitimately show a difference
  for). Every later step references these variables instead of a literal
  `19`/`20`/`email_campaigns`/`workshops`.
- **Step 7's constraint-count diff** states the actual invariant: any
  difference is confined to `$ALTERED_TABLES` and caused by the rolled-back
  migration's own `down.sql`, and is **empty** whenever the migration only
  creates new tables — which is the case for HEAD's migration
  (`000022_create_subscriber_events`) right now. Verified directly: that
  migration has no `ALTER TABLE` on a pre-existing table at all, so the old
  "always exactly one line, `email_campaigns`" framing was already false for
  it, independent of this fix.
- **Row-count, sequence, and index diffs** build their exclusion lists from
  `$NEW_TABLES`/`$EXCLUDE_SQL` rather than a hand-typed `('workshops',
  'workshop_interests')`. The row-count query itself is now generated from
  `information_schema.tables` rather than a fixed list of nine table names,
  so a future migration's new table is picked up automatically once it's no
  longer HEAD's own new table. The sequence diff derives `$NEW_TABLES`' own
  sequence name(s) via `pg_get_serial_sequence` against the
  restored-and-migrated database, rather than assuming a fixed
  `<table>_id_seq` spelling.
- **`### What the drill found` removed**, per the acceptance criteria's
  explicit permission to remove rather than re-derive a section that is
  fundamentally a recorded observation. Renamed to `### Where to find a
  fresh run's actual figures`, stating why (an append-only migration set
  makes "what this schema currently looks like" wrong by construction,
  eventually, no matter how carefully measured) and pointing at the drill's
  own live output — step 1a's echoed variables and step 7's diffs — instead
  of a written-down table. Content that is genuinely durable (Extensions,
  Roles and ownership, Order and dependencies, the two real script bugs
  found, the two format-idempotency behaviors) was kept; run-specific
  numbers inside it were either generalized or explicitly marked
  illustrative-from-a-past-run rather than a claim about current HEAD.
- **The `set -e` rationale** now states step 7's constraint diff is
  non-zero *exactly when* `$ALTERED_TABLES` is non-empty (not "always
  exactly one line") — still a legitimate, permanent reason not to add
  `set -e`, regardless of which case is live for any given HEAD.

**Two real bugs found and fixed while proving this, both by running the
drill for real rather than by inspection** (see `## Verification`):

1. The table-name extraction regex was `[a-z_]+` (letters and underscore
   only) — a table name containing a digit (this section's own proof
   migration, `scratch_probe_0256`) was silently truncated at the first
   digit to `scratch_probe_`, desyncing every exclusion built from it and
   producing a real "relation does not exist" error at step 8. Fixed to
   `[a-z_][a-z0-9_]*`.
2. The first sequence-diff query used a broken `${EXCLUDE_SQL//\'/}`
   transformation that stripped the quotes an `IN (...)` clause needs,
   producing `ERROR: column "subscriber_events" does not exist` (Postgres
   parsed the unquoted table name as a column reference). Replaced with a
   `pg_get_serial_sequence`-based derivation against `$DST_DSN`, tested and
   confirmed working before it went into the document.

## Verification

All runs used the project's own `scripts/testdb.sh` per-run databases,
dropped after; a throwaway migration file (never committed) for the
second-HEAD proof; and no shared or seeded row was targeted (`CLAUDE.md`
§8b). `uptime` stayed under ~2.3 throughout the drill runs; not a load
artifact.

- **`scripts/testdb.sh template` re-run first**, per the issue's own
  practical note — was at migration 20 (stale), rebuilt to 22
  (`opencircuit_test_template is at migration 22`).
- **Ran the drill's derivation and each step manually first** (`ISSUE=0256a`,
  HEAD=22) to validate the approach before trusting it in the document:
  step 1a correctly derived `HEAD_VERSION=22`, `NEW_TABLES=subscriber_events`,
  `ALTERED_TABLES=` (empty); step 1b rolled back to `21|f`; step 2 seeded
  nine tables after correcting my own seed SQL against the actual schema
  (not guessed — `\d` on every target table first); step 4/5/6 ran for real
  (`backup.sh`, `restore.sh`, `migrate up` printing `22/u
  create_subscriber_events`); step 7's constraint diff was genuinely
  **empty** (`diff` exit 0) with `subscriber_events` excluded — confirming
  the "empty when `ALTERED_TABLES` is empty" rule directly, not assumed;
  row-count, sequence, and index diffs all empty; step 8/8b's ownership
  queries against `$DST_DSN` succeeded with no permission errors; step 9
  dropped both databases cleanly. This run surfaced bug 2 above (the
  sequence-diff query), fixed before the next run.
- **Added a throwaway migration** (`migrations/000023_scratch_probe_0256.{up,down}.sql`,
  never committed — `git status --porcelain -- migrations/` showed only
  these two untracked files while present) that both creates a new table
  (`scratch_probe_0256`) and `ALTER TABLE`s a pre-existing one (`users`,
  adding a CHECK constraint) — deliberately shaped like migration 20's own
  footprint, to exercise the non-empty-`ALTERED_TABLES` branch the first
  run couldn't reach. `scripts/testdb.sh template` rebuilt to migration 23.
- **Extracted the drill's actual bash block verbatim from `docs/deployment.md`**
  (`sed -n '978,1278p'`, i.e. the literal committed text, not a
  paraphrase) and ran it as a real script (`ISSUE=0256b`), substituting only
  the `-f seed.sql` placeholder with a real seed file and `<password>` with
  the local dev password. This run surfaced bug 1 above (the digit-truncation
  regex): `NEW_TABLES` came back as `scratch_probe_` and the constraint diff
  showed two differing lines (`scratch_probe_0256|2` — should have been
  excluded — and `users|7` vs `users|8`) instead of the expected one; step
  8 failed with `ERROR: relation "scratch_probe_" does not exist`. Fixed the
  regex in the document.
- **Re-ran the identical extracted-verbatim script** (`ISSUE=0256c`,
  HEAD=23, same scratch migration) against the fixed document: step 1a
  correctly printed `NEW_TABLES: scratch_probe_0256`,
  `ALTERED_TABLES: users`; step 7's constraint diff showed **exactly one**
  differing pair, `users|7` / `users|8` — matching `$ALTERED_TABLES` exactly
  and nothing else; row-count/sequence/index diffs empty; step 8/8b
  succeeded; step 9 dropped both databases. **Exit 0.**
- **Removed the scratch migration**, rebuilt the template back to migration
  22 (`opencircuit_test_template is at migration 22`), and **re-ran the same
  extracted-verbatim script once more** (`ISSUE=0256d`) against the
  now-real HEAD=22, with **no further edit to the document** between this
  run and the `0256c` run above: step 1a printed `ALTERED_TABLES: none`;
  step 7's constraint diff was genuinely empty again; the explicit
  "ALTERED_TABLES is empty … expect NO output" line printed with nothing
  following it; step 8/8b succeeded; step 9 dropped both databases. **Exit
  0.** This is the pair the acceptance criteria ask for: HEAD, and HEAD with
  one further migration stacked on top, both passing against the identical,
  unedited document text.
- **Database census**: `select datname from pg_database where datname like
  'opencircuit%'` before and after all of the above showed only the four
  permanent databases (`opencircuit`, `opencircuit_live`,
  `opencircuit_test`, `opencircuit_test_template`) — every scratch database
  created by the drill (`_0256asrc`/`dst` through `_0256dsrc`/`dst`) was
  dropped by the drill's own step 9, none leaked.
- **No leftover processes**:
  `pgrep -fl 'go test|\.test |psql|pg_dump|pg_restore'` clean after every
  run.
- **Fence balance**: `docs/deployment.md` has 52 ``` lines (26 balanced
  pairs) after the edit — no unclosed code block introduced.
- **No pinned figures remain** in the touched section: grepped for
  `migration 20`, `version=19`, `version=20`, `settings|6`, `settings|7`,
  and `What the drill found` — the only two `migration 20` hits left are
  explicitly historical/illustrative (one names it as the migration that
  originally motivated the exclusion pattern, one is marked "an
  illustrative migration number from when this was proven, not a claim
  about current HEAD" in the `#0228`/`#0235` ownership narrative, which
  documents a specific past proof and does not claim to describe current
  HEAD). No occurrence of the word "settings" remains in the section at
  all — satisfies "settings is not asserted as a fixed count anywhere."
- All temporary files (`/tmp/drill_*`, `/tmp/seed_0256*.sql`,
  `scripts/.dev_guard_test_*` — none from this issue, but checked) removed
  after use; `migrations/000023_scratch_probe_0256.{up,down}.sql` deleted,
  confirmed by `git status --porcelain -- migrations/` showing nothing.

## Files changed

- `docs/deployment.md` — the restore drill's expectations (migration
  version, new/altered table sets, row-count/sequence/index/constraint
  exclusion lists) are now derived from `migrations/` and the live database
  at run time instead of hand-written; `### What the drill found` replaced
  with `### Where to find a fresh run's actual figures`, keeping durable
  content and pointing at live output for anything that would otherwise go
  stale.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Exact token counts pending the orchestrator's transcript accounting — this
subagent has no access to its own usage totals to report here without
fabricating them.

## Review notes

Independent phase-3 review. All work ran in a throwaway detached `git worktree`
pinned at `b7ceb36` (which contains all four of `#0256`–`#0259`), never the
shared checkout, and never against `shortlinks`. `uptime` was 1.9–2.5
throughout. `git cat-file -t 32992cf` → `commit`.

**The section removal is real.** `### What the drill found` existed at
`32992cf^` (line 1261) and is gone at `32992cf`; what remains is a prose
paragraph explaining why it was removed. Verified with `git show
32992cf^:docs/deployment.md | grep -n 'What the drill found'` against the same
grep at HEAD.

### The two-branch proof — run, not read

The drill's executable lines were **extracted verbatim from the document**
(`sed -n '978,1278p' docs/deployment.md`) and run as-is. The only substitutions
were the two the document itself delegates to the follower: `seed.sql` (step 2
is explicitly "your own INSERT ... RETURNING id statements") and `<password>` →
`opencircuit` in `DST_DSN`. **No other edit, and the identical extracted text
was used for both branches.**

**Branch A — at HEAD (`ISSUE=0256reva`), exit 0.** Derived, not typed:
`HEAD migration: 22 (000022_create_subscriber_events.up.sql)`, `NEW_TABLES =
subscriber_events`, `ALTERED_TABLES = none`. Rollback took (`22/d`, version 21);
seed applied (12 INSERTs across users, subscribers, workshops,
subscriber_interests, workshop_interests, suppressions, email_campaigns,
campaign_interests, email_sends, email_events, outbound_queue, audit_log);
backup 60K; restore reported version 21; step 6 applied `22/u
create_subscriber_events` naming it (not "no change"). **Step 7's constraint
diff printed nothing at all**, which is exactly what the invariant requires when
`ALTERED_TABLES` is empty. Row-count, sequence and index diffs each printed
nothing. Step 8b as the app role: `subscribers` = 5, `subscriber_events` = 0.
Step 9 dropped both databases.

**Branch B — throwaway migration stacked on top (`ISSUE=0256revb`), exit 0.** A
private `000023_scratch_probe_0256rev` was added, deliberately doing *both*
things at once: `CREATE TABLE scratch_probe_0256rev` (a digit-bearing name) and
`ALTER TABLE subscribers ADD CONSTRAINT chk_probe_0256rev`, with a prose comment
containing the phrase "with an ALTER TABLE" to exercise the comment strip.
Built against a **private** template (`TEMPLATE_DB=opencircuit_tmpl0256rev`) so
the shared `opencircuit_test_template` was never rebuilt — `check_template_fresh`
refuses on a version mismatch rather than rebuilding, which is what makes that
safe. Results: `HEAD migration: 23`, `NEW_TABLES = scratch_probe_0256rev`
(**full name, digits intact**), `ALTERED_TABLES = subscribers`. Step 6 applied
`23/u`. Step 7's constraint diff printed **exactly one differing pair, on
`subscribers` (14 → 15), and nothing else** — the single constraint the probe's
up.sql adds and its down.sql removes. Row-count, sequence and index diffs
printed nothing.

**So the stated invariant holds in both directions**: the constraint diff is
confined to `ALTERED_TABLES`, and is empty when the migration only creates
tables. Neither branch needed a document edit, which is the whole point of the
issue.

### Both bugs the implementer found are real and their fixes are load-bearing

- **Table-name regex.** Against the probe migration, the old class
  `CREATE TABLE [a-z_]+` yields `scratch_probe_` — truncated at the first
  digit — while `[a-z_][a-z0-9_]*` yields `scratch_probe_0256rev`. Every
  exclusion downstream (`EXCLUDE_SQL`, `EXCLUDE_SEQ_SQL`, the row-count list)
  is built from that name, so the old class desyncs all of them. Reproduced
  directly, then proved end to end by branch B, which is a run *against* such
  a migration rather than an inspection of the pattern.
- **Sequence-exclusion query.** The old text hardcoded `sequencename <>
  'workshops_id_seq'`. At today's HEAD that is doubly wrong: it excludes a
  sequence present on *both* sides, and fails to exclude the new table's.
  Measured on two databases at versions 21 and 22: the old query emits a
  spurious `> subscriber_events_id_seq|` line and exits 1 — a false drill
  failure — while the derived `pg_get_serial_sequence`-based exclusion
  (`'subscriber_events_id_seq'`) emits nothing and exits 0. Branch B further
  shows the derivation works on a digit-bearing table name.

### Cleanup

`pg_database` censused before and after: identical apart from
`opencircuit_test_0127`, which belongs to the concurrent `#0126`/`#0127` agent
and was left alone. No database matching any of my tokens survives; the private
template was dropped; `/tmp/backup-drill-*` removed; the probe migration and
`seed.sql` removed from the worktree. `shortlinks` was never referenced.
