# 0187 — Four refinements to the citation guard, including one non-test Go file outside its roots

| | |
|---|---|
| **Status** | open |
| **Platform** | All |
| **Module** | handlers / web / infra |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |

## Description

`#0181`'s pass-2 review measured the guard's coverage exactly — 82 glob keys against 82 files on disk, set-identical; 94 Go files collected against 94 non-test `.go` under `internal/` and `cmd/`, set-identical — and found four refinements worth making. None is a live leak.

**1. One non-test `.go` file is outside the walk's roots.** `web/embed.go` is the 95th; the walk covers 94. No exposure today (it is an embed directive), but "94 of 95" is an odd place for a guard whose whole purpose is not needing a boundary argument.

**2. `issues/NNNN.md` is absent from the pattern, though `docs/*.md` was added.** The widened pattern now catches `CLAUDE.md`, `PRD §`, `Issues.md`, `HANDOFF.md`, and `docs/*.md`. A path like `issues/0138.md` is the *least* readable of the set to an admin, and it is the one spelling not covered — an asymmetry rather than a decision.

**3. The `docs/\S*\.md` term would fire on an external URL** containing `/docs/x.md`. Nil in the tree today; worth a moment's thought about whether the pattern should require the path not be preceded by a scheme.

**4. The failure message's paths are inconsistent** — `./branding.ts` for `web/src/lib` files versus `views/...` for others. Cosmetic, but the message exists to be pasted into a search.

## Acceptance criteria

- [ ] The Go walk covers every non-test `.go` in the repo, or records why a file is deliberately outside it
- [ ] `issues/NNNN.md` is covered by the pattern, or the asymmetry is recorded as deliberate
- [ ] Decide on the external-URL case for `docs/*.md` — tightening the pattern, or accepting the false positive as vanishingly unlikely and cheaper than the complexity
- [ ] The failure message prints paths in one consistent form
- [ ] `#0181`'s proofs still hold: both real-string regressions fire, the four Svelte expression forms and the twelve further shapes still catch, and the five acceptable misses still miss
- [ ] `ISSUE=0187 scripts/check.sh go ./internal/...` and `scripts/check.sh web` both pass with zero new false positives

## Notes

Reported by `#0181`'s pass-2 phase-3 review under `CLAUDE.md` §9.

Worth reading that review's coverage measurement before touching anything: it counted the glob's keys against the real file population and the Go walk's collection against `find`, both set-identical, and confirmed `_test.go` exclusion is **load-bearing** — the guard file's own error string cites `#0181`, so scanning tests would make the guard fail on itself.

A fifth item from the same review was housekeeping rather than an issue and is already done: the working checkout's `web/node_modules` still carried `@types/node` after the revert, and `npm ci` cleared it.

## Relation

- Found by: [#0181](0181.md) — the citation guard
- Relates to: [#0172](0172.md) — the first sweep this guard replaces
