# 0064 — Write the deployment runbook

| | |
|---|---|
| **Status** | resolved |
| **Module** | deploy / docs |
| **Platform** | All |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-18 |
| **Commit** | `ccf134d`, `1afe2b1` (bounce fix — CSP `script-src` hash and its recompute snippet corrected; see `## Bounce fix (2026-08-24)`) |
| **Closed** | 2026-08-24 |

> 🟢 Resolved 2026-08-24 — the third CSP `script-src` hash is the right one, and
> this time it is proved from a browser's own account of what it expects rather
> than from any extraction: Chromium names
> `sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8=` as the required value
> when served a wrong one, executes the guard under exactly that value with zero
> violations, and a real `vite build` reproduces the same hash.

## Description

The end-to-end procedure for standing up and updating production on EC2, alongside the existing ShortLinks install.

## Acceptance criteria

- [x] `docs/deployment.md` covering: EC2 provisioning, PostgreSQL setup, config file creation, build, migrations, seed, systemd, Apache, TLS, first admin login — 10 numbered steps added, each with its own "not verified against a real box" note; see `## Verification`
- [x] Apache vhost for `opencircuitsf.com` with **the apex redirecting to `www`** — www is canonical in production and `WEBAUTHN_RP_ORIGIN` depends on it — and `flushpackets=on` on `/api/events` — already correct in `deploy/apache/opencircuitsf.com.conf` (built by `#0066`); confirmed by reading the file, not assumed

> **Criterion corrected 2026-08-18.** This read "`www` redirecting to apex", which is backwards: the live host 301s the apex to www, `#0066` built the vhost that way, and a passkey ceremony fails with an opaque error if `WEBAUTHN_RP_ORIGIN` stops matching the browser's real origin.
- [x] systemd unit for `opencircuit` mirroring the ShortLinks hardening (`NoNewPrivileges`, `ProtectSystem=strict`, `PrivateTmp`, restricted address families) — already present in `deploy/systemd/opencircuit.service` (plus more: `PrivateDevices`, `ProtectKernelTunables`, `ProtectKernelModules`, `ProtectControlGroups`, `RestrictNamespaces`, `LockPersonality`); documented in the runbook's systemd step with what is and isn't proven about it
- [x] Coexistence with the ShortLinks install documented: separate ports, databases, service accounts, and config files — new "Coexistence with the ShortLinks install" section with a side-by-side table
- [x] Route 53 record table from PRD §10.2 reproduced with real values — every name/type/static value is copied verbatim from PRD §10.2; the Elastic IP and SES-issued DKIM CNAME targets are left as explicit `[PLACEHOLDER: ...]` markers rather than invented, since they cannot be known before an instance/SES account exists (`CLAUDE.md` §10 items 2 and 6)
- [x] SES setup: domain verification, DKIM, custom MAIL FROM, DMARC ramp from `p=none` to `p=quarantine` to `p=reject`, production-access request — documented as a 9-step sequence in deploy order, cross-checked against `PRD.md` §10.2–§10.4 and `docs/email-setup.md` for consistency; **not executed** — no AWS credentials capable of it exist in this environment (`CLAUDE.md` §10 item 2), consistent with the user's 2026-08-23 deferral
- [x] IAM instance role policy documented, scoped per PRD §10.5 — a JSON policy document matching §10.5 (SES send scoped to the identity ARN + configuration set, S3 `GetObject`/`DeleteObject` scoped to the inbound bucket prefix, no `ses:*`, no wildcard `Resource`); JSON-validated (`python3 -m json.tool`); one condition key (`ses:configuration-set`) flagged as unverified against AWS's own reference since no live AWS access exists to confirm it
- [x] Security headers configured: HSTS, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and a CSP with no `unsafe-inline` — added to `deploy/apache/opencircuitsf.com.conf`, syntax-checked with a real local `httpd -t`. **Met in full for `script-src`** (locked to `'self'` plus one hash for the SPA's single inline bootstrap script, no `unsafe-inline`; the hash bug that caused the 2026-08-24 bounce is fixed and re-verified in a real browser — see `## Review notes` and `## Implementation notes` below); **not met for `style-src`**, which keeps `unsafe-inline` because multiple Svelte components use inline `style="..."` HTML attributes (static and per-instance-dynamic) that CSP's `style-src` also governs — see `## Gotchas` and the runbook's own "Security headers" section for the full reasoning, since a real refactor of `web/src/` would be needed to close this and `web/` was out of this pass's scope
- [x] Redeploy procedure and a troubleshooting section for "deployed but the site did not change" — the runbook documents `scripts/deploy.sh` (which already existed and already implements this) plus a manual fallback and a 7-item troubleshooting list adapted from ShortLinks' `DEPLOYMENT.md`
- [ ] The whole runbook followed once on a clean instance and corrected where it was wrong — **not met, and cannot be met yet.** Deployment is deliberately deferred (user, 2026-08-23; `CLAUDE.md` §10) and no EC2 instance exists to follow this against. Left unticked rather than claimed; the runbook's own opening paragraph states this plainly and names what would close it

## Carried in from `#0077`'s review (2026-08-19)

- [x] **Document the loopback trust model.** `middleware.ClientIP` trusts
      `X-Forwarded-For` only when the peer is loopback, then takes the rightmost
      entry — the single hop Apache appends. `cmd/opencircuit/main.go:504`
      hardcodes `127.0.0.1:<port>` and there is no config knob to move it, so the
      anchor is enforced in code. The runbook must state the operational
      consequence: **nothing else on that host may proxy or tunnel into
      `127.0.0.1:8080`** — another local process, a host-network container, or an
      SSH tunnel would be trusted as if it were Apache. — Documented in a
      dedicated "Loopback trust model and the CDN failure mode" section. The
      line-504 citation above is now stale (the file has grown since `#0077`
      wrote it); the runbook cites `internal/middleware/clientip.go` and the
      `addr := fmt.Sprintf("127.0.0.1:%d", cfg.Port)` symbol in
      `cmd/opencircuit/main.go` (currently line 1167) instead of a bare line
      number, since line numbers shift and the symbol doesn't.
- [x] **Record the CDN failure mode.** If a CDN or second proxy is ever put in
      front, the rightmost entry becomes that proxy's egress IP: every user
      collapses into one rate-limit bucket and `signup_ip` records the CDN, with
      **no error**. `middleware.ClientIP` must be revisited before any such change.
      — Same section as above.

## Carried in from `#0045`'s plan (2026-08-21)

Two deploy-time facts the code cannot enforce, both of which must appear in the
runbook:

- [x] **The SES configuration set must have open and click tracking disabled.**
      An enabled configuration set injects a tracking pixel **server-side**, at
      send time. That violates `CLAUDE.md` §9's no-open-tracking rule with no
      code change, and **no test in this repo can observe it** — the messages we
      generate are clean either way. It is invisible until someone inspects a
      delivered message. — Runbook's "SES setup" step 6.
- [x] **`MAX_SEND_RATE=1` while the SES account is in the sandbox** (1 message
      per second). Note `CLAUDE.md` §5: there is no performance requirement in
      this project — this value paces for SES's quota, not for throughput. —
      Runbook's Configuration step, cross-referencing
      `docs/configuration.md`'s existing "Developing against the SES sandbox"
      section rather than duplicating it.

## Review notes

**Bounced 2026-08-24.** Independent phase-3 review of `ccf134d`. Almost all
of this is strong work and unusually honest about its own limits — but one
ticked criterion does not hold, and the runbook hands the operator a
correction procedure that reproduces the same error. Criterion 8 has been
un-ticked; the rest stand and are re-verified below.

### What fails — the CSP `script-src` hash is wrong, and so is the recompute command

Criterion 8 claims `script-src` is "locked to `'self'` plus one hash for the
SPA's single inline bootstrap script", and `docs/deployment.md` states the
hash is `base64(sha256(<the exact bytes between <script> and </script> in the
*source* web/index.html>))`. **It is not.** The value shipped in
`deploy/apache/opencircuitsf.com.conf` was computed over the script's content
**with the leading newline stripped**, because the recompute snippet in
`docs/deployment.md` anchors on `r'<script>\n(.*?)</script>'`. CSP hashes the
element's child text content exactly, whitespace included.

Measured, not reasoned about:

| what was hashed | result |
|---|---|
| exact bytes between `<script>` and `</script>` (what the doc says) | `sha256-aV6Z5Fp2xyBUYGmN3Q9e0BQeIOKsDdCa35MPtxT7byg=` |
| the same, minus the leading `\n` (what the snippet does) | `sha256-dwSwJdScBQq2rtRDgx+PNrnX/IUc7TDIKGH+8kn188Y=` ← **shipped** |

Then confirmed in a **real browser** rather than from spec prose — Chromium
via Playwright, against a local server on port 47318 whose identity was
verified by a build-specific marker string in the response body
(`CLAUDE.md` §8b), serving the exact inline script under the exact shipped
CSP header:

```
[SHIPPED]  sha256-dwSwJdScBQq2rtRDgx+PNrnX/IUc7TDIKGH+8kn188Y=
  CSP violations: Executing inline script violates the following
                  Content Security Policy directive 'script-src 'self' …

[COMPUTED] sha256-aV6Z5Fp2xyBUYGmN3Q9e0BQeIOKsDdCa35MPtxT7byg=
  CSP violations: NONE — script allowed
```

The runbook is right that this fails **closed** rather than open — the
pre-paint theme guard is simply blocked and the page flashes the wrong theme
— so this is a correctness bug, not a security regression. But the runbook's
"if the printed hash differs, update the vhost" escape hatch does not save
anyone here, because the command it gives prints the *same wrong value* it
already shipped. That is what makes this worth bouncing rather than noting.

**What the next pass must do:**

1. Put the correct hash for `web/index.html`'s source into
   `deploy/apache/opencircuitsf.com.conf` —
   `sha256-aV6Z5Fp2xyBUYGmN3Q9e0BQeIOKsDdCa35MPtxT7byg=` as of `ccf134d`,
   but recompute it rather than copying this line.
2. Fix the recompute snippet in `docs/deployment.md`'s "Security headers"
   section so it captures the element's whole child text content — drop the
   `\n` from the regex (`r'<script>(.*?)</script>'`), and make sure it
   selects the bootstrap script rather than the `<script
   type="application/ld+json">` example that appears inside an HTML comment
   further up the file (a naive `<script...>` scan matches that comment too —
   this reviewer hit it).
3. Keep the existing, correct caveat that the hash is computed from source
   and not from `npm run build` output.
4. Re-verify the way this review did: the hash must be observed to *allow*
   the script in a browser, not merely to have been produced by a script.

### What was re-verified and does hold

- **Apex → www is intact** (`CLAUDE.md` §7, the settled direction). The vhost
  has `ServerName www.opencircuitsf.com`, `ServerAlias opencircuitsf.com`,
  and `RewriteCond %{HTTP_HOST} !^www\.opencircuitsf\.com$ [NC]` →
  `RewriteRule ^ https://www.opencircuitsf.com%{REQUEST_URI} [L,R=301]`.
  Not inverted. `docs/deployment.md` states the same direction in all four
  places it mentions it (lines 18, 120, 316, 1400). `WEBAUTHN_RP_ORIGIN`'s
  dependency is safe.
- **`httpd -t` reproduced independently.** A self-signed cert and a minimal
  wrapper config `Include`ing the real vhost unmodified, against this
  machine's real Apache: `Server version: Apache/2.4.67 (Unix)` → `Syntax
  OK`. The `deploy/apache/README.md` note that this is *not* checked against
  AL2023's `httpd` (production is 2.4.68 per `CLAUDE.md` §7) is accurate.
- **The `style-src 'unsafe-inline'` limitation is real and correctly
  disclosed — neither hidden nor overstated.** `grep -rn 'style="' web/src/`
  finds **23** inline style attributes, including the dynamic
  `Logo.svelte:44` `style="--logo-size: {size}px; --logo-mask-url:
  url('{maskUrl}');"` the docs name, and static ones in `Admin.svelte`,
  `CampaignStats.svelte`, `Account.svelte`, `Workshops.svelte`,
  `Campaigns.svelte`, `WorkshopEditor.svelte`. The three technical claims
  supporting the compromise all check out: CSP hash/nonce sources do not
  apply to `style` **attributes** without CSP3 `'unsafe-hashes'`; the
  interpolated `Logo.svelte` value has no fixed string to hash; and direct
  CSSOM assignment (`element.style.setProperty`) is not governed by
  `style-src`. The criterion is reported as **half-met**, in the issue and in
  the runbook, rather than silently narrowed — which is the right handling.
- **Placeholders are marked, not invented.** 15 `[PLACEHOLDER: CLAUDE.md §10
  item 6]` markers across the server-facts table, the Route 53 A records, the
  DKIM CNAME targets and the AWS account ID. The document's own opening
  paragraph and its closing "What this document is, and is not, verified
  against" section both state that nothing here has been run on a real host,
  and the final criterion ("followed once on a clean instance") is correctly
  left **unticked**.
- **The IAM policy JSON parses** (`json.loads` on the single `json` fenced
  block: valid, `Version` + `Statement`), and the `ses:configuration-set`
  condition key is flagged as unverified against AWS's own reference rather
  than asserted.
- **No live AWS or SES call was made** by the implementation or by this
  review (`CLAUDE.md` §10).
- The `#0077` loopback-trust / CDN-failure-mode carry-in and the `#0045`
  SES-tracking / `MAX_SEND_RATE=1` carry-in are both present, and the
  stale `main.go:504` citation is correctly replaced by a symbol reference.

### One cross-issue inconsistency, not blocking

`docs/deployment.md`'s restore-drill step 4 says *see "Ownership" below*, but
the bullet it points at is titled **"Roles and ownership (corrected —
`#0228`)"**. A `grep` for `Ownership` finds only the forward reference
itself. Worth a one-word fix whenever this file is next touched.

### Cross-check of the four backup issues against each other

`#0062`, `#0228`, `#0229` and this issue were compared for disagreement about
what ships. They agree: local `pg_dump` to `BACKUP_ROOT`, `rsync` **pull** to
an offsite Mac mini, `restore.sh` reassigning object ownership to
`RESTORE_OWNER`, a systemd timer plus an `OnFailure=` alert, and **no S3** —
recorded consistently in `PRD.md` §10.6, `#0062`'s corrected criteria 1–4,
`#0229`'s `## Decision`, and this runbook's Backups section. The only residue
is `#0062`'s one-line `## Description`, which still says "Nightly `pg_dump` to
S3" — reported under `#0229`'s review notes.

## Bounce fix (2026-08-24)

Implementation pass responding to the 2026-08-24 bounce above. Scope: only
the CSP `script-src` hash and its recompute procedure, per the bounce's four
numbered instructions. Nothing else in `docs/deployment.md` or
`deploy/apache/` was touched.

**A second, independent bug was found while fixing the first — the review's
own "browser-verified" replacement hash was also wrong.** The bounce's
target value, `sha256-aV6Z5Fp2xyBUYGmN3Q9e0BQeIOKsDdCa35MPtxT7byg=`, does
**not** match a real browser's own account of the correct hash. Verified
with Playwright/Chromium, ground truth taken from Chromium's own CSP
violation message (which names the exact hash it expects whenever a wrong
one is supplied — not derived from any Python extraction, so it cannot
share this pass's or the prior pass's extraction bug):

```
Served under CSP: sha256-0000...====  (deliberately wrong)
  console: "...a hash ('sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8='), or a nonce..."

Served under CSP: sha256-dwSwJdScBQq2rtRDgx+PNrnX/IUc7TDIKGH+8kn188Y=  (ccf134d, shipped)
  console: "...a hash ('sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8='), or a nonce..."
  → blocked

Served under CSP: sha256-aV6Z5Fp2xyBUYGmN3Q9e0BQeIOKsDdCa35MPtxT7byg=  (bounce's claimed-correct value)
  console: "...a hash ('sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8='), or a nonce..."
  → blocked (also wrong)

Served under CSP: sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8=  (this pass)
  → zero script-src violations
  → positive execution proof: seeded localStorage.theme='dark' before
    navigation, observed document.documentElement.getAttribute('data-theme')
    === 'dark' after load — only the guard script itself produces this
```

Root cause of the bounce's own error, reasoned out for the record (not
just observed): `web/index.html` line 38 contains the literal substring
`<script>` **inside prose inside an HTML comment**
(`... embedding it verbatim inside <script> is safe ...`, part of the
comment explaining the JSON-LD substitution). A text/regex search for the
first `<script>` occurrence — which is what both the `ccf134d` recompute
snippet and, apparently, the bounce's own re-verification did — finds that
prose substring 16 lines before the real bootstrap `<script>` tag, then
reads forward to the next `</script>` (the real one), capturing the wrong
~1.5&nbsp;KB span: the tail of the comment, the `%%OC_JSONLD%%` token, three
`<link>` tags, and the real script's own opening tag as literal text. Two
different regexes over that same wrong span (one keeping the `\n`, one
stripping it) produce two different wrong hashes — which is exactly the
`ccf134d` value and the bounce's value, respectively. Neither is the hash of
what the browser actually parses as the `<script>` element's child text.

**Fix applied:**

1. `deploy/apache/opencircuitsf.com.conf` — the shipped hash is now
   `sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8=`.
2. `docs/deployment.md`'s "Security headers" section — the recompute
   snippet now uses `html.parser.HTMLParser` instead of a regex/text
   search: it walks actual parsed tags (so comment text is never visited —
   `handle_data` is not called for comment content) and selects the one
   `<script>` element carrying neither a `type` nor a `src` attribute,
   which uniquely identifies the bootstrap guard and excludes both the
   `type="module"` bundle entry and any `type="application/ld+json"` block.
   A worked-through explanation of both prior bugs was added inline so the
   next person recomputing this hash understands *why* a plain text search
   is unsafe here, not just that it was wrong.
3. The recompute snippet's closing guidance now says explicitly not to
   trust a freshly printed hash on its own — confirm it in a real browser,
   the same way this pass did — since two rounds in a row show a wrong
   extraction can be self-consistent.

`httpd -t` re-run against the real, unmodified
`deploy/apache/opencircuitsf.com.conf` via the same minimal wrapper config
the original pass used (self-signed cert, `Include` unmodified):
`Server version: Apache/2.4.67 (Unix)` → `Syntax OK`. Apex→www re-confirmed
intact: vhost still has `ServerName www.opencircuitsf.com`, `ServerAlias
opencircuitsf.com`, `RewriteCond %{HTTP_HOST} !^www\.opencircuitsf\.com$
[NC]` → 301 to `www.opencircuitsf.com%{REQUEST_URI}`; `docs/deployment.md`
still states the same direction everywhere apex/www is mentioned (not
re-audited line-by-line since nothing in that area was touched this pass,
but `grep` shows no inverted phrasing introduced).

**This claim rests on a real browser (Chromium via Playwright), not
reasoning from spec prose or from a Python computation alone** — the same
evidentiary bar the bounce asked for, applied one level deeper: both a
negative probe (Chromium's own violation message, immune to Python
extraction bugs by construction) and a positive execution probe (observable
DOM side effect with zero violations). No live AWS/SES call was made
(`CLAUDE.md` §10); irrelevant to this fix.

## Review notes — re-review 2026-08-24 (bounce fix `1afe2b1`)

**Approved.** Independent phase-3 re-review of `ccf134d` + `1afe2b1`. The
bounce's one blocking finding is fixed, and the fix is right — verified from a
browser rather than from any extraction, which is what the previous two rounds
both got wrong.

### The CSP hash, verified three independent ways

None of these reuses the implementation's own snippet or the bounce's own regex.

1. **A parser written from scratch for this review** (`html.parser`, walking
   parsed tags, selecting the one `<script>` with neither `type` nor `src`)
   against `web/index.html`: child text 475 bytes →
   `sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8=`. The only other
   `<script>` element in the file is the `type="module"` bundle entry, whose
   child text is empty.

2. **Chromium's own account of what it expects — the ground truth.** A private
   HTTP server on port 51873 (`allow_reuse_address = False`, so the bind
   *proving* it was mine rather than another agent's; a build-specific marker
   string served at `/marker` and asserted present before any measurement, per
   `CLAUDE.md` §8b) served `web/index.html` verbatim under four different CSPs.
   Chromium named the same required hash every time, and only the shipped value
   actually executed the guard:

   | CSP served | result |
   |---|---|
   | `sha256-AAAA…` (deliberately wrong) | blocked; console names `sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8=` |
   | `sha256-dwSwJdScBQq2rtRDgx+PNrnX/IUc7TDIKGH+8kn188Y=` (`ccf134d`) | blocked; console names the same hash |
   | `sha256-aV6Z5Fp2xyBUYGmN3Q9e0BQeIOKsDdCa35MPtxT7byg=` (the bounce's own value) | blocked; console names the same hash |
   | `sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8=` (shipped, `1afe2b1`) | **zero violations**, and `document.documentElement.getAttribute('data-theme') === 'dark'` after seeding `localStorage.theme='dark'` — only the guard script produces that |

   So the bounce's own "browser-verified" claim is confirmed to have been
   wrong, and the replacement is confirmed to be right, by the same instrument.

3. **Against a real `vite build`, which closes the caveat the runbook
   discloses.** `docs/deployment.md` states the hash "was computed from the
   unminified source, not from a real `npm run build` output … minification can
   reformat inline-script whitespace and change the hash." That caveat is now
   discharged: this review ran `npx vite build --outDir <scratchpad>/dist-0064rev
   --emptyOutDir`, which never touches the shared `web/dist/` (`CLAUDE.md` §8b) —
   `web/dist/index.html`'s SHA-256 was `7bb5f856…` before and after, and `git
   status --porcelain -- web/dist/` was empty afterwards. Vite copies the inline
   script through byte-for-byte, and the runbook's own snippet run against that
   real build output prints
   `sha256-KLoCdoLOAQC6Tl5qFMi7s/7fwSxANUdbZFjnX7Vhau8=` — the shipped value.

### The recompute snippet, extracted verbatim and run

The `python3 - <<'PYEOF' … PYEOF` block was extracted from
`docs/deployment.md` programmatically (not retyped) and executed unchanged:

- as written, pointed at `web/dist/index.html`: exits with
  `no bare <script> element found — did the markup change?` — the correct
  fail-loud behaviour against `#0141`'s tracked placeholder, not a silent
  wrong answer;
- with only the path swapped to `web/index.html`: prints the shipped hash;
- with only the path swapped to the real build output: prints the shipped hash.

It genuinely does not visit comment text — that is what fixes both prior bugs,
and `web/index.html` still contains the two traps (`<script
type="application/ld+json">…</script>` on line 30 and a bare `<script>` in
prose on line 38) that a text search falls into.

### Everything else re-verified independently

- **`httpd -t` → `Syntax OK`** against the **real, unmodified** vhost, on
  `Apache/2.4.67 (Unix)`, via a fresh minimal wrapper built for this review
  (self-signed cert; only the two `SSLCertificateFile`/`SSLCertificateKeyFile`
  paths rewritten — line count byte-identical otherwise). Still *not* proof
  against AL2023's 2.4.68, exactly as `deploy/apache/README.md` says.
- **Apex → www intact** (`CLAUDE.md` §7): `ServerName www.opencircuitsf.com`,
  `ServerAlias opencircuitsf.com`, `RewriteCond %{HTTP_HOST}
  !^www\.opencircuitsf\.com$ [NC]` → `R=301` to `www…%{REQUEST_URI}`. No
  inverted phrasing anywhere in `docs/deployment.md`.
- **`ProxyPass /api/events … flushpackets=on` precedes the wildcard
  `ProxyPass /`** (lines 17 and 20).
- **systemd hardening** — `NoNewPrivileges=true`, `ProtectSystem=strict`,
  `PrivateTmp=true`, `RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX` all
  present in `deploy/systemd/opencircuit.service`.
- **35 of 35 fenced `bash`/`sh` blocks** in `docs/deployment.md` pass `bash -n`
  (extractor strips blockquote prefixes, so the `drop.sql` and provisioning
  fences are included rather than reported as artifacts).
- **The single `json` block parses** (`json.loads`: `Version`, `Statement`).
- **All 14 unique `#NNNN` citations** in `docs/deployment.md` resolve to an
  existing `issues/NNNN.md`.
- **`style-src 'unsafe-inline'` disclosure is accurate**: `grep -rno 'style="'
  web/src/` still finds exactly **23** inline style attributes. Reported as
  half-met rather than silently narrowed — correct handling.
- **The final criterion stays unticked.** No EC2 instance exists; deployment is
  deferred (`CLAUDE.md` §10). Resolving with it open is deliberate.
- **The cross-issue nit the bounce raised is now fixed by `#0235`**: the restore
  drill's step 4 says *see "Roles and ownership" below*, matching the bullet's
  real title.
- **No live AWS or SES call was made** by this review.

**What rests on real infrastructure and what does not.** The CSP hash and the
`httpd -t` result are real-tool results on this machine (a real browser, a real
Apache, a real Vite build). Everything else in this runbook — provisioning,
systemd install, TLS, SES, IAM, first login — remains unexecuted prose, which
the document says plainly in its own closing section.

### Reported to the orchestrator, not fixed here

- **`#0062`'s one-line `## Description` still reads "Nightly `pg_dump` to S3
  with a proven restore."** — re-confirmed on 2026-08-24, still present, and
  `#0062` is already `resolved`/`Closed`, so a reviewer should not edit it.
  Cross-checking `#0062`, `#0064`, `#0228`, `#0229` and `#0235` for
  disagreement about what ships found no other residue: every remaining `s3`
  reference in `docs/deployment.md`, `PRD.md` and the IAM policy is the
  **inbound-email** bucket, which is a different and real subsystem.


## Notes

PRD §10, §11. ShortLinks' `DEPLOYMENT.md` is the model, including its troubleshooting section on deploys that appear not to take effect — that section exists because the problem actually happened.

The CSP is worth getting right at deploy time rather than retrofitting: Vite emits hashable assets, so `unsafe-inline` is genuinely avoidable here.

The final criterion is the real one — a runbook that has never been followed is a draft.

## Carried in from `#0072`'s review (2026-08-18)

- [x] **Correct `PRD.md` §9's `BASE_URL`, or stop treating it as the config
      template's source.** `#0072` fixed `.env.example` (apex → www) and
      `docs/configuration.md`, but `PRD.md` §9's configuration block still shows
      `BASE_URL=https://opencircuitsf.com` — the apex — two lines above a
      `WEBAUTHN_RP_ORIGIN=https://www.opencircuitsf.com` whose own comment states
      that www is canonical and that the apex 301s to it. That is the exact
      internal inconsistency `#0072` was filed about, surviving in a third source
      of truth. Since this runbook is what documents
      `/etc/opencircuit/config.env`, and `deploy/systemd/README.md` says that file
      "supplies every variable from `.env.example`", anyone regenerating the
      config from PRD §9 reintroduces the defect `#0072` just removed. Either
      correct the PRD line (a user-owned document — ask before editing) or state
      in the runbook that `.env.example` is authoritative and PRD §9 is
      illustrative. — Checked `PRD.md` §9 directly (`sed -n '/^## 9\. /,/^## 10\.
      /p' PRD.md`): it already shows the `www` form with an inline comment
      recording the 2026-08-18 correction, so this half of the defect was
      already fixed by someone else's pass before this dispatch, not by this
      one — not re-touched, to avoid conflicting with `#0229`'s own recent PRD
      edit in the same file. The runbook's Configuration step additionally
      states outright that `.env.example`/`docs/configuration.md`, not
      `PRD.md` §9, is authoritative for the template — the belt-and-suspenders
      half of this criterion.
- [x] **The second acceptance criterion above is backwards** — it says the vhost
      should redirect `www` → apex. Production and `deploy/apache/opencircuitsf.com.conf`
      both do the opposite (apex → www, `RewriteCond %{HTTP_HOST} !^www\.`), and
      `WEBAUTHN_RP_ORIGIN` depends on www staying canonical. Already flagged in
      `CLAUDE.md` §7; repeated here so it is not missed at implementation time.
      — The criterion text above already carries its own 2026-08-18 correction
      blockquote; nothing further to do here.

## Implementation notes

`#0062`/`#0228`/`#0229` (all resolved/in-progress just before this dispatch)
already carried a well-developed "Backups" section in `docs/deployment.md`
into this pass — that section is preserved verbatim from what those issues
wrote, since it was already correct and already carried the right honesty
framing this pass is asked to apply everywhere else. Everything else in
`docs/deployment.md` is new: ten numbered steps (Prerequisites through First
admin login), a Coexistence-with-ShortLinks table, a real-valued DNS table, a
SES setup sequence, an IAM policy document, a Security headers section, a
Redeploy procedure, a Troubleshooting section (adapted from ShortLinks'
`DEPLOYMENT.md`), the loopback-trust-model/CDN-failure-mode carry-in, and a
closing "What this document is, and is not, verified against" section that
states plainly, once, what the whole document rests on — rather than
scattering that framing unevenly.

**Two real code/config changes landed alongside the documentation**, both in
`deploy/`, which this dispatch was explicitly scoped to touch:

- `deploy/apache/opencircuitsf.com.conf` gained five `Header always set …`
  directives: HSTS, `X-Content-Type-Options`, `X-Frame-Options`,
  `Referrer-Policy`, and a `Content-Security-Policy`. This was a **real gap**
  before this pass — the vhost had no security headers at all.
- `deploy/apache/README.md` gained a note on the headers and the `mod_headers`
  dependency.

**The CSP's `script-src` hash was computed programmatically, not typed by
hand** (`CLAUDE.md` §8's warning about hand-typed escapes landing as literal
bytes applies here too, even though this is a hash rather than an escape
sequence — a manual transcription error would silently produce a hash that
matches nothing). Computed with Python's `hashlib`/`base64` against the exact
bytes between `<script>` and `</script>` in `web/index.html`'s **source**,
not a real `npm run build` output — `web/` was explicitly out of this pass's
scope and `web/dist/` is a shared mutable resource other concurrent agents
may be building into (`CLAUDE.md` §8b), so running `npm run build` here was
avoided deliberately rather than overlooked. The runbook states this
limitation inline, next to the hash, with the exact recompute command to run
against the real build output before this CSP is ever pointed at a real
deploy.

**A real, load-bearing gap found and reported, not silently narrowed:** the
criterion asks for "a CSP with no `unsafe-inline`", but `style-src` cannot
be locked down that way against the current codebase without a `web/src/`
refactor — see the acceptance-criteria checkbox above and the runbook's own
"Security headers" section for the full reasoning (inline `style="..."`
attributes, some static, some genuinely per-render dynamic and therefore not
hashable at all). Reported here rather than either (a) shipping a CSP that
silently keeps `style-src 'unsafe-inline'` with no explanation, which would
read as the criterion being met when it partially isn't, or (b) attempting
the `web/src/` refactor, which is out of this issue's scope and this pass's
file permissions.

**One stale fact corrected while writing this, not left to compound:** the
`#0077` carry-in cites `cmd/opencircuit/main.go:504` for the loopback-bind
line. That line number is no longer accurate — the file has grown since
`#0077`'s review wrote it, and the actual bind line is now at 1167. The
runbook cites the symbol (`addr := fmt.Sprintf("127.0.0.1:%d", cfg.Port)`)
and the file it lives in instead of a bare line number, and this issue's
own acceptance-criteria checkbox above says so explicitly rather than
silently perpetuating the stale reference.

**`CLAUDE.md` §10 item 6 (server-side details) was treated as genuinely
unknown, not filled in.** Every place the runbook would otherwise need an
instance ID, size, region, SSH access detail, `DocumentRoot`, installed
vhost file, certbot schedule, or confirmation that the existing Postgres is
the target, it carries an explicit `[PLACEHOLDER: CLAUDE.md §10 item 6]`
marker instead. These markers are outside `scripts/deploy.sh`'s
`[PLACEHOLDER: ...]` gate, which is scoped to `web/src/` only (confirmed by
reading `scripts/deploy.sh` directly) — so they do not collide with that
mechanism, they intentionally reuse its convention.

**Not touched:** `PRD.md` (avoided deliberately — `#0229` edited it
immediately before this dispatch and re-editing risked a conflict per this
pass's own instructions; `PRD.md` §9's `BASE_URL` line was checked and found
already correct, so there was nothing to fix there either), `web/`,
`internal/`, `scripts/` (all out of scope), and no other issue file besides
`0064.md`, `0052.md`, and `0153.md`.

## Verification

`uptime` before starting: `load averages: 3.01 2.63 2.52` (idle, well under
the ~8 threshold — `CLAUDE.md` §5). This is a documentation-plus-config
deliverable; "verification" here means syntax-checking and cross-referencing
against the repo's real files, not exercising a running system, since no
real system exists. Every claim below states which.

- **Apache vhost, including the new security headers — syntax-checked
  against a real, local Apache, not assumed correct by inspection.**
  `httpd -v` on this machine reports Apache/2.4.67 (the closest available
  stand-in for AL2023's 2.4.68 — not the real target). Built a minimal
  wrapper config loading `mpm_event`, `proxy`, `proxy_http`, `rewrite`,
  `ssl`, and `headers`, pointed the vhost's two `SSLCertificateFile` paths at
  a throwaway self-signed cert (generated with `openssl req -x509`, since the
  real `/etc/letsencrypt/...` paths don't exist on this machine), and ran
  `httpd -t` against the **real, unmodified**
  `deploy/apache/opencircuitsf.com.conf` via `Include`. Result: `Syntax OK`.
  Re-ran this exact sequence a second time, verbatim as written into
  `docs/deployment.md`'s Apache step, to confirm the documented command block
  is actually copy-pasteable and not just something that worked once with
  different paths — same result, and all scratch files
  (`/tmp/{privkey,fullchain}.pem`, `/tmp/vhost-scratch.conf`,
  `/tmp/httpd-check.conf`, `/tmp/httpd-check-error.log`) removed afterward.
  **What this does not prove:** that AL2023's `httpd` package (a different
  build, 2.4.68 not 2.4.67) accepts the same file, that the headers appear
  correctly in a real HTTPS response, or that the CSP doesn't break some
  interaction the SPA's automated tests don't exercise.
- **The `script-src` CSP hash** — computed programmatically (Python
  `hashlib.sha256`/`base64.b64encode` against the exact inline-script bytes
  extracted from `web/index.html` with a regex, not typed by hand) —
  `sha256-dwSwJdScBQq2rtRDgx+PNrnX/IUc7TDIKGH+8kn188Y=`. Explicitly **not**
  checked against a real `npm run build` output (`web/` out of scope,
  `web/dist/` a shared resource per `CLAUDE.md` §8b) — the runbook states
  this limitation next to the hash with the exact recompute command.
- **The IAM policy JSON** — validated with `python3 -m json.tool` (parses
  cleanly) after fixing one mistake caught by that same check: an initial
  draft had a `//` comment inside the JSON block, which is not valid JSON;
  moved the caveat to prose outside the code fence instead of leaving invalid
  JSON in a document. Not validated against AWS (no credentials capable of
  it exist here, `CLAUDE.md` §10 item 2) — the one condition key whose exact
  name wasn't independently confirmed is flagged inline in the runbook.
- **Every `#NNNN` issue citation added in this pass** — extracted with
  `grep -oE '#[0-9]{4}'` against `docs/deployment.md` and checked that
  `issues/<NNNN>.md` exists for each; all 13 resolved.
- **Every relative markdown link added** (`configuration.md`,
  `email-setup.md`) — confirmed both files exist in `docs/` alongside
  `deployment.md`, so the relative links resolve.
- **Every fenced `bash` code block in the new/changed sections of
  `docs/deployment.md`** — extracted programmatically (including
  blockquote-prefixed fences, e.g. the `db/drop.sql` warning block) and run
  through `bash -n`. 35 blocks found, 34 passed on the first pass; the one
  failure was a deliberately-illustrative placeholder
  (`httpd -t -f <scratch-wrapper-with-...>`) that read as a real command but
  wasn't one — replaced with the actual, real command sequence used in the
  Apache-syntax-check bullet above, re-extracted, and re-checked: 35/35 pass.
  This is the "do not write a runbook step you have not at least
  syntax-checked locally" instruction applied literally, not just to the
  steps that looked risky.
- **`scripts/deploy.sh` and `scripts/db-reset.sh`** — read directly rather
  than assumed from memory or from ShortLinks' equivalent; the runbook's
  Redeploy and Troubleshooting sections describe what these scripts actually
  do (confirmed against their source), not what a same-named ShortLinks
  script does.
- **`deploy/systemd/opencircuit.service`'s hardening directives** — read
  directly; every directive the acceptance criterion names
  (`NoNewPrivileges`, `ProtectSystem=strict`, `PrivateTmp`, and
  `RestrictAddressFamilies`) is present, confirmed by `grep`, not by
  skimming.
- **No Go or web changes in this pass** — `ISSUE=0064 scripts/check.sh` was
  **not** run, deliberately: this dispatch's scope was `docs/`, `deploy/`,
  and three named `issues/` files only, and `scripts/check.sh`'s Go scope
  would pull in the concurrent `#0218`-adjacent `go vet` failure from
  `internal/handlers/citation_target_guard_test.go` (an untracked file
  belonging to a different, concurrently running agent — confirmed present
  via `git status --porcelain` before starting and unrelated to anything
  touched here) for no benefit, since nothing in this diff touches Go or web
  source.
- **Git hygiene**: `git status --porcelain -- docs/ deploy/ issues/0064.md`
  showed only this pass's own three modified files
  (`deploy/apache/README.md`, `deploy/apache/opencircuitsf.com.conf`,
  `docs/deployment.md`) before the commit below — no concurrent agent's
  work was present in `docs/` or `deploy/` to accidentally sweep in
  (`CLAUDE.md` §8a).

**Not verified, and explicitly stated as such throughout the document
itself** (`CLAUDE.md` §10's honesty requirement, applied to every step, not
just the ones this pass added): every provisioning, build, migration, seed,
systemd-install, TLS, first-login, SES, and IAM step, because no EC2
instance and no AWS account exist. `docs/deployment.md`'s own closing
section restates this in one place rather than requiring a reader to infer
it from scattered caveats.

## Gotchas

- **`web/dist/index.html` (the committed placeholder) is a tracked file**,
  not gitignored — only `web/dist/*` beyond it is ignored. This means
  running `npm run build` here would have rewritten a tracked file inside
  `web/`, which this dispatch was told not to touch and which `CLAUDE.md`
  §8b separately flags as a shared mutable resource. That is *why* the CSP
  hash rests on source rather than a real build, not an oversight — a future
  pass with `web/` in scope (or run in isolation, e.g. a worktree) should
  redo that one computation against real build output before this CSP goes
  live.
- **`httpd -t` needs an `ErrorLog` and `ServerName` even for a pure syntax
  check** — omitting them doesn't fail the check, it just prints a warning
  and then a *different* fatal error (`Cannot access directory '/usr/logs/'
  for main error log`) that looks like a real config problem but isn't one.
  Worth knowing before reading a `httpd -t` failure as "the vhost is wrong"
  when it might just be the wrapper.
- **The AL2023 `httpd` module path (`modules/`) differs from this macOS
  Apache's (`libexec/apache2/`)** — the wrapper config used to prove the
  vhost parses is not portable to AL2023 as written; only the vhost file
  itself (the thing actually being checked) is what matters for the real
  deploy, and it references no path that's macOS-specific.

## Files changed

- `docs/deployment.md` — rewritten from a partial stub (topology + Backups +
  Bootstrap admin + Open items, all preserved) into a full runbook: ten
  numbered setup steps, Coexistence-with-ShortLinks, DNS, SES setup, IAM,
  Security headers, Redeploy procedure, Troubleshooting, and the
  loopback-trust-model carry-in.
- `deploy/apache/opencircuitsf.com.conf` — added five security-header
  directives (HSTS, `X-Content-Type-Options`, `X-Frame-Options`,
  `Referrer-Policy`, `Content-Security-Policy`).
- `deploy/apache/README.md` — documented the new headers and their
  `mod_headers` dependency.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §10 — Infrastructure and Deployment · 89 lines
  `sed -n '/^## 10\. /,/^## [0-9]/p' PRD.md`
- Depends on: [#0010](0010.md) — Add the seed command for the bootstrap admin
- Depends on: [#0045](0045.md) — Build the send worker
- Depends on: [#0057](0057.md) — Configure SES inbound receipt rules
- Depends on: [#0062](0062.md) — Verify the database backup and restore path

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |
| 2026-08-24 | review | claude-opus-5 | — | — | — | — | — |
| 2026-08-24 | implement (bounce fix) | claude-sonnet-5 | — | — | — | — | — |
| 2026-08-24 | review (re-review) | claude-opus-5 | — | — | — | — | — |

Token/cache/cost columns left for the orchestrator to fill from this
subagent's transcript, per `issues/Issues.md`'s "Usage is recorded per
phase" — this subagent does not have access to its own dispatch-level usage
accounting (same convention `#0228`/`#0229` used).
