# 0363 — The claim-comment directional axis ignores a capitalised "Below"/"Above" entirely

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-01 |
| **Commit** | `aed6cec` |

## Description

`claimCommentGuardDirWordRe` in
`internal/outbox/claim_comment_referent_guard_test.go` is `\b(below|above)\b`
with **no `(?i)` flag**. So a sentence-initial `Below,` or `Above,` — the most
natural way to write a forward reference in Go prose — **never enters the
directional axis at all**.

**This is not a calibration trade.** `#0358` exists to record the axis's
deliberate trades, each of which buys something against a real opposing error.
This one buys nothing: nothing is gained by excluding a capitalised direction
word.

**It has already misled a review.** `#0347`'s third review probed the fixture
`// Below, ClaimDue …` believing it measured the axis's backwards-only window
limit. It did not — the regex never matched, so the axis was never entered, and
the probe measured nothing. `#0358`'s review found this by mutation: adding a
forward-looking window, the exact change that fixture claims to guard, leaves
the whole calibration table **green**.

**The fix is measured free.** `#0358`'s reviewer flipped the flag and measured:
`go test ./internal/outbox/` stays green, the whole-tree scan stays at **0
findings** across 271 files and 30,843 comment groups, and `#0342`'s sites A and
C are still caught.

It was left unfixed there only because `#0358`'s criterion 4 forbids changing
the window logic — hence this issue.

## Acceptance criteria

1. `claimCommentGuardDirWordRe` matches case-insensitively.
2. **Re-measure the whole-tree scan** and record it: findings, files, comment
   groups. The claim that the fix is free must be re-derived, not inherited from
   this issue — on 2026-08-31 five separate issues received a wrong identifier
   because prose was transcribed as though measured.
3. `#0342`'s sites A and C are still caught by
   `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`. If the
   flip changes what that test finds, stop and report.
4. **Convert `#0358`'s capitalisation row pair into a regression pin for the new
   behaviour** — the row that currently pins nothing must, after this change,
   fail when the flag is removed again. Prove that by mutation: remove the flag
   and confirm the row fails.
5. If the flip surfaces new findings anywhere in the tree, they are **real
   defects of the class `#0347` exists to catch** — report them rather than
   suppressing them, and do not widen an exemption to make the tree green.
6. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal enters
   any Go comment, and the floor harness population stays **42 total / 12
   non-exempt** (`#0323`'s trap). Re-run
   `scripts/go_file_visit_floor_guard_test.sh` and record it.
7. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout (§8).

## Relation

- Found by: [#0358](0358.md)'s review, which measured the fix as free and left
  it under that issue's criterion 4
- Depends on: [#0358](0358.md) — its calibration table is what criterion 4
  converts; land it first
- Relates to: [#0347](0347.md) — the axis this fixes, and whose third review was
  misled by this surface
- Relates to: [#0342](0342.md) — sites A and C, the real defects the axis exists
  to catch
- Relates to: [#0354](0354.md), [#0355](0355.md) — the same lesson in the
  sibling family: a fixture that cannot discriminate pins nothing

## Review notes

> 🔴 **Bounced 2026-09-01 (review, Opus).** `(?i)` was added to
> `claimCommentGuardDirWordRe`, but the resolution comparison it feeds is still
> case-**sensitive** — so a capitalised direction word no longer "enters the
> axis", it **bypasses the axis's resolution entirely and flags
> unconditionally**. The change converts a false negative into a false
> positive. Criteria 1, 4, 6 and 7 hold as written; criterion 5's "no new
> findings" is true of today's corpus by coincidence, not by construction.

### The defect

`findClaimCommentGuardFindings`, directional axis
(`internal/outbox/claim_comment_referent_guard_test.go`):

```go
dir := flat[dm[2]:dm[3]]          // now (?i), so this may be "Below"/"Above"
...
    if dir == "below" && o > endOff { ok = true }
    if dir == "above" && o < startOff { ok = true }
```

With `dir == "Below"` neither comparison can ever be true, `ok` stays `false`,
and **a finding is emitted for every target identifier in the window regardless
of what the file's code actually contains**. Before this change a capitalised
direction word was invisible (false negative, harmless); after it, every
capitalised direction word with a claim-machinery identifier in its 90-char
backward window is reported as a directional error even when the comment is
**correct** — the exact false-positive class `#0347`'s three review rounds spent
their effort eliminating (paren scoping, nearest-vs-every identifier).

Instrumented in a throwaway `git archive HEAD` export under the scratchpad
(never the tracked tree, §8b), printing the marker match, `dir`, the sentence,
the computed `lo` and which clamp produced it, the window, and the code offsets:

| fixture | comment is | findings at `f404358` | should be |
|---|---|---|---|
| `// ClaimDue claims the row below.` + `func ClaimDue() {}` below it | correct | 0 | 0 |
| `// ClaimDue claims the row Below.` + `func ClaimDue() {}` below it | **correct** | **1 — FALSE POSITIVE** | 0 |
| `func ClaimDue() {}` then `// ClaimDue is declared above.` | correct | 0 | 0 |
| `func ClaimDue() {}` then `// ClaimDue is declared Above.` | **correct** | **1 — FALSE POSITIVE** | 0 |

The finding text is self-contradicting, which is the clearest possible signal:

```
comment says "ClaimDue" is Below, but this file's code has no ClaimDue
occurrence Below the comment (occurrences anywhere in file: 1)
```

— one occurrence, and the instrumentation confirms it *is* below the comment
(`any offset > endOff? true`).

### Does the new pin row pin for its named reason? No.

The row's fixture is `"package outbox\n\n// ClaimDue claims the row Below.\n"` —
**no code at all**, so `facts.identOffsets["ClaimDue"]` is empty and `ok` is
`false` on *either* code path. The row therefore proves only that the direction
word **entered** the axis; it cannot distinguish "entered and resolved
correctly" from "entered and always flags". It is a genuine improvement on its
`#0358` predecessor (which never entered the axis, so mutation moved nothing)
and it *does* fail under the flag mutation — but it is blind to the very bug
this change introduced. This is `#0364`'s discriminant question arriving one
issue early, on a **non-zero** expectation row.

### Remedy — transcription, all three parts proved green in the export copy

**R1 — lowercase the comparison.** In the directional axis, after
`dir := flat[dm[2]:dm[3]]`, insert:

```go
			// #0363: claimCommentGuardDirWordRe carries (?i), so dir may be
			// capitalised ("Below"/"Above"). The resolution comparison below
			// must be case-insensitive or it can never be satisfied, and the
			// axis degenerates from "invisible to capitalised direction words"
			// (the bug this issue fixes) into "flags every capitalised
			// direction word unconditionally" -- a false positive on CORRECT
			// prose, which is strictly worse. Pinned by the resolution rows in
			// claimCommentGuardDirectionalCalibrationCases.
			dirLower := strings.ToLower(dir)
```

and change the two comparisons to `dirLower == "below"` / `dirLower == "above"`.
Leave `dir` in the finding's `detail` so the message still quotes the word as
the comment writes it. `strings` is already imported; `gofmt -l` stays clean.

**R2 — pin the resolution, not just axis entry.** Append three rows to
`claimCommentGuardDirectionalCalibrationCases`, immediately after the existing
`#0363` pin pair, under a doc comment saying they pin that a capitalised
direction word **resolves** rather than merely matching. Exact fixtures, all
three measured:

```go
	{
		name:                 "case-insensitivity resolution control: lowercase direction word resolves against a real declaration below",
		src:                  "package outbox\n\n// ClaimDue claims the row below.\nfunc ClaimDue() {}\n",
		wantClaimDueFindings: 0,
	},
	{
		name:                 "case-insensitivity resolution pin: capitalised direction word must RESOLVE, not merely enter the axis",
		src:                  "package outbox\n\n// ClaimDue claims the row Below.\nfunc ClaimDue() {}\n",
		wantClaimDueFindings: 0,
	},
	{
		name:                 "case-insensitivity resolution pin: capitalised Above must resolve too -- the other, separately-written comparison",
		src:                  "package outbox\n\nfunc ClaimDue() {}\n\n// ClaimDue is declared Above.\n",
		wantClaimDueFindings: 0,
	},
```

The third row is not redundant: the two comparisons are separate lines, and a
partial R1 that lowercases only the `below` line would pass the first two.
Measured in the export copy — with R1 all three are 0; without R1 rows two and
three are 1.

**R3 — correct the population-drift attribution in `## Verification`.** The
30,873 → 30,964 drift is **not** "other agents' concurrent, uncommitted edits".
Measured per commit with the harness's own metric (`grep -cE '^[[:space:]]*//'`
over `internal/` + `cmd/`, replicated independently):

| commit | comment-opening lines |
|---|---|
| `1467844` (`#0358` resolve) | 30,873 |
| `03d9981` (files `#0364`, markdown only) | 30,873 |
| `9cf6898` (`#0341` code, `internal/subscribers/`) | **30,956** (+83) |
| `5d3e2cb` (`#0362` resolve, markdown only) | 30,956 |
| `f404358` (this issue's own commit) | **30,964** (+8) |

Every line of the drift is **committed** work — `#0341`'s guard commit plus this
change's own net +8 comment lines. Nothing uncommitted contributed. (The tree
*is* dirty in `internal/subscribers/` now, from `#0341`'s post-bounce pass, but
that started after this measurement.)

**R4 — one clarifying sentence in `## Verification`.** The shape this issue's
`## Description` leads with — sentence-initial `// Below, ClaimDue …` — is
**still** 0 findings after the fix, because the window looks strictly backwards
from the direction word and the identifier follows it. That is the separately
pinned `identifier-after-direction-word false negative` trade, frozen by
`#0358` criterion 4, and it is correct that this issue does not change it. Say
so explicitly: an unqualified "the capitalised case now works" is what misled
`#0347`'s third review about this very fixture once already.

**R5 — re-verify.** Re-run criterion 2's whole-tree scan, criterion 4's flag
mutation (it must still fail exactly the flag pin row, by name, and nothing
else), `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`,
`scripts/check.sh guards` (42 / 12), and the gofmt sha equality.

### What this review re-verified independently, and what held

- **Criterion 4's mutation, re-run by me** in a `git archive HEAD` export:
  reverting `(?i)` fails **exactly one** sub-test —
  `TestClaimCommentGuardDirectionalAxisCalibrationTable/case-insensitivity_regression_pin:_capitalised_direction_word_flags_too`
  (`want 1 got 0`) — and nothing else moves: not the control row, not the other
  table rows, not `…FlagsKnownStaleSitesFromDb9bff7`, not
  `…IsCleanOnStoreGoParenScoping`, not `…IsCleanOnKnownNegativeFixture`, not the
  `store.go` tests. As reported. **Holds.**
- **Criterion 3.** Sites A and C still caught. **Holds.**
- **Criterion 5, scanned independently** with a standalone `go/parser` walker
  (not the guard's own code) over `internal/` + `cmd/`: 271 files, and of
  5,047 non-empty comment groups exactly **5** contain a capitalised
  `Below`/`Above`; 2 of those name a claim-machinery identifier somewhere in the
  group; **0** have one in the 90-char backward window. So the tree is green
  because **no comment currently sits in the population the flip newly admits**
  — a property of today's corpus, one reworded sentence away from a false
  positive. Not because the resolution works; it does not. That distinction is
  the whole finding, and it is why "0 new findings" was not sufficient evidence.
- **The two metrics reconcile**, as the implementer says: the in-package guard
  counts `len(f.Comments)` AST groups (**5,053**, reproduced exactly by my own
  standalone walker on the live tree), while `scripts/go_file_visit_floor_guard_test.sh`
  counts lines *opening* with `//` (**30,964** at `f404358`). The issue's
  "30,843" is the shell-harness metric transcribed into a sentence about the
  in-package one. My own AST count of *non-empty* groups is 5,047; the 6-group
  gap is directive comments (`//go:…`), whose `CommentGroup.Text()` is empty and
  which my walker skips and the guard's `len(f.Comments)` does not. Both
  harnesses agree at **0 findings**.
- **`t.Logf` instrumentation.** None survives in `f404358`; the committed file
  hashes `0a220bc62dcd73f26b9a945c5418799422589b211a937cb980b9661f801220fe`,
  identical to `gofmt` stdout (criterion 7) and to the export copy. **Holds.**
- **Criterion 6.** `scripts/check.sh guards` — `claimKindsGuardMinPlausibleCallSiteCount`
  population **42**, `claimKindsGuardMinPlausibleNonExemptCallSiteCount`
  population **12**, `TestNoClaimMachineryCallSyntaxLiteralInAnyComment` PASS
  (the new fixture text introduces no `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(`
  literal). 0 failures. **Holds.**
- **Nothing else in the axis moved.** The diff is the one flag plus the row
  pair's name/expectation/doc comment. No marker-set change, no clamp change,
  no every-identifier change, no window change — `#0358` criterion 4's freeze is
  respected.
- `ISSUE=0363r scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...`
  — `ok` on all three, zero skips, `gofmt -l` clean, VERIFICATION PASSED.

### On whether this guard family terminates

`#0358`'s review argued the family terminates because its table makes a **floor
claim** ("these behaviours are pinned") rather than an **inventory claim**
("these are all the behaviours"). Converting one row from control to regression
pin does **not** reintroduce a closed claim — a row asserting `1` is still one
pinned behaviour, not an assertion about the set. That reasoning survives.

What this round adds is a different failure mode, and it is not a closed-claim
problem: **a row can get its expected number for a reason other than the one it
names.** `#0358`'s capitalisation row got `0` because the axis never ran;
`#0363`'s got `1` because the axis always fires. Same defect, opposite sign,
both invisible to a table that only compares counts. That is a property of the
*fixtures*, fixable once and for all by making each row's fixture discriminate,
and it is not the §8 "seventh in-file check" trap — it strengthens the oracle
rather than adding another layer defending the file from edits to itself.

So: the family terminates, but not at `#0363`. It terminates when every
calibration row is shown to get its number for its named reason.

## Work log

- 2026-09-01 — Filed by the orchestrator from `#0358`'s review (§9).
- 2026-09-01 — Implemented (Sonnet). `claimCommentGuardDirWordRe` changed from
  `` \b(below|above)\b `` to `` (?i)\b(below|above)\b `` (one flag, as scoped).
  `#0358`'s capitalisation row pair in
  `claimCommentGuardDirectionalCalibrationCases` converted into a regression
  pin: the "capitalised direction word" row now asserts `wantClaimDueFindings:
  1` (was `0`), with the row's own doc comment rewritten to describe the fixed
  behaviour and the mutation proof. Commit `f404358`.
- 2026-09-01 — **Reviewed (Opus) — BOUNCED.** The `(?i)` flag is correct but
  incomplete: the resolution comparison it feeds (`dir == "below"` /
  `dir == "above"`) is still case-sensitive, so a capitalised direction word now
  flags unconditionally — a false positive on correct comments. Remedy written
  out in full in `## Review notes` (R1–R5) with all three replacement fixtures
  measured green in a throwaway export. **§3 escalation decision: do NOT
  escalate — stay on Sonnet.** This is the first bounce on this issue, and the
  remedy is transcription: the exact replacement lines, the exact fixture
  strings, the expected counts, and the re-verification list are all given.
- 2026-09-01 — **Implemented, second pass (Sonnet).** Applied R1–R5 as
  transcription, per §3. Before touching the tracked tree, reproduced the
  reviewer's false positive independently in a throwaway `git archive HEAD`
  export under the scratchpad (never the tracked tree, §8b): a fixture with a
  capitalised `Below`/`Above` and a *real* declaration on the correct side
  flagged 1/1/1 (want 0/0/0) at `HEAD` (`c562d6f`); after applying R1 in that
  same export, all three cleared to 0/0/0. Then applied R1 (the `dirLower`
  local, `strings.ToLower(dir)`, doc comment as given) and R2 (the three new
  calibration rows, verbatim) to the tracked file. `gofmt -l` clean; file sha
  equals `gofmt` stdout sha
  (`620ef277c3eca8bf72b4b16c274448a6eaead82f025879d41b7a54b30c3f6416`).
  Code commit `aed6cec` — `git show --stat HEAD` confirmed exactly one file,
  `internal/outbox/claim_comment_referent_guard_test.go`, 40 insertions / 2
  deletions. R3 and R4 folded into `## Verification` below. Status left
  `in-progress` for review (§9).

## Verification — second pass (R1–R5), all re-measured against `aed6cec`

**Pre-fix reproduction, independent of the review's own numbers.** Before
touching the tracked tree, built a throwaway `git archive c562d6f` export
under the scratchpad (never the tracked tree, §8b) and added a scratch test
with real code on the correct side of the comment (not the no-code
calibration fixtures, which cannot distinguish this bug — see the review's
"Does the new pin row pin for its named reason? No."). Result at `HEAD`
(pre-R1): `got=1 want=0` on both the capitalised-`Below` and
capitalised-`Above` fixtures (the lowercase control correctly stayed
`got=0 want=0`) — the false positive reproduces exactly as the review
described. Applying R1 in that same export cleared both to `got=0 want=0`.
Export deleted after.

**R1 applied.** `dirLower := strings.ToLower(dir)` inserted verbatim (doc
comment included) after `dir := flat[dm[2]:dm[3]]`; both comparisons changed
to `dirLower == "below"` / `dirLower == "above"`. `dir` itself untouched, so
the finding's `detail` string still quotes the word as written. `strings` was
already imported.

**R2 applied.** The three rows appended verbatim, immediately after the
existing `#0363` axis-entry pair, under the doc comment given in the review.
Full `TestClaimCommentGuardDirectionalAxisCalibrationTable` run against the
export copy:

- **With R1**: all rows green, including the three new resolution-pin rows
  (`0`/`0`/`0` as wanted) and the two existing axis-entry rows (`1`/`1` as
  wanted).
- **Without R1** (comparisons reverted to `dir == "below"`/`dir == "above"`,
  `dirLower` declaration removed to match the actual pre-R1 shape — not just
  commented out, which would leave an unused-variable build failure): the two
  new resolution-pin rows fail (`want 0 got 1`) on exactly
  `case-insensitivity_resolution_pin:_capitalised_direction_word_must_RESOLVE,_not_merely_enter_the_axis`
  and
  `case-insensitivity_resolution_pin:_capitalised_Above_must_resolve_too_--_the_other,_separately-written_comparison`.
  The resolution *control* row (lowercase, real code) and both pre-existing
  axis-entry rows are unaffected either way, as expected — they don't depend
  on R1. This is the "gets its number for a reason other than the one it
  names" failure mode from the review's closing section, now closed for this
  pair: the new rows fail specifically when R1 is absent, not for any other
  reason.

**Criterion 2 / R5 — whole-tree scan, re-measured (not inherited) against
`aed6cec`.** `TestNoCommentClaimsClaimMachineryCallFileCodeLacks`'s own
in-package `findings`/`visited`/`commentGroups` counters, read via a
temporary `t.Logf` added and then reverted — confirmed byte-identical
(`sha256 620ef277c3eca8bf72b4b16c274448a6eaead82f025879d41b7a54b30c3f6416`
before and after, `git diff` showing no residual change):

| | findings | files | comment groups (AST groups) |
|---|---|---|---|
| After this change (`aed6cec`) | 0 | 271 | 5055 |

The 5055 (vs. the first pass's 5053) is this change's own +2 non-empty
comment groups — the new R2 doc-comment block plus the retained #0363
doc-comment block are both in-file, and the file itself is inside the scan
roots. Independently cross-checked with a standalone Go program outside the
guard's own code (`go/parser`, `ParseComments`, over `internal/` + `cmd/`):
271 files / 5055 groups / 5049 non-empty groups — matching the in-package
count exactly, and the non-empty count up by exactly 2 from the review's own
independently-measured 5047, consistent with the same +2 explanation.
**0 findings** — nothing new surfaced (criterion 5 — nothing to report).

**Criterion 3.** `TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`
(`#0342` sites A and C) — PASS, unchanged.

**Criterion 4 — flag-removal mutation, re-run against the current (post-R1/R2)
file**, in a throwaway `git archive HEAD` export under the scratchpad (never
the tracked tree): reverting `claimCommentGuardDirWordRe` to
`` \b(below|above)\b `` (dropping `(?i)`, leaving R1/R2 otherwise intact)
makes **exactly one** sub-test fail —
`case-insensitivity_regression_pin:_capitalised_direction_word_flags_too`
(`want 1 got 0`) — and nothing else moves: not the new R2 resolution rows
(the capitalised word no longer matches the regex at all without `(?i)`, so
the axis is never entered and they correctly report 0 findings on either
code path), not the control rows, not
`TestClaimCommentGuardDirectionalAxisFlagsKnownStaleSitesFromDb9bff7`, not
`TestClaimCommentGuardDirectionalAxisIsCleanOnStoreGoParenScoping`, not
`TestClaimCommentGuardIsCleanOnKnownNegativeFixture`. Matches the first
pass's mutation result exactly, as expected since R1/R2 only touch the
resolution comparison, not the regex.

**R3 — population-drift attribution, corrected.** The issue's first-pass
`## Verification` attributed the 30,873 → 30,964 drift to "other agents'
concurrent, uncommitted edits" — wrong, per the review. Every line of that
drift is **committed** work (`1467844` → `9cf6898` +83 from `#0341`'s
committed code, `9cf6898` → `f404358` +8 from that pass's own comment lines).
Re-measured for this pass with the same harness metric
(`count_comment_opening_lines`, `grep -cE '^[[:space:]]*//'` over `internal/`
+ `cmd/`), via `git archive` exports at each commit:

| commit | comment-opening lines | delta | source |
|---|---|---|---|
| `f404358` (first `#0363` pass, reviewed) | 30,964 | — | baseline |
| `c562d6f` (`HEAD` at the start of this pass) | 30,974 | +10 | `#0341`'s post-bounce commits (`c01605d`, `8f5ed55`, `c562d6f`), committed |
| working tree, this pass's uncommitted diff | 30,996 | +22 | R1's doc comment (8 lines) + R2's doc comment and three new rows (14 comment-opening lines), all now committed in `aed6cec` |

So `aed6cec`'s own contribution is +22, entirely this pass's own comment
lines, and the prior +10 is `#0341`'s **committed** work landing between the
two `#0363` passes — not uncommitted concurrent editing at measurement time,
same correction the review made for the first pass's own drift.

**R4 — clarifying sentence.** The issue's `## Description` leads with
sentence-initial `// Below, ClaimDue …` as the motivating shape. That shape
is **still 0 findings** after this fix, and correctly so: the window looks
strictly backwards from the direction word
(`window := flat[lo:dm[0]]`), and in that sentence the identifier `ClaimDue`
follows the direction word rather than preceding it, so it never enters the
window at all. This is the separately-pinned
`identifier-after-direction-word false negative` trade
(`claimCommentGuardDirectionalCalibrationCases`), frozen by `#0358`
criterion 4, and this issue does not change it — R1/R2 only fix the
resolution comparison for direction words that *do* enter the axis with a
resolvable identifier already in the backward window. An unqualified "the
capitalised case now works" would repeat exactly the misreading that misled
`#0347`'s third review of this same fixture.

**Criterion 6.** `scripts/go_file_visit_floor_guard_test.sh`, via
`scripts/check.sh guards`: `claimKindsGuardMinPlausibleCallSiteCount`
population **42** (unchanged), `claimKindsGuardMinPlausibleNonExemptCallSiteCount`
population **12** (unchanged). `TestNoClaimMachineryCallSyntaxLiteralInAnyComment`
still PASS (no `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` literal in any
comment — the three new fixtures were checked specifically, per the
implementation brief).

**Criterion 7.** `sha256(internal/outbox/claim_comment_referent_guard_test.go)`
== `sha256(gofmt stdout)` — both
`620ef277c3eca8bf72b4b16c274448a6eaead82f025879d41b7a54b30c3f6416`. `gofmt -l`
reports clean via `scripts/check.sh`.

**Full runs:**

```
ISSUE=0363b scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
  # internal/outbox ok 0.767s (targeted sub-test run), full ok 0.732s,
  # internal/mailing ok 3.800s, internal/handlers ok 8.691s
  # zero skips, VERIFICATION PASSED
scripts/check.sh guards
  # all guard harnesses PASS, 0 failures, VERIFICATION PASSED
  # lsof -nP -iTCP:5173 -sTCP:LISTEN checked empty before and after; no pkill -f used
```

Own scratch database (`opencircuit_test_0363b`, provisioned via a full
`migrate up` per the `#0360` environment note — expected, not a regression)
dropped by `scripts/check.sh`'s own cleanup and confirmed absent afterward
(`scripts/testdb.sh drop 0363b` reported "does not exist"). `pgrep -f
'go test|\.test '` empty at the end of the session.

Commit `aed6cec` — `git show --stat HEAD` confirmed exactly one file,
`internal/outbox/claim_comment_referent_guard_test.go`, 40 insertions / 2
deletions.

Status left `in-progress` for review (§9) — this pass does not mark issues
`resolved`.
