# 0255 — Four more shapes the audit-metadata guard misses, one carrying an email

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | handlers / tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-24 |
| **Commit** | `1c6f616` (implement) |

## Description

`#0252` closed the deferred-assignment blind spot: `e := audit.Entry{}` followed
by `e.Metadata = map[string]any{"email": …}` now reports `unresolved` instead of
passing silently. Its review confirmed that fix, mutation-proved all four new
pins, and ran fourteen constructed shapes against the corrected header without
falsifying it.

It then found **four more shapes**, all predating that pass. Three are silent
passes; one of those carries an email.

**1. A one-line variant defeats the new check — and this is the consequential
one.**

```go
e := audit.Entry{Metadata: map[string]any{"slug": s}}
e.Metadata = map[string]any{"email": addr}
```

→ `hasEmail=false, unresolved=""`. **A silent pass carrying an email.**

The new path is gated on `if !metadataKeyFound`, so it never fires when the
literal *also* sets `Metadata` — and classification then comes from the
literal's map rather than from the assignment that actually wins at runtime.
The guard reads the losing value.

**2. An entry held in a struct field is invisible to both halves.**
`w.e = audit.Entry{}` then `w.e.Metadata = {"email": …}` → silent pass. Both
helpers require a bare `*ast.Ident`.

**3. Pointer mutation through a helper is not modelled.**
`e := audit.Entry{}` then `setMeta(&e)` → silent pass.

**4. Two false positives, both failing closed.**
`collectDeferredMetadataFieldIdents` matches on identifier *name* across the
whole function body, with no scope analysis and no ordering. So a variable of a
different type also named `e` in a disjoint block, or an `e.Metadata =`
appearing *before* `e = audit.Entry{…}`, each make an innocent site report
`unresolved`. The direction is safe — it fails closed — but **the message names
the wrong cause**, which costs whoever hits it real time.

## Root cause

`#0252`'s deferred-struct-field-assignment check
(`collectDeferredMetadataFieldIdents` + `compositeLitAssignedIdents`,
`internal/handlers/audit_email_metadata_guard_test.go`) had four unhandled
edges, confirmed by running each one through `scanAuditEntrySites` before
touching anything (see `## Verification` for the exact commands and output):

1. `scanAuditEntrySites`'s deferred-assignment check only ran
   `if !metadataKeyFound`, so a literal that ALSO set its own `Metadata` key
   skipped the check entirely and classification fell through to
   `classifyMetadataExpr` on the literal's own map — the value that **loses**
   at runtime, since the later `e.Metadata = ...` assignment is what the
   running code actually inserts. Reproduced exactly as the issue describes:
   `hasEmail=false, unresolved=""`.
2. `w.e = audit.Entry{}` / `w.e.Metadata = ...` — both
   `compositeLitAssignedIdents` (`ident := audit.Entry{...}`) and
   `collectDeferredMetadataFieldIdents` (`ident.Metadata = ...`) require a
   bare `*ast.Ident` on the relevant side of the assignment. `w.e` is an
   `*ast.SelectorExpr` on both sides, so neither helper ever sees it.
   Reproduced: `hasEmail=false, unresolved=""`.
3. `setMeta(&e)` — `collectDeferredMetadataFieldIdents` only inspects
   `*ast.AssignStmt` nodes matching `ident.Metadata = ...` directly in the
   enclosing function's body; a helper call that mutates the field through a
   pointer argument produces no such node, so it is invisible regardless of
   what the call does. Reproduced: `hasEmail=false, unresolved=""`.
4. Both false positives trace to the same root: `collectDeferredMetadataFieldIdents`
   matches a later `.Metadata = ...` assignment by identifier **name** only —
   no `go/types` scope resolution, no statement-order check against the
   literal's position. A same-named variable of an unrelated type in a
   disjoint block, and a `.Metadata = ...` that textually precedes the
   literal it gets attributed to, both trip the check and produce
   `unresolved`, with a message that asserted a same-variable runtime
   override the lookup had not actually established. Reproduced both:
   `unresolved` fires in both cases with the old (misleading) wording.

## Fix

- **Shape 1 (priority, fixed):** removed the `if !metadataKeyFound` guard
  around the deferred-assignment check in `scanAuditEntrySites`, so it now
  runs unconditionally whenever `literalIdents[cl]` resolves to an
  identifier that also appears in `deferredMetadataIdents` — regardless of
  whether the literal itself set a `Metadata` key. The site is reported
  `unresolved` either way, matching this guard's conservative default
  everywhere else (unclassifiable fails, it never passes).
- **Shapes 2 and 3 (pinned, not fixed):** both are structural blind spots
  the codebase does not currently produce (confirmed:
  `grep -rn '\.Metadata\s*=' internal cmd --include='*.go'` excluding
  `_test.go` files finds exactly one hit tree-wide,
  `internal/audit/read.go:159`, an unrelated type's field — no production
  `audit.Entry` is ever held in a struct field or mutated through a pointer
  argument). Following `#0252`'s own approach ("pin what you do not close"),
  added two new subtests to `TestAuditEmailMetadataGuardDocumentedSilentPassShapes`
  rather than attempting a structural fix (which would need `go/types`-level
  identifier resolution, a materially larger change for shapes with zero
  live risk today).
- **Shape 4 (message fixed, detection unchanged):** reworded the
  deferred-assignment failure message to describe what was actually found —
  "a struct-field assignment (%s.Metadata = ...) appears somewhere in this
  function ... possibly before or after this ... literal, and possibly on an
  unrelated variable of the same name, since this guard matches by
  identifier name only and has no scope or statement-order awareness" —
  instead of asserting a specific same-variable override. The detection
  still fails closed (unresolved, never silent) for both false-positive
  shapes; only the message's claim changed. Pinned by a new test,
  `TestAuditEmailMetadataGuardDocumentedFalsePositiveShapes`.
- Updated the file's header comment with a new `#0255` paragraph block (in
  the same falsifiable style `#0237`'s fourth pass and `#0252`'s pass used)
  describing all four shapes, which were fixed/pinned, and re-stating that
  the 45-site/87-key census is unchanged.
- Added `TestAuditEmailMetadataGuardFailsWhenLiteralAlsoSetsMetadataThenOverridden`,
  mutation-proved against the pre-fix gate (see `## Verification`), pinning
  shape 1's fix the same way `#0252`'s own fix was pinned.

## Verification

**Reproduced all four shapes through `scanAuditEntrySites` before writing any
fix**, via a temporary test file
(`internal/handlers/zzz_0255_repro_test.go`, deleted before this pass ends —
never committed):

```
=== RUN   TestZZZ0255Shape1LiteralAlsoSetsMetadata
    shape1: hasEmail=false unresolved=""
=== RUN   TestZZZ0255Shape2StructField
    shape2: hasEmail=false unresolved=""
=== RUN   TestZZZ0255Shape3PointerHelper
    shape3: hasEmail=false unresolved=""
=== RUN   TestZZZ0255Shape4FalsePositiveScopeCollision
    shape4-scope: hasEmail=false unresolved="Metadata is set via a struct-field
    assignment (e.Metadata = ...) after this audit.Entry{} literal, not inside
    it — ..."
=== RUN   TestZZZ0255Shape4FalsePositiveOrdering
    shape4-ordering: hasEmail=false unresolved="Metadata is set via a
    struct-field assignment (e.Metadata = ...) after this audit.Entry{}
    literal, not inside it — ..."
PASS
```

All five matched the issue's description exactly: shapes 1–3 silent
(`hasEmail=false, unresolved=""`), shape 4's two variants both firing
`unresolved` with the old, overclaiming message.

**After the fix**, the same five fixtures:

```
shape1: unresolved="a struct-field assignment (e.Metadata = ...) appears
  somewhere in this function (this literal also sets its own Metadata, which
  that assignment may or may not override) — possibly before or after this
  audit.Entry{} literal, ..."   [was silent; now fails closed]
shape2: hasEmail=false unresolved=""    [unchanged — pinned, not fixed]
shape3: hasEmail=false unresolved=""    [unchanged — pinned, not fixed]
shape4-scope:    unresolved="a struct-field assignment (e.Metadata = ...)
  appears somewhere in this function — possibly before or after this ...
  literal, and possibly on an unrelated variable of the same name, ..."
shape4-ordering: unresolved="a struct-field assignment (e.Metadata = ...)
  appears somewhere in this function — possibly before or after this ...
  literal, and possibly on an unrelated variable of the same name, ..."
```

**Full guard suite**, `ISSUE=0255 scripts/check.sh go ./internal/...`:

```
=== machine
20:32  up 4 days,  6:43, 17 users, load averages: 2.35 1.97 1.97
=== isolated test database for issue 0255
postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test_0255?sslmode=disable
=== go build
=== go vet
=== gofmt -l (#0161 — formatting drift)
(clean)
=== go test ./internal/... -p 2
ok  	github.com/brennanMKE/OpenCircuitSF/internal/audit	0.294s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/auth	0.789s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/config	0.333s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.265s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/devstore	0.214s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/events	0.472s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	3.122s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/interests	2.902s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	0.961s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/middleware	0.918s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/seo	0.235s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/sesnotify	0.377s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/subscribers	0.479s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/testdb	0.900s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/workshops	0.738s
=== skip audit — a package reporting [no test files] or 'no test files' proves nothing
(no skips)
=== leftover processes you may have started
(none)
VERIFICATION PASSED
```

Fifteen packages, all `ok`, zero skips, `gofmt -l` clean.

**Guard-specific tests, individually** (`go test ./internal/handlers/... -run
'TestAuditEmailMetadataGuard|TestAuditEntryEmailMetadataMatchesKnownSites' -v`):
all pass, including the two new subtests under
`TestAuditEmailMetadataGuardDocumentedSilentPassShapes` (`entry_held_in_a_struct_field,_not_a_bare_identifier`,
`pointer_mutation_through_a_helper_function`), the new
`TestAuditEmailMetadataGuardFailsWhenLiteralAlsoSetsMetadataThenOverridden`,
and the new `TestAuditEmailMetadataGuardDocumentedFalsePositiveShapes` (both
subtests).

**Census re-derived, not assumed**, with a second temporary test
(`internal/handlers/zzz_keycensus_test.go`, also deleted before this pass
ends) that walks the tree using the guard's own key-extraction functions
(`compositeLitStringKeys`, `collectLocalMapKeys`) rather than a regex
approximation, and also resolves `credentialMetadata`'s own return literal
the same way the guard does:

```
sites=45 distinctKeys=87 unresolved(non-guard-classified)=0
```

Exactly **45 sites, 87 distinct keys, 0 unresolved** — matching the pinned
figures with no site newly reporting unresolved. `TestAuditEntryEmailMetadataMatchesKnownSites`
itself also passed unchanged, confirming no drift against
`auditEmailMetadataKnownSites`.

**Mutation-proved the shape-1 fix and the shape-4 message fix**, per
`CLAUDE.md` §8a discipline (copy aside, mutate, confirm the failure,
restore, prove byte-identity):

```
$ shasum -a 256 audit_email_metadata_guard_test.go /tmp/....before-mutation
c2e0466b7c2d6ca732103cd5e35492453033dce9cca9dad373154887ecab2262  (before)
```

Reverted the fix back to the original `if !metadataKeyFound { ... }`-gated
check, rebuilt, and re-ran
`TestAuditEmailMetadataGuardFailsWhenLiteralAlsoSetsMetadataThenOverridden`:

```
--- FAIL: TestAuditEmailMetadataGuardFailsWhenLiteralAlsoSetsMetadataThenOverridden (0.00s)
    expected the site to be reported as unresolved — the literal's own
    "slug"-only Metadata is not what the running code uses; ...
FAIL
```

Confirmed the test fails exactly as expected against the pre-fix gate, then
restored the file and confirmed byte-identity:

```
$ shasum -a 256 audit_email_metadata_guard_test.go /tmp/....before-mutation
c2e0466b7c2d6ca732103cd5e35492453033dce9cca9dad373154887ecab2262
c2e0466b7c2d6ca732103cd5e35492453033dce9cca9dad373154887ecab2262
```

Re-ran the full guard suite after restoring — all pass, matching the runs
above.

No database was needed for this change (pure Go/AST); the only shared
resource touched was `internal/handlers/audit_email_metadata_guard_test.go`
itself, which is this issue's own scoped file (per dispatch instructions,
concurrent agents were working `docs/db_reset_guard_test.sh`,
`scripts/dev_guard_test.sh`, `scripts/testdb_gc_guard_test.sh`, and possibly
`internal/outbox`/`internal/mailing`/`internal/subscribers`/`migrations/` —
none of those were touched).

## Files changed

- `internal/handlers/audit_email_metadata_guard_test.go` — removed the
  `if !metadataKeyFound` gate around the deferred-assignment check (shape 1
  fix); reworded its failure message to describe a same-named struct-field
  assignment found "somewhere in this function" rather than asserting a
  specific override (shape 4 message fix); added two subtests to
  `TestAuditEmailMetadataGuardDocumentedSilentPassShapes` pinning shapes 2
  and 3 as documented, unfixed gaps; added
  `TestAuditEmailMetadataGuardFailsWhenLiteralAlsoSetsMetadataThenOverridden`
  pinning the shape 1 fix; added
  `TestAuditEmailMetadataGuardDocumentedFalsePositiveShapes` pinning shape
  4's two false-positive variants; added a new `#0255` paragraph block to
  the file's header comment.

## Acceptance criteria

- [x] Shape 1 no longer passes silently — reports `unresolved` unconditionally whenever the identifier is later mutated via `.Metadata = ...`, since this guard cannot prove which value (the literal's or the later assignment's) the running code actually uses — see `TestAuditEmailMetadataGuardFailsWhenLiteralAlsoSetsMetadataThenOverridden`, mutation-proved against the pre-fix gate
- [x] Shapes 2 and 3 **pinned** the way `#0252` pinned its three blind spots — two new subtests in `TestAuditEmailMetadataGuardDocumentedSilentPassShapes` ("entry held in a struct field, not a bare identifier" / "pointer mutation through a helper function"); neither shape exists in production code today (confirmed by grep, see `## Root cause`)
- [x] Shape 4's failure message now says what it actually detected — a same-named struct-field assignment appearing "somewhere in this function," hedged with "possibly before or after ... and possibly on an unrelated variable of the same name" — rather than asserting a same-variable override; detection itself still fails closed (no scope/ordering awareness added). Pinned by `TestAuditEmailMetadataGuardDocumentedFalsePositiveShapes`
- [x] Every shape run through `scanAuditEntrySites` directly, both before and after the fix, via temporary test files deleted before this pass ends — see `## Verification` for exact output, not reasoning
- [x] Census re-derived with the guard's own key-extraction functions (not the production `scanAuditEntrySites` alone, which only proves site/unresolved counts — a second temporary walker also counted distinct keys): **45 sites, 87 distinct keys, 0 unresolved**, no site newly reporting unresolved; `TestAuditEntryEmailMetadataMatchesKnownSites` passed unchanged
- [x] Header updated with a new `#0255` paragraph block in the established falsifiable style, naming all four shapes, which were fixed vs. pinned, and the unchanged census
- [x] `ISSUE=0255 scripts/check.sh go ./internal/...` passes with zero skips — see `## Verification`

## Notes

Reported by `#0252`'s phase-3 review under `CLAUDE.md` §9. None of the four
falsifies the committed text, but all sit directly beside a "now FIXED" claim,
which is what makes them worth closing together rather than leaving as known
gaps.

**Shape 1 deserves priority over the other three.** Shapes 2 and 3 are
structural blind spots of a kind the codebase does not currently produce.
Shape 1 is a *near-miss of the fix itself* — one extra field in the literal and
the new path silently declines to run — and it is a shape a developer would
write without thinking, since initialising some metadata inline and replacing it
later is ordinary code.

**The pattern across this whole file, now six rounds deep**, is that each fix
closes the shape it was shown and leaves the adjacent one open. `#0252`'s
approach — pin what you do not close — is the thing that has actually worked;
prefer it over chasing completeness.

## Relation

- Found by: [#0252](0252.md) — the pass whose fix shape 1 slips past
- Relates to: [#0237](0237.md) — the guard and its boundary-description style
- Relates to: [#0226](0226.md) — the privacy page this guard protects
- Relates to: [#0126](0126.md) — which adds a subscriber-event log and must not defeat this guard

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-25 | implement | claude-sonnet-5 | — | — | — | — | — (subagent cannot self-measure totals, `Issues.md` "Token usage" — dispatching orchestrator to fill in from this session's transcript) |
