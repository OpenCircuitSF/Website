# 0251 — `check.sh`'s pipefail guard cannot see its own definition

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | infra / tooling |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-08-24 |
| **Commit** | `2bc5534` |

## Description

`#0208` replaced a per-call-site pipefail grep with a single `runpipe()` funnel
and a marker-delimited self-check. A review ran **nine evasion mutants** against
it and eight were caught, including every form that defeated the old grep
(`/bin/bash -c`, `env bash -c`, a split `bash \`+newline+`-c`, `$SH -c`). It
also proved the self-check cannot flag itself in both directions.

Two gaps remain, and the first is a real regression against what the old check
provided:

**1. Nothing asserts that `runpipe()`'s own definition still spells
`-o pipefail`.** The guard deletes the `GUARD-0208` block before scanning, so
the definition is invisible to it. Delete `-o pipefail` from that one line and
**every check still passes** — while the property the whole issue exists to
enforce is gone. The review demonstrated this: with the flag stripped, a test
that printed `--- FAIL` still produced `VERIFICATION PASSED` and exit 0.

The old per-call-site grep had this positive assertion by construction, because
it looked *for* the flag rather than for its absence elsewhere. The refactor
made the structure better and lost the one check that mattered most.

**2. `run "$SHELL" -c "… | tail"` evades all three checks.** Contrived — nobody
writes that — and recorded for completeness rather than as a live concern.

## Acceptance criteria

- [x] A positive assertion exists that `runpipe()`'s definition sets `-o pipefail`, and it fails if the flag is removed
- [x] Proved by mutation: strip the flag, confirm the check fails loudly, restore, prove byte-identity with `shasum -a 256`
- [x] The assertion cannot be satisfied by the string appearing anywhere else in the file — it must be the definition
- [x] `#0208`'s existing nine-mutant evasion coverage still passes unchanged
- [x] Item 2 is either closed or recorded in the guard's comment as a known, accepted evasion
- [x] `bash -n` and `shellcheck` clean

## Implementation notes

Added a new check inside the existing `GUARD-0208` marker block (so it is
itself excluded from the negative scan, the same protection every other
check in that block already has), right after the existing call-count check
and before `# END GUARD-0208`:

```sh
RUNPIPE_DEF="$(sed -n "/${BR}/,/${ER}/p" "$0" | grep -vE '^[[:space:]]*#')"
if ! printf '%s\n' "$RUNPIPE_DEF" | grep -qE 'run[[:space:]]+bash[[:space:]]+-o[[:space:]]+pipefail[[:space:]]+-c'; then
  printf '...PIPEFAIL REGRESSION (#0208/#0251)...' >&2
  exit 2
fi
```

`$BR`/`$ER` are the same `RUNPIPE-0208` marker variables the existing
negative scan already builds (two concatenated string literals, so this
line's own source never spells the marker contiguously — same technique
`#0208` used for `$BG`/`$EG`). `sed -n "/${BR}/,/${ER}/p"` is the print-range
complement of the delete-range the negative scan already uses on the same
markers — it extracts runpipe()'s own definition **from `"$0"`**, not from
`$SCAN` (which has that block deleted), which is exactly why the existing
checks were blind to it. Comment lines inside the extracted range are
stripped so a mention of the phrase in a comment can't satisfy the match —
in practice the range currently contains only the `BEGIN`/`END` marker
comments and the one-line function definition, so after stripping,
`$RUNPIPE_DEF` is exactly that one line.

Scoping the extraction to the marker-delimited range (rather than grepping
the whole file for the phrase) is what satisfies "cannot be satisfied by the
string appearing anywhere else" — a call site elsewhere in the file that
happened to spell `run bash -o pipefail -c` would not be inside the
`RUNPIPE-0208` range and so could not make this check pass; only the actual
definition, sitting inside its own marker block, can.

**Item 2** (`run "$SHELL" -c "…"` evades every check) is not closed — it
remains genuinely unreachable through this design's grep-based approach, and
closing it would need a real shell parser. Recorded instead as a comment
directly above `step "self-check: pipefail funnel guard (#0208)"`, next to
the existing "what must NOT appear" paragraph, so a future reader sees it in
the same place they'd look for the guard's other known boundaries.

## Verification

- `bash -n scripts/check.sh` — clean.
- `shellcheck scripts/check.sh` — the same two pre-existing findings as
  before this change (`SC2164` on `cd "$REPO"`, `SC2086` on `$pkgs`); a
  third, new `SC2016` finding on the new regression message's `printf` (it
  contained a literal `"$0"` in a single-quoted string) was fixed by
  rewording the message rather than suppressing the warning — confirmed
  clean of new findings afterward.
- **The guard passes on the real, unmutated script**: `bash scripts/check.sh
  guards` and `ISSUE=0251chk scripts/check.sh go ./internal/config/...` both
  printed the `=== self-check: pipefail funnel guard (#0208)` step with no
  output following it (no regression detected), then proceeded normally.
- **Mutation proof (the core of this issue).** Built a private mutant
  (`scripts/.check_mutant_0251_stripflag.sh`, untracked, removed after) with
  `-o pipefail` stripped from `runpipe()`'s definition only (`runpipe() {
  run bash -c "$1"; }`). `bash scripts/.check_mutant_0251_stripflag.sh
  guards` printed
  `PIPEFAIL REGRESSION (#0208/#0251): runpipe()'s own definition no longer
  spells "run bash -o pipefail -c" — …` and exited **2**, before running
  anything else — confirming the check fails loudly and fails fast.
- **The gap this closes, reproduced directly** (not inferred): checked out
  `scripts/check.sh` as it stood immediately before this fix
  (`git show 9df49c1:scripts/check.sh`, the commit right before this one) into
  a private copy, applied the identical flag-strip mutation, added a
  temporary always-failing test
  (`internal/config/zzz_temp_0251_proof_test.go`, `t.Fatal`), and ran
  `ISSUE=0251old <old-mutant> go ./internal/config/...`: printed
  `--- FAIL: TestTemp0251ProofFailure`, `FAIL
  github.com/brennanMKE/OpenCircuitSF/internal/config`, then
  **`VERIFICATION PASSED`, exit 0** — the exact gap #0251 describes,
  reproduced against this codebase rather than assumed from the issue text.
  Ran the identical failing test against a same-shaped mutant built from the
  **current, fixed** `scripts/check.sh`
  (`scripts/.check_new_0251_mutant.sh`): the self-check fired immediately
  (`PIPEFAIL REGRESSION (#0208/#0251)`, exit 2) — the test suite never even
  ran, so the false "PASSED" is no longer reachable. Temp test file removed
  afterward; `git status --porcelain -- internal/config/` empty.
- **Byte-identity.** `shasum -a 256 scripts/check.sh` before any mutation
  work (`12a124f42ee217afbf95d782c843a69f9d457c40298129f7653f7efbcaa5545b`)
  matched the same hash after all mutant files were removed — every mutation
  ran against private untracked copies in `scripts/` (never `/tmp`, for the
  same `dirname`-resolves-`REPO` reason `db_reset_guard_test.sh`'s header
  documents), never the tracked file.
- **`#0208`'s nine-mutant table, re-run unchanged against the current
  script**, using the review's own discriminator (`bash <mutant> -count=5`
  — the funnel guard runs before the `-count` ban check, so the output
  cleanly distinguishes "guard caught it" from "guard passed, script moved
  on"):

  | # | mutation | result (this run) | matches #0208's review |
  |---|---|---|---|
  | 1 | `run /bin/bash -c "…"` | caught — shell `-c` | yes |
  | 2 | split `run bash \` newline `-c "…"` | caught — call count 8 | yes |
  | 3 | bare `bash -c "…"` added outside `run` | caught — shell `-c` | yes |
  | 4 | `run env bash -c "…"` added | caught — shell `-c` | yes |
  | 5 | `run zsh -o pipefail -c "…"` added | caught — second `pipefail` | yes |
  | 6 | `SH=bash; run "$SH" -c "…"` added | caught — shell `-c` | yes |
  | 7 | `run "$SHELL" -c "…"` added | **not caught** — reaches the `-count=5 is banned` check | yes (documented gap) |
  | 8 | one `runpipe` call site deleted | caught — call count 8 | yes |
  | 9 | `run /bin/bash -c "true"  # pipefail` | caught (via the `pipefail`-spelling check this run, vs. shell-`-c` in the original report — same outcome, different check fired first) | yes |

  8 of 9 caught, mutant 7 not caught — identical to `#0208`'s own review
  table. All nine mutant files removed afterward;
  `git status --porcelain -- scripts/` showed only `scripts/check.sh`
  (the legitimate fix) dirty throughout.
- `ISSUE=0251full scripts/check.sh` (bare default, full suite) —
  **VERIFICATION PASSED**; `scripts/check.sh guards` — **VERIFICATION
  PASSED**, all three guard scripts holding.
- Own databases `opencircuit_test_0251chk`, `_0251old`, `_0251new`,
  `_0251full` — all created and dropped by `scripts/check.sh`/`scripts/testdb.sh`
  itself.

## Files changed

- `scripts/check.sh` — new positive assertion inside the `GUARD-0208` block
  that `runpipe()`'s own definition (extracted from `"$0"` between the
  `RUNPIPE-0208` markers) still spells `-o pipefail`; a comment recording
  the unclosed `"$SHELL" -c` evasion as a known, accepted gap.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Exact token counts pending the orchestrator's transcript accounting
(`issues/Issues.md` "Getting exact token counts") — this subagent has no
access to its own usage totals to report here without fabricating them.

## Notes

Reported by `#0208`'s phase-3 review under `CLAUDE.md` §9.

**This is the guard-blind-spot class, applied to a guard.** `#0224` found a
fence checker that masked a phantom row; `#0230` found a lint counting one
backslash instead of parity; `#0237`'s guard missed the key style its own
codebase uses. Here a self-scanning guard is blind to precisely the line that
carries the property.

Worth noting the review found this by asking "what does this guard *not* look
at?" rather than by testing what it does. That question is the one that has
found every guard defect in this project.

## Relation

- Found by: [#0208](0208.md) — the funnel this completes
- Relates to: [#0224](0224.md), [#0230](0230.md), [#0237](0237.md) — the guard-blind-spot class
- Relates to: [#0140](0140.md) — the original "check.sh can now fail" work
