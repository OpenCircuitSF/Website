# 0357 — Two sentences in CLAUDE.md §8's satisfier entry invert when read alone

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | docs |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | — |

## Description

Two prose defects in `CLAUDE.md` §8's satisfier-set entry. Neither misleads a
reader who reads the surrounding sentence — both do when quoted or skimmed,
**which is exactly how `#0346`'s inversion propagated**: illustrative prose was
lifted out of context, transcribed into an issue as though measured, copied
into `CLAUDE.md`, and survived a full verification pass before a three-tree
`go/types` measurement caught it.

**1. A triple negative predicated of the wrong thing.** `#0350` added:

> — a type whose own method the commit never edited, though not a file it never
> touched:

Three negations in sixteen words, and "not a file…" is predicated of a *type*.
Excerpted without the gloss that follows the colon, it asserts the opposite of
the fact.

**Exact remedy** (from `#0350`'s review, which reported it rather than patching
it): replace with

> — a type whose own code the commit never changed, in a file it did touch:

One negation, no category slip, one word shorter, both facts intact. Everything
after the colon stays byte-identical.

**2. A dangling "it" that is now false under the likelier binding.** In the
value-receiver paragraph, untouched since `#0346`:

> `go/types` is the sound oracle, since … not something an AST walk can fully
> reconstruct on its own (**it** also can't see a method acquired by embedding
> … or hiding behind a generic receiver …)

The intended antecedent is *the AST walk*. A reader can bind "it" to
`go/types` — and since `#0349`/`#0353` that reading is **false**, and
contradicted by the very file the sentence points at, where
`types.Implements` resolves promotion and generic receivers.

**Exact remedy**: change "it also can't see" to "an AST walk also can't see".

## Acceptance criteria

1. Both replacements applied exactly as written above.
2. Everything else in the entry byte-identical — in particular everything after
   the colon in defect 1, and the rest of the value-receiver paragraph.
3. **Re-read the whole entry adversarially afterwards** and report any third
   sentence with the same property: correct in place, inverting when quoted.
   Do not fix a third one silently; report it.
4. Documentation only. `## 8a.` through EOF must be byte-identical (that covers
   §11's PRD table, guarded by `internal/db/prd_section_index_test.go`), the
   entry stays at the end of §8 before `## 8a.`, and no other §8 entry moves.
5. Any factual claim touched is re-verified from `git show` or a measurement,
   not copied from this issue. This entry has been wrong in print once already.

## Relation

- Found by: [#0350](0350.md)'s review, which resolved that issue and supplied
  both remedies verbatim
- Depends on: [#0350](0350.md) — which introduced defect 1
- Relates to: [#0346](0346.md) — which introduced defect 2 and whose bounce is
  the precedent for why quotable-in-isolation matters
- Relates to: [#0349](0349.md), [#0353](0353.md) — which made defect 2's
  misreading false rather than merely ambiguous
- Relates to: [#0356](0356.md) — the same family: prose that drifts out of
  agreement with the code it describes

## Note on the guard's current shape

Worth recording, because dispatches have repeated a stale description of it:
`internal/seo/invalidator_satisfier_guard_test.go` is **no longer an AST
scan**. Since `#0349` it is `go/types.Implements` over the package's full
named-type set, loaded via `golang.org/x/tools/go/packages` with
`GOOS=linux GOARCH=arm64` pinned to production (§7). It does **not** refuse to
answer on embedding — that tripwire and its allowlist were deleted — and it is
**not** build-tag-blind; the pin is what preserves the linux-only case.
`#0353`, `#0354` and `#0355` then added the alias branch and pinned it.

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0350`'s review (§9).
