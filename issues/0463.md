# 0463 — The closer's trailing-text leniency is defended by the same argument `#0460` retired

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | db / guards |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |
| **Closed** | 2026-09-05 |

## Description

`#0460` gated `issueStatus`'s fence closer on indentation but deliberately kept
its **trailing-text** leniency — a closer carrying trailing text still closes —
justified in the reworded doc comment as "closing it would trade a loud failure
for a silent one".

**That is the same one-sided direction argument `#0460` was filed to retire, one
level out.** `#0455` kept the closer lenient because closing early "can only
un-hide a row"; `#0460` exists because the closer's leniency feeds a *later
opener*, producing a dangerous divergence neither branch produces alone. The
trailing-text half is defended by the identical reasoning and, measurably, has
the identical flaw.

## The measurement, which is also the acceptance evidence

`#0460`'s reviewer generated **22,620 documents** — every sequence of length 1–4
over a 12-token fence alphabet, each followed by a metadata table — and scored
them against a purpose-written CommonMark fenced-code-block reference:

| | agrees | safe divergence | dangerous divergence |
|---|---|---|---|
| pre-`#0460` | 12,199 | 9,141 | 1,280 |
| post-`#0460` | 15,727 | 6,147 | **746** |

A large net improvement — **but not monotone.** 660 dangerous documents were
fixed and **126 became newly dangerous** that were correct before. All 126 are
length 4 and all contain a fence line indented four or more columns. **96 of the
126 require the trailing-text closer**; the other 30 require the tilde or
fence-length blindness `#0455`'s criterion 3 protects.

Minimal witness: opener at column 0, an indented triple-backtick, a
triple-backtick with an info string, then a bare triple-backtick. CommonMark
closes once and reads the row; the current walk hides it.

## Scope and the stopping rule

**Corpus exposure is nil** — every genuine issue file carries its status row at
line 5 with nothing fence-shaped above it — and has been through this whole
chain. This is fidelity work on a parser, not a live defect.

`#0460`'s review reported rather than bounced, on the precedent by which
`#0455`'s review reported `#0460`: no implementation obeying the stated criteria
could have avoided those 126.

**This should be the last level of this chain unless the measurement says
otherwise.** `CLAUDE.md` §8 records `#0258` spending six passes hardening a file
against shapes the previous pass had been shown, each addition buying the next
bounce. The distinguishing fact here is that the numbers are converging rather
than circling — 1,280 → 746 dangerous, with a named residual and its exact
composition. Criterion 4 exists to keep it that way.

## Acceptance criteria

1. The closing branch requires the fence line to carry no trailing text after
   its backtick run, reaching parity with `docTableRowLines`' closer on the
   half `#0092` hardened and `#0460` declined.
2. **Re-derive the 22,620-document measurement** before and after. Do not copy
   the figures above — every number inherited without measurement in this
   repo's recent history has been wrong at least once.
3. Confirm the four safe-direction controls and every earlier probe still hold:
   `#0454`'s fenced-example-row case, `#0455`'s three opener cases, and
   `#0460`'s three-line sequence.
4. **The change must reduce the dangerous count and must not create a new
   newly-dangerous set larger than the 30 attributable to tilde and
   fence-length blindness.** If it does, stop and report rather than adding a
   fourth level — that is the stopping rule, and reporting it is the correct
   outcome, not a failure.
5. Re-derive the corpus counts and confirm **zero classification changes**
   across all issue files. `#0460`'s review measured 462 files, 461 with a
   status, 30 `open`/`in-progress`.
6. **No new mechanism, no new helper, no new file** (§8, `#0258`). Do not
   refactor `docTableRowLines` — `#0455`'s criterion 2 and `#0176`'s `wontfix`
   both still stand.

## Relation

- `#0454` built the fence walk; `#0455` made the opener strict; `#0460` gated
  the closer on indentation, and its review measured this.
- `#0092` hardened the analogous closer in `docTableRowLines`.
- `#0176` is `wontfix` on the regex-versus-CommonMark trade.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/db/... ./internal/handlers/...`. Scan bytes with `python3`, never BSD `grep -P`.

## Work log

- 2026-09-05 — **Implemented (phase 2, Sonnet).**

  **Criterion 1 — the branch condition.** `issueStatus`'s closing branch
  previously left the fenced state on any triple-backtick line at 3 or fewer
  columns of indentation, with no check on what followed the backtick run.
  Added the missing half: the run must additionally account for the whole
  trimmed line (`run == len(trimmed)`), reusing `leadingRunLength` (already
  computed for the branch) and the existing `leadingIndentWidth` check
  unchanged — the same "run accounts for the whole trimmed line" rule
  `docTableRowLines`'s closer already applies via its own `run == len(trimmed)`
  check. No new helper; `docTableRowLines` itself untouched (confirmed by
  diff — the only files this pass modifies are the guard test file and this
  issue file).

  **Criterion 2 — the 22,620-document measurement, re-derived rather than
  copied.** Built a standalone `python3` script implementing three functions
  over the same 12-token fence alphabet described in `#0460`'s review (bare,
  3-space-indented, 4-space-indented and tab-indented triple-backtick lines;
  bare and 4-space-indented 4-backtick lines; a triple-backtick line with an
  ordinary info string and one with a backtick in its info string; bare and
  4-space-indented tilde lines; a plain prose line; a blank line) — every
  sequence of length 1–4 over that alphabet, each followed by a metadata
  table ending in a status row, is exactly 12 + 144 + 1,728 + 20,736 =
  22,620 documents, confirming the corpus's own arithmetic independently of
  trusting the prior review's count. The three functions: a line-by-line
  reimplementation of the pre-`#0463` `issueStatus` body (closer gated on
  indentation only), a reimplementation of the post-`#0463` body (closer also
  gated on trailing text), and a from-scratch CommonMark-faithful reference
  mirroring `docTableRowLines`'s fence state machine (fence character, opening
  run length, the 3-column indent cap on opener and closer, the
  backtick-in-info-string rule, and the closer's own "run accounts for the
  whole trimmed line" requirement) — no CommonMark library exists on this
  machine, so this reference was written for this pass rather than reused.

  Scoring every document as agreeing, safely divergent (the walk reads a row
  the reference hides), or dangerously divergent (the walk hides a row the
  reference reads):

  | | agrees | safe divergence | dangerous divergence |
  |---|---|---|---|
  | pre-`#0463` (closer gated on indentation only) | 15,727 | 6,147 | 746 |
  | post-`#0463` (closer also gated on trailing text) | 19,331 | 3,105 | **184** |

  The pre-`#0463` row reproduces `#0460`'s review's reported post-`#0460`
  figures exactly (15,727 / 6,147 / 746), which is the cross-check that this
  independently-written scoring script agrees with the prior measurement
  before trusting it to score the new behavior.

  **Criterion 4 — the stopping rule.** Dangerous divergences fall from 746 to
  184 (580 fixed) and the newly-dangerous set — documents that were
  agreeing or safely divergent before this change and became dangerous after
  it — is **18** documents, all of length 4. Every one of the 18 contains at
  least one token from the tilde or 4-backtick alphabet (checked
  programmatically: a document not containing one of those eight token
  variants never appears in the newly-dangerous set), which is exactly the
  tilde and fence-length blindness `#0455`'s criterion 3 protects and this
  issue does not touch. 18 is under the 30-document ceiling this issue's
  criterion 4 sets, so the stopping rule is **met**, not hit — the change
  reduces the dangerous count and its own newly-dangerous set stays inside
  the named budget. No fourth level of fixing is added.

  **Criterion 3 — every earlier probe re-run against the real compiled
  function.** `#0454`'s fenced-example-row case
  (`TestIssueStatusIgnoresFencedExampleRow`), `#0455`'s three opener cases
  (`TestIssueStatusDoesNotOpenFenceOnNonOpenerLine`), and `#0460`'s
  three-line sequence
  (`TestIssueStatusClosingBranchDoesNotHideRowBehindReopenedFence`) all pass
  unmodified. Of the four safe-direction controls in
  `TestIssueStatusSafeDirectionDivergencesUnchanged`, three (tilde fence,
  3-backtick fence nested in a 4-backtick fence, inline triple backtick
  mid-prose) are confirmed unmoved and stay in that test function; the
  fourth — a closer carrying trailing text — is exactly what this issue
  changes, so it now moves from `"open"` to `""` and is pinned separately in
  the new `TestIssueStatusClosingBranchRequiresNoTrailingText`, which also
  keeps a control proving an ordinary trailing-text-free closer still closes.
  Confirmed the "moves" direction against the real, compiled pre-fix function
  (not a re-implementation, per `#0441`'s review): in a throwaway `git
  worktree` pinned to this issue's own pre-fix commit, the real
  `issueStatus` returned `"open"` for the trailing-text-closer document; the
  post-fix working tree returns `""`. Worktree removed afterward;
  `git worktree list` no longer shows it, and another agent's own worktree
  was left untouched throughout.

  **Criteria 5 and 6 — corpus counts and zero classification changes,
  against the real corpus.** Used a temporary, uncommitted Go test (never
  part of the diff) to walk every `issues/*.md` file and record
  `issueStatus`'s real output per file, run once in the current working tree
  (post-fix) and once more in a second throwaway `git worktree` pinned to
  this issue's pre-fix commit. Both runs report **464** files, **463** with a
  non-empty status, and **29** reading `open` or `in-progress` — up from
  `#0460`'s review's 462/461/30 by two files filed since minus one resolved
  in between, consistent with ordinary corpus drift rather than a behavior
  change. The two per-file dumps are **byte-identical** across all 464
  files: zero classification changes. No new mechanism, no new helper, no
  new file was added to the shipped diff — the probe files used for this
  measurement were deleted before finishing, and `docTableRowLines` is
  untouched (`#0455`'s criterion 2 and `#0176`'s `wontfix` both still stand).

  **Verification.** `git status --porcelain` immediately before the scoped
  run showed only this issue's two files modified
  (`internal/db/issue_citation_guard_test.go`, this file); no other agent's
  files were dirty. `uptime` recorded, not reasoned from (§5): 1.26 / 1.71 /
  2.08 before the first run, 3.59 / 2.20 / 2.23 before the final one.
  `gofmt -l` reported clean before any edit and `gofmt -w` on the edited file
  left its SHA-256 digest unchanged. `ISSUE=0463 scripts/check.sh go
  ./internal/db/... ./internal/handlers/...` reported `go build`, `go vet`
  and `gofmt -l` clean; `internal/db` `ok` in 0.418s–0.612s and
  `internal/handlers` `ok` in 5.446s–5.892s across two runs (real elapsed
  time, never `0.00s`); the skip audit reported no skips; no leftover
  processes; `VERIFICATION PASSED` both times. Re-ran the following real
  declarations individually with `-count=1 -v`, all `PASS`:
  `TestIssueStatusIgnoresFencedExampleRow`,
  `TestIssueStatusDoesNotOpenFenceOnNonOpenerLine`,
  `TestIssueStatusSafeDirectionDivergencesUnchanged`,
  `TestIssueStatusClosingBranchRequiresNoTrailingText`, and
  `TestIssueStatusClosingBranchDoesNotHideRowBehindReopenedFence`. Both
  throwaway worktrees were removed with `git worktree remove --force` (never
  `git stash`, `checkout --`, `restore`, `reset --hard`, or `clean`, per
  §8a), and `git worktree list` afterward shows only the main checkout and
  another agent's own pre-existing worktree, untouched throughout. Scratch
  database dropped by `scripts/check.sh` on exit.

## Review notes

- 2026-09-05 — **Reviewed (phase 3, Opus). Pass.** All six criteria hold. The
  measurement was re-derived from scratch rather than checked, and it produces
  a stronger result than the issue asked for: **this chain is complete**, and
  the evidence for that is recorded below so nobody reopens it.

  **Method.** A **third** independently-written CommonMark fenced-code-block
  reference (`#0460`'s review wrote the first, the implementer the second),
  built from the spec's rules rather than from either predecessor: opener and
  closer capped at 3 columns, matching fence character, closing run at least
  the opening run, only whitespace after a closer's run, and no backtick in a
  backtick opener's info string. Alongside it, reimplementations of the walk
  in both its pre- and post-`#0463` forms. Separately, the **real compiled
  `issueStatus`** was driven from two throwaway detached `git worktree`s — one
  at `948324e` (the tree immediately before the fix), one at `275a6bf` — each
  carrying an identical uncommitted probe, per `#0441`'s rule that a harness
  reimplementing its subject is a copy agreeing with itself. No `git stash`,
  `checkout --`, `restore`, `reset --hard` or `clean` at any point (§8a);
  another agent held uncommitted work in `internal/config/` throughout and it
  was left alone. Both worktrees were removed afterwards and `git worktree
  list` shows only the main checkout and a third worktree belonging to another
  agent, untouched.

  **Criterion 2 — three independent references now agree to the document.**
  The corpus arithmetic is confirmed independently: 12 + 144 + 1,728 + 20,736
  = **22,620**. Scoring it:

  | | agrees | safe divergence | dangerous divergence |
  |---|---|---|---|
  | pre-`#0463` | 15,727 | 6,147 | 746 |
  | post-`#0463` | 19,331 | 3,105 | **184** |

  The pre-`#0463` row reproduces `#0460`'s review's reported post-`#0460`
  figures **exactly**, and the post-`#0463` row reproduces the implementer's
  **exactly**. Three references written by three different passes, from prose
  descriptions rather than from each other's code, landing on the same six
  numbers is strong evidence all three are right — and it is what lets the
  rest of this analysis lean on the reference.

  **Criterion 4 — the 18 attributions checked programmatically, not
  accepted.** The newly-dangerous set is **18** documents, every one of length
  4. Partitioning them by whether they contain any token from the tilde or
  4-backtick sub-alphabet: **18 of 18 do, and the count reachable without one
  is zero.** So the stopping rule is **met, not hit** — 18 is inside the
  30-document ceiling, and the entire newly-dangerous set is attributable to
  the tilde and fence-length blindness `#0455`'s criterion 3 protects and this
  issue does not touch. The implementer's characterisation is confirmed.

  **The chain is complete — this is the finding worth recording.** The same
  partition was run over the **whole** residual, not just the newly-dangerous
  slice: of the 184 dangerous documents remaining, **184 contain a tilde or
  4-backtick token, and none is reachable without one.** The same holds for
  all 3,105 remaining safe divergences. Composition of the 184: **78** need a
  tilde fence only, **52** a 4-backtick fence only, **54** both.

  Two direct measurements make the point rather than inferring it. Restricting
  the alphabet to the **8 tokens this chain is actually permitted to touch**
  (every backtick-only, single-length shape), post-`#0463` `issueStatus`
  agrees with the reference on **4,680 of 4,680** documents at depth ≤ 4 —
  zero safe divergences, zero dangerous — and on **37,448 of 37,448** at depth
  ≤ 5. Extending to a wider, differently-chosen 18-token backtick-only
  alphabet (1-, 2-, 3-, 4-, 5- and 8-space indents, tab and tab-plus-space
  indents, a closer followed only by spaces, three info-string variants, a
  whitespace-only line, a pipe-leading prose line), agreement is **111,150 of
  111,150** at depth ≤ 4. The perfect score is therefore not an artifact of
  the token choices the corpus happens to use.

  So `issueStatus` is now **exactly CommonMark-correct on the entire
  sub-language it is designed to parse.** Every divergence that remains,
  in either direction, requires tracking the fence *character* or the fence
  *length* — which is precisely `docTableRowLines`'s state machine, and
  extracting or duplicating that is declined by three standing decisions:
  `#0455`'s criterion 2, `#0454`'s review, and `#0176`'s `wontfix`. A fifth
  level is not merely unwarranted by this issue's stopping rule; it is out of
  scope by prior decision, it would be the `#0258` spiral §8 warns about, and
  corpus exposure has been a structural zero at every level of this chain.
  **Nothing further here is worth filing.**

  **Criterion 3 — four controls, exactly one moved, against the real compiled
  function.** Driving `issueStatus` itself from both worktrees: the
  closer-carrying-trailing-text control moves `"open"` → `""`, which is this
  issue's whole point; the tilde fence, the 3-backtick fence nested in a
  4-backtick one, and the inline triple-backtick mid-prose all return `"open"`
  both before and after. `#0455`'s three opener cases (4-space indent, tab
  indent, backtick in the info string) and `#0460`'s three-line sequence are
  likewise unmoved, as is a clean trailing-text-free closer. `#0454`'s
  fenced-example-row case passes unmodified. All five named declarations pass:
  `TestIssueStatusIgnoresFencedExampleRow`,
  `TestIssueStatusDoesNotOpenFenceOnNonOpenerLine`,
  `TestIssueStatusSafeDirectionDivergencesUnchanged` (now three subtests, the
  fourth having correctly moved out),
  `TestIssueStatusClosingBranchRequiresNoTrailingText`, and
  `TestIssueStatusClosingBranchDoesNotHideRowBehindReopenedFence`.

  **Criterion 5 — counts re-derived, zero classification changes.** Driving
  the real compiled function over one fixed corpus directory from both
  worktrees: **464** files, **463** carrying a status, **29** reading `open`
  or `in-progress` (424 `resolved`, 21 `open`, 8 `in-progress`, 7 `closed`, 3
  `wontfix`). The tree agrees: 464 `.md` files in `issues/`, of which 463 are
  `NNNN.md` and the one without a status is `issues/Issues.md` — `#0454`'s
  whole point, and the reason a harness globbing `*.md` reads one higher than
  the issue-file count. The two per-file dumps are **byte-identical** across
  all 464 files, SHA-256 `ebf308d7…daace0b`: **zero classification changes.**

  **One correction to the Work log, recorded rather than bounced.** Its drift
  sentence attributes 462/461/30 → 464/463/29 to "two files filed since minus
  one resolved in between". The two filed are right (`issues/0462.md` and
  `issues/0463.md`, confirmed with `git diff --diff-filter=A` against
  `#0460`'s measurement commit), but **three** issues resolved in that window,
  not one — `#0458`, `#0459` and `#0460` — so the arithmetic is 30 − 3 + 2 =
  29. As written it yields 31 and contradicts the 29 the same paragraph
  correctly reports. The measured figures are all correct and criterion 5's
  substantive requirement is proven byte-identically; only the prose
  attribution was off, so this is corrected here rather than round-tripped,
  on `#0455`'s review's precedent.

  **Criterion 6 — nothing was added.** The commit's only new declaration is
  `TestIssueStatusClosingBranchRequiresNoTrailingText`; no new helper, no new
  state variable inside `issueStatus` (still `inFence`, `trimmed`, `run`), no
  file added or deleted, two files touched. Criterion 1's change reuses
  `leadingRunLength` and `leadingIndentWidth` unmodified.
  `internal/db/docs_parity_test.go` is untouched — its last change remains
  `#0412`/`#0413` — so `docTableRowLines`, `#0455`'s criterion 2 and `#0176`'s
  `wontfix` all still stand.

  **Verification.** `uptime` recorded, not reasoned from (§5): load averages
  3.01 / 2.68 / 2.43 at the start, 1.83 / 2.03 / 2.17 before the scoped run.
  `git status --porcelain` immediately before showed only
  `internal/config/env_example_guard_test.go`, dirty from another agent and
  untouched here; no untracked file anywhere in the repository.
  `ISSUE=0463r scripts/check.sh go ./internal/db/... ./internal/handlers/...`
  reported `go build`, `go vet` and `gofmt -l` clean, `internal/db` `ok` in
  0.498s and `internal/handlers` `ok` in 5.656s (real elapsed time, never
  `0.00s`), the skip audit reporting no skips, no leftover processes, and
  `VERIFICATION PASSED`; the scratch database was dropped by the script on
  exit, and the databases were left at migration 27 (`scripts/db-reset.sh`
  and `scripts/testdb.sh template` were not run). `gofmt -l` over the tree is
  clean and piping the edited file through `gofmt` reproduces its SHA-256
  byte-for-byte, so the new doc comment is format-stable. Bytes were scanned
  with `python3`, never BSD `grep -P`: the `#0463` doc comment is pure ASCII
  and contains no doubled-quote or doubled-backtick sequence gofmt would
  rewrite (§8).
