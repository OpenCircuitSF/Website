# 0468 — The backup's interim failure signal is pull-based, and nothing pulls it

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | deploy / backups |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-08 |

## Description

`#0435` recommends leaving `BACKUP_ALERT_WEBHOOK_URL` unconfigured for now,
because no notification channel exists anywhere in this project, and offers
`journalctl -p err` and `systemctl --failed` as the interim signal.

`#0435`'s reviewer agreed with the recommendation and rejected the
justification:

> Both are pull-based and **nothing is scheduled to pull them**, so a silent
> nightly failure stays silent in exactly the way this issue exists to fix.

That is right. `opencircuit-backup.service` carries `OnFailure=`, so a failed
run does mark the unit failed — but a mark nobody reads is not a signal.

## Why it matters more than it looks

The failure mode is **silent and slow**. A backup that stops working produces no
output; it produces the *absence* of output, which is the hardest thing to
notice. And the window in which nobody notices is exactly the window in which
the backup is needed.

`#0434`'s review found a version of this already: a media backup that would have
deleted its own last copy while **exiting 0**, so `OnFailure=` would never have
fired at all. The interim answer to that class cannot itself be "someone will
check the journal".

## Acceptance criteria

1. Decide what the interim signal actually is. **A pull-based command counts
   only if something is scheduled to pull it** — and if that is the answer, the
   schedule is part of the deliverable, not a note.
2. If the honest answer is that there is no signal until a channel exists, say
   so plainly in `deploy/systemd/README.md` rather than implying `journalctl`
   fills the gap. An accurate statement of a gap is worth more than a
   reassurance that does not hold.
3. Consider the cheap options before the expensive one: a `systemd` timer that
   reports non-empty `systemctl --failed`, mail to a local mailbox, or a
   heartbeat the user checks on their own cadence. **`#0271` settled that
   `contact@opencircuitsf.com` is a real Google Workspace mailbox** — check
   whether that is a usable destination before proposing a new channel.
4. **Do not build a notification channel** without the user's decision. §9's
   restricted areas cover outward-facing sends, and this project's mail path is
   SES, which is still sandboxed pending `#0415`.
5. **Nothing runs on the box** without approval (§5b, §9).

## Also worth fixing — same file, one line

`deploy/systemd/README.md` carries **two near-identical "Create the system user"
sections**, pre-existing and not introduced by `#0435`. Fold or delete one, and
say which you kept and why.

## Relation

- `#0435` prepared the backup install and made the recommendation.
- `#0434` is the media leg whose defect would have exited 0.
- `#0415` blocks SES, so mail is not currently a channel.
- `#0271` established the Workspace mailbox.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/db/... ./internal/handlers/...`. No test reads `docs/deployment.md` or `deploy/systemd/README.md` — say so rather than implying coverage.

## Work log

- 2026-09-08 — Implementation (Sonnet). Decided criterion 1 the honest way:
  the interim signal is **not** a pull-based command, because nothing on the
  box is scheduled to pull one, and this pass builds no schedule and no
  channel (criteria 4 and 5). Confirmed read-only against the box
  (`ssh ec2`, load idle): `systemctl list-timers --all` lists 8 timers, all
  OS-owned (`certbot-renew`, `logrotate`, `sysstat-collect`/`-summary`,
  `systemd-tmpfiles-clean`, `fstrim`, `update-motd`,
  `refresh-policy-routes@ens5`) — none reads the journal or checks
  `systemctl --failed`; there is no `root`/`postgres`/`ec2-user` crontab
  (only the stock `/etc/cron.d/0hourly`); and no mail transfer agent is
  installed (`postfix`, `sendmail`, `mailx`, `msmtp`, `ssmtp` all absent —
  `command -v` on each returned nothing). So `#0271`'s
  `contact@opencircuitsf.com` Workspace mailbox is a real destination but not
  currently reachable *from this box*: nothing here can hand off SMTP to it
  without installing new software, and this project's own mail path (SES) is
  separately blocked by `#0415` and sandboxed regardless. Rewrote
  `deploy/systemd/README.md`'s alerting section (replacing the "the journal
  log is the alert" / "that is a real, if manual, signal" framing) to state
  plainly that there is currently no interim failure signal beyond a human
  choosing to run `journalctl -p err` or `systemctl --failed` on their own
  initiative, and to record why a scheduled pull was not built here even
  though it was considered: automating the check with a timer does not by
  itself put the result in front of anyone — it needs pairing with a
  destination a person already looks at (mailbox, chat webhook, or an
  existing on-box surface like `update-motd`), and choosing that destination
  is exactly the decision criterion 4 reserves for the user. Made the matching
  correction in `docs/deployment.md`'s Backups section, which repeated the
  same "journal-only alert path... as the interim signal" claim. Folded the
  two near-identical "Create the system user" sections in
  `deploy/systemd/README.md` (previously at lines 12 and 21) into one, keeping
  the first (the "main service runs as..." wording, which matches the phrasing
  the unit-purpose table above it already uses for `opencircuit.service`) and
  deleting the second as a pure duplicate — no content in the second added
  anything the first lacked. Built no notification channel, scheduled no
  timer, and changed nothing on the box; the three backup unit files are
  byte-identical to what `#0435`'s review already hashed
  (`opencircuit-backup.service` `edf5aa0b…`, `.timer`, `-alert.service`
  untouched — not opened by this pass). Worth filing once the user has a
  preference (not filed here, per `CLAUDE.md` §9): a scheduled
  `systemctl --failed` check paired with a real destination, sized to
  whatever cadence the user wants to be interrupted at. Scope held to
  `deploy/systemd/README.md`, `docs/deployment.md`, and this issue file; did
  not touch `scripts/db/`, any unit file, `internal/`, `web/`, `#0433`, or
  `#0467`. Left `Status: in-progress` for review (reviewer's call to set
  `resolved`, `CLAUDE.md` §9).
