# 0169 — `Optional[T].UnmarshalJSON` does not inherit `DisallowUnknownFields`, a latent hole in the strict-decode contract

| | |
|---|---|
| **Status** | open |
| **Platform** | All |
| **Module** | handlers |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |

## Description

`internal/handlers` has exactly **one** JSON-decoding contract: `decodeJSON` (`auth.go:392`) calls `DisallowUnknownFields()`, and all 20 request-body decodes route through it. `#0137`'s review established this by enumeration — an unknown key is *rejected*, never silently dropped.

Two decodes bypass it. One is fine: `ses_notifications.go:162` parses a third-party SNS envelope, where strictness would be wrong. The other is a latent hole:

**`Optional[T].UnmarshalJSON` (`optional.go:101`) calls `json.Unmarshal` on the inner value, which does not inherit the outer decoder's strictness.** So any struct decoded *through* an `Optional[T]` would accept unknown fields, silently, in a codebase whose stated contract is that it does not.

**Harmless today**: `Optional` is instantiated only as `Optional[int]`, and an int has no fields to be unknown. It becomes a real defect the moment someone writes `Optional[SomeStruct]` — which is exactly what a generic invites.

## Acceptance criteria

- [ ] `Optional[T].UnmarshalJSON` either enforces the same strictness as `decodeJSON`, or documents in one line that it does not and why that is acceptable
- [ ] If enforcing: a test with a struct type proves an unknown inner field is rejected. If documenting: a test pins the current permissive behavior so a future reader knows it is deliberate
- [ ] Check whether `Optional[T]`'s existing "request bodies only" doc line (added by `#0155`) should also say "scalar types only", if that is the chosen containment
- [ ] `ISSUE=0169 scripts/check.sh go ./internal/handlers/...` passes

## Notes

Reported by `#0137`'s phase-3 review under `CLAUDE.md` §9, found while verifying that the codebase has a single decode contract rather than two.

Worth noting the shape, which recurs in this tracker: a generic written for one concrete instantiation carries an assumption that holds only for that instantiation. `#0146` introduced `Optional[T]` deliberately as a *general* solution to the three-state problem, on the reasoning that the next nullable non-string column would need it — so the second instantiation is expected, not hypothetical.

## Relation

- Found by: [#0137](0137.md) — Slugs stay server-generated and immutable
- Relates to: [#0146](0146.md) — where `Optional[T]` was introduced
- Relates to: [#0155](0155.md) — which added its "request bodies only" doc line
