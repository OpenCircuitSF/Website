# 0082 — docs/database.md has gone stale on three separate migrations

| | |
|---|---|
| **Status** | open |
| **Module** | docs / db |
| **Platform** | All |
| **Phase** | 3 — Mailing list capture |
| **First seen** | 2026-08-19 |

## Description

`docs/database.md` carries a table of every migration and a sentence naming the range. It has now drifted **three times**:

- `#0009` added `000008` without updating it — caught in review.
- `#0023`/`#0025` added `000009`/`000010` — caught, updated.
- `#0026` added `000011` — **currently stale**: no row, and the prose still says "numbered `000001`–`000010`".

Each instance was caught by a human-equivalent reading rather than by anything mechanical, and one was missed until a later review.

## Why a test rather than another correction

Three occurrences is a pattern, not an accident. The table is genuinely useful — it explains *why* each migration exists, which filenames cannot — so the answer is not to delete it. But a doc that must be manually synchronised with a directory listing will drift again, and the next reviewer may not check.

## Acceptance criteria

- [ ] `docs/database.md` gains its `000011` row and the range prose is corrected
- [ ] A test asserts every `migrations/*.up.sql` has a corresponding row in `docs/database.md`, and that the stated range matches the actual highest migration
- [ ] **Mutation-proven**: add a throwaway migration file without a doc row, watch the test fail, remove it, report the output
- [ ] The test's failure message names the missing migration and the file to edit
- [ ] Consider whether the same drift risk applies to `CLAUDE.md`'s migration range statement — it has been corrected once already — and either cover it or record why not
- [ ] `go build ./...`, `go test ./... -count=1` green with `TEST_DATABASE_URL` exported and zero skips

## Notes

This is the same shape as `#0071`'s route-table parity guard: two things that must agree, with nothing enforcing it. That one found a real divergence the moment it was built.

Keep the test cheap and filesystem-only — it should not need a database.

## Relation

- Related: [#0071](0071.md) — Enforce the Go/TypeScript route-table parity with a test
- Related: [#0026](0026.md) — Build the subscribe endpoint with anti-abuse controls
