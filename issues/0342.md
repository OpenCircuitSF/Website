# 0342 — Two comments still assert what `#0297` removed

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | outbox / handlers |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | ffef8cf (partial — criteria 1, 2, 4, 5, 6 only; left in place) |

## Description

Two stale comments survived `#0297`'s two implementation passes and both of its
reviews. Neither affects behaviour, a bound, or a test.

**1. `internal/mailing/outbox_worker.go:572-576`** — the inline comment at
`pass`'s `trackClaimed` call site still reads *"so a `Stop` landing anywhere in
`sendOne` still sees it as outstanding — see `trackClaimed`'s doc comment"*.

That is the **identical falsehood** `#0297`'s bounce required be removed from
`trackClaimed`'s own doc comment, 150 lines away in the same file — and it now
cites the very doc comment that refutes it. `Stop` blocks on `doneCh`, which
cannot close mid-send, so it never observes a row there. The first review
enumerated four such sites; the implementer found and fixed a fifth on its own
initiative and missed this sixth.

**2. `internal/handlers/subscribe_intake_test.go:184-186`** — still says
"`#0294` has since raised `intakeOrphanStaleAfter` … 210s and `#0284` raised
`outboxOrphanStaleAfter` to 800s". Both are superseded by `#0297`: the windows
are now 30s and 70s. It labels its numbers historical, so it misleads less, but
the figures are wrong as stated.

Filed rather than bounced because a second bounce would have escalated
`#0297`'s next pass to Opus (§3) for a two-sentence comment edit — the wrong
trade under that section's own cost rule.

## Acceptance criteria

1. The `outbox_worker.go` inline comment states what is true: `Stop` blocks on
   `doneCh`, which cannot close mid-send, so `trackClaimed` never observes a row
   during `sendOne`. It must agree with `trackClaimed`'s doc comment rather than
   contradict it.
2. The `subscribe_intake_test.go` comment states the current windows (30s
   intake, 70s outbox) and attributes them to `#0297`, keeping the historical
   note accurate if it is retained.
3. **Sweep for further instances rather than fixing only these two.** Six sites
   have now been found across two passes and two reviews by enumeration, and
   each pass found one the last missed. Grep for every comment referencing
   `Stop`, `releaseAll`, `trackClaimed`, `ClaimDue`, `orphanStaleAfter`,
   `intakeOrphanStaleAfter`, or `outboxOrphanStaleAfter` and check each against
   the current code. Report the full list.
4. Comment-only. Prove it structurally — parse both revisions without comments
   and compare — not with `grep -v '^[+-]#'`, which a review this session found
   weak.
5. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal is
   introduced into any Go comment; a shell harness greps for those and comment
   text containing them inflates the population it measures. The tree is
   currently clean (verified zero matches) — keep it so and re-measure.
6. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout (§8).

## Relation

- Found by: [#0297](0297.md)'s second review, which declined to bounce for it
- Depends on: [#0297](0297.md) — whose change made both comments false
- Relates to: [#0284](0284.md), [#0294](0294.md), [#0295](0295.md) — the
  superseded window figures
- Relates to: [#0267](0267.md) — the misattached-doc-comment guard family, which
  did not catch these because they are inline rather than declaration comments

## Sweep results

Ran `grep -rn --include="*.go" -E '//.*(Stop\b|releaseAll|trackClaimed|
ClaimDue|orphanStaleAfter|intakeOrphanStaleAfter|outboxOrphanStaleAfter)'`
against `internal/` and `cmd/` (the only trees that mention any of these
identifiers at all — confirmed with a separate `grep -rl` pass over `cmd/`
and `docs/`, which found nothing). ~230 raw hits; narrowed by reading each
paragraph against the current code (current `intakeOrphanStaleAfter` = 30s,
`outboxOrphanStaleAfter` = 70s, both per §6.10/#0297's collapse to a
single-row bound). Every hit fell into one of four buckets:

1. **The two sites named in the Description** — fixed (criteria 1–2).
2. **Two further stale sites, found by this sweep, both asserting the
   superseded 20s intake window as a current fact rather than a historical
   one — fixed alongside the two named sites:**
   - `internal/handlers/subscribe_intake.go:236` (inside `intakePass`'s own
     doc comment) — present tense: "this sweep — which runs every 5s with a
     20s staleness window sized for THIS poller's own fast path — can never
     release a live claim…". `intakeOrphanStaleAfter` is 30s, not 20s.
     Corrected to 30s and attributed to `#0297`.
   - `internal/outbox/store.go:733` (`OrphanSweep`'s doc comment, explaining
     why the `kinds` parameter exists) — narrated the #0254 incident without
     an explicit "at the time" marker, so its "20s staleness window sized
     for ITS OWN fast path (`intakeOrphanStaleAfter`)" read as a current
     fact. Reworded to mark it explicitly as the value "at the time" and
     added "`#0297` has since raised it to 30s".
3. **Correctly historically-framed sites — left alone**, e.g.
   `internal/outbox/claim_kinds_call_site_guard_test.go:7` ("its 20s sweep
   released live mail claims…", clearly past tense narrating the #0254
   incident), and every figure in
   `internal/handlers/subscribe_intake_orphan_stale_after_test.go` and
   `internal/mailing/outbox_orphan_stale_after_test.go`, which explicitly
   label their old-formula figures (210s, 800s) as "pre-#0297" / "the OLD
   … test" and use them only as reversion-proof oracles, not as claims
   about current behavior.
4. **Unrelated matches on the word "Stop" or the substring "20s"** in other
   subsystems (`internal/middleware`, `internal/workshops`, `internal/audit`,
   `internal/testdb`, `cmd/opencircuit/ratelimit_wiring_test.go`,
   `internal/handlers/credentials_test.go`) — about `#0084`'s DB-op timeout
   constant or unrelated `Stop`/`http.Server` usages, not this issue's
   subject matter. Left alone.

`internal/mailing/worker.go` (the campaign-send worker, distinct from
`outbox_worker.go`) has no `trackClaimed`/`releaseAll` machinery at all —
confirmed by grep — so it carries none of this family's falsehood, only its
own (already-current) `orphanStaleAfter` = 70s references.

No file owned by another in-flight issue (`#0331`–`#0333`, `#0335`,
`#0336`, `#0337`, `#0338`) appeared among the sites needing a fix.

## Verification

Comment-only, proved structurally (criterion 4): parsed `HEAD` and the
working-tree revision of each touched file with `go/parser`
(`parser.ParseComments`), stripped every `ast.File.Comments` entry plus
every declaration's `.Doc`/`.Comment` field, and printed both through
`go/format`. The stripped output is byte-identical for all four touched
files (`internal/mailing/outbox_worker.go`,
`internal/handlers/subscribe_intake.go`,
`internal/handlers/subscribe_intake_test.go`,
`internal/outbox/store.go`) — confirmed with `diff`, not
`grep -v '^[+-]#'`.

`gofmt` stability (criterion 6): `sha256(file) == sha256(gofmt stdout)` for
all four files — confirmed equal.

No `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call-syntax literal introduced
(criterion 5): `scripts/go_file_visit_floor_guard_test.sh` re-run standalone
and via `scripts/check.sh guards` — `claimKindsGuardMinPlausibleCallSiteCount`
still measures a population of 42 (unchanged), all guards pass.

```
uptime            # 15:14, load averages 3.31 2.21 1.95 — not saturated
ISSUE=0342 scripts/check.sh go ./internal/mailing/... ./internal/handlers/... ./internal/outbox/...
scripts/check.sh guards
```

`go build`/`go vet`/`gofmt -l` clean; `internal/mailing` ok (3.432s),
`internal/handlers` ok (8.924s), `internal/outbox` ok (6.105s) — zero
skips, no package reported `0.00s`. `VERIFICATION PASSED`.
`scripts/check.sh guards` passed on a clean second run (`testdb_gc_guard_test.sh`,
`dev_guard_test.sh`, `db_reset_guard_test.sh`, `go_file_visit_floor_guard_test.sh`
all reported "all guards hold (0 failures)"; a first run's self-check
transiently failed, consistent with concurrent-agent interference on this
shared checkout per CLAUDE.md §5a/§8 rather than anything in this issue's
diff — none of this issue's files touch `scripts/check.sh`, and a rerun with
no changes passed cleanly). Scratch database `opencircuit_test_0342` was
already gone by the time this session tried to drop it (dropped automatically
by `check.sh`'s own exit trap). `pgrep -f 'go test|\.test '` clean at the end
of this session.

## Review notes

> 🔴 Bounced 2026-08-31 (Opus reviewer, phase 3). Criteria 1, 2, 4, 5
> and 6 all pass, independently re-verified. **Criterion 3 does not**: the
> sweep missed a site that is unambiguously false and that its own grep
> pattern matches twice over, and `## Sweep results` asserts a four-bucket
> partition that misclassifies it. The code commit `ffef8cf` is correct as far
> as it goes and stays in place — this is an additive follow-up pass, not a
> revert.

### What the reviewer independently confirmed as correct

- **Criterion 1.** `intakeOrphanStaleAfter` is `3 * intakeRowTimeout` where
  `intakeRowTimeout = mutateJobTimeout = 10s`, so **30s**;
  `outboxOrphanStaleAfter` is `2 * (sendMessageTimeout + writeStatusTimeout)`
  `= 2 * (30s + 5s)`, so **70s**. Read from the tree, not from any comment.
  `Stop` (`outbox_worker.go:380`) does block on `<-w.doneCh`, `doneCh` closes
  only in `Run`'s `defer` (line 329), and `w.claimed` is read only via
  `releaseAll`, only on the `doneCh` branch — so the rewritten inline comment
  at `outbox_worker.go:572-578` is true and now agrees with `trackClaimed`'s
  doc comment instead of contradicting it.
- **Criterion 2.** The `subscribe_intake_test.go:181-190` comment now states
  30s/70s and attributes the collapse to `#0297`, keeping the 210s/800s
  figures as explicitly historical.
- **Criteria 4 and 7-equivalent.** Re-derived by a *different* method than the
  implementer's: parsed each revision with `parser.ParseFile(..., 0)` so
  comments never enter the AST at all, printed through `go/format`, and hashed.
  The tool fails closed on empty input and was proved falsifiable first (a
  `2 *` → `3 *` mutation in `outboxOrphanStaleAfter` produced a diff).
  `ffef8cf^` vs `ffef8cf`: **byte-identical for all five files**
  (3732B / 9539B / 14283B / 11503B / 10959B).
- **Criterion 5.** No added line in `ffef8cf` contains a
  `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` literal, and a tree-wide grep for
  that call syntax inside a Go comment returns zero.
  `scripts/go_file_visit_floor_guard_test.sh` measures the population at 42,
  unchanged, with both floors plausible.
- **Criterion 6.** `sha256(file) == sha256(gofmt stdout)` for all five files,
  and `check.sh`'s `gofmt -l` step is clean.
- **The transient `guards` failure is credibly ambient, not a defect.** The
  reviewer's `scripts/check.sh guards` passed on its **first** run
  (`VERIFICATION PASSED`, exit 0) at load average 2.82. Nothing in `ffef8cf`
  touches `scripts/`, and the failing step was `check.sh`'s own
  `run`/`runpipe` self-check — the mechanism CLAUDE.md §8 already records as
  measuring the running process rather than the file. Attribution accepted; no
  action needed.

### Why criterion 3 fails

Criterion 3 exists because "each pass found one the last missed". This pass
found two genuine extras (`subscribe_intake.go:236`, `store.go:733` — both
verified correct) and then missed a third that is *more* clearly false than
either, in `internal/mailing/outbox_worker_test.go`, a file the sweep's own
keyword list reaches twice (it contains both `Stop` and `ClaimDue`). It is not
covered by `## Sweep results` bucket 3 (not listed) and not by bucket 4 (which
names only `internal/middleware`, `internal/workshops`, `internal/audit`,
`internal/testdb`, `cmd/opencircuit/ratelimit_wiring_test.go` and
`internal/handlers/credentials_test.go`). So the reported partition is not
merely incomplete, it is stated as exhaustive and is not.

### Remedy — exactly what the next pass must do

This is transcription, not design (CLAUDE.md §3). Three edits, all
comment-only, none in a file owned by another in-flight agent
(`#0331`–`#0333`, `#0335`–`#0338`).

**1. `internal/mailing/outbox_worker_test.go`, the `t.Cleanup` preamble inside
`TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued` (currently lines 743-749).**
It carries two false claims. First, *"id2 is deliberately left `'queued'` by
Stop's release"* — `id2` is never claimed at all: `SelectDue` claims nothing
and `pass`'s `stopCh` check runs before each `ClaimRow`. The same file
contradicts this at lines 705-710 ("a row not yet reached is simply still
'queued'") and 840-843 ("never claimed at all, so there was nothing for Stop to
release"), and the test's own `t.Fatalf` message says "never claimed". Second,
*"picked up by ANY later `OutboxWorker.ClaimDue` call"* — `OutboxWorker` has
had no `ClaimDue` call site since `#0297`; an AST census of the tree shows
`outbox_worker.go` holds exactly one `OrphanSweep` and one `SelectDue` call and
zero `ClaimDue`. Replace the comment body with (do **not** reintroduce
call-syntax literals, criterion 5):

    // The whole point of this test is that id2 is deliberately left
    // 'queued': pass's stopCh check runs before its ClaimRow, so id2 is
    // never claimed at all (#0297 — see this test's own doc comment
    // above, and the post-Stop assertions below). A 'queued', due-now
    // confirmation row left behind after this test returns is picked up
    // by ANY later OutboxWorker pass sharing this database — including
    // other tests in this same package/run — so it must be removed
    // explicitly rather than relying on ending in a terminal status the
    // way every other test in this file does.

**2. `internal/mailing/outbox_worker.go:452-454`, `untrackClaimed`'s doc
comment.** *"so a concurrent Stop no longer considers it outstanding"* asserts
exactly the interleaving `#0297`'s review had removed from `trackClaimed`'s doc
comment 26 lines above: a concurrent `Stop` is parked on `<-w.doneCh` and
cannot read `w.claimed` at any point during `sendOne`, so it never "considers
it outstanding" in the first place. Mirror `trackClaimed`'s corrected framing:

    // untrackClaimed removes id from the claimed set — called the instant
    // sendOne finishes with a row (sent, retried, or abandoned). A
    // concurrent Stop cannot observe the set before this runs — it blocks
    // on <-w.doneCh — so this is what keeps releaseAll's defensive net
    // accurate, not what prevents a live race; see trackClaimed's and
    // Stop's own doc comments.

**3. `internal/outbox/store.go:702-705`, `Release`'s doc comment.** *"used by
`OutboxWorker.Stop` at shutdown so a row that was claimed but not yet sent is
retried on restart rather than waiting out `orphanStaleAfter`"* states as the
live purpose what `Stop`'s own doc comment (`outbox_worker.go:362-373`) says is
now provably empty on every path that reaches it. Weaker than sites 1 and 2 but
the same family, and this file was already touched by `ffef8cf`. Append a
clause such as: `— since #0297 that is a defensive net rather than the ordinary
case; see OutboxWorker.Stop's own doc comment for why releaseAll normally finds
nothing to release.`

**4. Update `## Sweep results`.** Add these three to the enumeration and either
correct bucket 4's membership or drop its claim to be exhaustive. Per
`issues/Issues.md`, do not silently rewrite the existing bucket prose — strike
the superseded sentence and date the correction.

**5. Re-run the same proofs** and record them: the structural comment-only
diff (`ffef8cf` → new HEAD, not `HEAD^`), `gofmt` stability on the newly
touched files, `ISSUE=NNNN scripts/check.sh go ./internal/mailing/...
./internal/handlers/... ./internal/outbox/...`, and
`scripts/check.sh guards`.

**Reported to the orchestrator for filing** (§9 — subagents report, the
orchestrator allocates numbers): a separate, pre-existing defect in
`internal/outbox/claim_kinds_call_site_guard_test.go`'s
`claimKindsGuardMinPlausibleCallSiteCount` doc comment — "well above what a
narrowing to any single **file** could produce (at most 3 …)" omits the
`non-exempt` qualifier, and that constant counts *all* sites, of which
`internal/outbox/store_test.go` alone holds 20. Out of scope for both `#0334`
and this issue.


## Work log

- 2026-08-31 — Filed by the orchestrator from `#0297`'s second review (§9).
- 2026-08-31 — implement (`claude-sonnet-5`): fixed the two named sites, then
  swept `internal/`, `cmd/` for every comment mentioning `Stop`, `releaseAll`,
  `trackClaimed`, `ClaimDue`, `orphanStaleAfter`, `intakeOrphanStaleAfter`, or
  `outboxOrphanStaleAfter` and checked each against the current code. Found
  two further stale sites beyond the two named in the Description (see
  `## Sweep results` below). Token usage not captured by this subagent; left
  for the orchestrator's usual per-transcript accounting. Code commit
  `ffef8cf`. Left `in-progress` for review (§9 — the implementer never sets
  `resolved`).
- 2026-08-31 — review (`claude-opus-5`): **bounced**, status back to `open`.
  Criteria 1, 2, 4, 5, 6 independently re-verified and correct (live constant
  values read from the tree, `Stop`/`doneCh` ordering read from the source, a
  from-scratch comment-stripping proof by a different method than the
  implementer's, `gofmt` stability, and a clean
  `ISSUE=0342r scripts/check.sh go ./internal/mailing/... ./internal/handlers/...
  ./internal/outbox/...` plus `scripts/check.sh guards` at load average 2.82).
  Criterion 3's sweep missed `internal/mailing/outbox_worker_test.go:744` — two
  false claims that the sweep's own keyword list matches — and `## Sweep
  results` states an exhaustive partition that does not cover it. Remedy fully
  specified in `## Review notes` so the next pass stays on Sonnet (§3). Code
  commit `ffef8cf` left in place.
