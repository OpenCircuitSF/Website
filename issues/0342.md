# 0342 — Two comments still assert what `#0297` removed

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | outbox / handlers |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Commit** | ffef8cf (partial — criteria 1, 2, 4, 5, 6 only; left in place), db9bff7 (second pass — the three sites named in the bounce's remedy) |

## Description

Two stale comments survived `#0297`'s two implementation passes and both of its
reviews. Neither affects behaviour, a bound, or a test.

**1. `internal/mailing/outbox_worker.go:572-576`** — the inline comment at
`pass`'s `trackClaimed` call site still reads *"so a `Stop` landing anywhere in
`sendOne` still sees it as outstanding — see `trackClaimed`'s doc comment"*.

That is the **identical falsehood** `#0297`'s bounce required be removed from
`trackClaimed`'s own doc comment, 150 lines away in the same file — and it now
cites the very doc comment that refutes it. `Stop` blocks on `doneCh`, which
cannot close mid-send, so it never observes a row there. The first review
enumerated four such sites; the implementer found and fixed a fifth on its own
initiative and missed this sixth.

**2. `internal/handlers/subscribe_intake_test.go:184-186`** — still says
"`#0294` has since raised `intakeOrphanStaleAfter` … 210s and `#0284` raised
`outboxOrphanStaleAfter` to 800s". Both are superseded by `#0297`: the windows
are now 30s and 70s. It labels its numbers historical, so it misleads less, but
the figures are wrong as stated.

Filed rather than bounced because a second bounce would have escalated
`#0297`'s next pass to Opus (§3) for a two-sentence comment edit — the wrong
trade under that section's own cost rule.

## Acceptance criteria

1. The `outbox_worker.go` inline comment states what is true: `Stop` blocks on
   `doneCh`, which cannot close mid-send, so `trackClaimed` never observes a row
   during `sendOne`. It must agree with `trackClaimed`'s doc comment rather than
   contradict it.
2. The `subscribe_intake_test.go` comment states the current windows (30s
   intake, 70s outbox) and attributes them to `#0297`, keeping the historical
   note accurate if it is retained.
3. **Sweep for further instances rather than fixing only these two.** Six sites
   have now been found across two passes and two reviews by enumeration, and
   each pass found one the last missed. Grep for every comment referencing
   `Stop`, `releaseAll`, `trackClaimed`, `ClaimDue`, `orphanStaleAfter`,
   `intakeOrphanStaleAfter`, or `outboxOrphanStaleAfter` and check each against
   the current code. Report the full list.
4. Comment-only. Prove it structurally — parse both revisions without comments
   and compare — not with `grep -v '^[+-]#'`, which a review this session found
   weak.
5. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal is
   introduced into any Go comment; a shell harness greps for those and comment
   text containing them inflates the population it measures. The tree is
   currently clean (verified zero matches) — keep it so and re-measure.
6. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout (§8).

## Relation

- Found by: [#0297](0297.md)'s second review, which declined to bounce for it
- Depends on: [#0297](0297.md) — whose change made both comments false
- Relates to: [#0284](0284.md), [#0294](0294.md), [#0295](0295.md) — the
  superseded window figures
- Relates to: [#0267](0267.md) — the misattached-doc-comment guard family, which
  did not catch these because they are inline rather than declaration comments

## Sweep results

Ran `grep -rn --include="*.go" -E '//.*(Stop\b|releaseAll|trackClaimed|
ClaimDue|orphanStaleAfter|intakeOrphanStaleAfter|outboxOrphanStaleAfter)'`
against `internal/` and `cmd/` (the only trees that mention any of these
identifiers at all — confirmed with a separate `grep -rl` pass over `cmd/`
and `docs/`, which found nothing). ~230 raw hits; narrowed by reading each
paragraph against the current code (current `intakeOrphanStaleAfter` = 30s,
`outboxOrphanStaleAfter` = 70s, both per §6.10/#0297's collapse to a
single-row bound). ~~Every hit fell into one of four buckets:~~

**Correction (#0342, 2026-08-31).** That sentence overstated the sweep: the
same `~230`-hit grep matched three further sites that were overlooked during
triage rather than sorted into any of the four buckets below — one found by
this issue's own review (`internal/mailing/outbox_worker_test.go:743-749`,
matched twice over by the pattern), two more found by the second
implementation pass while fixing it
(`internal/mailing/outbox_worker.go:452-454`,
`internal/outbox/store.go:702-705`). See bucket 5. Buckets 1–4 remain
accurate for the hits they do cover; the partition just was not exhaustive
as stated.

Every hit fell into one of the buckets below:

1. **The two sites named in the Description** — fixed (criteria 1–2).
2. **Two further stale sites, found by this sweep, both asserting the
   superseded 20s intake window as a current fact rather than a historical
   one — fixed alongside the two named sites:**
   - `internal/handlers/subscribe_intake.go:236` (inside `intakePass`'s own
     doc comment) — present tense: "this sweep — which runs every 5s with a
     20s staleness window sized for THIS poller's own fast path — can never
     release a live claim…". `intakeOrphanStaleAfter` is 30s, not 20s.
     Corrected to 30s and attributed to `#0297`.
   - `internal/outbox/store.go:733` (`OrphanSweep`'s doc comment, explaining
     why the `kinds` parameter exists) — narrated the #0254 incident without
     an explicit "at the time" marker, so its "20s staleness window sized
     for ITS OWN fast path (`intakeOrphanStaleAfter`)" read as a current
     fact. Reworded to mark it explicitly as the value "at the time" and
     added "`#0297` has since raised it to 30s".
3. **Correctly historically-framed sites — left alone**, e.g.
   `internal/outbox/claim_kinds_call_site_guard_test.go:7` ("its 20s sweep
   released live mail claims…", clearly past tense narrating the #0254
   incident), and every figure in
   `internal/handlers/subscribe_intake_orphan_stale_after_test.go` and
   `internal/mailing/outbox_orphan_stale_after_test.go`, which explicitly
   label their old-formula figures (210s, 800s) as "pre-#0297" / "the OLD
   … test" and use them only as reversion-proof oracles, not as claims
   about current behavior.
4. **Unrelated matches on the word "Stop" or the substring "20s"** in other
   subsystems (`internal/middleware`, `internal/workshops`, `internal/audit`,
   `internal/testdb`, `cmd/opencircuit/ratelimit_wiring_test.go`,
   `internal/handlers/credentials_test.go`) — about `#0084`'s DB-op timeout
   constant or unrelated `Stop`/`http.Server` usages, not this issue's
   subject matter. Left alone.
5. **Three sites the same grep matched but the first triage overlooked —
   found by this issue's own review and by the second implementation pass,
   fixed by that pass:**
   - `internal/mailing/outbox_worker_test.go:743-749` (the `t.Cleanup`
     preamble inside `TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`) —
     carried two false claims: that id2 is "deliberately left 'queued' by
     Stop's release" (id2 is never claimed at all — `SelectDue` claims
     nothing and `pass`'s `stopCh` check runs before each row's `ClaimRow`,
     as the same file's own lines 705-710 and 840-843 and this test's own
     `t.Fatalf` message already say), and that a leftover row is picked up
     by "ANY later `OutboxWorker.ClaimDue` call" (`OutboxWorker` has had no
     such call site since `#0297`). Reworded to state the true mechanism
     and to say "OutboxWorker pass" rather than name a call site that no
     longer exists.
   - `internal/mailing/outbox_worker.go:452-454` (`untrackClaimed`'s doc
     comment) — asserted "a concurrent Stop no longer considers it
     outstanding", the identical interleaving `#0297`'s review had already
     required removed from `trackClaimed`'s doc comment 26 lines above (a
     concurrent Stop blocks on `<-w.doneCh` and cannot read the claimed set
     during `sendOne` at all, so it never "considered it outstanding" in
     the first place). Reworded to mirror `trackClaimed`'s corrected
     framing.
   - `internal/outbox/store.go:702-705` (`Release`'s doc comment) — stated
     as the live purpose of `Release` what `OutboxWorker.Stop`'s own doc
     comment says is now provably empty on every path that reaches it since
     `#0297`. A clause was appended noting it is now a defensive net rather
     than the ordinary case, without deleting the still-true description of
     what `Release` does.

`internal/mailing/worker.go` (the campaign-send worker, distinct from
`outbox_worker.go`) has no `trackClaimed`/`releaseAll` machinery at all —
confirmed by grep — so it carries none of this family's falsehood, only its
own (already-current) `orphanStaleAfter` = 70s references.

No file owned by another in-flight issue (`#0331`–`#0333`, `#0335`,
`#0336`, `#0337`, `#0338`) appeared among the sites needing a fix.

## Verification

Comment-only, proved structurally (criterion 4): parsed `HEAD` and the
working-tree revision of each touched file with `go/parser`
(`parser.ParseComments`), stripped every `ast.File.Comments` entry plus
every declaration's `.Doc`/`.Comment` field, and printed both through
`go/format`. The stripped output is byte-identical for all four touched
files (`internal/mailing/outbox_worker.go`,
`internal/handlers/subscribe_intake.go`,
`internal/handlers/subscribe_intake_test.go`,
`internal/outbox/store.go`) — confirmed with `diff`, not
`grep -v '^[+-]#'`.

`gofmt` stability (criterion 6): `sha256(file) == sha256(gofmt stdout)` for
all four files — confirmed equal.

No `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call-syntax literal introduced
(criterion 5): `scripts/go_file_visit_floor_guard_test.sh` re-run standalone
and via `scripts/check.sh guards` — `claimKindsGuardMinPlausibleCallSiteCount`
still measures a population of 42 (unchanged), all guards pass.

```
uptime            # 15:14, load averages 3.31 2.21 1.95 — not saturated
ISSUE=0342 scripts/check.sh go ./internal/mailing/... ./internal/handlers/... ./internal/outbox/...
scripts/check.sh guards
```

`go build`/`go vet`/`gofmt -l` clean; `internal/mailing` ok (3.432s),
`internal/handlers` ok (8.924s), `internal/outbox` ok (6.105s) — zero
skips, no package reported `0.00s`. `VERIFICATION PASSED`.
`scripts/check.sh guards` passed on a clean second run (`testdb_gc_guard_test.sh`,
`dev_guard_test.sh`, `db_reset_guard_test.sh`, `go_file_visit_floor_guard_test.sh`
all reported "all guards hold (0 failures)"; a first run's self-check
transiently failed, consistent with concurrent-agent interference on this
shared checkout per CLAUDE.md §5a/§8 rather than anything in this issue's
diff — none of this issue's files touch `scripts/check.sh`, and a rerun with
no changes passed cleanly). Scratch database `opencircuit_test_0342` was
already gone by the time this session tried to drop it (dropped automatically
by `check.sh`'s own exit trap). `pgrep -f 'go test|\.test '` clean at the end
of this session.

### Second pass (post-bounce), commit `db9bff7`

Fixed the three sites named in `## Review notes`' remedy
(`internal/mailing/outbox_worker_test.go:743-749`,
`internal/mailing/outbox_worker.go:452-454`,
`internal/outbox/store.go:702-705`) with the replacement text given there
verbatim, and updated `## Sweep results` (this pass's edits, above) to add
bucket 5 and strike-and-correct the "every hit fell into one of four
buckets" exhaustiveness claim.

**Comment-only, proved structurally (criterion 4), re-run by a third
method:** a small standalone `go/parser`/`go/format` tool
(`parser.ParseFile(fset, path, nil, 0)` — mode `0`, so comments never enter
the AST at all, a different mechanism than either the implementer's
`ast.File.Comments`-stripping approach or the reviewer's) compared
`ffef8cf` against this pass's new `HEAD` (`db9bff7`, **not** `ffef8cf`'s own
parent) for exactly the three files this pass touched. Byte-identical for
all three (`outbox_worker.go` 14283B, `outbox_worker_test.go` 28223B,
`store.go` 10959B — the first and third sizes match the reviewer's own
`ffef8cf^`-vs-`ffef8cf` measurement exactly, a useful cross-check even
though the compared range differs). The tool was proved falsifiable first:
it fails closed (non-zero exit, empty stdout) on a bad path, and a
code-level mutation to `store.go`'s `Release` (swapping the `UPDATE`'s
`StatusQueued`/`StatusSending` argument order) produced a real diff in the
stripped output before being discarded.

**`gofmt` stability (criterion 6):** `sha256(file) == sha256(gofmt stdout)`
for all three touched files — confirmed equal.

**No `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` call-syntax literal
introduced (criterion 5):** grepped the three touched files directly — the
three hits that exist are real code call sites
(`w.store.OrphanSweep(...)`, `w.store.SelectDue(...)`,
`store.ClaimDue(...)`), none in a comment. A tree-wide Python scan for the
same three literals restricted to lines starting with `//` returned zero
hits. `scripts/go_file_visit_floor_guard_test.sh`'s
`claimKindsGuardMinPlausibleCallSiteCount` still measures a population of
**42**, unchanged from the first pass.

```
uptime                                                            # 15:44, load averages 1.49 2.05 2.49 — not saturated
git show --stat HEAD                                              # db9bff7, 3 files, 17(+)/9(-), matches the pre-commit diff exactly
ISSUE=0342b scripts/check.sh go ./internal/mailing/... ./internal/handlers/... ./internal/outbox/...
scripts/check.sh guards
```

`go build`/`go vet`/`gofmt -l` clean; `internal/mailing` ok (3.586s),
`internal/handlers` ok (8.688s), `internal/outbox` ok (5.462s) — zero
skips, no package reported `0.00s`. `VERIFICATION PASSED`.
`scripts/check.sh guards` passed cleanly on its **first** run this time
(`db_reset_guard_test.sh`, `go_file_visit_floor_guard_test.sh` and the rest
all reported "all guards hold (0 failures)"; the outbox claim-kinds floor
external oracle independently re-measured the population at 42). Scratch
database `opencircuit_test_0342b` was already gone by the time this session
tried to drop it (dropped automatically by `check.sh`'s own exit trap).
`pgrep -f 'go test|\.test '` clean at the end of this session.

Confirmed via `git diff --cached --name-only` immediately before
committing that only this pass's three files were staged (the concurrently
running `#0335`/`#0337`/`#0338` session's dirty `internal/mailing/worker_test.go`
and `issues/0335.md`/`0337.md`/`0338.md` were left untouched throughout),
and via `git show --stat HEAD` afterward that the committed line counts
(17 insertions, 9 deletions, 3 files) match the diff reviewed before
staging exactly.

## Review notes

> 🔴 Bounced 2026-08-31 (Opus reviewer, phase 3). Criteria 1, 2, 4, 5
> and 6 all pass, independently re-verified. **Criterion 3 does not**: the
> sweep missed a site that is unambiguously false and that its own grep
> pattern matches twice over, and `## Sweep results` asserts a four-bucket
> partition that misclassifies it. The code commit `ffef8cf` is correct as far
> as it goes and stays in place — this is an additive follow-up pass, not a
> revert.

### What the reviewer independently confirmed as correct

- **Criterion 1.** `intakeOrphanStaleAfter` is `3 * intakeRowTimeout` where
  `intakeRowTimeout = mutateJobTimeout = 10s`, so **30s**;
  `outboxOrphanStaleAfter` is `2 * (sendMessageTimeout + writeStatusTimeout)`
  `= 2 * (30s + 5s)`, so **70s**. Read from the tree, not from any comment.
  `Stop` (`outbox_worker.go:380`) does block on `<-w.doneCh`, `doneCh` closes
  only in `Run`'s `defer` (line 329), and `w.claimed` is read only via
  `releaseAll`, only on the `doneCh` branch — so the rewritten inline comment
  at `outbox_worker.go:572-578` is true and now agrees with `trackClaimed`'s
  doc comment instead of contradicting it.
- **Criterion 2.** The `subscribe_intake_test.go:181-190` comment now states
  30s/70s and attributes the collapse to `#0297`, keeping the 210s/800s
  figures as explicitly historical.
- **Criteria 4 and 7-equivalent.** Re-derived by a *different* method than the
  implementer's: parsed each revision with `parser.ParseFile(..., 0)` so
  comments never enter the AST at all, printed through `go/format`, and hashed.
  The tool fails closed on empty input and was proved falsifiable first (a
  `2 *` → `3 *` mutation in `outboxOrphanStaleAfter` produced a diff).
  `ffef8cf^` vs `ffef8cf`: **byte-identical for all five files**
  (3732B / 9539B / 14283B / 11503B / 10959B).
- **Criterion 5.** No added line in `ffef8cf` contains a
  `.ClaimDue(`/`.OrphanSweep(`/`.SelectDue(` literal, and a tree-wide grep for
  that call syntax inside a Go comment returns zero.
  `scripts/go_file_visit_floor_guard_test.sh` measures the population at 42,
  unchanged, with both floors plausible.
- **Criterion 6.** `sha256(file) == sha256(gofmt stdout)` for all five files,
  and `check.sh`'s `gofmt -l` step is clean.
- **The transient `guards` failure is credibly ambient, not a defect.** The
  reviewer's `scripts/check.sh guards` passed on its **first** run
  (`VERIFICATION PASSED`, exit 0) at load average 2.82. Nothing in `ffef8cf`
  touches `scripts/`, and the failing step was `check.sh`'s own
  `run`/`runpipe` self-check — the mechanism CLAUDE.md §8 already records as
  measuring the running process rather than the file. Attribution accepted; no
  action needed.

### Why criterion 3 fails

Criterion 3 exists because "each pass found one the last missed". This pass
found two genuine extras (`subscribe_intake.go:236`, `store.go:733` — both
verified correct) and then missed a third that is *more* clearly false than
either, in `internal/mailing/outbox_worker_test.go`, a file the sweep's own
keyword list reaches twice (it contains both `Stop` and `ClaimDue`). It is not
covered by `## Sweep results` bucket 3 (not listed) and not by bucket 4 (which
names only `internal/middleware`, `internal/workshops`, `internal/audit`,
`internal/testdb`, `cmd/opencircuit/ratelimit_wiring_test.go` and
`internal/handlers/credentials_test.go`). So the reported partition is not
merely incomplete, it is stated as exhaustive and is not.

### Remedy — exactly what the next pass must do

This is transcription, not design (CLAUDE.md §3). Three edits, all
comment-only, none in a file owned by another in-flight agent
(`#0331`–`#0333`, `#0335`–`#0338`).

**1. `internal/mailing/outbox_worker_test.go`, the `t.Cleanup` preamble inside
`TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued` (currently lines 743-749).**
It carries two false claims. First, *"id2 is deliberately left `'queued'` by
Stop's release"* — `id2` is never claimed at all: `SelectDue` claims nothing
and `pass`'s `stopCh` check runs before each `ClaimRow`. The same file
contradicts this at lines 705-710 ("a row not yet reached is simply still
'queued'") and 840-843 ("never claimed at all, so there was nothing for Stop to
release"), and the test's own `t.Fatalf` message says "never claimed". Second,
*"picked up by ANY later `OutboxWorker.ClaimDue` call"* — `OutboxWorker` has
had no `ClaimDue` call site since `#0297`; an AST census of the tree shows
`outbox_worker.go` holds exactly one `OrphanSweep` and one `SelectDue` call and
zero `ClaimDue`. Replace the comment body with (do **not** reintroduce
call-syntax literals, criterion 5):

    // The whole point of this test is that id2 is deliberately left
    // 'queued': pass's stopCh check runs before its ClaimRow, so id2 is
    // never claimed at all (#0297 — see this test's own doc comment
    // above, and the post-Stop assertions below). A 'queued', due-now
    // confirmation row left behind after this test returns is picked up
    // by ANY later OutboxWorker pass sharing this database — including
    // other tests in this same package/run — so it must be removed
    // explicitly rather than relying on ending in a terminal status the
    // way every other test in this file does.

**2. `internal/mailing/outbox_worker.go:452-454`, `untrackClaimed`'s doc
comment.** *"so a concurrent Stop no longer considers it outstanding"* asserts
exactly the interleaving `#0297`'s review had removed from `trackClaimed`'s doc
comment 26 lines above: a concurrent `Stop` is parked on `<-w.doneCh` and
cannot read `w.claimed` at any point during `sendOne`, so it never "considers
it outstanding" in the first place. Mirror `trackClaimed`'s corrected framing:

    // untrackClaimed removes id from the claimed set — called the instant
    // sendOne finishes with a row (sent, retried, or abandoned). A
    // concurrent Stop cannot observe the set before this runs — it blocks
    // on <-w.doneCh — so this is what keeps releaseAll's defensive net
    // accurate, not what prevents a live race; see trackClaimed's and
    // Stop's own doc comments.

**3. `internal/outbox/store.go:702-705`, `Release`'s doc comment.** *"used by
`OutboxWorker.Stop` at shutdown so a row that was claimed but not yet sent is
retried on restart rather than waiting out `orphanStaleAfter`"* states as the
live purpose what `Stop`'s own doc comment (`outbox_worker.go:362-373`) says is
now provably empty on every path that reaches it. Weaker than sites 1 and 2 but
the same family, and this file was already touched by `ffef8cf`. Append a
clause such as: `— since #0297 that is a defensive net rather than the ordinary
case; see OutboxWorker.Stop's own doc comment for why releaseAll normally finds
nothing to release.`

**4. Update `## Sweep results`.** Add these three to the enumeration and either
correct bucket 4's membership or drop its claim to be exhaustive. Per
`issues/Issues.md`, do not silently rewrite the existing bucket prose — strike
the superseded sentence and date the correction.

**5. Re-run the same proofs** and record them: the structural comment-only
diff (`ffef8cf` → new HEAD, not `HEAD^`), `gofmt` stability on the newly
touched files, `ISSUE=NNNN scripts/check.sh go ./internal/mailing/...
./internal/handlers/... ./internal/outbox/...`, and
`scripts/check.sh guards`.

**Reported to the orchestrator for filing** (§9 — subagents report, the
orchestrator allocates numbers): a separate, pre-existing defect in
`internal/outbox/claim_kinds_call_site_guard_test.go`'s
`claimKindsGuardMinPlausibleCallSiteCount` doc comment — "well above what a
narrowing to any single **file** could produce (at most 3 …)" omits the
`non-exempt` qualifier, and that constant counts *all* sites, of which
`internal/outbox/store_test.go` alone holds 20. Out of scope for both `#0334`
and this issue.

---

> 🔴 **Bounced again 2026-08-31 (Opus reviewer, phase 3 — SECOND bounce).**
> Criteria 1, 2, 4, 5 and 6 pass, independently re-verified against `db9bff7`,
> and the three replacement comments are *true*, not merely different — each
> was checked against the live code rather than against the remedy text.
> **Criterion 3 fails again.** An independent enumeration run by a different
> method found **three more stale sites, all in `internal/mailing/outbox_worker.go`
> itself** — the file this issue has now edited twice. `## Sweep results`'
> corrected partition still does not cover them. Both code commits (`ffef8cf`,
> `db9bff7`) are correct as far as they go and stay in place; this is a third
> additive pass, not a revert.

### What this review independently confirmed as correct

- **The three replacement comments are true.** Read from the tree, not from the
  remedy:
  - `pass`'s per-id loop checks `<-w.stopCh` (outbox_worker.go:537) **before**
    that id's `ClaimRow` (:559), and `SelectDue` (internal/outbox/store.go) is a
    plain `SELECT … FOR UPDATE SKIP LOCKED` inside a read-only tx — it issues no
    `UPDATE` and claims nothing. So `outbox_worker_test.go:743-750`'s "id2 is
    never claimed at all" holds. It is also *deterministic*, not merely likely:
    `SelectDue` orders `next_attempt_at, id` and `pass` ranges over the returned
    slice in order, so the lower id is always claimed first.
  - `Stop` (:387) blocks on `<-w.doneCh`; `doneCh` closes only in `Run`'s
    `defer` (:329); and an AST/grep census shows `w.claimed` is **read** in
    exactly one place — `releaseAll` (:409-415) — which is called only on
    `Stop`'s `<-w.doneCh` branch. So `untrackClaimed`'s new doc comment
    (:452-457) is true.
  - `outbox.Store.Release`'s only non-test caller in the tree is `releaseAll`,
    and `Stop`'s own doc comment already states releaseAll "provably has nothing
    to release on every path that reaches it". So `store.go:702-708`'s appended
    "defensive net rather than the ordinary case" clause is true and consistent.
- **Criterion 4 — comment-only, re-derived by a third mechanism.** A `go/scanner`
  token-stream comparator (scanner mode `0`, which never emits `COMMENT` tokens —
  no AST, no formatter, a different mechanism from either prior proof). Token
  streams are **identical** for all three files across *both* ranges,
  `ffef8cf → db9bff7` and `db9bff7^ → db9bff7`: `outbox_worker.go` 3164 tokens,
  `outbox_worker_test.go` 6273, `store.go` 2242. Proved fail-closed (empty
  source and zero-token input both exit non-zero — and the guard actually fired
  once during this review, on a zsh word-splitting slip, CLAUDE.md §8) and
  falsifiable (swapping `Release`'s `StatusQueued`/`StatusSending` argument
  order in a scratch copy produced a token diff).
- **Criterion 5.** A Python comment extractor that respects string, rune and raw
  literals scanned 30105 comments across 269 Go files: **zero**
  `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literals in any Go
  comment. Proved falsifiable (a planted `.SelectDue(` inside a comment is
  detected) and free of false positives (a real call is correctly ignored).
  `scripts/check.sh guards` re-measured the external grep-based population at
  **42**, and `claimKindsGuardMinPlausibleNonExemptCallSiteCount`'s at 12 —
  both unchanged; all guards hold, 0 failures, exit 0.
- **Criterion 6.** `sha256(file) == sha256(gofmt stdout)` for all five files
  touched across both passes, and repo-wide `gofmt -l` is clean.
- **The `## Sweep results` correction convention is followed correctly.** The
  struck span, with `~~` removed, is byte-identical to the pre-correction text
  in `511979d^` (checked programmatically, not by eye), it appears nowhere
  unstruck, and it carries a dated `**Correction (#0342, 2026-08-31).**` note.
- **Verification run.** `uptime` 15:48, load average 3.02 — not saturated.
  `ISSUE=0342r2 scripts/check.sh go ./internal/mailing/... ./internal/handlers/...
  ./internal/outbox/...` → `go build`/`go vet`/`gofmt -l` clean; `internal/mailing`
  ok 3.583s, `internal/handlers` ok 9.351s, `internal/outbox` ok 5.779s; **zero
  skips**, no package at 0.00s; `VERIFICATION PASSED`.

### Why criterion 3 fails a second time

Every pass so far has swept with the **same instrument** — the keyword grep from
the criterion, then a human read of each matched paragraph. Three rounds of that
have each missed sites, because the failure is in triage, not in the grep.

This review therefore swept with a different instrument: a **referential-integrity
audit**. Parse every Go file under `internal/` and `cmd/` twice — once with
`parser.ParseComments` to extract comments, once with mode `0` (comments excluded
from the AST *by construction*) to take a census of the identifiers that actually
appear in **code**. Then, for each comment naming a claim-machinery identifier,
ask whether that identifier exists in the code of the declaration the comment is
attached to, and in the file at all. A comment that names a call site in a file
whose code contains no such call is the exact defect class this issue is about.
It scanned 269 files / 4963 comment groups / 456 identifier hits.

**Proved falsifiable before being trusted**: run against the `ffef8cf` snapshot
of `internal/mailing`, it flags `outbox_worker_test.go:746`'s
`OutboxWorker.ClaimDue` — the site the *first* review found by hand — and that
entry is absent from the current tree, confirming `db9bff7` closed it. It exits
non-zero on empty input and on zero identifier hits.

The census is unambiguous: **`internal/mailing/outbox_worker.go` contains zero
`ClaimDue` calls** (137 call expressions total: `SelectDue` 1, `ClaimRow` 1,
`OrphanSweep` 1, `Release` 1, `ClaimDue` **0**). It contains five comment
references to `ClaimDue`. Two are correctly past-tense and were rightly left
alone (`:30` and `:101`, both under explicit `# History` / `#0297 — collapsed`
headings, and `:509`, "the old ClaimDue-based version of this pass"). **Three are
present-tense assertions about a call that does not exist:**

| # | Site | The false claim |
|---|---|---|
| A | `outbox_worker.go:169` — `mailKinds`' **doc comment** | "Passed to `outbox.Store.ClaimDue`'s and `OrphanSweep`'s kinds filters **in pass, below**". In `pass`, `mailKinds` goes to `OrphanSweep` (:477) and `SelectDue` (:523). `ClaimRow` takes no kinds at all. |
| B | `outbox_worker.go:179` — same doc comment | "`ClaimDue(mailKinds...)` never claims such a row at all". No such call; and the variadic `(mailKinds...)` form was itself removed by `#0303`, which made `kinds` a required slice. The *conclusion* (the row stalls in `'queued'` forever) is still true; the named mechanism is not. |
| C | `outbox_worker.go:471` — **inline, inside `pass` itself**, five lines above the code it describes | "scoped to `mailKinds`, the same set `ClaimDue` **below** is scoped to". Nothing named `ClaimDue` is below. `SelectDue` at :523 is the call scoped to `mailKinds`. |

Site C is the same shape as the site the first review bounced on, in the same
file, and it says "below" about a function the second pass read closely enough to
quote in its own commit message. Sites A and B are `mailKinds`' declaration doc
comment, 280 lines from the edit the second pass did make in this file.

A fourth, weaker instance of the same family rides along:
`internal/mailing/outbox_worker_kinds_guard_test.go:35-38` — "After `#0254`
added `ClaimDue`'s kinds filter … not this worker (`ClaimDue` never returns
it)". Its historical anchor is accurate; the present-tense parenthetical is not,
and the paragraph ends by deferring to "`mailKinds`' own doc comment for the
corrected reasoning" — i.e. to site A.

**Where the reported partition still fails.** Sites A–D are not bucket 1, 2 or
5 (not listed), and not bucket 4 (which names only `internal/middleware`,
`internal/workshops`, `internal/audit`, `internal/testdb`,
`cmd/opencircuit/ratelimit_wiring_test.go`, `internal/handlers/credentials_test.go`).
Bucket 3 covers "correctly historically-framed sites — left alone", which A–D
are not. So for the second time the enumeration is stated as covering every hit
and does not.

**What came back clean.** A second, orthogonal axis found nothing: every comment
line in `internal/` and `cmd/` containing a superseded duration figure
(`20s`/`210s`/`800s`) — 33 of them — was checked, and **zero** sit in a
staleness-window context without a historical marker in their paragraph. Buckets
2 and 3 of `## Sweep results` are corroborated. The failure is confined to the
identifier axis.

### Remedy — exactly what the next pass must do

Transcription, not design (CLAUDE.md §3 — see the escalation note in
`## Work log`). Four comment-only edits in two files, neither owned by another
in-flight agent. **Do not introduce `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(`
call syntax** (criterion 5) — the replacement text below deliberately avoids it.

**A. `internal/mailing/outbox_worker.go`, `mailKinds`' doc comment (currently
lines 168-170).** Replace:

    // internal/handlers.SubscribeHandler's own recovery poller. Passed to
    // outbox.Store.ClaimDue's and OrphanSweep's kinds filters in pass, below,
    // so this worker never claims OR sweeps a row it cannot render.

with:

    // internal/handlers.SubscribeHandler's own recovery poller. Passed to
    // outbox.Store.SelectDue's and OrphanSweep's kinds filters in pass, below
    // — #0254 scoped ClaimDue, which this worker has not called since #0297;
    // ClaimRow takes no kinds and only ever claims an id SelectDue already
    // filtered — so this worker never selects OR sweeps a row it cannot
    // render.

**B. Same doc comment (currently line 179).** Replace the single line:

    // loud: ClaimDue(mailKinds...) never claims such a row at all, so render's

with the two lines:

    // loud: SelectDue, scoped to mailKinds, never selects such a row at all
    // (#0297; before that it was ClaimDue's own kinds filter), so render's

**C. `internal/mailing/outbox_worker.go`, inline at the top of `pass` (currently
lines 471-472).** Replace:

    	// #0254's review bounce: scoped to mailKinds, the same set ClaimDue
    	// below is scoped to, so this sweep can never release a live claim

with:

    	// #0254's review bounce: scoped to mailKinds, the same set SelectDue
    	// below is scoped to (#0254 scoped ClaimDue; this pass has not called
    	// it since #0297), so this sweep can never release a live claim

**D. `internal/mailing/outbox_worker_kinds_guard_test.go` (currently lines
35-38).** Replace:

    // switch failed LOUDLY (its default case). After #0254 added ClaimDue's
    // kinds filter, a Kind that exists in internal/outbox but is missing from
    // this list is claimed by NOBODY — not this worker (ClaimDue never returns
    // it), not internal/handlers.SubscribeHandler's recovery poller (which only

with:

    // switch failed LOUDLY (its default case). After #0254 added the kinds
    // filter — to ClaimDue then, to SelectDue since #0297 — a Kind that
    // exists in internal/outbox but is missing from this list is claimed by
    // NOBODY: not this worker (SelectDue never selects it), not
    // internal/handlers.SubscribeHandler's recovery poller (which only

**E. Sweep with the instrument, not the grep.** Do **not** re-run the keyword
grep and re-read paragraphs — that method has now missed sites three times.
Build the referential-integrity check described above (parse with mode `0` for a
code-identifier census, `ParseComments` for the comments, then diff the two per
declaration and per file), prove it falsifiable against the `ffef8cf` snapshot as
this review did, and **paste its post-fix output into `## Sweep results` as
bucket 6**. The bar for criterion 3 is now a mechanical result, not a judgement:
after the fix, no comment in `internal/mailing/outbox_worker.go` may assert
`ClaimDue` in the present tense, and the file's `ClaimDue` code-census must
remain `0`.

**F. Update `## Sweep results`.** Add bucket 6 with sites A–D. The struck
"four buckets" sentence stays exactly as it is — do not re-strike or reword it.
Per `issues/Issues.md`, strike the **original bytes** of any further superseded
claim and date the correction; do not paraphrase.

**G. Re-run the same proofs** and record them: the structural comment-only diff
(`db9bff7` → the new HEAD), `gofmt` stability on every newly touched file
(criterion 6 — A, B and D are declaration doc comments, exactly what gofmt
rewrites, CLAUDE.md §8), the criterion-5 banned-literal scan with the population
re-measured, `ISSUE=NNNN scripts/check.sh go ./internal/mailing/...
./internal/handlers/... ./internal/outbox/...`, and `scripts/check.sh guards`.

### Reported to the orchestrator for filing (§9 — subagents report, the orchestrator allocates numbers)

1. **A guard for stale call-site names in comments.** Three passes and three
   reviews have each found comments naming a call that `#0297` deleted, and the
   only thing that has reliably found them is an AST census. Proposed title:
   *"No Go comment may name a claim-machinery call that its file's code does not
   contain"*. A `internal/outbox` (or `internal/mailing`) guard test that parses
   each file twice — mode `0` for the code-identifier census, `ParseComments` for
   the comments — and fails when a comment names `ClaimDue`, `SelectDue`,
   `OrphanSweep`, `ClaimRow` or `ClaimBatch` in the present tense while the
   file's code contains zero calls to it, with an explicit allowlist for the
   history paragraphs that legitimately narrate a removed call in the past tense.
   This is the class of defect `#0267`'s misattached-doc-comment guard family
   does not reach, and it is the reason this issue has now bounced twice. The
   mutation is observable in Go (mutate the scan roots and the census itself
   changes), so per CLAUDE.md §8 the proof belongs in Go, not an external
   harness — though the floor constant, if it gets one, would want both homes.

2. **A stale determinism caveat in `outbox_worker_test.go:774-784`.** Proposed
   title: *"`TestOutboxWorker_Stop_LeavesUnclaimedRowsQueued`'s checkpoint
   comment still claims a claim order `#0297` made deterministic"*. The comment
   says "which of the two rows is the one blocked in `sendOne` is NOT guaranteed
   to be id1 … `pass`'s own loop order over the returned ids is what determines
   which gets claimed first", citing `#0264`. That was true under `ClaimDue`'s
   `UPDATE … RETURNING`, whose row order `ORDER BY` does not fix. Under `#0297`
   it no longer is: `SelectDue` is a plain `SELECT … ORDER BY next_attempt_at,
   id` and `pass` ranges over the returned slice in order, so the lower id is
   always claimed first — which is exactly why `db9bff7`'s new cleanup comment
   twelve lines above can correctly say "id2 is never claimed at all". Two
   comments in one file now give opposite answers to the same question. The
   test's pair-based assertion is good defensive practice and should stay; only
   the reasoning in the comment is stale. Out of scope here because `ClaimRow`
   is not in criterion 3's keyword list, and because it is a genuine judgement
   call rather than transcription.

3. **(Carried forward, still unfiled as far as this review can tell)** the
   `claimKindsGuardMinPlausibleCallSiteCount` doc-comment defect the first
   review reported — "well above what a narrowing to any single **file** could
   produce (at most 3 …)" omits the `non-exempt` qualifier, while the constant
   counts all sites and `internal/outbox/store_test.go` alone holds 20.

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0297`'s second review (§9).
- 2026-08-31 — implement (`claude-sonnet-5`): fixed the two named sites, then
  swept `internal/`, `cmd/` for every comment mentioning `Stop`, `releaseAll`,
  `trackClaimed`, `ClaimDue`, `orphanStaleAfter`, `intakeOrphanStaleAfter`, or
  `outboxOrphanStaleAfter` and checked each against the current code. Found
  two further stale sites beyond the two named in the Description (see
  `## Sweep results` below). Token usage not captured by this subagent; left
  for the orchestrator's usual per-transcript accounting. Code commit
  `ffef8cf`. Left `in-progress` for review (§9 — the implementer never sets
  `resolved`).
- 2026-08-31 — review (`claude-opus-5`): **bounced**, status back to `open`.
  Criteria 1, 2, 4, 5, 6 independently re-verified and correct (live constant
  values read from the tree, `Stop`/`doneCh` ordering read from the source, a
  from-scratch comment-stripping proof by a different method than the
  implementer's, `gofmt` stability, and a clean
  `ISSUE=0342r scripts/check.sh go ./internal/mailing/... ./internal/handlers/...
  ./internal/outbox/...` plus `scripts/check.sh guards` at load average 2.82).
  Criterion 3's sweep missed `internal/mailing/outbox_worker_test.go:744` — two
  false claims that the sweep's own keyword list matches — and `## Sweep
  results` states an exhaustive partition that does not cover it. Remedy fully
  specified in `## Review notes` so the next pass stays on Sonnet (§3). Code
  commit `ffef8cf` left in place.
- 2026-08-31 — implement, second pass (`claude-sonnet-5`): applied the three
  remedy edits verbatim (`internal/mailing/outbox_worker_test.go:743-749`,
  `internal/mailing/outbox_worker.go:452-454`,
  `internal/outbox/store.go:702-705`), left `ffef8cf` in place, and updated
  `## Sweep results` to add bucket 5 and strike-and-correct the "every hit
  fell into one of four buckets" exhaustiveness claim per the `Issues.md`
  correction convention. Re-ran all proofs by a third, independently-written
  comment-stripping method against `ffef8cf` → this pass's own new `HEAD`
  (not `ffef8cf^`): byte-identical for all three touched files, `gofmt`
  stable, no banned call-syntax literal introduced (outbox claim-kinds
  population still 42), scoped `check.sh` clean with zero skips, and
  `scripts/check.sh guards` clean on its first run. Code commit `db9bff7`.
  Agreed with every part of the remedy as written — verified each of the
  reviewer's factual claims (id2's claim state, the absent `ClaimDue` call
  site, `Stop`'s `doneCh` blocking, `Release`'s doc comment) against the
  tree independently rather than assuming them, and found them all correct.
  Left `in-progress` for review (§9 — the implementer never sets
  `resolved`).
- 2026-08-31 — review, second pass (`claude-opus-5`): **bounced again**, status
  back to `open`. Criteria 1, 2, 4, 5 and 6 independently re-verified against
  `db9bff7` and correct, and all three replacement comments checked against the
  live code and found **true** (stopCh-before-ClaimRow ordering, `SelectDue`'s
  claim-free SQL, `Stop`'s `doneCh` block, `w.claimed`'s single reader, and
  `Release`'s single non-test caller). Criterion 3 fails a second time: an
  independent enumeration by a **different instrument** — a referential-integrity
  audit cross-checking comment-named identifiers against a mode-`0` AST census of
  the code, proved falsifiable against the `ffef8cf` snapshot — found three more
  present-tense `ClaimDue` assertions in `internal/mailing/outbox_worker.go`
  (`:169`, `:179`, `:471`), a file whose code contains **zero** `ClaimDue` calls,
  plus a fourth in `outbox_worker_kinds_guard_test.go:35-38`. An orthogonal
  duration-figure axis came back clean, so buckets 2 and 3 are corroborated and
  the failure is confined to the identifier axis. Both code commits `ffef8cf` and
  `db9bff7` left in place.
- 2026-08-31 — **escalation decision (CLAUDE.md §3): NOT escalated; the next
  implementation pass stays on Sonnet.** This is the second bounce, which raises
  the question, but §3's test is whether the next pass must *decide* something or
  merely *apply* something. It is the latter: the four replacement comment blocks
  are given verbatim in `## Review notes`, the sweep instrument is specified
  concretely enough to build without design judgement, and the pass/fail bar for
  criterion 3 is now a mechanical census result rather than a reading. The two
  bounces are also not on the same design problem — the first was a missed site,
  the second is a missed site found only because the *method* changed, and the
  remedy hands over the method. Recorded here per §3's requirement to log the
  decision either way.
