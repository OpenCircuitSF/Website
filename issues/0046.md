# 0046 — Add campaign preview and test send

| | |
|---|---|
| **Status** | open |
| **Module** | mailing |
| **Platform** | All |
| **Phase** | 5 — Campaigns |
| **First seen** | 2026-08-18 |
| **Commit** | 5ec8774 |

## Description

Render a campaign without sending it, and deliver a real test message to an admin address before the campaign can go out.

## Root cause

Not a bug fix — new feature work. Before this issue, `#0045`'s Preflight
gate already checked `test_sent_at IS NOT NULL` (`no_test_send`), but
nothing in the codebase ever wrote that column, so **every** campaign was
permanently stuck at that one preflight failure — a campaign could never
actually leave `draft`. There was also no way for an operator to see a
campaign's rendered output at all before scheduling it.

## Fix

Two new endpoints in a new file, `internal/handlers/admin_campaign_preview.go`
(`AdminCampaignPreviewHandler`), following `AdminCampaignAudienceHandler`'s
precedent (`#0044`) rather than growing `AdminCampaignsHandler` further:

- **`POST /admin/campaigns/{id}/preview`** — loads the campaign's stored
  row, renders it through the injected `mailing.CampaignRenderer` (the same
  seam `#0045`'s `SendStore`/`Worker` use — in production,
  `mailing.MarkdownCampaignRenderer{}`, i.e. the real goldmark pipeline) with
  a non-functional sentinel manage token (`PREVIEW-DRY-RUN`), and returns
  `{"subject", "html", "text"}`. Never touches `email_sends`,
  `email_campaigns`, or `subscribers`.
- **`POST /admin/campaigns/{id}/test`** — renders the same way, assembles a
  `mailing.Message` the same way `mailing.BuildCampaignMessage` would
  (subject, HTML/text bodies, and the RFC 8058
  `List-Unsubscribe`/`List-Unsubscribe-Post`/`List-Id` headers via the
  already-exported `mailing.CampaignHeaders`) — but goes through the
  handler's own injected `render` seam rather than calling
  `BuildCampaignMessage` directly, so both endpoints share one renderer
  dependency and one is fake-able in tests. Delivers it through the real
  `mailing.Mailer` and, on success, calls the new
  `CampaignStore.MarkTestSent` to stamp `test_sent_at = now()`, then writes
  an `email_campaign.test_sent` audit row.

**Neither route accepts a request body — structurally, not by convention.**
`decodeJSON` is never called by either handler. `#0047`'s carried-in
constraint says preview/test must render the *stored* row, never an
unsaved client override; rather than accept a body and ignore particular
fields (a rule a later edit could forget to maintain), there is simply no
code path that reads one. Proved by mutation (see `## Verification`):
temporarily wiring a `subject`/`body_md` override into `Preview` made
`TestAdminCampaignPreview_Preview_IgnoresRequestBody` fail immediately.

**Test-send recipient: always the requesting admin's own address, never a
client-supplied one — a deliberate narrowing of this issue's own acceptance
criterion.** The criterion says "sends one real message to a specified
admin address"; an admin-suppliable destination would turn this endpoint
into a session-authenticated open relay for attacker-controlled HTML (the
campaign body itself is already attacker/admin-controlled the moment they
can edit it). The recipient is always `middleware.AuthUser.Email` — the
session `RequireAdmin` already authenticated — read fresh on every call,
never a request field. Proved by mutation: hardcoding a different `To`
address broke `TestAdminCampaignPreview_Test_SendsToRequestingAdminOnly`.

**Why the message goes to a dedicated synthetic subscriber row, not the
admin's real one, per this issue's own Notes.** A real, working
`manage_token` is required (so the unsubscribe link is genuinely
clickable), and the only place `FindByManageToken` resolves one is the
`subscribers` table. Reusing (or creating) a row *at* the admin's own email
address is unsafe two ways: (1) if that admin also happens to be a genuine
list member, clicking the test message's unsubscribe link would unsubscribe
their real subscription; (2) a `status='active'` row at that address would
count toward a real "all" campaign's audience. `ensureTestRecipient`
finds-or-creates one synthetic row per admin
(`campaign-test+admin-<id>@internal.opencircuitsf.test` — `.test` is
RFC 2606-reserved and never resolves), left at `status='pending'` for its
entire life so it is excluded from every audience predicate (all of
`internal/mailing`'s eligibility SQL filters `status = 'active'`
unconditionally), and rotates a fresh `manage_token` onto it before every
test send. The `Message.To` is still the admin's real inbox — only the
token is anchored to the disposable row. Proved by
`TestAdminCampaignPreview_Test_UsesDedicatedRecipientRow_NotARealSubscriber`,
which seeds the SAME address as a real, confirmed, active subscriber,
test-sends, confirms the embedded token belongs to a *different* row, and
confirms simulating a click on that token never changes the real
subscriber's status.

**What `Preflight` subset a test send requires: none of the ten codes,
called out explicitly rather than silently skipped.** `Test` does not call
`mailing.Preflight` at all.

- `physical_address_missing` / `reply_to_missing` / `list_domain_unset` are
  deploy-configuration prerequisites for a *real* send to subscribers
  (CAN-SPAM §7704 for the address; PRD §6.6 for reply-to). Requiring them
  here would make it impossible to review a campaign's rendering before
  that configuration lands — backwards, since testing early is exactly how
  an operator notices the address is still blank. `CLAUDE.md` §9's
  `physical_address` restriction is about the send **worker's** gate (the
  one that actually delivers to subscribers) staying unbypassable from the
  UI; a test send to the operator's own inbox is not the "commercial email
  message" CAN-SPAM regulates delivery of, and the worker's own,
  unconditional copy of `Preflight` is completely untouched by this issue —
  a campaign that has been test-sent a hundred times still cannot leave
  `scheduled` without `physical_address` set.
- `no_test_send` is circular here — this *is* the test send.
- `audience_invalid` / `empty_audience` describe the real audience; `Test`
  never touches it.
- `subject_empty` / `body_empty` / `body_render_failed` are not enforced as
  a separate check — letting an operator test-send an empty subject/body is
  the useful feedback loop the feature exists for. A genuine render failure
  still surfaces (422), not because this issue re-implements
  `body_render_failed`, but because rendering itself cannot succeed without
  one.

The one thing `Test` *does* require, unconditionally, is that a mailer is
actually configured: `h.mailer == nil` (`MAILER_NOOP=true` with no real
`SendStore`-backing configuration wired, or a deploy/test that never wired
one in) answers `503` before touching the database at all — not a
`Preflight` code, but the same operational precondition `#0027`'s
`MAILER_NOOP` guard already enforces for every other outbound path. Proved
by mutation: removing the nil check made the handler panic with a nil
pointer dereference on the interface call (see `## Verification`) — the
guard is load-bearing, not decorative.

**Fidelity decision: a test send goes through the identical
render+header-assembly path a real send uses, including the RFC 8058
headers.** The alternative — a simplified test-only rendering path — was
rejected: a test that renders differently from the real thing tells the
operator little. The one deliberate difference from a real send is the
recipient-safety mechanism above (the dedicated synthetic row), which does
not change what is rendered or which headers are attached, only which
`manage_token` anchors the unsubscribe link.

**`Campaign.TestSentAt` / `campaignView.TestSentAt` newly exposed.** The
`test_sent_at` column has existed since `#0045`'s migration `000018`, but
`CampaignStore`'s `campaignColumns`/`scanCampaign`/`Campaign` struct never
read it, and `#0041`'s `campaignView` never surfaced it (both explicitly
flagged as "deferred — column doesn't exist yet" in `#0041`'s own Fix).
Since this issue is the one that starts writing the column, it also starts
reading and surfacing it — `CampaignStore.MarkTestSent` is the sole writer;
`#0045`'s `SendStore.GatherPreflight` continues to read the column with its
own direct query, unaffected.

**`mailing.ResolveFromHeader` extracted from `Worker.resolveFromHeader`.**
Same `default_from_name` → `Message.From` composition (blank stays blank;
refuses anything but printable ASCII with no `"`/`\`), now a standalone
exported function the test-send path calls directly so its `From` header is
byte-identical to what a real send would produce, without duplicating the
safety checks or constructing a full `Worker`. `Worker.resolveFromHeader`
is now a one-line wrapper; every existing `worker_test.go` call site
compiles and passes unchanged.

**Wiring.** `adminRoutes`/`mountAndServe` (`cmd/opencircuit/main.go`) gained
`adminCampaignPreviewH *handlers.AdminCampaignPreviewHandler`, nil-guarded
exactly like `adminCampaignAudienceH` (omitted under `STORAGE=json`). In
production it is constructed with `sesSender` — the exact same
`mailing.Mailer` the send worker uses, including the log-only
`noOpMailingMailer` under `MAILER_NOOP=true` — so a test send behaves like
every other outbound path in this project under that flag, rather than
introducing a second, special "unconfigured" state. The ripple through
`cmd/opencircuit`'s eight other `mountAndServe` call sites (seven wiring
test files) follows `#0041`'s own precedent exactly: each passes `nil` with
a `not exercised by this test` comment, except `admin_wiring_test.go`,
which wires a real handler with a deliberately **nil mailer** — so its
admin-session case for `POST .../test` reaches the real handler and answers
`503` (proof enough that `RequireAdmin` let the request through, the same
standard that file already applies to `POST /admin/subscribers`'s `503`
when `manualAdd` is nil) without attempting a real network call from a
wiring test.

## Verification

`uptime` before starting: `load averages: 2.21 2.13 2.00` (and `1.44 1.61
1.78` immediately before the DB-backed run below) — idle machine, no load
caveat needed.

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
go build ./...
go vet ./...
go test ./internal/mailing/... ./internal/handlers/... ./internal/audit/... ./cmd/opencircuit/... -p 2 -count=1
```

`go build ./...` and `go vet ./...` both clean, no output. The four
packages named above (all DB-backed, `TEST_DATABASE_URL` set):

```
ok  	github.com/brennanMKE/OpenCircuitSF/internal/mailing	0.473s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers	1.855s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/audit	1.320s
ok  	github.com/brennanMKE/OpenCircuitSF/cmd/opencircuit	1.384s
```

Re-ran with `-v` and grepped the transcript: **448 `--- PASS`, 0 `--- FAIL`,
0 `SKIP`** across the four packages — no package reported `ok` in `0.00s`,
each genuinely talked to Postgres.

**New tests, all in `internal/handlers/admin_campaign_preview_test.go`, all
named and observed passing:**

- `TestContainsRemoteAsset_DetectsInjectedImage` — the shared helper the
  no-remote-asset assertions below use actually detects a violation (and
  does not flag an ordinary `<a href>` link).
- `TestAdminCampaignPreview_Preview_RendersStoredRow` — subject/html/text
  all present, sentinel token in the footer links, no remote-asset tag.
- `TestAdminCampaignPreview_Preview_CampaignNotFound` — 404.
- `TestAdminCampaignPreview_Preview_IgnoresRequestBody` — a body claiming a
  different subject/body_md has zero effect (the no-override guard).
- `TestAdminCampaignPreview_Preview_RawHTMLInBodyNeverPassesThrough` — a
  campaign body containing a literal `<img src="https://...">` never
  surfaces as a live `<img>` in the response (goldmark's safe mode, not
  bypassed).
- `TestAdminCampaignPreview_Test_SendsToRequestingAdminOnly` — a body
  naming `attacker@evil.example` has zero effect; `Message.To` is always
  the session's own address (the recipient-constraint guard).
- `TestAdminCampaignPreview_Test_MarksTestSentAndAudits` — `test_sent_at`
  nil before, set after, both in the response and in the stored row; one
  `email_campaign.test_sent` audit row.
- `TestAdminCampaignPreview_Test_CarriesRFC8058Headers` — all three headers
  present.
- `TestAdminCampaignPreview_Test_UsesDedicatedRecipientRow_NotARealSubscriber`
  — the dedicated-row safety proof described in `## Fix` above.
- `TestAdminCampaignPreview_Test_RepeatedSend_RotatesToken` — two test sends
  carry two different tokens.
- `TestAdminCampaignPreview_Test_NilMailer_Returns503` — 503, and
  `test_sent_at` stays nil.
- `TestAdminCampaignPreview_Test_MailerSendFails` — 502 on a mailer error,
  and `test_sent_at` stays nil (never stamped on a failed send).
- `TestAdminCampaignPreview_Test_CampaignNotFound` — 404, mailer never
  invoked.

Also ran `TestMountAndServe_AdminRoutesRequireSessionAndAdmin`
(`cmd/opencircuit`) individually — `PASS`, confirming both new routes are
registered only through the `adminRoutes` table (so
`TestAdminRoutesRegisteredOnlyViaTable`'s AST guard covers them with no
edit) and are genuinely behind `RequireAdmin`.

**Mutation proofs — every guard named above, broken and confirmed caught,
then restored.** For each: copied `internal/handlers/admin_campaign_preview.go`
to a scratch path first (`cp`, never `git checkout`/`restore`), made one
targeted edit, ran only the test naming that property, observed the
specific failure, then restored the file from the scratch copy (`cp` back)
and confirmed `diff` against the scratch copy was empty and
`go build ./... && go vet ./...` were clean again before moving to the
next guard.

1. **Recipient constraint.** Hardcoded `Message.To` to
   `"attacker@evil.example"` instead of `actor.Email`. `go test -run
   TestAdminCampaignPreview_Test_SendsToRequestingAdminOnly` failed on
   exactly that assertion: `Message.To = "attacker@evil.example", want the
   requesting admin's own address ...`.
2. **Mailer-configured precondition.** Removed the `if h.mailer == nil {
   ... }` block. `go test -run TestAdminCampaignPreview_Test_NilMailer_Returns503`
   failed with a real `runtime error: invalid memory address or nil
   pointer dereference` inside `Test` (`net/http: panic serving ...`) — not
   a graceful failure, an actual crash, which is exactly what the guard
   exists to prevent.
3. **No-unsaved-body-override.** Added a `decodeJSON` call to `Preview`
   that let a request-supplied `subject`/`body_md` override the stored
   row. `go test -run TestAdminCampaignPreview_Preview_IgnoresRequestBody`
   failed: `Subject = "ATTACKER SUBJECT", want the STORED subject ...` and
   the HTML body reflected the injected content.
4. **No-remote-asset rule.** Changed `Preview` to return `campaign.BodyMD`
   directly as both `html` and `text`, bypassing the renderer entirely.
   `go test -run TestAdminCampaignPreview_Preview_RawHTMLInBodyNeverPassesThrough`
   failed: the response contained the literal `<img
   src="https://evil.example/track.gif">` from the raw HTML embedded in the
   seeded campaign's `body_md`.

After each restoration: `go build ./...` / `go vet ./...` clean, and the
full `internal/handlers` + `cmd/opencircuit` run above was re-executed
green (448 pass / 0 fail / 0 skip) as the final step, confirming the
restored file is byte-identical to what was committed.

No test processes were left running (`pgrep -f 'go test|\.test '` empty
after every run) and no scratch database was created — all runs used the
shared `opencircuit_test`, and a post-run check confirmed zero leftover
rows matching this issue's seeded prefixes
(`campaign-test+admin-%`/`%admin-test-%`/`%admin-preview-%` in
`subscribers`, `zz-subtest-campaign-%` in `email_campaigns`) — every test's
`t.Cleanup` ran correctly.

## Files changed

- `internal/handlers/admin_campaign_preview.go` (new) —
  `AdminCampaignPreviewHandler`, `Preview`, `Test`, `ensureTestRecipient`,
  `testRecipientEmail`, `containsRemoteAsset`-adjacent doc comments (the
  actual helper lives in the test file).
- `internal/handlers/admin_campaign_preview_test.go` (new) — the 13 tests
  and 2 fakes listed in `## Verification`.
- `internal/handlers/admin_campaigns.go` — `campaignView.TestSentAt` +
  `toCampaignView` wiring.
- `internal/handlers/admin_campaigns_test.go` — `decodedCampaign.TestSentAt`.
- `internal/mailing/campaigns.go` — `Campaign.TestSentAt`, `campaignColumns`/
  `scanCampaign` now read `test_sent_at`, new `CampaignStore.MarkTestSent`.
- `internal/mailing/worker.go` — extracted `mailing.ResolveFromHeader`;
  `Worker.resolveFromHeader` now delegates to it.
- `internal/audit/actions.go` — `ActionEmailCampaignTestSent =
  "email_campaign.test_sent"`.
- `cmd/opencircuit/main.go` — constructs `adminCampaignPreviewH`, threads it
  through `adminRoutes`/`mountAndServe`, registers the two new routes.
- `cmd/opencircuit/admin_wiring_test.go`,
  `cmd/opencircuit/ratelimit_wiring_test.go`,
  `cmd/opencircuit/ses_notifications_wiring_test.go`,
  `cmd/opencircuit/shutdown_budget_wiring_test.go`,
  `cmd/opencircuit/shutdown_wiring_test.go`,
  `cmd/opencircuit/subscribe_wiring_test.go`,
  `cmd/opencircuit/unsubscribe_wiring_test.go` — threaded the new
  `mountAndServe`/`adminRoutes` parameter through every call site
  (`#0041`'s own established ripple pattern).

## Gotchas

- **`decodeJSON` writes its own error response when given a malformed body**
  — irrelevant here in the shipped code (neither handler calls it), but it
  mattered for mutation proof 3 above: the mutation test intentionally sent
  a *valid* JSON body so `decodeJSON`'s own error path never fired and the
  override logic itself was what got exercised.
- **`admin_wiring_test.go`'s admin-session case for `POST .../preview`
  actually renders `targetCampaign`** (seeded fresh per run, name-prefixed
  `zz-wiring-`), unlike most of that test's other routes, which only need
  to prove they were *reached*. This is harmless — Preview is read-only —
  but worth knowing if that test's campaign fixture ever changes shape.



- [x] `POST /admin/campaigns/{id}/preview` returns the rendered HTML and text without sending
- [x] `POST /admin/campaigns/{id}/test` sends one real message to a specified admin address
- [x] The test message uses a real manage token so the unsubscribe links can be clicked and verified
- [x] Test sends are recorded but do not create `email_sends` rows for the campaign
- [x] A campaign cannot leave `draft` until a test send has succeeded
- [x] Handler tests cover preview rendering and the test-send gate

## Carried in from `#0045`'s plan (2026-08-21)

- [x] **This issue owns writing `email_campaigns.test_sent_at`.** `#0045`'s
      migration `000018` adds the column; the send worker only reads it, and
      gates sending on `test_sent_at IS NOT NULL`. Deliberately **not**
      `test_sent_at >= updated_at` — a bare status bump would then deadlock the
      operator into re-testing forever.

      **Amended 2026-08-21 by `#0045`'s review, after reading the shipped
      code:** migration `000018` has landed, so the column exists. Content-edit
      invalidation has **also** already landed — in `#0045`, not `#0041`:
      `CampaignStore.Update` (`internal/mailing/campaigns.go`) clears
      `test_sent_at` to NULL via `CASE WHEN subject IS DISTINCT FROM $3 OR
      body_md IS DISTINCT FROM $5 THEN NULL ELSE test_sent_at END`. Note it is
      `subject`/`body_md` **only** — a `preheader`-only edit deliberately does
      not invalidate, since the preheader never reaches the rendered body a
      test send delivered. So this issue writes `now()` into the column on a
      successful test send and implements **no** invalidation logic of its
      own.

## Carried in from `#0047`'s plan (2026-08-21)

- [x] **Do not add an unsaved-body override to the preview endpoint.** `#0047`'s
      editor autosaves and then previews, because `#0046`'s preview and
      `#0044`'s audience both read the *stored* row. An override that previewed
      unsaved text is exactly what would let an operator preview one thing and
      send another.
- [x] **Return both HTML and plain text.**
- [x] **The preview HTML must be self-contained — no remote assets.** `#0047`
      renders it in a `sandbox=""` iframe rather than `{@html}`, and `CLAUDE.md`
      §9 forbids external CDNs and trackers regardless.

## Notes

PRD §6.6. Using a real, working token in the test send is the point — the operator should be able to click the unsubscribe link in the test message and see it work. A test that exercises everything except the compliance-critical links is not a test.

Whichever admin address receives the test must not end up suppressed by clicking it; use a token tied to a dedicated test subscriber row, not to a real list member.

## Review notes (2026-08-21, phase 3 — bounced)

Independently re-verified `5ec8774`. **`uptime` first: `load averages: 2.00 1.83
1.83`** — idle, no load caveat applies to anything below.

The shared database is at migration `18`, `dirty = f`, and this commit adds no
migration (correct — `test_sent_at` came from `#0045`'s `000018`). No `web/`
file is touched by the commit, so `npm run check` / `npm test` were correctly
**not** required and were not run; saying so explicitly rather than skipping the
frontend suite silently.

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
go build ./...   # clean, no output
go vet ./...     # clean, no output
go test ./internal/mailing/... ./internal/handlers/... ./internal/subscribers/... \
        ./internal/audit/... ./cmd/opencircuit/... -p 2 -count=1 -v
```

All five packages `ok` with non-zero durations (`mailing` 0.545s, `handlers`
2.199s, `subscribers` 1.637s, `audit` 0.379s, `cmd/opencircuit` 1.401s) —
**652 `--- PASS`, 0 `--- FAIL`, 0 `--- SKIP`, zero `SKIP` lines of any kind**,
counting subtests separately. (The drafted `## Verification`'s 448 is the same
run without `internal/subscribers`, which this issue writes to and which the
reviewer's command adds.) All 13 new tests were observed by name in the
transcript: `TestContainsRemoteAsset_DetectsInjectedImage`,
`TestAdminCampaignPreview_Preview_{RendersStoredRow,RawHTMLInBodyNeverPassesThrough,CampaignNotFound,IgnoresRequestBody}`,
and `TestAdminCampaignPreview_Test_{SendsToRequestingAdminOnly,MarksTestSentAndAudits,CarriesRFC8058Headers,UsesDedicatedRecipientRow_NotARealSubscriber,RepeatedSend_RotatesToken,NilMailer_Returns503,MailerSendFails,CampaignNotFound}`.

### What passes — settled, do not re-litigate on the next pass

**1. Requiring none of `Preflight`'s ten codes for a test send is correct.**
Accepted against `CLAUDE.md` §9. That rule is precise — "never make the
`physical_address` check in the send worker (`#0045`) bypassable from the UI" —
and this commit does not touch it: `worker.go` still refuses unconditionally on
a blank `physical_address` (and on a blank `reply_to`) before claiming a batch,
and `TestWorker_RefusesCampaignWithoutTestSend` plus the whole `preflight` suite
still pass. CAN-SPAM §7704 regulates *commercial electronic mail messages*
delivered to recipients; the statutory harm it addresses — a recipient who
cannot identify or reach the sender — does not exist when the recipient **is**
the sender, which the recipient constraint below makes structurally true. The
"operator tests, believes it's ready, discovers the gate at send time" risk is
real but already covered: `#0047` renders `unmet` from
`GET /admin/campaigns/{id}/preflight` continuously and separately, so
`physical_address_missing` is on screen the whole time — the test send was never
what would have told them. Gating the other way would be strictly worse: the PO
box is `CLAUDE.md` §10 open item 3, "not started", and an operator would be
unable to look at their own campaign until it arrived. Preview and Test both
render the address into the footer when set, which makes a test send exactly how
you notice it is blank.

**2. Always sending to the requesting admin's session address is correct and is
enforced server-side with no override.** `To: actor.Email` from
`middleware.UserFromContext`, and `decodeJSON` appears **zero** times in
`admin_campaign_preview.go` — the "no unsaved-body override" constraint is
structural, exactly as claimed. Reproduced both mutations rather than trusting
the draft:

- Hardcoding `To: "attacker@evil.example"` →
  `TestAdminCampaignPreview_Test_SendsToRequestingAdminOnly` fails on exactly
  that assertion (`Message.To = "attacker@evil.example", want the requesting
  admin's own address ...`).
- Adding a `decodeJSON` `subject`/`body_md` override into `Preview` →
  `TestAdminCampaignPreview_Preview_IgnoresRequestBody` fails, with the injected
  subject reaching the rendered body.

Both files restored by `cp` from a copy taken first (never `git checkout` /
`restore` / `stash`), `diff`-confirmed byte-identical afterwards. This genuinely
closes the open-relay risk: no code path lets a caller influence the
destination. It narrows this issue's literal acceptance criterion ("a *specified*
admin address") and the narrowing is right — **amend the criterion, not the
code.**

**3. Fidelity is genuine.** `Test`'s `mailing.Message` literal is field-for-field
identical to `Worker.sendOne`'s — same `To`/`From`/`Subject`/`HTMLBody`/
`TextBody`/`Headers`, same `h.render.Campaign` seam, same
`mailing.CampaignHeaders(baseURL, listDomain, manageToken)`, same
`mailing.ResolveFromHeader`. `Reply-To` is not a `Message` field at all —
`SESMailer.Send` applies it from `cfg.EmailReplyTo` — and the handler is wired
with the *same* `sesSender` instance the worker uses, so it is identical there
too. A preview cannot differ from what ships.

**4. No remote assets**, and the assertion is not tautological —
`TestContainsRemoteAsset_DetectsInjectedImage` proves the helper flags an
injected `<img>` and does *not* flag an ordinary `<a href>`.
`Preview_RawHTMLInBodyNeverPassesThrough` covers the goldmark path. Safe for
`#0047`'s `sandbox=""` iframe.

**5. Wiring, column, and audit are correct.** Both routes are registered only
through `adminRoutes()` and mounted behind `requireAdmin`, so
`TestAdminRoutesRegisteredOnlyViaTable`'s AST guard and
`TestMountAndServe_AdminRoutesRequireSessionAndAdmin` cover them non-vacuously
(the admin case reaches the real handler: 200 for preview, 503 for test on the
deliberately-nil mailer). `MarkTestSent` is the sole writer of `test_sent_at`
and correctly does not bump `updated_at`. Confirmed `#0041`/`#0045`'s
invalidation is still `subject`/`body_md` only, **not `preheader`** — and that
is still right: `CampaignRenderInput.Preheader` *does* reach the rendered
output, but a preheader-only edit changing the hidden inbox-preview line is not
worth deadlocking the operator into a re-test, and `#0047` surfaces
`test_sent_at` so the state is visible either way. Tests seed their own rows and
use `uniqueAdminCampaignName(t)` — no literal or seeded id is targeted (§8b).

### Why this is bounced

**A. The synthetic subscriber row leaks onto the operator-facing admin
subscribers screen.** This is the one thing the design had to get right and it
is the one thing not verified. The `## Fix` and the commit message both justify
the row on the grounds that `status='pending'` keeps it "out of every audience
count" — which is true, and is *not* the same claim as "invisible to the admin
screens", which is false.

Proven empirically, not inferred, on a **private** database
(`opencircuit_rev0046`, created, migrated to 18, and dropped afterwards; the
shared `opencircuit_test` was never written to) with a reviewer-authored test
that runs a real test send through the real mux and then reads the same store
methods `#0032`'s screen reads:

```
StatusCounts pending: before=0 after=1
List total:           before=0 after=1
LEAK: synthetic row "campaign-test+admin-1@internal.opencircuitsf.test"
      (status "pending") is listed on the admin subscribers screen
```

- `subscribers.Store.StatusCounts` is `SELECT status, count(*) FROM subscribers
  GROUP BY status` with no exclusion, and feeds the counts-by-status header
  rendered at `web/src/views/Admin.svelte:1375`. Every admin who has ever run a
  test send permanently inflates the **pending** bucket by one. An operator
  reading "Pending: 12" is being told twelve people are mid-double-opt-in when
  some of them are QA fixtures.
- `subscribers.Store.List` has no exclusion either, so the rows appear in the
  admin subscriber **table** and in its pagination `total`.

What *does* hold, verified independently in the same run and worth keeping: the
row never reaches an audience. `audienceWhere` is `s.status = $1` with
`$1 = 'active'`, structurally shared by `Preview` and `Materialize`, and
`RecheckEligibleTx` re-checks `status != StatusActive` inside the claiming
transaction immediately before each send; a probe of both `all` and
`none_selected` found the synthetic address in neither. It also cannot be walked
to `active` by `#0026`: `Confirm` needs the confirm token, which is random,
never emailed, never returned by any endpoint, and deliberately excluded from
`#0032`'s `subscriberView`. And `#0049` is unaffected — it reads `email_sends`,
and a test send creates no `email_sends` row.

So the *principle* of decision 3 is right and should survive: do not anchor the
test token to a real subscriber row. The *mechanism* — an ordinary `subscribers`
row indistinguishable from a real one — is what leaks.

**B. The same design opens an enumerable hard-bounce vector against SES
reputation.** `testRecipientEmail` is deterministic and its format is now public
in the source: `campaign-test+admin-<id>@internal.opencircuitsf.test`. A public
`POST /api/subscribe` naming that address hits `subscribe.go`'s
`case subscribers.StatusPending` branch, which calls `sendConfirmation` — a real
SES send to an RFC 2606 `.test` domain, i.e. a guaranteed hard bounce.
Rate-limited to once per hour per address, but admin ids are small integers and
trivially enumerable. Nothing catastrophic, but it is gratuitous: these rows are
mailable by an unauthenticated caller purely because they live in the
`subscribers` table at an address that can never resolve.

**C. `gofmt` regression introduced by this commit.** `internal/handlers/admin_campaigns.go`
is now unformatted — `CreatedAt`/`UpdatedAt` kept the old alignment when
`TestSentAt`/`CreatedBy` re-aligned around the new doc comment. `gofmt -l`
reports it; it was clean at `5ec8774^`. (`static_test.go` and
`sesnotify/verify_test.go` are also listed but were already unformatted before
this commit — not this issue's problem.) `go vet` does not catch formatting.

**D. Five doc-comment cross-references name tests that do not exist.**
`admin_campaign_preview.go`'s package comment cites
`TestPreview_IgnoresRequestBody_RendersStoredRowOnly`,
`TestTest_IgnoresRequestBody_SendsStoredRowOnly`,
`TestTest_SendsToRequestingAdminOnly_NeverAnAttackerSuppliedAddress`,
`TestTest_ClickingUnsubscribeInTestMessage_NeverAffectsARealSubscriberRow`, and
`TestTest_NilMailer_Returns503_NeverPanics`. None of those symbols exist
anywhere in the tree — the shipped tests use the
`TestAdminCampaignPreview_*` prefix. A comment that points at a test which is
not there is worse than no comment, because the next reader takes it as evidence
the property is covered.

**E. One genuine coverage gap behind (D).** There is no test that **`Test`**
(as opposed to `Preview`) ignores a request body claiming a different
`subject`/`body_md`. `Test_SendsToRequestingAdminOnly` sends a body, but only
asserts on the recipient. The structural argument covers it today; a test would
keep it covered.

### What the next implementation pass must do

1. **Stop the synthetic rows appearing on the admin subscribers screen.**
   Pick one and say which in `## Fix`:
   - *Preferred* — mark them in the schema (e.g. a `synthetic boolean NOT NULL
     DEFAULT false` column in a **new**, append-only migration `000019`;
     `CLAUDE.md` §1 forbids editing `000018`) and exclude on that flag in
     `StatusCounts`, `List`, and the subscribe flow. Explicit, greppable,
     survives someone changing the address format later.
   - *Acceptable* — exclude by the reserved `internal.opencircuitsf.test`
     domain in the same three places, with the constant as the single source of
     truth.
   - *Also acceptable* — drop the `subscribers` row entirely and give the test
     send a token that resolves through a separate mechanism, if one can be
     built without weakening `FindByManageToken`.
   Whichever is chosen, the exclusion must **not** touch `audienceWhere` — that
   predicate is already correct and must stay `status = 'active'` equality.
2. **Close (B)** as part of the same change: the subscribe flow must never send
   a confirmation to a synthetic/reserved-domain address.
3. **Add a regression test that fails without the exclusion** — the shape used
   above works: snapshot `StatusCounts` and `List` totals, run a real test send
   through the mux, assert both are unchanged and that no row matching the
   synthetic prefix is listed. Keep the existing audience probe alongside it so
   the property that *does* hold stays proven.
4. **Run `gofmt -w internal/handlers/admin_campaigns.go`** and confirm
   `gofmt -l ./cmd ./internal` adds nothing new versus `5ec8774^`.
5. **Fix the five stale test names in the package doc comment** to the names
   that actually shipped, and **add the missing `Test`-endpoint body-override
   test** from (E).
6. **Amend this issue's own acceptance criterion** — "sends one real message to
   a specified admin address" → "to the requesting admin's own session address;
   no address is accepted from the client" — so the narrowing in decision 2 is
   recorded as intended rather than as a deviation.

Everything in "What passes" above is settled. Do not rework the Preflight
decision, the recipient constraint, the render/header fidelity, the wiring, or
`MarkTestSent`.

### Downstream issues

`#0047` needs no change of **method or shape** — its plan already specifies
`POST /admin/campaigns/{id}/preview` and `POST .../test` (§4 and its `#0046`
relation block), and its requirement that campaign responses carry `test_sent_at`
is satisfied by `campaignView.TestSentAt`. Its one inaccurate line — "the
test-send control **defaults** its address to the signed-in admin" — is amended
in `issues/0047.md` by this review, since there is no address field to default.
`#0048` and `#0049` need no amendment: `#0048` is the SSE progress seam and is
untouched, and `#0049` reads `email_sends`, which a test send never writes.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §6.6 — Sending engine · 78 lines
  `sed -n '/^### 6\.6 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0042](0042.md) — Render campaign Markdown to HTML and plain text
- Depends on: [#0043](0043.md) — Build the campaign HTML email template
- Depends on: [#0045](0045.md) — Build the send worker
- Blocks: [#0047](0047.md) — Build the campaign compose UI
