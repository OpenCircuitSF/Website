# 0382 — `claimCommentGuardStoreGoParenSentinelRe`'s prose mis-states which exemption keeps `store.go` clean

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-01 |
| **Commit** | — |

## Description

The prose around `claimCommentGuardStoreGoParenSentinelRe` in
`internal/outbox/claim_comment_referent_guard_test.go` reads as though
**paren scoping** is what zeroes the `SelectDue` finding in `store.go`.

Measured by `#0374`/`#0375`'s reviewer: the paren-scoped window **does** contain
`SelectDue`, and the zero comes from **genuine resolution** — the identifier
really does resolve in the direction the comment claims. Paren scoping's actual
job there is excluding `ClaimRow`, which is a different exemption.

Cosmetic and non-blocking. But it is the same shape this phase keeps hitting: a
record that is itself misleading about the mechanism it describes — `#0358`'s
B2, `#0363`'s crossed labels, `#0366`'s "fixture-pinned" mix-up, and now this.

`#0374`'s `## Verification` carries the same attribution; its reviewer corrected
it there but deliberately did not repeat the fix in the committed comment, since
that was outside the issue's scope.

## Acceptance criteria

1. The prose says which exemption produces which outcome: `SelectDue` resolves
   genuinely within the paren-scoped window; paren scoping excludes `ClaimRow`.
2. **Re-measure before writing** — instrument the axis at that site and print
   the window, the in-window identifier set, the clamp that produced `lo`, and
   the resolution outcome. Do not transcribe this issue's account; **seven
   unsupported details reached issues this phase.**
3. The correction must not overstate in the other direction. Five issues bounced
   this phase for doc comments claiming more than the code delivers, and this
   file has now been corrected for that three times.
4. **The axis stays frozen** (`#0358` criterion 4): no window logic, marker set,
   clamp, or every-identifier change. Comment-only — prove it structurally with
   your own tool, built to a binary, proved falsifiable first and failing closed
   on empty input. **Note this file contains Go source fixtures inside raw
   strings whose `//` lines are not comments** — probe that hazard explicitly;
   two reviewers have.
5. **No `<file>.go:<int>` citation may enter any comment** — `#0356`'s guard is
   repo-wide and lives in `internal/handlers`, so a scoped `internal/outbox` run
   will not catch it. Run `./internal/handlers/...` too (see `#0381`).
6. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal in any
   comment (`#0323`); floor harness **42 total / 12 non-exempt**; `gofmt`
   stability.

## Relation

- Found by: [#0374](0374.md)/[#0375](0375.md)'s review
- Relates to: [#0358](0358.md), [#0363](0363.md), [#0366](0366.md) — the same
  misleading-record shape, three times already in this file
- Relates to: [#0381](0381.md) — the scoped-verification trap criterion 5 names

## Work log

- 2026-09-01 — Filed by the orchestrator from `#0374`/`#0375`'s review (§9).
