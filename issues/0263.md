# 0263 — Three comments that credit mechanisms the code does not use

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | All |
| **Module** | subscribers / web |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-24 |
| **Commit** | `a423a40` (items 1–2); item 3 addressed in `#0264`'s `611915b` — see Implementation notes |

## Description

Three inaccuracies found by `#0127`/`#0128`'s review. None is a behavioural
defect — every property they describe genuinely holds — but each credits the
**wrong mechanism**, which is the kind of comment that gets a correct safeguard
deleted as redundant.

**1. `AdminResendConfirmation`'s cooldown comment names a mechanism it does not
use.** `internal/subscribers/pending.go` claims
`ClaimAndEnqueueConfirmation`'s WHERE-clause claim. The code actually checks in
Go, after `SELECT … FOR UPDATE`. The property holds — the reviewer proved it
with eight concurrent resends (`wins=1 cooldowns=7 queued_delta=1`) — but **a
reader who believes the comment could remove the `FOR UPDATE` as redundant**,
and the guard would silently become a race. `issues/0128.md` repeats the claim.

**2. `formatPendingAge` advertises an output it cannot produce.**
`web/src/lib/pending.ts` documents a `"45 seconds"` result; anything under 60s
returns `'just now'`.

**3. `render()`'s comment is now stale.** It says the worker will not refuse a
missing `physical_address` "for a confirmation" — but `#0127` added the welcome
email, the first arguably-commercial message on that path. See `#0264`, which
covers whether the behaviour should change; this item is only the comment.

## Implementation notes

Phase 2 (Sonnet), 2026-08-24. Ran as one of three concurrent agents
(`CLAUDE.md` §5a), alongside `#0127`'s fix pass and `#0264`; `git status
--porcelain` showed only `scripts/check.sh` dirty (another agent's,
untouched) at the start.

**Item 1 — `AdminResendConfirmation`'s cooldown comment, corrected
(`internal/subscribers/pending.go`, commit `a423a40`).** Rewrote guard 4's
doc-comment bullet to say plainly that the cooldown is checked in Go
against the row already locked `FOR UPDATE`, not via a
claim-in-the-WHERE-clause `UPDATE` the way the public
`ClaimAndEnqueueConfirmation` enforces its own cooldown. Added an explicit
comment directly on the `SELECT ... FOR UPDATE` query naming what breaks
if it is removed (a read-then-write race letting two concurrent admin
clicks both win), per the criterion's own wording.

**`issues/0128.md`'s repetition, corrected in place** (markdown-only
follow-up commit, landing with this issue's own markdown update): the
Implementation notes bullet now states the actual mechanism and carries a
dated correction note rather than silently rewriting history.

**Item 2 — `formatPendingAge`'s doc, corrected (`web/src/lib/pending.ts`,
commit `a423a40`).** The example list no longer includes "45 seconds";
the doc now says plainly that anything under 60 seconds returns "just
now" with no seconds branch.

**Item 3 — `render()`'s stale comment.** Not touched in this issue's own
commit: `#0264` (the behavioural question this comment was stale about)
rewrites the same function's doc comment as part of deciding and
implementing the answer, in commit `611915b`. Doing it here too would have
meant two passes editing the same comment out of order within the same
concurrent-agent window; `#0264`'s version is the one that ships, and it
is accurate about which kinds reach `render()` and why welcome is the one
exception.

## Verification

`ISSUE=0127fix scripts/check.sh go ./internal/mailing/...
./internal/subscribers/... ./internal/outbox/... ./internal/handlers/...
./internal/db/... ./cmd/opencircuit/...` and `ISSUE=0127fix
scripts/check.sh` (full default: Go + web) — both run together with
`#0127`'s and `#0264`'s fix passes, since all three touch overlapping
verification scope. Build/vet/`gofmt -l` clean; all 17 Go packages `ok`,
zero skips; `npm run check` — 278 files / 0 errors / 0 warnings; `npm
test` — 35 test files / **3,852** tests passed (unchanged — this issue's
own changes are doc comments only, no new tests).

## Acceptance criteria

- [x] Each comment describes the mechanism actually used — verified by reading the code path, not the comment it replaces
- [x] `issues/0128.md`'s repetition of the cooldown claim is corrected in place
- [x] The `FOR UPDATE` gains a comment saying it is load-bearing, so the next reader does not remove it
- [x] `formatPendingAge`'s doc matches its real output boundaries
- [x] `render()`'s comment is accurate about which message kinds reach it after `#0127` — landed in `#0264`'s commit `611915b`, not this issue's own; see Implementation notes for why
- [x] `scripts/check.sh` and `scripts/check.sh web` both pass

## Notes

Reported by `#0127`/`#0128`'s phase-3 review under `CLAUDE.md` §9.

**Worth noting what let item 1 through.** `#0196`, `#0199` and `#0220` built
citation guards that check whether a cited *test or path exists*. None of them
can check whether a comment's *description of a mechanism* is true — that
requires understanding the code, not resolving a reference. This class has now
cost this project repeatedly (`#0190`, `#0192`, `#0216`, `#0237`'s seven
rounds), and the honest position is that review is the only instrument for it.

That is an argument for keeping mechanism claims **short and checkable**, or
omitting them: a comment saying "cooldown enforced here" cannot be wrong the way
"enforced by the WHERE clause in X" can.

## Relation

- Found by: [#0127](0127.md), [#0128](0128.md) — the pass that introduced items 1–3
- Relates to: [#0264](0264.md) — the behavioural question behind item 3; its commit `611915b` carries item 3's `render()` fix
- Relates to: [#0220](0220.md) — the citation guard, and why it cannot catch this class

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |

Phase 1 skipped — mechanical, disclosed corrections with no design
latitude (`CLAUDE.md` §3's skip criteria). Status stays `in-progress`:
this is the implementer's own report, not an independently re-verified
phase-3 review. The orchestrator fills in the token counts.
