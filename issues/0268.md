# 0268 — The citation guard does not scan the sections where claims are actually made

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | tooling |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-08-25 |
| **Commit** | `9f43726` (guard widening), `4dfb440` (`issues/0265.md`'s citations, which the widened guard requires) |

## Description

`#0265`'s guard resolves test-name citations in issue files. Its
`extractNamedSections` admits exactly two headings: `## Verification` and
`## Implementation notes`.

**That is not where most claims live.**

- A **fix pass** writes its evidence under `### Verification` *nested inside*
  `## Fix pass` — three of those exist today, none matched.
- A **reviewer** writes under `## Review notes`.

Both defects that bounced `#0265` a second time were in those unscanned
sections, as was the dangling citation one of them concerned.

**Measured by that review: 14 dangling citations in unscanned sections of
`open`/`in-progress` files today, against 0 in the scanned ones.**

So the guard currently enforces a rule over the smallest and cleanest part of
the corpus, while the sections where implementers and reviewers make their
strongest claims — *"I ran this and it printed that"* — go unchecked. That is
close to the opposite of the intent.

**This is not "scan everything".** `## Description` legitimately quotes names
that do not exist — `#0126`'s fabricated citations are quoted verbatim in
several issues as the subject matter. Scanning it would produce exactly the
day-one noise `#0265` scoped its way out of. The fix is to admit the sections
where evidence is *asserted*, not those where it is *discussed*.

## Acceptance criteria

- [x] `## Review notes`, and `### Verification` nested under `## Fix pass`, are scanned — as are any other headings where evidence is asserted; enumerate them from the corpus rather than guessing
- [x] `## Description` and `## Notes` remain unscanned, and the reason is written down so the exclusion is not mistaken for an oversight
- [x] **Measure before choosing.** Report the dangling-citation count per candidate section across `open`/`in-progress` files, and let that drive which are admitted — the way `#0265`'s own scope decision was driven
- [x] The 14 known dangling citations are fixed, or each is shown to be legitimately quoted subject matter
- [x] Mutation-proved: plant a fabricated citation in a `## Review notes` section of an open file, confirm it is named; restore, prove byte-identity with `shasum -a 256`
- [x] `go test ./internal/db/...` passes with zero skips against an isolated database

## Notes

Found by `#0265`'s second phase-3 review under `CLAUDE.md` §9.

**Expect this to fail loudly on first run**, and treat that as the point rather
than a problem: the 14 are real dangling citations in live issue files. Fixing
them is part of the work, not a prerequisite to it.

Note the guard will then scan the very sections this issue's own fix pass and
review will write. That is a feature — `#0265`'s own notes tripped both this
guard and `#0196`'s sibling while being written, and the discipline that
resolved it (do not spell a name you cannot resolve; describe it instead) is the
one everyone should be under.

## Implementation notes

**Where this was run.** A throwaway `git worktree` pinned at `b12fed1` (this
issue's `#0265` half, committed first), because `#0124` and `#0258` were both
mid-change in the shared checkout and `#0124` leaves `internal/handlers`,
`internal/mailing`, `internal/subscribers`, `cmd/opencircuit`, `internal/audit`
and `web/` dirty there. Nothing in the shared tree was read as input to a
measurement. Own database `opencircuit_test_0265esc`, created with
`scripts/testdb.sh create` and dropped at the end; the nine pre-existing
databases, `shortlinks` and `shortlinks_test` among them, were censused before
and after and are untouched. `uptime` before every run: load average 1.47–3.02,
under `CLAUDE.md` §5's ~8 threshold. `scripts/check.sh` was not invoked —
another agent is mid-pass on it (`#0258`) — so its steps were run directly and
are named individually under `## Verification`.

### The measurement that drove the scope (criterion 3)

Counted at `b12fed1` with the shipped `issueCitationExcluded` and `defined`
set, over all 268 issue files, restricted to the 23 that are `open` or
`in-progress`, and rolled up to the enclosing level-2 heading. This is the number the scope
decision was made from, not a section list picked by reading the issue:

| dangling | raw `Test…` tokens | level-2 section |
|---|---|---|
| **5** | 26 | `## Fix pass` |
| **4** | 10 | `## Description` |
| **2** | 23 | `## Review notes` |
| 0 | 11 | `## Verification` *(already scanned)* |
| 0 | 2 | `## Implementation notes` *(already scanned)* |

Every other level-2 heading appearing in an `open`/`in-progress` file —
`## Relation`, `## Notes`, `## Acceptance criteria`, `## Work log`,
`## Open questions`, `## Files changed`, `## Root cause`, `## Fix`, and a
scattering of one-off narrative headings — carries **no `Test…`-shaped token at
all** there, so the measurement supports admitting none of them.

Broken down one level further, before this pass edited anything, the 14 the
issue was filed on were: 6 in a numbered narrative subsection of `## Fix pass`,
4 in `## Description`, 2 in `## Fix pass`'s nested `### Verification`, and 2 in
a `### What must be fixed` subsection of `## Review notes`. **All 14 were in
`issues/0265.md`.** That breakdown is why admission is at the level-2 heading
rather than at a nested `### Verification`: the largest single cluster was not
under a `### Verification` at all, and a rule that admitted only the nesting the
issue named would have caught 2 of the 10 actionable ones. A section already
runs from its heading to the next level-2 heading, so admitting `## Fix pass`
admits everything nested beneath it for free — the same thing that has always
happened under `## Verification`.

### What was admitted, and what was not

Admitted, by normalized title: `verification`, `implementation notes`,
`fix pass`, `review notes`, plus four earlier spellings of the same two roles
that exist in the corpus (`review verification`, `review findings`,
`bounce fix`, `bounce-fix completion`). Those four carry zero dangling
citations anywhere in the corpus, `resolved` files included, so admitting them
costs nothing measured and closes the hole where renaming a heading would
defeat the guard.

Not admitted, each with the reason recorded in the guard's own file-level
comment so the exclusion cannot later be read as an oversight:

- **`## Description` and `## Notes`** — these *quote* fabricated names as
  subject matter. Four of the eleven dangling citations measured above are
  exactly that: `#0126`'s false citations, reproduced verbatim in this guard's
  own motivating issue so the defect can be discussed. Scanning them would fail
  the guard on the text that documents why the guard exists. The line is
  between prose that **asserts** evidence and prose that **discusses** it.
- **`## Plan` and `## Acceptance criteria`** — these propose work, and a
  proposed test may legitimately not exist yet. `#0265`'s original reasoning
  for excluding `## Plan`, unchanged.
- **`## Fix`, `## Root cause`, `## Files changed`, `## Gotchas`** — zero
  dangling *and* zero tokens in the enforced scope, so nothing measured
  supports admitting them; and in `resolved` files each carries legitimate
  quotes of deleted scratch tests and pre-rename names, which is the day-one
  noise `#0265` deliberately scoped away from.
- **`## Manual verification required`** — zero in the enforced scope; its two
  dangling citations in `resolved` files are both a bare `-run` prefix standing
  in for a real family.
- **`## Work log`** — token and cost accounting, not evidence about tests.

### The mechanism

`issueCitationSectionHeaders` changes from an exact-match map on the whole
heading line to a map keyed by *normalized title*, and a new `issueSectionTitle`
does the normalizing: strip `## `, drop any annotation introduced by an em-dash
or an opening parenthesis (whichever comes first), trim, lowercase. That is what
makes one entry match every real spelling in the tracker — a review-notes
heading carrying a phase and a date, a fix-pass heading carrying a date and a
parenthetical. An exact-match matcher would have missed every decorated variant,
which is a second reason a fix pass's evidence went unscanned.

### A third discount rule the widening made necessary: line-wrapped names

Widening the scan to `## Fix pass` and `## Review notes` means reading long-form
wrapped prose, and a test name broken across a line reads to the citation
pattern as a truncated prefix that resolves to nothing. This is not a
hypothetical: while this pass was verifying against the shared checkout,
another agent's `#0124` file briefly carried **six** such names in its
`## Verification` section — a section this guard already scanned before `#0268`
— and every one was reported as dangling although each joined into a real
defined function. That agent unwrapped them a minute later for its own reasons,
so no live instance survives, but the shape will recur every time an evidence
section quotes a long name near the right margin.

`isLineWrappedDefinedTestName` discounts a match followed immediately by a line
break when the match **joined with** the identifier characters continuing on
the next line names a function the tree defines. Indentation and blockquote
markers are skipped, since review notes are frequently quoted. It cannot
produce a false negative: a wrapped name whose halves do not join into
something real is still reported. `TestIssueCitationLineWrappedNameResolvesOnlyWhenJoinedNameIsDefined`
pins all four cases — wrapped and defined, wrapped inside a blockquote and
defined, wrapped and *not* defined, and not wrapped at all.

Measured effect: it discounts 5 citations corpus-wide and **0** in the enforced
scope, so it is not load-bearing today. It is here because the shape was
observed live and because it strictly reduces false positives.

### The 14, and what they turned out to be

Not one of them was a false claim about a test having run. All were **names
spelled in prose that the tree does not define**: `#0126`'s six fabricated
citations quoted as subject matter (4, in `## Description`, left alone); the
deliberately-invented identifier from `#0265`'s own mutation plant, quoted while
describing the plant (2); a placeholder identifier a test fixture declares as a
string, quoted while describing a fixture-ordering bug (2); and `-run` fragments
that are substrings of real functions but not themselves defined, spelled
plainly instead of pipe-adjacent (3). `#0265`'s own escalated pass had already
removed 3 of the 14 while correcting unrelated prose, leaving 11 measured here
and 7 to fix under the widened scope.

Each was fixed the way this issue's `## Notes` prescribes — **describe the
identifier, do not spell it**. That is a real constraint and it bit immediately:
a first attempt wrote a `Test…`-prefixed shape with a U+2026 ellipsis after it,
and the guard reported the prefix, because `#0196`'s wildcard discount needs
three ASCII dots and an ellipsis character is not that. The fix was to stop
naming the shape at all.

## Verification

- `uptime` first and throughout: load average 1.47–3.02.
- `gofmt -l internal/ cmd/` — empty. `go vet ./internal/db/...` — clean.
  `go build ./...` — clean in the pinned worktree, which also confirms it is
  isolated from `#0124`'s in-flight state in the shared checkout.
- `go test ./internal/db/... -p 2 -v` with `TEST_DATABASE_URL` pointed at
  `opencircuit_test_0265esc`: **16 top-level tests pass, zero skips**, up from
  13 before this change. `TestConnectAndPing` is among them and takes real time
  against a real database rather than reporting 0.00s.
- The three new tests were run and watched pass by name:
  `TestIssueSectionTitleNormalizesRealCorpusHeadings`,
  `TestIssueCitationGuardScansEvidenceSectionsNotSubjectMatter` and
  `TestIssueCitationLineWrappedNameResolvesOnlyWhenJoinedNameIsDefined`
  (4 subtests).
- **The guard failed loudly on first run against the widened set**, exactly as
  this issue predicted, naming 7 citations at their correct files and lines
  (all in `issues/0265.md`; 11 were measured across all sections, the other 4
  sitting in the deliberately-unscanned `## Description`). All 7 are fixed and
  `TestNoOpenIssueVerificationCitesUndefinedTestFunction` passes clean.
- **Mutation-proved against a real corpus file, not only a fixture.** Copied
  `issues/0262.md` aside, appended a fabricated citation immediately under its
  `## Review notes (phase-3 review of the fix pass, 2026-08-24)` heading — a
  *decorated* heading, so this also exercises the normalizer — and ran the
  guard: **failed**, naming the invented identifier at `issues/0262.md:171`.
  Restored from the copy; `shasum -a 256` matched
  (`ce8b80740a933166ccd439c28272245d879ba06d8521bbff712a9424fa5bc8d1` before and
  after), `git status --porcelain -- issues/0262.md` empty, guard clean again.
  This was done in the pinned worktree, a separate checkout, so `#0262`'s own
  agent was never exposed to the plant.
- **Mutation-proved in the other direction too.** Copied the guard file aside,
  deleted the six new entries from `issueCitationSectionHeaders`, and watched
  both new tests fail with their intended messages — the normalization test
  naming all seven decorated headings it expects to be scanned, and the scope
  test reporting 0 dangling citations where it expects 2. Restored;
  `shasum -a 256` matched
  (`fa652c6765c0ba2a30d44c615f34cff37feedf17c3bff2aa5ab7f4f1b303f988`).
- `go test ./internal/handlers/ -run TestNoCommentCitesUndefinedTestFunction -v`
  — PASS, so `#0196`'s sibling guard is unaffected by the new prose.
- The SPA suite was not re-run: this change touches one Go test file and two
  markdown files, so `web/` cannot be affected.
- `pgrep -f 'go test|\.test '` empty afterwards; `scripts/testdb.sh drop
  0265esc` run at the end; `pg_database` censused before and after.

## Files changed

- `internal/db/issue_citation_guard_test.go` — `issueCitationSectionHeaders`
  rekeyed by normalized title and widened from 2 entries to 8; new
  `issueSectionTitle`; `extractNamedSections` matches through it; new
  `isLineWrappedDefinedTestName` discount rule; three new tests; file-level
  comment gains the per-section measurement, the admission and exclusion
  rationale, the changed weight of each discount rule under the widened scan,
  and refreshed corpus figures.
- `issues/0265.md` — the 7 dangling citations in its now-scanned `## Fix pass`
  and `## Review notes` sections rewritten to describe identifiers rather than
  spell them.
- `issues/0268.md` — this record.

## Review notes — phase 3 (Opus, 2026-08-25)

**Bounced, on one item only.** The mechanism, the scope decision and every
measurement that drives it re-derive exactly; the guard's own doc comment
carries a corpus-total figure that does not reproduce and that contradicts a
second figure twelve lines below it. In the issue series whose subject is
unreproducible measurements in prose, that is not a residual to note and move
past.

Status left at `in-progress` rather than reverted to `open`, at the
orchestrator's instruction, matching how `#0265`'s two bounces were recorded.

**Where this was run.** A throwaway `git worktree` pinned at `e6d7ff0`, plus
three more pinned at `b12fed1`, `03db7ce` and `0d8aece` for the historical
figures, so `#0258`'s in-flight edit to `scripts/check.sh` and the concurrent
`#0124` review never entered a measurement. Own database
`opencircuit_test_0268rv` (`scripts/testdb.sh create`; the shared template was
already current, so no rebuild was needed), dropped at the end; `pg_database`
censused before and after, `shortlinks` and `shortlinks_test` never named by
any command. `uptime` first: load average 2.13–2.54. `scripts/check.sh` was
**not** invoked; its steps were run directly and are named below. Every
measurement harness was a scratch `_test.go` inside a pinned worktree, deleted
before the verification runs so its own function names could not enter the
guard's `defined` set.

`git cat-file -t` reports `commit` for `9f43726` and `e6d7ff0`.
`git show --stat 9f43726` touches exactly
`internal/db/issue_citation_guard_test.go`; `git show --stat e6d7ff0` exactly
`issues/0268.md`. Both match their messages.

### What must be fixed

**The corpus-wide raw-token figure in the guard's file-level comment does not
reproduce, and the comment states two different values for it.**

- Line 30 reads `2,796 raw` tokens for the current tree, and line 57 repeats
  `2,796` in the "`1,766` → widening" bullet.
- Line 45 reads `2,783 / 256 / 204 / 166 here` — a second, different total
  presented as the same current measurement.

Re-derived by counting matches of the shipped `issueTestCitationPattern` over
the bodies `extractNamedSections` returns, across all `issues/*.md`:

| Tree measured | Raw tokens in admitted sections |
|---|---|
| `issues/` at `03db7ce`, admitted set as shipped | 2,795 |
| `issues/` at `b12fed1` | 2,790 |
| `issues/` at `9f43726` (**the guard commit**) | 2,821 |
| `issues/` at `e6d7ff0` (**this issue's head**) | 2,828 |

No commit in this pass yields 2,796 or 2,783, and no variation of the admitted
set does either: restricting to the four core headings gives 2,775 / 2,770 /
2,801 / 2,808 at those same four trees, and restricting to the two pre-`#0268`
headings gives 1,770 / 1,770 / 1,802 / 1,809. Including or excluding
`Issues.md` changes nothing, because it contributes no token in an admitted
section.

Two things make this worth a bounce rather than a note:

1. The two figures cannot both be right, and they are twelve lines apart in a
   comment whose opening sentence promises that **every** figure in it is a
   point-in-time measurement taken at the tree this issue was implemented
   against. 2,796 is not that tree's value; the nearest match is the *starting*
   corpus at `03db7ce`, off by one.
2. The `1,766 → 2,796` bullet is an apples-to-apples comparison of one corpus
   under two admitted sets. At `03db7ce` that comparison is `1,766 → 2,795`.

Every other figure in the comment is correct — see below — which is exactly why
this one should be corrected rather than left as the only thing in the file a
reader cannot reproduce. The fix is to re-measure at the commit the next pass
lands on and write one value in both places, or to name the commit each figure
was taken at.

### Also worth fixing while you are in there, not blocking on its own

**`isLineWrappedDefinedTestName`'s "cannot produce a false negative" is
overstated**, in the same way `#0265`'s escalated pass corrected the pipe
rule's strength claim. The precise clause that follows it — *"a wrapped name
whose halves do not join into something real is still reported"* — is true, and
I proved it. The broader sentence is not: I built twelve adversarial fixtures
against the shipped rule and one is discounted while naming something the tree
does not define. Text of the shape

```
… ran SomeUndefinedPrefix
CompletingSuffix was never involved
```

is discounted whenever `SomeUndefinedPrefix` + `CompletingSuffix` is a defined
function, even though the citation was written as a standalone name and the
next line is unrelated prose. That is contrived — the smuggled name has to be a
literal prefix of a real function, immediately continued by that function's
exact remaining characters — so it is not a practical smuggling route, but the
sentence should say what the rule guarantees rather than more.

The other eleven fixtures behave conservatively and are worth recording, since
none of them is pinned by a test today: a blank line between the halves, a
markdown table cell, a list marker, a CRLF line ending, and a next-line token
that overshoots into a *superset* of a defined name are all **still reported**.
`TestIssueCitationLineWrappedNameResolvesOnlyWhenJoinedNameIsDefined` covers
four cases; these five are the interesting boundaries it does not.

**The heading enumeration has two corpus-attested gaps.** Criterion 1 asks for
the admitted set to be enumerated from the corpus. Normalizing every level-2
heading in all 268 files turns up two evidence-asserting spellings that
`issueSectionTitle` produces but `issueCitationSectionHeaders` does not admit:
a numbered second fix pass (two files) and a review-notes heading whose subject
is appended to the title rather than set off by an em-dash or a parenthesis
(one file). All three files are `resolved`, and I measured that all three
sections carry **zero** `Test…`-shaped tokens, so nothing is hidden today and
this does not affect the enforced scope — but the stated reason for
normalizing at all is that renaming a heading must not defeat the guard, and
these two spellings do.

### What passes, and how I checked it

**The per-section table (criterion 3) reproduces exactly.** I wrote my own
level-2 splitter rather than calling `extractNamedSections`, rolled every
`Test…` token up to its enclosing level-2 heading, and applied the shipped
`issueCitationExcluded` at `b12fed1` across all 268 issue files restricted to
the 23 that are `open` or `in-progress`:

| dangling | raw | level-2 section |
|---|---|---|
| 5 | 26 | `## Fix pass` |
| 4 | 10 | `## Description` |
| 2 | 23 | `## Review notes` |
| 0 | 11 | `## Verification` |
| 0 | 2 | `## Implementation notes` |

Those five are the **only** level-2 titles carrying any `Test…`-shaped token in
an `open`/`in-progress` file, so "every other heading carries none there" is
right. Total in scope: 11 dangling, all in `issues/0265.md`.

**The 14, and the breakdown that drove admitting at the level-2 heading.** At
`03db7ce` the same measurement gives **14**, again all in `issues/0265.md`.
Mapping each to the level-3 heading it sits under: 6 in the pipe-rule numbered
subsection of `## Fix pass`, 2 in that section's nested `### Verification`, 2 in
a `### What must be fixed` subsection of `## Review notes`, and 4 in
`## Description`. So a rule that admitted only the nesting the issue named would
have reported 2 of the 10 actionable ones. The load-bearing detail in the
scope argument is correct.

**`## Description` stays excluded, and the reason holds.** All four dangling
citations there are `#0126`'s fabricated names, reproduced verbatim in
`issues/0265.md`'s own `## Description` as the subject matter being discussed.
Scanning that section would fail the guard on the text that documents why the
guard exists.

**The four extra heading spellings are real, not invented.** Normalizing every
level-2 heading in the corpus shows each of the four occurring twice, and none
of the four carries a dangling citation anywhere in the corpus, `resolved`
files included — so admitting them costs nothing measured, as claimed.

**The discount-rule weights, and the reversal after widening.** Toggling each
rule and re-running the scan:

- Pre-widening, with the `b12fed1` guard over the `03db7ce` corpus: deleting
  the marker rule takes the enforced scope 0 → 1; deleting the pipe rule leaves
  it at 0. Corpus-wide 94 → 114 and 94 → 93 respectively.
- Post-widening at `e6d7ff0`: deleting the marker rule takes the enforced scope
  0 → **5**; deleting the pipe rule 0 → **2**; deleting the line-wrap rule
  leaves it at **0**. Corpus-wide the line-wrap rule moves the count by 5.

Every one of those matches. The reversal the issue flags — the pipe rule
becoming load-bearing only after the widening — is real.

**The derived chain reproduces; only the raw total does not.** At `e6d7ff0`:
`#0196`'s four rules leave **256**, the line-wrap rule **251**, the pipe rule
**199**, the marker rule **161**, enforced scope **0**. Dropping the line-wrap
rule from that ordering gives 256 / 204 / 166, which is where the comment's
second history entry's derived numbers come from and why they are internally
consistent with "the line-wrap rule moves the corpus count by 5".

**The line-wrap false-positive class is live in the committed corpus.** The
anecdote in `## Implementation notes` — six wrapped names briefly present in
another agent's working copy — is not checkable: I scanned every committed
revision of `issues/0124.md` and none has a `Test…` token at a line end, which
is consistent with the file having been unwrapped before it was committed, but
is not evidence either way. The rule does not need the anecdote. It discounts
exactly five citations corpus-wide today, and I listed them: two in
`issues/0041.md` and three in `issues/0126.md`, every one a genuine long test
name that markdown broke across a line and whose halves join into a defined
function. That is the shape, in committed files.

**The bare `-run` prefix residual is real, with two live instances.** Both sit
in `issues/0028.md`'s `## Manual verification required`, both are a bare
family prefix passed to `-run` with no pipe adjacent, so the pipe rule cannot
reach them and they are correctly described as a residual of an unadmitted
section rather than a defect.

### Verification I ran

In the pinned worktree at `e6d7ff0`, scratch harnesses removed and
`git status --porcelain` empty:

- `gofmt -l internal/ cmd/` — empty. `go vet ./internal/db/...` — clean.
  `go build ./...` — clean.
- `go test ./internal/db/... -p 2 -v` against `opencircuit_test_0268rv`:
  **16 top-level tests pass, zero skips**, including all three new ones by
  name — `TestIssueSectionTitleNormalizesRealCorpusHeadings`,
  `TestIssueCitationGuardScansEvidenceSectionsNotSubjectMatter` and
  `TestIssueCitationLineWrappedNameResolvesOnlyWhenJoinedNameIsDefined` — plus
  `TestNoOpenIssueVerificationCitesUndefinedTestFunction` and
  `TestIssueCitationExcludedDiscountRulesAreEachLoadBearing`.
- The zero-skip claim was itself checked rather than assumed:
  `TestConnectAndPing` **skips** with `TEST_DATABASE_URL` unset and **fails**
  when it names a database that does not exist, so it really talks to Postgres.
- `go test ./internal/handlers/ -run TestNoCommentCitesUndefinedTestFunction -v`
  — PASS.
- `pgrep -f 'go test|\.test '` empty afterwards; the scratch database dropped;
  `pg_database` census identical before and after apart from it.

### For the next pass

One item is blocking: correct the corpus-wide raw-token figure so the file
states a single value that reproduces at the commit it names. Re-measure; do
not copy the number from here, since the corpus moves with every issue edit —
including this one.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-25 | implement | claude-opus-5 | — | — | — | — | — |

Dispatched on Opus rather than Sonnet because it was worked together with
`#0265`'s escalated pass (`CLAUDE.md` §3), not because of this issue's own
difficulty. No subagent transcript was available to this session to sum exact
token counts, so model and phase are recorded per `issues/Issues.md`'s
"no transcript available" fallback rather than inventing figures.

**Total: —**

## Relation

- Found by: [#0265](0265.md) — the guard this completes
- Relates to: [#0196](0196.md), [#0220](0220.md) — the Go and TypeScript citation guards
- Relates to: [#0126](0126.md) — whose six fabricated citations motivated the guard, and which several issues legitimately quote
