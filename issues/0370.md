# 0370 — CLAUDE.md §11's "§6 … is 933 lines" prose is stale and guarded by nothing

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | docs |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-01 |
| **Commit** | — |

## Description

`CLAUDE.md` §11 carries, in free prose immediately below the section-index
table:

> §6 (the mailing list) is 933 lines — take the subsection, not the parent.

The table row directly above it now says **966**. The prose disagrees with the
guarded row by **33 lines**.

**It sits inches from a fully guarded table and is guarded by nothing.**
`internal/db/prd_section_index_test.go` parses the table rows and runs each
row's own `sed`; it never parses surrounding prose.

Confirmed genuinely pre-existing by `#0368`'s review — `git show 4ed36ef^:CLAUDE.md`
carries it verbatim, and `git log -S` dates it to `2e55a78`. `#0368` correctly
left it alone as out of scope.

**This is the exact drift class this phase has spent twenty issues on**, and the
person it misleads is the one deciding whether to extract §6 or a subsection —
the decision §11 exists to inform.

## The choice this issue must make

1. **Reword so the sentence carries no number.** "§6 is by far the largest
   section — take the subsection, not the parent" is as useful and cannot drift.
2. **Extend the guard** to any `§N … is <number> lines` prose in §11, so the
   number stays checked.

Option 1 is cheaper and removes the failure mode rather than detecting it.
Option 2 keeps a number a reader may actually want. **Choose deliberately and
record why** — and note that this phase's repeated finding (`#0330`, `#0347`,
`#0356`) is that removing a claim beats adding a checker for it.

## Acceptance criteria

1. The stale number is gone or guarded — not merely corrected to 966. **A
   corrected number drifts again on the next edit to §6**, which is how it
   reached 33 lines out.
2. If option 2: prove the extended guard discriminates. Change the prose number
   alone and confirm the test fails, naming it. **A check that cannot fail pins
   nothing** — `#0354`, `#0358` and `#0363` each shipped exactly that this
   phase.
3. Re-measure any number you state by running the relevant row's own `sed`
   command, never by arithmetic. §11 records two ways that table has been wrong:
   §6.2 drifted across two commits unnoticed, and §14 was wrong from birth
   because a regeneration applied `raw + 1` against the exception it had just
   documented. Note §14 has no following heading, so its count is taken **raw**
   at EOF — do not re-break it.
4. `internal/db/prd_section_index_test.go`'s existing tests still pass —
   `TestPRDSectionIndexParity` and `TestPRDSectionIndexTotalMatchesFile`, named
   explicitly in `## Verification` (§5: `ok` does not say what ran).
5. Documentation only. No `.go` file in the diff unless option 2 is chosen, in
   which case the guard change is the only one.

## Relation

- Found by: [#0368](0368.md)'s review, which resolved that issue
- Relates to: [#0368](0368.md) — which moved §6 to 966 and left this deliberately
- Relates to: [#0113](0113.md) — the §11 index regeneration and its two recorded
  failures
- Relates to: [#0356](0356.md), [#0359](0359.md) — the same class: a number in
  prose that nothing checks
- Relates to: [#0330](0330.md), [#0347](0347.md) — the "remove the claim rather
  than add a checker" precedent

## Decision — option chosen, and why

**Option 1: reword so the sentence carries no number.** Changed the prose at
`CLAUDE.md` (below the §11 table) from "§6 (the mailing list) is 933 lines —
take the subsection, not the parent." to "§6 (the mailing list) is by far the
largest section — take the subsection, not the parent." (the issue's own
suggested wording, verbatim).

Reasoning, recorded per criterion 1: a corrected number drifts again the next
time §6 grows — which is exactly how 933 reached 33 lines out, and this pass
demonstrated the point directly: while resolving `#0371` in the same session,
`PRD.md` §6.2's schema comment on `invite_resent_at` grew by 4 lines (to name
the real write-once enforcer instead of the migration's mistaken one), which
pushed §6's own guarded table row from 966 to 970. Had `#0370` corrected the
prose to 966, it would already be wrong again by the time this same commit
landed. This matches the phase's repeated finding (`#0330`, `#0347`, `#0356`):
removing a claim beats adding a checker for it. Extending the guard (option 2)
was rejected — it would pin a number nobody reads for precision (the sentence
only needs to convey "take the subsection"), for the cost of a new regex-based
prose scanner that itself becomes one more thing to keep in sync.

## Verification

`uptime` at start: `load averages: 1.72 2.50 2.91` — idle, no timing failure
observed or accommodated.

**Re-measured by running each row's own `sed` command** (criterion 3), not by
arithmetic — before and after this pass's edits (the §6.2 comment correction
landed as part of `#0371`, in the same commit set, and moves §6's own row):

```
$ sed -n '/^## 6\. /,/^## [0-9]/p' PRD.md | wc -l
971   (raw; guard convention is raw-1 for a found terminating heading → 970)
$ sed -n '/^### 6\.2 /,/^#\{2,3\} [0-9]/p' PRD.md | wc -l
356   (raw → 355)
$ wc -l PRD.md
1857 PRD.md
```

`CLAUDE.md` §11's `§6 Mailing List` row updated 966 → 970, `§6.2 Database
schema` row updated 351 → 355, and the preamble's `` `PRD.md` is N,NNN lines ``
sentence updated 1,853 → 1,857 — all three driven by `#0371`'s PRD.md edit
landing in the same pass, not by this issue's own change (this issue only
removed the stale prose number; it added no lines to `PRD.md`). §14 was not
touched and was not re-measured incorrectly: confirmed still raw (no
terminating heading) at 14, unchanged.

**Criterion 4** — named tests, run individually with `-v` against a scratch
database (`opencircuit_test_0370`, dropped after):

```
=== RUN   TestPRDSectionIndexParity
--- PASS: TestPRDSectionIndexParity (0.00s)
=== RUN   TestPRDSectionIndexTotalMatchesFile
--- PASS: TestPRDSectionIndexTotalMatchesFile (0.00s)
PASS
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db	0.231s
```

`ISSUE=0370 scripts/check.sh go ./internal/db/... ./internal/subscribers/...
./cmd/...` → `go build`, `go vet`, `gofmt -l` all clean; `internal/db`,
`internal/subscribers`, `cmd/opencircuit` all `ok`, zero skips reported by the
skip audit, `VERIFICATION PASSED`. `scripts/check.sh guards` → all five guard
scripts (`check_guard_test.sh`, `testdb_gc_guard_test.sh`,
`dev_guard_test.sh`, `db_reset_guard_test.sh`,
`go_file_visit_floor_guard_test.sh`) plus the outbox/citation self-checks
held, `VERIFICATION PASSED`.

**Criterion 5, documentation only.** `git diff --stat -- CLAUDE.md PRD.md`
after this pass: `CLAUDE.md | 9 +++++----`, `PRD.md | 16 ++++++++++------`.
`git diff --stat -- '*.go'` shows only files two other concurrent agents were
editing (`internal/handlers/admin_pending_test.go`,
`internal/subscribers/store.go`), neither touched by this pass. No `.go` file
in this issue's diff — option 2 (extending the guard) was not chosen, so no
guard code changed either.

Scratch database `opencircuit_test_0370` dropped (`scripts/testdb.sh drop
0370` confirmed "does not exist"); no leftover `go test`/`.test` processes
(`pgrep -fl` empty).

## Work log

- 2026-09-01 — Filed by the orchestrator from `#0368`'s review (§9).
- 2026-09-01 — implement (claude-sonnet-5, phase 2 subagent, batched with
  `#0371`): chose option 1 (reword, no number) over option 2 (extend the
  guard) — reasoning above. Re-measured every affected `CLAUDE.md` §11 row by
  running its own `sed` command rather than by arithmetic; the §6 and §6.2
  row updates and the PRD.md total-line preamble update were driven by
  `#0371`'s PRD.md §6.2 comment correction landing in the same pass, not by
  this issue's own edit. Verified with `ISSUE=0370 scripts/check.sh go
  ./internal/db/... ./internal/subscribers/... ./cmd/...` (named tests
  `TestPRDSectionIndexParity` and `TestPRDSectionIndexTotalMatchesFile` run
  individually and confirmed `PASS`) and `scripts/check.sh guards`, both
  `VERIFICATION PASSED`, zero skips. No `.go` file touched. Left status
  `in-progress` for review (§9 — only the reviewer sets `resolved`). Usage/cost
  not recorded here — no transcript token accounting available to this
  subagent; the orchestrator should append the usage row per
  `issues/Issues.md`'s `## Work log` convention.
