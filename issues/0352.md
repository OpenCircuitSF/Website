# 0352 — Doc comments in the claim-kinds guard cite line numbers that drift silently and are checked by nothing

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-08-31 |
| **Closed** | 2026-08-31 |
| **Commit** | 9618626 |

> 🟢 Resolved 2026-08-31 — both citations are gone, replaced with
> identifiers I re-measured myself and found unique and correct. My own
> whole-file survey, run with a wider pattern than the one reported, finds
> zero remaining `<file>.go:<int>` citations and zero of any looser
> line-citation form across all 1040 lines. The change is comment-only by my
> own independently-built prover **and** against the `#0330` guard
> mechanisms, which are byte-for-byte untouched. Criterion 5's decline is a
> sound scope call and I endorse the follow-up.

## Description

`internal/outbox/claim_kinds_call_site_guard_test.go:204`, inside
`claimKindsGuardMinPlausibleNonExemptCallSiteCount`'s `#0322` paragraph, cites
`internal/mailing/worker.go:575` for the unrelated
`mailing.SendStore.OrphanSweep` name collision. Measured by AST census today,
that call is at **`worker.go:625`**.

`nameMatchesGuardedMethod`'s doc comment ~300 lines below in the same file
already carries the correct number. **So one file states two different
locations for the same call**, and nothing detects the divergence: the shell
harness checks constants and required-site names, and
`internal/db/prd_section_index_test.go` covers only `PRD.md`.

**Correcting `575` to `625` buys nothing.** The next unrelated edit to
`worker.go` re-breaks it — which is exactly what already happened here. A line
number in a doc comment drifts on every edit to the cited file and is checked
by nothing, and this project already knows the answer: §11's PRD index was
deliberately rewritten to be **heading-anchored rather than line-numbered**,
"so they survive edits to the PRD".

This is the third comment-accuracy defect in this one guard family
(`#0334`, `#0345`, and now this), and today three separate reviews had to
re-measure numbers in this file.

**Note for whoever picks this up**: `#0330`'s implementer reported this defect
at `nameMatchesGuardedMethod`'s comment citing `worker.go:543`/`:575`. That
report is itself wrong — `nameMatchesGuardedMethod` is correct, the string
`:543` appears nowhere in the file, and the single stale citation is the one at
line 204. Re-measure rather than trusting either that report or this paragraph.

## Root cause

Two paragraphs of `claim_kinds_call_site_guard_test.go` cite the same real
call site — `mailing.SendStore.OrphanSweep`'s invocation inside
`Worker.drainCampaign` — by line number, and the numbers had drifted apart
(`:575` vs. the correct `:625`) after an unrelated edit to `worker.go`.
Nothing in the guard family checks a doc comment against the tree it
describes, so the divergence was invisible until a human (or reviewer)
happened to re-measure.

## Fix

Re-measured both citations myself, independent of this issue's own prose,
`#0330`'s implementer report, and the file's own (correct) claim:

- **Method**: `grep -n "OrphanSweep" internal/mailing/worker.go
  internal/mailing/worker_store.go internal/mailing/worker_store_test.go`,
  cross-checked against which enclosing function/test each hit falls under
  (`grep -n '^func ' internal/mailing/worker.go` bracketing the call line).
- **Result**: the call lives at `internal/mailing/worker.go:625`, inside
  `func (w *Worker) drainCampaign(...)` — **not** `Worker.recoverOrphans`,
  the name this issue's own criterion 2 example used; no such identifier
  exists anywhere in the tree (`grep -rn recoverOrphans internal/mailing`
  returns nothing), so that example was illustrative, not a fact to copy.
  `worker_store_test.go`'s three call sites split as one call inside
  `TestSendStore_OrphanSweep_ResetsOnlySendingRows` and two inside
  `TestSendStore_OrphanSweep_LeavesFreshlyClaimedRowAlone`.

Replaced both citations (the stale one at the old line ~204 and the
already-correct one at the old line ~527, in `nameMatchesGuardedMethod`'s
doc comment) with those names — `Worker.drainCampaign`'s call, and the two
named test functions — rather than updating `575` to `625`. A repo-wide
survey (`grep -noE '[A-Za-z0-9_./]+\.go:[0-9]+'` over the file) found
**exactly these two** `<file>.go:<int>` citations in the whole file, both
now removed; the same grep against the post-edit file returns zero matches.

No check, constant, assertion, or the required-sites list in this file was
altered — confirmed by `scripts/go_file_visit_floor_guard_test.sh` (still
42 total / 12 non-exempt) and by `ISSUE=0352 scripts/check.sh guards`
passing unchanged.

**Criterion 5 — extending the citation-guard family to flag `<file>.go:<int>`
repo-wide — judged out of scope for this pass, not silently skipped.**
Reason: `internal/handlers`' existing citation-guard family
(`dangling_test_citation_guard_test.go`, `citation_target_guard_test.go`,
`misattached_doc_comment_guard_test.go` — ~2,200 lines together) is the
product of five issues' worth of design iteration each (`#0190`, `#0192`,
`#0196`, `#0199`, `#0216`, `#0220`), with mutation-tested exclusion rules
and an external shell oracle per guard. A `<file>.go:<int>` pattern has a
harder false-positive surface than a path or `CLAUDE.md §N` citation: it
must be told apart from a runtime-computed `%s:%d` diagnostic format
string (this very file uses one, e.g. `"%s:%d: %s called with no
kinds..."`), from a legitimate external stack-trace or log-line reference,
and from the guard family's own self-citations of its own line numbers in
places that are fine to leave as-is. That is real design latitude — per
`CLAUDE.md` §3's own criteria for when a planning pass earns its cost
("its acceptance criteria are ambiguous enough that two implementers would
build different things") — and mixing an unplanned ~500-line guard into a
comment-only fix (criterion 6) would also blow past this issue's own
proved scope. Recommending a new issue, planned by Opus (Phase 1) before
a Sonnet implementation pass, rather than building it here unplanned.

## Verification

- `grep -noE '[A-Za-z0-9_./]+\.go:[0-9]+'
  internal/outbox/claim_kinds_call_site_guard_test.go` — zero matches after
  the edit (two matches, `worker.go:575` and `worker.go:625`, before).
- **Criterion 6 (comment-only, structural proof).** Built a standalone Go
  tool (`commentdiff`, compiled to a binary, read via an explicit
  `VERDICT:` line per §8's fail-open catalogue) that parses two files with
  `go/parser` WITHOUT `ParseComments` (comments never enter the AST),
  re-renders each with `go/printer` under an identical config, and compares
  SHA-256 of the rendered bytes.
  - **Falsifiability proved first**: fed a real code-changing fixture pair
    (`const N = 1` vs. `const N = 2`, both carrying an identical comment) —
    tool reported `VERDICT: CODE-DIFFERS`, distinct hashes.
  - **Fail-closed proved**: an empty file, a structurally broken file
    (unbalanced brace — `go/parser` genuinely refuses to parse it, unlike a
    bare `go/scanner` tokenizer, which would happily tokenize broken
    syntax), a missing file, and a bad argument count all produced
    `VERDICT: ERROR: <reason>`, never `VERDICT: COMMENT-ONLY`.
  - **Real comparison**: `commentdiff <(git show HEAD:…) <working tree copy>`
    → `VERDICT: COMMENT-ONLY`, `sha256(canonical A) = sha256(canonical B) =
    d364b589…eece665`.
- **Criterion 7.** `bash scripts/go_file_visit_floor_guard_test.sh` — "all
  guards hold (0 failures)"; the outbox section reports
  `claimKindsGuardMinPlausibleCallSiteCount=5` against a population of
  **42**, `claimKindsGuardMinPlausibleNonExemptCallSiteCount=8` against
  **12** — both unchanged from before the edit (confirmed by grep-counting
  `\.ClaimDue(\|\.OrphanSweep(\|\.SelectDue(` against the HEAD and working
  copies of the file: 6 and 6, identical).
- **Criterion 8 (gofmt stability).** `shasum -a 256
  internal/outbox/claim_kinds_call_site_guard_test.go` equals `gofmt
  internal/outbox/claim_kinds_call_site_guard_test.go | shasum -a 256`
  (`09168fd9…4df98bf` both sides); `gofmt -l` reports nothing.
- `ISSUE=0352 scripts/check.sh go ./internal/outbox/... ./internal/mailing/...
  ./internal/handlers/...` — `go build`, `go vet`, `gofmt -l` clean;
  `go test` reports `ok` for all three packages
  (`internal/outbox` 0.546s, `internal/mailing` 3.736s, `internal/handlers`
  7.518s) with zero skips; `VERIFICATION PASSED`.
- `ISSUE=0352 scripts/check.sh guards` — all guard suites, including
  `go_file_visit_floor_guard_test.sh` and `db_reset_guard_test.sh`,
  `VERIFICATION PASSED`.
- `uptime` checked before each run (load averages 1.45–3.36, well under the
  ~8 threshold); test database `opencircuit_test_0352` dropped by
  `scripts/check.sh` on exit (`scripts/testdb.sh drop 0352` confirmed
  "does not exist" afterward).

## Files changed

- `internal/outbox/claim_kinds_call_site_guard_test.go` — replaced the two
  `<file>.go:<int>` citations of `mailing.SendStore.OrphanSweep`'s call
  site (one stale, one already correct) with the declaration/test-function
  names (`Worker.drainCampaign`;
  `TestSendStore_OrphanSweep_ResetsOnlySendingRows`,
  `TestSendStore_OrphanSweep_LeavesFreshlyClaimedRowAlone`). Comment-only;
  no check, constant, assertion, or required-sites list touched.

## Acceptance criteria

1. The stale citation at line 204 no longer states a wrong location.
2. **Cited positions are replaced with stable identifiers rather than corrected
   numbers** — name the declaration and the call (`Worker.recoverOrphans`'s call
   to `SendStore.OrphanSweep`; `worker_store_test.go`'s three sites), the way
   §11's heading-anchored index does. A pass that merely updates `575` to `625`
   does not resolve this issue.
3. Apply this throughout the file, not only at line 204 — a survey of every
   `<file>.go:<int>` citation in it, each one either removed in favour of an
   identifier or justified in place.
4. Any location stated is re-measured against the tree, not copied from this
   issue or from any comment. Record the method.
5. **Consider extending the existing citation-guard family** in
   `internal/handlers` — which already catches comments citing unresolvable
   paths and undefined test functions — to flag `<file>.go:<int>` citations in
   Go doc comments repo-wide. If you judge that out of scope or not worth it,
   say so explicitly with a reason rather than silently skipping it; that
   judgement is part of this issue's deliverable.
6. Comment-only. Prove it structurally: parse both revisions with comments
   excluded, compare bytes, tool proved falsifiable first and failing closed on
   empty input (§8 — three agents found genuine fail-opens in their own
   instruments this session).
7. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal
   introduced into any Go comment (`#0323`'s trap — documenting the pattern once
   inflated the measured population 35 → 36). Re-run
   `scripts/go_file_visit_floor_guard_test.sh` and record the population
   (currently 42 total / 12 non-exempt).
8. `gofmt` stability: file sha256 equals sha256 of `gofmt` stdout — these are
   declaration doc comments, exactly what §8's smart-quote gotcha rewrites.

## Relation

- Found by: [#0330](0330.md)'s review, which corrected the implementer's own
  mis-location of the defect
- **Depends on: [#0330](0330.md)** — its second pass is editing this same file;
  land that first or the two will conflict
- Relates to: [#0347](0347.md) — the same class, comments naming things the
  code does not bear out; consider whether that guard should cover this
- Relates to: [#0334](0334.md), [#0345](0345.md) — the other two
  comment-accuracy defects in this same guard
- Relates to: [#0322](0322.md) — whose paragraph carries the stale citation
- Relates to: [#0113](0113.md) — the PRD index made heading-anchored for
  exactly this reason

## Work log

- 2026-08-31 — Filed by the orchestrator from `#0330`'s review (§9).
- 2026-08-31 — Implementation (Sonnet, phase 2). Re-measured both citations
  independently (grep + manual function-boundary check, not copied from
  this issue, `#0330`, or the file's own text), replaced both with stable
  identifiers, proved the change comment-only with a purpose-built
  falsifiable/fail-closed AST tool, re-ran the external shell harness
  (unchanged: 42 total / 12 non-exempt) and `ISSUE=0352 scripts/check.sh
  go/guards` (all green). Judged criterion 5 out of scope for this pass
  and recorded the reason above. Code commit `9618626`. Status left
  `in-progress` for review.
- 2026-08-31 — Review (Opus, phase 3). All eight criteria independently
  re-verified against my own `go/ast` census, my own citation survey, and my
  own comment-only prover; set `resolved`. No `CLAUDE.md` §3 escalation
  decision arises — this is the issue's first review and it passed, so
  implementation stays on Sonnet by default. Two items reported to the
  orchestrator rather than filed (§9): criterion 2's own
  `Worker.recoverOrphans` identifier is fiction, confirmed independently,
  and 30 further `<file>.go:<int>` citations survive elsewhere in the tree,
  at least two of them already stale.

## Review notes

> 🟢 Resolved 2026-08-31 (review, `claude-opus-5`) at `9618626`/`a008e88`.
> All eight criteria met. The fix does the right thing for the right reason:
> it replaces two drifting line numbers with identifiers I verified are
> unique, correct, and genuinely more stable — not line numbers in disguise.
> I re-derived every number and every attribution from my own `go/ast` census
> and my own citation survey, reusing nothing from the report, and built my
> own comment-only prover on a different mechanism from the implementer's.

### The `recoverOrphans` finding — confirmed, and it is the issue's own defect

The implementer is right, and I confirmed it independently.
`grep -rn recoverOrphans .` over the whole repo returns **three hits, all
inside `issues/0352.md` itself** — criterion 2's example text, and the
implementer's two references to it. **Zero occurrences in any Go file.**

The real call site is `internal/mailing/worker.go:625`, and my AST census
places it inside `func (w *Worker) drainCampaign` (declared at `:620`, next
declaration `drainLoop` at `:674`, so `625` falls inside its span). The
implementer used `Worker.drainCampaign` — the correct identifier — rather
than transcribing the one its own acceptance criterion named. **That is the
right call, and it is the behaviour `CLAUDE.md` asks for** ("Check an issue's
own greenfield language before following it" generalises: an issue's
acceptance criteria are not automatically facts about the tree).

This is the fourth wrong identifier written into an issue today. Worth the
orchestrator's attention as a pattern, not just an instance: the common
thread is a reviewer's *illustrative* phrasing being transcribed into a
criterion as though it were *measured*.

### My own citation survey (criterion 3)

Run against `6730380`'s copy of the file and the working tree, with a pattern
deliberately **wider** than the implementer's — it accepts `.sh`, `.md`,
`.sql`, `.ts`, `.svelte`, `.json`, `.html` as well as `.go`, and tolerates
whitespace around the colon:

| | before (`6730380`) | after |
|---|---|---|
| `<file>.<ext>:<int>` citations | **2** | **0** |

Before: `204: worker.go:575` (stale) and `527: worker.go:625` (correct).
After: none. Exactly the two reported, and no third the report missed.

I then swept all 1040 lines for three *looser* shapes a narrow grep would
miss — a bare `:NNN`, the word `line`/`lines` followed by digits, and `at`
followed by 2–4 digits. **Zero hits of any of them.** So the file today
states no positional citation of any form. Criterion 3 is met on the whole
file, not just at line 204.

### The identifiers are stable, and checkable — criterion 2 met on the merits

The question the orchestrator asked was whether any replacement is "as
fragile as a line number in disguise". It is not, and the distinction is
principled rather than aesthetic:

- A **line number** is invalidated by an edit to any *unrelated* part of the
  cited file. That is what makes it drift *silently* — nothing about the
  edit relates to the citation, so nobody thinks to look.
- An **identifier** is invalidated only by an edit to *the cited thing
  itself*. That edit is exactly the occasion on which a maintainer would
  update the citation. This is §11's heading-anchored reasoning applied
  correctly.

All three replacements resolve to exactly one declaration in the tree, so
none is ambiguous:

| Identifier | Declaration |
|---|---|
| `Worker.drainCampaign` | `internal/mailing/worker.go:620` |
| `TestSendStore_OrphanSweep_ResetsOnlySendingRows` | `internal/mailing/worker_store_test.go:91` |
| `TestSendStore_OrphanSweep_LeavesFreshlyClaimedRowAlone` | `internal/mailing/worker_store_test.go:137` |

There is a second, quieter gain worth recording: `internal/handlers`'
existing `citation_target_guard_test.go` already checks that **cited test
function names resolve**. So the two `TestSendStore_*` citations are now the
*shape* an existing guard family can check, where a line number is
structurally uncheckable by it. The fix moves the citations into a
checkable class, which is the deeper point of the issue.

### Independent census — every attribution confirmed

`go/ast` walk written for this review from the guard's spec (roots
`internal/` + `cmd/`, prune `node_modules`/`dist`, `*.go`, selector-call name
in {ClaimDue, OrphanSweep, SelectDue}, exempt = directory is
`internal/outbox`), plus enclosing-`FuncDecl` span attribution. Reused none of
the guard's helpers and no number from any issue or report:

**269 files visited · 36 total call sites · 12 non-exempt · 24 exempt** —
reproducing `#0330`'s and `#0345`'s censuses exactly.

The four `mailing.SendStore.OrphanSweep` collision sites, with enclosing
functions:

| Site | Enclosing function |
|---|---|
| `worker.go:625` | `Worker.drainCampaign` |
| `worker_store_test.go:109` | `TestSendStore_OrphanSweep_ResetsOnlySendingRows` |
| `worker_store_test.go:151` | `TestSendStore_OrphanSweep_LeavesFreshlyClaimedRowAlone` |
| `worker_store_test.go:165` | `TestSendStore_OrphanSweep_LeavesFreshlyClaimedRowAlone` |

The one-in-the-first-test / two-in-the-second split the new comment asserts is
**exactly right**. Both comments' arithmetic still resolves to four, matching
the population. Sub-claims spot-checked too: `Worker.store` really is declared
`*SendStore` (`worker.go:312`), and the call really does go through it.

### Criterion 6 — comment-only, proved by my own instrument

I did not reuse the implementer's `commentdiff`. Mine compares a **`go/scanner`
token stream with comments unscanned** — a different basis from their
`go/printer` round-trip, so the two agree by measurement rather than by shared
implementation. Because `go/scanner` alone fails open on broken syntax (it
tokenizes garbage happily), every input must **also** parse cleanly under
`go/parser` first. Verdict is an explicit `VERDICT:` line, never an exit code.

Proved falsifiable **before** trusting it, on two different code-change shapes:

- `const N = 1` vs `const N = 2`, identical comments → `VERDICT: CODE-DIFFERS`
- `func Foo()` vs `func Bar()` (a pure rename — the shape a "comment-only"
  edit could most plausibly smuggle) → `VERDICT: CODE-DIFFERS`
- positive control, two genuinely different comments around identical code →
  `VERDICT: COMMENT-ONLY`

Proved fail-closed on five inputs, each producing `VERDICT: ERROR`, never
`COMMENT-ONLY`: an empty file on either side; **both sides empty** (the
classic `0 == 0` fail-open, and the one that matters most); structurally
broken Go; a missing file; wrong argument count.

Real run — `6730380`'s copy vs both `9618626` and the working tree:

```
tokens=2144  sha=822fecb12afe915bfa5169583ca34f8324b6021794426d1b2031441193c8baca   (both sides)
VERDICT: COMMENT-ONLY
```

### Comment-only *against the guard mechanisms*, not merely by parse

This is the check that a parse-based proof does not give you, and it matters
here because `scripts/go_file_visit_floor_guard_test.sh` greps the file's
**text**, comments included — so a comment edit *can* move it (`#0323`'s
trap), and a comment could in principle plant a decoy line matching the
harness's anchored patterns (`#0258`'s shape). Measured, pre vs post:

| | pre | post |
|---|---|---|
| `\.(ClaimDue\|OrphanSweep\|SelectDue)\(` occurrences | 6 | 6 |
| `requiredNonExemptSitePresent(allSites,` occurrences | 1 | 1 |
| `claimKindsGuardRequiredNonExemptCallSites` entries | both named | both named |
| top-level `const`/`var`/`func`/`type` declaration list | identical | identical |
| **every non-comment line** | **identical** | **identical** |

`diff` of all non-comment lines reports no differences at all, and no added
line carries guarded-method call syntax. Nothing `#0330` settled has moved:
its four harness items still `PASS` with their corrected message text intact,
and the required-list lines are byte-identical (only their line numbers
shifted 383/384 → 385/386, which the harness's shape-anchored grep does not
depend on).

### Criterion 5 — the scope call is sound, and I endorse it

**Verdict: a sound scope call, not declining work the issue asked for.**
Three independent reasons, in ascending order of weight:

1. **The criterion's own terms allow it.** It says "*Consider* extending…
   If you judge that out of scope or not worth it, say so explicitly with a
   reason… that judgement is part of this issue's deliverable." A reasoned
   decline is a satisfying outcome by construction, and the implementer gave
   the reason in the issue, in the commit message, and to the reviewer.
2. **Criterion 6 forbids the alternative.** This issue also requires the
   change be comment-only. Building a ~500-line guard here cannot satisfy
   both criteria at once. The issue's own structure forces the decline.
3. **It is genuine design latitude under §3.** I verified the load-bearing
   fact: the existing family is **2,173 lines** across the three named files
   — the "~2,200" is accurate, not rhetorical. And the exclusion design is
   really unsettled. A follow-up must *decide*, not merely apply: whether to
   scan comments only or all text; how to tell a citation from a **fixture**;
   whether to fix the existing population or grandfather it (an allowlist
   being exactly §8's "copy of the answer stored next to the question");
   and, per §8's direction-of-comparison rule, whether the oracle belongs in
   Go or in the external harness. Two implementers would build different
   things. §3's planning trigger is met.

One correction to the implementer's reasoning, which does not change the
verdict. It cites runtime `%s:%d` diagnostic format strings as a
false-positive class — but those live in **string literals**, and the natural
design (scanning doc comments) excludes them for free. I found two such
format strings in the tree, one in this very file at `:722`; neither is in a
comment. That supporting example is weaker than presented. The other two
classes are real, and I confirmed both by inspection:
`internal/mailing/outbox_kind_detection_test.go:30` cites itself inside an
indented block that is a **rendered `go test` failure transcript**, not a
citation at all; `internal/handlers/citation_target_guard_test.go` carries 11,
which are the citation guard's own **prose about citations**. A naive guard
flags both.

**I endorse the follow-up**, and it should carry a planning pass. My census
gives it a concrete scope the recommendation did not have: **30
`<file>.go:<int>` citations survive in Go comments across 7 files** — this
fix removed 2 of 32. Spot-checking two at random found **both already stale**:

- `internal/seo/sitemap_test.go:80` says "`sitemap.go:86` checks only
  `w.Status`, never `w.Published`", but `sitemap.go:86` is
  `workshops, err := s.source.Workshops()`.
- `internal/mailing/templates_test.go:404` says SESMailer "only rejects
  neither being set — `mailer.go:105`", but `mailer.go:105` is a **blank
  line**.

So the class is not hypothetical and is not confined to this guard. That is
the argument for filing.

### Verification I ran

```
uptime                                                    # 2.23–2.72 throughout, idle
ISSUE=0352r scripts/check.sh go ./internal/outbox/... ./internal/mailing/... ./internal/handlers/...
#   build/vet/gofmt -l clean · outbox ok 0.432s · mailing ok 3.545s ·
#   handlers ok 7.913s · (no skips) · VERIFICATION PASSED
bash scripts/go_file_visit_floor_guard_test.sh            # all guards hold (0 failures), exit 0
scripts/check.sh guards                                   # VERIFICATION PASSED
gofmt -l internal/outbox/claim_kinds_call_site_guard_test.go   # clean
#   sha256(file) == sha256(gofmt stdout) == 09168fd922882442f3a378d59045af7ac6b1a28eb6ec1fd603bf0f28b4df98bf
```

- **Criterion 7.** Population **42 total / 12 non-exempt** — unchanged, and
  both `#0304`/`#0322` floor sections plus all four `#0330` items `PASS`.
  Zero `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call syntax on any
  added (`+`) line of `9618626`.
- **Criterion 8.** File sha equals `gofmt` stdout sha, `gofmt -l` silent. I
  also scanned the added lines for §8's two smart-quote sequences — doubled
  `'` and doubled backtick — **zero of either**, so these declaration doc
  comments are stable under a future `gofmt -w`.
- `9618626` touches exactly one file (+8/−3) and no script; `6730380..HEAD`
  touches only it and `issues/0352.md`.
- Own database `opencircuit_test_0352r`, dropped by `scripts/check.sh` on
  exit. No leftover `go test`/`.test` processes. `:5173` confirmed free
  before the `guards` run and released after. The pre-existing
  `/private/tmp/oc-live` worktree was not touched; no mutation was performed
  on the shared tree at all — every comparison ran against `git show` copies
  in my own scratchpad.

### Observation — not a defect, not for filing

`nameMatchesGuardedMethod`'s doc comment enumerates the third argument of
every real `SendStore.OrphanSweep` call as "a literal duration, a named
duration variable, or a signed duration expression". The four real arguments
are `orphanStaleAfter`, `0`, `time.Hour`, and `-time.Hour` — the bare `0` is
an untyped int literal, which the enumeration does not name. The comment's
*substantive* claim is unaffected and I verified it holds: `classifyKindsArg`
switches only on `*ast.Ident` (`nil`/`AllKinds`), `*ast.SelectorExpr`
(`.AllKinds`), and an empty `*ast.CompositeLit`, so a `*ast.BasicLit` `0`
falls through to `kindsArgOther` and is never read as "no kinds". Pre-existing
text, untouched by this diff, outside this issue's criteria, and cosmetic.
Recorded so the next reader does not re-derive it.
