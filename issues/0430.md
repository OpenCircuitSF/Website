# 0430 — The deferred-S3-backup rationale rests on an AWS account that now exists

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | docs / backups |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-04 |
| **Commit** | — |

## Description

`docs/deployment.md`'s backup section quotes `#0229`'s `## Decision` as the
reason S3 upload was deferred:

> building it needs an AWS account, **which does not exist yet** (`CLAUDE.md` §10
> item 2), and the user deferred all AWS work to deployment, same as SES.

**The account exists** — `378152330719`, with SES live in it and an IAM instance
role attached (`#0426`, confirmed against the box). So the stated precondition
for the deferral has been met, and the sentence now reads as a live blocker that
is not one.

The passage also cites `CLAUDE.md` §10 item 2 for that claim; §10 item 2 records
SES as "all but done" and the account as real.

**Note `#0426` already corrected the *adjacent* instance** of this same clause,
a few hundred lines further down, with a dated correction naming the account.
This one was left because it belongs to the backups subsystem rather than IAM —
the right scope call, and why it is filed separately.

## What this issue is actually about — read before editing

**The deferral may still be correct.** "The account does not exist" was one
reason among several; the other — *"the user deferred all AWS work to
deployment"* — is a scheduling decision, not a technical precondition, and
deployment has now happened.

So the question is not merely whether the sentence is stale. It is:

- Is S3 backup upload still deliberately deferred, and if so **on what current
  grounds**?
- Or was the account's absence the load-bearing reason, in which case the
  deferral's basis has lapsed and someone should decide afresh?

**Do not silently rewrite the rationale into a different one.** If the current
grounds are not recorded anywhere, say so — an honest "the original reason has
lapsed and no replacement is recorded" is more useful than a manufactured
justification. `#0421` faced exactly this with PRD §10.3's "closest to San
Francisco" rationale and correctly declined to invent a replacement.

## The backups themselves are not in question

`docs/deployment.md` records that `backup.sh` takes local dumps with a
`find -mtime` prune. This issue is about the **documented reason** for having no
offsite leg, not about whether backups run. If you find the local backups are
themselves not running, that is a separate and more urgent finding — report it
rather than folding it in.

## Acceptance criteria

1. The passage no longer asserts the account does not exist. **Verify the
   account's existence yourself**, read-only — do not copy the ID from this
   issue.
2. The current status of S3 backup upload is recorded accurately: still
   deferred with stated current grounds, or the lapse of the original grounds
   recorded plainly. **Do not invent a rationale.**
3. `PRD.md` §10.6 is checked for the same claim and corrected or left with a
   reason. Editing `PRD.md` means **§11's line counts must stay exact**,
   verified by the row's own `sed` (`#0113`) — note §11 and `PRD.md`'s total
   were changed twice today by `#0422` and `#0423`, so read the current state.
4. `#0229` is `resolved`; per §9 its settled content stays. If it needs a
   forward-pointing note, follow `#0421`'s precedent — additive only, and prove
   it additive.
5. Follow `#0072`'s dated-note convention; leave genuine decision-log history
   intact per `#0271`'s `## Scope`. Say which occurrences you judged to be which.
6. **No mechanism** (`#0330`/`#0347`/`#0356`/`#0381`).
7. Run `./internal/db/...` and `./internal/handlers/...` (§5's repo-wide-guard
   table). Per §5 name the specific tests that ran — real function names, not a
   `-run` prefix, which has bounced four issues today. Re-run after writing your
   issue-file edits. No Go-file-plus-line-number citation (`#0352`), scanned
   with `python3` bytes rather than BSD `grep -P`.

## Relation

- Found by: [#0426](0426.md)'s implementation pass, which corrected the adjacent
  instance and scoped this one out
- Relates to: [#0229](0229.md) — whose decision this quotes
- Relates to: [#0426](0426.md) — the same false premise, different subsystem
- Relates to: [#0421](0421.md) — the precedent for declining to invent a
  replacement rationale
- Relates to: PRD §10.6 — the backups section

## Work log

- 2026-09-04 — Filed by the orchestrator from `#0426`'s implementation pass (§9).
  The quoted passage and `#0426`'s adjacent correction were both read before
  filing; the account's existence is established by `#0426`'s own measurement.
