# 0166 — Deleting the client renderer dropped `#0138`'s body-link URL policy, and nothing replaced it server-side

| | |
|---|---|
| **Status** | resolved |
| **Platform** | All |
| **Module** | mailing / handlers |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-22 |
| **Commit** | `58c4909`, `599be2a` |
| **Closed** | 2026-08-22 |

## Description

`#0136` moved workshop body rendering to goldmark on the server and deleted `renderMarkdownPreview`, which had run **every body link** through `isSafeLinkHref`. Goldmark's safe mode refuses dangerous *schemes* but enforces less than that validator did, so three forms now render as live links where they previously did not:

1. **`//evil.host/x`** — resolves to `https://evil.host/x`.
2. **`///evil.host/x`** — resolves off-site identically. Any replacement rule must match the **normalized** form, not a literal two-slash prefix.
3. **`data:image/png|gif|jpeg|webp`** — survives as a live `<a href="data:…">`, because goldmark's `IsDangerousURL` allowlists those four types. The deleted validator rejected all `data:`. Found by `#0136`'s review and **not disclosed by the implementer**, unlike the first form.

**None of this is a live vulnerability.** `#0138` decided body links may point off-site by design, so `//evil.host` grants nothing an ordinary `https://evil.host` link didn't; §9's self-contained rule governs loads the page performs, not navigations a reader chooses. `data:image/svg+xml` **is** still refused (verified), and browsers block top-level `data:` navigation. The property is also inherited unchanged from campaigns, which render through the same function.

**The reason it needs an owner** is bookkeeping, not risk: `#0138`'s acceptance criterion 5 explicitly extended its ruling to "link destinations in Markdown body text". That box is ticked and `#0138` is resolved — but `#0136` deleted the only code enforcing it. The tracker currently claims a rule that nothing implements.

## Acceptance criteria

- [x] Decide, and record: does `#0138`'s body-link rule still apply? Either restore it server-side, or amend `#0138`'s criterion 5 with a note saying it no longer holds and why — **amended, not restored.** `#0138`'s criterion 5 now carries a dated note explaining that the *decision* still holds (body links may resolve off-site by design) but the *enforcement mechanism* it named was deleted by `#0136` and was not rebuilt; body-link destinations now follow goldmark's own scheme policy, same as `email_campaigns.body_md` always has. See `## Fix` below for the full reasoning.
- [x] ~~If restoring: enforce on the **normalized** destination so `//host`, `///host`, and backslash spellings are all caught, and decide whether `data:image/*` should stay allowed~~ **Struck — moot.** Nothing was restored, so there is no validator to normalize destinations for. `data:image/*` stays allowed (goldmark's existing behavior), recorded as a deliberate accept in `#0138`'s amendment, not silently left alone.
- [x] Whatever is decided applies to `email_campaigns.body_md` too, or the decision records why the two bodies differ — they share `mailing.RenderMarkdownHTML` — **applies identically.** Both workshop bodies and campaign bodies render through the exact same `mailing.RenderMarkdownHTML`/goldmark configuration; nothing in this change is workshop-specific, so there is no divergence to record.
- [x] Tests cover all three forms plus the legitimate cases that must keep working (`https://`, `mailto:`, root-relative) — `TestRenderMarkdownHTML_BodyLinkPolicyDocumented` (`internal/mailing/campaign_markdown_test.go`) covers all three narrowed forms plus `data:image/svg+xml` (still refused) plus the three legitimate cases, table-driven. `#0136` already separately pinned the `//evil.host` form in `internal/handlers/admin_workshop_preview_test.go`'s `TestAdminWorkshopPreview_ProtocolRelativeLinkInheritsCampaignBehavior`; the new test fills in `///evil.host`, `data:image/png`, and the legitimate-case side, at the shared `mailing` package level so it documents both workshop and campaign bodies at once.
- [x] Add a `cover_image` `data:` case to `#0157`'s fixture (`testdata/url_validators.json`) — one case appended (`cover_image-scheme-shape-3139`, `data:image/png;base64,…`, `want:false`), verified structurally rejected by both the real Go `isSafeCoverImage` and the real TS `isSafeCoverImage` (executed, not assumed) before appending. See `## Fix` for why this was a hand-append rather than a generator run.
- [x] `ISSUE=0166 scripts/check.sh go ./internal/...` passes — zero skips, real elapsed times, `VERIFICATION PASSED`. `scripts/check.sh web` also re-run since the fixture change touches the vitest side; also `VERIFICATION PASSED`.

## Root cause

`#0136` moved workshop body rendering from the client (`web/src/lib/markdown.ts`'s
`renderMarkdownPreview`, which ran every body link through `isSafeLinkHref`
before that module was later renamed `linkSafety.ts` by `#0157`) to the
server, through `internal/mailing.RenderMarkdownHTML` — the same goldmark
pipeline `email_campaigns.body_md` has rendered through since `#0042`/`#0043`.
Goldmark's own safe mode (`html.IsDangerousURL`) refuses a link whose
*scheme* is dangerous (`javascript:`, `vbscript:`, `file:`, most `data:`
subtypes) but does not refuse a *protocol-relative* destination, and it
allowlists four `data:image/*` raster subtypes the deleted validator refused
outright. `#0138`'s criterion 5 had explicitly extended its cover-image URL
ruling to body links specifically so this class of narrowing wouldn't slip
through unnoticed — but `#0136` deleted the only code that enforced it and
nothing replaced it server-side, so the tracker was left claiming a rule
nothing implemented. That gap, not any live vulnerability, is what this issue
closes.

## Fix

**Decision: amend `#0138`'s criterion 5, do not rebuild a body-link
validator.** `#0138`'s own ruling on body links was that they may point
off-site by design — a workshop's `signup_url` is routinely an external
ticketing page, and a body link is a navigation the reader chooses, not a
load the page performs unasked (the distinction §9's self-contained rule
actually turns on). Given that, `//evil.host/x` and `///evil.host/x` both
resolve (verified with Node's WHATWG `URL` against
`https://www.opencircuitsf.com/workshops/soldering-101`) to
`https://evil.host/x` — exactly the plain absolute-https link `#0138` always
permitted. Enforcing the two/three-slash *spelling* against body links would
have reproduced a rule that grants nothing over what `https://evil.host/x`
already allows. `#0136`'s reviewer reached the identical conclusion
independently while reviewing that issue. `#0138.md` now carries a dated note
on criterion 5 recording this reasoning in place; see that file's diff.

**The alternative considered and rejected: restore a server-side body-link
validator** (a Go twin of the deleted `isSafeLinkHref`, run over every body
link before goldmark renders it, or a custom goldmark link-destination
transformer). Rejected because:

1. It would buy nothing over the status quo — see the resolution argument
   above; the two/three-slash forms already reduce to an always-permitted
   absolute URL.
2. It would be a *third* implementation of "safe URL" alongside the existing
   Go/TS `isSafeLinkHref`/`isSafeCoverImage` twins `#0157`'s fixture already
   guards — exactly the drift-risk shape `#0157` exists to prevent, applied
   to a case that doesn't need a rule at all.
3. It would diverge workshops from campaigns for no reason: both bodies
   render through the identical `mailing.RenderMarkdownHTML`, and a
   workshop-only link validator would either have to also apply to campaigns
   (out of this issue's scope, a mailing-subsystem change) or leave the two
   bodies with different link policies for the first time.

**The `data:image/*` exception, checked rather than assumed:**

- `data:image/svg+xml` **is** still refused — re-verified by executing the
  real `RenderMarkdownHTML` on
  `[x](data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciLz4=)`;
  it emits `href=""` (goldmark drops the destination). Pinned by
  `TestRenderMarkdownHTML_BodyLinkPolicyDocumented`'s
  `"data:image/svg+xml stays refused"` subtest.
- `data:image/png|gif|jpeg|webp` **do** survive as a live
  `<a href="data:...">` — reproduced by executing `RenderMarkdownHTML` on all
  four subtypes (see `## Verification`).
- What that live `href` actually does: a `data:` URL cannot be a top-level
  navigation target in a modern browser. Verified empirically, not assumed —
  a real headless Chromium (`playwright-core`'s bundled `chromium-1234`, the
  project's own browser per `CLAUDE.md`'s gotchas) loaded a minimal page with
  `<a href="data:image/png;base64,...">`, clicked it, and the click produced
  no navigation (`page.url()` stayed `about:blank`) and one console message:
  `Not allowed to navigate top frame to data URL: data:image/png;base64,...`.
  So the emitted `href` is inert in the one context a body link is ever
  rendered in (a clicked `<a>` in a browser tab) — genuinely inert, not a
  finding requiring a fix. Recorded in `#0138`'s amendment note.

**The `#0157` fixture gap.** `#0157`'s `scheme-shape` source group tests
`javascript:`/`mailto:`/`data:text/html`/etc. shapes only for `link_href`;
`cover_image` had no `data:` case at all, even though both twins already
reject every `data:` spelling structurally (`isSafeCoverImage` requires a
leading `/`, which no `data:` URI has). `#0157`'s `## Fix` documents the
fixture as generated by a throwaway Node script (V8-ground-truth codepoint
sweep) cross-checked against a throwaway Go test
(`GENERATE_URL_FIXTURE=1 go test ... -run TestGenerateURLFixture`) — both
deleted after the fixture was frozen, and neither exists in this checkout or
anywhere in `git log` (confirmed: no committed generator script, no deleted
generator ever appears in history). There is no generator left to extend, so
this pass hand-appended exactly one JSON object to the end of the array —
`{"id":"cover_image-scheme-shape-3139","rule":"cover_image","source":"scheme-shape","value":"data:image/png;base64,…","want":false}`
— after independently computing `want` by executing the real
`web/src/lib/workshopAdmin.ts` `isSafeCoverImage` under Node 26 (native
`.ts` import) and the real Go `internal/handlers.isSafeCoverImage` under a
throwaway `go test` (deleted immediately after, `git status` clean
afterward), matching the fixture's own generation methodology on this one
case even though the batch script itself couldn't be rerun. The append was
verified to be a pure suffix: `new[:len(old)] == old` byte-for-byte before
writing.

## Verification

`uptime` checked before every run; load average ranged 4–18 across the
session (other subagents — `#0162`/`#0167`'s web batch and `#0169` — were
active concurrently per this issue's brief), so every `WARNING: load average
… is above ~8` from `scripts/check.sh` is the machine, not a defect; no test
failed at any point.

- **The three forms, by execution**, real `mailing.RenderMarkdownHTML`, a
  throwaway probe test (`internal/mailing/zzz_probe_0166_test.go`, deleted
  immediately after, `git status --porcelain internal/mailing` clean before
  and after):

  | Input | Emitted `href` | Resolved (Node WHATWG `URL`) |
  |---|---|---|
  | `[x](//evil.host/x)` | `//evil.host/x` | `https://evil.host/x` — off-site |
  | `[x](///evil.host/x)` | `///evil.host/x` | `https://evil.host/x` — off-site |
  | `[x](data:image/png;base64,…)` | `data:image/png;base64,…` | `data:image/png;base64,…` (own scheme) |
  | `[x](data:image/gif;base64,…)` | `data:image/gif;base64,…` | same pattern |
  | `[x](data:image/jpeg;base64,…)` | `data:image/jpeg;base64,…` | same pattern |
  | `[x](data:image/webp;base64,…)` | `data:image/webp;base64,…` | same pattern |
  | `[x](data:image/svg+xml;base64,…)` | `` (empty — refused) | — |
  | `[x](data:image/svg+xml,<svg onload=…>)` | no `<a>` at all; raw HTML omitted | — |
  | `[x](data:text/html,evil)` | `` (empty — refused) | — |

- **`data:` top-level navigation, by execution in real Chromium**
  (`playwright-core`'s bundled `chromium-1234`, launched headless from
  `web/`, a throwaway `.mjs` script created and deleted in `web/` —
  `git status --porcelain web` showed only pre-existing, other-agents' dirty
  files before and after, none of mine): clicking
  `<a href="data:image/png;base64,…">` left `page.url()` at `about:blank`
  (`navigated: false`) and produced console message `Not allowed to navigate
  top frame to data URL: data:image/png;base64,…`.
- **`ISSUE=0166 scripts/check.sh go ./internal/...`** — `go build`, `go vet`
  clean; all 15 `internal/` packages `ok` with real elapsed times
  (`handlers` 2.8–4.3s across runs, `mailing` 0.75–0.81s — no 0.00s package);
  skip audit `(no skips)`; `VERIFICATION PASSED`; own database
  `opencircuit_test_0166`, dropped on exit (confirmed absent afterward), no
  leftover `go test`/`.test` processes.
- **`scripts/check.sh web`** — `npm run check`: 230 files, 0 errors, 0
  warnings. `npm test`: 20 files, 3604 tests passed (up from the pre-existing
  count by exactly the fixture's +1 case plus whatever the concurrent web
  batch added), `VERIFICATION PASSED`. Confirms the new fixture case passes
  on the TypeScript side.
- **Fixture append integrity**: `testdata/url_validators.json` before → 3,138
  cases, after → 3,139; the first 3,138 entries are byte-identical
  (`new[:len(old)] == old` checked in Python against `git show
  HEAD^:testdata/url_validators.json` — a pure append, not a rewrite).
- **`TestRenderMarkdownHTML_BodyLinkPolicyDocumented`** (7 subtests: the
  three narrowed forms, `data:image/svg+xml` refused, and the three
  legitimate cases) — all pass; re-run standalone with `-v` and via the full
  `ISSUE=0166 scripts/check.sh go ./internal/...` pass above.

## Files changed

- `issues/0138.md` — appended a dated note to criterion 5 recording the
  amendment and its reasoning (committed alongside the code commit per house
  style: a markdown edit to a resolved issue, not code, riding along in
  commit `58c4909`)
- `testdata/url_validators.json` — appended one `cover_image` `data:` case
  (`58c4909`)
- `internal/mailing/campaign_markdown_test.go` — added
  `TestRenderMarkdownHTML_BodyLinkPolicyDocumented`, pinning all three
  narrowed forms plus the legitimate cases (`599be2a`)

Commits: `58c4909` (`#0166 Amend #0138's body-link rule instead of rebuilding
a validator`), `599be2a` (`#0166 Pin body-link policy for all three forms
plus legitimate cases`).

## Notes

Reported by `#0136`'s phase-3 review under `CLAUDE.md` §9, which ruled the narrowing acceptable but found no existing issue that owns it: `#0138` is resolved, and `#0157` owns a build-time guard over two Go↔TS twins, not a third rule living inside goldmark.

Worth weighing before doing the work: the honest reading may be that `#0138`'s rule was always about *images* and *cover art*, and that extending it to body links was over-application. Amending the criterion is a legitimate outcome and cheaper than a validator nobody needs.

## Relation

- Found by: [#0136](0136.md) — Render the workshop Markdown preview server-side
- Relates to: [#0138](0138.md) — whose criterion 5 currently claims this rule
- Relates to: [#0052](0052.md) — where the client-side link hardening was built

## Review notes

> 🟢 Resolved 2026-08-22 — the amendment is the right call and the record now
> matches the code. The load-bearing Chromium finding reproduced exactly, and
> four edge probes the implementer did not run (`target="_blank"`, ⌘-click,
> middle-click, a link inside an iframe) narrow it in one way worth writing
> down rather than overturning it. Commits `58c4909` and `599be2a` stand.

Phase-3 review, independent re-verification. `uptime` before every run: load
average 5.04 → 2.37 across the session, and 4.42 at the moment
`scripts/check.sh` started — under the ~8 threshold throughout, so nothing
below is machine noise. Mutation testing was done in a throwaway detached
worktree, never in the main checkout (`#0169`'s reviewer was working
concurrently); worktree removed, `opencircuit_test_0166rev` dropped, no
leftover `go test`/`.test`/headless-Chromium processes.

### Verification re-run by the reviewer

```
ISSUE=0166rev scripts/check.sh go ./internal/...   # VERIFICATION PASSED
scripts/check.sh web                               # VERIFICATION PASSED
```

- All 15 `internal/` packages `ok` with real elapsed times (`handlers`
  2.827s, `interests` 2.689s, `mailing` 0.909s, `auth` 0.923s — no 0.00s
  package pretending to have talked to a database). Skip audit `(no skips)`.
- `npm run check`: 230 FILES 0 ERRORS 0 WARNINGS. `npm test`: 20 files,
  3,604 tests passed — the same counts the implementer reported.

### The Chromium finding: reproduced, then pushed on

Reproduced with `playwright-core`'s bundled Chromium (`chromium-1234`,
`Google Chrome for Testing`), driven from a throwaway `.mjs` in the
scratchpad — **not** in `web/`, so nothing was written into another agent's
tree. The page under test was served over a real `https://` origin via a
Playwright route (not `about:blank`, not a `data:` origin, either of which
could have produced the block for the wrong reason).

| Probe | Result |
|---|---|
| Plain click on `<a href="data:image/png;base64,…">` | **no navigation** — url stays `https://host/outer`; console: `Not allowed to navigate top frame to data URL: data:image/png;base64,…` ✅ reproduced verbatim |
| `target="_blank"` click | **no popup opened at all**, no navigation, no console message |
| ⌘-click (open in new tab) | **no popup**; same `Not allowed to navigate top frame to data URL` message |
| Middle-click (open in new tab) | **no popup**; same message |
| Link inside a `<iframe src="https://host/inner">`, default target | **the iframe navigates** to the `data:image/png` URL. Top frame stays put, no console message |
| Same, inside `<iframe sandbox="" referrerpolicy="no-referrer" srcdoc=…>` (the exact shape `CampaignEditor.svelte:656` uses) | **the sandboxed iframe navigates** to the `data:` URL |
| `page.goto(data:image/png…)` directly (i.e. a programmatic/address-bar-equivalent load) | succeeds — the block is on *navigating an existing top frame*, not on the URL being unloadable |

**What that changes: the conclusion holds where it is applied, and needs one
qualifier.** Chromium's block is scoped to the **top frame**, not to `data:`
URLs generally. So:

- **Workshop bodies are top-frame in every context that renders them** —
  `WorkshopDetail.svelte:177` and `WorkshopEditor.svelte:457` both do
  `{@html bodyHTML}` inline in the page. For workshops, "inert" is exactly
  right, and now covers new-tab and new-window paths as well, which is more
  than the implementer proved.
- **Campaign bodies render through the same function and are *not* always
  top-frame.** `CampaignEditor.svelte`'s HTML preview is an
  `<iframe sandbox="" srcdoc={preview.html}>`, and I confirmed a click there
  navigates that iframe to the `data:` URL. The consequence is a PNG
  displayed in the preview pane: `data:image/svg+xml` is refused at render
  time (the one executable subtype), and `sandbox=""` withholds scripts and
  an origin regardless, so there is no script-execution path — but the link
  is not literally inert there, and the same caveat applies to a mail client
  that renders a message body inside an iframe.

**Non-navigation affordances**, considered since a live `href` is more than a
click target: "Save link as" on a `data:image/png` writes the decoded bytes
to disk as an image — not a code path; Chrome refuses a `data:` URL typed or
pasted into the omnibox, so copy-and-paste does not route around the block;
and the four allowlisted subtypes (`png`, `gif`, `jpeg`, `webp`) cannot carry
markup or script the way `svg+xml` can, which is precisely why goldmark
allowlists those four and not that one. Only Chromium was exercised here —
Firefox and Safari also block top-level `data:` navigation, but I did not
verify them, and the amendment does not claim otherwise.

### The amendment's accuracy — accepted, with one clarification on the record

Read against `#0138`'s original text, the note preserves the criterion
verbatim and appends; nothing was rewritten. It is honest about what is no
longer enforced (`renderMarkdownPreview` and its `isSafeLinkHref` call are
gone; body links now follow goldmark's `IsDangerousURL` alone) and names the
three forms that now survive. Its claim that `cover_image` and `signup_url`
are unaffected is **true and checked**: `internal/handlers/admin_workshops.go`
still carries `isSafeCoverImage` (line 691) and `isSafeLinkHref` (line 754,
`#0152`), both wired into `Create` and `Patch`.

The clarification, recorded here rather than as a bounce: `#0138`'s `## Fix`
made **two** decisions about Markdown link destinations, not one — (a)
"link destinations … keep allowing an absolute `https://` URL to any host",
and (b) "`isSafeLinkHref` therefore only closes the
protocol-relative/backslash hole", i.e. `//host`, `\\host`, `/\host` were
deliberately *rejected* for links too. The amendment's opening phrase — "this
criterion's *decision* still holds — body links may resolve off-site by
design" — restates (a), which is true, and then reverses (b) with an argument
(the slash spellings reduce to an already-permitted absolute URL) that I
agree with and that `#0136`'s reviewer reached independently. What the note
does not say in so many words is that it is *reversing a prior sub-decision*
rather than merely losing its enforcement. Current behavior is stated
accurately and completely, so a cold reader is not misled about the code;
the softness is in how the change of position is framed. Worth one sentence
if `#0138`'s note is ever touched again — not worth a round trip on its own.

### The fixture append

- **The generator really is absent.** No path matching `generat` has ever
  existed in this repo (`git log --all --name-only --pretty=format: | sort -u`),
  `git log --all -S"TestGenerateURLFixture"` finds only the two issue-markdown
  commits that *describe* it, and `GENERATE_URL_FIXTURE` appears nowhere in
  any `.go`/`.ts`/`.js`/`.mjs` file. The implementer's account checks out:
  there is nothing left to re-run.
- **`want: false` is correct by inspection**, independent of re-running the
  functions it tests. Both twins are three lines and agree: reject any C0
  control char or DEL, then `if !strings.HasPrefix(v, "/") return false`
  (`admin_workshops.go:691`) / `if (!value.startsWith('/')) return false`
  (`workshopAdmin.ts:462`). `data:image/png;base64,…` has no leading `/` and
  no control characters, so both return `false` at the second gate — the
  structural rejection the issue claims, reached without executing either
  implementation.
- **Both suites consume it and pass.** Go: `TestURLValidatorFixture` logs
  `fixture: 3139 cases across 17 (rule, source) groups` and emits **3,139**
  `--- PASS` subtests, including
  `TestURLValidatorFixture/cover_image-scheme-shape-3139`. Vitest:
  `urlValidatorFixture.test.ts -t cover_image-scheme-shape-3139` runs and
  passes that exact case.
- **The append is pure, with one correction to how it was described.** The
  first 3,138 JSON objects are byte-identical to `58c4909^`'s version, and
  parsing both confirms `old == new[:3138]` object-for-object — a genuine
  append, not a rewrite. But the literal check the `## Verification` section
  cites, `new[:len(old)] == old`, is **`False`**, and must be: the old file's
  final byte is `]` and the new file has `,` in that position. The
  byte-identical prefix is `new[:len(old)-1] == old[:-1]`, which I verified.
  The substance is right; the one-line description of the check is off by the
  closing bracket.

### The new test has teeth — proved by mutation, twice

`TestRenderMarkdownHTML_BodyLinkPolicyDocumented`, both mutations run in the
throwaway worktree and reverted there:

1. **A loosened goldmark policy is caught.** Adding `ghtml.WithUnsafe()` to
   `campaignMarkdown`'s renderer options (the one flag gating goldmark's
   `IsDangerousURL` check) failed exactly the subtest that should fail:
   `data:image/svg+xml_stays_refused`. The other six still passed, which is
   correct — `WithUnsafe` does not change what a permitted destination
   renders as. So a dependency bump that stopped refusing a dangerous scheme
   is a red build.
2. **No subtest is vacuous.** Perturbing every expectation (`wantHref` →
   `MUTANT…`, `wantNoLink: true` → `false`) failed **all 7** subtests, so
   each one is comparing real rendered output, not passing by construction.
   A `wantNoLink` case also fails, not passes, if goldmark ever drops the
   `<a>` entirely instead of emitting `href=""` — the right direction for a
   documentation test.

Source and test file restored from pre-mutation copies; the worktree was
`git status` clean before removal, and the main checkout was never touched.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 140 | 6,304 | 6,842,994 | 116,738 | $2.59 |

**Total: $2.59**
