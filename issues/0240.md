# 0240 — Two documented constraints are narrower than the code enforces

| | |
|---|---|
| **Status** | open |
| **Platform** | All |
| **Module** | docs / handlers |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-24 |

## Description

Two places where prose describes a tighter rule than anything actually checks.
Both are the same failure: a **convention** written down as though it were an
**enforcement**, so a reader trusts a guarantee that does not exist.

**1. `cover_image` is not restricted to `/assets/`.** `#0138` and `#0153` both
say cover images must be a same-origin path *(`/assets/…`)*. `isSafeCoverImage`
— `internal/handlers/admin_workshops.go:691` and its TypeScript twin — actually
accepts **any** absolute same-origin path: it requires a leading `/`, rejects
`//` (protocol-relative), and rejects control characters. The `/assets/` prefix
is convention only.

That matters because `#0153`'s decision to ship no upload endpoint rests partly
on the field being tightly constrained. It is constrained enough to stop the
attack `#0138` was about — protocol-relative and off-origin URLs — but an admin
can point it at any path on the site, and a reader of either issue would not
know that.

**2. `#0062`'s one-line `## Description` still reads "Nightly `pg_dump` to S3
with a proven restore."** S3 was decided against in `#0229`: what ships is a
local dump plus an `rsync` pull to an offsite Mac mini. The body of `#0062` was
corrected; its summary line was not — and the summary line is what tooling and
issue listings surface. This has now been re-confirmed twice by two separate
reviews, each leaving it alone because `#0062` is resolved.

It is precisely the belief `#0229` exists to stop people holding.

## Acceptance criteria

- [ ] `#0138`'s and `#0153`'s wording matches what `isSafeCoverImage` enforces, or the validator is tightened to match the wording — decide which, and say why
- [ ] If the wording changes, it states what IS enforced (leading `/`, no `//`, no control characters) rather than replacing one vague claim with another
- [ ] `#0062`'s `## Description` describes the design that shipped
- [ ] Editing a resolved issue's description is done deliberately and noted in its work log, so the correction is not mistaken for reopened work

## Notes

Both reported by `#0064`/`#0235`/`#0231`/`#0153`'s phase-3 review under
`CLAUDE.md` §9. The `#0062` line was independently re-confirmed by the earlier
`#0228`/`#0229` review too.

**On editing resolved issues.** Neither reviewer touched `#0062`, correctly —
`CLAUDE.md` §9 forbids changing an issue's *status* by inference, and both erred
toward caution. Correcting a factually wrong description is not a status change,
but it should be an explicit act with a work-log line, which is why it is a
criterion here rather than something to do in passing.

## Relation

- Found by: [#0231](0231.md), [#0153](0153.md) — the review round that surfaced both
- Relates to: [#0138](0138.md) — where the `cover_image` validator and its wording were written
- Relates to: [#0229](0229.md) — the decision that made `#0062`'s summary line wrong
- Relates to: [#0062](0062.md) — the resolved issue carrying that line
