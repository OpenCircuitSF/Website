# 0443 — `ArchiveSource`'s doc still says its store "is expected to be" wired in, and it already is

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | seo |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |

## Description

`internal/seo/archive.go`'s `ArchiveSource` interface doc says `#0123`'s real
store **"is expected to be wired in from the start"**, contrasting it with
`WorkshopSource`'s staged `#0051`/`#0054` rollout.

`#0123` is `resolved`, and `campaignArchiveSEOSource` is wired unconditionally
in `servePostgres`. The sentence describes a future that has already happened.

## Why this is the same defect `#0439` closed, not a smaller one

`#0439`'s subject was that a doc comment can assert **a false state of the
world** rather than merely carry a stale reference — the second is a pointer at
something that moved, and a reader has a cue to check it; the first is a claim
in the document's own voice, and a reader has none.

`is expected to be` is that exact idiom, four files away from the two instances
`#0439` removed. `#0439`'s implementer found it, judged it a **permanent
design-contrast statement** — true regardless of resolution status — and left it
alone. `#0439`'s reviewer **disagreed on review**, and this issue is that
disagreement:

> The tense does not carry the historical reading. It is the same
> forward-looking construction the issue removed four files away, and a reader
> arriving from `sitemap.go`'s "nilable, see `ArchiveSource` doc (`#0123`)" can
> take it as "not yet wired" — the precise harm the issue names.

The design contrast itself is worth keeping. Only the tense is wrong.

## Why it was reported rather than folded in

`#0439`'s reviewer passed the issue anyway, on this lineage's own precedent:
`#0425`'s third pass deliberately left `NewRenderer`'s sibling instance out of
scope and `#0425`'s reviewer *reported* the `WorkshopSource` instance rather
than widening its issue — which is exactly how `#0439` came to exist. Widening
an issue to whatever the sweep turns up is how a two-line fix becomes a
six-pass one.

## Acceptance criteria

1. `is expected to be wired in` → `was wired in`, preserving the design
   contrast intact. The reviewer specified this remedy as **one word**; do not
   redesign the sentence around it.
2. Confirm by reading the call site that `campaignArchiveSEOSource` really is
   passed unconditionally in `servePostgres`, so the new past tense is true.
   `#0439`'s review established the full construction chain — `buildSEOSite` has
   exactly two non-test callers under a total, exclusive `DevMode()` dispatch —
   so this should be a confirmation, not an investigation.
3. **No new mechanism** (`#0437`'s criterion 5, `#0439`'s criterion 5, `#0258`).
   `#0439`'s review examined pinning this class of claim with a test and
   upheld the decision not to: the property that matters is already guarded by
   `TestServePostgres_OneSEOSiteFlowsToEveryConsumer`, and a guard whose subject
   is a doc comment's truth is the bounce-buying shape `CLAUDE.md` §8 warns
   about. Do not revisit it.
4. Re-run the sweep once with a phrase net **not** keyed on an issue-number
   pattern — `#0439`'s reviewer did exactly that and got 13 hits, with
   `archive.go` the only remaining instance. Confirm that still holds and say so.

## Relation

- `#0439` removed the same idiom from `WorkshopSource` and `NewRenderer`, and
  its review found this one.
- `#0425` established the replacement pattern and the report-don't-widen
  precedent.
- `#0123` built the archive and is the issue this sentence names.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/seo/... ./internal/handlers/... ./internal/db/... ./cmd/opencircuit/...`. gofmt rewrites doc comments — run `gofmt -w` and confirm the hash is stable.

## Work log

- 2026-09-05 — Implemented. `internal/seo/archive.go`'s `ArchiveSource` doc
  comment: `#0123's real store is expected to be wired in from the start` →
  `#0123's real store was wired in from the start`, one word changed, design
  contrast with `WorkshopSource`'s staged `#0051`/`#0054` rollout left intact.

  Confirmed the new past tense is true by reading the call site directly:
  `cmd/opencircuit/main.go:255`, inside `servePostgres`, calls
  `buildSEOSite(cfg, workshopSEOSource{store: workshopsStore},
  campaignArchiveSEOSource{store: campaignsStore})` unconditionally — struct
  values, one tab of indentation, no enclosing conditional. Per `#0439`'s
  established chain, `buildSEOSite` has exactly two non-test callers under a
  total, exclusive `Config.DevMode()` dispatch (`servePostgres` when
  `!DevMode()`, `serveDevMode` passing literal `nil, nil` when `DevMode()`),
  so `campaignArchiveSEOSource` reaching the real archive store is not
  conditional on anything this sentence's tense could still be hedging
  against.

  Re-ran the sweep with a phrase net not keyed on `#\d{4}`: `is expected to`,
  `are expected to`, `has not landed`, `not landed yet`, `nil until`,
  `until #\d{4}`, `once #\d{4}`, `when #\d{4}`, `lands`, `will be wired`,
  `yet to`, `placeholder until`, `not yet` — over every `.go` file in
  `internal/seo`. Result: the fixed instance in `archive.go` is gone; the
  remaining hits are the sanctioned `#0020` quotation/paraphrase
  (`workshop.go`, `sitemap_test.go`), three past-tense rename notes
  (`site.go`, `invalidator_satisfier_guard_test.go`), and unrelated
  runtime-state "not yet" usages (`seo.go`'s cache-miss comments,
  `workshop.go`'s `StartsAt` doc, `card_test.go`). `archive.go` was indeed
  the last remaining instance of the idiom in the package — confirmed, not
  merely repeated from the issue.

  No new mechanism added (criterion 3): the decision from `#0439`'s review
  not to pin this class of claim with a test stands untouched — the property
  that matters (`*seo.Site` reaching every consumer) is already guarded by
  `TestServePostgres_OneSEOSiteFlowsToEveryConsumer`, unchanged by this edit.

  `gofmt -w internal/seo/archive.go` left the file's SHA-256 unchanged
  (`bbbf33c9dde6d24ae69dadcb587e7819d82aeb96fa7271fa13cab5623446261d` before
  and after), and `gofmt -l` reports clean.

  `uptime` before the run: `load averages: 7.64 6.00 5.39` — under the ~8
  threshold, so no timing result here is attributable to machine load.

  ```
  ISSUE=0443 scripts/check.sh go ./internal/seo/... ./internal/handlers/... ./internal/db/... ./cmd/opencircuit/...
  ```

  `go build`/`go vet` clean, `gofmt -l` `(clean)`, all four packages `ok`
  with real elapsed time (`internal/seo` 1.512s, `internal/handlers`
  13.883s, `internal/db` 12.756s, `cmd/opencircuit` 1.957s), skip audit
  `(no skips)`, `VERIFICATION PASSED`. `check.sh` dropped
  `opencircuit_test_0443` on exit per its normal behavior; recreated it by
  hand (`scripts/testdb.sh create 0443`) to name and run the specific
  functions this issue touches with `-count=1` (avoiding a stale cached
  result), then dropped it again:

  ```
  go test ./internal/handlers/... -run '^(TestNoCommentCitesUnresolvedPathOrSection|TestNoDocCommentNamesADifferentDeclarationInSameFile)$' -v
  --- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.15s)
  --- PASS: TestNoDocCommentNamesADifferentDeclarationInSameFile (0.06s)

  go test ./internal/db/... -run '^TestPRDSectionIndexParity$' -v -count=1
  --- PASS: TestPRDSectionIndexParity (0.00s)

  go test ./internal/seo/... -run '^TestSitemap_ExcludesUnpublishedWorkshops$' -v -count=1
  --- PASS: TestSitemap_ExcludesUnpublishedWorkshops (0.00s)

  go test ./cmd/opencircuit/... -run '^TestServePostgres_OneSEOSiteFlowsToEveryConsumer$' -v -count=1
  --- PASS: TestServePostgres_OneSEOSiteFlowsToEveryConsumer (0.34s)
  ```

  `docs/deployment.md` and `internal/mailing/campaigns.go` were dirty from
  other concurrent agents at commit time and were left untouched, per §8a.
  No `scripts/db-reset.sh`, no `scripts/testdb.sh template`, no
  `./scripts/dev.sh --built`. Nothing worth filing found.
