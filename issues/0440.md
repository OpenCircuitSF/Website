# 0440 — A ninth account of the static route set omits `/archive`, this one in TypeScript

| | |
|---|---|
| **Status** | open |
| **Platform** | Web |
| **Module** | web / router |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |

## Description

`web/src/lib/router.test.ts`'s `parsePath` test states in its own text that it
covers **every** static PRD §5.1 route plus the auth routes. Its `cases` array
does not include `/archive`.

Found by `#0437`'s implementer while fixing the four Go-side fixtures that
omitted the same route. It did not fix this one: another agent was active in
`web/` at the time, and `#0437`'s scope named `internal/seo/` and
`internal/handlers/routes_test.go`. The restraint was correct; the omission is
still real.

## Why it matters more than one missing table row

This is the **ninth** independent account of the static route set to drift out
of agreement with production since `#0123` added `/archive`:

- `#0425` found and corrected **seven** — doc comments and prose across
  `internal/seo`.
- `#0437` found and corrected **four fixtures** in test bodies, a class `#0425`
  had not looked at.
- This is the ninth, and the first on the **TypeScript** side.

Nine drifts of one fact is not nine mistakes; it is one missing mechanism. Every
account is hand-maintained, and adding a tenth static route will drift out of
whichever of them the next author does not happen to remember.

**The specific hazard here** is that the test's own prose *claims completeness*.
A reader checking whether `/archive` is covered reads "every static PRD §5.1 /
auth route", believes it, and does not look at the array. That is worse than a
list with no claim attached to it.

## Acceptance criteria

1. `/archive` appears in `router.test.ts`'s `parsePath` `cases`, asserting
   whatever the sibling static routes assert.
2. **Decide, and record the decision, whether the completeness claim should be
   enforced rather than written.** The cheap version is a test asserting the
   `cases` set *equals* the router's own static route set, so adding a route to
   the router without touching the test fails. If that is not practical from
   `router.ts`'s exports, say why in the work log and leave the prose claim
   accurate instead.
3. If criterion 2 lands as a derived check, the prose claim can stay; if it does
   not, **weaken the prose to what the array actually covers**. A claim of
   completeness that nothing enforces is what made this hard to see.
4. Consider whether `#0437`'s four Go fixtures deserve the same treatment — see
   its `## Review notes` for that reviewer's verdict on the structural gap, and
   do not duplicate whatever it already decided.

## Relation

- `#0123` added `/archive` as a static route.
- `#0425` corrected seven prose and doc-comment accounts.
- `#0437` corrected four Go test fixtures and reported this one.

Verify with `scripts/check.sh web`. If the fix touches a Go comment or an issue
file, §5's table also requires `internal/handlers` and `internal/db`.
