# 0454 — `issues/Issues.md` is scanned as though it were an issue, because its template contains a status row

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | db / guards |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |

## Description

`issues/Issues.md` carries a literal status row inside the **issue-file-format
template** it documents. `issueStatus` reads that row, concludes the document is
an open issue, and both citation-guard families then scan the template's own
example sections as though they were a real issue's evidence.

Found by `#0446`'s reviewer while re-deriving that issue's corpus sweep: the
in-scope file count includes a document that is not an issue.

## Why it is worth closing, and why it is not urgent

**It is clean today**, and pre-existing — `#0446` did not introduce it. So this
is not a live failure.

It matters for two reasons:

1. **The guards' scope should be describable.** `#0446` settled that the
   allowlist of scanned sections stays, on the grounds that the admitted set is
   small and template-backed while the unadmitted set is 47 one-off heading
   spellings. That argument rests on knowing exactly which files are in scope,
   and a file that is in scope only by accident undermines it.
2. **The template will eventually demonstrate something the guards forbid.**
   `Issues.md` documents conventions, and documenting a convention naturally
   involves showing its shape. `CLAUDE.md` §8 records that writing about a
   citation guard tends to write the exact pattern it forbids — measured at nine
   trips across six issues in one phase. The one document whose job is to *show*
   the format is the likeliest place for that to happen next, and it would fail
   with a confusing message about a file nobody thinks of as an issue.

## Acceptance criteria

1. Decide whether the scan should skip `Issues.md` by name, or whether
   `issueStatus` should require something an actual issue file has and the
   template does not. **Record the reasoning**, not just the choice — a name
   skip is simple but goes stale if the file is ever renamed, while a structural
   test is more robust and easier to get subtly wrong.
2. Whichever you choose, prove it by measurement: the in-scope file count must
   drop by exactly one, and every genuine issue file must remain in scope. State
   the before and after counts. `#0446`'s review established the current figures
   — 454 issue files read, 33 `open`/`in-progress` — so re-derive rather than
   copy them.
3. **No new mechanism** (§8, `#0258`, and `#0446`'s own criterion 5). This is a
   predicate change, not a new guard.
4. Confirm the change does not alter which *sections* are scanned in real issue
   files. `#0446` widened that set to include the work-log section, and that
   widening was proved to bite; this must not narrow it.
5. Add nothing to `Issues.md` itself unless the chosen fix requires it — and if
   it does, say why a change to the document was preferable to a change in the
   guard.

## Relation

- `#0446` widened the citation guards' section allowlist, and its review found
  this.
- `#0352` established the ban the line-citation guard enforces.
- `CLAUDE.md` §8's citation-guard-prose entry explains why the template is the
  likeliest future tripwire.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/db/... ./internal/handlers/...`. Scan bytes with `python3`, never BSD `grep -P`.

## Work log

- 2026-09-05 — **Implemented (phase 2, Sonnet).**

  **Criterion 1 — decision and reasoning.** Chose the structural option, not a
  name skip of `Issues.md`. The distinguishing fact is not the filename but
  where a Status row physically sits: `issueStatus`'s regex already matches
  the template's worked example because that example is written to look
  exactly like a real metadata row — that is the document's whole point. What
  a genuine issue file has, and the template's demonstration does not, is that
  the row is never inside a fenced (```` ``` ````-delimited) code block. Made
  `issueStatus` fence-aware: it now walks lines, toggles fence state on a line
  whose trimmed form starts with a triple-backtick, and only returns a match
  found outside any fence. A name skip would have been one line, but goes
  stale the moment the template is renamed (the issue's own stated risk); the
  fence check instead asks the question that actually separates "a real
  issue" from "prose demonstrating what one looks like," so it keeps working
  under a rename with no further edit, and it fixes all three call sites of
  the shared `issueStatus` function (the two citation-guard families plus the
  closed-row guard) in one place rather than needing a per-loop filename
  filter added to each.

  Confirmed by direct byte inspection that this distinction is real and not
  coincidental for the one case that matters: `issues/Issues.md`'s only
  Status-row-shaped line sits inside its `## Issue file format` example fence,
  and a corpus-wide check of every other `issues/*.md` file found zero files
  whose real Status row is ever inside a fence — so the new predicate changes
  behavior for exactly one document.

  **Criterion 2 — measurement, re-derived.** Wrote a standalone `python3`
  script (bytes, not BSD `grep -P`, per `CLAUDE.md` §8) reimplementing both the
  old (unfenced-agnostic) and new (fence-aware) status extraction and ran both
  over every file the directory scan actually sees today:
  **455** `issues/*.md` files total (454 numbered issue files plus
  `Issues.md` itself). Before this fix, all 455 register a non-empty status
  (the guards' generic in-scope set), and **31** of them read `open` or
  `in-progress` — the set the citation guards actually restrict evidence
  scanning to. After the fix, the non-empty-status set drops to **454** and
  the `open`/`in-progress` set drops to **30**, a drop of exactly one in both
  counts, and in both cases the sole file leaving the set is `issues/Issues.md`
  — every one of the 454 genuine issue files kept the identical status
  classification it had before. (These counts describe today's tree and will
  drift with ordinary work, same caveat the sibling guard's own comment
  states about its corpus figures — re-derive rather than copy forward.)

  **Criterion 3 — no new mechanism.** One function's body changed
  (`issueStatus`), reusing the existing `issueStatusPattern` regexp unmodified
  against each line instead of the whole file. No new guard, no new
  extraction path, no new shared list. Added one test function exercising the
  changed predicate directly plus two regression fixtures (an unfenced real
  row, and a real row followed by an unrelated later fence), which is proof
  of the existing mechanism, not new mechanism itself.

  **Criterion 4 — the work-log widening still bites.** Did not touch
  `issueCitationSectionHeaders` or `extractNamedSections`, so the scanned
  section set for real issue files is unchanged by construction. Re-ran
  `#0446`'s own mutation proof directly rather than assuming it: it still
  plants a Go-file-plus-line-number citation under a `## Work log` heading in
  an isolated `t.TempDir()` fixture, reports it, and reports clean once the
  citation is replaced by naming the enclosing declaration instead — all
  unaffected by this change, since that fixture's Status row was never fenced.

  **Criterion 5 — nothing added to `Issues.md`.** The template's example row is
  exactly what a document explaining the issue-file format is supposed to
  contain; the defect was in how the guard read it, not in what the document
  says. No edit to `issues/Issues.md` was needed or made.

  **The `#0392` trap.** This file's own `## Work log` is in the scanned set the
  guard now correctly still applies to real issues. The template shape at
  stake here is named above by description ("a Status row," "a fenced code
  block") rather than reproduced as a literal example in this prose. The one
  concrete reproduction of the template's shape lives in
  `TestIssueStatusIgnoresFencedExampleRow`'s fixture, as an asserted Go string
  literal rather than a comment — `#0384`'s precedent — where it is pinned by
  an assertion instead of being decorative, and a comment-scoped guard has no
  reason to ever see it.

  **Verification.** `uptime` fluctuated between roughly 3 and 13 across this
  pass's several runs — recorded each time, never reasoned from, per
  `CLAUDE.md` §5's load warning. Two intermediate `ISSUE=0454 scripts/check.sh
  go ./internal/db/... ./internal/handlers/...` runs failed, and both were
  confirmed to be someone else's transient state, not this change: the first
  hit a `go build` failure in `internal/subscribers/store.go` (`undefined:
  growthArrivalConsentBases`) while another agent's 213-line in-flight diff to
  that file was mid-save (`git status` showed it modified throughout; this
  issue's scope explicitly excludes touching it); the second, after that
  build error resolved on its own, hit the repo-wide dangling-test-citation
  guard failing over a citation in that same package's test file to a
  not-yet-defined name, and a re-run moments later hit it again naming a
  *different* not-yet-defined name — both proving it was the other agent's
  own file still being written, not a citation this pass introduced. Neither
  failure touched `internal/db`, and both cleared on their own within two to
  three minutes without any edit from this pass. `gofmt -w` on the one edited
  file left its SHA-256 hash unchanged (already clean; confirmed by comparing
  the digest before and after the rewrite).

  The run that passed: load averages 4.14 / 4.83 / 4.95. `go build`, `go vet`,
  and `gofmt -l` all clean; `internal/db` `ok` in 0.507s and `internal/handlers`
  `ok` in 5.605s, neither `0.00s`; the skip audit reported `(no skips)`; no
  leftover processes. `VERIFICATION PASSED`. Re-run individually with `-v`
  against real declarations (not `-run` prefixes), all `PASS`:
  `TestIssueStatusIgnoresFencedExampleRow`,
  `TestNoOpenIssueVerificationCitesUndefinedTestFunction`,
  `TestIssueSectionTitleNormalizesRealCorpusHeadings`,
  `TestIssueCitationGuardScansEvidenceSectionsNotSubjectMatter`, and
  `TestIssueLineCitationGuardScansWorkLog` (the last being #0446's own
  mutation proof for criterion 4) — also separately confirmed directly against
  the real `../../issues` tree (including this file) in a throwaway scratch
  database dropped immediately after, rather than only the isolated fixtures
  above. Every scratch database created during this pass (`0454`, plus
  `0454dbg` and `0454dbg2` used only to capture full, untruncated `-v` output
  while diagnosing the two unrelated failures above) was dropped before this
  pass finished — the first two by `scripts/check.sh` and `scripts/testdb.sh
  drop` respectively, the last by `scripts/check.sh` on the final passing run.
  Re-run once more immediately before committing, unchanged and with no
  intervening edit: load 5.02 / 4.97 / 5.00, `internal/db` `ok` in 0.692s,
  `internal/handlers` `ok` in 5.445s, `VERIFICATION PASSED` again.
