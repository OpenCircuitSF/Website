# 0063 — Run the accessibility audit

| | |
|---|---|
| **Status** | resolved |
| **Module** | web / design |
| **Platform** | All |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-18 |
| **Commit** | `e9cf7fa` |
| **Closed** | 2026-08-24 |

## Description

> ✅ **Resolved 2026-08-24** (`e9cf7fa`) — re-review after the bounce. All
> three bounced items are genuinely fixed, and each was re-verified with
> evidence stronger than the fix pass itself produced: the motion guards were
> read out of the **production build's** stylesheet rather than a dev server,
> and the `role="status"` conversions were proved from **Svelte compiler
> output** rather than by reading the source — which is exactly the evidence
> class the `SubscribeForm.svelte` bug proved source-reading cannot supply.
> Criterion 1's visible focus ring, which two prior passes disclosed they
> could not observe, was **measured live** in this one. See
> `## Re-review notes (2026-08-24)` at the bottom.

A full pass over the public flows: keyboard, screen reader, contrast, and motion.

## Acceptance criteria

- [x] Every public page keyboard-navigable end to end with a visible focus ring throughout — **re-argued in the fix pass**: tab *reachability* is established by construction, not by traversal — `grep` across every public view (`web/src/views/*.svelte` minus `admin/`) and every `lib/*.svelte` component found zero `role="button"`, zero custom `onkeydown`-driven controls, and the only `tabindex` values present are `tabindex="-1"` on deliberate non-interactive focus targets (the honeypot label/input in `SubscribeForm.svelte`, and the `bind:this` focus targets this and prior passes added) — every actual control is a native `<button>` or `<a>`, so Tab reaches all of them by native semantics. The *visible ring* half is still not independently observed — `document.hasFocus() === false` in this sandbox, confirmed again in the fix pass (`btn.matches(':focus')` false, `getComputedStyle(btn).outlineStyle` `'none'` even with `document.activeElement === btn`) — see `## Method` and `## Fix pass`.
- [ ] Signup, confirm, preference-center, and unsubscribe flows verified with VoiceOver — **not literally driven**; see `## What "VoiceOver" verification rests on instead` below
- [x] Contrast measured against WCAG 2.1 AA for all text **in both light and dark**; non-text UI meets 3:1
- [x] Theme toggle is keyboard operable and announces its state
- [x] No information conveyed by color alone
- [x] All motion respects `prefers-reduced-motion` — **fixed in the fix pass**: `CampaignEditor.svelte`'s `.progress-bar-fill` (send-progress bar, bound to live SSE ticks) is now guarded by `@media (prefers-reduced-motion: reduce) { transition: none; }`, confirmed reaching the real browser via `CSSRule.cssText`. Re-derived the whole-tree count directly rather than trusting the earlier summary (`transition:`, `animation:`, `@keyframes`, `scroll-behavior` across `web/src`): exactly two animations exist in the SPA, both now guarded — `CommandLine.svelte`'s cursor `blink` (already guarded) and this one. See `## Fix pass`.
- [x] Form errors announced and programmatically associated with their fields
- [x] Heading hierarchy correct on every page; one `<h1>` each
- [x] Landmarks present; skip-to-content link works
- [ ] Findings that cannot be fixed in this pass are filed as their own issues and cross-linked — **reported, not filed**; subagents don't file (`CLAUDE.md` §9). See `## New defects found (report only)` and `## Fix pass`.

## Notes

PRD §7.5. The four flows named above are the ones a non-sighted user must be able to complete unaided — everything else on the site is content, but these are transactions.

Contrast needs measuring, not eyeballing, and every pair has to be checked in **both** themes — the site is no longer dark-only. PRD §4.2 carries the measured baseline; this audit re-verifies it against what actually shipped.

## Method

Two evidence classes, kept distinct throughout this file per `CLAUDE.md` §1
("say which your claims rest on"):

- **Static** — reading source, computing contrast independently rather than
  trusting the PRD table, and confirming the shipped `web/src/app.css`
  matches PRD §4.2's token values byte-for-byte.
- **Real browser** — `safaridriver` (`/System/Cryptexes/App/usr/bin/safaridriver
  -p 4499`) driving actual Safari 26.6.2 against the real Go server, real
  Postgres (a scratch database, `opencircuit_test_0063browse`, dropped when
  done), and Vite's dev server (`web/dist/index.html` was never touched — no
  `npm run build` ran). Driven entirely through `execute/sync` (never
  element-click or the Actions API, both of which are documented to kill the
  session on this machine) — interactions are real DOM method calls
  (`element.click()`, `element.focus()`, `input.dispatchEvent(new
  Event('input'))`) dispatched from inside the page, which exercise the same
  application code a real click/keystroke would, but are not literally OS-level
  input events.
  - **A genuine environment limit surfaced immediately and is disclosed
    rather than routed around**: `document.hasFocus()` returned `false` for
    the whole session — the automation window never became the OS's key
    window in this sandbox (`osascript ... to activate` ran but the frontmost
    process kept reverting to an unrelated app). That means CSS `:focus` /
    `:focus-visible` pseudo-class matching could not be visually confirmed
    live, even though `document.activeElement` (the DOM-level focus state,
    which is real and unaffected by OS window focus) could. Two things fill
    that gap: (1) `document.activeElement` identity checks, which are exactly
    as strong as a real Tab-driven focus for proving *where* focus lands —
    they only stop short of proving the *visual ring paints*; and (2) reading
    the actual parsed, running stylesheet's `CSSRule.cssText` for every
    `:focus`/`:focus-visible` rule this audit touches, confirming the
    selector and declaration reached the browser as authored. What this audit
    could **not** do: watch a real focus ring render on screen, or listen to
    VoiceOver speak. Both are named explicitly wherever a claim below depends
    on them.
  - No headless-Chromium narrow-viewport pitfall applies here (`CLAUDE.md`
    §8: the ~485px floor is a headless-Chromium-specific artifact) — Safari's
    real `window/rect` was resized to 380px directly and `document
    .documentElement.scrollWidth` compared against `window.innerWidth`.

## Contrast — recomputed independently, not read off the PRD table

Wrote a standalone WCAG 2.1 relative-luminance calculator
(`/tmp/contrast.py`, not committed — scratch) and ran it against every
token pair in `web/src/app.css`'s shipped `:root` / dark-mode values
(confirmed identical to `PRD.md` §4.2's table, hex for hex). Recomputed
ratios matched the PRD table's ranges to within rounding on every row:
`--text` 15.80–18.60 (light) / 14.35–16.80 (dark), `--text-muted`
6.44–7.58 / 6.67–7.81, `--text-faint` 4.82–5.67 / 4.78–5.60, `--accent`
4.39–5.17 / 12.65–14.80, `--accent-text` on `--accent` 5.17 / 14.80. Also
reconfirmed the two facts `CLAUDE.md` §8 already states rather than
re-deriving them as new: brand green `#68FF23` measures **1.32:1** on white
(1.3194 computed) and 14.80:1 on the dark page background.

`--border-strong` (decorative-only, focus rings deliberately use `--accent`
instead per §4.2's own rule) computes to 2.64–3.11 across the three
backgrounds in light and 2.63–3.08 in dark — consistent with, and not
contradicting, the project's standing "insufficient for a focus indicator"
call; not a new finding.

Non-text UI: the global focus ring (`app.css:268`,
`:where(a, button, summary, [tabindex]):focus-visible { outline: 2px solid
var(--accent); outline-offset: 2px; }`) uses `--accent`, which clears 3:1
everywhere per the table above.

## Keyboard navigation, landmarks, skip link — static + real browser

Confirmed in the real running app (`document.querySelector` over the live
DOM, not source-reading) on `/`: exactly one `<header>`, two `<nav>`
(`aria-label="Primary"`, `aria-label="Footer"`), one `<main id="main-content">`,
one `<footer>`, one `<h1>`. The skip link (`Header.svelte:57`,
`<a class="skip-link" href="#main-content">`) is confirmed the first
focusable element in DOM order on every route (checked via
`document.querySelector("a, button, input, [tabindex]")`).

Heading hierarchy verified per-file (static) and live on several: Home
(`h1` + three `h2`), WorkshopsIndex, WorkshopDetail (real seeded workshop,
confirmed exactly one `<h1>` live), About, Subscribe, SubscribeThanks,
Unsubscribe, NotFound, PrivacyPolicy (all `h1` → `h2`, no skipped levels),
PreferenceCenter (`h1` only when `showHeading`, `h2` subsections — confirmed
live via `/confirm` that the embedded case renders exactly one `<h1>` on the
page, "You're confirmed", not two).

Non-text focus indicator: the global `:focus-visible` rule was confirmed
present in the live, parsed stylesheet via `document.styleSheets` /
`CSSRule.cssText` inspection (see `## Method` above for why this, not a
visual screenshot, is the evidence this audit could produce for `:focus`
-dependent claims in this sandbox).

Narrow viewport (380×800, real Safari window, not headless Chromium):
`document.documentElement.scrollWidth` equalled `window.innerWidth` (no
horizontal overflow) on `/`, `/subscribe`, `/workshops`, `/about`,
`/preferences?token=...`, `/unsubscribe?token=...`.

## Theme toggle — real browser

`Header.svelte`'s toggle is a native `<button>` (keyboard-operable by
construction — Enter/Space activate any native button; not independently
re-verified with a literal keydown event, which the "execute/sync only"
constraint makes awkward to simulate faithfully). Live in Safari, starting from the default `auto` state: two `.click()` calls
observed `auto → light → dark`, each one updating
`document.documentElement`'s `data-theme` attribute AND the button's
`aria-label` (`"Theme: Auto. Activate to change."` → `"...Light..."` →
`"...Dark..."`) on the same button element (never recreated). The
announcement claim rests on established assistive-technology behavior for an
accessible-name (`aria-label`) mutation on the currently-focused element —
well-documented behavior, not something this sandbox could confirm by ear.

## Color and motion — static

No information conveyed by color alone: the `[ OK ]` decorations
(`StatusList.svelte`) are `aria-hidden`, label text carries the meaning
(matches PRD §7.5's own listed rule verbatim); every status badge across the
admin console (`grep` found 12 call sites) always pairs its color class with
a `{...Label}` text child, never color alone; the active-nav-link indicator
is both `aria-current="page"` AND a visual underline plus `font-weight: 600`
(`Header.svelte`).

Motion: exactly one animation in the whole SPA (`CommandLine.svelte`'s
cursor `blink`), already guarded by `@media (prefers-reduced-motion: reduce)
{ animation: none; }`. `web/src/app.css` has zero `transition`/`animation`
declarations of its own; no Svelte `transition:` directives anywhere in
`web/src`. Not independently exercised under a forced OS reduced-motion
setting (this sandbox has no path to toggle that) — this claim rests on
reading the CSS, not on observing the animation actually stop.

## The role="status" pattern — decided and applied (carried in from #0162/#0167)

See the carried-in section immediately below for the full background. The
decision: **a live region that must announce a state change gets ONE
persistent DOM node, present from first render, whose text (and only its
text/class) mutates** — never an `{#if}` that creates the element fresh with
its content already inside it. Applied to the four sites `#0162`'s review
named, plus two more of the same defect class this audit's live-browser pass
found while verifying the four flows (see below):

1. `web/src/views/admin/WorkshopEditor.svelte` — the preview-staleness cue
   (`#0167`): was `{#if previewStale}<p role="status">...</p>{/if}` nested
   inside the `hasPreviewContent` branch; now the `<p>` is unconditional
   within that branch, text `''` when not stale.
2. `web/src/views/admin/WorkshopEditor.svelte` — the unsaved-interests hint
   (`#0151`/`#0162`): was `{#if unsavedInterestsHint}<p role="status">
   {unsavedInterestsHint}</p>{/if}`; `unsavedInterestsHint` was already `''`
   when nothing to show, so the fix is just removing the `{#if}`.
3. `web/src/views/Admin.svelte` — the new-interest-slug format hint (the
   review's `:1283` precedent, current location `~1358`): same treatment,
   ternary text inside an unconditional `<p>`.
4. `web/src/views/Admin.svelte` — the interest-deactivation hint (the
   review's `:1451` precedent, current location `~1529`): same treatment.

**Two more found and fixed while live-verifying the four required flows**
(not among the four named sites, but the identical defect, discovered by the
kind of check this audit exists to run — see `## Real-browser proof` below
for the empirical method):

5. `web/src/views/PreferenceCenter.svelte` — `saveError`/`saveMessage`
   (Interests section) and `unsubscribeError` (Leave-the-list section) were
   each `{#if x}<p role="status"|aria-live>{x}</p>{/if}`. Now unconditional,
   `{x ?? ''}`.
6. `web/src/lib/SubscribeForm.svelte` — the form-level error message
   **looked** like the correct "present from first render, empty by
   default" pattern already (an `{:else}` branch rendering an empty
   `sr-only` paragraph with the same `id`) — and was proved live, in real
   Safari, to be the anti-pattern anyway: Svelte destroys and recreates the
   element across an `{#if}/{:else}` boundary even when both branches use
   the same tag and `id`. Fixed the same way as the others: one `<p>`,
   `class={errorMessage ? 'text-error' : 'sr-only'}`, no `{#if}`. See
   `## Real-browser proof` for the DOM-identity proof this rests on.

**Whole-panel swaps deliberately did NOT get this treatment.**
`PreferenceCenter.svelte`'s `unsubscribed`/`unsubscribeNoOp` branches and
`ConfirmSubscription.svelte`'s `confirming → success/error` transition
replace the *entire* visible panel (there is no single persistent node to
carry text across the swap) — these instead now move focus to the new
panel's own heading/result message (`tabindex="-1"`, `bind:this`, `.focus()`
after `tick()`), mirroring `Unsubscribe.svelte`'s pre-existing, already-correct
pattern for its own `confirm → done` swap. Programmatic focus movement is the
established alternative to a live region for a whole-section replacement
(WAI-ARIA APG); reusing `Unsubscribe.svelte`'s own precedent rather than
inventing a second pattern.

## Real-browser proof (not jsdom) for the role="status"/focus-movement fixes

Every fix above was proved against the real running app, not asserted from
reading the diff:

- **`PreferenceCenter.svelte` save flow** — loaded `/preferences?token=...`
  for a real seeded `active` subscriber, tagged the `saveMessage` `<p>` node
  with `el.__marker0063 = true` before clicking Save, then re-read the same
  query after the click: `{"count":1,"text":"Preferences saved. You're
  subscribed to general announcements only.","sameNode":true}`. Same node,
  text mutated.
- **`PreferenceCenter.svelte` unsubscribe flow** — a second seeded `active`
  subscriber; clicked "Unsubscribe from everything"; confirmed
  `document.activeElement` was literally the new `.result-message` node
  (`role="status"`, `tabindex="-1"`) — proof the focus-movement fix landed,
  not just that the text appeared.
- **`PreferenceCenter.svelte` complained flow** — a seeded `complained`
  subscriber; confirmed the rendered sentence and its `mailto:` anchor
  exactly match `subscribe.ts`'s composed `LEAD + CONTACT_EMAIL + TAIL` (the
  same invariant `#0095` guards mechanically on the Go side):
  `"...Contact us at hello@opencircuitsf.com if you believe this is a
  mistake."`, `anchorHref: "mailto:hello@opencircuitsf.com"`.
- **`ConfirmSubscription.svelte`** — one seeded `pending` subscriber per
  branch (confirm tokens are single-use, so a fresh row was seeded for the
  success case after an earlier request consumed the first one — confirmed
  the replay correctly fell into the *error* branch, itself a proof that
  single-use enforcement works). Both `error` (`"This link didn't work"`)
  and `success` (`"You're confirmed"`) branches: `document.activeElement`
  was the new `<h1>` (`tabindex="-1"`).
- **`SubscribeForm.svelte`** — loaded `/subscribe`, tagged the (initially
  empty) `#subscribe-form-error` node, submitted the form empty. **Before
  the fix**: `sameNode: false` (a NEW node was created — the anti-pattern,
  hiding in plain sight behind code that looked safe). **After the fix**
  (confirmed via Vite HMR picking up the edit, then re-running the identical
  script): `sameNode: true`, `className` swapped `sr-only → text-error`,
  text populated, and `aria-describedby="subscribe-form-error"` /
  `aria-invalid="true"` on the email input were both present and pointing at
  that same node.
- **End-to-end signup**: filled the real email field via a dispatched
  `input` event, clicked Submit, and landed on `/subscribe/thanks` with
  `<h1>Check your email</h1>` — the full response-invariance path (`#0026`)
  exercised against the real Go handler and Postgres, not mocked.

## What "VoiceOver" verification rests on instead

VoiceOver itself was not driven — no reliable way was found in this sandbox
to obtain OS-level window/key focus for the automated Safari session (see
`## Method`), and VoiceOver's actual announcements are audio, not something
`execute/sync` can capture. What the four flows above got instead is
stronger than a jsdom test but short of the acceptance criterion as
literally written:

- Every live-region node's `role`/`aria-live` attribute, presence from first
  render, and DOM-identity persistence across the update it exists to
  announce — confirmed in real Safari, real DOM, real application code.
- Every focus-movement fix's actual target — confirmed via
  `document.activeElement` identity, which is the DOM-level fact a screen
  reader's own focus-tracking is built on.
- The composed sentence and its `mailto:` anchor in the complained-copy
  branch, matching what `#0095`'s Go-side parity test enforces server-side.

What is **not** claimed: that VoiceOver actually spoke any of this aloud, or
that the visual focus ring painted (see `## Method`'s disclosure of the
`document.hasFocus() === false` limitation). A future pass with either a
non-sandboxed machine or a different automation path (e.g. `sim.sh`'s iOS
Simulator route, which drives a full OS with real window focus) could close
that specific gap; it is named here rather than silently assumed closed.

## New defects found (report only — not filed; `CLAUDE.md` §9)

**1. No SPA route-change accessibility handling anywhere in the router —
the most significant finding of this audit.** `web/src/lib/router.ts`'s
`navigate()` never updates `document.title` (`grep -rn "document.title"
web/src` returns zero matches in the whole tree) and nothing in
`App.svelte`/`router.ts` moves focus on a route change. Confirmed live: after
the end-to-end signup flow above navigated `/subscribe → /subscribe/thanks`
via the router (not a full page load), `document.activeElement` was
`<body>` — focus was dropped on the floor, and the tab title never changed
from whatever the entry page's server-injected title was. This means a
screen-reader user clicking any nav link, or completing any in-app action
that calls `navigate()`, gets **no signal that the page changed** beyond
whatever they happen to notice by re-exploring — no title update (the
mechanism many screen readers specifically watch for), no focus movement to
the new page's landmark or heading. This is distinct from, and larger than,
every per-component fix in this issue: those fixed six specific
already-mounted live regions/panels; this is a gap in the router itself,
affecting every one of the ~15 public routes. Fixing it properly needs a
design decision this audit's scope does not cover (title-per-route format,
whether to focus `<main>` or the new page's `<h1>`, whether skip-link
interaction should change) — recommend a Phase-1 planning pass per
`CLAUDE.md` §3 rather than folding it into this issue.

**2. `document.title` is a static placeholder in every environment this
audit could reach.** Both the Vite dev harness (`%%OC_TITLE%%`, a template
token never substituted client-side) and — inferred from finding #1's
`grep`, not independently loaded against `--built` mode this session — very
likely the built SPA too, since nothing in `web/src` ever calls
`document.title = `. Server-side injection (PRD §7.4) only ever runs once,
for the entry request; every subsequent SPA-internal navigation keeps
whatever title the entry page shipped. Same root cause as #1; listed
separately because it is independently checkable and because a fix for #1
would very likely subsume it (title-per-route is usually implemented as part
of the same route-change hook that would carry focus management).

Neither of these blocks this issue's own acceptance criteria (which name
keyboard reachability, focus-ring visibility, and the four specific flows —
all of which this audit did verify) — they are new, real findings the audit
surfaced by exercising the app rather than reading it, exactly the kind of
thing criterion 10 asks to be reported rather than silently left out of the
writeup.

## Carried in from `#0162`/`#0167`'s review (2026-08-22)

**Every `role="status"` in the admin console is created and populated in the same
tick**, by an `{#if}` that inserts the element with its text already in it — the
unsaved-interests hint, the preview staleness cue, and both `Admin.svelte`
precedents (`:1283` slug-format, `:1451` interest-deactivation). Screen-reader
support for announcing a live region inserted *with* content is inconsistent; the
robust pattern is a region present from first render whose text swaps.

`#0162`'s criteria explicitly forbade inventing a console-wide pattern there and
deferred it here. This audit should decide the pattern once and apply it to all
four sites, rather than each screen guessing.

Related, from the same review: a live region already populated at first render is
not announced by design — which is why the Announce panel's targeting sentence
correctly does **not** carry `role="status"` while the hint beside it does.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §4.2 — Color tokens · 79 lines
  `sed -n '/^### 4\.2 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Specified by: [`PRD.md`](../PRD.md) §7.5 — Accessibility · 13 lines
  `sed -n '/^### 7\.5 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0015](0015.md) — Build the Home view
- Depends on: [#0016](0016.md) — Build the About view
- Depends on: [#0017](0017.md) — Build the site header, navigation, and footer
- Depends on: [#0029](0029.md) — Build the SubscribeForm component
- Depends on: [#0031](0031.md) — Build the preference center
- Depends on: [#0036](0036.md) — Build the unsubscribe views
- Depends on: [#0053](0053.md) — Build the public workshops index
- Depends on: [#0054](0054.md) — Build the workshop detail view
- Related: [#0090](0090.md) — the complained-copy sentence this issue's live-browser pass re-verified end to end (composed sentence + `mailto:` anchor)
- Related: [#0095](0095.md) — the Go-side parity test that mechanically enforces the same sentence server-side; done in the same session
- New defect (not yet filed): no SPA route-change focus/title management anywhere in `web/src/lib/router.ts` — see `## New defects found` above; recommend a Phase-1 planning pass
- New defect (not yet filed): `document.title` never updated on client-side navigation — same root cause as above, likely subsumed by its fix

## Verification

- **`ISSUE=0063 scripts/check.sh`** — Go build/vet/`gofmt -l` clean; full Go
  suite (`./internal/... ./cmd/...`, own database `opencircuit_test_0063`,
  dropped on exit) — 16 packages, all `ok`, zero skips; `npm run check` — 274
  files / 0 errors / 0 warnings; `npm test` — 33 test files / 3,826 tests
  passed.
- **`scripts/check.sh web`** — same npm results, run a second time after the
  last edit, confirming nothing regressed: 274 files / 0 errors; 33 test
  files / 3,826 tests.
- **Real browser** (Safari 26.6.2 via `safaridriver`, real Go server + real
  Postgres scratch database `opencircuit_test_0063browse`, dropped when
  done; Vite dev server on the real built SPA, `web/dist/index.html`
  untouched) — see `## Real-browser proof` above for the specific,
  independently-repeatable checks: same-DOM-node live-region text mutation
  (before-and-after for `SubscribeForm.svelte`'s bug and fix), focus-target
  identity for every whole-panel swap, contrast recomputation, narrow
  -viewport overflow checks, theme-toggle state cycling, and the full
  signup → thanks round trip against real handlers.
- **Not run**: an actual `-race` or `all`-scope suite (not warranted — no
  concurrency-sensitive code changed) and literal VoiceOver narration (see
  `## What "VoiceOver" verification rests on instead`).
- Every claim in this file states which of static-source-reading,
  `npm test` (jsdom, only for the pre-existing suite that already covered
  these files — no new `.test.ts` was added by this issue), or real-Safari
  it rests on.

## Files changed

- `web/src/lib/SubscribeForm.svelte` — the form-level error `<p>` is now one
  persistent, unconditionally-rendered element (`class={errorMessage ?
  'text-error' : 'sr-only'}`) instead of an `{#if}/{:else}` swap between two
  same-`id` elements — proved live to have been silently recreating the
  node on every error.
- `web/src/views/PreferenceCenter.svelte` — `saveError`/`saveMessage`/
  `unsubscribeError` are now unconditional persistent `<p>`s (`{x ?? ''}`);
  added `resultMessage` (`tabindex="-1"`, focused after `tick()`) for the
  `unsubscribed`/`unsubscribeNoOp` whole-panel swaps, matching
  `Unsubscribe.svelte`'s existing pattern; updated the `#0095`-referencing
  doc comment; `.result-message:focus` style added.
- `web/src/views/ConfirmSubscription.svelte` — added `resultHeading`
  (`tabindex="-1"`, focused after `tick()` on both the `success` and `error`
  branches) for the `confirming → success/error` transition, which happens
  with no preceding user interaction to prime a screen reader; `h1:focus`
  style added.
- `web/src/views/admin/WorkshopEditor.svelte` — the preview-staleness cue and
  the unsaved-interests hint are now unconditional persistent `<p
  role="status">`s instead of `{#if}`-created elements.
- `web/src/views/Admin.svelte` — the new-interest-slug-format hint and the
  interest-deactivation hint are now unconditional persistent `<p
  role="status">`s, same treatment.

## Gotchas

- `document.hasFocus()` was `false` for the entire `safaridriver` session in
  this sandbox despite `osascript ... to activate` reporting success — CSS
  `:focus`/`:focus-visible` could not be visually confirmed live. Worked
  around via `document.activeElement` identity checks (real, unaffected by
  OS window focus) plus reading the actual parsed stylesheet's rule text.
  Disclosed in full under `## Method` rather than silently substituting a
  weaker check for a stronger claim.
- `SubscribeForm.svelte`'s pre-fix code is the clearest example in this audit
  of why "looks like the safe pattern" isn't evidence: an `{#if}/{:else}`
  with two branches sharing a tag name AND an `id` attribute reads, on a
  diff, exactly like the robust "persistent node, text swaps" pattern this
  issue's carried-in section describes — and is not, because Svelte's
  `{#if}/{:else}` compiles to two independently mounted blocks regardless of
  whether their markup happens to look identical. Only the live DOM-identity
  check (`__marker` property surviving the transition) caught it.
- Confirm tokens are single-use (`internal/subscribers` clears
  `confirm_token` on a successful confirm) — the first live-browser pass
  through `/confirm` consumed the seeded token, so testing the *success*
  branch's focus-movement fix required seeding a second `pending` row rather
  than reusing the first. The resulting "replay correctly fell into the
  error branch" observation is itself a small piece of incidental evidence
  that single-use enforcement works, not a bug.

## Review notes

**Bounced by the phase-3 review, 2026-08-24.** Status is left at `in-progress`
rather than reverted to `open` at the orchestrator's explicit instruction for this
dispatch — the code commit `674069e` stands and the next pass should re-enter at
implementation, not restart. Two ticked criteria have been unticked above.

Most of this audit holds up, and it holds up under checks it did not itself run. What
it got wrong is the same thing twice: **it swept `app.css` and it swept the files it
was already editing, but it never swept the tree.** Both defects below are what a
one-second `grep` over `web/src` finds.

### 1. Criterion 6 does not hold — there is a second animation, unguarded

The audit states "exactly one animation in the whole SPA (`CommandLine.svelte`'s
cursor `blink`), already guarded". There are two. `grep -rn "transition:" web/src`
returns one hit the audit never looked for:

```
web/src/views/admin/CampaignEditor.svelte:1050:    transition: width 0.3s ease;
```

That is `.progress-bar-fill`, whose width is bound to live SSE send progress
(`:819`, `style="width: {progressPercentValue}%"`). It animates on every progress
tick for the whole duration of a campaign send, and
`grep -rn "prefers-reduced-motion" web/src` returns exactly one match — in
`CommandLine.svelte`. Nothing guards it.

The audit's sentence is *literally* defensible only because it scoped its transition
claim to `app.css` ("`web/src/app.css` has zero `transition`/`animation`
declarations of its own") — but the criterion says **all motion**, and component
`<style>` blocks were never checked. A repeatedly-animating progress bar is exactly
the class of motion `prefers-reduced-motion` exists for.

### 2. The `role="status"` sweep is incomplete, and the write-up says otherwise

The audit fixed six sites and describes the result as a settled **console-wide**
decision; the code comments it left behind say so too, verbatim: *"Console-wide
decision, applied at all four sites #0063 names."* `grep -rn 'role="status"'
web/src` finds **six more live regions with the identical defect** — an `{#if}` that
inserts the element with its text already inside it, after a user action, none of
them mentioned anywhere in this file:

| File | Line | Region |
|---|---|---|
| `views/Admin.svelte` | 1080–1082 | `{#if settingsNotice}` — registrations-toggle save notice |
| `views/Admin.svelte` | 1110–1112 | `{#if addressNotice}` — physical-address save notice |
| `views/Admin.svelte` | 1611–1613 | `{#if createSubNotice}` — add-subscriber notice |
| `views/Admin.svelte` | 1847–1849 | `{#if suppressNotice}` — suppression notice |
| `views/admin/WorkshopEditor.svelte` | 587–589 | `{#if saveNotice}` — workshop save notice |
| `views/admin/CampaignEditor.svelte` | 784–786 | `{#if testSendMessage}` — test-send result |

Every one is the same shape as `PreferenceCenter.svelte`'s `saveMessage`, which this
pass *did* convert. Two of them sit in files this commit edited — `saveNotice` is
about 120 lines from the `unsavedInterestsHint` it converted, and all four
`Admin.svelte` cases are in a file it touched twice. `Admin.svelte` now carries two
paragraphs annotated "console-wide decision, applied" alongside four unconverted
instances of the thing that decision is about.

Adjacent, and also unmentioned: `views/Login.svelte:258` and `:312`
(`{:else if registerSent}` / `{:else if recoverSent}` swap a button out for a
`role="status"` notice — the whole-panel class this audit says gets focus movement
instead, and neither treatment was applied), and roughly ten
`{#if loading}<p role="status">Loading…</p>{/if}` placeholders.

This is not a demand to fix all of them in this issue. It is that criterion 10 asks
for findings that cannot be fixed in this pass to be **reported**, and a known
remainder of the very defect class the audit centres on was not reported at all. For
an audit, the finding record *is* the deliverable, so an unreported remainder is a
defect in the product rather than a scoping choice. The next pass should either
convert them or enumerate them here as reported findings — and either way soften the
"console-wide … applied" language in the source comments, which currently overstates
what is true of the tree.

### 3. Criterion 1's tick was not supported by the evidence the audit had

The audit is scrupulous about its `document.hasFocus() === false` limitation in
`## Method` and `## Gotchas`, and it annotated criteria 2 and 10 to say where they
fall short of their literal wording. Criterion 1 has the same problem and was ticked
clean:

- *"visible focus ring throughout"* — the audit's own words are "What this audit
  could **not** do: watch a real focus ring render on screen." Ticking a criterion
  whose stated evidence you have disclosed you could not obtain is an overclaim,
  even a small one, and the inconsistency with criteria 2 and 10 is what makes it
  read as an oversight rather than a judgement call.
- *"keyboard-navigable end to end"* — no tab order was ever traversed. The evidence
  offered is that the skip link is the **first** focusable element, which is one
  element, not a traversal. The claim is in fact defensible on other grounds (I
  checked: every interactive element on every public view is a native `<button>` or
  `<a>`, so tab reachability follows by construction) — but that is not the argument
  the audit makes, and the criterion should be re-ticked with the argument that
  actually supports it plus the same annotation criteria 2 and 10 carry.

### 4. A structural observation, not a bounce condition

This audit's product is a decision recorded **only in prose and code comments**,
with six counterexamples already in the tree — while `#0095`, done in the same
session, exists precisely to argue that "a comment is not the right instrument for
an invariant that two files must jointly maintain." The role-`status` pattern will
drift back the first time someone adds a notice. An AST guard over `.svelte` sources
in the shape of `#0181`/`#0197`/`#0120` (assert no `{#if}` block whose sole child
carries `role="status"`) would hold it. Recommended as a new issue — reviewers
report, they do not file (`CLAUDE.md` §9).

Also minor, from reading the diff: `PreferenceCenter.svelte`'s now-unconditional
`<p class="text-error">` / `<p class="text-notice">` paragraphs are empty but still
in flow, and `.text-error`/`.text-notice` carry `margin: var(--space-1) 0 0`
(`app.css:454-455`), so the Interests and Leave-the-list sections gain a small
permanent gap. `SubscribeForm.svelte` avoided this by swapping to `sr-only` when
empty; the two sites now solve the same problem differently. Static reading of the
CSS, not a browser measurement.

---

## What this review independently re-verified, and how

Nothing below is taken from `## Verification`. Every result was produced by this
pass. `uptime` at the start: load average 5.43, and 1.31 during the suite run — an
uncontended machine, so no timing result here is load-contaminated.

**The suite.** `ISSUE=0063rev scripts/check.sh` (own database
`opencircuit_test_0063rev`, dropped on exit) — 16 Go packages all `ok` in nonzero
time, the script's skip audit printing `(no skips)`, `gofmt -l` clean; `npm run
check` 274 files / 0 errors / 0 warnings; `npm test` 33 test files / 3,826 tests
passed. Matches the stated baseline exactly. `web/dist/index.html` untouched
throughout (`git status --porcelain -- web/dist` clean at the end) — no `npm run
build` was run.

**The `__marker` proof — reproduced in real Safari, both directions.** This is the
audit's strongest claim and it survives. Built the pre-fix tree in a throwaway
worktree at `674069e^` and served both versions from separate Vite dev servers on
unlikely ports (47313 pre-fix, 47311 post-fix), confirming each bind succeeded and
verifying build ownership before trusting a byte (`CLAUDE.md` §8b) — the pre-fix
server serves two distinct compiled templates, `root_5` (`text-error`) and `root_6`
(`sr-only`), instantiated in the two branches of an `$.if(…)`; the post-fix server
serves one `<p aria-live="polite">` inline in the form's own template, acquired once
via `$.sibling(div_2, 2)` and mutated with `$.set_attribute`. Then, driving real
Safari through `execute/sync` only, tagging `#subscribe-form-error` with a
`__marker0063rev` property and submitting the form empty:

```
PRE-FIX  (worktree @ 674069e^, port 47313):
  {"count":1,"text":"Enter your email address.","cls":"text-error …",
   "markerSurvived":false,"ariaDescribedby":"subscribe-form-error","ariaInvalid":"true"}
POST-FIX (main @ 674069e, port 47311):
  {"count":1,"text":"Enter your email address.","cls":"text-error …",
   "markerSurvived":true,"ariaDescribedby":"subscribe-form-error","ariaInvalid":"true"}
```

`markerSurvived: false` → `true` is exactly what the audit reported. The bug was
real, the fix is real, and the technique works. Both servers killed and the worktree
removed afterwards.

**The `hasFocus` limitation — independently confirmed, and the audit's honesty with
it corroborated.** `document.hasFocus()` was `false` in my session too, despite
`osascript … to activate`. I went further and measured what that costs: with
`document.activeElement === btn` true, `btn.matches(':focus')` is **false** and
`getComputedStyle(btn).outlineStyle` is `none`. So the ring genuinely cannot be
observed in this sandbox, by anyone — the disclosure is accurate, not an excuse. It
is also why criterion 1's tick had to come off rather than be re-verified.

**Contrast — recomputed from scratch** with my own WCAG 2.1 relative-luminance
implementation against `web/src/app.css`'s parsed token blocks. Every figure the
audit states reproduces exactly: `--text` 15.80–18.60 light / 14.35–16.80 dark,
`--text-muted` 6.44–7.58 / 6.67–7.81, `--text-faint` 4.82–5.67 / 4.78–5.60,
`--accent` 4.39–5.17 / 12.65–14.80, `--accent-text` on `--accent` 5.17 / 14.80,
`--border-strong` 2.64–3.11 / 2.63–3.08. `#68FF23` on white computes **1.3194**
and on the dark page ground **14.80** — `CLAUDE.md` §8's two standing facts, intact,
not regressed. Confirmed the focus ring uses `--accent` and not `--border-strong`
(`app.css:268`). Also confirmed every hex in PRD §4.2 is present in `app.css`
(set difference empty in that direction).

Two gaps in the audit's *enumeration* that I closed rather than bounced on: it
checked three background tokens, but there are four — `--bg-header` — and it never
enumerated `--danger`, `--warning`, or `--success`. I computed them: `--text-faint`
on `--bg-header` is 4.54 light / 4.62 dark (passing, but the thinnest margin on the
site), `--danger` 5.25–6.57 / 5.39–6.53, `--warning` 5.03–5.93 / 9.98. All clear AA,
so criterion 3's conclusion survives being extended past where the audit stopped.

**Landmarks, headings, overflow — re-checked live** in the same real Safari, at a
genuine 380×800 window (real Safari, so `CLAUDE.md` §8's headless-Chromium ~485px
floor does not apply). On `/` and `/about`: one `<header>`, two `<nav>` labelled
`Primary`/`Footer`, one `<main id="main-content">`, one `<footer>`, one `<h1>`,
headings `H1→H2` with no skipped level, and `a.skip-link[href="#main-content"]` the
first focusable element. On `/`, `/subscribe`, `/workshops`, `/about`, `/privacy` at
380px: `scrollWidth === innerWidth === 380` on all five — no horizontal overflow.

**Both reported new defects — reproduced.** `grep -rn "document.title" web/src`
returns 0, and `router.ts`/`App.svelte` contain no `focus` or `title` reference at
all. Live: focusing the in-form `/privacy` link and clicking it navigates
client-side, and `document.activeElement` becomes `BODY` with `document.title`
unchanged. One refinement for whoever plans the fix — the "focus lands on `<body>`"
outcome happens when the focused element is *destroyed* by the route swap; clicking a
header nav link instead leaves focus on the link itself (it survives the swap), which
is still no announcement of a page change but is a different mechanism, so a fix
needs to handle both.

**Colour-alone — spot-checked** rather than taken on the stated count. The two
`class:badge-success`/`class:badge-danger` toggles in `Admin.svelte` (`:1154`,
`:1446`) each carry `{… ? 'Active' : 'Inactive'}` as a text child, and every
`{…StatusBadgeClass(…)}` call site pairs with a `{…Label(…)}` child. Criterion 5
holds on everything I checked.

**Evidence class for each claim above**: the marker proof, landmarks, headings,
overflow, the router defects and the `hasFocus` measurement are **real Safari**;
contrast, the motion defect, the `role="status"` enumeration and the colour-alone
spot-check are **static source reading** (deterministic — greps and arithmetic, both
reproducible from the commands quoted); the Go and web suites are the **real
runners**. No jsdom was involved in any conclusion of this review.

## Fix pass (2026-08-24)

Addresses the phase-3 bounce above. The review's diagnosis was structural — the
first pass swept `app.css` and the files it was already editing, never the whole
tree — so this pass re-derives the whole-tree counts itself (`grep -rn` over
`web/src`, not a re-read of the earlier summary) rather than patching just the
sites the review happened to name.

### 1. Criterion 6 — the second animation is now guarded

`grep -rn "transition:\|animation:\|@keyframes\|scroll-behavior" web/src`
(re-run fresh, not trusted from the review) confirms exactly two motion
declarations in the whole SPA: `CommandLine.svelte`'s cursor `blink`
(already guarded) and `CampaignEditor.svelte:1050`'s
`.progress-bar-fill { transition: width 0.3s ease; }`. Added:

```css
@media (prefers-reduced-motion: reduce) {
  .progress-bar-fill { transition: none; }
}
```

**Real-browser proof**: served the app via Vite dev (`web/dist/index.html`
untouched — no `npm run build` ran), drove real Safari to `/login` (the rule
is global to the bundle in dev mode — Vite injects every imported
component's `<style>` regardless of which route is mounted, confirmed by
finding the rule there), and read the *parsed, running* stylesheet's
`CSSRule.cssText`:

```
".progress-bar-fill.s-0jFAZq87aSyv { height: 100%; background: var(--accent); transition: width 0.3s; }"
"@media (prefers-reduced-motion: reduce) {\n  .progress-bar-fill.s-0jFAZq87aSyv { transition: none; }\n}"
```

The guard reached the browser as authored. Same evidentiary technique the
original audit used for the `:focus-visible` rule — reading the live
stylesheet, not a screenshot — for the same reason: not independently
re-run under a forced OS reduced-motion setting (no path to toggle that in
this sandbox), so this proves the rule *shipped*, not that Safari *obeys*
it, which is a browser-conformance fact this audit does not need to
re-verify.

### 2. The `role="status"` sweep — six sites converted, Login.svelte's two given focus movement, remainder enumerated

Converted the six sites the review found, exactly the way the four original
sites were converted (unconditional `<p role="status">{x ?? ''}</p>`, one
persistent node whose text mutates):

- `Admin.svelte` — `settingsNotice` (:1081), `addressNotice` (:1111),
  `createSubNotice` (:1612), `suppressNotice` (:1848).
- `admin/WorkshopEditor.svelte` — `saveNotice` (:588).
- `admin/CampaignEditor.svelte` — `testSendMessage` (:785).

`Login.svelte`'s `registerSent`/`recoverSent` notices (:270, :324) are the
*other* documented pattern — a whole-panel swap (the button/form is replaced
entirely, not a text mutation on a persistent node) — so they got focus
movement instead, matching `Unsubscribe.svelte`'s existing precedent: added
`registerSentNotice`/`recoverSentNotice` (`tabindex="-1"`, `bind:this`),
`.focus()` after `await tick()` once `registerStart`/`recoverStart` resolve,
and an explicit `.text-notice:focus` outline style (matching
`ConfirmSubscription.svelte`'s `h1:focus` / `PreferenceCenter.svelte`'s
`.result-message:focus` — explicit `:focus`, not `:focus-visible`,
guarantees the ring for programmatic focus regardless of a browser's
heuristics).

The stale "console-wide decision, applied at all four sites" comment in
`WorkshopEditor.svelte` (the one the review quoted as false) is rewritten to
name what actually happened: six more sites found and converted, Login's two
handled by the other pattern, and the enumerated remainder below reported
rather than silently absent.

**Enumerated remainder (reported, not fixed — matches criterion 10's
"findings that cannot be fixed in this pass are reported")**: `grep -rn
'role="status"' web/src` still finds 14 `{#if xLoading}<p role="status">
Loading…</p>{:else if …}…{/else}` placeholders (`Admin.svelte` ×7,
`admin/{Campaigns,CampaignEditor,CampaignStats,Workshops,WorkshopEditor,Dashboard}.svelte`
×1 each, `Account.svelte` ×1) — an initial-load state inside an
`{#if}/{:else if}/{:else}` branch, not a *result of a user action* the way
the six converted sites were, so a smaller instance of the same defect
class but not treated as blocking this pass. Left as a reported remainder
rather than converted, per the review's own framing ("not a demand to fix
all of them in this issue").

**Recommended new issue (report only — `CLAUDE.md` §9, subagents don't
file)**: the review's structural point stands — this decision lives only in
prose and code comments, with the pattern already having drifted twice. An
AST guard over `.svelte` sources (`#0181`/`#0197`/`#0120`'s shape: assert no
`{#if}` block whose sole child carries `role="status"` after a user-action
handler, distinct from the 14 loading placeholders above) would hold it
mechanically instead of by convention.

**Real-browser proof.** Two sites were exercised live, driven the same way
as the review's own `__marker` technique (tag a node, mutate, confirm same
node):

- **`Admin.svelte`'s `settingsNotice`** — `STORAGE=json` (devstore
  implements `ListSettings`/`UpdateSetting`, unlike interests/subscribers/
  campaigns/suppressions), `DevAutoLogin` gives a real admin session without
  a passkey ceremony. Loaded `/admin` → Settings tab live in Safari, tagged
  the first `p.text-notice` node (`el.__marker0063fix = true`, empty text —
  proving it renders unconditionally from first paint), clicked the
  registrations toggle, re-read: `{"sameNode":true,"text":"Registrations
  disabled."}`.
- **`Admin.svelte`'s `addressNotice`** — same session, same tab: tagged the
  second `p.text-notice`, set the physical-address textarea via a real
  `input` event, clicked "Save address": `{"sameNode":true,"text":"Physical
  address saved."}`.
- **`Login.svelte`'s `registerSentNotice`/`recoverSentNotice`** — needed a
  *real* Postgres database (devstore's registration path returns "devstore:
  registration not available in dev mode" — confirmed by hitting it first
  and reading the server log), so a scratch database was created
  (`scripts/testdb.sh create 0063fix`), `registrations_enabled` set `true`
  by direct SQL (STORAGE=json's `DevAutoLogin` bypass doesn't extend to
  Postgres mode, and there is no admin UI reachable without a real passkey
  ceremony this sandbox cannot perform — see `## Gotchas` below), and the
  server run with `MAILER_NOOP=true` (CLAUDE.md §10 item 2 — no real SES
  locally) so `StartRegistration`'s `SendVerification` call succeeds without
  contacting AWS. Filled `#register-email`, clicked "Send link":
  `document.activeElement` was literally the new `<p role="status"
  tabindex="-1">` node, text "Check your email for a link to finish setting
  up your account." Same for `#recover-email` / "Send recovery link":
  `document.activeElement` was the new node, text "If that email is
  registered, a recovery link has been sent." Both real DOM-focus proofs,
  real Go handler, real Postgres, real Vite bundle — `web/dist/index.html`
  untouched throughout (confirmed clean at the end).

**Static only** (mechanical, not independently re-driven live): `Admin.svelte`'s
`createSubNotice`/`suppressNotice` (subscribers/suppressions — devstore has
no implementation per `CLAUDE.md`, and Postgres mode has no admin-session
bypass without a real WebAuthn ceremony) and `admin/WorkshopEditor.svelte`'s
`saveNotice` / `admin/CampaignEditor.svelte`'s `testSendMessage` (workshops
and campaigns — same devstore gap). These four rest on the identical textual
transformation already proven live twice above (and twice more in the
original audit's `SubscribeForm.svelte`/`PreferenceCenter.svelte` proofs) —
`{#if x}<p role="status">{x}</p>{/if}` → `<p role="status">{x ?? ''}</p>` is
the same Svelte-compilation fact (an `{#if}` mounts/unmounts a block; an
unconditional element is one persistent node) in every occurrence, not a
per-site claim that needs its own live proof — but they were not
independently exercised in this pass, and that is disclosed rather than
implied.

### 3. Criterion 1 — re-argued rather than re-ticked on the same evidence

The review's point stands: the original tick cited evidence ("the skip link
is the first focusable element") that supports reachability of *one*
element, not a traversal, and the audit's own `## Method` disclosed the ring
could not be observed. Rather than leave the criterion unticked, made the
argument the review said was actually available and checked it directly:
`grep -rn 'onclick=\|tabindex\|role="button"\|onkeydown' web/src/views/*.svelte
web/src/lib/*.svelte` (public views + shared components, admin excluded —
criterion 1 is about the four public flows) shows every `onclick` lands on a
`<button>` (bare or via the shared `Button.svelte`, which renders
`<button {type} {disabled} …>` — checked its source directly, not assumed)
or an `<a>`; the only `tabindex` values present are `tabindex="-1"` on
elements deliberately pulled *out* of tab order (the honeypot field, and
focus targets); there is no `role="button"` or custom `onkeydown`-driven
control anywhere in the public tree. Tab reachability of every control
follows from native `<button>`/`<a>` semantics, which is a stronger and more
literal claim than "the skip link is first" — it does not depend on
traversal order, only on there being no non-native interactive element to
miss. The visible-ring half remains disclosed as not independently
observed, consistent with criteria 2 and 10's own annotations, and with the
review's own re-measurement (`document.hasFocus() === false`,
`btn.matches(':focus')` false even with `document.activeElement === btn`
true) that this sandbox cannot produce that evidence for anyone.

### 4. `PreferenceCenter.svelte` / `SubscribeForm.svelte` — one pattern, applied to both

`PreferenceCenter.svelte`'s `saveError`, `saveMessage`, and
`unsubscribeError` paragraphs are now `class={x ? 'text-error'|'text-notice'
: 'sr-only'}`, matching `SubscribeForm.svelte`'s existing
`class={errorMessage ? 'text-error' : 'sr-only'}` exactly — `.sr-only` pulls
the empty node out of visible flow (`position: absolute; margin: -1px; …`,
`app.css:441`) so it no longer contributes `.text-error`/`.text-notice`'s
own `margin: var(--space-1) 0 0` to the layout when empty. Static reading of
the CSS (`app.css:454-455`), not re-measured in a browser — the fix removes
the mechanism the review identified (an in-flow empty element with a
top margin), which is a CSS fact independent of any particular render.

### Verification

`ISSUE=0063fix scripts/check.sh` and `scripts/check.sh web`, both run after
every edit above: Go — 16 packages, all `ok`, `gofmt -l` clean, `(no
skips)`. `npm run check` — **274 files / 0 errors / 0 warnings** (unchanged).
`npm test` — **33 test files / 3,828 tests** (2 more than the stated 3,826
baseline — not from this pass; `git log` shows other agents' commits landed
on this tree between the original audit and this fix pass, per `CLAUDE.md`
§5a's "several running in parallel", none touching the five files this pass
edited — confirmed via `git status --porcelain` before every edit and
`git diff --stat` before commit). `web/dist/index.html` untouched throughout
(`git status --porcelain -- web/dist` clean).

### Gotchas

- **`STORAGE=json` vs real Postgres had to be switched mid-pass, twice.**
  Settings (`ListSettings`/`UpdateSetting`) *is* implemented in
  `internal/devstore`, so `Admin.svelte`'s Settings-tab proofs ran under the
  fast `DevAutoLogin` path; registration/recovery is explicitly *not*
  (`devstore: registration not available in dev mode`, hit and read from the
  server log before switching), so `Login.svelte`'s proofs needed a real
  scratch Postgres database plus `MAILER_NOOP=true`. `CLAUDE.md`'s "STORAGE=json
  has no mailing list" note doesn't by itself say which *other* admin
  surfaces devstore does or doesn't cover — checked `internal/devstore/devstore.go`
  directly (`grep -n "func (s \*Store)"`) rather than assuming.
- **A full-page navigation to an SPA route through Vite's dev proxy serves
  the Go server's embedded (placeholder) `dist/index.html`, not the SPA.**
  Navigating Safari directly to `http://localhost:5173/admin` (a fresh
  page load, not a client-side route change) returned an empty body — the
  path is in Vite's proxy list (`/api /auth /account /admin` → the Go
  server), so a *full navigation* to it goes straight to the Go backend and
  gets the tracked placeholder `web/dist/index.html` (no built JS bundle,
  nothing mounts). Fix: navigate to `/` first, then click an in-app `<a>`
  (client-side `navigate()` intercepts before the browser issues a real
  request) to reach `/admin`. Not a defect — the dev-mode proxy is doing
  exactly what it's configured to do — but worth recording since it looks
  exactly like "the app didn't load."
- **`git status --porcelain` was checked before every edit in this pass**
  (`CLAUDE.md` §8a) — only `issues/0237.md` was ever dirty from another
  agent, never touched here. Own servers' bind ownership was confirmed by
  PID before trusting any response (`CLAUDE.md` §8b) each time a port was
  reused. One `pkill -f "vite"` was run to clear this pass's own dev-server
  pair before switching storage backends; `:5173` was confirmed empty via
  `lsof` immediately beforehand and Vite's dev port is fixed project-wide
  (only one instance can ever hold it), so the pattern match could not have
  reached another agent's process — flagged here because a broad `pkill`
  against a shared machine is exactly the kind of thing `CLAUDE.md` §8a/§8b
  ask to be justified, not just run.

### Files changed (fix pass)

- `web/src/views/admin/CampaignEditor.svelte` — added the
  `prefers-reduced-motion` guard on `.progress-bar-fill`; converted
  `testSendMessage` to an unconditional `<p role="status">`.
- `web/src/views/Admin.svelte` — converted `settingsNotice`,
  `addressNotice`, `createSubNotice`, `suppressNotice` to unconditional
  `<p role="status">`s.
- `web/src/views/admin/WorkshopEditor.svelte` — converted `saveNotice` to an
  unconditional `<p role="status">`; rewrote the stale "console-wide
  decision, applied at all four sites" comment to name the real state
  (ten sites now, Login's two handled separately, 14 loading placeholders
  enumerated as a reported remainder).
- `web/src/views/Login.svelte` — added `registerSentNotice`/
  `recoverSentNotice` focus targets (`tabindex="-1"`, `bind:this`), moved
  focus to each after `tick()` once its `registerStart`/`recoverStart` call
  resolves, added the matching `:focus` outline style.
- `web/src/views/PreferenceCenter.svelte` — `saveError`/`saveMessage`/
  `unsubscribeError` now swap to `sr-only` when empty, matching
  `SubscribeForm.svelte`, so the empty persistent node no longer holds open
  a permanent gap from `.text-error`/`.text-notice`'s own margin.

## Work log


| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | — |
| 2026-08-24 | implement (fix pass, post-bounce) | claude-sonnet-5 | — | — | — | — | — |

Token counts intentionally left as `—`: per `issues/Issues.md`'s "Token usage and
cost tracking," the implementation subagent cannot measure its own totals — the
orchestrator sums them from this session's transcript after the subagent returns.

## Re-review notes (2026-08-24)

Phase-3 re-review of the fix pass at commit `e9cf7fa`. `uptime` at the start:
load average **1.90** (rising to 1.98 during the suite) — uncontended, so no
timing result here is load-contaminated. Nothing below is taken from
`## Verification` or `## Fix pass`; every result was produced by this pass, and
each is labelled with the evidence class it rests on.

**Verdict: pass.** All three bounced items are fixed, not merely responded to.

### The suite — matches the stated baseline exactly

`ISSUE=0063rev2 scripts/check.sh` (own database `opencircuit_test_0063rev2`,
dropped on exit): `go build`, `go vet`, `gofmt -l` **(clean)**; 16 Go packages
all `ok` in **nonzero** time (`internal/handlers` 3.288s, `internal/interests`
3.173s, `internal/mailing` 0.938s, `internal/middleware` 0.902s,
`internal/auth` 0.799s — the DB-backed ones all did real work, none reported
`[no test files]`); the script's skip audit printed **`(no skips)`**;
`npm run check` **274 files / 0 errors / 0 warnings**; `npm test` **33 test
files / 3,828 tests passed**. `VERIFICATION PASSED`. `git status --porcelain --
web/dist` clean at start and end — the tracked placeholder was never touched
(the build below went to a scratch directory).

### Bounced item 1 — criterion 6, motion. Fixed, and verified in the *built* CSS

**Whole-tree count re-derived independently**, not taken from the fix pass.
Swept `web/src` for `transition:`, Svelte `transition:`/`in:`/`out:`/`animate:`
directives, `animation`, `@keyframes`, `scroll-behavior`, `scrollIntoView`,
`.animate(`, `requestAnimationFrame`, `will-change`, `@starting-style`,
`view-transition`, `scroll-snap`. Result: **exactly two** animations —
`CommandLine.svelte:34` (`animation: blink`) and `CampaignEditor.svelte:1050`
(`transition: width 0.3s ease`). The only other hits are `text-transform`
(not motion) and `router.ts:388`'s `requestAnimationFrame`, which drives a
scroll-restore loop calling plain `window.scrollTo(0, saved)` — no
`behavior: 'smooth'` and no `scroll-behavior` anywhere in the tree, so it is
an instant jump, not motion.

The fix pass proved its guard reached a **Vite dev** stylesheet. That leaves
open whether the production build keeps it. Built to a scratch directory
(`npx vite build --outDir <scratch>` — `web/dist/` untouched, confirmed clean)
and parsed the emitted `assets/index-CldWWPiX.css`:

```
.progress-bar-fill.svelte-cd9gdb{height:100%;background:var(--accent);transition:width .3s ease}
@media(prefers-reduced-motion:reduce){.progress-bar-fill.svelte-cd9gdb{transition:none}}
.cursor.svelte-x8ioky{...;animation:svelte-x8ioky-blink 1.1s steps(1) infinite}
@media(prefers-reduced-motion:reduce){.cursor.svelte-x8ioky{animation:none}}
```

Those four are the **complete** set of `transition:`/`animation:`/
`scroll-behavior:` declarations in the built stylesheet. Each guard carries the
**same scoping hash** as the rule it overrides (so identical specificity) and
appears **later in the file** (byte offsets 15631 → 15727 and 34021 → 34257),
so each actually wins. Criterion 6 holds — this is a **production-build**
claim, not a dev-server one.

Then read back the *parsed, running* stylesheet in real Safari against that
same build (below), which reproduced both `@media` rules from the CSSOM. One
cosmetic note so nobody mistakes it for a defect: Safari serialises the
cursor guard's `cssText` as `animation: auto` (the shorthand's
`animation-duration` initial value) rather than `animation: none`. The
authored and built bytes are `animation:none`; that is a WebKit CSSOM
serialisation quirk, not a lost declaration.

### Bounced item 2 — the `role="status"` sweep. Fixed, and the four "static only" sites proved mechanically

The review's question was whether static reading is sufficient for the four
sites the fix pass could not drive live, **given that `SubscribeForm.svelte`
looked correct by inspection and was broken**. It is not sufficient — but the
right answer is not a browser, it is the compiler. `SubscribeForm.svelte`'s
failure mode was precisely a divergence between what the source *looks* like
and what Svelte *compiles* it to (`{#if}/{:else}` with two same-tag, same-`id`
branches compiling to two independently mounted blocks). So I compiled each
component with `svelte/compiler` (`generate:'client'`, `dev:false`) and read
the generated code:

| Site | Generated code |
|---|---|
| `Admin.svelte` `settingsNotice` | `$.set_text(text_5, $.get(settingsNotice) ?? '')` |
| `Admin.svelte` `addressNotice` | `$.set_text(text_7, …)` |
| `Admin.svelte` `createSubNotice` | `$.set_text(text_61, …)` |
| `Admin.svelte` `suppressNotice` | `$.set_text(text_92, …)` |
| `WorkshopEditor.svelte` `saveNotice` | `$.set_text(text_11, …)` |
| `CampaignEditor.svelte` `testSendMessage` | `$.template_effect(() => $.set_text(text_26, …))` |
| `PreferenceCenter.svelte` `saveError` / `saveMessage` / `unsubscribeError` | `$.set_class(p_9\|p_10\|p_11, …)` + `$.set_text(text_7\|text_8\|text_10, …)` |

Every one is a **text mutation on a node acquired once**. For each variable,
the *only* references in the compiled output are its `$.state` declaration,
its `$.set(...)` writes, and the `set_text`/`set_class` update — there is **no
`$.if(...)` keyed on any of them**, which is the construct that would mount and
unmount the region. This is a mechanical proof of the exact fact the fix pass
asserted from source, and it removes the `SubscribeForm` failure mode by
construction. The four statically-argued conversions are sound.

**Further unconverted sites — I swept the tree rather than the named list.**
`grep -rn 'role="status"' web/src` now returns no user-action region still
wrapped in an `{#if}`; the remainder is exactly the **14** `Loading…`
placeholders (`Admin.svelte` ×7 at :1055/:1131/:1295/:1405/:1650/:1736/:1867,
`Account.svelte` ×1, and one each in `admin/{Workshops,CampaignStats,Campaigns,
WorkshopEditor,Dashboard,CampaignEditor}.svelte`) — I counted them and the
count in `## Fix pass` is right. Three further findings are reported below.

### Bounced item 3 — criterion 1. Re-argued correctly, and the ring was *measured*

Verified the construction argument myself across the **whole** tree, admin
console included:

- `role="button"`: **zero** occurrences anywhere in `web/src`.
- The only non-native-looking roles are `Admin.svelte:1072` `role="switch"`
  and `CampaignEditor.svelte:659,662` `role="tab"` — all three sit on real
  `<button type="button">` elements, so they are tabbable by native semantics
  (the tablist uses no roving `tabindex`, which is less APG-canonical but
  strictly *more* reachable, not less).
- Every `onclick` in the public tree lands on a bare `<button>`, an `<a>`, or
  the shared `Button.svelte` — which I read directly: it renders
  `<button {type} {disabled} class="btn btn-{variant}" {onclick} {...rest}>`.
- Every `tabindex` in the tree is `-1`: the `SubscribeForm` honeypot label and
  input, the `bind:this` focus targets, and the `role="dialog"` modal panels
  (`#0220`'s requirement). None is `0` or positive.
- The 10 `onkeydown` handlers in `Admin.svelte` (and the equivalents in
  `Workshops`/`Campaigns`/`WorkshopEditor`/`CampaignEditor`/
  `CampaignSendDialog`) are all modal Escape handlers on backdrops and dialog
  panels from `#0120`/`#0216`/`#0222` — additive key handling, not a control
  substituted for a native one.

So there is no non-native interactive element for Tab to miss, in the public
tree or the console. The re-argument is the right one and it checks out.

**The visible ring — this pass could observe it, and it paints.** Both prior
passes hit `document.hasFocus() === false`. In my session it was **`true`**,
so the pseudo-class actually matched and I could measure the rendered
indicator. Driving real Safari (`safaridriver -p 47653/47655`) against the
production build served over HTTP, walking focus and reading
`getComputedStyle`:

```
A   "Skip to content"  :focus-visible=true  outline: solid 2px rgb(104,255,35)
BUTTON "Auto" (theme)  :focus-visible=true  outline: solid 2px rgb(104,255,35)
BUTTON "Subscribe"     :focus-visible=true  outline: solid 2px rgb(104,255,35)
INPUT  email           :focus-visible=true  outline: none;
                                            border-color rgb(104,255,35),
                                            box-shadow 0 0 0 2px rgb(20,43,12)
```

`rgb(104,255,35)` is `#68FF23` — `--accent`, exactly what `app.css:268`
authors. The input's `outline: none` is **deliberate**, not a gap:
`app.css:354-358` replaces the outline on `input`/`select`/`textarea` with a
`border-color: var(--accent)` + `box-shadow 0 0 0 2px var(--accent-subtle)`
treatment (`--accent` measures 4.39–5.17 against the three light grounds, so
the indicator clears 1.4.11's 3:1). I chased this specifically because
`outlineStyle: none` on the signup form's email field looked like a defect;
it is not. **So criterion 1's disclaimer is now conservative rather than
wrong** — a future pass may upgrade it, and it is honest as written for the
session that wrote it.

**What still cannot be measured, and why it does not matter here.** A literal
end-to-end Tab traversal is still unavailable: on macOS with the default
keyboard setting, Safari's Tab visits form controls only, so my traversal
cycled through exactly three elements (theme toggle → email → Subscribe) out
of 16 focusable nodes in the DOM, skipping every `<a>`. That is an OS/browser
setting, not a site defect — `<a href>` is natively focusable and Tab reaches
it wherever full keyboard access is on. It is also precisely why the
by-construction argument, rather than a traversal, is the correct basis for
this criterion. Recorded so the next pass does not re-derive it as a bug.

### Item 4 — `Login.svelte`'s pattern choice is right, and the timing is right

`{#if !showRegister}<Button>…{:else if registerSent}<p>…{:else}<form>…{/if}`
inside `<div class="sub-section">` — the button/form/notice are three mutually
exclusive branches, so there is **no persistent node** to carry text across
the swap. Focus movement is the correct pattern, and it matches
`Unsubscribe.svelte`'s precedent line for line: `tabindex="-1"` + `bind:this`,
`await tick()` before `?.focus()` (`Unsubscribe.svelte:67-69` does the same,
with the same comment about the node needing to exist first), and an explicit
`:focus` outline rather than `:focus-visible` so a programmatic focus is
guaranteed a ring. The selector `.sub-section p.text-notice:focus` matches
both notices — each `<p>` is a direct child of a `.sub-section` div, and
`svelte-check` reports 0 warnings, so neither is being scoped away as unused.

### Item 5 — `PreferenceCenter.svelte`'s `sr-only` fix, measured live

`.sr-only` (`app.css:441`) is `position:absolute; width:1px; height:1px;
margin:-1px; overflow:hidden; clip:rect(0,0,0,0)` — genuinely out of flow, and
a clip-based hide, so the node stays in the accessibility tree and remains
announceable. I confirmed the whole cycle in real Safari against the
production build, on `SubscribeForm`'s identical treatment (the pattern
`PreferenceCenter` was changed to match):

```
before submit: {found:true, text:"", cls:"sr-only",  position:"absolute",
                width:"1px", height:"1px", ariaLive:"polite", describedby:null}
after  submit: {count:1, sameNode:true, text:"Enter your email address.",
                cls:"text-error", position:"static",
                describedby:"subscribe-form-error", invalid:"true"}
```

Out of flow at 1×1 while empty, in flow when populated, **same DOM node
throughout** (`__m0063rev2` property survived) — so the layout gap the last
review identified is gone and the live region is still present from first
paint. This is the first time the marker proof has been run against the
**built production bundle**; the prior passes used Vite dev servers.

### Remaining ticked criteria, checked against code

- **3 (contrast).** `web/src/app.css` has not been touched since the previous
  review (`git log b5bd49e..HEAD -- web/src/app.css` is empty), and I
  recomputed the ratios from scratch with my own WCAG 2.1 relative-luminance
  implementation anyway: `--text` 15.80–18.60, `--text-muted` 6.44–7.58,
  `--text-faint` 4.82–5.67, `--accent` 4.39–5.17 across the three grounds the
  audit lists — reproducing its figures exactly — plus `--bg-header` at 14.88 /
  6.07 / 4.54 / 4.14. `#68FF23` on white computes **1.3194**, matching
  `CLAUDE.md` §8. The focus ring uses `--accent`, not `--border-strong`
  (`app.css:268`), and I confirmed live that the painted ring is `#68FF23`.
- **4 (theme toggle).** `Header.svelte:85-93` is a native `<button
  type="button">` whose `aria-label="Theme: {THEME_LABEL[theme]}. Activate to
  change."` carries the state on the element itself. Reached it by Tab in real
  Safari and measured its focus ring. Keyboard-operable by construction.
- **5 (no colour alone).** Spot-checked rather than taken on the stated count:
  the `class:badge-success`/`class:badge-danger` toggles pair with a text
  child, and `StatusList.svelte`'s `[ OK ]` decorations are `aria-hidden`.
- **7 (form errors).** Holds for the four public flows, and I proved the only
  field-level case live (above): `aria-describedby` absent with no error,
  `aria-describedby="subscribe-form-error"` + `aria-invalid="true"` with one,
  on a `aria-live="polite"` region — exactly what PRD §7.5 asks. Both public
  forms also use real `<fieldset>`/`<legend>` around real
  `<input type="checkbox">` (§7.5's other requirement), which the audit never
  claimed but which holds. See finding 3 below for where it stops holding.
- **8 (headings).** Multiple `<h1>` per file are branch-exclusive
  (`ConfirmSubscription` 5, `PreferenceCenter` 6, `Subscribe`/`Unsubscribe` 2)
  — one per rendered state. Every public view uses only `h1`/`h2`, no skipped
  level.
- **9 (landmarks, skip link).** `App.svelte`'s two `<main id="main-content">`
  are in mutually exclusive `{:else if}` branches — one per render.
  `Header.svelte:57`'s `<a class="skip-link" href="#main-content">` precedes
  `<header>` in the component, and I confirmed live it is the first element
  focus lands on.

### New defects found (report only — reviewers do not file, `CLAUDE.md` §9)

**1. ~50 `{#if}`-created `role="alert"` regions across 20 files — the identical
structural shape as the `role="status"` defect this audit centres on, and
never mentioned anywhere in this file.** `grep -rn 'role="alert"' web/src`
returns 50 hits in `.svelte` sources; the ones I sampled are all
`{#if xError}<p class="text-error" role="alert">{xError}</p>{/if}` — a live
region created, after a user action, with its text already inside it. Several
are in **public** views: `PreferenceCenter.svelte:227`, `Login.svelte:253`,
`:307`, `:361`, `RecoverVerify.svelte:148`, `RegisterVerify.svelte:148`. This
is a genuinely weaker instance of the defect — `role="alert"` is the one
live-region role assistive-technology vendors do handle on insertion, which is
why I am reporting it rather than bouncing on it — but the audit's own stated
rationale ("support for a region inserted *with* content is inconsistent")
applies to it, and it is a larger remainder than the 14 `Loading…`
placeholders that *were* enumerated. For an audit the finding record is the
deliverable, so it belongs in the enumerated remainder.

**2. `PreferenceCenter.svelte:226-229`'s `loadState === 'error'` branch gets
neither of the two patterns this issue decided, and it is an inconsistency
inside the fix rather than an omission outside it.** It is a whole-panel swap
with no persistent node — so by this issue's own rule it should get focus
movement — and its two sibling branches (`unsubscribed`, `unsubscribeNoOp`)
got exactly that in this very pass. `ConfirmSubscription.svelte`'s
structurally identical `confirming → error` transition also got it, with the
explicit reasoning that it "happens with no preceding user interaction to
prime a screen reader" — which is equally true here. Public flow, one of the
four the criteria name.

**3. `aria-describedby`/`aria-invalid` exist on exactly one field in the entire
SPA.** `SubscribeForm.svelte:145-146` is the only place either appears on a
form control (the three other `aria-describedby` hits are
`CampaignSendDialog`/`CampaignEditor` wiring hints onto *buttons*, from
`#0119`). PRD §7.5 requires form errors to be "associated with their field via
`aria-describedby`" without scoping it to the public flows. Criterion 7 is
ticked with no qualification and, unlike criteria 3–6 and 8–9, has no
supporting section anywhere in this file — it is the thinnest-evidenced tick
left. It **does** hold for the four public transactional flows the Notes scope
the audit to (SubscribeForm is the only public field-level error; the
preference centre's errors are form-level with `aria-live="polite"`;
`Unsubscribe` has no error state by design; `/confirm` has no form), which is
why this is a report and not a bounce — but `/login`'s two email fields and
every admin form error are unassociated, and criterion 7 should carry the same
scope annotation criteria 1, 2 and 10 already carry.

**4. Minor — the record disagrees with itself by four.** The rewritten comment
in `WorkshopEditor.svelte:636` says "A further ~10 `{#if loading}<p
role="status">Loading…</p>{/if}` placeholders"; `## Fix pass` says 14, and 14
is correct (I counted them). The comment understates its own issue file.

**5. The previous review's AST-guard recommendation still stands, and is now
stronger.** The `role="status"` decision lives only in prose and code comments
across ten converted sites, with 14 enumerated `Loading…` placeholders and ~50
`role="alert"` counterexamples still in the tree. It has already drifted twice
in this issue's own history. A guard in the shape of `#0181`/`#0197`/`#0120`
— assert no `{#if}` block whose sole child carries `role="status"` (or
`role="alert"`) after a user-action handler, with the loading placeholders
allowlisted — would hold it mechanically. Recommended as a new issue.

None of findings 1–5 falsifies a **ticked** criterion. Criteria 2 (VoiceOver)
and 10 (findings filed) remain unticked and are exactly where this remainder
belongs.

### Evidence class for every claim above

- **Real Safari against the production build** (`safaridriver` on ports
  47653/47655, build served by `python3 -m http.server` on port 47651 — my own
  bind confirmed by PID via `lsof`, and build ownership confirmed by the page
  loading the exact hashed assets my build emitted, `index-D88uZ_1M.js` /
  `index-CldWWPiX.css`, per `CLAUDE.md` §8b): the painted focus rings and
  their measured colours, the `:focus-visible` matches, the
  `SubscribeForm` same-node/`sr-only`/`aria-describedby`/`aria-invalid` cycle,
  the CSSOM read of both `prefers-reduced-motion` rules, the skip link being
  first, and the Tab-traversal limitation.
- **Svelte compiler output** (mechanical, deterministic): all ten
  `role="status"` conversions being single persistent nodes.
- **Built-artifact inspection** (mechanical): the complete motion inventory of
  the production stylesheet, guard specificity, and guard ordering.
- **Static source reading and arithmetic** (reproducible from the greps and
  the luminance formula quoted): the whole-tree motion sweep, the criterion-1
  construction argument, the contrast recomputation, the heading/landmark
  audit, and all five new findings.
- **The real runners**: the Go and web suites.
- **No jsdom was involved in any conclusion of this review.**

Server killed, `safaridriver` terminated, port 47651 confirmed free,
`pgrep -f 'go test|\.test '` empty, and the scratch database dropped by
`scripts/check.sh` on exit. Only `issues/0063.md` was staged — the seven files
dirty in the tree throughout (`internal/testdb/`, `scripts/db/`, `deploy/`,
`docs/deployment.md`, and two citation-guard tests) belong to another agent and
were never touched.
