# 0391 — Fence pairing is positional, so an odd fence count shifts every later block

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | db / tooling |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-02 |
| **Commit** | — |

## Description

`#0384`'s predicate-2 helper, `issueLineCitationFencedBlockContainsTestMarker`,
collects every ``` ``` ``` offset in a section and pairs them **positionally** —
first with second, third with fourth.

A scanned section containing an **odd** number of fence runs — a stray ``` ``` ```
mentioned in prose, or a fence written inside a nested block — **shifts every
subsequent pair.**

Two consequences, and the second is the serious one:

1. A genuine `go test -v` transcript can be **false-flagged**, because its real
   block never forms a pair.
2. In the shifted case, **a span of ordinary prose is treated as a block** — so a
   stale citation sitting in that prose, next to anything matching the marker
   pattern, is **silently exempted**. That is a fail-open.

**Latent, not live.** `#0384`'s reviewer scanned every scanned section of every
`open`/`in-progress` file for an odd fence count and found **zero**. The guard is
correct on all real content today, and this was outside `#0384`'s criteria.

## The fix the review specified

Recognise fences **line-anchored** — a fence is a line whose first non-space run
is three or more backticks — and treat an **unterminated final fence** as running
to the end of the section, rather than indexing raw byte offsets.

## Acceptance criteria

1. Fence detection is line-anchored, and an unterminated final fence runs to the
   end of the section.
2. **Prove the fail-open is closed**: construct a section with an odd fence count
   where a stale citation sits in prose beside a marker-matching line, confirm
   the current code exempts it, and confirm the fixed code flags it. That
   demonstration is the deliverable — the defect is invisible without it.
3. **Prove the false-flag half too**: a genuine transcript in a section with an
   odd fence count must be exempt after the fix and is flagged before it.
4. `#0384`'s existing proofs must still hold — each of its three mechanisms
   (predicate 1, predicate 2, the section split) is the **sole** thing standing
   between the guard and a real hit in the corpus, proved by disabling each in
   turn against the real issue files rather than fixtures. Re-run all three.
5. The corpus survey must still come back **zero non-exempt** across
   `open`/`in-progress` files, and the guard must still catch a planted citation.
   A fix that quiets the guard is worse than the fail-open.
6. **Do not add a mechanism to defend the mechanism.** §8's `#0258` case is six
   passes of exactly that, and three guard families this phase (`#0330`,
   `#0347`, `#0356`) ended by recording a limit rather than closing it. This is a
   parsing correction, not a new layer — if it starts growing into one, stop and
   report.
7. Note the **known accepted residual** and do not claim to close it: a stale
   citation *can* still be exempted by wrapping it in a fence carrying a
   fabricated `--- PASS:` marker. `#0384`'s reviewer proved that path is open and
   accepted it, because defeating the guard requires forging tool output and
   `#0356` carries the identical blind spot. **The file discloses this in its own
   header comment** — keep that disclosure accurate.
8. Run `./internal/db/...` and `./internal/handlers/...` (`#0381`). **Your own
   issue-file edits are in scope of this very guard while the issue is
   `in-progress`** — re-run after writing them, and do not introduce a
   `<file>.go:<int>` citation (`#0352`). Per §5 name the specific tests that ran.
   `gofmt` stability.

## Relation

- Found by: [#0384](0384.md)'s review, which resolved that issue
- Depends on: [#0384](0384.md) — the guard this corrects
- Relates to: [#0356](0356.md) — the Go-comment guard with the identical
  forged-marker residual
- Relates to: [#0330](0330.md), [#0347](0347.md) — the don't-add-a-layer
  precedent criterion 6 rests on

## Verification

**The fix (criterion 1).** `issueLineCitationFencedBlockContainsTestMarker` in
`internal/db/issue_line_citation_guard_test.go` no longer indexes raw
three-backtick substring offsets. A new `issueLineCitationFenceLinePattern`
regex recognizes a fence line-anchored — a line whose first non-space
characters, after optional leading spaces/tabs, are a run of three or more
backticks — via `regexp`'s multiline `^` anchor. Fence-line matches are
consumed in document order and paired open/close/open/close; a trailing,
unpaired fence-line match (an odd total) is treated as open through the end
of the section (`len(text)`) via a new small helper,
`issueLineCitationLineEnd`, rather than being dropped by the old loop's
`i+1 < len(fenceOffsets)` bound. Line-anchoring is what stops a bare mention
of a fence in the middle of an ordinary sentence from counting as a fence at
all — the raw substring search the old code did found it regardless of
context.

**Criteria 2 and 3 — the demonstrations, both against real production code,
not a description of the bug.** `#0384`'s original implementation was
preserved byte-for-byte (renamed only) as a test-only function,
`preFix0391FencedBlockContainsTestMarker`, purely so both demonstrations
exercise the actual bug rather than a paraphrase of it.

- `TestIssueLineCitationFencePairingClosesFailOpen` (criterion 2, the
  serious half): a fixture with a stray fence-shaped mention in the middle
  of a sentence (never alone on its own line), a stale synthetic citation
  and a decoy marker-shaped line both sitting in the ordinary prose right
  after it, and one real fenced block later in the text — three raw
  backtick-run occurrences total, an odd count. The preserved pre-fix
  function pairs the stray mid-sentence mention with the real block's
  opening fence, so the "block" it forms spans exactly that prose and
  (wrongly) reports the citation as exempt. The fixed function never counts
  the mid-sentence mention as a fence at all, so no block covers that prose
  and the citation is correctly left un-exempt. Both assertions are in the
  one test, so it fails if either half of the before/after stops holding.
- `TestIssueLineCitationFencePairingStopsFalseFlaggingUnterminatedTranscript`
  (criterion 3): a fixture with one complete, marker-free block followed by
  a second, genuine block whose closing fence never arrives before the
  section text ends — three raw occurrences total, again odd, this time
  from a real unterminated block rather than a stray mention. The preserved
  pre-fix function's pairing loop simply stops on the unpaired trailing
  fence, so the transcript's own citation gets no benefit from the
  predicate and is wrongly left un-exempt (a false flag). The fixed
  function treats that trailing fence as open through the end of the
  section, finds the marker inside it, and correctly exempts the citation.

**Criterion 4 — re-run against the real issue files, not fixtures, and one
finding differs from `#0384`'s review.** Working in an isolated
`git archive HEAD` export under the scratchpad (never the shared tree, per
§8a), each of the three mechanisms was disabled in turn and the real-corpus
test (`TestNoOpenIssueEvidenceCitesGoFileByLineNumber`) re-run, both before
and after this issue's fix:

- Predicate 1 (the diagnostic-suffix check) disabled: fails both before and
  after the fix, naming the same real hit — the `suppressions.go` compiler
  diagnostic quoted in `issues/0258.md`'s Fix pass section.
- The evidence/subject-matter section split disabled: fails both before and
  after the fix. Before the fix it named one real hit (the
  `lock_diagnosis_test.go` citation in `issues/0339.md`'s Description); by
  the time the after-fix pass ran, a concurrently-filed `issues/0393.md` had
  landed in the shared tree with a second real hit (a `main.go` citation in
  its own, unscanned Design section), so the after-fix run named both. Both
  are real, current corpus content, not planted.
- Predicate 2 (the fenced-block marker check) disabled: **passes clean, both
  before and after the fix** — this differs from `#0384`'s review, which
  found a real predicate-2-only hit in `issues/0389.md`'s Verification
  transcript. `issues/0389.md` has since moved to `resolved` and is therefore
  now out of either guard's scope (the status filter, not the section
  split), so no file currently in the live `open`/`in-progress` corpus has a
  citation that depends solely on predicate 2. Predicate 2 remains
  necessary — it is exactly what the two demonstrations above exercise, and
  a genuine `go test -v` transcript's own citation still needs it once one
  reaches an in-progress issue file's Verification section again — but it is
  not, right now, the sole thing standing between the guard and a live
  corpus hit. Recorded rather than smoothed over, per §3's re-measure rule.

**Criterion 5 — corpus survey and planted-citation catch, both still hold.**
`TestNoOpenIssueEvidenceCitesGoFileByLineNumber` passes clean at HEAD with
this fix applied (zero non-exempt across `open`/`in-progress` files), and
`TestIssueLineCitationGuardCatchesPlantedStaleCitationAndSparesCorrectingQuotation`
(an isolated `t.TempDir()` fixture, never the shared tree) still reports
exactly one planted, non-exempt citation and still spares the correcting
quotation in the same fixture file.

**Criterion 6 — no new layer.** The change is confined to how
`issueLineCitationFencedBlockContainsTestMarker` recognizes a fence; the
exemption logic itself (`issueLineCitationExcluded`, both predicates, the
section split) is untouched. The one new production regex
(`issueLineCitationFenceLinePattern`) and the one new helper
(`issueLineCitationLineEnd`) are both internal to the parsing correction
itself, not a second mechanism layered on top of it. The temptation that did
come up: adding an injectable function variable so an end-to-end,
guard-level (not just predicate-level) before/after comparison could run
through `scanIssueDirForLineCitations` with the old and new fence logic
swapped in. Declined — that would have added a production-code seam whose
only purpose is testability, exactly the shape criterion 6 warns against;
the preserved historical function plus direct predicate-level assertions
prove the same defect and its fix without adding one.

**Criterion 7 — the residual is unchanged and still disclosed.** The
forged-marker gaming path `#0384`'s review found (wrapping a citation in a
fence carrying a fabricated `--- PASS:`-shaped line still exempts it) is
untouched by this fix — the new fence detection changes what counts as a
fence, not what the marker predicate is willing to forgive inside one. The
function's own doc comment states this explicitly rather than claiming
closure.

**Criterion 8 — verification run.** `uptime` before the run: load averages
1.87 2.25 3.54. `ISSUE=0391 scripts/check.sh go ./internal/db/...
./internal/handlers/...` — `go build`, `go vet`, `gofmt -l` all clean;
`internal/db` and `internal/handlers` both `ok`, zero skips,
`VERIFICATION PASSED`. Individually confirmed by name via `go test
./internal/db/... -run 'IssueLineCitation|NoOpenIssueEvidence' -v`:
`TestNoOpenIssueEvidenceCitesGoFileByLineNumber`,
`TestIssueLineCitationGuardScanIsImplausibleBelowFloor`,
`TestIssueLineCitationGuardCatchesPlantedStaleCitationAndSparesCorrectingQuotation`,
`TestIssueLineCitationGuardExcludesResolvedFilesByDefault`,
`TestIssueLineCitationExcludedPredicatesAreEachIndependentlySufficient` (all
six subtests), `TestIssueLineCitationPatternMatchesRealGoVetDiagnosticShape`,
`TestIssueLineCitationFencePairingClosesFailOpen`, and
`TestIssueLineCitationFencePairingStopsFalseFlaggingUnterminatedTranscript`
— all pass. `gofmt -l` silent on the edited file and its digest is unchanged
by `gofmt -w`, confirmed by `shasum -a 256` before and after. Scanned the
edited file byte-for-byte with `python3` (never BSD `grep -P`, per §8) for
doubled `''`, doubled backtick inside a `//` comment, and any
`<file>.go:<int>` token inside a `//` comment specifically — all zero; the
`.go:` shapes that do appear are inside Go string-literal test fixtures and
the `regexp.MustCompile` source, never in a doc comment. This section and
the rest of this issue's own edits were re-scanned by
`TestNoOpenIssueEvidenceCitesGoFileByLineNumber` itself after being written,
since `issues/*.md` sits inside `internal/db`'s own scan root and this issue
is `in-progress` — still passes. Own scratch database
(`opencircuit_test_0391`) will be dropped at the end of this pass; no
leftover `go test`/`.test` processes. All mutation work for criterion 4 was
done in a `git archive HEAD` export under the scratchpad, never in the
shared tree.

## Work log

- 2026-09-02 — Filed by the orchestrator from `#0384`'s review (§9).
- 2026-09-03 — implement (claude-sonnet-5, phase 2 subagent): rewrote
  `issueLineCitationFencedBlockContainsTestMarker`'s fence detection to be
  line-anchored (a fence is a line whose first non-space run is three or
  more backticks) and to treat an unterminated final fence as open through
  the end of the section, closing both the fail-open and the false-flag
  named in this issue's Description. Preserved `#0384`'s original
  implementation byte-for-byte as a test-only function so both required
  demonstrations exercise the real bug and its real fix rather than a
  description of either. Re-ran all three of `#0384`'s sole-mechanism
  proofs against the live `open`/`in-progress` corpus in an isolated
  `git archive` export (never the shared tree): predicate 1 and the section
  split still each protect a live real hit; predicate 2 does not right now,
  because its one prior real-corpus example (`issues/0389.md`) resolved
  since `#0384`'s review and left the guard's scope — recorded as a factual
  finding, not papered over. Confirmed the corpus survey still comes back
  zero non-exempt and the planted-citation discrimination test still holds.
  Considered and declined an injectable-function seam for an end-to-end
  before/after comparison, as a live instance of criterion 6's warning.
  Verified with `ISSUE=0391 scripts/check.sh go ./internal/db/...
  ./internal/handlers/...`, `VERIFICATION PASSED`, zero skips; confirmed
  `gofmt` stability by digest. Left status `in-progress` for review (§9 —
  only the reviewer sets `resolved`). Did not touch `internal/outbox` or
  `issues/0390.md` (a concurrent agent's scope). Usage/cost not recorded
  here — no transcript token accounting available to this subagent.
