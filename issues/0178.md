# 0178 — Three Go-authored strings cite our PRD and `CLAUDE.md` at admins, and `demotionExplanation` has no test

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | All |
| **Module** | mailing / handlers / web |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-23 |

## Description

**1. `#0172`'s defect, one layer down — in Go.** `#0172` removed tracker and `CLAUDE.md` citations from admin-facing copy, and both its sweep and its review read **`web/` only**. But `lib/preflight.ts` deliberately owns no code→message table: `parseUnmet` passes the **server's** `message` through untouched. That makes Go strings admin-facing copy, and three of them cite our internal documents:

- `internal/mailing/preflight.go:171` — "…**PRD §6.6** requires a monitored reply address…", rendered in the pre-send checks panel
- `internal/handlers/admin_subscribers.go:553` — "…and **CLAUDE.md §9** refuses to let unsubscribe touch a complained row…"
- `internal/handlers/admin_subscribers.go:728` — the same shape

An admin has read neither document. **CAN-SPAM §7704 in the same family is fine** — a statute is something they can look up; our PRD is not. The `EMAIL_REPLY_TO` / `EMAIL_LIST_DOMAIN` env names are defensible too, being deploy-time codes with no in-SPA affordance.

**2. `demotionExplanation` has no test.** It is not imported by any `*.test.ts`, despite having a real branch (`scheduled_at` present versus the `'an earlier time'` fallback) and being **the only explanation an admin gets for a campaign that was demoted back to draft**.

`#0175`'s review flagged this as the genuine test gap, while explicitly arguing the *opposite* for the physical-address hint: that one should **not** be pinned verbatim, because the refusal is already enforced in three places in Go, and `#0174` exists precisely because a phrase-match test masqueraded as a behavior guard.

## Acceptance criteria

- [x] The three Go strings say what happened in the admin's terms, with the citation moved to a code comment beside the string
- [x] The `complained` messages keep their force — `CLAUDE.md` §9 says a complained subscriber never auto-resubscribes and only an admin clears that state; the copy must not imply the refusal is incidental or bypassable
- [x] **Sweep the Go side the way `web/` was swept**: find every string that reaches an admin through an API `message`/`error` field and read it. `#0172` swept `web/`; nobody has swept the strings Go authors
- [x] `demotionExplanation` gets a semantic test covering both branches — asserting what the sentence *claims*, not one phrasing of it
- [x] Decide whether any other `web/src/lib/` copy function reaching admins is untested, and say what you found
- [x] `ISSUE=0178 scripts/check.sh go ./internal/...` and `scripts/check.sh web` both pass

## Root cause

Same shape as `#0172`, one layer down. `lib/preflight.ts`'s `parseUnmet`
deliberately owns no code→message table — its doc comment says so — and
passes the server's `message` field through untouched to the pre-send
checks panel. `admin_subscribers.go`'s `Suppress` and `manualAddMessage`
similarly write their `message`/`Message` response fields straight from Go
string literals with no client-side translation layer. That makes any Go
string assigned to those fields admin-facing copy, and three of them said
so by citing `PRD §6.6` or `CLAUDE.md §9` directly — documents describing
*our* reasoning to ourselves, not something an admin has read or can look
up (unlike CAN-SPAM §7704, cited in the neighboring physical-address
message, which they can).

## Fix

Reworded each string to state the fact itself rather than cite where the
fact comes from, and moved the citation to a `//` comment immediately above
the string (mirroring `#0172`'s HTML-comment convention in `.svelte`):

- `internal/mailing/preflight.go:171` — "Reply-To address is not configured
  (EMAIL_REPLY_TO). PRD §6.6 requires a monitored reply address, not
  noreply@." → "Reply-To address is not configured (EMAIL_REPLY_TO). Set it
  to an address someone reads, not noreply@." (`EMAIL_REPLY_TO` kept — it's
  deploy-time configuration with no in-SPA affordance, the same judgment
  `#0175`'s review made).
- `internal/handlers/admin_subscribers.go:553` (`Suppress`'s no-op-on-
  `complained` branch) — "This subscriber has complained, and CLAUDE.md §9
  refuses to let unsubscribe touch a complained row — no status change." →
  "This subscriber has complained. By design, unsubscribing a complained
  row makes no status change — that is enforced, not a glitch." "By design"
  / "enforced, not a glitch" carries the same force the citation used to:
  the refusal is deliberate and absolute, not incidental.
- `internal/handlers/admin_subscribers.go:728` (`manualAddMessage`,
  `StatusComplained` branch) — "This address has previously complained and
  CLAUDE.md §9 refuses to auto-resubscribe it. Nothing was sent." → "This
  address has previously complained. By design, adding it here does not
  resubscribe it. Nothing was sent." Same force, same absence of a
  bypassable-sounding reading.

**Go-side sweep** (criterion 3): grepped every `message`/`Message`
literal and every non-generic `writeError(...)` string across
`internal/handlers`, `internal/mailing`, `internal/subscribers`, and
`internal/audit` (the packages that can write an API `message`/`error`
field), and separately grepped the whole tree for `"..."` literals citing
`#0\d\d\d`, `CLAUDE.md`, or `PRD §`. Findings:

- No other rendered string cites the tracker, `CLAUDE.md`, or `PRD` —
  `#0175`'s review had already found these were the only three.
- Every other `message`/`error` literal (e.g. `admin_workshops.go`'s
  `capacityErrorMessage`, `admin_suppressions.go`'s removal message,
  `auth.go`'s `recoverGenericMessage`, `admin_subscribers.go`'s
  `ClearComplaint`/generic `Suppress` messages, the dozens of plain
  `writeError` validation strings) is self-contained, field-name- or
  format-specific, and cites nothing internal. Judged fine.
- No Go test pins any of the three old strings verbatim — `preflight_test.go`
  asserts the `PreflightCodeReplyToMissing` *code*, never the message text;
  no test in `internal/handlers` references the old `admin_subscribers.go`
  wording. So rewriting them broke nothing, and (echoing `#0175`'s own
  finding) nothing would have caught a silent wording change here either.

**`demotionExplanation` test** (criterion 4): added a `describe
('demotionExplanation', …)` block to `web/src/lib/campaigns.test.ts` with
five semantic assertions rather than a pinned phrase — matching the
`cancelCopy` tests' existing style in the same file:
1. when `scheduled_at` is present, the message contains the exact
   `formatDateTime(scheduled_at)` output (computed via the real helper, not
   hardcoded);
2. when `scheduled_at` is absent, the message contains no 4-digit,
   date-shaped token — the fallback branch, asserted by what's *absent*
   rather than pinning `'an earlier time'`;
3. the message says sending was refused and the campaign is in draft;
4. the message does not name an internal component ("worker") as the agent
   of the refusal — the property `#0175`'s rewrite was for;
5. the message points at the checks panel as the actionable next step.

**Other untested `web/src/lib/` copy functions reaching admins**
(criterion 5): checked every exported string-returning function in the
lib modules that render to the admin console (`admin.ts`,
`campaignProgress.ts`, `campaignStats.ts`, `audience.ts`, `sendConfirm.ts`,
`workshopAdmin.ts`, `preflight.ts`, `campaigns.ts`) against what their
`*.test.ts` actually imports and exercises. All are imported and called
with real assertions **except one**: `sendConfirm.ts`'s `confirmHint`
(the typed-count send dialog's mismatch hint, `CampaignSendDialog.svelte`)
is not imported by `sendConfirm.test.ts` at all. It has three real
branches (empty input → `null`, matching input → `null`, mismatched input →
a hint string naming the expected count) and is genuinely untested. Not
fixed here — out of this issue's scope, which named `demotionExplanation`
specifically — reported per `CLAUDE.md` §9 (subagents report, they don't
file).

## Verification

- `uptime` before both runs: load average ~1.6–2.1, well under the ~8
  threshold — no timing readings to discount.
- `ISSUE=0178 scripts/check.sh go ./internal/...`: build and vet clean, all
  15 packages `ok` (`audit`, `auth`, `config`, `db`, `devstore`, `events`,
  `handlers`, `interests`, `mailing`, `middleware`, `seo`, `sesnotify`,
  `subscribers`, `testdb`, `workshops`), zero skips, `VERIFICATION PASSED`.
  Scratch database `opencircuit_test_0178` dropped automatically on exit.
- `scripts/check.sh web`: `npm run check` → 230 files / 0 errors / 0
  warnings. `npm test` → 20 files, **3,620 passed** (baseline 3,615 + the 5
  new `demotionExplanation` tests), `VERIFICATION PASSED`.
- No leftover `go test`/`.test` processes after either run (`pgrep -f`
  came back empty).

## Files changed

- `internal/mailing/preflight.go` — reworded the reply-to-missing message,
  moved `PRD §6.6` to a comment.
- `internal/handlers/admin_subscribers.go` — reworded the `Suppress`
  no-op message and `manualAddMessage`'s `complained` branch, moved
  `CLAUDE.md §9` to comments above each.
- `web/src/lib/campaigns.test.ts` — added the `demotionExplanation`
  semantic test block (5 cases) and its `formatDateTime` import.

Code commit: `a38a070` — "#0178 Rewrite Go-authored admin copy that cited
PRD/CLAUDE.md, add demotionExplanation test".

## Notes

Reported by `#0175`'s phase-3 review under `CLAUDE.md` §9, found on a third sweep axis — strings authored in Go that render verbatim in the console. Two prior passes read `web/` and could not have seen these.

That is now the fifth time this project has found something by reading rather than searching, and the second where the *axis* mattered more than the effort: `#0172`'s sweep was thorough and correct within `web/`, and simply could not reach across the language boundary.

## Relation

- Found by: [#0175](0175.md) — Admin copy naming "the send worker"
- Relates to: [#0172](0172.md) — the same defect, swept only in `web/`
- Relates to: [#0045](0045.md) — the preflight gate one of these strings explains

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 114 | 7,850 | 4,970,181 | 96,862 | $2.00 |

**Total: $2.00**
