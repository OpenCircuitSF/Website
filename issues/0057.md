# 0057 — Configure SES inbound receipt rules

| | |
|---|---|
| **Status** | in-progress |
| **Module** | inbound / deploy |
| **Platform** | All |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-18 |

## Description

The AWS-side plumbing that routes mail sent to `unsubscribe@lists.opencircuitsf.com` into S3 and notifies the service.

## Plan

Planning pass, 2026-09-03. **Nothing in AWS or DNS was created, modified or
deleted.** Every fact below was read live — from Route 53, from IMDS and
`/etc/opencircuit/config.env` on `i-0e3bd89e87d1c2364`, from read-only AWS API
calls, and from the AWS endpoint tables — rather than taken from `PRD.md` or
from `docs/email-setup.md`, both of which are stale here (see
"What the docs get wrong", below).

### Ground truth, measured

| Fact | Value | How it was read |
|---|---|---|
| AWS account | `378152330719` | `aws sts get-caller-identity` |
| Production region | **`us-east-1`** | IMDS `placement/region`; `AWS_REGION=us-east-1` in `config.env`; the live MAIL FROM MX names `feedback-smtp.us-east-1.amazonses.com` |
| Route 53 zone for the domain | `Z0825067RV8QY5UIKS96` (`opencircuitsf.com.`) | `aws route53 list-hosted-zones` |
| Apex MX | `opencircuitsf.com. 3600 IN MX 1 SMTP.GOOGLE.COM.` | `aws route53 list-resource-record-sets` |
| `lists.opencircuitsf.com` | **no record set of its own.** It resolves only through the zone's wildcard `*.opencircuitsf.com. 300 IN CNAME ec2.smallsharptools.com` | same |
| Sending identity | `mailing.opencircuitsf.com` — SPF TXT plus three `*._domainkey…dkim.amazonses.com` CNAMEs | same |
| Custom MAIL FROM | `bounce.mailing.opencircuitsf.com MX 10 feedback-smtp.us-east-1.amazonses.com` | same |
| `s3://opencircuitsf-inbound` | **does not exist**, and the name is unclaimed globally | `get-bucket-location` → `NoSuchBucket`; `head-bucket` → 404, against controls returning 403 for an existing bucket and 404 for a random name |
| Instance role | `opencircuit-instance`, attached to `i-0e3bd89e87d1c2364` | IMDS `iam/security-credentials/` |
| Existing SES events topic | `arn:aws:sns:us-east-1:378152330719:opencircuit-ses-events` | `config.env` |
| Existing config set | `opencircuit-transactional` | `config.env` |
| Production flags | `SES_SANDBOX=false`, `SEND_WORKER_ENABLED=true` | `config.env` |
| `EMAIL_LIST_DOMAIN` | `lists.opencircuitsf.com` — **already set in production** | `config.env` |
| `SES_INBOUND_BUCKET` | already exists in `internal/config` as an **optional** variable; **absent** from production `config.env` | `internal/config/config.go` |

### The region answer: `us-east-1`, and no migration

`PRD.md` §10.3 selects `us-west-2` and tells you to verify the inbound-region
list before committing. `docs/email-setup.md` repeats `us-west-2` in five
places. **Production is `us-east-1` and has been since deploy day.**

The AWS *Email Receiving endpoints* table lists **`us-east-1` →
`inbound-smtp.us-east-1.amazonaws.com`** as the first row. So the region the
project actually sits in supports SES inbound, the existing sending identity's
region is usable for receiving, and the whole project stays in one region with
no change at all. **Do not migrate to `us-west-2`.** §10.3's recommendation was
written against an assumed region that was never the one built.

An earlier attempt to establish this by resolving `inbound-smtp.<region>.amazonaws.com`
per region is **not** a valid test — all sixteen regions probed returned an A
record, including ones absent from the receiving table. The endpoint table is
the oracle; DNS is not.

### Does this depend on `#0415`?

**No.** SES's sandbox constrains *outbound* only — recipient must be a verified
identity, 200 messages/day, 1/second. AWS's email-receiving prerequisites list
exactly three items (domain verification, MX record, resource permissions) and
production access is not among them. Inbound receipt rules work identically
whether or not `ProductionAccessEnabled` is true, so `#0057` can proceed while
`#0415` is still unresolved.

The one honest connection is indirect and not blocking: path 3 only receives
traffic once campaigns carrying the `mailto:` form of `List-Unsubscribe` are
actually sent, and that does depend on sending working.

### What is already in place, versus what could not be read

**Verified as existing** (read-only, this pass): the Route 53 zone and every
record in it; the `mailing.opencircuitsf.com` DKIM and SPF records; the
`bounce.mailing.opencircuitsf.com` custom MAIL FROM MX in `us-east-1`; the
`opencircuit-instance` role attached to the instance; the production
`AWS_REGION`, `SES_CONFIGURATION_SET`, `SES_EVENTS_TOPIC_ARN`,
`EMAIL_LIST_DOMAIN`, `SES_SANDBOX` and `SEND_WORKER_ENABLED` values.

**Verified as NOT existing**: the `opencircuitsf-inbound` S3 bucket.

**Could not be read, and why.** Both credentials reachable from here are denied
every SES, SNS and IAM read action:

- the workstation and the box both default to the IAM user
  `certbot-dns-updater`, denied `ses:GetAccount`, `ses:ListEmailIdentities`,
  `ses:ListReceiptRuleSets`, `ses:DescribeActiveReceiptRuleSet`,
  `sns:ListTopics`, `iam:ListRoles`, `s3:ListAllMyBuckets`;
- the instance role `opencircuit-instance`, queried through IMDS with the
  static credentials suppressed, is denied **the same set** — it evidently
  carries send permissions only.

So the following remain **unverified** and must be checked by the user before
anything is created: whether the SES identity and configuration set exist as
`CLAUDE.md` §10 item 2 claims; whether the `opencircuit-ses-events` topic and
its auto-confirmed subscription exist; whether any receipt rule set already
exists in `us-east-1`; and what `opencircuit-instance`'s policy actually grants.
No credential was created or hunted to get further.

### The blast radius nobody has flagged: the active rule set is account-wide

**A region has exactly one active receipt rule set, for the whole account.**
Activating `opencircuit-inbound` in `us-east-1` therefore *replaces* whatever
is active there now. This account also hosts ShortLinks and other services.

Since `ses:DescribeActiveReceiptRuleSet` is denied to both available
identities, **this is the first thing to check and it is a hard gate.** If a
rule set is already active, the correct move is to add a rule to *that* set,
not to create and activate a new one.

### Two corrections to this issue's own acceptance criteria

**1. One action, not two.** The criteria call for "Action 1 writes to S3" and
"Action 2 publishes to SNS". Implemented literally that is wrong for `#0058`.
A standalone SNS receipt-rule action publishes the *message content* (capped at
150 KB) and carries no S3 object key. The **S3 action's own optional `TopicArn`
field** is the one that fires after the object is written and whose
notification names the object key that `#0058` needs to fetch by. So the rule
takes a **single `S3Action` with `TopicArn` set**, which satisfies both
criteria with one action and is the only shape that makes `#0058` implementable.

**2. The domain must be verified as an SES identity first.** No criterion
mentions this. AWS's setup prerequisites are explicit: "Verify your domain with
Amazon SES by publishing DNS records to prove that you own it", *then* publish
the MX. `lists.opencircuitsf.com` is not currently an SES identity, so a
second, separate DNS record is required beyond the MX. Verification for
receiving needs only the `_amazonses` TXT token; DKIM is not required on a
receive-only domain.

### The DNS changes — two new record sets, both additive

Zone `Z0825067RV8QY5UIKS96`. **Both are `CREATE`s at names that hold no record
set today. The apex `opencircuitsf.com. MX 1 SMTP.GOOGLE.COM.` is not read,
not written, and not in either change batch.**

| # | Name | Type | Current value | New value |
|---|---|---|---|---|
| D1 | `_amazonses.lists.opencircuitsf.com` | TXT | *(none; wildcard supplies a CNAME)* | the verification token SES returns from `CreateEmailIdentity` |
| D2 | `lists.opencircuitsf.com` | MX | *(none; wildcard supplies `CNAME ec2.smallsharptools.com`)* | `10 inbound-smtp.us-east-1.amazonaws.com`, TTL 300 |

**The one non-obvious consequence.** Per RFC 4592 a wildcard does not apply at
a name that owns any record. Creating D2 therefore stops
`lists.opencircuitsf.com` resolving to `ec2.smallsharptools.com` /
`44.222.209.183` for *every* type, not just MX — the name will answer NODATA
for A and CNAME.

That was checked rather than assumed, and it is safe: `EMAIL_LIST_DOMAIN` is
consumed in exactly one place, `mailing.CampaignHeaders`, and only to build the
`mailto:unsubscribe@<listDomain>?subject=…` form of `List-Unsubscribe`. Nothing
constructs an HTTPS URL on it. The `List-Id` header uses the deliberately
different singular `list.opencircuitsf.com`, which RFC 2919 makes an opaque
identifier that never needs to resolve. So nothing in the service or the SPA
depends on `lists.opencircuitsf.com` resolving to the web server.

TTL 300 on both is deliberate — it matches the zone's other short-lived records
and keeps the rollback window to five minutes.

### The AWS objects

All in **`us-east-1`**, account `378152330719`.

| # | Object | Name / ARN | Notes |
|---|---|---|---|
| A1 | SES domain identity | `lists.opencircuitsf.com` | receive-only; no DKIM, no MAIL FROM, never used to send |
| A2 | S3 bucket | `opencircuitsf-inbound` | must be in a receiving region; `us-east-1` qualifies, so no `IamRoleArn` on the action is needed |
| A3 | S3 Block Public Access | all four flags on `opencircuitsf-inbound` | the bucket holds inbound mail — it must never be public |
| A4 | S3 bucket policy | on `opencircuitsf-inbound` | allows `ses.amazonaws.com` `s3:PutObject` on `…/unsubscribe/*` only, conditioned on `AWS:SourceAccount` **and** `AWS:SourceArn` of the exact receipt rule |
| A5 | S3 lifecycle rule | `expire-inbound-30d`, prefix `unsubscribe/` | `Expiration: 30 days` — the issue's own criterion |
| A6 | SNS topic | `arn:aws:sns:us-east-1:378152330719:opencircuit-inbound-mail` | separate from the existing `opencircuit-ses-events`; do not reuse it, `#0037`'s handler dispatches on `TopicArn` |
| A7 | SNS topic policy | on A6 | allows `ses.amazonaws.com` `SNS:Publish`, same two conditions |
| A8 | SES receipt rule set | `opencircuit-inbound` | **or a rule added to the already-active set** — see the gate above |
| A9 | SES receipt rule | `unsubscribe`, in A8 | `Recipients: ["unsubscribe@lists.opencircuitsf.com"]`, `Enabled: true`, `ScanEnabled: true`, `TlsPolicy: Optional`; one `S3Action` → bucket A2, `ObjectKeyPrefix: "unsubscribe/"`, `TopicArn`: A6 |
| A10 | Activate A8 | `SetActiveReceiptRuleSet` | **the account-wide step** |
| A11 | IAM policy on `opencircuit-instance` | inline `opencircuit-inbound-s3` | `s3:GetObject`, `s3:DeleteObject` on `arn:aws:s3:::opencircuitsf-inbound/unsubscribe/*` — the prefix, not the bucket |
| A12 | SNS HTTPS subscription | A6 → `https://www.opencircuitsf.com/api/ses/inbound` | **deferred — see below** |

**`TlsPolicy: Optional`, not `Require`.** `Require` bounces senders that do not
offer STARTTLS. On an unsubscribe address, refusing a request because the
sender's MTA is old is the wrong failure, and RFC 8058 treats unsubscribe
handling as something that must not error.

**`Recipients` names the one mailbox, not the domain.** Scoping the rule to
`unsubscribe@lists.opencircuitsf.com` means anything else sent to the subdomain
gets SES's default 550 rather than being silently captured into S3.

**Why A12 is deferred.** `POST /api/ses/inbound` does not exist —
`#0058` builds it, and only `POST /api/ses/notifications` (from `#0037`, now
resolved) is wired today. An SNS HTTPS subscription that cannot be confirmed
by the endpoint stays `PendingConfirmation` and delivers nothing. Creating the
topic now and the subscription in `#0058` costs nothing: mail still lands in
S3 in the meantime, so nothing is lost, it simply waits. `SES_INBOUND_BUCKET`
belongs in `config.env` at the same time, since adding it requires a service
restart it does not otherwise need.

### Ordering — the MX goes last

This is the whole safety argument. Every AWS object is inert until the MX
exists, so the sequence builds the destination first and only then routes mail
to it. Each step is independently reversible.

1. **Gate.** `aws ses describe-active-receipt-rule-set --region us-east-1`. If a
   rule set is already active, stop and add a rule to it instead of A8/A10.
2. A1 — create the identity, capture the verification token.
3. D1 — publish the TXT; wait for the identity to report verified.
4. A2, A3, A5, A4 — bucket, then lock it down, then lifecycle, then policy.
5. A6, A7 — topic and its policy.
6. A8, A9 — rule set and rule.
7. A10 — activate.
8. **D2 — the MX.** Mail begins flowing only here.
9. Verify end to end: send a real message to
   `unsubscribe@lists.opencircuitsf.com` and confirm the object appears under
   `s3://opencircuitsf-inbound/unsubscribe/`.
10. A11 — the instance-role grant. Last, because nothing reads the bucket until
    `#0058`.

### Rollback

The failure mode is lost mail, so every step has an explicit undo and the
riskiest step is both last and the fastest to reverse.

| Undo | Effect |
|---|---|
| **Delete record set D2** (`lists.opencircuitsf.com MX`) | Inbound routing stops immediately; within the 300s TTL the wildcard resumes answering for that name. **This alone fully reverts the routing change.** |
| `SetActiveReceiptRuleSet` with no name, or back to the previously active set | SES stops applying the rule |
| `DeleteReceiptRule` / `DeleteReceiptRuleSet` | removes A9/A8 |
| Delete the SNS topic A6 | removes A6/A7 |
| Empty and delete the bucket A2 | removes A2–A5 |
| Delete record set D1, delete identity A1 | removes the verification |
| Delete the inline policy A11 | removes the instance-role grant |

**Confirming apex mail still flows** — the check that matters, because a
mistake here costs real human mail:

- `dig +noall +answer MX opencircuitsf.com` must still read
  `1 smtp.google.com.` and nothing else. Run it before and after and diff.
- `dig +short MX lists.opencircuitsf.com` should read
  `10 inbound-smtp.us-east-1.amazonaws.com` — and it must be the *only*
  MX at that name.
- `dig +short MX bounce.mailing.opencircuitsf.com` must still read
  `10 feedback-smtp.us-east-1.amazonses.com`, unchanged.
- Send a message from an outside address to a real Workspace mailbox on the
  apex and confirm delivery.

Nothing in this plan touches the apex, `bounce.mailing.`, `www`, the wildcard,
or the `/.well-known/` Apache exception.

### Split: user versus agent

**The user does everything that creates, activates or bills** — every row in
the AWS table and both DNS records. All of it is outward-facing, and A10 is
account-wide.

**An agent can do**, with no AWS access:

- write the step-by-step runbook into `docs/email-setup.md` (the last
  acceptance criterion), including correcting that file's five `us-west-2`
  references to `us-east-1`;
- prepare the two policy documents and the `ReceiptRule` JSON as files for the
  user to apply verbatim;
- run the `dig` before/after checks and confirm the apex MX is unchanged;
- once the user reports the bucket exists, confirm from the box that the
  service's credentials resolve to `opencircuit-instance` (they do —
  `opencircuit.service` runs as user `opencircuit` with `ProtectHome=true` and
  no `~/.aws`, so the SDK reaches IMDS and never sees the `certbot-dns-updater`
  static keys sitting in another user's home);
- add `SES_INBOUND_BUCKET` to the documented configuration — though the
  production `config.env` edit and restart belong with `#0058`.

### The escape hatch stays open

The issue's own note offers it and it remains the right call if any of the
above turns fiddly: point the `mailto:` at a monitored mailbox and process
unsubscribes by hand. Paths 1 and 2 are the mandatory ones; path 3 is optional
in v1, and launch volume makes manual handling honest. In particular, if the
gate at step 1 finds another service already owns the active rule set in
`us-east-1`, take the escape hatch rather than reshuffling another project's
mail handling.

### What the docs get wrong

`#0414` already records that `docs/email-setup.md` and `docs/aws-iam-setup.md`
document a `From:` address production does not use. **A second, independent
staleness in the same file surfaced here**: `docs/email-setup.md` states the
region as `us-west-2` in five places — the region heading, the custom MAIL FROM
MX value, the production-access example, the SNS topic ARN, and two
`--region us-west-2` command examples. Production is `us-east-1`, and the live
MAIL FROM MX proves it. `PRD.md` §10.2's DNS table is wrong the same way, and
additionally names the MAIL FROM subdomain `mail.opencircuitsf.com` where
production uses `bounce.mailing.opencircuitsf.com`. This is not `#0414` — that
issue is about the `From:` local part — and it is reported for filing rather
than filed here, per `CLAUDE.md` §9.

## Obstacles

| # | Obstacle | Avoidable? | What clears it |
|---|---|---|---|
| 1 | **No AWS identity available to this project can read SES, SNS or IAM.** Both `certbot-dns-updater` and `opencircuit-instance` are denied every relevant read. Nothing in §10 item 2 could be confirmed. | No | The user runs the reads from an admin identity, or grants a scoped read-only policy (`ses:Get*`, `ses:List*`, `ses:Describe*`, `sns:ListTopics`, `sns:GetTopicAttributes`) to an identity an agent can use. **Do not hunt or create credentials.** |
| 2 | **The active receipt rule set is account-wide in the region** and its current state is unreadable (obstacle 1). Activating a new set could displace another service's. | No — it is a hard gate | `aws ses describe-active-receipt-rule-set --region us-east-1` from an admin identity, **before** anything is created. Empty → proceed. Non-empty → add a rule to the existing set instead. |
| 3 | **DNS changes require Route 53 write access**, which `certbot-dns-updater` may hold for `_acme-challenge` only. | No | The user applies D1 and D2. |
| 4 | **The receiving domain must be an SES identity first** — an extra DNS record and a wait no acceptance criterion mentions. | No | Sequenced as steps 2–3; verification is usually minutes but the MX (step 8) must not precede it. |
| 5 | **`POST /api/ses/inbound` does not exist**, so an SNS HTTPS subscription cannot confirm. | Yes | Defer A12 to `#0058`. Mail accumulates in S3 meanwhile; nothing is lost. |
| 6 | **The `us-west-2` figure appears in `PRD.md` §10.2/§10.3, `docs/email-setup.md` (five places) and this issue's own Notes.** An implementer following any of them builds in the wrong region. | Yes | This plan supersedes them. The `docs/email-setup.md` fix is in scope via the last acceptance criterion; the `PRD.md` fix is not, and is reported for filing. |
| 7 | **Editing `issues/0057.md` puts it in `internal/db`'s scan root** (`CLAUDE.md` §5). | Yes | `ISSUE=0057 scripts/check.sh go ./internal/db/...`. No `Test…` name and no bare-line-number file citation is written into this file, which is what the two issue-citation guards there enforce on an `open` issue. |
| 8 | **A concurrent agent owns `internal/seo`, `cmd/`, `web/` and `issues/0273.md` (`#0273`), and another session owns `scripts/testdb.sh` (`#0331`–`#0333`).** | Yes | This pass touches only `issues/0057.md`. Do not run `scripts/db-reset.sh` or `scripts/testdb.sh template`. Stage narrowly and check the index before committing (`CLAUDE.md` §8a). |

## Acceptance criteria

Status as of the 2026-09-11 implementation pass — see `## Verification`
below for exactly what each line rests on. Nothing below is checked unless
it was independently proven this pass; several items that are merely
*prepared* (a ready-to-apply file exists) are still unchecked, because
preparing a file is not the same as the AWS object existing.

- [ ] MX record for `lists.opencircuitsf.com` → `10 inbound-smtp.<region>.amazonaws.com` — **not done.** `deploy/aws/D2-route53-change-batch-mx.json` is ready; applying it is the user's step, deliberately last (see the runbook)
- [ ] **The apex `opencircuitsf.com` MX is untouched** — true today (`dig` confirms `1 smtp.google.com.`, unchanged), but this is a standing invariant nothing in this pass was in a position to violate, not an accomplishment to check off; re-verify with the runbook's own `dig` checks after every AWS/DNS step the user takes
- [ ] SES receipt rule set `opencircuit-inbound` with a rule matching the unsubscribe recipient — **not done.** `deploy/aws/A9-ses-receipt-rule.json` is ready; the account-wide gate (`describe-active-receipt-rule-set`) must run first
- [ ] Action 1 writes the message to `s3://opencircuitsf-inbound/unsubscribe/{messageId}` — **not done** (rule not created); `deploy/aws/A9-ses-receipt-rule.json`'s single `S3Action` (`ObjectKeyPrefix: "unsubscribe/"`) is ready and matches what `#0058`'s shipped `sesnotify.SESEvent.ObjectKey()` reads
- [ ] Action 2 publishes a notification to the `opencircuit-inbound-mail` SNS topic — **not done** (topic and rule not created); satisfied by the **same** `S3Action`'s own `TopicArn` field per this issue's own correction — not a second, separate action
- [ ] SNS topic subscribed to `POST /api/ses/inbound` over HTTPS — **not done.** Also no longer blocked on the handler not existing (it does, `#0058`, resolved 2026-09-10) — now blocked only on the topic (A6) existing and `#0058`'s config vars being set and the service restarted, itself out of this issue's scope; see the runbook's "After this issue" section
- [ ] S3 lifecycle rule deletes objects after 30 days — **not done.** `deploy/aws/A5-s3-lifecycle.json` (`expire-inbound-30d`, prefix `unsubscribe/`, 30 days) is ready
- [ ] IAM instance role granted `s3:GetObject` and `s3:DeleteObject` on that bucket prefix only — **not done.** `deploy/aws/A11-iam-inline-policy.json` is ready, scoped to `arn:aws:s3:::opencircuitsf-inbound/unsubscribe/*` (the prefix, not the bucket — also corrected in `docs/deployment.md`'s and `docs/aws-iam-setup.md`'s own IAM-policy examples this pass, which both still showed the bucket-wide form)
- [ ] End-to-end verified by sending a real message and observing the object land in S3 — **not done**; requires the AWS writes above first
- [x] Setup documented step by step in `docs/email-setup.md` — done this pass: a full runbook ("Inbound unsubscribe (Phase 4, `#0057`)"), the account-wide gate, the ordering, the RFC 4592 argument re-verified against current code, and rollback

**Independently confirmed this pass, not itself a criterion:** A1 (SES
domain identity) and D1 (its verification TXT) and A2 (the S3 bucket)
already exist in AWS — read-only checks, not created by this pass. See
`## Verification`.

## Notes

PRD §6.5 path 3, §10.2, §10.5. **Do not point the apex MX at SES.** That would hijack all mail to `opencircuitsf.com`, including human mailboxes. The dedicated `lists.` subdomain is the entire reason this is safe.

SES inbound is region-limited — verify the current inbound-supported region list before committing, and keep the whole project in one region. PRD §10.3 selects `us-west-2`.

This path is the optional one of the three unsubscribe mechanisms. If it proves fiddly, point the `mailto:` at a monitored mailbox and process manually — volume at launch makes that entirely workable.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §6.5 — Unsubscribe — three paths, all required · 93 lines
  `sed -n '/^### 6\.5 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Specified by: [`PRD.md`](../PRD.md) §10.3 — SES region choice · 7 lines
  `sed -n '/^### 10\.3 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0007](0007.md) — Extend the config loader for the new environment variables
- Depends on: [#0035](0035.md) — Add List-Unsubscribe headers to campaign email
- Blocks: [#0058](0058.md) — Parse inbound unsubscribe mail
- Blocks: [#0064](0064.md) — Write the deployment runbook

## Work log

| Date | Phase | Model | Tokens | Cost |
|---|---|---|---|---|
| 2026-09-03 | plan | claude-opus-5 | — | — |
| 2026-09-11 | implement | claude-sonnet-5 | — | — |
| 2026-09-11 | review | claude-opus-5 | — | — |

Planning pass only; status stays `open`. **No AWS resource, DNS record or
production configuration was created, modified or deleted** — every AWS and DNS
fact in `## Plan` came from a read-only call, and both available identities
(`certbot-dns-updater` and the `opencircuit-instance` role reached through
IMDS) are denied every SES, SNS and IAM read, which the plan records as
obstacle 1 rather than working around.

Three things the plan corrects against this issue's own criteria and Notes:
the region is `us-east-1`, not `us-west-2` (production has always been
`us-east-1`, and the AWS receiving-endpoint table lists it, so no migration);
the rule takes one `S3Action` with its `TopicArn` set rather than two separate
actions, because a standalone SNS action carries no S3 object key and `#0058`
needs one; and the receiving domain must be verified as an SES identity before
the MX is published, which no criterion mentioned.

Two items reported to the orchestrator for filing (`CLAUDE.md` §9, not filed
here): `docs/email-setup.md` names `us-west-2` in five places against a
production region of `us-east-1` — a staleness distinct from `#0414`'s `From:`
address finding, in the same file; and `PRD.md` §10.2's DNS table is wrong on
both the region and the custom MAIL FROM subdomain, which is
`bounce.mailing.opencircuitsf.com` in production, not `mail.opencircuitsf.com`.

**2026-09-11 implementation pass.** Delivered every piece of this issue that
does not require an AWS write — no AWS resource, DNS record, or production
configuration was created, modified, or deleted by this pass either. Status
moved to `in-progress`: the AWS/DNS side is still entirely the user's to run.

Ground truth was re-measured (read-only) rather than trusted from the plan,
and it had moved since 2026-09-03, in the good direction: `#0058` (parse
inbound mail) shipped and resolved 2026-09-10, and two of the plan's AWS
objects — **A1 (the SES domain identity) and D1 (its `_amazonses.lists.…`
verification TXT), and A2 (the S3 bucket `opencircuitsf-inbound`)** — now
exist. `head-bucket` on the bucket returns 403 (exists, unreadable by this
identity) against a random-name control's 404; the TXT record resolves with
the exact value `"APWUrtnPLURlLWOGg0ybU3t6HbptTzDE77f8JE1YHX0="`. Nothing
else in the plan's AWS table exists yet, and D2 (the MX) correctly does not
either — confirmed by `dig`, matching the plan's safety argument that the MX
must go last.

**Built:**

- `deploy/aws/` (new directory, mirroring the existing `deploy/apache/`,
  `deploy/systemd/`, `deploy/certbot/` convention, each with its own
  `README.md`): ready-to-apply JSON for A4 (S3 bucket policy), A5 (S3
  lifecycle), A7 (SNS topic policy), A9 (the receipt rule — one `S3Action`
  with `TopicArn` set, `TlsPolicy: Optional`, per this issue's own two
  corrections), A11 (the IAM inline policy, scoped to the `unsubscribe/`
  prefix, not the bucket), and both Route 53 change batches (D1, kept for
  reference/rollback only since it's already applied; D2, the MX). Every
  file parses as JSON (`python3 -m json.tool`, all seven clean) and was
  sanity-checked against the exact AWS CLI shape it targets via
  `aws <service> <op> --generate-cli-skeleton` for
  `create-receipt-rule`/`create-receipt-rule-set`/`set-active-receipt-rule-set`/
  `describe-active-receipt-rule-set`, `s3api put-bucket-lifecycle-configuration`/
  `put-bucket-policy`/`put-public-access-block`, `sns set-topic-attributes`,
  and `iam put-role-policy` (`aws-cli/2.36.43`, real skeletons, not
  recalled from memory). None was applied to AWS. `deploy/aws/README.md` is
  the file manifest — it also records why the policy/rule documents
  themselves cannot carry the "header comment" the task asked for: IAM/S3/
  SNS/SES all reject an unrecognized top-level key in a policy or rule
  document, so a literal in-file comment would make the file inapplicable.
  Only the two Route 53 change batches carry one, via the `Comment` field
  that is itself part of that API's schema.
- `docs/email-setup.md`: a full step-by-step runbook, "Inbound unsubscribe
  (Phase 4, `#0057`)" — the account-wide gate first and marked as a hard
  stop, A1/D1/A2 marked done so the user skips re-running them, the MX
  sequenced last with the RFC 4592 consequence **re-verified against the
  current code this pass** (`grep -rn "EmailListDomain\|EMAIL_LIST_DOMAIN"
  internal/` still shows exactly one consumer,
  `internal/mailing/campaign_headers.go`, building only the `mailto:` form
  — nothing constructs an HTTPS URL on the domain), the rollback table, and
  the apex-mail `dig` confirmations. Also updated: the file's own top
  cross-reference and its "Not created, on purpose" DNS note, both of which
  still said the inbound path was entirely "not built."
- `docs/configuration.md`: added the **missing** `SES_INBOUND_TOPIC_ARN` row
  — `#0058` added the variable to the loader and `.env.example` but never to
  this reference doc — and expanded `SES_INBOUND_BUCKET`'s row with the
  production bucket name/prefix and current state.
- `docs/deployment.md`: corrected the install step's variable count/list (it
  said "five of the six" naming six vars; it's now seven vars, five of them
  shipped with real values) and corrected the `## IAM` section's
  `InboundBucketScoped` target-policy JSON from bucket-wide
  (`arn:aws:s3:::opencircuitsf-inbound/*`, matching `PRD.md` §10.5 as
  written) to prefix-scoped (`.../unsubscribe/*`, matching this issue's own
  A11), with a correction note in the file's established style.
- `docs/aws-iam-setup.md`: same bucket-wide → prefix-scoped correction in
  its "What is deliberately not in it" section, plus removing the now-false
  claim that the bucket doesn't exist.

**Deliberately not fixed, reported instead:** `PRD.md` §10.5 itself still
specifies the bucket-wide IAM shape; `docs/aws-iam-setup.md` and
`docs/deployment.md` are corrected against this issue's plan, but the PRD
disagreement is real and worth its own fix, similar to `#0293`'s tracking of
other PRD staleness. `docs/unsubscribe.md`'s opening line ("Not yet
implemented — Phase 4") is stale given `#0033`–`#0039` and `#0058` are
mostly/fully resolved — out of scope for this pass (a Phase-4-wide doc
audit, not an AWS-config issue), reported rather than touched. The two
remaining `us-west-2` mentions in `docs/email-setup.md`'s `## Region`
section, and the one in `docs/deployment.md`'s historical numbered SES-setup
list, were checked and are **not** staleness — they are the deliberate
"this was the wrong value, do not use it" corrective text (`#0418`), so left
as-is rather than deleted.

**Cross-checked against `#0058`'s shipped code, not just its issue file**
(`internal/config/config.go`, `internal/inbound/s3.go`,
`internal/sesnotify/event.go`, `.env.example`): the bucket field is
`cfg.SESInboundBucket` / `SES_INBOUND_BUCKET`; the topic field is
`cfg.SESInboundTopicARN` / `SES_INBOUND_TOPIC_ARN`
(`arn:aws:sns:us-east-1:378152330719:opencircuit-inbound-mail`, exactly A6);
`ObjectKey()` reads `receipt.action.objectKey` with a documented fallback to
`objectKeyPrefix + messageId` — both consistent with A9's
`ObjectKeyPrefix: "unsubscribe/"`. No disagreement found between the plan
and the shipped code; `#0500` (open) doesn't change any of this — it's a
post-activation confirmation task for the fallback branch specifically, not
something that changes what to build now.

## Verification

**No AWS resource, DNS record, or production configuration was created,
modified, or deleted.** Every claim below rests on a read-only check.

- `dig +noall +answer MX opencircuitsf.com` → `1 smtp.google.com.`, matching
  the plan and unchanged.
- `dig +noall +answer MX lists.opencircuitsf.com` → empty (still only the
  zone wildcard), confirming D2 has not been applied.
- `dig +noall +answer MX bounce.mailing.opencircuitsf.com` → `10
  feedback-smtp.us-east-1.amazonses.com.`, unchanged.
- `dig +noall +answer TXT _amazonses.lists.opencircuitsf.com` →
  `"APWUrtnPLURlLWOGg0ybU3t6HbptTzDE77f8JE1YHX0="`, confirming D1/A1.
- Every file in `deploy/aws/*.json` parses with `python3 -m json.tool`
  (seven files, all clean) and was checked against a real
  `--generate-cli-skeleton` for the AWS operation it targets.
- `grep -rn "EmailListDomain\|EMAIL_LIST_DOMAIN" internal/` confirms the
  RFC 4592 safety argument still holds against current code: one consumer,
  building only a `mailto:` URI.
- `grep -c us-west-2 docs/email-setup.md` → `2`, both read and confirmed
  intentional corrective text, not staleness.

`uptime` before the scoped Go run: `5.52 4.79 3.49` — not saturated. Ran
twice, once mid-pass and once after the final edit to this file itself
(since `internal/db` scans `issues/`):

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
ISSUE=0057 scripts/check.sh go ./internal/db/... ./internal/handlers/...
```

Both runs: `VERIFICATION PASSED`; `go build`, `go vet`, `gofmt -l` all
clean; zero skips; both packages reported `ok` with real elapsed time each
time (final run: `internal/db 0.499s`, `internal/handlers 7.751s`). Scratch
database `opencircuit_test_0057` dropped on exit by `check.sh` itself both
times; `pgrep -f 'go test|\.test '` clean afterward. This issue touches no
`web/` or Go source, so `npm run check`/`npm test` were not run.

**What remains — every item below is the user's, per this issue's own
"Split: user versus agent" and `CLAUDE.md` §8b/§9 (outward-facing, and A10
is account-wide):**

1. The account-wide gate: `aws ses describe-active-receipt-rule-set --region us-east-1`.
2. Confirm A1's verification actually completed
   (`aws sesv2 get-email-identity --email-identity lists.opencircuitsf.com --region us-east-1`
   → `VerificationStatus: SUCCESS`).
3. Confirm A2's bucket lockdown state (public-access-block, lifecycle,
   policy) and apply A3/A5/A4 if any are missing.
4. A6 — create the SNS topic; A7 — set its policy.
5. A8 — create the rule set (only if the gate was empty); A9 — create the
   rule; A10 — activate (only if the gate was empty).
6. D2 — the MX, last.
7. End-to-end verify: send a real message to
   `unsubscribe@lists.opencircuitsf.com`, confirm the object lands in
   `s3://opencircuitsf-inbound/unsubscribe/`.
8. A11 — the IAM grant.
9. Outside this issue's scope but needed before the pipeline is fully live:
   `#0058`'s deploy — set `SES_INBOUND_BUCKET` and `SES_INBOUND_TOPIC_ARN`
   in production `config.env` and restart `opencircuit.service` — then A12,
   the SNS HTTPS subscription, which can only confirm once that restart has
   happened.

Full command-by-command detail, expected output, and rollback are in
`docs/email-setup.md`'s "Inbound unsubscribe (Phase 4, `#0057`)" section.

## Review notes — 2026-09-11 (phase 3)

Reviewed `ed2c897` adversarially and independently: every JSON document
parsed and checked against the real botocore service model shipped with
`aws-cli/2.36.43` (not a recalled API shape), every DNS claim re-measured
against the zone's own authoritative nameserver, and the RFC 4592 safety
argument re-derived from the whole tree rather than from `internal/`.

**Status stays `in-progress`, deliberately.** Nine of the ten acceptance
criteria are AWS or DNS writes the user has not performed and an agent must
not perform (this issue's own "Split: user versus agent"; `CLAUDE.md` §8b,
§9). Marking this `resolved` would assert that inbound unsubscribe works,
which is false — no receipt rule, no SNS topic, no MX exists. The agent-side
deliverable is nearly right but carries one blocking defect, below.

### Confirmed correct, independently

- **No change batch touches the apex.** Both Route 53 batches contain
  exactly one `Changes` entry each, `Action: CREATE`, at
  `_amazonses.lists.opencircuitsf.com` (TXT) and `lists.opencircuitsf.com`
  (MX). Nothing at the apex, `www`, the wildcard, `bounce.mailing.`, or any
  `_domainkey` name. `dig +noall +answer MX opencircuitsf.com` still reads
  `1 smtp.google.com.`, unchanged.
- **`CREATE` is the right action for both.** Authoritative queries confirm
  `lists.opencircuitsf.com` owns no record set today — it answers byte-identically
  to a random probe name, so the answer is wildcard synthesis. `CREATE` therefore
  succeeds, and would fail safely (rather than overwrite) if that stopped
  being true.
- **A4 and A7 both carry `AWS:SourceAccount` *and* `AWS:SourceArn`.** The
  SourceArn is syntactically valid for a SES receipt rule
  (`arn:aws:ses:<region>:<account>:receipt-rule-set/<set>:receipt-rule/<rule>`)
  and names the exact rule. A4's principal is `ses.amazonaws.com`, its action
  is `s3:PutObject` alone, and its resource is the `unsubscribe/` prefix, not
  the bucket.
- **A11 is prefix-scoped and does not break `#0058`.** Read
  `internal/inbound/s3.go`: the `s3API` seam declares exactly `GetObject` and
  `DeleteObject`, and `Fetch`/`Delete` are the only callers. Nothing lists the
  bucket or reads outside `unsubscribe/`, so narrowing the grant is safe and the
  `docs/deployment.md` / `docs/aws-iam-setup.md` edits are correct on the
  substance.
- **A9 is one `S3Action` carrying `TopicArn`**, `Recipients` is the single
  mailbox, `TlsPolicy: Optional`, `ScanEnabled: true`. Every member name and
  nesting matches the SES `ReceiptRule`/`S3Action` model; `TlsPolicy`'s enum
  is `["Require", "Optional"]`. `ObjectKeyPrefix: "unsubscribe/"` matches
  `sesnotify.SESEvent.ObjectKey()`, which prefers the notification's own
  `objectKey` and falls back to `objectKeyPrefix + messageId` — both land
  under `unsubscribe/`.
- **A5 matches `BucketLifecycleConfiguration`** (`Rules[]` with `ID`,
  `Filter.Prefix`, `Status`, `Expiration.Days`).
- **The RFC 4592 safety argument holds** — re-derived, not accepted. Every
  consumer of `config.Config.EmailListDomain` funnels into
  `mailing.CampaignHeaders`, which uses it only in
  `"mailto:unsubscribe@" + listDomain + "?subject=..."`. Checked the whole
  tree, not just `internal/`: `cmd/opencircuit/main.go`'s four call sites all
  pass it as `ListDomain` into `SendStore`/`WorkerDeps`/`OutboxWorkerDeps`,
  which reach `CampaignHeaders` and nothing else. `web/src/` never mentions
  the host at all, and there is no `https://lists.opencircuitsf.com` anywhere
  in the repository. Losing A/CNAME resolution at that name breaks nothing.
- **Region.** Every remaining `us-west-2` string in `docs/email-setup.md` (2),
  `docs/aws-iam-setup.md` (2) and `docs/deployment.md` (10) was read in
  context: all are corrective or explicitly-historical text, several inside a
  block prefaced "This plan was not what actually happened". The implementer's
  claim is accurate.
- **Honesty.** Only the documentation criterion is checked, and it is the only
  one provable without an AWS write. `## Verification` separates measured from
  assumed correctly.

### Defect 1 (blocking) — both Route 53 change batches exceed the API's 256-character `Comment` limit

`ChangeBatch.Comment` is shape `ResourceDescription`, `{"type": "string",
"max": 256}` in Route 53's own service model (`2013-04-01`). The committed
comments are **327 characters (D1)** and **720 characters (D2)**. Botocore
does not enforce string length client-side, so both calls reach the API and
are rejected there — `aws route53 change-resource-record-sets` fails with an
`InvalidInput` validation error naming `Comment`.

This is the worst possible place for it: D2 is the last step of the runbook,
the one that turns mail on, and the user will be applying it by hand having
just been told every preceding step succeeded. `--generate-cli-skeleton`
cannot catch this — a skeleton describes structure, not constraints — which
is exactly why the implementer's verification passed.

**Remedy.** Trim both `Comment` values to at most 256 characters and move the
prose into `deploy/aws/README.md`, which has no such limit. Suggested D2
comment (147 chars), which keeps the load-bearing warning:

```
#0057 D2 -- SES inbound MX for lists.opencircuitsf.com. Apply LAST, after the receipt-rule-set gate. See deploy/aws/README.md and docs/email-setup.md.
```

Suggested D1 comment (139 chars):

```
#0057 D1 -- SES identity verification TXT for lists.opencircuitsf.com. ALREADY APPLIED 2026-09-11; kept for rollback only. Do not re-run.
```

Then assert the limit mechanically rather than by eye, so this cannot
regress: `python3 -c "import json,sys;d=json.load(open(sys.argv[1]));assert
len(d.get('Comment',''))<=256"` over `deploy/aws/D*.json`, or a short Go
guard in `internal/db` (which already scans the repo root).

### Defect 2 — `docs/deployment.md` states a fact about `docs/aws-iam-setup.md` that the same commit made false

`docs/deployment.md`'s new correction note reads:

> `docs/aws-iam-setup.md`'s "What is deliberately *not* in it" section still
> shows the bucket-wide form and has the same staleness — reported, not fixed
> here

but `ed2c897` **did** fix that section, in the same commit. The sentence was
true when drafted and false when committed.

**Remedy.** Replace that clause with a statement of what is actually true:
`docs/aws-iam-setup.md`'s "What is deliberately *not* in it" section carries
the same correction as of this pass; `PRD.md` §10.5 is the remaining
disagreement and is reported for filing.

### Defect 3 — the wildcard's measured description is wrong in the one doc whose value is measured facts

`docs/email-setup.md`'s RFC 4592 paragraph reads "the zone's
`*.opencircuitsf.com` CNAME wildcard ... (today: `ec2.smallsharptools.com`,
IP `98.84.75.184`)", and the "What is already done" table repeats "wildcard
CNAME". Measured against the zone's authoritative nameserver
(`ns-923.awsdns-51.net`):

- the wildcard is an **A** record, not a CNAME — a `CNAME` query at a probe
  name returns an empty answer while the `A` query returns a direct A;
- it answers **`98.84.75.184`**;
- `ec2.smallsharptools.com` resolves to **`44.222.209.183`** — a different
  host, and the value in `CLAUDE.md` §7.

So the parenthetical pairs a stale CNAME target with a freshly-measured IP,
producing a claim that is wrong twice over. It does not change the safety
conclusion (a wildcard A is suppressed at an owning name exactly as a
wildcard CNAME is, and the rollback still restores it within the 300s TTL),
but it will mislead whoever verifies the rollback.

**Remedy.** In both places, say "the zone's `*.opencircuitsf.com` wildcard
**A** record (today `98.84.75.184`, the web server)". Drop
`ec2.smallsharptools.com` from this issue's orbit entirely — it is no longer
what the wildcard points at.

### Recommendation (not a defect) — guard the one step that can fail on A4's prefix scoping

A4 grants `s3:PutObject` on `unsubscribe/*` only. SES performs a write
against the bucket when a receipt rule with an `S3Action` is created (the
`AMAZON_SES_SETUP_NOTIFICATION` object), and this review could not confirm
from here whether that write honours `ObjectKeyPrefix`. If it does not, step
5 of the runbook fails with `Could not write to bucket`, which reads like a
policy mistake rather than a prefix mismatch.

**Remedy.** Add one sentence to runbook step 5: if `create-receipt-rule`
fails with a "could not write to bucket" error, temporarily apply A4 with
`"Resource": "arn:aws:s3:::opencircuitsf-inbound/*"`, create the rule, then
re-apply the prefix-scoped A4. Cheap, and it converts a confusing dead end
into a known branch.

Related, worth one line in the runbook: `put-bucket-lifecycle-configuration`
and `put-bucket-policy` both **replace** the whole configuration rather than
merging. Step 1 already tells the user to read the current state first, which
mostly covers it, but the bucket predates this pass and the consequence of
applying blind is silent loss of whatever was there.

### Read-only AWS access, recorded as fact

`aws route53 get-hosted-zone --id Z0825067RV8QY5UIKS96` is denied to
`certbot-dns-updater` (`AccessDenied`, `route53:GetHostedZone`), so the zone
id could not be confirmed by API. It is corroborated three other ways: it is
the id already recorded in `docs/email-setup.md` and `docs/deployment.md` for
records that demonstrably resolve, and the denial message itself confirms
account `378152330719` — the account id baked into every policy document
here.
