# 0220 — Nothing checks TypeScript comments' citations, and it has now cost five issues

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | All |
| **Module** | web / handlers |
| **Phase** | 7 — Inbound and polish |
| **First seen** | 2026-08-23 |
| **Commit** | `89a80ef` |

## Description

`#0216`'s phase-3 review bounced it for shipping **three new false statements** —
in the issue whose entire purpose was removing false statements. The sharpest:

> the new text asserts "CLAUDE.md has no section specifically on jsdom's fidelity
> to a real browser". §1's bullet is headed literally **"jsdom is not a
> browser."** and landed **nineteen minutes earlier**.

The reviewer's judgement is worth preserving: *an unfilled ellipsis is
self-advertising; a confident false claim about the repo's binding guidance is
not.* The "fix" was worse than the defect.

**This is the fifth instance of the class** — `#0190`, `#0192`, `#0196`,
`#0199`, and now `#0216` itself. `#0196` built a guard for it that walks Go
comments and fails when one cites a `Test*` identifier no function defines. It
does not cover TypeScript, and it does not cover cited **paths** or
**`CLAUDE.md §N`** references in any language.

A guard resolving those against the tree would have caught **three of `#0216`'s
five defects mechanically**, including the one its own fix introduced.

**A second gap, from the same review.** `tabindex="-1"` on the modal panels is
unguarded. Without it `focus()` is a silent no-op — so the handler-host ===
focus-target equivalence `#0216`'s new guard proves would still not make Escape
work. All 11 sites carry it today; nothing asserts they keep it. The review
estimates two lines inside the guard's existing per-panel walk.

## Implementation notes

Ported `#0196`'s design (`internal/handlers/dangling_test_citation_guard_test.go`)
rather than inventing a second one, for two more citation shapes: a
repository PATH, and a `CLAUDE.md §N` section reference. Both are resolved
against their own ground truth (files actually on disk; `CLAUDE.md`'s own
numbered `## N. ` / `## Na. ` headings, read fresh off disk every run so the
guard tracks the file rather than a hardcoded list) instead of a fixed
allowlist.

**Two files, one design, mirrored, not shared.** Go comments
(`internal/handlers/citation_target_guard_test.go`) reuse `#0196`'s own
`citedTestScanRoots`/`walkGoFiles`/`stripCommentMarkers`/`toRepoRelativePath`
helpers directly (same package, both source files clean in git status when
this started, so none of `#0196`'s concurrent-edit caution applied).
TypeScript/Svelte comments (`web/src/lib/citationTargetGuard.test.ts`) are a
separate implementation in a separate language and engine (V8 regex vs
RE2), following `#0198`'s own precedent for the sibling admin-facing-string
guard: mirrored reasoning, not shared code.

**Comment extraction, the actual new work for the TS/Svelte half.**
TypeScript has no public "walk every comment" AST node — comments are
trivia. `ts.createScanner(target, /*skipTrivia*/ false, variant, source)`
tokenizes `SingleLineCommentTrivia`/`MultiLineCommentTrivia` directly, which
is the standard technique (confirmed by a small throwaway probe against the
already-installed `typescript` package before writing the real scanner).
For `.svelte`, `svelte/compiler`'s own `ast.comments` (JS comments in
`<script>`/`<script module>`) and the fragment tree's `Comment` node type
(HTML comments) — the same split `citationGuard.test.ts`'s own header
already documents for a different purpose.

**Comment-group joining, needed for real (not just by analogy with Go).**
A per-physical-line-only match missed a real instance:
`internal/seo/dist_placeholder_guard_test.go:116`'s `hashedAssetPattern`
doc comment carries its `"e.g."` marker on the line BEFORE the citation it
excuses (`"...hashed asset reference, e.g.\n// \`/assets/index-BDtqW4JY.js\`
or \`/assets/index-TVYljy6F.css\`..."`), so both languages' guards join
consecutive `//`-style lines with no gap into one text blob before matching
— mirroring Go's own `*ast.CommentGroup` grouping — and keep a `/* */` or
`<!-- -->` block as its own single (but internally multi-line) blob, split
the same way `#0199`'s block-comment line-number fix does, so a reported
line is the citation's own physical line, not the block's opening line.

**Self-scan (criterion 5).** Both guards scan `_test.go`/`.test.ts` files,
including themselves — `#0181`'s exclude-the-test-file trick is not
available here, since `#0216`'s own defects (the dangling test-file names,
`web/HANDOFF`) landed in `.test.ts` files, and excluding those would hide
exactly the shape this issue exists to catch. Both files stay clean by
construction: every genuine path or section cited in their own real
comments resolves (verified directly — the Go guard's own tree-wide run
includes itself, and the TS guard additionally has a dedicated
`'scans its own file (self-reference) and finds zero hits'` test asserting
it in isolation), and every fictitious example needed to prove the guard
*fires* is built from string concatenation inside a `src` fixture variable,
never written as a literal comment token — the same technique `#0196`'s own
file states for itself.

**The false-positive rule, in full** (criterion 4) — six structural
exclusions, each tied to a specific real instance the dry run against the
whole tree turned up, none a blanket loosening:

1. **URL-embedded** — `"://"` within the 24 characters before the match
   (`internal/handlers/citation_guard_test.go`'s own
   `"https://example.com/docs/guide.md"` reasoning-comment example).
2. **Deliberately hypothetical**, introduced by `"e.g."`, `"for example"`,
   `"such as"`, `"hypothetical"`, `"imagine"`, `"not a real path"`,
   `"does not exist"`, `"no such file"`, `"made up"`, or `"placeholder"`
   within the preceding 60 characters (`citationGuard.test.ts:76`'s
   `"issues/NNNN.md (a path to one tracker file, e.g."`).
3. **Historical** — the comment itself says the target no longer exists
   (`"deleted in"`, `"the deleted"`, `"was deleted"`, `"removed in"`,
   `"no longer exists"`, `"used to live at"`, and, web-only,
   `"never written"` for `modalKeydown.test.ts`'s own honest history of
   `#0216`'s dangling citations) — the real instances are
   `internal/handlers/request.go:25`'s `"(deleted in #0002 along with the
   rest of internal/handlers/redirect.go)"` and
   `testutil_test.go:7`'s `"...from the deleted\ninternal/handlers
   /url_filters_test.go"`.
4. **Named external dependency** — `"goldmark"` appearing anywhere earlier
   in the same comment BLOCK (unbounded within that one block, not a fixed
   window, since what licenses reading a later path as "inside that
   dependency" is the surrounding explanation, not just the adjacent
   phrase) — the real instances are `internal/mailing
   /campaign_markdown.go`'s three references to goldmark's own
   `renderer/html/html.go` and `renderer/renderer.go`, which this repo does
   not and should not contain.
5. **This repo's own `NNNN` placeholder convention** (`issues/NNNN.md`'s
   filing template) — any path segment, extension stripped, that is
   literally `NNNN`.
6. **Two (or more) whole filenames — or known bare document names (`PRD`,
   `HANDOFF`, `README`, `LICENSE`) — joined by a bare `"/"` as a list
   separator, not a nested path** — real instances:
   `worker_test_helpers_test.go:2`'s `"campaigns_test.go/audience_test.go"`,
   and `internal/mailing/campaign_markdown.go:14`'s `"PRD/CLAUDE.md §9"`
   (meaning "PRD §9 and CLAUDE.md §9", not a path under a nonexistent
   `PRD/` directory). Structural: **any** intermediate segment already
   looking like a complete filename with a recognized extension, or being
   a known bare document name, excludes the whole citation — never a
   name-based allowlist of the whole path. [Corrected during #0220's
   phase-3 follow-up: this previously said "every", not "any" — the
   implementation matches the FIRST qualifying segment, not all of them.
   They coincide for the two-segment real instances above, so the
   difference went unnoticed until a three-segment probe
   (`Admin.svelte/lib/totally_nonexistent_xyz.ts`, silently excluded by
   its first segment alone) split them. "Any" is what ships; see
   `## Review notes` below and the two guard files' own doc comments for
   the accepted cost.]

   Web-only seventh: a glob-style wildcard prefix (`"*.name.test.ts"`) is
   discounted the same way `#0196`'s Go guard discounts a trailing `*` —
   the real instance is `modalKeydown.test.ts`'s own corrected text
   (post-`#0216`), which replaced three dangling citations with an honest
   `"*.escapeWiring.structuralGuard.test.ts"` shape describing what the old
   citations looked like, not naming a specific file.

**Path resolution** (both languages, identical logic): exact repo-relative
match; the same path with `.md` appended (the bare `HANDOFF`/`README`/
`LICENSE` form); a real path that ENDS WITH `"/" + cited` (the shortened
form this tree actually uses — `logout_all_test.go:285`'s
`"handlers/auth.go"` for `internal/handlers/auth.go`;
`"admin/Campaigns.svelte"` for `web/src/views/admin/Campaigns.svelte`); the
same suffix check with `.md` appended; and, ONLY if none of those resolves
and the first segment carries no extension, one retry with that first
segment stripped (`WorkshopsIndex.svelte:7`'s real
`"WorkshopCard/lib/workshops.ts"`, meaning "`WorkshopCard`[.svelte] and
`lib/workshops.ts`", not a nested path under a nonexistent `WorkshopCard/`
directory). Deliberately bounded to stripping exactly one leading segment,
not iterated — a general "any suffix resolves" rule would also rescue a
genuinely dangling multi-segment citation whose tail happened to
coincidentally match some unrelated real file.

**The web guard's one extra shape.** `#0216`'s own historical defect named
three test FILES with no directory at all
(`"CampaignEditor.escapeWiring.structuralGuard.test.ts"`, bare), which the
path pattern (requiring at least one `/`) cannot see. A second, narrower
pattern (`citationTargetBareTestFilePattern`) catches a bare
`"Name.thing.test.ts"`-shaped reference and resolves it by basename only
against the real tree — the weakest, most permissive check either guard
performs, deliberately scoped to exactly that one shape. No Go-side
counterpart: nothing in the Go tree cites a bare `_test.go` name the same
way.

**`@types/node`, deliberately not added.** The TS guard needs real
filesystem access (`node:fs`, `node:path`, `node:url`) — `import.meta.glob`
(the technique `citationGuard.test.ts` uses) only loads a STATIC set of
files matching a glob known at build time, not an arbitrary repo-relative
path string discovered at runtime from inside a comment. `#0181`'s own
review pass (see `citationGuard.test.ts`'s header) had already tried and
REMOVED an `@types/node` dependency and a `"node"` entry in
`tsconfig.json`'s `types` array in favor of `import.meta.glob`, specifically
to avoid the dependency (`npm ls --all` confirms `@types/node` is not
installed — only present, unmet, as an optional peer of vite/vitest).
Keeping that precedent intact: `web/src/lib/nodeFsShim.d.ts` is a minimal
ambient declaration for exactly the handful of functions the guard calls,
not a real `@types/node` substitute. It affects type-checking only — none
of the three modules is ever imported outside `_test.ts` files, so
`vite build`'s shipped `dist/` bundle never sees any of it.

**`dist/` is not pruned from the repo-path walk**, unlike Go's own vendored
roots (`node_modules`, `.git`) — several real comments cite
`"dist/index.html"` / `"web/dist/index.html"` (`cmd/opencircuit/main.go`,
`static.go`, `confirm.go`, `web/embed.go`, `internal/seo`), and
`web/dist/index.html` is a TRACKED PLACEHOLDER (`docs/frontend.md:16-18`)
that exists on disk in a fresh checkout regardless of whether `npm run
build` has run. Pruning it the way the Test*-citation guard's own walk
prunes `dist` would have made every one of those legitimate citations
unresolvable. [Corrected during #0220's phase-3 follow-up: this previously
cited `CLAUDE.md §1`, which does not make this claim — see `## Review
notes` below, criterion 2.]

**`tabindex="-1"` guard** (`web/src/lib/modalTabindexGuard.test.ts`,
criterion 6) — its own file rather than a two-line addition inside
`modalFocusWiring.structuralGuard.test.ts`'s existing per-panel loop, as
the issue's own Notes estimated: that file's 14 synthetic fixtures
assert EXACT violation counts (`toHaveLength(1)`, etc.) for the unrelated
onkeydown/focus-effect defect, and none of them carries `tabindex="-1"`
today — adding a second violation class to that same loop would have
either broken every one of those counts or required editing all 14
fixtures for no benefit to what they prove. [Corrected during #0220's
phase-3 follow-up: the file has 15 `it(` blocks, one of which is the
repo-wide scan, not a synthetic fixture — 14, not 12. This count was wrong
at every point in the file's history; see `## Review notes` below.] The
new file reuses the same
AST-walk technique (`svelte/compiler`'s `parse`, `RegularElement` nodes)
with its own, separate violation list. Confirmed against the real tree: all
11 sites `modalFocusWiring.structuralGuard.test.ts` finds carry
`tabindex="-1"` today, matching `#0216`'s own Notes ("All 11 sites carry it
today").

**Mutation-reinstatement (criterion 3) — what a citation-existence guard
can and cannot catch.** `#0216`'s history names several distinct defects
across two reviews; only some are citations of a specific wrong target,
which is the only shape this class of guard can mechanically resolve:

- **Mechanically catchable, and reinstated from real git history:**
  - `web/src/lib/modalKeydown.test.ts` at `d9a9e01^` cited
    `"web/src/views/Admin.escapeWiring.structuralGuard.test.ts"` and two
    bare siblings — none exists.
  - `web/src/views/admin/CampaignSendDialog.behavior.test.ts` at `ddbde2b`
    cited `"web/HANDOFF"` — `web/` has no `.md` file at all.
- **Not mechanically catchable, and not attempted:** the "CLAUDE.md has no
  section on jsdom's fidelity" claim from the `#0216fix` bounce is a claim
  of ABSENCE with no specific section number cited — there is nothing for
  a citation-resolver to check; and the "two more sites" / a double-count
  in this issue's own arithmetic are miscounts, not citations of a wrong
  target. Both are a categorically different defect class (verifying a
  claim's truth, not a reference's existence) that this guard does not
  attempt to solve — recorded here rather than silently narrowed to fit,
  in the same register this tracker uses elsewhere for an honest scope
  boundary.

## Acceptance criteria

- [x] A guard fails when a comment cites a repository path that does not exist — covering `.ts` and `.svelte` as well as `.go`
- [x] It fails when a comment cites a `CLAUDE.md` section heading that does not exist
- [x] Reinstating `#0216`'s dangling citations makes it fail, naming each — demonstrated by mutation (see `## Implementation notes` for which of `#0216`'s defects are mechanically citable and which are not)
- [x] It does not fire on prose that merely resembles a path, on a URL, or on a deliberately hypothetical example — say what the false-positive rule is
- [x] It scans its own file without tripping, and the technique is stated (`#0196`'s guard has this property; `#0181`'s solves it by excluding `_test.go`, which this cannot do)
- [x] `tabindex="-1"` is asserted on every `role="dialog" aria-modal="true"` panel, with a mutation proving it bites
- [x] `#0196`'s Go guard still passes, and `#0216`'s focus-wiring guard still finds its 11 sites
- [x] `ISSUE=0220 scripts/check.sh go ./internal/...` and `scripts/check.sh web` both pass

## Notes

Reported by `#0216`'s phase-3 review under `CLAUDE.md` §9.

Criterion 2 is the one that would have caught the worst instance. Section
headings are stable strings in a file the guard can read; resolving `§N` against
`CLAUDE.md`'s own headings is cheap.

Worth reading before starting: `#0196`'s guard for the mechanics and its
discount rules (wildcards, `TestSentAt`, `*TestPool`, "renamed from"), and
`#0199` for the three blind spots that turned up in it afterwards — the same
shapes will appear here.

Note the class has an asymmetry worth designing around: a **missing** citation
guard costs a reviewer's time, but a **false-positive** one gets silenced, and
then costs everything. `#0199`'s review made that argument about a discount rule
that was too broad.

## Verification

`uptime` before every run stayed in the 1.7–2.8 load-average range, well
under the ~8 threshold — nothing here is a timing reading.

**Go**: `ISSUE=0220 scripts/check.sh go ./internal/...` — build, vet, and
`gofmt -l` clean; all 15 packages report `ok` with real elapsed times
(`handlers` 3.214s, `interests` 2.976s — no DB-backed package at 0.00s);
skip audit `(no skips)`; `VERIFICATION PASSED`. Own database
`opencircuit_test_0220`, dropped by `check.sh` on exit. Targeted re-run,
individually: `go test ./internal/handlers/ -run
'TestNoCommentCitesUnresolvedPathOrSection|TestCitationTarget|
TestPathCitationResolves' -v` — all 5 new tests `PASS`; `-run
TestNoCommentCitesUndefinedTestFunction` (`#0196`'s own guard) — `PASS`;
`-run TestNoAdminFacingStringCitesInternalDocs` (`#0181`'s guard) — `PASS`.

**Web**: `scripts/check.sh web` — **PASSED**. `npm run check`: 273 files
(270 baseline + `citationTargetGuard.test.ts`, `modalTabindexGuard.test.ts`,
`nodeFsShim.d.ts`), 0 errors, 0 warnings. `npm test`: 32 test files (30
baseline + 2), 3817 tests passed (3794 baseline + 23 — exactly
`citationTargetGuard.test.ts`'s 17 and `modalTabindexGuard.test.ts`'s 6).
No leftover processes. Targeted re-run, individually:
`npx vitest run src/lib/citationTargetGuard.test.ts
src/lib/modalTabindexGuard.test.ts src/lib/citationGuard.test.ts
src/lib/modalFocusWiring.structuralGuard.test.ts` — 4 files, 99 tests, all
passed (confirms `#0181`'s and `#0216`'s guards are unweakened, and
`modalFocusWiring.structuralGuard.test.ts`'s own `sites.length >= 11`
assertion still holds — `#0216`'s focus-wiring guard still finds its 11
sites).

**Mutation proofs**, per §8a (no `git stash`/`git checkout --`/`git
restore`; every mutation applied with `Edit`, reverted with a `cp`-restored
copy taken first, confirmed with `shasum -a 256` byte-identity and an empty
`git diff --stat`):

- `web/src/lib/modalKeydown.test.ts`, reinstated to `d9a9e01^`'s header
  (`git show d9a9e01^:web/src/lib/modalKeydown.test.ts`): guard failed,
  naming all four —
  `web/src/lib/modalKeydown.test.ts:7:
  web/src/views/Admin.escapeWiring.structuralGuard.test.ts`,
  `:7: Admin.escapeWiring.structuralGuard.test.ts`,
  `:8: CampaignEditor.escapeWiring.structuralGuard.test.ts`,
  `:9: Campaigns.escapeWiring.structuralGuard.test.ts`. Reverted from a
  `cp` copy taken before the edit; `shasum -a 256` matched
  (`db25c5b60249deb18def6f6d0f7e2328ffa98e7686b1f027d6e3df6a664563c0`) before and after; `git diff --stat` empty
  throughout.
- `web/src/views/admin/CampaignSendDialog.behavior.test.ts`, reinstated to
  `ddbde2b`'s `"see web/HANDOFF or issues/0094.md's..."` clause: guard
  failed — `web/src/views/admin/CampaignSendDialog.behavior.test.ts:22:
  web/HANDOFF`. Reverted from a `cp` copy taken before the edit; `shasum
  -a 256` matched (`ab4c55fdef313e6c0f0b14f80a1afc9e52011e89229c69838c1803bb1facdf34`) before and after; `git diff
  --stat` empty throughout.
- Every synthetic "catches" test (5 in the Go file, 4 in the web file, 1 in
  the tabindex file) independently proves the corresponding rule fires
  against a fictitious example never written as a literal comment token in
  this file's own source (verified: the self-scan test finds zero hits
  from the guard's own file).
- Every synthetic "exclusion" test (in both files) proves the false-positive
  rule holds for the exact real instance that motivated it — see
  `## Implementation notes`' "false-positive rule, in full" for the mapping
  from rule to real line.

## Files changed

- `internal/handlers/citation_target_guard_test.go` (new) — the Go half of
  the guard.
- `web/src/lib/citationTargetGuard.test.ts` (new) — the TypeScript/Svelte
  half.
- `web/src/lib/modalTabindexGuard.test.ts` (new) — criterion 6.
- `web/src/lib/nodeFsShim.d.ts` (new) — ambient declarations for
  `node:fs`/`node:path`/`node:url`, added instead of an `@types/node`
  devDependency (see `## Implementation notes`).

## Relation

- Found by: [#0216](0216.md) — the false-coverage claims in `#0120`'s new tests
- Relates to: [#0196](0196.md) — the Go-only citation guard this extends
- Relates to: [#0199](0199.md) — that guard's blind spots
- Relates to: [#0190](0190.md), [#0192](0192.md) — earlier instances of the class

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-24 | implement | claude-sonnet-5 | — | — | — | — | usage not captured by this subagent; orchestrator to record from transcript |
| 2026-08-24 | implement (bounce fix) | claude-sonnet-5 | — | — | — | — | usage not captured by this subagent; orchestrator to record from transcript |
| 2026-08-24 | implement (second bounce fix) | claude-sonnet-5 | — | — | — | — | usage not captured by this subagent; orchestrator to record from transcript |
| 2026-08-24 | implement (third bounce fix) | claude-sonnet-5 | — | — | — | — | usage not captured by this subagent; orchestrator to record from transcript |
| 2026-08-24 | implement (fourth bounce fix) | claude-sonnet-5 | — | — | — | — | usage not captured by this subagent; orchestrator to record from transcript |

## Review notes — 2026-08-24 (phase 3, BOUNCED)

**Status deliberately left at `in-progress`, not reverted to `open`** — the
code commit `a224d6b` stands, the guards work, and what is wrong is confined
to five comment/prose statements. This needs a short follow-up pass, not a
re-implementation.

### The guards themselves work — verified, not assumed

All mutations ran in a throwaway `git worktree` at `HEAD` with
`web/node_modules` symlinked in (no `git stash`, no shared-tree revert,
`CLAUDE.md` §8a). Baseline in the worktree:
`npx vitest run src/lib/citationTargetGuard.test.ts
src/lib/modalTabindexGuard.test.ts` → 23/23.

- **Criterion 3, reinstating `#0216`'s real dangling citations.** Prepended
  `// See web/src/views/Admin.escapeWiring.structuralGuard.test.ts for the
  sibling guard.` to `web/src/lib/modalKeydown.test.ts`. The guard failed
  naming **both** shapes at once:
  `modalKeydown.test.ts:1: [path] web/src/views/Admin.escapeWiring.structuralGuard.test.ts`
  and `modalKeydown.test.ts:1: [path] Admin.escapeWiring.structuralGuard.test.ts`
  — the full-path pattern and the bare-test-file pattern independently.
- **Criteria 1 and 2 together.** Prepended
  `// Per CLAUDE.md §42 and web/HANDOFF, this is the rule.` → the guard
  reported `[path] web/HANDOFF` and `[section] CLAUDE.md §42`. Both shapes
  the issue was filed for resolve against real ground truth.
- **Criterion 6, `tabindex="-1"`.** Verified the guard mechanism bites and
  that all 11 sites carry it today (tree-wide test, zero violations).
- **Criteria 7 and 8.** `ISSUE=0220rev scripts/check.sh` — Go all `ok`,
  **zero skips**, `internal/handlers` 3.203s, `internal/db` 0.198s,
  `internal/auth` 0.646s, `internal/audit` 0.667s (scoped follow-up run);
  `npm run check` 274 files / 0 errors / 0 warnings; `npm test` 33 files /
  3825 tests. `#0196`'s guard and `#0216`'s 11-site survey both still pass.

### What must be fixed before this resolves

This is the issue whose entire premise is that a shipped comment must be
true. `#0216` was bounced for shipping false statements inside a fix for
false statements. Five statements in this diff are wrong, and one of them
misdescribes the guard's own behavior in a way that matters.

**1. Exclusion rule 6 is documented as "every", implemented as "any" — and
the difference swallows a real dangling citation.**

`internal/handlers/citation_target_guard_test.go`'s comment (and the TS
mirror, and `## Implementation notes` above) says:

> Every INTERMEDIATE segment (not the last) already looking like a complete
> filename with a recognized extension, OR being one of this repo's own bare
> document names, is what tells the two shapes apart structurally.

The code returns `true` on the **first** such segment:

```go
for _, seg := range segments[:len(segments)-1] {
    if citationTargetFileLikeSegmentPattern.MatchString(seg) || citationTargetBareDocNames[seg] {
        return true
    }
}
```

For the two-segment real instances (`campaigns_test.go/audience_test.go`,
`PRD/CLAUDE.md`) "any" and "every" coincide, which is why it was not
noticed. At three segments they diverge. Demonstrated directly, two probes
in separate comment blocks in `modalKeydown.test.ts` in the worktree:

| probe | flagged? |
|---|---|
| `Admin.svelte/lib/totally_nonexistent_xyz.ts` | **no — silently excluded** |
| `handlers/lib/totally_nonexistent_xyz.ts` (control) | yes |

Neither path exists. The first is excluded solely because its *first*
segment ends in `.svelte`. So a genuinely dangling citation of the shape
`SomeFile.svelte/dir/gone.ts` or `foo.go/pkg/bar.go` passes this guard
today, contrary to the rule as written.

The behaviour may well be the one you want (it is the natural generalisation
of `WorkshopCard/lib/workshops.ts`). **The comment is what has to change** —
say "any", and say what that costs, since criterion 4 is explicitly *"say
what the false-positive rule is"* and the rule as stated is not the rule as
run. Fix it in three places: the Go comment, the TS file, and
`## Implementation notes` above ("Structural: every intermediate segment…").

**2. `CLAUDE.md §1` is cited for a claim `CLAUDE.md` does not make, with a
quotation `CLAUDE.md` does not contain.**

`citation_target_guard_test.go:318–320`:

> `web/dist/index.html` is a tracked placeholder (CLAUDE.md §1, "the built
> SPA is embedded via //go:embed")

`CLAUDE.md` §1 contains one line about `dist`, the repository-layout row:
`` | `web/` | Svelte 5 SPA; built to `web/dist/` and embedded via
`//go:embed all:dist` | ``. It is not the quoted sentence, and §1 says
nothing whatever about `web/dist/index.html` being **tracked** or a
**placeholder**. The fact is true and it is documented — at
`docs/frontend.md:16–18` ("`web/dist/index.html` is committed as a minimal
placeholder … `web/dist/*` is otherwise gitignored (`!web/dist/index.html`…)").
Cite that. This is the precise defect class `#0216` was bounced for: a
confident, checkable claim about the repo's binding guidance that does not
hold. The guard cannot catch it — §1 exists, so the reference resolves —
which is exactly why it needs a human to not write it.

**3. A doc comment in the TS guard names a symbol that does not exist.**

`web/src/lib/citationTargetGuard.test.ts:120` opens
`// citationTargetTestFileLikePattern additionally recognizes …`. The
constant it documents is `citationTargetBareTestFilePattern`;
`citationTargetTestFileLikePattern` appears nowhere else in the tree (`grep
-rn` returns that one line). This is `#0196`'s exact guarded class, landing
in the language `#0196` does not cover — in the file added to cover it.

**4. The fixture count is wrong, in two files.**

`web/src/lib/modalTabindexGuard.test.ts:14` and `:18` say
`modalFocusWiring.structuralGuard.test.ts`'s *"12 synthetic fixtures"* /
*"all twelve fixtures"*; `## Implementation notes` above says *"that file's
twelve synthetic fixtures"* and *"editing all twelve"*. The real number is
**14** (15 `it(` blocks, one of which is the repo-wide scan). It was 14 at
`a224d6b^` too — `git show a224d6b^:web/src/lib/modalFocusWiring.structuralGuard.test.ts
| grep -c '  it('` returns 15 — so this was never right at any point in the
file's history: `#0222` took it from 10 to 14, and 12 is neither.

**5. Two garbled sentences in `citationTargetGuard.test.ts`.**

- line ~72: *"same HANDOFF/README/LICENSE bare-suffix exception for the real
  historical the `"web" + "HANDOFF"` defect"* — a dangling "the".
- line ~120: *"additionally recognizes a bare a bare `"Name"` + …"* —
  duplicated words.

### What I looked for and did not find

- Whether any of the six exclusions is a fig leaf for a real dangling
  citation. Rules 1 (URL), 3 (historical), 5 (`NNNN`) and the web-only
  wildcard are tightly scoped and each is tied to an instance I confirmed
  exists. Rule 2 (`e.g.`/`for example`/… within 60 characters) is the
  broadest by construction — I probed it: a fabricated
  `internal/handlers/totally_nonexistent_abc.go` preceded by "for example,"
  is silently excluded, while the same citation with no marker is flagged.
  That is the declared behaviour working as designed, and the window is
  bounded at 60 characters, so a marker on one line does not excuse an
  unrelated citation three lines later (confirmed: the control probe on the
  next line of the same merged block *was* flagged). Rule 4 (goldmark) is
  unbounded within a comment block and is declared as such; a genuinely
  dangling path later in `campaign_markdown.go`'s long header would be
  invisible. Worth a sentence in the header saying so, but it is stated in
  the issue, so I am not counting it against the criteria.
- Whether the guards' own files pass their own scan by *gaming* the `e.g.`
  rule. They do use it — the TS header describes `#0216`'s dangling
  citations behind "e.g." and "do not exist" — but the surrounding text
  literally says those targets do not exist, so the exclusion is being used
  for what it is for.
- Whether `TestPathCitationResolvesShortenedAndLeadingWordForms`'s mid-test
  `t.Skip` ("docs/README.md does not exist in this checkout") could silently
  swallow the trailing `web/HANDOFF` assertion. `docs/README.md` exists, so
  it does not fire — but a `t.Skip` positioned before the load-bearing
  assertion in the same function is a latent hole; consider `t.Log`/`if`
  instead so the last assertion always runs.
- `web/dist/index.html` is untouched by this review (`git status --porcelain
  -- web/dist` empty).

## Fix pass — 2026-08-24 (addressing the phase-3 bounce)

Fixed the five accuracy defects the review named, plus the `t.Skip` latent
hole, without touching the guard mechanism itself (judged sound and
mutation-proved by the review).

1. **Exclusion rule 6, "every" vs "any"**: chose to fix the DOCUMENTATION,
   per the review's own instruction ("The comment is what has to change").
   Updated three places: `internal/handlers/citation_target_guard_test.go`'s
   `pathCitationIsExcluded` doc comment (now says "ANY intermediate segment
   ... not a requirement that every intermediate segment qualify", and
   states the accepted cost — a citation shaped like a file-like first
   segment followed by a genuinely-dangling remainder is silently excluded);
   `web/src/lib/citationTargetGuard.test.ts` gained the same explanation
   above its own (previously undocumented) equivalent loop; and this
   issue's own `## Implementation notes` §6 above, corrected in place with a
   bracketed note pointing at this section. The behavior itself (matching
   the FIRST qualifying segment) was NOT changed — the review left that
   choice open and reasoned it was probably fine; changing code to match
   stale prose when the prose was what drifted would have been the wrong
   fix.
2. **`CLAUDE.md §1` cited for a claim it doesn't make**: retargeted to
   `docs/frontend.md:16-18`, which actually states "`web/dist/index.html` is
   committed as a minimal placeholder ... `web/dist/*` is otherwise
   gitignored". Fixed in both
   `internal/handlers/citation_target_guard_test.go`'s
   `citationTargetSkipDirs` doc comment and this issue's own Implementation
   notes (which had the identical wrong citation).
3. **Wrong symbol name in a doc comment**:
   `web/src/lib/citationTargetGuard.test.ts`'s doc comment now says
   `citationTargetBareTestFilePattern` (the real constant), not
   `citationTargetTestFileLikePattern` (which appears nowhere else in the
   tree).
4. **Fixture count, "12" → 14**: counted directly —
   `modalFocusWiring.structuralGuard.test.ts` has 15 `it(` blocks, one of
   which is the repo-wide scan, so 14 synthetic fixtures, confirmed via
   `git show a224d6b^:...` to have been 14 (never 12) at any point in the
   file's history. **`modalTabindexGuard.test.ts`, the file the review
   pinpointed (lines 14 and 18) as carrying the wrong count, is NOT in this
   pass's file scope** (the dispatch named exactly five files this
   implementer may touch, and that file is not one of them) — corrected
   this issue's own `## Implementation notes` instead, with a bracketed
   note flagging that `modalTabindexGuard.test.ts` itself still needs the
   same one-word fix (`twelve`/`12` → `14`, two places) as a follow-up. **This
   is a known gap in this fix pass, not an oversight** — see the
   dispatching agent's report.
5. **Two garbled sentences**: "for the real historical the `"web" +
   "HANDOFF"`" → "for the real historical `"web" + "HANDOFF"`" (dropped the
   stray "the"). "additionally recognizes a bare a bare `"Name"`" →
   "additionally recognizes a bare `"Name"`" (deduplicated), corrected in
   the same edit that fixed defect 3 above since both sat in the same
   paragraph.
6. **`t.Skip` before a load-bearing assertion**: replaced the `t.Skip` in
   `TestPathCitationResolvesShortenedAndLeadingWordForms` with `t.Error`, so
   a missing `docs/README.md` fixture is reported as a failure rather than
   aborting the function before the `web/HANDOFF` assertion runs. Confirmed
   `docs/README.md` still exists in this checkout (so the branch does not
   fire today either way) and re-ran the test — still `PASS`.

**A defect introduced while writing the fix, caught before it shipped**: my
first draft of defect 1's explanatory comment wrote out the literal
three-segment probe path
(`Admin.svelte/lib/totally_nonexistent_xyz.ts`) split across two comment
lines, with the tail segment (`lib/totally_nonexistent_xyz.ts`) repeated as
its own bare fragment. Since this guard scans ALL comments in the tree
(including its own file, per criterion 5), that repeated fragment was a
second, independent match the main guard test flagged as an unresolved
citation — caught immediately by `ISSUE=0220fix scripts/check.sh go
./internal/handlers/...` failing on `TestNoCommentCitesUnresolvedPathOrSection`
naming its own new line. Reworded to describe the shape without spelling
out a second bare slash-joined fragment; re-ran and confirmed clean. Left in
here as a small, concrete demonstration that criterion 5's self-scan
property is doing real work, not just passing by construction.

**Verification**: `go test ./internal/handlers/ -run
'TestNoCommentCitesUnresolvedPathOrSection|TestCitationTarget|TestPathCitationResolves'
-v` — all 5 tests `PASS`. `ISSUE=0220fix scripts/check.sh go ./internal/...
./cmd/...` — all 16 Go packages `ok`, zero skips, `gofmt -l` clean.
`scripts/check.sh web` — 274 files / 0 errors; 33 test files / 3826 tests
(one more than this issue's own 3825/3817 baselines — `#0226`'s sibling fix
pass, running concurrently in the same tree, added its own one test;
nothing in this issue's own two files' test counts changed). `uptime`
stayed at load-average ~2.9–3.4 throughout — not a timing reading. Own
database `opencircuit_test_0220fix`, dropped on exit.

**Files touched by this pass**: `internal/handlers/citation_target_guard_test.go`,
`web/src/lib/citationTargetGuard.test.ts`, `issues/0220.md`. Did not touch
`modalTabindexGuard.test.ts` (see defect 4 above), `citationGuard.test.ts`,
`modalFocusWiring.structuralGuard.test.ts`, or any file outside this pass's
assigned scope.

## Review notes — 2026-08-24 (second phase-3 review, BOUNCED)

**Status left at `in-progress`.** Commits `daea56f` and `ae9fb95` stand; the
guard mechanism is sound and every one of the previous review's five named
defects is genuinely fixed. But the fix pass introduced a **sixth false
statement**, in the same comment it wrote to correct the first one — and this
is the issue whose entire premise is that a shipped comment must be true.
`uptime` first: load average 1.4–2.5 throughout.

### The five named defects: all genuinely fixed, each re-checked

1. **"every" → "any" (exclusion rule 6).** `pathCitationIsExcluded`'s doc
   comment in `internal/handlers/citation_target_guard_test.go` now says "ANY
   intermediate segment … this is a match on the FIRST such segment, not a
   requirement that every intermediate segment qualify", and
   `web/src/lib/citationTargetGuard.test.ts` gained the same explanation above
   its previously-undocumented mirror loop. `## Implementation notes` §6 carries
   the bracketed correction. **Fixing the documentation rather than the code was
   the right call** — see the next section, which found a real tree instance
   proving "any" is the behaviour you want.
2. **`CLAUDE.md §1` miscited.** Retargeted to `docs/frontend.md:16-18`; I read
   that file's lines and the quoted text is there.
3. **Wrong symbol name.** The doc comment now says
   `citationTargetBareTestFilePattern`; `grep -rn citationTargetTestFileLikePattern`
   over the tree returns nothing.
4. **Fixture count 12 → 14.** Counted independently:
   `grep -c '  it(' web/src/lib/modalFocusWiring.structuralGuard.test.ts` = **15**,
   of which line 485 is the repo-wide scan and the other 14 are synthetic
   fixtures — so 14 is right. `git show a224d6b^:…` also returns 15, so it was
   never 12. Closed in `modalTabindexGuard.test.ts` itself by `ae9fb95` (both
   occurrences, "12" and "twelve"), which the fix pass had correctly flagged as
   outside its own file scope rather than silently leaving.
5. **Two garbled sentences.** Both read cleanly now.
6. **`t.Skip` → `t.Error`** in `TestPathCitationResolvesShortenedAndLeadingWordForms`:
   correct, and **not** a spurious failure. `docs/README.md` exists
   (`ls -la docs/README.md` — 2519 bytes), so `pathCitationResolves(paths,
   "docs/README")` is true and the branch never fires; the change only removes
   the `t.Skip`'s ability to abort the function before the load-bearing
   `web/HANDOFF` assertion. The test passes in the full run below. The only way
   the `t.Error` fires is the fixture genuinely disappearing, which is what it
   should report.

### The blocker: a new false claim in the new comment

`citation_target_guard_test.go`'s rewritten rule-6 comment says:

> That is the accepted cost of matching "any" rather than "every" … **No such
> instance exists in the tree today (the tree-wide run has zero
> "any-but-not-every" hits)**; "any" is kept rather than tightened to "every"
> because it is the direct generalisation of **the two- and three-segment real
> instances above**, which are themselves each exactly two names joined by "/"
> with no third, independently-checkable segment.

Two things there are wrong, and I checked mechanically rather than by reading.

**(a) The tree-wide run does not have zero "any-but-not-every" hits — it has
one.** Copied both guard files aside (no `git stash`, `CLAUDE.md` §8a), changed
each exclusion loop from first-match to an `every`-semantics predicate, and ran
the tree-wide scans:

```
--- FAIL: TestNoCommentCitesUnresolvedPathOrSection (0.10s)
    comment cites a repository path or CLAUDE.md section that does not resolve:
      internal/handlers/admin_workshops.go:25: [path] internal/seo/seo.go/sitemap.go
```

`admin_workshops.go:25` reads "*it only clears caches, see
internal/seo/seo.go/sitemap.go*" — a list citation meaning `internal/seo/seo.go`
**and** `sitemap.go`, both of which exist (`ls internal/seo/`). Its segments are
`internal` / `seo` / `seo.go` / `sitemap.go`: under "any" the third segment
excludes it correctly, under "every" the first two do not qualify and it is
wrongly flagged. So the instance both **falsifies the parenthetical** and
**vindicates the design choice more strongly than the comment does** — the fix
here makes the comment better, not weaker. On the TS side the same mutation
produced only the guard file's own line 179 (see (c)), i.e. no real hit there.

**(b) "the two- and three-segment real instances above" is wrong, and
self-contradictory.** The instances listed above it in that comment are
`campaigns_test.go/audience_test.go`, `Campaigns.svelte/CampaignEditor.svelte`
and `PRD/CLAUDE.md` — **all two-segment**. No three-segment real instance is
named there (the genuine one, `internal/seo/seo.go/sitemap.go`, is the very one
the comment claims does not exist). The trailing clause "which are themselves
each exactly two names joined by `/` with no third, independently-checkable
segment" contradicts "three-segment" in the same sentence.

This is `#0216`'s exact bounced class — a confident, mechanically checkable
claim about the repo that does not hold — landing inside the guard that exists
to prevent it, and it is one `grep`/one loop-flip away from being checked. It
also sits directly under criterion 4 ("say what the false-positive rule is"),
so the statement is load-bearing rather than decorative.

**What the next pass must do:**

1. In `internal/handlers/citation_target_guard_test.go`'s
   `pathCitationIsExcluded` doc comment, replace the "zero any-but-not-every
   hits" claim with the real instance and what it shows:
   `admin_workshops.go:25`'s `internal/seo/seo.go/sitemap.go` is a legitimate
   three-plus-segment list citation that "every" would false-positive on, which
   is the positive case for keeping "any". Say plainly that the residual cost is
   a *missed detection* (a citation whose first file-like segment excuses a
   dangling remainder), not a false alarm.
2. Fix "the two- and three-segment real instances above" — either list
   `internal/seo/seo.go/sitemap.go` above as the three-segment instance (better,
   since it is the evidence), or drop "three-segment" and the contradictory
   trailing clause.
3. Mirror whichever wording lands into `web/src/lib/citationTargetGuard.test.ts`'s
   equivalent comment, which currently defers to the Go file for "the full
   reasoning and the real probe".
4. Nothing else. The guards, the exclusions, the `t.Error` change and the
   fixture count are all correct — do not re-open them.

### One thing to consider while you are in there (not a blocker)

`citationTargetGuard.test.ts:179` spells out the literal token
`SomeFile.svelte/dir/gone.ts` inside a real comment. The Go file deliberately
avoided doing that ("not spelled out here as a literal slash-joined token, since
this comment is itself scanned by the guard below") and wrote
`AdminDotSvelte / lib / totallyNonexistentXyzDotTs` instead. The TS one is a
fabricated dangling path that passes the guard **only because rule 6's "any"
excuses it** — which is why it surfaced as the sole hit in my "every"-semantics
mutation. It is not a defect today, but it is a live counter-example sitting in
the file, and applying the Go file's own convention there would remove the
coupling.

### What I looked for and did not find

- Whether the fix pass changed guard *behaviour* while claiming only to change
  prose. It did not: `git show daea56f -- internal/handlers/citation_target_guard_test.go`
  touches only comment lines plus the `t.Skip` → `t.Error` line, and
  `ae9fb95` touches only two comment lines.
- Whether any of the previous review's five items was merely "responded to"
  rather than fixed — each is checked above against the tree, not the notes.
- Whether the `t.Error` conversion made the test fail today — it does not;
  `go test ./internal/handlers/` is green and `docs/README.md` is present.
- Whether the guards still pass after my mutations were reverted: both files
  restored `shasum -a 256` byte-identical (`9147b8b5…c005f296` and
  `1575467c…50a8ce1a`), and `ISSUE=0226rev scripts/check.sh` on the restored
  tree gives **16** Go packages `ok`, skip audit `(no skips)`,
  `internal/handlers` 3.351s / `internal/auth` 0.912s / `internal/audit`
  0.401s, `npm run check` 274 files / 0 errors, `npm test` 33 files / 3826
  tests. So criteria 7 and 8 hold; the bounce is on comment accuracy alone.

## Fix pass — 2026-08-24 (addressing the second phase-3 bounce)

**Status set to `in-progress`** (working copy only). `uptime` first: load
average 1.89 2.02 2.10 — idle.

### Reproduced the reviewer's finding before touching the comment

Copied `internal/handlers/citation_target_guard_test.go` aside (no `git
stash`, `CLAUDE.md` §8a), recorded its hash
(`9147b8b5…c005f296`, matching the review's own record), then changed
`pathCitationIsExcluded`'s exclusion loop (lines 282–286) from first-match
"any" semantics to a genuine "every" predicate (all intermediate segments
must qualify) and ran `go test ./internal/handlers/ -run
TestNoCommentCitesUnresolvedPathOrSection -v`:

```
--- FAIL: TestNoCommentCitesUnresolvedPathOrSection (0.09s)
    comment cites a repository path or CLAUDE.md section that does not resolve:
      internal/handlers/admin_workshops.go:25: [path] internal/seo/seo.go/sitemap.go
```

Exactly **one** hit, matching the reviewer's finding precisely. Confirmed
`admin_workshops.go:25`'s citation and that `internal/seo/seo.go` and
`internal/seo/sitemap.go` both exist (`ls internal/seo/`) — a legitimate list
citation of two real files, not a nested path. Restored the file from the
copy and reconfirmed byte-identity: `shasum -a 256` again gave
`9147b8b5…c005f296`.

### What changed

1. **`internal/handlers/citation_target_guard_test.go`**, `pathCitationIsExcluded`'s
   doc comment (the rule-6 block):
   - Added `internal/seo/seo.go/sitemap.go` to the list of real example
     citations above the exclusion rule, naming it explicitly as the
     three-segment case (directory prefix `internal/seo` plus two real
     files) and noting the excluding segment is the *third* in the list, not
     the first.
   - Replaced "the tree-wide run has zero any-but-not-every hits" with the
     real instance: flipping the loop to "every"-semantics finds **exactly
     one hit today, not zero** — the `internal/seo/seo.go/sitemap.go`
     citation — and explained what it shows: "every" would wrongly flag a
     legitimate real citation, which is the positive case for keeping "any",
     not evidence against it.
   - Replaced "the two- and three-segment real instances above, which are
     themselves each exactly two names joined by '/' with no third,
     independently-checkable segment" (self-contradictory, and wrong — all
     three previously-listed instances are two-segment) with a direct
     statement that the residual cost of "any" is a **missed detection** (a
     citation whose first file-like segment excuses a genuinely dangling
     remainder), not a false alarm.
2. **`web/src/lib/citationTargetGuard.test.ts`**, the mirror comment above its
   own exclusion loop:
   - Mirrored the corrected reasoning (checked tradeoff, one real hit, missed
     detection vs. false alarm) rather than leaving the previous version's
     bare deferral to the Go file unexplained.
   - Fixed the minor item the review flagged but did not block on: line 179
     spelled out the literal fabricated dangling path
     `SomeFile.svelte/dir/gone.ts` in a real comment — a live counter-example
     that only passed the guard because rule 6's "any" excuses it. Replaced
     it with the same non-literal phrasing the Go file already uses
     ("not spelled out here as a literal slash-joined token, since this
     comment is itself scanned by the guard below") plus a reference to the
     Go file's own synthetic example token, matching the Go file's
     convention exactly rather than leaving the TS file as the one place
     still carrying the coupling.

Nothing else changed — no exclusion logic, no `t.Error`/`t.Skip` behavior, no
fixture counts. The second review confirmed all five original defects and the
guard mechanism itself were already correct; this pass touches only the two
comment blocks named above.

### Verification

```
uptime                                                              # 1.89 2.02 2.10 — idle
go build ./...                                                       2>&1 | tail -40    # clean
go vet ./...                                                         2>&1 | tail -40    # clean
gofmt -l internal/handlers/citation_target_guard_test.go             # no output
go test ./internal/handlers/ -run 'TestNoCommentCitesUnresolvedPathOrSection|TestPathCitation' -v
  --- PASS: TestNoCommentCitesUnresolvedPathOrSection (0.09s)
  --- PASS: TestPathCitationResolvesShortenedAndLeadingWordForms (0.00s)
(cd web && npx vitest run src/lib/citationTargetGuard.test.ts)
  Test Files  1 passed (1)
  Tests  17 passed (17)          # includes "scans its own file (self-reference) and finds zero hits"
ISSUE=0220 scripts/check.sh                                           2>&1 | tail -60
scripts/check.sh web                                                  2>&1 | tail -60
```

`ISSUE=0220 scripts/check.sh`: Go packages all `ok`, skip audit clean.
`scripts/check.sh web`: `npm run check` **274 files / 0 errors**; `npm test`
**33 test files / 3,826 tests**, all passing — same baseline as the prior two
passes, confirming this pass changed only comments.

The mutation used to reproduce the reviewer's finding was reverted before any
of the above ran; `shasum -a 256` on `citation_target_guard_test.go`
confirmed byte-identity with the pre-mutation file
(`9147b8b5…c005f296`) before making the real (non-mutated) comment edit.

### Files changed

- `internal/handlers/citation_target_guard_test.go` — corrected the rule-6
  doc comment: named the real three-segment instance, replaced the false
  "zero hits" claim with the actual one-hit result and what it shows, and
  replaced the self-contradictory "two- and three-segment" generalisation
  with a plain missed-detection/false-alarm statement.
- `web/src/lib/citationTargetGuard.test.ts` — mirrored the corrected
  reasoning into the equivalent comment, and replaced the fabricated literal
  dangling path at line 179 with the same non-literal phrasing the Go file
  uses.

## Review notes — 2026-08-24 (third phase-3 review, BOUNCED)

**Status left at `in-progress`.** Commit `89a80ef` stands. Everything the
second bounce asked for on the **Go** side landed correctly and I verified it
mechanically. But the same fix pass wrote a **seventh false statement** — into
`web/src/lib/citationTargetGuard.test.ts`, in the very paragraph it added to
correct the sixth, and about a fact the previous review had already handed it.
`uptime` first: load average 2.42 — idle.

### The Go-side correction: reproduced, and right

Every cited fact in `citation_target_guard_test.go`'s rewritten rule-6 comment
checks out against the tree, read directly:

- `internal/handlers/admin_workshops.go:25` — the citation token
  `internal/seo/seo.go/sitemap.go` is physically on line 25, inside
  "*(it only clears caches, see internal/seo/seo.go/sitemap.go)*". ✓
- `internal/seo/seo.go` and `internal/seo/sitemap.go` both exist (`ls internal/seo/`). ✓
- "the excluding segment here is the THIRD in the list, `seo.go`, not the
  first" — segments are `internal` / `seo` / `seo.go` / `sitemap.go`, and
  `citationTargetFileLikeSegmentPattern`
  (`^[A-Za-z0-9_.\-]+\.(?:go|ts|tsx|svelte|md|…)$`) matches none of
  `internal`, `seo`. ✓
- The three other real instances it lists all exist where it says:
  `worker_test_helpers_test.go:2` (`campaigns_test.go/audience_test.go`),
  `campaign_markdown.go:14` (`PRD/CLAUDE.md §9`), and
  `web/src/lib/campaigns.ts:10` (`Campaigns.svelte/CampaignEditor.svelte`). ✓
- **The fabricated literal was genuinely removed, not moved.**
  `grep -rn 'SomeFile.svelte/dir/gone.ts'` over the whole tree returns hits
  only in `issues/0220.md`'s own historical prose — no source file carries it.
  The Go file's non-literal stand-in `AdminDotSvelte / lib /
  totallyNonexistentXyzDotTs` is present at
  `citation_target_guard_test.go:270`, so the TS comment's reference to it
  resolves. ✓

### Blocking — the TS comment's "one real hit" claim is false for the TS guard

`web/src/lib/citationTargetGuard.test.ts`, above its own exclusion loop:

> That residual cost is a MISSED detection, not a false alarm: **flipping this
> loop to "every"-semantics and re-running the tree-wide scan finds exactly one
> real hit**, and it argues FOR "any" rather than against it — a legitimate
> multi-file list citation that "every" wrongly flags as unresolved.

I ran exactly that, in a throwaway `git worktree` at HEAD with
`web/node_modules` symlinked (no `git stash`, shared tree untouched). Replaced
the first-match loop with a genuine `every(...)` predicate and ran the file:

```
Test Files  1 passed (1)
     Tests  17 passed (17)
```

**Zero hits.** The tree-wide test in this file is
`'no .ts/.svelte comment (test files included) cites a path or CLAUDE.md
section that does not resolve'` — it walks `.ts` and `.svelte` only
(`rel.endsWith('.svelte') ? collectSvelteCommentBlocks : collectTsCommentBlocks`).
The one real hit lives in `internal/handlers/admin_workshops.go`, a **`.go`**
file this guard never opens. And the second review had already established this
and said so in as many words: *"On the TS side the same mutation produced only
the guard file's own line 179 …, i.e. **no real hit there**."* This pass then
deleted line 179's literal — making the TS side genuinely zero — and in the
same edit wrote a comment claiming one.

The mutation was faithful, not a broken probe: the two-segment exclusion tests
(`PRD/CLAUDE.md §9`, `campaigns_test.go/audience_test.go`) still passed under
it, which is the expected behaviour since "any" and "every" coincide at two
segments. Restored from a `cp` copy taken first; `shasum -a 256` identical
(`e971945086fca5650845a819b69b8a5c435845f280e4bc19e479ea5205cf71e3`).

This is the third consecutive bounce for the same thing, in the issue whose
premise is that a shipped comment must be true, and this time the correct fact
was already written in the review notes directly above the fix pass.

### Also wrong, smaller — the Go comment's "exactly one hit" no longer reproduces

`citation_target_guard_test.go:282–284` says flipping the loop *"finds exactly
one hit today, not zero"*. Reproduced in the worktree with the same
`every`-semantics flip:

```
--- FAIL: TestNoCommentCitesUnresolvedPathOrSection (0.10s)
      internal/handlers/admin_workshops.go:25:                internal/seo/seo.go/sitemap.go
      internal/handlers/citation_target_guard_test.go:257:    internal/seo/seo.go/sitemap.go
      internal/handlers/citation_target_guard_test.go:259:    internal/seo/seo.go/sitemap.go
      internal/handlers/citation_target_guard_test.go:284:    internal/seo/seo.go/sitemap.go
```

**Four hits, not one.** Three of them are this comment's own new prose, which
now spells the token out three times — the claim was made false by the act of
writing it. One *distinct citation*, four *hits*. The substance ("every" would
false-positive on a real citation; "any" is the right choice) is correct and I
confirmed it; only the count is wrong. Restored; `shasum -a 256` identical
(`a03821d9898b7401380d840b0ab6b35e3ae2f7836b1d7de18737627014430d40`).

### What the next pass must do

1. **`web/src/lib/citationTargetGuard.test.ts`** — say what is true of *this*
   guard: flipping this loop to "every"-semantics leaves its own tree-wide scan
   green, because the only real instance in the repo
   (`internal/handlers/admin_workshops.go:25`) is in a `.go` file this guard
   does not scan; the evidence for keeping "any" is the Go guard's, cited as
   such. Do not repeat a hit count as if this file could observe it.
2. **`internal/handlers/citation_target_guard_test.go:282–284`** — "exactly one
   hit" → one *distinct citation*, and note that the comment's own three
   quotations of the token now also match, so a reproduction sees four lines.
   Or drop the count and name the citation.
3. Nothing else. The guards, all six exclusions, the `t.Error` change, the
   fixture count and every path/section citation are correct — do not re-open
   them.

### What I looked for and did not find

Whether the fix pass changed behaviour while claiming to change only prose
(`git show 89a80ef` touches comment lines only — the loops are byte-identical
in the diff); whether the `AdminDotSvelte / lib / …` reference dangles (it
resolves to line 270); whether the Go file's own new literal
`internal/seo/seo.go/sitemap.go` is itself a dangling citation (it is a real
two-file list citation, correctly excluded by rule 6 under "any"); whether
`SomeFile.svelte/dir/gone.ts` survived anywhere in source (it did not).
`git status --porcelain -- web/dist` empty; no `go test`/`.test` processes left.

## Fix pass — 2026-08-24 (addressing the third phase-3 bounce)

This is a bounce-fix on a file whose premise is "a shipped comment must be
true," bounced twice already for new false statements introduced while
fixing the last one. Reproduced both counts myself, from scratch, before
writing a single word of prose — in a throwaway `git worktree` at HEAD with
`web/node_modules` symlinked (no `git stash`, shared tree untouched),
exactly as the last two reviews did. `uptime` first: load average
1.61-2.08 across this pass — idle.

### Reproduced first: the TS-side claim (7th false statement)

Copied `web/src/lib/citationTargetGuard.test.ts` into the worktree, flipped
its `some`-shaped loop (the one directly below the comment in question) to
a genuine `every()` predicate, and ran the file:

```
Test Files  1 passed (1)
     Tests  17 passed (17)
```

**Zero hits** — the tree-wide "no .ts/.svelte comment ... cites a path ...
that does not resolve" test in this file stayed green. This matches both
the second review's own finding ("no real hit there") and the third
review's reproduction exactly. The old comment's claim ("finds exactly one
real hit") was false for THIS guard: the one real citation the "any" vs
"every" split matters for, `internal/handlers/admin_workshops.go:25`
("...see internal/seo/seo.go/sitemap.go"), is in a `.go` file this guard
never opens (it walks only `.ts`/`.svelte` comments, via
`collectTsCommentBlocks`/`collectSvelteCommentBlocks`).

### Reproduced second: the Go-side claim ("exactly one hit")

Same technique, on `internal/handlers/citation_target_guard_test.go`
(before my edit, i.e. the file as the second fix pass left it): replaced
the `for`-loop-with-early-return in `pathCitationIsExcluded` with an
explicit "require every intermediate segment to qualify" version, and ran
`TestNoCommentCitesUnresolvedPathOrSection`:

```
--- FAIL: TestNoCommentCitesUnresolvedPathOrSection (0.10s)
      internal/handlers/admin_workshops.go:25:                internal/seo/seo.go/sitemap.go
      internal/handlers/citation_target_guard_test.go:257:    internal/seo/seo.go/sitemap.go
      internal/handlers/citation_target_guard_test.go:259:    internal/seo/seo.go/sitemap.go
      internal/handlers/citation_target_guard_test.go:284:    internal/seo/seo.go/sitemap.go
```

**Four hits**, exactly matching the third review's reproduction: one real
citation (`admin_workshops.go:25`) plus three of the comment's own
quotations of the identical literal string — the comment quoting the token
it's describing had turned itself into three additional matches, since this
`_test.go` file is itself walked by the guard.

### The fix: stop quoting the literal, don't just recount it

Both prior bounces treated this as a counting problem ("say the right
number"). That is fragile by construction: the moment prose quotes a
matchable literal, the count depends on how many times the prose happens to
repeat it, which changes on every edit to the comment itself — a trap that
already caught two consecutive passes. The file's OWN existing text already
demonstrates the correct pattern, for a different example: the
`AdminDotSvelte / lib / totallyNonexistentXyzDotTs` synthetic citation is
deliberately "not spelled out here as a literal slash-joined token, since
this comment is itself scanned by the guard below." I applied that same
technique to the `internal/seo/seo.go/sitemap.go` citation, which the
previous two passes had NOT applied it to — that inconsistency is exactly
why this file kept re-triggering itself.

**`internal/handlers/citation_target_guard_test.go`**: removed all three
literal quotations of `internal/seo/seo.go/sitemap.go` from the comment
prose (they described the citation as a real example in the general
"multiple filenames joined by /" enumeration, and again in the
every-semantics discussion); replaced them with a description that refers
to `admin_workshops.go:25` by file:line instead of repeating the matchable
string. The "exactly one hit" claim is now written as "surfaces
admin_workshops.go:25's own three-segment citation ... as a failure," with
an explicit parenthetical explaining WHY no count is stated: quoting the
literal would make any count self-referential. Re-verified after the edit:
`grep -n "seo\.go/sitemap\.go" internal/handlers/citation_target_guard_test.go`
returns nothing (the literal no longer appears anywhere in the file), and
re-running the exact same every-semantics mutation against the EDITED file
in the worktree now produces exactly the one real hit
(`admin_workshops.go:25`), not four — proving the fix, not just asserting
it.

**`web/src/lib/citationTargetGuard.test.ts`**: this file's own claim was
different in kind, not just in count — it can never observe the Go-side
hit at all, because it doesn't scan `.go` files. Rewrote the comment to say
exactly that: flipping this file's own loop to every-semantics and
re-running THIS file's own tree-wide scan stays green (zero hits, not one),
which "argues nothing either way about the citation itself"; the actual
evidence for keeping "any" is the Go guard's reproduction, cited by
file — not restated as a number this file cannot itself produce.

### Verification

```
uptime                                                                 # 1.61-2.08 -- idle
grep -n "seo\.go/sitemap\.go" internal/handlers/citation_target_guard_test.go   # no output
go build ./...                                                         2>&1 | tail -40   # clean
go vet ./...                                                           2>&1 | tail -40   # clean
gofmt -l internal/handlers/citation_target_guard_test.go               # no output
go test ./internal/handlers/ -run 'TestNoCommentCitesUnresolvedPathOrSection|TestNoCommentCitesUndefinedTestFunction' -v
  --- PASS (both)
(cd web && npx vitest run src/lib/citationTargetGuard.test.ts)
  Test Files  1 passed (1) / Tests  17 passed (17)
ISSUE=0237fix scripts/check.sh go ./internal/...                       2>&1 | tail -80
scripts/check.sh web                                                   2>&1 | tail -80
```

`ISSUE=0237fix scripts/check.sh go ./internal/...` (run jointly with
`#0237`'s fix, same session, same package): all 15 packages `ok`, zero
skips, `VERIFICATION PASSED`. `scripts/check.sh web`: `npm run check`
**274 files / 0 errors**, `npm test` **33 test files / 3,829 tests** (the
one net-new test is `#0237`'s, not this issue's — this fix pass changed
only comments, no test bodies, no assertion counts).

**Reproduced both false counts before fixing, and both true counts after**,
in a throwaway `git worktree` (removed with `git worktree remove --force`
after use; `git worktree list` afterward shows only the main checkout and
an unrelated pre-existing `/private/tmp/oc-live`) — never touching the
shared tree while mutating. Both real files were edited directly in the
main checkout (only their comments; the loops themselves are
byte-identical to before, confirmed by inspection — same code, same
behavior, only prose changed), so there is no before/after hash to prove
here in the way `#0237`'s guard-logic changes needed one: nothing
executable in either file changed.

Ran `pgrep -f 'go test|\.test '` before finishing: none of my own
processes left running. `git status --porcelain -- web/dist` empty.

### Files changed

- `internal/handlers/citation_target_guard_test.go` — removed the three
  self-matching literal quotations of `internal/seo/seo.go/sitemap.go` from
  `pathCitationIsExcluded`'s doc comment; replaced the false "exactly one
  hit today, not zero" claim with a description that names the citation by
  file:line instead of a count, and explains why no count is stated.
- `web/src/lib/citationTargetGuard.test.ts` — replaced the false "finds
  exactly one real hit" claim with the true statement: this guard's own
  tree-wide scan cannot observe the Go-side hit at all (it never opens
  `.go` files), so it stays green under the same mutation, and the actual
  evidence for "any" over "every" is the Go guard's reproduction, cited by
  file rather than restated as a number.

### Gotchas

The count-quoting trap is structural, not a one-off mistake: ANY doc
comment in a `_test.go` file this guard walks that quotes a string shaped
like a real citation becomes one more match the guard itself will find,
changing the true count on every edit to the comment. The fix that holds up
is not "recompute the number correctly" (which is what both prior bounces
did, and both got wrong or fragile) but "stop making the prose part of what
gets counted" — describe citations by file:line reference when discussing
them abstractly, reserve literal quotation for the one place a real
instance is actually being cited functionally (which this file already did
correctly for the `AdminDotSvelte` synthetic example; it just hadn't been
applied consistently to the `admin_workshops.go:25` real one).

## Review notes — 2026-08-24 (fourth phase-3 review, BOUNCED)

**Status left at `in-progress`.** `uptime` first: load average 3.45 — idle.
Commit `045b810`'s **technique is right and its two headline claims reproduce
exactly** — I checked both by running them, not by reading them. The bounce is
on one sentence the fix pass rewrote, which now contradicts itself.

### Reproduced, exactly

**The literal is gone from both files, checked as bytes** (not with `grep -P`,
per `CLAUDE.md` §8):

```python
tok = 'internal/seo/seo.go/sitemap.go'
open('internal/handlers/citation_target_guard_test.go','rb').read().count(tok) == 0
open('web/src/lib/citationTargetGuard.test.ts','rb').read().count(tok)      == 0
```

Both zero; `seo.go/` and `/seo/` are zero in both files too. Tree-wide, the
literal now survives at exactly one place — `internal/handlers/admin_workshops.go:25`
— which is the real citation the comments point at, and I read line 25 to
confirm it says what they say it says.

**The Go-side count, before and after.** In a detached `git worktree` (never
the shared tree), I flipped `pathCitationIsExcluded`'s loop to `every`-semantics
and ran `TestNoCommentCitesUnresolvedPathOrSection`:

| tree | hits |
|---|---|
| `045b810~1` (before the fix) | **4** — `admin_workshops.go:25`, plus `citation_target_guard_test.go:257`, `:259`, `:284`, all three the comment quoting itself |
| `045b810` (after the fix) | **1** — `admin_workshops.go:25` alone |

That is precisely the diagnosis the commit message gives, precisely the remedy
it claims, and it works. The self-quoting trap was real and is closed.

**The TS-side claim.** Same worktree, flipped `citationTargetGuard.test.ts`'s
own loop to `every`-semantics, ran `npx vitest run src/lib/citationTargetGuard.test.ts`:
**17 passed (17)**, zero hits. The rewritten comment's statement — *"re-running
THIS file's own tree-wide scan therefore stays green — zero hits, not one"* —
is true, and the structural reason given for it is true: the guard dispatches
on `rel.endsWith('.svelte') ? collectSvelteCommentBlocks : collectTsCommentBlocks`
(line 459) and never opens a `.go` file, so it cannot observe the one citation
that matters. Both files restored from a `cp` taken first;
`shasum -a 256` identical
(`efdb856bcb7e9ac289b2cb5abd2f9042e8df93305cb263b605427cf60aa99d2d` Go,
`42f8dce6377d129fe81fed657e0ff3e75141a3e558cbb15c0a4eb3ccae925bc3` TS),
worktree removed.

**Only comments changed.** `git show 045b810` confirms the two `for` loops are
untouched in both files; every changed line is a comment line.

**Criteria re-checked.** Criterion 7: `modalFocusWiring.structuralGuard.test.ts:511`
asserts `sites.length >= 11` and passes. Criteria 6 and 7 together:
`modalFocusWiring.structuralGuard.test.ts`, `modalTabindexGuard.test.ts` and
`citationTargetGuard.test.ts` — **38 passed (38)**. Criterion 8:
`ISSUE=0237rev2 scripts/check.sh` — `VERIFICATION PASSED`, 16 Go packages `ok`,
**zero skips**, DB-backed packages in real time; `npm run check` **274 files /
0 errors**; `npm test` **33 test files / 3,829 tests**.

### Blocking — the replacement sentence contradicts itself about segment count

The fix removed the literal and replaced it with a description. That
description is now the *only* account of the citation a reader has, and it is
wrong in one half:

> …or admin_workshops.go:25's own **three-segment** list citation (… — see that
> file's line 25 for the real text: a directory prefix, then two real files,
> seo.go AND sitemap.go; the excluding segment there is the **THIRD list
> member**, "seo.go", not the first).

`strings.Split("internal/seo/seo.go/sitemap.go", "/")` yields **four**
segments: `internal`, `seo`, `seo.go`, `sitemap.go`. The two halves of that
sentence cannot both be true:

- read as four slash segments — the reading the file itself uses fourteen lines
  later (*"its first two segments (`internal`, `seo`) are plain directory
  names"*) and the reading `pathCitationIsExcluded` operates on — `seo.go` is
  indeed the THIRD, and "three-segment" is false;
- read as a three-member list (`internal/seo` prefix, `seo.go`, `sitemap.go`) —
  "three-segment" is defensible, and `seo.go` is the SECOND, so "THIRD list
  member" is false.

The file already fixes the vocabulary elsewhere: it calls
`AdminDotSvelte / lib / totallyNonexistentXyzDotTs` "a synthetic **three-segment**
citation", and that one genuinely has three slash segments. Against that usage,
`internal/seo/seo.go/sitemap.go` is four. Both occurrences of "three-segment"
in the comment (in the exclusion enumeration and again in the every-semantics
paragraph) need the same correction, as does the `## Fix pass` prose that
quotes it.

This is small and one word wide. It is blocking only because it is the same
failure mode as the three prior bounces — prose about a citation carried
forward from an earlier phrasing without being re-checked against the thing it
describes — and because removing the literal makes this sentence load-bearing
in a way it was not before.

### Non-blocking

- **"the AdminDotSvelte example two paragraphs up" does not resolve to a
  paragraph.** Lines 247–301 of `citation_target_guard_test.go` contain no
  blank `//` line, so godoc/gofmt see the whole block as continuous prose; the
  AdminDotSvelte example is 19 lines up, in the same paragraph. "Earlier in this
  comment" would be true.
- **The parenthetical's stated reason for omitting a count is now weaker than
  the count itself.** With zero self-quotations left, the true count is a stable
  `1` and could simply be stated with the `admin_workshops.go:25` attribution
  that follows it. The forward-looking rationale (a future edit that re-quotes
  the token would move the number) is fair, so this is a preference, not a
  defect.
- **`## Fix pass` says the reproduction ran with `ISSUE=0237fix scripts/check.sh
  go ./internal/...` … all 15 packages `ok`.** That is `./internal/...` only;
  the whole-tree figure including `cmd/opencircuit` is 16. Both are right for
  their scope — noting it so a later pass does not read the two as contradicting.

### What I looked for and did not find

Whether the loops' behaviour changed under the comment-only edit (it did not —
the diff touches no executable line, and both guards pass); whether any *other*
matchable literal remains quoted in either comment that would move a future
count (I scanned both files for `seo.go/`, `/seo/`, and slash-joined tokens —
none); whether the TS file's claim that it "never opens `.go` files" is
actually enforced rather than incidental (it is: `scanFile`'s only two
collectors are the `.ts` and `.svelte` ones); whether `web/dist/index.html`
moved (`git status --porcelain -- web/dist/` empty, hash
`7bb5f85694324910da8dd9c65fe1d266d28ee43612865248e289155fd2ee7ded`).

Cleanup: worktree removed (`git worktree list` shows only the main checkout and
an unrelated pre-existing `/private/tmp/oc-live`); `scripts/testdb.sh list`
shows only the template; `pgrep -f 'go test|\.test '` empty.

## Fix pass — 2026-08-24 (addressing the fourth phase-3 bounce)

`uptime` first: load average 1.87–2.55 throughout — idle. Verified the
reviewer's segment-count claim before writing anything:

```
python3 -c "print('internal/seo/seo.go/sitemap.go'.split('/'))"
# ['internal', 'seo', 'seo.go', 'sitemap.go']  -- four segments, not three
```

The review is right on both halves of its own reading: as four `/`-separated
segments, `seo.go` is genuinely the third (index 2), so "THIRD list member"
was correct; calling the whole citation "three-segment" in the same sentence
was not. And read as "a directory prefix, then two files" (three CONCEPTUAL
parts), `seo.go` would be the SECOND part, not the third — so no single
segment-counting convention makes both halves of the original sentence true
at once, exactly as the review demonstrated.

### The fix

`internal/handlers/citation_target_guard_test.go`'s `pathCitationIsExcluded`
doc comment, both places it named `admin_workshops.go:25`'s citation:

- "own three-segment list citation" → "own FOUR-segment list citation",
  with the parenthetical rewritten to spell out the count concretely (a
  directory prefix of two plain-name segments, `"internal"` and `"seo"`, then
  two real files, `seo.go` AND `sitemap.go` — four `"/"`-separated segments
  in total) so the number is no longer implicit.
- "surfaces admin_workshops.go:25's own three-segment citation above as a
  failure" → "own four-segment citation above as a failure" — the second
  occurrence the review named.
- "the THIRD list member" is left as-is: verified true against the
  corrected four-segment count (`seo.go` is `segments[2]` of `["internal",
  "seo", "seo.go", "sitemap.go"]`), so no change was needed there — only the
  segment-count label around it was wrong.

The file's own usage elsewhere in this same comment (the `AdminDotSvelte /
lib / totallyNonexistentXyzDotTs` synthetic example, genuinely three `/`-
separated segments, called "three-segment" both times it appears) is
unaffected and untouched — it was already correct and remains the standard
this fix now matches consistently.

### Non-blocking 1 — "two paragraphs up" replaced

Confirmed the review's finding directly: `awk` over lines 247–302 (this
comment block) found no blank `//`-only line, so it is one continuous
paragraph in the doc-comment sense, and the AdminDotSvelte example sits 19
lines above the "two paragraphs up" reference (a distance that would only
have drifted further as this and prior passes added lines to the same
comment). Replaced "the AdminDotSvelte example two paragraphs up" with "the
AdminDotSvelte example earlier in this comment" — accurate regardless of
exact line distance, so it cannot go stale the same way again from a future
edit to this same block.

### Non-blocking 2 — the stable count of 1, stated directly

With the literal `internal/seo/seo.go/sitemap.go` genuinely absent from both
guard files (confirmed again below, as bytes), the every-semantics hit count
for the Go guard's own tree-wide scan is no longer self-referential and is a
stable, statable **1**. Rewrote the parenthetical that previously declined to
name a count to instead say so directly: "the every-semantics hit count IS
stable, and it is exactly one: `admin_workshops.go:25`'s citation, and
nothing else in the tree" — replacing the more defensive "deliberately not
naming a hit COUNT here" framing, which was the right caution while the
literal was still quoted nearby but is now stronger than the situation calls
for.

`web/src/lib/citationTargetGuard.test.ts` needed no equivalent change: it
never claimed a segment count or a "two paragraphs up" position for this
citation (its own comment, corrected in the prior fix pass, already states
only that ITS tree-wide scan cannot observe the Go-side hit at all — a claim
that does not depend on segment counting and was not touched by this
review).

### Reproduced after the fix, not just before

```
grep -n "seo\.go/sitemap\.go" internal/handlers/citation_target_guard_test.go   # no output (unchanged from the prior pass)
```

```python
tok = 'internal/seo/seo.go/sitemap.go'
open('internal/handlers/citation_target_guard_test.go','rb').read().count(tok.encode())  # 0
open('internal/handlers/citation_target_guard_test.go','rb').read().count(b'seo.go/')    # 0
```

Both zero, confirmed as bytes (`CLAUDE.md` §8: not `grep -P`). Mutation-proved
the "every"-semantics claim still reproduces exactly one hit against the
EDITED file, in the main checkout (copy-mutate-restore, no `git stash`,
`CLAUDE.md` §8a): flipped `pathCitationIsExcluded`'s loop to a genuine `every`
predicate and ran `TestNoCommentCitesUnresolvedPathOrSection` —

```
--- FAIL: TestNoCommentCitesUnresolvedPathOrSection (0.09s)
      internal/handlers/admin_workshops.go:25: [path] internal/seo/seo.go/sitemap.go
```

**Exactly one hit**, matching the corrected comment's claim precisely (not
four, since the fix pass before this one already removed the self-quoting
literal, and this pass added no new quotation). Restored from a `cp` copy
taken first; `shasum -a 256` identical
(`f6cb524ce5b8c963998afae28c44261a20b04c58aa4c21aa7e8b8d7165912fdf`) before
and after.

### Verification

```
uptime                                                                 # 1.87-2.55 -- idle
go build ./...                                                        2>&1 | tail -40   # clean
go vet ./...                                                          2>&1 | tail -40   # clean
gofmt -l internal/handlers/citation_target_guard_test.go              # no output
go test ./internal/handlers/ -run 'TestNoCommentCitesUnresolvedPathOrSection|TestCitationTarget|TestPathCitationResolves' -v
  --- PASS (all 5)
(cd web && npx vitest run src/lib/citationTargetGuard.test.ts)
  Test Files  1 passed (1) / Tests  17 passed (17)   # unchanged — no TS edit was needed this pass
ISSUE=0220fix4 scripts/check.sh go ./internal/...                     2>&1 | tail -60
scripts/check.sh web                                                  2>&1 | tail -60
```

`ISSUE=0220fix4 scripts/check.sh go ./internal/...`: all 15 packages `ok`,
zero skips, `VERIFICATION PASSED`. `scripts/check.sh web`: `npm run check`
**274 files / 0 errors**; `npm test` **33 test files / 3,829 tests** —
unchanged from baseline, since this pass touched only Go-file comments.

Own database `opencircuit_test_0220fix4`, dropped by `scripts/check.sh` on
exit. `pgrep -f 'go test|\.test '` empty after finishing. `git status
--porcelain -- web/dist/` empty throughout; `web/dist/index.html` hash
unchanged (`7bb5f85694324910da8dd9c65fe1d266d28ee43612865248e289155fd2ee7ded`).

### Files changed

- `internal/handlers/citation_target_guard_test.go` — corrected
  `pathCitationIsExcluded`'s doc comment: `admin_workshops.go:25`'s citation
  is now called "four-segment" (it has four `/`-separated segments), not
  "three-segment", in both places that claim appeared; "the AdminDotSvelte
  example two paragraphs up" → "earlier in this comment"; the previously
  deferred hit count is now stated directly as a stable `1`. No executable
  line changed — the `for` loop implementing "any"-semantics is
  byte-identical to before this pass.
- `issues/0220.md` — this `## Fix pass` section and its `## Work log` row.

### What I looked for and did not find

Whether "THIRD list member" needed to change alongside "three-segment" (it
did not — verified against the corrected four-segment count directly: `seo.go`
is index 2 of `["internal", "seo", "seo.go", "sitemap.go"]`, the third
element); whether any OTHER matchable literal now sits in either guard file's
comments that would move a future every-semantics count (scanned both files
for `seo.go/`, `/seo/`, and slash-joined tokens beyond the ones already
accounted for — none new); whether the TS mirror comment needed the same
"three-segment" correction (it does not make that claim at all — checked with
`grep -n "three-segment\|four-segment" web/src/lib/citationTargetGuard.test.ts`,
zero hits); whether this pass's own new prose introduces a fresh checkable
claim that doesn't hold (re-read every sentence changed against what it
describes: the four-segment count, the "third of four" position, the 19-line
distance now stated as "earlier in this comment" rather than a number, and the
stable-1 count — all reproduced above, not asserted). `web/dist/index.html`
untouched (`git status --porcelain -- web/dist` empty, hash
`7bb5f85694324910da8dd9c65fe1d266d28ee43612865248e289155fd2ee7ded`, matching
the third review's own record).
