# 0461 — The next divergence family is whitespace and quoting, and the scan's oracle does not model it

| | |
|---|---|
| **Status** | resolved |
| **Platform** | Server |
| **Module** | config / deploy |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |
| **Closed** | 2026-09-05 |
| **Commit** | `6d102ab`, `674d65b` |

## Description

`#0457` closed the backslash family. `#0457`'s reviewer then swept an **extended
alphabet** — `#0453`'s original set plus `'`, `"`, space, `#` and a lowercase
letter, 54,240 values — through the real `godotenv.Unmarshal` and the scan.

Result: still **zero false positives**, and **4,155 unflagged divergences — all
of which contain a space, and none of which contain a backslash.** So the
backslash family really is closed, and the next family is whitespace and
quoting.

The reviewer also identified the structural reason, which is more useful than
the count: **the scan's oracle does not model quote stripping at all.** It
treats systemd as keeping the literal bytes, which is true for an unquoted
value and not for a quoted one. That gap is pre-existing and applies to **all
four shapes**, not just this new family.

## Why it is not urgent

`.env.example` holds 28 quote bytes today and **all of them are inside full-line
comments the scan skips**, so nothing in the file is currently mis-modelled.
This is about the next line someone writes — the same footing `#0450`, `#0453`
and `#0457` were on.

## The thing to decide before writing code

Whether to model quoting properly or to **refuse quoted values outright** in the
production template. Refusal is cruder and much easier to get right; modelling
means encoding two parsers' quote rules and keeping them in step, which is how
this lineage has produced four issues so far.

`#0453`'s reviewer noted godotenv's single-versus-double-quote split governs
expansion, and `#0457`'s confirmed it governs escapes identically — so the split
is at least consistent. systemd's rules are not the same ones.

**Record the decision either way**; do not build both.

## Acceptance criteria

1. Decide and record: model quoting, or forbid quoted values in `.env.example`.
2. Whichever you choose, the whitespace-driven divergences the extended-alphabet
   sweep found are either flagged or explicitly out of scope with a stated
   reason. **Re-derive the 4,155 figure** rather than copying it — every number
   inherited without measurement in this lineage has been wrong at least once.
3. **Verify by probing `godotenv.Unmarshal` at runtime**, never by reading its
   regex. Read the module via `go list -m -f '{{.Dir}}' github.com/joho/godotenv`; never scan the filesystem (`CLAUDE.md` §5).
4. **No false positives**, measured over both the original and extended
   alphabets, and state both counts. A false positive fires on the file that is
   copied verbatim onto production (`#0423`).
5. **Do not touch** the floor (`minScannedEnvExampleAssignments`),
   `checkScannedFloor`, or the four-shape structure. Four reviews have now
   verified that machinery, including that the fail-closed proof passes the
   **package constant** rather than a hardcoded number — which is why §8's
   external-harness rule does not apply.
6. **No new mechanism** beyond what the decision in criterion 1 strictly
   requires (§8, `#0258`).

## Relation

- `#0441` built the scan; `#0450`, `#0453` and `#0457` widened it; `#0457`'s
  review measured this family and named the oracle gap.
- `#0458` tracks the missing regression pins for all the widenings — check
  whether it should land first, so this one starts from a pinned baseline.
- `#0423` made `.env.example` the production template via a literal `cp`.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/config/... ./internal/db/... ./internal/handlers/...`. Scan bytes with `python3`, never BSD `grep -P`.

## Decision (criterion 1)

**Forbid quoted values outright, rather than modelling quoting.** A value is
"quoted," in this file's new fifth check, exactly when `joho/godotenv`
v1.5.1's own `hasQuotePrefix` (`parser.go`) would treat it that way: its
first non-whitespace character (after the same leading-whitespace trim
`locateKeyName` performs) is `'` or `"`.

Reasoning, read from `parser.go`'s `extractVarValue` and confirmed by probing
`godotenv.Unmarshal` at runtime (never by reasoning from the source alone —
`CLAUDE.md` §5):

- A quoted value's parsing is not a small variation on the unquoted path the
  other four checks assume — it is a different pipeline entirely. Only the
  quote character itself is trimmed (not whitespace), a single-quoted value
  gets **no** escape processing or expansion at all, and a double-quoted
  value runs `expandEscapes` (which can turn `\n`/`\r` into real control
  bytes, and silently drops the backslash from any other `\x` escape except
  `\$`) **before** `expandVariables`. Probed directly:

  | raw | godotenv loads | note |
  |---|---|---|
  | `'  x  '` | `"  x  "` | interior whitespace preserved — unquoted trims it |
  | `'cost$FOO'` | `cost$FOO` | single quote: **no** expansion, unlike unquoted `cost$FOO` |
  | `"cost$FOO"` | `cost` | double quote: expansion still runs |
  | `"cost\n"` | `cost` + a real newline byte | double-quote escape processing, no unquoted equivalent |
  | `"cost\t"` | `costt` (literal `t`, backslash stripped) | `unescapeCharsRegex` only spares `\n`, `\r`, `\$` |
  | `'unterminated` | parse **error** | boot failure, not a silent divergence — the `#0429` half of this bug class |
  | `abc'def'` | `abc'def'` unchanged | quote NOT at position 0 → **not** quoted at all; unquoted checks still apply correctly |

- `systemd`'s `EnvironmentFile=` quoting grammar is a separate grammar, not
  the one `#0450`'s, `#0453`'s and `#0457`'s reviews verified is at least
  internally consistent for `godotenv`'s own expansion and escaping — and
  this repository has no `systemd` parser to probe the way criterion 3
  requires for `godotenv`. There is no way to state, for an arbitrary
  quoted value, whether the two parsers would actually agree.
- Modelling both grammars and keeping them in lockstep is exactly the
  pattern that produced five issues in this lineage (`#0441`, `#0450`,
  `#0453`, `#0457`, this one) for **one** mechanism — dollar expansion —
  alone. Refusal is cruder, but it is a policy decision this repo can
  actually verify (does a value start with a quote character or not?)
  rather than a divergence prediction it cannot.
- This closes more than the named family: since `envTrailingComment`,
  `envDollarExpansion`, and the trailing-backslash check all read the raw
  value text irrespective of quoting, each could misfire or mis-describe a
  quoted value today (e.g. `"cost # not a comment"` — double-quoted, no
  comment stripping at all inside quotes — loads as `cost # not a comment`
  verbatim, yet the pre-existing trailing-comment check would still call it
  a stripped comment). Forbidding quoted values outright and reporting on
  them **before** the other three checks run (see the `continue` in
  `scanEnvExampleValueShapes`) means those three now only ever see confirmed
  -unquoted text — the domain their own doc comments already assumed.

**This is a genuine fifth shape, not a widening of the `$VAR` family** —
unlike `#0453`'s and `#0457`'s extensions, it shares no mechanism with
"`godotenv` expands a bare `$`, `systemd` doesn't." `minScannedEnvExampleAssignments`,
`checkScannedFloor`, and the four original shapes' own matching logic and
messages are untouched (criterion 5) — confirmed by diff: the only changes
are one new regexp variable, one new `if`/`continue` block inserted between
the existing `export`-prefix check and the existing `envTrailingComment`
check, one new row in the per-shape table, and doc-comment additions.

## Criterion 2 — re-derived divergence count

**Not copied.** `#0457`'s cited "4,155 unflagged, all containing a space,
none containing a backslash" could not be exactly reproduced because its
exact corpus construction is not fully specified in its write-up. Two
independent re-derivations were built instead (throwaway module outside this
repo, `github.com/joho/godotenv v1.5.1`, the real dependency):

**Corpus.** Combinations of length 1–4 over the 15-character extended
alphabet (`#0453`'s ten — `$ ( ) { } A 5 _ \ x` — plus `#0457`'s five: `'`,
`"`, space, `#`, one lowercase letter): `15¹+15²+15³+15⁴ = 54,240` values,
matching the cited corpus size exactly. Unlike the earlier three widenings'
"`cost`-prefixed suffix" corpus (`10¹+…+10⁵ = 111,110`, reproduced below), a
`cost`-prefixed corpus **cannot** exercise quoting at all: `hasQuotePrefix`
only ever looks at the value's first character, and "`cost`" always occupies
that position. So this corpus uses each combination **as the whole value**,
which is the only construction that can put a quote character at position 0.

**Sanity check against the already-published figure.** Run the same
methodology (combinations as the whole value, no prefix) over `#0453`'s
**original** ten-character alphabet at length 1–5 (111,110 values, no quote
characters in that alphabet at all): **17,661 divergent, 17,661 flagged, 0
unflagged** — reproducing `#0457`'s own cited total (`14,637 + 3,024 =
17,661`) exactly. This gives confidence the comparator model below is sound.

**Results, extended alphabet, 54,240 values, pre-`#0461` guard (four
shapes). Systemd oracle: trims leading and trailing whitespace — not the
raw-bytes model `envDollarExpansion`'s doc comment names:**

| | conservative model | charitable model |
|---|---|---|
| quoted values (leading quote char) | 738 | 738 |
| divergent (by that model's definition) | 3,754 | 3,378 |
| flagged by the four existing shapes | 3,030 | 3,024 |
| **unflagged** | **724** | **354** |
| — of which begin with a quote character | **724 (100%)** | **354 (100%)** |
| — of which contain a space | 160 | 109 |
| — of which contain a backslash | 26 | 14 |

Two comparator models, both applied to the SAME corpus, because there is no
way to know which (if either) matches `systemd`'s actual behavior for a
quoted value (the whole point of criterion 1):

- **Conservative**: any quoted value is a divergence candidate — `godotenv`
  strips the surrounding quote characters at minimum, which already differs
  from the file's raw bytes regardless of what `systemd` does with them.
- **Charitable**: assumes `systemd` *also* strips a matching pair of
  surrounding quote characters and does nothing else, so a quoted value only
  counts as divergent when its *interior* handling (expansion, escaping,
  whitespace preservation) changed something beyond quote removal.

**Retracted.** An earlier draft of this section claimed that "contains a
space" and "no backslash" describe one sample of the same underlying family as
quoted values. #0461's review measured that false. Whitespace trimming of an
unquoted value is a separate mechanism on a separate code path: locateKeyName
trims the leading whitespace off the value before hasQuotePrefix is consulted,
and extractVarValue's unquoted branch trims both ends of what remains. It is
reached only when hasQuotePrefix returns false, so this issue's early continue
never runs for it. The two published characterisations are each correct of a
different corpus-and-oracle pair and cannot both hold of one measurement;
neither figure should be reused without both assumptions attached.

**After adding the fifth shape**: re-ran both models — **0 of the 724
(conservative) and 0 of the 354 (charitable) previously-unflagged
divergences remain unflagged.** The whitespace-and-quoting family is fully
in scope and flagged, not carved out.

## The whitespace family — out of scope, with the reason (criterion 2)

Not flagged, and deliberately so: whether it diverges at all is undecidable
here. godotenv trims leading and trailing whitespace from an unquoted value.
Whether systemd's EnvironmentFile= does the same is the systemd-side question
criterion 1 already declined to answer, and the whole family swings on it —
thousands of divergences under a raw-bytes model, zero under a trimming one. A
check that fired would be asserting the first model; staying silent asserts the
second. This scan can honestly assert neither, so it asserts nothing and the
gap is named here instead of guessed at. Nothing is mis-modelled today: all 20
assignment values in .env.example are free of leading and trailing whitespace,
the same "this is about the next line someone writes" footing #0450, #0453 and
#0457 were on.

## Criterion 3 — runtime probes

All behavioral claims above about `godotenv` were verified by calling the
real `godotenv.Unmarshal` in a throwaway module outside this repository
(`go list -m -f '{{.Dir}}' github.com/joho/godotenv` → the real v1.5.1
source, read for structure, never a filesystem scan), not by reading
`parser.go`'s regexes and reasoning about them. `systemd`'s own quoting
rules were **not** probed — there is no `systemd` `EnvironmentFile=` parser
available in this environment, which is exactly why criterion 1 chose
refusal over modelling instead of attempting to reverse-engineer them from
documentation.

## Criterion 4 — false positives

**Zero, measured two ways, over both alphabets:**

1. **Regex correctness.** Across both the 111,110-value original-alphabet
   sweep and the 54,240-value extended-alphabet sweep, `envQuotedValuePrefix`
   was compared against an independently-written "does this value's first
   non-whitespace character equal `godotenv`'s own quote-detection
   condition" check: **0 classification mismatches**, and **0** cases where
   the shipped regex fires on a value that is not actually leading-quoted
   (e.g. `abc'def'`, quote at position 4, never flagged).
2. **The real file.** `.env.example` holds 28 quote bytes today (confirmed
   by reading it as bytes with `python3`, never BSD `grep -P`); **all 28 are
   inside full-line comments**, which `scanEnvExampleValueShapes`'s outer
   loop skips before ever reaching `envAssignmentLine` — re-confirmed after
   this change with the same byte-level scan, unchanged: zero non-comment
   lines contain a quote byte, so the new check fires zero times against the
   committed file.

"False positive" for this new shape does not mean quite what it means for
the other four: since the decision is a categorical forbid rather than a
divergence prediction, a flagged quoted value might turn out, for some
individual case, not to actually diverge under `systemd` — that is the
`export`-prefix precedent (`#0441` criterion 5) applied to a second shape,
not a defect. What is checkable, and checked, is that the check fires
**only** on genuinely quoted values and **never** on the shipped template.

## Criteria 5 and 6 — protected machinery, no extra mechanism

`minScannedEnvExampleAssignments` (still `20`), `checkScannedFloor`, and the
four original shapes' regexes, messages, and ordering are byte-for-byte
unchanged — confirmed by diff. The change is additive: one new package-level
regexp (`envQuotedValuePrefix`), one new `if`/`continue` block in
`scanEnvExampleValueShapes` (placed after the `export`-prefix check, before
`envTrailingComment`, so a quoted value's raw text never reaches the three
checks that assume unquoted text), one new row in
`TestScanEnvExampleValueShapes_DetectsEachDivergentShape`'s table, and doc
comments. No new test file, no new floor, no second scan.

Per criterion 1's own framing ("if your decision adds a shape, that is a
fifth shape — say so"), this **is** declared a fifth shape rather than
squeezed into the `$VAR` family the way `#0453` and `#0457` were: it shares
no mechanism with "`godotenv` expands a bare `$`, `systemd` doesn't."

## Regression pin

Added `"quoted value (#0461)"` to `TestScanEnvExampleValueShapes_DetectsEachDivergentShape`'s
table (`badLine: SECRET='shh'` — chosen with no `$` in it at all, so the
dollar-expansion check misfiring on raw quoted text could not independently
produce this case; only the new check can).

**Revert proof**, in a throwaway `git worktree` detached at this issue's
parent commit (`ab2866f`, removed afterward; the shared tree was never
touched — `CLAUDE.md` §8a): with the new `if`/`continue` block removed and
everything else (including the new table row) left in place, only
`quoted_value_(#0461)` fails — `got 0 violation(s) for "SECRET='shh'", want
exactly 1: []` — and all seven other subtests, plus every other test in the
package, still pass. Baseline (with the fix) reruns green on all eight
subtests.

## Verification

```
uptime → load averages: 2.21 2.10 2.18   (recorded, not reasoned from)
ISSUE=0461 scripts/check.sh go ./internal/config/... ./internal/db/... ./internal/handlers/...
```

`VERIFICATION PASSED`. `go build`, `go vet`, `gofmt -l` all clean. Zero
skips. Real elapsed time in every package, not `0.00s`: `internal/config`
0.139s, `internal/db` 0.400s, `internal/handlers` 5.497s. `gofmt -w` on the
changed file left its SHA-256 unchanged (already gofmt-clean). `git status
--porcelain` showed only this issue's own two files dirty throughout.

Test declarations exercised, by name:

    func TestEnvExampleCoversLoaderVariables(t *testing.T)
    func TestEnvExampleValuesAvoidDivergentParserShapes(t *testing.T)
    func TestEnvExampleValueShapeFloorFailsClosedOnEmptyExtraction(t *testing.T)
    func TestScanEnvExampleValueShapes_DetectsEachDivergentShape(t *testing.T)

including all eight of that last test's subtests (the original four, the
three `#0458` widening pins, and this issue's new fifth-shape pin).

## Files changed

- `internal/config/env_example_guard_test.go` — added `envQuotedValuePrefix`
  and its doc comment, the `if`/`continue` block in
  `scanEnvExampleValueShapes`, one new table row and its doc comment in
  `TestScanEnvExampleValueShapes_DetectsEachDivergentShape`, and short
  narrative notes on two other doc comments pointing at the fifth shape. The
  floor, `checkScannedFloor`, and the four original shapes are untouched.
- `.env.example` — not touched; its 28 quote bytes are all inside
  full-line comments the scan already skips (confirmed before and after).
- `issues/0461.md` — this record.

## Work log

**2026-09-05, implement.** Read the full `#0441`→`#0450`→`#0453`→`#0457`→
`#0458` lineage and their review notes. Decided criterion 1 (forbid quoted
values outright) before writing any code, per the issue's own instruction.
Read `joho/godotenv` v1.5.1's `parser.go` via `go list -m -f '{{.Dir}}'
github.com/joho/godotenv` and probed `hasQuotePrefix`/`extractVarValue`'s
behavior at runtime in a throwaway module — quote-prefix detection, single-
vs double-quote escape/expansion differences, whitespace preservation inside
quotes, and unterminated-quote parse errors. Re-derived the divergence count
independently under two comparator models (724 / 354 unflagged out of
54,240), reproduced `#0457`'s own cited 17,661-divergence total exactly as a
sanity check on the methodology, and confirmed under both models that 100%
of unflagged divergences begin with a quote character and 0% of non-quoted
values are ever left unflagged. Implemented the fifth shape as a package-
level regexp plus one `if`/`continue` block ahead of the three existing
value-side checks (so they only ever see confirmed-unquoted text), added and
proved a regression-pin table row via a throwaway-worktree revert (only the
new subtest fails when the widening is reverted), confirmed `.env.example`'s
28 quote bytes remain entirely inside skipped comments, confirmed
`gofmt -w` leaves the file's hash unchanged, and ran the full scoped
verification (`ISSUE=0461`) clean with zero skips and real elapsed times.
Status left at `in-progress` for review.

**2026-09-05, implement (second pass).** Bounced once, on criterion 2 only;
the reviewer's remedy was prose-only and fully specified verbatim in `##
Review notes`, so no code changed and no sixth shape was added. Applied the
three edits as written: (1) replaced `envQuotedValuePrefix`'s doc-comment
closing paragraph with the oracle-dependence explanation; (2) added the
systemd-oracle caption to the `## Criterion 2` results table and replaced the
"Neither model reproduces" closing paragraph with the retraction; (3) added
the new `## The whitespace family — out of scope, with the reason (criterion
2)` section stating the disposition. Also corrected the regression pin's
stated parent commit from `275a6bf` to `ab2866f` (confirmed via `git log
--oneline -1 6d102ab^`); the guard file is byte-identical at both revisions,
so the revert proof itself is unaffected. `gofmt -w` on the changed Go file
left its SHA-256 unchanged. Status left at `in-progress` for review.

## Review notes

**Bounced 2026-09-05 — one criterion, criterion 2. The code is right; the
record is wrong in a way this lineage exists to prevent.** The fifth shape,
its regex, its short-circuit, its message, its regression pin and its
verification all hold up under independent re-measurement. What does not hold
up is the claim used to retire the whitespace half of this issue's own title.
The code commit stays in place; the next pass changes prose only.

### The fifth shape is justified — approved, against the four prior extensions

`#0453` and `#0457` both chose extension over a fifth shape, and both were
right to: each was still "godotenv treats a dollar sign specially, systemd
does not," reached from the same `FindString` at the same violation site. A
fifth shape there would have bought a fifth branch and a fifth table row for
no new mechanism.

This one is different in kind, and the difference is checkable rather than
rhetorical. Quoting is not a variant of expansion; it is the **branch
condition** that decides which parsing pipeline runs at all. `hasQuotePrefix`
is consulted first, and everything the other three value-side checks assert —
whitespace trimming, comment stripping, escape handling, expansion — is the
behaviour of the branch it did *not* take. Modelling quoting as a sixth
alternative inside `envDollarExpansion` would have been incoherent, because a
quoted value's divergence is not caused by anything `envDollarExpansion`
matches.

The short-circuit is what earns it. `#0457`'s review named the real defect as
"the scan's oracle does not model quote stripping at all ... applies to all
four shapes." A widening cannot close that, because the gap is not a missed
pattern — it is three checks reasoning about text whose parsing path they
never established. The `continue` establishes it. That is the argument, it
holds, and the same effect is not achievable by extending an existing shape.
Approved as a fifth shape.

### Refusal as a constraint — approved, and reversible

Nothing in `.env.example` needs a quoted value: all 20 assignment lines carry
bare values today, and the install step in `docs/deployment.md` copies the file
verbatim without rewriting values, so refusal constrains only what a future
editor may type. The forbidden constructions all have unquoted spellings that
load identically, which is what the violation message tells the operator to
write. Should a value ever genuinely need quoting — an embedded space, most
plausibly — the refusal is one deleted `if` block away, and reversing it
restores exactly the pre-`#0461` behaviour because the block is purely
additive. This is the `export ` precedent applied a second time, and it is
applied correctly.

### Criterion 4 — zero false positives, both alphabets, re-derived

Measured independently, with the scan code extracted verbatim from the
committed file rather than retyped, against the real `godotenv` v1.5.1
resolved through `go list -m`. `envQuotedValuePrefix` was compared value by
value against a replication of `locateKeyName`'s cutset trim followed by
`hasQuotePrefix`:

| corpus | values | classification mismatches | fires on unquoted | misses quoted |
|---|---|---|---|---|
| original alphabet, length 1–5, `cost` prefix | 111,110 | 0 | 0 | 0 |
| original alphabet, length 1–5, whole value | 111,110 | 0 | 0 | 0 |
| extended alphabet, length 1–4, `cost` prefix | 54,240 | 0 | 0 | 0 |
| extended alphabet, length 1–4, whole value | 54,240 | 0 | 0 | 0 |

330,700 classifications, zero disagreements. Targeted cases behave: a quote
not at position 0 is untouched, a leading quote behind spaces or a tab is
caught, and both quote kinds report the right kind. On the shipped file,
scanned as bytes with `python3`: 28 quote bytes, every one on a blank or
full-line-comment line, zero on any of the 20 assignment lines — the new check
fires zero times against the file that is copied onto production. The
implementer's criterion-3 probe table reproduces row for row, including the
unterminated-quote parse error and the `\t` backslash strip.

### Criterion 2 — this is the bounce

The re-derivation was run honestly and its methodology was sanity-checked,
which is the right instinct. But the sanity check cannot see the thing that
changed, and the conclusion drawn from it is false.

**Two things were changed from the lineage's construction, and only one was
disclosed.** The issue file says the corpus moved from `cost`-prefixed to
whole-value, and explains why — correctly; a prefix makes a leading quote
unreachable. It does not say that the **systemd oracle** also moved, from "keeps
the raw bytes" — the model named in `envDollarExpansion`'s own doc comment and
the model the reproduced 17,661 rests on — to one that trims surrounding
whitespace. Both moves are needed to reach "100% begin with a quote character",
and the undisclosed one is doing most of the work.

Measured over the same 54,240-value extended alphabet, pre-`#0461` scan:

| corpus | systemd oracle | unflagged | quote-led | contain a space | unflagged after the fix |
|---|---|---|---|---|---|
| `cost` prefix | raw bytes | 3,458 | 0 (0%) | 3,458 (100%) | **3,458 — unchanged** |
| whole value | raw bytes | 5,799 | 504 (8.7%) | 5,415 | 5,295 |
| whole value | trims whitespace | 504 conservative / 104 charitable | 100% | — | 0 |

So **"all contain a space" and "all begin with a quote" can both be true, but
never of the same measurement.** The first describes the lineage's own corpus
under the lineage's own oracle; the second describes a different corpus under a
different oracle. They are not one family seen from two angles, and the exact
counts differ from both reviews' published figures (a reminder that neither
number should be quoted again without the pair of assumptions attached).

**Why the sanity check could not catch this.** The original ten-character
alphabet contains no space, so the raw-bytes and whitespace-trimming oracles
agree on every one of its 111,110 values. Reproducing 17,661 divergent /
17,661 flagged / 0 unflagged confirms it under *both* oracles and *both*
prefixes — verified here. The check is insensitive to precisely the variable
that moved, so passing it established nothing about the swap.

**What follows.** The whitespace family is a separate godotenv mechanism on a
separate code path: `locateKeyName` trims the leading whitespace off the value
before `hasQuotePrefix` is ever consulted, and `extractVarValue`'s unquoted
branch trims both ends of what remains. It is reached only when
`hasQuotePrefix` returns false — so the fifth shape's `continue` never runs for
it, and the fix closes none of it. Concretely, with the committed scan:
`MAX_SEND_RATE=10 ` and `MAX_SEND_RATE= 10` each load as `10` and each draw
zero violations. The issue file's claim that "contains a space" and "no
backslash" describe one sample of the quoted-value family is therefore false,
and the same claim ships inside a doc comment.

That leaves criterion 2 unmet as written: the whitespace-driven divergences are
neither flagged nor out of scope with a stated reason. They were reclassified
out of existence.

### Remedy — prose only, no code change

Leave `internal/config/env_example_guard_test.go`'s `envQuotedValuePrefix`
regex, its `if`/`continue` block, its violation message, and the new table row
in `TestScanEnvExampleValueShapes_DetectsEachDivergentShape` exactly as
committed. Do not add a sixth shape — criterion 6 forbids it and criterion 2
explicitly permits a stated out-of-scope disposition instead. Three edits:

**1.** In `envQuotedValuePrefix`'s doc comment, replace the sentence beginning
"The re-derived finding holds regardless of the exact corpus" and running to
the end of that paragraph with:

> That finding is oracle-dependent, and the dependence is the point. Against a
> systemd model that keeps the raw bytes — the model the four shapes above were
> proved against — the unflagged divergences over the extended alphabet are
> dominated by whitespace rather than quoting, and this fifth shape closes none
> of them. Against a systemd model that trims leading and trailing whitespace
> the way godotenv's unquoted branch does, every remaining unflagged divergence
> is quote-led and this shape closes all of them. Which model is right is a
> systemd question this environment cannot settle — the same reason #0461 chose
> refusal over modelling. Both counts, and the whitespace family's disposition,
> are in issues/0461.md.

**2.** In `## Criterion 2`, add the systemd oracle to the results table as an
explicit column or caption, stating that the reported counts use a
whitespace-trimming systemd model rather than the raw-bytes model
`envDollarExpansion`'s doc comment names; and replace the closing paragraph
that begins "Neither model reproduces" with a retraction:

> **Retracted.** An earlier draft of this section claimed that "contains a
> space" and "no backslash" describe one sample of the same underlying family as
> quoted values. #0461's review measured that false. Whitespace trimming of an
> unquoted value is a separate mechanism on a separate code path: locateKeyName
> trims the leading whitespace off the value before hasQuotePrefix is consulted,
> and extractVarValue's unquoted branch trims both ends of what remains. It is
> reached only when hasQuotePrefix returns false, so this issue's early continue
> never runs for it. The two published characterisations are each correct of a
> different corpus-and-oracle pair and cannot both hold of one measurement;
> neither figure should be reused without both assumptions attached.

**3.** Add a section giving the whitespace family its stated disposition:

> ## The whitespace family — out of scope, with the reason (criterion 2)
>
> Not flagged, and deliberately so: whether it diverges at all is undecidable
> here. godotenv trims leading and trailing whitespace from an unquoted value.
> Whether systemd's EnvironmentFile= does the same is the systemd-side question
> criterion 1 already declined to answer, and the whole family swings on it —
> thousands of divergences under a raw-bytes model, zero under a trimming one. A
> check that fired would be asserting the first model; staying silent asserts the
> second. This scan can honestly assert neither, so it asserts nothing and the
> gap is named here instead of guessed at. Nothing is mis-modelled today: all 20
> assignment values in .env.example are free of leading and trailing whitespace,
> the same "this is about the next line someone writes" footing #0450, #0453 and
> #0457 were on.

Re-run `ISSUE=NNNN scripts/check.sh go ./internal/config/... ./internal/db/...
./internal/handlers/...` afterwards — edit 1 touches a Go doc comment, so
`internal/handlers`'s citation guards are in scope, and edits 2 and 3 touch
issue prose, which `#0446` put in scope while the issue is open.

### What passed, so the next pass does not re-litigate it

- **Criterion 5, protected machinery byte-unchanged.** `minScannedEnvExampleAssignments`
  (still 20), `checkScannedFloor`, `envAssignmentLine`, `envTrailingComment`
  and `envDollarExpansion` all hash identically across the commit, and each of
  the six existing violation messages is present exactly once and textually
  identical. The Go diff is 116 insertions and 0 deletions.
- **Criterion 6, no new mechanism.** One package-level regexp, one `if`/`continue`,
  one table row, doc comments. No new test file, no second floor, no second scan.
- **The regression pin holds.** Revert proof re-run independently in a throwaway
  `git worktree` detached at the code commit, with the `if`/`continue` block
  removed and the table row left in place: only
  `TestScanEnvExampleValueShapes_DetectsEachDivergentShape/quoted_value_(#0461)`
  fails, reporting `got 0 violation(s) for "SECRET='shh'", want exactly 1: []`,
  while the other seven subtests and every other test in the package pass. The
  worktree was removed afterward; the shared tree was never touched.
- **Verification is green.** `go build`, `go vet` and `gofmt -l` clean, zero
  skips, real elapsed time in all three packages.

One small correction for the record: the revert proof names `275a6bf` as this
issue's parent commit. The parent is `ab2866f`. The proof is unaffected — the
guard file is byte-identical at both revisions, confirmed by object hash — but
the next pass should fix the hash while editing the section.

### Verification run for this review

```
uptime → load averages: 2.28 2.46 2.33   (recorded, not reasoned from)
ISSUE=0461r scripts/check.sh go ./internal/config/... ./internal/db/... ./internal/handlers/...
```

`VERIFICATION PASSED`. Zero skips; `internal/config` 0.240s, `internal/db`
0.745s, `internal/handlers` 5.964s — real elapsed time, none reporting 0.00s.
`gofmt -l` clean. `git status --porcelain` carried only another agent's
`issues/0462.md` throughout, which was left untouched.

Test declarations exercised, by name:

    func TestEnvExampleCoversLoaderVariables(t *testing.T)
    func TestEnvExampleValuesAvoidDivergentParserShapes(t *testing.T)
    func TestEnvExampleValueShapeFloorFailsClosedOnEmptyExtraction(t *testing.T)
    func TestScanEnvExampleValueShapes_DetectsEachDivergentShape(t *testing.T)

### For the orchestrator

Two things to consider filing, neither blocking this issue:

1. **The whitespace family as its own issue**, if the disposition above should
   become a decision rather than a documented gap.
2. **The systemd side is not actually unverifiable — only unverifiable on this
   laptop.** Production runs systemd on Amazon Linux 2023, so a single
   transient unit with an `EnvironmentFile=` would settle the trimming and
   quoting questions this whole lineage has been reasoning around. That is
   outward-facing and approval-gated, so it is a proposal, not a step.

On whether the lineage is finished: **the quoting half is, and the dollar-and-
backslash half has been since `#0457`.** Once the record above is corrected,
the only thing left in this family is the whitespace question, and its honest
answer is a one-time recorded decision rather than a sixth mechanism — so
`#0461` should be where the chain ends, the way `#0463` closed out the
fence-walk chain.

### Second review, 2026-09-05 — passed, criterion 2 now met

The second pass was a prose-only transcription of the three verbatim edits
above, plus the parent-commit correction. Four checks, all hold.

**1 — the replacement sentence is true, and true conditionally.** Verified
against this review's own first-pass measurements (the corpus/oracle table
above), not re-derived. *Raw-bytes half:* over the lineage's own
`cost`-prefixed extended-alphabet corpus, 3,458 unflagged divergences, 0%
quote-led, 100% containing a space, and 3,458 still unflagged after the fifth
shape — dominated by whitespace, closes none. *Trimming half:* over the
whole-value corpus under a trimming oracle, every remaining unflagged
divergence is quote-led and none remain after the fix. Both halves hold, and
the sentence correctly states that this environment cannot settle which model
systemd implements. One nuance for the record, not a defect: over the
*whole-value* corpus under raw bytes the majority is still whitespace (5,415 of
5,799) but the shape does close the 504 quote-led ones, so "closes none of
them" is exact with "them" read as the whitespace family or as the lineage's
own `cost`-prefixed corpus — which is the corpus the four earlier shapes were
proved against, and the one the sentence's own parenthetical names. The
load-bearing claim, that the finding is oracle-dependent, is true under either
reading. The text is this review's own verbatim remedy.

**2 — the results table labels its oracle, and contrasts it.** The caption now
names the whitespace-trimming systemd model and says explicitly that it is not
the raw-bytes model `envDollarExpansion`'s doc comment names. That missing
contrast was the whole of the bounce; it is present, and it is the contrast
rather than a bare label.

**3 — the whitespace disposition exists and gives a reason.** It is a separate
section from the retraction and does the thing criterion 2 asks: states the
family is out of scope, and why — whether it diverges at all is undecidable
without a systemd-side probe, a check that fired would assert one model while
silence asserts the other, and nothing in the shipped file is mis-modelled
today because all 20 assignment values are free of surrounding whitespace. The
retraction paragraph is separate and does not stand in for it.

**4 — prose only, confirmed structurally.** The guard file was re-printed from
its parsed AST with comments discarded, at the commit under review and at its
parent, and the two are byte-identical: SHA-256 `de881c54…5e18d9e4`, 11,389
bytes on both sides, with the extraction asserted non-empty before hashing.
(The first attempt at this hashed the empty string because of a package clash
in the scratch directory, and would have passed fail-open — `CLAUDE.md` §8's
own trap, caught by the length assertion rather than by luck.) No sixth shape,
no new package-level regexp, no new branch: any of those would move the
stripped hash. The parent-commit correction is right — the code commit's parent
is `ab2866f`, and the guard file's blob is identical at `275a6bf` and
`ab2866f`, so the revert proof is unaffected exactly as the work log claims.

### The `.env.example` divergence lineage is complete

Stated explicitly, so it is not reopened by default. `#0450`, `#0453` and
`#0457` closed the expansion, escaping and comment families; `#0461` closes
quoting, and it does so with a branch-condition short-circuit rather than a
fifth alternative in the dollar family. Nothing in this family remains **open**.
What remains is **dispositioned**: the whitespace family is recorded above as
out of scope with its reason, because the question it turns on is a systemd
question and not a `godotenv` one. `#0464` is a *measurement* proposal — run a
transient unit with an `EnvironmentFile=` on the production host and settle the
systemd side by observation — not a sixth widening of this scan; if it is ever
done, it converts the disposition into a decision without adding a mechanism
here. Absent that measurement, a sixth shape would be asserting a model nobody
has verified, which is the failure this lineage has been avoiding since
`#0450`. This is where the chain ends, as `#0463` ended the fence-walk chain.

### Verification run for this review

```
uptime → load averages: 1.75 2.42 2.43   (recorded, not reasoned from)
ISSUE=0461d scripts/check.sh go ./internal/config/... ./internal/db/... ./internal/handlers/...
```

`VERIFICATION PASSED`. Zero skips; `internal/config` 0.140s, `internal/db`
0.543s, `internal/handlers` 5.562s — real elapsed time, none reporting 0.00s.
`go build`, `go vet` and `gofmt -l` clean; the changed Go file is hash-stable
at SHA-256 `c0c6afd9…91a3716`. `git status --porcelain` was empty before the
run and carried only this review's own edit afterwards.

Test declarations exercised, by name:

    func TestEnvExampleCoversLoaderVariables(t *testing.T)
    func TestEnvExampleValuesAvoidDivergentParserShapes(t *testing.T)
    func TestEnvExampleValueShapeFloorFailsClosedOnEmptyExtraction(t *testing.T)
    func TestScanEnvExampleValueShapes_DetectsEachDivergentShape(t *testing.T)

### For the orchestrator

Nothing to file. Both follow-ups this review raised on its first pass are
placed: the whitespace family's disposition is now recorded in this issue, and
the systemd-measurability proposal is `#0464`. Resolved.
