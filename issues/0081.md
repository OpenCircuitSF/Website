# 0081 — The async send queue has no shutdown, no regression guard, and leaks goroutines

| | |
|---|---|
| **Status** | resolved |
| **Module** | handlers / mailing |
| **Platform** | All |
| **Phase** | 3 — Mailing list capture |
| **First seen** | 2026-08-19 |
| **Commit** | `94265f8` — #0081 Add graceful shutdown and guard the async-send property; `ab65d0a` — #0081 Synchronise the send WaitGroup and fix the shutdown ordering (bounce fix) |
| **Closed** | 2026-08-19 |

## Description

`#0026` moved `mailer.Send` off the request goroutine into an in-process queue (256-deep, 4 workers), which closed a 100%-accurate enumeration oracle. The queue itself now needs the lifecycle work that was out of that issue's lane. Three defects, found and reproduced by `#0026`'s reviewer.

> 🟢 Resolved 2026-08-19 — Re-reviewed after the `sendWG` bounce and approved: the reviewer rebuilt the race harness from scratch rather than re-running the implementer's, and against `ab65d0a^` (`a1d0335`) it panicked with `sync: WaitGroup is reused before previous Wait has returned` in `Close`'s waiter goroutine at rounds **300, 2,100 and 5,700** across three runs, while `ab65d0a` survived **100,000 rounds plain and 200,000 rounds under `-race`** with no panic and no reported data race; the implementer's account of *why* its first attempt also failed was independently reproduced too — re-applying that exact intermediate shape (`Add(1)` inside `closeMu` but *before* the `closed` check) panicked again at rounds ~800 and <100 — and the happens-before argument for the final shape was verified by reading rather than accepted: every surviving `Add(1)` executes inside a `closeMu` critical section that observed `closed == false`, `closed` is written `true` exactly once under the same mutex and never back, so any critical section observing `false` necessarily precedes `Close`'s in the mutex's total order, and Go's memory model then chains that `Add` → `Unlock` → `Close`'s `Lock` → `closed = true` → `Unlock` → the `go` statement → the waiter goroutine's `Wait()`, which is a complete edge for *every* `Add` that ever runs, with the closed-drop path touching `sendWG` not at all and the queue-full path's `Add`/`Done` pair balanced inside the same protected branch — no third interleaving was found, and the test-only `waitForSends` was checked as well (`concurrentSubscribe` joins every request goroutine before calling it, so no `Add` is ever concurrent with that `Wait` either). On the rest: both drop paths still call `releaseSendClaim` on their own detached 5s context, so nothing leaks a claim by skipping the `WaitGroup`; `enqueueSend` is reachable only from `sendConfirmation`/`sendAlreadySubscribed` on the request goroutine, the package spawns no other goroutines and hijacks no connections, so `srv.Shutdown` returning **nil** really does mean no request goroutine is inside `enqueueSend`; `defer signal.Stop(sigCh)` sits immediately after `signal.Notify` with no return between them, so the listener-failure branch unregisters too; and `TestMountAndServe_SIGTERMReleasesInFlightClaim` still holds under the new ordering (claim released — `confirm_sent_at=<nil>`, `mountAndServe` returns `nil`, retry sends exactly 1 email). `go build`/`go vet` clean; `go test ./... -count=1` green across all 13 packages with `TEST_DATABASE_URL` exported and **zero** skips (including `internal/middleware`, whose earlier failure was another agent's uncommitted temp file and is absent from the commit); `-race` clean on `cmd/opencircuit` and `internal/handlers`; `-count=2` clean on `cmd/opencircuit`; `ab65d0a` touches exactly three files and `issues/0026.md`'s correction is committed, accurate, and its `## Review verification` §5 cross-reference checks out. One residual is **routed rather than blocking**: the reordering inverted, and worsened, the shared-context risk — `srv.Shutdown` does not cancel request contexts, and the long-lived `GET /api/events` SSE stream returns only on client disconnect, so one open dashboard stream makes `Shutdown` burn the *entire* shared `shutdownTimeout` (demonstrated: a 2s budget fully consumed, `context.DeadlineExceeded`, 0s left) and hands `subscribeH.Close` an already-expired context, at which point `Close` still flips `closed`, cancels `sendCtx` and closes `sendQueue` but returns without waiting — so the releases are started and then killed by process exit, reviving the very hour-long silence this issue exists to fix. Under the old order that budget starved `Shutdown` instead, whose worst case is only a dropped SSE connection that `EventSource` reconnects, so the new order is worse whenever a stream is open and equivalent otherwise; the fix is not to revert the ordering (which is right) but to give each step its own budget instead of sharing one, and to soften the two comments that state the no-goroutine-inside-`enqueueSend` guarantee unconditionally when it holds only on `Shutdown`'s nil return.

## 1. SIGTERM loses the email *and* silences the retry

There is no graceful shutdown anywhere in the repo: `mountAndServe` ends in `http.ListenAndServe`, no `signal.Notify` exists in any file, and the systemd unit sets no `KillSignal`/`TimeoutStopSec`.

Reproduced state after a kill mid-send: `confirm_sent_at` **stamped**, 0 emails sent, and **5 retries after restart → 0 emails**. The address goes quiet for up to an hour, self-healing only when the cooldown expires. The claim is never released.

**This is a regression from the pre-`#0026` behaviour**, and `#0026`'s `## Gotchas` claim that it is "no worse than before" is false — previously a killed send left `confirm_sent_at` NULL and the visitor's retry worked immediately. Every deploy is a `SIGTERM`.

## 2. Nothing guards the async-send property

Mutation-proved by the reviewer: **putting `mailer.Send` back on the request goroutine leaves the entire suite green.** The property that closed the oracle is unprotected, and the next person to touch this file can undo it silently.

## 3. `NewSubscribeHandler` leaks four goroutines per construction

The queue channel is never closed, so every handler constructed — including in tests — leaves four workers running.

## Acceptance criteria

- [x] A `Close(ctx)` that closes the send queue and waits, bounded; an `http.Server` with `signal.Notify` in `cmd/opencircuit/main.go`; `TimeoutStopSec` in the systemd unit
- [x] On shutdown, queued and in-flight claims are **released**, so a visitor's retry sends immediately rather than being silenced for the cooldown window
- [x] **Reproduce the current failure first** — kill mid-send, confirm `confirm_sent_at` stamped and retries silent — then show it fixed
- [x] A **deterministic, non-timing** regression test for the async property: use a blocking mailer and assert the `202` is written *before* the mailer is entered. Mutation-proven by moving `Send` back onto the request goroutine and watching it fail
- [x] `NewSubscribeHandler`'s goroutines are cleaned up; no leak across repeated construction
- [x] Correct `#0026`'s `## Gotchas` — the "no worse than before" claim is false and should not stay in the record
- [x] `go build ./...`, `go vet ./...`, `go test ./... -count=1` green with `TEST_DATABASE_URL` exported, zero skips, `-race` clean

## Root cause

`#0026`'s phase-3 rework (`5d86b4d`) moved `mailer.Send` off the request goroutine and made the resend cooldown an atomic **claim-before-send**
(`ClaimConfirmationSend`/`ClaimAlreadySubscribedSend`, an `UPDATE` that stamps `confirm_sent_at` *before* the message is even enqueued). That claim is
exactly right for concurrency (a claim taken and then never sent is `ReleaseConfirmationClaim`'d on a normal send failure), but it created a new failure
mode that stamping-after-send never had: if the goroutine holding the claim disappears — a `SIGTERM` with no handler, which is every deploy and every
systemd restart — the compensating release code never runs. Pre-`#0026`, a killed synchronous send meant the stamp itself never happened
(`confirm_sent_at` stayed `NULL`); post-`#0026`, the stamp happens first and the send (and its release-on-failure) happens second, off-goroutine, so a
kill in between leaves the claim stamped with nothing sent and no release. Nothing in the repo caught this: no graceful shutdown existed anywhere
(`mountAndServe` ended in a bare `http.ListenAndServe`, no `signal.Notify`, no `KillSignal`/`TimeoutStopSec` in the systemd unit), so a real `SIGTERM`
terminated the process mid-send with zero chance for any Go code — deferred or otherwise — to run. `#0026`'s own `## Gotchas` recorded this as "no worse
than before this fix," which the reproduction below (and its own reviewer, `## Review verification` §5–6) shows is false: it is strictly worse, because
the earlier synchronous design had no separate claim step to leave stranded.

The second and third defects are consequences of the same gap: because nothing ever called anything resembling `Close`, there was no regression test
possible for "does a response ever wait on the mailer" that would also fail if someone quietly reintroduced a wait — and the four worker goroutines
`NewSubscribeHandler` starts had no path to ever terminate, leaking on every construction (production has one long-lived handler, so this was invisible
there; the test binary accumulates one leak per constructed handler across this package's ~20 call sites).

### Reproduction, before the fix (captured via a throwaway test, `internal/handlers/zz_shutdown_repro_test.go`, deleted before the commit — matching `#0026`'s own `zz_timing_proof_test.go` convention)

A Go test process cannot `SIGTERM` itself without killing the test binary running the assertions, so "process death mid-send" is modeled as "the
goroutine holding the claim is abandoned — nothing ever signals it, nothing ever calls its release path." That is exactly what a real, unhandled
`SIGTERM` does to every in-flight goroutine: the Go runtime stops the process immediately and no deferred function anywhere executes. From the
database's point of view the two are indistinguishable. Concretely: a signup was submitted against a mailer that blocks forever inside `Send` (release
channel never closed), the worker was confirmed to have actually entered `Send` (not merely queued), and — critically — `#0081`'s own new `Close`
method was **not called anywhere in this reproduction**, standing in for "nothing in `main.go` invokes it," which was true for every deploy before this
issue's fix:

```
$ go test ./internal/handlers/... -run TestZZ_SIGTERMReproduction_BeforeShutdownWiring -v -count=1
=== RUN   TestZZ_SIGTERMReproduction_BeforeShutdownWiring
    zz_shutdown_repro_test.go:56: BEFORE reproduction -- immediately after the 202: id=1 status=pending confirm_sent_at=2026-08-19 04:04:55.157201 -0700 PDT emails actually sent=0
    zz_shutdown_repro_test.go:78: BEFORE reproduction -- after a restart, 5 retries by the same visitor -> 0 emails sent (the visitor saw 5x 202)
--- PASS: TestZZ_SIGTERMReproduction_BeforeShutdownWiring (0.09s)
```

Matches the issue's reported failure state exactly: `confirm_sent_at` stamped immediately after the `202`, 0 real emails sent, and 5 retries against a
brand-new handler (standing in for the process restarting) sent **0** more — every one silenced by the stamped-but-unreleased cooldown. The visitor saw
6 total `202` responses and received nothing.

## Fix

- **`internal/handlers/subscribe.go`** — `SubscribeHandler` gained `sendCtx`/`sendCancel` (a cancellable context every worker's per-job timeout is now
  derived from, `context.WithTimeout(h.sendCtx, sendJobTimeout)`, replacing `context.Background()`) and `closeMu`/`closed` (guarding `enqueueSend`
  against a send on a channel `Close` has closed). `Close(ctx context.Context) error`: idempotent, safe to call concurrently with in-flight `Subscribe`
  requests. It flips `closed`, cancels `sendCtx` (interrupting any send that is queued-but-not-started or actively in flight — if the mailer respects
  context cancellation, which the real `SESMailer`'s AWS SDK call does, this makes a stuck send fail in milliseconds rather than running out
  `sendJobTimeout` or forever), and closes `sendQueue` so every `runSendWorker` goroutine exits once drained. Every interrupted send takes
  `processSendJob`'s **existing** failed-send path — logged, claim released via `releaseSendClaim`'s own short-lived 5s context (deliberately not
  derived from `sendCtx`, so a release attempt is never itself defeated by the cancellation that doomed the send it's compensating for) — so this fix
  is additive to code that was already correct for an ordinary failure; it just gives shutdown a way to trigger that same path instead of the goroutine
  vanishing with no path triggered at all. `enqueueSend` now holds `closeMu` across its channel-send `select`, so it can never race `Close`'s own
  `close(h.sendQueue)` (which would panic if a send landed on it after close) — either the job is queued before `Close` closes the channel, or
  `closed` is already visible and the job takes the same drop-and-release path a saturated queue already used.
- **`cmd/opencircuit/main.go`** — `mountAndServe` now builds an `*http.Server` instead of calling `http.ListenAndServe` directly, registers
  `signal.Notify(sigCh, syscall.SIGTERM, syscall.SIGINT)`, and on receiving either signal: calls `subscribeH.Close(ctx)` (nil-guarded for dev mode) then
  `srv.Shutdown(ctx)`, both bound by one shared `shutdownTimeout` (20s) context. `Close` runs first so any request still inside `enqueueSend` when
  shutdown begins is already guarded by `closeMu`/`closed` rather than racing `srv.Shutdown`'s own drain of in-flight handlers. The three existing
  `mountAndServe`-based wiring tests (`admin_wiring_test.go`, `ratelimit_wiring_test.go`, `subscribe_wiring_test.go`) needed **no changes** — they never
  send a signal, so `mountAndServe` still blocks for the life of the test binary exactly as their own comments describe; the new behavior is additive,
  not a signature or behavior change on the path they exercise.
- **`deploy/systemd/opencircuit.service`** — added `KillSignal=SIGTERM` (systemd's existing default, set explicitly so the pairing with the process's
  own handler is visible from this file alone) and `TimeoutStopSec=30`, which must exceed `main.go`'s `shutdownTimeout` (20s) or systemd `SIGKILL`s the
  process before the graceful sequence can finish.
- **`internal/handlers/subscribe_test.go`** — added `blockingMailer` (a `Mailer` whose `Send` blocks until a test-controlled `release` channel closes
  or its `ctx` is cancelled, closing `entered` the instant `Send` is called), and three new permanent tests (below). `subscribeMux` now takes `*testing.T`
  and registers `t.Cleanup(func() { h.Close(...) })`, so this package's ~20 call sites stop leaking `sendWorkerCount` goroutines per construction —
  before `Close` existed there was no way to stop them at all.

### Bounce fix (`ab65d0a`) — the reviewer's two blocking findings, plus the non-blocking `signal.Stop` note

- **`internal/handlers/subscribe.go` — `enqueueSend`'s `sendWG.Add(1)`.** The reviewer reproduced `sync: WaitGroup is reused before previous Wait has
  returned` at ~5,500 rounds of concurrent `enqueueSend`/`Close`, because `Add(1)` ran before `closeMu` was ever taken — no happens-before edge to
  `Close`'s `sendWG.Wait()` at all. The first attempt at a fix (move `Add(1)` inside `closeMu`, but still before the `closed` check, keeping the
  closed-drop path's `Add`+`Done()` pair) still panicked, at a similar rate (~5,000 rounds) — a subtler instance of the same defect: `Close`'s
  `sendWG.Wait()` call itself is **not** guarded by `closeMu` (only the decision to `Add` was), so an `Add`+`Done()` pair on the closed-drop path could
  still race a `Wait()` already in flight. `sync.WaitGroup`'s contract needs the `Add` to be *sequenced* ahead of `Wait`, not merely gated by some
  condition checked under a mutex. The actual fix moves `sendWG.Add(1)` to run only in the branch that is provably sequenced strictly before `Close`'s
  critical section — after the `closed` check has observed `false`, still holding `closeMu` — and removes `Add`/`Done` from the closed-drop path
  entirely (a job dropped there was never queued, so it was never "in flight" from `Close`'s point of view and needs no `WaitGroup` accounting at all).
  Because `Close` sets `closed = true` under the same `closeMu` before ever releasing it, and only spawns its `Wait()`-calling goroutine after
  releasing `closeMu`, every surviving `Add` now has a real mutex-plus-go-statement happens-before chain to `Close`'s `Wait()` call. Verified clean
  across 100,000 rounds of the reproduction harness (see `## Verification` below), `-race` included.
- **`cmd/opencircuit/main.go` — shutdown ordering.** `mountAndServe` called `subscribeH.Close(ctx)` before `srv.Shutdown(ctx)`, contradicting `Close`'s
  own doc comment ("by the time it returns no request goroutine can still be inside `enqueueSend`" — only true once `srv.Shutdown` has already drained
  every in-flight handler). Reordered to `srv.Shutdown` first, `subscribeH.Close` second, on the same shared `shutdownTimeout` context; a non-nil
  `Shutdown` error is captured rather than returned immediately, so `Close` still runs (and releases any stranded claims) before the function returns.
  This also makes the `Add`/`Wait` race above structurally unreachable in production, not merely fixed in isolation: no request goroutine can be
  inside `enqueueSend` once `Close` starts waiting.
- **`cmd/opencircuit/main.go` — `signal.Stop`.** Added `defer signal.Stop(sigCh)` right after `signal.Notify`, covering every return path (including
  the `errCh`/listener-failed branch, which previously left the registration live forever in that case too). See `## Gotchas` for why this matters
  beyond tidiness.

## Verification

```
export TEST_DATABASE_URL='postgres://opencircuit:opencircuit@localhost:5432/opencircuit_test?sslmode=disable'
go build ./...   # clean
go vet ./...     # clean
go test ./... -count=1
```

Every package **except** `internal/middleware` reports `ok` at real (non-zero) durations, zero `SKIP`, zero `FAIL`
(`cmd/opencircuit` 0.68–1.7s, `internal/handlers` 2.75–3.26s, `internal/subscribers` 3.16–3.36s, etc.; `internal/testdb` and `web` correctly report
`[no test files]`). `internal/middleware` fails independently, in `zz_review0080_temp_test.go` — a concurrently in-progress subagent's own throwaway
reproduction file for `#0080`, outside this issue's lane (`internal/middleware` is explicitly excluded above) and untouched by this commit; excluded
from the "zero failures" claim for that reason, not overlooked.

`go test ./cmd/opencircuit/... ./internal/handlers/... -race -count=1`: both `ok`, clean.

### New permanent tests (`internal/handlers/subscribe_test.go`)

- `TestSubscribe_AsyncSendDoesNotBlockResponse` — the deterministic, non-timing regression test the acceptance criteria call for: a `blockingMailer`
  whose `release` is never closed within the test (only by `subscribeMux`'s `t.Cleanup`-registered `Close`, at teardown, after every assertion), driven
  directly over `ServeHTTP`. Asserts `ServeHTTP` returns within a 2s hang-guard **while the mailer is provably still blocked**, that the response is
  `202`, and that the mailer really was entered (proving a send was actually queued, so the fast response means something).
- `TestSubscribeHandler_Close_ReleasesInFlightClaimAndRetrySendsImmediately` — the fixed-state counterpart to the before-reproduction above, this time
  calling `Close` explicitly: claim stamped while the send is stuck → `Close(ctx)` returns in well under its 3s bound (proving interruption, not a wait)
  → claim confirmed released (`confirm_sent_at` back to `NULL`) → a second `Close` call is a no-op → an immediate retry (no cooldown wait) against a
  fresh handler sends exactly 1 real email.
- `TestNewSubscribeHandler_NoGoroutineLeakAcrossConstruction` — constructs and closes 50 handlers in a loop, comparing `runtime.NumGoroutine()` before
  and after (with a small settle window and slack for scheduler tail latency, not for correctness). Observed: `goroutines before=3 after=3 across 50
  constructions (each starting 4 workers, 200 total if leaked)`.

### Real end-to-end proof, over a real OS signal (`cmd/opencircuit/shutdown_wiring_test.go`, new)

`TestMountAndServe_SIGTERMReleasesInFlightClaim` drives the real `mountAndServe` (the exact function `servePostgres` calls) over a real TCP listener,
with a mailer that blocks `Send` on a channel the test never closes, and sends the test process a **genuine** `syscall.SIGTERM` — safe, because
`mountAndServe`'s own `signal.Notify` intercepts it (Go delivers an intercepted signal to the registered channel instead of taking the default,
process-terminating action):

```
=== RUN   TestMountAndServe_SIGTERMReleasesInFlightClaim
    shutdown_wiring_test.go:171: before SIGTERM: confirm_sent_at=2026-08-19 04:10:06.333037 -0700 PDT emails actually sent=0
2026/08/19 04:10:06 opencircuit: received terminated, shutting down gracefully
2026/08/19 04:10:06 ERROR subscribe: async send failed subscriber_id=1 kind=confirmation err="context canceled"
    shutdown_wiring_test.go:192: after SIGTERM-driven shutdown: confirm_sent_at=<nil>
    shutdown_wiring_test.go:232: retry: 1 real email sent — the visitor's next attempt is no longer silenced
--- PASS: TestMountAndServe_SIGTERMReleasesInFlightClaim (0.09s)
```

`mountAndServe` returned in well under its 20s `shutdownTimeout` — 0.09s total for the whole test, signal to return included — because `sendCtx`'s
cancellation interrupts the blocked send almost immediately rather than the process waiting it out. Run alongside the rest of the package's
`mountAndServe`-based wiring tests (which start listeners and never signal them, by design — they "outlive the test within the test binary"), the real
`SIGTERM` this test sends also reaches those already-registered `sigCh`s and gracefully shuts them down too; harmless, since each `mountAndServe` call
only ever shuts down its own listener/handler, and their own assertions had already completed. Verified by running the whole package:
`go test ./cmd/opencircuit/... -v` — all tests still pass, no interference.

### Mutation proof (performed for real: mutated, run, observed to fail, reverted, re-run to confirm green — restored byte-identical, verified with `diff`
against a pre-mutation copy saved to the scratchpad)

`enqueueSend` mutated to call `h.processSendJob(job)` synchronously instead of queuing — putting `mailer.Send` back on the request goroutine, the exact
defect the async property exists to prevent:

```
$ go test ./internal/handlers/... -run TestSubscribe_AsyncSendDoesNotBlockResponse -v -count=1
=== RUN   TestSubscribe_AsyncSendDoesNotBlockResponse
    subscribe_test.go:1208: Subscribe did not return within 2s while the mailer is blocked — mailer.Send appears to be running on the request goroutine, which would reopen #0026's timing oracle
--- FAIL: TestSubscribe_AsyncSendDoesNotBlockResponse (2.05s)
FAIL
```

Reverted (`diff` against the pre-mutation copy: identical); `go build ./...`, `go vet ./...`, and the full non-`internal/middleware` suite re-confirmed
green afterward.

### Uniform `202` and queue bounds — re-confirmed unaffected

`TestSubscribe_UniformResponseAcrossBranches` (11 branches, full header set minus `Date`) and every existing `#0026` concurrency/cooldown/timing test
pass unchanged. `sendQueueCapacity` (256) and `sendWorkerCount` (4) are untouched; a saturated queue still drops-and-releases via the same
`enqueueSend` path (now additionally guarded by `closeMu`, which only ever short-circuits it faster, never blocks it). `sendJobTimeout` (30s) and
`subscribeResendCooldown`/`subscribeAlreadySubscribedCooldown` (1h each) are unchanged — the 30s+5s vs. 1h ratio the issue asked to preserve is intact.

### Bounce-fix verification, 2026-08-19 (`ab65d0a`)

**Reproduced the panic first**, against `94265f8` unmodified, with a throwaway harness (`internal/handlers/zz_review0081_wg_repro_test.go`, deleted
before the commit — same `zz_*` convention as this issue's own earlier reproduction file): a job held in flight behind a gated test mailer (so
`Close`'s waiter goroutine genuinely parks in `sendWG.Wait()` at count 1), then that job's completion raced against a second, fully independent,
concurrent `enqueueSend` call:

```
$ go test ./internal/handlers/... -run TestZZ_SendWGRace_Repro -v -count=1
=== RUN   TestZZ_SendWGRace_Repro
    zz_review0081_wg_repro_test.go:91: completed 5000 rounds clean
panic: sync: WaitGroup is reused before previous Wait has returned

sync.(*WaitGroup).Wait(...)
github.com/.../internal/handlers.(*SubscribeHandler).Close.func1()
	internal/handlers/subscribe.go:447 +0x2c
created by ...Close in goroutine ...
	internal/handlers/subscribe.go:446 +0x1d0
FAIL
```

Panicked between round 5,000 and round 10,000 — consistent with the reviewer's own ~5,500-round report. A first-pass fix (`Add(1)` moved inside
`closeMu`, but still ahead of the `closed` check) was tried against the same harness and **also panicked**, at a similar round count — see `## Fix`
above for why that shape was still wrong. The final fix (`Add(1)` moved to run only in the branch sequenced strictly before `Close`'s critical
section, with the closed-drop path no longer touching `sendWG` at all) was then run past the reviewer's 30,000-round bar, clean, under `-race`, at an
even higher count:

```
$ go test ./internal/handlers/... -run TestZZ_SendWGRace_Repro -v -count=1 -race
=== RUN   TestZZ_SendWGRace_Repro
    zz_review0081_wg_repro_test.go:91: completed 5000 rounds clean
    ...
    zz_review0081_wg_repro_test.go:94: completed 100000 rounds clean
--- PASS: TestZZ_SendWGRace_Repro (3.65s)
PASS
```

100,000 rounds clean (over 3x the reviewer's 30,000-round bar), `-race` included. The harness file was deleted before this commit.

**Ordering and SIGTERM end-to-end, re-confirmed under the new order:** `TestMountAndServe_SIGTERMReleasesInFlightClaim` (unchanged) still passes with
`srv.Shutdown` now running before `subscribeH.Close`:

```
$ go test ./cmd/opencircuit/... -run TestMountAndServe_SIGTERM -v -count=1
=== RUN   TestMountAndServe_SIGTERMReleasesInFlightClaim
    shutdown_wiring_test.go:172: before SIGTERM: confirm_sent_at=2026-08-19 05:36:09.140707 -0700 PDT emails actually sent=0
2026/08/19 05:36:09 opencircuit: received terminated, shutting down gracefully
2026/08/19 05:36:09 ERROR subscribe: async send failed subscriber_id=1 kind=confirmation err="context canceled"
    shutdown_wiring_test.go:193: after SIGTERM-driven shutdown: confirm_sent_at=<nil>
    shutdown_wiring_test.go:233: retry: 1 real email sent — the visitor's next attempt is no longer silenced
--- PASS: TestMountAndServe_SIGTERMReleasesInFlightClaim (0.09s)
```

Claim released, process exits 0, retry sends exactly 1 real email — identical observable outcome to the pre-reorder run, now reached via the ordering
`Close`'s own doc comment always claimed.

**Full suites, `TEST_DATABASE_URL` exported, 2026-08-19:**

- `go build ./...` clean; `go vet ./...` clean.
- `go test ./... -count=1`: every package `ok` (including `internal/middleware`, which was `#0080`'s throwaway-reproduction concern in the prior
  verification pass and has since landed clean), zero `SKIP`, zero `FAIL`. `internal/testdb` and `web` correctly report `[no test files]`.
- `go test ./cmd/opencircuit/... ./internal/handlers/... -race -count=1`: both `ok`.
- `go test ./cmd/opencircuit/... -count=2`: `ok`.
- `go test ./cmd/opencircuit/... -race -count=3`: `ok`.

## Files changed

- `internal/handlers/subscribe.go` — `sendCtx`/`sendCancel`/`closeMu`/`closed` fields; `processSendJob` derives its per-job timeout from `sendCtx`
  instead of `context.Background()`; `enqueueSend` gated by `closeMu`/`closed`; new `Close(ctx context.Context) error`.
- `internal/handlers/subscribe_test.go` — `blockingMailer` test double; `subscribeMux` takes `*testing.T` and registers a bounded `Close` cleanup (all
  ~20 call sites updated); three new permanent tests (`TestSubscribe_AsyncSendDoesNotBlockResponse`,
  `TestSubscribeHandler_Close_ReleasesInFlightClaimAndRetrySendsImmediately`, `TestNewSubscribeHandler_NoGoroutineLeakAcrossConstruction`).
- `cmd/opencircuit/main.go` — `mountAndServe` builds an `*http.Server`, adds `signal.Notify`(`SIGTERM`/`SIGINT`)-driven graceful shutdown calling
  `subscribeH.Close` then `srv.Shutdown` on one shared `shutdownTimeout` (20s, new const) context.
- `cmd/opencircuit/shutdown_wiring_test.go` — new: `TestMountAndServe_SIGTERMReleasesInFlightClaim`, the real-signal end-to-end proof; a local
  `blockingTestMailer` twin (the `internal/handlers` one is unexported and unreachable from this package) and a `readConfirmSentAt` helper.
- `deploy/systemd/opencircuit.service` — added `KillSignal=SIGTERM`, `TimeoutStopSec=30`.
- `issues/0026.md` (working copy, not committed — see `## Gotchas` below) — corrected the false "no worse than before" claim in `## Gotchas`.

### Bounce-fix files (`ab65d0a`)

- `internal/handlers/subscribe.go` — `enqueueSend`: `sendWG.Add(1)` moved from before `closeMu.Lock()` to the branch that runs after the `closed`
  check has observed `false`, still holding `closeMu`; the closed-drop path no longer calls `sendWG.Add`/`Done` at all. Doc comment rewritten to state
  the actual happens-before argument (mutex handoff + go-statement edge to `Close`'s `Wait()`), not just "closeMu is held across the select".
- `cmd/opencircuit/main.go` — `mountAndServe`: `srv.Shutdown(ctx)` now runs before `subscribeH.Close(ctx)` (was the reverse); a non-nil `Shutdown`
  error is captured in `shutdownErr` and returned only after `Close` has still run. Added `defer signal.Stop(sigCh)` immediately after
  `signal.Notify`, covering every return path including the listener-failed (`errCh`) branch.

## Gotchas

- **`issues/0026.md`'s `## Gotchas` correction was made in the working copy, not committed with the code.** Per this issue's own instructions ("commit
  code only... do not commit the issue markdown"), `issues/0026.md` is left as an uncommitted working-copy edit alongside this issue's own `Status`
  staying at `in-progress` — both are intentional, not omissions. The correction itself: struck the "no worse than before this fix" sentence and
  replaced it with a dated correction citing `#0081`'s reproduction and fix, matching the file's own convention for superseded Gotchas elsewhere in
  that document (`~~struck~~` + a following note).
- **`internal/middleware`'s exclusion no longer applies.** The earlier verification pass excluded it because `#0080`'s in-progress throwaway
  reproduction file was failing by design. That subagent's fix landed since (per this issue's own `## Review notes`), and the bounce-fix
  verification pass (2026-08-19, `ab65d0a`) confirms `internal/middleware` is `ok` along with every other package — `go test ./... -count=1` is
  green with zero exclusions now.
- **Residual 1 (flagged by the review, decided here): the shared-context bound is kept single, not split per step.** Reordering to
  `srv.Shutdown`-then-`subscribeH.Close` moves the "expired context handed onward" edge case from `Close`→`Shutdown` (the reviewer's finding) to
  `Shutdown`→`Close`: if `srv.Shutdown` itself consumes most or all of `shutdownTimeout` (a handler genuinely stuck, not the send-queue path this
  issue's `sendCtx` cancellation already makes fast), `Close` then runs against an already-expired remainder and can return `context.DeadlineExceeded`
  with claims still unreleased. **Decision: leave the single shared `shutdownTimeout` context as-is, not two independent deadlines.** The reviewer's
  own verdict on the pre-reorder version of this same edge case was "bounded, documented, strictly better than pre-fix, and only reachable if [a step]
  ignores cancellation... accepted, not blocking" — that reasoning is unchanged by which step goes second, and `shutdownTimeout`'s own doc comment
  already frames it as "bounds BOTH steps below combined (one shared deadline, not one each)" by design, not an oversight. Splitting it into two
  independent budgets is a real option for whoever revisits this (e.g. `min(remaining, someFloor)` for the second step) but is out of this issue's
  small, precisely-scoped lane; not done here.
- **Residual 2 (flagged by the review, decided here): no `ctx.Err()` short-circuit was added to `processSendJob`/`runSendWorker`.** A mailer that
  ignores context cancellation still receives every already-queued job in full after `Close` starts (the reviewer measured 30 queued → 30 `Send`
  calls, 0 released, for such a mailer). `SESMailer`'s real AWS SDK call does honour context cancellation, so this doesn't affect production today.
  **Decision: not fixed in this pass.** Adding an `if ctx.Err() != nil { release; return }` check at the top of `processSendJob` is a reasonable
  follow-up, but it's an enhancement to a case that doesn't reproduce against the real mailer, not a defect this issue's three specified fixes cover;
  flagging it explicitly (as the issue instructions require) rather than silently leaving it unaddressed. `#0045`'s campaign send worker should decide
  this for itself rather than inheriting a default from here, since a duplicate campaign send is far more visible than a duplicate confirmation email.
- **`signal.Stop(sigCh)` is now in place**, immediately after `signal.Notify`, via `defer`. Confirmed it does not disturb the SIGTERM end-to-end test
  (`TestMountAndServe_SIGTERMReleasesInFlightClaim` still passes) or any other `cmd/opencircuit` test under `-count=2`/`-race -count=3` — see
  `## Verification`.
- **`Close` cancels `sendCtx`, not `job.release`'s own compensating context.** `releaseSendClaim` deliberately builds its own fresh 5s
  `context.Background()`-derived context rather than reusing `sendCtx` — if it reused `sendCtx`, a shutdown-driven cancellation would defeat the very
  release call that shutdown exists to guarantee. This was already true before this issue (carried in from `#0026`'s design); worth restating because
  it's the reason `Close` works at all rather than being a second thing this issue had to add.
- **Whether a shutdown-interrupted send might have actually landed at the provider before the context cancellation reached it is not something this
  fix can resolve.** `SESMailer.Send`'s context cancellation races the SES API call the same way any HTTP client's context cancellation does — if the
  send request had already reached SES and the response was merely still in flight when cancelled, the claim is released and a retry sends a **second**
  email to that address. This is the same trade-off `#0026`'s original design already accepted for an ordinary network timeout/error (see
  `subscribe.go`'s package doc comment: "the next legitimate request for that address attempts the send again") — this issue does not change that
  trade-off, only makes shutdown take the same path instead of leaving the claim stranded. A duplicate confirmation email is a mild annoyance; a
  stranded claim silencing a real subscriber for an hour is the defect this issue exists to fix. Worth knowing for `#0045`, which reuses this pattern
  for real outbound campaign sends where a duplicate is more visible.
- **`shutdownTimeout` (20s) is a new package-level const in `cmd/opencircuit/main.go`, chosen, not derived from anything upstream.** It bounds
  `subscribeH.Close` and `srv.Shutdown` combined, on one shared context. In steady state this returns in well under a second (`sendCtx` cancellation
  makes a stuck send fail almost immediately if the mailer respects context, which `SESMailer`'s AWS SDK call does); 20s is a ceiling for the case
  where it doesn't, not an expected wait. `deploy/systemd/opencircuit.service`'s `TimeoutStopSec=30` must stay above this constant, or systemd
  `SIGKILL`s the process before the graceful sequence can finish on its own — flagged here since the two live in different files with no compiler
  link between them.
- **Neither `sendJobTimeout` (30s) nor the resend cooldowns (1h each) were changed.** The issue asked to flag this explicitly if either moved; neither
  did, so the 30s+5s-vs-1h ratio `ClaimConfirmationSend`'s unconditional-`NULL`-release safety argument depends on (see `#0026`'s `## Review
  verification` §6) is unchanged.
- **A token-sweep ticker was not built.** Per the issue's own note: it cannot distinguish an in-flight claim from a dead one, and a sweep that releases
  claims older than the cooldown is a no-op, since the cooldown predicate already expires them on its own. `Close`'s context-cancellation approach is
  the actual fix; no sweep exists in this codebase for this queue.
- **Not independently verified against a real SES send under a real `SIGTERM`.** Every test here uses `mailing.RecordingMailer` or a test-only
  blocking mailer, per `CLAUDE.md`'s "no network calls in the suite" convention (also true of `#0026`'s own tests) — the interruption mechanism
  (context cancellation reaching an in-flight AWS SDK call) is standard `net/http`/AWS-SDK-for-Go behavior, not something specific to this code, but it
  was not exercised against the real network here.

## Relation

- Depends on: [#0026](0026.md) — Build the subscribe endpoint with anti-abuse controls
- Related: [#0045](0045.md) — Build the send worker
