# 0447 — Nothing reloads Apache after a certbot renewal, so the site will serve an expired cert

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | deploy / TLS |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |

## Description

`certbot-renew.timer` runs and will renew the wildcard cert. **Nothing then
tells Apache to pick up the new one.** `mod_ssl` keeps serving the certificate
it loaded at start, so the renewal succeeds and the site goes on presenting the
old file until it expires.

The current cert is valid to **2026-11-16**. Renewal fires around 30 days out,
so from roughly **2026-10-17** the box holds a fresh cert that nothing is
serving, and on 2026-11-16 `www.opencircuitsf.com` starts throwing certificate
errors in every browser — along with `go.opencircuitsf.com` and every other name
the wildcard covers.

## How it was measured

`#0436`'s reviewer established it read-only, four independent ways:

- `/etc/letsencrypt/renewal-hooks/deploy/` — **empty**
- `/etc/letsencrypt/renewal-hooks/post/` — **empty**
- `/etc/sysconfig/certbot` — `PRE_HOOK=""`, `POST_HOOK=""`, `DEPLOY_HOOK=""`
- `certbot-renew.service` — a bare `ExecStart=/usr/bin/certbot renew --quiet`

And `/etc/letsencrypt/renewal/opencircuitsf.com.conf` has **no `installer =`
line**, so certbot itself never touches the web server either. That absence is
correct and deliberate — it is what keeps renewal from rewriting the vhost that
carries the `/.well-known/` carve-out (`CLAUDE.md` §7) — but it means the reload
has to come from somewhere else, and it does not.

## Why nobody noticed

The cert was issued recently enough that the timer has never had a renewal to
deploy. Everything observable says healthy: the timer fires on schedule, the
cert is valid, the site serves correctly. The failure is scheduled, not present.

`#0436` corrected the runbook's certbot command and, in the same pass, wrote
that "Certbot's own renewal timer needs no manual step here" — the opposite of
what the box shows. That sentence is `#0436`'s to fix; **the missing hook is
this issue's**, and it is a change to the box, which `#0436`'s criteria forbid.

## Acceptance criteria

1. A deploy hook exists that reloads Apache after a successful renewal — either
   a script in `/etc/letsencrypt/renewal-hooks/deploy/` or `DEPLOY_HOOK` in
   `/etc/sysconfig/certbot`. Pick one and say why.
2. It must reload rather than restart, and must be a **no-op when nothing
   renewed** — a deploy hook runs only on successful renewal, which is why it is
   the right hook of the three.
3. **Prove it without waiting for expiry.** `certbot renew --dry-run` exercises
   the renewal path; confirm the hook is invoked. A dry run does not deploy a
   real cert, so say explicitly what your proof does and does not establish.
4. The asset belongs in the repo under `deploy/`, not only on the box, so a
   rebuilt server gets it. Wire it into `docs/deployment.md`'s install steps.
5. **This is a change to production.** It needs the user's approval before it is
   applied to the box. Prepare it, prove it locally, and say exactly what needs
   running — do not apply it unasked (`CLAUDE.md` §5b, §9).
6. Do not touch the vhost, the renewal conf, or the `installer` absence. The
   `/.well-known/` carve-out serving this domain's Bluesky DID is in that vhost
   and the Go service 404s that path.

## Relation

- `#0436` corrected the certbot command and its reviewer found this.
- `#0435` is the **backup** timer, a different unit entirely — no overlap.
- `CLAUDE.md` §7 records the cert, the authenticator, and the carve-out.

Verify the box read-only with `systemctl cat certbot-renew.service`, `sudo ls /etc/letsencrypt/renewal-hooks/deploy/`, and `sudo certbot certificates`. **Change nothing without approval.**

## Verification

```
uptime                                                       # 16:19 up 17 days, load averages 1.89 1.93 1.97
ISSUE=0447 scripts/check.sh go ./internal/db/... ./internal/handlers/...
```

```
ok  	github.com/brennanMKE/OpenCircuitSF/internal/db        0.639s
ok  	github.com/brennanMKE/OpenCircuitSF/internal/handlers  6.240s
```

Zero skips; both packages show real elapsed time, not `0.00s`. **What this
confirms and what it does not:** these are the repo-wide citation and
metadata guards (`CLAUDE.md` §5, `internal/handlers` walks the whole repo
tree; `internal/db` scans `issues/`, `PRD.md`, `CLAUDE.md`,
`migrations/`, `docs/database.md`) plus this file's own `_test.go` suites —
they confirm this issue file and its Work log don't contain a dangling
Go-file-plus-line-number citation or an unresolved path/section reference,
and that nothing else in the tree regressed. **No test in either package
reads `docs/deployment.md`** — confirmed by byte-scanning both packages'
`_test.go` files for the string, twice, before writing this — so a green
run here says nothing about whether this issue's actual substance (the hook
script's behavior, the docs wiring) is correct. That is established
separately above by `bash -n`, `shellcheck`, and the local invocation test,
plus the box-side read-only re-verification recorded in the Work log — not
by this Go test run.

Local shell checks (not part of `scripts/check.sh`, since it has no
Bash-script scope beyond `scripts/check.sh guards`):

```
bash -n deploy/certbot/reload-apache-deploy-hook.sh   # clean
shellcheck deploy/certbot/reload-apache-deploy-hook.sh # clean, exit 0
```

## Work log

- 2026-09-06 — **Implementation pass (Sonnet).** Re-verified the four
  read-only findings directly via `ssh ec2` rather than trusting the filing:
  `systemctl cat certbot-renew.service` still shows the bare
  `ExecStart=/usr/bin/certbot renew --quiet`;
  `/etc/letsencrypt/renewal-hooks/deploy/` is still empty;
  `/etc/sysconfig/certbot` still has `PRE_HOOK=""`, `POST_HOOK=""`,
  `DEPLOY_HOOK=""`; `sudo certbot certificates` still lists the
  `opencircuitsf.com` cert (`Domains: opencircuitsf.com *.opencircuitsf.com`,
  expiry 2026-11-16 20:12:34 UTC). Nothing on the box changed. One read from
  the prompt's suggested list was unusable as given: `apachectl -S` returned
  `apachectl: The "-S" option is not supported` on this box's `apachectl`,
  not a permission refusal — noted rather than worked around.

  Also confirmed the box renews certificates for five other, unrelated
  domains under the same certbot install (`beerbeerbeer.me`,
  `eurekaplatforms.com`, `gregariousbots.social`, `sstools.co-wildcard`,
  `terriblename.com`), all sharing this Apache process — which is part of
  why a deploy-hooks-directory script (fires for any of them) is the better
  fit than a per-lineage `DEPLOY_HOOK`.

  **Criterion 1/2.** Added `deploy/certbot/reload-apache-deploy-hook.sh`, a
  certbot deploy hook: it requires `RENEWED_LINEAGE` (set by certbot only
  for real deploy-hook invocations) so it refuses to run any other way, runs
  `httpd -t`, and only on success runs `systemctl reload httpd` — reload,
  never restart. Chose the deploy-hooks-directory form over `DEPLOY_HOOK` in
  `/etc/sysconfig/certbot` because the box's `certbot-renew.service` (read
  above) never sources that file or splices its variables onto the command
  line, so `DEPLOY_HOOK` would sit inert for the timer path without also
  editing the systemd unit. A directory script needs no such edit — certbot
  scans `/etc/letsencrypt/renewal-hooks/deploy/` unconditionally on every
  `renew` invocation. The "no-op when nothing renewed" requirement is met by
  the hook type itself (certbot only invokes a deploy hook after a
  successful renewal), not by logic in the script.

  **Criterion 3.** Read `certbot --help renew` (on the box, read-only) to
  get this right rather than assume it: plain `--dry-run` does **not** run
  deploy hooks at all; `--run-deploy-hooks` is required, and per that same
  help text it runs the hook only if the dry run succeeds and does so
  against `RENEWED_LINEAGE` pointing at the **real, currently active**
  certificate, not the discarded staging one. Wrote
  `sudo certbot renew --dry-run --run-deploy-hooks` into the approval
  request accordingly, plus a paragraph in `docs/deployment.md` §9 stating
  plainly what that proves (the hook is discovered, runs, `httpd -t` and the
  reload both succeed, Apache keeps serving) and what it does not (no real
  certificate is issued or deployed; the timer unit's own unflagged
  invocation was not exercised, only the same underlying directory-scan
  mechanism it also relies on). Nothing was run on the box — this command is
  part of the pending approval request, not something this pass executed.

  **Criterion 4.** Added `deploy/certbot/reload-apache-deploy-hook.sh` and
  `deploy/certbot/README.md`, and rewrote the tail of `docs/deployment.md`
  §9 (the paragraph that previously said only "a gap … tracked separately as
  `#0447`") into the hook's install/verify/prove sequence, matching how
  `deploy/apache/` and `deploy/systemd/` are already wired into that
  document.

  **Criterion 5.** Nothing was applied to the box. `docs/deployment.md` §9
  now states in its own words that the install/dry-run sequence needs the
  user's approval before any of it runs, and this report gives the same
  sequence standalone.

  **Criterion 6.** No installed vhost, no `renewal/opencircuitsf.com.conf`,
  and no `installer =` line was touched, on the box or in the repo. The hook
  only calls `httpd -t` and `systemctl reload httpd`.

  Verified locally: `bash -n deploy/certbot/reload-apache-deploy-hook.sh`
  and `shellcheck deploy/certbot/reload-apache-deploy-hook.sh` both clean;
  invoking the script with `RENEWED_LINEAGE` unset (via an explicit
  `bash -c`, not the interactive shell directly, per `CLAUDE.md` §8) exits 1
  with the intended refusal message before it ever reaches `httpd -t`.

  Did not touch `#0435` (backup timer, unrelated unit) or `#0464` (systemd
  measurement proposal); both stay open and separate, as instructed.
