# 0374 — The calibration fixtures cannot detect a flattening step that does nothing

| | |
|---|---|
| **Status** | open |
| **Platform** | Server |
| **Module** | outbox |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-01 |
| **Commit** | — |

## Description

Every one of the 24 rows in `claimCommentGuardDirectionalCalibrationCases` is a
**single-line** comment fixture. So `strings.Join(strings.Fields(text), " ")` —
the flattening step `#0366` factored into `claimCommentGuardFlattenComment` — is
a **semantic no-op on all of them**.

Measured by `#0366`'s review: replacing that function's body with
`return text` leaves the entire `internal/outbox` package **green**.

**Pre-existing, not introduced by the extraction.** The same review confirmed
the mutation passes identically on the pre-`#0366` file with both inline copies
mutated. The extraction neither created nor widened this.

It matters because the real corpus is not single-line: `internal/outbox/store.go`
wraps the phrase the `store.go` sentinel depends on across two comment lines, and
`#0342`'s site C was found only after joining wrapped text. Flattening is
load-bearing **in the tree** and unpinned **in the table**.

## The recommendation this issue carries

`#0366`'s reviewer recommended **recording, not closing** — and gave the reason:
closing it means adding a multi-line fixture whose only purpose is to exercise
the flattening step, which is exactly the `#0330` / `#0347` / `#0356` shape those
three families ended by refusing.

`#0258`'s caution is now explicitly binding here: the guard file carries **six**
in-file mechanisms, and each new one is fresh mutable surface.

**So the default deliverable is one honest sentence, not a fixture.** If a pass
believes a multi-line fixture genuinely earns its place, it must say why this
case differs from the three that were refused — and be prepared for that
argument to be rejected.

## Acceptance criteria

1. The limit is **recorded where a reader of the flattening function meets it**
   — in `claimCommentGuardFlattenComment`'s own doc comment — naming precisely
   what is unpinned: every calibration fixture is single-line, so the table
   cannot detect a no-op flattening.
2. It also records that the step **is** load-bearing against the real tree, and
   names an example (`store.go`'s two-line wrapped phrase; `#0342`'s site C), so
   the next reader does not conclude the function is dead code and delete it.
   **That is the live risk of recording rather than closing** — address it
   explicitly.
3. The claim is **re-verified, not transcribed**: reproduce the `return text`
   mutation yourself, confirm the package stays green, and confirm it also stays
   green on the pre-`#0366` file so the pre-existence claim holds. Five issues
   this phase received a wrong identifier because prose was transcribed as though
   measured.
4. **Do not add a fixture** unless criterion 1's exception argument is made and
   holds. If you add one, it must discriminate — prove the `return text`
   mutation now fails, naming the row.
5. The axis stays frozen (`#0358` criterion 4). No window logic, marker set,
   clamp, or every-identifier change.
6. No `.ClaimDue(` / `.OrphanSweep(` / `.SelectDue(` call-syntax literal enters
   any Go comment; floor harness stays **42 total / 12 non-exempt** (`#0323`);
   `gofmt` stability.

## Relation

- Found by: [#0366](0366.md)'s review
- Depends on: [#0366](0366.md) — which factored the flattening into one function
- Relates to: [#0342](0342.md) — site C, found only by joining wrapped text
- Relates to: [#0330](0330.md), [#0347](0347.md), [#0356](0356.md) — the three
  families that ended by recording rather than closing
- Relates to: [#0258](0258.md) — the six-mechanism caution now binding on this
  file
- Relates to: [#0375](0375.md) — the sibling latent gap from the same review

## Work log

- 2026-09-01 — Filed by the orchestrator from `#0366`'s review (§9).
