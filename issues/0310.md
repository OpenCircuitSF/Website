# 0310 — The live-region guard says two `{#key}` blocks where there are three

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Web |
| **Module** | web / accessibility |
| **Phase** | 8 — Polish and follow-ups |
| **First seen** | 2026-08-27 |

## Description

Three comments in `web/src/lib/liveRegionGuard.structuralGuard.test.ts` (around
lines 411, 555, and 1546) say *"the two real `{#key activeId}` blocks
(`Workshops.svelte`, `Campaigns.svelte`)"*.

**There are three**: `Workshops.svelte:162`, `Campaigns.svelte:186`, and
`Campaigns.svelte:190` — `Campaigns.svelte` has two. The slip is files counted
as blocks.

**The load-bearing half is correct**, and `#0306`/`#0307`'s review verified it
independently by AST scan: none of the three wraps a live-region element, and
all three wrap exactly one child component. So `#0307`'s conclusion that the
`KeyBlock` fix is latent stands, and nothing is broken.

What makes it worth fixing rather than ignoring: **the sentence advertises itself
as measured** — "checked via the same AST walk, not assumed" — while carrying a
count that was not. A comment that claims to be a measurement is relied on as
one, which is `#0263`'s class.

## Acceptance criteria

1. All three comments say **three**, and name the third site
   (`Campaigns.svelte:190`) so the count can be checked against the tree without
   re-deriving it.
2. Re-derive the count yourself before writing it. Do not copy the number from
   this issue — that is the mistake being fixed.
3. While there, check the other "checked via the AST walk" claims in that file
   for the same files-vs-blocks slip, and report what you found even if nothing.
4. No behaviour change: this is comments only. `git diff` filtered to non-comment
   lines must be empty.
5. `ISSUE=NNNN scripts/check.sh web` passes, and the site counts are unchanged
   (38 / 61 / 32 / 20).

## Relation

- Found by: [#0306](0306.md)/[#0307](0307.md)'s review, by AST scan
- Relates to: [#0307](0307.md) — which added the `KeyBlock` handling these
  comments describe
- Relates to: [#0263](0263.md) — comments that credit a mechanism or a
  measurement the code does not have

## Work log

Re-derived the count independently rather than copying the issue's own
number: `grep -n '{#key' web/src/views/admin/Workshops.svelte
web/src/views/admin/Campaigns.svelte` returns exactly three lines —
`Workshops.svelte:162`, `Campaigns.svelte:186`, `Campaigns.svelte:190` — and
inspection of each site confirms every one wraps exactly one child component
(`WorkshopEditor`, `CampaignEditor`, `CampaignStats` respectively), not a
`role="status"/"alert"` element directly, matching what the existing
comments already claimed about the load-bearing half.

Fixed all three "the two real `{#key activeId}` blocks" comments (lines 411,
~557, ~1546 as the issue named) to say "three" and name all three sites with
line numbers, per criterion 1.

Criterion 3 — swept every other "checked via the AST walk"/"checked, not
assumed"/"AST scan" claim in the file (`grep -n "AST\|checked via\|not
assumed\|re-measured"`) for the same files-vs-blocks slip:

- Line 1 header, line 157/164 (package doc comment on the AST-only
  instrument) — no site count, not affected.
- Line 408-409 (the "zero real sites hit any of the three" `{#each...{:else}`
  / `{#await}` claims) — these ARE file-level claims ("has no matches
  anywhere in web/src" / "does not appear anywhere in the tree"), correctly
  phrased as zero, not miscounted as blocks-vs-files.
- Line 525 ("zero real sites hit this today ... see the re-derived counts in
  the real-tree test's own comment") — a cross-reference, not itself a count.
- Line 1197 (script-AST parsing note) — no site count.

**Nothing else carries the files-vs-blocks slip.** The three fixed comments
were the only ones asserting a specific non-zero count of real sites for a
newly-tracked block kind.

## Verification

`git diff` for `web/src/lib/liveRegionGuard.structuralGuard.test.ts` filtered
to non-comment lines is empty (all three hunks are comment-only edits;
confirmed by reading the diff directly — no code line touched).

`ISSUE=0310 scripts/check.sh web`: PASSED — `npm run check` 287 files/0
errors/0 warnings; `npm test` 40 files, 3983 tests passed, including the
real-tree site-count assertions this issue's criterion 5 requires stay
unchanged (38/61/32/20 — asserted elsewhere in this same file and unaffected
by a comment-only change).
