# 0052 — Build the workshops admin UI

| | |
|---|---|
| **Status** | resolved |
| **Module** | workshops / admin |
| **Platform** | All |
| **Phase** | 6 — Workshops |
| **First seen** | 2026-08-18 |
| **Closed** | 2026-08-22 |
| **Commit** | `0089855`, `b562800` |

## Description

The admin screen for creating and editing workshops, with Markdown body editing and interest tagging.

## Root cause

Not a bug — this was unbuilt: #0051 shipped the workshops CRUD API with no
admin UI on top of it. Nothing to diagnose, only to build.

## Fix

Added a Workshops subtab to `Admin.svelte`: a `Workshops.svelte` master list
(sortable by date, status badge, a "New workshop" title-only create modal)
and a `WorkshopEditor.svelte` detail view (every field, a Markdown textarea
with a client-side live preview, interest-tag checkboxes reusing the
existing taxonomy from `lib/admin.ts`, and Publish/Unpublish/Cancel/Delete
actions each behind a confirmation modal). All non-trivial logic — status
vocabulary, transition offers, datetime-local↔RFC 3339 conversion, form
validation, sort order, the 409 conflict check — lives in a new pure module
`lib/workshopAdmin.ts`, unit-tested with no DOM. Body preview rendering
lives in a second new pure module, `lib/markdown.ts` (see Gotchas).

Two acceptance criteria could not be built as literally worded, because the
already-committed #0051 API contract does not support them:

- **Slug**: "editable before first publish and locked after" — #0051's
  `POST /admin/workshops` ignores any client-supplied `slug`, and
  `PATCH /admin/workshops/{id}` has no `slug` field at all. There is no route
  that can ever change a slug, before or after publish. Built as always
  read-only, with the reasoning shown inline in the editor and documented in
  `lib/workshopAdmin.ts`'s header comment (`isSlugEditable()` always returns
  `false`) — same shape as `lib/workshops.ts`'s (#0053) own flagged
  discrepancy against #0051.
- **Cover image**: "upload or path entry" — #0051 has no upload endpoint
  (no multipart route, no storage integration), and adding one is a backend
  change outside this issue's admin-UI-only scope. Built as path/URL text
  entry only.

Both are called out in the code, in `## Gotchas` below, and left as open
questions for the reviewer rather than silently narrowed.

### Fix pass 2 (commit `b562800`) — closing the two blocking review findings

**Finding 1 (`web/src/lib/markdown.ts`, link-scheme bypass).** `isSafeLinkHref`
checked the raw destination text, but a browser strips ASCII tab/CR/LF
anywhere in a URL and strips leading C0 controls before resolving it, so a
destination containing one of those characters didn't *look* like it had a
scheme, fell through to the "no scheme = relative = safe" branch, and then
resolved to `javascript:` once the browser normalized it. Fixed by rejecting
outright any destination containing a character below U+0020 or U+007F,
before the scheme allowlist ever runs (`HAS_CONTROL_CHAR` in
`markdown.ts`) — the "simplest defensible rule" the review suggested, rather
than a per-string denylist. This closes all five confirmed-executable inputs
from the review's table.

Also fixed the same-file correctness bug the review flagged alongside it:
`renderInline` previously substituted links first and then ran the
bold/italic regexes over the *whole* line, so `_`/`*` characters inside an
already-built `href="..."` attribute got rewritten in place (`[x](/a_b_c)` →
`href="/a<em>b</em>c"`). `renderInline` now does a single left-to-right scan
that applies emphasis only to the literal text between link matches, so
emphasis regexes never see already-built link markup.

The module header comment's "safe to render with `{@html}`" claim is now
accurate under this model (unconditional escaping first, link destinations
validated against their post-normalization form) and says so explicitly,
rather than the narrower/incorrect claim the review found holed.

**Finding 2 (modal focus).** Confirmed the review's diagnosis: all
`modal-backdrop`/`modal` pairs in `Workshops.svelte` (the create modal) and
`WorkshopEditor.svelte` (the transition-confirm and delete-confirm modals —
3 `modal-backdrop` divs total across the two files; the delete-conflict state
is a content swap inside the same delete modal, not a fourth backdrop) had a
correct Escape handler on the panel but nothing ever moved focus into it, so
keydown never reached either handler. Fixed by adding `bind:this={...ModalEl}`
plus a `$effect` per modal that calls `void tick().then(() => el?.focus())`
once its open-state goes true — the same shape already working in
`CampaignSendDialog.svelte`. Also dropped the now-redundant
`e.stopPropagation()` on the panel's non-Escape keydown branch, per the
review's suggested fix (no keyboard analogue of click-outside-to-dismiss).
Corrected the header/doc comments in both files to describe this shape rather
than the unreached one.

## Verification

```
uptime                                   # 17:41, load average 3.23 3.00 2.99 — idle, not a factor
cd web && npm run check                  # svelte-check: 225 FILES 0 ERRORS 0 WARNINGS
cd web && npm test                       # vitest run: 18 test files, 424 tests passed (58 new)
cd web && npm run build                  # vite build succeeded, 198 modules transformed
ISSUE=0052 scripts/check.sh web          # re-ran both of the above through the canonical script: VERIFICATION PASSED
```

New test files, run and observed passing by name:

- `web/src/lib/markdown.test.ts` — 22 tests (`renderMarkdownPreview` block/inline
  rendering, HTML escaping, dangerous-link-scheme rejection; `isSafeLinkHref`).
- `web/src/lib/workshopAdmin.test.ts` — 36 tests (status labels/badges,
  publish/unpublish/cancel offer logic and confirm copy, datetime-local↔RFC
  3339 round-trips, interest toggling, date sort (asc/desc, undated-last),
  `validateWorkshopForm`'s full validation matrix, the 409 conflict check,
  and the form↔`AdminWorkshop` mappers).

Not verified by hand: no browser/Postgres run against a real admin session.
`STORAGE=json` has no workshops tables (CLAUDE.md §5), and a real Postgres
run needs a passkey ceremony to authenticate as admin, which isn't scriptable
in this environment. The wire contract (`AdminWorkshop`, `CreateWorkshopFields`,
`PatchWorkshopFields` in `lib/types.ts`/`api.ts`) was instead cross-checked
field-by-field against `internal/handlers/admin_workshops.go`'s `workshopView`,
`createWorkshopRequest`, and `patchWorkshopRequest` structs as committed by
#0051, and `npm run check` confirms every call site type-checks against those
shapes.

### Verification pass 2 (commit `b562800`) — the two bounced findings

```
uptime                                   # 18:12, load average 1.79 1.96 2.31 — idle, not a factor
ISSUE=0052 scripts/check.sh web          # svelte-check: 228 FILES 0 ERRORS 0 WARNINGS; vitest: 19 files, 450 tests — VERIFICATION PASSED
cd web && npm run build                  # vite build succeeded, 201 modules transformed
```

(File/test counts above include a concurrent agent's `#0054` work in the
shared tree — `web/src/lib/markdown.test.ts` alone is 33 tests, up from 22.)

**Finding 1, proved by execution, not eyeballing.** `web/src/lib/markdown.test.ts`
adds a `control-character scheme bypass (#0052 finding 1)` describe block
under `isSafeLinkHref` with one test per confirmed-executable input from the
review's table (tab-in-scheme, CR-in-scheme, tab-before-colon, leading
U+0001, leading NUL, plus a line-feed and trailing DEL variant) — all assert
`isSafeLinkHref(...) === false` — and a companion
`renderMarkdownPreview control-character link bypass` test asserting the
rendered output contains neither `<a ` nor `javascript:` for each. A sibling
`still accepts the legitimate cases` test re-asserts `https://…`, `/…`,
`mailto:…`, and `/a_b_c?utm_source=…` are still accepted. Ran and observed:
`npx vitest run src/lib/markdown.test.ts` — 33/33 passed.

Then cross-checked independently of the unit tests, against the actual
WHATWG URL parser (Node's `URL`, which implements the same algorithm a
browser applies to resolve an `href`), reproducing exactly the review's
methodology:

```
"java\tscript:alert(1)"   -> protocol= javascript:
"java\rscript:alert(1)"   -> protocol= javascript:
"javascript\t:alert(1)"   -> protocol= javascript:
"<U+0001>javascript:alert(1)" -> protocol= javascript:
"<NUL>javascript:alert(1)"  -> protocol= javascript:
```

confirming all five inputs really do resolve to `javascript:` when treated
as a relative URL — i.e. they were genuinely exploitable, not theoretical —
and that `isSafeLinkHref` (now proven above to reject all five) closes the
gap. Legitimate inputs (`https://example.com`, `/workshops/soldering-101`,
`mailto:hello@opencircuitsf.com`, `/a_b_c?utm_source=newsletter`) were run
through the same parser and resolve to their expected, non-`javascript:`
protocols.

**Finding 2, proved by driving a real browser, not by reading the diff.**
No passkey ceremony is scriptable in this environment (same constraint the
review's finding 6 accepted for pass 1), so the fix could not be exercised
inside the live authenticated admin console. Instead, the exact mechanism the
review used to *find* the bug — where focus lands relative to the backdrop
subtree, and whether the panel's keydown handler is reachable — was
reproduced faithfully in a real Chromium instance via Playwright: a
standalone page implementing the identical shape now in
`Workshops.svelte`'s create modal (`bind:this` equivalent, a microtask-based
`tick().then(() => el?.focus())` equivalent, and the panel's
`onkeydown={(e) => { if (e.key === 'Escape') close(); }}` with no
`stopPropagation`). Observed:

```
active element after open: panel
backdrop display before Escape: block
creating after Escape: false backdrop display after Escape: none
PASS: focus lands on panel and Escape closes it
```

This confirms the causal mechanism (focus moves into the panel subtree, so
the panel's own Escape handler now receives the key) works in a real browser
engine. It does **not** substitute for exercising the compiled Svelte
component inside an authenticated session — that remains unverified by hand,
same as the rest of this admin console per the original pass's finding 6.

New/changed test coverage, run and observed passing by name:

- `web/src/lib/markdown.test.ts` — 11 new tests: 8 under
  `isSafeLinkHref > control-character scheme bypass (#0052 finding 1)`
  (7 bypass-rejection cases + 1 legitimate-cases-still-pass case), 1 under
  `renderMarkdownPreview control-character link bypass`, and 4 under
  `renderMarkdownPreview link/emphasis ordering (#0052 finding 1, correctness
  bug)` covering the `_`/`**` in-href mangling and multi-link-per-line
  behavior.

## Files changed

- `web/src/lib/types.ts` — added `AdminWorkshop`, `AdminWorkshopsListResponse`.
- `web/src/lib/api.ts` — added `listAdminWorkshops`, `getAdminWorkshop`,
  `createWorkshop`, `updateWorkshop`, `deleteWorkshop`, and the
  `CreateWorkshopFields`/`PatchWorkshopFields` request shapes.
- `web/src/lib/workshopAdmin.ts` (new) — pure logic: status vocabulary,
  transition offers/confirm copy, datetime-local↔RFC 3339, interest
  toggling, date sort, form validation, the 409 check, form↔wire mappers.
- `web/src/lib/workshopAdmin.test.ts` (new) — 36 unit tests for the above.
- `web/src/lib/markdown.ts` (new) — the dependency-free Markdown→HTML
  preview renderer.
- `web/src/lib/markdown.test.ts` (new) — 22 unit tests for the above.
- `web/src/views/admin/Workshops.svelte` (new) — the list/create screen.
- `web/src/views/admin/WorkshopEditor.svelte` (new) — the detail/edit screen.
- `web/src/views/Admin.svelte` — added the `workshops` section: import, tab
  type, tab button, and render branch.

## Gotchas

- **Slug and cover-image discrepancies** — see `## Fix` above; both are
  documented inline in `lib/workshopAdmin.ts`'s header comment for whoever
  picks this up next (this issue's own criteria vs. #0051's committed API).
- **Blanking an optional field on Save does not clear it server-side.**
  `PATCH`'s optional fields distinguish "key absent → unchanged" from
  "key present as `""` → cleared" (see
  `internal/handlers/admin_campaigns.go`'s `normalizeOptionalCampaignField`),
  but this editor's `save()` (via `workshopAdmin.ts`'s `emptyToUndefined`)
  omits the key whenever a field is blank, so clearing a previously-set
  summary/location/signup URL/cover image and saving leaves the old value in
  place. This is not a workshops-specific bug — `CampaignEditor.svelte` has
  the identical `preheader === '' ? undefined : preheader` shape already
  committed and reviewed under #0047 — so this follows the codebase's
  existing, accepted convention rather than deviating from it. Flagging it
  here in case the reviewer wants it fixed in both places at once rather than
  separately.
- **No markdown preview endpoint exists for workshops.** Unlike campaigns
  (`POST /admin/campaigns/{id}/preview`, goldmark-rendered server-side),
  #0051 shipped no equivalent for workshops, so `lib/markdown.ts` is a
  minimal client-side renderer (headings, paragraphs, bold/italic, links,
  lists) rather than goldmark parity. It escapes all input before applying
  markup and allowlists link schemes (http/https/mailto/relative), so it is
  safe to render with `{@html}`, but it will not match goldmark's output
  exactly for anything outside that subset.
- **Git hygiene note for whoever reviews this**: a concurrent agent had
  `docs/database.md`, `internal/audit/actions.go`,
  `internal/handlers/admin_workshops.go`, `internal/workshops/store.go`, and
  `internal/workshops/store_test.go` staged in the shared working tree while
  this commit was made. The first commit attempt swept all of it in
  (`git commit` with no pathspec commits the whole index, not just
  explicitly-`git add`ed paths); caught immediately via `git show --stat`,
  undone with `git reset --soft HEAD~1` + `git restore --staged <their
  paths>` (no working-tree content touched), and re-committed with only this
  issue's 9 files. Confirmed via `git show --stat HEAD` on the corrected
  commit and `git status --porcelain` showing their files still modified and
  unstaged, untouched throughout.

## Review notes

> 🔴 Bounced 2026-08-22 — the Markdown preview renderer can emit a link the
> browser resolves to `javascript:`, so `{@html}` in the admin console is not
> safe as claimed. Code commit `0089855` stays in place; fix on top of it.

Phase-3 review, independent re-verification. Machine was idle throughout
(`uptime`: load average 2.53 at start — well under the ~8 threshold, so nothing
below is machine noise).

### What passed

- **Commit hygiene is clean.** `git show --stat 0089855` lists exactly the 9
  intended `web/` files (2007 insertions, 1 deletion) — no `docs/`, no
  `internal/`, no `cmd/`. The concurrent agent's five Go/doc files that were
  swept into the first commit attempt are not in the final commit, and nothing
  was lost: they landed intact in `cbc932e` (`#0134`) immediately afterwards.
  The `## Gotchas` account of the incident and its repair is accurate.
- **`scripts/check.sh web` — VERIFICATION PASSED**, re-run by the reviewer:
  `svelte-check` 225 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS; `vitest
  run` 18 test files, 424 tests passed. The 58 new tests were re-run by name
  with `--reporter=verbose` and every assertion read.
- **The tests have teeth.** Two mutations were injected and both were caught
  (4 failures, 54 passes): (a) `sortWorkshopsByDate` returning
  `[...undated, ...dated]` failed the undated-last test with a concrete
  `[1,2]` vs `[2,1]` diff; (b) `isSafeLinkHref` returning `true` for any scheme
  failed the `javascript:`/`data:`/`vbscript:` rejection tests. Both files were
  restored byte-for-byte from backups and `git status web/src/lib/` is clean.
- **The lib/component split is real.** `workshopAdmin.ts` (356 lines) holds
  every status literal, comparison, sort, conversion and validation; both
  `.svelte` files are markup plus wiring and contain no status string
  comparisons or `.trim()` of their own. This satisfies CLAUDE.md's rule and
  `#0094`'s concern.
- **The wire contract matches.** All 18 fields of `AdminWorkshop` were
  cross-checked one-by-one against `internal/handlers/admin_workshops.go`'s
  `workshopView` (names, JSON tags, optionality, `capacity` as number,
  `interest_ids` non-optional) and `AdminWorkshopsListResponse` against
  `workshopsListResponse`. No drift found.

### Finding 1 (blocking) — the Markdown preview renderer emits executable markup

`web/src/lib/markdown.ts`'s own header comment says it "allowlists link
schemes ... so it is safe to render with `{@html}`". It is not. The
scheme check runs on the *raw* destination text, but browsers normalise a URL
before resolving it — stripping ASCII tab/CR/LF anywhere in the string and
stripping leading C0 controls. `isSafeLinkHref` does neither, so a
destination that does not *look* like it has a scheme is waved through as
"relative" and then resolves to `javascript:`.

Attack attempts run against the committed module (results are verbatim
`renderMarkdownPreview` output; `[TAB]`, `[CR]`, `[U+0001]`, `[NUL]` stand for
literal control characters in the source text):

| Input | Emitted `href` | Verdict |
|---|---|---|
| `[x](javascript:alert(1))` | — link dropped, `<p>x)</p>` | blocked ✅ |
| `[x](JaVaScRiPt:alert(1))` | — link dropped | blocked ✅ |
| `[x]( javascript:alert(1))` (leading space) | — link dropped | blocked ✅ |
| `[x](data:text/html;base64,…)` | — link dropped | blocked ✅ |
| `[x](vbscript:msgbox(1))` | — link dropped | blocked ✅ |
| `[x](&#106;avascript:…)` | `&amp;#106;avascript:…` (single-decodes to an inert relative path) | blocked ✅ |
| `[x](java[TAB]script:alert(1))` | `java[TAB]script:alert(1` | **BYPASS** ❌ |
| `[x](java[CR]script:alert(1))` | `java[CR]script:alert(1` | **BYPASS** ❌ |
| `[x](javascript[TAB]:alert(1))` | `javascript[TAB]:alert(1` | **BYPASS** ❌ |
| `[x]([U+0001]javascript:alert(1))` | `[U+0001]javascript:alert(1` | **BYPASS** ❌ |
| `[x]([NUL]javascript:alert(1))` | `[NUL]javascript:alert(1` | **BYPASS** ❌ |

Every one of those five is confirmed executable, not theoretical — feeding the
emitted value to the WHATWG URL parser (the same algorithm a browser applies to
an `href`) returns `protocol = "javascript:"` and
`href = "javascript:alert(1)"` for all five:

```
"java\tscript:alert(1)"   -> protocol= javascript:  href= "javascript:alert(1)"
"javascript\t:alert(1)"   -> protocol= javascript:  href= "javascript:alert(1)"
"<U+0001>javascript:alert(1)" -> protocol= javascript:  href= "javascript:alert(1)"
"<NUL>javascript:alert(1)" -> protocol= javascript:  href= "javascript:alert(1)"
"java\rscript:alert(1)"   -> protocol= javascript:  href= "javascript:alert(1)"
```

The link regex is `\(([^)]+)\)`, so the payload cannot contain parentheses —
but that is not a mitigation, only an inconvenience: `javascript:location='…'`
needs none. `WorkshopEditor.svelte:335` renders the result with `{@html}` in a
page holding an authenticated admin session.

Escaping itself is sound and was probed separately: raw `<script>`,
`<img onerror>` and `<svg onload>` are inert in paragraphs, headings and list
items; quote breakout out of the `href` attribute is impossible because `"` and
`'` are already `&quot;`/`&#39;` by the time the attribute is built; the
`rel="noopener noreferrer"` attribute cannot be displaced. The single hole is
the scheme check.

**Also a plain correctness bug in the same function.** `renderInline` applies
the bold/italic regexes to a string that already contains the generated
`<a href="…">`, so any `*` or `_` inside a destination is rewritten *inside the
attribute value*:

```
[x](/a_b_c)      ->  <a href="/a<em>b</em>c" …>
[x](/a**b**c)    ->  <a href="/a<strong>b</strong>c" …>
```

Underscores in URLs are ordinary (`?utm_source=…`, snake_case paths), so this
silently mangles real links in the preview. Not a security hole — `<` inside a
quoted attribute is literal — but wrong, and untested.

**What the next pass must do:**

1. Normalise before deciding. Strip every ASCII tab/CR/LF from the destination
   and strip leading C0 controls (U+0000–U+001F) and whitespace, *then* run
   the scheme test on the normalised value, *and emit the normalised value* —
   not the original. Simplest defensible rule: reject outright any destination
   containing a character below U+0020, then keep the existing allowlist.
2. Apply the emphasis regexes **before** link substitution, or splice links out
   into placeholders so no inline pass can ever rewrite an attribute value.
3. Add tests for every row of the table above plus the `_`/`*`-in-href cases.
   The existing 22 tests cover only the naive `javascript:`/`data:` forms and
   pass against the broken code, which is why this shipped.
4. Correct the module header and this issue's `## Gotchas`: both currently
   assert the renderer is safe to `{@html}`, which the next reader will trust.

**On dependency-free vs. a vetted library.** The "no external CDNs" rule in
CLAUDE.md §9 governs what the *browser* loads at runtime, not what npm bundles
at build time — the SPA already builds from Svelte/Vite/TypeScript, so a
vendored renderer would not have violated it. Rolling one by hand was therefore
a choice, not a constraint, and this finding is the cost of it. The better
answer is neither: `internal/mailing/campaign_markdown.go` already renders
`body_md` with goldmark plus a sanitising pass, and campaigns already expose
`POST /admin/campaigns/{id}/preview`. A matching workshops preview endpoint
would give the admin a preview that is byte-identical to what the site actually
publishes, with zero new client-side attack surface. That is out of this
issue's admin-UI-only scope — **file it as a follow-up** (see below), and
harden `markdown.ts` now.

### Finding 2 (blocking, small) — Escape does not close the new modals either

The commit message, both component headers and `## Gotchas` all claim the four
new modals "close on Escape from the modal's own keydown handler rather than
relying on bubbling," and that this avoids `#0120`. Verified: **they do not
close on Escape**, for the mirror-image reason.

`#0120`'s premise is that focus is *inside* the panel, so a
`stopPropagation` guard on the panel eats the key. The new modals put a correct
Escape handler on the panel — but nothing ever moves focus into it. Neither
`Workshops.svelte`'s create modal (lines 184–210) nor `WorkshopEditor.svelte`'s
transition and delete modals (lines 470–535) has an `autofocus`, a
`bind:this` + `.focus()`, or a `<svelte:window>` listener; the panels carry
`tabindex="-1"` but it is never used. On open, focus stays on the trigger
button, which sits *outside* the backdrop subtree — so the keydown never
traverses either handler and Escape does nothing.

Contrast the working shape already in the tree:
`CampaignSendDialog.svelte:43` does `void tick().then(() => dialogEl?.focus())`.
(That component *is* still a genuine `#0120` instance — with focus correctly
inside, its panel's `onkeydown={(e) => e.stopPropagation()}` at line 77 then
eats the key before the backdrop's `handleKeydown` at line 67. `Campaigns.svelte`
line 234 and `CampaignEditor.svelte` line 831 carry the same shape. The
implementer's claim about those two files is correct.)

Net user-visible behaviour is unchanged from the rest of the console — Escape
does nothing either way — so this adds no *new* class of defect. It is blocking
only because three code comments and a commit message assert a fix that does
not exist, and the next reader will believe them.

**Fix:** add `bind:this={modalEl}` and `$effect(() => void tick().then(() =>
modalEl?.focus()))` to each of the four modals. That makes the existing Escape
handlers reachable and simultaneously fixes the initial-focus half of the
`aria-modal="true"` accessibility gap. Then the comments become true. Drop the
`e.stopPropagation()` on the non-Escape branch while in there — there is no
keyboard analogue of click-outside-to-dismiss, which is exactly what `#0120`
says.

### Finding 3 (not blocking) — rulings on the two unticked criteria

Both of the implementer's claims were verified against the committed API and
both are **accurate**. Neither is this implementer's fault, and neither should
be re-attempted in the UI.

- **Slug.** `createWorkshopRequest` has no `slug` field and
  `patchWorkshopRequest` has none either (its doc comment: "deliberately NO
  slug field — slug is immutable through this route"). There is no route that
  can change a slug, before or after publish. Building the field as read-only
  is the only honest option. **Where it lands:** file a follow-up against the
  workshops API. The criterion and the API disagree and one of them must move —
  and the `## Notes` rationale here (an edited slug breaks every shared link)
  actually argues *for* the API's position, so the likely resolution is
  amending this criterion to "auto-generated and immutable," the same way
  CLAUDE.md §7 records `#0064`'s criterion as a known defect to correct before
  implementing. Do not leave it as an unticked box on a resolved issue.
- **Cover image.** Confirmed by grep: there is no `multipart` handling and no
  upload route anywhere in `internal/handlers/` or `cmd/opencircuit/main.go`.
  "Upload **or** path entry" is a disjunction and path entry is built, so this
  criterion is arguably already satisfied as worded; the validation
  (`isSafeCoverImage`: site-relative `/…` or `http(s)://…`) is sound.
  **Where it lands:** file a follow-up for an upload endpoint if uploads are
  actually wanted, otherwise tick this box on the next pass and note that the
  "or path entry" branch is the one taken. Minor, worth a line in the follow-up
  either way: `isSafeCoverImage` accepts a leading `/`, which also admits the
  protocol-relative `//evil.example/x.png` — an off-site asset load, contrary to
  the self-contained-site rule in CLAUDE.md §9.

### Finding 4 (not blocking) — the 409 path is correct

Traced end to end. `store.go:675` maps SQLSTATE 23503 on
`email_campaigns_workshop_id_fkey` to `ErrHasCampaigns`;
`admin_workshops.go:518` answers `StatusConflict` with `cannot delete a
workshop that an email campaign still references; cancel it instead`;
`writeError` wraps it as `{"error": …}`; `api.ts` line 91 reads
`err?.error ?? err?.message`, so `ApiError.message` carries the server's exact
sentence; `confirmDelete` routes it through `isDeleteConflict(409)` into the
dedicated `deleteConflict` modal state, which renders that message plus its own
explanation. The "Cancel workshop instead" shortcut calls `switchToCancel`,
which closes the delete dialog and opens the cancel-confirm, whose
`confirmTransition` PATCHes `{status: 'canceled'}` — the exact value
`workshops_status_check` and the store accept. Matches what the server expects.
(Once Finding 2 is fixed the shortcut will also be reachable by keyboard on
open, which it currently is not.)

### Finding 5 (not blocking) — blanking an optional field does not clear it

Verified. `validateWorkshopForm`'s `emptyToUndefined` yields `undefined` for a
blanked field, `JSON.stringify` drops the key, and the server treats an absent
key as "unchanged" (`patchWorkshopRequest`'s pointer fields), so clearing a
summary/location/signup URL/cover image and saving leaves the old value.

The implementer's precedent claim is also correct:
`CampaignEditor.svelte:384` is literally `preheader: preheader === '' ?
undefined : preheader`, committed and reviewed under `#0047`. So this follows
the convention rather than deviating from it.

**Ruling: acceptable here, and it needs its own issue covering both places.**
Following an established convention is the right call for an implementer;
fixing it inside this issue would have left campaigns inconsistent and put an
unrequested behaviour change in an admin-UI commit. But the convention *is*
wrong — an admin who deletes the contents of a field and presses Save has
unambiguously asked for it to be empty, and getting the old value back with a
"Saved." confirmation is a silent data-integrity surprise. File one issue
covering `WorkshopEditor.svelte` and `CampaignEditor.svelte` together, since
the fix (send `""` and let the server's `normalizeOptionalCampaignField`-style
empty-means-clear path run) is the same shape in both. Not a reason to bounce.

### Finding 6 (not blocking) — the absence of a hand run against Postgres

Judged **sufficient for this issue**. The passkey ceremony genuinely is not
scriptable here, `STORAGE=json` genuinely has no workshops tables, and the
field-by-field cross-check the implementer substituted was re-done
independently above and holds for all 18 fields. The wire contract is proven.

What remains unproven is only in-browser wiring — that the subtab renders, the
sort button flips, the checkboxes bind — none of which any test can reach,
because it all lives in `.svelte` files (the gap `#0094` already tracks). That
is the accepted state of every admin view in this tree, not a new deficiency.

Worth recording, though: Finding 2 is exactly the class of defect a five-minute
hand run would have caught and no amount of type-checking ever will. When the
admin console next gets exercised by hand, Escape-closes-the-modal belongs on
the checklist.

### Summary for the next implementer

Two blocking items, both inside the files already committed:

1. **`web/src/lib/markdown.ts`** — close the control-character scheme bypass
   (normalise, then check, then emit the normalised value), stop the emphasis
   passes from rewriting `href` attribute contents, add tests for every bypass
   listed above, and correct the "safe to `{@html}`" claims in the module header
   and in this issue's `## Gotchas`.
2. **`Workshops.svelte` / `WorkshopEditor.svelte`** — focus each modal panel on
   open so the Escape handlers actually fire, then the comments claiming
   `#0120` was avoided become true.

Everything else — the list, the sort, the form, the interest tagging, the
status transitions, the 409 handling, the lib/component split, the type
contract — was re-verified and is correct. This is a narrow bounce on a
largely sound change.

Follow-ups for the orchestrator to file (none of them block this issue's next
pass):

- A workshops Markdown preview endpoint mirroring
  `POST /admin/campaigns/{id}/preview`, so the admin preview matches what
  goldmark actually publishes and `lib/markdown.ts` can be deleted.
- The slug criterion vs. `#0051`'s immutable-slug API — amend one of them.
- A cover-image upload endpoint, or amend the criterion to path entry only;
  include the protocol-relative `//host/…` gap in `isSafeCoverImage`.
- "Blanking an optional field does not clear it," covering both
  `WorkshopEditor.svelte` and `CampaignEditor.svelte`.

## Review notes (pass 2)

> 🟢 Resolved 2026-08-22 — both blocking findings from `## Review notes` are
> genuinely closed. Verified independently against the WHATWG URL parser and by
> driving the **real compiled Svelte components** in headless Chromium, not by
> reading the diff. Code commits `0089855` + `b562800` stand.

Second phase-3 review, of the fix pass only — the rest of the UI was re-verified
in pass 1 and is not re-litigated here. Machine idle throughout (`uptime`: load
average 1.96 at start, 1.46 at the verification run — far under the ~8
threshold, so nothing below is machine noise).

### Commit hygiene

`git show --stat b562800` lists exactly the four intended files —
`web/src/lib/markdown.ts`, `web/src/lib/markdown.test.ts`,
`web/src/views/admin/Workshops.svelte`, `web/src/views/admin/WorkshopEditor.svelte`
(201 insertions, 33 deletions). Nothing from the concurrent `#0054` or `#0135`
agents was swept in: no `internal/`, no `docs/`, no `web/src/views/WorkshopDetail.svelte`.

### Verification re-run by the reviewer

```
ISSUE=0052rev2 scripts/check.sh web    # svelte-check 228 FILES 0 ERRORS 0 WARNINGS;
                                       # vitest 19 files / 450 tests passed — VERIFICATION PASSED
```

(228 files / 450 tests include the concurrent `#0054` work sharing this tree.)

**The new tests have teeth — proved by mutation, both restored byte-for-byte
(sha256 verified against a backup taken before the mutation):**

- Weakening `HAS_CONTROL_CHAR` from `/[\x00-\x1f\x7f]/` to `/[\x00]/` failed
  **6** tests, with a concrete emitted value in the diff
  (`<a href="java<TAB>script:alert(1"`).
- Reverting `renderInline` to the old substitute-links-then-emphasise order
  failed **3** tests, showing the exact corruption
  (`href="/x<em>y"` … `href="/p</em>q"`).

They are behavioural assertions on full expected output strings, not
restatements of the implementation.

### Finding 1 — closed. The control-character bypass is dead, and I could not
find a new one.

All five confirmed-executable inputs from pass 1 are rejected, verified three
ways: `isSafeLinkHref` returns `false`; `renderMarkdownPreview` emits no `<a>`
at all; and nothing reaches a URL parser to be normalised back into
`javascript:`.

**I attacked it again from angles the last pass did not use.** 60 destinations
were fed through `renderMarkdownPreview`, the emitted `href` was HTML-unescaped
the way an attribute value is parsed, and the result was resolved with Node's
`URL` against `https://www.opencircuitsf.com/admin/workshops/1`. **Zero produced
a protocol outside `http:`/`https:`/`mailto:`, and zero injected any tag.** The
same battery was then re-run in a real browser (below), with `page.on('dialog')`
armed — no dialog ever fired.

Attempts that failed to break it, and *why* they fail — this is the part worth
recording, because it is what says the rule is load-bearing rather than lucky:

| Angle | Input | Result |
|---|---|---|
| Percent-encoded controls | `java%09script:`, `java%00script:` | `%` is not a valid scheme character, so the URL parser never enters scheme state — resolves relative. Percent-decoding happens *after* scheme parsing, never before. |
| Percent-encoded scheme | `%6a%61%76%61%73%63%72%69%70%74:alert(1)` | same — relative. |
| Unicode whitespace the parser strips | U+00A0, U+FEFF, U+2028, U+2029, U+3000 leading | rejected by `isSafeLinkHref`: JS `String.prototype.trim` treats all of these as whitespace, so `trim()` exposes the `javascript:` scheme to the allowlist. |
| Unicode whitespace JS does *not* trim | U+200B, U+0085, U+180E leading; U+200B/U+FEFF/U+00AD/U+200D/U+2028 spliced mid-scheme | accepted as a relative path — and that is correct: the WHATWG parser strips only C0-or-space (leading/trailing) and tab/CR/LF (anywhere), so these never become a scheme. Resolved protocol is `https:` in every case. |
| Homoglyph / full-width scheme | `U+FF4A` + `avascript:`, `javascript` + `U+FF1A`, Cherokee `U+13A0` + `avascript:` | scheme parsing is strict ASCII; no IDNA mapping is applied to schemes. Relative. |
| Entity encodings before/after unescaping | `&#106;avascript:`, `&#x6a;avascript:`, `&#x6a avascript:`, `&amp;#x6a;avascript:`, `java&#9;script:`, `java&#0;script:` | `escapeHtml` runs first and turns the leading `&` into `&amp;`, so the attribute value unescapes back to a *literal* `&#x6a;…`, which is not a scheme. Double-escaping is idempotent in the safe direction. |
| Attribute breakout | `x" onclick="alert(1)`, `x&quot; onclick=&quot;…`, `x><img src=q onerror=alert(1)>` | `"`/`'`/`<`/`>` are already entities by the time the attribute is built. Confirmed in the live DOM: the emitted `<a>` carries exactly `href,rel` and no event-handler attribute. |
| Raw `<script>`, `<img onerror>`, `<svg onload>` in the body | — | inert text; no such element exists in the preview DOM. |
| Scheme-relative and backslash forms | `//evil.host/x`, `\\evil.host/x` | accepted, and both resolve to `https://evil.host/x`. **Not a bypass** — an ordinary `[x](https://evil.host/x)` is allowed by design, so this grants nothing new. See the non-blocking note below. |
| Newline inside a destination | `[x](java<LF>script:…)` | `renderMarkdownPreview` splits on `\n` before inline rendering, so the two halves rejoin as a paragraph with a *space*: `java script:alert(1)`. The URL parser does not strip interior spaces, so it resolves to a relative path, `https:`. |

**Emphasis-ordering bug fixed and not over-corrected.** `[x](/a_b_c)` →
`<a href="/a_b_c" …>` with no `<em>`; `[x](/a**b**c)` keeps its asterisks.
Ordinary emphasis still renders everywhere it should: paragraphs, headings,
`<ul>`/`<ol>` items, multiple links on one line, text on both sides of a link,
`__bold__`/`**bold**`/`_em_`/`*em*`. Verified against the live preview pane in a
browser as well as at module level.

**The module header now matches the code.** It states the actual rule — reject
any destination containing a character below U+0020 or U+007F, before the
scheme test — and the `{@html}` safety claim is true under that rule. One
wording nit, not worth another pass: the summary line says destinations are
"validated against their post-normalization form", when the code in fact
*refuses* anything whose normalised form would differ. The paragraph two above
it states the real mechanism plainly, so no reader is misled.

### Finding 2 — closed, and verified in a real browser against the real components

**On the implementer's Playwright evidence: insufficient as offered, but the
conclusion is right.** A standalone page that re-implements "the identical
shape" proves the *browser* honours `focus()` + Escape — which was never in
doubt. It cannot prove the compiled Svelte 5 component does, and that is exactly
where the pass-1 defect lived: `$effect` dependency tracking and `bind:this`
timing are Svelte semantics, not browser semantics. A lookalike page cannot fail
the way the real one did.

So I built the real thing rather than accepting it. The actual
`Workshops.svelte` and `WorkshopEditor.svelte` from this tree were mounted by
Vite + `@sveltejs/vite-plugin-svelte` with `window.fetch` stubbed to serve the
`#0051` wire shapes, served on an unlikely port, and driven with Playwright
(Chromium, 900x800 — comfortably clear of the ~485 px minimum-width trap in
CLAUDE.md §8). Server ownership was proved before trusting a single byte: the
page carries a unique marker attribute for this build and it was asserted first
(CLAUDE.md §8b). **16 of 18 checks passed; both failures were diagnostics, not
defects — one was my own bad selector, the other is the non-blocking note
below.**

| Check | Result |
|---|---|
| create modal: `document.activeElement` is the `role="dialog"` panel, inside the backdrop subtree | ✅ |
| create modal: Escape with default focus closes it | ✅ |
| create modal: Escape **from inside the focused text input** closes it (bubbles to the panel) | ✅ |
| create modal: typing into the title field still works with `stopPropagation` dropped | ✅ |
| create modal: Enter in the title field still submits (`POST /admin/workshops` observed) | ✅ |
| transition (publish/unpublish/cancel) modal: focus lands on the panel; Escape closes | ✅ |
| delete modal: focus lands on the panel; Escape closes | ✅ |
| delete-conflict: **same** backdrop DOM node identity before and after the 409, one backdrop total | ✅ — the implementer's "content swap, not a fourth backdrop" claim is correct |
| delete-conflict: renders the server's exact 409 sentence | ✅ |

Dropping `e.stopPropagation()` on the non-Escape branch breaks nothing: there is
no `<svelte:window>` or document-level keydown listener anywhere in `web/src`,
and the only ancestors are other components' own backdrops (siblings, not
ancestors), so the bubbled key reaches nothing. The double Escape handler
(backdrop + panel) is idempotent — both just set the open-state false.

**The three doc comments are now accurate.** Both component headers describe
`bind:this` + `tick().then(focus)` on open, which is what the code does and what
the browser confirms.

### Non-blocking, for follow-up issues

1. **Focus escapes the delete modal when the 409 conflict state swaps in.**
   Reproduced: open Delete → press Delete → the server answers 409 → the
   `{#if deleteConflict}` branch removes the focused Delete button from the DOM,
   Chromium reverts `document.activeElement` to `<body>` (outside the backdrop),
   and Escape stops working. `$effect` does not re-fire because neither
   `deleteOpen` nor `deleteModalEl` changed. One Tab lands on "Cancel workshop
   instead" and Escape works again from there, so the dialog is not unreachable —
   just degraded. **Not a regression**: before `b562800` Escape never worked in
   any of these modals, so this is strictly an improvement that stops one state
   short. It belongs with the console-wide focus-management gap (no focus trap,
   no focus restoration on close) that `#0120` already circles.
2. **Emphasis inside link *text* no longer renders.** `[**bold**](/p)` now emits
   `<a …>**bold**</a>`; before the ordering fix the second pass caught it. This
   is the cost of the single left-to-right scan applying `applyEmphasis` only to
   the text *between* links. Cosmetic, in a renderer that explicitly disclaims
   goldmark parity, and it degrades to visible literal asterisks rather than to
   anything wrong. Worth a line in the "workshops preview endpoint" follow-up
   already queued from pass 1 — that endpoint would delete `markdown.ts` and the
   question with it.
3. **`//host/…` in a link destination** is accepted and resolves off-site. Safe
   for *links* (external `https://` links are allowed by design), but it is the
   same shape as the `isSafeCoverImage` protocol-relative gap pass 1 flagged —
   fold it into that follow-up so both get one decision.

### The two unticked criteria — still correctly unticked

Re-confirmed against the committed API, unchanged since pass 1: `#0051`'s
`createWorkshopRequest`/`patchWorkshopRequest` carry no `slug` field, and there
is no upload route anywhere in `internal/handlers/`. Both need an API change or
a criterion amendment, neither of which belongs in an admin-UI issue. The
orchestrator is filing those separately; the boxes stay unticked here.

### Summary

Both blocking findings are closed, the fix introduced no new defect I could
find, and the tests that guard the fix fail when the fix is removed. Resolved.

## Acceptance criteria

- [x] List view with status, date, and title; sortable by date
- [x] Create and edit form covering every field, with Markdown editing and preview for the body — fixed in `b562800`: the preview renderer's scheme check now normalizes/rejects control characters before checking, closing the `javascript:` bypass from `## Review notes` finding 1; see `## Fix`/`## Verification` pass 2
- [x] Interest tagging drives both the public page's pre-checked subscribe form and campaign targeting
- [ ] Slug auto-generated from the title, editable before first publish and locked after — auto-generated and locked, but not editable before first publish; see `## Fix`/`## Gotchas`
- [x] Publish, unpublish, and cancel actions with confirmation
- [ ] Cover image upload or path entry — path/URL entry only, no upload control; see `## Fix`/`## Gotchas`
- [x] `npm run check` and `npm test` pass

## Notes

PRD §5.2. Locking the slug after first publish protects shared links — an edited slug breaks every social post and email that already pointed at the old URL. If a slug genuinely must change later, that is a deliberate action with a redirect, not a casual edit.

## Relation

- Specified by: [`PRD.md`](../PRD.md) §5.2 — Admin routes (session + `is_admin` required) · 16 lines
  `sed -n '/^### 5\.2 /,/^#\{2,3\} [0-9]/p' PRD.md`
- Depends on: [#0009](0009.md) — Port the admin users, settings, and audit views
- Depends on: [#0013](0013.md) — Build the terminal motif components
- Depends on: [#0051](0051.md) — Build the workshops CRUD API
- Blocks: [#0056](0056.md) — Add the announce-to-list shortcut

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-22 | implement | claude-sonnet-5 | 142 | 5,252 | 9,936,285 | 176,144 | $3.72 |
| 2026-08-22 | review (bounced) | claude-opus-5 | 62 | 7,492 | 2,609,081 | 126,678 | $2.28 |
| 2026-08-22 | implement (pass 2) | claude-sonnet-5 | 180 | 5,517 | 9,880,824 | 135,509 | $3.56 |

**Total: $9.56**
