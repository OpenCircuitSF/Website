# 0455 — Two fence shapes can silently drop a genuine issue out of every guard's scope

| | |
|---|---|
| **Status** | in-progress |
| **Platform** | Server |
| **Module** | db / guards |
| **Phase** | 8 — Archive, imports, and delivery health |
| **First seen** | 2026-09-05 |

## Description

`#0454` made `issueStatus` ignore a status row that sits inside a fenced code
block, so the format template in `issues/Issues.md` stops being read as a real
issue. Its reviewer probed the hand-rolled fence walk against **18** documents
and found it diverges from CommonMark in six ways. Four diverge in the **safe**
direction — a row that should be ignored is read anyway.

**Two diverge in the dangerous direction**, both by opening a fence the real
parser would not:

1. a fence line **indented four or more columns**, or tab-indented, above the
   status row;
2. a **backtick inside a backtick opener's info string** above the status row.

In either case `issueStatus` returns empty, the file is classified as not an
issue, and **every guard silently stops scanning it** — including
`TestResolvedOrClosedIssueCarriesClosedRow`, which then checks nothing and says
nothing.

## Why this is not urgent, and why it is still worth closing

**Exposure today is zero, and structurally so.** All 454 genuine issue files
carry the status row at line 5 (one at line 7), with **zero** fence-shaped lines
above it, because the metadata table is the second block by the template's own
definition. `issueStatus` returns at the first unfenced match, so the corpus's 19
indented and 4 four-backtick fence lines — all lower in their files — cannot
reach the result.

So this is a latent hazard, not a live failure. What makes it worth a line of
code is the failure *mode*: a guard that silently stops guarding is the shape
this repo has shipped twice already (`#0258`, `#0428`), and the whole point of
`#0454` was to make the guards' scope describable.

## The remedy, as its reviewer measured it

Roughly one line: gate the fence toggle on `leadingIndentWidth(line) <= 3`, and
reject a backtick appearing in a backtick opener's info string. **Both helpers —
`leadingIndentWidth` and `leadingRunLength` — already exist in `internal/db`**,
hardened by `#0086`, `#0089`, `#0092` and `#0224`.

## Acceptance criteria

1. Both dangerous-direction divergences are closed, reusing the two existing
   in-package helpers.
2. **Do not refactor `docTableRowLines`.** `#0454`'s review established that
   extracting fence state out of that heavily-hardened function — which another
   guard family depends on — exceeds a predicate change, and `#0176` is the
   standing precedent for declining exactly that trade.
3. Leave the four **safe-direction** divergences alone unless closing them falls
   out for free. A row wrongly read as real is the failure this repo started
   with and is loud; a row wrongly ignored is silent. Fixing the silent half is
   the whole job.
4. Prove it by running the **real** `issueStatus` against the divergent shapes,
   as `#0454`'s reviewer did — not a re-implementation, which is a copy of the
   subject agreeing with itself (`#0441`'s review made this point).
5. Re-derive the corpus counts before and after and confirm **no genuine issue
   file changes classification**. `#0454` established 455 files, 454 with a
   status, 30 `open`/`in-progress` — re-derive rather than copy.
6. **No new mechanism** (§8, `#0258`). A predicate change and its test.

## Relation

- `#0454` added the fence walk and its review found these two shapes.
- `#0446` widened the scanned section set; that widening must keep biting.
- `#0176` is `wontfix` on the same regex-versus-CommonMark trade and is why
  criterion 2 says not to reopen it.

Verify with `ISSUE=NNNN scripts/check.sh go ./internal/db/... ./internal/handlers/...`. Scan bytes with `python3`, never BSD `grep -P`.

## Work log

- 2026-09-05 — **Implemented (phase 2, Sonnet).**

  **Criterion 1 and criterion 6 — the predicate change.** `issueStatus`'s fence
  toggle previously entered the fenced state on *any* line whose trimmed form
  started with a triple backtick, with no check on that line's indentation or
  on what followed the backtick run. Changed only the opening branch (the
  transition from not-fenced to fenced): it now opens a fence only when
  `leadingIndentWidth(line) <= 3` and the text after the backtick run
  (`leadingRunLength(trimmed, '`')`) contains no further backtick — reusing
  both existing helpers unmodified, exactly as the issue's remedy specifies.
  A line that looks fence-shaped but fails that gate is no longer swallowed:
  it falls through to the ordinary status-row check on the same line, the
  same way any non-fence line already did. The closing branch (fenced to
  not-fenced) is untouched — it still closes on any triple-backtick line
  regardless of that line's own indentation or trailing text, which is
  deliberate: closing-side leniency is a safe-direction divergence (a fence
  that closes "too early" only ever un-hides a row, never hides one), and
  criterion 3 says to leave the safe-direction divergences alone. No new
  state variable, no new helper, no touch to `docTableRowLines` — the diff is
  the body of `issueStatus` plus its doc comment, and two new test functions.

  **Criterion 4 — proof against the real function.** Added
  `TestIssueStatusDoesNotOpenFenceOnNonOpenerLine`, exercising the real,
  compiled `issueStatus` (not a re-implementation) against three documents,
  each carrying a genuine unfenced Status row preceded by one of the two
  dangerous shapes: a fence line indented four columns, a tab-indented fence
  line, and a backtick-opener fence line whose info string itself contains a
  backtick. All three now return the row's real status instead of "", which
  is the fix — before this change, all three returned "" (confirmed by
  running the pre-fix body inline during development; not shipped). Also
  added `TestIssueStatusSafeDirectionDivergencesUnchanged`, running the real
  function against the four safe-direction shapes `#0454`'s review measured —
  a tilde-fenced row, a row inside a 3-backtick fence nested in a 4-backtick
  fence, a row after a closer carrying trailing text, and a row after inline
  triple-backtick text mid-prose — and asserting each still resolves to the
  row's status, unchanged from before this fix. `TestIssueStatusIgnoresFencedExampleRow`
  (`#0454`'s own test) continues to pass unmodified, confirming the original
  fix's fenced-template case is untouched.

  **Criterion 5 — corpus counts re-derived, not copied.** Wrote a temporary
  in-package Go test (never committed) that read every `issues/*.md` file,
  computed status with both the pre-#0455 `issueStatus` body (inlined for
  this measurement only) and the real post-fix `issueStatus`, and reported
  any file where the two disagreed. Result at this commit: **456** files in
  `issues/` (455 numbered issue files plus `Issues.md` — `#0455.md` itself is
  the file that grew the corpus by one since `#0454`'s count), **455** with a
  non-empty status both before and after (the sole empty one is `Issues.md`,
  fenced per `#0454`'s fix, unaffected by this issue's change), and **27**
  reading `open` or `in-progress` both before and after — lower than
  `#0454`'s 30 because ordinary work resolved issues in between, exactly the
  drift `#0454`'s own note warned would happen. The list of files whose
  classification changed between the pre- and post-#0455 predicate was
  **empty** — every genuine issue file kept the identical status it had
  before this change, confirming the fix only affects documents matching one
  of the two dangerous shapes, none of which exist in the corpus today
  (matching this issue's own "exposure is zero" framing).

  **Criterion 2 and criterion 3 confirmed by diff.** `docTableRowLines` and
  its doc comment are untouched — checked with `git diff` before staging.
  The four safe-direction cases above are asserted unchanged rather than
  merely assumed.

  **Verification.** `uptime` before the scoped run: load averages 2.74 / 3.27
  / 4.21 — recorded, not reasoned from. `git status --porcelain` checked
  immediately before and after the run showed only this issue's own two
  files modified (`internal/db/issue_citation_guard_test.go`, this file);
  `issues/0343.md`, `issues/0344.md`, and `issues/0453.md` were dirty from
  other agents throughout and were not touched. `gofmt -w` on the one edited
  Go file left its SHA-256 digest unchanged (checked before and after).
  `ISSUE=0455 scripts/check.sh go ./internal/db/... ./internal/handlers/...`
  reported `go build`, `go vet`, and `gofmt -l` clean; `internal/db` `ok` in
  0.493s and `internal/handlers` `ok` in 5.233s (neither `0.00s`); the skip
  audit reported `(no skips)`; no leftover processes; `VERIFICATION PASSED`.
  Re-ran the three real test declarations individually with `-count=1 -v`,
  all `PASS`: `TestIssueStatusDoesNotOpenFenceOnNonOpenerLine`,
  `TestIssueStatusSafeDirectionDivergencesUnchanged`, and
  `TestIssueStatusIgnoresFencedExampleRow`. The isolated scratch database
  (`opencircuit_test_0455`) was built directly from `migrations/` rather than
  cloned from the shared template, since the template is at migration 28 and
  this working tree's `migrations/` is at 27 — `scripts/check.sh` reported
  this itself and handled it; nothing here touched the template or ran
  `scripts/db-reset.sh`. Dropped by `scripts/check.sh` on exit.
