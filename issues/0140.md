# 0140 — `scripts/check.sh` prints VERIFICATION PASSED over failing tests, so no verification claim in this tracker is trustworthy

| | |
|---|---|
| **Status** | in-progress |
| **Module** | infra / scripts |
| **Platform** | All |
| **Phase** | 0 — Foundation |
| **First seen** | 2026-08-22 |

## Description

`scripts/check.sh` — the canonical verification command every issue is told to run, and the one `CLAUDE.md` §5 says "encodes every rule so they cannot be forgotten" — **reports success when the tests it ran failed.**

It sets `set -uo pipefail` in the outer shell, but runs every check through an inner shell:

```bash
run bash -c "go test $pkgs -p 2 -count=1 2>&1 | tail -$TAIL"
```

The inner `bash -c` does **not** inherit `pipefail`, so the pipeline's exit status is `tail`'s, which is always 0. Proof:

```
$ bash -c 'false | tail -1'; echo $?
0
$ bash -o pipefail -c 'false | tail -1'; echo $?
1
```

Every check is affected in every mode — `go build`, `go vet`, `go test`, `npm run check`, `npm test` — nine call sites in total. A compile error, a vet finding, a failing test, and a type error all print their output and are then reported as `VERIFICATION PASSED`.

The skip audit was unaffected and kept working, which is part of why this survived: the script *looked* rigorous.

## How it was found

`#0054`'s phase-3 review ran the suite and watched `scripts/check.sh` print `FAIL github.com/.../cmd/opencircuit` followed by `VERIFICATION PASSED` in the same output. That is what led it to look at the runner rather than the test.

The failing test it caught — `TestMountAndServe_WorkshopMutationInvalidatesSharedSEOSite` — had passed for its implementer only because a stray `npm run build` had overwritten the embedded placeholder. Reverting that file (correctly) broke the test, and the broken script said nothing.

## Acceptance criteria

- [x] Every inner shell runs with `pipefail` — `run bash -o pipefail -c` at all nine call sites
- [x] A deliberately failing package makes the script report `VERIFICATION FAILED` — confirmed against `./cmd/opencircuit/...`, which currently has a genuinely failing test: the script now prints `FAILED (1)` and `VERIFICATION FAILED`
- [x] A guard test, or a self-check inside the script, that fails if the pipefail regression is ever reintroduced
- [x] The tree's real state is established under the fixed script — a full `scripts/check.sh all` run whose result is read rather than assumed
- [x] Any issue resolved this session whose verification rested solely on `VERIFICATION PASSED` is re-checked. `#0130`, `#0131`, `#0050`, `#0051`, `#0053`, `#0134` all cite it. Most also cite named test runs read directly, which are unaffected — establish which, if any, rest on the script alone

## Implementation notes

**Regression guard (criterion 3).** Added a self-check inside `scripts/check.sh`
itself, right after the `run()` helper is defined, so it executes on every
invocation before any build/vet/test work starts — it cannot be skipped by
forgetting to run a separate test file:

```bash
BAD_SITES="$(grep -n 'run bash -c' "$REPO/scripts/check.sh" | grep -v 'pipefail' || true)"
if [ -n "$BAD_SITES" ]; then
  printf '...PIPEFAIL REGRESSION (#0140): scripts/check.sh has a "run bash -c" call site
  missing "-o pipefail"...' >&2
  echo "$BAD_SITES" >&2
  exit 2
fi
```

It scans the script's own source for any `run bash -c` call site that doesn't
also mention `pipefail` on the same line — i.e. exactly the regression shape
described above (`run bash -c "... | tail ..."` without `-o pipefail`).

Proved by mutation: removed `-o pipefail` from the `go_test` call site
(originally line 92, `run bash -o pipefail -c "go test $pkgs -p 2 -count=1
2>&1 | tail -$TAIL"` → `run bash -c "..."`), ran `scripts/check.sh go
./internal/audit/...`, and the guard fired immediately (before `go build` even
ran), exit code 2:

```
=== self-check: pipefail regression guard (#0140)
PIPEFAIL REGRESSION (#0140): scripts/check.sh has a "run bash -c" call site
missing "-o pipefail". That silently discards the piped command's exit status
(it becomes tail's, which is always 0), so a failing build/vet/test/npm run
would print FAILED output and still report VERIFICATION PASSED. See
issues/0140.md. Offending line(s):
92:  run bash -c "go test $pkgs -p 2 -count=1 2>&1 | tail -$TAIL"
```

Restored the line and confirmed byte-identical recovery with `shasum -a
256` (`26cb5fa6...fa37da` matched a copy taken before the mutation, both
before and after).

**Criterion 4 — tree's real state.** `uptime` was ~2.0 (idle, well under the
~8 threshold) before the run. `ISSUE=0140all scripts/check.sh all` (own
isolated database) reported `VERIFICATION PASSED`: `go build`/`go vet` clean;
all 16 Go packages `ok` including `cmd/opencircuit` (0 skips); `npm run
check` — 234 files, 0 errors, 0 warnings; `npm test` — 23 files, 3730 tests
passed. Re-ran `ISSUE=0140chk scripts/check.sh all` and confirmed the actual
shell exit code was `0` (not just the printed banner).

The test named in this issue's notes as failing at filing time,
`TestMountAndServe_WorkshopMutationInvalidatesSharedSEOSite`
(`./cmd/opencircuit/...`), now passes — confirmed individually with `go test
./cmd/opencircuit/... -run TestMountAndServe_WorkshopMutationInvalidatesSharedSEOSite
-v`: `--- PASS`. It was tangled with `#0141`'s `web/dist/index.html`
placeholder per this issue's notes; whatever left that file in its correct
state resolved it before this run — not independently investigated further,
per this issue's scope (fix + guard the script, not chase `#0141`).

**Criterion 5 — audit of `#0130`, `#0131`, `#0050`, `#0051`, `#0053`,
`#0134`.** Read each issue's `## Verification` section. All six quote actual
per-package `go test` output (explicit `ok  .../pkg  N.NNNs` lines covering
every package in the run, not truncated) and/or named `-run '...' -v` test
runs with individual `--- PASS` lines quoted directly — not just the closing
`VERIFICATION PASSED` banner. Since the pipefail bug only discarded the exit
*status* one layer down and never touched the piped *content* (tail still
printed whatever `go test`/`npm test` wrote, including a `FAIL` line had one
occurred), a verification that quotes the actual per-package/per-test output
is unaffected by the bug regardless of whether the banner was trustworthy at
the time. None of the six rest solely on the banner:

- `#0130` — quotes the `ok  .../internal/handlers  2.066s` line from
  `scripts/check.sh`, plus two additional direct `-shuffle` runs outside the
  script entirely. Not banner-only.
- `#0131` — quotes per-package `ok` lines from `scripts/check.sh go
  ./internal/mailing/... ./internal/handlers/...`, plus four named
  `TestAdminCampaignStats_*` tests run directly with `-v` and individually
  confirmed. Not banner-only.
- `#0050` — quotes the `ok  .../internal/db  0.311s` line, plus a direct
  `-run 'TestDatabaseDocMigrationParity|TestDocTableRowLines' -v` rerun with
  every subtest's `--- PASS` listed. Not banner-only.
- `#0051` — quotes the full 16-package `ok` listing with real per-package
  timings from `scripts/check.sh go ./internal/... ./cmd/...`, plus four
  separate named-test blocks (workshops CRUD, admin/public workshop handlers,
  admin-routes auth, doc-parity) each run directly with `-v` and PASS lines
  quoted. Not banner-only.
- `#0053` — no Go tests touched (web-only change); quotes `scripts/check.sh
  web`'s actual `npm run check` (219 files, 0 errors) and `npm test` (366
  tests passed) counts twice (before/after a manual round-trip), plus manual
  `curl` verification against a real built binary with bundle-hash ownership
  checked. Not banner-only.
- `#0134` — quotes the full per-package `ok` listing with real timings from
  `ISSUE=0134 scripts/check.sh go ./internal/...`, plus three named `-run
  '...' -v` blocks (targeted regression test, full `internal/workshops`
  package, doc-parity) with explicit `--- PASS` lines. Not banner-only.

No re-verification of these six is warranted by this audit; nothing here
relied on the discarded exit status. Reported for the orchestrator's own
call, per `CLAUDE.md` §9 — not reopened or edited.

## Verification

- `bash -n scripts/check.sh` — syntax OK.
- `ISSUE=0140g scripts/check.sh go ./internal/audit/...` — guard step passed
  silently, isolated database created/dropped, `ok`, `VERIFICATION PASSED`.
- Mutation test on the `go_test` call site: guard fired with exit code 2
  before any build/vet/test ran; restored; `shasum -a 256` byte-identical
  before and after.
- `ISSUE=0140all scripts/check.sh all` and `ISSUE=0140chk scripts/check.sh
  all` — both `VERIFICATION PASSED`, second run's shell exit code confirmed
  `0` directly (not inferred from the banner). `uptime` before each run was
  ~1.9–2.0, well under the ~8 busy threshold (`CLAUDE.md` §5).
- All scratch databases created during this session (`0140g`, `0140all`,
  `0140chk`, `0140test`) were dropped; `pgrep -f 'go test|\.test '` empty at
  the end.

## Notes

Fixed immediately by the orchestrator rather than queued, because every dispatch in flight depends on the script telling the truth. The remaining criteria are the parts that need someone else's eyes: the regression guard, and the audit of what the broken script may have waved through.

**The general lesson is worth keeping.** The script was trusted precisely because it was written to be trustworthy — it centralizes `TEST_DATABASE_URL`, forces `-p 2`, refuses `-count=N`, audits for silent skips. None of that helps when the exit status is discarded one layer down. A verification tool that cannot fail is worse than no tool, because it converts every "I ran the suite" into false assurance.

## Relation

- Found by: [#0054](0054.md) — Build the workshop detail view
- Relates to: [#0002](0002.md) — the earlier case of a verification run that proved nothing (`TEST_DATABASE_URL` unset)

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | implement | claude-sonnet-5 | 60 | 2,475 | 1,785,340 | 80,821 | $0.88 |

**Total: $0.88**
