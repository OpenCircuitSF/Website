// Single source of truth for the product name shown in the UI.
// Import APP_NAME wherever the brand/title appears (header, auth pages).
// Do NOT use this for functional values (WebAuthn rp id/name).
export const APP_NAME = 'Open Circuit SF';
